# AI-hint: Cargo manifest for mios-template-compile -- compiled template round-trip compiler.
[package]
name = "mios-template-compile"
version.workspace = true
edition.workspace = true

[dependencies]
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
