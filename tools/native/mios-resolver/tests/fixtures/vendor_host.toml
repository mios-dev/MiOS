# AI-hint: Fixture overlay for mios-resolver characterization tests -- vendor + host.
[meta]
mios_version = "0.3.0"

[identity]
role = "mini"
host_name = "mios-host"

[ports]
stack_id = 1
hermes = 8080
cockpit = 9090
