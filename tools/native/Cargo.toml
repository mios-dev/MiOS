# AI-hint: Rust workspace Cargo manifest for MiOS native tools.
[workspace]
members = [
    "mios-version-check",
    "mios-wallpaperd",
    "generate-names-registry",
    "mios-ssot-walk",
]
resolver = "2"
