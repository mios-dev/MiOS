# AI-hint: Cargo manifest for the systemd unit + Quadlet drop-in generator, with a golden-master parity test against usr/lib/systemd/system.
[package]
name = "mios-unit-gen"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = { version = "0.8", features = ["preserve_order"] }
thiserror = "1.0"
