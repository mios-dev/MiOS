# AI-hint: Cargo manifest for mios-template-conform -- compiled template conformance checker.
[package]
name = "mios-template-conform"
version.workspace = true
edition.workspace = true

[dependencies]
toml = "0.8"
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
walkdir = "2.4"
