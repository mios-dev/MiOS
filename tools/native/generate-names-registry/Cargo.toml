# AI-hint: Rust SSOT names registry generator Cargo manifest.
[package]
name = "generate-names-registry"
version.workspace = true
edition.workspace = true

[dependencies]
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
regex = "1.10"
walkdir = "2.4"
