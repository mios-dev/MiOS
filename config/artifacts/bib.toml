# AI-hint: Defines bootc-image-builder filesystem constraints for the root mountpoint, specifically enforcing a minimum 80 GiB size to ensure sufficient disk space for runtime operations.
[[customizations.filesystem]]
mountpoint = "/"
minsize = "80 GiB"
