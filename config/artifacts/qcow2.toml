# AI-hint: Defines the bootc-image-builder configuration for generating QCOW2 disk images, specifying user credentials, kernel boot parameters, and filesystem partition sizes...
# AI-doc: usr/share/doc/mios/manual/_harvest/config_artifacts_qcow2_toml.md

[[customizations.user]]
name     = "mios"
# Replace with: openssl passwd -6 'your-password'
password = "$6$REPLACEME_WITH_SHA512_HASH$REPLACEME"
# Replace with your real ssh-ed25519 public key
key      = "ssh-ed25519 AAAA_REPLACE_WITH_REAL_PUBKEY mios@operator"
groups   = ["wheel", "render", "video"]

[customizations.kernel]
append = "rd.driver.blacklist=nouveau modprobe.blacklist=nouveau iommu=pt"

[[customizations.filesystem]]
mountpoint = "/"
minsize    = "150 GiB"

[[customizations.filesystem]]
mountpoint = "/var/home"
minsize    = "20 GiB"
