# /triage (alias /tr) Prompt for OpenAI Codex

Diagnose failing commands, detect flaky tests, and generate standalone reproduction scripts:
- Run: `python3 scripts/triage.py "<command>"`
- Emits classification in `TRIAGE.md` and generates `./repro.sh`.
