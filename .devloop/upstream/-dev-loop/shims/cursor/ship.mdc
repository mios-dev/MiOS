---
description: Safe trunk merge, worktree pruning, and changelog update (alias /sh)
globs: ["*"]
alwaysApply: false
---

# Cursor Rule: /ship & /sh

When ready to deliver a completed feature lane:
- Execute `python3 scripts/ship.py <source_branch> --target main`.
- Prunes worktrees and updates `CHANGELOG.md`.
