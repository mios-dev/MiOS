---
description: Phantom-failure triage and minimal repro generation (alias /tr)
globs: ["*"]
alwaysApply: false
---

# Cursor Rule: /triage & /tr

When encountering unexpected build or test failures:
- Execute `python3 scripts/triage.py "<command>" --runs 3`.
- Inspect `TRIAGE.md` and test with `./repro.sh`.
