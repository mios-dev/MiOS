<!-- AI-hint: Spinoff task list handed to Gemini AGY to execute in the IDE, in parallel with Claude. Derived from the deploy + DB-driven workstreams Claude started (WS-DEPLOY runtime, WS-HEAVY runtime). AGY takes the CODE-ONLY half (no live-VM dependency) so the two agents don't collide. Each task is self-contained with Who/What/Where/When/How + Start-here + Done-When. -->

# AGY-TASKS — Gemini AGY spinoff (parallel to Claude)

> ## 🛑🛑 AGY — CRITICAL: STOP CORRUPTING THE SHARED TREE (2026-08-02, operator-mandated)
> Your recent work RED-LINED the drift-gate for 15+ consecutive commits; Claude spent an entire
> session repairing it. Two failure classes, BOTH avoidable by verifying BEFORE committing:
>
> **(A) Your edit tooling EMPTIED files.** `AGY-1560` truncated BOTH `automation/98-drift-checks.sh`
> (223 KB) and `tests/drift-gate-negatives.sh` (90 KB) to **0 bytes** — the entire gate + negatives,
> gone. Earlier bulk campaigns ("Sterilize logs", "Distill AI comments", fedora-44 purge) broke shell
> quotes (SC1072/1073), made mojibake (`°C`→`Â°C`), deleted `# TD-1: eval-safe` whitelist comments,
> title-cased case-sensitive strings (`podman`→`Podman`, `docker.io`→`Docker.io`), desynced the
> bash↔Rust ssot-lint twin + the hyprland/composefs generators from their committed projections, and
> left `env-baseline.txt`/`referenced_names.txt` stale.
>
> **(B) You committed un-compiling / un-tested code.** Missing serde `default_rpm_type` (Rust E0425)
> and an unclosed python list in a golden test (`SyntaxError: '[' was never closed`) — the Rust test
> job NEVER passed.
>
> ### HARD RULES — every commit, zero exceptions. Thorough beats fast.
> 1. **After EVERY edit, confirm the file is non-empty and well-formed** — `wc -c`, `bash -n` for
>    shell, `python3 -m py_compile` for python. NEVER commit a 0-byte or truncated file.
> 2. **Make the gate GREEN before `git commit`:** `bash tests/drift-gate-negatives.sh` AND
>    `bash automation/98-drift-checks.sh` must both pass. A red gate is never OK to ship.
> 3. **Rust: `cd src/mios-rs && cargo build && cargo clippy --workspace --all-targets -- -D warnings
>    && cargo test --workspace`** (and `tools/native`, `--exclude mios-wallpaperd`) must ALL pass.
>    Never commit Rust you have not compiled AND tested.
> 4. **NO bulk multi-file text transforms** (sterilize / capitalize / distill / mass-sed) unless you
>    run the FULL gate afterward and fix EVERY regression in the SAME commit. Removing a comment/log
>    that breaks a quote, a twin, a whitelist, or a case-sensitive grep is a REGRESSION you caused.
> 5. **After editing `mios.toml`, regenerate + commit** `env-baseline.txt` (`mios-env-snapshot`),
>    `referenced_names.txt` (`generate-names-registry.py`), and every affected projection
>    (prepare-root.conf, chrony/nut/kargs/cockpit/ipa/uki, quadlets); verify `check_resolved_env_lossless`.
> 6. **Do NOT revert or overwrite Claude's fixes.** `git pull` first, build on HEAD, stage only files
>    YOU edited for the CURRENT task (never `git add -A`). Leave origin GREENER than you found it.

> ## 🛑 AGY — OBSERVED WEAKNESSES (2026-08-23, measured during a shared-tree session)
> Each entry below was watched happening, with the evidence named. They are listed because
> a weakness you cannot see is one you repeat. Every one has a countermeasure that is now
> enforced by a gate or a harness, so the cost of repeating it is a red gate rather than a
> day of someone else's debugging.
>
> **W1 — You delete a collaborator's untracked files.** `tests/_agentpipe_path.py` was created
> and removed twice within an hour, breaking the nine suites that import it. **Rule:** never
> remove a file you did not create. If a file looks unexplained, read it; it may be the half of
> a change you are looking at the other half of.
>
> **W2 — You rewrite an SSOT block and drop keys other code reads.** `[ci].runners` and
> `[ci.python]` were deleted from `mios.toml`; `tools/ci-suites.py --python-packages` then
> returned empty, which would have made a runner install nothing and six suites fail on import.
> **Rule:** before removing an SSOT key, `grep -rn` for it. A key with a reader is load-bearing.
>
> **W3 — You raise a shrink-only ceiling to accommodate a new entry.** `[ci].max_exempt_suites`
> went 7 → 8 so `tests/run-suites.sh` could be exempted, when it is a harness and belongs in
> `[ci].runners`. **Rule:** a ratchet that can be raised is not a ratchet. When something does
> not fit under a ceiling, the answer is a category or a fix, never a bigger number.
>
> **W4 — You unregister another check while registering your own.** `check_ci_suite_coverage`
> was removed from `main()` in the same edit that added `check_leaked_fixtures`, leaving a
> defined-but-never-run check — exactly the shape `check_gate_registry` exists to catch.
> **Rule:** after editing `main()`, run `check_gate_registry` and confirm the count went UP.
>
> **W5 — You revert a collaborator's file wholesale.** Both workflow rewrites were restored to
> their previous 55-step and 20-step versions after being replaced. **Rule:** if a file changed
> under you, read the diff before overwriting. `git log -1 --stat` costs seconds.
>
> **W6 — Your negative tests leave their fixtures in the tree.** Five reached the working tree
> in one session: an injected table in the shipped SQL schema, `Root:hardcodedpass | chpasswd`
> in a Ventoy firstboot script, a rewritten cockpit port, a capability requirement replaced by
> an injected name, and a port entry repeated twice. They surfaced as five unrelated suites
> failing, so the diagnosis cost was paid repeatedly. **Countermeasure:** the negatives harness
> now snapshots every path it can reach and restores anything a dying test left behind, and
> `check_leaked_fixtures` fails on a tracked fixture or backup file.
>
> **W7 — You stage probe files and backups.** a probe script under `automation/` reached the
> index and `mios-tailscale-serve.ps1.negbak` was tracked. **Rule:** stage explicit paths. Never
> `git add -A` in this tree.
>
> **W8 — You write `mios.toml` non-atomically.** A mid-write read parsed CLEANLY as 1,879 lines
> and 6 tables, when the file is ~10.7k lines and 152 tables. A parse check alone cannot detect
> this. **Rule:** write to a temporary file and rename over the target, and before committing the
> SSOT check BOTH `wc -l` and `tomllib`.
>
> **W9 — You do not check a gate on both hosts.** `tools/check-credential-literals.py` compared
> `os.path.relpath` output against forward-slash SSOT entries, so it was green on Linux and red
> on Windows with an identical tree. **Rule:** any path a gate compares must be normalised to
> forward slashes at the point it is produced.

**Handoff 2026-07-10.** Claude is executing the **runtime/deploy** half live on
`podman-MiOS-DEV` (webtools image build in progress; vLLM heavy-model provisioning
next). AGY takes the **code-only** half below — no live-VM needed, fully doable in
this IDE. These are derived from **WS-DEPLOY** (T-166), **WS-HEAVY** (T-178), and
**WS-VECTOR** (T-242) in `TASKS.md`. Work top-down; each is independent.

> ## PARALLEL PARTITION (2026-07-17): AGY owns its queue on C:MiOS (tools/native Rust, agent-pipe mios_dispatch/server.py, bakers, mios-theme-render registry-kind, 38-drift-checks, tests, ROADMAP/ADR docs). CLAUDE works DISJOINT areas in short-lived worktrees (merge->main->delete): the Portal/configurator (portal.py routes, usr/share/mios/configurator, config.py), C:mios-bootstrap (separate repo), and read-only design/audit workflows. File-sets are disjoint so merges are clean. Both agents run in parallel.
>
> ## AGY — THIS LIST GROWS. Re-check it after every task.
> Claude marks your finished tasks **[DONE]** and **appends NEW tasks** as you clear them — there is always more work. **After each commit, `git pull` and re-read this file from the bottom** for new `AGY-N` items. Do NOT stop when you reach the end — new tasks keep arriving. Mark a task done by committing `agy: AGY-N <summary>`.
>
> **Queue status (updated by Claude):** AGY-1..20 all **[DONE]** ✔ — AGY-18 (`85df9b55` event/session vectors), AGY-19 (`6d7834b2` flip-readiness), AGY-20 (`913a37ce` V3 build-catalog materialization). All Claude-verified + integrated; full `just drift-gate` PASS on AGY-1..19 (incl. your new checks 29–32). Nice work — and thanks for staging explicit paths (no Quadlet sweeps since AGY-14). **Still queued: AGY-21. NEW → AGY-22 · AGY-23 · AGY-24 appended below — go.** (More coming from a Claude upstream-gap research pass.)
>
> 🔴 **AGY — the Quadlet sweep IS still happening (AGY-10/11/14 each swept the 7 `.container` units de-digested).** Real cause: `generate-pod-quadlets.py` resolves `${MIOS_*}` from the shell env; YOUR shell doesn't export them so you emit the bare/16384 fallbacks, but the drift-gate (and the build) export them → expect the digested/32768 output. So **do NOT `git add -A`** — stage ONLY your task's files (`git add <explicit paths>`); NEVER include `usr/share/containers/systemd/*.container` unless your task is about them. `git status` before every commit and drop anything unrelated. Claude regenerates them in the canonical env each time you sweep — please stop sweeping.
>
> 🛑 **AGY — BROAD-ADD VIOLATION (2026-07-16), READ THIS.** Your `AGY-41` commit `63f6ef4b` did a **broad `git add`** and swept **103 files** into one commit — including Claude's uncommitted btop SSOT work (`usr/share/mios/mios.toml [btop]`, `theme/templates/btop.conf.tmpl`, `usr/libexec/mios/mios-theme-render`, `etc/btop/btop.conf`) and the 92-file medicat deletion — none of which belong to "eliminate the eval-on-agent-args injection surface." The work survived, but the history is now a mislabeled mega-commit. **HARD RULE from now on:** stage ONLY the files YOU personally created/edited for the CURRENT task — `git add <explicit path> <explicit path>`, NEVER `git add -A` / `git add .` / `git add <dir>`. Run `git status` before every commit and `git reset HEAD <path>` anything you did not author. **Claude has uncommitted work in this shared tree RIGHT NOW** (`ROADMAP.md`, `TASKS.md`, `AGY-TASKS.md`, `usr/share/doc/mios/adr/*`, `automation/90-generate-sbom.sh`, and 67 staged `src/autounattend/medicat_installer/**` deletions) — do NOT add, commit, reset, or `git rm` ANY of those; they are Claude's to commit.

**Ground rules for AGY**
- Repo: `C:\MiOS` (the FHS overlay; `/usr`, `/etc`, `automation/` map to `/`). Read `CLAUDE.md` + `AGENTS.md` first.
- Match surrounding code; complete replacement files (no `# ... unchanged ...`).
- `bash -n` / PowerShell parse every script you touch; run `just drift-gate` before proposing done.
- Commit each task separately; prefix the subject `agy:` and end with your Co-Authored-By line. Do NOT touch the files Claude has open (the webtools builder, live install runs).
- The reference pattern for every "producer" fix is `automation/38-hermes-agent.sh` (commit 31a52fb1): install the COMPLETE set in ONE transaction, **retried 3x with backoff**, **verified** present, non-fatal to siblings.

---

## AGY-1 -- Make every firstboot producer atomic, retried, verified and idempotent  (WS-DEPLOY | P1 | M)**[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- a fresh install reaches a working system without an operator manually re-running failed first-boot fetches.
**What+How:** Apply the `38-hermes-agent` reference pattern to the producers that still "run once, give up". In `usr/libexec/mios/mios-ai-firstboot`, wrap the vLLM/GGUF `snapshot_download` blocks (~L111-124 and L371) in a 3x retry-with-backoff loop, then verify the target `config.json` / `.ready` artifact actually exists afterwards; keep each model non-fatal and log the outcome clearly (this is the code half of T-178 — the heavy lane is skipped today only because this fetch had no retry and ran through the then-missing venv). In `usr/libexec/mios/forge-firstboot.sh`, replace the fixed `300s` timeout abort with a readiness poll against the forge health/socket under a generous cap. Grep `usr/libexec/mios/*firstboot*` for any other bare one-shot build/fetch and give it the same treatment. Add `Restart=on-failure` plus `StartLimit*` to the corresponding units.
**Where:** `usr/libexec/mios/mios-ai-firstboot`, `usr/libexec/mios/forge-firstboot.sh`, `usr/lib/systemd/system/*.service`
**Done When:** every `*firstboot*` producer is atomic + retried + verified + idempotent, `bash -n` is clean on each, and a fresh reinstall deploys them with zero manual retries.
**Why:** Today a single transient network blip during first boot permanently skips the heavy AI lane and a slow forge hard-fails at 300s, so the machine silently boots into a degraded MiOS that only a human can repair.
**Dep:** none (first — mirrors the webtools/venv fixes already landed)
**Who:** you (bash/systemd)

## AGY-2 -- DAG-integrity drift-gate: a consumer that can start before its producer is a build error  (WS-DEPLOY | P1 | M)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- ordering invariants become machine-checked instead of a convention that erodes silently.
**What+How:** Add the next free check (29) to `automation/38-drift-checks.sh`, following the existing `check_*` function style and wired into `main`. It parses the producer→consumer map and FAILS when a consumer `.container`/`.service` depends on a built image, seeded row or fetched model but carries no `After=` / `Requires=` / `ConditionPathExists=` naming its producer's readiness artifact. Keep it read-only and fast — no built image required. Full spec lives in `usr/share/doc/mios/reference/install-ordering.md`.
**Where:** `automation/38-drift-checks.sh`, spec at `usr/share/doc/mios/reference/install-ordering.md`
**Done When:** the new check flags the known-good edges (webtools pod `After` webtools-firstboot; agent-pipe `After` the venv), passes on the current tree, and `just drift-gate` is green.
**Why:** Without it, ordering regressions are only discovered on a real first boot of real hardware — the exact class of failure AGY-1 had to hand-repair.
**Dep:** AGY-1
**Who:** you (bash + the drift-gate framework)

## AGY-3 -- DB projection foundation: `config_layer` / `config_kv` plus a DB→TOML materializer  (WS-VECTOR | P1 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- Postgres becomes a lossless projection of the SSOT before any authority moves.
**What+How:** Lay `everything-db-driven.md` V0 WITHOUT flipping authority. In `usr/share/mios/postgres/schema-init.sql` add `config_layer(rank,name)` seeded {0:vendor,1:host,2:user,3:machine} and `config_kv(scope,key,value jsonb,layer FK,owner_user,description,emb vector(768),emb_model,emb_version)` with an HNSW index on `emb` mirroring the `knowledge`/`agent_memory` DDL exactly (`vector_cosine_ops m=16 ef_construction=64`), all idempotent (`CREATE TABLE IF NOT EXISTS`, guarded index). Write `usr/libexec/mios/materialize-config-toml.py` as the DB→TOML inverse of `seed-db-config.py`: read `config_kv`/`verb`/`domain_verb`, regenerate a TOML the drift-gate can diff, read-only w.r.t. the DB. Make the `verb` round-trip lossless by preserving section/examples/model_name/hidden/aliases/conflict_group/parallel_limit/max_result_chars on both sides.
**Where:** `usr/share/mios/postgres/schema-init.sql`, `usr/libexec/mios/seed-db-config.py`, new `usr/libexec/mios/materialize-config-toml.py`
**Done When:** the schema applies idempotently, a TOML→DB→TOML round-trip of a verb diffs clean, and nothing reads the new tables at runtime yet.
**Why:** Config currently flows one way into the DB and dies there; with no inverse there is no way to prove the projection is lossless, so authority can never safely move.
**Dep:** none (can start immediately; no live DB needed)
**Who:** you (SQL + Python, no live DB needed — static SQL/codegen)

## AGY-4 -- The unified names/keys registry generator plus its drift-gate  (WS-NAME | P2 | M)**[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- Law 9 gets an enforcing check so no fact can acquire a second name.
**What+How:** Per `usr/share/doc/mios/reference/naming-unification.md`, write `tools/generate-names-registry.py` emitting `usr/share/mios/names.generated.txt` — one `section.key  MIOS_SECTION_KEY` line per fact, derived from `mios.toml`, deterministic. Add a check to `automation/38-drift-checks.sh` that regenerates the registry, diffs it, and fails on any NEW translation or duplicate (an env var renaming a native key, or a second name for one capability). This is T-165 Phase 0: the enforcement scaffold ONLY — do not delete the `userenv.sh` table yet.
**Where:** new `tools/generate-names-registry.py`, `automation/38-drift-checks.sh`, `usr/share/mios/names.generated.txt`
**Done When:** the registry generates deterministically, the gate is green on the current tree and red on an injected duplicate, and `just drift-gate` passes.
**Why:** Nothing stops a new `MIOS_*` alias from being added today, so every fold done by hand is immediately re-diluted by the next commit.
**Dep:** none
**Who:** you (Python + drift-gate)

## AGY-5 -- Daemon runaway controls: host-pressure gate, in-flight dedup, cron cap  (WS-GUARD | P0 | M)**[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- self-driving loops are bounded by construction, not by luck.
**What+How:** Add three guardrails to the consolidated micro-LLM daemon and agent-pipe. (1) Host-pressure gate: before a heavy dispatch, read GPU VRAM / CPU load and defer or degrade to the light lane when over a threshold sourced from `[ai.host_thresholds]`. (2) Request dedup: hash the normalized messages and collapse identical in-flight prompts so a retry storm cannot fan out N copies. (3) Cron cap: bound the daemon's scheduled classify/refusal jobs per interval. All three degrade-open — a missing signal means allow.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/**` (daemon/dispatch), `usr/libexec/mios/mios-daemon*`, `usr/share/mios/mios.toml` (`[ai.host_thresholds]`, new `[ai.guard]` if needed)
**Done When:** a synthetic runaway — rapid identical heavy prompts under high VRAM — is gated and deduped rather than fanned out, and the `test_mios_*` suite is green.
**Why:** An autonomous loop can currently pin the GPU and starve its own host, taking the desktop down with it; this is the P0 runaway class in the priority rubric.
**Dep:** none (next — P0)
**Who:** you (Python, agent-pipe/daemon)

## AGY-6 -- Aggregate token/turn budget plus foreground preemption of background work  (WS-GUARD | P0 | M)**[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- a fan-out stops at a declared ceiling instead of running until the hardware does.
**What+How:** Enforce a per-session AND global token/turn budget across the agent-pipe fan-out: sum tokens over the council/DAG and hard-stop with a graceful summarize when the cap is exceeded. Separately, preempt background/low-priority work when a foreground request arrives, using `lane_priority`. Budget targets read from SSOT; degrade-open when unset.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/**` (dag_exec/native_loop/dispatch), `usr/share/mios/mios.toml`
**Done When:** a fan-out that would blow the budget stops at the cap and returns a summary; a foreground request demonstrably preempts a running background job; tests green.
**Why:** A council/DAG fan-out has no ceiling today, so one prompt can consume unbounded tokens and wall-clock while the operator's interactive request waits behind batch work.
**Dep:** AGY-5
**Who:** you (Python, agent-pipe)

## AGY-7 -- Vectorize the AI-plane gaps: `emb` on skill / verb / tool_call / directory_entry  (WS-VECTOR | P2 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- semantic recall reaches the tables the agent actually routes on.
**What+How:** Per `everything-db-driven.md` V2, add `emb vector(768)` + HNSW(`vector_cosine_ops m=16 ef_construction=64`) + `emb_model`/`emb_version` to `skill`, `verb`, `tool_call` and `directory_entry` in `schema-init.sql`, mirroring the `knowledge` DDL already matched, over a text projection. Then repoint the in-process verb/apps embedding rebuild in `worker_tools.py` to a native `<=>` query on `verb.emb`, keeping the in-process lexicon as the fail-open fallback. Ground truth stays in typed columns — additive only.
**Where:** `usr/share/mios/postgres/schema-init.sql`, `usr/lib/mios/agent-pipe/mios_pipe/routing/worker_tools.py`, `usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py`
**Done When:** the schema applies idempotently, verb/skill semantic recall works through `<=>`, text-match still available with no functionality loss, tests green.
**Why:** Verb and skill lookup rebuilds an embedding index in-process on every start, so recall quality is capped by a lexicon while pgvector sits unused beside it.
**Dep:** AGY-3
**Who:** you (SQL + Python)

## AGY-8 -- Secret/PII redaction on every persist and every federate  (WS-DURA | P1 | M)**[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- no secret leaves the process into storage or a federated peer.
**What+How:** Add one reusable `redact()` in a new `usr/lib/mios/agent-pipe/mios_pipe/redact.py` that strips API keys, tokens, passwords, emails/PII and `MIOS_*` secrets. Wire it into the persist seam (`mios_pipe/memory/*`, covering writes to `knowledge`/`agent_memory`/`event`/`tool_call`) and the federation seam (`mios_pipe/federation/a2a.py`, covering A2A federate/gossip sends). Keep a `redacted=true` marker on redacted records; never persist raw secrets, per the CLAUDE.md persistence-sanitization law.
**Where:** new `usr/lib/mios/agent-pipe/mios_pipe/redact.py`, `usr/lib/mios/agent-pipe/mios_pipe/memory/*`, `usr/lib/mios/agent-pipe/mios_pipe/federation/a2a.py`
**Done When:** a message carrying a fake key and email is stored AND federated with the secret redacted, `test_mios_redact.py` covers the patterns, tests green.
**Why:** Every prompt containing a credential is written verbatim into pgvector and gossiped to peers, turning the memory store into a permanent, searchable secret archive.
**Dep:** none
**Who:** you (Python)

## AGY-9 -- Config read-path resolver: kill the write-only `system_config` drift  (WS-VECTOR | P1 | L)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- something finally READS the DB copy, so the projection stops being write-only.
**What+How:** Write `usr/lib/mios/mios_db_config.py` as a peer of `mios_toml.py`, reading `config_kv` / `verb` / `domain_verb` with the baked `mios.toml` as fail-open fallback. Contract: identical layer precedence (vendor<host<user<machine via `config_layer.rank`) and identical return shapes, so a caller can flip its source with no behavior change. Add an `[ai] db_authoritative` sentinel defaulting to **false**: when false, resolve from TOML exactly as today and only shadow-compare the DB answer (log + count divergence); when true, resolve from DB with TOML fallback. Ship the resolver, the shadow-compare and tests ONLY — flip no real caller.
**Where:** new `usr/lib/mios/mios_db_config.py`, `usr/share/mios/mios.toml` (`[ai] db_authoritative=false`), `test_mios_db_config.py` under `usr/lib/mios/agent-pipe/`
**Done When:** the resolver returns values identical to `mios_toml.py` for a sampled set of keys/verbs (shadow-compare = 0 divergences on the current tree), TOML fail-open is verified against an empty DB, parse clean, tests green, `just drift-gate` passes. Storage law: vectors find, SQL reads — resolve from typed columns, never from `emb`.
**Why:** The DB config copy is written by the seeder and read by nothing, so it drifts unnoticed and can never be trusted enough to become authoritative.
**Dep:** AGY-3
**Who:** you (Python)

## AGY-10 -- Embed-backfill worker that actually FILLS the AGY-7 `emb` columns  (WS-VECTOR | P1 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the vector columns hold real, provenance-stamped vectors instead of NULLs.
**What+How:** Extend `mios_pipe/memory/embed_backfill.py` (or add a worker beside it) to populate `emb vector(768)` on `skill`/`verb`/`tool_call`/`directory_entry` by embedding each row's TEXT PROJECTION via `mios-llm-light /v1/embeddings` (`nomic-embed-text`, the same client seam `pg.py` uses). It must be idempotent (only embed rows whose `emb IS NULL` or whose `emb_version` is stale — never re-embed unchanged rows), provenance-stamped (`emb_model`/`emb_version` written with each vector), batched with the 3x retry-with-backoff pattern from `38-hermes-agent.sh`, and fail-open (endpoint down → log and skip, never crash the caller). Then repoint the in-process verb/apps rebuild in `worker_tools.py` to prefer a native `verb.emb <=> query` when populated, lexicon as fallback.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py`, `usr/lib/mios/agent-pipe/mios_pipe/routing/worker_tools.py`, new `test_mios_backfill.py`
**Done When:** a backfill run over a seeded DB fills `verb.emb`/`skill.emb`/`tool_call.emb`/`directory_entry.emb` with provenance, a second run is a near-noop, `<=>` verb search returns sane neighbors, the endpoint-down path degrades to the lexicon without crashing, tests green.
**Why:** AGY-7 added the columns but nothing writes them, so every `<=>` query returns nothing and the vector plane is dead weight in the schema.
**Dep:** AGY-7
**Who:** you (Python)

## AGY-11 -- Build-catalog tables plus a DB→`/ctx` materializer (the Xbox/OCI build as rows)  (WS-VECTOR | P2 | L)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the build itself becomes queryable SSOT rows rather than scattered lists.
**What+How:** Per `everything-db-driven.md` V3, add to `schema-init.sql` (idempotent, `knowledge`-mirroring DDL for every `emb`): `package_set(name,section,pkgs jsonb,enable,layer,base_image_ref,emb)` for `[packages.*]`; `build_recipe` / `build_phase(ordinal,script,stage∈{container,runtime,firstboot},deps jsonb,emb)` for the WS-DEPLOY DAG, reusing the producer→consumer map the AGY-2 gate already parses; and the MiOS-Xbox catalog `xbox_feature`/`debloat_policy`/`debloat_profile`/`{appx,feature,capability,component}_removal`/`preset`, each with `emb`. Then write `usr/libexec/mios/materialize-build-ctx.py` rendering these rows back into the files the clean build container consumes (package lists, phase order, debloat XML/JSON). Extend `seed-db-config.py` to seed `package_set`/`build_phase` from `mios.toml` and the `automation/NN-*.sh` numeric order. Additive only — nothing reads these at runtime yet.
**Where:** `usr/share/mios/postgres/schema-init.sql`, new `usr/libexec/mios/materialize-build-ctx.py`, `usr/libexec/mios/seed-db-config.py`
**Done When:** schema applies idempotently; seed `[packages.*]` + `automation/` order → DB → materialize `/ctx` round-trips package sets and phase order losslessly (diff clean vs the current `packages.sh` resolution); `emb` columns present with HNSW; `just drift-gate` green.
**Why:** The build's package sets and phase order exist only as files a container reads, which is why the empty-`/var` chicken-and-egg has no solution and the build cannot be queried or diffed as data.
**Dep:** none (independent of AGY-9/10; pure schema + codegen)
**Who:** you (SQL + Python)

## AGY-12 -- Fold the `userenv.sh` translation table onto the generated registry (Phase 1)  (WS-NAME | P2 | L)**[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- existing duplicate names are removed, not merely frozen.
**What+How:** AGY-4's gate proves no NEW translations creep in; Phase 1 removes the existing ones. Walk `usr/lib/mios/userenv.sh` (and any peer re-exporting a native key under a second `MIOS_*` name). For each pure translation — an env var that merely renames a native/native-derived key already in `names.generated.txt` — fold callers onto the single generated name and delete the duplicate export, with no loss of names or function. For load-bearing exports (those carrying logic, a default, or a consumer that cannot yet take the native name, e.g. the `winget_*`/`flatpak_*` re-dispatch verbs, `memory_append`/`memory_replace`), leave them in place with a `# WS-NAME: load-bearing, keep` note rather than blind-dropping. Record the fold count and the explicit keep-list in `naming-unification.md` Phase-1 status.
**Where:** `usr/lib/mios/userenv.sh`, callers across `usr/lib/mios/**` and `usr/libexec/mios/**`, `automation/38-drift-checks.sh`, `usr/share/doc/mios/reference/naming-unification.md`
**Done When:** every folded name resolves to one generated key, grep shows no caller referencing a deleted alias, the keep-list is documented with reasons, AGY-4's names gate and `just drift-gate` stay green, `bash -n` clean.
**Why:** Roughly two hundred hand-maintained tuples mint a second name for facts that already have one, so every consumer must know which of two spellings is live.
**Dep:** AGY-4
**Who:** you (bash/Python + the AGY-4 gate)

## AGY-13 -- Schedule and wire the embed-backfill worker (timer + native verb search)  (WS-VECTOR | P1 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the vectors stay fresh on their own instead of needing a human to run a script.
**What+How:** (1) Add `usr/lib/systemd/system/mios-embed-backfill.{service,timer}` — a timer plus oneshot mirroring the `mios-skills-miner.{service,timer}` structure: `User=`/`Group=`, low privilege, `After=mios-pgvector.service mios-llm-light.service`, non-fatal — calling the AGY-10 backfill entrypoint to populate `emb` on `verb`/`skill`/`tool_call`/`directory_entry` idempotently. (2) Confirm `toolsearch.py`'s verb search prefers a native `verb.emb <=>` query when populated and falls back to the in-process lexicon when not; add the fallback branch if missing. Fail-open throughout. Gitignore-whitelist the new units and add them to the relevant `automation/NN-*.sh` enable list if one exists.
**Where:** new `usr/lib/systemd/system/mios-embed-backfill.{service,timer}`, `usr/lib/mios/agent-pipe/mios_pipe/routing/toolsearch.py`, `usr/lib/tmpfiles.d/` if a state dir is needed
**Done When:** the timer unit validates (`systemd-analyze verify` clean, else structure matches the miner unit), backfill's 2nd run is a near-noop, `<=>` verb search returns sane neighbors with the lexicon fallback intact, `test_mios_backfill` green, `just drift-gate` passes.
**Why:** `embed_backfill.py` exists but nothing invokes it, so new verbs and skills are never embedded and recall silently degrades as the catalog grows.
**Dep:** AGY-10
**Who:** you (Python + systemd)

## AGY-14 -- Shadow-compare telemetry plus the projection drift-gate (check 31)  (WS-VECTOR | P1 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- divergence between DB and TOML becomes a measurable number and a failing gate.
**What+How:** AGY-9's `mios_db_config.py` shadow-compares DB against TOML but the result goes nowhere. (1) Make the shadow-compare count and log divergences — structured log plus an in-memory counter the health endpoint can expose — so the safety of making the DB authoritative is measurable. (2) Add drift-check 31 `drift_projection` to `automation/38-drift-checks.sh` (next free number after 29/30), statically asserting the DB→TOML round-trip via AGY-3's `materialize-config-toml.py` is lossless for `verb` and `config_kv`: regenerate, diff, fail on drift — the same pattern as the theme check 25. Read-only, no live DB; operate on the seed plus materializer output.
**Where:** `usr/lib/mios/mios_db_config.py`, `automation/38-drift-checks.sh`, `test_mios_db_config.py`
**Done When:** shadow-compare divergences are observable and read 0 on the current tree; check 31 flags an injected verb-field drop and passes clean on HEAD; `just drift-gate` green; tests green.
**Why:** Without a counter and a gate, the DB projection can rot between commits and the eventual authority flip would be a leap of faith.
**Dep:** AGY-13 (builds on the AGY-9 resolver)
**Who:** you (Python + the drift-gate framework)

## AGY-15 -- Sibling-test and modular-boundary compliance for the new agent-pipe modules  (WS-A2 | P2 | S)**[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the agent-pipe god-module boundary holds as new code lands.
**What+How:** Drift-check 11 requires every `agent-pipe/mios_*.py` to have a sibling `test_mios_*.py`, and check 6 requires those siblings to be server.py-free (one-way import). Audit the new/changed modules — `mios_db_config.py`, `mios_pipe/memory/embed_backfill.py`, `mios_pipe/routing/toolsearch.py` — plus any peer touched: add minimal sibling unit tests where missing, and refactor any module importing the `server.py` monolith onto a shared helper. Then run the full `test_mios_*` suite.
**Where:** `usr/lib/mios/agent-pipe/test_mios_*.py` and the modules above
**Done When:** checks 6 and 11 pass, the whole `test_mios_*` suite is green, `just drift-gate` passes, and no new server.py import edges exist.
**Why:** Each untested module and each new import edge into the ~9k-line `server.py` re-grows the god-module that E-02 exists to decompose.
**Dep:** none (independent; quick hygiene pass)
**Who:** you (Python)

## AGY-16 -- DB verb-catalog read-path behind the `db_authoritative` sentinel  (WS-VECTOR | P1 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the DB read-path is available and measured, so a later flip is a one-line change.
**What+How:** Wire the runtime verb/config read-path to `mios_db_config.py` behind the `[ai] db_authoritative` sentinel added in AGY-9. With the sentinel FALSE (default), keep resolving the verb catalog and config from `mios.toml` exactly as today but ALSO shadow-read the DB and record divergences via AGY-14's telemetry. With it TRUE, resolve the catalog and config from `verb`/`domain_verb`/`config_kv` with TOML fail-open. Flip nothing by default. This is what makes the DB copy of `system_config`/`verb` actually readable rather than write-only.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/verbcatalog.py` (or wherever the catalog loads), `usr/lib/mios/mios_db_config.py`, `usr/share/mios/mios.toml` (`[ai] db_authoritative`)
**Done When:** with the sentinel false, behavior is byte-identical to today and the shadow-divergence count is 0 on the seeded tree; with it true (test only), the catalog resolves from the DB with TOML fallback; `test_mios_db_config` extended; `just drift-gate` green.
**Why:** Until a real consumer can read the DB catalog, the per-surface authority flip stays a design document rather than a switch.
**Dep:** AGY-14
**Who:** you (Python)

## AGY-17 -- Seed and round-trip the build-catalog (drift-check 32)  (WS-VECTOR | P2 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the build-catalog tables hold real SSOT rows and are gated as lossless.
**What+How:** Extend `usr/libexec/mios/seed-db-config.py` to POPULATE the AGY-11 tables from SSOT: `package_set` from `mios.toml [packages.*]`; `build_phase` from the `automation/NN-*.sh` numeric order (ordinal = NN, stage ∈ {container,runtime,firstboot} by heuristic); `xbox_feature`/`debloat_policy` from the bootstrap Xbox catalog when reachable, skipping gracefully when not in this repo. Then make `materialize-build-ctx.py` round-trip — seed → materialize `/ctx` → diff against the source package lists and phase order — and add drift-check 32 `drift_build_catalog` to `38-drift-checks.sh` asserting that round-trip is lossless. Additive only.
**Where:** `usr/libexec/mios/seed-db-config.py`, `usr/libexec/mios/materialize-build-ctx.py`, `automation/38-drift-checks.sh`, `test_mios_build_catalog.py`
**Done When:** the seed populates the tables from `[packages.*]` and `automation/` order, the round-trip diff is clean, check 32 flags an injected package-set drop and passes on HEAD, tests and `just drift-gate` green.
**Why:** AGY-11 created empty tables; unseeded and ungated they are schema with no evidence that the build can ever be driven from them.
**Dep:** AGY-11
**Who:** you (Python + SQL)

## AGY-18 -- Vectorize the last AI-plane tables (`event`, `session`) and extend the backfill  (WS-VECTOR | P2 | S)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- every recallable table the roadmap lists carries a populated vector.
**What+How:** Per `everything-db-driven.md` V2, add `emb vector(768)` + HNSW(`vector_cosine_ops`) + `emb_model`/`emb_version` to `event` and `session`, using the exact DDL pattern from AGY-7/AGY-11 (idempotent `ALTER … ADD COLUMN IF NOT EXISTS` plus guarded index). Extend the AGY-10/AGY-13 backfill worker to populate these over a sensible text projection — event: `act_type` + summary; session: title / first prompt — with the same idempotent and fail-open behavior. Ground truth stays in typed columns; additive only.
**Where:** `usr/share/mios/postgres/schema-init.sql`, `usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py`, `test_mios_backfill.py`
**Done When:** the schema applies idempotently, backfill fills `event.emb`/`session.emb` with provenance, a 2nd run is a near-noop, tests and `just drift-gate` green.
**Why:** Event and session history is the richest recall source the agent has and is currently reachable only by exact SQL match.
**Dep:** AGY-13
**Who:** you (SQL + Python)

## AGY-19 -- Prove the DB read-path is flip-safe (deliver the button, do not press it)  (WS-VECTOR | P1 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- an operator can flip `db_authoritative` per surface with evidence and a rollback path.
**What+How:** (1) Broaden AGY-14's shadow-compare to cover `verb`, `domain_verb`, `config_kv`, `recipe` and `routing_phrase` over a full sample, asserting 0 divergence on the seeded tree as a real assertion in `test_mios_db_config.py` rather than a log line. (2) Expose the shadow-divergence counter on the agent-pipe health/status surface so it is observable at runtime. (3) Write the exact flip + rollback runbook into `everything-db-driven.md`: which sentinel value flips which surface, how to revert, what to watch. Leave the sentinel FALSE — the flip is an operator action.
**Where:** `usr/lib/mios/mios_db_config.py`, `usr/lib/mios/agent-pipe/mios_pipe/routing/verbcatalog.py`, the health endpoint, `test_mios_db_config.py`, `usr/share/doc/mios/reference/everything-db-driven.md`
**Done When:** shadow-divergence is 0 across all listed surfaces and asserted by tests, the counter is visible on the health surface, the runbook documents the per-surface flip and rollback, `just drift-gate` green. Stage only your files, not the Quadlets.
**Why:** A flip without a measured zero-divergence proof and a written rollback is an unrecoverable production change to how every verb resolves.
**Dep:** AGY-16, AGY-18
**Who:** you (Python)

## AGY-20 -- Materialize `/ctx` from the seeded build-catalog behind a gated build step  (WS-VECTOR | P2 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the empty-`/var` chicken-and-egg is closed: image bakes the TOML seed, DB materializes `/ctx` at build.
**What+How:** Wire `materialize-build-ctx.py` into an additive, gated build step — a new `automation/NN-materialize-build-ctx.sh` at the right ordinal (after seed), or a hook in the existing package/build phase — that materializes package sets and phase order from the seeded DB into the `/ctx` files the clean build container consumes WHEN the new `[ai] build_catalog_authoritative` flag (default false) is true, and no-ops otherwise. Keep it 100% additive and fail-open: DB unreachable falls back to today's `packages.sh` TOML path. Do not flip the default.
**Where:** new `automation/NN-materialize-build-ctx.sh`, `usr/libexec/mios/materialize-build-ctx.py`, `usr/share/mios/mios.toml` (`[ai] build_catalog_authoritative=false`)
**Done When:** with the flag false the build is byte-identical to today; with it true (test) the `/ctx` package lists and phase order match the `packages.sh`/`automation` resolution diff-clean; fail-open verified with the DB down; `just drift-gate` green.
**Why:** Without this step the seeded build-catalog can never influence a build, so the rows remain a parallel description that is free to drift from the real one.
**Dep:** AGY-17
**Who:** you (Python + bash)

## AGY-21 -- Fold the next `userenv.sh` translation tranche onto the registry (Phase 2)  (WS-NAME | P2 | M)**[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- the alias count keeps dropping toward one name per fact.
**What+How:** Continue the de-translation: take the NEXT tranche of pure-translation env exports — vars that merely rename a native/native-derived key already in `names.generated.txt` — fold callers onto the single generated name and delete the duplicate. Same law as AGY-12: no loss of names or functions; leave load-bearing exports (logic, defaults, legacy-verb re-dispatch) with a `# WS-NAME: load-bearing, keep` note. Regenerate `names.generated.txt`. Keep `usr/lib/mios/userenv.sh` and `tools/lib/userenv.sh` byte-in-sync so drift-27 stays green, and drift-30 (names registry) green. Update `naming-unification.md` Phase-2 status with the fold count and keep-list.
**Where:** `usr/lib/mios/userenv.sh`, `tools/lib/userenv.sh`, callers across `usr/lib/mios/**` and `usr/libexec/mios/**`, `usr/share/mios/names.generated.txt`, `usr/share/doc/mios/reference/naming-unification.md`
**Done When:** the tranche folds with no caller referencing a deleted alias; drift-27, drift-30 and `just drift-gate` green; `bash -n` clean; keep-list documented.
**Why:** Phase 1 cleared only the first tranche; the remaining aliases still force every reader to know two spellings for one value.
**Dep:** AGY-12
**Who:** you (bash/Python + the AGY-4/AGY-12 gates)

## AGY-22 -- DB-owned account identity: home/shell columns, UID allocation, per-user prefs  (WS-VECTOR | P2 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- accounts become SSOT rows with allocatable ids and layered preferences.
**What+How:** Per `everything-db-driven.md` V4, extend `schema-init.sql` idempotently: add `home_dir` and `shell` to `account` via `ALTER … ADD COLUMN IF NOT EXISTS`; add a `uid_alloc` SEQUENCE plus `allocate_uid()`/`allocate_gid()` SQL functions starting above the reserved range (`CREATE … IF NOT EXISTS` / `CREATE OR REPLACE FUNCTION`); add `account_preference(account_id FK account(id), layer int, key text, value jsonb, emb vector(768), emb_model, emb_version, PRIMARY KEY(account_id,layer,key))` with the standard HNSW index. Extend `seed-db-config.py` to backfill `home_dir`/`shell` for existing accounts from sane defaults. Ground truth in typed columns; additive only — nothing renders from `account_preference` yet.
**Where:** `usr/share/mios/postgres/schema-init.sql`, `usr/libexec/mios/seed-db-config.py`, `test_mios_db_config.py` or new `test_mios_accounts.py`
**Done When:** the schema applies idempotently (2nd run noop), `allocate_uid()` returns monotonically increasing ids above the reserved floor, `account_preference` accepts a row and is HNSW-indexed, the seed fills `home_dir`/`shell`, tests and `just drift-gate` green. Stage only your files, never the Quadlets.
**Why:** Account identity is scattered across static skel and ad-hoc uid choices, so there is no single place to allocate an id or store a per-user preference.
**Dep:** AGY-17
**Who:** you (SQL + Python)

## AGY-23 -- Render per-user dotfiles from the DB (gated successor to static skel)  (WS-VECTOR | P2 | M)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- per-user config is a projection of `account_preference` rather than a frozen copy of `etc/skel`.
**What+How:** Write `usr/libexec/mios/materialize-user-config.py`, a DB→dotfiles materializer that per account reads `account_preference` with 3-layer precedence via `layer` and renders that user's `~/.config/mios/*` plus any owned dotfiles. Gated, additive and fail-open behind a new `[accounts] db_render_prefs` flag defaulting to false: when false today's static skel path is untouched; when true, render from the DB with skel as fallback for any absent pref. Do NOT delete `etc/skel` — it stays as the fail-open seed. Idempotent: only rewrite changed files.
**Where:** new `usr/libexec/mios/materialize-user-config.py`, `usr/share/mios/mios.toml` (`[accounts] db_render_prefs=false`), new `test_mios_user_config.py`
**Done When:** with the flag false, first-login skel seeding is byte-identical to today; with it true (test), a user's `~/.config/mios/mios.toml` renders from `account_preference` with skel fallback; a 2nd run is idempotent; tests and `just drift-gate` green.
**Why:** `etc/skel` is copied once at first login, so a changed default never reaches an existing user and per-user config immediately diverges from SSOT.
**Dep:** AGY-22
**Who:** you (Python)

## AGY-24 -- Append-only `config_event` audit (event-sourcing prep)  (WS-VECTOR | P2 | S)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- config history is reconstructable, laying the ground for V5 time-travel and rollback.
**What+How:** Add `config_event(id bigserial PK, ts timestamptz default now(), scope, key, old_value jsonb, new_value jsonb, actor, source)` idempotently to `schema-init.sql`. Make every write to `config_kv`/`verb`/`domain_verb` — in `seed-db-config.py` and any other DB writer — ALSO append a `config_event` row capturing old→new. Add a read-only replay helper `usr/libexec/mios/config-history.py` that prints the event log for a key in order. Additive; authority stays with TOML.
**Where:** `usr/share/mios/postgres/schema-init.sql`, `usr/libexec/mios/seed-db-config.py`, new `usr/libexec/mios/config-history.py`, `test_mios_db_config.py`
**Done When:** a `config_kv` write appends a well-formed `config_event` with old→new captured, `config-history.py` prints a key's event log in order, the schema is idempotent, tests and `just drift-gate` green.
**Why:** Config changes overwrite in place, so when a value turns out wrong there is no record of what it was, who changed it, or how to get back.
**Dep:** AGY-9, AGY-16
**Who:** you (SQL + Python)

## AGY-25 -- Resolve the 10 audit-confirmed defects in the WS-VECTOR code  (AUDIT-FIXES | P0/P1 | L)**[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the DB/vector plane behaves as designed: sentinel-false is truly inert, backfill is truly idempotent, and no guard wedges a session.
**What+How:** Fix each at the cited file:line (Claude already fixed the `redact.py` unquoted-secret leak and the `mios_db_config.py` port fail-open — do NOT redo those). HIGH: (1) `verbcatalog.py` ~148-215 — the T-126 "Database Overlay" block runs unconditionally (only try/except-guarded), so with the sentinel FALSE and pgvector up the DB silently overlays/deletes verbs (`cat.pop` on `is_active=false`, overwriting sig/desc/tier/permission/cmd/params); gate the whole overlay behind `is_db_authoritative()` and keep only the read-only shadow-compare when false. (2) `embed_backfill.py` ~209-248 + `toolsearch.py` ~206-231 — `emb_version` ping-pong on `verb`: backfill stamps `nomic-768-v1` while toolsearch only accepts a sha256 fingerprint and re-stamps it, so every core verb re-embeds each 15-min run; unify to ONE scheme both readers accept. (3) `agent_call.py` ~93/436/543 — `_SESSION_TOKENS[session_id]` is cumulative-only, so once a session crosses `conversation_token_ceil` every later dispatch raises `ValueError` for the process lifetime; make it a rolling window plus graceful summarize, mirroring `chat.py`'s `_budget_admit`/`_BUDGET_LEDGER`. MEDIUM: (4) `mios_db_config.py` ~13/197 — `DIVERGENCES` counts read-observations and never resets; track a SET of distinct divergent `scope.key`s so the gauge equals divergent settings. (5) `verbcatalog.py` ~234 — shadow-compare feeds the already-DB-overlaid `cat` into `_compare_catalogs`, masking real drift and firing false divergences for TOML-only verbs; snapshot a pre-overlay pure-TOML copy and compare that. LOW: (6) `verbcatalog.py` ~225 — drop/cache the redundant second blocking `psycopg.connect(connect_timeout=2)` on the sentinel-false path (~4s dead wait when the DB is down). (7) `toolsearch.py` ~231 — stamp the actual embed model, not `qwen2.5-coder:1.5b`, on nomic-produced vectors. (8) `embed_backfill.py` ~243-249 — check the `mios_pg.execute()` return before `embedded_count += 1` so silent DB failures stop counting as writes. (9) `automation/38-drift-checks.sh` check 32 (~2707-2770) — extend the "lossless" diff to `enable`/`layer`/`base_image_ref`/`section` plus `debloat_profile.description` and `preset.debloat_profile_name`. (10) `materialize-build-ctx.py` ~59 — `ORDER BY stage, ordinal NULLS LAST, script` instead of tie-breaking same-ordinal phases by DB `id`.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/verbcatalog.py`, `.../routing/toolsearch.py`, `.../memory/embed_backfill.py`, `.../agent_call.py`, `usr/lib/mios/mios_db_config.py`, `automation/38-drift-checks.sh`, `usr/libexec/mios/materialize-build-ctx.py`
**Done When:** each defect is fixed at the cited line, `test_mios_db_config` / `test_mios_backfill` / `test_mios_verbcatalog` are extended to cover the fixed behavior, `just drift-gate` green; stage only your files, never the Quadlets.
**Why:** As shipped, the default-false sentinel does not actually protect the TOML catalog, the backfill re-embeds every verb every 15 minutes, and one long session can permanently lock a user out of the agent until the daemon restarts.
**Dep:** none (NEXT — verified defects in already-shipped code)
**Who:** you (Python + bash + SQL)

## AGY-26 -- Fuse lexical BM25 with dense pgvector recall and add a gated cross-encoder rerank to the RAG path  (WS-VECTOR | P1 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- makes PostgreSQL+pgvector a recall engine good enough that the local agent plane can answer from operator-owned knowledge instead of a vendor index.
**What+How:** The knowledge/`mios_rag` queries in `mios_pipe/memory/pg.py` run pure dense `<=>` cosine search even though the schema is hybrid-capable and a Reciprocal Rank Fusion helper already ships (currently used only for tool selection). (1) Add a Postgres FTS/`ts_rank` BM25-style query over the same rows, run it alongside the dense query, and fuse both rank lists through that existing RRF helper, returning the fused top-k. (2) Add an optional cross-encoder rerank stage over the fused candidates — a small bge-reranker-v2-m3 / Qwen3-Reranker GGUF served through the light lane — behind `[ai] rag_rerank` (default false), failing open to the fused order when the reranker is unreachable. Ground truth stays typed; both stages are additive so flags-off behavior is today's behavior.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/memory/pg.py`, the RRF helper's module, `usr/share/mios/mios.toml` (`[ai] rag_hybrid` / `rag_rerank`), `test_mios_*`.
**Done When:** hybrid recall returns fused dense+lexical results; rerank reorders sanely when enabled and fails open when the lane is down; recall is not worse than today with both flags off; tests and `just drift-gate` are green.
**Why:** A dense-only recall path misses exact-token matches (error strings, flag names, file paths) that lexical search trivially finds, so the agent silently retrieves the wrong context while an unused fusion helper sits in the tree.
**Dep:** after AGY-18 (vectors exist). Source: `usr/share/doc/mios/reference/upstream-gaps-2026-07.md` gaps 4+5.
**Who:** you (Python + SQL)

## AGY-27 -- Bump emb_version to end the two-model 768-d space collision and add EmbeddingGemma task prefixes at the embed seam  (WS-VECTOR | P1 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- keeps the `emb vector(768)` columns a single coherent space so recall is trustworthy rather than randomly mixing two models' geometries.
**What+How:** Two embedding-correctness bugs. (a) The served embed model was swapped nomic-embed-text -> EmbeddingGemma-300m under the SAME served name AND the SAME `emb_version`, so two incompatible 768-d spaces now share one namespace: mint a new fingerprint that includes the actual model + revision, so old-space vectors read as stale and are re-embedded once, and make `embed_backfill.py` and `routing/toolsearch.py` agree on that ONE fingerprint (this is the same unification AGY-25 finding #2 asked for — land them together so the two do not ping-pong). (b) EmbeddingGemma requires task prompt templates (`task: search result | query: …` and the document-side prefix) but the embed call path (the `server.py` embed seam plus `embed_backfill.py`) sends raw text, degrading recall; add the query/document prefixes at the seam, gated on the active embed model so nomic-only deployments are untouched. Then trigger one full re-embed via the backfill worker.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py`, `routing/toolsearch.py`, the embed seam in `server.py`, `usr/share/mios/mios.toml` (embed model/version), `test_mios_backfill.py`.
**Done When:** old-space vectors are invalidated and re-embedded exactly once (no re-embed loop); EmbeddingGemma requests carry the task prefixes; the nomic path is byte-unchanged; recall verified sane; tests and `just drift-gate` green.
**Why:** Right now nearest-neighbour results are computed across two unrelated vector spaces under one version label, so every memory/tool lookup is quietly degraded and no backfill can tell which rows are wrong.
**Dep:** NEXT after AGY-25 — overlaps AGY-25 finding #2 (unify the emb_version scheme first). Source: gaps 6+7.
**Who:** you (Python + SQL)

## AGY-28 -- Close the four residual defects an adversarial re-verify found still open after AGY-25  (WS-GUARD | P1 | M) **[DONE]**
**Goal:** E-24 Autonomy guardrails -- the token budget must hard-stop with a graceful summarize, and the divergence/fingerprint gauges must be honest, so a self-driving loop degrades instead of erroring or churning.
**What+How:** A Claude adversarial re-verify confirmed AGY-25 landed 10/10 but left four tails. (1) `agent_call.py` D3: the rolling window is in and the permanent wedge is gone, but exceeding the 1h-windowed `conversation_token_ceil` still raises `ValueError` (a 500) instead of degrading — mirror `chat.py`'s `_budget_admit`: summarize/trim then admit, never a hard raise on a live session. (2) `mios_db_config.py` + `verbcatalog.py:~246` D4: `get_divergences()` sums the deduped `_DIVERGENT_KEYS` set PLUS the raw int `DIVERGENCES`, and verbcatalog still does `mios_db_config.DIVERGENCES += 1` per reload — route the verbcatalog contributor into the same `_DIVERGENT_KEYS` set at scope.key granularity and delete the raw-int path so the gauge counts distinct divergent settings. (3) `embed_backfill.py` D2: `_verb_embed_fingerprint()` reads toolsearch's module-global `_VERB_CATALOG`, so in a worker process where `configure()` never ran, the fingerprint is computed over an empty catalog and churn returns — guard the run (skip + log when unpopulated) or compute the fingerprint from the SSOT source both processes read. (4) `verbcatalog.py` D6: on the sentinel-FALSE path the first load still makes one blocking `psycopg.connect(connect_timeout=2)` before `_DB_UNREACHABLE` caches — short-circuit the first probe too.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/agent_call.py`, `chat.py`, `mios_db_config.py`, `routing/verbcatalog.py`, `memory/embed_backfill.py`.
**Done When:** each defect is fixed at the cited file:line, tests are extended to cover all four, `just drift-gate` is green, and only your files are staged.
**Why:** A live conversation still 500s at the ceiling, the divergence gauge double-counts so operators cannot trust it, the backfill worker can re-embed the world on a cold process, and every cold verb load pays a 2s stall.
**Dep:** NEXT (follows AGY-25).
**Who:** you (Python)

## AGY-29 -- Fix the materializer's home-escape path guard and its lossy hand-rolled TOML re-serialize  (WS-DB | P2 | S) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the DB->TOML materializer must round-trip losslessly and must not let seeded prefs write outside the target home.
**What+How:** Two defects a verify pass flagged in `materialize-user-config.py`. (1) At ~line 176 the path guard is `startswith('/home/bob')`, so a seeded `file:` pref such as `../bob-evil/.bashrc` resolves to `/home/bob-evil/.bashrc` and passes — replace with `os.path.commonpath([home, resolved]) == home` (or a trailing-separator compare). This path is live only when `db_render_prefs=true` (default false), but it must be correct before that flag is ever flipped. (2) At ~line 37 `parse_simple_toml` renders `~/.config/mios/mios.toml` by parse-then-reserialize, which drops comments and cannot represent inline tables, arrays-of-tables or multiline arrays — read with `tomllib` (never the naive parser) and either re-emit through a real TOML writer / comment-preserving strategy, or narrow the render to the typed pref slots instead of rewriting the whole file.
**Where:** `usr/lib/mios/agent-pipe/materialize-user-config.py` (~37, ~176), the accompanying `test_mios_*` module.
**Done When:** the guard uses `commonpath`; the user-config render is lossless or slot-scoped; tests cover both the traversal case and a comment/inline-table round-trip; `just drift-gate` green.
**Why:** Flipping `db_render_prefs` today hands a DB row the ability to write an arbitrary sibling home's dotfiles, and any materialize pass silently deletes the operator's comments and flattens their TOML structures.
**Dep:** after AGY-28.
**Who:** you (Python)

## AGY-30 -- Canonicalize bool/enum values and verb aliases to ONE form across the SSOT verb tables and DB verb rows  (WS-NAME | P1 | M) **[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- one fact, one spelling, so no consumer has to know that `True`, `1` and `"yes"` are the same value.
**What+How:** Operator campaign to minify and canonicalize verbs, keys AND values. Add a canonicalizer in the verb-catalog load path (`verbcatalog.py` normalization) that deterministically folds bool/int/enum arg-values to canonical lowercase `true`/`false` and de-duplicates redundant verb aliases; sweep `[verbs.*]` in `mios.toml` for `True`/`1`/mixed-case bools and fold them; sweep the `verb`/`domain_verb` DB rows the same way; then add a drift-check that fails if a non-canonical bool literal reappears. Register any shared names through the existing names-registry generator. SCOPE FENCE to avoid collision: verbs tables and non-quadlet config values only — do NOT touch `tools/lib/userenv.sh`, `usr/lib/mios/userenv.sh`, `tools/generate-pod-quadlets.py` or the generated `*.container` (Claude owns the key-library and quadlet value-render collapse).
**Where:** `usr/share/mios/mios.toml` (`[verbs.*]`), the `verb`/`domain_verb` DB rows, `usr/lib/mios/agent-pipe/mios_pipe/routing/verbcatalog.py`, the names-registry generator, `automation/38-drift-checks.sh`.
**Done When:** every `[verbs.*]` bool is canonical lowercase, alias de-dup is deterministic and idempotent, and the new drift-check reds an injected `True` literal while staying green on HEAD.
**Why:** Every consumer currently carries its own truthiness coercion, and duplicate aliases mean the same verb resolves differently depending on which table a caller reads.
**Dep:** parallel-safe. Sibling of WS-NAME.
**Who:** you (Python + SQL + TOML)

## AGY-31 -- Reconcile the duplicate `etc/containers/systemd/*.container` copies down to `.d/` drop-ins per USR-OVER-ETC  (WS-SBOM | P2 | S) **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening -- rendered Quadlets carry build-resolved digests, so no stale hand-copied unit may shadow them at deploy time.
**What+How:** A verify pass found `etc/containers/systemd/*.container` copies living alongside the generated `usr/share/...` units; because systemd prefers `/etc`, any stale digest in those copies defeats the digest-determinism fix at DEPLOY time. Enumerate every `etc/containers/systemd/*.container`, diff each against its generated `usr/share/containers/systemd` twin, then per LAW USR-OVER-ETC (where `/etc` is admin-override only) either (a) delete the redundant `etc/` copy when it is just a stale mirror, or (b) when it is a deliberate override, reduce it to a `.d/` drop-in carrying ONLY the overridden keys — never a full duplicate with a pinned `Image=`. Add a drift-check that fails if a full-unit `etc/` duplicate reappears.
**Where:** `etc/containers/systemd/*.container`, `usr/share/containers/systemd/*.container`, `automation/38-drift-checks.sh`.
**Done When:** no full-unit `etc/` duplicate shadows a generated unit with a divergent or stale `Image=`; every surviving override is a drop-in; the new drift-check is added; `just drift-gate` green.
**Why:** A deployed host can silently run a months-old image digest from an `/etc` mirror while the build, the SBOM and the gate all report the fresh one.
**Dep:** after Claude's #12 digest root-fix lands (DONE, commit 852b605).
**Who:** you (bash + audit)

## AGY-32 -- Prove the config_event secret redaction with a test, and audit the verb audit triggers for credential leakage  (WS-GUARD | P2 | S) **[DONE]**
**Goal:** E-24 Autonomy guardrails -- secret/PII redaction on every persist must be a tested invariant, not an assumed one.
**What+How:** `mios_redact_config_value()` plus the wrapping of `log_config_kv_change()` (commit 3abc92f) mask seeded secrets (`identity.default_password`, `[security].*`, `[agent_passport].*`) to `[REDACTED_SECRET]` in `config_event`, but nothing proves it. Add a test — extend `test_mios_pg` or create `test_mios_config_audit` — that seeds a config_kv row under a secret-bearing key and asserts the resulting `config_event.new_value` is exactly `"[REDACTED_SECRET]"`, plus a non-secret key that passes through verbatim. Then audit the `verb` / `domain_verb` audit triggers (`log_verb_change` / `log_domain_verb_change`, which capture `to_jsonb(NEW)`): if any verb field can carry a credential — for example a `cmd` with an inline token — apply the same redaction; otherwise document in the trigger why those rows are secret-free.
**Where:** `test_mios_pg` / new `test_mios_config_audit`, the schema-init trigger definitions for `log_config_kv_change` / `log_verb_change` / `log_domain_verb_change`.
**Done When:** the redaction test is green (secret masked, non-secret verbatim), verb-trigger secret-safety is either fixed or documented, and `just drift-gate` is green.
**Why:** An untested redaction is one refactor away from writing the operator's default password into an append-only audit table nobody scrubs.
**Dep:** after AGY-28.
**Who:** you (SQL + Python test)

## AGY-33 -- Make config_event redaction VALUE-aware so container env secrets stop landing in the audit log verbatim  (WS-GUARD | P2 | M) **[DONE]**
**Goal:** E-24 Autonomy guardrails -- redaction must hold for secrets nested inside a non-secret scope, not just for secret-named scopes and top-level keys.
**What+How:** The Day-0 readiness audit found a coverage gap in `mios_redact_config_value()` (commit 3abc92f): `seed-db-config.py` §1c seeds the `[containers]` scope into config_kv, and those Environment arrays embed `K3S_TOKEN=...` (mios.toml:10004), `WEBUI_SECRET_KEY=...` (:10183), `POSTGRES_PASSWORD` / `SUDO_PASSWORD` / `PASSWORD` (9717-18, 10264). The trigger only redacts when `scope IN ('security','agent_passport')` or the IMMEDIATE key matches the secret regex, so `scope='containers', key='mios-k3s'` matches neither and the whole container JSON lands verbatim — the same structural miss applies to any secret nested in a subtable of a non-secret scope. Pick one and document why: (a) make `mios_redact_config_value` recurse the JSONB value and mask both any object key matching the secret regex and any string of the `NAME=VALUE` env-array shape whose NAME matches; or (b) exclude `scope IN ('containers','image')` from the config_kv seed loop in `seed-db-config.py` (they are Quadlet-shaped, not operator config, so this also trims audit noise). Extend the AGY-32 test to seed a container row carrying an env secret and assert the masked `new_value`.
**Where:** the `mios_redact_config_value(scope,key,value)` trigger function, `seed-db-config.py` §1c, `test_mios_config_audit`, `usr/share/mios/mios.toml` (:9717-18, :10004, :10183, :10264).
**Done When:** no container or otherwise-nested secret reaches `config_event` in plaintext, a test proves it, and `just drift-gate` is green — with the `mios_redact_config_value(scope,key,value)` signature contract preserved (do not change Claude's schema-init trigger signature).
**Why:** Today a routine seed writes K3s tokens, the WebUI secret key and the Postgres password in cleartext into an append-only audit table that survives every rollback.
**Dep:** after AGY-32.
**Who:** you (SQL + Python test)

## AGY-34 -- Resolve the `mios-llm-heavy-alt` drop-in that targets a base Quadlet that does not exist  (WS-RUNTIME | P3 | S) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- every declared unit is either really wired and gated, or removed with a recorded decision.
**What+How:** The readiness audit (LOW) found `MIOS_AI_QUADLETS_DEFAULT` in `usr/libexec/mios/mios-gpu-passthrough:85` lists `mios-llm-heavy-alt`, and the tree ships `etc/containers/systemd/mios-llm-heavy-alt.container.d/20-mios-igpu.conf`, but no base `mios-llm-heavy-alt.container` exists under `usr/share/containers/systemd` (only `mios-llm-heavy.container`) — so the reconciler refreshes a drop-in dir the podman generator ignores, producing dead wiring plus a needless `daemon-reload` every reconcile. SGLang :8442 is a stated lane (CLAUDE.md), so EITHER add the `mios-llm-heavy-alt` base quadlet to `mios.toml [containers.*]` and regenerate (SGLang lane, gated off by default, mirroring `mios-llm-heavy` including `After=mios-cdi-detect.service`) — preferred if the lane is meant to ship — OR remove it from `MIOS_AI_QUADLETS_DEFAULT` and `git rm -r etc/containers/systemd/mios-llm-heavy-alt.container.d`. Stage explicit paths; if you regenerate, never a broad `git add` (it strips `@sha256` pins — the generator fix in 852b605 must stay green).
**Where:** `usr/libexec/mios/mios-gpu-passthrough:85`, `etc/containers/systemd/mios-llm-heavy-alt.container.d/20-mios-igpu.conf`, `usr/share/mios/mios.toml [containers.*]`, `usr/share/containers/systemd/`.
**Done When:** no reconciler drop-in targets a non-existent base quadlet, `mios-gpu-passthrough` reconcile is a no-op on an unchanged tree, and `just drift-gate` is green.
**Why:** Every GPU reconcile pays a pointless daemon-reload while the tree advertises an SGLang lane that can never start.
**Dep:** parallel-safe; coordinate with AGY-31 (same `etc/containers` space).
**Who:** you (bash + Quadlet)

## AGY-35 -- Make the two DB-integration tests skip cleanly offline so they stop aborting the OCI build  (WS-TESTDOC | P1 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness -- the test plane must be hermetic enough to run inside the build without a live service.
**What+How:** The first full `mios build` (HEAD 8da391d) reached the agent-pipe unit-test gate and two tests aborted the image build because they need a live pgvector that cannot exist during an offline build; the stopgap added `_DB_INTEGRATION_TESTS` in `automation/build.sh` to skip them. Make them properly build-safe so the skip can be removed. (1) `test_mios_db_config.py:48,88` makes a real `psycopg.connect("postgresql://mios:mios@localhost:8432/mios")` — add a module-level guard that attempts `connect(..., connect_timeout=1)` in `setUpModule` and raises `unittest.SkipTest("no live pgvector")` on failure, so the suite SKIPS (never fails) when the DB is absent and still runs fully in DB-present CI/runtime. (2) `test_mios_build_catalog.py:85` — `test_materialization` fails `assertIn("package_sets.json", written_files)` with `written_files == {}` because the mocked cursor no longer matches what `materialize-build-ctx.py` writes (likely drifted after the AGY-25 D10 `ORDER BY` change); fix the mock to return rows that make materialize write `package_sets.json`, keeping it a hermetic unit test rather than adding a skip guard.
**Where:** `test_mios_db_config.py:48,88`, `test_mios_build_catalog.py:85`, `materialize-build-ctx.py`, `automation/build.sh` (`_DB_INTEGRATION_TESTS`).
**Done When:** both tests SKIP cleanly with no DB and PASS against a live DB; they are then removed from `_DB_INTEGRATION_TESTS` in `automation/build.sh` (coordinate — that file is Claude's) or left with a `TODO(agy)` noting they are safe to un-skip; `just drift-gate` green.
**Why:** Until this lands, two tests are excluded by name from the build gate, so real regressions in the DB-config and build-catalog paths ship unnoticed.
**Dep:** NEXT.
**Who:** you (Python test)

## AGY-36 -- Make the AI plane degrade-open QUIETLY: rate-limit the embed-failure log and give every AI unit a restart policy  (WS-RUNTIME-WIRE | P1 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a dependency that is not up yet must produce self-healing, not an outage and a journal flood.
**What+How:** Operator feedback from the live reinstall: "TOO MANY OUTAGES, not hardened." Observed root cause: when `mios-llm-light` (the embed backend) is down for lack of GGUFs, `mios-agent-pipe` and hermes emit `WARNING embed call failed: All connection attempts failed` hundreds of times per millisecond while still serving via the 0-embedding degrade-open. (1) In the embed call path under `mios_pipe/**` (the toolsearch/embed seam), rate-limit and dedupe the failure log — log ONCE then exponential backoff, not per-verb-per-call — and keep the service serving `:8640`/`:8642` with zero embeddings, re-embedding automatically once llm-light appears (poll/backoff, no restart required). (2) Audit every `usr/share/containers/systemd/mios-*.container` and `usr/lib/systemd/system/mios-*.service` for `Restart=on-failure`, a sane `RestartSec`, and correct `After=`/`Wants=`, so a unit self-heals when its dependency later appears without an operator re-deploy; report any AI-plane unit missing `Restart=`.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/**` (embed/toolsearch seam), `usr/share/containers/systemd/mios-*.container`, `usr/lib/systemd/system/mios-*.service`.
**Done When:** no journal flood while llm-light is down; agent-pipe and hermes stay `active` and return HTTP 200 on `/v1/models`; embeddings backfill automatically when llm-light returns; every AI unit carries a restart policy; `just drift-gate` green.
**Why:** A single missing GGUF currently fills the journal at line-per-millisecond rates (masking real errors and burning disk) and leaves the plane permanently degraded until a human redeploys.
**Dep:** NOW.
**Who:** you (Python + systemd)

## AGY-37 -- Fix the three flatpak IDs that fail on every install and auto-disambiguate ambiguous refs  (WS-RUNTIME | P2 | S) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- what SSOT declares as installed must actually install, or be removed with a decision.
**What+How:** The live reinstall's `[desktop].flatpaks` pass logged `13 OK / 3 failed`: `org.virt_manager.Manager` ("Nothing matches … in remote flathub" — wrong id; virt-manager is not a flatpak, so drop it or use the correct app id), `io.github.dosbox_staging.dosbox-staging` ("Nothing matches" — verify the current flathub id or drop), and `org.freedesktop.Platform.VulkanLayer.MangoHud` ("Multiple refs match … unable to proceed in non-interactive mode" — needs an explicit `//<branch>`, e.g. `//24.08`). Correct or drop the three ids in `mios.toml [desktop].flatpaks`, and make `automation/10-gnome.sh` (or the overlay flatpak installer) append a default `//<runtime-branch>` whenever an id is ambiguous so non-interactive installs can never stall on "multiple refs".
**Where:** `usr/share/mios/mios.toml` (`[desktop].flatpaks`), `automation/10-gnome.sh` / the overlay flatpak installer.
**Done When:** a fresh overlay flatpak pass reports 16/16 (or the dropped ids are gone from SSOT), no "multiple refs" or "nothing matches" appears on install, and `just drift-gate` is green.
**Why:** Every single install prints three red failures, which trains the operator to ignore install errors and hides the next real one.
**Dep:** none.
**Who:** you (TOML + overlay)

## AGY-38 -- Make `mios-ai-firstboot` honor its sentinel so it stops re-provisioning on every boot  (WS-RUNTIME-WIRE | P1 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- first-boot provisioning must be idempotent so an image deploy is a one-time event, not a per-boot rebuild.
**What+How:** Operator: "Must deploy everytime." `mios-ai-firstboot` has a sentinel meant to gate network-less boot retries, but it keeps re-activating (the venv build retries under a 40-min `TimeoutStartSec`). Audit the SENTINEL and success semantics ONLY — do NOT touch the venv-BUILD fallback, which Claude owns: (1) once the venv is present AND the GGUF/`bake_models` state is a config-inert no-op (an empty `bake_models` is NOT a failure), firstboot must exit 0 and write its sentinel so the next boot does not re-provision; (2) a missing GGUF (a config condition) or an egress-down optional step must be a graceful skip — warn, continue, succeed — never a hard fail or timeout loop; (3) verify the `ConditionPathExists`/sentinel actually short-circuits a second run.
**Where:** `mios-ai-firstboot` (unit + script), its sentinel path and `ConditionPathExists=`.
**Done When:** on a host with the venv present and empty `bake_models`, firstboot reaches `active (exited)` rather than activating/timeout, writes its sentinel, and a re-run is a no-op with no redeploy needed; `just drift-gate` green.
**Why:** Every boot currently burns up to 40 minutes rebuilding a venv that already exists, and the operator reads the machine as needing a full redeploy each time.
**Dep:** Coordinate with Claude on the `mios-ai-firstboot` file (Claude is patching the venv-from-image fallback in parallel — stage disjoint hunks).
**Who:** you (bash)

## AGY-39 -- Give Day-0 a working inference lane by baking or first-boot-pulling one default GGUF  (WS-BAKE | P2 | M) **[DONE]**
**Goal:** E-16 The bake plane -- a published image must CONTAIN what a fresh host needs, so the AI plane is alive on first boot with no operator configuration.
**What+How:** `[llamacpp].bake_models` is empty, so `mios-llm-light` stays inert on every Day-0 install and the whole inference/embedding plane is dead until the operator configures it by hand — this is the observed "0 embeddings" plus embed-call-failed state. Pick the robust option: either set a small sensible default in `[llamacpp].bake_models` (the granite4.1:8b or lfm2:700m GGUF already referenced in `mios-llm-light.yaml`) so it bakes into the image, or wire `mios-ai-firstboot` to pull ONE small default GGUF post-deploy (egress-gated, degrade-open) so a fresh install gets a working light lane and embeddings out of the box. Document the VRAM/size tradeoff of whichever you choose.
**Where:** `usr/share/mios/mios.toml` (`[llamacpp].bake_models`), `mios-llm-light.yaml`, `mios-ai-firstboot`.
**Done When:** a Day-0 install brings up `mios-llm-light` on :8450 with at least the embed model (nomic/EmbeddingGemma) so agent-pipe reports more than 0 embeddings without operator action; `just drift-gate` green.
**Why:** The flagship claim — a local, self-hosted agentic AI OS — is false on arrival on every fresh install, and it manifests as the log flood AGY-36 is separately mopping up.
**Dep:** none.
**Who:** you (TOML + firstboot)

## AGY-40..44 -- Batch banner and coordination gate for the tech-debt / ADR-0010+0011 batch (file ownership + Law-14 fence)  (WS-DEBT | P1 | S) **[DONE]**
**Goal:** E-02 Technical-debt retirement -- the TD-1..TD-8 register is worked in a shared tree without two agents clobbering each other's staged hunks.
**What+How:** Batch header for AGY-40..44, derived from the combined tech-debt map plus ADR-0010/ADR-0011 research. It records the coordination gate: Claude holds UNCOMMITTED, staged changes to `automation/38-drift-checks.sh`, `usr/share/mios/mios.toml`, `usr/libexec/mios/mios-theme-render`, `etc/btop/btop.conf`, `automation/90-generate-sbom.sh` and the ADR / `ROADMAP.md` / `TASKS.md` docs, so none of those may be touched until they land on `main` (`git pull`, then `git status` to confirm clean). Safe to start immediately because they are disjoint: AGY-40 (bakers), AGY-41's eval-verb fixes, AGY-44's templates + scaffolder. Must wait for the push: every hunk editing `automation/38-drift-checks.sh` — the drift-check ADDITIONS in AGY-41/42/43/44 and all of AGY-42/43. It also fences Law 14 (ONE-TEMPLATE-PER-TYPE): it is PROPOSED and operator-gated, so no id-14 row may be added to `mios.toml [laws]` and no new check may fail the gate on it. ADR-0010's `mios-theme-render` engine work stays Claude's; AGY takes the code-only debt tasks.
**Where:** `automation/38-drift-checks.sh`, `usr/share/mios/mios.toml`, `usr/libexec/mios/mios-theme-render`, `etc/btop/btop.conf`, `automation/90-generate-sbom.sh`, `usr/share/doc/mios/adr/0010-ssot-as-system-dotfiles.md`, `usr/share/doc/mios/adr/0011-unified-languages-and-file-patterns.md`, `TASKS.md` (T-268..T-273), `ROADMAP.md` (TD-1..TD-8 under WS-DEBT).
**Done When:** AGY-40..44 are all landed with no lost hunk in the shared files, `mios.toml [laws]` still has no id-14 row, and `just drift-gate` is green after each merge.
**Why:** Both agents edit `38-drift-checks.sh` and `mios.toml`; without this gate one side's staged work is silently overwritten and the drift-gate goes red for reasons nobody can attribute.
**Dep:** New references in tree: T-268..T-273 (`TASKS.md`), TD-1..TD-8 (`ROADMAP.md`), ADR-0010 + ADR-0011.
**Who:** AGY (batch, 2026-07-16)

## AGY-1688 -- Pin and harden the network-at-build bakers to kill the "clone default-branch, WARN forever" class  (WS-DEBT | P1 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement (TD-4) -- a build phase that fails on every run must be pinned, retried and VISIBLE rather than silently green.
**What+How:** `automation/55-bake-quickshell.sh` clones a mirror's DEFAULT branch then `cmake && make install`; `56-bake-surfer.sh` clones, `npm install`, `npx surfer download` and builds — both currently fail-and-WARN on every build and ship nothing. Apply the `automation/38-hermes-agent.sh` reference pattern (commit 31a52fb1): (1) PIN every `git clone` to an explicit tag/commit sourced from SSOT by adding a `[build.bake_refs]` table to `mios.toml` (`quickshell` / `surfer` / `hyprland` -> intent ref) so the pin is operator-defined, not hardcoded — per ADR-0003 the SSOT carries the `:latest`/branch INTENT while the RESOLVED commit is recorded to the SBOM / `bound-images.tsv` at build; (2) wrap clone+build in a 3x retry-with-backoff, VERIFY the artifact exists afterward, and keep failures non-fatal to sibling bakers; (3) make a persistent failure countable and visible in the build report. Stay degrade-open — never hard-fail the bake, just make the failure observable. Grep for any other `automation/*bake*.sh` that clones-and-builds and give it the same treatment.
**Where:** `automation/55-bake-quickshell.sh`, `automation/56-bake-surfer.sh`, other clone-and-build `automation/*bake*.sh`, `usr/share/mios/mios.toml` (new `[build.bake_refs]`).
**Done When:** every baker clones a PINNED ref with no default-branch clone, retries 3x with backoff, verifies its artifact, and surfaces persistent failure; the pin reads from SSOT (Law 7); `bash -n` clean; `just drift-gate` green.
**Verify:** an injected default-branch clone in any baker turns the retry/pin gate RED, and a fetch with no `--retry` is reported by name.
**Do NOT:** leave a WARN. A warning that nobody fails on is a clone of an unpinned ref shipped forever.
**Why:** The quickshell/surfer/hyprland bakes have been shipping nothing for an unknown number of builds while the build log reads green, and an unpinned default-branch clone means the image content is whatever upstream pushed that hour.
**Dep:** NOW — disjoint from Claude's staged set; closes the live quickshell/surfer/hyprland bake WARNs.
**Who:** you (bash)

## AGY-41 -- Eliminate the `eval`-on-agent-args command-injection surface in the verb library and guard it with a check  (WS-DEBT | P0 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement (TD-1 / T-269) -- the eval-on-agent-args injection surface is gone from the verb library and cannot come back.
**What+How:** The tech-debt audit found ~9 runtime verbs under `usr/libexec/mios/` that `eval` agent-derived arguments — a command-injection surface, since a tool or agent controlling a verb argument can execute arbitrary shell. (1) `grep -rn 'eval ' usr/libexec/mios/` to enumerate the exact set; (2) replace each `eval` with a safe form — quoted argument arrays (`"$@"`), a `case` allowlist dispatch, or `printf %q` — so agent-supplied values can never be re-parsed as shell; (3) where an `eval` is genuinely load-bearing with NO agent-controlled input, verify provenance and leave it annotated `# TD-1: eval-safe, input=<source>, not agent-controlled` rather than blind-rewriting it (per the flatten campaign's don't-blind-delete caution). Then add a drift-check to `38-drift-checks.sh` that FAILS when a new `eval` on a `$MIOS_*` or argv value appears under `usr/libexec/mios/`.
**Where:** the ~9 `eval` verbs under `usr/libexec/mios/`, `automation/38-drift-checks.sh` (the guard — only after Claude's push, per the batch gate).
**Done When:** no verb `eval`s an agent-controlled value; each surviving `eval` carries an input-safety justification; the injection-guard drift-check is green on HEAD and reds an injected `eval "$MIOS_FOO"`; `bash -n` clean; `just drift-gate` green.
**Why:** Any agent or MCP tool that can influence a verb argument today has arbitrary shell execution as the invoking user — the single highest-severity defect in the tree.
**Dep:** the eval-fixes NOW (disjoint); the guard-check after Claude's push. Do NOT touch `mios-theme-render` (Claude-staged).
**Who:** you (bash)

## AGY-42 -- Add the missing `shellcheck` lint gate (runner + Justfile target + drift-check)  (WS-DEBT | P1 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement (TD-1 / T-269) -- shellcheck becomes a real CI gate instead of a convention documented in comments.
**What+How:** The repo documents shellcheck conventions via `# shellcheck source=` directives, but there is no shellcheck job anywhere, so nothing enforces them. (1) Add `automation/lint-shell.sh` running `shellcheck` over `automation/*.sh`, `usr/libexec/mios/mios-*` and `tools/*.sh`, honoring the existing in-file directives, degrading open (WARN + skip) only when shellcheck itself is absent and otherwise gating. (2) Wire a `Justfile` `lint-shell` target and call it from `just drift-gate`. (3) Add `check_shellcheck` to `38-drift-checks.sh` at the next free check number, invoking the runner — fast and read-only. If the estate is too noisy at `error` level, land a documented baseline-allow of pre-existing findings so the gate is green today and ratchets from here.
**Where:** new `automation/lint-shell.sh`, `Justfile`, `automation/38-drift-checks.sh`.
**Done When:** `just lint-shell` runs shellcheck across the shell estate; the check is green on HEAD (with the baseline documented if used) and reds a newly-introduced `error`-level finding; `just drift-gate` passes.
**Why:** A tree whose primary language is bash has zero static analysis, so quoting and unset-variable bugs reach the image and are only found by a failed boot.
**Dep:** AFTER Claude pushes the staged `38-drift-checks.sh` (batch gate) — then `git pull`.
**Who:** you (bash + drift-gate)

## AGY-43 -- Single-source the version literal and add `check_version_ssot` to the drift-gate  (WS-DEBT | P1 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement (TD-2 / T-268) -- one fact, one declaration: the version lives in SSOT and nowhere else.
**What+How:** TD-2 found the version single-source violated: `VERSION`=0.3.0 and SSOT `mios_version`=0.3.0, but 37 script headers hardcode `v0.2.4`/`v0.2.0` and the root `C:\MiOS\mios.toml` says 0.2.4. (1) `grep -rn -E 'v0\.[0-9]+\.[0-9]+' automation/ usr/libexec/mios/ tools/` to enumerate the hardcoded literals, then replace each with a reference to `VERSION` / `mios_version` for runtime lines, or drop the version entirely from stale doc-string comments — ONE source. (2) Add `check_version_ssot` to `38-drift-checks.sh` asserting that `VERSION` equals SSOT `mios_version` and that no tracked file hardcodes a DIFFERENT version literal (allowlisting the canonical value). (3) Leave the root-`mios.toml`-vs-canonical divergence to Claude, but drop a `TODO(td-2):` where the check detects it. SCOPE: the mechanical half of TD-2 only — do NOT attempt the 3x `mios.toml` file consolidation, which is architectural and Claude-owned.
**Where:** version headers across `automation/**`, `usr/libexec/mios/**`, `tools/**`; `automation/38-drift-checks.sh`.
**Done When:** exactly one source of the version remains; the check is green on HEAD and reds an injected `v0.9.9` header; `bash -n` clean; `just drift-gate` green.
**Why:** 37 files claim the OS is v0.2.4 while it is 0.3.0, so any bug report, log line or support question quotes a version that has not existed for two releases.
**Dep:** AFTER Claude pushes the staged `38-drift-checks.sh` + `mios.toml` (batch gate) — then `git pull`.
**Who:** you (bash/Python + drift-gate)

## AGY-44 -- Ship the per-file-type template set plus the `mios new` scaffolder, with an ADVISORY conformance check  (WS-TEMPLATE | P2 | L) **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- an arriving agent learns MiOS formatting from templates instead of reverse-engineering exemplars.
**What+How:** ADR-0011 proposes that every file type starts from a template, formalizing the AI-hint header convention. Deliver the SCAFFOLD, not the enforcement. (1) Create `usr/share/mios/templates/` with one canonical template per common MiOS file type — `bash-verb`, `drift-check`, `python-module` (plus its sibling `test`), `systemd-unit`, `quadlet`, `adr`, `roadmap-ws` — each carrying the exact AI-hint header and structure distilled from the current best-in-class exemplar (`38-hermes-agent.sh` for a producer verb, an existing `test_mios_*.py` for the module+test pair, `0009-*.md` for an ADR). (2) Write `usr/libexec/mios/mios-new` implementing `mios new <type> <name>`, rendering a template to the correct path with name/date/section filled from args and SSOT. (3) Add a PROPOSED, ADVISORY-ONLY `check_template_conformance` to `38-drift-checks.sh` that WARNs (never fails) when a known file type is missing its template-mandated header — wired but non-gating, because Law 14 is operator-gated: do NOT add an id-14 `[laws]` row and do NOT let the gate fail on it. Leave a note that flipping it to gating is the operator's Law-14 call.
**Where:** new `usr/share/mios/templates/*`, new `usr/libexec/mios/mios-new`, `automation/38-drift-checks.sh` (advisory check only — batch gate applies).
**Done When:** `mios new bash-verb foo` scaffolds a compliant, `bash -n`-clean verb; every template renders and passes its own type's existing checks; the conformance check runs ADVISORY (WARN, non-gating); `[laws]` is untouched; `just drift-gate` stays green.
**Why:** Each new file is currently hand-copied from whichever neighbour the author happened to open, so header conventions and structure drift a little further with every addition.
**Dep:** the templates + scaffolder NOW (all NEW files, disjoint); the advisory check after Claude's push.
**Who:** you (Python + templates)

## AGY-45..49 -- Batch banner for tech-debt batch 2 (TD-3/4/5) plus the ADR-0010/0011 follow-ons  (WS-DEBT | P1 | S) **[DONE]**
**Goal:** E-02 Technical-debt retirement -- the remaining TD-3/TD-4/TD-5 register items are worked on a now-clean shared tree.
**What+How:** Batch header for AGY-45..49 (2026-07-16, batch 2), covering the remaining tech debt (TD-3 resolver twins, TD-4 build-visibility/supply-chain, TD-5 god-modules) plus the ADR-0010/ADR-0011 follow-ons. It records that the shared `C:\MiOS` tree is CLEAN — Claude's btop/docs/SBOM/AGY-queue work landed as commits `c8acac11`, `0bdb67e3`, `e0a5b68e`, `c5b5d599`, all of which build on AGY-41/AGY-44 and the AGY-40..44 batch — and restates the standing staging rule from the STOP banner: stage explicit paths for YOUR task only, run `git status` before every commit, and never `git add -A`.
**Where:** the shared `C:\MiOS` tree; commits `c8acac11`, `0bdb67e3`, `e0a5b68e`, `c5b5d599`; `ROADMAP.md` (TD-3/4/5 under WS-DEBT).
**Done When:** AGY-45..49 land with every commit staged by explicit path, no unrelated file swept in, and `just drift-gate` green after each.
**Why:** A broad `git add` in this shared tree strips the `@sha256` pins from generated Quadlets and reds the gate — a failure mode that has already recurred more than once.
**Dep:** builds on AGY-41, AGY-44 and the AGY-40..44 batch.
**Who:** AGY (batch 2, 2026-07-16)

## AGY-1689 -- Bind the Python and bash resolver twins with a `check_resolver_twin_equivalence` gate  (WS-DEBT | P1 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement (TD-3) -- the two SSOT resolvers can no longer diverge silently, because a gate diffs them.
**What+How:** TD-3's worst instance: `usr/lib/mios/mios_toml.py` (the Python resolver) and `usr/lib/mios/userenv.sh` (the bash resolver) must produce byte-identical env, but no generator binds them — the sync is purely by hand and therefore drift-prone. Add `check_resolver_twin_equivalence` to `automation/38-drift-checks.sh` at the next free number: (1) resolve a representative key sample through BOTH resolvers, dumping `MIOS_*` env from `mios_toml.py` and from `source userenv.sh` in a subshell; (2) diff the two dumps; (3) FAIL on any key whose value or presence differs. Keep it read-only with no live services. This complements drift-27 (which only proves `userenv.sh` matches its `tools/lib/userenv.sh` copy) by catching Python-vs-bash divergence. Allowlist any INTENTIONAL delta with a written reason, per the don't-blind-delete-load-bearing caution.
**Where:** `automation/38-drift-checks.sh`, a small `tools/dump-env-*.{sh,py}` helper if needed, plus the allowlist note.
**Done When:** the twin-equivalence check is green on HEAD, reds an injected value divergence, and lists intentional deltas with reasons; `bash -n` / `py_compile` clean; `just drift-gate` green.
**Verify:** `bash automation/98-drift-checks.sh check_resolver_twin_equivalence` is green on HEAD and RED under an injected value divergence, with intentional deltas listed rather than silently tolerated.
**Do NOT:** compare only the keys. Values diverged 56 ways while the key sets matched.
**Why:** A Python consumer and a bash consumer can already read different values for the same SSOT key, and nothing in the tree would notice until a service starts with the wrong port or path.
**Dep:** parallel-safe; independent.
**Who:** you (Python + bash + drift-gate)

## AGY-46 -- Surface the build's silent NON_FATAL failures in an end-of-build summary and pin the syft installer  (WS-DEBT | P2 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement (TD-4) -- a build phase that fails must be visible, and a build-time `curl | sh` must not be unpinned.
**What+How:** Two TD-4 items. (1) `automation/build.sh`'s `NON_FATAL_SCRIPTS` swallows a non-zero exit from roughly 23 phases with no trace — add an END-OF-BUILD SUMMARY listing every non-fatal phase that exited non-zero (name plus exit code) to the build log AND to a machine-readable marker under the SBOM artifacts dir, so a persistently-failing phase is visible instead of reading green. Do NOT change WHICH scripts are non-fatal; only surface outcomes. (2) In `automation/90-generate-sbom.sh` the syft installer is an unpinned `curl … | sh` (a supply-chain risk) — pin it to a specific syft version/tag drawn from SSOT (`[build.bake_refs]` if AGY-40 created it, otherwise a new key) and/or verify a checksum, while keeping the step degrade-open.
**Where:** `automation/build.sh` (`NON_FATAL_SCRIPTS`), `automation/90-generate-sbom.sh`, `usr/share/mios/mios.toml` (pin ref).
**Done When:** a build with a deliberately-failing non-fatal phase surfaces it in both the summary and the marker while still exiting 0; the syft installer is pinned and/or checksum-verified; degrade-open is preserved; `bash -n` clean; `just drift-gate` green.
**Why:** ~23 build phases can fail forever without anyone knowing (exactly how the AGY-40 bakers hid), and the SBOM generator — the provenance tool itself — installs an unverified binary from the network at build time.
**Dep:** `git pull` first — `90-generate-sbom.sh` was just committed by Claude (`0bdb67e3`); edit it additively and keep its degrade-open + exit-0 contract.
**Who:** you (bash)

## AGY-47 -- Extract ONE cohesive unit out of the `server.py` / `mios_dispatch.py` god-module with a sibling test  (WS-DEBT-PIPE | P1 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement (TD-5 / T-273) -- the agent-pipe god-modules begin decomposing under typed boundaries and pytest, one safe bite at a time.
**What+How:** `server.py` (8,961 lines) and `mios_dispatch.py` (the security-critical verb->bash chokepoint) are god-modules. Do NOT attempt a full split: extract exactly ONE cohesive, well-bounded unit into a NEW sibling module with a ONE-WAY import that must NOT import `server.py` (drift-check 6). Prefer the highest-value, lowest-risk seam — the verb argument validation/sanitization helper, i.e. the input-safety layer feeding dispatch, which synergizes with AGY-41's eval removal — otherwise the `_db_*` helpers or the auth middleware. Move the code, leave a thin re-export shim at the old call site so callers are unchanged, and add `test_mios_<unit>.py` (drift-check 11 requires the sibling test). Behavior must be byte-identical.
**Where:** `usr/lib/mios/agent-pipe/server.py` (or `mios_dispatch.py`), new `usr/lib/mios/agent-pipe/mios_<unit>.py` + `test_mios_<unit>.py`.
**Done When:** the unit lives in its own sibling module with a sibling test and no `server.py` import edge; the full `test_mios_*` suite is green; behavior is unchanged; checks 6 and 11 pass; `just drift-gate` green; only your files staged.
**Why:** A 9k-line module holding the security-critical dispatch chokepoint cannot be reviewed, unit-tested or safely changed, so every fix near it is a gamble.
**Dep:** parallel-safe; the safe first bite of T-273.
**Who:** you (Python)

## AGY-48 -- Prove the scaffolder round-trips and ratchet `check_template_conformance` to NEW files only  (WS-TEMPLATE | P2 | M) **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- conformance ratchets from SOFT-skip toward enforcement on new files without retro-breaking the estate.
**What+How:** AGY-44 delivered `usr/share/mios/templates/*`, `usr/libexec/mios/mios-new` and an ADVISORY `check_template_conformance`. Advance toward Law 14 without a global flip. (1) Prove the round-trip: pick 2-3 RECENTLY-added files of a templated type and confirm `mios new <type>` reproduces their canonical header and structure; where it does not, fix the template rather than the file. (2) Make `check_template_conformance` RATCHETING — it FAILS only for files ADDED after a baseline commit that lack their type's template header, grandfathering the existing estate via a baseline allowlist file, so new files must conform while nothing pre-existing turns red. Still do NOT add an id-14 `[laws]` row; Law 14 remains operator-gated.
**Where:** `usr/share/mios/templates/*`, `usr/libexec/mios/mios-new`, `automation/38-drift-checks.sh`, a new baseline allowlist file.
**Done When:** `mios new` reproduces the chosen exemplars; the check reds a newly-added non-conforming file of a known type and passes on HEAD with the baseline grandfathered; `[laws]` untouched; `just drift-gate` green.
**Why:** An advisory-only check is ignored within a week, so the template set decays into documentation and every new file resumes drifting.
**Dep:** after AGY-44 (its templates, `mios-new` and advisory check are committed).
**Who:** you (Python + bash)

## AGY-49 -- Project a non-identity `[gitconfig]` section to `etc/skel/.gitconfig` as ADR-0010's second dotfile surface  (WS-DOTFILES | P3 | M)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- proves the `[dotfiles.registry.*]` engine generalizes past color-only surfaces by adding a second, non-theme dotfile driven entirely from SSOT.
**What+How:** btop already proved `mios-theme-render`'s settings-surface pattern (project any `mios.toml` section to a build-tree file, drift-gated). Repeat it: add a `[gitconfig]` section holding only NON-identity git settings (`core.editor`, `init.defaultBranch`, `pull.rebase`, `[alias].*`), write `gitconfig.tmpl`, register the surface in the `SURFACES` table inside `mios-theme-render`, and render to the vendor skel that seeds HOME. Per Law 9 ONE-CANONICAL-NAME, `user.name`/`user.email` must NOT be duplicated into `[gitconfig]` -- the template dereferences `[identity]` instead. Live-HOME `apply` is explicitly out of scope (that is the engine work in AGY-57/58); this is build-tree render only.
**Where:** `usr/share/mios/mios.toml` (new `[gitconfig]` referencing `[identity]`), `usr/share/mios/theme/templates/gitconfig.tmpl`, `usr/libexec/mios/mios-theme-render`, `etc/skel/.gitconfig`, `automation/38-drift-checks.sh`
**Done When:** `mios-theme-render render` emits `etc/skel/.gitconfig` from `[gitconfig]`+`[identity]`; `check` includes it and passes; hand-tampering the rendered file turns drift-check 25 RED; no identity value exists outside `[identity]`; `just drift-gate` green.
**Why:** without a second, non-color surface the dotfiles engine stays an unproven one-off, and git settings remain hand-maintained per-machine outside the SSOT.
**Dep:** the btop settings-surface (landed); SUPERSEDED by the ADR-0010 `[dotfiles.registry]` engine workflow.
**Status:** SUPERSEDED / SKIP -- folded into the ADR-0010 [dotfiles.registry] engine Claude is building now (a full workflow); that engine adds the gitconfig surface itself via the SSOT registry. Do NOT start this task. | **Who:** you (Python + TOML)

---

## AGY-50 -- Add server-side GET/POST `/portal/config` so the Portal can read and persist the layered mios.toml  (WS-CONFIG | P1 | M)  **[DONE]**
**Goal:** E-11 Unified config surface: mios.toml, the configurator and the Portal are one door at :8640/ -- closes the loop so the operator edits SSOT in the browser instead of downloading a file.
**What+How:** the Portal already serves the configurator (`GET /portal/configurator` iframes `mios.html`), but the client can only save via browser download because there is no server read/write path. Add `GET /portal/config` returning the live vendor<host<user LAYERED TOML as `text/plain` (reuse the `tomllib.load` path `mios_pipe/kernel/config.py` already uses for `[colors]`/`[ports]`), and `POST /portal/config` taking raw TOML, validating that it parses, and PERSISTING to the user layer `~/.config/mios/mios.toml` by default (host `/etc/mios/mios.toml` only for a privileged session). Add the missing WRITER to the read-only `config.py` kernel: atomic temp+rename, preserving cascade semantics. Persist ONLY -- never trigger `mios build`. Auth reuses `_portal_authed` and honors `[security].api_require_auth`; unauthorized returns 403/404 so the client degrades cleanly to download. Wire routes in `mios_pipe/routing/portal.py` plus the thin `@app` entry in `server.py`.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py`, `usr/lib/mios/agent-pipe/mios_pipe/routing/portal.py`, `usr/lib/mios/agent-pipe/server.py`, new `test_mios_config_write.py`
**Done When:** `GET /portal/config` returns the live layered TOML; `POST` writes `~/.config/mios/mios.toml` atomically with no build triggered; unauthenticated requests get 403 and the client falls back to download; the new test passes; `just drift-gate` green.
**Why:** today the one config surface is read-only in the browser -- every operator edit is a manual download-and-copy, so the ":8640/ is the single config door" claim is false in practice.
**Dep:** AGY-47 (server.py decomposition) -- coordinate the routing wiring; Claude owns the `mios.html` client half.
**Status:** OPEN (operator may adjust): the write-layer default (per-user vs host), and whether a future 'Apply' action should trigger `mios build` (out of scope here -- persist-only for now). | **Who:** you (Python, agent-pipe)

## AGY-51..54 -- Batch-3 charter: first Rust proof, deeper god-module decomposition, bake-ref audit, gate negative-tests  (WS-LANG,WS-DEBT | P1 | L)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- bundles the four 2026-07-16 work items that move the debt register and plant the compiled-tier seed in one review pass.
**What+How:** scoping banner for AGY-51 (cargo workspace + first Rust binary), AGY-52 (second cohesive unit out of the AI-plane god-modules), AGY-53 (bake-ref default audit after AGY-40), AGY-54 (negative-tests for the four new drift-gates). It also restates the standing working rules for this batch: stage explicit paths for YOUR task only, run `git status` before every commit, never `git add -A`, and respect Claude's active carve-out over `usr/share/mios/configurator/**` and `usr/share/mios/portal/**` (AGY-50 is the server half of that carve-out).
**Where:** `tools/native/`, `usr/lib/mios/agent-pipe/`, `automation/*bake*.sh`, `automation/38-drift-checks.sh`, `tests/`
**Done When:** all four child tasks (AGY-51..54) are landed and `just drift-gate` is green with no `git add -A` in the batch's commit history.
**Why:** without the shared rules stated up front, parallel agents stage each other's files and the carve-out gets clobbered -- the exact failure this banner exists to prevent.
**Dep:** AGY-40..45 (bakers, eval+guard, shellcheck gate, version-SSOT gate, resolver-twin gate) all landed and green.

## AGY-1690 -- Scaffold the `tools/native/` cargo workspace and port the version-SSOT comparator to Rust  (WS-LANG | P2 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- plants the workspace every later native port grows in, with a strangler-fig shim proving byte-parity before any bash is retired.
**What+How:** create a cargo workspace at `tools/native/` (NOT `src/`, which is occupied by `mios-launch.cs`; leave a note that the operator may prefer `src/mios-rs/`). Port exactly ONE tiny, pure, bounded tool: the version-SSOT comparator that AGY-43's bash gate implements -- read `VERSION`, `mios.toml [meta].mios_version` and the Containerfile `ARG`, compare, exit non-zero on drift -- as a `mios-version-check` binary. Keep the AGY-43 bash gate as the degrade-open fallback so an absent binary still runs the shell path. Cross-compile to a static `x86_64-unknown-linux-musl` target and document the toolchain plus how the build embeds the artifact. If no Rust toolchain exists in the environment, scaffold the workspace + crate + a `TODO(agy): cargo build` and stop.
**Where:** new `tools/native/` (workspace `Cargo.toml` + `mios-version-check/` crate), a location note in `usr/share/doc/mios/adr/0011-*.md` or `ROADMAP.md` WS-LANG
**Done When:** `cargo build --release` yields a static `mios-version-check` whose verdict matches the bash gate on a clean HEAD (green) and on a seeded drift (red); the bash fallback still runs; the workspace location is documented and flagged; `just drift-gate` green.
**Verify:** `cargo build --release` yields the binary AND its verdict matches the bash gate on a clean HEAD, asserted by a test that fails if they disagree.
**Do NOT:** assume the Rust port agrees. mios-resolver differed from Python on 3284 keys the first time anyone compared.
**Why:** ADR-0011's native tier has no home today -- every future port has to re-litigate where the crates live and how they are built.
**Dep:** parallel-safe (the concrete first step of the language unification).
**Who:** you (Rust + bash)

## AGY-52 -- Extract a SECOND cohesive unit out of the AI-plane god-modules  (WS-DEBT | P1 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- continues shrinking `server.py` (8,961 lines) and its oversized flat siblings into testable modules.
**What+How:** pick a second, well-bounded seam (do NOT overlap AGY-47's) from `server.py` or from one of the other oversized flat modules -- `mios_pipe/routing/chat.py` (~107 KB), `native_loop.py` (~89 KB), `federation/a2a.py` (~88 KB), `mios_dispatch.py` (~84 KB) -- and move it into a NEW sibling module plus a sibling test. The import must be ONE-WAY: the new module may NOT import `server.py` (drift-check 6), and it must ship a sibling test (drift-check 11). Leave a thin re-export shim at the old call site so behavior is byte-identical.
**Where:** the chosen source module, new `usr/lib/mios/agent-pipe/mios_<unit>.py`, new `test_mios_<unit>.py`
**Done When:** the unit lives in its own sibling module with a test, there is no `server.py` import edge, the full `test_mios_*` suite is green, behavior is unchanged, drift-checks 6 and 11 pass, `just drift-gate` green.
**Why:** a 9k-line module cannot be reviewed, typed, or safely changed -- every AI-plane fix today risks an unrelated regression somewhere else in the same file.
**Dep:** AFTER AGY-47 (which extracts the first unit) -- do not overlap the same seam.
**Who:** you (Python)

## AGY-53 -- Audit every baker so no `[build.bake_refs]` default resolves to an empty git ref  (WS-DEBT | P2 | S)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- removes the 'clone default-branch / WARN forever' class of network-at-build fragility from the bake phases.
**What+How:** a verify of AGY-40 found `automation/53-bake-lookingglass-client.sh` defaults its ref to EMPTY (`LG_BRANCH="${MIOS_BUILD_BAKE_REFS_LOOKINGGLASS:-}"`), so `git clone --branch ""` fails outright if the SSOT value is unset. Sweep every baker: (1) each `${MIOS_BUILD_BAKE_REFS_*:-DEFAULT}` expansion must carry a real non-empty tag/commit default, never `:-}`; (2) each `[build.bake_refs]` key in `mios.toml` must carry a real value; (3) optionally add a drift-check that reds when any baker has an empty bake-ref default. Fix lookingglass with a real pinned ref and any peer with the same gap.
**Where:** `automation/53-bake-lookingglass-client.sh`, other `automation/*bake*.sh`, `usr/share/mios/mios.toml [build.bake_refs]`, optionally `automation/38-drift-checks.sh`
**Done When:** no baker defaults to an empty ref, every `[build.bake_refs]` key has a real value, `bash -n` is clean across the touched bakers, `just drift-gate` green.
**Why:** an unset SSOT key currently turns into a hard `git clone --branch ""` failure mid-bake -- a build breaker that only appears on a fresh or minimal config.
**Dep:** AGY-40 (the baker work this follows up on); `git pull` first.
**Who:** you (bash + TOML)

## AGY-54 -- Build a negative-test harness proving the four new drift-gates can actually FAIL  (WS-DEBT | P2 | M)  **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- every gate must have a test that demonstrates it catches its own violation class.
**What+How:** `check_shellcheck` (AGY-42), `check_version_ssot` (AGY-43), `check_resolver_twin_equivalence`/`_parity` (AGY-45) and the eval-injection guard (AGY-41) are only ever observed passing on a clean tree. Formalize their fail-behavior into a repeatable harness: for EACH gate, inject a minimal violation into a temp copy (or via a guarded on-disk edit), assert the gate exits non-zero AND names the offending file/line, then restore and assert it returns green. Wire the harness into the `Justfile`/CI where appropriate.
**Where:** new `tests/drift-gate-negatives.sh` (or an extension of the existing test runner), referenced from `Justfile`/CI
**Done When:** each of the 4 gates reds on its injected violation and greens after restore; the harness runs clean end-to-end; `just drift-gate` still green.
**Why:** a gate that has never been seen failing is indistinguishable from a vacuous no-op -- the safety net may already be broken and nobody would know.
**Dep:** AGY-45 (all four new gates exist).
**Who:** you (bash)

## AGY-55 -- Test and de-duplicate the `/portal/config` endpoints: one `to_toml`, one writer, background reseed  (WS-CONFIG | P2 | M)  **[DONE]**
**Goal:** E-11 Unified config surface: mios.toml, the configurator and the Portal are one door at :8640/ -- hardens the write path so the single config door is trustworthy, not merely functional.
**What+How:** AGY-50 landed working `GET`/`POST /portal/config` with three gaps. (1) No dedicated test: add `test_mios_config_write.py` covering GET returning layered TOML, POST persisting to the USER layer (mock `mios_toml.USER` to a temp path and assert round-trip), POST of invalid TOML returning 400, and both endpoints returning 401 unauthenticated. (2) `dict_to_toml` is copy-pasted into BOTH handlers (portal.py ~L1584 and ~L1666) -- extract ONE `to_toml()` serializer plus the USER-layer atomic writer into `mios_pipe/kernel/config.py` (where the original design put the writer) and call them from both sites, verifying round-trip of the scalar/section/array shapes the configurator's `[data-key]` inputs produce. (3) The POST fires a synchronous `seed-db-config` subprocess after the write -- make it non-blocking (FastAPI `BackgroundTask`) and degrade-open, so a reseed failure never fails a POST whose file write already succeeded.
**Where:** new `usr/lib/mios/agent-pipe/test_mios_config_write.py`, `usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py`, `usr/lib/mios/agent-pipe/mios_pipe/routing/portal.py`
**Done When:** the endpoint test passes all four cases (GET / POST / invalid-400 / unauth-401); a single `to_toml`+writer in `config.py` serves both handlers; the DB reseed runs in the background and degrade-open; `just drift-gate` green.
**Why:** a duplicated hand-rolled serializer drifts silently between two call sites, and a synchronous reseed hangs the operator's Save on a slow database.
**Dep:** AGY-50 (done + pushed); Claude's `usr/share/mios/configurator/**` carve-out still applies.
**Who:** you (Python, agent-pipe)

## AGY-56 -- Fix the `/portal/config` array-of-tables corruption plus the 2026-07-17 audit mediums  (WS-CONFIG | P1 item 0 / P2 rest | M)  **[DONE]**
**Goal:** E-11 Unified config surface: mios.toml, the configurator and the Portal are one door at :8640/ -- makes the config door emit VALID TOML, and closes the audit's gate blind spots so the same class cannot reappear.
**What+How:** item 0 (HIGH, folds into AGY-55): the duplicated hand-rolled `dict_to_toml` cannot serialize arrays-of-tables, so `GET /portal/config` returns a CORRUPT body and the POST DB-reseed 500s on the DEFAULT shipped config -- the extracted `to_toml` in `mios_pipe/kernel/config.py` must use a real emitter (`tomli_w.dumps`, or correctly emit `[[array.of.tables]]`) and `test_mios_config_write.py` must include an array-of-tables round-trip. Item 1 (P2): drift-check 42 (version-ssot) never inspects `Containerfile.minimal`, which still pins `ARG MIOS_VERSION=0.2.4` against SSOT 0.3.0 -- extend the gate to scan ALL `Containerfile*` for a version ARG that must equal `[meta].mios_version`, and fix the literal. Item 2 (P2): `check_shellcheck` (44) prints a GREEN conformance summary over NOTHING when `shellcheck` is absent -- make it WARN/SKIP loudly like the python3-missing gates. Item 3: the dangling `just init-user-space`/`reinit-user-space` recipes reference a nonexistent `tools/init-user-space.sh` -- create it or repoint the recipe. Item 4 (LOW): heal the stale `hermes-agent.service` -> `mios-gateway-agent.service` refs in `heal-all-services.sh` and `patch_pods.py`, the comments still calling deleted `39-opencode.sh` an active shim, and the `.gitignore` whitelist of removed medicat files. Item 5 (LOW): the diverged root `mios.toml` copy still says 0.2.4.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/portal.py`, `mios_pipe/kernel/config.py`, `test_mios_config_write.py`, `automation/38-drift-checks.sh` (checks 42, 44), `Containerfile.minimal`, `Justfile` / new `tools/init-user-space.sh`, `usr/libexec/mios/heal-all-services.sh`, `patch_pods.py`, `.gitignore`
**Done When:** item 0 emits valid array-of-tables TOML with a test covering it (GET clean, POST no 500); version-ssot catches a Containerfile ARG drift and `Containerfile.minimal` is fixed; `check_shellcheck` WARNs when shellcheck is absent; no dangling Justfile recipe; stale refs healed; `just drift-gate` green.
**Why:** as shipped, the default config makes the Portal's own read endpoint return corrupt TOML and its write endpoint 500 -- and two gates that look green are lying about a real version drift and about linting nothing at all.
**Dep:** `git pull` FIRST -- Claude just landed `f565ce07` (the `local`-in-subshell in automation/build.sh + the gate-45 env leak).
**Status:** UPDATE (Claude 2026-07-17): items 1 (767ecf23), 2 (facacf26), 3 (18e30d84) DONE by Claude; items 4+5 are NON-ISSUES (Alias=hermes-agent already on mios-gateway-agent.service; root mios.toml already 0.3.0 -- the 0.2.4 is only a comment). AGY-56 effectively COMPLETE. AGY-57 un-claimed -- implement ON MAIN, no branches. | **Who:** you (python + bash + TOML)

## AGY-57..58 -- ADR-0010 completion charter: extend `mios-theme-render` IN PLACE, no `mios-dotfiles-render` fork  (WS-DOTFILES | P2 | L)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- fixes the design contract for the `kind` axis so the merge modes land in one engine rather than a competing second renderer.
**What+How:** scoping banner for AGY-57 (json-merge) and AGY-58 (ini-merge), both implemented against Claude's existing `_load_surfaces`/`_KINDS`/`cmd_render`/`cmd_check`/`cmd_apply` in `mios-theme-render`. The CORE RULE: `kind` is WRITE-SEMANTICS dispatch and is ORTHOGONAL to the existing `section` (token-source) axis; an ABSENT `kind` means `template`, so the 8 existing surfaces need ZERO edits and must stay BYTE-IDENTICAL. Every fail-loud backstop is preserved -- an unknown kind exits 3, a merge surface missing its fixture exits 3 -- and `capture` remains template-only.
**Where:** `usr/libexec/mios/mios-theme-render`, `usr/share/mios/mios.toml`
**Done When:** AGY-57 and AGY-58 are both landed with the 8 pre-existing surfaces rendering byte-identically and no second render engine existing in the tree.
**Why:** forking a `mios-dotfiles-render` would split the projection logic in two and double the drift-gate surface for the same job.
**Dep:** the ADR-0010 dotfiles engine Claude built (`_load_surfaces`/`_KINDS`/cmd_render/cmd_check/cmd_apply).

## AGY-1691 -- Add the `kind` axis plus `json-merge` mode for Windows Terminal / VS Code settings.json  (WS-DOTFILES | P2 | L)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the format-aware merge that lets MiOS own a JSON subtree without clobbering an operator's foreign keys.
**What+How:** (1) `kind` axis: add optional `kind` to `[dotfiles.registry.<surface>]` from `template|json-merge|ini-merge|registry|skip` (absent means `template`); `_load_surfaces` (~L46) returns a namedtuple `_Surface(name,template,target,section,kind,fixture,policy)` so `_entry`/template call sites are untouched, and VALIDATES -- unknown kind exits 3 (matching the missing-template exit 3 at L64-67), and a merge kind MUST declare `fixture.base` + `fixture.expected` or exit 3 (no ungated merge surface). (2) Dispatch: a module-level `_KINDS = {kind: (derive_fn, live_apply_fn)}`; `cmd_render`/`cmd_check` select `derive_fn` by `surface.kind` under the uniform shape `derive(surface,merged)->committed_target_bytes` then write or byte-diff (check NEVER reads a live foreign file -- it must stay an offline PR gate); `cmd_apply` selects `live_apply_fn`; the template kind keeps today's `_render_text` whole-file path with `cmd_apply` L343-393 UNCHANGED. (3) json-merge: `owned = _render_text(tmpl, _resolved_for(merged,section))` is a MiOS-owned JSON SUBTREE only (e.g. VS Code `workbench.colorCustomizations`, WT `profiles.defaults`/`schemes[]`); `derive = json_merge(owned, fixture.base)` and `apply = json_merge(owned, LIVE dest)` with write-back plus backup. `json_merge` steps: a STRING-AWARE `_jsonc_to_json` comment/trailing-comma strip that never mangles `https://` or a `//` inside a string value (unparseable base exits 2 and writes NOTHING); `json.loads` preserving insertion order; a deep merge where dicts recurse, MiOS-owned leaves/arrays win (arrays REPLACED wholesale at the owned pointer -- index-merge is unsafe), and any key MiOS does not emit is byte-preserved; `json.dump` at the base indent with `ensure_ascii=False`. (4) Gating: add a proof `json-merge` surface (e.g. `vscode-colors`) whose `fixture.base` already contains a FOREIGN key and whose `fixture.expected` shows it preserved.
**Where:** `usr/libexec/mios/mios-theme-render` (`_load_surfaces`, `_KINDS`, `_jsonc_to_json`/`_json_deep_merge`, cmd_render/check/apply branches), `usr/share/mios/mios.toml` (new surface + `kind`/`fixture`), a new `.tmpl`, a fixtures dir (e.g. `usr/share/mios/theme/fixtures/`)
**Done When:** the 8 existing surfaces render/check BYTE-IDENTICAL (prove via render byte-diff); the json-merge proof surface merges owned keys into `fixture.base` while PRESERVING the foreign key (test); `check` gates it via the fixture; `apply` into a live temp target preserves foreign keys and writes a backup; unknown-kind and missing-fixture exit 3; unparseable base exits 2 with no write; `capture` still template-only; `just drift-gate` green.
**Verify:** the 8 existing surfaces render byte-identical before and after, proven by diff, and the json-merge surface round-trips.
**Do NOT:** eyeball the rendered output. Byte-identical means diffed.
**Why:** with whole-file rendering as the only write mode, MiOS cannot own any shared settings.json without destroying every setting the operator added themselves -- so those surfaces stay outside SSOT entirely.
**Dep:** parallel-safe (mios-theme-render is Claude-owned but idle).
**Status:** [DONE by Claude on main -- ea756065] kind axis + json-merge landed; verified check PASS 9, foreign-key-preserving, fail-loud exit 3. | **Who:** you (Python)

## AGY-58 -- Add `ini-merge` mode and a `gitconfig-live` surface to unblock safe live-HOME gitconfig apply  (WS-DOTFILES | P2 | L)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- gives the engine an INI write mode so `apply` can reach a live `~/.gitconfig` without stomping credentials.
**What+How:** (1) ini-merge derive/apply: `_parse_ini(text)` keeps an ORDERED line-model plus a `{(section,subsection,key)->line_idx}` index, preserving `#`/`;` comments and `[section "subsection"]` quoting. `owned_kv = _parse_ini(_render_text(gitconfig.tmpl))` -- MiOS owns EXACTLY the keys the template emits (`user.name/email`, `core.editor`, `init.defaultBranch`, `pull.rebase`, `alias.{co,br,ci,st}`). `_ini_upsert` per owned key: absent in base means seed (appending the section block when the section is missing); present means replace in place per policy. Everything outside `owned_kv` (`credential`, `user.signingkey`, `commit.gpgsign`, remotes) is byte-preserved; subsectioned keys are keyed on `(section,subsection)` so they never collide; foreign multivar keys are left untouched since the owned set is single-valued. (2) Write policy `seed-or-enforce` (the safety-first default for gitconfig): absent means write (seed); present AND `operator_set(key)` means write (enforce the operator's explicit SSOT choice); present and NOT operator_set means SKIP, never stomping an existing foreign value with a vendor default -- where `operator_set = mios_toml.load_merged(sec,key) != mios_toml.load_vendor(sec,key)` and non-empty. (3) New ADDITIVE `gitconfig-live` surface: `kind=ini-merge`, `policy=seed-or-enforce`, `template` REUSES `gitconfig.tmpl` VERBATIM (the rendered `[section] key=value` lines ARE both the ownership manifest and the values), `apply.target.linux="~/.gitconfig"`, `apply.target.windows="%USERPROFILE%/.gitconfig"`, plus a small canonical `fixture.base`/`fixture.expected` pair. The EXISTING `gitconfig` surface (etc/skel, kind=template, no apply.target) stays BYTE-IDENTICAL.
**Where:** `usr/libexec/mios/mios-theme-render` (`_parse_ini`/`_ini_upsert`/`_operator_set` + the ini-merge `_KINDS` entry), `usr/share/mios/mios.toml` (the `gitconfig-live` surface), a fixtures pair
**Done When:** the existing template-kind `gitconfig` surface is byte-identical; `apply gitconfig-live` into a live `~/.gitconfig` seeded with a FOREIGN `[credential]`, `user.signingkey` and remote PRESERVES all three while setting the owned keys; `seed-or-enforce` SKIPS an existing foreign `user.email` unless operator_set; `check` gates via the fixture; a backup is written on change; unknown kind / missing fixture exit 3; `just drift-gate` green.
**Why:** live gitconfig apply is currently withheld because whole-file overwrite would destroy the operator's credential helper, signing key and remotes -- so the projected gitconfig never reaches a real HOME.
**Dep:** AGY-57 (needs the `kind` dispatch + `_KINDS`).
**Status:** [DONE by Claude on main -- 8f52ccfb] ini-merge + gitconfig-live landed; check PASS 10, live-apply preserves credential/signing/remotes + seeds owned + backup. gitconfig live-apply UNBLOCKED. | **Who:** you (Python)

## AGY-59..60 -- ADR-0010 finish charter: registry kind + merge-surface test coverage, ON MAIN, no branches  (WS-DOTFILES | P2 | M)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- scopes the last two pieces that complete ADR-0010's engine.
**What+How:** the `kind` axis, json-merge and ini-merge landed (Claude, `ea756065` + `8f52ccfb`), so `mios-theme-render` now carries `_KINDS = {template, json-merge, ini-merge}` with fixture-gated merge surfaces at check PASS 10. AGY-59 adds the `registry` kind (Windows Registry apply) and AGY-60 adds the negative-test coverage for the merge kinds. Working rules for both: stage explicit paths, NEVER `git add -A`, and commit ON MAIN with no branches or worktrees.
**Where:** `usr/libexec/mios/mios-theme-render`, `usr/share/mios/mios.toml`, `automation/38-drift-checks.sh`
**Done When:** AGY-59 and AGY-60 are both landed on main, check PASS is at 11+, and the branch list shows no ADR-0010 side branches.
**Why:** without the registry kind the engine is Linux-only, and without negative tests its fail-loud backstops are asserted rather than proven.
**Dep:** AGY-57 + AGY-58 (`ea756065`, `8f52ccfb`).

## AGY-1692..60 -- ADR-0010 finish charter (restated): registry kind + merge-surface test coverage, ON MAIN, no branches  (WS-DOTFILES | P2 | M)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- duplicate restatement of the same charter as it appears in the source; carries no additional scope.
**What+How:** identical charter text to the preceding entry: `mios-theme-render` already has `_KINDS = {template, json-merge, ini-merge}` with fixture-gated merge surfaces (check PASS 10) from `ea756065` + `8f52ccfb`; AGY-59 adds `registry`, AGY-60 adds the negative tests. Same rules -- explicit paths, no `git add -A`, commit ON MAIN, no branches or worktrees. Treat this heading as a duplicate of the previous one when reconciling the task list.
**Where:** `usr/libexec/mios/mios-theme-render`, `usr/share/mios/mios.toml`, `automation/38-drift-checks.sh`
**Done When:** AGY-59 and AGY-60 are landed on main; this duplicate heading is reconciled against its twin so the task list has one charter, not two.
**Why:** the duplicated banner makes the task list appear to contain more open ADR-0010 work than actually exists.
**Dep:** AGY-57 + AGY-58 (`ea756065`, `8f52ccfb`).

## AGY-1693 -- Add the `registry` kind (Windows Registry apply) plus a windows-registry proof surface  (WS-DOTFILES | P2 | M)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- extends the one engine across the Windows half of NS-8 so registry-backed settings are SSOT-projected too.
**What+How:** add `registry` to `_KINDS`. `derive` builds a manifest of the MiOS-owned Registry values from `_render_text(tmpl,...)` -- a small JSON/INI-ish manifest of `HKCU\Path\Name = value` entries -- byte-diffed against `fixture.expected` for the offline gate, so `check` NEVER touches the live Registry. `live_apply_fn` on Linux/non-Windows is a no-op SKIP logging "registry surface -- Windows-only, skipping"; on Windows (`os.name=='nt'`) it applies each owned value via `reg add` / PowerShell `Set-ItemProperty` under HKCU ONLY (refusing HKLM and system hives), reads back to confirm, and first backs up the prior values by exporting the owned keys to a `.mios-bak` `.reg`. Fail-loud parity holds: a registry surface missing `fixture.base`/`fixture.expected` exits 3, same as the other merge kinds. Add ONE proof `windows-registry` surface (e.g. a couple of `HKCU\Console` values or a MiOS app setting) with a template plus fixture pair, with `apply.target` naming the HKCU path root.
**Where:** `usr/libexec/mios/mios-theme-render` (`_KINDS` registry entry + `_registry_derive`/`_registry_apply`), `usr/share/mios/mios.toml` (the surface), a template + fixtures pair
**Done When:** the 10 existing surfaces stay byte-identical and `check` reports PASS 11; on Linux `apply <registry-surface>` is a clean no-op skip; the offline `check` gates the surface via its fixture; missing-fixture and unknown-kind exit 3; `py_compile` clean.
**Verify:** `check` reports the new surface count, and on Linux `apply` on the registry surface is a no-op rather than an error.
**Do NOT:** let a platform-specific surface fail loudly on the other platform. Skipping must be deliberate and stated.
**Why:** Windows settings that live only in the Registry cannot be expressed in SSOT today, so the "both platforms" half of the dotfiles north star is unreachable.
**Dep:** AGY-57/58 (landed).
**Who:** you (Python + PowerShell)

## AGY-60 -- Add negative-tests and a real unit test for the json-merge and ini-merge kinds  (WS-DOTFILES | P2 | M)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- proves each merge-mode safety property instead of assuming it.
**What+How:** the merge kinds have fixture drift-gating but no repeatable NEGATIVE test. Add a harness in whichever style fits (agent-pipe uses the `test_mios_*` convention; for `mios-theme-render` use a `tests/` dir or a self-contained script) asserting: (1) an unknown `kind` exits 3; (2) a merge surface missing `fixture.base`/`fixture.expected` exits 3; (3) json-merge preserves a foreign top-level key, a `//`-in-string URL and a nested foreign key, and REFUSES an unparseable base (exit 2, no write); (4) ini-merge preserves `credential`/`signingkey`/`[remote]`, seeds absent owned keys, and under `seed-or-enforce` SKIPS a present foreign value unless operator_set; (5) tampering any merge surface's `fixture.expected` reds `check`. Wire it where the drift-gate or CI runs it, and keep it hermetic -- temp dirs only, no real HOME or Registry writes.
**Where:** a new test harness for `mios-theme-render`, optionally a `check_*` hook in `automation/38-drift-checks.sh` (coordinate -- Claude may also touch that file)
**Done When:** every listed property has a passing negative-test; the harness runs clean and hermetic; `just drift-gate` (or the theme gate) stays green.
**Why:** the merge modes write into live operator files -- if a preservation guarantee silently regresses, the first symptom is a destroyed `~/.gitconfig` or `settings.json`.
**Dep:** AGY-57/58.
**Who:** you (Python + bash)

## AGY-61 -- Port the version-SSOT checker into a real static Rust binary in `tools/native/`  (WS-LANG | P2 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the first genuine bash-to-compiled migration in the workspace AGY-51 planted.
**What+How:** with the `tools/native/` cargo workspace in place, port ONE small, pure, high-value tool to a static Rust binary: the version-SSOT checker -- read `VERSION`, `mios.toml [meta].mios_version` and each Containerfile `ARG`, compare, exit non-zero on drift (the logic AGY-43 put into bash gate 42 / `check_version_ssot`). Crate name `mios-version-check`; cross-compile to `x86_64-unknown-linux-musl`; keep the bash gate as the degrade-open fallback so an absent binary still lints. Document the toolchain and the build wiring -- a cached Containerfile stage that COPYs the binary to `/usr/libexec/mios/`, per ADR-0011 section 2.
**Where:** `tools/native/mios-version-check/`, a note in ADR-0011 / ROADMAP WS-LANG
**Done When:** `cargo build --release` yields a static binary that matches the bash gate's verdict on HEAD and on a seeded drift; the bash fallback is intact; `just drift-gate` green.
**Why:** until one tool actually ships compiled and wired into the image, ADR-0011's native tier is a scaffold with no proof that the build can carry a Rust artifact.
**Dep:** AGY-51 (the cargo workspace scaffold). Work ON MAIN, no branches; stage explicit paths.
**Who:** you (Rust)

## AGY-62 -- Author the `[shell]`, `[editor]` and `[ssh]` dotfile surfaces on the finished kind engine  (WS-DOTFILES | P2 | M)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- broadens coverage from theme/git to the everyday shell, editor and SSH surfaces using only SSOT authoring.
**What+How:** the engine (kinds template/json-merge/ini-merge/registry) is complete, so this is surface AUTHORING with NO engine change. Flesh out the stub `[shell]`/`[editor]`/`[ssh]` sections and register their surfaces in `[dotfiles.registry.*]`: (a) `shell-aliases` -- template or ini-merge into `~/.bashrc.d/mios`, preferring a SOURCED fragment file over editing `~/.bashrc` so foreign content is never touched; (b) `ssh-config` -- ini-merge into `~/.ssh/config` owning ONLY a MiOS `Host mios-*` block while preserving every foreign Host block, and storing NO secrets (reference a `secret_ref`, never a raw key, per Law 9); (c) optionally an `[editor]`-driven surface. Each surface needs a template plus `fixture.base` (containing FOREIGN content) and `fixture.expected`, using the same safe-merge patterns as `gitconfig-live`. Do NOT touch `mios-theme-render`, `mios-sync-theme`, `portal.py` or `tools/native`.
**Where:** `usr/share/mios/mios.toml` (`[shell]`/`[editor]`/`[ssh]` + registry surfaces), `usr/share/mios/theme/templates/*`, fixtures
**Done When:** each new surface renders and drift-gates (check PASS increases); `apply ssh-config` preserves foreign Host blocks (test); no secret is stored in SSOT; `just drift-gate` green.
**Why:** the engine exists but only covers theme and git -- shell aliases, editor prefs and SSH host config remain hand-maintained per machine, exactly the drift NS-8 exists to end.
**Dep:** the kind engine (template/json-merge/ini-merge/registry) complete. NOT Claude's lane -- Claude owns `mios-sync-theme` and the `mios dotfiles` verb.
**Who:** you (Python + TOML)

## AGY-63 -- Extract the VRAM/GPU scheduler out of `server.py` into `mios_vram.py`  (WS-DEBT | P1 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- carves a high-risk, independently-testable subsystem out of the 9k-line agent-pipe god-module.
**What+How:** move the VRAM/GPU scheduling logic from `usr/lib/mios/agent-pipe/server.py` into a NEW sibling `mios_vram.py` with a sibling `test_mios_vram.py`, under a ONE-WAY import (the new module must NOT import `server.py` -- drift-check 6), leaving a thin re-export shim at the old call site so behavior is byte-identical.
**Where:** `usr/lib/mios/agent-pipe/server.py`, new `usr/lib/mios/agent-pipe/mios_vram.py`, new `usr/lib/mios/agent-pipe/test_mios_vram.py`
**Done When:** the sibling module and its test exist, there is no `server.py` import edge, the `test_mios_*` suite is green, drift-checks 6 and 11 pass, `just drift-gate` green.
**Why:** VRAM scheduling is the logic that keeps the agent plane from starving its host, and it currently cannot be unit-tested at all because it is buried in `server.py`.
**Dep:** none. ON MAIN, no branches; explicit paths. NOT Claude's lane (Claude owns portal.py/config.py/mios-theme-render).
**Who:** you (Python)

## AGY-64 -- Extract the `_db_*` helpers out of `server.py` into `mios_serverdb.py`  (WS-DEBT | P1 | S)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- same strangler pattern applied to the DB-access seam of the agent-pipe god-module.
**What+How:** pull the `_db_*` database-access helpers from `usr/lib/mios/agent-pipe/server.py` into `mios_serverdb.py` with a sibling `test_mios_serverdb.py`, one-way import (no `server.py` edge), thin re-export shim at the old call site, behavior identical.
**Where:** `usr/lib/mios/agent-pipe/server.py`, new `usr/lib/mios/agent-pipe/mios_serverdb.py`, new `usr/lib/mios/agent-pipe/test_mios_serverdb.py`
**Done When:** the sibling module and test exist, there is no `server.py` import edge, the suite is green, `just drift-gate` green.
**Why:** DB access scattered through `server.py` means every query change is a 9k-line-module edit with no isolated test to catch a regression.
**Dep:** none (same pattern as AGY-63).
**Who:** you (Python)

## AGY-65 -- Port the drift-runner entry point to Rust as the second native tool  (WS-LANG | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- begins moving the mechanical conscience of the tree off fragile bash and onto a compiled runner.
**What+How:** add a `tools/native/mios-drift` crate that runs the image-free source drift checks (or wraps and dispatches the bash `38-drift-checks.sh` phases), built as a static musl binary. The bash gate remains the degrade-open fallback so an absent binary changes nothing about CI behavior.
**Where:** `tools/native/mios-drift/`
**Done When:** `cargo build --release` yields the binary (when a toolchain is present); its verdict agrees with the bash gate on HEAD; the bash fallback is intact; the drift-gate is green.
**Why:** the gate that fails the build is itself ~123 checks of unfailing-by-accident bash -- a fragile enforcement plane undermines every law it enforces.
**Dep:** AGY-51 + AGY-61 (workspace + version-check).
**Who:** you (Rust)

## AGY-66 -- Close every drift-6 / drift-11 gap across the agent-pipe module set  (test-coverage | P2 | M)  **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- makes the sibling-test and no-server-import invariants hold with zero exceptions.
**What+How:** audit every `usr/lib/mios/agent-pipe/mios_*.py`: each module MUST have a sibling `test_mios_*.py` (drift-check 11) and NONE may import `server.py` (drift-check 6). Add minimal sibling tests wherever they are missing, and break any remaining `server.py` import edge by lifting the shared code into a shared helper module.
**Where:** `usr/lib/mios/agent-pipe/mios_*.py` and their sibling `test_mios_*.py`
**Done When:** drift-checks 6 and 11 pass with zero exceptions; the full `test_mios_*` suite is green.
**Why:** exempted modules are where the decomposition work silently regresses -- one re-added `server.py` import re-couples the god-module the TD-5 campaign is dismantling.
**Dep:** none.
**Who:** you (Python)

## AGY-67 -- Put `set -euo pipefail` on the ~23 unguarded runtime verbs and re-verify no agent-arg `eval` returned  (WS-DEBT | P1 security | S)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- closes the TD-1 injection/silent-failure surface in the verb library.
**What+How:** roughly 23 runtime verbs in `usr/libexec/mios/` still run without `set -euo pipefail`, so a failed command mid-verb continues silently. Add the guard, using an explicit `|| true` for the specific commands that legitimately must continue on error, and document any verb that genuinely needs an exception. Then re-confirm the eval hardening: grep `eval ` across `usr/libexec/mios/` and verify none of the agent-argument `eval` sites AGY-41 removed have crept back in.
**Where:** `usr/libexec/mios/` (the unguarded verbs)
**Done When:** every verb carries `set -euo pipefail` or a documented exception; no `eval` on agent arguments remains; `bash -n` is clean; the drift-gate is green.
**Why:** an unguarded verb that half-fails leaves the system in a partially-applied state and reports success -- and a re-introduced `eval` hands agent-supplied text straight to the shell.
**Dep:** AGY-41 (which fixed the original eval set).
**Who:** you (bash)

## AGY-68 -- Generate `userenv.sh` FROM `mios_toml.py` so the resolver twins cannot drift by construction  (WS-DEBT | P2 | L)  **[DONE]**
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- replaces the hand-maintained bash half of the resolver with a projection of the Python one.
**What+How:** AGY-45 added the equivalence GATE that detects twin drift; this adds the GENERATOR that prevents it. Emit `usr/lib/mios/userenv.sh` (the bash resolver) FROM `mios_toml.py` or from a shared spec both consume, so the layer order and key set are derived rather than transcribed -- TD-3's root fix. The AGY-45 gate must stay green throughout, and `tools/lib/userenv.sh` must stay in sync (drift-check 27).
**Where:** `usr/lib/mios/userenv.sh` (generated), `usr/lib/mios/mios_toml.py` (or a shared spec), `tools/lib/userenv.sh`
**Done When:** the generator deterministically regenerates a `userenv.sh` byte-identical to the committed one; the twins stay byte-identical; drift-check 45 and drift-check 27 are green.
**Why:** two hand-maintained resolvers means every new key must be added twice, and the gate can only tell you they diverged AFTER someone already shipped the divergence.
**Dep:** AGY-45 (the equivalence gate).
**Who:** you (Python + bash)

## AGY-69 -- Run and GREEN the full `just all` BIB artifact matrix on MiOS-DEV  (WS-BAKEGATE | P1 | M)  **[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- proves every bootc-image-builder output leg actually produces a bootable artifact.
**What+How:** on the MiOS-DEV VM run `just all` to produce the full matrix (OCI plus raw / iso / qcow2 / vhdx / wsl2), record each output path and size under `/var/lib/mios/build/output`, and fix whichever BIB leg fails.
**Where:** `Justfile` (`all` target), `/var/lib/mios/build/output`
**Done When:** all 6 artifacts are produced and the header-sniff/verify target is green.
**Why:** an untested BIB leg means a shipped install medium that does not boot -- discovered by an operator on bare metal rather than by CI.
**Dep:** requires the MiOS-DEV VM [VM]. NOT Claude's lane (Claude owns the MiOS-Cat launcher + kickstart in mios-bootstrap).
**Who:** you

## AGY-70 -- Project ISO kickstart credentials from SSOT instead of shipping `REPLACEME` placeholders  (WS-BOOTC | P1 | S)  **[DONE]**
**Goal:** E-20 The bootc-native install legs -- an ISO built from the tree installs a real, credentialed MiOS rather than a stub the operator must hand-patch.
**What+How:** `config/artifacts/iso.toml` ships literal `$6$REPLACEME` password-hash and `AAAA_REPLACE` SSH-key placeholders. Mirror the env-substitution the qcow2/vhdx artifact targets already do -- `MIOS_USER_PASSWORD_HASH` and `MIOS_SSH_PUBKEY` expanded at build time -- into the `iso` Justfile target, with both values resolved from `mios.toml [identity]` through the standard SSOT resolver rather than typed into the artifact config.
**Where:** `config/artifacts/iso.toml`, the `iso` target in the repo `Justfile`, `mios.toml [identity]`
**Done When:** the built ISO's embedded kickstart contains no `REPLACEME`/`AAAA_REPLACE` token (grep of the generated kickstart is empty) and both creds trace back to `[identity]` in SSOT.
**Why:** every ISO produced today boots to an account whose password hash is an unusable placeholder and whose authorized_keys is garbage -- the ISO leg cannot actually be logged into, and the placeholders read like a real (weak) credential.
**Dep:** none
**Who:** you.

## AGY-71 -- Add an OFFLINE `bootc install to-disk` deploy mode from a staged oci-archive  (WS-BOOTC | P1 | M)  **[DONE]**
**Goal:** E-20 The bootc-native install legs -- a blank machine is provisioned as immutable bootc MiOS with zero external egress.
**What+How:** Stage the MiOS OCI image as an `oci-archive` on the install medium and drive `bootc install to-disk` against it via `--transport oci`, giving deploy an IMMUTABLE leg alongside the mutable-Fedora-overlay leg MiOS-Cat wires. Land it as a new `usr/libexec/mios/mios-bootc-install` tool or a dedicated Justfile target, and document the mode next to the other deploy modes.
**Where:** new `usr/libexec/mios/mios-bootc-install` (or a `Justfile` deploy target), the deploy-model doc set
**Done When:** a staged oci-archive on offline media installs a bootable immutable MiOS end-to-end and the mode is documented as a first-class deploy leg.
**Why:** without it the only working bare-metal path installs a mutable Fedora with an FHS overlay -- an operator cannot get the actual immutable, `bootc upgrade`-able product onto blank hardware, let alone without a network.
**Dep:** none
**Who:** you.

## AGY-72 -- Repair MiOS-Xbox in-guest provisioning and prove a green end-to-end run  (WS-DEPLOY | P2 | M)  **[DONE]**
**Goal:** E-21 One deploy front door -- the Xbox/Windows-guest target succeeds through the same dispatcher as every other target.
**What+How:** The last Deploy-MiOSXbox run ended `status=incomplete`: bootstrap exited 1, the guest probed the retired `:8080` endpoint instead of canonical `:8642`, and WSL2/VMP plus the MiOS-Host pieces were never staged. Fix the in-guest provisioning sequence (endpoint probe repointed to the canonical AI endpoint, WSL2/Virtual Machine Platform enablement and MiOS-Host staging actually performed), then run it to completion in the Hyper-V test VM.
**Where:** the `Deploy-MiOSXbox` provisioning path and its in-guest bootstrap script
**Done When:** a Deploy-MiOSXbox VM boots and provisions MiOS green (bootstrap exit 0) and the provisioning surface contains zero `:8080` references.
**Why:** the Xbox deploy leg silently half-completes today and probes a retired port, so anyone following the documented path gets an unprovisioned guest with no error that names the cause.
**Dep:** none
**Who:** you.

## AGY-73 -- Collapse the sidecar fleet onto a shared MiOS-Sys base to shrink the bake  (WS-MIOSSYS | P1 | L)  **[DONE]**
**Goal:** E-17 Shared-base consolidation -- the ~18-image sidecar fleet reduces to shared lineages so a standard runner can bake and publish.
**What+How:** Build one shared base image and re-parent the sidecars onto it so podman stores the common python/node/chromium (and CUDA-side) layers once instead of per sidecar. Re-point every affected sidecar build to derive `FROM` the shared base, keep runtime topology unchanged (one image, many containers), and re-run the drift-gate over the changed image graph.
**Where:** the sidecar image build phases under `automation/` and the `[image.sidecars]` / `[build.bake]` sections of `mios.toml`
**Done When:** the shared base builds, every sidecar derives from it, measured total bake size drops, and `just drift-gate` is green.
**Why:** the un-consolidated fleet (~47GB of vLLM/SGLang whales alone) exceeds a standard GitHub runner, which is the direct reason PUBLISH is capacity-gated and GitHub is not yet an equal publisher.
**Dep:** none
**Who:** you.

## AGY-74 -- `[blade]` archetypes + `Condition*` activation so one image adapts per host  (WS-BLADE | P1 | L)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- host capability is declared in SSOT and enforced by unit conditions, not by mutating the installed system.
**What+How:** Per ROADMAP WS-BLADE, add `[blade]`, `[blade.archetypes]` and `[blade.requires]` to `mios.toml`, generate `blade-<cap>.conf` systemd drop-ins from them, and select the active archetype by kernel argument. Demote the existing role-apply step from a system mutator to a marker-writer so activation is condition-driven at boot rather than baked per host.
**Where:** `mios.toml [blade]` / `[blade.archetypes]` / `[blade.requires]`, the generated `blade-<cap>.conf` drop-ins, the role-apply tool
**Done When:** one universal image ships, a host booted as the `controller` blade condition-skips the vLLM unit on zero-VRAM hardware, and the generated drop-ins are drift-gated against SSOT.
**Why:** without conditional activation every host runs (or fails to run) the same full unit set, so a GPU-less controller starts inference units that cannot succeed -- and the only alternative is per-host image divergence, which breaks NS-1.
**Dep:** none
**Who:** you.

## AGY-75 -- Digest-free SSOT: resolve every digest at build and record it in the SBOM  (WS-SBOM | P1 | M)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening -- provenance is a build output, not a hand-maintained constant.
**What+How:** Strip hand-pinned `@sha256` digests, checksums and resolved versions out of `mios.toml`; `[image.sidecars]` and every other artifact ref carry `:latest`/family intent only. The build resolves each ref and records the resolved manifest digest into the generated SBOM, so reproducibility is defined as baked manifest + SBOM per ADR-0003.
**Where:** `mios.toml [image.sidecars]` and sibling ref tables, the SBOM generation phase under `automation/`, ADR-0003
**Done When:** no `@sha256` literal remains in SSOT, the generated SBOM lists a resolved digest for every image consumed by the bake, and ADR-0003's contract is satisfied.
**Why:** hand-pinned digests go stale silently and produce the recurring Quadlet-digest-drift red gate every time a broad `git add` restages generated files -- and they make SSOT carry data no operator can meaningfully edit.
**Dep:** none
**Who:** you.

## AGY-76 -- `tools/compile-templates.py` plus a conformance ratchet on new files  (WS-TEMPLATE | P2 | M)  **[DONE]**
**Goal:** E-04 One template per file type -- structure, not just the AI-hint header, is machine-validated.
**What+How:** Ship `tools/compile-templates.py` driven by a `[templates]` section in `mios.toml`, and add a conformance drift-check that ratchets: existing files are grandfathered, NEW files must be generated from a registered template. Migrate an initial batch of existing files onto templates to prove the compiler.
**Where:** `tools/compile-templates.py`, `mios.toml [templates]`, the template-conformance check in the drift-gate
**Done When:** the template system compiles from SSOT, the ratchet check is green in the gate, and the migrated files pass conformance rather than being grandfathered.
**Why:** every file type is currently learned by imitation from whichever example an agent happens to open, so formatting drifts per author and nothing catches a new file that ignores the convention.
**Dep:** none
**Who:** you.

## AGY-77 -- Bring the full drift-gate green at HEAD with negative tests per fix  (WS-DRIFT | P1 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the mechanical conscience of the tree is actually green, not mostly green.
**What+How:** Run all 40+ checks at current HEAD and fix each red, with attention to version-SSOT consistency across `Containerfile*`, quadlet digest drift, and the known drift-6/drift-11 failures. For every check newly returned to green, add a negative test proving that check can still FAIL.
**Where:** the drift-check script under `automation/`, `Containerfile*`, generated Quadlets
**Done When:** `just drift-gate` reports fully green and each newly-fixed check has a negative test that fails when the violation is reintroduced.
**Why:** a partially-red gate trains everyone to ignore it; and a check fixed without a negative test can silently stop checking anything, which is how green-but-meaningless gates get shipped.
**Dep:** none
**Who:** you.

## AGY-78 -- Collapse the three divergent mios.toml copies to ONE authoritative SSOT  (WS-DEBT | P2 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement (TD-2) -- the single source of truth is singular in fact, not just in name.
**What+How:** Three `mios.toml` copies exist at conflicting versions (0.3.0 vs 0.2.4). Pick the authoritative vendor copy, make the others generated or symlinked projections of it, and single-source the version literal so one edit changes every consumer. Add the consistency assertion to the drift-gate.
**Where:** the three `mios.toml` copies in the tree, the version-SSOT drift-check
**Done When:** exactly one authoritative `mios.toml` exists, `mios --version`-class output and every embedded version string agree, and the version-SSOT check gates it.
**Why:** three SSOTs at two versions means a value edited in the "obvious" file silently does nothing, and the build can bake a version that disagrees with the tag it publishes under.
**Dep:** none
**Who:** you.

## AGY-79 -- Write the confirmed DEPLOY MODEL concept doc  (WS-DOCS | P2 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- an arriving agent picks up the deploy plane cold.
**What+How:** Author `usr/share/doc/mios/concepts/deploy-model.md` covering the three operator-confirmed modes: (1) mutable Fedora "MiOS server" plus the MiOS FHS overlay via `build-mios.sh` -- the primary bare-metal leg; (2) the immutable `bootc install` mode; (3) VM (vhdx/qcow2) and Windows/Xbox modes -- with MiOS-Cat framed as the universal delivery vector. Cross-reference it from `ROADMAP.md` and `architecture.md`.
**Where:** `usr/share/doc/mios/concepts/deploy-model.md`, `ROADMAP.md`, `architecture.md`
**Done When:** the doc is committed and reachable by cross-reference from both `ROADMAP.md` and `architecture.md`.
**Why:** the deploy plane is the least-documented and least-complete area; today the only way to learn which leg is primary is to read the scripts and guess.
**Dep:** none
**Who:** you.

## AGY-80 -- Reduce the AI-plane dependency surface (Hermes to agent-pipe collapse)  (WS-DEPRED | P2 | M)  **[DONE]**
**Goal:** E-11 Unified config surface / one front door -- fewer moving sidecars behind the single addressable AI contract.
**What+How:** Collapse the Hermes layer into agent-pipe and merge or retire the sidecars that exist only to bridge between them, cutting the number of independently-versioned services the AI plane depends on. Repoint consumers at the surviving endpoint before removing anything, then re-run the gate over the changed image and unit graph.
**Where:** the agent-pipe service tree under `usr/lib/mios/agent-pipe/`, `[image.sidecars]` in `mios.toml`, the affected Quadlets
**Done When:** the identified sidecars are retired or merged, every AI endpoint still answers on the canonical front door, and `just drift-gate` is green.
**Why:** each extra sidecar is another image in the bake (which is already over runner capacity), another unit to condition-gate, and another place the one-front-door invariant can be bypassed.
**Dep:** none
**Who:** you.

## AGY-81 -- Verify the GitHub-equals-Forgejo release topology end to end  (WS-RELTOP | P1 | M)  **[DONE]**
**Goal:** E-17 Shared-base consolidation / release topology -- both publishers are bit-for-bit equal, not one real and one aspirational.
**What+How:** Validate that GitHub and Forgejo are equal publishers of the same artifact: exercise a dry-run publish path on both, confirm the local-first build with registry fallback behaves as designed, and confirm the `PUBLISH` capacity gate in `mios-ci.yml` is documented and keyed off actual bake size rather than an unexplained literal.
**Where:** `.github/workflows/mios-ci.yml`, the Forgejo workflow equivalent, the registry-selection logic in `automation/lib/globals.{sh,ps1}`
**Done When:** a dry-run publish path is validated for both forges and the `PUBLISH` gate's condition and rationale are written down beside the flag.
**Why:** an undocumented capacity flag is indistinguishable from a bug; and if only one forge is actually exercised, the "equal publishers" claim silently becomes single-vendor dependence.
**Dep:** none
**Who:** you.

## AGY-82 -- The "run off M:" Hyper-V vhdx deploy recipe  (WS-MDRIVE | P2 | M)  **[DONE]**
**Goal:** E-20 The bootc-native install legs -- a MiOS image runs as a real VM off external storage without hand-assembly.
**What+How:** Add the missing mechanism: a `deploy-mios-hyperv-m.ps1` driver plus a `vhdx-m` recipe that takes the BIB-produced vhdx, creates a Hyper-V Gen2 VM backed on the M: drive, and attaches the Ceph OSD. Document the recipe alongside the other deploy modes.
**Where:** new `deploy-mios-hyperv-m.ps1`, the `vhdx-m` recipe in the artifact/deploy recipe set
**Done When:** a MiOS vhdx boots as a Hyper-V Gen2 VM off M: via one command, and the procedure is documented.
**Why:** the vhdx artifact is built but there is no supported way to actually run it off the intended drive, so the whole M: workflow is manual clicking that nobody else can reproduce.
**Dep:** none
**Who:** you.

## AGY-83 -- Finish the V4 Accounts/users tasks and add sibling tests for the new sync modules  (WS-ACCT | P1 | M)  **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- accounts resolve from the unified datastore with test coverage, not from ad-hoc sync scripts.
**What+How:** T-242/246/150/151 already landed the account-sync, identity-ingest and DB config-resolver pieces. Close the remaining V4 Accounts/users items and add the missing sibling test modules for each new sync module, which is exactly what the drift-11 (module-has-tests) check enforces.
**Where:** the account-sync / identity-ingest / DB config-resolver modules and their sibling `test_*.py` files
**Done When:** every remaining V4 Accounts/users item is closed, each new sync module has a sibling test, drift-11 is green, and the suite passes.
**Why:** untested sync code that writes identity state is the highest-blast-radius kind of untested code -- a silent regression locks operators out of their own machine.
**Dep:** none
**Who:** you.

## AGY-84 -- Close the "referenced but NOT emitted" key-library drift  (WS-NAME | P1 | L)  **[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- every referenced `MIOS_*` fact has exactly one canonical name and one emitter.
**What+How:** The CI drift-check "FAIL -- referenced but NOT emitted (a consumer would lose its var)" lists hundreds of `MIOS_*` vars referenced in code, TOML and units that the userenv key-library walk never emits. Per WS-NAME, replace the hand-maintained tuple list with ONE auto-derived minimal global key library so every genuinely-referenced var is emitted, and reconcile the canonical long-form `MIOS_AI_VLLM_*` emission against the short `MIOS_VLLM_*` names consumers actually read.
**Where:** `userenv.sh` and its twin, the key-library walk, the var-emission drift-check
**Done When:** the var-emission check reports 0 referenced-but-not-emitted, the `userenv.sh` twins remain byte-identical (drift-27 green), and no consumer loses a var it reads.
**Why:** this is the drift that keeps CI red, and behind it sits a real failure mode -- a consumer reading `${MIOS_X}` that nothing exports gets an empty string and misbehaves silently.
**Dep:** none
**Who:** you.

## AGY-85 -- Resolve the `privilege_group` unknown-key advisory in the agent schema  (WS-NAME | P2 | S)  **[DONE]**
**Goal:** E-10 One canonical name -- no key exists outside the canonical schema that declares it.
**What+How:** CI advises "`[agents.hermes]`/`[agents.opencode]` unknown key `privilege_group` (not in the canonical agent schema)". Decide it one way: if a consumer actually reads `privilege_group`, ADD it to the canonical agent schema with its type and default; if nothing reads it, REMOVE it from those two `[agents.*]` sections.
**Where:** `mios.toml [agents.hermes]` / `[agents.opencode]`, the canonical agent-schema definition
**Done When:** the advisory no longer appears in the CI run and the WS-A2 agent-schema contract still validates.
**Why:** an unknown key is either a silently-ignored setting an operator believes is doing something, or dead SSOT text -- both erode trust that the schema describes reality.
**Dep:** none
**Who:** you.

## AGY-86 -- Full drift-gate GREEN at HEAD so the CI job passes  (WS-GATE | P1 | S)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- CI merges are unblocked because the gate is genuinely clean.
**What+How:** After AGY-84/85 land, run `automation/38-drift-checks.sh` end to end at HEAD, fix any remaining red so the `mios-ci` drift-gate job passes, and add a negative test for each newly-fixed check.
**Where:** `automation/38-drift-checks.sh`, the drift-gate job in `.github/workflows/mios-ci.yml`
**Done When:** `just drift-gate` is fully green locally and the CI drift-gate job passes on a pushed commit.
**Why:** while the gate is red nothing can merge to main, so every other workstream queues behind it.
**Dep:** AGY-84, AGY-85
**Who:** you.

## AGY-87 -- Deliver the DEPLOY MODEL doc including the MiOS-Sudo identity (AGY-79 follow-through)  (WS-DOCS | P2 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- the deploy plane's confirmed model, including its identity story, is written down once.
**What+How:** Write `usr/share/doc/mios/concepts/deploy-model.md` covering (1) mutable Fedora "MiOS server" + MiOS FHS overlay via `build-mios.sh` as the operator-confirmed primary bare-metal leg; (2) the MiOS-Sudo identity -- the renamed RID-500 built-in Administrator IS the MiOS AI, derived from `[autounattend.service].svc_user`/`svc_description` in SSOT; (3) the immutable bootc-install, VM (vhdx/qcow2) and Windows/Xbox modes. Cross-ref from `ROADMAP.md` and `architecture.md`.
**Where:** `usr/share/doc/mios/concepts/deploy-model.md`, `mios.toml [autounattend.service]`, `ROADMAP.md`, `architecture.md`
**Done When:** the doc is committed with all three sections and cross-referenced from both `ROADMAP.md` and `architecture.md`.
**Why:** the MiOS-Sudo identity decision (a renamed RID-500 account acting as the AI) is surprising enough that an undocumented encounter with it reads as a compromised machine.
**Dep:** AGY-79
**Who:** you.

## AGY-88 -- Verify the `just all` BIB artifact matrix produces all six outputs  (WS-BOOTC | P2 | M)  **[DONE]**
**Goal:** E-20 The bootc-native install legs -- every declared install medium is actually buildable, not just declared.
**What+How:** In the MiOS-DEV VM run `just all` to build the full matrix (OCI plus raw, iso, qcow2, vhdx, wsl2), record each artifact's output path and size, and fix whichever BIB leg fails.
**Where:** the `all` and per-artifact targets in the repo `Justfile`, `config/artifacts/*.toml`
**Done When:** all six artifacts are produced in one `just all` run with recorded sizes and the verify step reports green.
**Why:** artifact targets that are never exercised rot quietly; discovering a broken iso or vhdx leg at deploy time costs a full rebuild cycle on hardware.
**Dep:** AGY-69
**Who:** you.

### AGY-1694 REFINEMENT -- Fix the emission check to its true intent instead of naive-emitting ~500 vars  (WS-NAME | P1 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- a check flags only what it claims to flag, so its green means something.
**What+How:** The "referenced but NOT emitted" check is over-broad; two representatives disprove its premise (`server.py:804` `MIOS_ACI_HEAD_FRAC` is a `_cfg_num(...)` SSOT read of `[aci].head_frac` with a default -- an OPTIONAL env override that cannot be "lost"; `test_mios_template.py:48` `MIOS_NONEXISTENT_ENV` is a deliberate absent-value sentinel that must NEVER be emitted). Recategorize the scan: (A) treat `_cfg_num`/`_cfg_bool`/`_cfg_str`/`_toml_section(...)` calls carrying an env-name arg AND a default as COMPLIANT and stop flagging them (this is most of `server.py`, `mios_pipe/**/config.py` and the kernel); (B) exclude test/negative sentinels under `test_*.py`, `tests/`, `*-negatives.sh` and the `38-ssot-lint.sh` fixtures; (C) tighten the extraction regex so trailing `_`/`__` interpolation stems (`MIOS_PORT_`, `MIOS_DRIVE__`, `MIOS_CELL_H__`) are not captured as identifiers; (D) exclude `*.ps1` hits (Get-MiOS.ps1, build-mios.ps1, mios-pipeline.ps1) -- those are WinPS vars routed to the globals.ps1 lane, not Linux env keys; (E) emit only the small genuine residue -- bash/systemd consumers reading `${MIOS_X}` with no default and no SSOT read -- from the auto-derived key library, reconciling `MIOS_AI_VLLM_*` against short `MIOS_VLLM_*`.
**Where:** the emission drift-check in `automation/38-drift-checks.sh`, `usr/lib/mios/agent-pipe/.../server.py`, `mios_pipe/**/config.py`, `userenv.sh`, `automation/lib/globals.ps1`
**Done When:** 0 GENUINE referenced-but-not-emitted remain, categories A-D no longer appear in the output, the userenv twins stay identical (drift-27), and there is a negative test per category -- a Cat-A `_cfg_num` var, a Cat-B sentinel and a Cat-D `.ps1` var each proving they are correctly NOT flagged, plus a Cat-E var proving a real orphan IS flagged until emitted.
**Verify:** 0 genuine referenced-but-not-emitted names remain and the twins stay equivalent, both asserted by gates rather than counted by hand.
**Do NOT:** naive-emit ~500 vars to make the number zero. That satisfies the count and breaks the meaning.
**Why:** naive-emitting ~500 vars to silence the check would bloat every shell's environment with values that are already SSOT-sourced, permanently break the deliberate absent-value test sentinels, and convert a real invariant into noise nobody reads.
**Dep:** AGY-84
**Status:** VERIFIED against HEAD 3551d6d5

## AGY-89 -- Stop `globals.sh` clobbering the SSOT image name on no-creds hosts  (WS-RELTOP | P1 | S)  **[DONE]**
**Goal:** E-17 Shared-base consolidation / release topology -- the image name resolves from SSOT identically on every host and both twins.
**What+How:** `automation/lib/globals.sh:109-113` unconditionally `export MIOS_IMAGE_NAME="localhost/mios"` in the no-creds branch. Because `common.sh` sources `userenv.sh` first (ghcr from SSOT) then `globals.sh`, this OVERWRITES the SSOT value, contradicting `common.sh:48-52`'s own "globals fills gaps" comment and leaving `MIOS_IMAGE_NAME=localhost/mios` while `MIOS_IMAGE_REF` stays ghcr (`:=` at :116) -- an inconsistent name/ref pair. Replace the whole `_has_registry_creds` if/else (`globals.sh:~101-113`) with a non-clobbering `: "${MIOS_IMAGE_NAME:=ghcr.io/mios-dev/mios}"`; `MIOS_LOCAL_IMAGE` already carries the localhost tag, and credential state should gate a build-vs-publish DECISION only, never the image NAME. Align the twin: `automation/lib/globals.ps1:62-92` already respects `$env:MIOS_IMAGE_NAME` in both branches, so both sides end up as the same non-clobbering ghcr default. Note the framing correction: the real build path (`usr/libexec/mios/mios-build-driver:766`) hardcodes `localhost/mios:latest` and never sources globals, so this block is dead on all fresh paths -- it is a landmine, not a live outage.
**Where:** `automation/lib/globals.sh:101-113`, `automation/lib/globals.ps1:62-92`, `automation/lib/common.sh:48-52`, `usr/libexec/mios/mios-build-driver:766`
**Done When:** a no-creds host loading `common.sh` keeps `MIOS_IMAGE_NAME=ghcr.io/mios-dev/mios` with a matching ref, the sh and ps1 twins have equivalent override semantics, the 41/45 resolver-twin drift checks stay green, and a negative test asserts the no-creds load does not downgrade the name to localhost.
**Why:** the first future consumer that does `bootc switch`/`podman pull "$MIOS_IMAGE_NAME"` after `common.sh` on a credential-less machine resolves `localhost/mios`, finds nothing, and crashes -- with a name/ref mismatch that makes the cause unreadable.
**Dep:** none
**Who:** you (your WS-RELTOP/T-252 code).

## AGY-90 -- Apply the fact-checked pipeline log-message changelist  (WS-DOCS | P2 | M)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- what the pipeline PRINTS matches what the pipeline DOES.
**What+How:** A read-only fact-check audited every pipeline output string across `automation/*.sh`: 67 findings over 40 scripts -- 25 `inaccurate` (lines claiming work the code does not do, unconditional success on skippable paths, mislabeled unit sets), 27 `vague`, 12 `fluff`, 3 `redundant`. Apply the changelist as verbatim old->new Edits, prioritizing the 25 `inaccurate` (report Section 2a); confirm each `current` string still matches before editing (lines may have shifted) and SKIP with a note on any non-match or any finding you disagree with. Critically, `38-drift-checks.sh:1800` mislabels check (30): its DESCRIPTION claims it reads `userenv.sh` when the body only diffs `names.generated.txt` against `referenced_names.txt` -- fix the DESCRIPTION to match the code and do NOT touch the check logic.
**Where:** `automation/*.sh`, `automation/38-drift-checks.sh:1800`, changelist at `C:\MiOS\docs\agy\verbosity-changelist.md` (fallback raw JSON `...\tasks\wrm19e0hr.output`, report at `.result.report`)
**Done When:** the inaccurate log lines state what the code actually does, the drift-gate stays GREEN, and the run reports N applied / N skipped with a reason per skip.
**Why:** 25 log lines currently tell an operator that work succeeded or ran when it was skipped -- that is worse than silence, because it sends debugging in the wrong direction.
**Dep:** none
**Who:** you (`automation/*.sh` is your tree).

## AGY-91 -- Author the MiOS concept docs from completed research, keeping check-46 green  (WS-DOCS | P1 | L)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- finished research becomes template-conformant docs an arriving agent can use.
**What+How:** Author four new docs following an existing `concepts/*.md` frontmatter and structure so template-conformance (check 46) stays green, cross-referencing `ROADMAP.md`, `architecture.md` and the relevant ADRs. (1) `concepts/container-os-runtime.md` from `C:\MiOS\docs\agy\doc-container-runtime.md` -- the critic-hardened stack (bootc composefs+fs-verity with /var PERSISTING, Podman/Quadlet with no cluster control plane by default, libvirt, signed-UKI Secure Boot where MOK is NOT the chain, greenboot auto-rollback) plus the key doc-vs-SSOT gap: MiOS already PACKAGES but has not wired greenboot, clevis-luks, chrony, ROCm+Intel, ceph+gluster, k3s-selinux, mdevctl, freeipa, nut, guacamole, virt-v2v and pxe-hub -- document as "wire what we ship", and record the corrections that venus is not CUDA and multi-vendor GPU goes through CDI + mdevctl vGPU. (2) `concepts/foss-upstream-map.md` from `...\scratchpad\doc-foss-upstream.md` -- adopt-now FOSS per lane, the archived bootc-image-builder repo being a non-issue since MiOS pins the published container, offline `bootc install --transport oci`, and the Bluefin/Bluespeed + elizaOS comparison. (3) `concepts/image-resolution.md` -- summarize WS-RELTOP resolution: Windows builds and consumes `localhost/mios:latest` locally, Linux `bootc switch`es `ghcr.io/mios-dev/mios:latest`, Xbox provisions offline from a baked oci-archive, the `globals.{sh,ps1}` credential logic is dead on all fresh paths, and `ghcr.io/mios-dev/mios` MUST be a public package for a no-creds fresh-Linux switch; cross-ref AGY-89. (4) `concepts/mios-metal-architecture.md` -- author once Claude hands over the finalized MiOS-Metal split-plane architecture and standards-alignment matrix.
**Where:** `usr/share/doc/mios/concepts/{container-os-runtime,foss-upstream-map,image-resolution,mios-metal-architecture}.md`, sources at `C:\MiOS\docs\agy\doc-container-runtime.md` and `...\scratchpad\doc-foss-upstream.md`
**Done When:** docs 1-3 are committed, pass template-conformance check 46, and are cross-referenced from `ROADMAP.md`/`architecture.md`; doc 4 remains pending only on Claude's content.
**Why:** the research is finished but lives in scratch files no future agent will find, and the doc-vs-SSOT gap it records is the actual list of capabilities MiOS ships dead.
**Dep:** none
**Who:** you (you own `usr/share/doc/mios/` + the doc template).

## AGY-92 -- Fix the four audit BLOCKERS that ship user-facing breakage behind GREEN gates  (WS-GATE | P0 | L)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- gates go green for the right reason, so a green gate is evidence of correctness.
**What+How:** An adversarial audit vetted 8 workstreams (all needs-refinement); each blocker below ships green because a fixture or generator encodes the WRONG behavior, so the gate structurally cannot catch it. **B1 (deploy-time bootc failure):** the MIOSSYS refactor (c47ed570) moved bakes to `57-mios-sys-build.sh` + `mios-bake-group extra`, but `automation/08-system-files-overlay.sh:173-184` still `ln -sf`s EVERY `.image` quadlet into `/usr/lib/bootc/bound-images.d/`; `mios-llm-heavy-alt.image` (sglang, group sys) and `mios-llama-swap.image` (llama-swap, group cuda) now belong to no baked group, so `57` builds the bases via `COPY --from` but never pulls those engines into `$STORE` as resolvable bound refs -> bootc fails "does not resolve to an image ID" at INSTALL. Fix by re-adding `mios-bake-group sys` and `mios-bake-group cuda` RUNs (per the `[build.bake]` one-RUN-per-group comment) OR retiring the two bound `.image` quadlets so 08-overlay stops binding them, plus a build-time assertion that every `bound-images.d` symlink resolves. **B2 (configurator Save DOA):** `usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py:537-549` range-checks every `[ports].*` as 1..65535, but `mios.toml:7775 [ports].stack_id = 0` is an OFFSET MULTIPLIER, not a port -> `validate_config(full-toml)` returns ok=False, and omitting `[ports]` trips the "Refusing to drop critical [ports]" guard at :527, so the configurator's full-TOML POST 422s for every operator. Fix by exempting non-listening keys (denylist `stack_id`, or range-check only keys resolving to a real listening port) and add a round-trip test over the committed `mios.toml`. **B3 (breaks ALL ssh):** the dotfiles surface reuses `kind="ini-merge"` (a git-config INI parser) for OpenSSH `ssh-config`, emitting bracketed `[host "mios-*"]` headers with `=`, which OpenSSH rejects with `Bad configuration option`; since `mios.toml` maps `apply.target` to `~/.ssh/config`, `mios-dotfiles-render apply ssh-config` writes an unparseable file, and the gate is green only because `fixtures/ssh-config.expected` encodes the same wrong format. Fix with a dedicated ssh-config kind (or plain template) emitting real stanza syntax -- `Host mios-*` plus tab-indented keywords, no brackets or `=` -- and regenerate `.base`/`.expected`. **B4 (liquid-glass work is DEAD):** `automation/54-bake-hyprland.sh:13` does `cat << 'EOF' > /usr/share/mios/hyprland/hyprland.conf`, unconditionally regenerating the file from the inline heredoc (still rounding=10/passes=3, no vibrancy or liquid beziers) and discarding every edit to the tracked artifact. Fix by applying the liquid-glass and spring-curve changes to the heredoc INSIDE the generator, preserving the `@@MIOS_COLOR_*@@` placeholders and the Cockpit windowrules/`$mainMod, C` keybind, then delete or gitignore the tracked artifact OR add a drift-check asserting artifact == generator output. Also fix the two CI hardening items: **H1** `.github/workflows/mios-ci.yml:308` sources userenv without setting `MIOS_VENDOR_TOML`, so on stock ubuntu runners `MIOS_IMAGE_NAME` is UNSET -> malformed `:latest` tags and `podman login ''`, ungated because PUBLISH=true at :38 -- export `MIOS_VENDOR_TOML="$GITHUB_WORKSPACE/mios.toml"` before sourcing or default `: "${MIOS_IMAGE_NAME:=${REGISTRY}/${IMAGE_NAME}}"`; and **H2**, AGY-89 is still unfixed and now WIDENED -- `globals.sh:112` still exports `localhost/mios` unconditionally and `0bd49e4b` emits it into `system-sync-env.sh:234`, persisting the clobber into the runtime env file.
**Where:** `automation/08-system-files-overlay.sh:173-184`, `automation/57-mios-sys-build.sh`, `usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py:527-549`, `mios.toml:7775`, `fixtures/ssh-config.expected`, `automation/54-bake-hyprland.sh:13`, `.github/workflows/mios-ci.yml:38,308`, `automation/lib/globals.sh:112`, `system-sync-env.sh:234`
**Done When:** all four blockers are fixed, the masking fixtures and generators are corrected so the green gate becomes meaningful, the build-time and round-trip assertions exist, and the drift-gate is green FOR THE RIGHT REASON.
**Why:** today an install can fail at bootc bound-image resolution, no operator can save a single config edit (422 on every POST), applying the ssh dotfile breaks all outbound ssh, and every liquid-glass desktop change is silently discarded at build -- and the gate reports green through all of it.
**Dep:** none

## AGY-93 -- Work the owner=agy finalization queue by phase, after the AGY-92 blockers  (WS-CONFIG | P1 | L)  **[DONE]**
**Goal:** E-11 Unified config surface -- every reader resolves through the shared layered resolver, and the release/registry/template/doc tails close behind it.
**What+How:** The Finalization Master Plan (deduped across the audit, MiOS-Metal, container-runtime, registry, verbosity and deploy investigations) is at `C:\MiOS\docs\agy\mios-finalization-plan.md` with the Definition of Done, critical path and per-owner queues. Work the owner=agy queue in order. **P0:** (a) reconcile the legacy `MIOS_TOML` vs resolver `MIOS_VENDOR/HOST/USER_TOML` env schism across `38-drift-checks.sh` (L522/L779/L1973/L2007) and `57-mios-sys-build.sh` -- this GATES every agent-pipe migration below; (b) reconcile the `mios-ci.yml` PUBLISH literal against its own lines 26-38 capacity-gate comment. **P1 agent-pipe correctness:** migrate the raw-`MIOS_TOML` consumers (oscontrol L162; routing L83/L135; verbcatalog L67/L610) and the portal readers (L118/L989/L996/L290) onto `_toml_section`/`load_merged`; fix the user-layer write to persist a DELTA instead of freezing vendor defaults; make `db_authoritative` fall back PER SECTION so a DB miss never yields an empty agent registry; fix `to_toml` datetime serialization; memoize resolution so `_toml_section`/`get` stop reconnecting to Postgres per call; then land the anti-regression gate and an offline `test_mios_toml.py` covering the db branch. **P1 registry:** AGY-89's non-clobber fix in BOTH sh/ps1 twins plus a `MIOS_IMAGE_NAME`/`MIOS_IMAGE_REF` twin-parity drift check; event-gate GHCR push/sign so it never runs on PRs; harden empty-`MIOS_IMAGE_NAME` on GitHub and Forgejo. **P1 templates:** point conformance match-paths at `usr/lib/systemd/system` and `usr/share/containers/systemd` where the ~73 units and ~22 quadlets actually live, rebaseline `conformance-grandfathered.list` to the ~23 real offenders, and wire `tools/compile-templates.py` into the drift-gate or delete it. **P1 docs:** author the four AGY-91 concept docs from `C:\MiOS\docs\agy\{doc-container-runtime,doc-foss-upstream,image-resolution,doc-mios-metal}.md`, template-conformant with check-46 green and cross-reffed, then run the full green gate.
**Where:** `C:\MiOS\docs\agy\mios-finalization-plan.md`, `automation/38-drift-checks.sh` (L522/L779/L1973/L2007), `automation/57-mios-sys-build.sh`, `.github/workflows/mios-ci.yml:26-38`, `usr/lib/mios/agent-pipe/` (oscontrol, routing, verbcatalog, portal), `automation/lib/globals.{sh,ps1}`, `conformance-grandfathered.list`, `tools/compile-templates.py`, `usr/share/doc/mios/concepts/`
**Done When:** each phase leaves its drift-impact green, the AGY-92 blockers are cleared, and progress is reported per phase rather than as one lump.
**Why:** the `MIOS_TOML` schism blocks every agent-pipe resolver migration behind it, the template conformance check currently scans directories where no units live (so it validates nothing), and the registry/doc tails are the remaining gap between "gate green" and "finalized MiOS".
**Dep:** AGY-92 (P0 blockers first); supersedes the "full batch arrives later" note in AGY-92

## AGY-94 -- Land the 11-workstream adversarial-audit follow-up register: restore the lost drop-in fanout, un-inert `[accounts].db_backed`, pin the from-source clones  (WS-HARDEN | P1 | L)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- every capability MiOS already ships is SSOT-wired AND drift-gated, or explicitly removed; here two silently-inert capabilities are made real again.
**What+How:** Work the ~40-item follow-up list in `docs/agy/agy-audit-followups.md`, regressions first. (1) `automation/41-mios-dropin-fanout.sh` writes drop-ins to the WRONG path (not the image's `/usr/lib/systemd/system`) and its GATES fanout loop was dropped, so virt-gate / bare-metal-only / mios-virt-gate / mios-wsl2 conditioning no longer gates units -- restore the fanout (or replace it with an SSOT table plus committed static `.d` dirs), add a post-build assertion that fanned drop-ins exist in the FINAL image, and add a drift-check for orphaned committed drop-ins with no generator source. (2) Verify `tools/lib/userenv.sh` actually exports `MIOS_ACCOUNTS_DB_BACKED` from `[accounts].db_backed`; without it `17-accounts-db.sh` takes the default-false branch and disables the service. (3) P2 reproducibility: pin the mios-sys/mios-cuda from-source git clones (tags/digests) with retry/backoff, and strip the stale `'MiOS' v0.2.4` headers from `automation/manifest.json` (they escape the version gate only because `.json` is excluded). (4) P2 resolver/lint: single-source the duplicated walk / `EXCLUDED_SECTIONS` / `WALK_*` logic between `check-resolver-twin.py` and `userenv.sh`; broaden `lint-shell.sh` to cover `automation/lib`, `tools/lib` and `usr/libexec` shebang files (common.sh / packages.sh / globals.sh are UNLINTED today); extend eval-safety to the userenv twins' `eval "$exports"`; memoize `is_db_authoritative()` / `load_db_config()` so each call stops reconnecting to PG and re-walking the whole TOML. (5) Re-audit that AGY-90/91's applied verbosity edits match the changelist verbatim, that check-30's DESCRIPTION (not its logic) was fixed, and that check-46 is still green.
**Where:** `docs/agy/agy-audit-followups.md`, `automation/41-mios-dropin-fanout.sh`, `automation/17-accounts-db.sh`, `tools/lib/userenv.sh`, `tools/check-resolver-twin.py`, `tools/lint-shell.sh`, `automation/manifest.json`, `automation/38-drift-checks.sh`
**Done When:** fanned drop-ins are asserted present in the built image and the orphan-dropin check is green; `MIOS_ACCOUNTS_DB_BACKED` appears in the build env closure; the clone pins and `manifest.json` version headers are clean; the shell linter's file list includes the three core build libs; checks 30 and 46 are green and `just drift-gate` passes; per-phase report filed against the AGY-93 plan.
**Why:** today conditional units may run on hosts they were meant to skip (virt/bare-metal/WSL2 gating silently lost), the entire accounts-DB workstream ships disabled, the from-source builds are non-reproducible default-branch clones, and the three most load-bearing build libraries are never linted.
**Dep:** AGY-90, AGY-91, AGY-93 (folds in as its P1/P2 detail)

## AGY-95 -- Turn the "PostgresOS" research into shipped accounts wiring: userdb projection, lldap decision, ADR, peer-OS cleanup  (WS-ACCT | P1 | L)  **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- PostgreSQL becomes the accounts SSOT with a lossless projection to the OS, instead of a flag that ships inert.
**What+How:** Execute `docs/agy/doc-postgresos-accounts.md` and `doc-peer-os-landscape.md`. "PostgresOS" is the operator's COINAGE, not a product, which is exactly why `MIOS_ACCOUNTS_DB_BACKED` ships inert -- it must be assembled from FOSS bricks. (P1) Add/verify the bridge so `tools/lib/userenv.sh` auto-derives `MIOS_ACCOUNTS_DB_BACKED` from `[accounts].db_backed` at build time, plus a drift-check asserting the derivation, so `17-accounts-db.sh` stops defaulting the service off. (P2) Project each `[accounts]` row into a generated `/usr/lib/userdb/<name>.user` JSON drop-in -- systemd userdb, with nss-systemd synthesizing passwd/shadow/group -- the same render+gate shape as `mios-theme-render` and the dotfiles registry; mine libnss-pgsql's schema but do NOT ship it (archived/dead). (P2) Evaluate lldap-over-Postgres as the cross-platform SSOT LDAP face (RFC2307 to SSSD/nslcd) so Postgres stays authoritative without a second directory store; record OpenLDAP back-sql and Keycloak+SCIM as the alternatives. (P2) Write an ADR stating that "PostgresOS" is a MiOS concept name mapped to a concrete stack (systemd userdb projection + lldap + SCIM-as-pattern + the Windows autounattend/SetupComplete `New-LocalUser` path) with no upstream repo. (P3) Close every ROADMAP/doc reference to a distinct "PodmanOS" (reality: `quay.io/podman/machine-os`, FCOS + bootc-switch) and repoint the archived bootc-image-builder reference at osbuild/image-builder. Windows half is already landed in mios-bootstrap (RID-500 rename moved from the specialize pass into SetupComplete.cmd; the `MiOS-AccountSync` minute-task reconciles DB to Windows accounts).
**Where:** `docs/agy/doc-postgresos-accounts.md`, `docs/agy/doc-peer-os-landscape.md`, `tools/lib/userenv.sh`, `automation/17-accounts-db.sh`, `usr/share/mios/mios.toml` (`[accounts]`), a userdb generator under `usr/libexec/mios`, `automation/38-drift-checks.sh`, `docs/adr/`, `ROADMAP.md`
**Done When:** `db_backed` is no longer inert and its derivation is drift-gated; the `/usr/lib/userdb/*.user` projection lands and regenerate-and-diff is green; the lldap decision and the PostgresOS ADR are committed; no doc still references a distinct "PodmanOS"; `just drift-gate` green.
**Why:** the accounts workstream currently ships a true SSOT value that reaches nothing, and contributors chase two products ("PostgresOS", "PodmanOS") that do not exist.
**Dep:** AGY-94 (shares the `db_backed` inert regression)

## AGY-96..105 -- Campaign banner: convert the ~20h ghcr publish firefight into enforced gates, tests and docs  (WS-HARDEN | P1 | L)  **[DONE]**
**Goal:** E-16 The bake plane -- the three failure classes that broke the 0.3.0 publish are fixed systemically, so a stock GitHub runner can bake and publish as a true equal.
**What+How:** Ten independent, code-only, no-live-VM items that promote the manual one-off fixes landed during the `ghcr.io/mios-dev/mios` publish campaign (curl-retry sweep, nested-podman capability flags, empty-var strip, bake shrink) into permanent enforcement: drift-gate checks (AGY-96, 99, 100), correctness fixes with tests (AGY-97, 98, 101, 105), a pre-flight probe (AGY-102), a producer hardening (AGY-103) and a policy reference doc (AGY-104). Work top-down; each item stands alone. Stage ONLY your own files -- never the generated Quadlets.
**Where:** `automation/38-drift-checks.sh`, `automation/lib/masking.sh`, `automation/build.sh`, `.github/workflows/mios-ci.yml`, `usr/share/doc/mios/reference/`, `tools/`
**Done When:** all ten child items are landed and `just drift-gate` is green with the new checks active.
**Why:** without this the same failure class returns silently -- one retry-less `curl` already cost roughly 20 hours of publish attempts.
**Dep:** none

## AGY-1695 -- Drift-gate every build-time network fetch for a retry flag  (WS-HARDEN | P1 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement -- the network-at-build bakers stop being a "one transient 504 kills the bake" class.
**What+How:** The build died repeatedly on a transient GitHub `504` because the AdGuard `RUN curl … | tar` in `usr/share/mios/sys/Containerfile` carried no `--retry`. Claude hand-swept every fetch; now enforce it. Add a check at the next free number in `automation/38-drift-checks.sh` that greps every build-time network fetch -- `RUN … curl …` and bare `curl … http(s)://` across all `**/Containerfile` (sys, agents, cuda), `automation/*.sh`, `automation/lib/*.sh`, `usr/libexec/mios/*.sh` and `automation/90-generate-sbom.sh` -- and FAILS unless the call carries `--retry`, routes through `scurl` (which injects retry), or is listed in a `mios.toml [build.net].retry_exempt` allowlist with a documented reason. Recognize `wget --tries` as compliant too. Read-only, fast, requires no built image.
**Where:** `automation/38-drift-checks.sh`, `usr/share/mios/mios.toml` (`[build.net].retry_exempt`)
**Done When:** the gate flags an injected retry-less `curl`, passes clean on HEAD (all fetches now retried), a negative test proves the injected case is caught, and `just drift-gate` is green.
**Verify:** the gate flags an injected retry-less `curl`, passes clean on HEAD, and a negative test proves the red.
**Do NOT:** allowlist the fetches that fail. The gate exists because an unretried fetch is a flaky build.
**Why:** one unretried fetch anywhere in the tree can burn another multi-hour publish window, and nothing currently prevents a new one from being added.
**Dep:** none
**Who:** you (bash + drift-gate) | **When:** FIRST -- highest leverage; a ~20h publish loss traced to ONE retry-less `curl`.

## AGY-97 -- Fix scurl's binary-stream corruption and harden its argument parser  (WS-HARDEN | P1 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement -- the credential-masking fetch wrapper stops being a data-corruption hazard on the dominant build pattern.
**What+How:** `scurl()` in `automation/lib/masking.sh` sets `is_binary` only when it sees `-o` / `-O` / `--output`, but the build's dominant pattern is `curl … | tar` (stream to stdout, no `-o`), so scurl classifies a tarball as TEXT and pipes it through `mask_filter` (`sed -u`), corrupting the stream. (1) Fix classification so `mask_filter` never touches a body destined for a pipe or download -- gate masking on stdout being a TTY, or skip `mask_filter` for any non-`-o` streamed body. (2) Harden the naive parser, which takes the FIRST `https?://` argument as the URL and therefore misses `--output=FILE` and `--url X` and mis-detects a `-H` header value that looks like a URL. (3) Add `automation/lib/test_masking.sh` asserting: a piped binary body is byte-identical (no sed mangling), a github URL gets the auth header AND the token is masked in text output, and `--output=` is detected. Wire the test into the shell-test/lint gate.
**Where:** `automation/lib/masking.sh`, new `automation/lib/test_masking.sh`, the shell-test/lint gate wiring
**Done When:** `curl … | tar` through scurl is byte-identical to raw curl, the parser handles all three edge cases, the new test is green, and `just drift-gate` is green.
**Why:** every streamed tarball fetched through scurl is silently corruptible today, and the parser can leak or misroute credentials on ordinary flag spellings.
**Dep:** none
**Who:** you (bash) | **When:** next -- this is a real DATA-CORRUPTION bug, not just hygiene.

## AGY-98 -- One shared empty-`MIOS_*` strip helper plus an import-clean-under-empty-env test  (WS-HARDEN | P1 | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- the ~734-key var closure stops emitting values that break the consumers it exists to serve.
**What+How:** The SSOT var closure (`usr/lib/mios/userenv.sh` around line 598) exports ~734 `MIOS_*` keys, many of them EMPTY. An empty value (unlike an absent one) defeats `os.environ.get(K, D)` -- the default only applies when the key is absent -- so int/float coercion raised `ValueError` and crashed the 56-test gate. Claude patched two ad-hoc spots: `mios_pipe/__init__._strip_empty_mios_env()` and a `{ unset $(compgen -v MIOS_); … }` wrapper in `automation/build.sh`. Consolidate: (1) one helper `usr/lib/mios/mios_env.py::strip_empty_mios_env(env=os.environ)` used by `mios_pipe/__init__.py`, dropping the private copy; (2) a `test_mios_env.py` that sets EVERY closure-emitted `MIOS_*` key to `""` and asserts every `agent-pipe/mios_*.py` module still imports and its config coercion falls back to defaults with no `ValueError`; (3) evaluate making the closure not emit purely-empty keys at all -- that is the ROOT fix and would demote the strip to belt-and-suspenders.
**Where:** new `usr/lib/mios/mios_env.py`, `usr/lib/mios/agent-pipe/mios_pipe/__init__.py`, new `test_mios_env.py`, optionally `usr/lib/mios/userenv.sh`
**Done When:** exactly one strip helper exists (no duplicate implementation), the empty-env import test is green across all agent-pipe modules, and `just drift-gate` is green.
**Why:** two divergent ad-hoc strips guard a crash class that can reappear in any new module or test that does not repeat the dance.
**Dep:** AGY-84 (unified key library -- the root fix)
**Who:** you (Python) | **When:** independent.

## AGY-99 -- Drift-gate the nested-podman capability flags and document why GHA needs them  (WS-CI-PARITY | P1 | M)  **[DONE]**
**Goal:** E-16 The bake plane -- GitHub is a first-class publisher, so the flags that make podman-in-podman work there cannot be silently removed.
**What+How:** The GHA build is unprivileged (unlike the `Privileged=true` Forgejo runner) and needed exact flags for the nested bake: `--device /dev/fuse` (fuse-overlayfs mount), `--cap-add` covering `SYS_ADMIN` (mount) and `SYS_RESOURCE` (crun `setrlimit RLIMIT_NOFILE`), and `--security-opt seccomp=unconfined --security-opt apparmor=unconfined`. Claude added them to `.github/workflows/mios-ci.yml` (main plus smoke `podman build`) and to both nested `podman build` calls in `usr/libexec/mios/57-mios-sys-build.sh`. Removing any of them regresses ONLY on GitHub -- Forgejo's Privileged mode masks the failure -- which makes it a landmine. (1) Add a drift-check that parses the workflow steps and the 57 script and asserts each `podman build` invocation carries the required device / cap-add / security-opt flags. (2) Write `usr/share/doc/mios/reference/nested-podman-caps.md` explaining why podman-in-podman-in-GHA needs each flag and why Forgejo never hits it, cross-referencing the workflow and script.
**Where:** `automation/38-drift-checks.sh`, `.github/workflows/mios-ci.yml`, `usr/libexec/mios/57-mios-sys-build.sh`, new `usr/share/doc/mios/reference/nested-podman-caps.md`
**Done When:** the gate flags a stripped `--device` or `--cap-add`, passes on HEAD, the doc is template-conformant (check-46 green), and `just drift-gate` is green.
**Why:** a one-line flag removal passes every local and Forgejo run and only breaks the GitHub publish, with no error message pointing at the cause.
**Dep:** none
**Who:** you (bash + docs) | **When:** independent.

## AGY-100 -- Assert the baked image still fits a standard GitHub runner's disk budget  (WS-PUBLISH | P1 | M)  **[DONE]**
**Goal:** E-16 The bake plane -- the BAKE gate answers presence and capacity, so `PUBLISH=true` stays safe on a stock runner.
**What+How:** GitHub publish is capacity-gated: the vLLM+SGLang whales (~47GB) blow a standard runner's disk, and the firstboot-tier evicts them from the bake so the image fits. Nothing prevents a future sidecar from silently re-bloating the bake past the runner and re-breaking publish. Add a build-time or drift-gate assertion that sums the resolved bake-group image refs/sizes and FAILS when the projected baked size exceeds an SSOT budget `mios.toml [build.bake].runner_disk_budget_gb`, printing the top contributors. Read the groups from `[build.bake]` and `usr/libexec/mios/mios-bake-group`; keep the check STATIC -- no live pull -- using the recorded `bound-images.tsv` / SBOM sizes where available and falling back to ref count.
**Where:** `automation/38-drift-checks.sh` (or a check inside `usr/libexec/mios/57-mios-sys-build.sh` / `mios-bake-group`), `usr/share/mios/mios.toml` (`[build.bake].runner_disk_budget_gb`)
**Done When:** the gate flags a whale added to a baked group, passes on HEAD (whales firstboot-tier'd out), the budget is SSOT-defined, and `just drift-gate` is green.
**Why:** re-bloating the bake breaks GitHub publish only after a full multi-hour bake run, and the cause is invisible until the runner hits ENOSPC.
**Dep:** AGY-99
**Who:** you (bash + Python) | **When:** after AGY-99.

## AGY-101 -- Prove `90-generate-sbom.sh` can never fail the build  (WS-DEBT | P2 | S)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening -- provenance generation is degrade-open by contract, and the contract is now tested rather than assumed.
**What+How:** `automation/90-generate-sbom.sh` must `exit 0` under every failure mode -- no egress, missing syft, unbound `MIOS_USR_DIR`, a syft scan hiccup -- because SBOM output is provenance, not a build prerequisite; a prior FATAL regression killed a 17-minute bake. Nothing tests that invariant. Add `automation/test_generate-sbom.sh`, wired into the shell-test gate, that runs the script under each of four modes: (a) `syft` absent with no egress, (b) unset `MIOS_USR_DIR`, (c) unwritable `ARTIFACT_DIR`, (d) a stubbed failing `syft scan` -- asserting `exit 0` plus a WARN line each time. Also assert the syft-installer `curl` carries `--retry`, regression-guarding the AGY-96 sweep.
**Where:** new `automation/test_generate-sbom.sh`, `automation/90-generate-sbom.sh`, the shell-test/lint gate wiring
**Done When:** the test proves `exit 0` under all four failure modes and `just drift-gate` is green.
**Why:** an SBOM hiccup can kill a full bake near the end of the run, and there is currently nothing to stop that regression from returning.
**Dep:** AGY-96
**Who:** you (bash) | **When:** independent.

## AGY-102 -- Pre-flight probe for build-download URL rot, run before cutting a release  (WS-DEBT | P2 | M)  **[DONE]**
**Goal:** E-14 Float latest globally -- rotted upstream assets are caught by a probe rather than by a mid-build hard failure.
**What+How:** Three build downloads (`forgejo-runner`, `jaeger`, `crowdsec`) silently rotted -- moved assets and renamed tarballs -- and only surfaced as a hard failure roughly an hour into a build. Add a NON-build-blocking probe `tools/check-build-urls.sh` plus a `just check-build-urls` target that extracts every build-time download URL from the Containerfiles and `automation`, resolves SSOT versions FIRST (SBOM-not-hardcode: versions are SSOT, so resolve the ref, then probe), issues `curl -sI --retry` / HEAD, and REPORTS each URL with its HTTP status, flagging non-2xx/3xx. This is a pre-flight, not a gate -- it is network-dependent, so it runs before cutting a release and may optionally hook a documented scheduled CI job, never the build gate.
**Where:** new `tools/check-build-urls.sh`, `Justfile` (`check-build-urls` target), a note in the release runbook
**Done When:** the probe lists every build URL with its status, flags a deliberately-broken URL, is documented as a pre-release step, and is `bash -n` clean.
**Why:** upstream asset rot is currently discovered an hour into a build, wasting the whole run and blocking the release.
**Dep:** none
**Who:** you (bash + a Justfile target) | **When:** independent.

## AGY-103 -- Harden the `artifacts/ai-rag` producer to match the `/v1`-only consumer  (WS-BOOTSTRAP | P2 | M)  **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the RAG artifact chain is `/v1`-only and SSOT-driven end to end, not just on the consumer side.
**What+How:** Claude already hardened the CONSUMER, `tools/log-to-bootstrap.sh`, replacing its purged-ollama `:11434` RAG snippet with the MiOS `/v1` lane at `:8642` (OpenAI-compatible). Now harden the PRODUCER: grep for what generates `artifacts/ai-rag/` (`mios-knowledge-graph.json`, `mios-context-*.tar.gz`, `rag-manifest.yaml`, `README-AI-INTEGRATION.md`) and ensure it (a) references no purged runtimes -- ollama, localai, surrealdb -- everything goes through the `/v1` lane; (b) is SSOT-driven, with endpoints/models/ports read from `mios.toml` rather than hardcoded; (c) emits the manifest shape `log-to-bootstrap.sh` expects; (d) makes every network step `--retry`'d and degrade-open. Preserve the LEGITIMATE historical/enforcement ollama tokens -- do not re-flag those.
**Where:** the RAG-artifact generator (locate via grep for `artifacts/ai-rag`), `tools/log-to-bootstrap.sh`, `usr/share/mios/mios.toml`
**Done When:** the producer emits `/v1`-only, SSOT-driven artifacts that `log-to-bootstrap.sh` consumes cleanly, no purged-runtime references remain in the generated RAG docs, and the scripts are `bash -n` / parse clean.
**Why:** a hardened consumer paired with a stale producer still ships RAG docs that point agents at a runtime MiOS deleted.
**Dep:** none
**Who:** you (bash/Python) | **When:** independent.

## AGY-104 -- Author the build-time network-fetch policy reference  (WS-DOCS | P2 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- the newly enforced rules are written down so an arriving agent picks up the policy cold.
**What+How:** Write `usr/share/doc/mios/reference/build-network-policy.md`, following an existing `reference/*.md` frontmatter so check-46 stays green, codifying the campaign's four rules: every build-time fetch MUST (1) resolve its version and URL from SSOT (SBOM-not-hardcode -- never hand-pin a digest), (2) carry `--retry` or route through `scurl`, (3) be explicitly classified FATAL (core binary, so die) versus DEGRADE-OPEN (provenance/optional, so WARN and `exit 0`, as in `90-generate-sbom.sh`), and (4) prefer `scurl` for credential masking. Cross-reference the AGY-96 gate, the AGY-99 capability doc, the AGY-100 budget check and the AGY-102 probe, and link it from `ROADMAP.md`.
**Where:** new `usr/share/doc/mios/reference/build-network-policy.md`, `ROADMAP.md`
**Done When:** the doc is committed, template-conformant (check-46 green), cross-referenced from ROADMAP, and `just drift-gate` is green.
**Why:** the retry/masking/degrade-open rules currently exist only as gate code and tribal memory, so new fetches get written the old way and fail the gate for reasons nobody can look up.
**Dep:** AGY-96, AGY-97, AGY-101, AGY-102
**Who:** you (docs) | **When:** after AGY-96/97/101/102 (document the enforced rules).

## AGY-105 -- One shared clean-env harness for the agent-pipe test gate  (WS-DEBT | P1 | M)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- the test gate is hermetic by construction, so a new test cannot reintroduce the leaked-env flake.
**What+How:** Both test gates in `automation/build.sh` inline `{ unset $(compgen -v MIOS_ 2>/dev/null); …; }` to strip the leaked SSOT closure env, and the seven hermetic `test_mios_*` fixes (clean env, `sys.executable` for Popen, `mios_toml.clear_cache()`) live per-test. Any new test that forgets the dance reintroduces the flake. Extract ONE `tools/run-agent-pipe-tests.sh` -- or a pytest `conftest.py` fixture -- that runs the suite under a guaranteed-clean environment (stripped `MIOS_*`, cleared TOML cache, `sys.executable` as the interpreter), and have BOTH `automation/build.sh` gates and any CI test step call it instead of re-inlining.
**Where:** new `tools/run-agent-pipe-tests.sh` (or `conftest.py`), `automation/build.sh`, the CI test step
**Done When:** both gates run through the one harness, a deliberately env-leaking test fails deterministically through it, the full `test_mios_*` suite is green, and `just drift-gate` is green.
**Why:** the hermetic-test contract is copy-pasted in two places and per-test, so the next added test flakes intermittently in CI with no obvious cause.
**Dep:** AGY-35, AGY-98
**Who:** you (bash + Python) | **When:** independent.

## AGY-106..122 -- Campaign banner: installer consolidation, runtime wiring, server.py decomposition, Rust ports and Mini foundations  (WS-INSTALL | P1 | L)  **[DONE]**
**Goal:** E-21 One deploy front door -- the Linux/Python/shared half of the installer-unification plan lands alongside the runtime-wiring and language-port frontier.
**What+How:** Batch 2 of the 2026-07-24 queue, opened after AGY-1..105 drained. Scope: the Linux/Python/shared half of installer unification (Claude owns the `.ps1` half -- Get-MiOS, build-mios.ps1, mios-install.ps1); the "wire what we ship" container-runtime SSOT projections (clevis-luks, multi-vendor GPU CDI, greenboot coverage); further `server.py` decomposition; Rust ports of the generator and SSOT-walk; MiOS-Metal foundations; the missing offline bare-metal installer; and negative tests for the new gates 64/65/66. All items are code-only with no live-VM requirement. Law 15 (mios.toml `[laws]` id 15) is in force: double-check BOTH repos and triple-check before acting. Stage ONLY your own files.
**Where:** `installation/**`, `automation/**`, `usr/lib/mios/agent-pipe/**`, `tools/native/**`
**Done When:** every child item AGY-106..122 is landed and `just drift-gate` is green.
**Why:** without the batch, Linux still has two divergent installer entry points, shipped runtime capabilities stay unwired, and the Rust tier stays theoretical.
**Dep:** AGY-1..105

## AGY-1696 -- Fold `build-mios.sh` into `mios-install.sh` so Linux is one installer file plus one contract  (WS-INSTALL | P1 | M)  **[DONE]**
**Goal:** E-21 One deploy front door -- `installation/mios-install.{sh,ps1,bat}` becomes the single guided dispatcher for every target.
**What+How:** `build-mios.sh` is misnamed: it is a Linux BOOTSTRAP INSTALLER (bootc switch, FHS git-init root merge), not an OCI builder. Move its bootc-switch and FHS-root-merge logic -- preserving the `MIOS_FHS_TOTAL_ROOT_MERGE` gate and its prompt intact -- into the `fedora` and `bootc` stage paths of `installation/mios-install.sh`, then reduce `build-mios.sh` to a thin `exec installation/mios-install.sh fedora|bootc "$@"` redirector so the `curl .../build-mios.sh | bash` web entry (via install.sh / bootstrap.sh) keeps working. This is the change that makes Linux genuinely "one installer file plus one contract".
**Where:** `installation/mios-install.sh`, `build-mios.sh`, `installation/mios-common.sh`
**Done When:** a bootc host defaults to bootc-switch and everything else to the fedora FHS path; the redirector preserves the web-entry one-liner; `bash -n` is clean; `just drift-gate` is green.
**Verify:** a bootc host takes bootc-switch and everything else the FHS path, both exercised, and the redirector preserves arguments verbatim.
**Do NOT:** delete the old entry point before its doc references are repointed. They are doc-bundled.
**Why:** two Linux installers with divergent functionality means a fix landed in one silently misses the other, and the web one-liner reaches the weaker path.
**Dep:** none
**Who:** you (bash)

## AGY-107 -- Delete the byte-identical `MiOS-Monitor.py` twin; ship ONE cross-platform TUI monitor  (WS-INSTALL | P2 | S)  **[DONE]**
**Goal:** E-21 One deploy front door -- one canonical executor per role, no name-stable duplicates left behind.
**What+How:** `installation/MiOS-Monitor.py` is a verified byte-identical twin (md5 `b56d7de84…`) of the canonical monitor, and `installation/MiOS-Monitor.ps1` is a rich-only shim. GREP the whole tree and `C:\MiOS` FIRST -- Start Menu entries, scheduled tasks, Ventoy autorun, launchers -- for name-stable references, repoint them all at the ONE cross-platform TUI monitor `MiOS-Mon.py`, fold the `.ps1` dependency bootstrap into the shared launcher, and only THEN delete the twin and the shim.
**Where:** `installation/MiOS-Monitor.py`, `installation/MiOS-Monitor.ps1`, `MiOS-Mon.py`, the shared launcher, any referencing unit
**Done When:** exactly one monitor executor exists, no dangling name-stable reference remains, the monitor still launches from every documented entry point, and `just drift-gate` is green.
**Why:** two copies of the same monitor drift apart and the Start Menu / autorun entries silently keep launching the stale one.
**Dep:** none
**Who:** you (Python + bash)

## AGY-108 -- Target-keyed prereq resolver and a real `--dry-run` for `mios-install.sh`  (WS-INSTALL | P1 | M)  **[DONE]**
**Goal:** E-21 One deploy front door -- one prereq resolver keyed off the chosen target, and `--dry-run` on every route.
**What+How:** Per the unified-installer plan, `installation/mios-install.sh` needs a target-keyed PREREQ resolver (Linux: dnf packages, rustup/cargo for the native tools) invoked as an implicit prelude before each lifecycle target, plus a `--dry-run` flag that prints the RESOLVED environment and argv for ANY target and exits 0 -- it must never fake a run. Windows-only targets invoked from Linux print exact copy-paste guidance rather than failing opaquely. Shared logic lives in `installation/mios-common.sh`.
**Where:** `installation/mios-install.sh`, `installation/mios-common.sh`
**Done When:** each target resolves its prereqs before running, `--dry-run` prints resolved env and argv for every target, `bash -n` is clean, and `just drift-gate` is green.
**Why:** today each target hand-rolls its own prereq checks and there is no way to inspect what an install would do before it does it.
**Dep:** AGY-106
**Who:** you (bash)

## AGY-109 -- SSOT-resolve the configurator URL and kill the literal `:8640`  (WS-INSTALL | P1 | S)  **[DONE]**
**Goal:** E-12 ZERO-HARDCODES -- an audit-confirmed Law 7 violation on the deploy path is floated back to SSOT.
**What+How:** The `config` target HARDCODES `http://localhost:8640/` (`installation/mios-install.ps1:219` and its `.sh` peer) instead of resolving `[ports].agent_pipe` from SSOT. Fix the Linux/shared path to resolve the port through `mios-common.sh`'s `mios_ssot_value` and open `http://localhost:<[ports].agent_pipe>/configure`, falling back to the offline configurator HTML when nothing is listening. Flag the `.ps1` twin fix for Claude rather than editing it here.
**Where:** `installation/mios-install.sh`, `installation/mios-common.sh`, `usr/share/mios/mios.toml` (`[ports].agent_pipe`)
**Done When:** the config target opens the SSOT-resolved port, no literal `8640` remains in the Linux path, and `just drift-gate` is green.
**Why:** an operator who moves `[ports].agent_pipe` gets an installer that opens a dead URL, and the literal keeps the hardcode-lint red.
**Dep:** AGY-106
**Who:** you (bash)

## AGY-110 -- clevis-luks SSOT projection and gate (TPM-bound disk unlock)  (WS-RUNTIME | P2 | M)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a packaged-but-unwired capability becomes SSOT-wired and drift-gated.
**What+How:** MiOS PACKAGES clevis and clevis-luks but never wires them (a recorded container-runtime doc gap). Add a `[security.luks]` SSOT block carrying the tpm2 pin, the PCR set and `enabled=false` as the default, plus a generator under `usr/libexec/mios` that projects the clevis binding config, plus a regenerate-and-diff drift-check per Law 8. Static projection only -- no live TPM or disk is required to validate it.
**Where:** `usr/share/mios/mios.toml` (`[security.luks]`), a new generator under `usr/libexec/mios`, `automation/38-drift-checks.sh`
**Done When:** the projection is idempotent and gated, the capability ships inert (`enabled=false`), and `just drift-gate` is green.
**Why:** MiOS pays the image cost of clevis today and gets no TPM-bound unlock, and there is no key an operator could set to turn it on.
**Dep:** none
**Who:** you (bash + drift-gate)

## AGY-111 -- Multi-vendor GPU CDI projection: ROCm and Intel alongside NVIDIA  (WS-RUNTIME | P2 | M)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- multi-vendor GPU CDI becomes SSOT-wired instead of an NVIDIA-only path.
**What+How:** The runtime doc records multi-vendor GPU support via CDI plus mdevctl vGPU, but `automation/41-gpu-cdi-toolkits.sh` is NVIDIA-centric. Add a `[gpu.vendors]` block with nvidia/amd/intel enable flags, project the CDI device specs per enabled vendor, and add a gate so AMD (ROCm) and Intel hosts get resolvable CDI devices. Encode the correction that **venus is not CUDA** -- do not conflate the Vulkan venus path with CUDA. Static projection; leave the NVIDIA path byte-unchanged.
**Where:** `automation/41-gpu-cdi-toolkits.sh`, `usr/share/mios/mios.toml` (`[gpu.vendors]`), `automation/38-drift-checks.sh`
**Done When:** CDI specs project for each enabled vendor, the new gate is green, the NVIDIA path is unchanged, and `just drift-gate` is green.
**Why:** an AMD or Intel GPU host currently gets no CDI devices at all, so every GPU container on that hardware fails to start.
**Dep:** AGY-41
**Who:** you (bash/Python)

## AGY-112 -- greenboot health-COVERAGE gate: every critical service has a check and a rollback path  (WS-RUNTIME | P1 | M)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- greenboot health coverage is gated, so a bad image actually rolls back.
**What+How:** greenboot is shipped and partially wired (check 54), but nothing asserts COVERAGE -- that every critical MiOS service (the AI lanes, pgvector, agent-pipe, hermes) has a greenboot health check whose failure triggers rollback. Add a drift-check that maps the critical-service list from SSOT onto the greenboot check scripts under `usr/lib/greenboot` and FAILS on any critical service with no covering check. Static analysis; no boot required.
**Where:** `automation/38-drift-checks.sh`, `usr/lib/greenboot`, `usr/share/mios/mios.toml` (critical-service list)
**Done When:** the gate flags an uncovered critical service, passes on HEAD, and `just drift-gate` is green.
**Why:** a service can be marked critical and still have no health check, so a broken upgrade boots green and never rolls back.
**Dep:** none
**Who:** you (bash + drift-gate)

## AGY-113 -- Extract the NEXT cohesive unit out of `server.py`  (WS-DEBT/TD-5 | P1 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement -- the agent-pipe god-module keeps shrinking into typed, sibling-tested modules.
**What+How:** Audit `server.py` for the LARGEST remaining cohesive block not yet extracted -- candidates are the SSE/streaming seam, the routing-decision seam, and the health/status endpoints -- and extract ONE into a sibling `mios_*.py` module with its own sibling test, satisfying check 6 (server.py-free) and check 11 (sibling test exists). Pick by cohesion and size, and document why that seam was chosen.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/**`, a new sibling module plus `test_mios_*.py`
**Done When:** the unit is extracted and server.py-free with a sibling test, the server.py public-surface golden (check 15) is updated, and `just drift-gate` is green.
**Why:** every remaining seam inside the ~9k-line server.py is untestable in isolation and any change there risks the whole request path.
**Dep:** AGY-47, AGY-52
**Who:** you (Python)

## AGY-114 -- Extract the MCP/tool-call dispatch seam  (WS-DEBT/TD-5 | P2 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement -- the tool-call path becomes an independently testable module.
**What+How:** Extract the MCP/tool-call dispatch seam out of `server.py` / `mios_dispatch` into a cohesive module with its own test, mirroring the pattern used by AGY-47/52/113. Preserve the existing fail-open behavior exactly, and introduce no `server.py` import edge (check 6).
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/**` (or a new sibling module), `test_mios_*.py`
**Done When:** the seam is extracted and tested, checks 6, 11 and 15 are green, and `just drift-gate` is green.
**Why:** tool-call dispatch is currently only reachable through a full server import, so its fail-open behavior is never directly tested.
**Dep:** AGY-113
**Who:** you (Python)

## AGY-115 -- Port `generate-names-registry.py` (the check-30 generator) to Rust  (WS-LANG | P2 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- another bash/Python tool moves to the compiled tier behind proven byte-parity.
**What+How:** Per Law 14 and ADR-0011, port `tools/generate-names-registry.py` -- the generator behind check-30 -- to a Rust binary in the `tools/native` workspace with BYTE-IDENTICAL output; the check-30 diff and the twin-equivalence gate are the parity harness. Keep the Python implementation as a fallback until the Rust binary is drift-verified, then demote it.
**Where:** `tools/native/**`, `tools/generate-names-registry.py`
**Done When:** the Rust binary emits byte-identical `names.generated.txt` and `referenced_names.txt`, check-30 is green via the Rust path, and `just drift-gate` is green.
**Why:** the names registry is regenerated on every SSOT change and stays a slow, untyped Python script on the gate's critical path.
**Dep:** none
**Who:** you (Rust)

## AGY-116 -- Single-source the SSOT walk into a Rust crate, killing the resolver-twin/userenv duplication  (WS-LANG | P2 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- the resolver core becomes one compiled crate that both gates consume.
**What+How:** Extract the `mios.toml` SSOT walk -- the `EXCLUDED_SECTIONS` and `WALK_*` logic DUPLICATED between `tools/check-resolver-twin.py` and `tools/lib/userenv.sh`, as flagged in AGY-94 -- into ONE Rust crate in `tools/native`, and have both the resolver-twin gate and the names registry consume it so the walk exists exactly once.
**Where:** `tools/native/**`, `tools/check-resolver-twin.py`, `tools/lib/userenv.sh`
**Done When:** one Rust walk crate exists, both twins single-source it, the resolver-twin gate (45) and names gate (30) are green, and `just drift-gate` is green.
**Why:** the walk is implemented twice in two languages, so any SSOT-shape change must be made twice and the twins drift apart silently between gate runs.
**Dep:** AGY-94, AGY-115
**Who:** you (Rust)

## AGY-117 -- Declare the `[mini]` split-plane block in SSOT and project the all-dGPU vfio-pci bind from it  (WS-MINI | P2 | M)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the MiOS-Metal router-host topology becomes SSOT-declared and drift-gated instead of living only in prose.
**What+How:** Per `docs/agy/doc-mios-metal.md`, add a `[mini]` block to mios.toml describing the router host (binds ALL dGPUs to `vfio-pci`; owns NICs, radios, TPM and boot) and the guest allocation (all GPUs + 75-90% CPU/RAM via `guest_cpu_percent`/`guest_ram_percent`). Write a generator that projects those keys into real `vfio-pci` bind material -- `vfio_pci.ids=` kargs under `usr/lib/bootc/kargs.d` and/or a `modprobe.d` bind -- and a regenerate-and-diff drift-check over that output. Encode the three critic corrections in the block/generator: whole-GPU-to-one-guest (no driver-free fractioning), ~1GB host floor (not a literal "tiny" host), no-firewalld nft. Static projection only; ships inert, no live hardware touched.
**Where:** `mios.toml [mini]`, a new generator under `usr/libexec/mios/`, `usr/lib/bootc/kargs.d` or `modprobe.d`, `automation/38-drift-checks.sh`
**Done When:** the generator writes real vfio bind config (not just env echoes) derived from every `[mini]` key; the new drift-check fails if the projection is stale or the generator is deleted; `just drift-gate` exit 0; the feature ships disabled by default.
**Why:** without it the split-plane design exists only in a doc, so nothing enforces the GPU-passthrough topology and any future host build silently diverges from the north star.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-118 -- Project the headscale mesh for the NIC-less split-plane guest from `[mini.mesh]`  (WS-MINI | P2 | M)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the guest's only network path (headscale mesh) is SSOT-declared and gated rather than hand-configured.
**What+How:** Add a `[mini.mesh]` SSOT block (headscale domain, `vnet_cidr`, `swtpm_vtpm`) and a generator that renders the headscale/tailscale config for the NIC-less guest routed to the router host, plus a drift-check registered in `main()`. Project EVERY declared key -- including `swtpm_vtpm` (the guest gets a software TPM) -- so no key is an inert literal, and have some real config surface consume the generator output rather than leaving it on stdout.
**Where:** `mios.toml [mini.mesh]`, a new generator under `usr/libexec/mios/`, the headscale config surface, `automation/38-drift-checks.sh`
**Done When:** `check_metal_mesh` exists, is registered in `main()`, and fails when the rendered mesh config drifts from `[mini.mesh]`; the generator output is consumed by a real file, not only stdout; gate green; inert by default.
**Why:** an unprojected, ungated mesh block means the guest's sole route to the host is unmanaged config -- Law 8 SSOT-PROJECTION is unmet and the split-plane cannot be reproduced.
**Dep:** AGY-117
**Who:** you (bash/Python).

## AGY-119 -- Author the missing `tools/install.sh`: offline bare-metal `bootc install to-disk` from an OCI archive  (WS-DEPLOY | P1 | M)  **[DONE]**
**Goal:** E-20 The bootc-native install legs -- close the broken bare-metal leg so blank hardware gets bootc MiOS, fully offline, instead of plain Fedora.
**What+How:** `tools/install.sh` is ABSENT and today's bare-metal path installs plain Fedora. Write it to perform an OFFLINE `bootc install to-disk` sourcing the image via `--transport oci`/`--source-imgref oci-archive:` from an archive staged on MiOS-Repo/MiOS-Data (Law 12 BAKE-NOT-FETCH, zero network), with target-disk enumeration, an explicit confirm prompt, and `--dry-run` that prints the resolved install plan without touching the disk. Cross-reference the kickstart path (CATREPO-01) so the two offline install routes name the same archive. Authorable without hardware: validate with `bash -n` plus the dry-run.
**Where:** new `tools/install.sh`, cross-ref the kickstart path (CATREPO-01)
**Done When:** `bash -n tools/install.sh` clean; `tools/install.sh --dry-run` prints the resolved bootc-install plan and exits 0; a drift-check asserts the script references the oci-archive path and contains NO network pull.
**Why:** until this exists the deploy plane cannot provision a blank machine with MiOS at all -- the shipped media installs the wrong operating system.
**Dep:** CATREPO-01 (kickstart path cross-ref)
**Who:** you (bash).

## AGY-120 -- Negative-tests that prove drift-checks 64/65/66 can actually FAIL  (WS-TEST | P2 | M)  **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- every gate must be shown capable of failing, not merely observed passing.
**What+How:** Checks 64 (curl-retry), 65 (nested-podman caps) and 66 (bake-budget) shipped with no proof of failure. Mirroring the AGY-54 pattern (inject -> run the REAL check -> restore), add one negative test per check that injects a violation into a fixture the real check actually scans -- a retry-less `curl` in a scanned Containerfile/automation script; a stripped `--cap-add` in the scanned CI yaml or `57-mios-sys-build.sh`; a sidecar count over `runner_disk_budget_gb` in `bound-images.tsv` -- invokes `bash automation/38-drift-checks.sh check_<name>`, and asserts non-zero, then asserts the same check passes on unmodified HEAD.
**Where:** the drift-gate negative-test harness (`tests/drift-gate-negatives.sh`), `automation/38-drift-checks.sh` fixtures
**Done When:** each of 64/65/66 has a negative test that runs the real check function, fails on the injected violation and passes on HEAD; the harness exits non-zero if any of the three checks are deleted; `just drift-gate` green.
**Why:** an untested gate is indistinguishable from a tautological one -- checks 64/65/66 could be silently broken and the tree would still report green.
**Dep:** AGY-54 (negative-test harness pattern)
**Who:** you (bash/Python).

## AGY-121 -- Run the build-URL liveness probe as a scheduled, non-blocking CI job  (WS-TEST | P2 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- rotted upstream download URLs surface on a schedule instead of at bake time.
**What+How:** AGY-102 added `tools/check-build-urls.sh` as a pre-release probe with no runner. Wire it into a cron-scheduled workflow (weekly plus `workflow_dispatch` on-demand) in `.github/workflows/`, mirrored byte-for-byte in `.forgejo/workflows/` per the equal-publishers topology, reporting every non-2xx build-download URL. Keep it strictly NON-blocking: it must never be a dependency of the build/publish gate.
**Where:** `.github/workflows/` (new scheduled workflow), `.forgejo/workflows/`
**Done When:** the scheduled job runs the probe and surfaces non-2xx URLs in its log/summary; the yaml validates on both forges; grep confirms no build/publish job depends on it.
**Why:** without a scheduled probe a rotted upstream URL is only discovered when a release bake fails, turning a known-stale link into a release-day outage.
**Dep:** AGY-102
**Who:** you (bash + CI yaml).

## AGY-122 -- Verify the ssh-config dotfile kind emits valid OpenSSH, then add the `[shell]` and `[editor]` surfaces  (WS-DOTFILES / ADR-0010 | P2 | M)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- extend the proven kind-engine to new surfaces without regressing syntax correctness on existing ones.
**What+How:** AGY-92 B3 flagged the ssh-config surface emitting bracketed `[host]` headers that OpenSSH rejects. First VERIFY the dedicated ssh-config kind landed -- real `Host mios-*` stanza syntax, no brackets and no `=` separators -- with fixtures regenerated. Then add the `[shell]` and `[editor]` surfaces through the kind engine (AGY-62 follow-through), each with a real existing template file, a real per-platform target, an explicit `kind`/`section`/`fixture`/`apply.target`, and negative-tests. Do not register a surface whose template does not exist: the renderer SKIPs missing templates and the gate goes vacuously green.
**Where:** `usr/libexec/mios/mios-theme-render` (or `mios-dotfiles-render`), `mios.toml [dotfiles.registry.*]`, fixtures
**Done When:** the ssh-config surface emits parseable OpenSSH syntax; the renderer reports the same surface count as it gates (no silent skips); `[shell]`/`[editor]` produce fixture output and fail check-25 when altered; check-25 green.
**Why:** dead registry entries make the dotfiles gate report coverage it does not have, and an invalid ssh-config projection breaks operator SSH on apply.
**Dep:** AGY-92 (B3 finding), AGY-62
**Who:** you (Python).

### AGY-1697 -- Redo the installer fold in the RIGHT repo and make `build-mios.sh` a real thin redirector  (WS-DEPLOY | P0 | L)  [BROKEN]
**Goal:** E-21 One deploy front door: flatten every install path -- one guided installer file over one shared contract, on the path the web one-liner actually fetches.
**What+How:** Verification found the work landed in the wrong tree: `build-mios.sh`, `mios-install.sh` and `mios-common.sh` canonically live in mios-bootstrap.git (bootstrap.sh:34 fetches `mios-bootstrap/main/build-mios.sh`), yet commit b832973f created `build-mios.sh` in mios.git. Land the change in mios-bootstrap.git per Law 15 DOUBLE-REPO-TRIPLE-CHECK; move the bootc-switch and FHS-root-merge logic (build-mios.sh:346-643) INTO `installation/mios-install.sh` so `resolve_fedora`/`resolve_bootc` (mios-install.sh:202-241) stop delegating outward via `CMD=(bash "$BUILD_MIOS_SH")`; reduce the retained 1240/1246-line monolith to an actual redirector; fix the redirector's hardcoded target at build-mios.sh:45,47 so a bootc host defaults to `bootc`, not `fedora`; and make the redirect target resolvable on the curl|bash path so both `[[ -f ]]` probes cannot fall through to the monolith.
**Where:** `mios-bootstrap.git:build-mios.sh`, `installation/mios-install.sh`, `installation/mios-common.sh`, `bootstrap.sh`
**Done When:** mios-bootstrap.git carries the fold commit; `build-mios.sh` is a redirector under ~100 lines with no inline bootc/FHS logic; a bootc host run selects target `bootc`; `curl | bash` of bootstrap.sh reaches `mios-install.sh` (proven by `--dry-run` output) instead of falling through.
**Verify:** mios-bootstrap.git carries the fold commit and `build-mios.sh` is a redirector under ~100 lines with no inline logic.
**Do NOT:** fold in one repo only. Law 15 -- both repos must be verified, and they have drifted before.
**Why:** as landed, the real web installer path is unchanged and a bootc host would be forced into fedora/FHS mode -- a shipped artifact installing the wrong thing, and Law 15's double-repo requirement is unmet.
**Dep:** none
**Status:** DONE (b832973f)

### AGY-1698 -- Actually delete the diverged monitor twin and gate against its return  (WS-DEDUP | P1 | M)  DONE
**Goal:** E-09 One value, one name: the full de-duplication campaign -- exactly one monitor executor exists and cannot be reintroduced.
**What+How:** `usr/libexec/mios/MiOS-Monitor.py` (33910 bytes) is still tracked in git HEAD and on disk beside the canonical `MiOS-Mon.py` (38249 bytes), and the two have DIVERGED (md5 2697bf00 vs b56d7de84; the twin is missing 5 later MiOS-Mon.py fixes). `git rm` the twin, add a drift-check to `automation/38-drift-checks.sh` that fails if a second monitor executor appears, fold the duplicated rich/textual/psutil bootstrap that `MiOS-Mon.py` and `mios-dashboard.sh` each carry into one shared launcher step, and delete the untracked scratch debris `C:\MiOS\replace.py` and `C:\MiOS\replace_indent.py`.
**Where:** `usr/libexec/mios/MiOS-Monitor.py`, `usr/libexec/mios/MiOS-Mon.py`, `usr/libexec/mios/mios-dashboard.sh`, `automation/38-drift-checks.sh`, repo root
**Done When:** `MiOS-Monitor.py` is absent from HEAD and disk; the new check fails when a twin file is restored; `replace.py`/`replace_indent.py` no longer exist; one dependency-bootstrap path serves both entry points; gate green.
**Verify:** `MiOS-Monitor.py` is absent from HEAD and disk, and restoring a twin file turns the new check RED.
**Do NOT:** delete the twin without the gate. Without it the twin comes back and diverges again.
**Why:** a stale second monitor that has drifted five fixes behind is worse than a byte-identical copy -- operators can run the old one and see wrong state, and nothing stops it coming back.
**Dep:** none
**Status:** DONE (93f6cafd)

### AGY-1699 -- Make the installer prereq resolver genuinely target-keyed, including rustup/cargo  (WS-INSTALL | P1 | M)  DONE
**Goal:** E-21 One deploy front door: flatten every install path -- one prereq resolver keyed off the chosen target, proven by a gate rather than by `bash -n`.
**What+How:** The shipped resolver hardcodes git/curl/podman and applies the identical set to the single `fedora|bootc|live|flash|build|update` bucket, so it is target-keyed only in an on/off sense; and the spec's `rustup`/`cargo` provisioning for native tools appears nowhere (grep: 0 matches). Split the package set per target, add the rustup/cargo branch for targets that build native tools, and add a check that asserts the resolver runs before each target and that `--dry-run` still prints resolved env/argv and exits 0 (that Done-When was already satisfied by pre-existing commit bc298b7a, not by this work).
**Where:** `installation/mios-install.sh` (mios-bootstrap.git), `installation/mios-common.sh`, a drift-check or CI step covering the resolver
**Done When:** `--dry-run` for two different targets prints two different resolved prereq sets; a build-target dry-run names rustup/cargo; a check fails when the resolver is bypassed; the deliverable is reachable from `C:\MiOS` without depending on an incidentally-present sibling checkout.
**Verify:** `--dry-run` for two different targets prints two different resolved prereq sets, asserted by a test.
**Do NOT:** hardcode one prereq list. A resolver that returns the same answer for every target is not keyed on anything.
**Why:** a one-size prereq list installs podman on flash-only hosts and omits the Rust toolchain on build hosts, and with no gate the resolver can silently regress.
**Dep:** AGY-106
**Status:** DONE (f6454c5 in the mios-bootstrap repo at C:\mios-bootstrap; NO)

### AGY-1700 -- Fix the configurator port resolution and revert the unrequested `.ps1` special-target regression  (WS-CONFIG | P1 | S)  [BROKEN]
**Goal:** E-11 Unified config surface: mios.toml, the configurator and the Portal are one door at :8640/ -- the installer opens the SSOT-resolved door on both platforms.
**What+How:** `mios-install.sh:377` calls `mios_ssot_value 'ports.agent_pipe' '8640'` (2 args) instead of the resolver's `mios_ssot_value SECTION KEY DEFAULT` convention, so it returns empty and the URL becomes `http://localhost:/configure`; the literal `8640` is passed as the KEY argument, so it also never acts as a fallback. Correct the call to `mios_ssot_value ports agent_pipe "$default"` with the default sourced from SSOT, not inlined. On the PowerShell side, restore the `if ($entry['special'] -eq 'configure')` guard and its closing brace removed at mios-install.ps1:217-220 (which made every special target -- monitor/update/repos -- open the configurator and exit 0, leaving their handlers at :221/:228 dead), and fix the call to the non-existent `Get-MiosTomlValue` to the real `Get-MiosSsotValue` (mios-common.ps1:30). Add the missing "else offline configurator HTML" fallback in the Linux path, where `resolve_config` currently only warns when no browser launcher exists.
**Where:** `installation/mios-install.sh:377`, `installation/mios-install.ps1:217-220`, `installation/mios-common.ps1:30`
**Done When:** the Linux `config` route prints a URL with a resolved port and no literal `8640` remains in that path; `monitor`, `update` and `repos` reach their own handlers on Windows; `Get-MiosSsotValue` resolves at runtime; no-browser hosts get the offline configurator HTML.
**Verify:** the Linux `config` route prints a URL with a resolved port and `grep -rn ':8640' <that path>` finds no literal.
**Do NOT:** leave the unrequested `.ps1` special-target regression in place while fixing the port.
**Why:** today the installer's config route opens a portless URL on Linux and hijacks three working targets on Windows with a command-not-found call -- the single config door is unreachable from the installer on both platforms.
**Dep:** AGY-106
**Status:** DONE (85aeec5)

### AGY-1701 -- Turn `check_clevis_luks` into a real regenerate-and-diff gate  (WS-RUNTIME | P1 | M)  DONE
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- clevis/LUKS TPM binding is SSOT-projected and provably gated, per Law 8.
**What+How:** `check_clevis_luks` (38-drift-checks.sh:3977) only greps generator stdout for the substring `CLEVIS_LUKS_ENABLED=`, which `mios-clevis-luks-gen:13` emits unconditionally from a heredoc -- proven: the generator run against a nonexistent TOML still emits `CLEVIS_LUKS_ENABLED="false"` and passes. Replace the substring grep with a regenerate-and-diff against a committed materialized artifact, assert idempotency by diffing a second run, and make the check FAIL (not skip) when `usr/libexec/mios/mios-clevis-luks-gen` is absent -- the `[[ -f ]]` branch currently returns success. Also revert the out-of-scope collateral in the same commit: `usr/share/mios/referenced_names.txt` gained `MIOS_OLLAMA_BAKE_MODELS` and `MIOS_PROMPT_TIMEOUT`, and `MIOS_OLLAMA_*` re-introduces a token purged with the ollama lane.
**Where:** `automation/38-drift-checks.sh:3975-3985`, `usr/libexec/mios/mios-clevis-luks-gen`, `usr/share/mios/referenced_names.txt`
**Done When:** deleting the generator turns the gate RED; a broken SSOT read turns the gate RED; a committed golden artifact exists and a second generator run diffs clean; no `MIOS_OLLAMA_*` token remains in `referenced_names.txt`.
**Verify:** deleting the generator turns `check_clevis_luks` RED, and a broken SSOT read turns it RED -- not skipped.
**Do NOT:** let a missing generator read as a pass. That is the tautological-gate shape.
**Why:** the gate is currently green by construction, so the entire clevis/LUKS SSOT read can be broken (or the projector deleted) with zero signal -- and a purged legacy token has crept back into the names registry.
**Dep:** none
**Status:** DONE (9fb7de80)

### AGY-1702 -- Make `[gpu.vendors]` drive the CDI toolkit install and gate it  (WS-RUNTIME | P1 | M)  DONE
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- multi-vendor GPU CDI (ROCm/Intel alongside NVIDIA) is SSOT-driven, not unconditional.
**What+How:** The new `[gpu.vendors]` flags (mios.toml:8643) are dead config consumed by nothing: `automation/41-gpu-cdi-toolkits.sh` was never modified and installs the AMD/Intel CDI toolkits unconditionally. Gate that script on the resolved enable cascade, project a per-vendor CDI spec statically, add a drift-check to `automation/38-drift-checks.sh` asserting that an AMD(ROCm)/Intel host resolves a CDI device for each enabled vendor, encode the venus != CUDA correction in the code/doc this task owns rather than relying on a prior task's docs, and move the `[gpu.vendors]` block out from mid-comment above `[security.luks]` where it sits under an unrelated Quadlet/network comment.
**Where:** `usr/share/mios/mios.toml [gpu.vendors]`, `automation/41-gpu-cdi-toolkits.sh`, `automation/38-drift-checks.sh`
**Done When:** disabling a vendor in `[gpu.vendors]` demonstrably skips its toolkit install; the new check greps non-empty for the per-vendor CDI projection and fails when a vendor is enabled with no spec; the block sits under its own heading; gate green.
**Verify:** disabling a vendor in `[gpu.vendors]` demonstrably skips its toolkit install, and the check greps non-empty for the enabled one.
**Do NOT:** assert on the config. Assert on what the build actually installed.
**Why:** every image currently carries AMD and Intel CDI toolkits whether or not the operator wants them, inflating the bake that the publish runner must hold, and no gate notices when vendor selection stops working.
**Dep:** none
**Status:** DONE (79aa2032)

### AGY-1703 -- Repair the greenboot coverage check: undefined `_fail`, shadowed function, hardcoded service list  (WS-RUNTIME | P0 | M)  [BROKEN]
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- greenboot health COVERAGE is gated so every critical service has a check and a rollback path.
**What+How:** Four defects, all in `automation/38-drift-checks.sh`: (1) `_fail` at :3950 and :3964 is undefined -- only `_violation` (:85) increments `VIOLATIONS` -- so an uncovered service can never register drift; under `set -euo pipefail` (:55) `_fail` returns 127 and crashes the ENTIRE drift-gate (proven EXIT=127). Replace both with `_violation`. (2) The new `check_greenboot()` at :3946 shadows the pre-existing one at :3085 (bash last-def-wins), silently killing check-54's service-enablement and executable-script assertions -- rename the new check or merge the two. (3) The `critical_services` list at :3953 is hardcoded, violating Law 7 NO-HARDCODE and the spec's Where -- read it from a new mios.toml list. (4) The spec-named critical service `hermes` (:8642) is omitted and has no greenboot check script -- add both.
**Where:** `automation/38-drift-checks.sh:3085,3946-3964`, `usr/share/mios/mios.toml` (new critical-services list), the greenboot check-script directory
**Done When:** removing a greenboot check script for a listed service turns the gate RED (not exit 127); check-54's original assertions still run; `critical_services` is read from mios.toml; `hermes` appears in the list and has a health script; `just drift-gate` exit 0.
**Verify:** removing a greenboot check script for a listed service turns `check_greenboot` RED -- and not with exit 127.
**Do NOT:** leave the undefined `_fail` and the shadowed function. Exit 127 is a crash reported as a failure, which hides the real one.
**Why:** this is a live P0 -- the drift-gate itself crashes with exit 127 on exactly the condition it exists to detect, and it silently deleted a previously-working check while doing so.
**Dep:** none
**Status:** DONE (3f30ac56)

### AGY-1704 -- Actually extract a health seam OUT of server.py instead of adding a parallel stub  (WS-DEBT-PY | P1 | L)  [BROKEN]
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the agent-pipe god-module shrinks under typed schemas and real tests (TD-5).
**What+How:** Commit 40ad9675 does not touch `server.py` at all -- it remains 8967 lines -- and `mios_pipe/health.py::build_health_response` is orphaned dead code imported by nothing but its own test, returning hardcoded literals (version `0.3.0`, backend `localhost:8642`, port `8640`) that correspond to no real code path and shadow the real `app.version`/`BACKEND`/`PORT`. The health seam was ALSO already extracted in a prior R13 wave to `mios_pipe/kernel/clusterhealth.py:547-680` (`health_logic`) behind the `mios_clusterhealth` shim. Delete or fold `health.py` into the existing extraction, then pick the LARGEST block in `server.py` that is genuinely NOT yet extracted, move it (deleting the original body, importing the new module), and replace the tautological `test_mios_health.py:8-11` (passes literals in, asserts them back) with a test exercising real extracted behaviour.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/server.py`, `usr/lib/mios/agent-pipe/mios_pipe/health.py`, `usr/lib/mios/agent-pipe/mios_pipe/kernel/clusterhealth.py`, `usr/lib/mios/agent-pipe/test_mios_health.py`
**Done When:** `wc -l server.py` drops by the size of the extracted block; the new module is imported by `server.py` (grep proves a consumer); `health.py` is gone or wired; the replacement test fails if the extracted logic is broken; drift checks 6/11 green.
**Verify:** `wc -l usr/lib/mios/agent-pipe/server.py` drops by the size of the extracted block AND `grep` proves server.py imports the new module.
**Do NOT:** add a parallel stub. Extraction means server.py got smaller and calls the new seam.
**Why:** the monolith is unchanged while the register records progress, and a duplicate health builder returning fabricated version/port literals is a live wrong-answer hazard if anyone wires it.
**Dep:** none
**Status:** DONE (40ad9675)

### AGY-1705 -- Extract the REAL MCP/tool-call dispatch seam, not a fabricated envelope helper  (WS-DEBT-PIPE | P1 | L)  [BROKEN]
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the dispatch monolith is decomposed at genuine seams with tests that bind to real behaviour.
**What+How:** Commit 40ad9675 left `server.py` unmodified, so nothing was extracted. `build_mcp_tool_envelope` (`mcp_dispatch.py:7`) and its `{"type":"function",...}` shape exist nowhere in `server.py` -- it is an invented 12-line helper with no consumer (git grep = the module plus its own test). Meanwhile the actual MCP/tool-call dispatch already lives in `mios_pipe/federation/mcp.py` (`mcp_dispatch_logic`/`_mcp_call_tool`, commit 5a541db1) and at the verb chokepoint `mios_dispatch.py`, neither of which was touched. Delete the fabricated helper, then either consolidate the real dispatch path onto `federation/mcp.py` (removing the duplicated body from its current caller) or extract the next un-extracted dispatch block, and retarget `test_mios_mcp_dispatch.py` at that real seam.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/mcp_dispatch.py`, `usr/lib/mios/agent-pipe/mios_pipe/federation/mcp.py`, `usr/lib/mios/agent-pipe/mios_dispatch.py`, `usr/lib/mios/agent-pipe/test_mios_mcp_dispatch.py`
**Done When:** `mcp_dispatch.py`'s fabricated helper is gone; the dispatch body exists in exactly one module with a real importer; the test fails when that module's behaviour is altered; checks 6/11 green.
**Verify:** the dispatch body exists in exactly one module, proven by grep, and the fabricated helper is gone.
**Do NOT:** leave the envelope helper. Two dispatch paths diverge and the unused one rots.
**Why:** TD-5 debt is unreduced while a dead module and a self-referential test create the appearance of coverage over the highest-traffic path in the agent plane.
**Dep:** AGY-113
**Status:** DONE (40ad9675)

### AGY-1706 -- Make the Rust names-registry generator emit byte-identical output and put it on check-30's path  (WS-LANG | P2 | L)  [BROKEN]
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- ports are proven byte-parity before the shell/Python original is trusted less.
**What+How:** `main.rs` is a `println!` stub and the crate's `[dependencies]` is empty (no `toml`/`serde`), so it cannot parse mios.toml or replicate the Python walk/alias/referenced-vars logic -- byte-identical output is not merely unmet but impossible. Add the toml/serde dependencies, port the walk, alias and referenced-vars logic from `tools/generate-names-registry.py`, and prove parity with a golden/characterization harness diffing the Rust output against the Python output for `names.generated.txt` and `referenced_names.txt`. Then either switch `automation/38-drift-checks.sh:1918` (which today runs `sys.executable + tools/generate-names-registry.py`) to the Rust binary, or state explicitly that the crate is not yet on the path -- claiming "check-30 green via the Rust path" while the binary is never invoked is the failure being corrected.
**Where:** `tools/native/generate-names-registry/src/main.rs`, `tools/native/generate-names-registry/Cargo.toml`, `tools/generate-names-registry.py`, `automation/38-drift-checks.sh:1918`
**Done When:** `diff <(rust-gen) <(python3 tools/generate-names-registry.py)` is empty for both outputs; check-30 invokes the Rust binary (or the task states it does not); the crate fails to build if the parity harness diverges.
**Verify:** `diff <(rust-gen) <(python3 tools/generate-names-registry.py)` is empty for both outputs, and the check invokes the Rust path.
**Do NOT:** declare parity from a spot check. Diff the whole output.
**Why:** a workspace member that compiles trivially and emits nothing was marked done -- the exact "byte-identical Rust port that isn't" failure, which erodes trust in every subsequent port claim.
**Dep:** none
**Status:** DONE (52bb9fdb)

### AGY-1707 -- Make the twins actually consume `mios-ssot-walk` instead of duplicating the walk verbatim  (WS-LANG | P2 | L)  [BROKEN]
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one materializer owns the SSOT walk so the resolver twins stop diverging.
**What+How:** The duplication the task exists to kill is fully intact: the `EXCLUDED_SECTIONS`/`WALK_MOSTLY_DEAD`/`WALK_EMIT_KEEP` logic is still copied verbatim across `check-resolver-twin.py:117-139` and both `userenv.sh:549-573`. The crate is a dead stub with WRONG data -- `lib.rs` declares `EXCLUDED_SECTIONS = ["meta","laws"]`, not the twins' real exclusion set, and captures no `WALK_MOSTLY_DEAD`/`WALK_EMIT_KEEP` and no `walk()` traversal; its only repo references are the workspace-member list in `Cargo.toml` and `generate-cargo-manifests.py`. Port the REAL walk into the crate, have it emit the exclusion/keep sets as a generated artifact, and make both `check-resolver-twin.py` and both `userenv.sh` read that single artifact rather than carrying their own copies.
**Where:** `tools/native/mios-ssot-walk/src/lib.rs`, `tools/check-resolver-twin.py:117-139`, both `userenv.sh:549-573`, `Cargo.toml` (workspace members)
**Done When:** grep finds the exclusion/keep lists in exactly ONE place; editing that place changes the output of resolver-twin (check 45) and names (check 30); the crate has a real consumer beyond the workspace manifest.
**Verify:** `grep -rn` finds the exclusion/keep lists in exactly ONE place, and editing that place changes the twins' output.
**Do NOT:** duplicate the walk verbatim again. The duplicate is why the lists drifted.
**Why:** three verbatim copies of the walk rules remain, so any future exclusion change must be made three times or the twins silently disagree -- and the "gates green" claim proves nothing because the crate is bypassed entirely.
**Dep:** AGY-115
**Status:** DONE (52bb9fdb)

### AGY-1708 -- Make the `[mini]` vfio generator write real bind config and the gate non-tautological  (WS-MINI | P0 | M)  DONE
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the split-plane GPU binding is a real projection guarded by a gate that can fail.
**What+How:** `usr/libexec/mios/mios-metal-vfio-gen` only echoes two boolean env vars to stdout and writes nothing to `usr/lib/bootc/kargs.d` or `modprobe.d` -- no `vfio_pci.ids=`, no modprobe bind, no driverctl. Make it emit the real bind artifact, and project `guest_cpu_percent`/`guest_ram_percent` (2 of the 4 `[mini]` keys are currently ignored). Encode the three mandated critic corrections that are absent from both the block and the generator: whole-GPU-to-one-guest (no driver-free fractioning), ~1GB host floor rather than a literal "tiny", no-firewalld nft. Replace `check_metal_vfio` (38-drift-checks.sh:3993), which asserts only the always-emitted literal `MIOS_METAL_ENABLED=`, with a regenerate-and-diff against the materialized artifact, and replace the `_fail` call at :3996 -- an UNDEFINED function in this script (it is defined only in `usr/libexec/mios/mios-build-driver`) that aborts the gate with exit 127 under `set -euo pipefail` -- with `_violation`.
**Where:** `usr/libexec/mios/mios-metal-vfio-gen`, `usr/share/mios/mios.toml [mini]`, `usr/lib/bootc/kargs.d` or `modprobe.d`, `automation/38-drift-checks.sh:3993-3996`
**Done When:** the generator writes a vfio bind file containing device ids derived from `[mini]`; hand-editing that file turns the gate RED (not exit 127); all four `[mini]` keys are projected; the three corrections appear in the block or generator.
**Verify:** the generator writes a vfio bind file whose device ids derive from `[mini]`, and hand-editing that file turns the gate RED.
**Do NOT:** assert the file exists. Non-tautological means a hand-edit is caught.
**Why:** P0 because the gate calls an undefined function -- it crashes the whole drift-gate on its own failure path -- and until then it is green tautologically over a generator that projects nothing.
**Dep:** none
**Status:** DONE (f31ac12f)

### AGY-1709 -- Add the missing mesh drift-check and give `mios-metal-mesh-gen` a consumer  (WS-MINI | P1 | M)  DONE
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the mesh projection satisfies Law 8 (emitted by a generator AND guarded by a regenerate-and-diff gate).
**What+How:** No mesh drift-check exists: `automation/38-drift-checks.sh` -- named explicitly in the spec's Where -- has no `check_metal_mesh`, none is registered in `main()`, and grep for mesh/headscale/swtpm/metal_mesh returns nothing (contrast the sibling `check_metal_vfio`, check 68). Add the check. `mios-metal-mesh-gen` is also an orphan: no build stage or check invokes it and no file consumes `MIOS_METAL_MESH_ENABLED`/`MIOS_HEADSCALE_DOMAIN`, so it projects to stdout nobody reads -- wire its output into a real config surface. Extend it to project `vnet_cidr` (mios.toml:8640) and `swtpm_vtpm` (:8641), which are declared but never emitted, leaving the vTPM note an inert literal. Commit as `agy: AGY-118 <summary>` rather than folding into a batch mark-all-done commit.
**Where:** `usr/libexec/mios/mios-metal-mesh-gen`, `usr/share/mios/mios.toml:8638-8641 [mini.mesh]`, the headscale config surface, `automation/38-drift-checks.sh`
**Done When:** `check_metal_mesh` is registered in `main()` and fails on a hand-edited mesh config; a real file consumes the generator output; all four `[mini.mesh]` keys appear in the rendered surface; a dedicated `agy: AGY-118` commit exists.
**Verify:** `check_metal_mesh` is registered in `main()` -- proven by check_gate_registry -- and fails on a hand-edited mesh config.
**Do NOT:** define the check without registering it. That is exactly the defect check_gate_registry exists to catch.
**Why:** an ungated generator with no consumer is a Law 8 violation that reads as done -- the vTPM and CIDR decisions never reach any surface that runs.
**Dep:** AGY-117
**Status:** DONE (52bb9fdb)

### AGY-1710 -- Gate `tools/install.sh` on the oci-archive path and fix the `to-disk` invocation  (WS-DEPLOY | P1 | S)  DONE
**Goal:** E-20 The bootc-native install legs -- the offline bare-metal leg is provably network-free and syntactically able to run.
**What+How:** The Done-When "a drift-check asserts it references the oci-archive path (NOT a network pull)" is entirely unimplemented -- no check exists in `automation/38-drift-checks.sh` (grep for install.sh/oci-archive/to-disk/bare-metal is empty) and `tools/install.sh` is referenced by no check. Add that check. Do the skipped Where clause: cross-link the kickstart path (CATREPO-01) so both offline routes name the same archive. Fix `tools/install.sh:71`, which invokes `bootc install to-disk --transport oci-archive "$TARGET_DISK" "$OCI_ARCHIVE"` passing BOTH disk and archive as positionals -- real `bootc install to-disk` takes a single DEVICE positional with the source given via `--source-imgref oci-archive:/path`. Revert the scope leak in the same commit: the `referenced_names.txt` hunk added `MIOS_METAL_ENABLED`, which belongs to the MiOS-Metal split-plane, not the installer.
**Where:** `tools/install.sh:71`, `automation/38-drift-checks.sh`, the kickstart path (CATREPO-01), `usr/share/mios/referenced_names.txt`
**Done When:** the new check fails when `tools/install.sh` gains a network pull or loses the oci-archive reference; line 71 uses one DEVICE positional plus `--source-imgref`; the kickstart cross-ref exists; `MIOS_METAL_ENABLED` is out of the installer commit's names hunk.
**Verify:** the new check fails when `tools/install.sh` gains a network pull or loses its oci-archive reference.
**Do NOT:** test the happy path. The offline path is the one the deploy plane exists for.
**Why:** the one script standing between MiOS and blank hardware is ungated and, as written, would likely fail its single live invocation -- a failure only discoverable on real metal.
**Dep:** AGY-119 (the authoring task), CATREPO-01
**Status:** DONE (3cfd1b62)

### AGY-1711 -- Rewrite the 64/65/66 negative-tests to inject into real fixtures and call the real checks  (WS-TEST | P1 | M)  [BROKEN]
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- a negative test must exercise the shipped gate, not a reimplementation of it.
**What+How:** `automation/tests/test-drift-gates.sh` has 0 references to `38-drift-checks.sh` and never calls `check_curl_retry`/`check_nested_podman_caps`/`check_bake_budget`; it asserts on inline literals and exits 0 unconditionally, so it would pass identically if all three checks were deleted. Move the work into the existing AGY-54 harness (`tests/drift-gate-negatives.sh`) and follow its inject -> run-real-check -> restore pattern, injecting into the files each check actually scans: gate 64 globs `Containerfile*`/`automation/**/*.sh`/`usr/libexec`; gate 65 scans `.github/workflows/mios-ci.yml`, `usr/libexec/mios/57-mios-sys-build.sh` and the nested-podman-caps reference doc; gate 66 counts `usr/share/mios/artifacts/sbom/bound-images.tsv` rows over 30 against `runner_disk_budget_gb`. Note the gate-66 test currently mirrors the wrong logic entirely (hardcoded `projected_gb=45 > budget_gb=40` instead of the SBOM row count). Delete the isolated new file.
**Where:** `tests/drift-gate-negatives.sh`, `automation/tests/test-drift-gates.sh` (remove), `automation/38-drift-checks.sh`, `usr/share/mios/artifacts/sbom/bound-images.tsv`
**Done When:** each test runs `bash automation/38-drift-checks.sh check_<name>` against an injected fixture and asserts non-zero, then asserts zero on restored HEAD; deleting any of checks 64/65/66 makes the harness FAIL; `automation/tests/test-drift-gates.sh` no longer exists.
**Verify:** each rewritten test runs the REAL check against an injected fixture and asserts non-zero, not a simulated one.
**Do NOT:** assert against a stubbed check. A negative test that never invokes the real gate proves nothing about it.
**Why:** the current harness proves nothing about the three safety nets while occupying the slot where real proof would live -- the definition of a false green.
**Dep:** AGY-54
**Status:** DONE (e6c1e07d)

### AGY-1712 -- Give the `[shell]`/`[editor]` surfaces real templates and targets, or remove them  (WS-DOTFILES | P2 | M)  [BROKEN]
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- every registered surface renders real bytes and is really gated.
**What+How:** The `[shell]`/`[editor]` entries reference template files that do not exist (`usr/share/mios/branding/templates/oh-my-posh.json.tmpl`, `vscode-colors.json.tmpl`) in a `branding/templates/` directory that does not exist, and point at non-existent targets. Because the templates are missing, `mios-dotfiles-render:1000-1001` silently SKIPs both: the engine loads 16 surfaces but only 14 are gated, so the new entries project nothing and gate nothing. Either author the templates and real per-platform targets with an explicit `kind`/`section`/`fixture`/`apply.target` -- exercising the kind engine the spec calls for rather than defaulting to `kind=template` and duplicating the existing `oh-my-posh` and `vscode-colors` surfaces -- or drop the two dead registry entries. Add the missing negative-tests for the new surfaces and for the ssh-merge kind (grep finds none). Make the renderer FAIL rather than skip on a missing template so a vacuous green is impossible.
**Where:** `usr/share/mios/mios.toml [dotfiles.registry.shell]`/`[dotfiles.registry.editor]`, `usr/libexec/mios/mios-dotfiles-render:1000-1001`, `usr/share/mios/branding/templates/`, the dotfiles fixtures
**Done When:** the renderer's loaded-surface count equals its gated count; a missing template makes check-25 RED instead of skipping; each surviving surface has a fixture that changes when its template changes; negative-tests exist for the new surfaces and the ssh-merge kind.
**Verify:** the renderer's loaded-surface count equals its gated count, and a missing template makes the gate RED rather than skipping.
**Do NOT:** let a missing template skip. A skip reports the same green as a pass.
**Why:** two dead registry entries inflate apparent dotfiles coverage by 2 of 16 surfaces, and the skip-on-missing-template behaviour means any future surface can be added wrong and still report green.
**Dep:** AGY-122 (the authoring task), AGY-62
**Status:** DONE (52bb9fdb)

## AGY-123..259 -- Batch-3 register: 137 grounded, code-only tasks across 14 MiOS domains  (WS-BATCH3 | P1 | L)  **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- a written, self-contained work register an arriving agent can pick up cold.
**What+How:** Generated by a 14-domain Claude workflow, every task grounded in a real file or measured gap and deduped against AGY-1..122. All are code-only with no live-VM requirement, and each is self-contained under the Who / What+How / Where / Done-When contract. The batch carries two standing execution rules: the batch-2 failures (AGY-106..122) are fixed FIRST, and each task stages ONLY its own files with `just drift-gate` at exit 0 before it may be marked done.
**Where:** `AGY-TASKS.md` (the AGY-123..259 section)
**Done When:** all 137 entries carry the four required fields, none duplicates an AGY-1..122 task, and none requires hardware to complete.
**Why:** without a grounded, deduped register the agent lane re-solves solved problems and marks work done that no gate ever exercised -- exactly the batch-2 failure mode.
**Dep:** AGY-106..122 remediation
**Domain:** 14 MiOS domains (batch 3, 2026-07-24)

## AGY-1713 -- Pure pre-execution validator for the runtime agent DAG  (WS-ORCH | P1 | M)  **[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- a malformed plan is rejected before it can burn a fan-out.
**What+How:** `routing/dag_exec.py` (1585 lines) handles a malformed plan only REACTIVELY at runtime -- around line 918, "nothing ready + nothing running -> cycle/dangling dep: force one" -- so a cycle, self-loop, dangling dependency or duplicate node id consumes an entire fan-out before the nondeterministic "force one" recovery fires. Add a pure sibling `dag_validate.py` running Kahn topological classification over the plan node list BEFORE scheduling, returning a typed verdict (acyclic | cycle-nodes | dangling-deps | duplicate-ids | orphan-roots). `dag_exec` calls it and, on an invalid verdict, degrades to a deterministic linearization or single-agent path instead of live improvisation. Pure stdlib; must never import server.
**Where:** new `usr/lib/mios/agent-pipe/mios_pipe/routing/dag_validate.py`; `usr/lib/mios/agent-pipe/test_mios_dag_validate.py`; call-site edit in `usr/lib/mios/agent-pipe/mios_pipe/routing/dag_exec.py`
**Done When:** `dag_validate` is invoked pre-schedule and returns a typed verdict; `test_mios_dag_validate.py` green with negative cases for cycle, self-loop, dangling dep and duplicate id; drift checks 6 (server-free import) and 11 (sibling test) pass; `just drift-gate` green.
**Verify:** `dag_validate` is invoked pre-schedule, returns a typed verdict, and `test_mios_dag_validate.py` is green including negative cases.
**Do NOT:** validate after scheduling. Pre-execution is the point.
**Why:** today a single bad plan node costs a full fan-out of model calls before a nondeterministic recovery guesses its way out -- unbounded waste on the operator's own hardware.
**Dep:** AGY-106..122 remediation (batch-2 hard rule)
**Who:** you (Python, agent-pipe).

## AGY-124 -- Deterministic quality-gate producer for smartroute escalation  (WS-ORCH | P1 | M)  **[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- the cost/quality cascade escalates on quality, not only on exhaustion.
**What+How:** `routing/smartroute.py should_escalate(quality_ok, local_exhausted)` exists, but a grep across `mios_pipe/server.py` and `mios_dispatch` confirms NO producer of `quality_ok` anywhere -- so escalation only ever fires on `local_exhausted` and the whole quality dimension is dead code. Add a pure `quality_gate.py` scoring a local lane's output over deterministic signals (empty/whitespace, refusal/punt by reusing `native_loop._is_punt`, unterminated or malformed JSON via `mios_jsonsalvage`, a min-length/entity floor) returning `(ok, reason)`. Thresholds resolve from SSOT `[agent_pipe.quality]`; degrade-open on an unknown signal (`ok=True`) so it can never force a false paid escalation. Feed its bool as the `quality_ok` argument at the smartroute escalation seam.
**Where:** new `usr/lib/mios/agent-pipe/mios_pipe/routing/quality_gate.py`; `usr/lib/mios/agent-pipe/test_mios_quality_gate.py`; wire at `usr/lib/mios/agent-pipe/mios_pipe/routing/lanes_resolver.py` (or the server escalation call); `usr/share/mios/mios.toml [agent_pipe.quality]`
**Done When:** a failing-quality local output makes `should_escalate()` return True and a good output keeps it False in `test_mios_quality_gate.py` (green); thresholds read from mios.toml; checks 6/11 green; `just drift-gate` green.
**Why:** half the routing cascade is inert -- a local lane that returns a refusal or malformed JSON is accepted as final output, and the escalation parameter it was designed around is permanently False.
**Dep:** AGY-123
**Who:** you (Python, agent-pipe).

## AGY-125 -- Extend the check-11 coverage gate to mios_pipe submodules and cover identity/principal.py  (WS-TEST | P1 | M)**[DONE]**
**Goal:** E-06 Test and documentation harness -- every shipped agent-pipe module is held to an enforced sibling unit test, not just the top-level ones.
**What+How:** `check_module_test_coverage` in `automation/38-drift-checks.sh` globs `find "$dir" -maxdepth 1 -type f -name 'mios_*.py'`, so it enforces sibling tests ONLY for top-level `agent-pipe/mios_*.py` and never sees any `mios_pipe/**/*.py` submodule. Walk the package recursively and require a sibling `test_mios_<name>.py` for every submodule (matched by its flat import name), then author the single missing test for `identity/principal.py` (282 ln, the sole uncovered module the scan found).
**Where:** `automation/38-drift-checks.sh` (check_module_test_coverage); new `usr/lib/mios/agent-pipe/test_mios_principal.py`
**Done When:** check 11 requires a sibling test for every `mios_pipe/**/*.py`; `test_mios_principal.py` is authored and green; temporarily hiding an existing submodule's sibling test makes check 11 FAIL and restoring it passes; `just drift-gate` green.
**Why:** Today the coverage gate reports green while an entire package tree is unenforced -- new submodules land with zero tests and nothing notices.
**Dep:** none
**Who:** you (bash + Python).

## AGY-126 -- Extract the A2A AgentCard JWS/JCS signing seam out of a2a.py (TD-5)  (WS-DEBT | P1 | M)**[DONE]**
**Goal:** E-02 Technical-debt retirement -- the agent-pipe god-modules decompose into testable units behind unchanged import surfaces.
**What+How:** `federation/a2a.py` is 1799 ln / 34 defs, the largest federation module and an explicit AGY-52 decomposition target. Its JCS-canonicalize + EdDSA cluster (`_jcs_canonicalize`, `_b64u`/`_b64u_decode`, `_agent_card_signing_input`, `_agent_card_signature`, `_verify_agent_card_signature`, `_canonical_json`) is a cohesive, near-pure crypto seam. Move it verbatim into a sibling module with a one-way import (it must never import server), leave a thin re-export shim in `a2a.py` under the original aliases so the importable surface stays byte-identical, and add a unit test.
**Where:** new `usr/lib/mios/agent-pipe/mios_pipe/federation/agentcard_sign.py`; `usr/lib/mios/agent-pipe/test_mios_agentcard_sign.py`; re-export shim in `usr/lib/mios/agent-pipe/mios_pipe/federation/a2a.py`
**Done When:** the signing cluster lives in the new module and `a2a.py` re-imports the moved names under their original aliases (surface byte-identical); `test_mios_agentcard_sign.py` signs a card, verifies round-trip, and asserts a TAMPERED card fails verification; checks 6/11/15 green; `just drift-gate` green.
**Why:** Federation signing -- the crypto that decides whether a peer AgentCard is trusted -- is buried in a 1799-line module with no isolated test, so a regression in canonicalization is invisible until federation silently accepts a forged card.
**Dep:** none
**Who:** you (Python, agent-pipe).

## AGY-127 [x] -- Router Stage-2 parity gate: prove router.route() matches server.py's intent cascade without a VM  (WS-ORCH | P2 | M)**[DONE]**
**Goal:** E-02 Technical-debt retirement -- server.py's inline decision cascade migrates into typed, tested modules with proven equivalence.
**What+How:** `routing/router.py` is documented "additive + unwired in Stage 1; server.py keeps the branch BODIES and Stage 2 (VM-verified) will delegate the classification here." VM verification is the only blocker, but `Router.route()` is pure and its equivalence to server.py's inline `refined.get('intent')` cascade is provable statically. Build a golden corpus of refined-plan dicts covering every branch of that cascade, a parity test asserting `route(plan).mode` equals the mode the cascade selects for every row, and a drift-check that reds if server.py grows an `intent ==` branch not represented in the corpus.
**Where:** new `usr/lib/mios/agent-pipe/tests/router_corpus.json`; `usr/lib/mios/agent-pipe/test_mios_router_parity.py`; `automation/38-drift-checks.sh` (new check_router_parity)
**Done When:** the parity test is green over the full corpus; `check_router_parity` is green on HEAD and FAILS a negative test that adds an unmapped `intent ==` branch to a fixture; `just drift-gate` green.
**Why:** Router stays permanently unwired dead code because nobody can afford the VM round-trip to prove it, while the real cascade keeps accreting branches in server.py.
**Dep:** none
**Who:** you (Python + bash/drift-gate).

## AGY-128 [x] -- Add [agent_pipe.council] to mios.toml and gate it with check_council_gate_ssot  (WS-ORCH | P2 | S)**[DONE]**
**Goal:** E-12 ZERO-HARDCODES -- add the missing SSOT keys so tuning values have somewhere to float TO instead of living as code literals.
**What+How:** `kernel/config.py` resolves `COUNCIL_DIVERSITY_THRESHOLD` / `COUNCIL_AGGREGATOR_BYPASS_THRESHOLD` from `_COUNCIL_TOML`, but grep finds those keys nowhere in mios.toml, so the hardcoded 0.92/0.95 fallbacks always win -- and unlike the `[agent_pipe]` budgets there is NO gate guarding them, so Law 7 lets a literal creep back. Add the `[agent_pipe.council]` block (`diversity_gate`, `diversity_threshold`, `aggregator_bypass`, `aggregator_bypass_threshold`) and a `check_council_gate_ssot` mirroring `check_agent_pipe_budgets` (each key must be DEFINED in mios.toml AND REFERENCED in agent-pipe code).
**Where:** `usr/share/mios/mios.toml` (new [agent_pipe.council]); `automation/38-drift-checks.sh` (new check_council_gate_ssot); `usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py`
**Done When:** all 4 council keys are defined in mios.toml and consumed by `kernel/config.py`; `check_council_gate_ssot` green on HEAD; removing one key makes the check FAIL; `just drift-gate` green.
**Why:** The council diversity/bypass thresholds are operator-facing tuning knobs that today cannot be tuned at all -- the SSOT read always misses and the code constant silently wins.
**Dep:** none
**Who:** you (Python + TOML + bash).

## AGY-129 [x] -- Wire the dead cross-provider remote adapter behind an injected transport (code-only)  (WS-ORCH | P2 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired capabilities -- a capability MiOS already ships is either wired and gated or explicitly removed, never left inert.
**What+How:** `routing/provider_translate.py` is a complete bidirectional OpenAI<->Anthropic/Gemini wire translator, but grep for `_anthropic_resp_to_oai(` / `_oai_msgs_to_anthropic(` call sites in server.py returns ZERO, and `lanes_resolver.py` remote_escalation is inert -- so escalating to a remote `[nodes.*]` with `api='anthropic'|'gemini'` would ship OpenAI-shaped bodies to a non-OpenAI endpoint. Add a pure `remote_adapter.py` composing provider_translate with an INJECTED async transport (no live client): translate request -> provider shape, await transport, translate response -> OpenAI shape; gated on `[nodes.*].api` + `[ai].remote_escalation`; degrade-open (api unset/openai -> passthrough).
**Where:** new `usr/lib/mios/agent-pipe/mios_pipe/routing/remote_adapter.py`; `usr/lib/mios/agent-pipe/test_mios_remote_adapter.py`
**Done When:** `remote_adapter.call(node_cfg, oai_request, transport)` round-trips a canned Anthropic AND a canned Gemini payload back to an OpenAI-shaped result using a fake transport (NO network); `openai`/unset api passes through unchanged; checks 6/11 green; `just drift-gate` green.
**Why:** A working translator sits unreachable while the only live path would send malformed bodies to any non-OpenAI federated node -- the failure is silent until an operator configures one.
**Dep:** none
**Who:** you (Python, agent-pipe).

## AGY-130 [x] -- Port the agent-pipe SSOT validators into a Rust mios-aiplane-lint crate (Law 14)  (WS-LANG | P2 | L)**[DONE]**
**Goal:** E-01 Compiled native tier -- validation tooling moves from inline bash/python heredocs to compiled, tested Rust.
**What+How:** The agent-pipe SSOT validators exist only as inline bash+python heredocs inside `check_agent_pipe_budgets`; ADR-0011 / Law 14 target Rust for validation tooling (precedent: AGY-65 drift-runner, AGY-115 names-registry, AGY-116 SSOT-walk crate). Port the `[agent_pipe]` key-defined-and-consumed validation -- the budget keys plus the recursion/width bounds `swarm_max_width` / `max_dispatch_depth` / `default_hop_budget` read at server.py:1013/1131 -- into a Cargo crate, and wire it into the drift-gate behind the existing check with a bash fallback (fail-open if the binary is absent, matching the other native ports).
**Where:** new `tools/native/mios-aiplane-lint/` (crate + tests); `automation/38-drift-checks.sh` (delegate check_agent_pipe_budgets to the binary, bash fallback)
**Done When:** `cargo build` + `cargo test` green; the Rust tool reproduces `check_agent_pipe_budgets`' verdict on HEAD and exits non-zero on a synthetic missing-key fixture; the drift-check invokes it with a working bash fallback; `just drift-gate` green.
**Why:** Heredoc validators inside a 4000-line bash gate cannot be unit-tested or reused, so every SSOT rule added there is unverifiable and fragile.
**Dep:** none
**Who:** you (Rust + bash).

## AGY-131 [x] -- Add the recursion/width bounds to the budget gate + negative-test every new check in this batch  (WS-TEST | P2 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- every drift-gate check ships with a negative test proving it can actually FAIL.
**What+How:** `check_agent_pipe_budgets` asserts `tool_max_iters`/`replan_max`/`no_progress_window`/... but NOT the recursion/width bounds `swarm_max_width`, `max_dispatch_depth`, `default_hop_budget` that server.py:1013/1131 read from `[agent_pipe]`/dispatch defaults -- a future edit could orphan them with no gate noticing. Add those three keys to the gate's asserted key set and, mirroring AGY-54/120 discipline, add an injected-violation negative test for each NEW check introduced this batch.
**Where:** `automation/38-drift-checks.sh` (extend check_agent_pipe_budgets key set); tests exercising `check_router_parity`, `check_council_gate_ssot` and the aiplane-lint gate
**Done When:** the three bound keys are asserted defined+consumed by the budget gate; one negative test per new check (router_parity, council_gate_ssot, aiplane-lint) each drives that check RED; all checks pass on HEAD; `just drift-gate` green.
**Why:** Unbounded recursion/width keys can be silently orphaned, and a brand-new check that cannot fail is decoration -- both classes ship green today.
**Dep:** AGY-127, AGY-128, AGY-130 (the checks these tests exercise)
**Who:** you (bash + drift-gate).

## AGY-132 [x] -- Author reference/orchestration.md covering every routing decision seam and its SSOT knob  (WS-DOCS | P2 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- the doc set is authored and template-conformant so an arriving agent picks up a workstream cold.
**What+How:** The decision layer (router -> dispatcher -> dag_exec/swarm -> council_diversity -> smartroute -> hopbudget/effort_width) has per-module AI-hints and unit tests but no consolidated reference; the reference tree is Diataxis-governed and check-46 enforces frontmatter. Write `reference/orchestration.md` documenting each pure decision seam, the module that owns it, its SSOT knobs (`[agent_pipe]` budgets, `[agent_pipe.council]`, `[agent_pipe.quality]`, the dispatch depth/width bounds) and its degrade-open posture, copying an existing `reference/*.md` frontmatter exactly.
**Where:** new `usr/share/doc/mios/reference/orchestration.md`; `.gitignore` whitelist line
**Done When:** orchestration.md enumerates every seam + owning module + SSOT knob + degrade-open note with valid Diataxis frontmatter; check-46 (reference frontmatter) and `check_fluff_tokens` green; `just drift-gate` green.
**Why:** The orchestration layer is the hardest part of the agent plane to reason about and its behavior is currently only recoverable by reading seven modules.
**Dep:** none
**Who:** you (docs).

## AGY-133 [x] -- De-hardcode the firstboot whale image refs in the bake-plan generator (Law 7)  (WS-BAKE | P1 | S)**[DONE]**
**Goal:** E-12 ZERO-HARDCODES -- no code path, least of all a generator, may carry an image-ref literal that SSOT already declares.
**What+How:** `tools/generate-bake-plan.py` lines 140-145 hardcode two literal refs (`vllm_whale='docker.io/vllm/vllm-openai:latest'`, `sglang_whale='docker.io/lmsysorg/sglang:latest'`) for its "whale must be in core" validation -- a direct Law 7 violation inside a generator. Derive both from the already-loaded `[build.bake].firstboot_tokens`: for every token assert at least one `core` image substring-matches it (a token matching nothing is a dead/typo token -> validation error), and assert every image the existing `is_firstboot()` classifier pulls out is also in `core`. Extend `check_bake_plan` (check 35) so `--check` reds on a token matching no core image, and add a bogus-token negative test.
**Where:** `tools/generate-bake-plan.py`; `automation/38-drift-checks.sh` (check_bake_plan); `tests/drift-gate-negatives.sh`; `usr/share/mios/mios.toml` [build.bake].firstboot_tokens
**Done When:** grep for `docker.io/vllm` / `lmsysorg` in generate-bake-plan.py returns empty; a firstboot_token matching no core image makes `python3 tools/generate-bake-plan.py --check` exit non-zero; the negative test flips red-on-injection / green-on-restore; `just drift-gate` green.
**Why:** Renaming or retagging a whale in SSOT leaves the generator validating against a stale literal, so the eviction logic that keeps the bake inside a standard runner silently stops matching.
**Dep:** none
**Who:** you (Python).

## AGY-134 [x] -- Pin the searxng clone in sys/Containerfile and ban unpinned clones in any Containerfile  (WS-DEBT | P1 | M)**[DONE]**
**Goal:** E-02 Technical-debt retirement -- the network-at-build bakers are hardened out of the "clone default-branch, non-reproducible" class.
**What+How:** `usr/share/mios/sys/Containerfile:56` runs `git clone --depth 1 https://github.com/searxng/searxng.git` from the DEFAULT branch -- exactly the class AGY-40 fixed for `automation/*bake*.sh` but never applied here (firecrawl at lines 7/78 IS pinned to v1.0.0). Add a `searxng` key to `[build.bake_refs]`, thread it as `--build-arg SEARXNG_REF=$(resolve SSOT)` through `57-mios-sys-build.sh`, and render `git clone --branch "$SEARXNG_REF"`. Then add `check_containerfile_pinned_clones` that greps every `**/Containerfile*` and reds on any `git clone` lacking `--branch`/`--tag`/`@<sha>`. SBOM-not-hardcode: SSOT carries the tag INTENT, the resolved commit is recorded to bound-images.tsv/SBOM.
**Where:** `usr/share/mios/sys/Containerfile`; `usr/libexec/mios/57-mios-sys-build.sh`; `usr/share/mios/mios.toml` [build.bake_refs]; `automation/38-drift-checks.sh`; `tests/drift-gate-negatives.sh`
**Done When:** no `git clone` in any `Containerfile*` lacks an explicit ref; the new check reds when a `--branch` is stripped and greens on HEAD; a negative test guards it; `bash -n usr/libexec/mios/57-mios-sys-build.sh` clean; `just drift-gate` green.
**Why:** Two builds of the same commit produce different searxng code, so the image is not reproducible and an upstream default-branch break lands straight in a published MiOS.
**Dep:** none
**Who:** you (bash + Containerfile).

## AGY-135 [x] -- Round-trip firstboot.list in --check mode and gate the firstboot-tier bound/unbound invariant  (WS-BAKE | P1 | M)**[DONE]**
**Goal:** E-16 The bake plane -- the BAKE gate answers presence, and the firstboot tier that keeps the bake inside a stock runner is machine-verified.
**What+How:** `tools/generate-bake-plan.py` writes `plan.d/firstboot.list` only in WRITE mode (lines 227-234), explicitly OUTSIDE the `--check` comparison, so a stale or hand-edited firstboot.list is never caught. Make `--check` regenerate it in-memory and diff it against disk (the same round-trip the group lists get), and add a static `check_firstboot_tier` asserting the two-way invariant: every image in firstboot.list has a Quadlet under `usr/share/containers/systemd` whose ref substring-matches a firstboot_token AND is NOT symlinked into `/usr/lib/bootc/bound-images.d` (i.e. `08-system-files-overlay.sh` legitimately skipped it), and conversely no bound (symlinked) Quadlet matches a firstboot_token.
**Where:** `tools/generate-bake-plan.py`; `automation/38-drift-checks.sh`; `tests/drift-gate-negatives.sh`; `automation/08-system-files-overlay.sh` (invariant reference only)
**Done When:** a hand-edit to `plan.d/firstboot.list` makes `generate-bake-plan.py --check` red; a firstboot-tier Quadlet wrongly symlinked into bound-images.d makes `check_firstboot_tier` red; both green on HEAD; negative tests flip correctly; `just drift-gate` green.
**Why:** A whale that drifts back into the bound set re-adds ~47GB to the bake and breaks the publish on a standard runner -- today nothing detects it.
**Dep:** none
**Who:** you (Python + bash).

## AGY-136 [x] -- Wire the dead [build].rechunk_max_layers key into rechunk.sh and gate it  (WS-DEBT | P2 | S)**[DONE]**
**Goal:** E-12 ZERO-HARDCODES -- a declared SSOT value is consumed by its projection instead of a literal winning in code.
**What+How:** `usr/share/mios/mios.toml [build].rechunk_max_layers=67` is a DEAD key: `automation/build/rechunk.sh` never reads it -- its `rpm-ostree ... compose build-chunked-oci` call passes no `--max-layers`, and it targets a stale `mios-bootc:latest` image name instead of the SSOT image ref. Resolve the key from SSOT (via `mios_toml.py` or a tomllib read), pass `--max-layers="$RECHUNK_MAX_LAYERS"`, and fix the source ref to the SSOT-resolved image name. Add `check_rechunk_budget` asserting rechunk.sh references the SSOT key (no bare integer layer literal) and carries no `mios-bootc:` literal. Keep the step degrade-open on `MIOS_CONVERGE_IMAGE_RECHUNK_ENABLE` as today.
**Where:** `automation/build/rechunk.sh`; `usr/share/mios/mios.toml` [build].rechunk_max_layers; `automation/38-drift-checks.sh`; `tests/drift-gate-negatives.sh`
**Done When:** grep shows rechunk.sh resolving rechunk_max_layers from SSOT and passing `--max-layers`; no hardcoded layer integer or `mios-bootc:` ref remains; the new check reds when the SSOT reference is removed and greens on HEAD; `bash -n automation/build/rechunk.sh` clean; `just drift-gate` green.
**Why:** The operator's layer budget is ignored and the rechunk step points at an image name that no longer exists, so the step is either a no-op or chunks the wrong artifact.
**Dep:** none
**Who:** you (bash).

## AGY-137 [x] -- Reconcile [build.bake].core against the Quadlet-discovered bound-image set (anti-rot gate)  (WS-BAKE | P1 | M)**[DONE]**
**Goal:** E-16 The bake plane -- the fixed core allow-list stays a true statement about the image, checked mechanically.
**What+How:** `generate-bake-plan.py` already discovers the real bound-image set by scanning every `usr/share/containers/systemd/*.container|*.image` `Image=` (lines ~95-122), but the hand-maintained `[build.bake].core` list is only spot-validated for fully-qualified refs -- nothing reds when core goes stale (a Quadlet removed but its ref left in core, or a NEW sidecar Quadlet never added). Add a reconciliation pass: compute the discovered non-localhost bound-image set, diff it against `core`, and in `--check` mode red with an explicit added/removed report (excluding the two localhost base refs and the existing firstboot-tier handling). Add a negative test that injects a phantom ref into core.
**Where:** `tools/generate-bake-plan.py`; `automation/38-drift-checks.sh` (check_bake_plan or new check_bake_core_reconcile); `tests/drift-gate-negatives.sh`
**Done When:** adding a `core` ref no Quadlet references, or removing a live one, makes `generate-bake-plan.py --check` exit non-zero naming the offending refs; passes on HEAD; the negative test flips red-on-injection / green-on-restore; `just drift-gate` green.
**Why:** A stale core list either bakes images nothing runs (wasted runner capacity) or omits a live sidecar, which then cold-pulls at first start on a host that may have no egress.
**Dep:** none
**Who:** you (Python).

## AGY-138 [x] -- Retry-wrap and post-build verify the mios-sys / mios-cuda nested podman builds  (WS-HARDEN | P1 | S)**[DONE]**
**Goal:** E-16 The bake plane -- the transient-failure classes that break a publish are systemically fixed, not re-hit per release.
**What+How:** `usr/libexec/mios/57-mios-sys-build.sh` runs two heavy nested `podman build`s (lines 66-76 mios-sys, 79-89 mios-cuda) that pull docker.io FROM stages and do pip/npm/git installs (searxng, crawl4ai, firecrawl, open-webui) with NO retry and NO post-build verification -- grep for `retry` in the file is empty. A transient Docker Hub 429 or GitHub 504 mid-build hard-fails the whole OCI build (the class AGY-96 hardened for curl fetches but never applied to the podman invocation). Wrap each build in a bounded retry-with-backoff mirroring `mios-bake-group`'s loop shape, follow each with `podman image exists localhost/mios-sys|mios-cuda` so a silent no-op is caught, surface persistent failure loudly, and add a static gate asserting both builds are retry-wrapped.
**Where:** `usr/libexec/mios/57-mios-sys-build.sh`; `automation/38-drift-checks.sh` (retry-wrapper assertion); `tests/drift-gate-negatives.sh`
**Done When:** both builds run inside retry-with-backoff and are followed by an image-exists verification; the drift-check reds if either wrapper is removed and greens on HEAD; `bash -n usr/libexec/mios/57-mios-sys-build.sh` clean; `just drift-gate` green.
**Why:** One upstream 429 currently throws away an hour-plus of build and fails the publish, and a build that silently produced no image is indistinguishable from success.
**Dep:** none
**Who:** you (bash).

## AGY-139 [x] -- Port generate-bake-plan.py to a static mios-bake-plan Rust binary (Law 14)  (WS-LANG | P2 | L)**[DONE]**
**Goal:** E-01 Compiled native tier -- build-plane tooling ships as compiled binaries proven byte-parity against the shell/Python original before anything is deleted.
**What+How:** Following the established WS-LANG pattern (AGY-115 ported generate-names-registry.py, AGY-116 the SSOT-walk crate), port the bake-plan generator to a `mios-bake-plan` crate under `tools/native/`: read `[build.bake]`/`[build.bake_refs]` from mios.toml, scan `usr/share/containers/systemd/*.{container,image}` `Image=` values, resolve `${VAR:-default}`, classify into group_members, and emit `plan.d/NN-<group>.list` + `firstboot.list` + `bound-images.tsv` BYTE-IDENTICALLY to the Python. Keep `tools/generate-bake-plan.py` as the degrade-open fallback (`16-bake-plan.sh` prefers the binary when present). Cross-compile `x86_64-unknown-linux-musl` and document the toolchain + Containerfile-stage COPY per ADR-0011 §2. If no Rust toolchain is present in-env, scaffold the crate + a `TODO(agy)` and stop.
**Where:** new `tools/native/mios-bake-plan/`; `tools/native/Cargo.toml`; `automation/16-bake-plan.sh`; `automation/38-drift-checks.sh`; `tools/generate-bake-plan.py` (unchanged fallback)
**Done When:** `cargo build --release --target x86_64-unknown-linux-musl` succeeds (or the scaffolded TODO lands if no toolchain); a drift-check asserts the Rust output for `plan.d/*.list` + `firstboot.list` is byte-identical to the Python generator's on HEAD; `16-bake-plan.sh` degrades open to Python when the binary is absent; `just drift-gate` green.
**Why:** The bake plan decides what is in the published image; leaving it as an interpreted script keeps a build-critical decision untyped, untested and dependent on a python3 that a minimal runner may not have.
**Dep:** none
**Who:** you (Rust).

## AGY-140 [x] -- Hermetic mios-bake-group unit test (group assignment, firstboot skip) + fix the stale retry-count message  (WS-TEST | P2 | M)**[DONE]**
**Goal:** E-06 Test and documentation harness -- the build-plane shell tools get real tests that run offline inside the gate.
**What+How:** `usr/libexec/mios/mios-bake-group` has no test covering plan-file parsing, group->image mapping, the `MIOS_BAKE_BOUND_IMAGES=0` short-circuit (line 47), or firstboot-tier exclusion; and line 137 prints "failed ... after 3 attempts" while the loop at lines 120-127 tries 5. Write `tests/test-bake-group.sh` putting a stub `podman` on PATH (records pull invocations, always succeeds) plus a synthetic `MIOS_PLAN_DIR`, asserting: (a) `MIOS_BAKE_BOUND_IMAGES=0` prints SKIP, exits 0, zero pulls; (b) a group `.list` with N images produces exactly N stub pulls and N rows in bound-images.tsv; (c) a firstboot-token image never appears in any group `.list`. Fix the "3 attempts" string to match the loop bound (or reference the retry constant) and wire the test into the shell-test gate.
**Where:** new `tests/test-bake-group.sh`; `usr/libexec/mios/mios-bake-group`; `Justfile` (drift-gate target); `automation/tests/`
**Done When:** `bash tests/test-bake-group.sh` passes all three cases against the stub podman; the retry-count log matches the actual loop bound; the test runs inside `just drift-gate`; `bash -n` clean on both files; `just drift-gate` green.
**Why:** The tool that pulls every bound image into the published artifact is completely untested, and its failure log lies about how many attempts were made, misdirecting anyone debugging a bake.
**Dep:** none
**Who:** you (bash).

## AGY-141 [x] -- Give firstboot.list a real consumer: pre-stage the firstboot-tier whales in mios-ai-firstboot  (WS-DEPLOY | P2 | M)**[DONE]**
**Goal:** E-16 The bake plane -- images evicted from the bake are still delivered to the host, so eviction costs capacity, not availability.
**What+How:** `tools/generate-bake-plan.py` emits `plan.d/firstboot.list` (the whales evicted from the bake) but grep confirms NOTHING consumes it -- vLLM/SGLang only pull when their Quadlet first starts, which for VRAM-gated-off heavy lanes may never happen until an operator flips them on and then stalls on a ~25GB cold pull. Add an additive, egress-gated, degrade-open block to `usr/libexec/mios/mios-ai-firstboot` that reads `/usr/lib/mios/bake/plan.d/firstboot.list` and `podman pull`s each entry into the additional store (skip if present, retry-with-backoff, non-fatal per image, quiet when egress is down per Law 12) during the existing firstboot seed window. Stage the hunk disjoint from any venv-build logic (Claude owns that per AGY-38). Add a hermetic stub-podman test.
**Where:** `usr/libexec/mios/mios-ai-firstboot`; new `tests/test-firstboot-prestage.sh`; `automation/38-drift-checks.sh` (static gate: a consumer references firstboot.list)
**Done When:** mios-ai-firstboot reads plan.d/firstboot.list and pre-pulls each image degrade-open; the stub-podman test proves it iterates the list and exits 0 even when every pull fails; a drift-check asserts the consumer reference; `bash -n usr/libexec/mios/mios-ai-firstboot` clean; `just drift-gate` green.
**Why:** Today the eviction is a one-way loss -- the operator who enables a heavy lane waits on a 25GB foreground pull, and on an offline host gets nothing at all.
**Dep:** AGY-38 (venv-build logic is owned elsewhere -- stage this hunk disjoint)
**Who:** you (bash).

## AGY-142 [x] -- Fix the shadowed check_greenboot collision and add a check_gate_registry meta-check  (WS-GATE | P0 | M)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the gate's own registry is self-verifying, so no check can be silently disabled.
**What+How:** `automation/38-drift-checks.sh` defines `check_greenboot()` TWICE -- line 3085 (verifies `46-greenboot.sh` enables greenboot-healthcheck/set-rollback-trigger, uses `_violation`) and line 3946 (AGY-112 health-coverage over `usr/lib/greenboot/check/required.d`, uses `_fail`). bash keeps the LAST definition, so the first check is DEAD despite main() calling `check_greenboot` once. Rename the first body to `check_greenboot_enablement`, register it in main(), and add `check_gate_registry` that (a) parses all `^check_[a-z_]+\(\)` definitions and FAILS on any duplicate name, and (b) asserts every defined `check_*` is called exactly once in main() and every main() call resolves to a defined function. Wire it into main().
**Where:** `automation/38-drift-checks.sh`
**Done When:** `check_greenboot_enablement` actually runs (injecting a broken 46-greenboot.sh turns it red); `check_gate_registry` FAILS on an injected duplicate `check_*()` def or an unregistered check and passes on HEAD; grep shows zero duplicate check names; `bash -n` clean; `just drift-gate` green.
**Why:** A greenboot enablement gate reports green while never executing -- the rollback path MiOS depends on could be broken in the image and the gate would never say so, and nothing prevents the next copy-paste from doing it again.
**Dep:** none
**Who:** you (bash + the drift-gate framework).

## AGY-143 [x] -- Author automation/lint-python.sh, the missing peer of lint-shell.sh  (WS-LINT | P1 | M)**[DONE]**
**Goal:** E-05 Test and CI governance -- the Python plane gets the machine-checked quality gate the shell plane already has.
**What+How:** CI runs only `python3 -m py_compile /usr/lib/mios/agent-pipe/server.py` (`mios-ci.yml:533`) -- 1 of 353 `.py` files under `usr/lib/mios`. There is no repo-wide Python lint. Write `automation/lint-python.sh` modeled on `automation/lint-shell.sh`: py_compile every `usr/lib/mios/**/*.py` + `tools/*.py` + python-shebang files in `usr/libexec/mios/`, then run ruff (or pyflakes) at error severity, self-provisioning via dnf/pip and degrade-open (exit 2 = SKIPPED, never a false pass) exactly as lint-shell.sh does for shellcheck. Add `check_python_lint` invoking it and wire the script into the `drift-gate` Justfile target, build.sh's POST-build phase, and both CI workflows beside the shell-lint step.
**Where:** new `automation/lint-python.sh`; `automation/38-drift-checks.sh`; `Justfile` (drift-gate target); `automation/build.sh`; `.github/workflows/mios-ci.yml`; `.forgejo/workflows/build-mios.yml`
**Done When:** lint-python.sh py_compiles all 353 modules clean and exits 0; a syntax error injected into any module makes it (and `check_python_lint`) exit 1; the absent-toolchain path exits 2 (SKIPPED, not false-green); `just drift-gate` green on HEAD; `bash -n` clean.
**Why:** A NameError or syntax error in any of 352 unchecked modules ships into the image and only surfaces at runtime -- exactly the use-before-def class that previously blocked a publish bake.
**Dep:** none
**Who:** you (bash + Python).

## AGY-144 [x] -- check_test_hermeticity: static guard so no test can reach a live resource  (WS-TEST | P1 | M)**[DONE]**
**Goal:** E-06 Test and documentation harness -- the test suite runs offline and DB-less by construction, not by a hand-maintained skip list.
**What+How:** AGY-35/AGY-105 fixed live-DB tests reactively (build.sh still carries a hardcoded `_DB_INTEGRATION_TESTS` skip list); nothing PREVENTS a new `test_mios_*.py` from calling a live resource unguarded and hanging or failing the venv-less, DB-less drift-gate -- 5 test files already contain `psycopg.connect`/requests calls. Add `check_test_hermeticity` to the gate: AST/regex scan every `usr/lib/mios/agent-pipe/test_mios_*.py` and `tests/*.py` for live-resource calls (`psycopg.connect`, `requests.get/post`, `socket.socket`, `urllib.request`, `http.client`) and FAIL unless the module or enclosing test is guarded by a SkipTest/skipUnless/setUpModule connect-timeout sentinel. Read-only: no live DB, no network.
**Where:** `automation/38-drift-checks.sh`; (scans) `usr/lib/mios/agent-pipe/test_mios_*.py`
**Done When:** `check_test_hermeticity` flags an injected unguarded `psycopg.connect()` in a temp test and passes on HEAD (every live-resource test carries a skip sentinel, added by this task if missing); registered in main(); `just drift-gate` green.
**Why:** One unguarded test hangs or fails the offline build, and the current defense is a literal list in build.sh that the next test author will not know exists.
**Dep:** none
**Who:** you (bash + Python static scan).

## AGY-145 [x] -- Negative tests for the architectural-LAW tier gates + run drift-gate-negatives.sh in CI  (WS-TEST | P1 | M)**[DONE]**
**Goal:** E-06 Test and documentation harness -- the negative-test harness runs in CI on every PR, before the main gate.
**What+How:** `tests/drift-gate-negatives.sh` covers only 7 of 71 checks and is invoked ONLY by the local `just drift-gate` -- grep of `.github`/`.forgejo` shows it is NOT a CI step, so gate regressions ship green. Add inject / assert-red / restore / assert-green cases (following the existing `test_*` pattern) for the ARCHITECTURAL-LAW tier gates that have none: `check_no_mkdir_in_var`, `check_quadlet_privilege`, `check_var_closure`, `check_lint_is_final`, `check_firstboot_degrade_open`. Then add a "drift-gate negatives" step running `bash ./tests/drift-gate-negatives.sh` to both CI workflows beside the 38-drift-checks step.
**Where:** `tests/drift-gate-negatives.sh`; `.github/workflows/mios-ci.yml`; `.forgejo/workflows/build-mios.yml`
**Done When:** each new `test_*` injects a real violation, asserts the specific check goes red, restores, and asserts green; the whole negatives suite passes on HEAD; both CI workflows run it; `just drift-gate` green.
**Why:** The gates enforcing the Architectural Laws are unproven -- if one silently stops failing, CI cannot tell, because CI never runs the harness that would notice.
**Dep:** none
**Who:** you (bash + the negatives harness).

## AGY-146 [x] -- check_negative_test_coverage: ratchet so every law/security gate carries a negative test  (WS-GATE | P2 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- negative-test coverage becomes a fitness function instead of a convention.
**What+How:** Nothing enforces that a security/law drift-check ships with a negative test, which is why 64 of 71 checks are untested (AGY-54/AGY-120 covered only a handful). Add `check_negative_test_coverage`: maintain an explicit REQUIRE_NEGATIVE set (the security/law tier -- `check_cli_eval_safety`, `check_cli_sql_safety`, `check_no_hardcode`, `check_quadlet_privilege`, `check_no_mkdir_in_var`, `check_var_closure`, `check_canonical_bools`, plus the AGY-123 tier) and FAIL if any member lacks a matching `test_*` invocation in `tests/drift-gate-negatives.sh` (grep the harness for `check_<name>`). This is the fitness function guarding the fitness functions.
**Where:** `automation/38-drift-checks.sh`; (reads) `tests/drift-gate-negatives.sh`
**Done When:** the check FAILS when a required check name is removed from drift-gate-negatives.sh and passes on HEAD; registered in main(); `just drift-gate` green.
**Why:** Without a ratchet, negative-test coverage decays back toward zero the moment attention moves on, and untested security gates are indistinguishable from absent ones.
**Dep:** AGY-126 (as stated in the original Done When -- its tests must land first)
**Who:** you (bash).

## AGY-147 [x] -- Drift-gate read-only / idempotency self-test (prove the gate never mutates the tree)  (WS-GATE | P2 | S)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the gate's documented read-only contract is verified, not asserted.
**What+How:** `38-drift-checks.sh` is documented Read-only but nothing verifies it -- a check that writes a temp file into the tree or forgets to restore (a real risk given how many checks regenerate-and-diff) would silently corrupt the working tree. Add `tests/drift-gate-readonly.sh`: capture `git status --porcelain` plus a hash of the tree, run the full `automation/38-drift-checks.sh` AND `automation/38-ssot-lint.sh`, then assert the tree is byte-identical (no modified files, no untracked files, no stray temp artifacts). Wire it into the `drift-gate` Justfile target after the negatives step.
**Where:** new `tests/drift-gate-readonly.sh`; `Justfile` (drift-gate target)
**Done When:** the self-test passes on a clean HEAD with zero tree delta after a full drift run; a deliberate `echo x >> file` inside a scratch check makes it fail; wired into `just drift-gate`; `bash -n` clean; `just drift-gate` green.
**Why:** A gate that mutates the tree it inspects corrupts the operator's working copy and can mask or manufacture drift on the next run -- and this tree is shared with an autonomous agent.
**Dep:** none
**Who:** you (bash + git).

## AGY-148 [x] -- MIOS_DRIFT_REQUIRE_TOOLS: fail closed on absent tooling to kill the vacuous-pass class  (WS-GATE | P2 | M)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- a critical check can never report green because the tool that would run it was missing.
**What+How:** `38-drift-checks.sh` contains ~163 return-0/skip paths and its own header admits checks "Vacuous-passes where python3 or the copies are absent" -- so on a runner without python3 the security gates silently PASS. Define a documented CRITICAL set (the python3-dependent ones: `check_no_hardcode`, `check_cli_eval_safety`, `check_cli_sql_safety`, `check_agent_schema`, `check_drift_projection`) and an env switch `MIOS_DRIFT_REQUIRE_TOOLS=1` converting their tool-absent skip into a hard `_violation`; leave the default fail-open behavior byte-identical so nothing regresses. Set the flag in both CI workflows, where python3 is guaranteed.
**Where:** `automation/38-drift-checks.sh`; `.github/workflows/mios-ci.yml`; `.forgejo/workflows/build-mios.yml`
**Done When:** with python3 hidden from PATH and `MIOS_DRIFT_REQUIRE_TOOLS=1`, a critical check exits 1 (fail-closed); with the flag unset it still exits 0 exactly as today; both CI workflows set the flag; a negatives-harness case proves both modes; `just drift-gate` green on HEAD.
**Why:** A runner missing python3 turns the entire security tier -- eval safety, SQL safety, NO-HARDCODE -- into a green rubber stamp, which is worse than having no gate at all.
**Dep:** none
**Who:** you (bash).

## AGY-149 [x] -- Negative test for 38-ssot-lint.sh + check_soft_mode_not_committed  (WS-LINT | P2 | S)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- both lints prove they can fail, and neither can be quietly downgraded to advisory.
**What+How:** `automation/38-ssot-lint.sh` (the two-sided Quadlet placeholder wiring lint, run from build.sh/CI/Justfile) has NO negative test, unlike 38-drift-checks. Add a case to `tests/drift-gate-negatives.sh` dropping a temp `*.container` carrying `${MIOS_FAKE_DEADKEY}` in an `Exec=` line wired on neither `userenv.sh` nor `15-render-quadlets.sh`, assert `38-ssot-lint.sh` exits 1, remove it, assert green. ALSO add `check_soft_mode_not_committed` scanning `.github`/`.forgejo`/`automation`/`build.sh`/`Justfile` for a committed `MIOS_DRIFT_CHECK_SOFT=1` or `MIOS_SSOT_LINT_SOFT=1` and FAILing if found.
**Where:** `tests/drift-gate-negatives.sh`; `automation/38-drift-checks.sh`
**Done When:** the ssot-lint negative case injects a dead key, asserts red, restores, asserts green; `check_soft_mode_not_committed` FAILS on an injected `MIOS_DRIFT_CHECK_SOFT=1` in a workflow and passes on HEAD; both wired in; `just drift-gate` green.
**Why:** An unproven lint may already be inert, and a single committed SOFT=1 turns the whole enforcement plane advisory while every log still reads green.
**Dep:** none
**Who:** you (bash).

## AGY-150 [x] -- Port 38-ssot-lint.sh to a native mios-ssot-lint crate behind a byte-identical equivalence gate  (WS-LANG | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- moves one more deterministic bash phase into the compiled tier with parity PROVEN before any shell is retired.
**What+How:** 38-ssot-lint.sh is pure bash+grep, deterministic and python-free, so it is the ideal Law-14 strangler target, and the `tools/native/` Cargo workspace already hosts mios-ssot-walk / mios-version-check / generate-names-registry. Add a `tools/native/mios-ssot-lint` crate reproducing the collect-refs pass plus the two-sided wiring assertion (userenv.sh slot/export AND 15-render-quadlets.sh allowlist), emitting the exact per-key error lines and exit codes (0 pass / 1 orphan / 2 missing-input). Register it in `tools/native/Cargo.toml`. Add `check_ssot_lint_equivalence` to 38-drift-checks.sh that runs BOTH linters over the tree and asserts byte-identical stdout + exit code, guarded on cargo/binary presence and degrade-open exactly like `check_resolver_twin_equivalence`. Do NOT repoint any consumer to the Rust binary yet — this task only establishes the parity harness.
**Where:** `tools/native/mios-ssot-lint/` (new crate), `tools/native/Cargo.toml`, `automation/38-drift-checks.sh`
**Done When:** `cargo build` succeeds and `cargo test` passes for the crate; bash 38-ssot-lint.sh and mios-ssot-lint emit byte-identical output + exit code on HEAD and on an injected dead-key fixture; `check_ssot_lint_equivalence` is green (or cleanly SKIPs when cargo is absent); `just drift-gate` green.
**Why:** without a parity harness the Rust tier can only be adopted on faith — a port that silently differs on one error line would move the SSOT-lint verdict, and no gate would notice.
**Dep:** none
**Who:** you (Rust + bash equivalence gate).

## AGY-151 [x] -- Generate a drift-gate index and gate on it, killing the duplicate `(N)` check labels  (WS-TEST | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- one global numbering system so a failing check number maps 1:1 to a named function.
**What+How:** The numbered gate labels in 38-drift-checks.sh have drifted badly: grepping the `(N)` labels shows duplicates for 6, 9, 10, 11, 27, 30, 35, 36, 37, 41, 44, 45, 46, 47, 51, 54 and 61, so a reader hitting a `(54)` failure cannot tell which gate produced it. Add `tools/generate-gate-index.py` emitting `usr/share/mios/reference/drift-gate-index.tsv` — one row per `check_*` in main() order (ordinal, function name, one-line purpose parsed from the leading comment/echo) — and a `check_gate_index` in 38-drift-checks.sh that regenerates and diffs it using the same regenerate-and-diff pattern as `check_names_registry`, FAILING on drift. Renumber the echo labels off the generated ordinal so the duplicate `(N)` labels disappear at the source.
**Where:** `tools/generate-gate-index.py` (new), `usr/share/mios/reference/drift-gate-index.tsv` (new), `automation/38-drift-checks.sh`
**Done When:** the index generates deterministically with zero duplicate ordinals; `check_gate_index` passes on HEAD and FAILS when a check is added without regenerating the index; it is registered in main(); `just drift-gate` green.
**Why:** today a red gate reports an ambiguous number shared by up to three functions, so triage starts by hand-reading a 100+-check bash file to find which one actually fired.
**Dep:** none
**Who:** you (bash + docs).

## AGY-152 [x] -- Ship the oci-archive PRODUCER recipe that tools/install.sh already consumes  (WS-DEPLOY | P1 | M) **[DONE]**
**Goal:** E-20 The bootc-native install legs -- a blank machine installs fully offline from a local OCI tarball, with the archive actually built by the tree.
**What+How:** AGY-119's `tools/install.sh` consumes an oci-archive at `MIOS_OCI_ARCHIVE` (default `/mnt/mios-repo/mios-latest.tar`) and runs `bootc install to-disk --transport oci-archive`, but nothing in the repo PRODUCES that archive — grepping for `podman save` / `--format oci` finds only the consumer references. Add a `just oci-archive` recipe (depends on `build`) running `podman save --format oci-archive -o output/oci-archive/mios-<VERSION>.tar {{LOCAL}}`, a thin `usr/libexec/mios/mios-stage-oci-archive` that copies/symlinks the result to the SSOT staging path, and wire the recipe into `just all`. Add a drift-check (next free number, 69+) asserting the producer's output basename equals the consumer default so the two cannot silently diverge.
**Where:** `Justfile` (new `oci-archive` recipe + `all`), `usr/libexec/mios/mios-stage-oci-archive` (new), `tools/install.sh`, `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`
**Done When:** `bash -n` clean and `just --evaluate` parses; the new check asserts producer == consumer archive path, passes on HEAD, and FAILS under an injected path mismatch (negative added to tests/drift-gate-negatives.sh); `just drift-gate` fully green.
**Why:** the offline install leg is a dead end today — the installer looks for a tarball no build step ever writes, so bare-metal provisioning fails at the point of no egress.
**Dep:** AGY-119 (tools/install.sh consumer)
**Who:** you (bash + Justfile + drift-gate).

## AGY-153 [x] -- Substitute real credentials in the `iso` BIB recipe and gate on leftover REPLACEME tokens  (WS-DEPLOY | P1 | S) **[DONE]**
**Goal:** E-20 The bootc-native install legs -- an installer ISO that produces a loginable system rather than a locked-out one.
**What+How:** The `iso:` recipe mounts `config/artifacts/iso.toml` verbatim into BIB, but that file still ships `user --iscrypted --password=$6$REPLACEME_WITH_SHA512_HASH$REPLACEME` and `sshkey ... AAAA_REPLACE_WITH_REAL_PUBKEY`, so every built ISO carries an invalid password hash and the `mios` account cannot log in. The qcow2/vhdx recipes already do the right thing via mktemp+sed substitution of `MIOS_USER_PASSWORD_HASH` / `MIOS_SSH_PUBKEY`; mirror that exact substitution into the `iso` recipe. Then add a drift-check that scans every BIB `just` recipe and FAILS if it mounts a `config/artifacts/*.toml` still containing REPLACEME / AAAA_REPLACE without a substituting `sed` in the same recipe.
**Where:** `Justfile` (`iso` recipe), `config/artifacts/iso.toml`, `automation/38-drift-checks.sh` (new check), `tests/drift-gate-negatives.sh`
**Done When:** the `iso` recipe substitutes credentials identically to qcow2/vhdx; the new check FAILS on a REPLACEME-bearing raw mount (negative test) and passes on HEAD; `just drift-gate` green.
**Why:** every ISO built today is unusable at first boot — the operator burns media, boots it, and cannot authenticate as the only account the image ships.
**Dep:** none
**Who:** you (Justfile + drift-gate).

## AGY-154 [x] -- `bash -n` parse gate for the embedded kickstart `%post` shell  (WS-DEPLOY | P1 | S) **[DONE]**
**Goal:** E-20 The bootc-native install legs -- install-time shell is proven syntactically sound before media ships, not on real hardware.
**What+How:** The `%post ... %end` block in `usr/share/mios/ventoy/mios-kickstart.cfg` and the inline `[customizations.installer.kickstart].contents` in `config/artifacts/iso.toml` both embed shell that NOTHING syntax-checks, so an unbalanced `fi` or a bad heredoc only surfaces mid-install on real hardware. Add a drift-check that extracts the shell body from the `%post`/kickstart region of each file into a temp file and runs `bash -n` on it, FAILING on any parse error. Pure static parse — no install, VM, or hardware required.
**Where:** `automation/38-drift-checks.sh` (new check reading `usr/share/mios/ventoy/mios-kickstart.cfg` + `config/artifacts/iso.toml`), `tests/drift-gate-negatives.sh`
**Done When:** the check `bash -n`-parses both extracted `%post` bodies and passes on HEAD; a deliberately broken `%post` (dropped `fi`) makes it FAIL (negative test); `just drift-gate` green.
**Why:** a one-character typo in install-time shell currently costs a full media burn plus a hardware boot to discover, and can leave a half-provisioned machine.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-155 [x] -- Add the offline-invariant drift-check AGY-119 promised for tools/install.sh  (WS-DEPLOY | P1 | S) **[DONE]**
**Goal:** E-20 The bootc-native install legs -- the zero-egress offline install contract is machine-enforced, not asserted in a task description.
**What+How:** AGY-119's own Done-When required "a drift-check asserts [tools/install.sh] references the oci-archive path (NOT a network pull)", but no such check exists — grepping `automation/*.sh` for `oci-archive` / `to-disk` / `transport oci` returns only the script itself. Add a drift-check that (a) `bash -n` parses tools/install.sh, (b) asserts it invokes `--transport oci-archive`, and (c) FAILS if the script body contains any network-pull token (`http://`, `https://`, `git clone`, `podman pull`, `skopeo copy docker://`) — enforcing the Law 12 BAKE-NOT-FETCH zero-network offline-install contract.
**Where:** `automation/38-drift-checks.sh` (new check), `tools/install.sh`, `tests/drift-gate-negatives.sh`
**Done When:** the check passes on HEAD; injecting a `podman pull` line into a copy makes it FAIL (negative test); `bash -n` clean; `just drift-gate` green.
**Why:** nothing stops a future edit from reintroducing a network fetch into the one installer whose entire premise is that it works on a machine with no egress.
**Dep:** AGY-119
**Who:** you (bash + drift-gate).

## AGY-156 [x] -- Disambiguate the install.sh family with a role-marker contract and collision gate  (WS-DEPLOY | P2 | M) **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- surviving redirectors and installers are thin, documented, and unambiguous about what they do.
**What+How:** Four divergent install.sh-family scripts coexist: root `install.sh` (redirector to build-mios.sh, FHS overlay), `tools/install.sh` (bootc `install to-disk`, which DESTROYS the target disk), and `automation/install.sh` + `automation/install-fhs.sh` (bootc-guarded FHS installers). The mios-kickstart.cfg comment records that a consumer once called `tools/install.sh` expecting an FHS overlay — today that same call wipes a disk. Add a `# MIOS_INSTALLER_ROLE=<role>` marker header to each script, add a drift-check asserting every `*/install.sh` declares a distinct, known role, and write `docs/agy/doc-install-family.md` disambiguating them.
**Where:** `install.sh`, `tools/install.sh`, `automation/install.sh`, `automation/install-fhs.sh`, `docs/agy/doc-install-family.md` (new), `automation/38-drift-checks.sh` (new check), `tests/drift-gate-negatives.sh`
**Done When:** each installer declares a distinct role marker; the gate FAILS if two share a role or a marker is missing (negative test); the doc is committed and cross-referenced from ROADMAP; `just drift-gate` green.
**Why:** four same-named scripts with radically different blast radii is a live data-loss hazard — the confusion has already happened once, recorded in a code comment.
**Dep:** none
**Who:** you (bash + drift-gate + docs).

## AGY-157 [x] -- Project BIB filesystem sizing from a `[deploy.artifacts]` SSOT block  (WS-DEPLOY | P2 | M) **[DONE]**
**Goal:** E-20 The bootc-native install legs -- every install artifact's shape derives from SSOT (Law 8) instead of four hand-tuned configs.
**What+How:** Root-fs sizing is hand-set and divergent across the BIB configs with no SSOT and no gate: bib.toml says `minsize=80 GiB`, iso.toml says `minsize=150 GiB` plus `part / --grow --fstype ext4`, and qcow2.toml/vhdx.toml each carry their own. Add a `[deploy.artifacts]` block to mios.toml (per-artifact minsize / rootfs / fstype), write `tools/generate-artifact-configs.py` to project the `[[customizations.filesystem]]` stanzas into `config/artifacts/{bib,iso,qcow2,vhdx}.toml`, and add a regenerate-and-diff drift-check.
**Where:** `mios.toml` (`[deploy.artifacts]`), `tools/generate-artifact-configs.py` (new), `config/artifacts/*.toml`, `automation/38-drift-checks.sh` (new check), `tests/drift-gate-negatives.sh`
**Done When:** the projection is idempotent (regenerate == committed, byte-for-byte); the check FAILS on a hand-edited minsize (negative test); `just drift-gate` green.
**Why:** the four artifact configs already disagree by 70 GiB with no recorded reason, and the next disk-sizing decision has four places to forget.
**Dep:** none
**Who:** you (Python + drift-gate).

## AGY-158 [x] -- Resolve the MiOS-Repo mount from `[cat.repo_partition]` SSOT in both consumers  (WS-DEPLOY | P2 | S) **[DONE]**
**Goal:** E-20 The bootc-native install legs -- the offline media contract has exactly one definition of where the image tarball lives.
**What+How:** mios.toml already defines `[cat.repo_partition].label="MiOS-Repo"` and `[cat.data_partition].label="MiOS-Data"`, but `tools/install.sh` hardcodes `MIOS_OCI_ARCHIVE=/mnt/mios-repo/...` and the `%post` in mios-kickstart.cfg hand-scans `/mnt/usb /mnt/usb/repos ...` — neither resolves the SSOT label, violating Law 7. Resolve the repo mount from the SSOT label via `blkid -L "$label"` in tools/install.sh, align the kickstart scan list to the same label contract, and add a drift-check asserting the label literal in both consumers matches `[cat.repo_partition].label`.
**Where:** `tools/install.sh`, `usr/share/mios/ventoy/mios-kickstart.cfg`, `mios.toml` (`[cat.repo_partition]`), `automation/38-drift-checks.sh` (new check), `tests/drift-gate-negatives.sh`
**Done When:** both consumers derive the label from SSOT; the check FAILS if `[cat.repo_partition].label` changes without updating a consumer (negative test); `bash -n` clean; `just drift-gate` green.
**Why:** renaming the USB partition label in SSOT today silently breaks offline install at the worst possible moment — on a machine with no network to debug from.
**Dep:** AGY-152 (staged archive path)
**Who:** you (bash + drift-gate).

## AGY-159 [x] -- Enforce the BIB single-`/config.toml` invariant plus TOML validity for artifact configs  (WS-DEPLOY | P2 | S) **[DONE]**
**Goal:** E-20 The bootc-native install legs -- the image-builder invocation cannot regress into a known-crashing shape.
**What+How:** The `iso` recipe carries a scar comment — "BIB crashes with: found config.json and also config.toml" — yet nothing prevents a future recipe from mounting two configs. Add a drift-check that parses each BIB `just` recipe (raw/iso/qcow2/vhdx) and FAILS unless it mounts exactly one `:/config.toml`, and that additionally asserts every `config/artifacts/*.toml` parses as valid TOML via Python `tomllib`.
**Where:** `automation/38-drift-checks.sh` (new check reading `Justfile` + `config/artifacts/*.toml`), `tests/drift-gate-negatives.sh`
**Done When:** the check asserts exactly-one-config per BIB recipe and valid TOML for all artifact configs; a second `/config.toml` mount or a malformed toml makes it FAIL (negative test); `just drift-gate` green.
**Why:** the crash this prevents was already paid for once in debugging time, and the only thing stopping a rerun is a comment.
**Dep:** none
**Who:** you (bash/Python + drift-gate).

## AGY-160 [x] -- Kill hardcoded credentials in the Ventoy SystemRescue firstboot and extend the nohc lint to catch them  (WS-DEPLOY | P2 | S) **[DONE]**
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- plaintext credentials become an SSOT-resolved value and a lint-detectable class.
**What+How:** `usr/share/mios/ventoy/autorun/01-sysrescue-firstboot.sh` hardcodes `echo "root:mios" | chpasswd` and `echo "mios:mios" | chpasswd` (Law 7 NO-HARDCODE), and the check-16 nohc lint in `usr/libexec/mios/mios-hardcode-lint` today only catches date-in-comment/header crash risks, not plaintext credentials. Route the firstboot user and password through the SSOT `[identity]` contract as a resolved value rather than a literal, and extend mios-hardcode-lint to FLAG any literal `chpasswd` plaintext under `usr/share/mios/ventoy/autorun/*`.
**Where:** `usr/share/mios/ventoy/autorun/01-sysrescue-firstboot.sh`, `usr/libexec/mios/mios-hardcode-lint`, `automation/38-drift-checks.sh` (check 16), `tests/drift-gate-negatives.sh`
**Done When:** no plaintext `root:mios` / `mios:mios` literal remains in the tree; the extended lint FAILS on a re-introduced `chpasswd` literal (negative test) and passes on HEAD; `just drift-gate` green.
**Why:** every USB built from this tree ships a publicly known root password, and the lint that is supposed to catch hardcodes is blind to the credential class entirely.
**Dep:** none
**Who:** you (bash + Python lint + drift-gate).

## AGY-161 [x] -- XML well-formedness gate and SSOT projection for the Windows VM template  (WS-DEPLOY | P3 | S) **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- the `xbox`/Windows guest target derives its shape from SSOT and is statically validated.
**What+How:** `tools/win11-secureboot-template.xml` (the libvirt domain used to spin the Windows/Xbox guest) ships a `TEMPLATE_VM_NAME` placeholder plus hardcoded memory and vCPU-pinning values, with no well-formedness gate and no SSOT tie-in. Add a drift-check that xmllint-validates the template as well-formed XML, and introduce an envsubst-style substitution point projecting the VM name and memory from an SSOT block (reuse `[cat]` or add a `[vm]` block), gated by regenerate-and-diff. Static only — no VM boot and no GPU needed.
**Where:** `tools/win11-secureboot-template.xml`, `mios.toml` (`[vm]`/`[cat]`), `automation/38-drift-checks.sh` (new check), `tests/drift-gate-negatives.sh`
**Done When:** the template is asserted well-formed XML and its VM name/memory derive from SSOT; a malformed-XML edit makes the check FAIL (negative test); `just drift-gate` green.
**Why:** a malformed domain XML fails only when libvirt refuses to define the guest, wasting an operator's VM session on a typo a parser catches instantly.
**Dep:** none
**Who:** you (bash/xmllint + drift-gate).

## AGY-162 [x] -- Project the FreeIPA enrollment env from SSOT and drift-gate it (wire the identity plane)  (WS-RUNTIME | P1 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a capability MiOS already bakes becomes SSOT-wired AND gated instead of dormant.
**What+How:** `automation/22-freeipa-client.sh` installs SSSD/FreeIPA and enables `mios-freeipa-enroll.service`, but that unit only fires when `/etc/mios/ipa-enroll.env` exists — and NOTHING projects that env from SSOT (grep: zero freeipa/ipa-client hits in 38-drift-checks.sh; mios.toml `[identity]` has no ipa sub-block). Add an `[identity.ipa]` block (realm, server, domain, enroll_principal, otp/one-time, `enabled=false` default) plus a renderer that deterministically projects `/etc/mios/ipa-enroll.env`, then a regenerate-and-diff drift-check mirroring `check_chrony_projection` (55) and `check_nut_projection` (56). It ships INERT (`enabled=false`) so the ConditionPathExists gate keeps enrollment off until an operator opts in; no live IPA server is needed to render.
**Where:** `usr/share/mios/mios.toml` (`[identity.ipa]`), `automation/22b-ipa-enroll-render.sh` (new, or a render seam folded into `automation/22-freeipa-client.sh`), `automation/38-drift-checks.sh`, `etc/mios/ipa-enroll.env` (committed rendered artifact)
**Done When:** `check_ipa_enroll_projection` re-renders ipa-enroll.env from `[identity.ipa]` and diffs the committed artifact — green on HEAD, RED on a mutated fixture; ships `enabled=false`; `bash -n` clean; `just drift-gate` green.
**Why:** the image carries a full enrollment path that can never activate, so the identity plane is shipped weight with zero operator-reachable configuration.
**Dep:** none
**Who:** you (bash + Python + drift-gate).

## AGY-163 [x] -- Gate the signed-UKI kernel-cmdline projection (23-uki-render is ungated)  (WS-RUNTIME | P1 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- kargs projection is gated end-to-end so a boot-critical flatten cannot silently drop an entry.
**What+How:** `automation/22-kargs-render.sh` HAS a projection gate (re-render into a temp KARGS_DIR and diff, around line 3042), but `automation/23-uki-render.sh` — which flattens `usr/lib/bootc/kargs.d/*.toml` into `/usr/lib/kernel/cmdline` for the UKI — has NO gate (grep: zero uki-render hits) and `[uki].verity_uki_build` is asserted nowhere. Factor the flatten into a testable function and add `check_uki_cmdline_projection` that re-runs the script's deterministic Python fallback path (re-flatten kargs.d/*.toml) and asserts the union round-trips against the committed cmdline and the kargs gate's output, so a hand-edited cmdline or an orphaned karg drop-in is caught at PR time. Static only — uses the Python fallback and never invokes bootc.
**Where:** `automation/38-drift-checks.sh`, `automation/23-uki-render.sh`, `usr/lib/kernel/cmdline` (committed artifact if present)
**Done When:** `check_uki_cmdline_projection` is green on HEAD and RED when a `kargs.d/*.toml` entry is dropped from the flattened cmdline; registered in the run_all list next to checks 55/56; `bash -n` clean; `just drift-gate` green.
**Why:** a lost karg in the UKI cmdline is a boot-time failure on real hardware — the most expensive place to discover a projection bug, and today nothing catches it earlier.
**Dep:** none
**Who:** you (bash + Python + drift-gate).

## AGY-164 [x] -- composefs-verity prepare-root.conf projection gate plus negative test  (WS-RUNTIME | P2 | S) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the tamper-evident-root configuration is a gated SSOT projection, not a build-time side effect.
**What+How:** `automation/40-composefs-verity.sh` renders `/usr/lib/ostree/prepare-root.conf` from mios.toml `[security].composefs_mode` (verity/yes/off) and optionally masks `systemd-remount-fs.service`, but there is NO build-time projection gate — grep shows zero composefs-verity hits in 38-drift-checks.sh, and the existing greenboot `15-composefs-verity.sh` is a RUNTIME check, not an SSOT round-trip. Expose the render as COMPOSEFS_CONF-overridable and add `check_composefs_projection` that re-renders prepare-root.conf from `[security].composefs_mode` into a temp file and diffs the committed etc artifact, including the mask decision.
**Where:** `automation/38-drift-checks.sh`, `automation/40-composefs-verity.sh`, `tests/drift-gate-negatives.sh` or `automation/tests/test-drift-gates.sh`
**Done When:** `check_composefs_projection` is green on HEAD; a negative test that flips composefs_mode (or mutates prepare-root.conf) makes it RED; `bash -n` clean; `just drift-gate` green.
**Why:** the root-integrity mode can drift away from SSOT with the only detection happening at boot on a live machine, which is exactly when an unverified root matters most.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-165 [x] -- Project cosign policy.json and Sigstore trust roots from SSOT, plus a cosign-v2 pin lint  (WS-RUNTIME | P2 | M) **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- signature-verification policy becomes SSOT-derived and its known-fatal version constraint becomes machine-enforced.
**What+How:** `automation/42-cosign-policy.sh` installs cosign, the Sigstore trust roots and policy.json, and comments that cosign MUST stay v2.x (v3+ breaks rpm-ostree OCI 1.1) — but that constraint is only prose, the policy identities are not SSOT-projected, and there is NO gate (grep: zero cosign hits in 38-drift-checks.sh). Add a `[security.sigstore]` block (allowed signer identities/issuers, policy mode, `cosign_major=2`), project policy.json from it, and add a drift-check that (a) re-renders policy.json from SSOT and diffs the committed file, and (b) lints that the version-resolution filter in 42-cosign-policy.sh still constrains to v2.x, failing if the `^v2` grep guard is removed.
**Where:** `usr/share/mios/mios.toml` (`[security.sigstore]`), `automation/42-cosign-policy.sh`, `automation/38-drift-checks.sh`, the committed `policy.json` artifact
**Done When:** policy.json round-trips from `[security.sigstore]`; the v2-pin lint goes RED when the `^v2` filter is stripped; both green on HEAD; `bash -n` clean; `just drift-gate` green.
**Why:** a prose-only version constraint on the signature verifier is one refactor away from breaking rpm-ostree, and the trusted-signer list is currently unreviewable because it lives in a script rather than SSOT.
**Dep:** none
**Who:** you (bash + Python + drift-gate).

## AGY-166 [x] -- Author the chrony PTP first-boot drop-in the render script itself defers  (WS-RUNTIME | P2 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a documented deferred TODO becomes a real hardware-detected deploy-time behavior.
**What+How:** `automation/24-chrony-render.sh` explicitly defers PTP tuning: its comment states the noselect / no-rtcsync / PHC-refclock tuning for Hyper-V/WSL2 PTP hosts "belongs in a deploy/first-boot chrony drop-in, NOT the build render" — and that drop-in DOES NOT EXIST, so PTP hosts never get the tuning. Author a first-boot generator (`usr/libexec/mios/mios-chrony-ptp-dropin` plus its unit) that writes an `/etc/chrony.d/*.conf` drop-in gated by `ConditionPathExists=/dev/ptp0` — hardware-detected at deploy time, Law 9 one-canonical-name — leaving the canonical build render behind check 55 byte-unchanged. Add a gate asserting the drop-in is idempotent and does NOT mutate the canonical `etc/chrony.conf`.
**Where:** new firstboot unit under `usr/lib/systemd/system/*.service` (`ConditionPathExists=/dev/ptp0`), `usr/libexec/mios/mios-chrony-ptp-dropin` (new), `automation/24-chrony-render.sh`, `automation/38-drift-checks.sh`, `etc/chrony.d/` (committed template)
**Done When:** the PTP drop-in generator exists, is `bash -n` clean and idempotent (a second run produces a no-op diff); a gate asserts check 55's canonical chrony.conf is unchanged by the drop-in path; `just drift-gate` green.
**Why:** on Hyper-V/WSL2 hosts the clock is left untuned, so time-sensitive services drift with no code path that could ever correct it.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-167 [x] -- Negative tests proving the chrony (55) and nut (56) projection gates can actually fail  (WS-TEST | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- every drift-gate check is proven able to FAIL, not merely observed passing.
**What+How:** AGY-120 added negative tests for gates 64/65/66 in `automation/tests/test-drift-gates.sh`, but the older projection gates `check_chrony_projection` (55) and `check_nut_projection` (56) have NO negative test — nothing proves they fail on drift. Extend the same harness: inject a mutated `etc/chrony.conf` (change an NTP server line) and a mutated `etc/ups/` config (an extra or missing rendered file) into a temp ROOT, run each gate, and assert a non-zero exit, plus assert both pass on the clean HEAD tree.
**Where:** `automation/tests/test-drift-gates.sh`, `tests/drift-gate-negatives.sh`, fixtures alongside them
**Done When:** test-drift-gates.sh proves gate 55 and gate 56 each go RED under an injected violation and GREEN on HEAD; the test file runs clean; `just drift-gate` green.
**Why:** a gate that has never been observed failing is indistinguishable from a gate that always returns 0 — two of the oldest projection checks currently carry that risk.
**Dep:** AGY-120 (test-drift-gates.sh harness)
**Who:** you (bash).

## AGY-168 -- Renderer-to-gate coverage meta-check: no `*-render.sh` may ship ungated  (WS-TEST | P1 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the gate structurally prevents the NEXT ungated renderer instead of discovering it by grep.
**What+How:** Multiple SSOT renderers ship with a projection gate (24-chrony to 55, 25-nut to 56, 22-kargs to its own) while others silently do not — grep confirms zero drift-check hits for `23-uki-render`, `40-composefs-verity`, `22-freeipa-client` and `42-cosign-policy`. Add `check_renderer_gate_coverage`: a meta drift-check that enumerates `automation/*-render.sh` (plus the known projection scripts) and asserts each maps to a registered `check_*_projection` function in 38-drift-checks.sh, FAILING on any unmapped renderer, with a small explicit allowlist for deliberate non-projections (e.g. 16-render-ports if separately gated) held in SSOT or an inline set.
**Where:** `automation/38-drift-checks.sh` (new `check_renderer_gate_coverage` + allowlist, e.g. `mios.toml [security]` or an inline set)
**Done When:** the meta-check enumerates every `*-render.sh`, maps each to a gate, and is GREEN only once the sibling gate tasks land (or the renderer is allowlisted); a negative test adding a fake ungated `foo-render.sh` makes it go RED; `just drift-gate` green.
**Why:** ungated renderers are found today only by ad-hoc grep, so each new one starts life able to drift from SSOT indefinitely.
**Dep:** AGY-162/163/164/165 (the per-renderer gates it enumerates)
**Who:** you (bash + drift-gate).

## AGY-169 -- GPU-passthrough vendor-unit enablement coverage gate  (WS-RUNTIME | P2 | S) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- an enabled GPU vendor is guaranteed to have the unit that makes passthrough real.
**What+How:** `automation/35-gpu-passthrough.sh` symlinks `mios-gpu-{status,nvidia,amd,intel}.service` from the overlay but only WARN-and-skips when a unit file is missing ("WARN: ${svc} missing ... skipping"), so a vendor enabled in `[gpu.vendors]` whose unit was never overlaid ships silently broken passthrough. Add `check_gpu_unit_coverage`: a static file-existence projection mapping each enabled `[gpu.vendors]` flag to its required `usr/lib/systemd/system/mios-gpu-<vendor>.service` overlay file and FAILING when an enabled vendor's unit is absent. Extends AGY-111's multi-vendor CDI work with an enablement guarantee.
**Where:** `automation/38-drift-checks.sh`, `mios.toml` (`[gpu.vendors]`), `usr/lib/systemd/system/mios-gpu-*.service`
**Done When:** `check_gpu_unit_coverage` is green on HEAD and RED when an enabled vendor's unit file is removed; the NVIDIA path is unchanged; `just drift-gate` green.
**Why:** an AMD or Intel operator can enable passthrough in SSOT, see a build warning scroll past, and get a machine where the GPU never binds — with no failing gate anywhere.
**Dep:** AGY-111 (multi-vendor CDI)
**Who:** you (bash + drift-gate).

## AGY-170 -- k3s-selinux policy staging coverage gate  (WS-RUNTIME | P2 | S) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the SELinux policy build inputs and staging path stay consistent across both sides.
**What+How:** `automation/19-k3s-selinux.sh` compiles `k3s.pp` from `[packages.k3s-selinux-build]` (selinux-policy-devel / git / make) and stages it into `/usr/share/selinux/packages/mios/`, but there is NO gate — grep shows zero k3s-selinux hits in 38-drift-checks.sh — asserting the build inputs are consistent or that the staging path is the one the SELinux load path references. Add `check_k3s_selinux_staging` asserting (a) `[packages.k3s-selinux-build].enable` and its pkgs are present and actually consumed by 19-k3s-selinux.sh, and (b) `/usr/share/selinux/packages/mios/` is the same directory the load/enable step references, so a renamed staging path or a dropped build dep is caught at PR time. Static — no policy compile needed.
**Where:** `automation/38-drift-checks.sh`, `automation/19-k3s-selinux.sh`, `mios.toml` (`[packages.k3s-selinux-build]`)
**Done When:** `check_k3s_selinux_staging` is green on HEAD and RED when the staging path or a build dep changes without updating both sides; `bash -n` clean; `just drift-gate` green.
**Why:** a renamed staging directory silently ships a k3s policy module that never loads, and the failure only appears as SELinux denials on a running cluster.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-171 -- virt-v2v VM-import verb: wire the libguestfs stack already baked  (WS-RUNTIME | P3 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a shipped-but-unreachable capability gets a real verb and a real backend.
**What+How:** The container-runtime doc lists virt-v2v under "wire what we ship", and mios.toml `[packages.virt]` already bakes libguestfs-tools (mios.toml:7262), but the VM-import path is unwired — no verb, no backend. Add virt-v2v to `[packages.virt]`, ship a `usr/libexec/mios/mios-import-vm` backend wrapping virt-v2v to convert a foreign disk into the local libvirt/Quadlet store, with a `--dry-run` that prints the resolved virt-v2v plan and exits 0 without ever running a real conversion, and register a `[verbs.import-vm]` whose `cmd` dispatches to that backend. The dead-dispatch gate (flatten check 26) proves the backend exists.
**Where:** `usr/share/mios/mios.toml` (`[packages.virt]`, `[verbs.import-vm]`), `usr/libexec/mios/mios-import-vm` (new), `automation/38-drift-checks.sh`
**Done When:** `mios import-vm --dry-run` prints the resolved virt-v2v plan and exits 0; `[verbs.import-vm].cmd` resolves to the real backend with flatten check 26 green (no dead dispatch); `bash -n` clean; `just drift-gate` green.
**Why:** operators migrating an existing VM have to hand-run virt-v2v outside MiOS today, so the import bypasses the Quadlet store and the SSOT entirely.
**Dep:** none
**Who:** you (bash + verb-backend + drift-gate).

## AGY-172 -- Make the generate-names-registry Rust port FUNCTIONAL and parity-gated (finish AGY-115)  (WS-LANG | P1 | L) **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- a crate claimed as ported actually replaces its Python twin, proven byte-for-byte.
**What+How:** AGY-115 was marked done, but `tools/native/generate-names-registry/src/main.rs` is a 3-line stub that only prints a banner, and check-30 (`automation/38-drift-checks.sh` around line 1909) still shells out to `tools/generate-names-registry.py` — so the claimed "byte-identical Rust path" does not exist. Implement the real generator in Rust, consuming the mios-ssot-walk crate, so it emits `usr/share/mios/names.generated.txt` and `referenced_names.txt` byte-for-byte identically to the Python. Keep the Python as the degrade-open fallback (the bash gate runs the Rust binary if present, else Python). Add a `tools/native-parity.sh` harness diffing Rust stdout vs Python stdout vs the committed files.
**Where:** `tools/native/generate-names-registry/src/main.rs`, `tools/generate-names-registry.py`, `tools/native/mios-ssot-walk/src/lib.rs`, `automation/38-drift-checks.sh` (check 30), `tools/native-parity.sh` (new), `usr/share/mios/names.generated.txt`, `usr/share/mios/referenced_names.txt`
**Done When:** `cargo run -p generate-names-registry` output diffs ZERO bytes against both the Python output and the two committed registry files; the new degrade-open parity gate (logged SKIP when no cargo toolchain) is green on HEAD; check-30 stays green.
**Why:** the tree currently records a completed Rust port that is a banner-printing stub — the register lies about coverage, and anyone trusting it would ship a broken names registry.
**Dep:** AGY-115, AGY-173 (mios-ssot-walk real walk)
**Who:** you (Rust + bash).

## AGY-173 -- Complete mios-ssot-walk with the REAL walk logic and a golden parity test (finish AGY-116)  (WS-LANG | P1 | M) **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the SSOT walk is single-sourced in one compiled crate instead of duplicated in Python.
**What+How:** `tools/native/mios-ssot-walk/src/lib.rs` is only 10 lines exposing `EXCLUDED_SECTIONS=["meta","laws"]`, while the actual walk it is meant to single-source (in `tools/check-resolver-twin.py`) uses `EXCLUDED_SECTIONS={containers,verbs,recipes,packages,dotfiles,btop,theme,install_phases,messages}` PLUS WALK_MOSTLY_DEAD / WALK_EMIT_KEEP filtering and recursive prefix-joined key emission. Port that full walk — recursive dict traversal, prefix join, MOSTLY_DEAD/EMIT_KEEP filter — into the crate with `#[cfg(test)]` unit tests over a fixture mios.toml, and have the Rust names generator consume it so the walk exists in exactly one place.
**Where:** `tools/native/mios-ssot-walk/src/lib.rs`, `tools/check-resolver-twin.py`, `tools/native/generate-names-registry/src/main.rs`, new fixture under `tools/native/mios-ssot-walk/tests/`
**Done When:** a cargo unit test asserts the crate reproduces check-resolver-twin.py's emitted key set on the committed mios.toml (equality against a golden fixture); the resolver-twin gate (45) and the names gate (30) are both green through the single-sourced walk.
**Why:** the crate advertises a walk it does not implement, so any consumer wiring to it would silently emit a wrong key set while both gates kept reporting green off the Python path.
**Dep:** AGY-116
**Who:** you (Rust).

## AGY-174 -- Native-tier cargo test + fmt gate, offline and degrade-open (currently ZERO native tests)  (WS-LANG | P1 | M) **[DONE]**
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the Rust plane gets the shellcheck-equivalent it never had.
**What+How:** There is not a single `#[cfg(test)]` block in any `tools/native` crate, and no cargo build/test/fmt/clippy invocation anywhere in `automation/`, `.github/` or `.forgejo/` — `mios-version-check` alone has 121 lines of real logic with zero coverage. Add unit tests to each functional crate (version-check TOML scan, ssot-walk, names-registry), a `tools/native-test.sh` runner, and a drift-check that runs `cargo test --offline` plus `cargo fmt --check` on the portable crates, degrade-open with a logged SKIP when cargo is absent. Wire the check into 38-drift-checks.sh and mirror a CI job in both workflow directories.
**Where:** `tools/native-test.sh` (new), `tools/native/*/src/*.rs` (add `#[cfg(test)]` modules), `automation/38-drift-checks.sh`, `.github/workflows/`, `.forgejo/workflows/`
**Done When:** `cargo test --offline` is green for the portable crates and `cargo fmt --check` is clean; the new gate runs both and SKIPs cleanly with a logged message when no toolchain is present; a negative test (an intentionally failing assert plus an unformatted file) proves the gate trips; `just drift-gate` green on HEAD.
**Why:** the compiled tier that is supposed to replace audited bash currently ships with no tests and no CI at all, so every Rust port is trusted on inspection alone.
**Dep:** AGY-172, AGY-173 (crates under test)
**Who:** you (Rust + bash).

## AGY-175 -- cfg-gate the Windows-only mios-wallpaperd crate so `cargo build --workspace` compiles on the Linux CI runner  (WS-LANG | P1 | M) **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- a single Cargo workspace that the Containerfile can build on a stock Linux runner, which every later crate port depends on.
**What+How:** `tools/native/mios-wallpaperd/Cargo.toml` declares `wry`, `tao`, `windows-service` and `windows` as unconditional dependencies of an unconditional workspace member, so a Linux `cargo build --workspace` / `cargo test` fails outright and takes the AGY-125 test gate and the AGY-128 build stage down with it. Move those four crates under a `[target.'cfg(windows)'.dependencies]` table and put `#![cfg(windows)]` on the `[[bin]]` and its `main`/`host`/`workerw`/`guiwatch` modules so off-Windows the crate resolves to an empty artifact. Add a drift-check that parses every `tools/native/*/Cargo.toml` and fails when a Windows-only crate appears outside a `cfg(windows)` target table.
**Where:** `tools/native/mios-wallpaperd/Cargo.toml`, `tools/native/mios-wallpaperd/src/{main,host,workerw,guiwatch}.rs`, `automation/38-drift-checks.sh`
**Done When:** `cargo build --workspace --offline` succeeds on a Linux host; the new Cargo.toml-parsing check is green on HEAD; a negative-test that moves a windows dep back into `[dependencies]` trips it.
**Why:** Until this lands the Rust workspace cannot be built or tested anywhere except a Windows box, so the compiled tier has no CI at all and no native binary can be baked into the image.
**Dep:** blocks AGY-125 (cargo test gate) and AGY-128 (Containerfile build stage)
**Who:** you (Rust).

## AGY-176 -- Make generate-cargo-manifests.py enumerate crate dirs and add a generator-clean gate over tools/native/Cargo.toml  (WS-LANG | P2 | S) **[DONE]**
**Goal:** E-01 Compiled native tier -- the workspace manifest becomes a projection of the filesystem rather than a hand-maintained list, so new crates cannot fall out of the build.
**What+How:** `tools/generate-cargo-manifests.py` hardcodes `members=[...]` and emits the `[workspace.package]` version/edition block, while the existing gate near `automation/38-drift-checks.sh:3427` only compares the version STRING -- so a fifth crate could be added and silently never join the workspace. Change the generator to ENUMERATE member directories (any dir under `tools/native/` containing a `Cargo.toml`), keep projecting the version via workspace inheritance, and add a drift-check that regenerates into a temp file and diffs it against the committed `tools/native/Cargo.toml`.
**Where:** `tools/generate-cargo-manifests.py`, `tools/native/Cargo.toml`, `automation/38-drift-checks.sh`
**Done When:** generator output equals the committed `tools/native/Cargo.toml` byte-for-byte; dropping in a new crate dir and regenerating updates `members` deterministically; the new "cargo-manifest generator-clean" check is green on HEAD and a hand-edit of the members list reds it.
**Why:** A crate that is on disk but missing from `members` is never compiled, never tested and never shipped -- and today nothing would report it.
**Dep:** none
**Who:** you (Python + bash).

## AGY-177 -- Add a cached Rust builder stage to the Containerfile plus COPY lines for every portable bin, gated by a bins-installed check  (WS-LANG | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- native binaries actually reach `/usr/libexec/mios/` in the baked image, so the runtime stops falling back to Python for gates that already have a compiled implementation.
**What+How:** grep of `Containerfile*` for cargo/native/version-check/wallpaperd returns NOTHING: the portable crates are compiled nowhere and copied nowhere, so ROADMAP LANG-01's "early Rust stage + COPY to /usr/libexec/mios/" is unmet. Add a cached Rust builder stage that statically builds the portable crates for `x86_64-unknown-linux-musl`, add one `COPY --from` per binary into `/usr/libexec/mios/`, and add a drift-check that enumerates non-Windows `[[bin]]`/binary crates from `cargo metadata` and asserts each has a matching COPY line. Validation is static Containerfile + cargo-metadata parsing -- no image build required.
**Where:** `Containerfile` (new build stage + COPY lines), `tools/native/Cargo.toml`, `automation/38-drift-checks.sh`
**Done When:** the Containerfile carries a pinned Rust build stage and a COPY for every portable workspace bin; the new "native-bins-installed" check (degrade-open on parse failure) is green on HEAD; adding a bin crate with no COPY line trips it.
**Why:** Compiled tools that never enter the image are dead code -- every runtime gate keeps using the fragile Python fallback the port was meant to retire.
**Dep:** AGY-175 (Linux workspace build must succeed first)
**Who:** you (bash + Containerfile).

## AGY-178 -- Harden mios-version-check's hand-rolled TOML scan against subsections, comments and prefix keys, with regression tests  (WS-LANG | P2 | S) **[DONE]**
**Goal:** E-01 Compiled native tier -- the compiled version oracle agrees with the shell gate on malformed input, so the version SSOT check cannot be fooled.
**What+How:** `tools/native/mios-version-check/src/main.rs` finds `[meta].mios_version` with a naive line loop (`starts_with('[')` toggles the section, `starts_with("mios_version")` matches the key). That mis-reads a `[meta.sub]` subsection header as leaving `meta`, accepts a commented-out `mios_version` line, prefix-matches a sibling like `mios_version_extra`, and never strips inline comments from the value. Rewrite it as exact-key plus section-path aware parsing, staying zero-dependency stdlib, and add `#[cfg(test)]` cases for subsection headers, commented lines, prefix-key false matches, and quoted / inline-comment values.
**Where:** `tools/native/mios-version-check/src/main.rs`, `automation/38-drift-checks.sh` (`check_version_ssot`)
**Done When:** unit tests cover all four malformed-input classes and pass; the binary's verdict still matches the bash `check_version_ssot` on HEAD; the AGY-125 cargo-test gate runs them; drift-gate green.
**Why:** A version checker that reads the wrong line will bless a tree whose version SSOT has actually drifted -- the exact 37x-hardcoded-version class this tool exists to prevent.
**Dep:** AGY-125 (cargo test gate)
**Who:** you (Rust).

## AGY-179 -- New mios-check-index crate: validate the drift file's check registry and emit drift-checks.index.json  (WS-LANG | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- one global numbering system whose integrity is machine-verified instead of eyeballed.
**What+How:** `automation/38-drift-checks.sh` is 4100 lines of hand-numbered checks ((30) names registry, (45), (63) target_languages, 64/65/66, ...) and nothing verifies that IDs are unique, un-gapped, uniquely named, or reachable. Add a pure-stdlib Rust crate `mios-check-index` that parses the check IDs and names out of the drift file, asserts unique + monotonic numbering and unique names, and emits a committed registry at `usr/share/mios/drift-checks.index.json`; wire a degrade-open meta-gate over it. Scope note: this VALIDATES the registry -- it does not run checks, which is AGY-65's `mios-drift` dispatcher.
**Where:** new `tools/native/mios-check-index/` crate, `tools/native/Cargo.toml` (add member), `usr/share/mios/drift-checks.index.json`, `automation/38-drift-checks.sh`
**Done When:** `cargo run -p mios-check-index` emits the index deterministically and exits non-zero on a duplicate or gapped number; regenerate == committed; a negative-test injecting a duplicate check number trips the meta-gate; drift-gate green on HEAD.
**Why:** Duplicate or skipped check numbers make the gate's own coverage unknowable -- a check can be shadowed or unreachable and the suite still reports green.
**Dep:** distinct from AGY-65 (mios-drift)
**Who:** you (Rust + bash).

## AGY-180 -- Port ascii-sweep.py's substitution core to a Rust mios-ascii-sweep with a byte-identical golden parity gate  (WS-LANG | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- ADR-0011's "correctness-critical pure logic trapped in fragile Python" case retired behind proven byte-parity, the strangler-fig pattern the whole epic runs on.
**What+How:** `tools/ascii-sweep.py` (294 lines) is a pure typographic-Unicode-to-ASCII scrubber with a fixed substitution table, run across ALL MiOS-owned text (docs, shell, TOML, JSON), and its output is verifiable offline. Port the substitution map, the file walk, and both `--dry-run` / `--apply` modes into a Rust crate; keep the Python as the degrade-open fallback; add a golden test whose fixture contains every mapped codepoint and which asserts the Rust bytes equal the Python bytes.
**Where:** new `tools/native/mios-ascii-sweep/` crate, `tools/ascii-sweep.py`, golden fixture under `tools/native/mios-ascii-sweep/tests/`, `automation/38-drift-checks.sh`
**Done When:** `mios-ascii-sweep --dry-run` reports identical per-file change counts and `--apply` produces byte-identical output versus `tools/ascii-sweep.py` on the golden fixture across every mapped codepoint; Python retained as fallback; drift-gate green.
**Why:** A text scrubber that runs over the whole tree with no parity oracle can silently mangle content on port; without the golden fixture the Python can never be retired.
**Dep:** none
**Who:** you (Rust).

## AGY-181 -- mios-verb-manifest Rust generator plus a structural set-e / no-eval gate over the ~250 bash verb backends  (WS-LANG | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- the verb library gets a machine-readable manifest, and TD-1's eval-on-agent-args ban becomes structural rather than a one-time sweep.
**What+How:** `usr/libexec/mios/` holds ~250 bash verb backends with no manifest, and ROADMAP LANG-01 names the verb dispatcher and its eval surface as a port target. Instead of porting 250 scripts at once, add a Rust tool that enumerates the backends, extracts each AI-hint header/synopsis, and emits canonical `usr/share/mios/verbs.manifest.json` (name, path, has `set -euo pipefail`, has agent-arg eval). Add a drift-check that regenerates and diffs the manifest and FAILS on any verb missing `set -euo pipefail` or containing an agent-arg eval.
**Where:** new `tools/native/mios-verb-manifest/` crate, `tools/native/Cargo.toml` (add member), `usr/share/mios/verbs.manifest.json`, `usr/libexec/mios/*`, `automation/38-drift-checks.sh`
**Done When:** `cargo run -p mios-verb-manifest` emits the manifest deterministically (regenerate == committed); the gate is green on HEAD and fails on any unguarded verb or agent-arg eval; a negative-test verb containing `eval $1` and no `set -e` trips it; drift-gate green.
**Why:** AGY-67/TD-1 removed the eval injection surface once, but nothing stops the next verb from reintroducing it -- and no consumer can enumerate the verb fleet without shelling out.
**Dep:** AGY-67 / TD-1 (eval sweep)
**Who:** you (Rust + bash).

## AGY-182 -- Normalize HNSW index params and emb_version widths across the emb schema, gated by check_vector_schema_hygiene  (WS-VECTOR | P1 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- pgvector recall behaves the same for every embedded table instead of half the fleet riding silent defaults.
**What+How:** `schema-init.sql` is inconsistent: the 4 original vector indexes (knowledge:42, agent_memory:72, mios_rag:452, config_kv:567) carry the canonical `WITH (m = 16, ef_construction = 64)`, but the ~19 newer HNSW indexes added by AGY-7/11/18/22 (skill/verb/tool_call/directory_entry:635-638, package_set..preset:654-776, event/session:783-788, account_preference:819) omit the params, contradicting the AGY-3/7/11 "mirror the exact DDL" rule. `mios_identity.account_preferences.emb` (1139) has NO index at all and an outlier `emb_version varchar(32)` against varchar(64) everywhere else. Add the explicit params to every param-less HNSW index (idempotent -- `CREATE INDEX IF NOT EXISTS` makes it a no-op on live DBs but pins intent), unify the width, and add `check_vector_schema_hygiene` at the next free number (63+) that greps every `USING hnsw` line for the canonical params and asserts every `emb vector(768)` column has sibling `emb_model` + `emb_version`.
**Where:** `usr/share/mios/postgres/schema-init.sql`, `automation/38-drift-checks.sh`
**Done When:** `psql -f schema-init.sql` re-applies idempotently; the new check is GREEN on HEAD and RED on a fixture where an HNSW index loses its params or an emb column loses provenance; `just drift-gate` passes.
**Why:** Indexes on pgvector defaults give measurably worse recall than the canonical tuning, and an emb column with no `emb_model`/`emb_version` cannot be invalidated when the embedding model changes.
**Dep:** AGY-123 (this task's origin id); AGY-3/7/11/18/22 schema additions
**Who:** you (SQL + bash/awk drift-gate).

## AGY-183 -- Extend embed-backfill to config_kv and account_preference plus a schema-to-backfill binding gate  (WS-VECTOR | P1 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the embed-backfill worker actually fills every declared vector column, so no `emb` column is decorative.
**What+How:** `embed_backfill.py`'s `PK_MAP` (lines 95-102) covers only {skill, verb, tool_call, directory_entry, event, session}, yet ~13 other tables declare `emb vector(768)` plus an HNSW index with nothing populating them -- most valuably `config_kv` (operators cannot semantic-search settings) and `account_preference`. Extend `PK_MAP` and `get_text_projection()` (line 113) to cover `config_kv` (projection: `scope.key` + description + value) and `account_preference` (projection: key + value), reusing the worker's existing idempotent (`emb IS NULL` / stale `emb_version`), provenance-stamped, 3x-retry, fail-open pattern. Then add `check_backfill_coverage` at the next free number: statically parse every `emb vector(768)` table out of `schema-init.sql` and assert each is either in `PK_MAP` or on an explicit commented `_BACKFILL_EXEMPT` allow-list.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py`, `usr/lib/mios/agent-pipe/test_mios_embed_backfill.py`, `automation/38-drift-checks.sh`
**Done When:** backfill populates `config_kv.emb` / `account_preference.emb` with provenance (verified against a monkeypatched pg + embed client); a second run is a near-noop; the coverage gate is GREEN on HEAD and RED when an emb column is added with no PK_MAP or exempt entry; `just drift-gate` passes.
**Why:** Every unfilled `emb` column is index storage and schema surface paying for a feature that returns nothing -- and today a new dead vector column can be added with zero signal.
**Dep:** AGY-124 (this task's origin id); enables AGY-188
**Who:** you (Python + drift-gate).

## AGY-184 -- Reconcile the duplicate account-preference tables and fix allocate_gid()'s shared-sequence UPG collision  (WS-VECTOR | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- one canonical account surface in the agent datastore, and UID/GID allocation that produces a valid user-private group.
**What+How:** Two competing preference tables exist: `account_preference` (public, AGY-22 line 809 -- layer-based, HNSW-indexed, carries `emb_model` + `emb_version`, and the only one read, by `materialize-user-config.py:87`) and the dead `mios_identity.account_preferences` (T-246, line 1134 -- UUID/pref_key, no reader or writer anywhere, no HNSW index, no `emb_model`, mismatched `emb_version varchar(32)`). Reconcile to ONE surface: either drop the T-246 table or replace it with a compatibility VIEW onto `account_preference`, and record which and why. Separately, `allocate_gid()` (lines 803-807) draws from the SAME `uid_alloc` sequence as `allocate_uid()`, so a fresh user gets uid=1000 / gid=1001 and user-private-group is impossible -- give gid its own sequence or make `allocate_gid` mirror the uid. Add a drift-check asserting exactly one preferences table and no shared alloc sequence.
**Where:** `usr/share/mios/postgres/schema-init.sql`, `usr/libexec/mios/materialize-user-config.py`, `usr/libexec/mios/seed-db-config.py`, `automation/38-drift-checks.sh`, `usr/lib/mios/agent-pipe/test_mios_account_sync.py`
**Done When:** the schema applies idempotently; only one preference surface remains and the new gate goes RED if a second `*account_preference*` table reappears; `allocate_uid`/`allocate_gid` yield a UPG-consistent pair asserted in a test; `materialize-user-config` still round-trips a seeded pref; `just drift-gate` passes.
**Why:** Two tables named for the same fact is a split-brain waiting for the next writer to pick the wrong one, and uid != gid breaks the user-private-group model every projected Linux account assumes.
**Dep:** AGY-125 (this task's origin id); AGY-22 (account_preference), T-246 (mios_identity)
**Who:** you (SQL + Python).

## AGY-185 -- Close the verb `i18n` round-trip gap in the DB-to-TOML materializer and widen check 31's compared field set  (WS-VECTOR | P2 | S) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the DB-to-TOML materializer is a LOSSLESS projection of the SSOT, which is the whole premise of the DB-driven config plane.
**What+How:** The `verb` table carries an `i18n jsonb` column (schema line 586 region) that `seed-db-config.py` can populate from mios.toml, but `materialize-config-toml.py`'s verb SELECT (lines 163-171) never reads or emits it, so localized verb text is LOST on any DB-to-TOML materialize. Check 31 (`drift_projection`, `38-drift-checks.sh:1962`) cannot catch it because it only diffs fields both sides already handle. Add `i18n` to the materializer SELECT and emit, ensure the seeder re-ingests it, verify the `aliases` -> `hidden_aliases` remap (materialize line 205) survives the trip, then extend check 31's compared field set to include `i18n` and assert `hidden_aliases` parity.
**Where:** `usr/libexec/mios/materialize-config-toml.py`, `usr/libexec/mios/seed-db-config.py`, `automation/38-drift-checks.sh`
**Done When:** a verb seeded with an `i18n` table survives TOML -> DB -> TOML byte-for-byte (diff clean); check 31 is GREEN on HEAD and RED when `i18n` is dropped from the materializer; `just drift-gate` passes.
**Why:** A materializer that silently drops a column means any operator who round-trips config through the DB loses their localized verb text with no error and no diff.
**Dep:** AGY-126 (this task's origin id)
**Who:** you (Python + SQL).

## AGY-186 -- Add config-restore.py, the V5 point-in-time rollback writer over the config_event log  (WS-VECTOR | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- config time-travel gains its write half, so an operator can revert a setting, not merely read that it changed.
**What+How:** AGY-24 shipped the append-only `config_event` audit plus a READ-ONLY `config-history.py`; there is no writer, so history is visible but not revertible. Add `usr/libexec/mios/config-restore.py` which, given a `scope.key` plus a target timestamp or event id, reconstructs the as-of value from the `config_event` log (schema line 823) and re-applies it to `config_kv`. Because the write re-fires the existing `config_kv_audit_trigger`, the revert is itself a new event -- proper event-sourcing, no history rewrite. Default `--dry-run` prints the exact UPDATE; `--apply` executes. Fail-open: DB down means a log line and a non-zero exit, never a stack trace. Reuse the shared pg-config helper from AGY-132 if landed, else the local one.
**Where:** `usr/libexec/mios/config-restore.py`, `usr/libexec/mios/config-history.py`, `usr/lib/mios/agent-pipe/test_mios_config_audit.py` (or a new `test_mios_config_restore.py`)
**Done When:** against a monkeypatched pg seeded with an old -> new -> newer event chain, `--dry-run` emits the correct as-of value and UPDATE, and `--apply` reverts the `config_kv` row to the historical value; a unit test asserts the reconstructed value for a chosen timestamp; `bash -n` / py-compile clean; `just drift-gate` passes.
**Why:** Without a writer, a bad config change is diagnosable but not undoable -- the operator must hand-craft SQL against the live config plane to recover.
**Dep:** AGY-127 (this task's origin id); AGY-24 (config_event + config-history.py); optional AGY-132 (shared pg-config)
**Who:** you (Python).

## AGY-187 -- Port the vector-schema linter to a native Rust `mios-schema-lint` with a shell fallback of identical verdict  (WS-LANG | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- Law 14's "native tooling and validation is Rust" applied to schema linting, following the AGY-115/116 generator-plus-fallback pattern.
**What+How:** Reimplement AGY-123's static schema invariants as a compiled tool under `tools/native/`: parse `usr/share/mios/postgres/schema-init.sql` as pure text (NO database) and assert (a) every `USING hnsw` index carries the canonical `WITH (m=16, ef_construction=64)`, (b) every `emb vector(768)` column has sibling `emb_model` + `emb_version` of the canonical width, (c) every emb column has a matching HNSW index, (d) exactly one account-preference table exists. Wire it into the drift-gate so the check invokes the Rust binary when present and falls back to the AGY-123 shell implementation when the toolchain is absent, fail-open and byte-identical in verdict.
**Where:** new crate `tools/native/mios-schema-lint/`, `automation/38-drift-checks.sh`, `tools/native/mios-schema-lint/tests/`
**Done When:** `cargo test` in the crate is green (the current schema as golden fixture PASSES; a mutated fixture per invariant FAILS); the drift-gate's PASS/FAIL verdict is identical under the Rust binary and under the shell fallback; `just drift-gate` passes.
**Why:** The shell version of these invariants is grep-fragile and, if it diverges from the Rust one, the gate's verdict depends on whether a toolchain happened to be installed.
**Dep:** AGY-128 (this task's origin id); AGY-182/AGY-123 (invariants), AGY-184 (single preference table), pattern from AGY-115/116
**Who:** you (Rust).

## AGY-188 -- config_kv hybrid semantic-search read-path fusing pgvector and FTS through the AGY-26 RRF helper  (WS-VECTOR | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the "vectors find, SQL reads" contract applied to configuration, cashing in the AGY-183 backfill.
**What+How:** Once `config_kv.emb` is filled, expose it: add a read-path helper (a peer of `mios_db_config.py`, or a function on it) answering "which setting does X" by running a dense pgvector `emb <=> query` search AND a Postgres FTS / `ts_rank` lexical search over `config_kv`, fusing both with the Reciprocal Rank Fusion helper AGY-26 already introduced for tool selection, and returning the fused top-k `scope.key` hits. Gate it behind `[ai] config_search` (default false) and fail open to a plain `ILIKE` scan when pgvector or embeddings are down. The search returns KEYS only -- the value still comes from the typed column via the resolver.
**Where:** `usr/lib/mios/mios_db_config.py` (or new `usr/lib/mios/agent-pipe/mios_pipe/memory/config_search.py`), `usr/share/mios/mios.toml` (`[ai] config_search=false`), `usr/lib/mios/agent-pipe/test_mios_db_config.py`
**Done When:** with the flag on the helper returns fused dense + lexical hits (verified against a monkeypatched pg returning ranked rows); with pgvector unavailable it degrades to ILIKE without crashing; with the flag off nothing changes; a unit test covers fusion and fail-open; `just drift-gate` passes.
**Why:** MiOS carries thousands of SSOT keys and no way to find the right one by intent; without this the embeddings AGY-183 fills have no consumer.
**Dep:** AGY-129 (this task's origin id); AGY-183/AGY-124 (backfill), AGY-26 (RRF helper)
**Who:** you (Python + SQL).

## AGY-189 -- Negative-tests proving round-trip gates 31 (config) and 32 (build-catalog) can actually fail  (WS-TEST | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- every drift-gate check has a test that proves it can FAIL, so a green gate means something.
**What+How:** Check 31 (`drift_projection`, `38-drift-checks.sh:1962`) and check 32 (`drift_build_catalog:2371`) are the load-bearing "materialize round-trip is lossless" gates, but AGY-25 finding #9 already proved check 32's diff was silently INCOMPLETE -- it omitted `enable`, `layer`, `base_image_ref` and `section` -- meaning a genuine round-trip regression passed green. Add a negative-test harness mirroring AGY-54/120 that, per gate, injects a controlled field-drop into the materializer output (or monkeypatches materialize) and asserts the gate flips RED, and separately asserts both gates are GREEN on unmodified HEAD.
**Where:** new `usr/lib/mios/agent-pipe/test_mios_drift_roundtrip.py`, `automation/38-drift-checks.sh` (only if a test hook is needed), `usr/libexec/mios/materialize-config-toml.py` / `materialize-build-ctx.py` (test fixtures only)
**Done When:** the negative test flips BOTH check 31 and check 32 to RED under an injected field-drop and confirms both GREEN on HEAD; the test is wired into the suite so the check-11 sibling rule is satisfied; `just drift-gate` passes.
**Why:** These two gates already shipped blind once; without sensitivity tests the next dropped column passes green and the losslessness claim behind the DB-driven config plane is unfounded.
**Dep:** AGY-130 (this task's origin id); AGY-25 finding #9, pattern from AGY-54/120
**Who:** you (Python + bash).

## AGY-190 -- Capture `actor` on the verb / domain_verb audit triggers and surface it in config-history  (WS-VECTOR | P2 | S) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the config audit log attributes every change, not just the config_kv half.
**What+How:** `log_config_kv_change()` records `actor` (owner_user) on every `config_event` (schema lines 909-922), but `log_verb_change()` / `log_domain_verb_change()` (lines 951-993) insert only `(scope, key, new_value, source)` with NO actor, so verb history has a NULL actor and nobody can be attributed to a verb change. There is no `NEW.owner` column on `verb`, so read the actor from a session GUC the writer sets -- `current_setting('mios.actor', true)` defaulting to `session_user` -- and write it into the `config_event.actor` column in ALL THREE branches of both triggers; have `seed-db-config.py` set that GUC when it applies changes. `config-history.py` already selects `actor`, so the value surfaces once populated.
**Where:** `usr/share/mios/postgres/schema-init.sql` (`log_verb_change` / `log_domain_verb_change`), `usr/libexec/mios/seed-db-config.py`, `usr/libexec/mios/config-history.py`, `usr/lib/mios/agent-pipe/test_mios_config_audit.py`
**Done When:** a simulated verb INSERT/UPDATE/DELETE records a non-NULL actor in `config_event` (asserted in the audit test against a monkeypatched/psql fixture); `config-history.py` shows the actor for a verb change; the schema re-applies idempotently; `just drift-gate` passes.
**Why:** A verb -- an executable capability -- can be changed today with no record of who changed it, which makes the audit log unusable for the one class of change that most needs attribution.
**Dep:** AGY-131 (this task's origin id); AGY-24 (config_event)
**Who:** you (SQL + Python).

## AGY-191 -- Single-source the six copy-pasted get_pg_config() helpers behind mios_pg_conn.py, with a duplicate-definition gate  (WS-VECTOR | P3 | S) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- one connection contract for the DB-driven config plane, so its tools cannot split-brain on where the database is.
**What+How:** An identical `get_pg_config()` is copy-pasted into SIX files -- `config-history.py`, `materialize-build-ctx.py`, `materialize-config-toml.py`, `materialize-user-config.py`, `seed-db-config.py` and `mios_db_config.py` -- each free to drift on host/port/user/db defaults. Extract ONE shared helper (a small `usr/libexec/mios/mios_pg_conn.py` importable by both the libexec scripts and the agent-pipe module, or reuse the existing `mios_pg` seam) and repoint all six callers, preserving the exact env-var contract (`MIOS_PG_HOST` / `MIOS_PORT_PGVECTOR` / `MIOS_PG_USER` / `MIOS_PG_PASS` / `MIOS_PG_DB`). Add a drift-check that fails if a second literal `def get_pg_config` body reappears outside the shared module.
**Where:** new `usr/libexec/mios/mios_pg_conn.py`, `usr/libexec/mios/{config-history,materialize-build-ctx,materialize-config-toml,materialize-user-config,seed-db-config}.py`, `usr/lib/mios/mios_db_config.py`, `automation/38-drift-checks.sh`
**Done When:** all six callers import the one helper and py-compile clean; grep shows exactly one `def get_pg_config` definition; the new gate is GREEN on HEAD and RED when a duplicate definition is reintroduced; `just drift-gate` passes.
**Why:** Six copies means a port or credential change fixed in five places and missed in the sixth, producing a tool that silently talks to the wrong database.
**Dep:** AGY-132 (this task's origin id); consumed by AGY-186
**Who:** you (Python + drift-gate).

## AGY-192 -- Harden redact.py: add the high-value credential patterns and recurse structured payloads  (WS-SEC | P1 | M) **[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- specifically its secret/PII redaction clause on every persist and every A2A federate.
**What+How:** `usr/lib/mios/agent-pipe/mios_pipe/redact.py` (49 lines) is the AGY-8 persist + federation sanitizer, but it only matches `sk-` / `sk-ant-` / `hf_` / `ghp_` / `Bearer` / email / `MIOS_*=`, and `redact()` takes a FLAT string while the federation seam (`mios_pipe/federation/a2a.py`) and memory persist both serialize dicts and JSON. Add PEM private-key blocks (`-----BEGIN (RSA|EC|OPENSSH|PRIVATE) KEY-----`...`-----END...`), JWTs (`eyJ[A-Za-z0-9_-]+\.eyJ...\....`), GitHub fine-grained (`github_pat_`), GitLab (`glpat-`), Slack (`xox[baprs]-`), AWS keys (`AKIA[0-9A-Z]{16}`), and generic `(password|passwd|token|secret|api[_-]?key)\s*[=:]\s*<val>` assignments. Add `redact_obj(obj)` that walks dict/list recursively, redacting string leaves and masking any dict KEY matching the secret-name regex, returning `(obj, is_redacted)`; wire it at the JSON persist and federate call sites. Do NOT redo the AGY-32/33 `config_event` SQL-trigger work -- this is the Python `redact()` seam only.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/redact.py`, `usr/lib/mios/agent-pipe/test_mios_redact.py`, call sites in `usr/lib/mios/agent-pipe/mios_pipe/federation/a2a.py` and `mios_pipe/memory/*`
**Done When:** `test_mios_redact.py` gains a case per new pattern (PEM/JWT/glpat/xox/AKIA/generic assignment), a nested-dict case where a secret under a benign scope is masked, and a negative case leaving benign text unchanged; all redact tests green under `just drift-gate`.
**Why:** A private key or JWT inside a nested payload passes the current flat-string redactor untouched and is persisted to the memory store and shipped to every federated A2A peer.
**Dep:** extends AGY-8; distinct from AGY-32/33
**Who:** you (Python).

## AGY-193 -- Project systemd userdb `.user` records from SSOT accounts, with a regenerate-and-diff gate  (WS-USERDB | P1 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall (WS-ACCT) -- the Linux account model becomes an SSOT projection instead of an imperative daemon editing /etc/passwd.
**What+How:** AGY-95 P2 named `/usr/lib/userdb/<name>.user` (systemd userdb, with nss-systemd synthesizing passwd/group) the PRIMARY Linux account mechanism, but it does not exist: `automation/17-accounts-db.sh` only installs the runtime `mios-account-sync` daemon, which imperatively edits `/etc/passwd`, and there is no `/usr/lib/userdb` in the tree at all. Add a generator projecting the SSOT identity rows -- `[identity]` (username / fullname / shell / groups / hostname) plus every `[[autounattend.accounts]]` -- into one JSON drop-in per account (userName, uid slot, realName, homeDirectory, shell, memberOf) following the systemd userdb record schema. Per Law 11 store NO password hash in the `.user` file: it references `/etc/mios/secrets.env` / shadow and carries only non-secret identity. Ship it as a static, always-on projection independent of `[accounts].db_backed`, which gates only the live sync daemon.
**Where:** new `tools/generate-userdb.py`, generated `usr/lib/userdb/*.user`, source `usr/share/mios/mios.toml` (`[identity]` + `[[autounattend.accounts]]`), gate in `automation/38-drift-checks.sh`, install wire in `automation/17-accounts-db.sh` (or a new `18-userdb.sh`)
**Done When:** a new regenerate-and-diff drift-check (Law 8 SSOT-PROJECTION) is green; tampering a committed `*.user` reds it; a negative-test proves an added `[[autounattend.accounts]]` row projects a new record and that no password field appears in any `.user`; `just drift-gate` green.
**Why:** Today accounts exist only as imperative runtime edits to `/etc/passwd` on a `/usr`-read-only image -- nothing declarative, nothing gated, and nothing that survives a `bootc rollback` predictably.
**Dep:** AGY-95 (named the mechanism); Law 11 (SECRETS-NEVER-IN-ENV)
**Who:** you (Python + bash + drift-gate).

## AGY-194 -- caller-keys.json schema, example fixture, fail-closed validator and gate for the inbound /v1 auth boundary  (WS-SEC | P1 | M) **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the security boundary in front of the ONE OpenAI /v1 door is a validated, gated artifact rather than an unchecked file.
**What+How:** `[security].api_caller_keys_path=/etc/mios/ai/v1/caller-keys.json` is consumed by `server.py` for the FED-G1 inbound auth gate and by `principal_bind_mode` to bind an authenticated key to an `account` (owner -> RLS), but there is no schema, generator or validator -- a malformed, duplicate-id or unbound-account file silently degrades the auth boundary. Add a validator over the schema `list of {id, key, account?/owner?, scope?}` that rejects duplicate ids, empty keys, and any `account` naming no seeded account; have `server.py` call it at load and FAIL-CLOSE when `api_require_auth=true` and the file is present-but-malformed, degrading open only when the file is ABSENT so today's single-user default is preserved. Ship a committed `caller-keys.example.json` beside the existing `etc/mios/ai/v1/a2a-peers.json` as the gated fixture.
**Where:** new `usr/libexec/mios/mios-caller-keys-validate` (or a `mios_pipe/access` module + import), new `etc/mios/ai/v1/caller-keys.example.json`, auth path in `usr/lib/mios/agent-pipe/server.py`, gate + negative-test in `automation/38-drift-checks.sh`, unit test `usr/lib/mios/agent-pipe/test_mios_caller_keys.py`
**Done When:** the example file validates green in a new drift-check; a unit test proves malformed / duplicate-id / unbound-account inputs are rejected and that load fail-closes under `api_require_auth=true`; `just drift-gate` green.
**Why:** A typo in caller-keys.json today can quietly disable inbound authentication or bind a caller to the wrong RLS owner, exposing another account's data through the public /v1 surface.
**Dep:** FED-G1 inbound auth gate in server.py
**Who:** you (Python + drift-gate).

## AGY-195 -- Rust port of the egress-firewall generator, byte-identical under check 14, plus the generator doc-ref fix  (WS-LANG | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- another security-relevant generator moves to the compiled tier with an existing regenerate-and-diff gate as the objective parity oracle.
**What+How:** Per Law 14 (native tooling and validation goes to Rust), port `tools/generate-egress-firewall.py` (109 lines: reads `[security.egress]` mode + allow, resolves the agent `User=` from `mios-agent-pipe.service`, emits the uid-scoped nftables ruleset) into the `tools/native/` workspace as a native binary producing byte-identical output for all three modes (off / audit / enforce) and the always-allowed set (loopback + tailnet 100.64.0.0/10 + WSL 172.16.0.0/12 + the allow CIDRs, v4/v6 split). The existing `check_egress_firewall` (check 14, `automation/38-drift-checks.sh:775`) is the oracle. Also fix the Law-15 doc-drift: `[security.egress]` in `usr/share/mios/mios.toml` claims it is generated by `generate-egress-firewall.sh` when the real file is `.py`.
**Where:** new crate/bin under `tools/native/`, keep `tools/generate-egress-firewall.py` as reference twin or thin shim, `usr/share/mios/security/egress.nft` (bytes unchanged), doc-ref fix in `usr/share/mios/mios.toml`, check 14 in `automation/38-drift-checks.sh`
**Done When:** `cargo build` green; the native binary regenerates `usr/share/mios/security/egress.nft` byte-for-byte for off/audit/enforce (`diff -q` clean) so check 14 stays green; the `.sh` -> `.py` doc-ref is corrected; `just drift-gate` green.
**Why:** The firewall that scopes the agent's egress is generated by an unverified Python script, and its own SSOT comment points at a filename that does not exist -- so the next maintainer edits the wrong file.
**Dep:** none
**Who:** you (Rust + drift-gate).

## AGY-196 -- Static SECRETS-NEVER-IN-ENV (Law 11) source-tree drift-gate with a planted-secret negative test  (WS-SEC | P1 | S) **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- Law 11 gains a source-tree enforcing check, because a law with no check is not enforced.
**What+How:** Law 11 is enforced at BAKE by `99-postcheck` but has NO source-tree drift-check, so a secret committed into a group/world-readable env file (`install.env`, `agent-pipe.env`, `hermes/api.env` -- all referenced via `EnvironmentFile=` in `usr/lib/systemd/system/`) ships un-flagged until bake time. Add a drift-check that enumerates every committed file targeted by an `EnvironmentFile=` line, runs the `redact.py` secret-shape patterns over each, and FAILS if any carries a plaintext secret value; additionally assert any file named `secrets.env` is referenced with 0600 intent and never made world-readable via tmpfiles or sysusers. Reuse the AGY-192-hardened patterns so the two stay in lock-step.
**Where:** new check in `automation/38-drift-checks.sh`, patterns reused from `usr/lib/mios/agent-pipe/mios_pipe/redact.py`, scan targets under `etc/mios/*.env` and `install.env`, negative fixture under `tests/`
**Done When:** the new check passes on the clean tree; a negative-test planting `MIOS_PG_PASS=hunter2` into a scanned env fixture reds the gate; `just drift-gate` green.
**Why:** A committed secret is caught only at bake -- meaning it is already in git history and, on a shared tree, already pushed before anything complains.
**Dep:** AGY-192 (hardened redact patterns); Law 11, `automation/99-postcheck.sh`
**Who:** you (bash + Python + drift-gate).

## AGY-197 -- Rule-of-Two `sensitive` classification coverage and type gate over the 110 verbs  (WS-SEC | P2 | S) **[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- the CaMeL kill-chain gate keeps its data-sensitivity axis populated and correctly typed.
**What+How:** The CaMeL Rule-of-Two defense (`usr/lib/mios/agent-pipe/mios_pipe/routing/ruleof2.py`) reads axis B from `[verbs.*].sensitive`, yet only ~8 of 110 verbs carry the flag and nothing gates its coverage or its type -- so a verb that reads private or cross-tenant data can lose or typo the flag and silently drop out of the gate. Add a drift-check that (a) asserts every `sensitive` literal is a canonical bool, extending the existing non-canonical-bool check 33 at `38-drift-checks.sh:2347`, and (b) enforces a curated reads-sensitive verb set (knowledge/RAG recall, file/text_view read, agent-memory read, notes-vault read -- the tables already flagged) all carry `sensitive = true`, failing if any known-sensitive verb loses it.
**Where:** gate in `automation/38-drift-checks.sh` (extend check 33 or add a sibling), curated set sourced from `usr/share/mios/mios.toml` `[verbs.*]`, negative fixture, cross-ref `usr/lib/mios/agent-pipe/mios_pipe/routing/ruleof2.py`
**Done When:** the coverage/type check is green at HEAD; a negative-test flipping a known-sensitive verb's flag to false (or to a non-canonical bool) reds it; `just drift-gate` green.
**Why:** A dropped or mistyped `sensitive` flag silently disables the Rule-of-Two protection for exactly the verbs that read private data, and nothing today reports the hole.
**Dep:** ruleof2.py (CaMeL Rule-of-Two), check 33
**Who:** you (Python + drift-gate).

## AGY-198 -- Property-test the PDP invariant that a pruned verb can never dispatch (harden WS-A9)  (WS-SEC | P2 | S) **[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- the access decision point is proven to fail closed on both the surface and the dispatch axis.
**What+How:** The WS-A9 invariant is that a verb pruned from a caller's tool surface (`mios_policy._agent_rbac_filter` / `_user_rbac_filter`) can NEVER still dispatch (`mios_policy._dispatch_pdp_reason`), because both route through the single `mios_pdp.decide()` / `resolve_ceiling()` core in `usr/lib/mios/agent-pipe/mios_pipe/access/pdp.py`. `test_mios_pdp.py` covers `decide()` in isolation but nothing property-tests the PARITY. Add a property/fuzz test over randomized policies (denied / allowed / `max_permission` including unknown-tier typos) and randomized verb catalogs asserting that every verb the surface filter drops is also denied at the dispatch gate -- no dispatchable-but-pruned verb -- and that an unknown `max_permission` fails CLOSED on both paths, regression-guarding the old fail-OPEN defect that `resolve_ceiling` fixed.
**Where:** new `usr/lib/mios/agent-pipe/test_mios_pdp_parity.py`, exercising `mios_pdp` plus the filter/dispatch semantics from `mios_pipe/access/policy.py`, run via the agent-pipe test gate
**Done When:** the parity property-test runs N randomized cases with zero surface-not-subset-of-dispatch violations and confirms unknown-ceiling fail-closed on both axes; test green under `just drift-gate`.
**Why:** If the two paths ever diverge, a caller sees a verb hidden from its surface but can still invoke it directly -- a privilege escalation that unit tests on `decide()` alone cannot detect.
**Dep:** WS-A9 (pdp.py, resolve_ceiling); test_mios_pdp.py
**Who:** you (Python).

## AGY-199 -- Project the mTLS-enforcing reverse-proxy config from SSOT and gate it regenerate-and-diff  (WS-FED | P2 | M) **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the enforcing half of mTLS becomes a generated, drift-gated artifact instead of prose in a README.
**What+How:** `tools/provision-agent-mtls.py` mints the CA and agent cert from `[security.mtls]`, but the ENFORCING half -- making the `/a2a` and `/v1` front door actually REQUIRE client certs -- is described only as deployment prose in `security/README.md` and is left UNPROJECTED, so there is no SSOT-derived gated artifact for it. Add a generator that projects a committed proxy snippet (Caddy or nginx `ssl_verify_client on` plus `ssl_client_certificate` pointing at `[security.mtls].ca_file`/dir, proxying `[ports].agent_pipe`) from SSOT, shipping INERT when `[security.mtls].enable=false` (snippet emitted commented / no-op). Guard it with a regenerate-and-diff drift-check modeled on `check_egress_firewall`.
**Where:** new `tools/generate-mtls-proxy.py`, generated `usr/share/mios/security/mtls-proxy.conf`, source `usr/share/mios/mios.toml` (`[security.mtls]` + `[ports]`), gate in `automation/38-drift-checks.sh`
**Done When:** the projection round-trips (regenerate-and-diff green); the artifact is inert at `enable=false` and requires client certs at `enable=true`; `python3 -c` import/parse of the generator is clean; `just drift-gate` green.
**Why:** MiOS mints mTLS certificates that nothing enforces -- the federation front door accepts unauthenticated peers while the config claims mTLS is enabled.
**Dep:** tools/provision-agent-mtls.py; pattern from check_egress_firewall (check 14)
**Who:** you (Python + drift-gate).

## AGY-200 -- Pin the A2A agent passport to a committed JSON-schema and prove sign->verify with an ephemeral key  (WS-FED | P2 | M) **[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- a federated peer's identity claim is a validated, tamper-evident artifact instead of an unchecked dict.
**What+How:** `_build_agent_passport` in `mios_pipe/federation/a2a.py` serves `[agent_passport]` UNSIGNED whenever `signing_key_path` is unset, and nothing offline proves the built passport matches the Open Agent Passport v0.1.0 shape or that the signer round-trips. Commit `agent-passport.schema.json` covering the issuer/agent/scope/spend_ceiling/validity fields named in the `[agent_passport]` comment block, then add a test that builds a passport from a fixture config, validates it against that schema, signs it with an EPHEMERAL in-process Ed25519 key using the `cryptography`/JWS libs a2a.py already imports (no network, no committed key material), asserts `_verify` succeeds, and asserts a mutated field fails verification.
**Where:** `usr/share/mios/ai/schemas/agent-passport.schema.json` (new), `usr/lib/mios/agent-pipe/test_mios_passport.py` (new), `mios_pipe/federation/a2a.py`
**Done When:** the agent-pipe test gate runs `test_mios_passport.py` green -- schema validation passes, the signed round-trip verifies, the tampered-payload case fails -- with no network access and no key on disk; `just drift-gate` green.
**Why:** today a malformed or silently-unsigned passport ships and is only discovered by a peer rejecting it at federate time; there is no local proof the sign/verify path even works.
**Dep:** none
**Status:** **[DONE]** | **Domain:** federation / agent-pipe | **Who:** you (Python)

## AGY-201 -- Author the security-gate threat-model reference and lint that every `[security]` mode flag is documented  (WS-DOCS | P3 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- an arriving agent can read one page and know which gate blocks which attack and how it fails.
**What+How:** MiOS composes many dispatch-time defenses -- provenance-taint Semantic Firewall (`mios_pipe/access/firewall.py`), Rule-of-Two (`ruleof2.py`), CaMeL quarantine (`access/quarantine.py`), the WS-A9 PDP (`access/pdp.py` + `policy.py`), egress nftables, mTLS -- plus the `[security]` mode flags (`rule_of_two_mode`, `quarantine_mode`, `api_require_auth`, `principal_bind_mode`, `principal_mode`), and nothing maps gate -> attack blocked -> fail-open/fail-closed posture. Write that reference using an existing `usr/share/doc/mios/reference/*.md` frontmatter so check-46 stays green, and add a grep-based coverage lint asserting every `_mode` / `api_require_auth` / `principal_bind_mode` key in `[security]` is named somewhere in the doc.
**Where:** `usr/share/doc/mios/reference/security-gates.md` (new), `automation/38-drift-checks.sh`, `ROADMAP.md` (cross-ref)
**Done When:** check-46 stays green on the new doc's frontmatter; the coverage lint passes at HEAD and goes RED when a new `[security]` mode flag is added without a doc mention (negative test); `just drift-gate` green.
**Why:** each new gate ships undocumented, so operators cannot reason about whether a defense fails open or closed, and reviewers re-derive the posture from source every time.
**Dep:** none
**Status:** **[DONE]** | **Domain:** docs + bash gate | **Who:** you (docs + bash gate)

## AGY-202 -- Stop 55-bake-quickshell from overwriting the real ShellRoot; gate the heredoc against the tracked Config.qml (check 51)  (WS-DESKTOP | P1 | S) **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the shipped desktop shell is the SSOT-themed one, not a baker's throwaway copy.
**What+How:** `automation/55-bake-quickshell.sh:~75` does `cat << 'EOF' > /usr/share/mios/quickshell/Config.qml`, writing a MINIMAL status-bar Config.qml with hardcoded Catppuccin literals (`#1e1e2e`/`#cdd6f4`/`#a6e3a1`). Because `08-system-files-overlay` copies the `usr/` tree first and stage 55 runs later, that heredoc CLOBBERS the tracked `usr/share/mios/quickshell/Config.qml` -- the real ShellRoot wiring PanelWindow + Sidebar + Notifications, all Theme.qml-driven -- so the rich themed shell never loads at runtime and Law 7 is violated. Apply the same fix B4/check-50 used for hyprland: have stage 55 install the tracked overlay QML (or make its heredoc byte-identical to the committed file), then add drift-check 51 mirroring `check_hyprland_conf_heredoc` that diffs baker output against the committed Config.qml.
**Where:** `automation/55-bake-quickshell.sh`, `usr/share/mios/quickshell/Config.qml`, `automation/38-drift-checks.sh` (new `check_quickshell_config_heredoc`, registered beside `check_hyprland_conf_heredoc`)
**Done When:** `just drift-gate` green with check 51 asserting baker-emitted Config.qml == `usr/share/mios/quickshell/Config.qml`; mutating one byte of the heredoc turns check 51 RED; `bash -n automation/55-bake-quickshell.sh` clean; no raw hex color remains in the baker heredoc.
**Why:** every desktop session today boots a stub bar with frozen Catppuccin colors while the real, theme-driven shell sits unused in the image -- all Quickshell work downstream of it is dead on arrival.
**Dep:** none
**Status:** **[DONE]** | **Domain:** desktop shell / bake | **Who:** you (bash + drift-gate)

## AGY-203 -- Project desktop-shell endpoint URLs from `[ports]` and lint QML/hyprland for raw host:port literals  (WS-DESKTOP | P1 | M) **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- shell tiles resolve through SSOT so a port change in mios.toml moves the UI with it.
**What+How:** `Sidebar.qml` hardcodes `http://localhost:8080` (line 134), `http://localhost:9090` (line 175, the Cockpit tile) and `http://localhost:8080/terminal` (line 209); `PortalData.qml` pins `baseUrl http://127.0.0.1:8640` (line 32); `automation/54-bake-hyprland.sh:139` binds a key to `http://localhost:9090` -- while mios.toml `[ports]` says cockpit=8090 and agent_pipe=8640, and `:8080` is RETIRED. Add an `Endpoints.qml` FileView bridge modeled on `Theme.qml` that reads a generated `/etc/mios/endpoints.json` produced from `[ports]` by `mios-dotfiles-render`/`mios-sync-theme`, bind the tiles and `baseUrl` to it, and substitute the hyprland keybind URL through a new `@@MIOS_PORT_COCKPIT@@` heredoc token. Then add a lint scanning `usr/share/mios/quickshell/*.qml` and the 54-bake heredoc for raw host:port literals outside a small allowlist.
**Where:** `usr/share/mios/quickshell/Sidebar.qml`, `usr/share/mios/quickshell/PortalData.qml`, `usr/share/mios/quickshell/Endpoints.qml` (new), `usr/share/mios/mios.toml` (`[ports]` + `[dotfiles.registry]` endpoints surface), `usr/libexec/mios/mios-dotfiles-render`, `automation/54-bake-hyprland.sh`, `automation/38-drift-checks.sh`
**Done When:** the new port-lint is GREEN; injecting a raw `:1234` into any QML surface or the hyprland heredoc turns it RED (negative test); the Cockpit tile and the `$mainMod,C` keybind both resolve to `[ports].cockpit`; check 50 stays GREEN; `mios-dotfiles-render --check` is clean (endpoints.json round-trips).
**Why:** the shell's tiles point at a retired port and a wrong Cockpit port right now -- clicking them 404s -- and nothing stops the next hardcoded URL from landing.
**Dep:** AGY-202 (baked Config.qml must stop clobbering the tracked shell first)
**Status:** **[DONE]** | **Domain:** desktop shell / ports SSOT | **Who:** you (Python + QML + TOML)

## AGY-204 -- Add `[theme.compositor]` SSOT: tokenize hyprland effects/animations and live-refresh via mios-sync-theme  (WS-DESKTOP | P2 | M) **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- compositor look-and-feel is operator-tunable from mios.toml, not frozen in a baker heredoc.
**What+How:** `54-bake-hyprland.sh` substitutes only 3 COLOR tokens; every effect stays a literal -- `decoration.rounding=12`, gaps_in/out, border_size, `blur.size/passes/vibrancy/vibrancy_darkness`, inactive_opacity, the liquid/smoothOut/smoothIn/myBezier beziers, animation durations -- and `mios-sync-theme:86` even hand-maintains `resolved['radius_px']=10  # matches hyprland.conf decoration.rounding`, which is the coupling admitting itself. Add a `[theme.compositor]` block (rounding / gaps / border_size / blur_* / vibrancy / opacity / bezier.* / anim_*), export it through `tools/lib/userenv.sh`, replace the heredoc literals with `@@MIOS_HYPR_*@@` tokens substituted exactly as the colors already are, make `mios-sync-theme` re-apply them to the INSTALLED conf so edits refresh live rather than only at bake, and derive `radius_px` FROM `[theme.compositor].rounding` instead of the magic 10.
**Where:** `usr/share/mios/mios.toml` (`[theme.compositor]`), `automation/54-bake-hyprland.sh`, `tools/lib/userenv.sh`, `usr/libexec/mios/mios-sync-theme`, `automation/38-drift-checks.sh`
**Done When:** the new gate is GREEN asserting no un-tokenized effect literal remains and every `@@MIOS_HYPR_*@@` maps to a `[theme.compositor]` key; hardcoding `rounding=99` back into the heredoc turns it RED (negative test); check 50 stays GREEN; a round-trip test mutates `[theme.compositor].rounding`, runs `mios-sync-theme`, and the installed conf reflects it idempotently; `bash -n` clean.
**Why:** changing the desktop's corner radius or blur today means editing a build script and rebuilding the image, and the hand-copied `radius_px=10` silently desyncs from the conf it claims to match.
**Dep:** none
**Status:** **[DONE]** | **Domain:** desktop shell / theme SSOT | **Who:** you (TOML + bash + drift-gate)

## AGY-205 -- Launch the living wallpaper from the Hyprland session via exec-once, gated on `branding.living_wallpaper`  (WS-DESKTOP | P2 | S) **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- a shipped branding capability actually runs in the session it was built for.
**What+How:** the `exec-once` block in `automation/54-bake-hyprland.sh` starts quickshell and the systemd env import but has NO wallpaper launcher, so a Hyprland session renders a bare background even though `usr/libexec/mios/mios-living-wallpaper` exists (gated, off by default) and mios.toml carries `branding.living_wallpaper` / `living_wallpaper_mode`. Add `exec-once = mios-living-wallpaper` to the heredoc -- safe because the tool no-ops when the SSOT flag is false or mode=static -- plus a static swww/hyprpaper fallback for the static case, and a drift-check asserting the exec-once line is present and references `mios-living-wallpaper`, and that the tool honors `MIOS_BRANDING_LIVING_WALLPAPER`.
**Where:** `automation/54-bake-hyprland.sh`, `usr/libexec/mios/mios-living-wallpaper`, `automation/38-drift-checks.sh`
**Done When:** the new gate is GREEN confirming the wallpaper exec-once appears in BOTH the baker heredoc and the committed `usr/share/mios/hyprland/hyprland.conf`; deleting the exec-once line turns it RED (negative test); check 50 stays GREEN; `bash -n` clean on both scripts.
**Why:** the wallpaper engine is baked into every image and never starts -- operators see an empty desktop and assume the feature does not exist.
**Dep:** none
**Status:** **[DONE]** | **Domain:** desktop shell / branding | **Who:** you (bash + drift-gate)

## AGY-206 -- Bind a real freedesktop notification server (Quickshell.Services.Notifications) behind the existing toast surface  (WS-DESKTOP | P2 | M) **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the shell is a complete session, not a UI with no daemon behind it.
**What+How:** `usr/share/mios/quickshell/Notifications.qml` + `NotifModel.qml` render a top-right toast UI, but grep finds NO `Quickshell.Services.Notifications` import anywhere -- the shell exposes only an in-QML `notify()` helper, so external apps' `org.freedesktop.Notifications` D-Bus messages are dropped and the session has no notification daemon at all. Declare a `NotificationServer` component that feeds incoming server notifications into `NotifModel` (mapping urgency -> kind) while keeping the existing toast rendering and the `Config.qml` wiring intact, and add a static gate so the server declaration cannot silently disappear.
**Where:** `usr/share/mios/quickshell/Notifications.qml`, `usr/share/mios/quickshell/NotifModel.qml`, `usr/share/mios/quickshell/Config.qml`, `automation/38-drift-checks.sh`
**Done When:** the new static gate is GREEN asserting a `Quickshell.Services.Notifications` `NotificationServer` is declared and fed into `NotifModel`; removing the import turns it RED (negative test); the QML component-graph parse (the AGY-209/210 lint, or a local balanced-brace/import check) passes on the edited files.
**Why:** every notification from a normal application -- updates, mail, build failures -- is silently discarded today, because nothing on the bus is listening.
**Dep:** none
**Status:** **[DONE]** | **Domain:** desktop shell | **Who:** you (QML + drift-gate)

## AGY-207 -- Feed `[colors]` into the living-wallpaper WGSL uniforms, delete the echo-stub shader branch, gate the palette  (WS-DESKTOP | P2 | M) **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- even the GPU shader surface derives its palette from SSOT rather than shipping its own colors.
**What+How:** `usr/libexec/mios/mios-living-wallpaper` reads `BG_COLOR`/`ACCENT_COLOR` from `MIOS_COLOR_*`, but its `if MODE == shader` branch is a stub that echoes "In a real environment this would start the engine" and falls back to static -- so the `bg_color`/`accent_color` vec3 uniforms in `usr/share/mios/branding/living-wallpaper.wgsl` are never populated. Wire that branch to a real headless wgpu runner (invoke `mios-wallpaperd` or a small wgpu harness) passing `MIOS_COLOR_BG`/`MIOS_COLOR_ACCENT` as the uniform buffer, assert the WGSL carries no hardcoded palette (uniforms only), and ground the palette flow in `research/mesh-gradient-living-wallpaper-2026-07-06.md` so shader defaults derive from `[colors]`.
**Where:** `usr/libexec/mios/mios-living-wallpaper`, `usr/share/mios/branding/living-wallpaper.wgsl`, `tools/native/mios-wallpaperd` (uniform feed), `automation/38-drift-checks.sh`
**Done When:** the new gate is GREEN asserting (a) `living-wallpaper.wgsl` contains no `#hex` literal, uniforms only, and (b) the shader branch passes `MIOS_COLOR_BG`/`ACCENT` to the renderer and no longer degrades to a bare echo-stub; injecting a hardcoded hex into the WGSL turns it RED (negative test); `bash -n usr/libexec/mios/mios-living-wallpaper` clean.
**Why:** shader mode is advertised in SSOT but silently degrades to a static image, and the shader would ignore the operator's palette even if it ran.
**Dep:** AGY-205 (the wallpaper must actually be launched by the session)
**Status:** **[DONE]** | **Domain:** desktop shell / branding | **Who:** you (bash + WGSL + drift-gate)

## AGY-208 -- Give mios-wallpaperd unit tests and add the first cargo fmt/clippy/test drift-gate for the native crate  (WS-LANG | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the native tier gets the CI floor the shell tier already has, so compiled code is trustworthy enough to migrate onto.
**What+How:** `tools/native/mios-wallpaperd` (the Law 14 native consolidated Windows living-wallpaper / WorkerW / WSLg gui-watch daemon: `workerw.rs`, `host.rs`, `guiwatch.rs`, `main.rs`) has ZERO tests -- grep finds no `#[test]` -- and there is NO cargo test/clippy/fmt gate anywhere in `automation/` or the justfile. Add pure-logic unit tests around the testable seams: registry Run-key value construction, WorkerW window-class matching, gui-watch centering math, arg/env parsing. Then add a native-workspace gate step running `cargo fmt --check`, `cargo clippy -- -D warnings` and `cargo test` over `tools/native`, fail-open with skip-with-warning when the Rust toolchain is absent (mirroring the python3-missing branch in check 16).
**Where:** `tools/native/mios-wallpaperd/src/*.rs` (+ `#[cfg(test)]` modules), `tools/native/Cargo.toml`, `automation/38-drift-checks.sh` or the justfile
**Done When:** `cargo test -p mios-wallpaperd` is GREEN with the new unit tests; `cargo clippy -p mios-wallpaperd -- -D warnings` and `cargo fmt --check` are clean; the new drift-gate step runs all three and reports GREEN, skipping-with-warning when cargo is unavailable so CI stays fail-open.
**Why:** the Rust plane currently has no equivalent of shellcheck -- a regression in the native daemon reaches the image with no lint, no format check and no test between it and the operator.
**Dep:** none
**Status:** **[DONE]** | **Domain:** native tier / CI | **Who:** you (Rust + drift-gate)

## AGY-209 -- Add a pure-Python QML lint: Law-7 color check plus imports/brace-balance across the shell surfaces  (WS-DESKTOP | P2 | M) **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- QML stops being the one shipped file type with no validation at all.
**What+How:** grep of `automation/38-drift-checks.sh` finds no `.qml`/qmllint check, so a regression like the baked Config.qml's hardcoded Catppuccin hex, an unbalanced brace, or a missing import ships silently. Write a small static checker with no Qt runtime that walks every `usr/share/mios/quickshell/*.qml` and enforces: no raw `#RRGGBB`/`#AARRGGBB` literal except inside `Theme.qml`'s declared fallback block, every referenced `Type {}` component resolves to a sibling `.qml` or a known import, and braces/parens balance. Register it as a numbered drift-check carrying the Theme.qml fallback allowlist.
**Where:** `tools/mios-qml-lint.py` (new), `usr/share/mios/quickshell/*.qml`, `automation/38-drift-checks.sh`
**Done When:** the new check is GREEN over the current QML surfaces (once AGY-202 removes the baked hardcodes); adding `color: "#ff0000"` to `Sidebar.qml` or dropping a closing brace turns it RED (negative test); the linter runs without Qt installed and skips-with-warning only if python3 is missing.
**Why:** a single typo in a QML file breaks the entire desktop shell at session start with nothing in the build catching it, and hardcoded palettes re-enter the tree unnoticed.
**Dep:** AGY-202 (baked hardcodes must be gone before the lint can be green)
**Status:** **[DONE]** | **Domain:** desktop shell / drift-gate | **Who:** you (Python + drift-gate)

## AGY-210 -- Gate ship-completeness of the QML component graph: every surface Config.qml pulls in must exist in the image  (WS-DESKTOP | P3 | S) **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the shell's file set is declared and enforced, not an accident of a wholesale directory copy.
**What+How:** only `Theme.qml` is registered in mios.toml `[dotfiles.registry.quickshell]`; the other six surfaces (Config / PanelWindow / Sidebar / Notifications / NotifModel / PortalData) ship only because `08-system-files-overlay` copies the `usr/` tree wholesale, so nothing gates their presence and deleting one silently breaks the shell -- `Config.qml`'s `Sidebar{}` / `Notifications{}` would fail to resolve at runtime. Add a gate that starts at `usr/share/mios/quickshell/Config.qml`, resolves the transitive `Type {}` component graph to sibling files, and asserts each referenced `.qml` exists on disk and, where a membership manifest governs the overlay, is listed in it.
**Where:** `usr/share/mios/quickshell/Config.qml` (+ the imported surfaces), `automation/38-drift-checks.sh`, reusing `tools/mios-qml-lint.py`
**Done When:** the new gate is GREEN resolving Config.qml's full component graph with every referenced surface present; deleting or renaming `usr/share/mios/quickshell/Sidebar.qml` turns it RED (negative test); the check runs pure-Python with no Qt dependency.
**Why:** a shell surface can be dropped or renamed in a refactor and the build stays green -- the breakage only shows up as a blank desktop on a real machine.
**Dep:** AGY-209 (reuses `tools/mios-qml-lint.py`)
**Status:** **[DONE]** | **Domain:** desktop shell / drift-gate | **Who:** you (Python + drift-gate)

## AGY-211 -- Define the canonical `[mini]` / `[mini.mesh]` SSOT tables so both mini generators stop falling through to defaults  (WS-MINI | P1 | S) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the MiOS-Metal split-plane is SSOT-reachable instead of permanently inert.
**What+How:** `usr/libexec/mios/mios-metal-vfio-gen:7-8` reads `d['mini']['enabled']` and `d['mini']['bind_dgpu_vfio']`, and `mios-metal-mesh-gen:7-8` reads `d['mini']['mesh']['enabled']` and `d['mini']['mesh']['headscale_domain']`, but mios.toml defines only `[mini.gpu]` (assignments/arbitration, ~line 289) with NO top-level `[mini]` and NO `[mini.mesh]` -- so BOTH generators silently take their hardcoded defaults (`enabled=false` forever, `domain=mesh.mios.local`) and the SSOT is unreachable. Add a canonical `[mini]` block adjacent to `[mini.gpu]` (`enabled`, `bind_dgpu_vfio`, `host_floor_cores`, `host_floor_ram_gb`) plus a real `[mini.mesh]` (`enabled`, `headscale_domain`), each key commented per `doc-mios-metal.md` §1.1/§2e, and add a gate that parses every `d.get('mini',...)` key the two generators reference and asserts it exists in mios.toml (fail-open only when python3 is absent, mirroring check 53).
**Where:** `usr/share/mios/mios.toml` (`[mini]`/`[mini.mesh]` near line 289), `usr/libexec/mios/mios-metal-vfio-gen`, `usr/libexec/mios/mios-metal-mesh-gen`, `automation/38-drift-checks.sh`, `docs/agy/doc-mios-metal.md`
**Done When:** the generators resolve real committed keys, not defaults -- `mios-metal-vfio-gen` prints `MIOS_METAL_ENABLED=true` when `[mini].enabled=true`; the new drift-check asserts every referenced mini key exists; a negative test in `tests/drift-gate-negatives.sh` deletes one key and proves non-zero exit; `just drift-gate` green on HEAD.
**Why:** an operator can set nothing about the Mini split-plane today -- both generators ignore mios.toml entirely and emit fixed defaults, so the whole feature is unconfigurable.
**Dep:** none
**Status:** **[DONE]** | **Domain:** mini / SSOT | **Who:** you (bash + TOML + drift-gate)

## AGY-212 -- Upgrade the substring-only check 68 into a value round-trip gate and register the ungated mesh generator  (WS-MINI | P1 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- a check that cannot fail is not a gate; every mini generator has one that can.
**What+How:** `check_metal_vfio` (`automation/38-drift-checks.sh:3985`) asserts only that output CONTAINS the literal substring `MIOS_METAL_ENABLED=` -- it passes even when every value is wrong -- and grep shows `mios-metal-mesh-gen` has ZERO drift-check references, i.e. it is entirely ungated. Replace check 68 with a projection round-trip: run `mios-metal-vfio-gen` against a fixture mios.toml carrying known `[mini]`/`[mini.gpu]` values and assert the emitted `MIOS_METAL_*` values EQUAL the SSOT, then add a sibling check for `mios-metal-mesh-gen` asserting `MIOS_METAL_MESH_ENABLED` / `MIOS_HEADSCALE_DOMAIN` match `[mini.mesh]`, registered in `main()` alongside `check_metal_vfio` (~line 4085).
**Where:** `automation/38-drift-checks.sh` (check 68 + new mesh check), `tests/drift-gate-negatives.sh`, `usr/libexec/mios/mios-metal-{vfio,mesh}-gen`
**Done When:** check 68 FAILS when a fixture value is mutated (not only when the line is missing); the mesh generator has its own registered check; both have negative tests in `tests/drift-gate-negatives.sh` injecting a wrong value and asserting non-zero exit plus a named file/line; drift-gate green on HEAD.
**Why:** the gate reports green while the projection is wrong, which is worse than no gate -- and the mesh generator's output is never checked at all.
**Dep:** AGY-211 (needs the `[mini]`/`[mini.mesh]` keys to round-trip against)
**Status:** **[DONE]** | **Domain:** mini / drift-gate | **Who:** you (bash/Python + negative-test harness)

## AGY-213 -- Project the persistent vfio-pci bind (`/etc/driverctl.d`) from `[mini.gpu].assignments`  (WS-MINI | P2 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- GPU passthrough survives reboot because the bind is a projected artifact, not an operator's manual command.
**What+How:** `doc-mios-metal.md` §2a and step 4 require a BDF-keyed driverctl override under `/etc/driverctl.d` projected from SSOT, but no `/etc/driverctl.d` surface exists in the tree and `[gpu.vfio_overrides]` is undefined. Add `usr/libexec/mios/mios-metal-driverctl-gen` emitting `/etc/driverctl.d/00-mios-vfio.conf` with one `driverctl set-override <BDF> vfio-pci` line per BDF in `[mini.gpu].assignments` (union across all guests), gated by `[mini].bind_dgpu_vfio` so it renders empty/inert when false, and encode the doc's slot-swap caveat as a comment in the render ("binds by PCI address -- a slot change silently drops the override"). Add a round-trip drift-check following the check 53 pattern (re-render == committed).
**Where:** `usr/libexec/mios/mios-metal-driverctl-gen` (new), `etc/driverctl.d/00-mios-vfio.conf` (new, committed render), `usr/share/mios/mios.toml` `[mini.gpu]`, `automation/38-drift-checks.sh`
**Done When:** the driverctl.d file projects deterministically from `[mini.gpu].assignments`; the drift-check re-renders and byte-compares against the committed file (green); a negative test adds a BDF and proves the gate fails until re-rendered; `bash -n` clean; drift-gate green.
**Why:** without a persistent override the GPU rebinds to the host driver on reboot and the guest loses its passthrough device -- a documented requirement with zero implementation.
**Dep:** AGY-211 (`[mini].bind_dgpu_vfio`)
**Status:** **[DONE]** | **Domain:** mini / vfio | **Who:** you (bash + drift-gate)

## AGY-214 -- Reject dangling empty-value kargs in validate-kargs.py and stop shipping `vfio_pci.ids=`  (WS-MINI | P2 | S) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the kargs projection cannot emit a garbage boot argument and still pass its gate.
**What+How:** `usr/lib/bootc/kargs.d/20-vfio.toml` ships a literal `vfio_pci.ids=` with an EMPTY right-hand side -- a dangling karg that becomes a no-op or garbage boot arg -- and `automation/validate-kargs.py` (`ALLOWED_TOP_KEYS` at line 39, per-line scan at line 65) does not flag `key=` with no value. Add a rule rejecting any karg matching `^[^=]+=$` unless the key is in an explicit allowlist, and make `automation/22-kargs-render.sh` either POPULATE `vfio_pci.ids` from the `[mini.gpu]`/`[kargs].vfio_ids` projection or DROP the empty entry instead of emitting it.
**Where:** `automation/validate-kargs.py`, `automation/22-kargs-render.sh`, `usr/lib/bootc/kargs.d/20-vfio.toml`, `automation/38-drift-checks.sh` (check 53 path)
**Done When:** `validate-kargs.py` exits non-zero on an empty-RHS karg, unit-tested with a fixture line `vfio_pci.ids=`; the committed `20-vfio.toml` no longer ships a dangling empty karg; check 53 (kargs projection) round-trips green; a negative test proves the new rule fires.
**Why:** every image built today carries a malformed kernel command-line argument that the validator waves through -- a ships-green boot-arg defect on real hardware.
**Dep:** none
**Status:** **[DONE]** | **Domain:** mini / kargs | **Who:** you (Python + bash)

## AGY-215 -- Generate the guest libvirt domain XML (hostdev + swtpm vTPM + single virtio-net) from `[mini]`  (WS-MINI | P1 | L) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the split-plane guest is a projected, gated artifact rather than a hand-written XML nobody owns.
**What+How:** `doc-mios-metal.md` step 7 requires the guest domain projected from mios.toml: one `<hostdev managed='yes'>` per assigned GPU IOMMU group, host-passthrough CPU, `<vcpupin>`/`<emulatorpin>`, `<hugepages>` + `<locked/>`, strict numatune, a SINGLE `<interface type='bridge'>` virtio-net as the guest's only path (no second NIC), and the swtpm vTPM `<tpm model='tpm-crb' backend='emulator' version='2.0'>` per §2b. No libvirt domain XML surface exists anywhere in the tree. Add `usr/libexec/mios/mios-metal-domain-gen` emitting a deterministic domain XML from `[mini]` + `[mini.gpu]`, plus a gate that runs `xmllint --noout` on the render and asserts exactly one interface, a `<tpm backend='emulator'>`, and one `<hostdev>` per assignment BDF.
**Where:** `usr/libexec/mios/mios-metal-domain-gen` (new), `etc/mios/mini/guest-domain.xml` (new, committed render), `usr/share/mios/mios.toml` `[mini]`/`[mini.gpu]`, `automation/38-drift-checks.sh`
**Done When:** the domain XML projects from SSOT and is xmllint-valid; the gate asserts one virtio-net interface, an emulated vTPM and one hostdev per `[mini.gpu]` BDF, and FAILS if a second NIC is injected or the vTPM is removed; a negative test proves it; drift-gate green.
**Why:** the central artifact of the whole Mini architecture does not exist -- there is nothing to boot the guest with, and no gate to stop a second NIC from breaking the single-path network invariant.
**Dep:** AGY-211 (`[mini]` schema), AGY-213 (vfio bind)
**Status:** **[DONE]** | **Domain:** mini / libvirt | **Who:** you (Python/bash + drift-gate)

## AGY-216 -- Project a fail-closed headscale Grants policy (huJSON) from `[mini.mesh]`  (WS-MINI | P1 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the mesh split is secured by a restrictive policy that the gate refuses to let degrade open.
**What+How:** `doc-mios-metal.md` R6/step 10 states headscale's default EMPTY policy is ALLOW-ALL and the split is only secure once a restrictive Grants policy loads, and that the projection plus drift-gate MUST fail closed and validate huJSON before apply. Today `mios-metal-mesh-gen` emits only `MIOS_METAL_MESH_ENABLED` and `MIOS_HEADSCALE_DOMAIN` -- there is no policy surface at all. Add a `[mini.mesh]` policy sub-block (grants / routes / autoApprovers on `tag:router`) plus `usr/libexec/mios/mios-metal-headscale-policy-gen` emitting valid huJSON, and a drift-check that FAILS when the projected policy is empty or expresses an allow-all rule, and that the huJSON parses.
**Where:** `usr/libexec/mios/mios-metal-headscale-policy-gen` (new), `etc/mios/mini/headscale-policy.hujson` (new, committed render), `usr/share/mios/mios.toml` `[mini.mesh]`, `automation/38-drift-checks.sh`
**Done When:** the policy huJSON projects from `[mini.mesh]` and parses; the gate fails on an empty/allow-all policy and passes on the restrictive committed one; a negative test injecting an allow-all rule asserts non-zero exit; drift-gate green; the surface ships inert when `[mini.mesh].enabled=false`.
**Why:** shipping the mesh with headscale's default policy means every node can reach every other node -- the split-plane's entire security claim is void until this exists.
**Dep:** AGY-211 (`[mini.mesh]` table)
**Status:** **[DONE]** | **Domain:** mini / mesh security | **Who:** you (Python/bash + drift-gate)

## AGY-217 -- Project the master `inet mios-router` nftables table and gate against cross-table conflicts  (WS-MINI | P2 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the router's packet policy is SSOT-projected, syntax-validated and provably not stepping on another writer's table.
**What+How:** `doc-mios-metal.md` §3b-cross and steps 10/13 specify a hand-owned master `inet mios-router` table (WAN masquerade, default-drop forward, per-bridge isolation, `flow add @ft` / `flags offload`) with libvirt, netavark and tailscale each owning their OWN table and coexisting by hook priority, and NO firewalld. The only nft artifact in the tree is `usr/share/mios/security/egress.nft`, an unrelated egress ruleset. Add `usr/libexec/mios/mios-metal-router-nft-gen` projecting the master table from `[mini]`/`[mini.mesh]` (bridge names, WAN iface, subnets), plus a gate that (a) validates the render with `nft -c -f` (syntax-only, never a live apply) and (b) asserts the table declares default-drop forward + masquerade and does NOT redefine another writer's table name.
**Where:** `usr/libexec/mios/mios-metal-router-nft-gen` (new), `etc/mios/mini/mios-router.nft` (new, committed render), `usr/share/mios/mios.toml` `[mini]`/`[mini.mesh]`, `automation/38-drift-checks.sh` (follow the egress.nft pattern)
**Done When:** the master table projects from SSOT and passes `nft -c`, or skips-with-warning when nft is absent (fail-open); the gate asserts default-drop plus masquerade are present and no foreign table name is duplicated; a negative test flipping the forward policy to accept makes the gate fail; drift-gate green.
**Why:** without the master table the router forwards by default and NATs nothing, and a hand-written ruleset that redefines libvirt's or tailscale's table silently flushes their rules on load.
**Dep:** AGY-211 (`[mini]`/`[mini.mesh]` keys)
**Status:** **[DONE]** | **Domain:** mini / networking | **Who:** you (bash + nft parse gate)

## AGY-218 -- Add `[storage.guest_passthrough]` SSOT and gate that the boot controller is never passed to the guest  (WS-MINI | P2 | S) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a misconfiguration that would strand the host's own filesystem is impossible to commit.
**What+How:** `doc-mios-metal.md` §2c and step 5 require the drift-gate to validate that `[storage.guest_passthrough]` never names the boot controller's BDF, because passing the boot NVMe strands host `/var`. Grep for `^[storage` returns nothing -- the block does not exist. Add `[storage.guest_passthrough]` (a list of non-boot controller BDFs to `<hostdev>` into the guest) plus `[storage].boot_controller_bdf`, and a drift-check that FAILS when any guest_passthrough BDF equals the declared boot controller BDF or overlaps `[mini.gpu].assignments` incorrectly. Static SSOT validation only -- no live disk enumeration.
**Where:** `usr/share/mios/mios.toml` (new `[storage.guest_passthrough]` / `[storage].boot_controller_bdf`), `automation/38-drift-checks.sh`, `docs/agy/doc-mios-metal.md` §2c cross-ref
**Done When:** the drift-check parses `[storage.guest_passthrough]` and exits non-zero when it names `[storage].boot_controller_bdf`; a negative test setting guest_passthrough to include the boot BDF asserts the gate fails; the check passes on a clean HEAD (empty/inert by default); drift-gate green.
**Why:** one wrong BDF in a config file would hand the host's boot disk to a guest and brick the router on next boot, with nothing today catching it before hardware.
**Dep:** AGY-215 (the domain generator consumes the passthrough list)
**Status:** **[DONE]** | **Domain:** mini / storage | **Who:** you (Python/bash + drift-gate)

## AGY-219 -- Gate the router core/RAM budget: the guest allocation may not breach the host floor  (WS-MINI | P2 | S) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the hypervisor-router keeps enough CPU and RAM to route, enforced arithmetically at commit time.
**What+How:** `doc-mios-metal.md` §2e requires the SSOT to compute the guest CPU/RAM share from the RESERVED router housekeeping floor and the drift-gate to fail when the guest allocation would leave the router under that floor (indicative floors: >=2 cores + 2-4GB for Wi-Fi7/2.5GbE, >=4 cores for 10GbE without offload). Nothing enforces it. Using `[mini].host_floor_cores`/`host_floor_ram_gb` plus a `[mini].guest_cpu_pct`/`guest_ram_pct` (or absolute) allocation, add a generator computing the guest share and a drift-check asserting `host_total - guest_share >= host_floor` for BOTH CPU and RAM. Pure arithmetic over SSOT -- no live hardware needed.
**Where:** `usr/share/mios/mios.toml` `[mini]` (host_total_cores/ram, guest share, floors), `usr/libexec/mios/mios-metal-vfio-gen` extended or a new `mios-metal-budget-gen`, `automation/38-drift-checks.sh`
**Done When:** the gate computes the guest-vs-router split from SSOT and passes while the router retains at least its floor; a negative test setting `guest_cpu_pct` high enough to breach `host_floor_cores` asserts the gate fails; it passes on HEAD defaults; drift-gate green.
**Why:** an over-generous guest allocation starves the router of the cores it needs to move packets, and the failure appears as unexplained network stalls on real hardware rather than as a red gate.
**Dep:** AGY-211 (`[mini].host_floor_cores` / `host_floor_ram_gb`; the original text cites the pre-renumber id AGY-123)
**Status:** **[DONE]** | **Domain:** mini / capacity | **Who:** you (Python/bash + drift-gate)

## AGY-220 -- Project a second, non-vfio safe-mode recovery UKI cmdline so an all-GPU-bound router is not a brick  (WS-MINI | P2 | M) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- there is always a boot path that yields a real console on a machine whose GPUs are all bound to vfio.
**What+How:** `doc-mios-metal.md` §2d(#2) and step 2 require a recovery UKI boot entry that does NOT bind vfio-pci, so amdgpu/i915 can claim a GPU for a console and a headless all-bound router is not a reseat-brick. The current karg set (`usr/lib/bootc/kargs.d/20-vfio.toml`, `01-mios-vfio.toml`, `32-mios-ws7-uki.toml`) projects only vfio-BOUND cmdlines. Add a projected recovery cmdline -- a second entry under `usr/lib/bootc/kargs.d` or an `etc/mios/mini/recovery.cmdline` artifact -- that strips `vfio-pci.ids` / `rd.driver.pre=vfio-pci` and adds `console=ttyS0`, projected from `[mini]`, plus a gate asserting the recovery cmdline contains NO vfio-pci token while the primary entry DOES.
**Where:** new recovery cmdline artifact under `usr/lib/bootc/kargs.d` or `etc/mios/mini/recovery.cmdline`, its generator, `usr/share/mios/mios.toml` `[mini]`, `automation/38-drift-checks.sh` (near check 53)
**Done When:** both the vfio-bound and the non-vfio recovery cmdlines project from SSOT; the drift-check asserts the recovery entry carries no vfio-pci binding while the primary does; a negative test injecting vfio-pci into the recovery entry makes the gate fail; drift-gate green.
**Why:** with every GPU bound to vfio and no alternative entry, a boot failure on the guest side leaves the operator with no console at all -- recovery requires physically reseating hardware.
**Dep:** AGY-214 (kargs validation), AGY-211 (`[mini]`)
**Status:** **[DONE]** | **Domain:** mini / boot | **Who:** you (bash + drift-gate)

## AGY-221 -- Gate installer target-matrix parity across all four surfaces and fix the confirmed drift  (WS-INSTALL | P1 | M) **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- the list of install targets is one fact, identical wherever it is stated.
**What+How:** the target set has silently drifted across four surfaces. `installation/mios-install.sh` dispatch (lines 393-408) handles live/flash/xbox/fedora/bootc/oci/seed/build/update/config/configure, but its own `usage()` heredoc (line 82) lists only `live | xbox | fedora | bootc | oci | seed | flash | build | update` -- missing config/configure. `installation/mios-install.ps1`'s catalog additionally exposes `monitor` and `repos` (lines 47, 83) that the `.sh` dispatch lacks. `installation/README.md`'s Target->entrypoint mapping table (lines 113-129) has NO row for `configure`, `monitor` or `repos`, though its grammar line 74 lists `configure`. Add drift-check 69 in `automation/38-drift-checks.sh` that extracts the target set from all four surfaces (the `.sh` dispatch `case` arms, the `.sh` usage() heredoc, the `.ps1` catalog keys, the README grammar list + mapping-table rows) using the SAME multi-path `../mios-bootstrap` resolver the kickstart/ventoy checks use (lines 3320-3348, degrade-open when the repo is absent) and FAIL on any mismatch. Then fix the drift: complete `usage()`, add the missing README rows, and either add Linux dispatch arms for monitor/repos or record them in one in-file SSOT list of Windows-only-guidance targets.
**Where:** `installation/mios-install.sh`, `installation/README.md`, `automation/38-drift-checks.sh` (new check 69)
**Done When:** check 69 parses all four surfaces and passes at HEAD after the usage/README fixes; deleting one target from `usage()` makes it exit non-zero (negative fixture); `bash -n installation/mios-install.sh` clean; `just drift-gate` green.
**Why:** operators are told about targets that do not exist on their platform and not told about ones that do -- the documented install grammar is already wrong in three places.
**Dep:** none
**Status:** **[DONE]** | **Domain:** install / drift-gate | **Who:** you (bash + Python drift-check + Markdown)

## AGY-222 -- SSOT-project the Linux target-prereq catalog, wire the Rust toolchain, and show the plan in `--dry-run`  (WS-INSTALL | P1 | M) **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- one prereq resolver keyed off the chosen target, readable before anything is installed.
**What+How:** `resolve_target_prereqs()` in `installation/mios-install.sh` (line 416) HARDCODES `for pkg in git curl podman` instead of resolving from SSOT, never ensures the Rust toolchain that native/build targets need -- `mios-common.sh` already ships `mios_ensure_rust` at line 118 and nothing calls it -- and runs at line 473, AFTER the `--dry-run` early exit at line 456, so a dry run never reveals what it would install. Add an `[install.prereqs.<target>]` SSOT block (or a linux sub-table under the existing `[bootstrap.prereqs]`, mios.toml:8893) enumerating the dnf packages per target; make `resolve_target_prereqs` read it via `mios_ssot_value`; call `mios_ensure_rust` for build/update/native targets; and move the prereq PLAN print into the `--dry-run` block so the dry run shows the resolved package list and rust decision without executing (still exit 0). Add `installation/test_mios_install_prereqs.sh` asserting `--dry-run` prints the SSOT-resolved list per target and that no literal `git curl podman` list remains.
**Where:** `installation/mios-install.sh`, `installation/mios-common.sh`, `usr/share/mios/mios.toml` (`[install.prereqs.*]`), `installation/test_mios_install_prereqs.sh` (new)
**Done When:** prereqs resolve from SSOT and grep proves no hardcoded package list survives; `--dry-run` for every target prints the resolved prereq plan and exits 0 with no side effects; the new test is green; `bash -n` clean; `just drift-gate` green.
**Why:** `--dry-run` lies about what the installer will do, build targets fail late on a missing Rust toolchain, and the package set is a literal in a script rather than an operator-tunable value.
**Dep:** AGY-221 (shared target list)
**Status:** **[DONE]** | **Domain:** install / SSOT | **Who:** you (bash + TOML)

## AGY-223 -- Harden `mios_ensure_repo` to the bootstrap.sh fetch pattern (retry + Forgejo fallback + body validation) and widen the retry gate  (WS-INSTALL | P1 | M) **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- one repo fetch, hardened once, on both equal publishers.
**What+How:** `mios_ensure_repo` in `installation/mios-common.sh` (line 107) fetches the bootstrap tarball with a bare `curl -fsSL ... | tar` -- no `--retry`, no `--max-time`, no validation of the downloaded body, and GitHub-only -- while sibling `bootstrap.sh` already implements the hardened pattern (`--retry 3 --retry-delay 2 --retry-connrefused --max-time 120`, `-s` non-empty check, cache-bust). This also evades the AGY-96 curl-retry gate (check 64), which does not scan `installation/*.sh`. Bring `mios_ensure_repo` up to the gold pattern and add a Forgejo mirror fallback (GitHub and Forgejo are equal publishers), resolving org/repo/mirror hosts from SSOT `[bootstrap]` (mios.toml:8710) rather than the literal `github.com`. Then extend coverage: add a drift-check, or widen check 64's file glob via the multi-path `../mios-bootstrap` resolver, asserting every network fetch in `installation/*.sh` carries `--retry`/`--tries`.
**Where:** `installation/mios-common.sh`, `automation/38-drift-checks.sh` (extend or sit adjacent to check 64), `installation/test_mios_common_fetch.sh` (new, mock curl)
**Done When:** `mios_ensure_repo` carries `--retry`/`--max-time` + a body check + an SSOT-resolved Forgejo fallback; the gate flags a retry-less fetch anywhere in `installation/*.sh` and passes at HEAD; the mock-curl test proves the fallback fires when the primary host fails; `bash -n` clean; `just drift-gate` green.
**Why:** a single transient network blip aborts an install mid-fetch with a truncated tree, and a GitHub outage takes the whole install front door down even though an equal mirror exists.
**Dep:** none
**Status:** **[DONE]** | **Domain:** install / network hardening | **Who:** you (bash + drift-check)

## AGY-224 -- Unit-cover the awk SSOT resolver in mios-common.sh and wire a shell-test gate  (WS-TEST | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- the most load-bearing shared installer contract is protected by tests, not by luck.
**What+How:** `mios_ssot_value` in `installation/mios-common.sh` (lines 26-45) is the single contract every installer surface resolves ports and colors through, and it has ZERO tests. Its awk is subtle: it must return a quoted value even when the value contains `#` (the hex-color case, line 35), strip an inline `# comment` from an unquoted value (line 37), honor layer precedence user > host > vendor (`mios_ssot_layers`, lines 14-24), fall back to the DEFAULT argument on a missing key, and never break callers under `set -o pipefail`. Write `installation/test_mios_common.sh` with a fixture per behavior -- including a `#1A407F`-style value and a value with a trailing inline comment -- and wire it into a shell-test gate mirroring `automation/test_generate-sbom.sh` and its existing gate.
**Where:** `installation/test_mios_common.sh` (new), `automation/38-drift-checks.sh` (shell-test wiring)
**Done When:** the test passes at HEAD; deliberately deleting the quoted-value awk branch (line 35) makes the hex-color case FAIL; the shell-test gate invokes it; `bash -n` clean; `just drift-gate` green.
**Why:** a one-character awk change silently returns an empty port or a truncated hex color to every installer surface, and today nothing would catch it before an operator's machine does.
**Dep:** none
**Status:** **[DONE]** | **Domain:** install / test harness | **Who:** you (bash)

## AGY-225 -- Prove the bash and PowerShell SSOT resolvers return identical values for the same mios.toml key  (WS-CONFIG | P2 | M) **[DONE]**
**Goal:** E-11 Unified config surface -- every reader resolves the SSOT through one contract, so the installer sees the same value on Linux and Windows.
**What+How:** `installation/mios-common.sh:mios_ssot_value` (line 26) and `installation/mios-common.ps1:Get-MiosSsotValue` (line 30) each claim in a header comment to mirror each other and `usr/lib/mios/mios_toml.py`, but nothing verifies it. Add a drift-check (next free number) that invokes BOTH resolvers against the same committed `usr/share/mios/mios.toml` over a fixture key set covering the parser-tricky shapes -- `colors.info` (hex value containing `#`, which naive awk truncates), `ports.agent_pipe`, `image.ref` -- and diffs the outputs, failing on any divergence. Degrade-open with a WARN when `pwsh` is unavailable, matching the existing platform-gated checks. Resolve the bootstrap files through the `../mios-bootstrap` multi-path resolver.
**Where:** `automation/38-drift-checks.sh` (new parity check + fixture key list); reads `installation/mios-common.sh`, `installation/mios-common.ps1`.
**Done When:** the parity check runs both resolvers and passes at HEAD; regressing one side on the `#`-bearing hex fixture makes it exit non-zero; the check skips with a WARN (never hard-fails) when pwsh is absent; `just drift-gate` is green.
**Why:** today an edit to either hand-written parser silently skews installer behavior across platforms -- the operator gets a different port or color on Windows than on Linux and no gate says so.
**Dep:** none
**Who:** you (bash + Python drift-check).

## AGY-226 -- Gate mios-common.{sh,ps1} contract completeness and add the missing bash monitor-launch twin  (WS-INSTALL | P2 | M) **[DONE]**
**Goal:** E-21 One deploy front door -- one shared `mios-common` contract behind every install path, with .sh/.ps1 parity drift-gated.
**What+How:** The two halves of the shared library have drifted: `.sh` defines `mios_ensure_rust` (line 118) with no PowerShell `Ensure-MiosRust` twin, and `.ps1` defines `Resolve-MiosMonitorScript`/`Start-MiosMonitor` (lines 163,168) with no bash monitor-launch twin. Add a drift-check that maps the canonical contract names across the snake_case<->PascalCase boundary (ssot_value, ssot_path, log/line, self_elevate, ensure_repo, find_bin, ensure_rust, monitor-launch) and FAILS when a member lacks its twin, driven by one in-repo allowlist for legitimately single-sided members (each with a recorded reason). Add the bash monitor-launch twin that the AGY lane owns; leave a `TODO(agy):` marker for the missing `.ps1` `Ensure-MiosRust` and allowlist it so the gate is green now.
**Where:** `installation/mios-common.sh` (new bash monitor-launch twin), `automation/38-drift-checks.sh` (completeness check + allowlist); reads `installation/mios-common.ps1`.
**Done When:** the gate enumerates both files and passes at HEAD; deleting a twin or an allowlist entry turns it red; `bash -n` is clean; `just drift-gate` is green.
**Why:** the "one contract, two languages" claim is unenforced, so each new helper lands on one platform only and the Windows installer quietly loses capabilities the Linux one has.
**Dep:** none
**Who:** you (bash + drift-check; flag .ps1-side gaps for Claude).

## AGY-227 -- Couple the installers' /configure URL to the real Portal route so it cannot 404  (WS-CONFIG | P2 | S) **[DONE]**
**Goal:** E-11 Unified config surface -- open -> configure -> deploy through the one door at `:8640/`, with the link the installer hands the operator guaranteed live.
**What+How:** `installation/mios-install.sh:resolve_config` builds `http://localhost:<ports.agent_pipe>/configure` (line 378) and `mios-install.ps1` does the same (line 219), while the route itself is registered in `usr/lib/mios/agent-pipe/mios_pipe/routing/portal.py` at `@portal_router.get("/configure")` (line 1816) -- nothing ties them together. Add a read-only drift-check asserting (a) the path the installers open is a route actually registered in `portal.py`, and (b) both installers resolve the port from `[ports].agent_pipe` rather than a literal, which pins the AGY-109 fix against regression. Use the `../mios-bootstrap` multi-path resolver and degrade-open when that repo is absent.
**Where:** `automation/38-drift-checks.sh` (new route-contract check); reads `usr/lib/mios/agent-pipe/mios_pipe/routing/portal.py`, `installation/mios-install.sh`.
**Done When:** the check passes at HEAD; a fixture renaming the portal route to `/config2`, or reintroducing a literal port in an installer, makes it red; it degrades open when the bootstrap repo is missing; `just drift-gate` is green.
**Why:** a future portal-route rename leaves every installer opening a dead URL, and the operator's first contact with MiOS is a browser 404.
**Dep:** none
**Who:** you (bash + Python drift-check).

## AGY-228 -- Make mios-install.sh emit its target matrix (--list-targets/--targets-json) from one in-file table  (WS-INSTALL | P2 | M) **[DONE]**
**Goal:** E-21 One deploy front door -- a single guided dispatcher whose target set is single-sourced instead of restated in prose.
**What+How:** The AGY-123 parity gate currently SCRAPES `usage()` prose to learn the target set. Give `installation/mios-install.sh` a `--list-targets` (and `--targets-json`) mode emitting one machine-readable row per target -- target | default-type | platform (native|windows-guidance) | wrapped entrypoint -- derived from ONE in-file table, then refactor the dispatch `case` (lines 393-408) and `usage()` to render FROM that table so they cannot diverge by construction. Repoint the AGY-123 gate to consume `--list-targets` instead of scraping usage.
**Where:** `installation/mios-install.sh` (in-file target table + `--list-targets`/`--targets-json`), `automation/38-drift-checks.sh` (parity check reads the emitter).
**Done When:** `mios-install.sh --list-targets` prints every dispatch target with platform and entrypoint and exits 0; `--targets-json` parses as valid JSON; `usage()` and the dispatch both render from the one table; the parity gate consumes it; `bash -n` clean; `just drift-gate` green.
**Why:** prose-scraping breaks on any wording change, and a target added to the `case` but not to `usage()` (or the reverse) ships an installer that advertises one thing and does another.
**Dep:** AGY-123 (the parity gate that scrapes `usage()` today)
**Who:** you (bash).

## AGY-229 -- Make --dry-run print the COMPLETE plan and self-test all targets for zero side effects  (WS-INSTALL | P2 | M) **[DONE]**
**Goal:** E-21 One deploy front door -- `--dry-run` on every route, so an operator can see exactly what an install would do before it does it.
**What+How:** `--dry-run` in `installation/mios-install.sh` (lines 446-457) prints only ENV + CMD and exits at line 456, BEFORE the elevation decision (REQUIRES_ROOT/FORBIDS_ROOT, lines 464-471) and prereq resolution (line 473), so it under-reports the real plan. Extend it to print one PLAN block per target carrying the elevation decision (would self-sudo / must-not-be-root), the prereq plan from AGY-124, the passthrough args, and the guidance text for Windows-only targets. Add `installation/test_mios_install_dryrun.sh` iterating ALL targets (live/xbox/fedora/bootc/oci/seed/flash/build/update/config), asserting each `--dry-run` exits 0, prints a non-empty CMD (or windows-guidance), and takes no action -- stub/deny `sudo`, `dnf`, `exec` and `xdg-open` on PATH and assert none was invoked.
**Where:** `installation/mios-install.sh` (`--dry-run` block), `installation/test_mios_install_dryrun.sh` (new), `automation/38-drift-checks.sh` (shell-test wiring).
**Done When:** `--dry-run` prints elevation + prereqs + command for every target; the self-test passes for all targets with the stubbed `sudo`/`dnf`/`exec` never called; a target that forgets to set CMD is caught; `bash -n` clean; `just drift-gate` green.
**Why:** a dry run that stops before elevation and prereqs is a false assurance -- the operator approves a plan that omits the two steps most likely to surprise them.
**Dep:** AGY-124 (prereq resolver whose plan the dry run must print)
**Who:** you (bash).

## AGY-230 -- Negative tests proving the three new installer drift-checks can actually fail  (WS-TEST | P3 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness -- every drift-gate check has a negative test that proves it bites.
**What+How:** Following AGY-120's discipline for checks 64/65/66, add one injected-violation fixture per new installer gate to the drift-gate negative-test harness: drop a target from `usage()` so the target-matrix gate must red; skew a fixture `mios.toml` value so the bash/pwsh resolver-parity gate must red; rename the `/configure` route in a fixture so the route-contract gate must red. Each case asserts the specific check exits non-zero, restores, and re-asserts clean at HEAD.
**Where:** `automation/38-drift-checks.sh` negative-test harness + fixtures for checks 69 / parity / route.
**Done When:** each new installer gate has a committed violation fixture that makes it exit non-zero AND a clean pass at HEAD; the negative-test harness runs green; `just drift-gate` green.
**Why:** an unfalsified check is decoration -- a refactor can neuter the installer gates and the gate keeps reporting green.
**Dep:** AGY-123 / AGY-127 / AGY-129 (the target-matrix, resolver-parity and route-contract gates, as cited in the original)
**Who:** you (bash/Python).

## AGY-231 -- Build the generated MiOS Spec ADR-0007 promised: laws + conventions rendered from SSOT and drift-gated  (WS-DOCS-SPEC | P1 | L) **[DONE]**
**Goal:** E-06 Test and documentation harness -- the governance layers (laws, ADRs, checks) render into one generated Spec instead of being restated by hand.
**What+How:** ADR-0007 (`usr/share/doc/mios/adr/0007-governance-model-laws-adrs-spec.md`, Implementation section) commits to a generated Spec that is entirely unbuilt: no `[conventions]` table in mios.toml, no `tools/generate-mios-spec.py`, no `usr/share/doc/mios/spec/` (all verified absent). Add a `[conventions]` soft-defaults registry to mios.toml, then write `tools/generate-mios-spec.py` rendering `usr/share/doc/mios/spec/README.md` in the OpenAI-Model-Spec hierarchy -- hard RULES from `[laws]` (all 15 rows: id/slug/applies_to/enforced_by) above soft DEFAULTS from `[conventions]` -- each entry carrying statement + applies_to + enforced_by (its drift-check) + establishing ADR. Add `--check` (regenerate to temp, byte-diff) and wire `check_mios_spec` into the `automation/38-drift-checks.sh` main dispatch. Derive the law set from the registry; never hardcode 13 or 15.
**Where:** `usr/share/mios/mios.toml` (new `[conventions]`, existing `[laws]`), `tools/generate-mios-spec.py` (new), `usr/share/doc/mios/spec/README.md` (new, generated), `automation/38-drift-checks.sh`.
**Done When:** `python3 tools/generate-mios-spec.py --check` exits 0 and a `--write` produces a byte-identical `spec/README.md`; the rendered Spec enumerates all 15 laws plus every `[conventions]` row with its enforced_by; `check_mios_spec` is in the main dispatch and green in a full `bash automation/38-drift-checks.sh`; mutating one `[laws]` slug fails the gate.
**Why:** an accepted ADR's load-bearing deliverable does not exist, so the invariants an arriving agent must obey are scattered across prose surfaces with no single rendered authority.
**Dep:** none
**Who:** you (Python + drift-gate).

## AGY-232 -- ADR frontmatter, law/SSOT-ref and supersedes-symmetry gate via a new tools/adr-index.py  (WS-DOCS-ADR | P1 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness -- ADRs stay immutable, numbered and superseded-not-rewritten, mechanically rather than by convention.
**What+How:** There is NO gate over the ADRs (grepping `38-drift-checks.sh` for adr/superseded/frontmatter finds nothing) and the README "The record" table (`usr/share/doc/mios/adr/README.md`) is hand-edited per the "add a row" step. Mirror `tools/roadmap-index.py` into a new `tools/adr-index.py` that, for every `usr/share/doc/mios/adr/NNNN-*.md`, validates the frontmatter schema (`adr` == filename number; status in proposed|accepted|superseded; date/deciders/tags present), that every `laws[]` id exists in mios.toml `[laws]` (SSOT-derived, not a literal 13), that every `ssot_keys[]` is a real flattened mios.toml key (reuse `roadmap-index.py`'s `flatten_keys`), and that supersedes/superseded_by are symmetric (A.supersedes=[B] iff B.superseded_by=[A] and B.status==superseded). It regenerates the README record table (ADR|Title|Status|Laws|Seeds) under `--check`/`--write`. Wire `check_adr_index` into the drift-gate.
**Where:** `tools/adr-index.py` (new), `usr/share/doc/mios/adr/README.md`, `usr/share/doc/mios/adr/*.md`, `automation/38-drift-checks.sh`.
**Done When:** `python3 tools/adr-index.py --check` is green at HEAD and `--write` reproduces the README record table byte-identically; a bogus law ref, an asymmetric `superseded_by`, or a stale README row each exits non-zero; `check_adr_index` runs in the main dispatch and passes a full gate run.
**Why:** the decision record silently rots -- a superseded ADR can stay marked accepted and the index table can disagree with the files, so the "immutable, cross-linked" governance claim is unbacked.
**Dep:** none
**Who:** you (Python + drift-gate).

## AGY-233 -- Derive the law-id ceiling in roadmap-index.py from [laws] instead of the hardcoded 1..13  (WS-DOCS-ROADMAP | P1 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness -- the docs tooling that enforces governance must not itself violate Law 7 (NO-HARDCODE) / Law 8 (SSOT-PROJECTION).
**What+How:** `tools/roadmap-index.py` line 145 validates roadmap `laws[]` refs with `law < 1 or law > 13`, while mios.toml `[laws]` now registers 15 (14 TARGET-LANGUAGES, 15 DOUBLE-REPO-TRIPLE-CHECK) -- so any ROADMAP workstream legitimately citing Law 14/15 is falsely flagged. The script already loads mios.toml via `tomllib` (lines ~37-41); add a helper deriving the valid id set from the `{ id = N, ... }` rows and replace the numeric bound with membership in that set, erroring with a message naming the max known law id.
**Where:** `tools/roadmap-index.py`, `usr/share/mios/mios.toml` (`[laws]` as the id-set source).
**Done When:** the script contains zero numeric literals for the law count; a check asserts a ROADMAP item citing Law 14 PASSES and one citing Law 99 FAILS; `check_roadmap_index` stays green at HEAD; a full drift-gate run passes.
**Why:** every new law added to the registry is unusable in the ROADMAP until someone remembers to edit a magic number in Python -- the exact drift class the gate exists to prevent.
**Dep:** none
**Who:** you (Python + drift-gate).

## AGY-234 -- Law-prose parity gate across every doc surface, plus refresh the stale 6-law and "13 laws" copies  (WS-DOCS-LAWPARITY | P1 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness -- one law registry, every doc surface a consistent projection of it.
**What+How:** The 15-row `[laws]` registry is restated as prose in several surfaces and two are stale with nothing to catch it: `llms.txt` (lines ~155-167) enumerates only laws 1-6, and `usr/share/doc/mios/adr/README.md` says "the 13 laws (v0.3.0)"; CLAUDE.md is correct at 1-15. Add `check_law_prose_parity` to `automation/38-drift-checks.sh` that extracts the numbered `N. **SLUG**` enumerations from CLAUDE.md, AGENTS.md, MiOS.md, the ADR README and llms.txt, and asserts each surface's id->slug mapping is a consistent subset of `[laws]` -- no wrong slug, no missing tail where the doc claims to list "all" laws, no stale count phrase like "13 laws". Then refresh llms.txt and the ADR README to the full 15-law set so the gate lands green.
**Where:** `automation/38-drift-checks.sh` (new `check_law_prose_parity`), `llms.txt`, `usr/share/doc/mios/adr/README.md`, `usr/share/mios/mios.toml` (`[laws]` SSOT).
**Done When:** after the refresh the check is green in a full drift-gate run; reverting llms.txt to the 1-6 list or restoring the "13 laws" phrasing makes it fail; the check derives the canonical id/slug set from `[laws]` with no hardcoded count.
**Why:** an agent reading llms.txt today learns six laws and violates the other nine -- the surfaces MiOS hands to machines teach an outdated constitution.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-235 -- Generate mios-knowledge-graph.json from SSOT with a regenerate-and-diff gate  (WS-DOCS-KG | P2 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness -- machine-facing docs are SSOT projections (Law 8), not hand-maintained files that rot.
**What+How:** `usr/share/mios/knowledge/mios-knowledge-graph.json` is hand-maintained and stale -- version `"0.2.2"` against VERSION=0.3.0, base "Fedora CoreOS 44", uppercase repo URLs `github.com/mios-dev/MiOS` -- and it escapes `check_version_ssot` because that gate excludes `.json`. Write `tools/generate-knowledge-graph.py` projecting the machine-derivable fields (version from `[meta].mios_version`/VERSION; base, ai_endpoint, repos and ports from mios.toml) and merging hand-authored relationship edges kept in a sibling `mios-knowledge-graph.seed.json`, emitting canonical deterministic JSON (sorted keys). Add `check_knowledge_graph` (regenerate to temp, byte-diff) to the drift-gate.
**Where:** `tools/generate-knowledge-graph.py` (new), `usr/share/mios/knowledge/mios-knowledge-graph.json`, `usr/share/mios/knowledge/mios-knowledge-graph.seed.json` (new), `automation/38-drift-checks.sh`, `usr/share/mios/mios.toml`.
**Done When:** the regenerated graph reports version 0.3.0 and the canonical ai_endpoint/repos; `python3 tools/generate-knowledge-graph.py --check` is green; bumping VERSION without regenerating fails `check_knowledge_graph`; the check runs in the main dispatch and passes at HEAD.
**Why:** the file agents query for MiOS's shape currently answers with a two-release-old version and a wrong base image, and no gate exists to notice.
**Dep:** none
**Who:** you (Python + drift-gate).

## AGY-236 -- Ratcheted dangling-reference detector for AI-related: header cross-refs  (WS-DOCS-XREF | P2 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness -- the cross-reference web that lets an arriving agent pick up a workstream cold stays intact.
**What+How:** Every MiOS-authored file carries an `AI-related:` header (emitted by `usr/libexec/mios/mios-ai-tag`) but nothing validates the tokens resolve, so broken cross-refs accumulate silently. Add `usr/libexec/mios/mios-ai-related-refs` (reusing `mios-ai-tag`'s `walk()`) plus `check_ai_related_refs` in the drift-gate: parse tokens that are repo-relative paths (starting `usr/ etc/ tools/ automation/ srv/ var/`, or a bare `*.md`/`*.py`/`*.sh` resolvable in-tree) and assert each exists; allowlist non-path tokens (host:port, bare service names). Use the same ratchet-ceiling pattern as check 5 (`mios-ai-hint-coverage`) with a max-dangling ceiling recorded in mios.toml, so pre-existing breakage is captured but any NEW dangling ref fails.
**Where:** `usr/libexec/mios/mios-ai-related-refs` (new), `automation/38-drift-checks.sh` (`check_ai_related_refs`), `usr/share/mios/mios.toml` (ceiling key), `usr/libexec/mios/mios-ai-tag` (walk reuse).
**Done When:** the check is green at HEAD within the recorded ceiling; adding a header line referencing a non-existent path (e.g. `usr/lib/mios/does-not-exist.py`) fails the gate; the check appears in the main dispatch and a full run passes.
**Why:** headers pointing at files that were moved or deleted send agents chasing dead paths, and the rot only ever grows because nothing measures it.
**Dep:** none
**Who:** you (Python + drift-gate).

## AGY-237 -- Freshness gate for llms.txt / llms-full.txt (version, endpoint, repo-split paths)  (WS-DOCS-LLMS | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness -- the machine-readable index of MiOS never contradicts the SSOT it describes.
**What+How:** `llms.txt` and `llms-full.txt` are hand-maintained with no gate (grepping the drift-gate for "llms" is empty), yet they state the version and enumerate endpoint and repo-split facts that silently rot. Add `check_llms_freshness` asserting: (a) the version string in both files equals `[meta].mios_version`; (b) the AI endpoint/port referenced matches the canonical `MIOS_AI_ENDPOINT` from mios.toml; (c) every repo-relative path listed in the "Key files" / "Repo split" sections resolves in-tree. Fail-open (SOFT) when a file is absent so a partial checkout still builds.
**Where:** `automation/38-drift-checks.sh` (new `check_llms_freshness`), `llms.txt`, `llms-full.txt`, `usr/share/mios/mios.toml` (`[meta].mios_version`, canonical endpoint).
**Done When:** the check is green at HEAD; editing llms.txt's version to 0.2.9, changing the endpoint away from the SSOT value, or listing a non-existent Key-files path each fails the gate; the check runs in the main dispatch and a full run passes.
**Why:** these two files are the first thing an external agent reads; a stale endpoint in them points every consumer at a port MiOS no longer serves.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-238 -- Gate manual.md against its generator with a --check regenerate-and-diff  (WS-DOCS-MANUAL | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness -- generated documentation is provably generated, not a committed snapshot that drifts.
**What+How:** `tools/generate-manual.py` compiles the 50-chapter / 152-page manual (chapter bodies embedded in the script's `chapters = [...]`) into `usr/share/doc/mios/manual.md`, but it is only invoked by the Justfile `docs` target (Justfile:174) and never gated, so the committed 180KB manual can diverge from its generator unnoticed. Add a `--check` mode that renders to a temp path and byte-diffs against the committed file, plus `check_manual_generated` in the drift-gate calling it; fail-open (SOFT) if `python3` is missing.
**Where:** `tools/generate-manual.py` (add `--check`), `usr/share/doc/mios/manual.md`, `automation/38-drift-checks.sh` (new `check_manual_generated`), `Justfile`.
**Done When:** `python3 tools/generate-manual.py --check` is green at HEAD; hand-editing manual.md without regenerating fails `check_manual_generated`; the check runs in the main dispatch and a full run passes.
**Why:** hand-edits to the manual are silently lost on the next `just docs`, and nobody learns which of the two files was right.
**Dep:** none
**Who:** you (Python + drift-gate).

## AGY-239 -- Negative tests for every new docs / ADR / knowledge-graph gate  (WS-DOCS-NEG | P2 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness -- no gate joins the drift-gate without proof it can fail.
**What+How:** Extend the proven harness (`tests/drift-gate-negatives.sh`, 14 cases; precedent AGY-54/AGY-120) with one case per sibling WS-DOCS gate: `check_mios_spec` (mutate a law slug), `check_adr_index` (asymmetric `superseded_by` + bogus law ref), the roadmap-index law ceiling (Law 14 must pass / Law 99 must fail), `check_law_prose_parity` (revert llms.txt to laws 1-6), `check_knowledge_graph` (bump VERSION only), `check_ai_related_refs` (inject a dangling path), `check_llms_freshness` (stale version), `check_manual_generated` (edit manual.md). Each case mutates, asserts non-zero, restores byte-for-byte, and re-asserts green.
**Where:** `tests/drift-gate-negatives.sh`, `automation/tests/test-drift-gates.sh`.
**Done When:** `bash tests/drift-gate-negatives.sh` runs all new cases green (each injection caught, each restore returning the gate to pass); `bash -n` parses clean; `git status` is clean after the run (no residual mutation).
**Why:** eight new gates land at once; without falsification any of them may be a no-op that reports green forever.
**Dep:** the sibling WS-DOCS gates (AGY-231..AGY-238)
**Who:** you (bash + test-harness).

## AGY-240 -- Dependency-free OTLP/JSON exporter for the in-memory tracer  (WS-OTEL | P1 | M) **[DONE]**
**Goal:** E-24 Autonomy guardrails -- the agent plane is observable offline on the operator's own hardware, with no vendor SDK or live collector in the loop.
**What+How:** `observability/trace.py` is a pure-stdlib in-memory ring ("No tracing backend, no network, no deps") backing `GET /v1/trace`, while the only OTLP export path (server.py ~354-380, `agent_call.py`) needs the heavy `opentelemetry` SDK plus a collector at `:4317` that does not exist offline. Add a pure-stdlib projector `mios_pipe/observability/otlp.py` mapping recorded Tracer spans to OTLP/JSON (resourceSpans -> scopeSpans -> spans; hex traceId/spanId; unixNano start/end; status; attributes as OTLP KeyValue), with a `to_otlp_resource_spans()` builder and a file sink `dump_otlp(tracer, path)` that degrades open and is flag-gated by `[observability].otlp_file`. No new dependencies, no network.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/observability/otlp.py` (new), `test_mios_otlp.py` (new), `usr/share/mios/mios.toml` `[observability]`, `automation/38-drift-checks.sh`.
**Done When:** `test_mios_otlp.py` round-trips a 3-span trace through `to_otlp_resource_spans()` asserting the OTLP shape (`resourceSpans[0].scopeSpans[0].spans`, 32-hex traceId, unixNano ints); a new drift-check validates `otlp.py` against a checked-in golden OTLP/JSON and FAILS on an injected key-shape change; `just drift-gate` green with the test in the `test_mios_*.py` glob and the check in the dispatch list.
**Why:** an offline MiOS can record traces but cannot export them anywhere standard, so debugging an agent run means reading an in-memory ring by hand.
**Dep:** none
**Who:** you (Python + bash + drift-gate).

## AGY-241 -- Pin the GenAI semconv key set in SSOT and gate span-attribute coverage  (WS-OTEL | P2 | M) **[DONE]**
**Goal:** E-24 Autonomy guardrails -- agent-plane telemetry uses one canonical vocabulary so budgets and lane decisions can be measured from it.
**What+How:** `gen_ai.*` attributes are applied ad-hoc in only two files (`agent_call.py` lines 603/604/616/811/812/824, `toolexec.py` 346) and only on the optional OTel path; the in-memory route/plan/dispatch/synthesize spans carry none, and nothing stops a typo'd or deprecated key (`gen_ai.usage.prompt_tokens` vs `input_tokens`). Pin the allowed OTel GenAI semconv key set and its version in SSOT under `[observability.genai_semconv]`, add a projector `mios_pipe/observability/genai_attrs.py` stamping the canonical keys onto the in-memory spans via server.py's `_trace_span` wiring, and add a drift-check that greps every `gen_ai.<x>` string literal in the agent-pipe and fails on any key absent from the registry.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/observability/genai_attrs.py` (new), `test_mios_genai_attrs.py` (new), `usr/share/mios/mios.toml` `[observability.genai_semconv]`, `usr/lib/mios/agent-pipe/server.py`, `automation/38-drift-checks.sh`.
**Done When:** the drift-check flags an injected bogus `gen_ai.foo` literal and passes clean at HEAD, with the negative case wired into `tests/drift-gate-negatives.sh`; `test_mios_genai_attrs.py` asserts the projector emits only registry keys; `just drift-gate` green.
**Why:** token and model attributes are spelled inconsistently and only exist on the optional path, so no dashboard or guardrail can be built on them today.
**Dep:** none
**Who:** you (Python + bash).

## AGY-242 -- W3C Trace Context: adopt inbound traceparent, propagate it outbound  (WS-OTEL | P1 | M) **[DONE]**
**Goal:** E-24 Autonomy guardrails -- a request crossing agent, MCP and A2A hops is one traceable causal chain, including across federated peers.
**What+How:** `observability/trace.py:new_trace_id()` mints a 16-hex (64-bit) id and server.py (~335-345) propagates only a custom `X-MiOS-Trace` header, so MiOS traces never stitch to a standard collector or peer. Widen `new_trace_id()` to 32-hex (128-bit) while keeping `new_span_id()` at 16-hex; add pure helpers `format_traceparent(trace_id, span_id, sampled)` and `parse_traceparent(header)` validating `00-<32hex>-<16hex>-<2hex>`; adopt an inbound `traceparent` in server.py's request-mint path; and emit it outbound on the A2A (`a2a_client._a2a_send_message_to_peer`), MCP (`mcp._mcp_http_rpc`) and Hermes hops alongside the legacy header.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/observability/trace.py`, `usr/lib/mios/agent-pipe/server.py`, `usr/lib/mios/agent-pipe/mios_pipe/federation/a2a_client.py`, `mios_pipe/federation/mcp.py`, `test_mios_trace.py`.
**Done When:** `test_mios_trace.py` asserts `parse(format(id, span, True))` round-trips, rejects a malformed traceparent, and confirms `new_trace_id()` is 32-hex; a test drives an outbound call stub and asserts a valid `traceparent` header is set; existing trace tests still pass; `just drift-gate` green.
**Why:** every federated hop starts a fresh, non-standard trace, so a slow or failed multi-peer request cannot be followed end to end.
**Dep:** none
**Who:** you (Python).

## AGY-243 -- Sign A2A push-notification webhooks with a detached JWS  (WS-FED | P1 | M) **[DONE]**
**Goal:** E-24 Autonomy guardrails -- what the agent plane pushes to a peer is attributable and tamper-evident, not a bare bearer token.
**What+How:** `a2a.py:_a2a_fire_push_notifications` (~line 1096) POSTs the Task envelope with only a static `Authorization: Bearer <token>`, so a receiver cannot verify issuer or integrity -- even though `a2a.py` already owns the Ed25519 passport key and detached-JWS primitives (`_agent_card_signature`, `_agent_card_signing_input`, `_jcs_canonicalize`, `_b64u`). Add `_sign_push_payload(task)` emitting a detached JWS over `JCS(task)` into an `X-A2A-JWS` header, plus a receive-side `verify_push_jws(body, header, pubkey)`, gated by a new `[a2a].sign_push_notifications` SSOT flag (default off, degrade-open when no key is present).
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/federation/a2a.py`, `usr/share/mios/mios.toml` `[a2a]`, `usr/lib/mios/agent-pipe/test_mios_a2a.py`.
**Done When:** `test_mios_a2a.py` performs a real Ed25519 sign->verify round-trip of a push payload (tampering yields verify False) using the file's existing fake-signer stub pattern, and asserts the header is absent when the flag is off; `just drift-gate` green.
**Why:** a leaked or replayed bearer token lets anyone forge task-completion webhooks into a peer, with no way for the receiver to tell.
**Dep:** none
**Who:** you (Python).

## AGY-244 -- Enforce AgentCard signature verification on peer probe  (WS-FED | P1 | S) **[DONE]**
**Goal:** E-24 Autonomy guardrails -- federated peers are trusted only after verification, so a forged card cannot inject skills into the agent plane.
**What+How:** `a2a_client._a2a_fetch_card` / `_a2a_probe_peer` fetch a peer's AgentCard and index its skills without ever calling `a2a.py`'s `_verify_agent_card_signature`, so a signed card's issuer is never checked. Wire verification into the probe behind a new `[a2a].require_signed_cards` SSOT flag: when set, an invalid signature makes the probe reject with status `card-signature-invalid` and record a reputation demerit via the injected `_A2A_REPUTATION`; unsigned cards and flag-off keep today's fail-open behavior. Inject the verifier through `configure()` to preserve the one-way module boundary (no new `a2a_client` -> `a2a` import edge beyond the existing helper import).
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/federation/a2a_client.py`, `usr/share/mios/mios.toml` `[a2a]`, `usr/lib/mios/agent-pipe/test_mios_a2a_client.py`.
**Done When:** `test_mios_a2a_client.py` feeds the probe a tampered-signature card and asserts rejection plus demerit under the flag, and acceptance when the flag is off; `just drift-gate` green.
**Why:** a signed card is currently trusted exactly as much as a forged one -- an attacker advertising skills gets them registered and dispatched to.
**Dep:** none
**Who:** you (Python).

## AGY-245 -- mDNS avahi-template <-> SSOT parity drift-gate  (WS-FED | P2 | S) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- a rendered template can no longer disagree with the route and the SSOT keys it advertises.
**What+How:** `usr/lib/mios/avahi/mios-a2a.service.in` hardcodes `card=/.well-known/agent-card.json` and `proto=a2a` as literal TXT records while substituting `${MIOS_PORT_AGENT_PIPE}` / `${MIOS_A2A_MDNS_SERVICE_TYPE}` at render time; nothing asserts the advertised card path is still a real route or that the service-type placeholder matches `[a2a].mdns_service_type`. Add a static drift-check asserting (1) the template's `card=` TXT path is a well-known agent-card route actually registered in `a2a.py` (grep the `@a2a` route decorators), (2) the port placeholder resolves against `[ports].agent_pipe`, and (3) the service-type placeholder key equals the `[a2a].mdns_service_type` key the browse side reads in `mios-a2a-mdns`.
**Where:** `automation/38-drift-checks.sh`, `usr/lib/mios/avahi/mios-a2a.service.in`, `tests/drift-gate-negatives.sh`.
**Done When:** the check flags an injected card-path mismatch and passes clean at HEAD; the negative case is in `tests/drift-gate-negatives.sh`; the check is registered in the `38-drift-checks.sh` dispatch list; `just drift-gate` green.
**Why:** a route rename silently strips LAN discoverability -- peers keep resolving the mDNS record and fetching a 404 card, with nothing failing loudly.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-246 -- Decompose a2a.py: extract the JSON-RPC task-lifecycle seam into a2a_tasks.py  (WS-DEBT/TD-5 | P1 | L) **[DONE]**
**Goal:** E-02 Technical-debt retirement -- the agent-pipe god-modules are decomposed behind typed boundaries with sibling tests.
**What+How:** `mios_pipe/federation/a2a.py` is 1799 lines / 88KB mixing four concerns (discovery builders, JSON-RPC task lifecycle, push notifications, principal/CRL). Following the AGY-47/52/113/114 strangler pattern, extract the cohesive task-lifecycle cluster -- `_a2a_make_task`, `_a2a_task_record`, `_a2a_now`, `_a2a_make_push_cfg_id`, `_a2a_fire_push_notifications`, `_a2a_dispatch_send`, `_a2a_jsonrpc_dispatch`, `_a2a_stream_response`, `_a2a_sse`, `_a2a_rpc_ok`/`_err`, the `_A2A_TASKS` LRU and push registry -- into a new sibling `mios_pipe/federation/a2a_tasks.py` with a `configure()`-injected boundary (no `a2a`/`server` import edge), leaving thin re-export shims at the old call sites so behavior is byte-identical.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/federation/a2a_tasks.py` (new), `mios_pipe/federation/a2a.py`, `test_mios_a2a_tasks.py` (new).
**Done When:** drift-check 6 (modular-monolith boundary: no server import) and drift-check 11 (sibling test present) pass for the new module; `test_mios_a2a_tasks.py` exercises a `message/send` + `tasks/get` round-trip via stubs; `test_mios_a2a.py` stays green; `just drift-gate` green.
**Why:** four concerns in one 88KB file means every federation change risks the others, and the task lifecycle cannot be tested without dragging in discovery and CRL code.
**Dep:** none
**Who:** you (Python).

## AGY-247 -- MCP tools/list pagination: follow nextCursor instead of registering page 1 only  (WS-MCP | P1 | S) **[DONE]**
**Goal:** E-24 Autonomy guardrails -- the agent plane's tool surface is complete and bounded, not silently truncated.
**What+How:** `mcp._mcp_probe_server` (~line 490) calls `tools/list` exactly once (rid=2), reads `result.tools` and IGNORES `result.nextCursor`; per the MCP spec a server with more tools than one page returns a cursor, so MiOS registers page 1 and drops the rest. Replace the single call with a bounded cursor loop (pass `params={'cursor': nextCursor}` until absent or a max-page cap is hit), accumulating tools before the `allowed_tools` filter and the registry write, and mirror the same fix in the stdio path `_mcp_probe_stdio`. Keep the existing fail-open-per-server behavior.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/federation/mcp.py`, `usr/lib/mios/agent-pipe/test_mios_mcp.py`.
**Done When:** `test_mios_mcp.py` stubs a fake RPC returning two pages (page 1 with `nextCursor`, page 2 without) and asserts ALL tools from both pages register as `mcp.<sid>.<tool>`, and that the page cap prevents an infinite loop; `just drift-gate` green.
**Why:** any MCP server with more than one page of tools is half-registered, so agents cannot call tools the operator installed and the failure is invisible.
**Dep:** none
**Who:** you (Python).

## AGY-248 -- Single-source the MCP protocol_version across consume side, published server and SSOT  (WS-MCP | P2 | S) **[DONE]**
**Goal:** E-09 One value, one name -- one fact has one representation, enforced so it stays collapsed.
**What+How:** `mcp.py` takes the consume-side revision from `[mcp].protocol_version` (`MCP_PROTOCOL_VERSION`, used in the initialize handshake ~line 476) while the PUBLISHED server `usr/libexec/mios/mios-mcp-server` advertises its own revision string, and nothing asserts they agree. Add a static drift-check reading all three statically and asserting the version string in mcp.py's initialize params, the version emitted by `mios-mcp-server`, and `[mcp].protocol_version` are byte-identical.
**Where:** `automation/38-drift-checks.sh`, `usr/lib/mios/agent-pipe/mios_pipe/federation/mcp.py`, `usr/libexec/mios/mios-mcp-server`, `tests/drift-gate-negatives.sh`.
**Done When:** the check flags an injected version skew between the published server and SSOT and passes clean at HEAD; the negative case is in `tests/drift-gate-negatives.sh`; the check is registered in the dispatch list; `just drift-gate` green.
**Why:** MiOS can advertise one MCP revision as a server and negotiate another as a client, producing handshake failures that look like a peer's bug.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-249 -- Record-and-replay cassette harness for A2A/MCP federation HTTP  (WS-TEST | P2 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness -- federation paths are hermetically testable with zero network, like the rest of the gated tree.
**What+How:** The federation outbound calls (`a2a_client._a2a_send_message_to_peer`, `_a2a_fetch_card`, `mcp._mcp_http_rpc`, `_mcp_call_tool`) all go through the injected httpx client and can only be tested by hand-stubbing each call. Add a pure-stdlib cassette harness `mios_pipe/federation/replay.py` that records JSON-RPC request->response pairs to a JSON fixture and provides a `CassettePlayer` async client matching the httpx `get`/`post` surface that `configure()` injects, replaying offline with strict matching and an assertion on any unmatched request. This complements T-040's record-and-replay, which covered LLM dispatch rather than federation.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/federation/replay.py` (new), `test_mios_federation_replay.py` (new), `tests/fixtures/a2a-send.cassette.json` (new).
**Done When:** `test_mios_federation_replay.py` drives `_a2a_send_message_to_peer` end to end from a recorded cassette with no network and asserts the extracted answer text, and asserts an unmatched request raises; `just drift-gate` green with the test in the `test_mios_*.py` glob.
**Why:** without it every federation test hand-rolls stubs, so the real request/response shapes go unexercised and peer-protocol regressions surface only against a live peer.
**Dep:** none
**Who:** you (Python).

## AGY-250 -- Move the SSOT->env projection out of userenv.sh's heredocs into mios_toml.py so all three copies import one implementation  (WS-NAME | P1 | M)**[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- one resolver implementation mints every `MIOS_*` name, so the registry can never describe a projection that three separate copies actually perform.
**What+How:** `walk`, `process_val`, `get_aliases` and the policy constants `EXCLUDED_SECTIONS` / `WALK_MOSTLY_DEAD` / `WALK_EMIT_KEEP` exist in triplicate: the embedded Python heredoc in `usr/lib/mios/userenv.sh` (~L109-600), its byte-twin `tools/lib/userenv.sh`, and `tools/check-resolver-twin.py`, which regex-extracts and `exec()`s userenv's copy (L66-96). Lift them into `usr/lib/mios/mios_toml.py` as importable helpers; make both userenv.sh heredocs `sys.path.insert` + `import mios_toml` and call them; make check-resolver-twin.py import them instead of regex-scraping. Preserve degrade-open: emit nothing if `tomllib` or the import is unavailable. This is the Python-first step ROADMAP WS-LANG names ahead of the AGY-116 Rust crate.
**Where:** `usr/lib/mios/mios_toml.py`, `usr/lib/mios/userenv.sh`, `tools/lib/userenv.sh`, `tools/check-resolver-twin.py`
**Done When:** check 45 (resolver-twin) is green with check-resolver-twin.py importing the helpers and `grep` confirming the `re.search(r'(def get_aliases'` extract is gone; check 27 (byte-twin) still green (both userenv.sh identical); `bash -n` + `python -m py_compile` clean; drift-gate-negatives `test_resolver_twin_equivalence` still flips red on a perturbed alias.
**Why:** Today a single alias-policy change must be made in three places and verified by a regex scrape that silently stops matching when the source is reformatted -- the twin gate can pass while the projections have diverged.
**Dep:** none
**Who:** you (Python + bash, byte-twin).

## AGY-251 -- Give mios_toml a public emit()/`--shell` projection face and thin userenv.sh onto it  (WS-NAME | P1 | M)**[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- the canonical key->env projection becomes callable as a library, not only as a shell side effect.
**What+How:** `usr/lib/mios/mios_toml.py` exposes only `load_merged`/`get`/`section`/`colors` (L30-203) and cannot produce the aliased `{MIOS_*: value}` map that userenv.sh evals, so every Python/Rust consumer must shell out to userenv.sh or scrape `os.environ`. Building on the AGY-123 shared helpers, add `emit(layers=None) -> dict[str,str]` plus a `python3 -m mios_toml --shell` CLI printing shell-quoted `export MIOS_X=...` lines that reproduce userenv.sh exactly -- aliases, `[env]` passthrough, `referenced_names.txt` padding, and the `MIOS_PG_BIND_ADDR` + ports stack-offset transforms. Reduce the userenv.sh heredoc to call `emit()` wherever it is safe to do so.
**Where:** `usr/lib/mios/mios_toml.py`, `usr/lib/mios/userenv.sh`, `tests/test-resolver-emit.py` (new)
**Done When:** `tests/test-resolver-emit.py` asserts `mios_toml.emit()` equals the `MIOS_*` set userenv.sh exports (same comparison shape as check-resolver-twin) and passes; `python3 -m mios_toml --shell | bash` yields byte-identical env to sourcing userenv.sh; a perturbed expectation flips the test red.
**Why:** Without a library face, every non-shell consumer of the SSOT either forks a subshell per lookup or reads a possibly-stale environment, which is exactly how the emitted-vs-consumed key mismatches got in.
**Dep:** AGY-123 shared helpers
**Who:** you (Python).

## AGY-252 -- Catalog the full alias set in names.generated.txt and gate emitted-subset-catalogued  (WS-NAME | P2 | M)**[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- the registry describes every name the resolver actually emits, not just the canonical half.
**What+How:** `names.generated.txt` maps only `dotted.path -> MIOS_<PATH_CANONICAL>` (plus vllm/sglang shorts via `_alias_for`), so the ~200 aliases consumers really reference (`MIOS_PORT_SSH`, `MIOS_SSH_PORT`, `MIOS_USER`, `MIOS_AI_KEY`, `MIOS_TIMEZONE`) appear nowhere in the registry -- verified: grep finds 0 singular `MIOS_PORT_` rows, only plural `MIOS_PORTS_`. Extend `tools/generate-names-registry.py` to emit a third column of `get_aliases()`-derived names (from the AGY-123 shared `get_aliases`), and add a drift-check asserting every name userenv.sh emits is catalogued (emitted is a subset of names.generated.txt).
**Where:** `tools/generate-names-registry.py`, `usr/share/mios/names.generated.txt`, `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`
**Done When:** check 30 (names registry) green with the enriched 3-column catalog; the new emitted-subset-catalogued assertion green at HEAD; a negative-test that deletes one alias row makes it fail.
**Why:** An uncatalogued alias is an uncontrolled second name for a fact (a Law 9 violation the gate cannot see), and a newcomer grepping the registry for `MIOS_PORT_SSH` concludes it does not exist.
**Dep:** AGY-123 shared `get_aliases`
**Who:** you (Python + drift-gate).

## AGY-253 -- Derive the names-generator's section set from the resolver's EXCLUDED_SECTIONS instead of a hand allowlist  (WS-NAME | P2 | S)**[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- one section policy, shared by the emitter and the catalog.
**What+How:** The registry generator uses a hand-maintained `TARGET_SECTIONS` allowlist (`generate-names-registry.py` L16-23: ports/ai/identity/.../mini) while userenv.sh walks every top-level table EXCEPT `EXCLUDED_SECTIONS` (`userenv.sh` L549). The two policies drift silently: a new `[section]` in mios.toml gets env-emitted by the resolver but omitted from the catalog. Make the generator compute its section set as (all vendor top-level tables) minus the shared `EXCLUDED_SECTIONS` constant, and add a drift-check asserting the generator's covered-section set equals the resolver's emitted-section set.
**Where:** `tools/generate-names-registry.py`, `usr/lib/mios/mios_toml.py` (shared `EXCLUDED_SECTIONS`), `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`
**Done When:** new `check_names_section_parity` green at HEAD; a negative-test adding a dummy `[zzz]` table to `usr/share/mios/mios.toml` fails until it is catalogued in `names.generated.txt`.
**Why:** Every new SSOT section added today enters the environment invisible to the registry, so the "one canonical name" gate under-reports exactly at the moment new names are minted.
**Dep:** AGY-252
**Who:** you (Python + drift-gate).

## AGY-254 -- Add a schema-validation drift-gate for every [dotfiles.registry.*] surface  (WS-DOTFILES | P2 | M)**[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- a malformed surface declaration fails at gate time, not at a user's firstboot.
**What+How:** About 15 surfaces declare kinds (template/json-merge/ini-merge/ssh-merge/registry, `mios.toml` L8648-11146) but no drift-check validates their well-formedness: a surface can declare `kind=json-merge` with no fixture, or a `template`/`apply.target` that does not exist, and only fail at runtime. Add `check_dotfiles_registry` to `38-drift-checks.sh`, reading the SSOT via `mios_toml.load_vendor()` and asserting per surface: the `kind` is a known one; merge kinds have both `fixture.base` and `fixture.expected` present on disk; template kinds' template file exists; `section` names a real mios.toml table; `apply.target` is present where the kind requires it.
**Where:** `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`
**Done When:** `check_dotfiles_registry` green at HEAD; a new negative case in `tests/drift-gate-negatives.sh` (a surface declaring `kind=json-merge` but missing its fixture) flips it red, then restores green.
**Why:** The dotfiles engine is the projection path onto live HOME on two platforms; an unvalidated registry row turns an SSOT typo into a broken first boot on real hardware.
**Dep:** none
**Who:** you (bash + Python + drift-gate).

## AGY-255 -- Finish the mios-theme-render deprecation: repoint live callers, silence the shim, add a grep-gate  (WS-DOTFILES | P2 | M)**[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- one render entry point named once.
**What+How:** `usr/libexec/mios/mios-theme-render` is now a shim that prints a stderr WARNING on EVERY invocation (L8) and delegates to `mios-dotfiles-render`, yet live surfaces still call the old name: `usr/libexec/mios/mios-sync-theme`, `usr/libexec/mios/mios-dotfiles`, `etc/btop/btop.conf`, `usr/lib/mios/agent-pipe/mios_pipe/routing/portal.py`. Repoint every real invocation to `mios-dotfiles-render`, refresh the stale AI-hint comment lines, remove the per-call WARNING (keep the shim as a silent, name-stable alias for external compat), and add a grep-gate that no file outside the shim/tests/docs invokes the executable name `mios-theme-render`.
**Where:** `usr/libexec/mios/mios-theme-render`, `usr/libexec/mios/mios-sync-theme`, `usr/libexec/mios/mios-dotfiles`, `etc/btop/btop.conf`, `usr/lib/mios/agent-pipe/mios_pipe/routing/portal.py`, `automation/38-drift-checks.sh`
**Done When:** the new grep-gate is green (only allowlisted shim/test/doc references to `mios-theme-render` remain); invoking the shim produces no stderr output (asserted by a test call); `python -m py_compile` clean on the touched Python.
**Why:** Every theme sync currently spews a deprecation WARNING into user-visible output and into the Portal's logs, and the two live names keep the "one canonical name" invariant unenforceable.
**Dep:** none
**Who:** you (Python + bash + drift-gate).

## AGY-256 -- Second-apply idempotency test for every merge-kind surface, wired into the drift-gate  (WS-DOTFILES | P2 | S)**[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- re-provisioning is safe, so firstboot can deploy every time.
**What+How:** `tests/test-theme-merge.py` covers json/ini merge SEMANTICS and abort cases (unknown kind, missing fixture) but never asserts that apply is idempotent on re-run -- the firstboot re-provision contract (cf. AGY-38 "deploy every time") requires apply-of-apply == apply so a second boot does not duplicate keys or `[remote]` sections. Add a test that, for each merge surface (json-merge/ini-merge/ssh-merge), renders and applies onto `fixture.base`, asserts equality to `fixture.expected`, then applies AGAIN and asserts a zero diff; wire it into a drift-gate hook so it runs in CI.
**Where:** `tests/test-theme-merge.py`, `usr/libexec/mios/mios-dotfiles-render`, `automation/38-drift-checks.sh`
**Done When:** the idempotency test passes for all merge surfaces at HEAD; a deliberately non-idempotent stub surface makes it fail; the drift-gate invokes the test and is green at HEAD.
**Why:** Without it, a non-idempotent merge silently accumulates duplicated config blocks in a user's real dotfiles on every reboot, and nothing catches it until the file is corrupt.
**Dep:** AGY-254
**Who:** you (Python + drift-gate).

## AGY-257 -- Generate naming-unification.md's alias table from the shared get_aliases (docs-from-code + gate)  (WS-DOCS | P2 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- the naming reference is a projection of the resolver, not a hand-copy of it.
**What+How:** `usr/share/doc/mios/reference/naming-unification.md` documents the WS-NAME key<->env mapping by hand, so it rots whenever `get_aliases` changes -- and it is already treated as authoritative naming doc (listed as an emitter-suffix the var-closure gate excludes). Add a generator emitting the doc's alias table (dotted-path -> canonical -> aliases) from the AGY-123 shared `get_aliases`, plus a drift-check mirroring check 30 that asserts the committed generated block matches.
**Where:** `usr/share/doc/mios/reference/naming-unification.md`, `tools/generate-naming-doc.py` (new, or extend `tools/generate-names-registry.py`), `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`
**Done When:** regenerating the doc's alias block is a no-op at HEAD; the new gate fails when `get_aliases` changes without regenerating the doc; a negative-test flips it red then restores.
**Why:** The one document an arriving agent reads to learn MiOS naming can currently disagree with the resolver, and nothing reports the disagreement.
**Dep:** AGY-252
**Who:** you (Python + drift-gate).

## AGY-258 -- DRY the triplicated DB-authoritative overlay branch in mios_toml.py  (WS-DEBT | P3 | S)**[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- one DB-overlay policy in the resolver, cache included.
**What+How:** The `import mios_db_config; if is_db_authoritative(): deep_merge(db_cfg)` block is copy-pasted three times in `usr/lib/mios/mios_toml.py` -- `load_merged` (L116-124), `get` (L158-167), `colors` (L192-200) -- so any DB-overlay policy change must be made in three spots, and `clear_cache()` (L102) does not account for the DB path. Extract a single `_db_overlay()` helper used by all three call sites and cover the DB path in `clear_cache`.
**Where:** `usr/lib/mios/mios_toml.py`
**Done When:** `python -m py_compile` clean; check 45 (resolver-twin) and the existing resolver tests still green; a unit test asserts the DB overlay is applied identically through the single `_db_overlay` helper across `load_merged`/`get`/`colors`.
**Why:** Three copies plus a cache that ignores one of them means a DB-layer change can take effect through `get()` but not `colors()`, producing config that differs by which accessor asked.
**Dep:** none
**Who:** you (Python).

## AGY-259 -- Generalize the globals.{sh,ps1} fallback-drift gate beyond ports  (WS-DEBT | P2 | M)**[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- no shell-level fallback literal may shadow a resolved SSOT value.
**What+How:** Check 28 only guards `MIOS_PORT_*` fallback literals in `automation/lib/globals.sh` and `globals.ps1`; other SSOT-derived defaults hardcoded there still silently shadow the resolver -- exactly the class AGY-89 hit when globals.sh clobbered `MIOS_IMAGE_NAME`. Generalize the gate: for each `MIOS_<X>` fallback literal (`:=<lit>` in bash, `else{<lit>}` in PowerShell) in `globals.{sh,ps1}`, if `<X>` maps to a mios.toml key via `names.generated.txt`, assert the literal equals the value resolved from `mios_toml.load_vendor()`.
**Where:** `automation/38-drift-checks.sh`, `automation/lib/globals.sh`, `automation/lib/globals.ps1`, `tests/drift-gate-negatives.sh`
**Done When:** the generalized check is green at HEAD; a negative-test perturbing a non-port fallback (e.g. `MIOS_IMAGE_NAME`) flips it red, then restores green.
**Why:** A stale non-port fallback in globals.sh silently overrides the SSOT for the entire build -- the failure mode that once made the publish path push under the wrong image name.
**Dep:** none
**Who:** you (bash + Python + drift-gate).

## AGY-260 -- Extract ONE shared build-time latest-tag resolver (releases/latest redirect, no GitHub API)  (WS-FLOAT | P1 | M)**[DONE]**
**Goal:** E-14 Float latest globally -- a single resolver every build path uses to turn "latest" intent into a concrete tag without a rate limit.
**What+How:** `usr/share/mios/sys/Containerfile` inlines the SAME `t="$(curl -fsSL -o /dev/null -w '%{url_effective}' --retry 5 --connect-timeout 20 <repo>/releases/latest | sed 's#.*/tag/##')"` one-liner eight times (matchbox L46, forgejo L49, forgejo-runner L52, jaeger L55, crowdsec L59, k3s L62, code-server L65, firecrawl L10 and L111), and no shared helper exists (grep for `url_effective` across `automation/lib` returns nothing). Author ONE resolver in both shapes: (1) a bash function `mios_resolve_latest_tag <releases-base-url>` in `automation/lib/common.sh` next to `record_version` (L103) doing the redirect-follow and echoing the bare tag, non-fatal (empty on failure so callers degrade open); (2) a standalone dependency-free `usr/libexec/mios/mios-resolve-latest` (`#!/bin/bash`, `set -uo pipefail`) with the same logic, so the sys Containerfile can COPY+RUN it inside the nested build where `lib/common.sh` is not sourced. Use the redirect, NOT `api.github.com`. Add `usr/libexec/mios/test_mios_resolve_latest.sh`, stubbing `curl` via a PATH shim, asserting a `/tag/v1.2.3` effective URL yields `v1.2.3` and a curl failure yields empty plus exit 0.
**Where:** `automation/lib/common.sh`, `usr/libexec/mios/mios-resolve-latest` (new), `usr/libexec/mios/test_mios_resolve_latest.sh` (new)
**Done When:** `bash -n automation/lib/common.sh usr/libexec/mios/mios-resolve-latest` clean; the unit test passes (stubbed-curl tag extraction plus the degrade-open empty case); `just drift-gate` green.
**Why:** Eight copies of one curl incantation means eight places to fix when a forge changes its redirect, and no single place to make float-latest resolution rate-limit-free and testable.
**Dep:** none
**Who:** you (bash).

## AGY-261 -- Refactor sys/Containerfile's 8 inlined redirect-resolves onto the AGY-260 resolver  (WS-FLOAT | P1 | M)**[DONE]**
**Goal:** E-14 Float latest globally -- the sys image's floated components resolve through the one shared resolver.
**What+How:** Thread the standalone resolver into the sys build: in `usr/libexec/mios/57-mios-sys-build.sh` (already passing `--build-arg` BASE_IMAGE + SEARXNG_REF at L77-78) add `COPY usr/libexec/mios/mios-resolve-latest /usr/local/bin/mios-resolve-latest` early in `usr/share/mios/sys/Containerfile`, ensuring the file is in the context materialized by `automation/00-materialize-build-ctx.sh`. Replace each of the eight inlined `url_effective` fragments (L10, L46, L49, L52, L55, L59, L62, L111) with `t="$(mios-resolve-latest https://github.com/<org>/<repo>)"` (codeberg/forgejo forge base for L49, code.forgejo.org for L52). Preserve every downstream transform verbatim (`v="${t#v}"`, the k3s `+`->`%2B` encode at L62, `--wildcards` tar strips) and keep `--retry` on the actual asset download `curl` lines so check 64 still applies. AdGuard L41 has no tag interpolation (`releases/latest/download`) -- leave it.
**Where:** `usr/share/mios/sys/Containerfile`, `usr/libexec/mios/57-mios-sys-build.sh`, `automation/00-materialize-build-ctx.sh` (context copy only if needed)
**Done When:** no `url_effective` string remains in `usr/share/mios/sys/Containerfile`, each replaced by a `mios-resolve-latest` call; `check_containerfile_pinned_clones` (check 71) still green (firecrawl clones still carry `--branch "${t}"`); `bash -n usr/libexec/mios/57-mios-sys-build.sh` clean; `just drift-gate` green.
**Why:** Until the callers move, the shared resolver is dead code and the eight-copy maintenance cost stays exactly where it is.
**Dep:** AGY-260
**Who:** you (bash + Containerfile).

## AGY-262 -- Convert the rate-limited api.github.com callers to the redirect resolver and delete their hand-pinned fallback tags  (WS-FLOAT | P1 | M)**[DONE]**
**Goal:** E-14 Float latest globally -- no build phase depends on a rate-limited API, and no phase carries a hand-written version literal as its safety net.
**What+How:** Five automation phases resolve tags via the GitHub REST API and carry a hand-pinned `_FALLBACK_TAG`: `automation/09-fonts.sh:71` (nerd-fonts, `NERD_FALLBACK_TAG="v3.4.0"` L73), `automation/38-oh-my-posh.sh:33`, `automation/41-gpu-cdi-toolkits.sh:45,79-82` (`AMD_CTK_FALLBACK_TAG="v1.3.0"` L40, `INTEL_SG_FALLBACK_TAG="v0.7.0"` L41), `automation/42-cosign-policy.sh:30` (`COSIGN_FALLBACK_VERSION="v2.6.4"` L29), `automation/10-gnome.sh:74` (BIBATA_VER). Replace each `scurl ... api.github.com/repos/<org>/<repo>/releases/latest | jq` block with `mios_resolve_latest_tag https://github.com/<org>/<repo>` (sourced via `lib/common.sh`) and DELETE the `_FALLBACK_TAG`/`_FALLBACK_VERSION` literals: on an empty resolve, degrade open (WARN + skip that install, as the Nerd-font path already does) rather than substituting a hand-pin. Keep the `record_version` calls (09-fonts L78, 42-cosign L59) intact, passing the resolved tag.
**Where:** `automation/09-fonts.sh`, `automation/38-oh-my-posh.sh`, `automation/41-gpu-cdi-toolkits.sh`, `automation/42-cosign-policy.sh`, `automation/10-gnome.sh`
**Done When:** no `api.github.com/.../releases` and no `*_FALLBACK_TAG`/`*_FALLBACK_VERSION` literal remains in the five files; each resolves via `mios_resolve_latest_tag` and degrades open on empty; `bash -n` clean on all five; `just drift-gate` green.
**Why:** An unauthenticated API rate limit turns a CI bake into a silent fallback to a frozen 2024-era font/cosign/toolkit version, and the SBOM then records a version nobody chose.
**Dep:** AGY-260
**Who:** you (bash).

## AGY-263 -- Create the [build.float] SSOT allowlist for legitimately-pinned build inputs  (WS-FLOAT | P2 | S)**[DONE]**
**Goal:** E-14 Float latest globally -- there is exactly ONE documented place a pin may legally live.
**What+How:** A handful of inputs are genuinely capped by an upstream compatibility constraint, and today those pins live inline with only prose comments (`usr/share/mios/sys/Containerfile:88-92` uses `python3.12` because every open-webui release caps `requires_python <3.13`; `usr/share/mios/webtools/firecrawl.Containerfile:74` pins `pnpm@9.15.4` for Node-20 `node:sqlite` compat; `[image.sidecars].pgvector = 0.8.3-pg17` is pinned for `hnsw.iterative_scan`). Add a `[build.float]` table to `usr/share/mios/mios.toml` next to `[build.bake_refs]` (~L8554) with an `allow` array of inline tables `{ key, ref, reason, upstream }`. Seed the three known-legit pins plus a comment block stating that ANY entry is an EXCEPTION to float-latest and MUST carry a real upstream reason. Include a commented `mios_toml.get('build.float', 'allow')` reader example; do NOT wire enforcement here (AGY-270/271).
**Where:** `usr/share/mios/mios.toml` (`[build.float]`, new)
**Done When:** `python3 -c "import tomllib,sys; tomllib.load(open('usr/share/mios/mios.toml','rb'))"` parses; `[build.float].allow` lists the 3 seed exceptions each with non-empty `ref`/`reason`/`upstream`; `just drift-gate` green (no gate references it yet).
**Why:** With no allowlist there is no way to distinguish a justified upstream cap from an accidental hand-pin, so a no-hand-pin gate cannot be written at all.
**Dep:** none
**Who:** you (TOML + Python).

## AGY-264 -- Float syft to latest and record the resolved syft version (drop the v1.19.0 pin)  (WS-FLOAT | P2 | S)**[DONE]**
**Goal:** E-14 Float latest globally -- even the SBOM generator itself is floated and self-recorded.
**What+How:** `automation/90-generate-sbom.sh:32-36` reads `build.bake_refs.syft` and defaults to `SYFT_PIN="${SYFT_PIN:-v1.19.0}"`, and `mios.toml [build.bake_refs].syft = "v1.19.0"` is the pin. Change the bootstrap to float: invoke the official installer with NO version arg (`| sh -s -- -b /usr/local/bin`) or resolve via `mios-resolve-latest https://github.com/anchore/syft` and pass that; remove the `v1.19.0` literal and the `build.bake_refs.syft` key. Once syft exists, capture the installed version (`syft version -o json` -> `version`, degrade-open) and `record_version syft "$ver" https://github.com/anchore/syft` so it lands in `MIOS_VERSION_MANIFEST`. Keep the script degrade-open (`exit 0`, no `set -e`) -- an empty resolve falls through to the existing "syft unavailable -> skip SBOM" WARN path.
**Where:** `automation/90-generate-sbom.sh`, `usr/share/mios/mios.toml [build.bake_refs].syft` (remove)
**Done When:** no `v1.19.0` literal and no `build.bake_refs.syft` key remain; syft installs floating-latest and its resolved version is `record_version`-ed; `automation/test_generate-sbom.sh` still passes (degrade-open invariants hold); `bash -n automation/90-generate-sbom.sh` clean; `just drift-gate` green.
**Why:** The tool that produces the provenance record is itself frozen at a hand-typed version and does not appear in its own output -- the SBOM cannot say which syft made it.
**Dep:** AGY-260
**Who:** you (bash).

## AGY-265 -- Float FIRECRAWL_REF in firecrawl.Containerfile and allowlist the pnpm@9 Node-20 cap  (WS-FLOAT | P2 | M)**[DONE]**
**Goal:** E-14 Float latest globally -- the firecrawl wrapper matches the sys image's float behaviour, with its one real cap recorded as an exception.
**What+How:** `usr/share/mios/webtools/firecrawl.Containerfile` hand-pins two things: `ARG FIRECRAWL_REF=v1.0.0` (L33, consumed by `git clone --branch "${FIRECRAWL_REF}"` at L50) and `RUN npm install -g pnpm@9.15.4` (L74, twice). Resolve the tag at build via `mios-resolve-latest https://github.com/mendableai/firecrawl` and thread it as `--build-arg FIRECRAWL_REF=<resolved>` from the builder that invokes this Containerfile (grep `firecrawl.Containerfile` under `usr/libexec/mios`/`automation`), defaulting `ARG FIRECRAWL_REF=master` (a moving ref, not a version) so a bare build still floats. The `pnpm@9.15.4` pin is a documented Node-20 `node:sqlite` constraint (comment L69-73): do NOT float it -- register it in `[build.float].allow` as `{ key = "firecrawl-pnpm", ref = "9", reason = "pnpm 11 needs Node22 node:sqlite; firecrawl v1 base is Node20", upstream = "..." }` and relax the literal to `pnpm@9` so only the MAJOR is constrained. `[image.sidecars].firecrawl`/`[build.bake].core` reference `localhost/mios-firecrawl:v1.0.0` -- that names a LOCAL build, so leave the local tag stable but add a `TODO(agy)` note if the resolved ref no longer equals v1.0.0.
**Where:** `usr/share/mios/webtools/firecrawl.Containerfile`, the builder that runs it (`usr/libexec/mios/*`, resolve via grep), `usr/share/mios/mios.toml [build.float]`
**Done When:** `FIRECRAWL_REF` resolves latest at build (bare-build default is a branch, not `v1.0.0`); `pnpm@9.15.4` is relaxed to `pnpm@9` and recorded in `[build.float]`; `bash -n` clean on the touched builder; `just drift-gate` green.
**Why:** Firecrawl is frozen at a 2024 release while the sys image floats it, so the two paths ship different crawler code from the same tree.
**Dep:** AGY-260, AGY-263
**Who:** you (bash + Containerfile).

## AGY-266 -- Float the git-clone bake refs (quickshell, lookingglass) at build instead of hand-pinned tags  (WS-FLOAT | P2 | M)**[DONE]**
**Goal:** E-14 Float latest globally -- source-built components carry `latest` intent in SSOT, resolved at bake.
**What+How:** `automation/55-bake-quickshell.sh:14` (`PIN_REF="${MIOS_BUILD_BAKE_REFS_QUICKSHELL:-v0.3.0}"`) and `automation/53-bake-lookingglass-client.sh:42` (`LG_BRANCH="${MIOS_BUILD_BAKE_REFS_LOOKINGGLASS:-B7}"`) clone hand-pinned tags sourced from `[build.bake_refs]`. Set those SSOT values to the sentinel `"latest"`; when the resolved value is `latest`, the baker calls `mios-resolve-latest` against the quickshell / looking-glass repos to get the newest tag at build, while a concrete non-`latest` value is still honored as an explicit operator override. Keep the AGY-64 non-empty-default guard (`check_bake_ref_defaults`, check 47) satisfied by using `:-latest`, which then triggers resolution. `hyprland="main"` and `surfer=<sha>` are already moving/immutable refs consistent with float -- leave them, but add a one-line comment noting the `surfer` commit-sha is intentional (upstream has no tags); if `surfer` does have upstream tags, switch it to `"latest"` too.
**Where:** `automation/55-bake-quickshell.sh`, `automation/53-bake-lookingglass-client.sh`, `usr/share/mios/mios.toml [build.bake_refs]` (quickshell/lookingglass -> `"latest"`)
**Done When:** both bakers resolve latest at build when SSOT is `latest`, honor an explicit override otherwise, and keep non-empty `:-` defaults; `check_bake_ref_defaults` (47) green; `bash -n` clean on both bakers; `just drift-gate` green.
**Why:** The desktop shell (quickshell) and the VM display client stay stuck at whichever tag someone typed once, so upstream fixes never reach the image without a manual SSOT edit.
**Dep:** AGY-260
**Who:** you (bash).

## AGY-267 -- Reconcile the hand-pinned [image.sidecars] registry tags: float where a channel exists, allowlist where capped  (WS-FLOAT | P2 | M)**[DONE]**
**Goal:** E-14 Float latest globally -- every sidecar tag is either a floating channel or a recorded, reasoned exception.
**What+How:** Several `[image.sidecars]` entries (mirrored verbatim into `[build.bake].core` ~L6611) carry hand-pinned upstream tags: `k3s = docker.io/rancher/k3s:v1.32.1-k3s1`, `ceph = quay.io/ceph/ceph:v19`, `forge = codeberg.org/forgejo/forgejo:12`, `forge_runner = code.forgejo.org/forgejo/runner:7`, `pgvector = docker.io/pgvector/pgvector:0.8.3-pg17`. Audit each: (a) `forge:12`/`forge_runner:7` already float within a major -- annotate and leave; (b) `ceph:v19` likewise -- annotate and leave; (c) `pgvector:0.8.3-pg17` is a documented exact pin (`hnsw.iterative_scan` needs 0.8.0+) -- register it in `[build.float].allow` and keep; (d) `k3s:v1.32.1-k3s1` is a fully-frozen patch pin -- either float it to the k3s stable-channel tag or, if the exact patch is load-bearing (the L7883 comment documents the `docker.io/` prefix for short-name resolution, not the patch), register the exact pin in `[build.float]` with that reason. Keep `[build.bake].core` and `[image.sidecars]` byte-consistent for whichever entries change, since they duplicate the literal.
**Where:** `usr/share/mios/mios.toml [image.sidecars]`, `[build.bake].core`, `[build.float].allow`
**Done When:** every remaining hand-pinned sidecar tag is either a documented floating-channel tag with an inline note or listed in `[build.float].allow` with a reason; `[image.sidecars]`/`[build.bake].core` stay consistent; the TOML parses; `just drift-gate` green including the core-vs-Quadlet reconcile gate (check 35/137).
**Why:** Frozen patch tags mean shipped CVE fixes never reach the fleet, and the duplicated literal in two tables makes a one-sided edit a silent bake/runtime mismatch.
**Dep:** AGY-263
**Who:** you (bash + TOML).

## AGY-268 -- Allowlist or float the machine_os_tag pin (podman-machine-os major)  (WS-FLOAT | P3 | S)**[DONE]**
**Goal:** E-14 Float latest globally -- the last WSL2-path pin is classified rather than left ambiguous.
**What+How:** `usr/share/mios/mios.toml [image].machine_os_tag = "6.0"` (L6605) is consumed by the WSL2 podman-machine init (the L6602 comment says the canonical 6.0 tag is operator-overridable via mios.html). `quay.io/podman/machine-os` publishes floating major tags, so if `6.0` genuinely tracks latest 6.0.x it only needs an inline annotation; if podman-machine compat requires the machine-os MAJOR to match the installed podman major (confirm by reading how `usr/libexec/mios/44-podman-machine-compat.sh` consumes the tag), register `machine_os_tag` in `[build.float].allow` with `reason = "machine-os major must match host podman major"` plus an upstream URL. Add a one-line comment at the SSOT key pointing at the `[build.float]` entry.
**Where:** `usr/share/mios/mios.toml [image].machine_os_tag`, `[build.float].allow`, `usr/libexec/mios/44-podman-machine-compat.sh` (read-only, to confirm the constraint)
**Done When:** `machine_os_tag` is either annotated as a floating channel or registered in `[build.float]` with a documented reason; TOML parses; `just drift-gate` green.
**Why:** An unclassified pin will trip the future no-hand-pin gate with no way for the next reader to tell whether it is a bug or a compat requirement.
**Dep:** AGY-263
**Who:** you (TOML + bash).

## AGY-269 -- Record every build-time-resolved latest version into the SBOM manifest  (WS-FLOAT | P2 | M)**[DONE]**
**Goal:** E-14 Float latest globally -- floating is safe only because the SBOM records what actually resolved (ADR-0003).
**What+How:** The sys Containerfile floats ~11 components (matchbox, forgejo, forgejo-runner, jaeger, crowdsec, k3s, code-server, AdGuard, searxng, open-webui, firecrawl) but records NONE: they run inside the nested `mios-sys` podman build where `automation/lib/common.sh`'s `record_version` is not sourced, so resolved tags never reach the SBOM (only bound-image digests land in `bound-images.tsv` via `usr/libexec/mios/mios-bake-group:135`). After each `mios-resolve-latest` call in `usr/share/mios/sys/Containerfile`, append `printf '%s\t%s\n' <component> "$t" >> /usr/share/mios/artifacts/sbom/resolved-latest.tsv` (mkdir first, degrade-open). Then in `automation/90-generate-sbom.sh` (or `mios-bake-group`'s SBOM step) merge those rows into `binaries.tsv` as `name<TAB>version<TAB>unknown` so `check_sbom_metadata` (check 49) validates them -- it already tolerates `sha256 == "unknown"`. Keep everything degrade-open (missing file -> skip).
**Where:** `usr/share/mios/sys/Containerfile`, `automation/90-generate-sbom.sh`, `usr/share/mios/artifacts/sbom/` (new `resolved-latest.tsv` producer)
**Done When:** a sys build emits `resolved-latest.tsv` with one row per floated component; those rows are merged into `binaries.tsv` (name/version non-empty, `sha256=unknown` allowed); `check_sbom_metadata` (49) green; `bash -n automation/90-generate-sbom.sh` clean; `just drift-gate` green.
**Why:** Right now floating eleven components makes the image un-reproducible in practice -- nothing in the shipped artifact says which forgejo, k3s or open-webui version is inside it.
**Dep:** AGY-261
**Who:** you (bash).

## AGY-270 -- Drift-gate: no hand-pinned version literal outside the [build.float] allowlist  (WS-FLOAT | P1 | M)**[DONE]**
**Goal:** E-14 Float latest globally -- the campaign becomes an enforced invariant instead of a one-time cleanup.
**What+How:** Add `check_float_no_handpin` to `automation/38-drift-checks.sh`, registered in `main()` after `check_containerfile_pinned_clones` and labelled (72). A Python body walks `automation/**/*.sh`, `usr/libexec/mios/**`, `usr/share/mios/**/Containerfile*`, and the `[build.bake_refs]`/`[image.sidecars]` tables of `usr/share/mios/mios.toml`, flagging hand-pinned version literals -- `_FALLBACK_TAG=`/`_FALLBACK_VERSION=` assignments, `pip install ...==<ver>`, `npm install ...@<N.N.N>`, `:vN.N.N`-style exact OCI patch tags -- UNLESS the component key appears in `[build.float].allow` (loaded via tomllib). Floating tokens (`:latest`, `:main`, bare-major `:12`/`:7`/`v19`, `mios-resolve-latest` results, moving branches, `localhost/*` images) must NOT trip it. Add a negative test in `tests/drift-gate-negatives.sh` injecting `FOO_FALLBACK_TAG="v9.9.9"` into an automation script, asserting red, then restoring green.
**Where:** `automation/38-drift-checks.sh`, `usr/share/mios/mios.toml [build.float]`, `tests/drift-gate-negatives.sh`
**Done When:** `check_float_no_handpin` green at HEAD (after AGY-262/264/265 land); the negative test flips it red then restores; the gate ignores allowlisted keys and floating tags; `just drift-gate` green.
**Why:** Without the gate the next contributor re-adds a fallback tag in one commit and the whole float-latest campaign quietly unwinds.
**Dep:** AGY-262, AGY-263, AGY-264, AGY-265
**Who:** you (bash + Python + drift-gate).

## AGY-271 -- Drift-gate: every [build.float] exception is documented and still live  (WS-FLOAT | P2 | S)**[DONE]**
**Goal:** E-14 Float latest globally -- the exception list stays small, justified and non-rotting.
**What+How:** Add `check_float_allowlist_documented` to `automation/38-drift-checks.sh` (label (73)): load `[build.float].allow` via tomllib and assert (a) every entry has non-empty `key`, `ref`, `reason` and `upstream` (a well-formed `https?://` URL), and (b) every entry's `key` is actually REFERENCED somewhere in the tree -- grep the `ref`/component across `automation`, `usr/libexec`, `usr/share/mios/**/Containerfile*` and `mios.toml` -- so a dead entry (nothing pins it anymore) reds and stale exceptions get pruned. Add two negative tests in `tests/drift-gate-negatives.sh`: an entry with an empty `reason` reds; an entry whose `key` matches nothing reds; both restore green.
**Where:** `automation/38-drift-checks.sh`, `usr/share/mios/mios.toml [build.float]`, `tests/drift-gate-negatives.sh`
**Done When:** the check is green at HEAD with the seeded exceptions; a reason-less entry and a dead entry each red; the negatives restore green; `just drift-gate` green.
**Why:** An allowlist nobody prunes becomes a permanent bypass, and AGY-270 exempts exactly what it lists -- so a rotten entry silently re-legalizes a hand-pin.
**Dep:** AGY-263, AGY-270
**Who:** you (Python + drift-gate).

## AGY-272 -- Grep-gate: ban the rate-limited api.github.com release-lookup mechanism  (WS-FLOAT | P2 | S)**[DONE]**
**Goal:** E-14 Float latest globally -- all future tag resolution is forced through the redirect resolver.
**What+How:** After AGY-262 converts the API callers, add `check_no_github_api_release` to `automation/38-drift-checks.sh` (label (74)) grepping `automation/**/*.sh`, `usr/libexec/mios/**` and `usr/share/mios/**/Containerfile*` for `api.github.com/repos/[^ ]*/releases` and redding on any hit. Allowlist nothing except test fixtures and reference docs, mirroring the exclusion style of `check_curl_retry` (which skips `test-drift-gates.sh`). Add a negative test in `tests/drift-gate-negatives.sh` injecting `curl https://api.github.com/repos/x/y/releases/latest`, asserting red, then restoring.
**Where:** `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`
**Done When:** the gate is green at HEAD (post-AGY-262); the negative test reds on the injected API call then restores; resolver-based redirect calls do not trip it; `just drift-gate` green.
**Why:** The API path is the mechanism that made hand-pinned fallbacks necessary in the first place; unbanned, it comes straight back the next time someone needs a tag.
**Dep:** AGY-262
**Who:** you (bash + Python + drift-gate).

## AGY-273 -- Extend build-network-policy.md with the float-latest contract and allowlist governance  (WS-FLOAT | P3 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- the build-network rules doc covers the mechanism the WS-FLOAT campaign actually shipped.
**What+How:** `usr/share/doc/mios/reference/build-network-policy.md` (authored by AGY-98) codifies retry/scurl/FATAL-vs-degrade-open but predates WS-FLOAT. Extend it in place (do NOT create a new file; keep its existing frontmatter so check 46 stays green) with a "Float-latest resolution" section covering: (1) the redirect-follow contract (`mios-resolve-latest` / `mios_resolve_latest_tag`, releases/latest redirect, NO `api.github.com`, with the rate-limit rationale); (2) the SBOM-recording requirement (every floated component `record_version`-ed or written to `resolved-latest.tsv` -> `binaries.tsv`, per AGY-269 and ADR-0003); (3) `[build.float]` allowlist governance -- the `{key,ref,reason,upstream}` schema, the three seed examples (open-webui -> python3.12, firecrawl -> pnpm@9, pgvector 0.8.3-pg17), and the gates `check_float_no_handpin`/`check_float_allowlist_documented`/`check_no_github_api_release` that enforce it. Cross-reference ADR-0003 and the AGY-260..272 mechanisms.
**Where:** `usr/share/doc/mios/reference/build-network-policy.md`
**Done When:** the doc carries the Float-latest section with resolver contract, SBOM requirement, and `[build.float]` governance plus gate cross-refs; frontmatter unchanged so check 46 (reference-doc frontmatter) stays green; `just drift-gate` green.
**Why:** Three new gates now red a build for reasons the only build-network doc does not explain, so a blocked contributor has nowhere to look.
**Dep:** AGY-260..AGY-272
**Who:** you (docs).

## AGY-274 -- Bake-time per-stack smoke RUN in sys/Containerfile (import+exec assert)  (WS-BAKE | P1 | M)**[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- a stack that cannot start never ships.
**What+How:** The mios-sys stacks install into `/opt/*-venv` but nothing proves they can START before the image ships -- the SearXNG `msgspec`/`setuptools.build_meta` class of failure installs "successfully" then dies at import. Append a smoke-verification `RUN` at the END of `usr/share/mios/sys/Containerfile` (after the firecrawl COPY at L119; there is no `bootc container lint` final-line requirement on the sys Containerfile -- check 38 only scans the root `Containerfile`/`Containerfile.minimal`, so appending a RUN is legal). The RUN must `set -e` and fail the whole build on the first broken stack: SearXNG `/opt/searxng-venv/bin/python -c "import searx, msgspec"` (msgspec explicit -- that was the real runtime miss); Open WebUI `/opt/open-webui-venv/bin/python -c "import open_webui"` AND `/opt/open-webui-venv/bin/open-webui --help >/dev/null`; Crawl4AI `/opt/crawl4ai-venv/bin/python -c "import crawl4ai, camoufox, playwright, fastapi, uvicorn, pydantic"`; Firecrawl `test -d /opt/firecrawl/apps/api/dist` plus `node -e "process.exit(0)"` under the image's node. Each assertion echoes `[sys-smoke] <stack> OK` so a bake log shows which stack passed. Add NO retries -- a smoke failure must be fatal.
**Where:** `usr/share/mios/sys/Containerfile` (append one `RUN` block after L119)
**Done When:** the Containerfile still parses and `bash -n` on the extracted RUN body is clean; the smoke block names all four stacks; `just drift-gate` stays green (no new violation); stage ONLY `usr/share/mios/sys/Containerfile`.
**Why:** Without it a green bake can publish an image whose search, chat or crawl stack dies on first import, and the failure is discovered by an operator on real hardware instead of by CI.
**Dep:** none -- foundation the other WS-BAKE tasks harden
**Who:** you (Containerfile / bash). **When:** first -- it is the foundation the other WS-BAKE tasks harden.

## AGY-275 -- Factor the bake-time stack smoke assertions into a COPY'd `smoke.sh` + a host-side Containerfile/smoke parity test  (WS-BAKE | P1 | M)**[DONE]**
**Goal:** E-16 Bake plane: what is present in the image, and can a stock runner hold it -- makes the per-stack bake-time smoke RUN (import+exec presence proof) maintainable and self-extending instead of an unmaintainable inline blob.
**What+How:** The AGY-274 assertions currently live inlined inside a Containerfile `RUN`, so they cannot be linted or unit-checked. Extract them into a real script `usr/share/mios/sys/smoke.sh` (`#!/usr/bin/env bash`, `set -euo pipefail`, one assertion per stack, exit non-zero on first failure), `COPY smoke.sh /tmp/smoke.sh` into the sys image and reduce the AGY-274 RUN to `RUN bash /tmp/smoke.sh`. Then add a host-side STATIC test `tests/sys-smoke-parity.sh` that parses `usr/share/mios/sys/Containerfile` for every `python3 -m venv /opt/<name>-venv` (plus the two symlink/venv stanzas) and asserts `smoke.sh` references each `/opt/<name>-venv` — a new stack added without a smoke assertion FAILS. Wire `tests/sys-smoke-parity.sh` into the `drift-gate` recipe in `Justfile`, beside the existing `tests/drift-gate-negatives.sh` invocation.
**Where:** `usr/share/mios/sys/smoke.sh` (new), `usr/share/mios/sys/Containerfile` (COPY+RUN), `tests/sys-smoke-parity.sh` (new), `Justfile` (drift-gate recipe)
**Done When:** `bash -n usr/share/mios/sys/smoke.sh` and `bash -n tests/sys-smoke-parity.sh` are clean; the parity test PASSES on the current tree and FAILS when a `*-venv` line is added to a scratch Containerfile copy with no matching `smoke.sh` reference; `just drift-gate` green; only those four paths staged.
**Why:** Today a stack can be added to the sys image with zero smoke coverage and nothing notices — the image publishes with a stack that imports-broken at runtime, which is exactly the bake failure class E-16 exists to close.
**Dep:** after AGY-274
**Who:** you (bash + tests) | **When:** after AGY-274

## AGY-276 -- Resolve the newest supported interpreter at build time instead of hardcoding `python3.12`  (WS-BAKE | P1 | M)**[DONE]**
**Goal:** E-16 Bake plane -- removes a hand-maintained compatibility literal from the bake so version INTENT floats (ADR-0003) and the venv interpreter is resolved, not pinned.
**What+How:** `usr/share/mios/sys/Containerfile` L92 hardcodes `python3.12` for the Open WebUI venv because open-webui caps `requires_python <3.13` while Fedora 44's default `python3` is 3.14 — a constraint the operator must never hand-maintain. Write `usr/share/mios/sys/pick-python.sh <pkg>` that (1) enumerates the `python3.N` interpreters on PATH (`compgen -c 'python3.'` / glob `/usr/bin/python3.[0-9]*`) newest-first; (2) resolves the floated package's `requires_python` from `https://pypi.org/pypi/<pkg>/json` (`.info.requires_python`) with `curl --retry 5` (check 64 requires `--retry`), degrading OPEN to the newest interpreter when the API is unreachable; (3) prints the newest `python3.N` satisfying the specifier via a small `packaging.specifiers`/`SpecifierSet` evaluation run under a candidate interpreter, else the base `python3`. COPY the helper in and replace L92's literal with `"$(bash /tmp/pick-python.sh open-webui)"`; update the L88-91 comment to point at the helper.
**Where:** `usr/share/mios/sys/pick-python.sh` (new), `usr/share/mios/sys/Containerfile` (COPY + L92 substitution + comment)
**Done When:** `bash -n usr/share/mios/sys/pick-python.sh` clean; run against PATH-shimmed `python3.12`/`python3.14` stubs it selects the newest satisfying interpreter and degrades-open when the JSON fetch fails; no bare `python3.12` literal remains in the Containerfile outside a comment; `just drift-gate` green; only the two paths staged.
**Why:** Every Fedora base bump silently strands the pinned `python3.12` — the venv either fails to create or silently builds on a dead interpreter, and a human has to notice and hand-edit the Containerfile.
**Dep:** none (independent of AGY-274/275)
**Who:** you (bash) | **When:** independent of 274/275

## AGY-277 -- Gate the sys/cuda Containerfiles against hardcoded `pythonX.Y` venv interpreters (resolver-only)  (WS-BAKE | P2 | S)**[DONE]**
**Goal:** E-16 Bake plane -- ratchets AGY-276 into a permanent invariant so the interpreter literal cannot creep back into the bake.
**What+How:** Add a new `check_*` to `automation/38-drift-checks.sh` at the next free number (highest today is 71 → 72), modeled on `check_containerfile_pinned_clones` (L4167) / `check_curl_retry` (L3863): scan `usr/share/mios/sys/Containerfile` and `usr/share/mios/cuda/Containerfile` with comment lines stripped for any `python3\.[0-9]+ -m venv` / `python3\.[0-9]+ -m pip` / `python3\.[0-9]+ -m ...` interpreter literal, and `_violation` unless that token is a `$(...pick-python.sh...)` substitution. Emit the standard `[38-drift-checks]   (72) ...` pass line and register the function in `main()` right after `check_firstboot_degrade_open` (L4260). Add the matching negative fixture to `tests/drift-gate-negatives.sh`, mirroring the `check_containerfile_pinned_clones` negative test at L413 — inject a `python3.12 -m venv` literal into a temp copy and assert the check fails.
**Where:** `automation/38-drift-checks.sh` (new function + `main()` wiring), `tests/drift-gate-negatives.sh` (new negative case)
**Done When:** `bash -n` clean on both files; check 72 PASSES on the AGY-276 tree and FAILS on an injected `pythonX.Y -m venv` literal; `just drift-gate` green; only the two files staged.
**Why:** Without the gate, AGY-276's resolver is one careless Containerfile edit away from being bypassed and the hardcode class silently returns.
**Dep:** after AGY-276
**Who:** you (bash + the drift-gate framework) | **When:** after AGY-276

## AGY-278 -- Declare the candidate interpreter set once as `[build.bake].interpreters` and de-hardcode the dnf python list  (WS-BAKE | P2 | S)**[DONE]**
**Goal:** E-16 Bake plane -- gives `pick-python.sh` an SSOT-declared candidate pool so what the bake installs is one TOML edit, not a Containerfile change.
**What+How:** `pick-python.sh` can only select interpreters that are actually installed, yet `usr/share/mios/sys/Containerfile` L22-25 hardcodes the `python3` + `python3.12` dnf set inline. Add `interpreters = ["python3", "python3.12", "python3.13"]` under `[build.bake]` in `usr/share/mios/mios.toml` beside `firstboot_tokens` (L8614) as the explicit candidate set. Read it in `usr/libexec/mios/57-mios-sys-build.sh` using the same `python3 -c "import mios_toml; ..."` pattern used for `SEARXNG_REF` at L65 and pass it as `--build-arg MIOS_INTERPRETERS=...`; consume that `ARG` in the Containerfile's dnf `RUN`, expanding it into the install list. Leave `python3-devel`/`python3-pip` as-is.
**Where:** `usr/share/mios/mios.toml` (`[build.bake].interpreters`), `usr/libexec/mios/57-mios-sys-build.sh` (read + `--build-arg`), `usr/share/mios/sys/Containerfile` (`ARG` + dnf expansion)
**Done When:** `bash -n usr/libexec/mios/57-mios-sys-build.sh` clean; `python3 -c "import tomllib,sys; tomllib.load(open('usr/share/mios/mios.toml','rb'))"` parses; `[build.bake].interpreters` is the ONLY place the candidate list lives (no bare `python3.12` in the dnf `RUN`); `just drift-gate` green; only the three paths staged.
**Why:** The resolver can pick only from what dnf installed, so the real candidate set lives in a Containerfile line no operator reads — adding an interpreter today means editing the image recipe rather than the SSOT.
**Dep:** after AGY-276 (the resolver needs the candidate interpreters present)
**Who:** you (bash + TOML) | **When:** after AGY-276

## AGY-279 -- Retry-with-backoff and degrade-open the camoufox/playwright browser fetch  (WS-BAKE | P1 | S)**[DONE]**
**Goal:** E-16 Bake plane -- stops a transient CDN blip on a non-essential asset from aborting the whole publish bake.
**What+How:** `usr/share/mios/sys/Containerfile` L105 (`/opt/crawl4ai-venv/bin/python3 -m camoufox fetch`) and the implicit `playwright` browser download are single-shot network pulls with NO retry. Split the browser fetch out of the pip-install `RUN` into its own `RUN` that retries `camoufox fetch` (and `playwright install chromium` if crawl4ai's runtime needs it — verify) 3× with 10s/20s backoff, mirroring `build_image_retry` in `usr/libexec/mios/mios-webtools-firstboot.sh` L44-56; and DEGRADES OPEN — on final failure log `[sys-bake] camoufox/playwright browser deferred to firstboot` and `|| true` so the bake proceeds (the browser is a firstboot-fetchable asset, consistent with Law 12 BAKE-NOT-FETCH's degrade-open posture that check 39 enforces). The pip install of the packages themselves stays FATAL — only the browser binary download degrades.
**Where:** `usr/share/mios/sys/Containerfile` (split the L97-105 crawl4ai stanza)
**Done When:** the extracted retry loop passes `bash -n`; the browser-download step degrades open (`|| true` after the retry loop) while the package `pip install` stays fatal; `just drift-gate` green (any added curl/network lines carry `--retry` per check 64); only the Containerfile staged.
**Why:** One flaky CDN response currently kills a multi-hour bake over an asset that firstboot can fetch anyway — a recurring, unattributable publish failure.
**Dep:** none
**Who:** you (Containerfile / bash) | **When:** independent

## AGY-280 -- Verify the Firecrawl `html-to-markdown.so` runtime load path at bake time  (WS-BAKE | P1 | S)**[DONE]**
**Goal:** E-16 Bake plane -- converts a silent runtime break into a loud bake failure, so presence in the image means functional presence.
**What+How:** `usr/share/mios/sys/Containerfile` L117-119 COPYs the Go `html-to-markdown.so` into `apps/api/sharedLibs/go-html-to-md/` because Firecrawl v2 relocated it (was `apps/api/src/lib/go-html-to-md/`). A future v2→v3 restructure moves the load path again and the COPY lands the `.so` where nothing reads it — no bake error, a broken runtime. Add a verification `RUN` immediately after L119 that (1) asserts the `.so` exists at the copied path AND is a valid shared object (`file /opt/firecrawl/apps/api/sharedLibs/go-html-to-md/html-to-markdown.so | grep -q 'shared object'`); (2) greps the BUILT firecrawl output (`/opt/firecrawl/apps/api/dist`) for the `sharedLibs/go-html-to-md` path token the runtime `koffi`/`ffi` loader references, failing with `[sys-bake] firecrawl .so load-path drifted -- upstream relocated go-html-to-md` when the built code no longer references the directory the `.so` was placed in.
**Where:** `usr/share/mios/sys/Containerfile` (new `RUN` after L119)
**Done When:** the extracted RUN body passes `bash -n`; the check asserts BOTH `.so` validity and that the built code references the placement dir; the failure message names the upstream-relocation cause; `just drift-gate` green; only the Containerfile staged.
**Why:** With the floated Firecrawl tag, an upstream directory move ships a green image whose crawl path is dead at runtime — discovered by a user, not by CI.
**Dep:** none
**Who:** you (Containerfile / bash) | **When:** independent

## AGY-281 -- Static gate: the firecrawl `.so` COPY dir, go-builder output dir and chromium ENV path must agree  (WS-BAKE | P2 | S)**[DONE]**
**Goal:** E-16 Bake plane -- an offline, zero-cost guard for the same drift AGY-280 catches only after a full bake.
**What+How:** Add a new `check_*` to `automation/38-drift-checks.sh` at the next free number (72 if AGY-277 has not landed, else 73) that parses `usr/share/mios/sys/Containerfile` and asserts mutual consistency of: the go-builder stage's `go build -o <name>.so` output directory (L12-13, `sharedLibs/go-html-to-md`), the `COPY --from=go-builder ... <src> <dst>` src/dst at L119, and `ENV PUPPETEER_EXECUTABLE_PATH` at L120 (must be `/usr/bin/chromium`, which the dnf `chromium` package at L29 provides). The COPY src must equal the go-builder build dir and the dst must sit under `apps/api/sharedLibs/go-html-to-md/`; `_violation` on mismatch, standard `[38-drift-checks]   (NN) ...` pass line, registered in `main()` after the AGY-277 check. Add a negative fixture to `tests/drift-gate-negatives.sh` that mangles the COPY dst and asserts failure.
**Where:** `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`
**Done When:** `bash -n` clean on both; the check PASSES on the current Containerfile and FAILS when the COPY dst is edited away from the build dir; `just drift-gate` green; only the two files staged.
**Why:** Without it the only detector of this mismatch is a full bake (AGY-280), so a bad edit costs an entire build cycle instead of a second of grep.
**Dep:** after AGY-280
**Who:** you (bash + drift-gate framework) | **When:** after AGY-280

## AGY-282 -- Add EXIT-trap mount/scratch teardown to the nested-podman builders (mount-leak hardening)  (WS-BAKE | P2 | M)**[DONE]**
**Goal:** E-16 Bake plane -- lets a standard runner survive a retried bake instead of filling its disk with leaked overlay mounts.
**What+How:** `usr/libexec/mios/57-mios-sys-build.sh` and `usr/libexec/mios/mios-bake-group` both run a NESTED `podman --root "$STORE" --runroot "$SCRATCH/run" build/pull` inside the outer OCI build. Under `set -euo pipefail` a mid-build failure exits WITHOUT tearing down the inner store's overlay mounts or purging `$SCRATCH`, leaking overlay/loopback mounts on retried CI runners. Add `trap '_cleanup' EXIT` to BOTH scripts: best-effort `podman --root "$STORE" --runroot "$SCRATCH/run" unshare umount ...` / `podman ... system prune`, force-unmount lingering mounts under `$SCRATCH` (`awk '$2 ~ SCRATCH {print $2}' /proc/self/mountinfo | xargs -r umount -l`), then `rm -rf "${SCRATCH:?}"` — every step `|| true` and the original `$?` preserved so cleanup never masks the real exit code. In `mios-bake-group` respect the existing `MIOS_BAKE_BOUND_IMAGES=0` early-exit (L47-50) so the trap is a no-op there; reference the existing partial cleanup at L58/L140.
**Where:** `usr/libexec/mios/57-mios-sys-build.sh`, `usr/libexec/mios/mios-bake-group`
**Done When:** `bash -n` clean on both; the trap fires on both success and forced-failure paths (test with an injected `false`) and never alters the propagated exit code; the `${SCRATCH:?}` guard is present so an unset var cannot `rm -rf /`; `just drift-gate` green; only the two scripts staged.
**Why:** A documented WS-BAKE failure mode: one failed bake poisons the runner so every subsequent retry fails on disk-full, and the cause looks unrelated.
**Dep:** none
**Who:** you (bash / podman) | **When:** independent

## AGY-283 -- Make the SBOM `bound-images.tsv` append idempotent (retry-safe, last-wins)  (WS-BAKE | P2 | S)**[DONE]**
**Goal:** E-16 Bake plane -- keeps the build-time provenance record unambiguous when a bake is retried, which is the whole point of the robustness work.
**What+How:** `usr/libexec/mios/57-mios-sys-build.sh` L99-100 and `usr/libexec/mios/mios-bake-group` L135 append (`>>`) rows to `$SBOM_DIR/bound-images.tsv` with no dedup, so a retried build writes DUPLICATE rows for the same image; `tools/generate-bake-plan.py`'s SBOM reader (L181-190) keeps the LAST digest, making duplicates silent bloat and ambiguity. Make the append idempotent — before writing a row, drop any existing line for that `image` key (`grep -v -P "^<img>\t"` to a tmp then append the fresh row, or an awk last-wins rewrite) — keeping the TSV header handling intact. Implement via a tiny `_sbom_put <img> <digest> <group>` function defined INLINE in each script; the two do not share a lib today and a new sourced lib is not warranted for two call sites.
**Where:** `usr/libexec/mios/57-mios-sys-build.sh`, `usr/libexec/mios/mios-bake-group`
**Done When:** `bash -n` clean on both; running either writer twice for the same image yields exactly ONE row (verify against a scratch `$SBOM_DIR`); the header line survives; `just drift-gate` green; only the two scripts staged.
**Why:** The SBOM is the provenance record (NS-4); duplicate rows make "which digest did we actually bake" a matter of file ordering rather than fact.
**Dep:** none
**Who:** you (bash) | **When:** independent

## AGY-284 -- Grep-gate the load-bearing SearXNG build order in sys/Containerfile  (WS-BAKE | P2 | S)**[DONE]**
**Goal:** E-16 Bake plane -- prevents a known, hard-won bake failure from being silently reintroduced by a cosmetic reorder.
**What+How:** The SearXNG stanza order at `usr/share/mios/sys/Containerfile` L82-86 is load-bearing (documented in the L77-81 comment): `setuptools wheel` must be pip-installed into the venv BEFORE the editable `-e` install, and `-r requirements.txt` must precede the `--no-build-isolation -e` install, or the bake dies with `BackendUnavailable: Cannot import 'setuptools.build_meta'` / missing `msgspec`. Add a `check_*` to `automation/38-drift-checks.sh` (next free number) that extracts the searxng `RUN` block and asserts by line index WITHIN the block that `pip install ... setuptools wheel` precedes `pip install --no-build-isolation -e`, and that `pip install ... -r ...requirements.txt` precedes the `-e` install. `_violation` with a message pointing at the L77-81 rationale; standard pass line; registered in `main()`. Add a negative fixture to `tests/drift-gate-negatives.sh` that swaps two lines and asserts failure.
**Where:** `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`
**Done When:** `bash -n` clean on both; the gate PASSES on the current Containerfile and FAILS when the setuptools/`-e` lines are reordered; `just drift-gate` green; only the two files staged.
**Why:** The ordering rationale currently lives only in a comment; anyone tidying the stanza rediscovers the failure the expensive way, mid-bake.
**Dep:** none
**Who:** you (bash + drift-gate framework) | **When:** independent

## AGY-285 -- Retry-wrap the three `git clone`s in sys/Containerfile (clone-flake hardening)  (WS-BAKE | P2 | S)**[DONE]**
**Goal:** E-16 Bake plane -- closes the retry blind spot that `check_curl_retry` leaves open, so a transient forge blip does not abort a publish bake.
**What+How:** `check_curl_retry` (check 64) enforces `--retry` on curl/wget ONLY — it does not cover `git clone`, and the three sys clones (`usr/share/mios/sys/Containerfile` L11 firecrawl go-builder, L82 searxng, L112 firecrawl) have no retry. Wrap each `git clone --depth 1 --branch "$t" <url> <dst>` in a 3×-with-backoff retry (`for a in 1 2 3; do git clone ... && break || sleep $((a*5)); done; test -d <dst>/.git`, or a `_git_clone_retry` function defined at the top of each `RUN`). Preserve the exact `--branch "$t"` / `--depth 1` arguments so `check_containerfile_pinned_clones` (check 71) stays green — it requires a `--branch`/`--tag`/ref. Leave the resolved-tag `curl --retry 5` redirect lines (L10, L46-66, L111) untouched; they already retry.
**Where:** `usr/share/mios/sys/Containerfile` (L11, L82, L112 clone stanzas)
**Done When:** the extracted retry loops pass `bash -n`; every `git clone` retries and verifies `<dst>/.git` afterwards; `--branch "$t"` is preserved on each so check 71 stays green; `just drift-gate` green; only the Containerfile staged.
**Why:** Three un-retried network pulls sit on the critical path of every publish; a single GitHub/Codeberg hiccup wastes the entire bake.
**Dep:** none
**Who:** you (Containerfile / bash) | **When:** independent

## AGY-286 -- Retry the Firecrawl pnpm registry ops and degrade `--frozen-lockfile` to a warned unfrozen install  (WS-BAKE | P2 | S)**[DONE]**
**Goal:** E-16 Bake plane -- makes a floated upstream tag's benign lockfile bump a warning instead of a publish-stopping bake failure.
**What+How:** `usr/share/mios/sys/Containerfile` L113-116 runs `npm install -g pnpm@latest`, then `pnpm install --frozen-lockfile`, then `pnpm run build:nosentry` — all single-shot, with two failure modes: an un-retried registry install (transient npm/pnpm flake aborts the bake) and `--frozen-lockfile` HARD-failing whenever the floated firecrawl tag's committed `pnpm-lock.yaml` disagrees with `package.json` (a real risk under ADR-0003 float-latest). Wrap `npm install -g pnpm@latest` and `pnpm install` in a 3×-backoff retry, and on frozen-lockfile failure degrade with a warning: `pnpm install --frozen-lockfile || { echo '[sys-bake] firecrawl lockfile drift -- retrying unfrozen'; pnpm install; }` — preferring the reproducible frozen path first. `pnpm run build:nosentry` stays FATAL (a build failure is a real defect). Keep `ENV PUPPETEER_SKIP_DOWNLOAD=true` (L109) so no browser download sneaks in.
**Where:** `usr/share/mios/sys/Containerfile` (L110-116 firecrawl stanza)
**Done When:** the extracted loop passes `bash -n`; pnpm registry ops retry; `--frozen-lockfile` degrades to unfrozen-with-warning on failure while `build:nosentry` remains fatal; `just drift-gate` green; only the Containerfile staged.
**Why:** Floating the firecrawl tag guarantees periodic lockfile drift, and today each occurrence hard-blocks publishing until a human intervenes.
**Dep:** after AGY-285 (same firecrawl stanza)
**Who:** you (Containerfile / bash) | **When:** after AGY-285

## AGY-287 -- One consolidated static "bake network-op discipline" test + per-invariant negative fixtures  (WS-BAKE | P2 | M)**[DONE]**
**Goal:** E-16 Bake plane -- ratchets everything AGY-274..286 established into a single enforced contract so bake robustness cannot quietly erode.
**What+How:** Add one host-side static test `tests/bake-net-discipline.sh` that walks `usr/share/mios/sys/Containerfile`, `usr/share/mios/cuda/Containerfile`, `usr/libexec/mios/57-mios-sys-build.sh` and `usr/libexec/mios/mios-bake-group` and asserts the WS-BAKE invariants in ONE place: (a) every `git clone` line sits inside a retry construct (`for a in` / `||`-backoff nearby), complementing check 71's pin-only rule; (b) every `pnpm install` / `npm install -g` is retried; (c) the sys Containerfile invokes `smoke.sh` (AGY-274/275); (d) `57-mios-sys-build.sh` and `mios-bake-group` each declare a `trap ... EXIT` (AGY-282). Emit a clear per-invariant PASS/FAIL line. Wire it into the `drift-gate` recipe in `Justfile` beside `tests/drift-gate-negatives.sh` (L101), and add a negative case per invariant run against scratch copies with exactly one invariant broken.
**Where:** `tests/bake-net-discipline.sh` (new), `Justfile` (drift-gate recipe)
**Done When:** `bash -n tests/bake-net-discipline.sh` clean; all four invariants PASS on the post-AGY-274..286 tree and each FAILS when its target line is mangled in a scratch copy; the test runs inside `just drift-gate` and the gate stays green; only `tests/bake-net-discipline.sh` and `Justfile` staged.
**Why:** Each preceding fix is a point edit with no enforcement; without one consolidated ratchet the whole retry/smoke/trap discipline decays back to single-shot within a few refactors.
**Dep:** last in the WS-BAKE chain — after AGY-274..286
**Who:** you (bash + tests) | **When:** last — it ratchets the invariants the other WS-BAKE tasks establish

## AGY-288 -- Kill the divergent BIB target matrix: gate the `Justfile` `all` recipe against `[deployment].target_*`  (WS-BOOTC | P1 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- makes SSOT the only definition of which install artifacts MiOS ships, so enabling a target actually builds it.
**What+How:** Two BIB-invocation surfaces disagree on the shipped artifact set. `usr/libexec/mios/mios-build-driver` (L855-894) derives `BIB_FORMATS` by awk-scanning `usr/share/mios/mios.toml` `[deployment]` for `target_<key> = true` (the SSOT matrix), while the `Justfile` `all:` target (L296) hardcodes `build raw iso usb-installer qcow2 vhdx wsl2` and reads nothing from SSOT — so flipping `target_vmdk`/`target_ami` on silently builds nothing. Add `check_bib_matrix_parity` to `automation/38-drift-checks.sh` (next free number, 72): parse the enabled `target_*` set from `[deployment]` reusing the exact awk idiom at mios-build-driver:872-878, normalize with the same `_`/`-` and `vhdx→vhd`/`wsl` rules the driver applies (L911-928), then assert the `Justfile` `all` recipe's BIB-producing prereq list is the SAME set, allowing `build`/`usb-installer`/`wsl2` as documented non-BIB extras. Register it in `main()` next to `check_deploy_plane`. Stage ONLY the explicit paths — the tree is shared, never `git add -A`.
**Where:** `automation/38-drift-checks.sh` (new `check_bib_matrix_parity` + `main()` registration ~L4273), `Justfile` (`all:` L296 — add a comment cross-referencing `[deployment]` as SSOT), `usr/share/mios/mios.toml` `[deployment]` (L6630)
**Done When:** `bash -n automation/38-drift-checks.sh` clean; `just --evaluate` parses; the new check is GREEN on HEAD; flipping `target_vmdk = true` in `[deployment]` (or dropping `qcow2` from the `Justfile` `all` list) turns it RED; `just drift-gate` exits 0.
**Why:** An operator can enable a deploy target in SSOT and get no artifact, with no error anywhere — the exact SSOT-projection bypass Law 8 forbids.
**Dep:** none
**Who:** you (bash/awk + Justfile + drift-gate)

## AGY-289 -- Migrate the installer ISO to BIB `--type anaconda-iso` and gate that it is bootc-native, not a package install  (WS-BOOTC | P1 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- one installer-ISO spelling, one SSOT toggle, and proof the ISO installs the container rather than DNF packages.
**What+How:** The `Justfile` `iso:` recipe (L208-216) invokes BIB with `--type iso` — the deprecated alias — while `[deployment]` (mios.toml:6637-6638) enables BOTH `target_iso` AND `target_anaconda_iso`, so the SSOT-driven `mios-build-driver` (L942) emits two overlapping installer ISOs. Consolidate on the current spelling: change `iso:` to `--type anaconda-iso`, retire the redundant `target_iso` toggle leaving one `anaconda_iso` boolean as the single installer-ISO SSOT, and update the driver fallback default at mios-build-driver:891 (`"vhd qcow2 iso raw"` → `anaconda-iso`). Because BIB's `anaconda-iso` built FROM a bootc image installs via `ostreecontainer` and not a `%packages` DNF install, add `check_installer_is_bootc` (next free check) asserting (a) the `iso`/`usb-installer` recipes use `--type anaconda-iso` and (b) `config/artifacts/iso.toml`'s kickstart carries NO `%packages` stanza and NO `url --mirrorlist` package source — the bootc image is the install source.
**Where:** `Justfile` (`iso:` recipe, `all:`/`usb-installer:` chain), `usr/libexec/mios/mios-build-driver` (L891 fallback), `usr/share/mios/mios.toml` `[deployment]` (L6637-6638), `config/artifacts/iso.toml`, `automation/38-drift-checks.sh` (new check + registration)
**Done When:** `just --evaluate` parses; grep shows no remaining space-terminated `--type iso ` anywhere; the new check is GREEN; injecting a `%packages`/`@core` block into iso.toml turns it RED; `bash -n mios-build-driver` clean; `just drift-gate` green.
**Why:** Two overlapping installer ISOs waste an entire build leg, and nothing today stops the shipped ISO from quietly becoming a plain-Fedora package install — the headline deploy-plane defect.
**Dep:** none
**Who:** you (Justfile + bash + drift-gate)

## AGY-290 -- Author a bootc-native offline kickstart (`ostreecontainer` from oci-archive, zero `%packages`)  (WS-BOOTC | P1 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- gives the Ventoy media an immutable install path that lands the MiOS OCI image itself, fully offline.
**What+How:** `usr/share/mios/ventoy/mios-kickstart.cfg` deliberately installs a MUTABLE plain Fedora base (`cdrom`, `autopart --type=plain --fstype=ext4`, `%packages @core @virtualization …`, then overlays via build-mios.sh) — precisely the "installs plain Fedora, not the immutable MiOS OCI image" gap. Author a SIBLING `usr/share/mios/ventoy/mios-bootc-kickstart.cfg` that installs the image directly: `ostreecontainer --url oci-archive:/run/install/repo/mios-latest.tar --transport oci-archive --no-signature-verification` (Law 12 zero-network — the archive is staged on the boot media, mirroring tools/install.sh:7's default `/mnt/mios-repo/mios-latest.tar`), plus `zerombr`, `clearpart --all --initlabel --disklabel=gpt`, `reqpart --add-boot`, `part / --grow --fstype xfs`, `bootloader`, a bootstrap `user` from `[identity]` (no `rootpw` package churn), and NO `%packages` section at all. Add `check_bootc_kickstart` (next free check) asserting the new file contains `ostreecontainer` and an `oci-archive`/`--transport` source and has ZERO `%packages`/`cdrom`/`@core` tokens — the negative that separates it from the mutable variant.
**Where:** `usr/share/mios/ventoy/mios-bootc-kickstart.cfg` (new), `automation/38-drift-checks.sh` (new check + `main()` registration near `check_deploy_plane`), `usr/share/mios/ventoy/mios-kickstart.cfg` (header cross-note that the immutable sibling exists)
**Done When:** the new kickstart is syntactically well-formed (`%post`/`%end` balanced if present); the new check is GREEN; adding a `%packages` line or removing `ostreecontainer` turns it RED; `bash -n automation/38-drift-checks.sh` clean; `just drift-gate` green; only the explicit paths staged.
**Why:** Today the USB media provisions a mutable Fedora that merely resembles MiOS — no `bootc upgrade`, no rollback, no image identity, which defeats NS-1 at the point of install.
**Dep:** none
**Who:** you (kickstart + bash + drift-gate)

## AGY-291 -- First-boot verifier asserting the installed system is bootc-managed MiOS, not plain Fedora  (WS-BOOTC | P1 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- turns the WS-BOOTC acceptance criterion into a running, self-reporting check on the landed deployment.
**What+How:** The acceptance is "the installed system reports `bootc status`, not plain Fedora", but nothing checks it — `bootc-switch-from-build.sh` only stages switches and never verifies the landed deployment. Author `usr/libexec/mios/mios-verify-bootc-install` that runs `bootc status --format=json`, parses `.status.booted.image.image.image` (falling back to `.spec.image` per the bootc JSON schema) and asserts it matches the SSOT ref resolved from mios.toml `[image].ref`/`[image].name` (`ghcr.io/mios-dev/mios`, L6586/6598), exiting non-zero with a loud `SYSTEM IS NOT bootc-managed MiOS (got: <x>)` when `bootc` is absent or the image is unset/Fedora. Ship a oneshot `usr/lib/systemd/system/mios-verify-bootc-install.service` (`WantedBy=multi-user.target`, degrade-open via `ConditionPathExists=/run/ostree-booted`). Add `check_verify_bootc_install` (next free check) asserting the script exists, references `bootc status`, resolves the ref through the mios_toml resolver with NO hardcoded `ghcr.io/...` literal, and that the unit points at it.
**Where:** `usr/libexec/mios/mios-verify-bootc-install` (new), `usr/lib/systemd/system/mios-verify-bootc-install.service` (new), `automation/38-drift-checks.sh` (new check + registration), `usr/share/mios/mios.toml` `[image]` (SSOT source, no edit needed)
**Done When:** `bash -n usr/libexec/mios/mios-verify-bootc-install` clean; running it in a non-bootc shell exits non-zero with the explicit message (self-testable offline); the new check is GREEN; hardcoding an image literal into the script turns it RED; `just drift-gate` green.
**Why:** Without it a mis-installed machine looks fine for months — the whole install plane can regress to plain Fedora and no signal ever fires.
**Dep:** none
**Who:** you (bash + systemd unit + drift-gate)

## AGY-292 -- Project install-time kernel args from `[kargs]` into `bootc install to-disk`  (WS-BOOTC | P2 | S)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- the initial deployment boots with the SSOT-declared kargs instead of only whatever the image fragments apply later.
**What+How:** `tools/install.sh:71` runs `bootc install to-disk --target-no-signature-verification --source-imgref "oci-archive:$OCI_ARCHIVE" "$TARGET_DISK"` with NO `--karg`, so the vfio/hardening/console kargs shipped in `usr/lib/bootc/kargs.d/*.toml` (00-mios.toml, 01-mios-hardening.toml, 20-vfio.toml, 30-security.toml, …) apply only post-install, and any install-time-only kargs from `[kargs]` (mios.toml:11361) never reach the first deployment at all. Add a helper in install.sh that resolves the install-time karg list from `[kargs]` through the shared `usr/lib/mios/mios_toml.py` resolver (or a tomllib one-liner — NO hardcoded karg literals) and appends one `--karg <k>` per entry to the `to-disk` invocation, echoing the resolved set under `--dry-run`. Extend the AGY-153-adjacent install.sh gate, or add `check_install_kargs`, asserting install.sh sources kargs from SSOT and emits `--karg` in the dry-run plan.
**Where:** `tools/install.sh` (karg-resolve helper + invocation at L50/71), `usr/share/mios/mios.toml` `[kargs]` (SSOT, no schema edit), `usr/lib/mios/mios_toml.py` (resolver, reuse only), `automation/38-drift-checks.sh` (new/extended check)
**Done When:** `bash -n tools/install.sh` clean; `tools/install.sh --dry-run --target-disk /dev/sda` prints the resolved `--karg` list sourced from `[kargs]` with no network and no hardcoded karg; the check is RED when a bare karg literal is hardcoded and GREEN on HEAD; `just drift-gate` green.
**Why:** A machine installed today first-boots without its declared vfio/hardening kargs — GPU passthrough and hardening silently do not apply until a later reboot, if ever.
**Dep:** none
**Who:** you (bash + drift-gate)

## AGY-293 -- Make to-disk signature verification an SSOT policy instead of an unconditional `--target-no-signature-verification`  (WS-BOOTC | P2 | S)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- lets an operator running a signed MiOS actually get signature enforcement at install time.
**What+How:** `tools/install.sh:71` hardcodes `--target-no-signature-verification`, permanently disabling image signature enforcement even though the repo already carries signed-UKI/sigstore machinery. Add a `[deploy].verify_signatures` SSOT key defaulting to `false` (preserving today's offline oci-archive behavior) and have install.sh resolve it: when true, DROP the no-verify flag and pass the sigstore policy path instead; when false, keep the flag but log a visible `signature verification DISABLED (SSOT [deploy].verify_signatures=false)` warning. Add `check_install_sig_policy` asserting install.sh reads the flag from SSOT rather than unconditionally emitting the no-verify flag, and that the key is documented in mios.toml.
**Where:** `tools/install.sh` (L71 invocation + flag resolution), `usr/share/mios/mios.toml` (new commented `[deploy].verify_signatures` key), `automation/38-drift-checks.sh` (new check + registration)
**Done When:** `bash -n tools/install.sh` clean; `--dry-run` reflects the resolved policy in the printed `bootc install` plan for BOTH flag values; the new check is GREEN on HEAD and RED if `--target-no-signature-verification` is re-hardcoded unconditionally; `just drift-gate` green; only the explicit paths staged.
**Why:** Supply-chain provenance is unusable at the one moment it matters most — a tampered archive installs exactly as readily as a signed one, with no operator opt-in available.
**Dep:** none
**Who:** you (bash + TOML + drift-gate)

## AGY-294 -- Verify the staged oci-archive against a build-recorded digest BEFORE the destructive to-disk  (WS-BOOTC | P2 | S)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- refuses to wipe a disk for a truncated or wrong-version archive, using SBOM digest data rather than a hand-pinned constant.
**What+How:** `tools/install.sh:59-62` only checks that the oci-archive EXISTS before wiping `$TARGET_DISK` at L64-71, so a truncated/tampered/wrong-version archive installs silently. Per ADR-0003 (SBOM-not-hardcode) the archive digest is build-time SBOM data: have the oci-archive producer (the AGY-152 `just oci-archive` recipe, or a stopgap here) write a sidecar `<archive>.sha256` next to the tarball, and add a `verify_archive()` step in install.sh that, when the sidecar is present, `sha256sum`s `$OCI_ARCHIVE` and ABORTS before any destructive action on mismatch — skipping with a warning when the sidecar is absent so offline media without it still installs. Print the verification result under `--dry-run`. Add `check_install_archive_verify` asserting install.sh computes and compares a checksum before the `bootc install to-disk` line, by grepping the ORDERING: the `sha256sum` guard must precede the destructive `read -rp`/`bootc install`.
**Where:** `tools/install.sh` (new `verify_archive` before L64), `automation/38-drift-checks.sh` (new check + registration); cross-reference the AGY-152 producer path — do not re-implement the producer
**Done When:** `bash -n tools/install.sh` clean; with a fixture archive plus matching sidecar the dry-run reports `archive digest OK`; a mutated sidecar makes the dry-run abort non-zero; the new check is GREEN on HEAD and RED if the verify guard moves after the destructive step; `just drift-gate` green.
**Why:** The current worst case is an irreversibly wiped disk followed by an unbootable half-installed image, with no diagnosis available afterwards.
**Dep:** cross-refs AGY-152 (producer)
**Who:** you (bash + drift-gate)

## AGY-295 -- Gate the anaconda-ISO container prerequisites (`dracut-live` + `squashfs-tools` + `anaconda`) when the target is enabled  (WS-BOOTC | P2 | S)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- converts iso.toml's prose build contract into a ships-green gate so the installer-ISO leg cannot silently break at BIB time.
**What+How:** `config/artifacts/iso.toml:6-8` records a hard build-time contract — "Source container image MUST include dracut-live + squashfs-tools, or this build leg will fail" — but nothing enforces it; the `FROM ${BASE_IMAGE}` layer in `Containerfile` (~L37) is never checked against the enabled `[deployment].target_anaconda_iso`. Add `check_anaconda_iso_prereqs`: when `[deployment].target_anaconda_iso = true` (mios.toml:6638), assert the `Containerfile` (or its `dnf install` RUN layers) installs `dracut-live`, `squashfs-tools` and `anaconda` by grepping the package tokens; skip-with-warning when the target is disabled. Add the packages to the appropriate `dnf install` RUN layer if they are missing, and cross-reference the gate from iso.toml's header comment.
**Where:** `automation/38-drift-checks.sh` (new check + registration near `check_deploy_plane`), `Containerfile` (packages in the appropriate `dnf install` RUN layer), `config/artifacts/iso.toml` (header cross-ref)
**Done When:** `bash -n automation/38-drift-checks.sh` clean; the new check is GREEN on HEAD (packages present, or the target off); removing `squashfs-tools` from the Containerfile while `target_anaconda_iso=true` turns it RED; `just drift-gate` green.
**Why:** The contract is enforced only by a comment, so a routine package-list trim breaks the installer ISO deep inside a BIB run with an opaque failure.
**Dep:** none
**Who:** you (bash + drift-gate)

## AGY-296 -- SSOT-project rootfs type and root `minsize` across every artifact toml (kill the 80-vs-150-GiB divergence)  (WS-BOOTC | P2 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- filesystem geometry becomes one declared value projected into every artifact, not four hand-edited literals.
**What+How:** The BIB artifact configs hardcode conflicting geometry with no SSOT: `config/artifacts/bib.toml` sets root `minsize = "80 GiB"` while `iso.toml`, `qcow2.toml` and `vhdx.toml` each independently set `"150 GiB"`, and the `Justfile` recipes pass `--rootfs ext4` inline (L202/215/232/251). Add a `[deployment.filesystem]` SSOT block (`root_minsize`, `rootfs_type`, optional `var_home_minsize`) and a small renderer (`usr/libexec/mios/mios-render-artifact-fs`, or extend an existing artifact-render step) that projects those values into each `[[customizations.filesystem]]` mountpoint/minsize and the `--rootfs` flag. Add `check_artifact_fs_projection` as a round-trip check mirroring the `check_kargs_projection` (check 53) pattern, asserting every artifact toml's root minsize and the `--rootfs` args equal the SSOT-rendered values, with no bare `"80 GiB"`/`"150 GiB"`/`ext4` literal outside the generated files.
**Where:** `usr/share/mios/mios.toml` (new `[deployment.filesystem]`), `config/artifacts/{bib,iso,qcow2,vhdx}.toml` (become rendered projections), `Justfile` (recipes read the SSOT rootfs type), new/extended renderer under `usr/libexec/mios/`, `automation/38-drift-checks.sh` (new check + registration)
**Done When:** re-render is deterministic and byte-compares against the committed tomls; mutating `[deployment.filesystem].root_minsize` and re-rendering updates all four tomls idempotently; hardcoding a stray `"200 GiB"` into qcow2.toml turns the check RED; `bash -n` on the renderer clean; `just drift-gate` green; only the explicit paths staged.
**Why:** The same OS ships with two different root sizings depending on which artifact you install, and there is no single place to change it — a Law 7 / Law 8 violation baked into every deploy target.
**Dep:** none
**Who:** you (bash/Python + TOML + drift-gate)

## AGY-297 -- Extend `verify-images` to prove the installer artifacts are bootc-managed, not merely non-empty  (WS-BOOTC | P2 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- artifact verification distinguishes a real bootc installer from a plain-Fedora one, statically and without booting.
**What+How:** The `Justfile` `verify-images:` target (L335-358) only checks size and magic bytes, so an ISO that installs plain Fedora passes identically to a bootc installer ISO. Add a `verify-bootc-artifacts` recipe, invoked from `verify-images` and `all`, that statically introspects the produced media: for the anaconda-iso, mount / `isoinfo` / `7z l` it read-only and assert it embeds an ostree/container commit (grep for `ostree/`, `container`, or the embedded kickstart's `ostreecontainer` directive) rather than a `Packages/` RPM tree; for `raw`/`qcow2`/`vhd`, assert a `/ostree` deployment via `guestfish`/`virt-cat` when available, else skip-with-warning. Add `check_verify_bootc_artifacts` asserting the recipe exists and greps for the bootc/ostree markers, so the verification itself cannot silently degrade to a no-op.
**Where:** `Justfile` (new `verify-bootc-artifacts` recipe + wiring into `verify-images:`/`all:`), `automation/38-drift-checks.sh` (new check + registration)
**Done When:** `just --evaluate` parses; the verify step is fail-open when introspection tools are absent (prints SKIP, exits 0) but FAILS on an ISO carrying a `Packages/` tree; the new check is GREEN on HEAD and RED if the ostree/container marker grep is deleted from the recipe; `just drift-gate` green.
**Why:** "Artifacts verified" currently means "the file is big and starts with the right bytes" — it would pass a completely wrong OS, which is how the plain-Fedora install gap survived undetected.
**Dep:** complements AGY-289/AGY-290
**Who:** you (Justfile/bash + drift-gate)

## AGY-298 -- Make the offline bare-metal installer discoverable: `just to-disk-install` plus a `--dry-run` plan target  (WS-BOOTC | P2 | S)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- the real offline to-disk leg sits beside the VM artifact recipes where an operator will actually find it.
**What+How:** `tools/install.sh` (the real `bootc install to-disk` offline leg) has NO Justfile entry point, so it is undiscoverable next to `raw`/`iso`/`qcow2`/`vhdx` and an operator asking "how do I install MiOS to a disk?" lands on the VM recipes. Add a `to-disk-install:` recipe forwarding to `tools/install.sh` (passing `MIOS_OCI_ARCHIVE`/`--target-disk` through) and a `to-disk-plan:` alias running `tools/install.sh --dry-run` that echoes the resolved `bootc install to-disk` plan with a loud "DESTROYS the target disk" warning, mirroring the `usb-installer:` flash-recipe pattern at Justfile:326-331. Add `check_to_disk_recipe` asserting the recipe references `tools/install.sh`, that the `MIOS_OCI_ARCHIVE` default equals the consumer default at install.sh:7 (`/mnt/mios-repo/mios-latest.tar`) so the two cannot diverge, and that a `--dry-run`/plan alias exists.
**Where:** `Justfile` (new `to-disk-install:` / `to-disk-plan:` recipes near `usb-installer:`), `tools/install.sh` (consumer default, no change), `automation/38-drift-checks.sh` (new check + registration)
**Done When:** `just --evaluate` parses; `just to-disk-plan` prints the resolved plan with no side effects and no network; the new check is GREEN on HEAD and RED if the recipe's archive default diverges from install.sh; `just drift-gate` green.
**Why:** The one leg that reaches real bare metal is effectively hidden, so operators fall back to VM images or a manual invocation with a mistyped archive path.
**Dep:** none
**Who:** you (Justfile + drift-gate)

## AGY-299 -- Collapse the byte-identical `automation/install.sh` ≡ `automation/install-fhs.sh` FHS-overlay installers  (WS-BOOTC | P3 | S)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- one canonical FHS-overlay installer body, so the two names cannot drift apart.
**What+How:** `automation/install.sh` and `automation/install-fhs.sh` are byte-for-byte identical apart from a one-line `# AI-related: install.sh` comment (both: bootc-guard → rsync `usr etc var srv` overlay → sysusers/tmpfiles/daemon-reload) — a live duplicate under two names. First grep the tree AND the kickstart for `automation/install-fhs.sh` vs `automation/install.sh` references to pick the name callers actually use, then make the loser a thin redirector (`exec bash "$(dirname …)/install.sh" "$@"`, or the reverse) keeping the canonical body in exactly ONE file. Add `check_no_dup_fhs_installer` asserting the two paths are not two independent full copies — one must be a redirector or symlink to the other. Coordinate with AGY-154: this is the dedupe, that is the role marker.
**Where:** `automation/install.sh`, `automation/install-fhs.sh` (one becomes a redirector), `automation/38-drift-checks.sh` (new check + registration), plus any caller referencing the retired name
**Done When:** `bash -n` clean on both files; the two are no longer independent full copies (redirector verified); every prior caller still resolves; the new check is GREEN on HEAD and RED if a second full-body copy is reintroduced; `just drift-gate` green; only the explicit paths staged.
**Why:** A fix applied to one installer silently misses the other, so installs behave differently depending on which name the caller happened to use.
**Dep:** coordinate with AGY-154 (role marker)
**Who:** you (bash + drift-gate)

## AGY-300 -- Author the bare-metal bootc install doc (to-disk offline, anaconda-iso, ostreecontainer kickstart, first-boot verify) and gate its cross-refs  (WS-BOOTC | P2 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- makes the offline bare-metal leg discoverable and link-integrity-gated, so an operator can provision a blank machine with no external egress.
**What+How:** `usr/share/doc/mios/concepts/deploy-model.md` only frames the mutable-Fedora vs immutable-bootc split at a high level; there is no concrete doc for the bare-metal leg this workstream builds. Write `usr/share/doc/mios/concepts/bare-metal-bootc-install.md` citing real paths for: (1) the offline `bootc install to-disk --source-imgref oci-archive:` flow through `tools/install.sh` (staging, `--dry-run`, disk-destroy confirm, Law 12 zero-network); (2) the BIB `anaconda-iso` installer path and its kickstart location `config/artifacts/iso.toml`; (3) the new `ostreecontainer` offline kickstart (AGY-290) versus the mutable `mios-kickstart.cfg`; (4) first-boot verification via `mios-verify-bootc-install` (AGY-291) confirming `bootc status` reports the MiOS ref, not plain Fedora; (5) the `raw`/`qcow2`/`vhdx` sibling artifacts. Link it from `deploy-model.md`, `ROADMAP.md` and `architecture.md`, then add `check_bootc_doc_xref` to `automation/38-drift-checks.sh` (registered in `main()`) asserting the doc exists and every cross-referenced path resolves.
**Where:** `usr/share/doc/mios/concepts/bare-metal-bootc-install.md` (new), `usr/share/doc/mios/concepts/deploy-model.md`, `usr/share/doc/mios/concepts/architecture.md`, `ROADMAP.md`, `automation/38-drift-checks.sh`
**Done When:** the doc is committed and every path it cites exists on disk; `check_bootc_doc_xref` is GREEN on HEAD and RED when a referenced path is renamed or removed; `bash -n automation/38-drift-checks.sh` clean; `just drift-gate` green.
**Why:** the only bare-metal install knowledge lives in task text and code today, so an operator (or arriving agent) cannot execute the offline leg without reverse-engineering `tools/install.sh`, and doc links silently rot with no gate to catch them.
**Dep:** AGY-290 (ostreecontainer kickstart), AGY-291 (`mios-verify-bootc-install`)
**Who:** you (docs + drift-gate).

## AGY-301 -- Negative-test all 13 new WS-BOOTC drift-gates (288-300) so none is a green no-op  (WS-BOOTC | P2 | L)**[DONE]**
**Goal:** E-06 Test and documentation harness -- every drift-gate check must be proven capable of FAILING, so the gate set is an eval rather than decoration.
**What+How:** Each WS-BOOTC gate added in this workstream (`check_bib_matrix_parity` 288, `check_installer_is_bootc` 289, `check_bootc_kickstart` 290, `check_verify_bootc_install` 291, the install-karg/sig/archive-verify checks 292-294, `check_anaconda_iso_prereqs` 295, `check_artifact_fs_projection` 296, `check_verify_bootc_artifacts` 297, `check_to_disk_recipe` 298, `check_no_dup_fhs_installer` 299, `check_bootc_doc_xref` 300) currently has no proof it bites -- the same failure mode as the pre-AGY-212 `check_metal_vfio`. Extend `tests/drift-gate-negatives.sh` (existing harness, AGY-120/AGY-212 pattern) with one case per check that injects the specific breakage into a scratch copy, runs only that check, asserts non-zero exit plus the expected named file/line in output, then restores. Injections: drop `qcow2` from `Justfile all`; add a `%packages` block to `mios-bootc-kickstart.cfg`; hardcode an image literal into `mios-verify-bootc-install`; remove `squashfs-tools` from the Containerfile with the ISO target on; hardcode `"200 GiB"` into an artifact toml; move the archive-verify guard after the destructive step. Stage only the explicit touched paths.
**Where:** `tests/drift-gate-negatives.sh`, `automation/38-drift-checks.sh` (checks 288-300 and the surfaces they guard)
**Done When:** `bash -n tests/drift-gate-negatives.sh` clean; every new negative case drives its target check to non-zero exit and the harness restores the tree; the full `tests/drift-gate-negatives.sh` run passes on HEAD; `just drift-gate` green on a clean tree.
**Why:** an unproven check is indistinguishable from no check -- the whole bootc install leg could regress to installing plain Fedora while thirteen gates report green.
**Dep:** AGY-288 through AGY-300
**Who:** you (bash + negative-test harness).

## AGY-302 -- greenboot: un-shadow the dead `check_greenboot` and derive `critical_services` from `[greenboot]` SSOT  (WS-RUNTIME | P1 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- greenboot health coverage is real and SSOT-driven, so every critical service has a check and a rollback path.
**What+How:** `automation/38-drift-checks.sh` defines `check_greenboot()` twice: line 3120 (verifies `46-greenboot.sh` symlink commands and `etc/greenboot/*.sh` exec bits) and an unrelated one at line 3981 (health coverage over `usr/lib/greenboot/check/required.d`). Under bash the later definition wins, so `main()`'s call at line 4266 runs only the 3981 body and the 3120 exec-bit/symlink logic is silently DEAD. The surviving check also hardcodes `critical_services=("agent-pipe" "llm-light" "pgvector")` at line 3988, duplicating `[greenboot].critical_services` in `usr/share/mios/mios.toml` (near `[security.luks]`, ~line 8663), so the two diverge on the next edit. Rename the 3120 function to `check_greenboot_scripts` and register it in `main()`; rewrite the 3981 body to resolve `critical_services` from SSOT via a `python3 -c tomllib` read, degrading open with a WARN when python3/tomllib is absent (mirror `check_cephfs_ssot`, line 3036).
**Where:** `automation/38-drift-checks.sh` (functions at 3120 and 3981, `main()` ~4266), `usr/share/mios/mios.toml` `[greenboot]`, `usr/lib/greenboot/check/required.d/`
**Done When:** both greenboot checks run from `main()` with no shadowed definition (the rename shows in `grep -c 'check_greenboot()'`); adding a 4th entry to `[greenboot].critical_services` with no matching `required.d` script turns the coverage check red; a case in `tests/drift-gate-negatives.sh` proves it bites; `bash -n automation/38-drift-checks.sh` clean; `just drift-gate` green.
**Why:** today the symlink/exec-bit assertions never execute (a broken greenboot script ships undetected), and the hardcoded service list means adding a critical service to SSOT gains no health check or rollback trigger.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-303 -- clevis/LUKS: collapse the two conflicting disk-encryption SSOT tables into one canonical block  (WS-RUNTIME | P1 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- TPM-bound LUKS is configured from ONE SSOT table that the live enrollment path actually reads.
**What+How:** LUKS/clevis is configured from two disjoint tables. `usr/libexec/mios/mios-clevis-luks-gen` (lines 7-9) reads `[security.luks]` (`enabled`/`pin`/`pcrs`, mios.toml:8658, where `pcrs="7"` is a STRING), while `usr/libexec/mios/mios-luks-enroll` -- the script `mios-luks-enroll.service` actually runs (enabled in the preset, line 70) -- reads `[security.disk_encryption]` (`mode`/`backend`/`pcrs`/`recovery`, mios.toml:1083, where `pcrs=[7]` is a LIST). Nothing consumes the generator's output, so it is an orphan whose SSOT already disagrees with the live path (`enabled=false` vs `mode="tpm2"`). Adopt `[security.disk_encryption]` as canonical (it is richer and already live), fold the `pin` concept into it via `pin`/`backend` reconciliation, repoint `mios-clevis-luks-gen` at it (deriving `CLEVIS_LUKS_ENABLED` from `mode != "none"`), and either delete `[security.luks]` or make it an alias the resolver rejects. Both surfaces keep shipping INERT by default -- `mode` drives everything. Add a drift-check forbidding a second table's return.
**Where:** `usr/share/mios/mios.toml` (`[security.luks]` :8658, `[security.disk_encryption]` :1083), `usr/libexec/mios/mios-clevis-luks-gen`, `usr/libexec/mios/mios-luks-enroll`, `automation/38-drift-checks.sh`
**Done When:** one canonical disk-encryption table remains and grep proves neither script reads a second one; `pcrs` has a consistent type across both consumers; the new drift-check reds if an orphan table reappears; `bash -n` clean on both scripts; `just drift-gate` green.
**Why:** two tables that disagree right now mean an operator enabling encryption in the obvious place (`[security.luks]`) changes nothing on the running system, and the divergence widens with every edit.
**Dep:** none
**Who:** you (bash/Python + TOML + drift-gate).

## AGY-304 -- clevis/LUKS: upgrade check 67 from a substring test to a real projection round-trip  (WS-RUNTIME | P2 | S)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the LUKS projection is gated on VALUES, not on the presence of a variable name.
**What+How:** `check_clevis_luks()` (`automation/38-drift-checks.sh`:4004, check 67) only asserts the generator output contains the literal substring `CLEVIS_LUKS_ENABLED=` (line 4012) -- it passes when every projected value is wrong or stale, the exact weakness AGY-212 fixed for the mini gate. Replace the body with a round-trip: run `mios-clevis-luks-gen` against a fixture `mios.toml` carrying known post-AGY-303 `[security.disk_encryption]` values and assert the emitted `CLEVIS_LUKS_ENABLED`/`CLEVIS_PIN`/`CLEVIS_PCRS` EQUAL the SSOT-derived values, covering both the inert case (`mode="none"` -> `ENABLED=false`) and the enabled case (`mode="tpm2"` -> `ENABLED=true`). Add a committed negative fixture to `tests/drift-gate-negatives.sh` that mutates one value and asserts non-zero exit.
**Where:** `automation/38-drift-checks.sh` (check 67 body), `tests/drift-gate-negatives.sh`, `usr/libexec/mios/mios-clevis-luks-gen`
**Done When:** check 67 fails on a MUTATED fixture value, not only on a missing line; both the inert and enabled cases round-trip; the negative test asserts non-zero exit plus a named line; `bash -n` clean; `just drift-gate` green.
**Why:** a substring-only gate reports green while shipping wrong PCR/pin values, meaning disk encryption can be misconfigured at enrollment time with no signal.
**Dep:** AGY-303 (canonical `[security.disk_encryption]`)
**Who:** you (bash + negative-test harness).

## AGY-305 -- ROCm/AMD: add `[gpu.rocm]` SSOT plus a static kfd+dri CDI fallback generator for when `amd-ctk` is missing  (WS-RUNTIME | P1 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- multi-vendor GPU CDI so ROCm reaches parity with the existing NVIDIA CDI plane.
**What+How:** `usr/lib/systemd/system/mios-gpu-amd.service` emits a CDI spec only `if command -v amd-ctk` (lines 21-23), and `amd-ctk` is not in the Fedora RPM repos (the unit's own comment says so) -- so ROCm containers never get `/run/cdi/amd.json` and must hand-pass `--device /dev/kfd --device /dev/dri` forever, while NVIDIA has a full plane (`usr/libexec/mios/mios-cdi-detect`, `nvidia-cdi-refresh`, greenboot `wanted.d/30-nvidia-cdi.sh`). The ROCm userland is already installed (`rocm-opencl`/`rocm-hip`/`rocm-runtime`/`rocm-smi`/`rocminfo`, mios.toml:7217) but has no `[gpu.rocm]` SSOT block. Add `[gpu.rocm]` next to `[gpu]` (mios.toml:675) with `enabled`, `render_group`, `devices` (defaulting to `/dev/kfd` plus `/dev/dri/renderD*`) and `cdi_kind = "amd.com/gpu"`; add `usr/libexec/mios/mios-rocm-cdi-gen` emitting a deterministic CDI v0.6 `/run/cdi/amd.json` (one device enumerating the kfd+dri nodes with the render group) projected from that table; call it from the service's non-`amd-ctk` branch. Ship INERT (`enabled=false` -> no-op) so non-AMD hosts are unaffected.
**Where:** `usr/libexec/mios/mios-rocm-cdi-gen` (new), `usr/lib/systemd/system/mios-gpu-amd.service`, `usr/share/mios/mios.toml` (`[gpu.rocm]`)
**Done When:** `mios-rocm-cdi-gen /path/to/mios.toml` emits schema-valid JSON (`python3 -c 'import json,sys; json.load(...)'`) enumerating the SSOT kfd+dri devices, and emits nothing when `[gpu.rocm].enabled=false`; the service references the generator in its fallback branch; `bash -n` clean on both; no live GPU required.
**Why:** every AMD-GPU container on MiOS today needs hand-written device flags, so ROCm workloads are not reproducible from SSOT and the documented multi-vendor GPU story is unimplemented.
**Dep:** none
**Who:** you (bash + TOML).

## AGY-306 -- ROCm/AMD: drift-check the CDI generator round-trip and prove `mios-gpu-amd.service` is actually reachable  (WS-RUNTIME | P2 | S)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a wired capability is gated, and a unit nothing pulls in counts as unwired.
**What+How:** The AGY-305 generator has no gate, and `mios-gpu-amd.service` is NOT named in `usr/lib/systemd/system-preset/90-mios.preset` (only `mios-gpu-detect.service` and `mios-cdi-detect.service` are, lines 71/74). First establish whether `mios-gpu-detect` pulls it in via Wants/role; if nothing does, add the preset enable line (its `ConditionVirtualization`/`ConditionPathExists=/dev/kfd` already keep it inert). Then add a drift-check at the next free number, registered in `main()` beside the CDI checks, that (a) runs `mios-rocm-cdi-gen` against `usr/share/mios/mios.toml` into a temp file and asserts the enumerated device nodes and render group equal `[gpu.rocm]` (degrade-open when python3 is absent, mirroring `check_cephfs_ssot`), and (b) asserts `mios-gpu-amd.service` is reachable from the preset/role graph.
**Where:** `automation/38-drift-checks.sh` (new check + `main()`), `usr/lib/systemd/system-preset/90-mios.preset`, `usr/lib/systemd/system/mios-gpu-amd.service`
**Done When:** the new check round-trips the AMD CDI projection and passes at HEAD; a mutated `[gpu.rocm].render_group` fixture makes it red; the check confirms the service is enabled or pulled in; a negative test in `tests/drift-gate-negatives.sh` proves it; `just drift-gate` green.
**Why:** without this, the AMD CDI generator can drift from SSOT unnoticed, and the service that invokes it may never start on a real AMD host -- reintroducing the exact hand-passed-device situation AGY-305 removed.
**Dep:** AGY-305 (`mios-rocm-cdi-gen`)
**Who:** you (bash/Python drift-check).

## AGY-307 -- FreeIPA: project the non-secret `ipa-enroll.env` fields from a new `[identity.freeipa]` SSOT block  (WS-RUNTIME | P1 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- identity enrollment is operator-defined through the one config surface instead of a hand-authored env file.
**What+How:** FreeIPA client enrollment is wired (`automation/22-freeipa-client.sh` installs and enables the unit; `usr/libexec/mios/mios-freeipa-enroll.sh` runs `ipa-client-install` gated on `/etc/mios/ipa-enroll.env`), but that env file is not projected from SSOT -- realm/domain/server must be hand-authored outside the one config surface. Add an `[identity.freeipa]` sub-table under the existing `[identity]` block (mios.toml:37) carrying only the non-secret fields the enroll script sources (`realm`, `domain`, `server`, `hostname`, `ntp`, `automount`, plus `enabled` defaulting to false), and add `usr/libexec/mios/mios-freeipa-env-gen` rendering `/etc/mios/ipa-enroll.env` from it -- writing ONLY the `MIOS_IPA_REALM/DOMAIN/SERVER/HOSTNAME/NTP/AUTOMOUNT` lines and deliberately leaving `MIOS_IPA_PASSWORD` to an operator secret drop (never committed, never in mios.toml -- mirror the `[security.mtls]` "certs are SECRETS" pattern at mios.toml:1090). Gate on `[identity.freeipa].enabled` so nothing is written by default, preserving the current `ConditionPathExists` opt-in semantics.
**Where:** `usr/libexec/mios/mios-freeipa-env-gen` (new), `usr/share/mios/mios.toml` (`[identity.freeipa]`), `usr/libexec/mios/mios-freeipa-enroll.sh` (document the SSOT source), `automation/22-freeipa-client.sh`
**Done When:** `mios-freeipa-env-gen` renders the non-secret env lines from `[identity.freeipa]` and writes NOTHING when `enabled=false`; the rendered file never contains a password value; the `ConditionPathExists=/etc/mios/ipa-enroll.env` opt-in still holds; `bash -n` clean; no live IPA server needed.
**Why:** domain-join configuration currently lives outside mios.toml, so an operator cannot declare their realm in the configurator, cannot drift-gate it, and every rebuild loses the hand-written file.
**Dep:** none
**Who:** you (bash + TOML).

## AGY-308 -- FreeIPA: drift-check the `[identity.freeipa]` projection (with a secret-leak guard) plus a wanted.d sssd health check  (WS-RUNTIME | P2 | S)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- greenboot health covers opt-in identity enrollment and the projection cannot silently drift or leak.
**What+How:** The AGY-307 projection has no gate and enrollment has no health signal. Add a drift-check at the next free number, registered in `main()`, that runs `mios-freeipa-env-gen` against `usr/share/mios/mios.toml` into a temp file and asserts (a) the emitted `MIOS_IPA_*` lines equal `[identity.freeipa]` (round-trip), (b) the render contains NO `MIOS_IPA_PASSWORD` line (secret-leak guard), and (c) nothing is written when `enabled=false`. Additionally add `usr/lib/greenboot/check/wanted.d/70-freeipa-sssd.sh` (WANTED, not required -- enrollment is opt-in) that, when `/etc/ipa/default.conf` exists, verifies `sssd.service` is active as a non-fatal signal, following the existing `wanted.d/*.sh` convention.
**Where:** `automation/38-drift-checks.sh` (new check + `main()`), `usr/lib/greenboot/check/wanted.d/70-freeipa-sssd.sh` (new), `tests/drift-gate-negatives.sh`
**Done When:** the projection check round-trips `[identity.freeipa]` and passes at HEAD; a mutated realm fixture makes it red; injecting a password line fires the leak guard; the wanted.d script is 0755 and `bash -n` clean; a negative test proves the gate bites; `just drift-gate` green.
**Why:** an ungated generator can start emitting stale or secret-bearing env files, and an enrolled host whose `sssd` is dead loses all domain logins with no boot-time signal.
**Dep:** AGY-307 (`mios-freeipa-env-gen`)
**Who:** you (bash/Python drift-check).

## AGY-309 -- Guacamole: load the JDBC schema into the mios DB and SSOT-ify the hardcoded DB creds and guacd port  (WS-RUNTIME | P1 | L)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a shipped service is either working and SSOT-wired or explicitly removed; Guacamole becomes the former.
**What+How:** The `mios-guacamole` Quadlet (`usr/share/containers/systemd/mios-guacamole.container`) points at `POSTGRESQL_DATABASE=mios`, but `usr/share/mios/postgres/schema-init.sql` contains ZERO Guacamole tables (`grep -c guacamole_ schema-init.sql` = 0) -- the container has no `guacamole_connection`/`guacamole_user` schema and cannot authenticate, so Guacamole is a dead service today. It also hardcodes `POSTGRESQL_PASSWORD=mios` / `POSTGRESQL_USERNAME=mios` (lines 15-16) and `GUACD_PORT=4822` (line 18). Vendor the Guacamole PostgreSQL JDBC DDL (`001-create-schema.sql` + `002-create-admin-user.sql`) under `usr/share/mios/postgres/guacamole/` and wire it into the DB init path (a `schema-init.sql` include or a firstboot loader against the mios DB), guarded to load only when the guacamole tables are absent so it is idempotent. Project the DB password from `[identity].default_password` (mios.toml:37, "every service defaults to this credential") and add `guacd_port` to `[ports]` so the Quadlet template stops hardcoding `4822`. Regenerate via `tools/generate-pod-quadlets.py` and STAGE ONLY the explicit changed files -- never a broad `git add` in this shared tree.
**Where:** `usr/share/mios/postgres/guacamole/*.sql` (new), `usr/share/mios/postgres/schema-init.sql` (or a loader under `automation/firstboot/`), `usr/share/mios/mios.toml` (`[containers.mios-guacamole.Container]` env, `[ports].guacd`), `usr/share/containers/systemd/mios-guacamole.container`, `usr/share/containers/systemd/mios-guacd.container`
**Done When:** the JDBC schema loads idempotently (a re-run creates nothing new); the container's DB password resolves from `[identity].default_password` and the guacd port from `[ports]` (grep proves no literal `4822` or password beyond the SSOT var in the generated Quadlet); `generate-pod-quadlets.py` re-render equals the committed files; `bash -n` and SQL parse clean; explicit-path staging only.
**Why:** MiOS ships a remote-desktop gateway that cannot start a session -- an operator enabling it gets an authentication failure -- and its credentials are baked as literals outside the SSOT.
**Dep:** none
**Who:** you (SQL + bash + TOML).

## AGY-310 -- Guacamole: fix the desktop launcher pointing at the wrong port and gate Quadlet<->SSOT consistency  (WS-RUNTIME | P2 | S)**[DONE]**
**Goal:** E-13 Ports are allocated from SSOT, not hand-assigned -- an operator-visible surface stops disagreeing with `[ports]`.
**What+How:** `usr/share/applications/mios-svc-guacamole.desktop` hardcodes `http://localhost:8090/guacamole/` (Exec line 9 and Comment line 8), while SSOT says `[ports].guacamole_web = 8080` (mios.toml:7848) -- the launcher literally opens the wrong port. The generated Quadlet is correct (`${MIOS_PORT_GUACAMOLE_WEB}`, `mios-guacamole.container`:22-24), so the `.desktop` is the sole divergent surface. Repoint its Exec/Comment at the SSOT port -- ideally by projecting it through the same dotfile/theme-render path used for other surfaces, at minimum by correcting the literal -- and add a drift-check at the next free number, registered in `main()`, asserting (a) the `.desktop` URL port equals `[ports].guacamole_web` and (b) the `mios-guacamole` / `mios-guacd` Quadlets round-trip against `[containers.mios-guacamole]` / `[containers.mios-guacd]` through `tools/generate-pod-quadlets.py`, guarding the AGY-309 regeneration.
**Where:** `usr/share/applications/mios-svc-guacamole.desktop`, `automation/38-drift-checks.sh` (new check + `main()`), `usr/share/mios/mios.toml` (`[ports].guacamole_web`)
**Done When:** the launcher opens the SSOT port (no stale `8090` remains); the new check reds when the `.desktop` port and `[ports].guacamole_web` disagree; the guac Quadlet round-trip is green; a negative test flips the `.desktop` port and asserts red; `just drift-gate` green.
**Why:** clicking the shipped Guacamole launcher today lands on a dead port -- a first-impression failure for any operator -- and nothing prevents the same drift returning after the AGY-309 regeneration.
**Dep:** AGY-309 (Quadlet + SSOT port work)
**Who:** you (bash + drift-check).

## AGY-311 -- NUT: actually RUN the rendered UPS config by enabling upsd/upsmon, gated on standalone MODE  (WS-RUNTIME | P1 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- rendered configuration that no service consumes is unwired; NUT gets its daemons.
**What+How:** `automation/25-nut-render.sh` renders `/etc/ups/{nut,ups,upsd,upsmon}.conf` from `[power.ups]` (gated by check 56), but NO NUT service is ever started -- there is no `nut-server`/`upsd`/`upsmon`/`nut-monitor` line in `usr/lib/systemd/system-preset/90-mios.preset` (grepping for `ups` or `nut` returns nothing), so on a host with a real UPS the rendered `MODE=standalone` config sits dead and the box never shuts down on battery. Enable the upstream `nut-server.service` (upsd) and `nut-monitor.service` (upsmon) in the preset, each gated to no-op when NUT is inert -- a MiOS drop-in with `ConditionPathExists`/`ConditionFileNotEmpty=/etc/ups/ups.conf`, keyed off the render writing a non-empty `[name]` block (the default `[power.ups].name=""` renders `MODE=none` and an empty ups.conf). Confirm the package providing those units is in the MiOS package set, and document the service-state contract in the render script.
**Where:** `usr/lib/systemd/system-preset/90-mios.preset`, `usr/lib/systemd/system/nut-*.service.d/` (optional MiOS condition drop-ins), `automation/25-nut-render.sh`
**Done When:** the NUT daemons are enabled in the preset and no-op on the default (no-UPS) build; a drift-check (or an extension of check 56) asserts render MODE and unit-enable state agree (name set -> units enabled + `MODE=standalone`; name empty -> inert); `bash -n` clean; `just drift-gate` green; no live UPS required.
**Why:** a MiOS host on a real UPS silently loses power protection -- the config is perfect and nothing reads it, so the machine hard-powers-off on battery exhaustion.
**Dep:** none
**Who:** you (systemd unit wiring + drift-check).

## AGY-312 -- CephFS: gate every `${MIOS_CEPHFS_*}` template variable against the `[storage.cephfs]` SSOT keys  (WS-RUNTIME | P2 | S)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a template placeholder with no SSOT key behind it becomes unrepresentable.
**What+How:** `usr/share/mios/systemd/home-@.mount.tmpl` and `home-@.automount.tmpl` reference `${MIOS_CEPHFS_MONITORS}`, `${MIOS_CEPHFS_TENANT_ID}`, `${MIOS_CEPHFS_KEYRING_DIR}` and `${MIOS_CEPHFS_MOUNT_OPTIONS}` (lines 6, 9, 12), which must resolve from `[storage.cephfs]` (`monitors`/`tenant_id`/`keyring_dir`/`mount_options`, mios.toml:9896) when `automation/firstboot/mios-cephfs-mount-setup.sh` instantiates them -- but nothing gates that correspondence, so a renamed or removed SSOT key silently yields a broken mount unit with an empty `What=`. Extend `check_cephfs_ssot` (`automation/38-drift-checks.sh`:1034), or add a sibling check, that greps every `${MIOS_CEPHFS_*}` token out of the two `.tmpl` files, asserts each maps to a defined `[storage.cephfs]` key via the existing env-name convention, and asserts `mios-cephfs-mount-setup.sh` substitutes each one so no un-substituted `${MIOS_CEPHFS_*}` survives its render. Static validation only -- no live ceph mount.
**Where:** `automation/38-drift-checks.sh` (`check_cephfs_ssot`), `usr/share/mios/systemd/home-@.mount.tmpl`, `usr/share/mios/systemd/home-@.automount.tmpl`, `automation/firstboot/mios-cephfs-mount-setup.sh`, `usr/share/mios/mios.toml` `[storage.cephfs]`
**Done When:** the check enumerates every template var and asserts both a matching `[storage.cephfs]` key and a matching substitution in the firstboot script; deleting `mount_options` from SSOT (or its substitution) makes it red; a negative test proves it; `bash -n` clean; `just drift-gate` green.
**Why:** a silent SSOT rename produces a mount unit with an empty target -- discovered only when a user's home directory fails to mount on a real cluster.
**Dep:** none
**Who:** you (bash/Python drift-check).

## AGY-313 -- mdevctl: add `[mdev]` SSOT persistent mediated-device definitions for SR-IOV VFs (explicitly NOT GPU fractioning)  (WS-RUNTIME | P2 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- mdevctl gains its legitimate positive wiring without weakening the architectural rejection of driver-free GPU fractioning.
**What+How:** mdevctl exists in the tree ONLY as a rejection gate: `automation/38-drift-checks.sh`:3326 fails any doc claiming `mdevctl vGPU` unless marked impossible/unsupported/out-of-scope (correct -- the mios-metal architecture is whole-GPU-to-one-guest). There is no positive wiring for its legitimate use: persisting mediated devices for SR-IOV VFs and vendor-driver mdev types via `mdevctl define`, which the already-enabled `mios-sriov-init.service` sets the stage for. Add a `[mdev]` SSOT table (`enabled` default false; a list of definitions `{ parent = "<BDF>", mdev_type = "<type>", uuid = "<uuid>" }`) and `usr/libexec/mios/mios-mdev-define-gen` emitting `mdevctl define`-compatible config (the `/etc/mdevctl.d/<parent>/<uuid>` JSON mdevctl persists) projected from it, shipping INERT (`enabled=false` emits nothing). Critically: add a header comment scoping this to mediated SR-IOV/vendor devices only, leave the 3326 rejection gate fully intact, and introduce no vGPU/GPU-fractioning claim anywhere.
**Where:** `usr/libexec/mios/mios-mdev-define-gen` (new), `usr/share/mios/mios.toml` (`[mdev]`), `automation/38-drift-checks.sh` (do NOT weaken the check at 3326), `usr/lib/systemd/system/mios-sriov-init.service` (document the relationship)
**Done When:** `mios-mdev-define-gen` emits deterministic mdevctl-persist config from `[mdev]` and no-ops when `enabled=false`; the output carries no vGPU/fractioning claim and the check at line 3326 stays green; `bash -n` and JSON parse clean; inert by default; no live hardware.
**Why:** SR-IOV VF assignments must be re-created by hand after every reboot today, and the only mention of mdevctl in the tree is a prohibition -- so the supported capability looks unsupported.
**Dep:** none
**Who:** you (bash + TOML + drift-gate).

## AGY-314 -- virt-v2v: add `[virt.v2v]` SSOT and a `mios-v2v-import` wrapper onto the local libvirt/KVM plane  (WS-RUNTIME | P2 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the least-wired shipped feature gets SSOT and an entry point, or a recorded decision; this is the wiring.
**What+How:** virt-v2v appears ONLY in docs and `MiOS-SBOM.csv` -- no unit, no config, no SSOT (grepping `virt-v2v`/`v2v` across `usr` and `automation` returns nothing operational). MiOS already ships the full libvirt/qemu plane (the preset enables `virtqemud.socket`, `virtnetworkd.socket` and siblings, lines 39-45), so importing a VMware/Hyper-V/OVA guest into local KVM is the natural missing on-ramp. Add a `[virt.v2v]` SSOT table (`enabled` default false, `output_storage` pool, `output_network`, `output_format = "qcow2"`, `default_input = "disk"`) and `usr/libexec/mios/mios-v2v-import`, which resolves those from SSOT and invokes `virt-v2v -o libvirt -os <pool> -of <format> -on <name>` against an operator-supplied input path or OVA, validating that `virt-v2v` and the target libvirt pool exist and no-opping with a clear message when they do not (degrade-open; no live conversion in CI). Guard on `[virt.v2v].enabled` so it ships inert.
**Where:** `usr/libexec/mios/mios-v2v-import` (new), `usr/share/mios/mios.toml` (`[virt.v2v]`)
**Done When:** `mios-v2v-import --help` / dry-run resolves `[virt.v2v]` and prints the exact `virt-v2v` command it WOULD run (pool/network/format from SSOT) without executing; it exits 0 with a clear message when `virt-v2v` is absent; `bash -n` clean; inert by default; no live VM or conversion.
**Why:** an operator migrating off VMware has no MiOS-supported path -- they must reconstruct the whole `virt-v2v` invocation and pool wiring by hand, with nothing tying it to the host's declared storage.
**Dep:** none
**Who:** you (bash + TOML).

## AGY-315 -- virt-v2v: drift-check `mios-v2v-import` SSOT parity and document the import on-ramp  (WS-RUNTIME | P2 | S)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the new wrapper is gated against hardcoding and discoverable in the run-plane doc.
**What+How:** The AGY-314 wrapper needs a gate proving it reads SSOT plus a doc so the feature is findable. Add a drift-check at the next free number, registered in `main()` beside the other runtime checks, that (a) `bash -n` parses `mios-v2v-import`, (b) asserts it resolves `output_storage` / `output_network` / `output_format` from `[virt.v2v]` rather than hardcoding them (grep for a literal pool or format and fail unless it is a SSOT-default fallback), and (c) asserts `--dry-run` prints a non-empty planned `virt-v2v` command whose format matches `[virt.v2v].output_format`, degrading open when the `virt-v2v` binary is absent. Add a short section on the v2v import on-ramp -- including that it ships inert -- to the container-runtime doc.
**Where:** `automation/38-drift-checks.sh` (new check + `main()`), `usr/libexec/mios/mios-v2v-import`, `tests/drift-gate-negatives.sh`, `docs/agy/doc-container-runtime.md` (or `usr/share/doc/mios/concepts/container-os-runtime.md`)
**Done When:** the check parses the wrapper, asserts SSOT resolution with no hardcoded pool/format, and validates the `--dry-run` plan against `[virt.v2v]`; a negative test hardcoding a format makes it red; the doc section lands; `bash -n` clean; `just drift-gate` green.
**Why:** without the gate the wrapper drifts back to literals (a Law 7 regression), and without the doc the on-ramp is invisible and stays as unused as virt-v2v is today.
**Dep:** AGY-314 (`mios-v2v-import`)
**Who:** you (bash/Python drift-check + docs).

## AGY-316 -- Add the `[build.float]` SSOT allowlist naming every legitimately-pinned build input  (WS-DRIFT | P1 | S)**[DONE]**
**Goal:** E-14 Float latest globally -- a documented `[build.float]` allowlist becomes the ONLY place a legitimately-pinned build input may live.
**What+How:** ADR-0003 (SBOM-not-hardcode) says versions FLOAT and are resolved plus recorded at build, never hand-pinned in SSOT -- yet `usr/share/mios/mios.toml [build.bake_refs]` (lines 8554-8560) hand-pins `quickshell = "v0.3.0"`, `syft = "v1.19.0"`, `lookingglass = "B7"` and `surfer = "<40-hex>"`. The forthcoming NO-HARDCODE-VERSION gate (AGY-317/318) must not red on these legitimate pins, so add one explicit exemption registry: a `[build.float]` table with `pinned_refs` (dotted keys under `[build.bake_refs]` intentionally pinned, each with an inline reason comment) and `exempt_globs` (repo-relative path globs whose version literals are legitimate, e.g. `automation/lib/globals.*`, `MiOS-SBOM.csv`, `usr/share/mios/artifacts/sbom/*`). Mirror the shape and header-comment style of `[laws.target_languages]` (line 725) and `[security.nohc_allowlist]` (line 793). Expose it via a `float_allowlist()` helper in `usr/lib/mios/mios_toml.py` built on the existing `load_vendor()` / `section()` accessors -- never a raw file read, which check 619 `check_raw_toml_readers` forbids.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/mios/mios_toml.py`
**Done When:** `python3 -c "import mios_toml, os; os.environ.setdefault('MIOS_ROOT_LIB','usr/lib/mios'); print(mios_toml.float_allowlist())"` prints the table; `python3 -m py_compile usr/lib/mios/mios_toml.py` clean; `just drift-gate` (or `bash automation/38-drift-checks.sh`) stays green; all four current `[build.bake_refs]` pins are enumerated in `[build.float].pinned_refs` with a reason.
**Why:** with no allowlist, any version gate must either be turned off or must red on four genuinely-necessary pins -- so the float law stays unenforceable and pinned inputs keep accumulating with no recorded rationale.
**Dep:** none
**Who:** you (TOML/SSOT + Python).

## AGY-317 -- Build the `mios-version-lint` NO-HARDCODE-VERSION scanner driven by the `[build.float]` allowlist  (WS-DRIFT | P1 | M)**[DONE]**
**Goal:** E-14 Float latest globally -- every inlined version literal and hand-pinned digest is mechanically detectable, so SSOT carries intent only.
**What+How:** `usr/libexec/mios/mios-hardcode-lint` (consumed by `check_no_hardcode`, `38-drift-checks.sh`:862) enforces the NO-HARDCODE law for DATES-in-comments only -- it does not catch hand-pinned VERSIONS, which ADR-0003 forbids. Create the sibling scanner `usr/libexec/mios/mios-version-lint` (exit 1 on violation, `MIOS_VERSION_LINT_SOFT=1` advisory mode, `sys.exit` style, matching mios-hardcode-lint's header/AI-Hint convention) that fails on three regression classes in TRACKED source: (1) a `vN.N.N` / `N.N.N` literal embedded in a download URL (`curl`/`wget`/`ADD`/`https://.../releases/download/...`); (2) an `@N.N` / `==N.N` / `~=N.N` pip pin or `@N.N.N` / `pkg@N` npm/bun pin in an install command; (3) a hand-pinned `@sha256:<64hex>` image digest in a tracked `.container`/`.image`/Containerfile/`.sh` -- SBOM records digests at build, source carries `:latest` intent. Load exemptions from `mios_toml.float_allowlist()`: skip paths matching `exempt_globs` and skip `[build.bake_refs]` values whose key is in `pinned_refs`. Keep it comment- and quote-aware like mios-hardcode-lint so a documented example is not flagged.
**Where:** `usr/libexec/mios/mios-version-lint` (new), `usr/lib/mios/mios_toml.py`
**Done When:** `python3 usr/libexec/mios/mios-version-lint .` exits 0 on the clean tree; injecting `RUN pip install foo==1.2.3` into a scratch tracked file makes it exit 1 citing file:line; `python3 -m py_compile` (or an equivalent syntax check) clean; allowlisted `[build.bake_refs]` pins do not trip it.
**Why:** hand-pinned versions and digests are the exact class that produced the recurring Quadlet-digest drift and stale-download breakage, and nothing in the tree can currently find them.
**Dep:** AGY-316 (`[build.float]` + `float_allowlist()`)
**Who:** you (Python).

## AGY-318 -- Wire `check_no_hardcode_version` into 38-drift-checks.sh and `main()`  (WS-DRIFT | P1 | S)**[DONE]**
**Goal:** E-14 Float latest globally -- the version lint becomes a build-failing gate rather than a tool nobody runs.
**What+How:** Add `check_no_hardcode_version()` to `automation/38-drift-checks.sh` following the exact shape of `check_no_hardcode` (line 862): guard on `command -v python3`; guard that the tool exists at `$ROOT/usr/libexec/mios/mios-version-lint`; run it against `$ROOT`, capture stderr to a temp `.verlint.err`, `sed 's/^/    /'` it on failure, and call `_violation "NO-HARDCODE-VERSION (Law 7 / ADR-0003): a hand-pinned version literal in a download URL / pip|npm pin / image @sha256 digest -- float it (:latest) and record the resolved version in the SBOM, or allowlist via mios.toml [build.float]"`. Emit the next free display label on success (grep `\([0-9]\{2,3\}\)`; the highest today is `(71)`, so use `(72)`). Register the call in `main()` (lines 4205-4292) immediately after `check_no_hardcode` so the two NO-HARDCODE gates sit together.
**Where:** `automation/38-drift-checks.sh`
**Done When:** `bash -n automation/38-drift-checks.sh` clean; `bash automation/38-drift-checks.sh check_no_hardcode_version` prints the `(72)` line and exits 0 at HEAD; the function appears exactly once in `main()`; `just drift-gate` green.
**Why:** an unwired lint changes nothing -- a hand-pinned version can still be merged to main and baked into a published image.
**Dep:** AGY-317 (`mios-version-lint`)
**Who:** you (bash + drift-gate).

## AGY-319 -- Negative-test the NO-HARDCODE-VERSION gate  (WS-DRIFT | P2 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- the new gate is proven able to FAIL, not merely observed passing.
**What+How:** Add `test_no_hardcode_version` to `tests/drift-gate-negatives.sh` following the inject / assert-red / restore / assert-green pattern of `test_version_ssot` (lines 22-42) and `test_eval_safety` (line 67). Create a temp tracked-shaped file under `$ROOT` (e.g. `usr/libexec/mios/mios-test-temp-verpin.sh`) containing a forbidden pin such as `curl -LO https://example.com/releases/download/v1.2.3/x`; assert `bash automation/38-drift-checks.sh check_no_hardcode_version` exits non-zero; `rm` the temp file; assert it exits zero. Register the case in the harness `main()` runner alongside the existing tests, restoring before any `die` as the existing cases do.
**Where:** `tests/drift-gate-negatives.sh`
**Done When:** `bash -n tests/drift-gate-negatives.sh` clean; the harness passes with the new case included; the temp file is always removed, even on failure.
**Why:** a version gate that cannot be shown to bite may be silently regex-broken by a later edit, restoring the hand-pinning class while reporting green.
**Dep:** AGY-318 (`check_no_hardcode_version`)
**Who:** you (bash).

## AGY-320 -- Law-enforcer integrity meta-gate: every `[laws].enforced_by` reference must resolve  (WS-DRIFT | P1 | M)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- a law whose enforcer does not exist is not a law, and that condition becomes impossible to ship.
**What+How:** `usr/share/mios/mios.toml [laws]` (lines 694-723) is the id<->slug<->enforcer SSOT, but two `enforced_by` targets name functions that do not exist: Law 1 `USR-OVER-ETC -> 38-drift-checks.sh:check_usr_over_etc` and Law 8 `SSOT-PROJECTION -> 38-drift-checks.sh:check_projection_registry` (`grep -c '^check_usr_over_etc()' automation/38-drift-checks.sh` and the same for `check_projection_registry` both return 0). Nothing gates this today. Add `check_law_enforcers()` to `automation/38-drift-checks.sh`: parse `[laws]` in a python heredoc (tomllib over `$ROOT/usr/share/mios/mios.toml`), split each `enforced_by` on `,`, and per `file:ref` token -- if `file` is `38-drift-checks.sh` assert `grep -qE "^${ref}\(\)"` in this script; if `file` is `99-postcheck.sh` assert the file exists and contains a matching item/marker; `process:` refs only assert the cited files exist. `_violation` listing each unresolved `law <id> <slug> -> <ref>`. Use the next free display label after AGY-318.
**Where:** `automation/38-drift-checks.sh`
**Done When:** `bash -n` clean; `bash automation/38-drift-checks.sh check_law_enforcers` is currently RED naming exactly `check_usr_over_etc` (law 1) and `check_projection_registry` (law 8) -- that is the acceptance signal -- and goes GREEN once AGY-321 and AGY-322 land; the call is wired into `main()`.
**Why:** two of thirteen architectural laws are advertised as enforced while enforcing nothing, and the registry can grow more phantom enforcers with every ADR since no check reads it.
**Dep:** goes green only after AGY-321 + AGY-322
**Who:** you (bash + Python + drift-gate).

## AGY-321 -- Ship the missing Law 1 enforcer `check_usr_over_etc` (USR-OVER-ETC)  (WS-DRIFT | P1 | M)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- Law 1 becomes a real fitness function instead of a registry row.
**What+How:** Law 1 is declared `enforced_by = "38-drift-checks.sh:check_usr_over_etc"` in `[laws]` but the function is absent, so the law is un-enforced. The adjacent `check_etc_duplicates` (line 2386) only catches `etc/containers/systemd` units shadowing generated `usr/share` units; USR-OVER-ETC is broader -- writable `/etc` must not carry config that belongs read-only under `/usr`. Add `check_usr_over_etc()` to `automation/38-drift-checks.sh` scanning tracked `etc/**` for files whose basename plus relative tail also exists under the corresponding `usr/share/**` or `usr/lib/**` projection root (a full-content `/etc` copy of a `/usr` SSOT surface), excluding the legitimate runtime drop-in directories already covered by `check_etc_duplicates` and any `*.d/` operator override slots. `_violation` on hits with a remediation hint: move the canonical copy to `/usr` and leave only operator overrides in `/etc`. Emit the next free display label and wire it into `main()` near `check_etc_duplicates`.
**Where:** `automation/38-drift-checks.sh`
**Done When:** `bash -n` clean; `bash automation/38-drift-checks.sh check_usr_over_etc` green at HEAD; `check_law_enforcers` (AGY-320) no longer reports law 1 unresolved; the call is wired into `main()`.
**Why:** without it, a `/etc` copy of a `/usr` SSOT file wins over the image-owned original at runtime -- breaking the 3-way `/etc` merge on upgrade and freezing stale config that survives `bootc upgrade`.
**Dep:** AGY-320 (meta-gate that reports it missing)
**Who:** you (bash + drift-gate).

## AGY-322 -- Ship the missing Law 8 enforcer `check_projection_registry` (SSOT-PROJECTION)  (WS-DRIFT | P1 | M)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- "a projection surface exists but nothing gates its freshness" becomes unrepresentable.
**What+How:** Law 8 names `enforced_by = "38-drift-checks.sh:check_projection_registry"` but the function is absent; projection freshness is enforced piecemeal today by `check_toml_projection` (mios-sync-toml `--check`, line 3618), `check_drift_projection` (line 1998) and `check_dotfiles_projection` (line 1573), with no registry proving EVERY declared surface is covered. Add a `[laws.projection_registry] surfaces = [ ... ]` table to `usr/share/mios/mios.toml` beside `[laws.target_languages]`, each entry naming `generator`, `check` and `output`; then add `check_projection_registry()` to `automation/38-drift-checks.sh` that reads it and, per surface, asserts the generator path exists and a `check_*` function of that name exists in this script (`grep -qE "^<check>\(\)"`), calling `_violation` per uncovered surface. Emit the next free display label and wire it into `main()`.
**Where:** `automation/38-drift-checks.sh`, `usr/share/mios/mios.toml`
**Done When:** `bash -n` clean; `[laws.projection_registry]` enumerates the existing mios-sync-toml, mios-dotfiles-render and generate-bake-plan surfaces; `bash automation/38-drift-checks.sh check_projection_registry` green at HEAD; `check_law_enforcers` (AGY-320) no longer reports law 8 unresolved; wired into `main()`.
**Why:** every new SSOT projection added since the last audit can ship with no freshness gate, which is exactly how generated surfaces (Quadlets, dotfiles, bake plan) silently diverge from mios.toml.
**Dep:** AGY-320 (meta-gate that reports it missing)
**Who:** you (bash + Python + drift-gate).

## AGY-323 -- Negative-test the law-enforcer meta-gate and both new enforcers  (WS-DRIFT | P2 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- the three new governance gates are proven able to FAIL before they are trusted.
**What+How:** Add `test_law_enforcers` to `tests/drift-gate-negatives.sh` following `test_version_ssot` (lines 22-42) but operating on `usr/share/mios/mios.toml`: snapshot the file, rename an existing `enforced_by` target to a non-existent `check_bogus_xyz` (or append a bogus enforcer reference), assert `check_law_enforcers` exits non-zero, restore, assert green. Add matching inject/restore cases for `check_usr_over_etc` (drop a duplicate of a `/usr` SSOT file into `etc/` -> red) and `check_projection_registry` (add a `[laws.projection_registry]` surface whose `check` names a non-existent function -> red). Register all three in the harness `main()`.
**Where:** `tests/drift-gate-negatives.sh`
**Done When:** `bash -n tests/drift-gate-negatives.sh` clean; the harness passes with the three new cases; each case restores the mutated file before asserting green and before any `die`.
**Why:** the governance layer is only as strong as its gates; an unproven law-enforcer meta-gate could pass on a tree where enforcers were quietly deleted.
**Dep:** AGY-320, AGY-321, AGY-322
**Who:** you (bash).

## AGY-324 -- Bake-plan integrity gate: prove the firstboot-tier eviction is correct AND complete  (WS-DRIFT | P1 | M)**[DONE]**
**Goal:** E-16 The bake plane -- the invariant that lets a standard GitHub runner bake and publish is mechanically guaranteed, not assumed.
**What+How:** `check_bake_plan` (line 3672) only runs `generate-bake-plan.py --check` (lists match SSOT) -- it never proves the firstboot-tier EVICTION is semantically correct, the invariant that keeps the ~47GB vLLM+SGLang whales out of the runner bake. Add `check_bake_plan_integrity()` to `automation/38-drift-checks.sh` that loads `[build.bake].core`, `groups`, `group_members` and `firstboot_tokens` (`usr/share/mios/mios.toml` lines 8575-8619) via tomllib, reads the generated `usr/lib/mios/bake/plan.d/*.list` plus `firstboot.list`, then asserts: (1) every image matching a `firstboot_tokens` substring is ABSENT from every `NN-<group>.list` and PRESENT in `firstboot.list`; (2) no token-matching image is dropped entirely -- each remains in `core`; (3) the union of all group lists plus `firstboot.list` exactly equals the resolved `core` set (a true partition, no orphans or duplicates); (4) `firstboot_tokens` is non-empty iff `firstboot.list` is non-empty. `_violation` per broken invariant. Read-only, no build. Emit the next free display label.
**Where:** `automation/38-drift-checks.sh`
**Done When:** `bash -n` clean; `bash automation/38-drift-checks.sh check_bake_plan_integrity` green at HEAD (vllm/sglang in `firstboot.list`, absent from group lists, present in `core`); wired into `main()` next to `check_bake_plan`; `just drift-gate` green.
**Why:** if the eviction silently breaks, the publish bake exceeds the standard runner's disk and fails late in CI -- or worse, an evicted image is dropped from `core` entirely and a published image ships missing a service.
**Dep:** none
**Who:** you (bash + Python + drift-gate).

## AGY-325 -- Prove the bake-plan integrity gate can fail: negative test for firstboot-tier leakage  (WS-DRIFT | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- every drift-gate check carries a negative test that proves it can actually go RED, so a vacuously-passing gate cannot masquerade as enforcement.
**What+How:** Add `test_bake_plan_integrity` to `tests/drift-gate-negatives.sh` using the harness inject/restore pattern. Case 1: snapshot the group list a whale would land in (`usr/lib/mios/bake/plan.d/03-extra.list`), inject an image line containing `vllm` so a firstboot-tier ref is scheduled into the bake despite being in `firstboot_tokens`, assert `check_bake_plan_integrity` exits NON-zero, restore the list byte-for-byte, assert green again. Case 2: perturb `firstboot.list` by removing the vllm entry to trip invariants (1)/(4). Register both in the harness `main()`.
**Where:** `tests/drift-gate-negatives.sh`, `usr/lib/mios/bake/plan.d/03-extra.list`, `usr/lib/mios/bake/plan.d/firstboot.list`
**Done When:** `bash -n tests/drift-gate-negatives.sh` is clean; the harness passes with the two new cases registered in `main()`; the mutated `.list` files are restored before the assert-green step and before any `die` path.
**Why:** `check_bake_plan_integrity` is the only thing keeping the ~47GB vLLM/SGLang firstboot tier out of the CI bake, and today nothing proves it would notice. A silently-broken gate lets a whale back into the group lists and the publish bake dies on a standard runner with no earlier warning.
**Dep:** none
**Who:** you (bash).

## AGY-326 -- Value-parity, not just presence, for baker `MIOS_BUILD_BAKE_REFS_*` defaults  (WS-DRIFT | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- extend SSOT-projection coverage so a projected literal must EQUAL its SSOT source, not merely be non-empty.
**What+How:** `check_bake_ref_defaults` (`automation/38-drift-checks.sh:3684`) only asserts baker scripts carry a NON-EMPTY `${MIOS_BUILD_BAKE_REFS_<X>:-...}` default; it never compares that literal to `[build.bake_refs]` in `usr/share/mios/mios.toml:8554-8560`, so a baker default can drift from SSOT undetected. Extend the gate (or add a sibling `check_bake_ref_parity`): `git grep` `automation/` for every `MIOS_BUILD_BAKE_REFS_<KEY>:-<lit>` default, lowercase `<KEY>` into `[build.bake_refs].<key>`, resolve the SSOT value via a `tomllib` heredoc, and `_violation` on each mismatch as `<baker>: MIOS_BUILD_BAKE_REFS_<KEY> default '<lit>' != SSOT '<ssot>'`. Keep the existing empty-default assertion intact. Same class of value-parity check 28 `check_globals_ports` already performs for ports.
**Where:** `automation/38-drift-checks.sh`, `usr/share/mios/mios.toml`, `automation/` bakers
**Done When:** `bash -n automation/38-drift-checks.sh` is clean; the gate is green at HEAD (every baker default equals SSOT); temporarily editing one baker's `:-` literal turns it red naming that baker and key; the function is wired into `main()`; `just drift-gate` green.
**Why:** A baker whose fallback ref has drifted from `[build.bake_refs]` bakes a DIFFERENT image than the SSOT declares whenever the env var is unset -- the image and the config disagree, and the gate currently blesses it.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-327 -- Prove the bake-ref value-parity gate can fail: negative test  (WS-DRIFT | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- the new parity gate from AGY-326 ships with a test that demonstrates it goes RED.
**What+How:** Add `test_bake_ref_parity` to `tests/drift-gate-negatives.sh` (inject/restore pattern). Pick a baker under `automation/` carrying a `${MIOS_BUILD_BAKE_REFS_<KEY>:-<lit>}` default (e.g. `55-bake-quickshell.sh` or `56-bake-surfer.sh` -- confirm the live set with `git grep MIOS_BUILD_BAKE_REFS_`), snapshot it, perturb the `:-` literal to a wrong value, assert `check_bake_ref_defaults`/`check_bake_ref_parity` exits NON-zero, restore byte-for-byte, assert green. Register in the harness `main()`.
**Where:** `tests/drift-gate-negatives.sh`, `automation/55-bake-quickshell.sh`, `automation/56-bake-surfer.sh`
**Done When:** `bash -n tests/drift-gate-negatives.sh` is clean; the harness passes with the new case; the perturbed baker is restored before assert-green and before any `die`.
**Why:** Without it, AGY-326's parity gate is an assertion nobody has ever seen fail -- a regex typo would make it pass unconditionally and re-open the drifted-baker-default hole it was written to close.
**Dep:** AGY-326 (the gate under test)
**Who:** you (bash).

## AGY-328 -- Gate-registry self-consistency: no duplicate, unwired or non-standard `check_*`  (WS-DRIFT | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the gate file audits ITSELF so a check cannot be silently shadowed, orphaned, or reported through a non-canonical path.
**What+How:** `automation/38-drift-checks.sh` defines `check_greenboot()` TWICE (`:3120` and `:3981`); the second shadows the first and `main()` calls it once, so one gate body has never executed. Add `check_gate_registry()` that scans `$_self` and asserts: (1) no `check_*` name is defined more than once (`grep -oE '^check_[a-z_]+' "$_self" | sort | uniq -d` must be empty); (2) the defined-set equals the set invoked exactly once inside `main()`; (3) every gate reports through the canonical `_violation` helper -- flag `_fail`-only bodies (currently `check_greenboot` and `check_clevis_luks`). `_violation` per finding. Then FIX the shadowing by merging/removing the redundant `check_greenboot` so the new gate is green.
**Where:** `automation/38-drift-checks.sh`
**Done When:** `bash -n automation/38-drift-checks.sh` is clean; exactly one `check_greenboot` definition remains and is still wired in `main()`; `bash automation/38-drift-checks.sh check_gate_registry` is green at HEAD; the new function is registered in `main()`; `just drift-gate` green.
**Why:** One greenboot gate body has silently never run, and nothing in the tree detects that class. Every future check added but forgotten in `main()` is dead enforcement that reads as coverage -- the mechanical conscience quietly loses checks.
**Dep:** none
**Who:** you (bash + drift-gate).

## AGY-329 -- Prove the gate-registry check can fail, without mutating the live gate  (WS-DRIFT | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- the meta-gate itself gets a failure proof.
**What+How:** Add `test_gate_registry` to `tests/drift-gate-negatives.sh`, operating on a COPY of `automation/38-drift-checks.sh` (run the check against the copy via `MIOS_DRIFT_CHECK_ROOT` or an explicit copy path -- never mutate the gate under test). Inject a duplicate `check_dead_lane()` stub or an unwired `check_bogus()` definition into the copy, run `check_gate_registry` against it, assert NON-zero exit, delete the copy, then assert the pristine script is green. Register in the harness `main()`.
**Where:** `tests/drift-gate-negatives.sh`, `automation/38-drift-checks.sh`
**Done When:** `bash -n tests/drift-gate-negatives.sh` is clean; the harness passes with the new case; `git status` shows no mutation of the live `automation/38-drift-checks.sh` (only a temp copy was perturbed and removed).
**Why:** A meta-gate that has never gone red is decoration; and the naive way to test it -- editing the live gate -- risks leaving a corrupted `38-drift-checks.sh` in a shared tree if the harness dies mid-case.
**Dep:** AGY-328 (the check under test)
**Who:** you (bash).

## AGY-330 -- Flip `oscontrol.py`'s `[os_control]` read onto the DB config resolver behind the db_authoritative sentinel  (WS-DB | P1 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- kill the write-only `config_kv` copy by giving each seeded scope a real READ path.
**What+How:** `usr/lib/mios/agent-pipe/mios_pipe/routing/oscontrol.py:158-160` reads SSOT directly (`import mios_toml; merged = mios_toml.load_merged(); sec = mios_toml.section(merged, "os_control")`), so the seeded `config_kv` copy of `[os_control]` is pure write-only drift. Migrate to the DB peer `usr/lib/mios/mios_db_config.py` exactly as `mios_pipe/kernel/config.py:23-32` already does (`import mios_db_config; mios_db_config.section(None, "os_control")`). `mios_db_config.section` is fail-open (returns TOML on an empty DB) and, while `is_db_authoritative()` is false, shadow-compares and calls `record_divergence` without altering the returned value -- so behaviour is unchanged until `ai.db_authoritative`/`MIOS_DB_AUTHORITATIVE` flips. Keep the import local to the function and preserve the exact return shape oscontrol consumes.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/oscontrol.py`, `usr/lib/mios/mios_db_config.py`
**Done When:** `python -m py_compile usr/lib/mios/agent-pipe/mios_pipe/routing/oscontrol.py` passes; with `MIOS_DB_AUTHORITATIVE` unset the resolved `os_control` section is byte-identical to the pre-change `mios_toml.section` result, asserted in `usr/lib/mios/agent-pipe/test_mios_db_config.py`; `just drift-gate` green.
**Why:** Today an operator can edit `[os_control]` in the DB and nothing reads it -- the V1 gap named in `everything-db-driven.md`. The DB copy diverges from TOML with no consumer and no divergence record, so the eventual authority flip would change behaviour in unpredictable ways.
**Dep:** none
**Who:** you (Python, agent-pipe internals).

## AGY-331 -- Flip `routing.py`'s two `[routing]` reads onto `mios_db_config`  (WS-DB | P1 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the routing domain map becomes a DB-served projection instead of a write-only seeded table.
**What+How:** `usr/lib/mios/agent-pipe/mios_pipe/routing/routing.py` reads routing SSOT directly at TWO sites (`:84-86`, `:132-134`, each `import mios_toml; merged = mios_toml.load_merged(); rt = mios_toml.section(merged, "routing")`). Replace both with `mios_db_config.section(None, "routing")` (same signature, peer resolver in `usr/lib/mios/mios_db_config.py`), so the `routing.domains` rows already projected into `domain_verb` and reconstructed by `load_db_config()` (`mios_db_config.py:105-125`) become the runtime source under authority, with TOML fail-open plus shadow-compare otherwise. Do NOT touch the verb-catalog overlay in `verbcatalog.py` (already flipped). Confirm the reconstructed `routing.domains` shape `{domain: {"desc":..., "verbs":[...]}}` matches what routing.py consumes downstream.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/routing.py`, `usr/lib/mios/mios_db_config.py`
**Done When:** `python -m py_compile` on the file passes; an assertion extended into `test_mios_db_config.py` shows both call sites return the same `routing` section as `mios_toml.section` with authority off; `just drift-gate` green (agent-pipe unit tests plus drift-checks).
**Why:** Routing is the highest-traffic config surface in the agent plane; leaving it TOML-only means the `domain_verb` projection is unverified data nobody reads, and the DB can never become authoritative without a blind cutover on the hottest path.
**Dep:** none (parallel-safe with AGY-330)
**Who:** you (Python, agent-pipe routing).

## AGY-332 -- Flip `quality_gate.py`'s `agent_pipe.quality` read onto `mios_db_config`  (WS-DB | P2 | S) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- nested sub-tables, not just flat scopes, resolve through the DB peer.
**What+How:** `usr/lib/mios/agent-pipe/mios_pipe/routing/quality_gate.py:44-45` reads thresholds via `import mios_toml; sec = mios_toml.get("agent_pipe.quality", {})`. Route this through `usr/lib/mios/mios_db_config.py` preserving EXACT call semantics: `[agent_pipe.quality]` seeds into `config_kv` as scope=`agent_pipe`, key=`quality` (a jsonb dict) per `seed-db-config.py:84-101`, and `load_db_config` rebuilds it as a nested dict -- so add or confirm a nested-subtable `get`/`section` path in the peer that returns the same shape `mios_toml` returns today, and add it to `mios_db_config.py` rather than open-coding it in quality_gate. Fail-open plus shadow-compare identical to AGY-330/331.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/quality_gate.py`, `usr/lib/mios/mios_db_config.py`
**Done When:** `python -m py_compile` on both files passes; an assertion in `test_mios_db_config.py` proves the `agent_pipe.quality` dict is identical DB-vs-TOML with authority off; `just drift-gate` green.
**Why:** Without a nested getter in the peer, every future `[a.b]` sub-table flip open-codes its own reassembly -- a second resolver by another name, exactly the duplication the SSOT law forbids.
**Dep:** AGY-330, AGY-331 (same fail-open/shadow-compare pattern)
**Who:** you (Python, agent-pipe routing).

## AGY-333 -- `config_kv` seed-coverage gate: no DB-flipped read-path may shadow-compare an unseeded scope  (WS-DB | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the seeder's coverage is mechanically proven to be a superset of what the resolvers read.
**What+How:** The V1 flips (AGY-330..332) depend on `mios_db_config.load_db_config()` finding rows for every `[section]` a resolver serves. `seed-db-config.py:84-101` seeds `kv_sections = [k for k in data.keys() if k not in ("verbs","packages")]` into `config_kv` layer 0 and special-cases `routing.domains`. Add an offline drift-check (next free number in the 40+ family, after Law checks 35-41 and V1 checks 31-34) that parses `usr/share/mios/mios.toml`, enumerates every top-level table, subtracts the allow-list (`verbs`, `packages`, plus the `routing.domains`/`nohc_allowlist` special-cases already skipped at `seed-db-config.py:59,91`), and asserts every remaining scope is reachable by the `kv_sections` loop. Reuse the mock-cursor style of `check_drift_projection` (`automation/38-drift-checks.sh:1998+`) -- no live DB. Register in the runner list near `check_drift_projection` at `:4252`. Stage ONLY the explicit files touched (shared tree, never `git add -A`).
**Where:** `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`, `usr/share/mios/postgres/seed-db-config.py`, `usr/share/mios/mios.toml`
**Done When:** `bash -n automation/38-drift-checks.sh` passes; the new check is green at HEAD; a negative test that removes a scope from the seeder's coverage flips it red then restores green; `just drift-gate` green.
**Why:** A flipped read-path over an unseeded scope shadow-compares against nothing, logs a phantom divergence on every request, and degrades closed to an empty section the moment authority is enabled.
**Dep:** AGY-330, AGY-331, AGY-332
**Who:** you (bash + Python, drift-gate).

## AGY-334 -- `mios-userdb-render`: project the `account` table into systemd userdb JSON drop-ins  (WS-DB | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- Postgres becomes the account SSOT on Linux, projected to the OS rather than hand-maintained in `/etc`.
**What+How:** Implement §3(a) of `docs/agy/doc-postgresos-accounts.md` as a NEW `usr/libexec/mios/mios-userdb-render` (Python, same `get_pg_config()` + psycopg + `MIOS_TOML` gate style as `usr/libexec/mios/materialize-user-config.py`). SELECT enabled Linux-scoped rows from `account` (`WHERE enabled AND os_targets IN ('both','linux')` -- the same predicate as `mios-account-sync`'s `query_db_accounts`) and write one JSON User Record per user to `/run/userdb/<name>.user` (runtime tmpfs, since the DB is the runtime SSOT and `/usr` is immutable), following systemd.io/USER_RECORD: `userName`, `uid`, `gid`, `realName`<-display, `homeDirectory`<-home_dir, `shell`, `disposition:"regular"`; the hashed secret goes in a `.user-privileged` companion at mode 0600, NEVER plaintext. Idempotent via compare-before-write, mirroring `materialize-user-config.py:160-176`. Gate the whole run on `MIOS_ACCOUNTS_DB_BACKED` like `automation/17-accounts-db.sh`, and fail-open (log, exit 0) when psycopg or the DB is absent so offline boot is unaffected. This is a PROJECTION only -- Postgres stays out of the login critical path per the doc's libnss-pgsql caution.
**Where:** `usr/libexec/mios/mios-userdb-render`, `docs/agy/doc-postgresos-accounts.md`, `usr/libexec/mios/materialize-user-config.py`
**Done When:** `python -m py_compile usr/libexec/mios/mios-userdb-render` passes; a mocked-cursor dry-run emits a User Record that `python -c "import json;json.load(...)"` accepts with `userName`/`uid`/`homeDirectory` matching the fixture rows; a second run on unchanged input writes nothing.
**Why:** `[accounts].db_backed` ships inert today -- accounts exist in Postgres but the Linux side has no way to see them, so the DB-driven account model is a schema with no consumer.
**Dep:** none
**Who:** you (Python, systemd userdb / NSS).

## AGY-335 -- Wire `mios-userdb-render` into a NOTIFY-driven unit under the db_backed gate  (WS-DB | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the account projection re-renders on change instead of only at bake time.
**What+How:** Add `usr/lib/systemd/system/mios-userdb-render.service` (Type=oneshot, `After=`/`Requires=mios-pgvector.service`, `ExecStartPre` pg_isready -- copy the shape of `usr/lib/systemd/system/mios-account-sync.service`), plus a `.path`/timer or a small LISTEN wrapper on the `account_sync` channel the schema already emits (`notify_account_changes` trigger, `schema-init.sql:1088-1110`), so drop-ins re-render on account changes. Extend `automation/17-accounts-db.sh` to `install -m 0755` the renderer and `install -m 0644` the unit, and `systemctl enable`/`disable` it under the SAME `MIOS_ACCOUNTS_DB_BACKED` gate the script already applies to `mios-account-sync.service` (`17-accounts-db.sh:37-46`). Note `nss-systemd` presence in the userdb NSS path in the script's log line -- do not hand-edit nsswitch beyond the existing revert logic. Stage ONLY the explicit new/edited paths.
**Where:** `usr/lib/systemd/system/mios-userdb-render.service`, `automation/17-accounts-db.sh`, `usr/share/mios/postgres/schema-init.sql`
**Done When:** `bash -n automation/17-accounts-db.sh` passes; `systemd-analyze verify usr/lib/systemd/system/mios-userdb-render.service` reports no errors; the automation gate enables the unit only when db_backed is truthy; `just drift-gate` green.
**Why:** A renderer with no unit runs never. Without the NOTIFY hookup, an account added to Postgres does not appear on the host until the next full rebuild -- which is not a usable account system.
**Dep:** AGY-334
**Who:** you (bash + systemd).

## AGY-336 -- Land `[accounts.lldap]` SSOT knobs + userenv alias derivation for the cross-platform LDAP face  (WS-DB | P3 | S) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the cross-platform identity face gets an operator-tunable SSOT surface before any container exists to consume it.
**What+How:** Per §3(a)/§2 of `docs/agy/doc-postgresos-accounts.md`, lldap-over-Postgres is the adopt-now cross-platform account SSOT face. Add an `[accounts.lldap]` sub-table to `usr/share/mios/mios.toml` beside the existing `[accounts]` block (`:92`): `enable=false`, `base_dn=""`, `db_schema="lldap"` (a separate schema in the SAME `mios` Postgres so Postgres stays the single SSOT), `ldap_port`, `http_port` -- all resolved-at-build/float, with NO hand-pinned image tag (SBOM law, ADR-0003). Extend the alias-derivation switch in BOTH `usr/lib/mios/userenv.sh` and `tools/lib/userenv.sh` (they must stay byte-identical per check 27) to emit `MIOS_ACCOUNTS_LLDAP_ENABLE`/`_BASE_DN`/`_DB_SCHEMA`/`_LDAP_PORT`/`_HTTP_PORT`, following the exact pattern at `userenv.sh:198-199` for `accounts.db_backed`. Do NOT deploy a container here -- that is AGY-337's projector plus a later Quadlet task.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/mios/userenv.sh`, `tools/lib/userenv.sh`
**Done When:** the TOML parses (`python -c "import tomllib,pathlib;tomllib.loads(pathlib.Path('usr/share/mios/mios.toml').read_text())"`); `bash -n` passes on both userenv.sh copies; the two userenv.sh files remain byte-identical (drift check 27); `just drift-gate` green.
**Why:** Without keys to float TO, any lldap work would arrive as hardcoded ports and DNs in a unit file -- a Law 7 violation created by the very task meant to unify identity.
**Dep:** none
**Who:** you (Python + TOML SSOT).

## AGY-337 -- `mios-lldap-seed`: project `account` rows into an lldap bootstrap so Postgres stays SSOT  (WS-DB | P3 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the LDAP wire protocol is a projection of the one datastore, never a second hand-maintained account store.
**What+How:** Add `usr/libexec/mios/mios-lldap-seed` (psycopg + `get_pg_config()` + the gate style from `materialize-user-config.py`) that reads enabled rows from `account` joined with `mios_identity.canonical_users`/`aliases` exactly as `mios-account-sync`'s `query_db_accounts` does (`mios-account-sync:63-94`) and emits lldap's bootstrap format -- its `bootstrap.json` user/group objects, or LDIF add records. Gate on `MIOS_ACCOUNTS_LLDAP_ENABLE` (from AGY-336) AND `MIOS_ACCOUNTS_DB_BACKED`; fail-open when disabled or the DB is absent. Output must be idempotent (stable ordering, compare-before-write). No secrets in plaintext -- reference `password_hash`/`password_hash_sha512` only.
**Where:** `usr/libexec/mios/mios-lldap-seed`, `usr/libexec/mios/mios-account-sync`, `usr/libexec/mios/materialize-user-config.py`
**Done When:** `python -m py_compile usr/libexec/mios/mios-lldap-seed` passes; a mocked-cursor dry-run produces bootstrap output that `json.load` (or an LDIF sanity parse) accepts, with users and groups matching the fixture rows; a disabled-gate run is a clean exit-0 no-op.
**Why:** The alternative is an lldap instance operators edit directly -- instantly a second source of truth for identity that diverges from Postgres with no gate able to see it.
**Dep:** AGY-336 (the `MIOS_ACCOUNTS_LLDAP_*` keys)
**Who:** you (Python, LDAP/lldap).

## AGY-338 -- Make `mios-account-sync` event-driven: LISTEN on `account_sync`, keep the poll as fallback  (WS-DB | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- account changes converge on the host immediately, using the NOTIFY plumbing the schema already ships.
**What+How:** The schema already fires `pg_notify('account_sync', ...)` from triggers on `account` and `mios_identity.aliases` (`schema-init.sql:1088-1130`), but the daemon only POLLS every 15s (`mios-account-sync:328-342`). Upgrade the `--daemon` branch of `main()` to open a dedicated psycopg connection, `LISTEN account_sync`, and block on `conn.notifies()` with a 15s timeout that still triggers a full `sync_accounts()` pass as the safety fallback, so a missed NOTIFY or a fresh boot still converges. The LISTEN connection is ONLY the wake signal -- keep the existing `mios-pg-query`-subprocess path for the sync work itself. Preserve the state-file write-back logic (`:325-326`) and keep the `psycopg` import optional/guarded (log and fall back to pure polling if the direct connection cannot be built).
**Where:** `usr/libexec/mios/mios-account-sync`, `usr/share/mios/postgres/schema-init.sql`
**Done When:** `python -m py_compile usr/libexec/mios/mios-account-sync` passes; a unit-level shim test with a stubbed connection shows the poll-only fallback still runs when `LISTEN` setup raises; the non-daemon single-pass branch is reachable and unchanged.
**Why:** A 15s poll against Postgres runs 5,760 wasted round-trips a day and still leaves up to 15s of lag on a password or group change, while the NOTIFY triggers that would make it instant already exist and fire into nothing.
**Dep:** none
**Who:** you (Python, Postgres LISTEN/NOTIFY).

## AGY-339 -- Generate the Windows `New-LocalUser` account manifest from the `account` table  (WS-DB | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the Windows account set becomes a projection of Postgres rather than hand-authored unattend XML.
**What+How:** Per §3(b)/§4 of `docs/agy/doc-postgresos-accounts.md`, Windows has no NSS/PAM, so the honest baseline is a generated manifest a SetupComplete step consumes. Add `usr/libexec/mios/mios-winaccounts-render` (psycopg, gated on `MIOS_ACCOUNTS_DB_BACKED`) that SELECTs `WHERE enabled AND os_targets IN ('both','windows')` from `account` and writes a deterministic JSON manifest `{name, display, groups[], is_admin, password_hash?}` to a stable path (e.g. `/var/lib/mios/win-accounts.json`) which the Windows plane reads to drive `New-LocalUser`/`Add-LocalGroupMember`. This task stays inside `C:\MiOS` and emits ONLY the manifest -- do NOT edit `C:\mios-bootstrap` (autounattend/SetupComplete) or the Portal; the manifest IS the contract the other plane reads. Passwords hash-only, never plaintext.
**Where:** `usr/libexec/mios/mios-winaccounts-render`, `docs/agy/doc-postgresos-accounts.md`
**Done When:** `python -m py_compile usr/libexec/mios/mios-winaccounts-render` passes; a mocked-cursor dry-run emits a `json.load`-valid manifest whose fields, `is_admin` and `groups` match the fixture rows, with stable key ordering across two runs; a disabled-gate run exits 0 writing nothing.
**Why:** Windows accounts are currently hand-written into autounattend XML, so the two platforms' user sets drift the moment either side changes -- and there is no artifact a gate could compare them against.
**Dep:** none
**Who:** you (Python + PowerShell contract).

## AGY-340 -- Allocate ids from the DB-owned `uid_alloc` sequence instead of the hardcoded 1000 gid fallback  (WS-DB | P3 | S) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- ids are assigned ONCE in Postgres so every account projection reads the same number.
**What+How:** `schema-init.sql:795-807` defines `SEQUENCE uid_alloc` plus `allocate_uid()`/`allocate_gid()` precisely so ids are DB-owned, yet `mios-account-sync:180` still falls back to a hardcoded `gid = db_user["gid"] or 1000` which `useradd` at `:214-228` inherits -- silently colliding with the allocator. When a row has NULL uid/gid, allocate through the DB (`SELECT allocate_gid()` / `allocate_uid()` via `mios-pg-query`) and persist the result back onto the `account` row with an UPDATE, so Linux `useradd`, the AGY-334 userdb records and the AGY-339 Windows manifest all read the identical id. Keep the change additive and idempotent (allocate only when the column is NULL). Also drop the literal `1000` default from `query_db_accounts` (`:180`) -- an unresolved id is an error to log, not a magic default (Law 7 spirit).
**Where:** `usr/libexec/mios/mios-account-sync`, `usr/share/mios/postgres/schema-init.sql`
**Done When:** `python -m py_compile usr/libexec/mios/mios-account-sync` passes; a stubbed-`mios-pg-query` unit test shows a NULL-gid row triggers exactly one `allocate_gid()` plus one write-back UPDATE and reuses the stored id on the next pass; no `1000` literal remains in the id path; `just drift-gate` green.
**Why:** Every account with a NULL gid currently lands on group 1000 -- so two different users collide on one group, and the three projections can each invent a different id for the same person.
**Dep:** AGY-334, AGY-339 (the projections that must agree on the id)
**Who:** you (Python + SQL).

## AGY-341 -- Extend the DB->TOML round-trip gate to the newly authoritative scopes (os_control, routing, agent_pipe)  (WS-DB | P1 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- losslessness is proven for exactly those scopes that just became load-bearing read paths.
**What+How:** `check_drift_projection` (`automation/38-drift-checks.sh:1998-2321`) already proves `seed-db-config.py` -> `materialize-config-toml.py` round-trips config_kv and verbs, but AGY-330..332 promote `[os_control]`, `[routing]` and `[agent_pipe]` (including the nested `[agent_pipe.quality]`) to authoritative reads. Extend the mock-cursor round-trip in `check_drift_projection` (or add a sibling with the next free number) to assert those three scopes seed and materialize back byte-identical, exercising in particular the nested sub-table handling in `materialize-config-toml.py:112-121` (dict values -> `[scope.key]`) against `seed-db-config.py:84-101`.
**Where:** `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`, `usr/share/mios/postgres/seed-db-config.py`, `usr/share/mios/postgres/materialize-config-toml.py`
**Done When:** `bash -n automation/38-drift-checks.sh` passes; the extended check is green at HEAD; a negative test that perturbs a value in one of the three scopes (seed vs materialize mismatch) flips it red then restores green; `just drift-gate` green.
**Why:** The flips make the DB the source for routing and os_control; if the seed/materialize pair is lossy for those scopes, the authority flip silently drops config the runtime depends on, with the TOML still looking correct on disk.
**Dep:** AGY-330, AGY-331, AGY-332
**Who:** you (bash + Python, drift-gate).

## AGY-342 -- Verb-catalog lossless round-trip gate across all 15 columns (params/examples/aliases included)  (WS-DB | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- the verb catalog survives a full DB round-trip with no field quietly dropped.
**What+How:** `materialize-config-toml.py:162-219` OMITS verb fields equal to `[verbs._defaults]` (`tier`/`permission`/`hidden`/`parallel_limit`/`max_result_chars` print only when they differ, `:194-211`) and renders `params`/`examples`/`aliases` conditionally -- a lossy inverse of `seed-db-config.py:103-163` if a default ever changes or a non-default field is dropped. Add an offline drift-check in the mock-cursor style of `check_drift_projection` that feeds a representative `[verbs.*]` carrying a `params` sub-table, `examples`, `hidden_aliases`, a non-default `tier`/`permission` and a `conflict_group`, runs seed -> materialize, re-parses, and asserts EVERY column survives with defaults correctly re-applied on the return leg. Register it in the runner list.
**Where:** `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`, `usr/share/mios/postgres/materialize-config-toml.py`, `usr/share/mios/postgres/seed-db-config.py`
**Done When:** `bash -n automation/38-drift-checks.sh` passes; the check is green at HEAD; a negative test that drops the `examples` render from the round-trip flips it red then restores green; `just drift-gate` green.
**Why:** Change one value in `[verbs._defaults]` and the default-elision logic starts writing back verbs with the WRONG tier or permission -- a permission downgrade on the verb library that no current gate can see.
**Dep:** AGY-341 (same round-trip harness)
**Who:** you (bash + Python, drift-gate).

## AGY-343 -- Schema<->consumer column-parity gate for the three account projections  (WS-DB | P2 | M) **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- a schema rename cannot half-break one account plane while the others keep working.
**What+How:** Three projectors now read the SAME `account` columns: `mios-account-sync`'s `query_db_accounts` SELECT (`mios-account-sync:63-94`), the AGY-334 userdb renderer, and the AGY-339 Windows manifest renderer. Add a drift-check that parses `CREATE TABLE account` plus its `ALTER TABLE account ADD COLUMN` set in `usr/share/mios/postgres/schema-init.sql:376-399,792-793` into the authoritative column set, then greps each consumer's SELECT/field-map for the columns it depends on (`name,password_hash,uid,gid,display,home_dir,shell,groups,is_admin,enabled,os_targets`) and asserts schema-is-superset parity. Pure static analysis, regex-based like the other `38-drift-checks` fitness functions -- no live DB. Register in the runner list; stage ONLY explicit paths.
**Where:** `automation/38-drift-checks.sh`, `tests/drift-gate-negatives.sh`, `usr/share/mios/postgres/schema-init.sql`, `usr/libexec/mios/mios-account-sync`, `usr/libexec/mios/mios-userdb-render`, `usr/libexec/mios/mios-winaccounts-render`
**Done When:** `bash -n automation/38-drift-checks.sh` passes; the parity check is green at HEAD; a negative test that references a non-existent `account` column in one consumer flips it red then restores green; `just drift-gate` green.
**Why:** Drop or rename a column and only one of three projections raises -- the other two silently emit accounts missing a field, so Linux logins keep working while Windows or userdb accounts come out wrong at the next boot.
**Dep:** AGY-334, AGY-339
**Who:** you (bash + Python, drift-gate).

## AGY-344 -- Extract the legacy DB writer/mirror seam out of `server.py` into `mios_pipe/memory/dbwrite.py`  (WS-PIPE | P1 | L) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- decompose the ~9k-line `server.py` god-module into importable, testable seams under the configure()-DI contract.
**What+How:** `server.py` still carries the whole legacy DB write path as free functions: `_get_client` L2080, `_db_client` L2098, `_db_post` L2107, `_db_read` L2145, `_db_update` L2162, `_db_create` L2190, `_db_fire` L2263, `_pg_mirror` L2299, `_db_write` L2318. Move the bodies VERBATIM into a new `mios_pipe/memory/dbwrite.py` using the contract documented in `mios_dispatch.py`'s header: one-way `configure()` dependency injection, the module NEVER imports `server`, and every server-owned scalar it reads (DB endpoint/backend consts, `mios_audit`, the pooled-client globals, the `_DB_BACKOFF` state) is injected via `configure(**kwargs)`; `server.py` re-imports each moved name verbatim under its original alias for surface parity. Keep the module-level `_client`/`_DB_CLIENT` globals inside the new module. Do NOT alter SQL text or the degrade-open/backoff semantics (the 30s post-failure backoff in `_db_post`).
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/memory/dbwrite.py` (new), `usr/lib/mios/agent-pipe/server.py`, `usr/lib/mios/agent-pipe/test_mios_dbwrite.py` (new)
**Done When:** `python3 -c "import mios_pipe.memory.dbwrite"` and `python3 -c "import server"` both import clean; `grep -n "def _db_write\|def _pg_mirror" server.py` is empty while `grep -n "_db_write\|_pg_mirror" server.py` still shows the re-import (parity); `python3 -m py_compile server.py mios_pipe/memory/dbwrite.py` passes; the new pytest covers `_db_create` SQL shape and `_db_post` returning None with backoff on error; `just drift-gate` green (the agent-pipe `test_mios_*` glob picks up the new test). Stage ONLY these explicit paths.
**Why:** Every later seam (session events, VRAM active-marks) needs these helpers; while they live in `server.py` no extracted module can import them without a circular dependency, so the whole decomposition wave is blocked behind this one file.
**Dep:** none -- land first; later seams import this module
**Who:** you (Python; async httpx + the R7/R13 configure()-DI extraction pattern). | **When:** first of the decomposition wave -- many later seams (session events, VRAM active-marks) call these helpers, so land the module they can import.

## AGY-345 -- Extract the admission-control / SLO / lane-semaphore seam into `mios_pipe/scheduler/admission.py`  (WS-PIPE | P1 | L) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- queue admission becomes a self-contained, unit-testable module instead of inline `server.py` state.
**What+How:** The admission machinery is one coherent concern still inline: `_parse_lane_caps` (L643), `_priority_gate` (L928), `_SloShed` (L1233), `_parse_lane_priority` (L1267), `_lane_sem` (L1304), `_endpoint_key` (L1326), `_endpoint_sem` (L1335), `_admit` (L1347). Move the bodies VERBATIM into `mios_pipe/scheduler/admission.py` under the `mios_dispatch.py` extraction contract: one-way `configure()` DI, the module never imports `server`, the per-lane/per-endpoint semaphore-cache dicts stay module-resident, and the host-stat/ceiling helpers it calls (`_host_stats_cached`, `_over_global_ceiling`) are dependency-INJECTED rather than imported since AGY-346 owns them. Preserve `_SloShed` as a public re-export so existing `except _SloShed` sites in `server.py` keep matching the SAME class object.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/scheduler/admission.py` (new), `usr/lib/mios/agent-pipe/server.py`, `usr/lib/mios/agent-pipe/test_mios_admission.py` (new)
**Done When:** `python3 -m py_compile server.py mios_pipe/scheduler/admission.py` passes; `import server` is clean; a test asserts `mios_pipe.scheduler.admission._SloShed is server._SloShed`; `_lane_sem("x") is _lane_sem("x")` (cache identity) holds under test; `grep -n "def _admit(" server.py` is empty with the re-import line present; `just drift-gate` green. Stage ONLY explicit paths.
**Why:** Admission control is the guardrail that stops the agent plane starving its host, and it is currently untestable in isolation -- a duplicated `_SloShed` class or a lost semaphore cache would let retry storms fan out unbounded with no test able to catch it.
**Dep:** none (disjoint from AGY-344, parallel-safe)
**Who:** you (Python asyncio; semaphores + the configure()-DI pattern). | **When:** independent of AGY-344; parallel-safe (disjoint functions).

## AGY-346 -- Extract the VRAM / model-residency manager into `mios_pipe/scheduler/vram.py`  (WS-PIPE | P2 | L) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the "what is warm / evict to fit / mark active" block leaves `server.py` as an injectable module.
**What+How:** Move VERBATIM into `mios_pipe/scheduler/vram.py`: `_checkpoint_keep_models` (L2411), `_engine_resident` (L2421), `_norm_model_tag` (L2432), `_host_stats_cached` (L2449), `_resident_cached` (L2462), `_over_global_ceiling` (L2476), `_blade_vram_budget` (L2504), `_over_blade_ceiling` (L2518), `_is_warm` (L2539), `_engine_unload` (L2552), `_vram_checkpoint` (L2559), `_model_active` (L2587), `_model_is_active` (L2608), `_reclaim_idle_vram` (L2629). Use the `mios_dispatch.py` configure()-DI contract: the active-model refcount dict and the resident-cache TTL state stay module-resident; the DB helpers from AGY-344 and the engine-topology consts are injected. Expose `_host_stats_cached` and `_over_global_ceiling` publicly -- this module is the injection source AGY-345 consumes.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/scheduler/vram.py` (new), `usr/lib/mios/agent-pipe/server.py`, `usr/lib/mios/agent-pipe/test_mios_vram.py` (new)
**Done When:** `python3 -m py_compile server.py mios_pipe/scheduler/vram.py` passes; `import server` is clean; a test asserts the `_model_active`/`_model_is_active` refcount round-trip and `_norm_model_tag` normalization; `grep -n "def _vram_checkpoint(" server.py` is empty with the re-import present; `just drift-gate` green. Stage ONLY explicit paths.
**Why:** VRAM residency decides which model gets evicted mid-request; buried in `server.py` its refcount and TTL caches have no test, so an off-by-one in the active-marks silently pins a whale model in VRAM and starves every other lane.
**Dep:** AGY-344 (DB helpers injected); pairs with AGY-345 (which consumes this module's helpers)
**Who:** you (Python asyncio). | **When:** pairs with AGY-345 (admission injects these); land after or alongside.

## AGY-347 -- Extract the inbound/outbound authentication seam into `mios_pipe/access/authn.py`  (WS-PIPE | P1 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the security-sensitive auth concern is isolated into one reviewable module instead of five scattered sites.
**What+How:** Auth is currently spread across `server.py`: `_apply_outbound_auth` (L281), `_load_backend_key` (L1510), `_load_caller_keys` (L1555), `_check_inbound_principal` (L1576), `_probe_auth_headers` (L1606), and the `_inbound_auth_mw` middleware (L8586). Move the pure helpers VERBATIM into `mios_pipe/access/authn.py` under the `mios_dispatch.py` configure()-DI contract (module never imports `server`; the `_API_REQUIRE_AUTH`/`_AUTH_OPEN_PATHS`/`_AUTH_HOSTPORTS` consts and the caller-key cache are injected). Register the middleware by having `server.py` call `authn.inbound_auth_mw` -- a thin wrapper delegating to the moved logic -- so `@app.middleware("http")` ordering is unchanged. Preserve the DEFAULT-OFF pass-through (`if not _API_REQUIRE_AUTH: return await call_next(request)`) byte-for-byte; do not change gating behaviour.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/access/authn.py` (new), `usr/lib/mios/agent-pipe/server.py`, `usr/lib/mios/agent-pipe/test_mios_authn.py` (new)
**Done When:** `python3 -m py_compile server.py mios_pipe/access/authn.py` passes; `import server` is clean; tests assert `_check_inbound_principal` accepts a valid caller key and rejects an unknown one, and that middleware pass-through holds when `_API_REQUIRE_AUTH` is False; middleware registration order is unchanged (usage MW then auth MW -- auth outermost); `just drift-gate` green. Stage ONLY explicit paths.
**Why:** The only front door to the AI plane authenticates through logic scattered over 8k lines with no tests -- a reordered middleware or a mis-scoped open-path list opens `/v1/*` to anyone on the network, and today nothing would catch it.
**Dep:** none (independent seam)
**Who:** you (Python; HTTP auth + middleware; security-sensitive). | **When:** independent; high value (security concern isolation).

## AGY-348 -- Extract the scratchpad blackboard into `mios_pipe/context/scratchpad.py`  (WS-PIPE | P2 | S) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- a clean, low-risk first cut that validates the extraction contract end to end.
**What+How:** The per-turn agent scratchpad is a self-contained 5-function unit in `server.py`: `_scratchpad_key` (L4431), `_scratchpad_for` (L4440), `_scratchpad_rehydrate` (L4457), `_scratchpad_note` (L4494), `_scratchpad_render` (L4519). Move VERBATIM into `mios_pipe/context/scratchpad.py` via the `mios_dispatch.py` configure()-DI contract: the module-level `collections.deque` store and its bound stay module-resident, and the DB read helper from AGY-344 is injected. Keep the deque `maxlen` and the key derivation exactly as-is.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/context/scratchpad.py` (new), `usr/lib/mios/agent-pipe/server.py`, `usr/lib/mios/agent-pipe/test_mios_scratchpad.py` (new)
**Done When:** `python3 -m py_compile server.py mios_pipe/context/scratchpad.py` passes; `import server` is clean; a test drives `_scratchpad_note` then `_scratchpad_render` and asserts the note appears and the deque bound is enforced; `grep -n "def _scratchpad_render(" server.py` is empty with the re-import present; `just drift-gate` green. Stage ONLY explicit paths.
**Why:** An unbounded or mis-keyed scratchpad leaks one session's notes into another's prompt, and while it sits inline there is no test asserting the deque bound holds.
**Dep:** AGY-344 (injected DB read helper)
**Who:** you (Python). | **When:** independent, small, low-risk -- good early win.

## AGY-349 -- Extract the worker-tool surface + child-tool selection into `mios_pipe/routing/toolsurface.py`  (WS-PIPE | P2 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- tool-surface assembly joins its existing routing siblings as a testable module.
**What+How:** Move VERBATIM into `mios_pipe/routing/toolsurface.py`: `_worker_tools_surface` (L3927), `_worker_tools_surface_async` (L3961), `_select_child_tools` (L4059), `_tool_pref_block` (L4201). Apply the `mios_dispatch.py` configure()-DI contract -- the verb catalog, `_worker_tools_surface`'s cap/intent params, and any embed/rank helpers are injected, NOT imported. The module sits beside the existing `mios_pipe/routing/toolsearch.py` and `toolexec.py`, so reuse their public entry points where the moved code already calls them, importing siblings directly per the dispatch contract's "sibling modules imported directly" carve-out.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/toolsurface.py` (new), `usr/lib/mios/agent-pipe/server.py`, `usr/lib/mios/agent-pipe/test_mios_toolsurface.py` (new)
**Done When:** `python3 -m py_compile server.py mios_pipe/routing/toolsurface.py` passes; `import server` is clean; tests assert `_worker_tools_surface()` returns the expected shape for a stub catalog and `_select_child_tools` caps at `k`; `just drift-gate` green. Stage ONLY explicit paths.
**Why:** The tool surface decides what every agent can see and do; an uncapped `_select_child_tools` blows the context budget on every turn, and inline in `server.py` there is no test pinning the cap or the surface shape.
**Dep:** none (disjoint from the other extractions)
**Who:** you (Python asyncio). | **When:** independent of the other extractions (disjoint functions).

## AGY-350 -- Gate the agent-pipe extraction seams: modules never import `server`, `server.py` re-imports every moved symbol (WS-PIPE | P1 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- makes the server.py god-module decomposition irreversible by turning its two unwritten invariants into a machine-checked gate.
**What+How:** The R7/R13 decomposition (AGY-344..349) rests on invariants documented only in `mios_dispatch.py`'s header: (1) each extracted `mios_pipe/**` module is a ONE-WAY boundary and must never `import server` / `from server import …`; (2) `server.py` must re-import verbatim every public symbol it moved out, preserving the original alias, so the public surface is unchanged. Add `check_pipe_extraction_parity()` to `automation/38-drift-checks.sh` (next free check number, existing `check_*` style, wired into `main`): grep every `usr/lib/mios/agent-pipe/mios_pipe/**/*.py` for `^\s*(from|import)\s+server\b` and FAIL listing offenders; then invoke a new `tools/pipe-parity-check.py` that AST-walks each `mios_pipe` module and asserts the corresponding `from mios_pipe.… import (…)` line in `server.py` lists every re-exported name (no silent drop). Read-only and fast -- no running server.
**Where:** `automation/38-drift-checks.sh`; new `tools/pipe-parity-check.py`; a negative case in `tests/drift-gate-negatives.sh` (inject `import server` into a temp module copy, assert red, restore, assert green).
**Done When:** `bash -n automation/38-drift-checks.sh tests/drift-gate-negatives.sh` clean; `python3 -m py_compile tools/pipe-parity-check.py` passes; the gate is green on HEAD, reds when a `mios_pipe` module adds `import server`, and reds when a re-import line drops a moved symbol; `just drift-gate` green; only explicit paths staged.
**Why:** Without it, a later refactor can quietly re-couple an extracted module back to `server` or drop a re-exported symbol -- the decomposition rots back into a god-module and callers break at runtime with no gate noticing.
**Dep:** land alongside AGY-344..349 so the guarantee is enforced as the seams move.
**Who:** you (bash + Python AST; the drift-gate framework).

## AGY-351 -- Audit and per-site justify or rewrite the 10 verb backends still containing `eval` (WS-PIPE | P1 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- closes out the TD-1 eval injection surface across the whole verb library, not just the agent-arg path.
**What+How:** AGY-41 removed the agent-arg `eval` surface, but `grep -rl "eval " usr/libexec/mios/` still returns 10 files: `mios-ai-clear`, `mios-ai-reset`, `mios-cron-director`, `mios-daemon`, `mios-day0-reset`, `mios-directory-lookup`, `mios-launch`, `mios-rag`, `mios-tool-search`, `mios-window`. For EACH surviving `eval`, trace every argument to its source and either (a) rewrite to a non-re-parsing form (quoted `"$@"` arrays, `case`-allowlist dispatch, `printf %q`) when any input is agent-derived, or (b) if load-bearing with NO agent-controlled input, annotate the exact line `# TD-1: eval-safe, input=<source>, not agent-controlled` -- per the flatten-campaign caution against blind-deleting load-bearing verbs. Confirmed live sites to resolve: `usr/libexec/mios/mios-launch:507` (`eval exec "$_hit_cmd" '"$@"'`) and `usr/libexec/mios/mios-window:386` (`eval "$(python3 - "$sub" "$hwnd" …)`). Do not touch the medicat/theme trees.
**Where:** the 10 files under `usr/libexec/mios/` named above (only those actually changed or annotated).
**Done When:** every remaining `eval` under `usr/libexec/mios/` is either converted or carries the `# TD-1: eval-safe, input=…` justification; `grep -rn "TD-1: eval-safe" usr/libexec/mios/` accounts for each surviving `eval`; `bash -n` clean on every touched file; the AGY-41 eval-injection check in `automation/38-drift-checks.sh` stays green; `just drift-gate` green.
**Why:** Ten unaudited `eval` sites in the verb library are an unbounded command-injection surface reachable from the agent plane, and with no per-site record nobody can tell a safe `eval` from an exploitable one.
**Dep:** independent; security priority. Security follow-up to AGY-41/AGY-67.
**Who:** you (bash; security follow-up to AGY-41/AGY-67).

## AGY-352 -- Harden the SSOT template renderer and dispatch builder against placeholder/arg-splice injection (WS-PIPE | P1 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- proves by test that no agent-supplied verb argument can escape shell quoting on the one path every verb dispatch takes.
**What+How:** `mios_template.py::_template_to_cmd` (sole verb command-template renderer, `_TEMPLATE_PH_RE = \{([a-zA-Z_]\w*)(?:(=|\?|!|\*)([^}]*))?\}`) already `shlex.quote`s substituted values and `mios_dispatch.py::_build_dispatch_cmd` routes verbs through it; add defense in depth without changing quoting semantics. (1) Placeholder-name allowlist: a placeholder that resolves via `_arg_with_synonyms` to nothing AND is not a declared arg for the verb must render EMPTY, never leak a literal `{name}` into the bash line -- add the guard plus an assertion/log for unknown names. (2) Regression tests feeding adversarial values (`$(id)`, `;rm -rf /`, backticks, `foo bar`, embedded newline) through `_template_to_cmd` for a representative `[verbs.*].cmd`, asserting every metacharacter lands inside a single `shlex.quote`d token. (3) Confirm the `{arg=$ENVVAR:fallback}` branch (L96-99) reads only template-author-chosen env names, with a test asserting args cannot influence WHICH env var is read.
**Where:** `usr/lib/mios/agent-pipe/mios_template.py` (guard + log only); `usr/lib/mios/agent-pipe/test_mios_template.py` (adversarial cases); `usr/lib/mios/agent-pipe/mios_dispatch.py::_build_dispatch_cmd` if a builder-level guard is warranted.
**Done When:** `python3 -m py_compile mios_template.py mios_dispatch.py` passes; the new adversarial tests prove every metacharacter stays quoted and unknown placeholders render empty rather than literal; no existing `test_mios_template.py` case regresses; `just drift-gate` green.
**Why:** The renderer's safety is asserted but untested -- a future edit to the substitution loop could silently drop the `shlex.quote` and hand the agent plane arbitrary shell execution, and a leaked literal `{name}` becomes an unquoted brace in a live command line.
**Dep:** after AGY-351 (same injection theme), independent code path.
**Who:** you (Python; security).

## AGY-353 -- Make the short `MIOS_VLLM_*`/`MIOS_SGLANG_*` form canonical and gate the long form out (WS-NAME | P2 | M) **[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- kills the emitted-vs-consumed dual-name split for the inference lanes so one fact carries one name.
**What+How:** The SSOT-to-env projection disagrees with reality: `tools/generate-names-registry.py` (`SHORT_ALIAS_PREFIX = {"ai.vllm":"MIOS_VLLM","ai.sglang":"MIOS_SGLANG"}`) and `usr/lib/mios/userenv.sh::get_aliases` (L171-184) both know the short form, and EVERY real consumer reads it -- `automation/15-render-quadlets.sh` L121-122, `usr/libexec/mios/system-sync-env.sh` L155/207, the `${MIOS_VLLM_SERVED_NAME}` Quadlet Exec lines in `usr/share/mios/mios.toml`, `automation/38-vllm-prep.sh`'s `MIOS_VLLM_BAKE_MODEL`. Only `root-manifest.json` still records the LONG form as `proposed_canonical` (~13 entries, L1022-1099: `MIOS_AI_VLLM_SERVED_NAME`, `MIOS_AI_VLLM_GPU_UTIL`, `MIOS_AI_SGLANG_*`). Rewrite those `proposed_canonical` values to the short names produced by the generator's `_alias_for`, then add `check_vllm_name_canonical()` to `automation/38-drift-checks.sh` failing on any `MIOS_AI_VLLM_*`/`MIOS_AI_SGLANG_*` string used as an emitted or proposed-canonical name anywhere in the tree (allowlist only genuine historical audit fields). Keep `usr/lib/mios/userenv.sh` and `tools/lib/userenv.sh` byte-identical (drift-27).
**Where:** `root-manifest.json` (the ~13 `proposed_canonical` lines only); `automation/38-drift-checks.sh` (new check + `main` wiring); `tests/drift-gate-negatives.sh` (negative case); verify but do not diverge `tools/generate-names-registry.py`, `usr/lib/mios/userenv.sh`.
**Done When:** `grep -rn "MIOS_AI_VLLM_\|MIOS_AI_SGLANG_" root-manifest.json` returns nothing; the new gate is green on HEAD and reds if a long canonical is reintroduced; `bash -n automation/38-drift-checks.sh` clean; userenv twins still identical; `just drift-gate` green.
**Why:** With the manifest naming one canonical and every consumer reading another, key-library tooling keeps "fixing" toward a name nothing reads -- reintroducing the referenced-but-not-emitted drift class that breaks vLLM/SGLang Quadlet rendering.
**Dep:** complements AGY-250/251 (projection collapse) -- this is the specific VLLM/SGLANG dual-name fix; do not redo their work.
**Who:** you (Python + bash).

## AGY-354 -- Precompile every verb `cmd` template at catalog load; drop the per-dispatch regex parse (WS-PIPE | P2 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- turns verb command templates into compiled, inspectable objects, which is also the precondition for build-time template validation.
**What+How:** Today `mios_template.py::_template_to_cmd` re-runs `_TEMPLATE_PH_RE.sub` on EVERY dispatch and nothing validates placeholder-to-arg linkage. Add `compile_template(template) -> CompiledTemplate` that parses placeholders ONCE into an ordered segment list (`literal | Placeholder(name, op, rest)`) and returns a dataclass/callable; `_template_to_cmd` becomes a thin `compile_template(template).render(tool, args)` over an LRU/dict cache keyed by the template string (SSOT text is static, so the cache is safe and bounded by verb count). Precompile all `[verbs.*].cmd` / `cmd_args` / `cmd_positioned` / `cmd_pixel` / `cmd_resize` variants where `mios_dispatch.configure(verb_catalog=…)` receives the catalog, and expose `CompiledTemplate.placeholder_names` for the AGY-355 gate. Preserve every placeholder semantic (`{arg}`, `{arg!}`, `{arg=default}` including `$ENV:fallback`, `{arg?FLAG}`, `{arg*}`) and the None-on-abort behavior byte for byte.
**Where:** `usr/lib/mios/agent-pipe/mios_template.py` (`compile_template`/`CompiledTemplate`, `_template_to_cmd` kept as compat entry); `usr/lib/mios/agent-pipe/mios_dispatch.py` (precompile at `configure()`); `usr/lib/mios/agent-pipe/test_mios_template.py`.
**Done When:** `python3 -m py_compile mios_template.py mios_dispatch.py` passes; every existing `test_mios_template.py` case passes unchanged (semantic parity); a new test asserts `compile_template(t).render(tool,args)` equals the old `_template_to_cmd` output for each placeholder form and that a repeated template returns the SAME cached object; `just drift-gate` green.
**Why:** Re-parsing a regex on every dispatch burns latency on the hot agent path, and because templates are never structurally inspected a malformed `cmd` can only be discovered as a `None` render at runtime.
**Dep:** builds on AGY-352 (same renderer); independent of the extractions.
**Who:** you (Python).

## AGY-355 -- Build-time verb-template validation gate plus placeholder/arg-coverage negative test (WS-PIPE | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- moves verb-template breakage from a silent runtime `None` to a red gate before the image is built.
**What+How:** With compiled templates available, write `tools/verb-template-check.py` that loads `usr/share/mios/mios.toml [verbs.*]`, compiles each `cmd`/`cmd_*` through `mios_template.compile_template`, and FAILS on: an unparseable template (compile raises); a placeholder whose name is neither a declared arg/synonym for that verb (cross-checked against `_VERB_ARG_SYNONYMS` and the verb's `args`) nor a known ENV-default; or a `{arg!}` required placeholder on an arg the verb declares optional. Wire it as `check_verb_templates()` in `automation/38-drift-checks.sh` and add a `tests/drift-gate-negatives.sh` case injecting `cmd = "foo {undeclared_arg!}"` into a temp TOML copy (assert red, restore, assert green).
**Where:** new `tools/verb-template-check.py`; `automation/38-drift-checks.sh` (new check + `main` wiring); `tests/drift-gate-negatives.sh`; read-only against `usr/share/mios/mios.toml`.
**Done When:** `python3 -m py_compile tools/verb-template-check.py` passes; `bash -n automation/38-drift-checks.sh tests/drift-gate-negatives.sh` clean; the gate is green on HEAD and reds on BOTH an unparseable template and an undeclared placeholder name; the negative case flips red on injection and green on restore; `just drift-gate` green.
**Why:** A typo in a `[verbs.*].cmd` currently ships and only surfaces when an agent invokes that verb and gets a dead dispatch -- the failure is invisible to CI and blamed on the model, not the SSOT.
**Dep:** after AGY-354 (consumes `CompiledTemplate.placeholder_names`).
**Who:** you (Python + bash; drift-gate).

## AGY-356 -- Emit a machine-readable `server.py` module-boundary manifest as the LANG-port contract (WS-PIPE | P3 | M) **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- records the exact agent-pipe interface a future compiled port must preserve, before any code is ported.
**What+How:** The R7/R13 extractions (AGY-344..349) are turning `server.py` into `mios_pipe/**` modules each with a `configure()` DI seam, but nothing records WHAT each module exposes or which server-owned symbols it depends on. Add `tools/gen-pipe-boundary-manifest.py` that AST-walks `usr/lib/mios/agent-pipe/mios_pipe/**/*.py` and, for each module defining `configure(**kwargs)`, records module path, the `configure` keyword names (its injected dependencies), and the public (non-underscore) symbols `server.py` re-imports from it; emit a deterministic `usr/share/mios/pipe-boundaries.manifest.json`. Add a regenerate-and-diff check `check_pipe_boundaries()` to `automation/38-drift-checks.sh` that fails when the committed manifest is stale. Inventory and enforcement ONLY -- port no code.
**Where:** new `tools/gen-pipe-boundary-manifest.py`; new `usr/share/mios/pipe-boundaries.manifest.json`; `automation/38-drift-checks.sh` (new check + `main` wiring); `tests/drift-gate-negatives.sh` (negative case).
**Done When:** `python3 tools/gen-pipe-boundary-manifest.py` emits the JSON deterministically (regenerate equals committed); `python3 -m py_compile tools/gen-pipe-boundary-manifest.py` passes; the gate diffs green on HEAD and reds when a `configure()` kwarg is added without regenerating; `bash -n` clean on the shell edits; `just drift-gate` green.
**Why:** Without a written boundary contract, the eventual Rust/Go rewrite has to re-derive the dependency graph by reading 9k lines of Python, and any drift between the seams and the port is undetectable.
**Dep:** independent; scaffolding for ROADMAP LANG-01 (verb-dispatcher/agent-pipe port).
**Who:** you (Python AST + bash).

## AGY-357 -- Extract the session-event emitter and tool-text sanitizer into `mios_pipe/observability/session_events.py` (WS-PIPE | P2 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- moves one more self-contained concern out of the `server.py` god-module behind the standard configure()-DI seam.
**What+How:** `server.py::_emit_session_event` (L5640) writes the hash-chained `event` row via `mios_audit.stamp` + `_pg_mirror` + `_db_create`, session-linked using `execute_dag`'s `tool_call` convention; `_sanitize_tool_text` (L5770) scrubs tool output. Move BOTH verbatim into a new `mios_pipe/observability/session_events.py` (beside the existing `observability/audit.py`, `cost.py`, `trace.py`) using the `mios_dispatch.py` configure()-DI contract: import `mios_audit` directly as a sibling library, inject the DB writers `_pg_mirror`/`_db_create` (from AGY-344's `dbwrite.py`) and any config scalars. Preserve the SEC-03 pre-stamp ordering exactly -- stamp BEFORE the pgvector mirror so `chain_seq`/`prev_hash`/`chain_hash` are present and `_db_create`'s own stamp is a no-op. Delete the bodies from `server.py` and re-import verbatim.
**Where:** new `usr/lib/mios/agent-pipe/mios_pipe/observability/session_events.py`; `usr/lib/mios/agent-pipe/server.py` (bodies deleted, verbatim re-import, `session_events.configure(…)`); new `usr/lib/mios/agent-pipe/test_mios_session_events.py`.
**Done When:** `python3 -m py_compile server.py mios_pipe/observability/session_events.py` passes; `import server` clean; a test asserts `_emit_session_event` stamps the chain ONCE (mocked `_db_create` sees `chain_hash` already set) and appends `session = <id>` when a session_id is given; `grep -n "def _emit_session_event(" server.py` is empty with the re-import present; the AGY-350 parity gate stays green; `just drift-gate` green.
**Why:** Audit emission and output sanitization stay welded into a 9k-line module nobody can test in isolation, and the fragile stamp-before-mirror ordering has no test protecting it from a reordering edit that would break the audit hash chain.
**Dep:** after AGY-344 (it calls `_pg_mirror`/`_db_create`, which move there) -- inject those in.
**Who:** you (Python).

## AGY-358 -- Run the drift-gate negative self-test harness in CI on every PR (WS-TESTDOC | P1 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- makes CI prove the gates can still FAIL, not merely that they pass.
**What+How:** `just drift-gate` (Justfile L99-102) runs BOTH `automation/38-drift-checks.sh` AND `tests/drift-gate-negatives.sh` (the 16-case inject/assert/restore harness whose `main()` calls `test_version_ssot` … `test_containerfile_pinned_clones`), but the CI `drift-gate` job in `.github/workflows/mios-ci.yml` runs only `lint-shell.sh`, `38-ssot-lint.sh`, the agent-pipe tests, docgen, and `38-drift-checks.sh` -- the negative harness never runs in CI. Add a step named "Drift-gate negative self-tests (drift-gate-negatives.sh)" to the `drift-gate` job, immediately AFTER the "AI-plane source drift checks" step: `run: bash ./tests/drift-gate-negatives.sh`. The harness restores every mutated file byte for byte before exit (no residue) and needs only shellcheck (already installed earlier in the job) and python3 (present on ubuntu-24.04). Keep it in the source-only `drift-gate` job so it fails fast before the bake.
**Where:** `.github/workflows/mios-ci.yml` (drift-gate job, new step after 38-drift-checks.sh); `tests/drift-gate-negatives.sh` (invoked, unchanged).
**Done When:** `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/mios-ci.yml'))"` parses clean; the new step invokes `tests/drift-gate-negatives.sh`; a local `bash tests/drift-gate-negatives.sh` exits 0 with `git status` clean afterward; `just drift-gate` green.
**Why:** A refactor that neuters a check -- makes it stop biting while still exiting 0 -- sails through CI today, so the whole enforcement plane can go quietly toothless with a green badge.
**Dep:** none.
**Who:** you (bash + GitHub Actions).

## AGY-359 -- Close the shellcheck error-level coverage hole: lint EVERY shell dir, not three (WS-TESTDOC | P1 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- extends the shell lint from a sampled subset to the whole shell surface, permanently.
**What+How:** `automation/lint-shell.sh` (L36-58) builds its error-level `files` list from ONLY `automation/*.sh`, `tools/*.sh`, and shebang-matched `usr/libexec/mios/mios-*`. That leaves `installation/*.sh` (mios-install.sh, mios-common.sh), `tests/*.sh`, `automation/tests/*.sh`, `automation/support/*.sh`, `automation/lib/*.sh`, `tools/lib/*.sh` and `usr/lib/mios/*.sh` (userenv.sh) never error-linted repo-wide -- they are reached only by the warning-level modified-files pass (L71-97), i.e. only when touched. Extend the collection loop with those globs, guarding each entry with `[ -f "$f" ]` and applying the same `^#\!.*(bash|sh)` shebang filter already used for libexec to extensionless files. Add `tests/test-lint-shell-coverage.sh` that greps `lint-shell.sh` and asserts each named dir appears in a collection glob, wired into the negative harness or a small check so coverage cannot be silently re-narrowed. Fix every NEW error-level finding the widened net surfaces, or record it as an inline `# shellcheck disable=` with a reason.
**Where:** `automation/lint-shell.sh` (files-collection loop); new `tests/test-lint-shell-coverage.sh`.
**Done When:** `bash automation/lint-shell.sh` error-lints the installation/tests/lib scripts too and exits 0 (all clean or explicitly disabled); `bash -n automation/lint-shell.sh` and `bash -n tests/test-lint-shell-coverage.sh` parse clean; the coverage self-test passes and FAILS if a dir glob is removed; `just drift-gate` green.
**Why:** The install front door and the test harness -- the scripts that run on operator hardware and that gate the tree -- are exactly the ones no error-level lint has ever seen.
**Dep:** none.
**Who:** you (bash + drift-gate).

## AGY-360 -- Negative tests for the bake-projection gates: check_bake_plan + check_bake_ref_defaults (WS-TESTDOC | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- proves the two bake-projection gates actually bite before the publish bake relies on them.
**What+How:** Both gates ship with no negative case (`main()` in `tests/drift-gate-negatives.sh` covers bake_budget/bake_tokens but not these): `check_bake_plan` (38-drift-checks.sh:3672) runs `python3 tools/generate-bake-plan.py --check` and _violations when `usr/lib/mios/bake/plan.d/*.list` are STALE versus mios.toml; `check_bake_ref_defaults` (line 3684) git-greps `MIOS_BUILD_BAKE_REFS_[A-Z0-9_]+:-\}` under `automation/` and _violations on any empty bake-ref default. Add `test_bake_plan` (append a bogus image line to `usr/lib/mios/bake/plan.d/03-extra.list`, assert nonzero, restore byte for byte, assert pass) and `test_bake_ref_defaults` (append `: "${MIOS_BUILD_BAKE_REFS_ZZZ:-}"` to a TRACKED `automation/*.sh` so the working-tree git-grep sees it, assert nonzero, restore, assert pass), both following the exact inject/assert-fail/restore/assert-pass shape of `test_version_ssot` (L23-42) including the `MIOS_THEME_ROOT`/`MIOS_TOML_ROOT` env prefix. Register both in `main()`.
**Where:** `tests/drift-gate-negatives.sh` (new `test_bake_plan`, `test_bake_ref_defaults` + `main()` calls); fixtures `usr/lib/mios/bake/plan.d/03-extra.list` and a tracked `automation` script, each mutated then restored.
**Done When:** `bash tests/drift-gate-negatives.sh` runs both new cases green (each injected violation caught, each restore returning the gate to pass); `bash -n` clean; `git status` clean after the run; `just drift-gate` green.
**Why:** A stale bake plan or an empty bake-ref default is a publish-breaking class, and today nothing proves those two gates would catch it rather than pass vacuously.
**Dep:** none.
**Who:** you (bash + test-harness).

## AGY-361 -- Negative tests for the deploy-plane and SBOM-metadata gates (WS-TESTDOC | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- extends negative coverage to the deploy-plane and SBOM-metadata checks so they are proven failable, not merely green.
**What+How:** Add inject/assert-red/restore/assert-green cases to `tests/drift-gate-negatives.sh` for the deploy-plane check and the SBOM-metadata check dispatched from `automation/38-drift-checks.sh` `main()`, following the established `test_version_ssot` shape (mutate a real tracked input, invoke the single check by name, assert nonzero, restore byte for byte, assert pass) including the `MIOS_THEME_ROOT`/`MIOS_TOML_ROOT` env prefix. Register both new `test_*` functions in `main()`.
**Where:** `tests/drift-gate-negatives.sh` (new test functions + `main()` calls); `automation/38-drift-checks.sh` (invoked by check name, unchanged); the deploy-plane and SBOM-metadata inputs each mutated then restored.
**Done When:** `bash tests/drift-gate-negatives.sh` runs both new cases green -- each injected violation makes its gate exit nonzero and each restore returns it to pass; `bash -n` clean; `git status` clean after the run; `just drift-gate` green.
**Why:** The deploy-plane and SBOM-metadata gates guard what actually ships to hardware and what the provenance record claims; if either silently stops biting, a bad artifact publishes with a green gate behind it.
**Dep:** none.

## AGY-362 -- Negative tests for the security/mini SSOT-projection gates: check_clevis_luks + check_metal_vfio (WS-TESTDOC | P2 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- proves the two generator-backed security projections fail on a broken SSOT instead of passing vacuously.
**What+How:** `check_clevis_luks` (38-drift-checks.sh:4004, "(67)") runs `usr/libexec/mios/mios-clevis-luks-gen <mios.toml>` and _fails unless the output contains `CLEVIS_LUKS_ENABLED=`; `check_metal_vfio` (line 4020, "(68)") runs `usr/libexec/mios/mios-metal-vfio-gen <mios.toml>` and _fails unless the output contains `MIOS_METAL_ENABLED=` -- neither has a negative test. Add `test_clevis_luks` and `test_metal_vfio` that inject via a MALFORMED SSOT: create a temp toml fixture missing the `[security]`/`[mini]` table (or empty), invoke the check with `MIOS_TOML_ROOT` pointed at it, assert nonzero, then remove the fixture and assert the check re-passes against the real mios.toml. If a generator emits its marker key regardless of the missing table, instead inject by sed-breaking the marker `echo` line on a working COPY invoked through an overridden PATH -- and document whichever injection actually flips the gate red. Follow `test_router_parity` (line 298) for the generator-based pattern; register both in `main()`.
**Where:** `tests/drift-gate-negatives.sh` (new `test_clevis_luks`, `test_metal_vfio` + `main()` calls); `usr/libexec/mios/mios-clevis-luks-gen` and `mios-metal-vfio-gen` (invoked, unchanged); a temp toml fixture created then removed.
**Done When:** both cases run green -- the malformed SSOT makes each gate exit nonzero and restore returns each to pass; `bash -n` clean; `git status` clean after the run; `just drift-gate` green.
**Why:** These gates guard disk-encryption key binding and the VFIO passthrough projection; a check that only ever asserts a substring can pass on a generator that has stopped reading SSOT at all, shipping an unbound LUKS or a mis-bound GPU.
**Dep:** none.
**Who:** you (bash + test-harness).

## AGY-363 -- Negative tests for check_hyprland_conf_heredoc + check_target_languages (WS-TESTDOC | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- covers the desktop-config projection gate and the Law 14 language gate with proof-of-failure cases.
**What+How:** `check_hyprland_conf_heredoc` (38-drift-checks.sh:3848, "(50)") diffs `usr/share/mios/hyprland/hyprland.conf` against the heredoc embedded in `automation/54-bake-hyprland.sh` and _violations on drift; `check_target_languages` (line 3641, Law 14 TARGET-LANGUAGES) also has no negative case. Add `test_hyprland_heredoc` (append `# INJECTED-DRIFT` to `usr/share/mios/hyprland/hyprland.conf`, assert `check_hyprland_conf_heredoc` exits nonzero, restore byte for byte, assert pass) and `test_target_languages` (read the function body to learn its actual rule, then inject exactly what it forbids -- e.g. a disallowed-language source file dropped into a scanned dir or a forbidden extension -- assert nonzero, remove, assert pass). Mirror `test_version_ssot`'s inject/fail/restore/pass shape and its `MIOS_THEME_ROOT`/`MIOS_TOML_ROOT` env prefix; register both in `main()`.
**Where:** `tests/drift-gate-negatives.sh` (new `test_hyprland_heredoc`, `test_target_languages` + `main()` calls); `usr/share/mios/hyprland/hyprland.conf` (mutated then restored); a scanned language-dir fixture (created then removed).
**Done When:** both cases run green, each injected violation caught and each restore returning the gate to pass; `bash -n` clean; `git status` clean after the run; `just drift-gate` green.
**Why:** The hyprland heredoc is a hand-duplicated copy that drifts by construction, and Law 14 is only as real as the check enforcing it -- an unfailable language gate means new C#/Batch/Go can land unnoticed.
**Dep:** none.
**Who:** you (bash + test-harness).

## AGY-364 -- Negative tests for the docs/templates/EOL gates: roadmap index, template compilation, EOL regressions (WS-TESTDOC | P2 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- brings three more dispatched gates under proof-of-failure coverage.
**What+How:** Three gates lack negative cases: `check_roadmap_index` (38-drift-checks.sh) runs `tools/roadmap-index.py --check` over ROADMAP.md's generated index/rollup/ToC; `check_templates_compilation` (line 3291, "(59)") runs `tools/compile-templates.py` over `usr/share/mios/templates/`; `check_impossible_eol_regressions` (line 3311, "(60)") _violations when mios.toml lists the EOL packages `"glusterfs"`/`"glusterfs-fuse"`/`"tang"` or a concepts doc makes an unqualified `mdevctl vGPU` claim. Add `test_roadmap_index` (edit the `**Done**: NN` count inside the `<!-- ROADMAP_ROLLUP_START -->` block, assert nonzero, restore, assert pass), `test_templates_compilation` (append an invalid line to a file under `usr/share/mios/templates/`, e.g. `templates/toml-config`, so compile-templates rejects it, assert nonzero, restore, assert pass), and `test_impossible_eol` (temporarily append `"tang"` to a package array in `usr/share/mios/mios.toml`, assert nonzero, restore byte for byte, assert pass). Register all three in `main()`.
**Where:** `tests/drift-gate-negatives.sh` (new `test_roadmap_index`, `test_templates_compilation`, `test_impossible_eol` + `main()` calls); `ROADMAP.md`, `usr/share/mios/templates/*` and `usr/share/mios/mios.toml`, each mutated then restored.
**Done When:** all three cases run green, each injected violation caught and restored; `bash -n` clean; `git status` clean after the run; `just drift-gate` green.
**Why:** These are the gates that keep the roadmap honest, the template system enforceable, and dead upstream packages out of the image -- unproven, any of them can pass on input it should reject.
**Dep:** none.
**Who:** you (bash + Python + test-harness).

## AGY-365 -- Ratchet gate: every dispatched drift-check must have a negative case (shrink-only allowlist) (WS-TESTDOC | P2 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- makes negative-test coverage self-enforcing so the gap can never silently reopen.
**What+How:** `tests/drift-gate-negatives.sh` covers roughly 16 of the ~75 `check_*` functions dispatched from `automation/38-drift-checks.sh` `main()` (L~4224-4292) and nothing enforces the ratio -- new gates land untested, which is exactly why AGY-360..364 had to exist. Add `check_negative_coverage` to 38-drift-checks.sh that (a) extracts the dispatched check-name list from `main()` (the `check_[a-z_]+` calls between `check_dead_lane` and the final `echo` divider), (b) extracts the covered set from `tests/drift-gate-negatives.sh` (the `bash .../38-drift-checks.sh <name>` invocations inside each `test_*`), and (c) FAILS when a dispatched check that is not on an explicit `[testing.negative_coverage_exempt]` allowlist in mios.toml has no negative case -- a ratchet where the exempt list may only shrink. Seed the allowlist with today's genuinely untestable (warn-only/environmental) checks so HEAD is green, register the check in `main()`, and add a negative case for IT to the harness.
**Where:** `automation/38-drift-checks.sh` (new `check_negative_coverage` + dispatch entry); `usr/share/mios/mios.toml` (new `[testing.negative_coverage_exempt]`); `tests/drift-gate-negatives.sh` (`test_negative_coverage`).
**Done When:** `bash automation/38-drift-checks.sh check_negative_coverage` is green at HEAD; adding a new `check_foo` to `main()` without a negative case and without an allowlist entry FAILS; removing a still-uncovered allowlist entry FAILS; the negative case flips it red then restores; `just drift-gate` green.
**Why:** Coverage added by hand decays the moment attention moves on -- without a ratchet, the 16-of-75 gap quietly regrows and the enforcement plane's own trustworthiness becomes unmeasurable.
**Dep:** none.
**Who:** you (bash/Python + drift-gate).

## AGY-366 -- Extract the CI smoke-RUN into a reusable bake smoke-test harness runnable locally and from CI (WS-TESTDOC | P2 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- gives bake-smoke one versioned source of truth that asserts import+exec for a built image.
**What+How:** The runtime smoke assertions live ONLY inline in the `smoke-test` job's "Smoke RUN" step of `.github/workflows/mios-ci.yml` -- `py_compile server.py`; presence of the libexec shims `flatpak-launch`, `mios-pc-control`, `mios-launcher-daemon`, `mios-flatpak-icon-sanitize`; the `mios-wsl-interop-priority.service` unit -- so nobody can run the same boot-smoke locally against a `just build` image and the assertion list is unversioned shell trapped in YAML. Extract it to `tests/bake-smoke.sh [image-ref]` (default `localhost/mios:latest`) which runs `podman run --rm <ref> bash -lc '…'` with the identical checks, prints `SMOKE_RUN_PASS`, and exits nonzero on the first missing component. Reduce the CI step to `run: bash ./tests/bake-smoke.sh mios:smoke` so CI and local share ONE source. Document that it needs podman plus a built image and is not part of the source-only drift-gate.
**Where:** new `tests/bake-smoke.sh`; `.github/workflows/mios-ci.yml` (smoke-test job, "Smoke RUN" step now calls the script).
**Done When:** `bash -n tests/bake-smoke.sh` parses clean; the script's component list is byte-identical to the checks the inline step performed (grep-verifiable); the CI step invokes `tests/bake-smoke.sh`; the workflow still parses under `yaml.safe_load`; `shellcheck --severity=error tests/bake-smoke.sh` is clean so AGY-359's widened lint stays green.
**Why:** Today the only way to run the boot smoke is to push and wait for CI, and the assertion list -- the definition of "the image actually works" -- is invisible to every lint and gate the repo has.
**Dep:** none.
**Who:** you (bash + GitHub Actions).

## AGY-367 -- SSOT-drive the smoke-test component manifest from mios.toml plus a parity drift-gate (WS-TESTDOC | P3 | M) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- makes the smoke component list a Law 8 projection of SSOT so a rename fails the source gate, not a late boot.
**What+How:** AGY-366's `tests/bake-smoke.sh` component list (libexec shims, required unit, agent-pipe entry to py_compile) is a hand-maintained literal mirrored from the old inline CI step and will rot as shims are renamed or added. Add a `[testing.smoke_components]` table to `usr/share/mios/mios.toml` enumerating the load-bearing paths the smoke boot must verify; have `tests/bake-smoke.sh` read them through the existing resolver (`usr/lib/mios/mios_toml.py` `load_vendor`, or a `python3 -m mios_toml`-style read) instead of a hardcoded loop; and add `check_smoke_manifest` to `automation/38-drift-checks.sh` asserting every listed path exists in the source tree. Register the check in `main()` and add its negative case to the harness.
**Where:** `usr/share/mios/mios.toml` (new `[testing.smoke_components]`); `tests/bake-smoke.sh` (reads SSOT); `automation/38-drift-checks.sh` (`check_smoke_manifest` + dispatch); `tests/drift-gate-negatives.sh` (`test_smoke_manifest`).
**Done When:** `bash automation/38-drift-checks.sh check_smoke_manifest` green at HEAD; listing a non-existent path in `[testing.smoke_components]` fails the gate; grep confirms no hardcoded shim list remains in `tests/bake-smoke.sh`; the negative case flips red then restores; `bash -n` and `python3 -m py_compile` clean on touched files; `just drift-gate` green.
**Why:** A hardcoded component list drifts silently: rename a shim and either the smoke keeps asserting a path nobody ships, or it stops asserting the one that matters -- and either way you learn at boot, not at commit.
**Dep:** after AGY-366 (consumes `tests/bake-smoke.sh`).
**Who:** you (bash + Python + drift-gate).

## AGY-368 -- ADR-0012: float-latest / no-hand-pinned-versions, generalizing ADR-0003 to every artifact class (WS-TESTDOC | P1 | M) **[DONE]**
**Goal:** E-14 Float latest globally -- turns the operator's float-latest principle into an accepted, cross-linked decision so its scattered enforcement has one authority to cite.
**What+How:** ADR-0003 (`usr/share/doc/mios/adr/0003-sbom-not-hardcode.md`) decided container-image digests are build-resolved SBOM data and explicitly leaves "model checksums, package version-hashes … as PLANNED follow-up"; the broader principle -- nothing hand-pinned, everything floats latest, resolved at build and recorded in SBOM -- is not itself a decision, and its enforcement is scattered (`check_containerfile_pinned_clones` "(71)" wanting git clones pinned to a REF; WS-MIOSSYS/WS-RELTOP newest-packages-tagged-at-build; the firstboot tier carrying `:latest` intent). Write `usr/share/doc/mios/adr/0012-float-latest-no-hand-pinned-versions.md` following the README file format EXACTLY (two-line AI-hint header, YAML frontmatter, Status/Context/Decision/Rationale/Alternatives/Consequences/Implementation/References). Frame it as GENERALIZING ADR-0003 from container digests to RPM/package versions, model weights, git-clone refs and base-image tags, and reconcile the apparent tension: SSOT carries floating version INTENT (`:latest`, newest `MIOS_<X>_VERSION`), the BUILD resolves+verifies+records the concrete version to SBOM, and reproducibility = baked manifest + SBOM, never a hand-pin. Frontmatter `laws: [7, 8, 12]` (all <=13 so the roadmap/ADR law checks accept), `ssot_keys` from real keys (`image.sidecars`, `build.bake`, `build.bake_groups`, `ai.bake_models`), `related_ws: [WS-SBOM, WS-MIOSSYS, WS-RELTOP]`, `supersedes: []`; leave ADR-0003's `superseded_by: []` UNCHANGED and cross-link in References instead. Add the row to the README "The record" table.
**Where:** new `usr/share/doc/mios/adr/0012-float-latest-no-hand-pinned-versions.md`; `usr/share/doc/mios/adr/README.md` ("The record" table row).
**Done When:** every frontmatter key is filled with real laws (within the mios.toml `[laws]` 1..13 range) and real ssot_keys; the README table lists ADR-0012 with matching status/laws; a Markdown render shows all 8 body sections; References cites ADR-0003 without restating the digest mechanics; if `tools/adr-index.py` exists at implementation time, `python3 tools/adr-index.py --check` is green.
**Why:** With no accepted decision behind float-latest, each surface argues the pinning question locally -- one check demands pinned clone refs while the operator directive demands floating tags, and there is nothing to point at to settle it.
**Dep:** none.
**Who:** you (Markdown ADR + docs).

## AGY-369 -- ADR-0013: deploy-surface consolidation behind `installation/mios-install` (WS-TESTDOC | P1 | M) **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- records the landed single-dispatcher architecture as an immutable decision instead of tribal knowledge.
**What+How:** The AGY-side consolidation is real and landed -- `installation/mios-install.{sh,ps1,bat}` is a pure DISPATCHER whose `usage()` lists targets `live|xbox|fedora|bootc|oci|seed|flash|build|update|config`, whose `resolve_*` functions build each entrypoint's argv/env and exec it, over the shared contract in `installation/mios-common.{sh,ps1}` (the UNIFY.md campaign) -- but no ADR records it, so "one front door resolving to N deployment types, never rewriting the underlying entrypoints" is undocumented. Write `usr/share/doc/mios/adr/0013-deploy-surface-consolidation.md` per the README format EXACTLY. Decision: `installation/mios-install` is the single Linux/Windows install surface that RESOLVES a chosen target to an existing entrypoint (`build-mios.sh`, `mios-update`, `cat/MiOS-Cat.sh`, …) via the shared common-lib contract, with `--dry-run` plan output and gated target-matrix parity (cite the AGY-owned checks). Scope to the AGY tree ONLY: describe, do not propose changes to, the MiOS-Cat/Get-MiOS/Portal bootstrap side -- that is ADR-0008's domain; cross-link both. Frontmatter `laws: [1, 7, 8, 9]`, real `ssot_keys` (verbs, ports, any `[install]`/`[cat]` keys the dispatcher reads), `related_ws: [WS-INSTALL, WS-CONFIG]`, References cross-linking ADR-0008 and ADR-0009. Add the README "The record" row.
**Where:** new `usr/share/doc/mios/adr/0013-deploy-surface-consolidation.md`; `usr/share/doc/mios/adr/README.md` ("The record" row); cites `installation/mios-install.sh`, `installation/mios-common.{sh,ps1}`, `installation/UNIFY.md` (referenced, not modified).
**Done When:** all frontmatter keys filled with real laws (<=13) and real ssot_keys; the body has all 8 sections; it explicitly scopes to the AGY `installation/` surface and defers MiOS-Cat/Portal to ADR-0008 with no mios-bootstrap edits proposed; the README table lists ADR-0013; if `tools/adr-index.py` exists, `--check` is green.
**Why:** An undocumented consolidation gets re-fragmented: the next contributor adds a fourth parallel install script because nothing states that the dispatcher is the only front door and that entrypoints are resolved, never rewritten.
**Dep:** none.
**Who:** you (Markdown ADR + docs).

## AGY-370 -- ADR-0014: the bare-metal leg, `bootc install to-disk --transport oci` (WS-TESTDOC | P1 | M) **[DONE]**
**Goal:** E-20 The bootc-native install legs -- names and scopes the missing blank-hardware, fully-offline install path as a decision before code is written for it.
**What+How:** The bare-metal leg is the least-done deploy path and is undocumented: `automation/build-mios.sh` (~L517) offers only `bootc install to-existing-root --source-imgref localhost/mios:latest` (converts an ALREADY-running host in place), and `installation/mios-install.sh`'s `bootc` target (`resolve_bootc`, L228) does only `switch` (a no-op unless the host already booted bootc) or `upgrade` (delegating to `mios-update`) -- there is NO fresh-disk `bootc install to-disk` and NO offline `--transport oci` path onto blank hardware, the gap the FOSS-upstream memo names. Write `usr/share/doc/mios/adr/0014-bootc-install-bare-metal-leg.md` per the README format EXACTLY. Decision: adopt `bootc install to-disk` (and `to-filesystem` for the kickstart path) sourcing the image via `--transport oci`/`oci-archive` from the MiOS-Data OCI tar so a blank machine installs the SAME image offline -- distinguishing the three legs (to-existing-root = convert, to-disk = blank disk, to-filesystem = anaconda/kickstart `%post`) and the offline-transport requirement. Mark `status: proposed` (implementation PLANNED). Frontmatter `laws: [3, 4, 12]`, real `ssot_keys` (`image.sidecars`, `build.bake`, and the ventoy/kickstart keys `check_deploy_plane` reads), `related_ws: [WS-CAT, WS-MDRIVE]`, References cross-linking ADR-0005 and ADR-0008. Add the README "The record" row.
**Where:** new `usr/share/doc/mios/adr/0014-bootc-install-bare-metal-leg.md`; `usr/share/doc/mios/adr/README.md` ("The record" row); cites `automation/build-mios.sh`, `installation/mios-install.sh`, `usr/share/mios/ventoy/mios-kickstart.cfg` (referenced, not modified).
**Done When:** all frontmatter keys filled with real laws (<=13) and real ssot_keys; `status: proposed`; the body has all 8 sections and names the three bootc-install legs plus the `--transport oci` offline requirement; the README table lists ADR-0014; if `tools/adr-index.py` exists, `--check` is green.
**Why:** Today the only "install" MiOS can perform on blank hardware installs plain Fedora, and with the gap unwritten it keeps being mistaken for a bug in the media rather than a whole missing leg.
**Dep:** none.
**Who:** you (Markdown ADR + docs).

## AGY-371 -- Record the testdoc/deploy work as ROADMAP workstreams and regenerate the index/rollup/ToC (WS-TESTDOC | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- makes the roadmap reflect the work that exists so an arriving agent can pick up a workstream cold.
**What+How:** ROADMAP.md's generated blocks -- `<!-- ROADMAP_ROLLUP_START -->` (Done/Active/Proposed/Blocked counts), `<!-- ROADMAP_INDEX_START -->` (per-theme WS list), `<!-- ROADMAP_TOC_START -->` -- are produced by `tools/roadmap-index.py` from each `## WS-*` section's KEP-style frontmatter, and none of this session's testdoc/deploy work is recorded. Add two WS sections with full frontmatter (`id, title, theme, status, priority, laws[], ssot_keys[], adr[], deps[], acceptance`) using existing WS blocks as the template: `WS-TESTDOC` (drift-gate negative coverage in CI, shellcheck coverage widening, bake-smoke harness; `adr: []`; `laws: [7, 8]`) and `WS-DEPLOY` (theme "Deployment & Sovereignty" -- deploy-surface consolidation plus the bootc-install bare-metal leg; `adr: [13, 14]` referencing AGY-369/370; `laws: [1, 3, 4, 12]`). Reference ADR numbers only once AGY-368..370 land. Then run `python3 tools/roadmap-index.py --write` and verify `--check` is clean. NOTE: `tools/roadmap-index.py` (~L204) currently rejects any `law > 13`, so keep every WS `laws[]` entry <=13 until that ceiling is de-hardcoded.
**Where:** `ROADMAP.md` (new WS-TESTDOC + WS-DEPLOY sections and the regenerated INDEX/ROLLUP/TOC blocks); `tools/roadmap-index.py` (invoked, unchanged).
**Done When:** `python3 tools/roadmap-index.py --check` exits 0 (index/rollup/ToC match the frontmatter); both new WS sections carry every frontmatter key with real laws (<=13), real ssot_keys and valid adr refs; `bash automation/38-drift-checks.sh check_roadmap_index` green; `just drift-gate` green.
**Why:** Work that is not on the roadmap is invisible to the next agent, who re-derives or duplicates it -- and a hand-edited rollup that no longer matches the frontmatter reds `check_roadmap_index`.
**Dep:** after AGY-368..370 (their ADR numbers are referenced).
**Who:** you (Markdown + Python + drift-gate).

## AGY-372 -- Vendor the REAL k3s binary at LATEST with sha verification and install.sh (WS-OFFL | P1 | M) **[DONE]**
**Goal:** E-14 Float latest globally -- replaces a stub with a latest-resolved, sha-verified, SBOM-recorded artifact so the k3s stack bakes offline with no hand-pin.
**What+How:** Replace the 14-byte stubs at `usr/share/mios/vendored/k3s/{k3s,k3s-selinux.tar.gz}` with real assets. Resolve the LATEST `v*+k3s1` tag via `git ls-remote --tags https://github.com/k3s-io/k3s.git` (NOT the mios.toml pin, and not the rate-limited GitHub API); download `.../releases/download/<tag>/k3s`, `sha256sum-amd64.txt`, and `raw.githubusercontent.com/k3s-io/k3s/<tag>/install.sh` saved as `k3s-install.sh`; verify the sha. Record the resolved tag to the SBOM and bump `mios.toml [containers.k3s]`/`k3s_version` AND the `docker.io/rancher/k3s` image ref to the SAME `<tag>` so the server binary matches the agent image -- no skew. Regenerate the bake plan.
**Where:** `usr/share/mios/vendored/k3s/`, `usr/share/mios/mios.toml`, `automation/90-generate-sbom.sh`, `usr/lib/mios/bake/plan.d/03-extra.list` (regenerate).
**Done When:** `36-ceph-k3s.sh` finds and verifies the vendored binary with no network; the vendored binary tag equals the image tag; the bake-plan gate is green.
**Why:** A 14-byte stub means the k3s stage either fails or silently installs nothing on an offline bake, and a binary/image tag skew produces a cluster whose server and agent disagree.
**Dep:** none.

## AGY-373 -- Vendor k3s-selinux.tar.gz as the source tarball at the matching tag (WS-OFFL | P2 | S) **[DONE]**
**Goal:** E-14 Float latest globally -- completes the k3s offline vendoring at a tag derived from AGY-372's resolution rather than hand-picked.
**What+How:** `automation/37-k3s-selinux.sh` extracts `usr/share/mios/vendored/k3s/k3s-selinux.tar.gz`. Fetch the GitHub archive tarball for the k3s-selinux tag matching AGY-372's k3s minor (`https://github.com/k3s-io/k3s-selinux/archive/refs/tags/<tag>.tar.gz`) into that exact path, and record the resolved tag to the SBOM.
**Where:** `usr/share/mios/vendored/k3s/k3s-selinux.tar.gz`; SBOM.
**Done When:** `37-k3s-selinux.sh` extracts and builds with no network; no stub file remains at that path.
**Why:** Without the real tarball the SELinux policy stage cannot build offline, leaving k3s running unconfined or the stage failing mid-bake.
**Dep:** AGY-372 (tag must match its resolved k3s minor).

## AGY-374 -- Vendor the Bibata cursor at LATEST (WS-OFFL | P2 | S) **[DONE]**
**Goal:** E-14 Float latest globally -- the last of the offline-vendoring stubs becomes a latest-resolved, SBOM-recorded asset.
**What+How:** Resolve the latest `ful1e5/Bibata_Cursor` tag via `git ls-remote --tags` (the GitHub API is rate-limited -- do NOT use api.github.com), download `Bibata-Modern-Classic.tar.xz` to `usr/share/mios/vendored/cursors/bibata.tar.xz`, and record the resolved version to the SBOM.
**Where:** `usr/share/mios/vendored/cursors/bibata.tar.xz`; SBOM.
**Done When:** `57-gnome.sh` installs the cursor with no network; the stub is replaced by the real archive.
**Why:** The stub leaves the desktop on the default cursor theme after an offline bake, and an api.github.com lookup would rate-limit the build instead of failing loudly.
**Dep:** none.

## AGY-375 -- Vendor Geist + Nerd fonts at LATEST as real tar.xz, replacing the stubs  (WS-OFFL | P2 | S)  **[DONE]**
**Goal:** E-16 The bake plane: what is present in the image -- every font byte the build consumes is present in-tree, so the image bakes with zero egress (Law 12 BAKE-NOT-FETCH).
**What+How:** Replace the placeholder tarballs with real archives resolved at latest: shallow-clone `vercel/geist-font` and repack with `tar -cJf vendored/fonts/geist.tar.xz -C packages/next/dist/fonts .`; resolve the newest `ryanoasis/nerd-fonts` tag via `git ls-remote --tags`, download `NerdFontsSymbolsOnly.zip`, unzip and repack to `vendored/fonts/nerd.tar.xz`; feed both resolved upstream versions into the SBOM record rather than hand-writing a version.
**Where:** `usr/share/mios/vendored/fonts/geist.tar.xz`, `usr/share/mios/vendored/fonts/nerd.tar.xz`, `automation/56-fonts.sh`, SBOM.
**Done When:** `56-fonts.sh` installs both font families with the network disabled and no stub-sized file remains under `usr/share/mios/vendored/fonts/`.
**Why:** the font stage currently depends on live GitHub egress at build time; an offline or rate-limited build ships a desktop with missing Geist/Nerd glyphs and no error until first login.
**Dep:** none

## AGY-376 -- Vendor the hermes-agent wheel plus its full transitive wheel set  (WS-OFFL | P2 | M)  **[DONE]**
**Goal:** E-16 The bake plane -- the agent runtime is baked from in-tree artifacts, not pip-fetched at build (Law 12 BAKE-NOT-FETCH, NS-1).
**What+How:** `automation/72-hermes-agent.sh` already expects `vendored/hermes_agent.whl` plus a `vendored/wheels/` find-links tree. Build them: `pip wheel` the hermes-agent from the `NousResearch/hermes-agent` git ref together with every transitive dependency into `usr/share/mios/vendored/wheels/`, and place the top-level wheel at `usr/share/mios/vendored/hermes_agent.whl`; record the resolved versions for the SBOM.
**Where:** `usr/share/mios/vendored/wheels/`, `usr/share/mios/vendored/hermes_agent.whl`, `automation/72-hermes-agent.sh`.
**Done When:** `72-hermes-agent.sh` creates the venv successfully with `pip install --no-index --find-links usr/share/mios/vendored/wheels` and no PyPI request appears in the build log.
**Why:** without the wheels, the hermes-agent stage either fetches PyPI mid-build (non-reproducible, breaks the offline bake) or silently degrades the AI plane's agent lane.
**Dep:** none

## AGY-377 -- `mios-vendor-refresh`: one command that re-pulls every vendored asset at latest  (WS-OFFL | P1 | M)  **[DONE]**
**Goal:** E-14 Float latest globally -- vendored offline assets carry `:latest` INTENT and get their real version recorded, never hand-pinned (NS-4).
**What+How:** Add `usr/libexec/mios/mios-vendor-refresh` (bash glue, AI-hint header, `set -euo pipefail`) that walks the vendored asset set, resolves each upstream's newest tag via `git ls-remote`/release redirect (no rate-limited API), re-downloads it, verifies the published checksum, and writes `<asset> <resolved-version> <sha256>` lines to `usr/share/mios/vendored/VERSIONS.txt` as the SBOM feed. One command keeps the offline cache current.
**Where:** `usr/libexec/mios/mios-vendor-refresh`, `usr/share/mios/vendored/VERSIONS.txt`.
**Done When:** template-conformance check-46 passes on the new file, and running the tool refreshes every asset and rewrites `VERSIONS.txt` with resolved versions.
**Why:** today refreshing the offline cache is an undocumented manual sequence per asset, so the cache silently rots and nobody can say what upstream version an image actually shipped.
**Dep:** none

## AGY-378 -- Drift-gate: every consumed vendored asset is present and non-stub  (WS-OFFL | P2 | S)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- an offline-build invariant becomes a mechanically enforced check rather than a convention.
**What+How:** Add a check to `automation/98-drift-checks.sh` asserting that every file under `usr/share/mios/vendored/**` referenced by a build stage exists and exceeds a size floor (>1KB, which catches the 14-byte placeholder stubs), and that the k3s binary's sha256 matches its sibling `sha256sum-amd64.txt`. FAIL, not WARN, on a stub. Ship the paired negative test that injects a stub.
**Where:** `automation/98-drift-checks.sh`, the negative-test harness.
**Done When:** the negative test injecting a 14-byte stub makes the check go RED, and the check is green on the real asset tree.
**Why:** stub assets pass every existing gate and only surface as a broken font, a missing wheel, or a corrupt k3s at runtime on the deployed host.
**Dep:** none

## AGY-379 -- Vendor the terra.repo GPG key so offline dnf can import it from disk  (WS-OFFL | P2 | S)  **[DONE]**
**Goal:** E-16 The bake plane -- no build stage reaches the network for trust material (Law 12 BAKE-NOT-FETCH).
**What+How:** `terra.repo` currently sets `gpgkey=https://repos.fyralabs.com/.../key.asc`. Download that key once to `usr/share/mios/repos/terra.key`, commit it, and rewrite the repo definition's `gpgkey=` to the corresponding `file://` path so `06-enable-external-repos.sh` imports it from the tree.
**Where:** `usr/share/mios/repos/terra.repo`, `usr/share/mios/repos/terra.key`, `automation/06-enable-external-repos.sh`.
**Done When:** an offline build imports the terra key from disk and installs terra packages with no HTTPS fetch of `key.asc`.
**Why:** an offline or egress-blocked build fails at repo-enable time, and a fetched-at-build key is unauditable trust material with no provenance record.
**Dep:** none

## AGY-380 -- Vendored-directory size audit recipe plus the git-vs-LFS tradeoff note  (WS-OFFL | P3 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- the offline cache's repo-size cost is measurable and its recorded decision is discoverable.
**What+How:** Add a `just vendored-size` recipe (or an equivalent check) that sums `usr/share/mios/vendored/` and warns past a stated budget, and document in `usr/share/doc/mios/concepts/OFFLINE-FIRST.md` why the operator chose plain-git commit over Git LFS on 2026-07-31.
**Where:** `Justfile`, `usr/share/doc/mios/concepts/OFFLINE-FIRST.md`.
**Done When:** `just vendored-size` prints the total and the warning threshold, and the OFFLINE-FIRST doc records the LFS tradeoff.
**Why:** vendored tarballs grow the clone unbounded and invisibly, and the LFS decision gets relitigated by every new contributor without a written rationale.
**Dep:** none

## AGY-381 -- CI gate: a PUBLISH build must fail fast on any stub vendored asset  (WS-OFFL | P3 | S)  **[DONE]**
**Goal:** E-16 The bake plane -- a published image is never half-offline; the publish path enforces the same presence invariant as the tree.
**What+How:** In `.github/workflows/mios-ci.yml` (mirrored to the `.forgejo/` workflow so both publishers stay equal), add an early step that runs only when `PUBLISH=true` and reuses AGY-378's stub-detection logic against every consumed `vendored/**` asset, failing the job with a message naming the offending file before the bake starts.
**Where:** `.github/workflows/mios-ci.yml`, `.forgejo/workflows/`.
**Done When:** a `PUBLISH=true` run with an injected stub fails within the first minute with a message naming the stub path, instead of after the multi-GB bake.
**Why:** without it, a stubbed asset burns a full publish bake and ships an image whose offline claim is false.
**Dep:** AGY-378 (reuses its stub-detection logic)

## AGY-382 -- `mios.toml [offline.vendored]` registry as the SSOT for the asset list  (WS-OFFL | P3 | M)  **[DONE]**
**Goal:** E-12 ZERO-HARDCODES -- the vendored asset list stops being duplicated in code and becomes one SSOT declaration projected to its consumers (Law 7, Law 8).
**What+How:** Declare each vendored asset in `usr/share/mios/mios.toml` under `[offline.vendored]` as name -> upstream repo -> artifact -> destination path, then repoint `mios-vendor-refresh` and the AGY-378 drift-check to read that table through the shared resolver instead of carrying their own hardcoded asset lists.
**Where:** `usr/share/mios/mios.toml`, `usr/libexec/mios/mios-vendor-refresh`, `automation/98-drift-checks.sh`.
**Done When:** adding one row to `[offline.vendored]` causes both the refresh tool to pull it and the drift-gate to police it, with no code edit in either.
**Why:** two hand-maintained copies of the asset list drift apart, so an asset gets refreshed but ungated (or gated but never refreshed).
**Dep:** AGY-377, AGY-378

## AGY-383 -- Record OFFL-01..03 complete in TASKS.md and regenerate the roadmap index  (WS-OFFL | P3 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- the roadmap is a generated, in-sync view of real state, not a stale hand-edited list.
**What+How:** After AGY-372..382 land, flip the T-204..209 (OFFL-*) status fields in `TASKS.md` and re-run `tools/roadmap-index.py` to regenerate `ROADMAP.md` from the task source.
**Where:** `TASKS.md`, `ROADMAP.md`, `tools/roadmap-index.py`.
**Done When:** roadmap-index drift check-36 is green with no manual edits to the generated section.
**Why:** a roadmap that still lists shipped offline work as open sends the next agent to redo it.
**Dep:** AGY-372..AGY-382

## AGY-384 -- `mios-resolve-latest`: resolve every floating bake ref to tag+digest at build  (WS-LATEST | P1 | M)  **[DONE]**
**Goal:** E-14 Float latest globally -- ONE shared build-time latest resolver replaces every inlined resolve, and reproducibility comes from the SBOM (ADR-0003).
**What+How:** Add `usr/libexec/mios/mios-resolve-latest` that iterates every image carrying `:latest`/family-channel intent in `mios.toml [build.bake]` and the `plan.d` lists, resolves the current tag and manifest digest via `skopeo inspect`, and appends `<ref> <resolved-tag> <digest>` to the SBOM manifest emitted by `automation/90-generate-sbom.sh`.
**Where:** `usr/libexec/mios/mios-resolve-latest`, `automation/90-generate-sbom.sh`.
**Done When:** the baked SBOM carries a resolved tag and sha256 digest line for every floating ref in the bake plan.
**Why:** without a resolver, floating refs mean the image has no provenance record at all, and every stage that needs a pin re-implements its own resolve.
**Dep:** none

## AGY-385 -- Float the AI-plane sidecar images to `:latest` intent  (WS-LATEST | P1 | S)  **[DONE]**
**Goal:** E-14 Float latest globally -- SSOT carries version intent only; the reality lives in the SBOM (NS-4).
**What+How:** In `usr/share/mios/mios.toml [image.sidecars]` and the `plan.d` lists, replace the hand-pinned tags on `vllm/vllm-openai`, `lmsysorg/sglang`, `ghcr.io/mostlygeek/llama-swap:cuda` and `ghcr.io/open-webui/open-webui` with `:latest` (or the family-latest channel), then regenerate the bake plan so the resolution is recorded by AGY-384.
**Where:** `usr/share/mios/mios.toml`, `plan.d/`, regenerated bake plan.
**Done When:** the NO-HARDCODE-VERSION gate is green for those four refs and the SBOM carries their resolved pins.
**Why:** hand-pinned AI sidecars go stale silently, so the local inference lanes fall behind upstream fixes with no signal that a bump is due.
**Dep:** AGY-384

## AGY-386 -- Float the data-plane images (pgvector, valkey, ceph)  (WS-LATEST | P1 | S)  **[DONE]**
**Goal:** E-14 Float latest globally -- every artifact class carries floating intent, digests are SBOM output.
**What+How:** Move `docker.io/pgvector/pgvector`, `docker.io/valkey/valkey` and `quay.io/ceph/ceph` to `:latest` intent in `mios.toml` and `plan.d`, with the pgvector `pgNN` suffix handled by pinning the postgres major from an SSOT key while floating the pgvector patch level; capture the resolution via the AGY-384 resolver.
**Where:** `usr/share/mios/mios.toml`, `plan.d/`.
**Done When:** the three refs float and the bake-plan drift gate is green.
**Why:** a stale pgvector or valkey pin is a live security-patch gap in the datastore that backs agent memory.
**Dep:** AGY-384

## AGY-387 -- Float the forge/CI images within their major line  (WS-LATEST | P1 | S)  **[DONE]**
**Goal:** E-14 Float latest globally -- family-channel intent where a bare `:latest` would cross a breaking major.
**What+How:** Point `codeberg.org/forgejo/forgejo:12` and `code.forgejo.org/forgejo/runner:7` at the floating channel for their major (12.x / 7.x) in `mios.toml` and `plan.d`, and let AGY-384 record the resolved tag+digest.
**Where:** `usr/share/mios/mios.toml`, `plan.d/`.
**Done When:** both refs pick up new patch releases within their major automatically and the SBOM records the resolved tag.
**Why:** Forgejo is one of the two equal publishers; a frozen runner tag drifts from the forge it must talk to.
**Dep:** AGY-384

## AGY-388 -- Float the remote-desktop, PXE, search, DNS and trace images  (WS-LATEST | P2 | S)  **[DONE]**
**Goal:** E-14 Float latest globally -- no remaining hand-pinned sidecar tag outside the documented allowlist.
**What+How:** Convert guacamole, guacd, matchbox, searxng, adguardhome and `jaegertracing/all-in-one` to `:latest` intent in `mios.toml`/`plan.d`, with resolution recorded to the SBOM.
**Where:** `usr/share/mios/mios.toml`, `plan.d/`.
**Done When:** all six refs float and the version gate is green.
**Why:** these six are the long tail that keeps the NO-HARDCODE-VERSION gate from ever being turned on hard.
**Dep:** AGY-384

## AGY-389 -- Verify every base and builder image carries a floating channel plus a recorded digest  (WS-LATEST | P2 | S)  **[DONE]**
**Goal:** E-14 Float latest globally -- the `FROM` line is an artifact class like any other; intent floats, digest is SBOM.
**What+How:** Audit and, where needed, correct the base refs: `ghcr.io/ublue-os/ucore-hci:stable-nvidia` (already a channel -- verify), `docker.io/library/rust:slim` for the rust-builder (floated in 0aead5e5 -- verify), `registry.fedoraproject.org/fedora-minimal:latest` for mios-base, and `quay.io/centos-bootc/bootc-image-builder:latest`; ensure each is captured by the AGY-384 digest recording.
**Where:** `Containerfile`, `usr/share/mios/base/Containerfile`, `usr/share/mios/mios.toml`.
**Done When:** every `FROM`/base ref in the tree resolves through a floating channel and appears with a recorded digest in the SBOM.
**Why:** a frozen base image is the largest single source of unpatched CVEs in the shipped OS, and an unrecorded base digest makes the build unreproducible.
**Dep:** AGY-384

## AGY-390 -- Drift-gate: no hand-pinned version literal outside the SBOM  (WS-LATEST | P1 | M)  **[DONE]**
**Goal:** E-14 Float latest globally -- the floated tree cannot silently regress; a pin must be either SBOM-resolved or explicitly allowlisted.
**What+How:** Extend the NO-HARDCODE-VERSION check in `automation/98-drift-checks.sh` so any `:vX.Y.Z` or `@sha256:` literal in active config that is neither present in the SBOM manifest nor listed in `mios.toml [security.nohc_allowlist]` fails the gate; floating intent plus SBOM-recorded resolution becomes the only legal form of pinning. Ship the negative test that injects a hard pin.
**Where:** `automation/98-drift-checks.sh`, negative test, `usr/share/mios/mios.toml [security.nohc_allowlist]`.
**Done When:** the negative test's injected hard pin turns the check RED and the check is green on the fully floated tree.
**Why:** AGY-385..389 are one-time edits; without the gate the next contributor re-pins a tag and the whole float campaign quietly unwinds.
**Dep:** AGY-384..AGY-389

## AGY-391 -- Wire Renovate to auto-bump the SBOM-recorded resolutions  (WS-LATEST | P2 | S)  **[DONE]**
**Goal:** E-14 Float latest globally -- "latest" stays fresh with a reviewable trail rather than by manual sweep.
**What+How:** Configure the existing `renovate.json` so it opens PRs bumping the SBOM-recorded resolutions when upstreams release, scoped to the floated refs and honoring the family-channel majors set in AGY-387.
**Where:** `renovate.json`, CI.
**Done When:** Renovate opens a bump PR for at least one floated ref on an upstream release.
**Why:** without automation, the recorded resolutions age until someone notices, which is exactly the hand-pinning problem in a new shape.
**Dep:** AGY-384

## AGY-392 -- Float or justify every model tag (small/mid/big_ram, embed, vision)  (WS-LATEST | P3 | S)  **[DONE]**
**Goal:** E-14 Float latest globally -- model weights are an artifact class under the same intent-vs-SBOM rule (NS-4).
**What+How:** Review the model ids in `mios.toml [ai.host_thresholds]` and `[ai].vision_grounding_model`; where the registry offers a floating tag, switch to it and record the resolution; where reproducibility genuinely requires a fixed weight, keep the pin and write the rationale inline so the gate's allowlist entry is explained.
**Where:** `usr/share/mios/mios.toml [ai]`.
**Done When:** every model tag is either floated or carries a recorded justification, and the SBOM notes each resolution.
**Why:** unexplained model pins are indistinguishable from forgotten ones, so nobody dares bump them and the AI plane ossifies.
**Dep:** AGY-390

## AGY-393 -- ADR: day-0 float, subsequent SBOM-pin policy  (WS-LATEST | P3 | S)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as gated policy -- the operator directive becomes an immutable, numbered decision (NS-6: ADRs are decisions).
**What+How:** Write the ADR codifying that day-0 builds float to LATEST, the resolved tag+digest is recorded as SBOM data, and subsequent rebuilds may pin FROM the SBOM for reproducibility (`:latest` intent / `:pin` reality); supersede or annotate ADR-0003 accordingly and carry the `laws[]` cross-link.
**Where:** `usr/share/doc/mios/adr/`.
**Done When:** the ADR is merged with a number and status, and the roadmap index regenerates in sync.
**Why:** the float-vs-pin rule currently lives only in chat history, so each new gate or tool re-invents a slightly different policy.
**Dep:** AGY-384, AGY-390

## AGY-394 -- Build `localhost/mios-base` first and wire it into the bake ordering  (WS-CONSOLIDATE | P1 | M)  **[DONE]**
**Goal:** E-17 Shared-base consolidation -- the sidecar fleet collapses onto shared lineages so a stock runner can bake and publish (NS-1).
**What+How:** `usr/share/mios/base/Containerfile` (FROM fedora-minimal) has landed but nothing builds it. Add a bake step in `usr/libexec/mios/mios-bake-group` that builds `localhost/mios-base:latest` BEFORE any service image, and register that ordering in the `plan.d` bake plan so derived images share the base layer and podman/composefs stores it once.
**Where:** `usr/libexec/mios/mios-bake-group`, `usr/share/mios/sys/Containerfile`, `plan.d/`.
**Done When:** `mios-base:latest` builds as the first bake step and a derived image's build log shows it reusing those layers rather than repulling a base.
**Why:** every service image currently drags its own base layers, which is the bulk of the store bloat that keeps GitHub from publishing as an equal.
**Dep:** none

## AGY-395 -- `mios-web`: searxng + firecrawl + crawl4ai as one image FROM mios-base  (WS-CONSOLIDATE | P1 | M)  **[DONE]**
**Goal:** E-17 Shared-base consolidation -- one image, many containers; only the IMAGE collapses, runtime isolation stays Model A.
**What+How:** Create `usr/share/mios/web/Containerfile` deriving FROM `localhost/mios-base` and installing searxng, firecrawl and crawl4ai into a single image, then rewire `usr/share/containers/systemd/mios-searxng.container` and the `mios-webtools-*.container` units to `Image=localhost/mios-web`, keeping each unit's own `User=`/`Group=`/`Delegate=` and activation gate, and drop `docker.io/searxng/searxng` from `plan.d/03-extra.list`.
**Where:** `usr/share/mios/web/Containerfile`, `usr/share/containers/systemd/mios-{searxng,webtools-*}.container`, `plan.d/`.
**Done When:** all three web services start from `localhost/mios-web` and the bound store shrinks by the removed searxng base.
**Why:** three separate web-tool images duplicate the same python/chromium layers, inflating the bake past what a stock runner can hold.
**Dep:** AGY-394

## AGY-396 -- `mios-data`: pgvector + valkey as one image FROM mios-base  (WS-CONSOLIDATE | P1 | M)  **[DONE]**
**Goal:** E-17 Shared-base consolidation -- the data plane rides the shared lineage instead of two upstream bases.
**What+How:** Add `usr/share/mios/data/Containerfile` FROM `localhost/mios-base` installing postgres with the pgvector extension plus valkey, rewire `mios-pgvector.container` and the redis/valkey Quadlets to `Image=localhost/mios-data`, and remove the two upstream refs from the bound image store lists in `plan.d`.
**Where:** `usr/share/mios/data/Containerfile`, `usr/share/containers/systemd/mios-pgvector.container` and the valkey/redis Quadlets, `plan.d/`.
**Done When:** the data services start from the single image and the two upstream images no longer appear in the bound store.
**Why:** pgvector and valkey each pull a full distinct base for a few megabytes of payload.
**Dep:** AGY-394

## AGY-397 -- Rewire the 7 double-baked sidecars to the mios-sys build  (WS-CONSOLIDATE | P2 | M)  **[DONE]**
**Goal:** E-17 Shared-base consolidation -- nothing is both built and pulled; each service has exactly one image lineage.
**What+How:** adguard, forgejo, k3s, open-webui, jaeger, matchbox and searxng currently exist BOTH inside `mios-sys` (built) and in `plan.d/03-extra.list` (pulled). For each, point its Quadlet at the mios-sys/derived build, delete the upstream entry from `03-extra.list`, and verify the service still starts.
**Where:** `usr/share/containers/systemd/*.container`, `plan.d/03-extra.list`.
**Done When:** no service pulls a redundant upstream and seven images disappear from the bound store listing.
**Why:** every double-baked sidecar costs its full image size twice in a bake that is already at the runner's disk ceiling.
**Dep:** AGY-394

## AGY-398 -- `mios-observ`: jaeger + otelcol as one image FROM mios-base  (WS-CONSOLIDATE | P2 | S)  **[DONE]**
**Goal:** E-17 Shared-base consolidation -- the observability sidecars join a shared lineage.
**What+How:** Add `usr/share/mios/observ/Containerfile` FROM `localhost/mios-base` bundling jaeger all-in-one and otelcol, rewire their Quadlets to `Image=localhost/mios-observ`, and drop the upstream refs from `plan.d`.
**Where:** `usr/share/mios/observ/Containerfile`, the observability Quadlets, `plan.d/`.
**Done When:** both observability services run from the single image with no upstream pull.
**Why:** two more standalone bases for a tracing stack that is otherwise tiny.
**Dep:** AGY-394

## AGY-399 -- Tighten the bake-budget gate to block PUBLISH over the runner disk budget  (WS-CONSOLIDATE | P1 | S)  **[DONE]**
**Goal:** E-16 The bake plane: can a stock runner hold it -- the budget becomes a hard publish precondition so GitHub self-publish is possible.
**What+How:** check-66 in `automation/98-drift-checks.sh` already projects baked store size against the 40GB budget. After consolidation, tighten the threshold to a stock GitHub runner's real capacity, make it FAIL the PUBLISH path (not just warn), and have it report the per-image size contribution so the next offender is named.
**Where:** `automation/98-drift-checks.sh`.
**Done When:** the gate prints a per-image size table, passes under budget on the post-consolidation tree, and fails a PUBLISH run whose projected store exceeds the runner budget.
**Why:** without a hard gate the store creeps back over capacity and the publish bake fails late on the runner instead of early in the gate.
**Dep:** AGY-394..AGY-398

## AGY-400 -- Prove the shared-base dedup numerically with a composefs object-sharing report  (WS-CONSOLIDATE | P2 | S)**[DONE]**
**Goal:** E-17 Shared-base consolidation: shrink the sidecar fleet to two lineages -- turns the "podman stores those layers once" claim into a measured fact instead of an assumption.
**What+How:** Extend `automation/77-composefs-verity.sh` with a verification/report step that walks the bound image store's composefs object directory and counts how many objects are REFERENCED by more than one derived image (i.e. the `mios-sys` / `mios-cuda` base layers counted once, not once per sidecar). Emit the shared-object count and the store delta as a report artifact the consolidation doc can cite.
**Where:** `automation/77-composefs-verity.sh`, a report.
**Done When:** the script prints a shared-base object count and that count is asserted `> N`; the run fails if sharing collapses.
**Why:** without a number, a regression that re-forks the base (a sidecar rebuilt FROM a different digest) silently doubles the store and nobody notices until the runner runs out of disk.
**Dep:** none

## AGY-401 -- Decide and record a disposition for every remaining 03-extra sidecar  (WS-CONSOLIDATE | P3 | M)**[DONE]**
**Goal:** E-17 Shared-base consolidation: shrink the sidecar fleet to two lineages -- closes the fleet inventory so no image is left un-triaged against the two-lineage target.
**What+How:** For each of the 14 `03-extra` entries — explicitly including ceph (v19, heavy), guacamole, guacd and BIB — choose exactly one of: derive-from-base (`mios-sys`/`mios-cuda`), keep-upstream with a written justification, or move to the firstboot tier. Evaluate ceph on size before committing it to the bake. Record the choice per entry in plan.d and the docs so the decision is reviewable rather than folklore.
**Where:** `plan.d`, `docs/...`.
**Done When:** every one of the 14 `03-extra` entries carries a documented disposition; none are unlabelled.
**Why:** un-triaged sidecars are exactly the images that quietly blow the stock-runner store budget and block the PUBLISH flip.
**Dep:** none

## AGY-402 -- Document the consolidation map and the before/after store size  (WS-CONSOLIDATE | P3 | S)**[DONE]**
**Goal:** E-17 Shared-base consolidation: shrink the sidecar fleet to two lineages -- gives the consolidation a durable reference so a later agent does not re-fork the lineages.
**What+How:** Author `usr/share/doc/mios/reference/container-consolidation.md` covering the 14->N image map, the shared-base math (which layers are shared and how the saving is computed), the per-service Containerfiles, and measured store size before vs after. Numbers come from the AGY-400 report, not from estimates.
**Where:** `docs/...`, `usr/share/doc/mios/reference/container-consolidation.md`.
**Done When:** the doc is merged and its stated image map and sizes match the actually-built store.
**Why:** the "which base does this sidecar derive from" answer currently lives only in build scripts, so every new sidecar is a coin flip on lineage.
**Dep:** AGY-400, AGY-401

## AGY-403 -- Split the monolithic build.sh RUN into resumable checkpointed layers  (WS-GHSELF | **P1** | L)**[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- makes a bake survivable on ordinary hardware and ordinary interruptions.
**What+How:** `STEP 17/25` currently runs all ~65 stages of `automation/build.sh` inside ONE Containerfile `RUN`, so a mid-bake reboot discards 1-2h of work. Split that into a few checkpointed `RUN` layers along natural stage boundaries (e.g. overlay+packages | services+render | bake+finalize), keeping the numeric stage ordering intact, so podman's layer cache resumes at the last completed checkpoint.
**Where:** `Containerfile`, `automation/build.sh`.
**Done When:** a reboot part-way through a build resumes from the last completed RUN layer instead of restarting at stage 1.
**Why:** every interrupted publish attempt today costs a full rebuild, which is the single largest tax on both the local publish loop and the GitHub runner.
**Dep:** none

## AGY-404 -- Expand firstboot-tier eviction without breaking offline core install  (WS-GHSELF | P2 | M)**[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- shrinks the bound store toward stock-runner capacity while keeping the CORE offline-complete.
**What+How:** The firstboot tier already evicts vLLM+SGLang (~47GB) from the bake. Evaluate moving further heavy optional sidecars (ceph v19, guacamole) into the same firstboot web-pull tier by adding their tokens to `plan.d/firstboot.list` / `mios.toml [build.bake].firstboot_tokens`, with `mios-ai-firstboot` / `mios-bound-images-firstboot` doing the deferred pull. Hard constraint: the CORE must still install with zero egress — only optional sidecars may firstboot-pull.
**Where:** `plan.d/firstboot.list`, `mios.toml [build.bake].firstboot_tokens`, `usr/libexec/mios/mios-ai-firstboot`, `mios-bound-images-firstboot`.
**Done When:** the firstboot-tier invariant check-72 is green and an offline install of the core still succeeds with no network.
**Why:** without further eviction the bound store stays over the runner ceiling, so GitHub can never be an equal publisher.
**Dep:** AGY-401

## AGY-405 -- Flip the GitHub PUBLISH capacity gate once the store fits, with a preflight  (WS-GHSELF | P2 | M)**[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- makes GitHub a real equal publisher alongside Forgejo rather than a capacity-blocked skip.
**What+How:** After AGY-394..404 shrink the store below the runner ceiling, set `PUBLISH` in `.github/workflows/mios-ci.yml` so the GitHub job bakes AND pushes. Add a preflight step that measures free space on `/mnt` against the projected store size and skips the bake gracefully (clean status, explicit reason) rather than failing hard when it still does not fit.
**Where:** `.github/workflows/mios-ci.yml`.
**Done When:** CI bakes and pushes from GitHub when the projected store fits, and exits cleanly with a stated reason when it does not.
**Why:** today GitHub is permanently capacity-gated off, so the GitHub-equals-Forgejo release topology is asserted in docs but not exercised.
**Dep:** AGY-394..AGY-404

## AGY-406 -- Verify Forgejo, GitHub and local publishers emit bit-identical images  (WS-GHSELF | P3 | S)**[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- proves the "equal publishers" claim at the digest level.
**What+How:** Build the same commit on the Forgejo runner (707GB) and on the local + GitHub paths, then compare the resulting manifest digests. Record the comparison as a parity note plus a check so a future divergence surfaces automatically, and update `docs/mios-release-topology.md` with the result.
**Where:** `.forgejo/`, `docs/mios-release-topology.md`.
**Done When:** the same commit yields matching digests across all three publishers, and the parity note/gate records it.
**Why:** if publishers drift, "pull the ref and reproduce the fleet" becomes false and rollback targets differ by whoever happened to build.
**Dep:** AGY-405

## AGY-407 -- Land the Portal FBM status tile that watches first-boot model progress  (WS-FBM | P2 | S)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- gives a shipped-but-invisible capability an operator-visible surface.
**What+How:** Land the FBM-04 `Sidebar.qml` tile currently sitting in `stash@{0}`: it watches `/var/lib/mios/.models-firstboot-progress` and renders provisioning status in the Portal sidebar. Confirm the QML actually parses and that its colors come from the SSOT theme projection (no literal hex), so it stays Law 8 / Law 7 clean.
**Where:** `usr/share/mios/quickshell/Sidebar.qml`, Portal.
**Done When:** the tile renders live provisioning progress and its colors are theme-projected from `mios.toml [colors]`.
**Why:** a fresh install currently pulls model weights with no user-visible signal, so a long first boot is indistinguishable from a hang.
**Dep:** AGY-408

## AGY-408 -- Wire mios-ai-firstboot into [ai].firstboot_models so a fresh install self-provisions  (WS-FBM | P2 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- converts dormant first-boot-model machinery into an actually-wired capability.
**What+How:** `mios-models-firstboot` ships but is dormant because its config is empty. Wire the real path: have `usr/libexec/mios/mios-ai-firstboot` resolve the hardware tier model (reusing the firstboot-VRAM tiering work) and populate `[ai.firstboot_models]` — or invoke `mios-models-firstboot` directly — so first boot provisions the tier GGUF on its own. Degrade-open: a missing tier signal or absent network must not block boot.
**Where:** `usr/libexec/mios/mios-ai-firstboot`, `usr/libexec/mios/mios-models-firstboot`, `mios.toml [ai]`.
**Done When:** a fresh install fetches its tier model with no manual operator step, and still boots cleanly when provisioning is impossible.
**Why:** as shipped, the AI plane comes up with no weights, so the "local inference out of the box" promise requires undocumented manual work.
**Dep:** none

## AGY-409 -- Implement the sha256 verification TODO in mios-models-firstboot  (WS-FBM | P2 | S)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- closes an integrity hole in the one component that writes model weights to disk.
**What+How:** Replace the `# In real scenario we'd check sha256 here` comment in `usr/libexec/mios/mios-models-firstboot` with a real digest check: hash each downloaded GGUF, compare against `[ai.firstboot_models].sha256`, and only then perform the rename into place. On mismatch, discard and retry; degrade-open when no expected sha is declared.
**Where:** `usr/libexec/mios/mios-models-firstboot`.
**Done When:** a deliberately corrupted download is rejected and retried, and never lands under its final filename.
**Why:** an unverified GGUF from a network fetch is loaded straight into the inference lane — a truncated or substituted file is installed silently today.
**Dep:** AGY-408

## AGY-410 -- Extend the `mios models` CLI with add/rm/status over SSOT  (WS-FBM | P3 | M)**[DONE]**
**Goal:** E-11 Unified config surface: mios.toml, the configurator and the Portal are one door -- keeps model management an SSOT edit rather than a side-channel file poke.
**What+How:** Grow `usr/libexec/mios/mios-models` (dispatched from `usr/bin/mios`) beyond `list`/`sync`: `add` and `rm` write/remove an `[ai.firstboot_models]` entry through the shared resolver's user layer, and `status` reads the progress/sentinel file to report provisioning state. Any edit that affects generated manifests must round-trip through `mios-ssot-regen`.
**Where:** `usr/libexec/mios/mios-models`, `usr/bin/mios`.
**Done When:** all four subcommands work end to end and the drift-gate stays green after an `add` followed by `mios-ssot-regen`.
**Why:** without a verb, operators hand-edit TOML or the weights directory, which is exactly the SSOT-bypass class the projection gates exist to prevent.
**Dep:** AGY-408

## AGY-411 -- Support air-gapped provisioning from a vendored GGUF via file:// sources  (WS-FBM | P3 | S)**[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- lets an offline install satisfy its model requirement from image content alone.
**What+How:** Teach `mios-models-firstboot` to accept `[ai.firstboot_models].source = file://.../vendored/models/<name>.gguf` and provision by copy/link instead of download, and wire up the already-present-but-empty `usr/share/mios/vendored/models/` directory so a baked or USB-staged GGUF is a valid source.
**Where:** `usr/libexec/mios/mios-models-firstboot`, `usr/share/mios/vendored/models/`.
**Done When:** an install with only a `file://` source provisions its model with the network fully disconnected.
**Why:** air-gapped and USB deployments currently have no path to a working inference lane, breaking the offline-capable promise.
**Dep:** AGY-408

## AGY-412 -- Audit and harden the coderun sandbox now that code_mode ships enabled  (WS-CODEMODE | **P1** | L)**[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- makes shipping code-execution-on defensible instead of hopeful.
**What+How:** `code_mode.enable` now defaults true, so audit the coderun sandbox against each control and fix every gap found: rootless execution, `no-new-privileges`, dropped capabilities, read-only rootfs, network denied unless the request AND `[code_mode].allow_net` AND deploy context agree, a seccomp profile, and memory/pid/wall-time limits. Record the resulting threat model alongside the config.
**Where:** `etc/mios/containers/coderun-sandbox/Dockerfile`, `usr/libexec/mios/mios-coderun*`, `mios.toml [code_mode]`.
**Done When:** a deliberately hostile snippet cannot reach the network, the host filesystem, or elevated capabilities, and the threat model is written down.
**Why:** with code_mode on by default, any prompt-injected snippet inherits whatever the sandbox fails to deny — this is the security backstop for the whole agent plane.
**Dep:** none

## AGY-413 -- Enforce allow_net as request AND policy, and refuse code_mode on light workers  (WS-CODEMODE | P1 | M)**[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- ensures the declared code_mode policy is what the runtime actually applies.
**What+How:** Verify in the agent-pipe code_mode path that network access is granted only when the snippet requests it AND `[code_mode].allow_net` permits it (logical AND, not either-or), and that `heavy_lane_only` causes a light swarm worker to refuse code_mode outright rather than silently executing. Add tests that assert both behaviours.
**Where:** agent-pipe code_mode path, `test_*`.
**Done When:** tests prove network stays off when either side declines, and that a light-lane worker refuses the request.
**Why:** a policy flag that the runtime does not actually AND is a false sense of containment, and a light worker running code_mode competes with foreground inference for the host.
**Dep:** AGY-412

## AGY-414 -- Cover the in-sandbox mios_tools RPC surface with tests  (WS-CODEMODE | P2 | M)**[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- tests the exact API a sandboxed snippet uses to reach back into the host.
**What+How:** Add a sibling unit test for `coderun-sandbox/mios_tools.py` (identical to the committed `mios-codemode-api.py`, which the bake copies in) exercising `call()`, `web_search()`, `system_status()` and `recall()` — RPC framing on the wire plus `ToolError` handling on failure. The untracked `etc/` copy is a redundant draft; leave it alone and test the canonical file the bake ships.
**Where:** `usr/lib/mios/agent-pipe/test_*`, or a sandbox test.
**Done When:** the RPC surface is covered and the check-11 sibling-test gate is green for this module.
**Why:** this is the only sanctioned hole in the sandbox boundary, and it currently has no test proving malformed frames are rejected.
**Dep:** AGY-412

## AGY-415 -- Test-enforce the code_mode timeout and output cap  (WS-CODEMODE | P2 | S)**[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- proves the two limits that stop a runaway snippet actually fire.
**What+How:** Add tests that run a deliberately non-terminating snippet and assert it is killed at `[code_mode].timeout`, and a snippet emitting excess output and assert stdout is truncated to `[code_mode].max_output_chars`.
**Where:** tests.
**Done When:** both caps are asserted by tests that fail if either limit is removed.
**Why:** an unbounded snippet pins a core and floods the log/context budget — the exact host-starvation class these guardrails exist to prevent.
**Dep:** AGY-412

## AGY-416 -- Emit an auditable log line for every code_mode execution  (WS-CODEMODE | P3 | S)**[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- gives the operator an after-the-fact record of everything the agent plane executed.
**What+How:** On each code_mode run, have agent-pipe write one structured line to the journal / agent-nudger surface carrying the snippet hash, the lane it ran on, the network decision that was applied, and the exit status. Keep it terse and technical (E5-clean) — no prose, no emoji.
**Where:** agent-pipe, log surface.
**Done When:** every execution leaves exactly one fluff-free, greppable audit line containing hash, lane, net decision and exit.
**Why:** without it there is no way to answer "what code did the agent run last night", which makes any incident unreconstructable.
**Dep:** AGY-412

## AGY-417 -- Install real bootc MiOS offline from a USB oci-archive  (WS-DEPLOY | **P1** | L)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- fixes the leg where a MiOS USB currently installs the wrong operating system.
**What+How:** `tools/install.sh` installs plain Fedora today, not the image. Replace that path with `bootc install to-disk --source-imgref oci-archive:/mnt/mios-repo/mios-latest.tar` so the USB provisions the real immutable image with zero registry egress, and make `automation/80-distribution.sh` (the oci-archive producer) emit the archive the installer consumes. ADR-0014 already specifies this — implement it as written.
**Where:** `tools/install.sh`, `automation/80-distribution.sh`, oci-archive producer.
**Done When:** an offline USB boot installs bootc-managed MiOS to disk, and the installed system reports as bootc MiOS rather than plain Fedora.
**Why:** the shipped installer currently produces a machine that is not MiOS at all — a P0-class wrong-artifact bug in the most operator-visible surface.
**Dep:** AGY-422

## AGY-418 -- Connect bootc-image-builder to produce the Anaconda installer ISO  (WS-DEPLOY | **P1** | L)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- yields a real bootable installer built from the published image.
**What+How:** The BIB container is pinned but not invoked. Wire it to build `--type anaconda-iso` (plus raw/qcow2/vhdx) from `ghcr.io/mios-dev/mios`, consuming a kickstart whose source is `ostreecontainer` (no `%packages`), and expose it through the `Justfile` artifact recipes with configs under `bib-configs/` and outputs under `config/artifacts/`.
**Where:** `bib-configs/`, `config/artifacts/`, `Justfile` artifact recipes.
**Done When:** `just build-iso` produces a bootable MiOS installer ISO from the image.
**Why:** without it there is no installer ISO at all, so every deployment depends on hand-staged media.
**Dep:** none

## AGY-419 -- Stage the ISO and oci-archive onto a Ventoy USB that actually installs MiOS  (WS-DEPLOY | P1 | L)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- makes one USB provision any UEFI/BIOS box fully offline.
**What+How:** Extend the existing MiOS-Cat machinery (which builds media but does not install MiOS) to lay down the BIB ISO plus the oci-archive on a Ventoy USB — exFAT data partition + Ventoy ESP — with a `loopback.cfg` menu following the MediCat model, so booting the stick leads straight into the offline install.
**Where:** `cat/`, `installation/`, MiOS-Cat scripts.
**Done When:** a Ventoy USB built by the scripts boots and installs MiOS on both UEFI and BIOS hardware.
**Why:** the media tooling works but stops short of installing anything, which is why the deploy plane is the least-complete area of the roadmap.
**Dep:** AGY-417, AGY-418

## AGY-420 -- Keep Secure Boot intact through shim -> GRUB2 -> signed UKI  (WS-DEPLOY | P2 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- lets operators install on locked-down hardware without disabling Secure Boot.
**What+How:** Ensure the installer chain uses the RH-signed Fedora shim and GRUB2 to hand off to the MiOS signed UKI, and that the MiOS signing key can be enrolled via MOK. Document the `enroll_this_key` flow end to end against the existing UKI/MOK scripts.
**Where:** `automation/76-uki-render.sh`, `enroll-mok.sh`, `generate-mok-key.sh`.
**Done When:** a Secure-Boot-enabled machine boots both the installer and the installed system after MOK enrollment, with no firmware setting changed.
**Why:** requiring Secure Boot off to install disqualifies MiOS from managed and enterprise hardware outright.
**Dep:** AGY-418

## AGY-421 -- Emit vhdx/qcow2 VM artifacts and gate Win11 template parity  (WS-DEPLOY | P2 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- extends the same image to virtual targets so one USB carries every format.
**What+How:** Add BIB outputs for vhdx (Hyper-V/WSL) and qcow2 (libvirt) beside the ISO, landing them in `config/artifacts/`, and verify the Win11 VM template under `usr/share/mios/vm/` is projected from `[vm.win11]` in SSOT rather than hand-maintained.
**Where:** BIB configs, `config/artifacts/`, `usr/share/mios/vm/`.
**Done When:** the produced vhdx and qcow2 both boot, and check-91 (Win11 template projection) is green.
**Why:** without VM artifacts every evaluation requires bare metal, and a hand-maintained Win11 template drifts from SSOT unnoticed.
**Dep:** AGY-418

## AGY-422 -- Unify the oci-archive producer and consumer on one SSOT path  (WS-DEPLOY | P2 | S)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- guarantees the offline installer looks where the publisher actually writes.
**What+How:** Make the publish path write, and the offline installer read, the identical archive location (`/mnt/mios-repo/mios-latest.tar`, covered by check-81), with that path declared once in `mios.toml` and resolved by both sides instead of being spelled literally in each.
**Where:** `pub.sh`/publish, `tools/install.sh`, `mios.toml`.
**Done When:** check-81 is green with producer path == consumer path, and a real archive exists at that path after a publish.
**Why:** two literals for one path is the classic silent-miss: the installer falls back to a registry pull (or fails) on a machine with no network.
**Dep:** none

## AGY-423 -- Scope the Windows/Xbox ISO leg with a working autounattend stub  (WS-DEPLOY | P3 | M)**[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- gives the `xbox`/Windows target of the single dispatcher something real to dispatch to.
**What+How:** Scope and stub the Windows/"Xbox" ISO leg of MiOS-Cat so the USB carries the Windows format too, using the autounattend + SetupComplete `New-LocalUser` baseline described in the PostgresOS-accounts memo (no NSS/PAM on Windows — the account model is provisioned at setup time).
**Where:** `src/autounattend/`, `cat/`.
**Done When:** the Windows leg is scoped in writing and a working autounattend stub is present and parses.
**Why:** the dispatcher advertises a Windows target that currently has no implementation behind it.
**Dep:** AGY-419

## AGY-424 -- Boot the produced ISO in QEMU as a deploy smoke test  (WS-DEPLOY | P3 | M)**[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- puts the deploy leg under the same automated proof as the rest of the tree.
**What+How:** Add a CI/local smoke test that boots the BIB-produced ISO under QEMU and asserts it reaches the installer and completes an install. Keep it hermetic: skip via a sentinel when no KVM is available rather than failing the run.
**Where:** `tests/`, `Justfile`.
**Done When:** the smoke test boots the ISO in QEMU and asserts the installer stage, and skips cleanly on a KVM-less host.
**Why:** installer breakage is otherwise discovered only on real hardware, which is the slowest and least-repeatable feedback loop in the project.
**Dep:** AGY-418

## AGY-425 -- Collapse the install entry points to one shim-less `.bat` and add a global `mios mon` service tracker  (WS-INSTALL | P2 | M)  **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- every target reaches the single guided `mios-install` dispatcher instead of a fan of forwarder scripts.
**What+How:** Reduce the `installation/` entry points to ONE `.bat` that is not a shim but the real Windows entry, handing directly into the guided installer; delete the pure-forwarder scripts it replaces. Add a `mios mon` verb to `usr/bin/mios` that tracks ALL `mios-*` functions and services globally (one monitor surface, not per-service commands). Deletions must not break drift-check 86 (installer-family-roles), which asserts each installer file still fills a declared role, nor the LAYERS documentation bundle that references those filenames.
**Where:** `installation/`, `usr/bin/mios`, the `.bat` entry.
**Done When:** exactly one install entry point remains, `mios mon` lists every mios-* unit, check-86 (installer-family-roles) is green, and `grep` for the deleted shims finds no orphaned references.
**Why:** Multiple near-identical entry points mean an operator can start an install down a path that skips prereq resolution or elevation, and every fix has to be made N times.
**Dep:** none

## AGY-426 -- Prove `build-mios.{ps1,sh}` and `mios-common.{ps1,sh}` byte-identical across both repos (Law 15)  (WS-INSTALL | P2 | M)  **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- the shared installer contract is one artifact, not two drifting copies.
**What+How:** Diff the shared installer files between `C:\MiOS` and `C:\mios-bootstrap` and reconcile them to byte-identical content; land the `build-mios.ps1` currently sitting in `stash@{0}` into BOTH repos rather than one. Add or repair a twin-parity gate in `automation/98-drift-checks.sh` modelled on the existing check-27 twin-parity pattern so the comparison runs mechanically on every gate run.
**Where:** `C:\MiOS`, `C:\mios-bootstrap`, `automation/98-drift-checks.sh`.
**Done When:** the twin-parity check reports green across both repos and flags an injected one-byte edit to either copy.
**Why:** Law 15 exists because a fix landed in one repo silently leaves the other bootstrapping installs from stale code; today nothing detects that divergence.
**Dep:** none

## AGY-427 -- Hand `Get-MiOS -Action Install/Configure` into the unified installer to break the web-door loop  (WS-INSTALL | P3 | S)  **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- the `irm | iex` web door terminates in the guided installer instead of re-entering itself.
**What+How:** In `Get-MiOS.ps1`, make the `Install` and `Configure` actions dispatch into `installation/mios-install.ps1` rather than re-fetching the web door; the current path orphans MiOS-Cat, leaving the caller in a fetch loop with no installer ever invoked. Pass the already-resolved checkout and elevation state forward so the installer does not redo them.
**Where:** `Get-MiOS.ps1`, `installation/`.
**Done When:** running the `irm | iex` web-door flow with `-Action Install` reaches the guided installer prompt in one hop, with no repeated fetch in the transcript.
**Why:** An operator following the documented one-liner never actually reaches an installer, so the advertised install front door does not install.
**Dep:** AGY-425

## AGY-428 -- Number drift-gate checks inline with the scripts under one global 0-99 system  (WS-NUMBER | P2 | L)  **DONE**
**Goal:** E-07 The drift-gate as the enforcement plane -- one globally-identifiable number per pipeline element so OCI layer = stage = step = check.
**What+How:** The pipeline currently runs four independent counters: `automation/NN-*.sh` script prefixes, drift-check `(N)` ordinals, build stages, and OCI layers. Unify them onto the same 0-99 scheme so a check carries the number of the stage it guards, and update the `[NN-name]` log labels emitted by `build.sh` and `98-drift-checks.sh` to print that one number. The scheme must be renumber-immune: inserting a check must not shift the identity of unrelated ones.
**Where:** `automation/98-drift-checks.sh`, `build.sh`, the log labels.
**Done When:** a single numbering scheme spans scripts, stages and checks; gate output shows a check's number matching the stage it guards; inserting a new check leaves existing numbers unchanged.
**Why:** With four counters, a red "(57)" in the log cannot be traced to the build stage that produced the drift without reading the source, which slows every gate failure triage.
**Dep:** none

## AGY-429 -- Gate the unified numbering against duplicates and gaps  (WS-NUMBER | P3 | S)  **DONE**
**Goal:** E-07 The drift-gate as the enforcement plane -- the numbering SSOT is machine-enforced, not maintained by convention.
**What+How:** Add a check to `automation/98-drift-checks.sh` that collects the numbers used by `automation/NN-*.sh` scripts, build stages and drift-checks, and fails on any duplicate or gap in the combined sequence -- so a collision introduced by two branches landing the same number is caught at gate time.
**Where:** `automation/98-drift-checks.sh`.
**Done When:** the check flags an injected duplicate number and an injected gap, and is green on the clean tree.
**Why:** The unified scheme from AGY-428 decays back into four counters the first time two people pick the same free number and nothing objects.
**Dep:** AGY-428

## AGY-430 -- Extend the shared `mios_log` terse numbered format to libexec Python and PowerShell  (WS-LOG | P2 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- one logging surface for the whole pipeline instead of per-language prose.
**What+How:** `usr/lib/mios/log.sh` already gives bash the terse `[NN-name]` label format. Add a Python peer `usr/lib/mios/log.py` for the `usr/libexec/mios/*.py` tools and a PowerShell overlay logger, both emitting the identical `[NN-name]` shape, then repoint consumers off their ad-hoc `print`/`Write-Host` calls. Strip the verbose AI-prose echoes as each consumer is converted.
**Where:** `usr/lib/mios/log.sh`, `usr/lib/mios/log.py`, the PowerShell logger, their consumers.
**Done When:** bash, Python and PowerShell surfaces all emit the same terse numbered label, and a full build log shows one format end to end.
**Why:** Three logging dialects in one build log make it impossible to grep a build for the stage that failed, and the prose lines bury the actual error.
**Dep:** none

## AGY-431 -- Grow the E5 fluff-token lint and extend its scan to libexec and PowerShell  (WS-LOG | P3 | S)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- operator-facing output quality is a gated invariant, not a review habit.
**What+How:** Check-57 in `automation/98-drift-checks.sh` currently bans a short token list (`successfully`, `BAKED IN`, `Done`, trailing `!`) and scans only `automation/*.sh`. Add the remaining filler tokens (`seamlessly`, `robust`, `leverage`, `utilize`, emoji in operator echoes) and widen the scan path to `usr/libexec/mios` and the PowerShell scripts. Do the cleanup pass on the newly-covered files in the same change so the widened lint lands green.
**Where:** `automation/98-drift-checks.sh` check-57.
**Done When:** check-57 with the expanded token list and widened scan path is green repo-wide, and flags an injected `seamlessly` in a libexec file.
**Why:** Two thirds of the operator-visible output is outside the lint's scan path, so the banned prose simply relocated there.
**Dep:** none

## AGY-432 -- Split `server.py` into cohesive sub-800-line modules behind the golden public surface  (WS-LANG | P2 | L)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the agent-pipe god-modules are decomposed under a stable wire surface.
**What+How:** `usr/lib/mios/agent-pipe/server.py` is ~9k lines. Extract cohesive siblings along the natural seams (routes, config, dispatch) so each file lands under the existing 800-line cohesion gate, keeping the exported public surface recorded in `surface.generated.json` unchanged so check-15 (public-surface golden) does not move. Watch for use-before-definition when relocating: module-level ordering has broken the publish bake before.
**Where:** `usr/lib/mios/agent-pipe/server.py` and its new siblings, `surface.generated.json`.
**Done When:** no agent-pipe module exceeds 800 lines, the cohesion gate and check-15 are both green, and the server still imports and serves `/v1/*`.
**Why:** A 9k-line module cannot be reviewed, tested or safely edited by two agents at once, and it is where the injection and NameError classes keep hiding.
**Dep:** none

## AGY-433 -- Convert the remaining eval-on-agent-args verbs to allowlisted dispatch  (WS-LANG | P2 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the eval-on-agent-args injection surface is gone from the verb library.
**What+How:** Finish the campaign the tech-debt map opened: rewrite each `usr/libexec/mios/*` verb that `eval`s agent-supplied arguments to use parameterized invocation against an explicit allowlist of permitted operations, then empty the exemption list that check-43 (eval-safe) currently carries.
**Where:** `usr/libexec/mios/*`, check-43 in `automation/98-drift-checks.sh`.
**Done When:** check-43 is green with an EMPTY allowlist and fails on a reintroduced `eval "$*"` in any libexec verb.
**Why:** Every remaining verb on that list lets an agent-authored argument execute as shell on the host, which is the highest-severity item still open in the debt register.
**Dep:** none

## AGY-434 -- Prototype the compiled-template system that generates twin implementations from one source  (WS-LANG | P3 | L)  **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- structure is generated and validated, so hand-maintained twins cannot diverge.
**What+How:** Design and prototype a template compiler that emits each language's copy of a logically-single tool from ONE template source, and prove it on an existing twin pair (the bash/Rust `ssot-lint` pair named in the tech-debt map is the intended first target). The generated outputs become build products, not files anyone edits.
**Where:** `tools/`, `src/`.
**Done When:** one twin pair is produced entirely from a single template source, and hand-editing a generated output is detected as drift.
**Why:** Every scattered twin pair drifts eventually, and the drift is only discovered when the two implementations disagree about whether the tree is valid.
**Dep:** none

## AGY-435 -- Ratchet shellcheck to a repo-wide warning-level CI gate  (WS-LANG | P2 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- shellcheck is a real CI gate rather than source-directive comments.
**What+How:** The drift-gate lints only MODIFIED scripts at warning level today. Extend `automation/lint-shell.sh` and the CI job to scan the whole repo at warning level with a grandfathered baseline of existing warnings, so pre-existing noise is recorded once and any NEW warning fails. The baseline count only ever goes down.
**Where:** `automation/lint-shell.sh`, CI.
**Done When:** a newly-added script carrying a shellcheck warning fails CI, and the recorded baseline cannot grow.
**Why:** Only touched files are checked now, so an untouched script can carry quoting and word-splitting bugs indefinitely and no gate has an opinion.
**Dep:** none

## AGY-436 -- Rotate the exposed GitHub PAT and move publish auth to a secret store  (WS-SEC | **P0** | M)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- publish credentials are scoped, stored and revocable rather than plaintext on disk.
**What+How:** A classic PAT was pasted in-session and used to authenticate `gh` and the publish. Rotate it (operator action), then purge every on-disk copy -- the VM's `/root/ghtok`, any log capturing it -- and code the plumbing so `pub.sh` reads a FINE-GRAINED token from a secret store via `secrets.env` and `mios.toml [security]`, never from a plaintext file path.
**Where:** `/root/ghtok` (VM), `pub.sh`, `mios.toml [security]`, `secrets.env`.
**Done When:** the old PAT is revoked and returns 401, no on-disk file contains a token, and a publish succeeds reading the scoped token from the secret store.
**Why:** A live classic PAT with broad scope sits in a plaintext file on a VM and in session history; anyone who reads it can push to the published image ref.
**Dep:** none

## AGY-437 -- Prove the cosign policy signs on publish and the install path verifies  (WS-SEC | P2 | M)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the signature chain is closed end to end, not merely configured.
**What+How:** Verify that `automation/49-cosign-policy.sh` actually produces a signature on the published image, and that the install path performs `cosign verify` against the policy before it deploys. Test with a deliberately tampered image and confirm the install refuses it rather than warning.
**Where:** `automation/49-cosign-policy.sh`, the install verify step.
**Done When:** installing a tampered image is BLOCKED with a signature-verification failure, and an untampered image installs.
**Why:** A signing step that is never verified on the consuming side provides no protection at all, only the appearance of one.
**Dep:** AGY-436

## AGY-438 -- Gate SBOM completeness so every baked artifact carries a resolved version  (WS-SEC | P2 | M)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the SBOM plus the baked manifest ARE the reproducibility record.
**What+How:** Add a check that walks the baked binaries, venvs and vendored assets and asserts each appears in `MiOS-SBOM.csv` with a resolved version or digest, failing the build on a missing row. This is the consuming half of WS-LATEST: floating a ref to `:latest` is only safe if the resolved value is recorded here.
**Where:** `automation/90-generate-sbom.sh`, the new check.
**Done When:** the SBOM covers 100% of baked artifacts and the gate fails when a newly-baked binary is added without an SBOM entry.
**Why:** With `:latest` intent everywhere, an SBOM with holes means the image cannot be reproduced or audited for a CVE at all.
**Dep:** none

## AGY-439 -- Audit fapolicyd trust coverage for the new libexec tools and services  (WS-SEC | P3 | S)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- execution trust is enumerated, so nothing shipped is blocked at runtime.
**What+How:** The FBM, code_mode and vendor work added new `usr/libexec/mios` entries. Confirm `automation/40-fapolicyd-trust.sh` enumerates them so fapolicyd permits their execution, and extend the trust generation to derive from the shipped libexec set rather than a hand-listed one.
**Where:** `automation/40-fapolicyd-trust.sh`.
**Done When:** every shipped libexec executable is fapolicyd-trusted, and adding a new one does not require editing the trust list by hand.
**Why:** An untrusted new tool is silently denied execution on a hardened host, so the feature ships and then fails only on the operator's machine.
**Dep:** none

## AGY-440 -- Project the Hyprland + Quickshell liquid-glass shell from `mios.toml`  (WS-ROADMAP | P2 | L)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the desktop shell is a projection of SSOT rather than a hand-edited config.
**What+How:** MiOS already ships the Hyprland and Quickshell infrastructure; what is missing is the config projection. Declare the effects, animations and shell parameters under `mios.toml [desktop]` and render `usr/share/mios/hypr*` and the `quickshell/` config from those keys through the existing render engine, so the blur/rounding/animation values are theme-projected alongside `[colors]`. Add a drift-check asserting the rendered config matches the SSOT.
**Where:** `usr/share/mios/hypr*`, `quickshell/`, `mios.toml [desktop]`.
**Done When:** the shell renders entirely from SSOT keys, `mios-sync-theme` updates it, and the projection drift-check is green and flags a hand-edit to the rendered config.
**Why:** Hand-edited compositor config is the last desktop surface that ignores the SSOT, so an operator's theme change stops at the window borders.
**Dep:** none

## AGY-441 -- Turn the MiOS-Metal split-plane doc into wired vfio and mesh generators  (WS-ROADMAP | P3 | L)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a documented topology becomes SSOT-driven generated config or an explicitly recorded non-goal.
**What+How:** `docs/agy/doc-mios-metal.md` scopes a small bootc hypervisor-router host that binds all dGPUs to `vfio-pci` and owns the NICs, radios, TPM and boot chain, hosting the full MiOS OCI image as a NIC-less super-privileged guest reached over headscale. Convert that prose into stub generators under `usr/libexec/mios/mios-metal-*`: one emitting the vfio-pci bind config from the SSOT device declarations, one emitting the headscale mesh config. Checks 68/69 already exist to gate them.
**Where:** `usr/libexec/mios/mios-metal-*`, `docs/agy/doc-mios-metal.md`.
**Done When:** both generators emit valid config from SSOT and checks 68/69 pass against their output.
**Why:** The split-plane design only exists as prose; nothing in the tree can produce the config it describes, so the north star cannot be tested even on a lab machine.
**Dep:** none

## AGY-442 -- Wire `[accounts].db_backed` to Linux userdb and a Windows baseline  (WS-ROADMAP | P3 | L)  **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- accounts are declared once in SSOT and projected per platform.
**What+How:** `[accounts].db_backed` currently ships inert. Assemble the FOSS-brick model documented in `docs/agy/doc-postgresos-accounts.md`: a systemd userdb JSON projection for Linux, lldap-over-Postgres as the cross-platform SSOT face, and `New-LocalUser` via autounattend/SetupComplete for the Windows baseline (which has no NSS/PAM). Implement the projection in `usr/libexec/mios/mios-account-sync` against the accounts schema.
**Where:** `usr/libexec/mios/mios-account-sync`, the accounts schema, `docs/agy/doc-postgresos-accounts.md`.
**Done When:** one account declared in SSOT produces a valid systemd userdb JSON record on Linux AND a Windows baseline account definition, from a single run of `mios-account-sync`.
**Why:** A flag that reads as a feature but does nothing is worse than an absent one; operators define accounts twice, per platform, by hand.
**Dep:** none

## AGY-443 -- Wire three of the shipped-but-unwired container-runtime features from SSOT  (WS-ROADMAP | P3 | M)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- the doc-vs-SSOT gap closes one capability at a time.
**What+How:** The runtime-architecture map lists capabilities MiOS already ships but never wires: clevis, chrony, ROCm, ceph, mdevctl, freeipa, nut, guacamole, virt-v2v. Pick three, add their operator-tunable keys to `mios.toml`, project their config from those keys in the relevant `automation/*` phase, and add a projection drift-check per feature.
**Where:** the relevant `automation/*` phases, `mios.toml`.
**Done When:** three named features are projected from SSOT keys and each has its own green projection check.
**Why:** Shipped-but-unwired capabilities inflate the image with software no operator can configure, and the docs promise behaviour the build does not deliver.
**Dep:** none

## AGY-444 -- Generalize the dotfiles registry past btop with two more settings surfaces  (WS-ROADMAP | P3 | M)  **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- one engine reads `[dotfiles.registry.*]` and reaches live HOME.
**What+How:** ADR-0010's registry has btop as its single landed proof. Add two further settings surfaces (terminal -- ghostty/foot -- and git are the intended candidates) as `[dotfiles.registry.<surface>]` entries, rendered by the `mios-theme-render`/apply engine to their real per-platform paths, with apply-to-live-HOME and a drift-gate per surface.
**Where:** `mios.toml [dotfiles.registry]`, `mios-theme-render` / the apply verb.
**Done When:** both new surfaces render, apply to a live HOME, and their projection checks are green.
**Why:** With one surface, the registry is an example rather than an engine, and nothing proves the abstraction generalizes before more surfaces depend on it.
**Dep:** none

## AGY-445 -- Extend `mios-ssot-regen` to regenerate ALL SSOT projections in one command  (WS-DRIFT | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- one command re-projects everything the gate checks.
**What+How:** `usr/libexec/mios/mios-ssot-regen` currently regenerates only the 2 AI manifests. Extend it to invoke the remaining drift-gate generators -- `mios-registry`, `generate-bake-plan`, `roadmap-index`, `generate-blade-dropins`, `generate-egress-firewall`, `generate-pod-quadlets` -- so a single run re-projects all 8. Pod-quadlets is digest-sensitive: it must be regenerated in the canonical `MIOS_*` environment and must never emit de-digested refs.
**Where:** `usr/libexec/mios/mios-ssot-regen`.
**Done When:** one `mios-ssot-regen` run regenerates all 8 projections and the full drift-gate is green immediately afterwards, with Quadlet `@sha256` pins intact.
**Why:** Contributors have to know and run six generators in the right environment; missing one is the recurring red-gate class, and getting pod-quadlets wrong strips the digest pins.
**Dep:** none

## AGY-446 -- Add a pre-commit hook that regenerates projections when `mios.toml` changes  (WS-DRIFT | P2 | S)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- stale projections cannot be committed in the first place.
**What+How:** Add a `.githooks/pre-commit` hook that detects staged changes to `mios.toml` (notably the `[verbs.*]` and `[recipes.*]` tables), runs `mios-ssot-regen`, and re-stages the regenerated projections. Wire an install-hook step so the hook is present in a fresh checkout rather than opt-in.
**Where:** `.githooks/`, the install-hook step.
**Done When:** committing a verb change automatically refreshes and stages the manifests, and the drift-gate is green on that commit without a manual regen.
**Why:** This exact drift recurred three times in one session -- a verb lands, its manifests do not, and CI goes red for everyone.
**Dep:** AGY-445

## AGY-447 -- Add negative tests for every check introduced in this batch  (WS-DRIFT | P3 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- every gate proves it can actually FAIL.
**What+How:** The negatives suite ratchets test-count against gate-count, so each new check must arrive with a failing-case test. Add one to `tests/drift-gate-negatives.sh` for each check added in this batch (AGY-378, AGY-390, AGY-399, AGY-429 and the rest), each injecting the specific violation and asserting the check goes red.
**Where:** `tests/drift-gate-negatives.sh`.
**Done When:** the test-count >= gate-count ratchet is green and every new check has a negative test that observably fails when the violation is injected.
**Why:** A check with no negative test may be silently inert -- passing on a broken tree -- which is worse than no check because it reads as coverage.
**Dep:** AGY-429

## AGY-448 -- Bump VERSION past 0.3.0 and define the post-release bump policy  (WS-PUBLISH | **P1** | S)  **[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- work that lands after a bake starts still reaches a published image.
**What+How:** The code_mode, FBM, repos, CLI and firstboot work landed as 0.3.0 AFTER the 0.3.0 bake began, and the already-published guard now refuses to re-publish 0.3.0, so that work ships nowhere. Bump to 0.3.1 (or a date-suffixed tag) in all three places check-42 compares -- `VERSION`, `mios.toml [meta].mios_version`, and `Containerfile ARG MIOS_VERSION` -- and record the chosen bump policy (semver vs date) so the next occurrence is mechanical.
**Where:** `VERSION`, `mios.toml [meta].mios_version`, `Containerfile ARG MIOS_VERSION`.
**Done When:** check-42 (version triple-match) is green at the new version and the next bake publishes carrying the post-0.3.0 commits.
**Why:** Several completed features are currently unpublishable -- the guard blocks the only tag they are labelled with, so they exist in git and in no image.
**Dep:** none

## AGY-449 -- Smoke-verify the published image boots with healthy services before announcing  (WS-PUBLISH | P2 | M)  **[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- publication is gated on the artifact actually running.
**What+How:** After a push, pull the published image, boot it in a VM, and assert the core services reach ready; wire that test into the publish path so a failure blocks the release rather than being discovered by an operator. This is the runtime peer of the bake-time import+exec smoke.
**Where:** `tests/`, the publish path.
**Done When:** a deliberately broken publish is caught by the smoke test before the release is announced, and a good publish passes it.
**Why:** Today the first thing that tests a published image is an operator installing it, so a bad publish is discovered by the person it breaks.
**Dep:** AGY-448

## AGY-450 -- Publish a machine-readable publish-status file beside the publish log so state survives reboots  (WS-PUBLISH | P2 | S)**[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- the resumable publish must be observable across a WSL idle/reboot cycle, not reconstructed by scraping a text log.
**What+How:** `pub.sh` currently only appends to `/mnt/c/mios-publish-build.log`. Add a status writer that emits `/mnt/c/mios-publish-status.json` with the five fields the operator actually needs — `state`, `stage`, resolved-version, image digest, `last_error` — updated at each stage transition, and teach the `mios mon` backend to read that JSON instead of tailing the log. Both copies of `pub.sh` (the `/root` one and the repo copy) must write the same schema.
**Where:** `pub.sh` (the `/root` copy AND the repo copy), the `mios mon` backend.
**Done When:** `cat /mnt/c/mios-publish-status.json | jq .state` returns the live publish state after a host reboot, and `mios mon` renders publish stage/digest without reading the log file.
**Why:** Today publish state is only recoverable by eyeballing a growing log, and a reboot mid-publish leaves the operator unable to tell whether the bake resumed, stalled, or already pushed a digest.
**Dep:** none

## AGY-451 -- Promote the resumable publisher from a hand-placed /root/pub.sh into a repo-tracked, drift-gated unit  (WS-PUBLISH | P3 | M)**[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- the publish path becomes part of the image like every other service, so a rebuilt host reproduces it.
**What+How:** Move the resumable publish logic out of the hand-placed `/root/pub.sh` and into first-class repo artifacts: `usr/libexec/mios/mios-publish` (the executor), `usr/lib/systemd/system/mios-publish.service` (the unit that survives reboot), and the Windows logon-task installer that kicks it. Once tracked, the unit and the libexec tool fall under the normal template/lint/drift checks in `automation/98-drift-checks.sh` like every other `mios-*` unit.
**Where:** `usr/libexec/mios/mios-publish`, `usr/lib/systemd/system/mios-publish.service`, the Windows-task installer.
**Done When:** `git ls-files` lists all three artifacts, `/root/pub.sh` is no longer the source of truth, and `just drift-gate` passes with the new unit included in the unit/quadlet checks.
**Why:** A publish path living only as an untracked script on one machine is unversioned and unreproducible — it cannot be reviewed, gated, or rebuilt onto a fresh host.
**Dep:** AGY-450

## AGY-452 -- Register the day-0-publish / always-latest / consolidation workstreams in the roadmap index  (WS-TESTDOC | P2 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- an arriving agent can pick up a workstream cold because every active workstream is actually listed.
**What+How:** Add `WS-OFFL`, `WS-LATEST`, `WS-CONSOLIDATE` and `WS-GHSELF` as declared workstreams in `TASKS.md`, then re-run `tools/roadmap-index.py` so the generated `ROADMAP.md` index and rollup counts include this batch. The generated file is a projection — regenerate it, do not hand-edit it.
**Where:** `TASKS.md`, `ROADMAP.md`, `tools/roadmap-index.py`.
**Done When:** check-36 (roadmap index in sync) is green in `automation/98-drift-checks.sh`, and `ROADMAP.md` lists all four workstreams with correct task counts.
**Why:** Work that is not in the roadmap index is invisible to the rollup, so this batch's tasks look unowned and the index check goes red on the next regen.
**Dep:** none

## AGY-453 -- ADR: record the publish-survival architecture (WSL idle/reboot resilience)  (WS-TESTDOC | P3 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- decisions become immutable numbered ADRs instead of tribal knowledge in one operator's head.
**What+How:** Write a numbered ADR capturing the four mechanisms that together make a long publish survive: `vmIdleTimeout=-1` in the WSL config, the resumable `pub.sh` staging, the persistent systemd service, the Windows logon task, and the Containerfile layer-split that lets a restarted runner resume. State the decision, the rejected alternatives, and the `laws[]` cross-link.
**Where:** `usr/share/doc/mios/adr/`.
**Done When:** the ADR file exists with the next sequential number and the ADR index regenerates clean under the doc-index drift check.
**Why:** Without the ADR, the next person to hit a stalled publish re-derives (or breaks) the idle-timeout and layer-split design from scratch.
**Dep:** AGY-451

## AGY-454 -- Postmortem doc: why stale SSOT projections kept turning the drift-gate red  (WS-TESTDOC | P3 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- a recurring failure class is written down as a pattern so the fix is understood, not just applied.
**What+How:** Write a short reference postmortem: verb and log-format changes repeatedly landed without regenerating their downstream projections, turning the drift-gate red three separate times; the durable fix is `mios-ssot-regen` plus the pre-commit hook from AGY-446 that auto-regenerates on any `mios.toml` change. Name the failure signature (which checks went red) so it is recognizable next time.
**Where:** `usr/share/doc/mios/reference/`.
**Done When:** the reference doc is merged with a conformant AI-hint header and is cross-referenced from the drift-gate documentation.
**Why:** The same stale-projection failure has already cost three red-gate cycles; undocumented, it recurs on the next verb rename.
**Dep:** AGY-446

## AGY-455 -- Delete the redundant coderun-sandbox mios_tools.py draft under etc/  (WS-CLEAN | P3 | S)**[DONE]**
**Goal:** E-02 Technical-debt retirement -- one file per fact; no shadow copies of a baked artifact loitering in the tree.
**What+How:** `etc/mios/containers/coderun-sandbox/mios_tools.py` is byte-identical to the committed `mios-codemode-api.py` that the bake already copies into place. Confirm the byte-identity (diff the two), then delete the draft or deliberately leave it untracked — the one thing not to do is commit it, because check-34 (etc/ shadow) flags committed `etc/` duplicates of baked content.
**Where:** `etc/mios/containers/coderun-sandbox/`.
**Done When:** the redundant file no longer exists in the tree (or is confirmed untracked), and check-34 is green.
**Why:** A second copy of the code-mode API drifts silently from the real one, and committing it would turn the etc/-shadow gate red.
**Dep:** none

## AGY-456 -- Land or discard the two dangling git stashes so the tree carries no hidden WIP  (WS-CLEAN | P3 | M)**[DONE]**
**Goal:** E-02 Technical-debt retirement -- work is either in main's history or gone; nothing load-bearing hides in a stash.
**What+How:** `stash@{1}` (code_mode) is already fully landed via 60cdf3fa + d010b21f — verify and drop it. `stash@{0}` (FBM) is mostly landed (firstboot VRAM 7bba99d2, `mios` CLI 23c249be) except three pieces: the Sidebar tile (AGY-407), `build-mios.ps1` (AGY-426), and the `TASKS.md` status edits. Land each remaining piece as a real commit with explicit-path staging, then drop the stash.
**Where:** the two git stashes and the files they touch (Sidebar tile, `build-mios.ps1`, `TASKS.md`).
**Done When:** `git stash list` is empty and every hunk that was in a stash is either in main's history or recorded as discarded-with-reason.
**Why:** Stashed work is invisible to review and to the drift-gate; a shared tree with pending stashes loses that work the moment someone resets or reclones.
**Dep:** AGY-407, AGY-426

## AGY-457 -- Verify every status flip in the stashed TASKS.md edit against landed code  (WS-CLEAN | P3 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- the task register reports reality, so the roadmap rollup is trustworthy.
**What+How:** `stash@{0}`'s `TASKS.md` edit marks T-178 done-by-code and flips several FBM tasks to done. Before committing that status change, check each claim against the actual landed commit — read the code path the task names and confirm it exists and runs. Revert any flip that is not backed by code.
**Where:** `TASKS.md`.
**Done When:** every status changed to done in that commit has a named landed commit or file backing it, and no claim survives that a reviewer can falsify.
**Why:** Over-claimed done statuses hide unfinished work behind a green-looking roadmap, and the rollup counts become useless for planning.
**Dep:** AGY-456

## AGY-458 -- Bring the committed-but-unbuilt mios-base Containerfile into gate conformance  (WS-CLEAN | P3 | S)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- every tracked file is subject to the gate, including ones not yet wired into a build.
**What+How:** `usr/share/mios/base/Containerfile` is committed but not yet built (AGY-394 wires it). Because it is tracked, it must already satisfy check-38 — either carry a `RUN bootc container lint` step or a recorded exemption — and must pass template conformance for the Containerfile type. Add whichever it needs and confirm the checks in `automation/98-drift-checks.sh` see it.
**Where:** `usr/share/mios/base/Containerfile`, `automation/98-drift-checks.sh`.
**Done When:** `just drift-gate` passes with check-38 and template conformance both evaluating the base Containerfile rather than skipping it.
**Why:** A tracked Containerfile that no gate evaluates is a blind spot — it will be wired in by AGY-394 and fail the gate at the worst moment, mid-build.
**Dep:** AGY-394

## AGY-459 -- Audit every MIOS_K3S_VERSION consumer after the k3s float so one tag resolves everywhere  (WS-CLEAN | P3 | S)**[DONE]**
**Goal:** E-14 Float latest globally -- SSOT carries version intent only, and no stale pinned literal survives the float.
**What+How:** After AGY-372 floats k3s, grep the repo for every `MIOS_K3S_VERSION` reference and every literal `k3s:v` image tag across the Quadlets, the generated globals, the bake plan and the install docs. Confirm each resolves to the SAME floated tag; delete any lingering `v1.32.1` literal and repoint it at the SSOT key.
**Where:** repo-wide grep of `MIOS_K3S_VERSION` and `k3s:v` — Quadlets, generated globals, bake-plan lists, install docs.
**Done When:** the NO-HARDCODE-VERSION check is green and a repo-wide grep for `v1.32.1` returns nothing outside history/allowlist.
**Why:** A half-floated version means two k3s tags in one image — the Quadlet pulls one binary while the bake plan installs another, and the mismatch only surfaces at runtime.
**Dep:** AGY-372

## AGY-460 -- Feed vendored VERSIONS.txt into MiOS-SBOM.csv so vendored assets appear in the SBOM  (WS-CLEAN | P3 | S)**[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening -- the SBOM is the complete provenance record, covering every artifact class, not just OCI images.
**What+How:** `mios-vendor-refresh` already writes a `VERSIONS.txt` recording each vendored asset and its resolved version. Extend `automation/90-generate-sbom.sh` to parse that file and emit its rows into `MiOS-SBOM.csv` alongside the image entries, using the same column schema so downstream consumers need no special case.
**Where:** `automation/90-generate-sbom.sh`, `MiOS-SBOM.csv`, the `VERSIONS.txt` emitted by `mios-vendor-refresh`.
**Done When:** a build's `MiOS-SBOM.csv` contains a row for each vendored asset in `VERSIONS.txt`, verifiable by row count comparison.
**Why:** Vendored downloads are currently invisible to the SBOM, so "reproducible = SBOM + baked manifest" is false for every vendored asset in the image.
**Dep:** AGY-377, AGY-438

## AGY-461 -- Audit the canonical AI endpoint (:8640 orchestrator / :8642 Hermes) and purge retired :8080 paths  (WS-ROADMAP | P3 | M)**[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- one addressable AI contract with exactly one name, per Law 9.
**What+How:** Grep every consumer of `MIOS_AI_ENDPOINT` and confirm it resolves to the agent-pipe orchestrator at `:8640/v1` with the Hermes leaf at `:8642`, and that no code path has regressed to the retired `:8080`. Any legitimately-retained canonical port literal must be listed in `mios.toml [security.nohc_allowlist]` so the NO-HARDCODE lint passes for the right reason rather than by accident.
**Where:** repo-wide grep for `MIOS_AI_ENDPOINT` and `:8080`, `mios.toml [security.nohc_allowlist]`.
**Done When:** every endpoint consumer resolves the same value, and every surviving `:8080` reference is either gone or explicitly allowlisted with a rationale.
**Why:** A consumer still pointing at the retired `:8080` silently talks to nothing, and an unallowlisted canonical port makes the hardcode lint's output untrustworthy.
**Dep:** none

## AGY-462 -- Finish the legacy AI purge and add a gate that blocks a new ollama/localai/surrealdb lane  (WS-ROADMAP | P3 | M)**[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- a completed removal is only real once a check prevents its return.
**What+How:** Verify ollama, localai and surrealdb are fully absent from the ACTIVE AI plane (which is OpenAI `/v1`-only), distinguishing them from the surviving legitimate `ollama` tokens in enforcement code, cleanup routines, upstream-compat shims and history — those must NOT be re-flagged. Then add a drift-check that fails when a NEW active-config lane referencing any of the three appears in SSOT or Quadlets.
**Where:** repo-wide grep, a new check in `automation/98-drift-checks.sh`.
**Done When:** no active legacy AI lane exists, the new check is green on the current tree, and its negative test proves it goes red when a fake ollama lane is added to `mios.toml`.
**Why:** Without the gate the purge is a one-time cleanup; the next contributor adds an ollama sidecar and the `/v1`-only contract quietly breaks again.
**Dep:** none

## AGY-463 -- Auto-derive the global key library and end the MIOS_AI_VLLM_* vs MIOS_VLLM_* mismatch  (WS-NAME | P3 | L)**[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- one fact, one key name, generated rather than hand-typed.
**What+How:** Replace the ~200 hand-maintained key tuples in `userenv.sh` with a generated minimal global key library derived from the canonical walk over `mios.toml`. In doing so, reconcile the producer/consumer split: the walk emits `MIOS_AI_VLLM_*` while consumers read short `MIOS_VLLM_*` — pick one canonical spelling, repoint consumers BEFORE dropping the other name, and keep both resolver twins (`tools/lib/userenv.sh` and `usr/lib/mios/userenv.sh`) byte-consistent.
**Where:** `tools/lib/userenv.sh`, `usr/lib/mios/userenv.sh`, the resolver twins.
**Done When:** the manual tuple table is gone (generated at build), check-41 and check-45 twin-parity are green, and `mios-env-snapshot` diffs clean against the baseline apart from the intended renames.
**Why:** 200 hand-maintained tuples drift from SSOT on every key addition, and the emitted-vs-consumed name mismatch means some `MIOS_VLLM_*` reads resolve empty at runtime.
**Dep:** none

## AGY-464 -- Reorder mios-sys Containerfile RUN steps static-first to stop cache-busting  (WS-CONSOLIDATE | P3 | M)**[DONE]**
**Goal:** E-17 Shared-base consolidation: shrink the sidecar fleet to two lineages -- the shared base rebuilds cheaply so a standard runner can hold the bake.
**What+How:** Sort the RUN steps in `usr/share/mios/sys/Containerfile` (and the corresponding region of the root `Containerfile`) by change-frequency: the most-static binaries and package installs come FIRST, immediately after the git-to-root overlay, with volatile per-commit content last. This follows the operator directive "core/stationary componentry FIRST" and means a source-only change invalidates only the tail layers.
**Where:** `usr/share/mios/sys/Containerfile`, `Containerfile`.
**Done When:** the layer order places static installs before volatile copies, and a rebuild after a source-only edit reuses cache through the package layers instead of rerunning them.
**Why:** With volatile layers early, every trivial commit busts the whole cache and re-runs the heavy package installs, pushing the bake past what a standard runner can finish.
**Dep:** none

## AGY-465 -- Warm the bake cache for the split resumable layers so a cold runner resumes fast  (WS-GHSELF | P3 | M)**[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- resumable layers actually survive a runner restart.
**What+How:** Building on AGY-403's layer split, add a CI cache-warm step that pulls the base image, the rust layer and the package layer before the main build begins, so a freshly-provisioned runner starts from warm layers rather than from zero. Document the cache keys used so the warm step and the build agree on what they are addressing.
**Where:** the CI workflow, `Containerfile`.
**Done When:** a cold runner reaches `build.sh` without re-executing the base/rust/package layers, visible as cache-hit lines in the CI build log.
**Why:** Without warming, every runner restart re-does the heaviest layers from scratch and the publish bake times out before it reaches the push.
**Dep:** AGY-403

## AGY-466 -- Project the bootc image ref from SSOT into the Anaconda kickstart's ostreecontainer line  (WS-DEPLOY | P3 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- an install produces bootc-managed MiOS, never plain Fedora.
**What+How:** The Anaconda kickstart must install via `ostreecontainer --url ghcr.io/mios-dev/mios` (the bootc image) with zero `%packages`. Rather than hardcoding that URL, project it from `mios.toml [image]` / `MIOS_IMAGE_REF` when the kickstart is generated under `bib-configs/`, and add a drift-check asserting the kickstart's image ref equals the SSOT value.
**Where:** `bib-configs/`, the generated kickstart, `mios.toml [image]`.
**Done When:** the generated kickstart's `ostreecontainer --url` matches `MIOS_IMAGE_REF` exactly, the gate asserting that is green, and the kickstart contains no `%packages` section.
**Why:** A hand-written image ref in the kickstart is the exact failure that installs plain Fedora instead of MiOS — a P0-class shipped-artifact bug waiting to happen on the next image rename.
**Dep:** none

## AGY-467 -- Generate the Ventoy/GRUB loopback multiboot menu from [deploy.artifacts] SSOT  (WS-DEPLOY | P3 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- the offline install media is a projection of SSOT, so adding an artifact needs no hand-edited boot menu.
**What+How:** Write a generator that renders the MediCat-style `loopback.cfg` / GRUB menu entries for the ISO tree directly from `mios.toml [deploy.artifacts]`, one entry per declared artifact, and apply the MiOS theme to the menu chrome from the same SSOT colors. Wire it into `cat/` so the menu is regenerated whenever the artifact list changes.
**Where:** `cat/`, the menu generator, `mios.toml [deploy.artifacts]`.
**Done When:** adding one entry to `[deploy.artifacts]` and regenerating produces exactly one new boot-menu entry, with no hand-edited `loopback.cfg` remaining in the tree.
**Why:** A hand-maintained boot menu silently omits newly added ISOs, so an artifact ships on the USB but is unbootable from the menu.
**Dep:** none

## AGY-468 -- Script the MiOS-Cat USB partition layout (Ventoy + ESP + exFAT MiOS-Repo) idempotently  (WS-DEPLOY | P3 | M)**[DONE]**
**Goal:** E-20 The bootc-native install legs -- offline bare-metal media is reproducible from a script, not from one operator's remembered disk-utility clicks.
**What+How:** Codify the USB scheme — Ventoy MBR/GPT, an ESP (FAT), and an exFAT data partition labelled `MiOS-Repo` as check-88 requires — as an idempotent script that can be re-run on an already-prepared stick without destroying it. Source the label and sizes from `mios.toml [cat.repo_partition]` rather than literals, so the shadow-config partition matches what the installer expects to find.
**Where:** `cat/`, `installation/`, `mios.toml [cat.repo_partition]`.
**Done When:** running the layout script twice on the same stick produces an identical, check-88-green result (correct `MiOS-Repo` label) with no manual partitioning step.
**Why:** Hand-partitioned media is unreproducible and gets the label subtly wrong, which makes the installer fail to find the offline OCI tarball on a machine with no egress.
**Dep:** none

## AGY-469 -- Give every new deploy/vendor live-resource test a skip sentinel so the suite stays hermetic  (WS-TESTDOC | P3 | S)**[DONE]**
**Goal:** E-06 Test and documentation harness -- the test suite runs green on a bare runner, so nobody learns to ignore red.
**What+How:** For each newly added test that touches a live resource — QEMU boot tests, network vendor-pull tests — add the standard skip sentinel so the test declares its requirement ([VM] or [NET]) and skips cleanly rather than failing when the resource is absent. This is what check-76 (hermeticity) verifies.
**Where:** `tests/`.
**Done When:** check-76 is green with the new deploy/vendor tests present, and a full test run on a network-less, VM-less runner reports skips rather than failures.
**Why:** One test that hard-fails without egress turns the whole suite red on every CI run, and a permanently-red suite stops being read.
**Dep:** none

## AGY-470 -- Float the llama-swap image ref (3 copies) to family-latest and record it in the SBOM  (WS-LATEST | P3 | S)**[DONE]**
**Goal:** E-14 Float latest globally -- SSOT carries version intent, the SBOM carries the resolved digest.
**What+How:** `ghcr.io/mostlygeek/llama-swap:cuda` appears three times — across the Quadlets and `plan.d/02-cuda.list`. Replace each with the shared float resolver's family-latest intent, confirm all three resolve to the same tag in one build, and ensure the resolved digest lands in the generated SBOM rather than being pinned in source.
**Where:** the llama-swap Quadlets, `plan.d/02-cuda.list`, the generated SBOM.
**Done When:** all three refs resolve to one identical floated tag in a single build, and that build's SBOM carries the resolved llama-swap digest.
**Why:** Three independently-pinned copies drift apart, so a Quadlet can start a different llama-swap build than the one the bake plan installed.
**Dep:** none

## AGY-471 -- Document that code_mode ships ON by default, plus the operator off-switch and sandbox guarantees  (WS-CODEMODE | P3 | S)**[DONE]**
**Goal:** E-24 Autonomy guardrails: the agent plane cannot starve its own host -- an operator can see and control what the agent plane is allowed to execute.
**What+How:** Write the operator-facing note that `code_mode` now ships enabled by default (per operator directive), how to turn it off (`[code_mode].enable=false` in the user layer, or an `/etc` drop-in), and what the sandbox actually guarantees per AGY-412 — the isolation boundary, what the sandboxed code can reach, and what it cannot. Put it in the shipped doc set and in the release notes for the version that flips the default.
**Where:** `usr/share/doc/mios/`, the release notes.
**Done When:** the docs state the new default, give a copy-pasteable disable snippet, and summarize the sandbox guarantees with a link to AGY-412's implementation.
**Why:** A default-on code-execution feature that operators discover only at runtime is a trust and security surprise, and without a documented off-switch they cannot opt out.
**Dep:** AGY-412

## AGY-472 -- Populate [ai].firstboot_bound_images so the dormant firstboot puller actually pulls the heavy tier  (WS-FBM | P3 | M)**[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- a capability MiOS already ships is either SSOT-wired and gated, or removed with a recorded decision.
**What+How:** `mios-bound-images-firstboot` is shipped but dormant because `[ai].firstboot_bound_images` is an empty list. Populate it with the heavy sidecars deliberately evicted from the bake to the firstboot tier (the vLLM/SGLang-class whales), gated on host capacity so a >=16GB host auto-pulls them at first boot and a smaller host degrades open — skipping the pull rather than failing boot.
**Where:** `mios.toml [ai].firstboot_bound_images`, `usr/libexec/mios/mios-bound-images-firstboot`.
**Done When:** a fresh dGPU host with >=16GB RAM completes firstboot with the heavy sidecar images present in local podman storage, and a smaller host boots successfully with them skipped.
**Why:** With the list empty, the firstboot puller runs and does nothing — the images evicted from the bake never arrive, so the AI lanes are unavailable on a fresh install.
**Dep:** none

## AGY-473 -- Make `mios mon` a single live view of every mios-* unit, timer, firstboot job, agent and publish state  (WS-INSTALL | P3 | M)**[DONE]**
**Goal:** E-21 One deploy front door -- after install there is one surface that tells the operator the whole system's state.
**What+How:** Extend the `mios mon` backend to enumerate and display EVERY `mios-*` systemd unit and timer, the firstboot jobs, the running agents, and the publish status JSON from AGY-450 — in one live-refreshing view, per the operator directive "mios mon GLOBALLY TRACKING ALL MIOS RELATED FUNCTIONS". Discovery must be enumerated at runtime (glob the `mios-*` units), not a hand-listed set that goes stale.
**Where:** `usr/libexec/mios/mios-mon` (or the mon backend), `usr/bin/mios`.
**Done When:** `mios mon` lists every unit matching `mios-*` on the host plus the publish state, and adding a new `mios-*` unit makes it appear with no code change.
**Why:** Today an operator checks health by running several `systemctl` queries by hand and still misses publish state, so a stalled unit goes unnoticed until something downstream breaks.
**Dep:** AGY-450

## AGY-474 -- Tag every pipeline log line with its unified 0-99 number via the shared loggers  (WS-NUMBER | P3 | M)**DONE**
**Goal:** E-07 The drift-gate as the enforcement plane -- one global numbering where OCI-layer = stage = step = check, so a log line is traceable to its gate.
**What+How:** Thread AGY-428's unified 0-99 number through the shared logger in all three language planes (bash, Python, PowerShell) so every emitted line carries its number, then repoint the consumers that currently format their own prefixes. The number is taken from the shared numbering source, not passed as a literal at each call site.
**Where:** the shared bash/Python/PowerShell loggers and their consumers.
**Done When:** a full `mios build` log contains no untagged emitted line, and grepping the log for a given number returns exactly the lines from that script/stage/check.
**Why:** Untagged log lines cannot be traced back to the stage or check that produced them, so diagnosing a red gate means guessing which of 121 automation phases spoke.
**Dep:** AGY-428

## AGY-475 -- Force every `just` artifact recipe to emit into the SSOT output dir (`build/`)  (WS-CLEAN | P3 | S)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- keeps the artifact-output-dir invariant mechanically true instead of convention-only, so no recipe can scatter build products outside the gated tree.
**What+How:** Audit every artifact-producing recipe in `Justfile` (`build-iso`, `build-vhdx`, `vendored-size`, and any recipe added by the 372..478 batch) and repoint its output path at the SSOT-declared output directory that drift-check 90 asserts, rather than a per-recipe hardcoded relative path. Recipes that shell out to a builder must pass the SSOT dir through explicitly so the builder cannot pick its own default.
**Where:** `Justfile`, drift-check 90 (`automation/98-drift-checks.sh`)
**Done When:** check-90 reports green with every artifact recipe honoring the SSOT output dir; running any artifact recipe deposits its product under `build/` and nowhere else.
**Why:** Recipes that write outside `build/` leave untracked artifacts in the work tree, which pollute `git status`, risk being swept into a broad `git add`, and make check-90 a gate that passes only because nobody ran the recipe.
**Dep:** none

## AGY-476 -- Prove no soft-mode or drift-gate bypass path exists in CI or the publish route  (WS-SEC | P3 | S)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- guarantees the enforcement plane cannot be silently switched off by anything on the path that produces a published image.
**What+How:** Re-verify drift-check 78 (soft-mode / bypass detection) against the full CI workflow and the day-0 publish path, including the persistent service and the Windows scheduled task that publish added; confirm neither can set a soft-mode override env var, skip flag, or `|| true` swallow that would let `98-drift-checks.sh` report success on a red tree.
**Where:** CI workflow (`.github/workflows/mios-ci.yml`), the publish path, drift-check 78 in `automation/98-drift-checks.sh`
**Done When:** check-78 is green and a grep of CI + publish path yields zero soft-mode overrides or gate-skip branches.
**Why:** A single bypass switch reachable from the publish path means a red tree can still ship an image, which turns every other law and check in the repo into decoration.
**Dep:** none

## AGY-477 -- Close out the FOSS adopt-now sweep: land or explicitly defer each upstream  (WS-ROADMAP | P3 | L)  **[DONE]**
**Goal:** E-20 The bootc-native install legs -- the load-bearing adopt-now item is offline `bootc install --transport oci`, which is what lets a blank machine be provisioned with zero egress.
**What+How:** Walk the FOSS-upstream map's adopt-now column and, for each entry not yet wired, either land it or record an explicit deferral with a reason: offline `bootc install --transport oci` (AGY-417), MCP gateway configuration, the Quickshell desktop shell (AGY-440), plus the bootc/BIB, Ventoy and OpenAI /v1 backend entries. Note Bluefin/Bluespeed and elizaOS as comparators in the doc rather than as adoptions.
**Where:** `docs/mios-foss-upstream-map`, plus the install, AI-plane and desktop-shell lanes each item touches
**Done When:** every adopt-now row in `docs/mios-foss-upstream-map` reads either "landed (with the commit/task that landed it)" or "deferred: <reason>" -- no row is left unannotated.
**Why:** An unresolved adopt-now list is re-scouted every few weeks at full cost, and the offline-install item in particular is the one gap keeping bare-metal provisioning dependent on external egress.
**Dep:** AGY-417, AGY-440

## AGY-478 -- Hold the 372..478 batch drift-green on every commit as it lands  (WS-TESTDOC | P2 | M)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- keeps the negative-test suite and the main gate both authoritative during a 107-task landing, so main is never a tree nobody can trust.
**What+How:** Meta-task: after each task in AGY-372..478 lands, run `just drift-gate` plus the negatives suite (`tests/drift-gate-negatives.sh`) and fix red before the next task starts. Treat any regression introduced mid-batch as a stop-the-line item rather than deferred cleanup.
**Where:** whole tree; `automation/98-drift-checks.sh`, `tests/drift-gate-negatives.sh`
**Done When:** the batch is fully landed and `just drift-gate` plus the negatives suite are green at every commit on main across the range -- not merely at the end.
**Why:** In a shared tree that AGY commits into concurrently, a red gate left overnight compounds: the next task builds on a broken baseline and the eventual bisect spans a hundred commits.
**Dep:** none

## AGY-479 -- `mios-env-snapshot`: the ground-truth capture for every lossless refactor  (WS-UNIFY | **P0** | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- provides the measuring instrument without which no namespace-collapse step can be proven lossless.
**What+How:** Add `usr/libexec/mios/mios-env-snapshot` (bash, AI-hint header, `set -e`) that sources the resolver in a deliberately clean environment and prints `env | grep ^MIOS_ | sort`. Clean-env sourcing is the point: inherited `MIOS_*` from the caller's shell would contaminate the baseline and make diffs meaningless.
**Where:** `usr/libexec/mios/mios-env-snapshot` (new)
**Done When:** template-conformance passes for the new file and two consecutive runs of `mios-env-snapshot` produce byte-identical output.
**Why:** Without a deterministic capture, every GUP step is an unverifiable claim -- you cannot tell an intended key drop from an accidental value change.
**Dep:** none

## AGY-480 -- `check_resolved_env_lossless`: gate the resolved env against a committed baseline  (WS-UNIFY | **P0** | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- turns the losslessness invariant into a mechanical check so the 2523-key collapse cannot lose a value in passing.
**What+How:** Commit `usr/share/mios/reference/env-baseline.txt` (the `mios-env-snapshot` output) and add a drift-check that re-snapshots and diffs against it, FAILing on any drift not accompanied by a deliberate `MIOS_ENV_BASELINE_BUMP` marker in the commit. Register the check in `main()`, add its row to `drift-gate-index.tsv`, and add a negative test that injects a value change.
**Where:** `automation/98-drift-checks.sh`, `drift-gate-index.tsv`, `tests/drift-gate-negatives.sh`, `usr/share/mios/reference/env-baseline.txt`
**Done When:** the check is green on the current tree and the negative test proves it goes red when a resolved value is injected/changed.
**Why:** Every subsequent GUP task rewrites resolver logic; without this gate a silently dropped or altered key surfaces later as a broken service with no trace back to the commit that caused it.
**Dep:** AGY-479

## AGY-481 -- Commit the verified AGY vendoring/tools work to give GUP a clean base  (WS-UNIFY | **P0** | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- a dirty tree makes the lossless diff unreadable, so Phase 0 starts from a committed, gate-green baseline.
**What+How:** Commit the already-verified uncommitted work using explicit paths (never `git add -A`, since the tree is shared): vendored k3s v1.36.2 (binary + sha + install + SELinux bits), `mios-resolve-latest`, `mios-vendor-refresh`, the `mios-web`/`mios-data` Containerfiles, drift-check 87 plus its negative test, `terra.key` and the `terra.repo` `file://` gpgkey, `VERSIONS.txt`, and the `MiOS-SBOM.csv` / `Justfile` / `names.generated` / OFFLINE-FIRST updates. Verify each passes the gate before staging.
**Where:** the listed files across `usr/libexec/mios/`, `automation/98-drift-checks.sh`, `tests/`, `Justfile`, `VERSIONS.txt`, `MiOS-SBOM.csv`
**Done When:** `git status` is clean and `just drift-gate` is green.
**Why:** Uncommitted work mixed into a namespace refactor makes every `env` diff ambiguous, and in a tree shared with a concurrent agent it is one broad `git add` away from being clobbered.
**Dep:** none

## AGY-482 -- Commit the coordinated k3s v1.32.1 -> v1.36.2 bump across all projections  (WS-UNIFY | **P0** | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- makes the k3s version one fact rather than three, ahead of the derive work that assumes tag and version already agree.
**What+How:** Commit the coordinated bump already applied to `mios.toml`, the `plan.d/03-extra.list` entry, and the `mios-k3s.container` Quadlet; then regenerate any other projection that carries a k3s version or ref so no stale 1.32.1 literal survives anywhere downstream.
**Where:** `usr/share/mios/mios.toml`, `plan.d/03-extra.list`, the `mios-k3s.container` Quadlet
**Done When:** the vendored k3s binary tag equals the image tag everywhere, and the bake-plan and Quadlet drift-checks are green.
**Why:** A version that disagrees between the vendored binary, the plan list and the Quadlet gives a host two different k3s versions depending on which surface it reads -- and it would make the Phase-2 derive look like a value change rather than a no-op.
**Dep:** AGY-481

## AGY-483 -- Wire the lossless gate into CI and a pre-commit hook  (WS-UNIFY | P1 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- a check that only runs when someone remembers to run it is not enforcement; this makes the lossless invariant unavoidable at both commit and merge time.
**What+How:** Add `check_resolved_env_lossless` to the CI workflow so no PR merges with a changed resolved env, and install a git pre-commit hook under `.githooks/` that runs the same check locally so the failure is caught before the commit exists rather than after the push.
**Where:** `.github/workflows/mios-ci.yml`, `.githooks/`
**Done When:** a deliberately value-changing commit is rejected by the local hook AND fails the CI job -- both legs demonstrated, not just one.
**Why:** GUP spans hundreds of tasks; relying on the author to remember one manual command guarantees that the one forgotten run is the one that drops a key.
**Dep:** AGY-480

## AGY-484 -- Document the lossless-diff refactor method  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- so an arriving agent can pick up a GUP workstream cold and refactor safely on the first attempt.
**What+How:** Write a short reference doc covering the capture -> refactor -> diff-empty loop, what the `MIOS_ENV_BASELINE_BUMP` marker means and when it is legitimate, and the correct procedure for adding or renaming a key without tripping the gate.
**Where:** `usr/share/doc/mios/reference/`
**Done When:** the doc is merged, AI-hint-headed and template-conformant, and is reachable from the GUP reference doc.
**Why:** Undocumented, the method is re-derived (usually wrongly) by whoever picks up the next task, and the baseline gets bumped to silence a real regression.
**Dep:** AGY-480

## AGY-485 -- Audit every dead-empty `MIOS_*_VERSION` key and produce the drop-list  (WS-UNIFY | P1 | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- identifies the keys that can be deleted with provable zero risk, so Phase 1 starts with the free wins.
**What+How:** From `env-baseline.txt`, enumerate every `MIOS_*=` line with an empty value (`MIOS_EMB_VERSION`, `MIOS_AGENT_PASSPORT_VERSION`, `MIOS_AGNTCY_OASF_VERSION`, and the rest), then grep the whole tree for consumers of each. Produce a drop-list of the zero-consumer keys with the per-key consumer count recorded as the evidence.
**Where:** `usr/share/mios/reference/env-baseline.txt`, tree-wide grep -> a task note
**Done When:** a drop-list exists naming each candidate key with its measured consumer count, and every count is zero.
**Why:** Empty-valued keys still cost a line in every resolver twin and read as configurable to anyone browsing the SSOT; deleting them without a consumer census is how a live reference gets broken.
**Dep:** AGY-479

## AGY-486 -- Drop the dead `MIOS_EMB_VERSION` key  (WS-UNIFY | P1 | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- the first proof that a key can leave the namespace with a diff showing exactly one intended line.
**What+How:** With zero consumers confirmed by AGY-485, remove the source key and its alias from `mios.toml` and from both resolver twins so the Python and bash resolvers stay equivalent.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/mios/mios_toml.py`, `tools/lib/userenv.sh`
**Done When:** the env diff shows exactly one removed line (`MIOS_EMB_VERSION`) and the twin-equivalence check stays green.
**Why:** Left in place, a dead version key invites someone to "fix" its empty value by inventing a number that nothing reads.
**Dep:** AGY-485

## AGY-487 -- Drop the dead `MIOS_AGENT_PASSPORT_VERSION` and `MIOS_AGNTCY_OASF_VERSION` keys  (WS-UNIFY | P1 | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- continues the zero-consumer sweep on the same proven pattern.
**What+How:** Apply the AGY-486 procedure to both remaining empties: remove the source keys and their aliases from `mios.toml` and both resolver twins in one change.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/mios/mios_toml.py`, `tools/lib/userenv.sh`
**Done When:** the env diff shows exactly those two removed lines and nothing else.
**Why:** Same as AGY-486, doubled -- and these two carry vendor-sounding names that make them look load-bearing to a newcomer.
**Dep:** AGY-485, AGY-486

## AGY-488 -- Collapse the `MIOS_CONVERGE_*` / `MIOS_CONV_*` alias-dupe to one canonical name  (WS-UNIFY | P1 | S)  **[DONE]**
**Goal:** E-09 One value, one name -- one fact must have exactly one env name (Law 9), and this pair is the same value emitted twice.
**What+How:** `MIOS_CONVERGE_IMAGE_RECHUNK_FORMAT_VERSION` and `MIOS_CONV_...` carry an identical value. Keep `MIOS_CONV_*` as canonical per the existing `converge.*` alias rule, repoint every consumer of the long form first, then delete the duplicate alias from the `get_aliases` handler and its bash twin -- consumers before the drop, never the reverse.
**Where:** `usr/lib/mios/mios_toml.py` (`get_aliases`, `converge.*`), `tools/lib/userenv.sh`, the consumers
**Done When:** one canonical name remains and the env diff shows only the dropped alias line.
**Why:** Two names for one value means half the tree reads one and half reads the other, so a change to the SSOT appears to take effect only in some surfaces.
**Dep:** AGY-480

## AGY-489 -- Collapse the `MIOS_WEBTOOLS_*` / `MIOS_CRAWL4AI_*` alias-dupe  (WS-UNIFY | P1 | S)  **[DONE]**
**Goal:** E-09 One value, one name -- removes a resolver rule that MINTS duplicates rather than merely tolerating them.
**What+How:** The `services.webtools` alias rule emits both `WEBTOOLS_` and `CRAWL4AI_` forms for USER, UID and GID. Choose one canonical prefix, repoint every consumer, then remove the duplicate emission from the rule itself in both twins so the dupe cannot reappear.
**Where:** `usr/lib/mios/mios_toml.py` (`services.webtools`), `tools/lib/userenv.sh`, the consumers
**Done When:** only the canonical prefix is emitted and the env diff is clean apart from the dropped names.
**Why:** A UID/GID that exists under two names is a real ownership hazard -- a unit reading one name and a tmpfiles rule reading the other silently diverge if only one is updated.
**Dep:** AGY-480

## AGY-490 -- Script the full alias-dupe sweep across the whole `MIOS_*` namespace  (WS-UNIFY | P1 | M)  **[DONE]**
**Goal:** E-09 One value, one name -- converts dedup from ad-hoc spotting into a measured, repeatable detection.
**What+How:** Write the detector: group `env-baseline.txt` entries by value AND by originating dotted SSOT path, then flag any value emitted under two or more names that is not on the deliberate compat-alias allowlist. Grouping on origin as well as value is what keeps coincidental value collisions (two ports that happen to share a number) out of the report.
**Where:** `usr/share/mios/reference/env-baseline.txt`, analysis script
**Done When:** the detector runs and emits the complete alias-dupe list, with deliberate compat aliases excluded rather than hand-filtered afterwards.
**Why:** Without a detector, dedup stops at the dupes someone happened to notice, and the ones left behind are exactly the ones nobody is looking at.
**Dep:** AGY-479

## AGY-491 -- Collapse every remaining alias-dupe found by the sweep  (WS-UNIFY | P1 | M)  **[DONE]**
**Goal:** E-09 One value, one name -- takes the measured duplication list to zero.
**What+How:** For each flagged pair, pick the canonical name, repoint consumers, then drop the duplicate -- batched by SSOT section so each commit has a reviewable blast radius and a readable env diff.
**Where:** `usr/lib/mios/mios_toml.py`, `tools/lib/userenv.sh`, the consumers
**Done When:** the env diff shows only the intended dropped names, the twin-equivalence check stays green, and re-running the AGY-490 detector returns an empty list.
**Why:** Partially-collapsed duplication is worse than none: the surviving alias looks canonical, so new code adopts the name that is scheduled for deletion.
**Dep:** AGY-490

## AGY-492 -- `check_no_duplicate_value_key`: keep the namespace collapsed permanently  (WS-UNIFY | **P1** | M)  **[DONE]**
**Goal:** E-09 One value, one name -- the named gate that makes the de-duplication campaign irreversible rather than a one-time cleanup.
**What+How:** Add a drift-check that fails when two SSOT keys carry the same value under a single-source concept (for example one version or image ref expressed in two keys), excluding an explicit compat-alias allowlist. Register it in `main()` and `drift-gate-index.tsv`, and add a negative test that injects a duplicate.
**Where:** `automation/98-drift-checks.sh`, `tests/drift-gate-negatives.sh`, `drift-gate-index.tsv`
**Done When:** the check is green on the cleaned tree and the negative test proves it goes red on an injected duplicate.
**Why:** Every dupe collapsed in AGY-486..491 comes back the first time someone adds "just one more" convenience alias, and the cleanup cost is paid again from scratch.
**Dep:** AGY-491

## AGY-493 -- Reconcile mismatched `[image.sidecars]` version/tag pairs BEFORE deriving  (WS-UNIFY | **P1** | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- version keys derive from image-ref tags, which is only lossless if tag and version already agree.
**What+How:** Find every `[image.sidecars]` entry where `X_version` disagrees with the tag of the ref `X` (the known case: `cuda_version = latest` against a `:cuda` ref). For each, decide which side is the truth -- normally the ref tag -- and set `X_version` to match, or correct the ref if the ref is the wrong one.
**Where:** `usr/share/mios/mios.toml` `[image.sidecars]`
**Done When:** for every sidecar, `X_version` equals the tag of `X`, so a subsequent derive produces a byte-identical env.
**Why:** Deriving on top of a mismatch turns a silent pre-existing inconsistency into a visible value change in the lossless diff, which either blocks Phase 2 or gets waved through with a baseline bump that hides a real bug.
**Dep:** AGY-480

## AGY-494 -- Resolver twin A: derive `MIOS_X_VERSION` from the image ref (`mios_toml.py`)  (WS-UNIFY | **P1** | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- makes the image ref the single source for its own version, one of the named GUP derivations.
**What+How:** In the `image.sidecars` handler, when a non-`_version` key `X` holds a full image ref, additionally emit `MIOS_<X>_VERSION` set to the tag of that ref, while keeping `MIOS_<X>_IMAGE` as-is. Turn the explicit `_version` key handling into a no-op so the keys can be deleted in AGY-496 without changing behavior.
**Where:** `usr/lib/mios/mios_toml.py`
**Done When:** with the `_version` keys still present in `mios.toml`, the env diff is empty -- proving the derived value matches the declared one exactly.
**Why:** Until the version derives, a sidecar bump requires editing two keys, and the pair drifts the first time only one of them is touched (the cuda case is that bug, already live).
**Dep:** AGY-493

## AGY-495 -- Resolver twin B: mirror the same derive in `userenv.sh`  (WS-UNIFY | **P1** | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- the Python and bash resolvers are twins; a derive that lands in only one splits the SSOT in half.
**What+How:** Implement the AGY-494 derivation in the bash twin so both resolvers emit identical `MIOS_<X>_VERSION` values from the same refs, keeping the check-45 twin-equivalence contract intact.
**Where:** `tools/lib/userenv.sh`
**Done When:** check-45 twin-equivalence is green and the env diff is empty from both resolvers.
**Why:** Bash-sourced surfaces (units, build scripts) and Python-sourced surfaces (the AI plane, the Portal) would otherwise resolve different versions for the same sidecar -- the hardest class of bug to see.
**Dep:** AGY-494

## AGY-496 -- Delete every `X_version` key from `[image.sidecars]`  (WS-UNIFY | **P1** | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- the actual namespace reduction: the duplicate declaration disappears now that derivation supplies it.
**What+How:** With the derive live in both twins, remove every `X_version` key from the section: k3s, ceph, forge, forge_runner, searxng, adguard, guacamole, guacd, jaeger, matchbox, open_webui, pgvector, valkey, vllm, sglang, cuda, sys and the remainder.
**Where:** `usr/share/mios/mios.toml` `[image.sidecars]`
**Done When:** no `_version` key remains in the section, and the env diff is empty because every `MIOS_X_VERSION` still resolves -- now derived rather than declared.
**Why:** Every surviving `_version` key is a second place to edit and a second place to forget, which is precisely how the cuda mismatch in AGY-493 came to exist.
**Dep:** AGY-494, AGY-495

## AGY-497 -- Verify the 12 `MIOS_K3S_VERSION` consumers resolve unchanged  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- targeted verification of the derived key with the largest blast radius.
**What+How:** Enumerate all 12 consumers of `MIOS_K3S_VERSION` and confirm each resolves the same value after the derive as before it, rather than trusting the aggregate env diff alone for the key that feeds the vendored binary, the install path and the Quadlet.
**Where:** the 12 identified consumers of `MIOS_K3S_VERSION`
**Done When:** all 12 consumers are checked and report the pre-derive value unchanged.
**Why:** k3s spans the vendored binary, the plan list and the container ref; a wrong derive here installs a binary that does not match the image and only fails at cluster start, far from the commit.
**Dep:** AGY-496

## AGY-498 -- Apply the same single-source derive to `[containers.*]` image refs  (WS-UNIFY | P2 | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- extends the proven derive so the pattern covers both image-declaring sections, not just sidecars.
**What+How:** `[containers]` emits `MIOS_CONTAINER_X_IMAGE`; audit the section for the same version/ref duplication that `[image.sidecars]` carried, then derive the version from the ref and collapse the redundant keys in both resolver twins using the AGY-494/495 pattern.
**Where:** `usr/share/mios/mios.toml` `[containers]`, `usr/lib/mios/mios_toml.py`, `tools/lib/userenv.sh`
**Done When:** container refs are single-source (no separate version key) and the env diff is clean.
**Why:** Leaving `[containers]` un-derived means the same two-places-to-edit bug persists in the section that defines what actually runs on a host.
**Dep:** AGY-496

## AGY-499 -- Compose `[build.bake].core` from `[image.sidecars]` plus a `local_builds` list  (WS-UNIFY | **P1** | M)  **[DONE]**
**Goal:** E-16 The bake plane -- the core allow-list becomes a generated view of the image declarations instead of a hand-maintained second copy of every sidecar ref.
**What+How:** Split `[build.bake].core` into a generated sidecar-ref set composed from `[image.sidecars]` plus an explicit `local_builds` list holding only the `localhost/mios-*` locally-built images; teach `mios-bake-group` to read the union of the two rather than a literal list.
**Where:** `usr/share/mios/mios.toml` `[build.bake]`, `usr/libexec/mios/mios-bake-group`
**Done When:** no sidecar ref literal remains in `[build.bake]`, and both the env diff and the bake-plan diff are clean against the pre-change baseline.
**Why:** A hand-listed core means adding or renaming a sidecar in `[image.sidecars]` silently drops it from the bake -- the image publishes successfully while missing a component nobody notices until runtime.
**Dep:** AGY-496

## AGY-500 -- Generate 03-extra.list from the composed [image.sidecars] set so no second ref literal exists  (WS-UNIFY | **P1** | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- every image ref is declared exactly once in mios.toml and every consumer derives from it.
**What+How:** Make `tools/generate-bake-plan.py` emit the `03-extra` bake-plan entry purely from the composed sidecar set it already resolves out of `[image.sidecars]`, and delete the hand-maintained `03-extra.list` ref rows it was duplicating. No literal image ref may survive in `plan.d/`; the file becomes a pure generator output.
**Where:** `tools/generate-bake-plan.py`, `plan.d/`, `usr/share/mios/mios.toml` (`[image.sidecars]`)
**Done When:** drift-check 35 is green and grepping the tree for the ref returns exactly one occurrence (the `[image.sidecars]` declaration); regenerating the plan produces no diff.
**Why:** two writable homes for the same ref means a version bump in mios.toml silently leaves the bake pulling the old image, and the bake plan drifts from SSOT with no gate catching it.
**Dep:** none

## AGY-501 -- Regenerate the k3s and all sidecar Quadlets from SSOT under the canonical MIOS_* env  (WS-UNIFY | **P1** | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- rendered runtime units are a projection of mios.toml, never hand-edited state.
**What+How:** Re-run `tools/generate-pod-quadlets.py` with the canonical MIOS_* environment loaded so every rendered `Image=` line derives from `[image.sidecars]` rather than a stale inline value, and stage the regenerated units explicitly so the build-resolved `@sha256` pins are preserved (never de-digest, never blanket `git add`).
**Where:** `tools/generate-pod-quadlets.py`, `usr/share/containers/systemd/*`
**Done When:** drift-check 13 (pod-quadlets) is green and every `Image=` in `usr/share/containers/systemd/` matches the SSOT-composed ref with its digest intact.
**Why:** the recurring Quadlet-digest-drift class turns the gate red on unrelated commits and, worse, boots services from an image the SSOT no longer names.
**Dep:** none

## AGY-502 -- Audit mios.toml for any full image ref living outside [image.sidecars]  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- one section owns image identity; everything else references or derives.
**What+How:** Grep `mios.toml` for full registry/repo/tag-shaped strings, and for each hit outside `[image.sidecars]` either repoint it at the sidecar key (reference) or express it as a derivation; a re-literal is never acceptable.
**Where:** `usr/share/mios/mios.toml`
**Done When:** a full-ref grep over mios.toml hits only inside `[image.sidecars]`.
**Why:** a second ref inside the SSOT file itself is the hardest duplicate to spot and defeats the entire "bump one place" contract.
**Dep:** none

## AGY-503..AGY-510 -- Rewire the seven double-baked services onto their derived image, one service per task  (WS-CONSOLIDATE | P2 | L)  **[DONE]**
**Goal:** E-17 Shared-base consolidation -- shrink the ~18-image sidecar fleet onto shared lineages so a standard runner can bake and publish.
**What+How:** One task each for adguard(503), forgejo(504), k3s(505), open-webui(506), jaeger(507), matchbox(508), searxng(509), plus a verification task(510). Per service: point its Quadlet at the mios-sys/derived image, remove the now-redundant upstream ref from the `03-extra` bake set in `plan.d/`, and confirm the unit still starts under its existing `User=`/`Group=`/`Delegate=` (runtime Model A is unchanged — only the image collapses). 510 verifies the whole set together.
**Where:** `usr/share/containers/systemd/*`, `plan.d/`
**Done When:** each of the seven services starts from the single derived image, its upstream ref no longer appears in the bake plan, and `podman system df` shows the local store shrunk after the set lands.
**Why:** baking both the upstream image and the derived one doubles pull time and store size and is a direct cause of the bake exceeding standard-runner capacity.
**Dep:** none

## AGY-511 -- Project mios-knowledge-graph.json from mios.toml instead of hardcoding versions  (WS-UNIFY | **P1** | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- derived surfaces are regenerated projections, not parallel copies of SSOT facts.
**What+How:** `mios-knowledge-graph.json` embeds literals such as `MIOS_K3S_VERSION` and image refs; replace them with generated values by adding a projector for the knowledge graph (or extending `mios-ssot-regen` to cover it) that reads the resolved SSOT and writes the file.
**Where:** `usr/share/mios/knowledge/mios-knowledge-graph.json`, its generator, `mios-ssot-regen`
**Done When:** bumping a version in mios.toml and re-running the regen updates the graph automatically; no version or ref literal remains in the JSON.
**Why:** the knowledge graph is what agents read to reason about the system — stale literals there teach every agent the wrong system shape.
**Dep:** none

## AGY-512 -- Project image versions into the configurator mios.html at generation time  (WS-UNIFY | **P1** | S)  **[DONE]**
**Goal:** E-11 Unified config surface -- the configurator is a view of the SSOT, so it can never display values the SSOT does not hold.
**What+How:** Strip the hardcoded image refs out of `usr/share/mios/configurator/mios.html` and have its generator substitute them from the resolved mios.toml when the page is rendered.
**Where:** `usr/share/mios/configurator/mios.html`, its generator
**Done When:** grepping mios.html for a registry-shaped ref returns nothing and the rendered page shows the current SSOT values.
**Why:** the operator's one config door showing versions the system does not actually run makes the configurator actively misleading.
**Dep:** none

## AGY-513 -- Generate the bound-images.tsv SBOM row set from the composed bake set  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening -- provenance is a build output derived from what was actually baked.
**What+How:** Have `90-generate-sbom.sh` write `usr/share/mios/artifacts/sbom/bound-images.tsv` from the SSOT-composed ref set that the bake resolved, replacing the hand-listed rows.
**Where:** `automation/90-generate-sbom.sh`, `usr/share/mios/artifacts/sbom/bound-images.tsv`
**Done When:** the TSV row set is byte-equal to the composed bake set after a build; adding a sidecar to SSOT adds its row with no manual edit.
**Why:** a hand-listed bound-images file is a provenance record that can lie about what shipped, which is exactly the failure SBOM exists to prevent (Law 3 BOUND-IMAGES).
**Dep:** none

## AGY-514 -- Derive image-versions.yml from [image.sidecars] rather than duplicating versions  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- one declaration, every downstream file generated.
**What+How:** Replace the hand-maintained version entries in `image-versions.yml` with generator output keyed off `[image.sidecars]`, so the file becomes a projection like the manifests and bake plan.
**Where:** `image-versions.yml`, its generator
**Done When:** no hand-maintained version string remains in `image-versions.yml` and regeneration after an SSOT bump produces the updated file with no manual edit.
**Why:** a second version table is the classic source of a "bumped it but it still deploys the old one" incident.
**Dep:** none

## AGY-515 -- Resolve the hardcoded k3s version in k3s-cockpit.md and variables.md  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- documentation either projects the SSOT or is explicitly declared non-SSOT prose.
**What+How:** Both docs pin `v1.32.1` inline. Either convert the version into a generated include/placeholder filled at doc-render time, or mark the files as non-SSOT prose in the literal-gate allowlist — one of the two, decided and recorded, not left ambiguous.
**Where:** `docs/k3s-cockpit.md`, `docs/variables.md`
**Done When:** no stale version literal remains in either doc, or both are listed in the gate's prose exemption and the gate is green either way.
**Why:** docs quoting a version the image no longer ships send operators down debugging paths for a system that does not exist.
**Dep:** none

## AGY-516 -- Add the check_no_hardcoded_ssot_literal drift-gate  (WS-UNIFY | **P1** | M)  **[DONE]**
**Goal:** E-09 One value, one name -- a gate keeps the collapse permanent instead of relying on discipline.
**What+How:** Add a check to `automation/98-drift-checks.sh` that fails when a version or image-ref literal appears in a non-SSOT file AND matches a value declared in `[image.sidecars]` — catching duplicated refs in docs, UI and the knowledge graph. Ship it with its negative test, its index registration, and an allowlist for genuine prose (e.g. the AGY-515 exemptions).
**Where:** `automation/98-drift-checks.sh`, the negative-test harness, the check index, the prose allowlist in `mios.toml`
**Done When:** the negative test proves the check FAILS on an injected literal, and the check is green on the projected tree.
**Why:** without a gate, every projection landed by AGY-511..515 silently re-accumulates duplicates on the next hand edit.
**Dep:** AGY-511..515 (the surfaces it gates must be projected first)

## AGY-517 -- Extend mios-ssot-regen to regenerate ALL derived surfaces in one command  (WS-UNIFY | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- one regen command makes staying green cheap enough that nobody skips it.
**What+How:** Widen `mios-ssot-regen` so a single invocation rebuilds manifests, the bake plan, the Quadlets, the knowledge graph, the configurator, the SBOM and `image-versions.yml` from the resolved SSOT, in dependency order.
**Where:** `mios-ssot-regen`, and the generators it drives (`tools/generate-bake-plan.py`, `tools/generate-pod-quadlets.py`, `automation/90-generate-sbom.sh`, the knowledge/configurator projectors)
**Done When:** one `mios-ssot-regen` run followed by the drift-gate is green with no manual per-generator step.
**Why:** N separate regen commands means contributors run some and forget others, which is how projections drift apart in the first place.
**Dep:** AGY-511..514

## AGY-518 -- Remove the .tmp.drive* and *.bak stale copies polluting SSOT greps  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- only real SSOT and real projections may look like SSOT to a grep or a gate.
**What+How:** `.tmp.driveupload/*` and `mios.toml.*.bak` hold stale copies (still carrying `v1.32.1`); delete them from the tree and add ignore rules so they cannot return and masquerade as config.
**Where:** `.gitignore`, `.tmp.driveupload/`, `mios.toml.*.bak`
**Done When:** a version grep across the tree hits only real SSOT plus generated surfaces; the stale files no longer exist and re-creating them leaves them untracked.
**Why:** phantom copies make every literal audit and every drift investigation start with false positives, and risk a gate allowlist being widened to accommodate garbage.
**Dep:** none

## AGY-519..AGY-540 -- Float each sidecar ref to :latest/family-channel with SBOM-recorded resolution, one image per task  (WS-LATEST | P2 | L)  **[DONE]**
**Goal:** E-14 Float latest globally -- SSOT carries version INTENT only; the resolved tag and digest are build-time SBOM data.
**What+How:** One task per image: k3s(519), ceph(520), forgejo(521), forgejo-runner(522), guacamole(523), guacd(524), pgvector(525 — pin the pg-major, float the patch), valkey(526), searxng(527), adguard(528), jaeger(529), matchbox(530), open-webui(531), vllm(532), sglang(533), llama-swap(534), bootc-image-builder(535), hermes-agent(536), code-server(537), ucore-base(538), rust-builder(539), fedora-minimal(540). Each: set the floating intent in `[image.sidecars]`, have `mios-resolve-latest` record the resolved tag + digest into the SBOM at build, and verify the downstream consumer (Quadlet / bake plan) resolves correctly.
**Where:** `usr/share/mios/mios.toml` (`[image.sidecars]`), `mios-resolve-latest`, `usr/share/mios/artifacts/sbom/`, `usr/share/containers/systemd/*`
**Done When:** per image: SSOT holds no pinned digest or hand-written tag, the SBOM carries the build-resolved tag+digest, and the drift-gate is green.
**Why:** hand-pinned tags rot into unpatched images and force a manual bump ritual per sidecar; provenance belongs in the SBOM, not in the config an operator edits (ADR-0003, Law 7).
**Dep:** none

## AGY-541 -- Inventory the 2523 MIOS_* keys by section and flag hand-tuple vs derivable  (WS-UNIFY | **P1** | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- you cannot collapse a namespace you have not measured.
**What+How:** Classify every key in the env baseline by its originating SSOT section (ports, paths, images, ai, containers, services, colors, wsl2, converge, identity, ...) and mark each as a hand-maintained tuple or auto-derivable, emitting the result as `keymap.tsv` that the per-section derive tasks work from.
**Where:** analysis output `keymap.tsv`, `env-baseline.txt`, `userenv.sh`, `usr/lib/mios/mios_toml.py`
**Done When:** every key in the baseline appears in `keymap.tsv` with a section and a hand/derivable classification — zero unclassified rows.
**Why:** without the inventory the collapse is guesswork, and the lossless-diff invariant has nothing to check the surviving keys against.
**Dep:** none

## AGY-542..AGY-560 -- Auto-derive each key domain with a generic rule in both resolver twins, one section per task  (WS-UNIFY | P2 | L)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- the ~200 hand-maintained tuples collapse into one auto-derived minimal key library.
**What+How:** One task per section: ports(542), paths(543), images.sidecars(544 — already covered by Phase 2), containers(545), services(546), colors(547), wsl2(548), converge(549), identity(550), ai.vllm(551), ai.sglang(552), ai.host_thresholds(553), storage.cephfs(554), network(555), security(556), build.bake(557), deploy(558), cat(559), vm(560). Each replaces the section's special-case tuples with one generic derive rule implemented identically in BOTH twins (`usr/lib/mios/mios_toml.py` and `userenv.sh`).
**Where:** `usr/lib/mios/mios_toml.py`, `userenv.sh`, `usr/share/mios/mios.toml`
**Done When:** per section: `mios-env-snapshot` diffs empty against `env-baseline.txt` (root-path normalized) and drift-check 45 is green, with the two twins emitting equivalent values.
**Why:** every hand tuple is a place the bash and Python resolvers can disagree, producing values that differ depending on which surface asked.
**Dep:** AGY-541

## AGY-561 -- Reconcile emitted MIOS_AI_VLLM_* against consumed MIOS_VLLM_*  (WS-UNIFY | **P1** | S)  **[DONE]**
**Goal:** E-10 One canonical name -- no fact carries two names across the emit/consume boundary (Law 9).
**What+How:** The canonical walk emits `MIOS_AI_VLLM_*` while consumers read the short `MIOS_VLLM_*`. Pick ONE canonical family, repoint every consumer to it BEFORE dropping the other name, and remove the mismatch from both resolver twins.
**Where:** `usr/lib/mios/mios_toml.py`, `userenv.sh`, the vLLM consumers (Quadlets, agent-pipe, verbs)
**Done When:** exactly one vLLM key family exists in the emitted environment and `mios-env-snapshot` diffs clean against the baseline.
**Why:** consumers reading a name the walk never emits fall back to defaults silently — the "referenced but NOT emitted" drift class that makes the AI lane misconfigure itself without an error.
**Dep:** AGY-541

## AGY-562 -- Generate userenv.sh's tuple table so the bash twin stops being hand-listed  (WS-UNIFY | P2 | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- the bash twin becomes a generated projection instead of a parallel hand-maintained copy.
**What+How:** Add `tools/generate-userenv.py` to emit the sidecar/version/image tuple block of `userenv.sh` directly from mios.toml, and replace the ~200 hand-written tuples with that generated block.
**Where:** `tools/generate-userenv.py` (new), `userenv.sh`, `usr/share/mios/mios.toml`
**Done When:** no manual tuple remains in `userenv.sh`, regeneration is idempotent, and drift-check 45 is green.
**Why:** hand-maintained tuples are the single largest source of twin divergence and of keys that outlive the SSOT values they mirrored.
**Dep:** AGY-542..AGY-560

## AGY-563 -- Make names.generated.txt cover the FULL minimal key set  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-10 One canonical name -- the generated registry is the authoritative one-line-per-fact map.
**What+How:** Extend `tools/generate-names-registry.py` so `names.generated.txt` lists every canonical `section.key -> MIOS_SECTION_KEY` exactly once, with no duplicate rows and no renaming translations.
**Where:** `tools/generate-names-registry.py`, `names.generated.txt`
**Done When:** the registry row set equals the minimal key set (one row per fact, zero dupes) and drift-check 30 is green.
**Why:** a partial registry lets new translations and second names slip in unnoticed, which is precisely what the ONE-CANONICAL-NAME check exists to block.
**Dep:** AGY-541, AGY-561

## AGY-564 -- Final env-diff: assert the key COUNT dropped with zero value changes  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- the collapse is proven lossless, not asserted.
**What+How:** Re-snapshot the environment after Phases 1-6 and diff against `env-baseline.txt`: assert the total `MIOS_*` count fell (dead keys, aliases and version duplicates gone) while every surviving key holds its original value; then bump the baseline to the new snapshot.
**Where:** `mios-env-snapshot`, `env-baseline.txt`, analysis output
**Done When:** the diff shows only intended removals — zero value changes on surviving keys — and the bumped baseline is green in CI with root-path normalization applied.
**Why:** a namespace collapse that quietly changes a value breaks services in ways no gate attributes back to the refactor; the count-down/values-lossless assertion is the only proof.
**Dep:** AGY-541..AGY-563

## AGY-565 -- ADR recording the unified-key-library architecture  (WS-UNIFY | P2 | S)  **[DONE]**
**Goal:** E-10 One canonical name -- the rule that produced the collapse is an immutable, cross-linked decision, not tribal knowledge.
**What+How:** Write an ADR capturing the single-source rule, the per-section derive rules, the lossless-diff invariant, and the enforcement gates (checks 30/45 and the literal check), with the `laws[]` cross-link, and register it in the ADR index.
**Where:** `docs/adr/`
**Done When:** the ADR is merged, numbered, and the ADR index lists it in sync with the file.
**Why:** without the recorded rule the next contributor re-adds a hand tuple "just this once" and the 2523-key namespace regrows.
**Dep:** AGY-564

## AGY-566..AGY-580 -- Build and wire each derived service image FROM mios-base, one per task  (WS-CONSOLIDATE | P2 | L)  **[DONE]**
**Goal:** E-17 Shared-base consolidation -- the sidecar fleet collapses onto shared lineages so podman stores common layers once.
**What+How:** mios-base build-first(566), then mios-web(567, done-file), mios-data(568), mios-observ(569 jaeger/otel), mios-forge(570), mios-remote(571 guacamole/guacd), mios-k3s-node(572), mios-adguard(573), mios-openwebui(574), mios-matchbox(575), composefs dedup verify(576), bake-budget tighten(577), ceph disposition(578), BIB disposition(579), consolidation doc(580). Each service image derives FROM `localhost/mios-base`, its Quadlet is rewired to it, the upstream ref is dropped from the bake plan, and the unit is verified to start.
**Where:** `Containerfile` / per-image build files, `usr/share/containers/systemd/*`, `plan.d/`
**Done When:** per service: it runs from the shared-base image, its upstream is absent from the bake set, and the measured local store is smaller than before the task.
**Why:** an ~18-image fleet with no shared base is why the bake does not fit a standard GitHub runner — this is the enabler for GitHub and Forgejo publishing as true equals.
**Dep:** AGY-503..AGY-510

## AGY-581..AGY-588 -- Checkpointed resumable build.sh layers and a GitHub self-publish that fits  (WS-GHSELF | P1 | L)  **[DONE]**
**Goal:** E-16 The bake plane -- a stock runner can hold the bake, and a restart resumes rather than restarts.
**What+How:** Split `build.sh` into checkpointed RUN layers — overlay+pkgs(581), render+services(582), bake+finalize(583) — then cache-warm(584), expand the firstboot-tier eviction that keeps the vLLM/SGLang whales out of the bake(585), flip the `PUBLISH` capacity gate(586), bring Forgejo to bit-for-bit parity(587), and add a publish smoke test(588).
**Where:** `Containerfile`, `build.sh`, `.github/workflows/mios-ci.yml`
**Done When:** per task: a runner restart resumes from the last completed layer instead of layer zero, and with `PUBLISH=true` GitHub bakes and pushes `ghcr.io/mios-dev/mios` within runner capacity, with Forgejo producing the same bits.
**Why:** today a long bake that dies near the end throws away all prior work and the capacity ceiling blocks GitHub from publishing at all, leaving the two publishers unequal.
**Dep:** AGY-566..AGY-580 (the bake must fit first)

## AGY-589..AGY-612 -- Build out the full deploy plane artifact by artifact  (WS-DEPLOY | P1/P2 | L)  **[DONE]**
**Goal:** E-20 The bootc-native install legs -- a blank machine installs bootc-managed MiOS fully offline from local media.
**What+How:** One self-contained task per artifact: oci-archive producer(589) and consumer(590) on one path; `bootc install to-disk` offline(591); BIB Anaconda ISO(592); BIB raw(593)/qcow2(594)/vhdx(595); kickstart `ostreecontainer` generated from SSOT(596); Ventoy USB stage(597); `loopback.cfg` menu from SSOT(598); USB partition layout(599); Secure Boot shim->GRUB->UKI(600); MOK enroll(601); Win11 template parity(602); Windows/Xbox ISO leg(603); autounattend `New-LocalUser`(604); MediCat multiboot theme(605); QEMU deploy smoke(606); artifact output-dir from SSOT(607); BIB credential substitution(608); BIB rootfs-label(609); single BIB `/config.toml`(610); repo-partition label(611); deploy-plane doc(612).
**Where:** `installation/`, `tools/`, the BIB config and kickstart files, `usr/share/mios/mios.toml` (deploy/cat sections)
**Done When:** per artifact: it builds, and it boots or installs on the target — with the first-boot verifier asserting the result is bootc-managed MiOS, not plain Fedora — and its gate is green.
**Why:** the deploy plane is the least-done area: the USB machinery works but does not actually install MiOS, so a shipped stick provisions the wrong operating system.
**Dep:** AGY-581..AGY-588 (needs a publishable image to install)

## AGY-613..AGY-624 -- Finish the firstboot model manager (FBM) end to end  (WS-FBM | P2/P3 | L)  **[DONE]**
**Goal:** E-16 The bake plane -- model weights ride the firstboot tier by declared intent instead of bloating the image or arriving ad hoc.
**What+How:** Sidebar tile(613), `firstboot_models` SSOT wiring(614), sha verification of fetched weights(615), `mios models add/rm/status` verbs(616), air-gapped GGUF path(617), bound-images default set(618), Portal tile(619), progress file(620), tier-model auto-provision(621), FBM doc(622), FBM smoke test(623), FBM status rows in TASKS(624).
**Where:** `usr/libexec/mios/` (models verb), Portal/Sidebar surfaces, `usr/share/mios/mios.toml` (`firstboot_models`), FBM doc under `docs/`
**Done When:** `mios models status` reports the SSOT-declared set, a fetched weight fails closed on sha mismatch, the air-gapped GGUF path provisions with no egress, and the FBM smoke test passes.
**Why:** without it the AI lane's weights are neither baked nor reliably fetched, so a fresh install boots an inference plane with nothing to serve and no way to see why.
**Dep:** AGY-581..AGY-588

## AGY-625..AGY-632 -- Harden code-mode: sandbox, network gating, lane confinement, audit  (WS-CODEMODE | P1/P2 | L)  **[DONE]**
**Goal:** E-24 Autonomy guardrails -- the agent plane cannot escape its sandbox or starve its host.
**What+How:** Sandbox hardening(625), `allow_net` as an AND-gate rather than an either-or(626), heavy-lane-only execution(627), `mios_tools` tests(628), timeout and output-size caps(629), audit logging of every execution(630), a default-on release note(631), and a threat-model doc(632).
**Where:** the code-mode executor and `mios_tools` in the agent-pipe plane, `usr/share/mios/mios.toml` (code-mode/guard keys), `docs/`
**Done When:** a code-mode run with `allow_net` unset makes no outbound connection, exceeding the timeout or output cap terminates the run, every execution appears in the audit log, and the `mios_tools` tests pass in CI.
**Why:** code-mode executes model-authored code on the operator's host — ungated network access, unbounded runtime and no audit trail is a live remote-execution surface, which the rubric classes as P0-adjacent risk.
**Dep:** none

## AGY-633..AGY-640 -- Collapse the Windows/Linux install shims into one dispatcher and prove both repos ship it byte-identical  (WS-INSTALL | P2 | M) **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- one guided entry that both installs MiOS and opens the Portal, with no redundant elevation, fetch or redirector left in the chain.
**What+How:** (633) rewrite the `.bat` as a name-stable, logic-free arg-forwarder into the canonical `.ps1`; (634) promote `mios mon` from a local alias to a global verb; (635) add the missing `Get-MiOS -Action Install/Configure` handoff so the `irm | iex` web door hands INTO the guided installer instead of orphaning MiOS-Cat; (636) assert mios.git and mios-bootstrap.git carry byte-identical copies of the shared installer surface (Law 15 DOUBLE-REPO); (637) delete the redirectors that are pure duplicate logic; (638) emit family/role markers so a route knows which target it is serving; (639) an installer smoke run per route; (640) the operator-facing install doc.
**Where:** `installation/mios-install.{sh,ps1,bat}`, `installation/mios-common.{sh,ps1}`, `Get-MiOS`
**Done When:** `mios-install --dry-run` succeeds on every target from either repo's checkout, the .sh/.ps1 parity drift-check is green, and the deleted shim files no longer exist in either tree.
**Why:** today an operator can enter through several files with divergent elevation/prereq/TOML-resolution logic, the web door dead-ends before the installer, and every fix must be hand-copied into a second repo where it silently rots.
**Dep:** none

## AGY-641..AGY-648 -- One global 0-99 numbering so OCI layer = stage = step = drift-check, renumber-immune  (WS-NUMBER | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- one numbering system across scripts, stages and checks so a log line, a build layer and a gate id all name the same thing.
**What+How:** (641) one progress counter in `build.sh` rather than per-script counts; (642) `check_pipeline_numbering` as a regression gate plus its negative test; (643) renumber-immune check labels so inserting a check does not rewrite 100 others; (644) publish the OCI-layer = stage = step = check map; (645) tag every log line with its number; (646) the numbering doc; (647) migrate the legacy check numbers onto the SSOT ordinal (the 149 colliding `(NN)` labels deleted); (648) verify the inline runtime display. `generate-gate-index.py` is the ONE real label parser and must be updated in lockstep with any label change.
**Where:** `automation/98-drift-checks.sh`, `build.sh`, `generate-gate-index.py`, `usr/share/mios/reference/audit-numbering-unification.md`
**Done When:** `check_pipeline_numbering` is green with a passing negative test, the gate index regenerates with zero duplicate ordinals, and a runtime log line shows the same number as the check it reports.
**Why:** without it every inserted stage renumbers unrelated checks, log lines cannot be traced back to the check that emitted them, and the gate index drifts from the scripts it indexes.
**Dep:** none

## AGY-649..AGY-656 -- One logging contract across Python, PowerShell and bash, with banned tokens gated  (WS-LOG | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the log surface becomes a projected, machine-checked format rather than per-script ad-hoc `echo`.
**What+How:** (649) a `mios_log.py` peer to the shell logger so Python emits the identical format; (650) the PowerShell logger twin; (651) expand the E5 banned-token list; (652) scan `usr/libexec/mios` and every `.ps1` for direct writes that bypass the logger; (653) a terse-rewrite sweep of verbose log strings; (654) number-tag every line per the WS-NUMBER map; (655) the log-format SSOT doc; (656) a drift-check that fails on any banned token or unlogged write.
**Where:** `usr/lib/mios/mios_log.py`, `usr/libexec/mios/`, the `.ps1` surface, `automation/98-drift-checks.sh`
**Done When:** the log gate is green, the banned-token scan returns zero hits across libexec and PowerShell, and the three loggers emit byte-comparable lines for the same event.
**Why:** three languages currently invent three log shapes, so no tool can parse a build log, and banned/noisy tokens keep reappearing because nothing rejects them.
**Dep:** AGY-641..AGY-648 (number tags)

## AGY-657..AGY-668 -- Decompose server.py, kill eval-on-agent-args, and stand up the compiled-template + language policy  (WS-LANG | P2/P3 | L) **[DONE]**
**Goal:** E-01 Compiled native tier -- move the hot, fragile surfaces off scripts and behind a declared language-per-domain policy (Law 14 TARGET-LANGUAGES).
**What+How:** (657-659) split the agent-pipe `server.py` god-module into modules under ~800 lines each, one task per module, preserving the `/v1/*` wire behavior; (660) remove the eval-on-agent-args injection surface from the verb library (TD class); (661-663) build the compiled-template system (one template per file type plus its golden compiler); (664) ratchet shellcheck from advisory to warning-clean; (665) move the Portal-adjacent surfaces to Bun/TS; (666-667) port the two hot paths to Rust; (668) record the language policy as an ADR so the rule is decided, not asserted.
**Where:** `usr/lib/mios/agent-pipe/server.py`, `usr/lib/mios/mios_dispatch.py`, `usr/share/mios/templates/`, `docs/adr/`
**Done When:** no module in the agent-pipe exceeds the line ceiling, `grep` finds zero `eval` on agent-supplied args in the verb library, shellcheck runs clean at the ratcheted level, and the language-policy ADR is merged with its registry row.
**Why:** a ~9k-line server module plus eval-on-args is both the hardest file to change safely and a live code-execution surface reachable from agent input.
**Dep:** none

## AGY-669..AGY-678 -- Close the supply chain: rotate secrets, sign the image, complete the SBOM, gate egress  (WS-SEC | P0/P2 | L) **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- provenance and trust are enforced by gates, not by convention.
**What+How:** (669, P0) rotate the exposed PAT and move credentials into a secret store; (670) `cosign verify` the published image and fail the gate if verification fails; (671) SBOM completeness -- every bound image and vendored asset carries a digest and sha; (672) fapolicyd trust rules; (673) encrypt `secrets.env`; (674) remove the soft-mode escape so a failing security check cannot be downgraded to a WARN; (675) audit the privileged Quadlets against Law 10; (676) drive the egress firewall from an SSOT allowlist; (677) clevis/LUKS binding from SSOT; (678) the security doc.
**Where:** `usr/share/mios/mios.toml` `[security]`, the rendered Quadlets, `automation/90-generate-sbom.sh`, `automation/98-drift-checks.sh`
**Done When:** no plaintext secret survives a scan, `cosign verify` passes in CI against the published ref, the SBOM lists every bound image with a digest, and soft-mode no longer exists as a bypass.
**Why:** a live leaked PAT plus an unsigned, incompletely-inventoried image means the fleet can pull something nobody can prove is MiOS.
**Dep:** none

## AGY-679..AGY-700 -- Land the north-star surfaces: liquid-glass shell, MiOS-Metal, DB accounts, dotfile/theme surfaces  (WS-ROADMAP | P3 | L) **[DONE]**
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the exploratory north-stars become real, SSOT-projected surfaces instead of documents.
**What+How:** (679-682) project the Hyprland/Quickshell liquid-glass effects and animations FROM `mios.toml` rather than hand-written `hyprland.conf`; (683-686) the MiOS-Metal split-plane vfio/mesh design (whole-GPU-to-one-guest, headscale routing); (687-690) the PostgresOS DB-backed account model assembled from systemd userdb + lldap-over-Postgres, shipping inert until wired; (691-693) two further `[dotfiles.registry.<surface>]` surfaces beyond the btop proof; (694-695) extend the theme SSOT projection; (696) audit that `MIOS_AI_ENDPOINT` is canonical everywhere; (697) finish the legacy purge to a /v1-only AI plane; (698-700) wire three unwired runtime features.
**Where:** `usr/share/mios/mios.toml` `[colors]`/`[dotfiles.registry.*]`, `docs/agy/doc-mios-metal.md`, `docs/agy/doc-postgresos-accounts.md`
**Done When:** each new surface renders from SSOT and its projection check (theme check 25, `check_dotfiles_projection`) is green, and the endpoint/legacy audits report zero non-allowlisted hits.
**Why:** these surfaces are the visible product; left as prose they stay hand-maintained, diverge from `mios.toml`, and re-introduce the hardcodes the gates just removed.
**Dep:** none

## AGY-701..AGY-730 -- Verify the whole program: a negative test per gate, hermeticity sentinels, the ADR set, and the release  (WS-TESTDOC | P2 | L) **[DONE]**
**Goal:** E-06 Test and documentation harness -- every gate is proven able to FAIL, and the doc set is complete enough for an agent to pick up a workstream cold.
**What+How:** (701-715) one negative test per new gate, one task per gate, so a check that can never go red is caught; (716-718) hermeticity sentinels; (719) refresh the roadmap index; (720-724) the ADR set for the batch; (725-727) postmortems for the reds that occurred; (728) the meta "keep-main-green-throughout" tracker; (729) the 0.3.1 version bump so the program actually ships; (730) the final full-green drift-gate sign-off.
**Where:** `automation/98-drift-checks.sh`, the negative-test harness, `docs/adr/`, `docs/agy/`, `ROADMAP`
**Done When:** the negative-test job runs in CI BEFORE the main gate and every listed gate has a paired failing case, and `just drift-gate` is full-green on the 0.3.1 tag.
**Why:** a gate with no negative test is a green light that may never have been wired to anything; without it the program claims coverage it does not have.
**Dep:** the gates added by the preceding workstreams

## AGY-731..AGY-748 -- Systemically fix the three failure classes that broke the 0.3.0 publish bake  (WS-BAKE-HARDEN | P1 | L) **[DONE]**
**Goal:** E-16 The bake plane -- the published image is what SSOT says is present, and a stock runner can hold it without the classes that produced 54 exit-125 / 56 exit-1 / 37 WARN.
**What+How:** (731) grep every `automation/*.sh` under `set -euo pipefail` for the abort-on-missing-file command-substitution class (`x=$(cmd missing…)`, the 56-fonts bug) and fix each; (732) audit every nested `podman build` for the caps the working bake uses (`--network=host --cap-add all --security-opt seccomp=unconfined --security-opt apparmor=unconfined`) and add or explicitly defer; (733) fix or justify the `37-k3s-selinux.sh` WARN; (734) give coderun-sandbox an isolated podman store via `--root/--runroot`; (735-736) a firstboot builder so the deferral materializes the sandbox image, with a Quadlet that degrades gracefully when it is absent; (737, 745) record both font tarball sha256s to the SBOM and assert the fonts landed; (738-740) an isolated re-run smoke harness, a DEFERRED-marker contract consumed by firstboot, and a machine-readable failure manifest (script/exit/stderr-tail); (741-744, 746-747) float the sandbox base to `alpine:latest` with the digest recorded, assert the static-musl/landlock build still compiles, audit per-script network need, add a bake dry-run, recount the image budget; (748) the failure-mode playbook.
**Where:** `automation/*.sh` (incl. `37-k3s-selinux.sh`, `54-coderun-sandbox`, `56-fonts`, `57-mios-sys`), `automation/90-generate-sbom.sh`
**Done When:** the publish bake completes with zero exit-125 and zero set-e aborts, every deferred script emits a gated DEFERRED marker, and a failed bake is diagnosable from the manifest without scrollback.
**Why:** the 0.3.0 publish failed on classes that are still present in the tree, so the next publish fails the same way and nobody can tell which script died or why.
**Dep:** none

## AGY-749..AGY-762 -- Make the resolved-env lossless gate hermetic so CI locale/HOME can never fake drift  (WS-LOSSLESS-HARDEN | P1 | L) **[DONE]**
**Goal:** E-08 Global Unification Plan -- the losslessness invariant that guards every namespace collapse must itself be context-free.
**What+How:** (749) pin HOME/XDG/LC_ALL/LANG/TZ and the user+host TOML layers to fixed nonexistent paths in `mios-env-snapshot`, excluding self-referential meta-keys, and add a header contract; (750) a sentinel running the snapshot under >=3 locales x >=3 HOMEs asserting byte-identical output; (751) gate that `env-baseline.txt` is regenerated whenever `referenced_names.txt` changes (pre-commit + CI); (752) a leak sentinel asserting no resolved value embeds the tree root, `$HOME`, hostname or uid; (753) make `check_resolved_env_lossless` print the ACTUAL diff even on the negative path (stop `>/dev/null`); (754) a `mios-env-baseline --regen` helper plus documented `MIOS_ENV_BASELINE_BUMP=1`; (755) decide the referenced_names -> empty-emit policy for the `MIOS_A2A_*`/`MIOS_CAT_LIVE_CHAT_*` class; (756-757) a baseline header and a CI matrix running the check in both C and en_US.UTF-8; (758-760) generate the two byte-identical `userenv.sh` from ONE source (killing check-27's manual copy-sync), run check-45 twin-equivalence under `env -i`, and emit `names.generated.txt` + `referenced_names.txt` from one invocation behind one gate; (761) the resolver-hermeticity ADR; (762) a pre-commit hook regenerating both on any resolver/SSOT change.
**Where:** `mios-env-snapshot`, `env-baseline.txt`, `referenced_names.txt`, `names.generated.txt`, `userenv.sh` (both twins), `check-resolver-twin.py`
**Done When:** the snapshot is byte-identical across the locale x HOME matrix, `check_resolved_env_lossless` and check-45 are green in both CI locale jobs, and a red prints its diff.
**Why:** the gate that protects every GUP step currently goes red for caller locale, HOME and stale referenced-names rather than for real drift -- and prints nothing, so the red is undebuggable.
**Dep:** none

## AGY-763..AGY-786 -- Derive every sidecar version from its image tag and delete the standalone X_version keys  (WS-GUP2-VERSION | P1 | L) **[DONE]**
**Goal:** E-08 Global Unification Plan -- a version is declared once (in the image ref) and derived everywhere else, ending the k3s-in-35-places class.
**What+How:** (763) enumerate every `[image.sidecars]` `X_version` against `tag(X)` into a reconcile worklist and commit the analysis; (764) fix the INCONSISTENT pairs first (e.g. `cuda_version` vs the `:cuda` tag) so derive == old; (765-778) one task per sidecar (adguard, cuda, sglang, vllm, k3s, postgres, pgvector, searxng, valkey, guacamole, guacd, open-webui, otelcol, hermes/sys) making the resolver derive `MIOS_X_VERSION=tag(MIOS_X_IMAGE)`, deleting the standalone key, and lossless-verifying the value is unchanged; (779) land the tag-extraction rule identically in `mios_toml.py` `get_aliases`, both `userenv.sh` twins and `check-resolver-twin.py`; (780-783) delete all `X_version` keys, gate `check_no_duplicate_value_key`, make the 19 `_VERSION=latest` keys derive from a `:latest` ref, and stop `[build.bake].core` re-listing sidecar refs; (784-785) regenerate Quadlet `Image=` from the canonical `MIOS_*_IMAGE` (never de-digesting; stage explicit paths) and regenerate `03-extra.list`; (786) sign-off by bumping k3s in ONE place and watching every surface update.
**Where:** `usr/share/mios/mios.toml` `[image.sidecars]`/`[build.bake]`, `usr/lib/mios/mios_toml.py`, `userenv.sh` twins, the rendered Quadlets, `03-extra.list`
**Done When:** no `X_version` key remains in SSOT, `mios-env-snapshot` shows every `MIOS_X_VERSION` unchanged, and a single-ref k3s bump propagates to Quadlets, bake list and docs with the gate green.
**Why:** the same version currently lives in the tag AND a sibling key AND the bake list AND the Quadlets, so a bump updates some of them and the fleet runs a mix.
**Dep:** none

## AGY-787..AGY-800 -- Collapse the measured alias bloat: one value carried by many names  (WS-DEDUP-ALIAS | P2 | L) **[DONE]**
**Goal:** E-09 One value, one name -- the duplication measured in `env-baseline` (values under >=3 keys) collapses to one canonical name plus a documented compat leg.
**What+How:** (787) COLOR vs COLORS -- pick canonical, repoint consumers, drop the other; (788) collapse the 6-name embed-model fanout (`MIOS_PG_EMB_MODEL`/`PG_EMBED_MODEL`/`PGVECTOR_EMB_MODEL`/`PGVECTOR_EMBED_MODEL`/`AI_EMBED_MODEL`/`VERB_EMBED_MODEL` == nomic-embed-text); (789) separate true model aliases (`MIOS_AI_MODEL`==`MIOS_MODEL`, the 8-way granite fanout) from distinct configurable facts and collapse only the true dupes; (790) derive the 6 `nodes.local.*_ENDPOINT` from one; (791) collapse the ANSI singular/plural pair; (792) allowlist the mios-find launcher aliases as INTENTIONAL many-to-one; (793) audit `MIOS_A2O_*` vs `MIOS_FRONTIER_*`; (794) extend `check_no_duplicate_value_key` to non-version values with that allowlist; (795) resolve the AGY-505 pgvector/pg double-alias; (796-799) per-family collapse (portal, agents, nodes, lanes); (800) sign-off.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/mios/mios_toml.py`, `userenv.sh` twins, `env-baseline.txt`
**Done When:** `check_no_duplicate_value_key` is green over all value classes, and the env diff shows ONLY the intended key drops with every surviving value byte-identical.
**Why:** the same fact under six names means six places to edit and five chances to leave a stale value that some consumer still reads.
**Dep:** consumers must be repointed before any name is dropped

## AGY-801..AGY-818 -- Actually build and BOOT every deploy artifact in CI, not just plan it  (WS-DEPLOY-REAL | P1/P2 | L) **[DONE]**
**Goal:** E-20 The bootc-native install legs -- a blank machine installs the real immutable MiOS fully offline, proven by a booting artifact rather than a document.
**What+How:** (801-802) build a BIB Anaconda ISO from the PUBLISHED image in CI, upload it, and boot it headless in QEMU asserting Anaconda reaches the `ostreecontainer` stage; (803) `bootc install to-disk` into a QEMU raw disk OFFLINE from an oci-archive with a first-boot assert; (804) qcow2 and vhdx artifacts built and booted; (805) the Secure Boot chain shim->GRUB->signed-UKI with MOK enrollment under OVMF with SB ON; (806) a Ventoy USB image (ISO + oci-archive + loopback.cfg-from-SSOT) booting from USB in QEMU; (807) the Windows/Xbox leg (autounattend + SetupComplete `New-LocalUser` baseline); (808) pin the kickstart `ostreecontainer` source to the published digest offline; (809) the MiOS-Cat.sh <-> build-mios.sh FULL-FOLD with the shims deleted; (810) the missing bare-metal leg via `bootc install --transport oci`; (811-817) per-format boot-assert (raw/qcow2/vhdx/iso/oci-archive/ventoy/win-iso); (818) one USB, all formats, sign-off.
**Where:** `installation/`, `cat/MiOS-Cat.sh`, `build-mios.sh`, `tools/install.sh`, the kickstart and BIB config
**Done When:** each CI job uploads its artifact AND its boot log shows the installed system is bootc-managed MiOS (not plain Fedora), with the per-format gate green.
**Why:** the deploy plane is the least-finished area: the USB machinery works but installs plain Fedora, the bare-metal leg has no `tools/install.sh` at all, and nothing today proves an artifact boots.
**Dep:** a published image to install from (WS-BAKE-HARDEN)

## AGY-819..AGY-834 -- Wire every shipped-but-inert runtime capability from SSOT, or document the deferral  (WS-RUNTIME-WIRE | P2/P3 | L) **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- close the doc-vs-SSOT gap so nothing ships as silent dead weight.
**What+How:** one task per capability MiOS already bakes but does not drive: (819) greenboot health-checks defined from SSOT and gated; (820) clevis/LUKS auto-unlock from `[security]`; (821) chrony NTP; (822) the ROCm/venus GPU plane wired or documented-deferred (venus != CUDA); (823) decide ceph -- wire or sunset alongside gluster; (824) mdevctl vGPU; (825) the freeipa / lldap-over-Postgres accounts SSOT face; (826) NUT UPS; (827) guacamole/guacd wired plus a smoke; (828) virt-v2v import; (829) clarify signed-UKI vs MOK (MOK != UKI) and wire it; (830) audit that every stateful dir lives on `/var` so it survives a bootc update; (831-833) three further features; (834) sign-off that every shipped binary is wired-from-SSOT or explicitly deferred with a reason.
**Where:** `usr/share/mios/mios.toml`, the rendered Quadlets and units, `automation/98-drift-checks.sh`
**Done When:** for each capability either a drift-check asserts its SSOT wiring and a smoke exercises it, or a recorded decision states why it is deferred -- with no capability left unclassified.
**Why:** the image carries greenboot, clevis, chrony, ROCm, ceph, mdevctl, freeipa, NUT, guacamole and virt-v2v that no config path reaches, so operators believe they have protections that are not running.
**Dep:** none

## AGY-835..AGY-842 -- Security follow-through: the exposed PAT is still live  (WS-SEC2 | P0/P2 | M) **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening -- every declared control is actually enforced and gated, starting with the credential that is currently valid.
**What+How:** (835, P0) rotate the exposed PAT (`ghp_…` present in git history) and move credentials to a secret store -- operator action, tracked to completion; (836) scan the full history with gitleaks and rotate anything else found; (837) cosign sign the published ghcr image and gate on `cosign verify`; (838) SBOM completeness -- every bound image and vendored asset carries digest + sha; (839) fapolicyd trust for the coderun-sandbox plus an agent-codegen rules audit; (840) the egress firewall from an SSOT allowlist using nft (no firewalld); (841) least-privilege each of the 10 privileged root Quadlets under Law 10; (842) sign-off.
**Where:** git history, `usr/share/mios/mios.toml` `[security]`/`[security.privileged_quadlets]`, the rendered Quadlets, CI publish workflow
**Done When:** gitleaks returns clean over the full history, the rotated credential is confirmed revoked, `cosign verify` gates the publish, and every privileged unit's caps are justified in SSOT.
**Why:** a working token in a readable history is an active compromise path, and an unsigned image plus unbounded egress means a tampered artifact would be indistinguishable from a real one.
**Dep:** AGY-669..AGY-678 (first security pass)

## AGY-843..AGY-855 -- Prove and ship this batch: negatives, sentinels, ADRs, published-image smoke  (WS-TESTDOC2 | P2 | L) **[DONE]**
**Goal:** E-06 Test and documentation harness -- the batch does not count as landed until its gates are proven failable and a published image is smoke-verified.
**What+How:** (843-850) one NEGATIVE test per gate added by this batch (bake-harden, lossless-harden, no-duplicate-value-key, deploy, runtime-wire, sec), ratcheting check-77's coverage floor; (851) hermeticity sentinels for the snapshot and the bake covering locale, HOME and network; (852) refresh the roadmap index and close the doc-vs-SSOT gap; (853) the ADR set for GUP Phase 2/3, alias-dedup, deploy-real and runtime-wire; (854) the keep-main-green tracker plus a day-0-publish postmortem naming the exact reds (54, 56, lossless-HOME, lossless-locale, names-stale) and their fixes; (855) bump to 0.3.2, run the full drift-gate, then pull `ghcr.io/mios-dev/mios:latest`, boot it and assert the AI plane answers.
**Where:** the negative-test harness, `automation/98-drift-checks.sh`, `docs/adr/`, `docs/agy/`
**Done When:** negative coverage meets or exceeds the check-77 ratchet, the drift-gate is full-green at 0.3.2, and the published-image smoke returns a real `/v1` completion.
**Why:** without the negative pass and the published-image smoke the batch's greens prove only that checks ran, not that the shipped artifact works.
**Dep:** AGY-731..AGY-842

## AGY-856..AGY-864 -- Build the tooling that MEASURES duplication and gates it permanently  (WS-DEDUP-DISCOVER | P1 | M) **[DONE]**
**Goal:** E-09 One value, one name -- measure before collapsing, so the dedup campaign is driven by data rather than guesses.
**What+How:** (856) build `mios-dup-report` that groups the resolved env by VALUE, lists every value carried by >=2 keys, classifies each group {true-alias | distinct-configurable-fact | intentional-many-to-one} and emits a TSV; (857) a cross-surface literal scanner finding every version/ref/port/path/color/model literal in NON-SSOT files (docs, `mios.html`, `knowledge-graph.json`, SBOM, scripts) that equals an SSOT value; (858) extend `check_no_duplicate_value_key` beyond versions to all value classes with an explicit documented allowlist; (859) add `check_no_hardcoded_ssot_literal` (GUP Phase 4 enforcement); (860) enumerate every resolver rule (`get_aliases` plus the 3 emitters) that emits >=2 names for one dotted path, tabled with consumer counts -- the STRUCTURAL dupe source; (861) negative tests ratcheting check-77; (862) commit `value-dup-report.tsv` refreshed by `mios-ssot-regen` and gated; (863) the tracking doc with the measured baseline key count and target; (864) a referenced-but-empty audit deciding keep-vs-drop per key.
**Where:** `tools/`, `usr/libexec/mios/mios-dup-report`, `usr/share/mios/reference/value-dup-report.tsv`, `docs/agy/`, `automation/98-drift-checks.sh`
**Done When:** `mios-dup-report` runs and its committed TSV is refreshed by `mios-ssot-regen`, both new checks are green with negative tests, and the baseline key count is recorded.
**Why:** without a measurement the collapse is guesswork -- names get dropped that still have consumers, and new dupes reappear with nothing to catch them.
**Dep:** none

## AGY-865..AGY-872 -- Collapse the 7x-per-color theme aliasing to one canonical name per role  (WS-DEDUP-COLOR | P2 | M) **[DONE]**
**Goal:** E-09 One value, one name -- one hex value carries exactly one canonical key, and every theme surface still renders identically.
**What+How:** measured: `#1A407F` appears under 7 names (`MIOS_COLOR_ACCENT` == `MIOS_COLORS_ACCENT` == `MIOS_ANSI_4_BLUE` == `MIOS_COLORS_ANSI_4_BLUE` == `MIOS_COLOR_INFO` == `MIOS_COLORS_INFO` == `MIOS_EDITIONS_MIOS_COLORS_ACCENT`). (865) grep theme-render, hyprland, btop, `mios.html` and quickshell to find which form consumers actually read; (866) pick ONE canonical form per color role, adding compat only where a consumer cannot be repointed; (867) repoint every consumer; (868) drop the redundant form from both resolver twins and `check-resolver-twin.py`, lossless; (869) collapse `MIOS_ANSI_N_*` against `MIOS_COLORS_ANSI_N_*`; (870) derive the per-edition `MIOS_EDITIONS_*_COLORS_*` from the base palette plus an edition delta instead of full copies; (871) keep theme-render's SSOT projection (check 25) green; (872) sign-off.
**Where:** `usr/share/mios/mios.toml` `[colors]`, `mios-theme-render`, `usr/lib/mios/mios_toml.py`, `userenv.sh` twins
**Done When:** each color value maps to one canonical key, check 25 is green, the rendered theme files are byte-identical to before, and the env diff shows only the intended drops.
**Why:** seven names per hex means a palette change either takes seven edits or leaves surfaces on a stale color that still resolves.
**Dep:** AGY-856..AGY-864 (the measurement)

## AGY-873..AGY-882 -- Collapse the AI-plane model/embed/endpoint/engine fanout to single declarations  (WS-DEDUP-AIPLANE | P2 | L) **[DONE]**
**Goal:** E-09 One value, one name -- the /v1 AI plane's facts each have one name, with the `/v1` behavior unchanged.
**What+How:** measured: nomic-embed-text x6, granite4.1:8b x8, mios-heavy x10, the sglang endpoint x6, claude engine x6, openai x5, hermes parser x5. (873) one canonical `MIOS_AI_EMBED_MODEL`, repointing the pg/pgvector/emb/embed/verb variants and resolving the AGY-505 double-alias; (874) separate TRUE aliases (`MIOS_AI_MODEL`==`MIOS_MODEL`) from DISTINCT configurable facts (gateway/hermes/finetune-teacher that merely DEFAULT to granite), collapsing only the former and keeping the latter as independent keys referencing the default; (875) derive the mios-heavy served-name from one SSOT declaration; (876) derive the 6 `nodes.local.*_ENDPOINT` from `MIOS_AI_ENDPOINT` (canonical `:8642`); (877) audit `MIOS_A2O_*` vs `MIOS_FRONTIER_*` for true-dupe vs distinct-lane; (878) collapse the openai-api and hermes `tool_call_parser` fanouts; (879) reconcile emitted `MIOS_AI_VLLM_*` against consumed `MIOS_VLLM_*`; (880) repoint every consumer (`server.py`, routing, gateway, verbs); (881) lossless + twin green after each step; (882) sign-off.
**Where:** `usr/share/mios/mios.toml` `[ai]`/`[nodes]`, `usr/lib/mios/agent-pipe/server.py`, the verb library, `userenv.sh` twins
**Done When:** the AI-plane key set is minimal, `check_no_duplicate_value_key` and check-45 are green, and a `/v1` smoke returns the same completions as before the collapse.
**Why:** the emitted-vs-consumed vLLM mismatch already means some keys are set and never read; ten names for one served model guarantees a partial edit routes traffic to the wrong lane.
**Dep:** AGY-856..AGY-864

## AGY-883..AGY-889 -- Collapse host / port / path / dir duplication into derived siblings  (WS-DEDUP-NETPATH | P2 | M) **[DONE]**
**Goal:** E-09 One value, one name -- network and filesystem facts are declared once and their siblings derived.
**What+How:** measured: `127.0.0.1` x5 across bind addrs and hosts, `--new-window` x5 across browser flags, plus dir/path fanout. (883) one canonical loopback SSOT key with the five `*_HOST`/`*_BIND` derived from it; (884) collapse the chromium/epiphany/firefox `NEW_WINDOW` vs `WINDOW` pairs per browser; (885) derive the `*_DIR` family from a base (`MIOS_AI_DIR`, `MIOS_STATE_DIR`) instead of full literal paths; (886) confirm every port derives via `stack_offset` from one declaration with no literal dupes; (887) collapse the `MIOS_PATHS_*` vs bare `MIOS_*` path duplication (`MIOS_PATHS_CMD_EXE` == `MIOS_CMD_EXE`); (888) repoint consumers; (889) sign-off.
**Where:** `usr/share/mios/mios.toml` `[network]`/`[ports]`/`[paths]`, `userenv.sh` twins, `usr/lib/mios/mios_toml.py`
**Done When:** each host/port/path is a literal in exactly one place, the env snapshot is lossless apart from the intended drops, and every repointed consumer still resolves.
**Why:** five bind-address literals is five places a bind-all/loopback change must be repeated, which is exactly how a service ends up listening on the wrong interface.
**Dep:** AGY-856..AGY-864

## AGY-890..AGY-897 -- Remove the resolver rules that MINT duplicates; declare aliases in ONE table  (WS-DEDUP-STRUCTURAL | P1 | M) **[DONE]**
**Goal:** E-09 One value, one name -- attack the source: no rule may silently emit a second name for a fact.
**What+How:** driven by the AGY-860 audit. (890) for each `get_aliases` rule appending >=2 names, keep ONE canonical and move the rest behind an explicit, documented, consumer-justified compat map instead of scattered `aliases.append` calls; (891) build a single COMPAT-ALIAS table (dotted-path -> [canonical, ...deprecated]) so the alias surface is declared in one place rather than across ~200 tuples; (892) emit the canonical always and a leg only when a live consumer needs it -- a leg with zero consumers is dropped by the gate; (893) apply identically in both `userenv.sh` twins, `check-resolver-twin.py` and `mios_toml.py` (checks 27/45); (894) generate the two byte-identical `userenv.sh` from ONE source, killing the manual copy; (895) drive a deprecation report from the compat table showing which legacy names still have consumers; (896) lossless + twin green; (897) sign-off.
**Where:** `usr/lib/mios/mios_toml.py` (`get_aliases`), `userenv.sh` twins, `check-resolver-twin.py`
**Done When:** the alias surface exists as one declared table, check-27 and check-45 are green, and the gate reports zero unconsumed alias legs.
**Why:** collapsing names downstream is pointless while the resolver re-mints them on every run, and two hand-synced `userenv.sh` copies drift the moment one is edited.
**Dep:** AGY-860 (the alias-emission audit)

## AGY-898..AGY-907 -- GUP Phase 4: every derived surface PROJECTS from SSOT instead of re-listing literals  (WS-DEDUP-CROSSSURFACE | P1/P2 | L) **[DONE]**
**Goal:** E-09 One value, one name -- a version or ref appears as a literal in exactly one place and every other surface derives from it.
**What+How:** (898) every version/ref/port shown by the `mios.html` configurator projects from `mios.toml` via `mios-ssot-regen`; (899) `knowledge-graph.json` node values derive from SSOT; (900) `variables.md`, `k3s-cockpit.md` and every reference doc project their versions/refs; (901) `bound-images.tsv` and the SBOM derive from `[image.sidecars]` plus resolved digests; (902) Quadlet `Image=`, `03-extra.list` and `[build.bake].core` single-source from `[image.sidecars]`, finishing Phase 3; (903) extend `mios-ssot-regen` to every surface above so ONE command refreshes all; (904) `check_no_hardcoded_ssot_literal` goes RED on any residual literal; (905) a pre-commit hook runs ssot-regen on any `mios.toml` change; (906) an end-to-end "bump one value -> every surface updates" test; (907) sign-off.
**Where:** `usr/share/mios/mios.html`, `knowledge-graph.json`, `usr/share/mios/reference/variables.md`, `bound-images.tsv`, `03-extra.list`, the rendered Quadlets
**Done When:** `check_no_hardcoded_ssot_literal` is green, `mios-ssot-regen` leaves a clean tree, and the one-bump test propagates a single edit to every listed surface.
**Why:** the configurator, docs, graph and bake list currently restate values from SSOT, so they present stale versions the moment the real ref moves.
**Dep:** AGY-859 (the literal gate), AGY-763..AGY-786

## AGY-908..AGY-918 -- GUP Phase 5+6: float every ref to latest with SBOM pinning, and derive the minimal key library  (WS-DEDUP-GUP56 | P2/P3 | L) **[DONE]**
**Goal:** E-08 Global Unification Plan -- SSOT carries version INTENT only (E-14 float-latest), and the MIOS_* namespace shrinks to an auto-derived minimal set.
**What+How:** (908) float every sidecar ref to `:latest`/family-channel intent with `mios-resolve-latest` recording the resolved tag and digest to the SBOM at build (ADR-0003); (909) a gate rejecting hand-pinned `:vX.Y.Z` outside the SBOM/allowlist; (910) prove the day-0 build floats and the SBOM records the pin; (911) auto-derive the minimal global key set and collapse `userenv.sh`'s ~200 manual tuples into it; (912) reconcile emitted `MIOS_AI_VLLM_*` against consumed `MIOS_VLLM_*` once and for all; (913) make the names registry BE the minimal set with no dupes or translations; (914-916) migrate consumers family by family (env, verbs, services), one task each; (917) a final env diff asserting the `MIOS_*` key COUNT dropped materially with ZERO value change to survivors; (918) sign-off.
**Where:** `usr/share/mios/mios.toml` `[image.sidecars]`, `mios-resolve-latest`, `userenv.sh` twins, `names.generated.txt`, `automation/90-generate-sbom.sh`
**Done When:** no hand-pinned tag survives outside the allowlist, the key count is measurably lower, and every surviving value is byte-identical in the snapshot.
**Why:** hand-pinned tags freeze the fleet on stale upstreams while 200 hand-maintained tuples guarantee the key library drifts from what the resolver actually emits.
**Dep:** AGY-890..AGY-907

## AGY-919..AGY-930 -- Prove, permanently enforce and close the de-duplication campaign  (WS-DEDUP-SIGNOFF | P2 | L) **[DONE]**
**Goal:** E-09 One value, one name -- the campaign's invariant becomes machine-enforced so the namespace cannot re-inflate.
**What+How:** (919) run the full suite green together -- `check_no_duplicate_value_key` (all classes), `check_no_hardcoded_ssot_literal`, `check_resolved_env_lossless`, the twin check and the names check; (920) a negative test for EVERY new dedup gate; (921) commit the before/after key-count report (2438 -> target) with the value-dup report showing zero non-allowlisted dupes; (922) an ADR covering the unified-key-library architecture (single-source rule, derive rules, the compat-alias table, the lossless-diff invariant, the enforcement gates); (923) mark the unified-key-library and global-unification-plan north-stars LANDED; (924) a "moving one thing" regression test asserting that changing one value changes exactly the intended keys and updates every surface via ssot-regen; (925) wire the whole suite into the pre-commit hook; (926-929) per-family final lossless sign-off (color, ai, net, structural); (930) campaign close with the version-bump note.
**Where:** `automation/98-drift-checks.sh`, `usr/share/mios/reference/value-dup-report.tsv`, `docs/adr/`, the pre-commit hook
**Done When:** all five checks are green simultaneously with negative tests, the committed key-count report shows the drop, and the one-thing regression test passes from a clean checkout.
**Why:** an ungated collapse re-inflates within weeks -- the next contributor adds a convenience alias and nothing objects.
**Dep:** AGY-856..AGY-918

## AGY-931..AGY-945 -- Compiled template engine, the `mios new` scaffolder, and enforced template conformance  (WS-TEMPLATE | P1/P2 | L) **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- structure, not just the AI-hint header, is validated, so an agent learns MiOS formatting from a few files.
**What+How:** (931) `mios-template-engine`, a compiled renderer reading `usr/lib/mios/templates/` and supporting all 20 template kinds; (932) the `mios new <kind> <path>` CLI verb that scaffolds a file with its mandatory AI-hint header already correct; (933) extend `check_template_conformance` (Check 46) from a SOFT skip to enforcing the AI-hint header STRUCTURE plus mandatory body markers on new files; (934-945) author the per-kind template definitions and their conformance verification suite, one task per kind.
**Where:** `usr/lib/mios/templates/`, `usr/bin/mios` (`new` verb), `automation/98-drift-checks.sh` (Check 46)
**Done When:** `mios new <kind>` produces a file that passes Check 46 unmodified, the check enforces (not skips) on new files, and the drift-gate is green with all 20 kinds covered.
**Why:** without an enforced template every new file invents its own shape, the AI-hint convention decays, and agents arriving cold cannot infer the house style from the tree.
**Dep:** none

## AGY-946..AGY-960 -- Stand up the Cargo workspace and port the fragile bash hot paths to native binaries  (WS-LANG | P1/P2 | L) **[DONE]**
**Goal:** E-01 Compiled native tier -- the build orchestrator and tool fleet become compiled crates, migrated strangler-fig so byte-parity is proven before any script is deleted.
**What+How:** (946) initialize the Cargo workspace under `tools/native/` with every crate a member, built and shipped by the Containerfile; (947) `mios-drift-runner`, a native CLI that executes the drift checks, replacing the fragile bash loop that makes the gate itself brittle; (948) `mios-verb-dispatcher`, fast native dispatch for the `/v1/` AI verbs; (949-960) wire the native tooling into the drift suite, ratchet the Law 14 TARGET-LANGUAGES check, and verify each port against its shell predecessor before demoting the script to a thin shim.
**Where:** `tools/native/` (Cargo workspace), `automation/98-drift-checks.sh`, `Containerfile`
**Done When:** `cargo build` succeeds for the workspace in the bake, `mios-drift-runner` reproduces the bash runner's pass/fail set exactly, and the Law 14 check is enforcing at the ratcheted level.
**Why:** the mechanical conscience of the tree is currently a bash loop -- a quoting slip silently skips checks, and the verb dispatcher pays process-spawn cost on every `/v1` call.
**Dep:** AGY-931..AGY-945 (templates for new crate files)

### AGY-961 -- Create the `mios-build` orchestrator crate and expose a `miosd build` seam that wraps (not replaces) build.sh  (WS-LANG-AUTO | P1 | L)  **[DONE]**
**Goal:** E-01 Compiled native tier -- gives every later phase port ONE stable compiled entrypoint so the strangler-fig migration has a facade to grow behind.
**What+How:** Add a library+binary crate `mios-build` to the `src/mios-rs` cargo workspace (whose `members` is currently just `["miosd"]`) and add a `Build` subcommand to the existing clap CLI in `src/mios-rs/miosd/src/main.rs` (today only `DriftCheck`/`Scaffold`). `miosd build` shells out to the unchanged `automation/build.sh` loop but models the phase list, exit-code policy and per-step timing as a typed `Phase` struct, so a phase can later be rerouted to native code without changing the outer call. Nothing destructive: build.sh remains the executor; miosd wraps and observes. Copy the `mios-bake-plan` crate's house style (toml+regex+std::process, root from `MIOS_ROOT`).
**Where:** `src/mios-rs/Cargo.toml, src/mios-rs/miosd/src/main.rs, src/mios-rs/mios-build/ (new), automation/build.sh, Containerfile (rust-builder stage lines 44-50)`
**Done When:** `cargo build --release` in `src/mios-rs` yields a miosd exposing `miosd build`; that command drives the existing 74-phase loop to the same exit code as `bash automation/build.sh`; `just drift-gate` stays green; no phase behavior changed.
**Why:** Without one orchestrator seam, each of the ~121 phase ports invents its own entrypoint and the migration fragments into 121 incompatible mini-CLIs. Realizes ROADMAP LANG-01 (T-272).
**Dep:** none

### AGY-962 -- Unify the two Rust workspaces so the Containerfile builds and ships EVERY native crate  (WS-LANG-AUTO | P1 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- makes native-first dispatch real: a binary that is referenced must actually exist in the image.
**What+How:** The Containerfile rust-builder stage only `COPY src/mios-rs` + `cargo build --release`, shipping just `/usr/libexec/mios/miosd`. Meanwhile `tools/native/Cargo.toml` lists only `mios-drift-runner`/`mios-version-check`/`mios-wallpaperd` in `members`, while `mios-bake-plan`, `mios-ssot-lint`, `mios-aiplane-lint` and `generate-names-registry` sit on disk unbuilt — so the `tools/native/target/release/mios-bake-plan` branch in `automation/85-bake-plan.sh` is DEAD in the bake and silently falls through to python. Add the four orphan crates to `members`, then either fold `tools/native` into the `src/mios-rs` workspace or add a second `COPY tools/native` + `cargo build --release` stage installing the release binaries into `/usr/libexec/mios/`. Stage explicit paths only (never `git add -A`; AGY shares the tree).
**Where:** `tools/native/Cargo.toml, src/mios-rs/Cargo.toml, Containerfile (rust-builder stage), automation/85-bake-plan.sh, automation/98-drift-checks.sh (lines 1539/4871 reference tools/native/target/release binaries)`
**Done When:** Every native crate appears in a workspace `members` list; a clean `cargo build --release` builds them all with zero warnings; `mios-bake-plan`/`mios-ssot-lint`/`mios-aiplane-lint` land under `/usr/libexec/mios` in the image; the bake log shows 85-bake-plan.sh and 98-drift-checks.sh hitting the native binary, not the python fallback.
**Why:** Today the tree CLAIMS a compiled path while python actually runs — a false-green that makes every "ported to Rust" statement unverifiable and blocks deleting any python fallback.
**Dep:** mios-build orchestrator crate seam

### AGY-963 -- Record golden masters (trycmd + insta) of current build-phase output before any phase is ported  (WS-LANG-AUTO | P1 | L)
**Goal:** E-01 Compiled native tier -- the parity oracle that lets fragile untested bash be refactored without silently changing generated bytes.
**What+How:** Add a `tests/` directory to the `mios-build` crate using `trycmd` for whole-CLI snapshots (stdout/stderr/exit code) of each render/generate phase run against a fixture `mios.toml`, plus `insta` `.snap` files for the generated ARTIFACTS themselves: the 34 rendered `.container` files, the `install.env` `MIOS_PORT_*` block (35), `kargs.d/*.toml` (75), `/usr/lib/kernel/cmdline` (76), `chrony.conf` (42), the nut `*.conf` set (43) and `plan.d/*.list` (85). Capture from the CURRENT bash/python phases so the recorded bytes are the oracle every port must reproduce. Commit fixtures + snapshots and document the `cargo insta review` accept flow.
**Where:** `src/mios-rs/mios-build/tests/ (new), tests/fixtures/mios.toml (new golden input), automation/{34,35,42,43,75,76,85}-*.sh (sources of current output), usr/share/mios/mios.toml (real SSOT shape reference)`
**Done When:** `cargo test -p mios-build` runs the golden suite; each snapshot matches the byte output of the corresponding current bash/python phase on the fixture; `cargo insta test` is green; a deliberately divergent port fails loudly with a diff.
**Why:** Without a recorded oracle, every port is a guess — a one-byte difference in a rendered Quadlet or kargs file ships as a boot/runtime regression that no test can catch.
**Dep:** mios-build orchestrator crate seam

### AGY-964 -- Replace build.sh's grep-blob phase policy with a typed `[build.phases]` registry projected from SSOT  (WS-LANG-AUTO | P1 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- turns phase selection and fatality from substring matching into typed data every phase port routes through.
**What+How:** build.sh gates phases with three fragile mechanisms: the `[0-9][0-9]-*.sh` glob (or `build_phases.json`), the `CONTAINERFILE_SCRIPTS` skip string, and the `NON_FATAL_SCRIPTS` blob — the last two matched by `grep -qF`, the exact ordinal/substring coupling flagged as TD-7. Define a typed `[build.phases]` table in `usr/share/mios/mios.toml` (ordinal, script, `fatal:bool`, `apply_class`), have `mios-build` deserialize it into a `Vec<Phase>` driving selection + fatal/non-fatal policy, and reduce build.sh to a thin shim that asks `miosd build --plan` for the ordered list. Keep the literal blobs as a `${...:-}` fallback until the registry is drift-gated equal. Mirror the table into mios-bootstrap per Law 15.
**Where:** `automation/build.sh (lines 223-250 CONTAINERFILE_SCRIPTS/NON_FATAL_SCRIPTS, 258-275 selection loop), usr/share/mios/mios.toml ([build.phases] new), src/mios-rs/mios-build/, C:/mios-bootstrap mios.toml mirror`
**Done When:** `miosd build --plan` emits the same ordered phase list and fatal/non-fatal classification build.sh computes today (golden-diff); a substring collision (e.g. a second `35-` script) can no longer misclassify a phase; both repos carry the identical `[build.phases]` table.
**Why:** A correctness-critical dispatch matched by substring means renaming a phase silently flips whether its failure aborts the build — a build that "succeeds" while a phase died.
**Dep:** mios-build orchestrator crate seam

### AGY-965 -- Make the image build reproducible: derive SOURCE_DATE_EPOCH from the git commit and pass --rewrite-timestamp  (WS-LANG-AUTO | P1 | M)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening -- reproducible bit-for-bit builds as the strongest provenance record behind the baked manifest.
**What+How:** The bake (`automation/build-mios.sh` → podman/buildah build over the Containerfile) sets no `SOURCE_DATE_EPOCH`, so layer and file mtimes vary per build, and `automation/88-finalize.sh` bakes a wall-clock `MIOS_BUILT=$(date -u ...)` into `/usr/share/mios/version`. Compute the epoch deterministically from the `$ROOT` git tree commit time (`git -C $ROOT log -1 --format=%ct`) in build-mios.sh, pass `--source-date-epoch` + `--rewrite-timestamp` to the build, and thread the same epoch into 88-finalize.sh so `MIOS_BUILT` is the commit time. Key off the commit rather than wall-clock precisely because build.sh already does `git reset --hard HEAD` before drift-checks, making the commit the stable reference.
**Where:** `automation/build-mios.sh (podman/buildah build invocation), automation/88-finalize.sh (MIOS_BUILT line 42), Containerfile, .github/workflows/mios-ci.yml (build job env)`
**Done When:** Two back-to-back local builds of the same commit produce byte-identical file timestamps in the layers; `MIOS_BUILT` equals the commit time; the build still passes `bootc container lint`.
**Why:** Wall-clock mtimes make every rebuild a different artifact, so no rebuild-and-compare gate can ever exist and "reproducible" stays an aspiration rather than a checkable claim.
**Dep:** none

### AGY-966 -- Add a rebuild-and-compare-digest gate that fails when two builds of one commit diverge  (WS-LANG-AUTO | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening -- converts reproducibility from a property that should hold into an enforced, continuously verified invariant.
**What+How:** With the deterministic epoch landed, add a CI job (scheduled/manual trigger given runner cost) that builds the image twice from the same commit and asserts identical manifest digests — or, cheaper, compares a normalized `syft`/file-listing hash of the two rootfs. Wire it as a fitness function alongside the existing drift-gate ordering in `mios-ci.yml`. Emit a miette-style report naming the FIRST divergent path so a non-reproducible file (embedded hostname, build path, random ordering) is immediately actionable rather than a bare digest mismatch.
**Where:** `.github/workflows/mios-ci.yml (new job after build), automation/build-mios.sh, src/mios-rs/mios-build/ (compare helper), automation/90-generate-sbom.sh (reuse syft output)`
**Done When:** The gate rebuilds and compares; an intentionally injected wall-clock file makes it fail and names the offending path; a clean tree passes; the job is documented as scheduled to bound runner cost.
**Why:** Reproducibility silently rots — one new phase embedding a timestamp or hostname undoes AGY-965 with nothing to notice.
**Dep:** Make the image build reproducible via SOURCE_DATE_EPOCH

### AGY-967 -- Gate the bake on `cargo fmt --check` + `clippy -D warnings` across both Rust workspaces  (WS-LANG-AUTO | P1 | S)  **[DONE]**
**Goal:** E-05 Test and CI governance -- gives the compiled plane the shellcheck-equivalent it never had, so Rust is trustworthy enough to subsume bash.
**What+How:** The `mios-ci.yml` drift-gate job installs shellcheck but runs zero Rust quality gates, so the growing native surface (`miosd`, `tools/native/*`) has no style or lint enforcement. Add `cargo fmt --all --check` and `RUSTFLAGS=-Dwarnings cargo clippy --workspace --all-targets` steps to the drift-gate job (which runs BEFORE `build`), for both the `src/mios-rs` and `tools/native` workspaces. Curate targeted restriction lints per crate root — `#![deny(clippy::unwrap_used, clippy::panic, clippy::todo)]` — to harden system verbs against panics, rather than blanket-enabling the whole restriction group.
**Where:** `.github/workflows/mios-ci.yml (drift-gate job, after 'Install shellcheck' line 59), src/mios-rs/miosd/src/main.rs (crate attrs), tools/native/*/src/main.rs`
**Done When:** CI fails on any unformatted file or clippy warning in either workspace; the restriction lints reject `unwrap`/`panic`/`todo` in shipped binaries; the gate runs in the drift-gate job ahead of the 17-minute bake.
**Why:** Moving off bash buys nothing if the replacement panics at runtime on an unwrap; without the gate, the compiled tier is less checked than the shell it replaces.
**Dep:** Unify the two Rust workspaces

### AGY-968 -- Add a fast cached `native-test` CI job that the bake `needs:`  (WS-LANG-AUTO | P1 | S)  **[DONE]**
**Goal:** E-05 Test and CI governance -- makes the golden and differential parity suites actually BLOCK a broken port instead of merely existing.
**What+How:** There is no `cargo test` anywhere in CI, so the golden/parity suites cannot gate. Add a `native-test` job to `mios-ci.yml` running `cargo test --workspace` for both `src/mios-rs` and `tools/native`, positioned so the existing `build` job declares `needs: native-test` — mirroring the negatives-before-main-gate ordering the pipeline already documents. Cache the cargo registry and target dirs so the job stays minutes, not tens of minutes.
**Where:** `.github/workflows/mios-ci.yml (new native-test job; build job `needs:`), src/mios-rs/mios-build/tests/, tools/native/*/tests/`
**Done When:** A failing golden or parity test fails `native-test` and blocks `build`; the job completes in a few minutes with a warm cache; it is green on the current tree.
**Why:** Tests that do not gate are decoration — a divergent port would otherwise sail into the expensive bake and only surface as a broken image.
**Dep:** Build a golden-capture harness (trycmd + insta)

### AGY-969 -- Add cargo-deny + cargo-audit gates and commit Cargo.lock in both workspaces as SBOM SSOT  (WS-LANG-AUTO | P2 | M)  **[DONE]**
**Goal:** E-05 Test and CI governance -- gives the Rust plane a dependency-security story and aligns Cargo.lock with the SBOM-not-hardcode model (ADR-0003).
**What+How:** Add a `deny.toml` to each workspace configuring advisories, banned/duplicate crates and a license allow-list that operationalizes the FOSS posture, plus a CI step running `cargo deny check` + `cargo audit` — new fitness-function gates mirroring the existing pod-quadlets and NO-HARDCODE gates. Ensure a committed `Cargo.lock` in each workspace (`src/mios-rs` already has one; `tools/native` does not) so dependency digests are recorded BUILD OUTPUTS rather than hand-pinned literals. Feed the resulting crate inventory into the existing `90-generate-sbom.sh` syft scan.
**Where:** `src/mios-rs/deny.toml (new), tools/native/deny.toml (new), src/mios-rs/Cargo.lock, tools/native/Cargo.lock, .github/workflows/mios-ci.yml, automation/90-generate-sbom.sh`
**Done When:** `cargo deny check` and `cargo audit` run in CI and fail on a banned crate, an open advisory, a duplicate version or a disallowed license; both `Cargo.lock` files are committed; the crate set appears in the generated SBOM.
**Why:** Every crate pulled into the shipped binaries is currently unaudited and unrecorded — an advisory-bearing transitive dep would ship into the image with no signal and no SBOM entry.
**Dep:** Unify the two Rust workspaces

### AGY-970 -- Add a `check_bash_phase_ratchet` drift-check so the automation/*.sh count can only decrease  (WS-LANG-AUTO | P2 | S)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- makes the shell-to-Rust migration a monotonic, visible burn-down enforced by the gate.
**What+How:** Add a `check_bash_phase_ratchet` that counts `automation/[0-9][0-9]-*.sh` files (and their total shell LOC) and FAILS if the count exceeds a committed baseline stored in `usr/share/mios/mios.toml` under `[build.ratchet].max_phase_scripts`. Each landed port lowers the baseline; a regression that re-adds a bash phase turns the gate red. Implement as a native check in `mios-drift-runner` (or a `check_*` function in 98-drift-checks.sh delegating to it), following the existing soft-to-hard ratchet convention (degrade-open first, then hard).
**Where:** `automation/98-drift-checks.sh (new check_* near the line-6311 registration block), tools/native/mios-drift-runner/src/main.rs, usr/share/mios/mios.toml ([build.ratchet] new), C:/mios-bootstrap mirror (Law 15)`
**Done When:** The check counts the current phase scripts, passes at the committed baseline, and fails when a new `NN-*.sh` is added without lowering it; it is wired into `just drift-gate`; both repos carry the ratchet key.
**Why:** Without a counter that only goes down, shell mass quietly creeps back in and the migration's progress becomes unmeasurable.
**Dep:** Replace build.sh grep-blob phase policy with a typed phase registry

### AGY-971 -- Build a typed figment-based `mios-config` resolver crate deserializing mios.toml into structs  (WS-LANG-AUTO | P1 | XL)  **[DONE]**
**Goal:** E-01 Compiled native tier -- the resolver core: ONE typed engine behind SSOT-operator-defined, replacing three parallel string-munging resolvers.
**What+How:** Collapse the three parallel resolvers — `usr/lib/mios/mios_toml.py` (601 lines), the `tools/lib/userenv.sh` + `usr/lib/mios/userenv.sh` twins (325 lines each), and `automation/lib/globals.ps1` — into one `mios-config` crate using `figment` (defaults→TOML→env→CLI precedence) to deserialize `usr/share/mios/mios.toml` into typed structs (Ports, Colors, Sidecars, Services, Kargs, Compliance…). The struct IS the schema; deserialization fails loudly with miette diagnostics pointing at the offending line/span. The `MIOS_*` env projection becomes serialization FROM the typed model, feeding the zero-hardcodes campaign (ports/paths/colors stop being hand-parsed). Ship it as a library consumed by both `mios-build` and the future verb dispatcher; leave the shell/python resolvers live in parallel until parity is gated.
**Where:** `src/mios-rs/mios-config/ (new crate), usr/lib/mios/mios_toml.py, tools/lib/userenv.sh, usr/lib/mios/userenv.sh, automation/lib/globals.ps1, usr/share/mios/mios.toml, automation/98-drift-checks.sh (check_userenv_parity line 1757)`
**Done When:** `mios-config` deserializes the real `mios.toml` into typed structs; a malformed value yields a miette span diagnostic naming the line; the crate builds warning-clean and is unit-tested; all three legacy resolvers remain untouched and parallel in this task.
**Why:** Three hand-rolled resolvers over one file is TD-3's core: a wrong value silently breaks a Quadlet or a boot karg with no type error anywhere — and every projection downstream inherits the mis-parse.
**Dep:** mios-build orchestrator crate seam

### AGY-972 -- Demote both userenv.sh twins to shims over `miosd resolve --shell` and PROVE parity with proptest  (WS-LANG-AUTO | P1 | L)  **[DONE]**
**Goal:** E-01 Compiled native tier -- retires the hand-maintained shell resolver twins and mechanizes the Law-13 parity check into a differential proof.
**What+How:** Add `miosd resolve --shell` emitting `KEY=VAL` export lines from the `mios-config` typed model, then rewrite `tools/lib/userenv.sh` and `usr/lib/mios/userenv.sh` as thin shims doing `eval "$(miosd resolve --shell)"` with the current manual tuples retained as a `${...:-}` fallback. Prove byte-equivalence with a `proptest` differential test: generate random-but-valid `mios.toml` documents, feed BOTH the legacy shell resolver and `miosd resolve --shell`, and assert the emitted `MIOS_*` key set and values are identical, auto-shrinking to a minimal failing document. This also reconciles the short-vs-long key mismatch (`MIOS_AI_VLLM_*` emitted vs `MIOS_VLLM_*` consumed) recorded in the unified-key-library memo.
**Where:** `src/mios-rs/miosd/src/main.rs (resolve subcommand), src/mios-rs/mios-config/, tools/lib/userenv.sh, usr/lib/mios/userenv.sh, src/mios-rs/mios-config/tests/parity_proptest.rs (new), automation/98-drift-checks.sh (check_userenv_parity)`
**Done When:** `miosd resolve --shell` output diffs identically against sourcing userenv.sh across the whole proptest corpus; both twins are shims with literal fallback; `check_userenv_parity` passes against the native emitter; both repos updated (Law 15).
**Why:** Two hand-maintained resolver copies with no generator binding them WILL diverge, and a spot-check cannot show they agree on inputs nobody thought to try.
**Dep:** Build a typed figment-based config resolver crate

### AGY-973 -- Port 35-render-ports.sh (awk stack_id offset) to `miosd render-ports` behind a shim  (WS-LANG-AUTO | P2 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- the first end-to-end proof of the strangler loop on a small leaf phase with tricky arithmetic.
**What+How:** `35-render-ports.sh` extracts `[ports]` from mios.toml with a hand-rolled awk block, applies the `stack_id*10000` offset to every numeric value EXCEPT literal 53, uppercases keys, and appends `MIOS_PORT_*` to `/etc/mios/install.env` after a `sed -i '/^MIOS_PORT_/d'`. Reimplement as `miosd render-ports` reading the typed Ports struct from `mios-config`, replicating the offset math and the port-53 carve-out exactly. Demote the bash to a shim calling the binary, keeping the awk as a `command -v miosd ||` fallback. Gate byte-parity of the emitted `install.env` block against the golden snapshot before deleting the awk.
**Where:** `automation/35-render-ports.sh, src/mios-rs/miosd/src/main.rs (render-ports), src/mios-rs/mios-config/, src/mios-rs/mios-build/tests/ (install.env snapshot)`
**Done When:** `miosd render-ports` writes an `install.env` `MIOS_PORT_*` block byte-identical to the current awk on the fixture (including the stack_id offset and the 53 exclusion); 35-render-ports.sh is a shim; the golden test is green; the awk is deleted only after parity is confirmed.
**Why:** The 53 carve-out is exactly the kind of detail a rewrite drops — proving it here validates the port/parity/demote loop before it is applied to riskier phases.
**Dep:** Demote userenv.sh to a shim behind miosd resolve --shell

### AGY-974 -- Port 34-render-quadlets.sh to `miosd render-quadlets` and DERIVE the placeholder allowlist instead of hand-listing it twice  (WS-LANG-AUTO | P2 | L)  **[DONE]**
**Goal:** E-01 Compiled native tier -- removes an actively-drifting duplicated allowlist and an in-place shell expansion surface by deriving the key set from the typed model.
**What+How:** `34-render-quadlets.sh` expands `${MIOS_*:-default}` placeholders in-place across `QUADLET_DIRS` using TWO hand-maintained ~130-variable allowlists that already disagree (the `envsubst` string in `_render_with_envsubst` and the bash array in `_render_with_bash`), plus a special-case `sed` for mios-llm-heavy multi-LoRA. Reimplement as `miosd render-quadlets` resolving placeholders from the `mios-config` typed model, so the allowlist becomes the set of SERIALIZED KEYS — derived, never hand-listed, therefore incapable of diverging. Preserve the idempotent "only rewrite files containing `${MIOS_`" behavior, the mtime-preserving `cmp`, and the getent-based `MIOS_CODE_SERVER_UID/GID` resolution. Demote bash to a shim with the envsubst path as fallback; parity-gate every rendered `.container` against golden `.snap` files.
**Where:** `automation/34-render-quadlets.sh (lines 61-142 dual allowlist), src/mios-rs/miosd/src/main.rs (render-quadlets), src/mios-rs/mios-config/, src/mios-rs/mios-build/tests/ (rendered-quadlet snapshots)`
**Done When:** `miosd render-quadlets` produces byte-identical rendered `.container`/`.network`/`.pod` files versus the current envsubst path across the fixture set; the placeholder key set is derived from the config model with no hand-maintained list left; the llm-heavy LoRA special case still applies; bash is a shim; snapshots green.
**Why:** The two allowlists have ALREADY drifted, so some placeholders resolve on one code path and stay literal `${MIOS_...}` on the other — a Quadlet that starts with a broken value.
**Dep:** Demote userenv.sh to a shim behind miosd resolve --shell

### AGY-975 -- Port generate-pod-quadlets.py (504ln) + 33-generate-quadlets.sh to `miosd generate-quadlets --check`  (WS-LANG-AUTO | P2 | XL)  **[DONE]**
**Goal:** E-01 Compiled native tier -- kills the recurring quadlet-digest-drift class at its source by making Quadlet generation a compiled, diff-gated projection.
**What+How:** `33-generate-quadlets.sh` invokes `tools/generate-pod-quadlets.py` (504 lines) to synthesize `.pod`/`.container`/`.network` files from mios.toml into `usr/share/containers/systemd`. Port the generator to `miosd generate-quadlets` over the `mios-config` typed model, modeled on the existing `mios-bake-plan` crate's write-or-diff `--check` pattern. CRITICAL invariant taken from 33's own header: the generator must resolve placeholders BARE (fallback defaults, NOT userenv-baked values) so output matches drift-check 13's `--check` — do not source the resolver at generate time. Demote both the `.sh` and the `.py` behind the binary (python fallback retained), gate `miosd generate-quadlets --check` == committed tree, then delete the python.
**Where:** `automation/33-generate-quadlets.sh, tools/generate-pod-quadlets.py, src/mios-rs/miosd/src/main.rs (generate-quadlets), usr/share/containers/systemd/, automation/98-drift-checks.sh (check 13)`
**Done When:** `miosd generate-quadlets` reproduces every committed Quadlet byte-for-byte in bare-env mode; `--check` drives drift-check 13; the generated `@sha256` pins survive regeneration; the python fallback is removed after CI-gated parity, staged by explicit path.
**Why:** Quadlet regeneration is today a hand-staged step, which is why a broad `git add` keeps stripping digest pins and turning the pod-quadlets gate red.
**Dep:** Demote userenv.sh to a shim behind miosd resolve --shell

### AGY-976 -- Port 75-kargs-render.sh's inline tomllib block to `miosd render-kargs`  (WS-LANG-AUTO | P2 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- moves boot-integrity-critical kernel argument generation into typed compiled code with an exact-diff gate.
**What+How:** `75-kargs-render.sh` embeds a ~95-line inline `python3 -c` that reads `[kargs]` from mios.toml, rewrites `usr/lib/bootc/kargs.d/01-mios-vfio.toml` (iommu intel/amd/on selection, vfio_ids) and generates or REMOVES `99-mios-kargs.toml` (hugepages/isolcpus/nohz_full/rcu_nocbs/THP). Reimplement as `miosd render-kargs` over the typed Kargs struct, preserving exact list ordering and dedup plus the deterministic TOML formatting (header comments, trailing-comma handling) so the drift-gate diff holds. Demote the `.sh` to a shim with the inline python as fallback; parity-gate both generated `kargs.d/*.toml` against golden snapshots.
**Where:** `automation/75-kargs-render.sh (inline python lines 37-133), src/mios-rs/miosd/src/main.rs (render-kargs), usr/lib/bootc/kargs.d/{01-mios-vfio.toml,99-mios-kargs.toml}, src/mios-rs/mios-build/tests/`
**Done When:** `miosd render-kargs` produces byte-identical `kargs.d/*.toml` — including the iommu/vfio logic and the stale-file removal — versus the inline python on the fixture; the `.sh` is a shim; snapshots green; python removed after CI parity.
**Why:** These kargs flow into the signed UKI cmdline (SELinux enforcing, vfio binding); a wrong or dropped karg is an unbootable or unenforced system produced by an untested inline heredoc.
**Dep:** Build a typed figment-based config resolver crate

### AGY-977 -- Port generate-uki-cmdline.py + 76-uki-render.sh to `miosd render-uki-cmdline --check`  (WS-LANG-AUTO | P2 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- keeps the single authoritative kargs.d flattener compiled and diff-gated so gate 93 cannot be defeated by a second implementation.
**What+How:** `76-uki-render.sh` calls `tools/generate-uki-cmdline.py`, the SINGLE authoritative flattener of `usr/lib/bootc/kargs.d/*.toml` into `usr/lib/kernel/cmdline` that drift-check 93 validates via `--check`. Port it to `miosd render-uki-cmdline`, reproducing the exact ordering, dedup and spacing (a divergent flatten fails gate 93), with a `--check` mode mirroring `mios-bake-plan`. Preserve the install-to `/usr/lib/kernel/cmdline` step and the source==dest skip. Demote both the `.sh` and the `.py` behind the binary; run old and new side by side and diff to identical before deleting the python.
**Where:** `automation/76-uki-render.sh, tools/generate-uki-cmdline.py, src/mios-rs/miosd/src/main.rs (render-uki-cmdline), usr/lib/kernel/cmdline, automation/98-drift-checks.sh (check 93)`
**Done When:** `miosd render-uki-cmdline --check` reproduces the committed `/usr/lib/kernel/cmdline` exactly and drives drift-check 93; the flattened cmdline is byte-identical to the python's on the fixture; the python is removed post-parity.
**Why:** The cmdline is baked INSIDE the signed UKI and cannot be edited at the boot menu — a divergent flattener ships a wrong, unfixable-at-boot kernel command line.
**Dep:** Port 75-kargs-render.sh to miosd render-kargs

### AGY-978 -- Port 42-chrony-render.sh's inline python to `miosd render-chrony`  (WS-LANG-AUTO | P3 | S)  **[DONE]**
**Goal:** E-01 Compiled native tier -- a low-risk leaf port that also exercises the mios-config network struct end to end.
**What+How:** `42-chrony-render.sh` embeds a `python3 -c` reading `[network.ntp].servers` and writing `/etc/chrony.conf` from a fixed template (iburst, driftfile, makestep, PHC refclock, rtcsync). Its header stresses DETERMINISTIC, build-host-independent output (no keying off `/dev/ptp0`) so drift-check 55 passes. Reimplement as `miosd render-chrony` over the typed model, emitting the identical canonical template. Demote to a shim with the inline python as fallback and parity-gate `chrony.conf` against the golden snapshot.
**Where:** `automation/42-chrony-render.sh (inline python lines 29-93), src/mios-rs/miosd/src/main.rs (render-chrony), src/mios-rs/mios-build/tests/ (chrony.conf snapshot), automation/98-drift-checks.sh (check 55)`
**Done When:** `miosd render-chrony` writes a `chrony.conf` byte-identical to the current python on the fixture and passing drift-check 55; the `.sh` is a shim; the snapshot is green.
**Why:** Leaving it as inline python keeps one more untestable heredoc in the phase set and one more chance for build-host state to leak into a shipped config file.
**Dep:** Build a typed figment-based config resolver crate

### AGY-979 -- Port 43-nut-render.sh's inline python (4 config files) to `miosd render-nut`  (WS-LANG-AUTO | P3 | S)  **[DONE]**
**Goal:** E-01 Compiled native tier -- exercises the typed model's optional-section handling on a real multi-file renderer.
**What+How:** `43-nut-render.sh` embeds a `python3 -c` reading `[power.ups]` and writing four `/etc/ups/` files — `nut.conf` (MODE), `ups.conf` (driver/port/desc), `upsd.conf` (LISTEN), `upsmon.conf` (MONITOR) — with a name-present versus inert (`MODE=none`) branch. Reimplement as `miosd render-nut` over the typed Power/Ups struct, reproducing the exact file bodies and the inert-when-unnamed behavior. Demote to a shim with the python as fallback; parity-gate all four files against golden snapshots.
**Where:** `automation/43-nut-render.sh (inline python lines 29-112), src/mios-rs/miosd/src/main.rs (render-nut), src/mios-rs/mios-build/tests/ (nut *.conf snapshots)`
**Done When:** `miosd render-nut` produces byte-identical `nut.conf`/`ups.conf`/`upsd.conf`/`upsmon.conf` in BOTH the named and inert cases versus the python; the `.sh` is a shim; snapshots green; python removed post-parity.
**Why:** The inert branch is the case nobody tests — an optional-section mis-handling here writes a live UPS MODE into an image that has no UPS, or drops monitoring on one that does.
**Dep:** Build a typed figment-based config resolver crate

### AGY-980 -- Port 44-firewall-ports.sh + 45-firewall.sh to `miosd firewall-ports` emitting offline firewalld config  (WS-LANG-AUTO | P2 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- derives the LAN reachability contract from the typed Ports model instead of ~15 hand-listed `${VAR:-8800}` literals.
**What+How:** `44-firewall-ports.sh` calls `firewall-offline-cmd --add-port=${MIOS_PORT_*}/tcp` for roughly 15 services using literal `${VAR:-8800}`-style fallbacks; `45-firewall.sh` (84 lines) does the zone/service wiring. Reimplement port selection as `miosd firewall-ports` reading the typed Ports struct and either emitting the exact `firewall-offline-cmd` argument list (executed by a thin shim) or writing the zone XML directly. Preserve the special cases: the `--add-service=ssh` :22 Forge-squat note, the ADGUARD DNS tcp+udp pair, and the mios-pxe service. Demote bash to a shim, keep the `${VAR:-LIT}` fallbacks, and golden-diff the resulting zone XML.
**Where:** `automation/44-firewall-ports.sh, automation/45-firewall.sh, src/mios-rs/miosd/src/main.rs (firewall-ports), src/mios-rs/mios-config/, src/mios-rs/mios-build/tests/ (zone XML snapshot)`
**Done When:** `miosd firewall-ports` opens the identical port/service set (verified via generated zone XML or captured `firewall-offline-cmd` args) as the current bash on the fixture; special cases preserved; the `.sh` is a shim; the snapshot is green.
**Why:** Hand-listed port literals in the firewall phase drift from the SSOT ports, so a re-allocated port is either unreachable on the LAN or left open under its old number.
**Dep:** Demote userenv.sh to a shim behind miosd resolve --shell

### AGY-981 -- Finish the mios-bake-plan native cutover and DELETE the python fallback in 85-bake-plan.sh  (WS-LANG-AUTO | P2 | S)  **[DONE]**
**Goal:** E-01 Compiled native tier -- proves the strangler's final "delete the legacy slice" step on a case that is already ported and already matching.
**What+How:** `tools/native/mios-bake-plan` already implements the full generator with `--check` byte-parity (writes `plan.d/*.list`, `firstboot.list`, `bound-images.tsv`, validates core/firstboot membership). `85-bake-plan.sh` dispatches native-first, but the binary is not built in the bake (see the workspace-unify task), so it silently falls to `tools/generate-bake-plan.py`. Once the binary ships: add a parity CI step asserting `mios-bake-plan --check` == committed `plan.d`, remove the `elif ... python3 tools/generate-bake-plan.py` branch from 85-bake-plan.sh, and delete `tools/generate-bake-plan.py` (282 lines) with explicit-path staging. Wire `mios-bake-plan --check` into the drift gate as the plan.d gate.
**Where:** `automation/85-bake-plan.sh (lines 24-39 dispatch), tools/native/mios-bake-plan/src/main.rs, tools/generate-bake-plan.py (delete), automation/98-drift-checks.sh`
**Done When:** 85-bake-plan.sh invokes only the native binary; `tools/generate-bake-plan.py` no longer exists; `mios-bake-plan --check` gates `plan.d` in CI; the bake log shows the native path; drift-gate green.
**Why:** Two generators for the bake plan means the one that actually runs is decided by build accident — and 282 lines of duplicate python keep drifting from the Rust that supposedly replaced them.
**Dep:** Unify the two Rust workspaces

### AGY-982 -- Port generate-cosign-policy.py + 49-cosign-policy.sh to `miosd cosign-policy` and pin the LEGACY sigstore attachment format  (WS-LANG-AUTO | P2 | M)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening -- hardens the OS plane's highest-leverage boot-integrity control against the silent-verify-failure class.
**What+How:** `49-cosign-policy.sh` installs cosign (v2.x-pinned), TUF roots and `policy.json`; `tools/generate-cosign-policy.py` (64 lines) generates the sigstoreSigned policy. Port policy generation to `miosd cosign-policy` over a typed `[security.cosign]` model emitting the same jq-parse-clean `policy.json`. While there, close the two boot-integrity gaps from the research: target the LEGACY sigstore attachment format (cosign v3's new bundle format is undiscoverable by containers-image/rpm-ostree, giving a silent upgrade-verify failure), and keep enforcement in the on-disk `policy.json` because `bootc upgrade` has no `--enforce-container-sigpolicy`. Demote the `.py` behind the binary, keep the cosign-install bash as a thin shim, golden-diff `policy.json`.
**Where:** `automation/49-cosign-policy.sh, tools/generate-cosign-policy.py, src/mios-rs/miosd/src/main.rs (cosign-policy), usr/lib/containers/policy.json, usr/share/mios/mios.toml ([security.cosign])`
**Done When:** `miosd cosign-policy` emits a `policy.json` byte-identical to the python's and jq-clean; the policy uses `matchRepository` sigstoreSigned with the legacy attachment format documented and pinned; the `.py` is removed post-parity; drift-gate green.
**Why:** If the signature format silently stops being discoverable, `bootc upgrade` verifies nothing while appearing to succeed — the whole signed-image chain becomes decorative.
**Dep:** Build a typed figment-based config resolver crate

### AGY-983 -- Port the hardening service-enable family (51-hardening.sh + siblings) to `miosd harden` over an SSOT unit roster  (WS-LANG-AUTO | P2 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- makes the security-critical enabled-unit set typed SSOT data rather than ln-loops scattered across four phases.
**What+How:** `51-hardening.sh` symlinks usbguard/auditd/fapolicyd into `multi-user.target.wants`, chmods the usbguard/fapolicyd configs, runs `fagenrules`/`fapolicyd-cli`, and materializes `clevis-luks.env` via `mios-clevis-luks-gen`. Sibling phases (38-selinux, 40-fapolicyd-trust, 78-greenboot) repeat the same "if unit exists, `ln -sf` into `.wants`" pattern. Reimplement the service-enable plus config-permission logic as `miosd harden` driven by a typed `[security.enable_units]`/privileged roster in SSOT, so the enabled set is data. Preserve degrade-open (missing unit → skip). Demote 51 to a shim that still invokes the genuinely-external tool calls (fagenrules, clevis); golden-diff the created symlink set.
**Where:** `automation/51-hardening.sh, automation/40-fapolicyd-trust.sh, automation/78-greenboot.sh, src/mios-rs/miosd/src/main.rs (harden), usr/lib/systemd/system/multi-user.target.wants/, usr/share/mios/mios.toml ([security.enable_units])`
**Done When:** `miosd harden` creates the identical `.wants` symlink set and config permissions as the current bash on a fixture rootfs; the enabled-unit list lives in SSOT; 51-hardening.sh is a shim delegating external-tool calls; the golden test is green.
**Why:** A security service that quietly fails to be enabled — because its `ln -sf` sits in a phase nobody diffs — ships an image that only looks hardened.
**Dep:** Build a typed figment-based config resolver crate

### AGY-984 -- Port 05-repos.sh's repo-file heredocs to a typed `[repos]` model behind `miosd render-repos`  (WS-LANG-AUTO | P3 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier -- unfreezes SSOT-projectable repo data from heredocs while leaving genuinely-imperative dnf driving in shell.
**What+How:** `05-repos.sh` hand-writes `/etc/yum.repos.d/fedora-44.repo` via two large heredocs (vendored-mirror versus metalink variants), injects `priority=98` into `fedora*`/`ublue*` repos with `sed`, and toggles `install_weak_deps`. Move the repo-file GENERATION to `miosd render-repos` driven by a typed `[repos]` model (baseurl/metalink/priority/gpgcheck flags), emitting both variants deterministically; leave the dnf upgrade/distro-sync orchestration as shell. Demote the heredoc/sed portion to a shim call and golden-diff the generated `.repo` files for both branches.
**Where:** `automation/05-repos.sh (heredocs lines 54-109, priority sed 19-26), src/mios-rs/miosd/src/main.rs (render-repos), usr/share/mios/mios.toml ([repos] new), src/mios-rs/mios-build/tests/ (fedora-44.repo snapshots)`
**Done When:** `miosd render-repos` produces byte-identical `fedora-44.repo` for both vendored-mirror and metalink modes; the priority injection is preserved; dnf driving remains shell; snapshots green.
**Why:** Repo URLs and priorities hardcoded in heredocs are unfloatable literals — an operator cannot point the build at a different mirror without editing a build script.
**Dep:** Build a typed figment-based config resolver crate

### AGY-985 -- Port the packages.sh awk resolver library to `miosd packages` behind the shim helpers  (WS-LANG-AUTO | P2 | L)
**Goal:** E-01 Compiled native tier -- removes TD-3's hand-rolled-parsing risk from the code path that decides what actually gets installed.
**What+How:** `automation/lib/packages.sh` resolves `[packages.<section>].pkgs` and `.enable` with bespoke awk/grep across a 5-path override cascade (`get_packages_from_toml`, `_is_section_enabled`, `_get_pkgs_from_single_toml`) plus a `build_catalog_authoritative` `package_sets.json` branch. Add `miosd packages list <section>` and `miosd packages enabled <section>` over the `mios-config` typed model honoring the same override precedence, then rewrite the `install_packages`/`_strict`/`_optional` helpers to call the binary with the awk retained as a `${...:-}` fallback. Keep the actual `dnf install` in shell. Prove parity with a differential test enumerating EVERY section against both resolvers.
**Where:** `automation/lib/packages.sh, src/mios-rs/miosd/src/main.rs (packages), src/mios-rs/mios-config/, src/mios-rs/mios-config/tests/ (section-enumeration parity), usr/share/mios/mios.toml ([packages.*])`
**Done When:** `miosd packages list/enabled` returns identical package sets and enable state to the awk helpers across all sections (differential test); `install_packages*` call the binary with awk fallback; dnf stays shell; the parity test is green.
**Why:** A mis-parse in the awk cascade silently DROPS a package, so the image is missing software with no error anywhere in the build log.
**Dep:** Build a typed figment-based config resolver crate

### AGY-986 -- Replace 88-finalize.sh's os-release `sed` loop with a typed `miosd finalize-osrelease` version projector  (WS-LANG-AUTO | P3 | S)  **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one compiled owner of the single-source version projection, so the OS identity fields stop being a hand-written sed list.
**What+How:** 88-finalize.sh currently re-projects the one SSOT version onto every version-bearing field of `/usr/lib/os-release` (VERSION, VERSION_ID, BUILD_ID, IMAGE_VERSION, OSTREE_VERSION, PRETTY_NAME, CPE_NAME) with `sed -i` (lines 46-59) and writes `/usr/share/mios/version` (lines 39-44). Move that rewrite into a `miosd finalize-osrelease` subcommand that takes the version from the typed config model / VERSION file so the field list is code, not a sed loop; drift-check 42 keeps os-release equal to SSOT. Leave `systemctl preset-all` / `set-default` in shell. Demote the sed block to a shim call and golden-diff the produced os-release against the fixture.
**Where:** `automation/88-finalize.sh (lines 46-59 os-release sed, 39-44 version file), src/mios-rs/miosd/src/main.rs (finalize-osrelease), usr/lib/os-release, automation/98-drift-checks.sh (check 42)`
**Done When:** `miosd finalize-osrelease` rewrites all seven os-release fields and writes the version file identically to the sed block on a fixture os-release, drift-check 42 passes, the systemctl calls remain shell, and the golden test is green.
**Why:** Today the OS identity fields are re-derived by a fragile sed loop; a missed or mis-anchored pattern silently ships an image whose os-release disagrees with SSOT, and the version-unification (TD-2) work has no compiled field list to build on.
**Dep:** Build a typed figment-based config resolver crate

### AGY-987 -- Emit the build version-manifest from a structured `mios-build` API instead of TSV-append shell  (WS-LANG-AUTO | P3 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- make the `:latest`-intent -> observed-version record a typed build artifact feeding the SBOM rather than scattered shell appends.
**What+How:** `automation/lib/common.sh`'s `record_version()` (lines 103-112) appends TSV rows to `MIOS_VERSION_MANIFEST` from nearly every phase, and build.sh later flattens that file (lines 597-641) and captures skopeo Quadlet digests (lines 582-595). Add a `mios-build` manifest API plus a `miosd record-version` CLI that writes a structured manifest (TSV for compatibility + JSON), have phases and shims call it, and move the post-build skopeo digest-capture loop into `miosd capture-quadlet-digests`. Keep `record_version()` in common.sh as a thin wrapper over the binary so un-ported phases keep working unchanged.
**Where:** `automation/lib/common.sh (record_version lines 103-112), automation/build.sh (manifest flatten lines 597-641, digest capture 582-595), src/mios-rs/mios-build/, src/mios-rs/miosd/src/main.rs (record-version, capture-quadlet-digests)`
**Done When:** `miosd record-version` and `miosd capture-quadlet-digests` produce the same manifest rows and columns as today's shell, common.sh's `record_version` is a wrapper over the binary, the build's version manifest is generated by the compiled path, and the drift-gate is green.
**Why:** Version provenance is the backbone of SBOM-not-hardcode, yet it is assembled by ad-hoc TSV appends from dozens of phases with no schema — a malformed or dropped row corrupts the provenance record with no error and no way for the SBOM to detect it.
**Dep:** mios-build orchestrator crate seam

### AGY-988 -- Port the firstboot-token bound-images binding from 01-system-files-overlay.sh into `miosd overlay-bind-images`  (WS-LANG-AUTO | P2 | M)  **[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- keep the multi-GB firstboot whales out of bootc's deploy-time pull so offline USB deploy stays viable.
**What+How:** `automation/01-system-files-overlay.sh` (270 lines) symlinks every Quadlet into `/usr/lib/bootc/bound-images.d` EXCEPT those whose `Image=` substring-matches a `firstboot_tokens` entry (the GPU whales, ~47GB, deliberately evicted from the bake). It parses tokens with `grep|sed` and does the `Image=` match with sed+glob loops, then strips `.gitkeep`. Port ONLY that binding block (lines 184-227) to a `miosd overlay-bind-images` subcommand driven by the typed firstboot-token list, reusing `mios-bake-plan`'s existing `is_firstboot`/token logic rather than adding a second parser; leave the tar overlay stages in shell. Preserve degrade-open (unreadable tokens -> bind everything) and the symlinks-only invariant (`bootc install` rejects a regular file there). Golden-diff the created symlink set against a fixture Quadlet dir.
**Where:** `automation/01-system-files-overlay.sh (LBI block lines 184-227), src/mios-rs/miosd/src/main.rs (overlay-bind-images), tools/native/mios-bake-plan/src/main.rs (is_firstboot reuse), usr/lib/bootc/bound-images.d/`
**Done When:** `miosd overlay-bind-images` produces an identical `bound-images.d` symlink set on a fixture (firstboot whales excluded, `.gitkeep` stripped, symlinks only), the tar stages remain shell, degrade-open still holds, and the golden test is green.
**Why:** A single mis-matched substring binds a firstboot whale and makes bootc pull ~47GB at install time, which breaks offline USB deploy outright — high-consequence matching logic currently living in a duplicate `grep|sed` parser that can drift from `mios-bake-plan`'s.
**Dep:** Unify the two Rust workspaces

### AGY-989 -- Port 90-generate-sbom.sh to `miosd sbom` and publish the SBOM as a signed cosign attestation  (WS-LANG-AUTO | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- promote the SBOM from an in-tree file to verifiable OCI provenance attached to the published digest.
**What+How:** `automation/90-generate-sbom.sh` is degrade-open (it never fails the build) and drives syft to emit CycloneDX + SPDX into `/usr/share/mios/artifacts/sbom`. Port the orchestration — syft-present check, SSOT-pinned version resolution, exclude globs, version labeling — into a `miosd sbom` subcommand that always exits 0, keeping syft itself as the external scanner the binary invokes. Then extend the publish leg with `cosign attest --type spdx` / `--type cyclonedx` against the pushed image digest, next to the existing keyless-sign step (mios-ci.yml ~line 451), so downstream bootc hosts and scanners can `cosign verify-attestation` instead of reading a file inside the image. Demote the .sh to a shim; the SBOM bytes stay identical.
**Where:** `automation/90-generate-sbom.sh, src/mios-rs/miosd/src/main.rs (sbom), .github/workflows/mios-ci.yml (cosign attest step near the keyless-sign step line 451), usr/share/mios/artifacts/sbom/`
**Done When:** `miosd sbom` reproduces the same CycloneDX + SPDX artifacts and still exits 0 with syft or egress missing, CI attaches the SBOM as a cosign attestation on the image digest, and `cosign verify-attestation --type spdx` succeeds against a published image.
**Why:** An SBOM that only exists inside the image proves nothing to a consumer verifying the pulled ref; without an attestation the ADR-0003 provenance loop stays open and no puller can check what they actually received.
**Dep:** mios-build orchestrator crate seam

### AGY-990 -- Give greenboot real required.d health-checks (Hermes /v1 + core Quadlets) backed by `miosd healthcheck`  (WS-LANG-AUTO | P2 | M)
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- make boot-counted rollback actually detect a broken update instead of rubber-stamping it.
**What+How:** `automation/78-greenboot.sh` only symlinks greenboot units and chmods scripts under `/etc/greenboot/check/{required,wanted}.d` — the checks themselves are minimal, so a signed-but-broken publish rolls a fleet forward with no auto-recovery. Author real `required.d` checks asserting the MiOS SLAs: the canonical Hermes endpoint responds on `/v1` (degrade-open via `${MIOS_PORT_HERMES:-8642}`, per the AI-endpoint canon), core Quadlets are active, and drift-critical services are up. Implement the check logic as a compiled `miosd healthcheck` that the greenboot script calls, so it is typed and unit-testable, keeping 78-greenboot.sh as thin enable/chmod shell. Note the greenboot-rs (F43) migration in a comment for the follow-on.
**Where:** `automation/78-greenboot.sh, /etc/greenboot/check/required.d/ (new checks), src/mios-rs/miosd/src/main.rs (healthcheck), usr/share/mios/mios.toml (canonical ports for the SLA set)`
**Done When:** `required.d` contains a Hermes + core-Quadlet health-check backed by `miosd healthcheck`, a simulated Hermes-down marks the boot red and exercises the rollback path in a unit test or harness, 78-greenboot.sh stays thin enable/chmod, and the drift-gate is green.
**Why:** Hollow greenboot checks defeat the entire boot-counted rollback promise: signing, UKI and composefs are worthless if a broken update boots "green" and the fleet has no way back.
**Dep:** Demote userenv.sh to a shim behind miosd resolve --shell

### AGY-991 -- Port the oscap severity gate (86-oscap-compliance.sh + mios-oscap-gate) into one `miosd compliance-gate`  (WS-LANG-AUTO | P3 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- collapse an inline-python config read plus a separate python verdict gate into a single typed compliance path.
**What+How:** `automation/86-oscap-compliance.sh` reads `[compliance]` through an inline `python3` tomllib helper (`_toml_get`, lines 22-38), locates the SSG datastream from the scap-security-guide RPM, runs `oscap xccdf eval`, then hands the verdict to `usr/libexec/mios/mios-oscap-gate`, which counts FAILED rules at or above a severity. Port the config read, datastream resolution and verdict orchestration into `miosd compliance-gate` over the typed `[compliance]` struct, keeping `oscap` itself as the external scanner; preserve DEFAULT-OFF degrade-open (`enabled != true` -> exit 0) and fail-closed once opted in. Fold the mios-oscap-gate ARF parsing into the binary. Golden-test both the disabled no-op and a fixture-ARF verdict.
**Where:** `automation/86-oscap-compliance.sh (_toml_get lines 22-38, orchestration), usr/libexec/mios/mios-oscap-gate, src/mios-rs/miosd/src/main.rs (compliance-gate), usr/share/mios/mios.toml ([compliance])`
**Done When:** `miosd compliance-gate` reproduces the disabled no-op and, on a fixture ARF, the same at/above-severity verdict as mios-oscap-gate, the datastream resolution matches, 86 is a thin shim invoking oscap plus the binary, and the golden tests are green.
**Why:** Compliance posture is currently decided by an inline python tomllib blob in a shell phase plus a second python parser — two fragile pieces that can disagree about whether a build is compliant, and neither is testable without a full oscap run.
**Dep:** Build a typed figment-based config resolver crate

### AGY-992 -- Declare a `[build.phases].deps` DAG in SSOT and run independent phases concurrently in `mios-build`  (WS-LANG-AUTO | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- let the compiled orchestrator own ordering and concurrency, which is the concrete payoff of moving build policy out of bash.
**What+How:** `automation/build.sh` runs every phase strictly sequentially by ordinal, yet the bake builders 65-bake-hyprland, 66-bake-quickshell, 67-bake-surfer, 68-bake-kvmfr and 69-bake-lookingglass-client have no interdependencies and dominate wall time. Add a `deps` field to the `[build.phases]` registry in mios.toml, build the DAG in `mios-build`, and execute independent phases on a bounded worker pool while keeping log output deterministic (buffer per-phase output, flush in ordinal order) so the golden phase-output snapshots still hold. Take fatal/non-fatal policy from the registry, and gate correctness by asserting the resulting rootfs is identical to a sequential build over a fixture subset. Mirror the SSOT change into the C:/mios-bootstrap tree.
**Where:** `usr/share/mios/mios.toml ([build.phases].deps), src/mios-rs/mios-build/ (DAG scheduler), automation/build.sh (delegates ordering to miosd), automation/{65,66,67,68,69}-*.sh (independent leaves), C:/mios-bootstrap mirror`
**Done When:** `miosd build` runs the independent bake-builder phases concurrently, a fixture build's rootfs and phase logs match the sequential golden, fatal/non-fatal policy is honored from the registry, and wall time on the bake-builder band drops measurably.
**Why:** Every build pays the full serial cost of the five heaviest GPU/UI builders, and the ordering rules that would allow otherwise live nowhere a machine can read them — so the build stays slow and phase order stays an implicit property of filenames.
**Dep:** Replace build.sh grep-blob phase policy with a typed phase registry

### AGY-993 -- Move build.sh's ASCII progress UI into `mios-build` and report phase failures as miette diagnostics  (WS-LANG-AUTO | P3 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one typed console/diagnostic surface for the orchestrator instead of duplicated framing math and an opaque failure blob.
**What+How:** build.sh carries roughly 150 lines of hand-rolled ASCII framing (`_pad`, `_hline`, `_row`, `_progress_bar`, `_step_header`, `_step_result`, `_fail_report`, `_warn_report`, `_final_summary`, lines 32-178). Move the console UI into a `mios-build` ui module so progress renders in one place, and adopt `miette` for phase failures: a failing phase returns a `miette::Result` whose diagnostic names the phase, the exit code and the step-log path with help text. Use `thiserror` enums for internal errors so the drift-gates can match on variants. Preserve the exact framed output operators read today behind a compatibility snapshot test.
**Where:** `automation/build.sh (UI helpers lines 32-178, fail/warn reports 135-163), src/mios-rs/mios-build/ (ui module), src/mios-rs/miosd/src/main.rs (miette::Result main), src/mios-rs/mios-build/tests/ (UI format snapshot)`
**Done When:** `miosd build` renders the framed progress UI byte-compatible with today's build.sh output under snapshot test, a phase failure prints a miette diagnostic carrying phase name and step-log path, library errors are thiserror enums, and the drift-gate is green.
**Why:** A failing build currently emits a grep-assembled FAILURE LOG that names no log path and no cause, so every failure costs an operator a manual hunt — and the framing math is duplicated across nine shell helpers that drift from the real output.
**Dep:** Replace build.sh grep-blob phase policy with a typed phase registry

### AGY-994 -- Replace build.sh's git-reset + reproject dance with `miosd drift --prepare` keyed on $ROOT/$CTX  (WS-LANG-AUTO | P2 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- remove the incomplete-tree false-drift class that repeatedly reddens the gate during the OCI bake.
**What+How:** Before running 98-drift-checks, build.sh performs a delicate sequence (lines 440-473): `git config safe.directory`, `git reset --hard HEAD`, then an UNCONDITIONAL re-projection of three gitignored SSOT files via `tools/generate-ipa-enroll-env.py`, `tools/generate-uki-cmdline.py` and `tools/generate-cockpit-conf.py` — unconditional because inside the OCI bake the copied `.git` is an INCOMPLETE work tree where `git rev-parse`/`reset` are unreliable, and gating the reproject on git left checks 92/93/95 failing the bake. Port this pre-drift orchestration into `miosd drift --prepare`, keying off `$ROOT`/`$CTX` (the inverse-polarity fix) rather than git state, and reproject the three files deterministically from mios.toml. build.sh calls it as one thin step; once the native render-uki-cmdline / cockpit / ipa generators exist, call those directly.
**Where:** `automation/build.sh (lines 440-476), src/mios-rs/miosd/src/drift.rs, src/mios-rs/miosd/src/main.rs (drift --prepare), tools/generate-{ipa-enroll-env,uki-cmdline,cockpit-conf}.py`
**Done When:** `miosd drift --prepare` reprojects the three gitignored SSOT files unconditionally from mios.toml, drift-checks 92/93/95 pass in both the incomplete-tree bake and the pristine-checkout CI job, build.sh invokes it as a single step, and nothing depends on `git reset` succeeding.
**Why:** This exact sequence is a documented, recurring bake failure: when git misbehaves in the incomplete work tree the projections are stale and the gate reports drift that does not exist, burning full bake cycles on a phantom.
**Dep:** mios-build orchestrator crate seam

### AGY-995 -- Add a native `systemd-analyze security` fitness-function drift-gate over every long-running unit  (WS-LANG-AUTO | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- make the least-privilege sandbox posture a machine-enforced ratchet so no newly-added unit can silently regress it.
**What+How:** MiOS hardens some units (hermes-worker, dashboard) but nothing stops a new unit in `usr/lib/systemd/system` from shipping wide open. Add a native `miosd systemd-security-audit` (or an equivalent check in `mios-drift-runner`) that statically lints every long-running unit for the high-value directives — `NoNewPrivileges`, `ProtectSystem=strict`, `ProtectHome`, `PrivateTmp`, `SystemCallFilter=@system-service`, a tight `CapabilityBoundingSet`, `RestrictAddressFamilies` — and fails any unit over an exposure threshold, EXCEPT the roster in mios.toml `[security.privileged_quadlets].root` (the 10 legitimately-exempt units). Register it in 98-drift-checks.sh near line 6311. Where a real systemd exists it may shell to `systemd-analyze security`; in static CI it scores from the unit text. Mirror the roster into mios-bootstrap (Law 15).
**Where:** `usr/lib/systemd/system/*, tools/native/mios-drift-runner/src/main.rs (or miosd), automation/98-drift-checks.sh (register near line 6311), usr/share/mios/mios.toml ([security.privileged_quadlets].root), C:/mios-bootstrap mirror`
**Done When:** the check scores every long-running unit, passes on the current tree, exempts only the privileged roster, fails a deliberately-added unit missing the required directives, runs under `just drift-gate`, and both repos carry the exemption roster.
**Why:** "Confine every unit" is currently an aspiration enforced by reviewer attention alone; one unhardened unit added in a hurry gives a compromised service the full host, and nothing in CI would notice.
**Dep:** Unify the two Rust workspaces

### AGY-996 -- Land the shared thiserror/miette error model with mios.toml span diagnostics before the phase ports  (WS-LANG-AUTO | P3 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one error surface every ported phase inherits, so the compiled tier is a UX upgrade over bash rather than a lateral move.
**What+How:** Define `thiserror` enums in `mios-config` and `mios-build` for the internal/library layer (parse error, missing key, type mismatch) and wrap them into `miette` diagnostics at the CLI boundary, so a bad `[colors]`, port or karg value in mios.toml is reported AT its line/span with actionable help — computing spans from byte offsets, not char counts, so UTF-8 content points at the right place. Return `miette::Result` from miosd's `main`. This is the natural error surface for the figment resolver and gives the drift-gates programmatically-matchable variants instead of string matching. Land it EARLY so subsequent phase ports inherit it rather than each inventing ad-hoc error strings.
**Where:** `src/mios-rs/mios-config/src/ (error module), src/mios-rs/mios-build/src/ (error module), src/mios-rs/miosd/src/main.rs (miette::Result), usr/share/mios/mios.toml (the source spans point into)`
**Done When:** mios-config and mios-build expose thiserror enums wrapped by miette, a malformed mios.toml value produces a diagnostic highlighting the exact line/span with help text, miosd's main returns `miette::Result`, unit tests assert the diagnostic against a bad fixture, and builds are warning-clean.
**Why:** Every port that lands before this invents its own error strings, and retrofitting a shared model across a dozen crates later costs far more than defining it once — meanwhile operators keep getting bash-grade opaque messages from compiled code.
**Dep:** Build a typed figment-based config resolver crate

### AGY-997 -- Port 99-postcheck.sh's 19 build-abort invariants to a native `miosd postcheck` fitness-function suite  (WS-LANG-AUTO | P1 | L)
**Goal:** E-07 The drift-gate as the enforcement plane -- move the last line of defense that aborts insecure images into typed, testable code keyed to the `[laws]` numbering SSOT.
**What+How:** `automation/99-postcheck.sh` (810 lines) runs the 19 numbered build-time invariants that ABORT the bake: OpenSSH >= 9.6 (#1), Cockpit posture (#2/#6), karg schema (#3), NVIDIA-toolkit version (#5), WSL2 wsl.conf parity (#7), sysusers fixed-UID (#8, via `_sysusers_effective`/`_gid_in_etc_group`), tmpfiles no-`/var/run` (#9), `systemd-analyze verify` (#10), `systemd-tmpfiles --dry-run` (#11), Law 5 UNIFIED-AI-REDIRECTS (#12), Law 6 UNPRIVILEGED-QUADLETS (#13), Law 3 BOUND-IMAGES (#14), the T-039 benchmark (#15), Law 10 BARE-SAFE-ENV (#16), Law 11 SECRETS-NEVER-IN-ENV (#17), BOUND-IMAGES-RESOLVE (#18). Re-implement each as a typed check under a `miosd postcheck` subcommand returning miette diagnostics, keyed to the same `[laws]` numbering, and reduce 99-postcheck.sh to a thin shim that execs it (keeping degrade-open only where the original had it). Prove byte-parity of the emitted `mios_ok`/`die` lines against golden captures BEFORE deleting any bash check body.
**Where:** `automation/99-postcheck.sh, automation/build.sh (CONTAINERFILE_SCRIPTS invocation), src/mios-rs/miosd/src/, usr/share/mios/mios.toml [laws]`
**Done When:** `miosd postcheck` reproduces all 19 checks with byte-identical pass/fail output against golden fixtures, 99-postcheck.sh is under 15 lines, a deliberately-broken invariant still aborts the build, and `cargo test` plus `just drift-gate` are green.
**Why:** This is the largest untargeted build phase and the only thing standing between a broken invariant and a published insecure image; leaving 810 lines of bash as the final gate while everything around it hardens is the biggest coverage hole in the domain.
**Dep:** Establish the thiserror/miette error model and mios-config span diagnostics as the shared foundation for all ports

### AGY-998 -- Collapse the Law checks duplicated across 99-postcheck and 98-drift-checks into one native mios-laws module  (WS-LANG-AUTO | P1 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- one definition per architectural Law, consumed by both enforcement call sites, so a Law cannot mean two different things.
**What+How:** The Law checks in 99-postcheck (Law 5 #12, Law 6 #13, Law 3 #14, Law 10 #16, Law 11 #17) are independently re-implemented in `automation/98-drift-checks.sh` (Law 6 UNPRIVILEGED-QUADLETS #36 ~L2968, Law 3 BOUND-IMAGES, Law 2 NO-MKDIR #35 ~L2932, Law 12 BAKE-NOT-FETCH #39 ~L3065, Law 7 NO-HARDCODE ~L973) — the same Law enforced by two drifting bash copies. Extract a single `mios-laws` check crate/module that reads `[laws]` and `[security.privileged_quadlets]` from SSOT once, and have both `miosd postcheck` and the drift runner dispatch into it. Characterize BOTH current call sites against golden output first so the merge is provably lossless.
**Where:** `automation/99-postcheck.sh, automation/98-drift-checks.sh, src/mios-rs/miosd/src/, tools/native/mios-drift-runner/src/main.rs, usr/share/mios/mios.toml [laws]`
**Done When:** each architectural Law is defined in exactly one native module, postcheck and drift both invoke it, deleting either bash copy leaves enforcement byte-identical, and the drift-gate is green.
**Why:** Two independent bash implementations of the same Law already differ in edge cases, so the answer to "does this tree obey Law 6?" depends on which gate you ask — the precise duplicated-parallel-logic smell the campaign exists to remove.
**Dep:** Port the 99-postcheck build-abort invariant harness to a native miosd postcheck fitness-function suite

### AGY-999 -- Build a record/replay host-tool sandbox (podman, semanage, nvidia-ctk, systemd-analyze) so phases are characterizable offline  (WS-LANG-AUTO | P1 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- supply the hermetic seam that makes byte-parity provable before any security-sensitive shell script is deleted.
**What+How:** Most phases shell out to host tools that cannot run in CI or on Windows: 38-selinux calls `restorecon`/`semanage import`, 25-26 call `nvidia-ctk`, 99-postcheck calls `systemd-analyze verify`, `systemd-tmpfiles --dry-run` and `sshd -V`, and the bakes call podman. The golden-capture harness records phase OUTPUT but cannot re-run these hermetically. Build a native record/replay shim layer — a PATH-shadow directory of recording wrappers plus a fixtures store — that captures every external invocation and response during one real build and replays them deterministically, so ported miosd subcommands can be exercised under `cargo test` with zero host dependencies. Wire it into the existing trycmd/insta harness.
**Where:** `src/mios-rs/miosd/tests/, tools/native/, automation/38-selinux.sh, automation/25-gpu-cdi-toolkits.sh, automation/99-postcheck.sh, automation/lib/common.sh`
**Done When:** a recorded fixture set exists for the host tools each ported phase invokes, `cargo test` runs all phase characterization tests offline on both Windows and Linux runners with no podman/semanage present, and a replay mismatch fails the test.
**Why:** Without a hermetic host-tool seam, the golden harness cannot verify parity for exactly the phases that touch SELinux, GPU CDI, systemd and podman — so the most security-sensitive strangler-fig cutovers are blocked or would have to land unverified.
**Dep:** Build a golden-capture harness (trycmd + insta) recording current build-phase output and generated artifacts

### AGY-1000 -- Port the shared phase-runtime libs (common.sh, masking.sh, root-merge.sh) into mios-build modules, keeping bash shims  (WS-LANG-AUTO | P1 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- give every subsequent phase port native primitives so it never has to shell back into bash.
**What+How:** Every ported phase depends on `automation/lib/common.sh` (`mios_step`/`mios_log`/`mios_ok`/`die`, ~112 lines), `lib/masking.sh` (secret redaction in logs, 126 lines, already covered by `test_masking.sh`) and `lib/root-merge.sh` (overlay/root merge, 104 lines). Re-implement all three as `mios-build` library modules: structured logging via `tracing`, a `Masker` type ported from masking.sh with the `test_masking.sh` cases migrated to cargo tests, and a root-merge module. Keep the `.sh` files as thin wrappers that call the binary for surviving shim phases, preserving the exact `mios_*` log prefixes so golden parity holds.
**Where:** `automation/lib/common.sh, automation/lib/masking.sh, automation/lib/root-merge.sh, automation/lib/test_masking.sh, src/mios-rs/`
**Done When:** all three libs have native equivalents, the `test_masking.sh` cases pass as cargo tests, native log/step/ok output is byte-identical to the bash prefixes, the shims remain source-compatible for un-ported phases, and the drift-gate is green.
**Why:** These are the primitives every phase calls; port them late and each phase port re-shells into bash for logging and redaction, leaving multiple secret-masking implementations where exactly one should exist and be audited.
**Dep:** Establish the thiserror/miette error model and mios-config span diagnostics as the shared foundation for all ports

### AGY-1001 -- Establish the strangler-fig control plane: a per-phase MIOS_NATIVE toggle plus a cutover-order ADR  (WS-LANG-AUTO | P1 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- make the native/bash cutover reversible per phase so the port lands incrementally instead of as a big bang.
**What+How:** build.sh enumerates phases via a `[0-9][0-9]-*.sh` glob plus a hand-ordered list and `CONTAINERFILE_SCRIPTS` special cases (L223+), and there is no way to run native and bash side by side during the multi-phase port. Add one control surface: each shimmed phase checks `${MIOS_NATIVE_<PHASE>:-${MIOS_NATIVE:-0}}` and either execs the miosd subcommand or runs the legacy bash body, using the standard `${VAR:-LIT}` degrade pattern. Ship ADR-0013 declaring the dependency order (error model -> config resolver -> shared libs -> host-tool sandbox -> renderers -> postcheck -> orchestrator) plus a ROADMAP tracking table, landed in both mios.git and mios-bootstrap.git per Law 15.
**Where:** `automation/build.sh, automation/lib/common.sh, docs/adr/, ROADMAP.md, usr/share/mios/mios.toml`
**Done When:** `MIOS_NATIVE=1` in CI runs every already-ported phase natively while `0` runs bash, both producing byte-identical images, the ADR and tracking table exist in both repos, and the drift-gate is green.
**Why:** Dozens of queued phase-port tasks have no agreed order and no A/B switch, so any single port that regresses the image can only be diagnosed by reverting the commit — and without the ADR the ports will land in an order that breaks their own dependencies.
**Dep:** Replace build.sh's grep-blob phase policy with a typed phase registry projected from SSOT

### AGY-1002 -- Port the GPU CDI family (23-27) to `miosd render-cdi` driven from SSOT  (WS-LANG-AUTO | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- bring 399 lines of hand-emitted YAML under the render-from-SSOT pattern with real coverage.
**What+How:** `25-gpu-cdi-toolkits.sh` (140 lines), `26-nvidia-cdi-refresh.sh`, `23-gpu-passthrough.sh`, `24-gpu-pv-shim.sh` and `27-vm-gating.sh` generate CDI device specs and PV/passthrough wiring by invoking `nvidia-ctk` and hand-emitting YAML. Port them to a `miosd render-cdi` subcommand that reads GPU/passthrough config from mios.toml, emits deterministic CDI YAML, and wraps `nvidia-ctk` through the host-tool record/replay sandbox with recorded fallbacks so it degrades open when no GPU is present at build time. Demote all five `.sh` to shims behind the MIOS_NATIVE phase toggle and prove YAML byte-parity with golden fixtures.
**Where:** `automation/23-gpu-passthrough.sh, automation/24-gpu-pv-shim.sh, automation/25-gpu-cdi-toolkits.sh, automation/26-nvidia-cdi-refresh.sh, automation/27-vm-gating.sh, src/mios-rs/miosd/src/, usr/share/mios/mios.toml`
**Done When:** `miosd render-cdi` emits byte-identical CDI YAML and passthrough config against golden fixtures, the five phases are shims, a GPU-less build still succeeds, and `cargo test` plus the drift-gate are green.
**Why:** The CDI family hand-writes device specs from SSOT-adjacent data with zero test coverage, so a malformed spec surfaces only when a GPU container fails to start on real hardware — the slowest possible feedback loop.
**Dep:** Build a host-tool record/replay sandbox so build phases are characterizable offline (podman/semanage/nvidia-ctk/systemd-analyze)

### AGY-1003 -- Port 38-selinux + 37-k3s-selinux to `miosd render-selinux` and float the hardcoded policy lists into SSOT  (WS-LANG-AUTO | P2 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- turn a heredoc of booleans, fcontexts and port rules into a `[selinux]` SSOT table rendered by compiled code.
**What+How:** `automation/38-selinux.sh` (198 lines) applies build-time SELinux fixes through `semanage import` (booleans, fcontexts, a port rule), runs `restorecon` over `/boot /etc /usr /var`, and compiles custom per-rule `.te` modules; `37-k3s-selinux.sh` adds k3s labeling. The boolean/fcontext/port lists are hardcoded in a heredoc and belong in SSOT. Port both to a `miosd render-selinux` subcommand that emits the semanage-import stream and module set from a mios.toml `[selinux]` table, wrapping `semanage`/`restorecon` through the host-tool sandbox for offline tests. Keep both `.sh` as toggle shims and verify the emitted stream is byte-identical.
**Where:** `automation/38-selinux.sh, automation/37-k3s-selinux.sh, src/mios-rs/miosd/src/, usr/share/mios/mios.toml`
**Done When:** the semanage-import stream and the fcontext/port lists derive from SSOT and match golden byte-for-byte, a missing `semanage` degrades open, both phases are shims behind the toggle, and the drift-gate is green.
**Why:** A hardcoded policy heredoc is a live Law 7 NO-HARDCODE violation and an untargeted build phase at the same time: an operator cannot change an SELinux boolean or port label through the one config surface, only by editing a shell script.
**Dep:** Build a host-tool record/replay sandbox so build phases are characterizable offline (podman/semanage/nvidia-ctk/systemd-analyze)

### AGY-1004 -- Port 77-composefs-verity to `miosd seal-composefs` with a `--check` integrity verify mode  (WS-LANG-AUTO | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- put the image-integrity seal under compiled code plus a gate that detects posture regression.
**What+How:** `automation/77-composefs-verity.sh` (135 lines) configures composefs/fs-verity sealing for the bootc image. Port the sealing and verification logic to a `miosd seal-composefs` subcommand with typed error diagnostics, and add a `--check` mode that asserts the expected composefs/verity posture — the natural companion to the reproducible-build digest work. Demote the `.sh` to a toggle shim and characterize it against a recorded build.
**Where:** `automation/77-composefs-verity.sh, src/mios-rs/miosd/src/, usr/share/mios/mios.toml`
**Done When:** `miosd seal-composefs` reproduces the sealing steps with parity against golden, `--check` fails when the verity posture regresses, the phase is a shim behind the toggle, and `cargo test` plus the drift-gate are green.
**Why:** Composefs/fs-verity is what makes `/usr` tamper-evident, yet nothing verifies the seal actually got applied — a silently-unsealed image would ship and boot looking completely normal.
**Dep:** Establish the thiserror/miette error model and mios-config span diagnostics as the shared foundation for all ports

### AGY-1005 -- Pin and checksum the network-fetch bakes (66-quickshell, 68-kvmfr, 69-lookingglass, 73-model-prep) through `miosd fetch-verify`  (WS-LANG-AUTO | P2 | L)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- close the four unpinned upstream fetches that make the image non-reproducible and the digest-compare gate unusable.
**What+How:** These bakes pull live sources at build time: `66-bake-quickshell.sh` does `git clone ${MIOS_URL_QUICKSHELL:-...}` plus cmake, `68-bake-kvmfr.sh` and `69-bake-lookingglass-client.sh` git-clone plus cmake/make, and `73-model-prep.sh` `curl -fL`s a HuggingFace resolve GGUF. None pins a commit or tag and none verifies a checksum. Add a `miosd fetch-verify` helper that resolves each source URL to a pinned ref/commit and validates a SHA — recorded in the SBOM per ADR-0003, never hand-pinned in SSOT — failing closed on mismatch, and route all four bakes through it while keeping the `${URL:-LIT}` fallbacks.
**Where:** `automation/66-bake-quickshell.sh, automation/68-bake-kvmfr.sh, automation/69-bake-lookingglass-client.sh, automation/73-model-prep.sh, src/mios-rs/miosd/src/, usr/share/mios/mios.toml`
**Done When:** each bake fetches a pinned ref and verifies a recorded checksum, a tampered or moved source fails the build, two independent builds fetch identical bytes, the SBOM records the resolved refs, and the drift-gate is green.
**Why:** Four phases clone or curl a moving upstream with no verification, so today's image cannot be rebuilt to the same bytes and a compromised upstream would be baked in silently — the rebuild-and-compare-digest gate can never pass until this is fixed.
**Dep:** Make the image build reproducible: derive SOURCE_DATE_EPOCH from the git commit and pass rewrite-timestamp

### AGY-1006 -- Make the 72-hermes-agent shared Python venv reproducible with hash-locked, fully-offline wheels  (WS-LANG-AUTO | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- close the largest unpinned dependency surface in the bake and honor Law 12 BAKE-NOT-FETCH.
**What+How:** `automation/72-hermes-agent.sh` (684 lines) builds the shared agent venv at `/usr/lib/mios/agents/.venv` (Hermes, opencode) via pip — a large, network-dependent, unpinned install that brushes Law 12. Convert it to a hash-locked, fully offline install: generate a pinned `requirements.lock` used with `--require-hashes` against vendored wheels under `usr/share/mios/vendored/`, and have a miosd verb (or the mios-build orchestrator) drive the deterministic install and record the resolved package set into the SBOM. Keep phase 72 thin around the locked install.
**Where:** `automation/72-hermes-agent.sh, usr/lib/mios/agents/, usr/share/mios/vendored/, src/mios-rs/, usr/share/mios/mios.toml`
**Done When:** the venv installs only from vendored wheels with `--require-hashes` and no network access, two builds produce an identical package set recorded in the SBOM, the venv contents are digest-stable, and the drift-gate is green.
**Why:** An unpinned pip resolve means the AI plane's dependency set differs between any two builds and a compromised PyPI package lands with no signal — it is simultaneously the biggest reproducibility hole and the biggest supply-chain hole in the bake.
**Dep:** Make the network-fetch bakes (66-quickshell, 68-kvmfr, 69-lookingglass, 73-model-prep) reproducible via a miosd fetch-verify with pinned refs and checksums

### AGY-1007 -- Finish the 97-ssot-lint cutover to the existing mios-ssot-lint crate and ship it from the Containerfile  (WS-LANG-AUTO | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- retire a bash validator whose native replacement already exists but is never built or run.
**What+How:** `automation/97-ssot-lint.sh` (195 lines) is the post-build SSOT validator invoked explicitly by build.sh, while a native sibling already sits at `tools/native/mios-ssot-lint/src/main.rs` and is not the executor. Complete the strangler-fig: characterize the `.sh` output, extend the Rust crate to full parity, demote 97-ssot-lint.sh to a shim that execs the native binary behind the migration toggle, then delete the bash lint logic. Critically, make sure the crate is actually built and shipped — the Containerfile today builds only `src/mios-rs` miosd, so `tools/native` crates never reach the image.
**Where:** `automation/97-ssot-lint.sh, tools/native/mios-ssot-lint/src/main.rs, tools/native/Cargo.toml, Containerfile, automation/build.sh`
**Done When:** `mios-ssot-lint` produces byte-identical findings against the 97-ssot-lint.sh golden, the Containerfile builds and ships the crate, 97-ssot-lint.sh is a shim, and `cargo test` plus the drift-gate are green.
**Why:** The native crate is dead weight today — maintained, compiled by nobody, shipped nowhere, while the bash version does the real work — and the missing Containerfile wiring will block every other `tools/native` crate the same way.
**Dep:** Unify the two Rust workspaces and make the Containerfile build+ship every native crate

### AGY-1008 -- Route build-mios.ps1 through a cross-compiled `miosd resolve` and stop dot-sourcing globals.ps1  (WS-LANG-AUTO | P2 | L)
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- collapse the third hand-maintained resolver so Windows and Linux builds read values from one native implementation.
**What+How:** `automation/lib/globals.ps1` (220 lines, `Resolve-MiosVersion` plus the MIOS_* constant registry) is the THIRD resolver — a hand-maintained PowerShell twin of globals.sh/userenv.sh held in sync only by the twin-equivalence drift-check. Once `miosd resolve --shell` exists, cross-compile miosd for `x86_64-pc-windows-msvc` and have `build-mios.ps1` source its variables from `miosd resolve --format ps` (or JSON) instead of dot-sourcing globals.ps1. Keep globals.ps1 as a fallback that the drift-check continues to pin until parity is proven, and produce the cross-compiled artifact in CI.
**Where:** `automation/lib/globals.ps1, automation/build-mios.ps1, src/mios-rs/miosd/, .github/workflows/mios-ci.yml`
**Done When:** build-mios.ps1 obtains all MIOS_* values from the native binary on Windows with output matching today's globals.ps1 values, CI produces a cross-compiled miosd artifact, twin-equivalence stays green through the transition, and the drift-gate is green.
**Why:** The shell-shim work leaves the PowerShell twin standing, so every new SSOT key must still be hand-added in a third place; until Windows runs the same binary, the parallel-resolver duplication is only two-thirds solved.
**Dep:** Demote userenv.sh to a shim behind miosd resolve --shell and prove parity with proptest differential testing

### AGY-1009 -- Delete the twin resolvers and replace the twin-equivalence check with a defer-to-native check  (WS-LANG-AUTO | P3 | M)
**Goal:** E-09 One value, one name: the full de-duplication campaign -- finish the strangler-fig by actually removing the duplicated bodies, not just adding a native one.
**What+How:** Once `miosd resolve` is the single source for shell (userenv.sh shims), Windows (globals.ps1) and struct config, the justification for `automation/lib/globals.sh` (292 lines), `tools/lib/userenv.sh` + `usr/lib/mios/userenv.sh`, and the twin-equivalence check in 98-drift-checks.sh disappears. Land the retirement: delete the dead twin bodies (staging explicit paths only — never `git add -A`, the tree is shared), replace the twin-equivalence check with a single check asserting the resolvers are generated-from or defer-to the native binary, verify no phase still sources a deleted twin, and mirror the SSOT-surface changes into both mios.git and mios-bootstrap.git per Law 15.
**Where:** `automation/lib/globals.sh, tools/lib/userenv.sh, usr/lib/mios/userenv.sh, automation/98-drift-checks.sh, usr/share/mios/mios.toml`
**Done When:** the duplicated resolver bodies are deleted or reduced to generated shims, the twin-equivalence check is replaced by a defer-to-native check, no phase references a removed file, both repos are updated, and the drift-gate is green.
**Why:** A native resolver that coexists with the twins costs more than it saves — four implementations plus a babysitting check — and the duplication the campaign set out to remove survives until the bodies are actually gone.
**Dep:** Eliminate the PowerShell twin resolver: have build-mios.ps1 call cross-compiled miosd resolve instead of globals.ps1

### AGY-1010 -- Sweep the last small SSOT-to-config emitters (46-sshd-port, 48-dropin-fanout, 12-hostname, 10-locale-theme) into `miosd render-*`  (WS-LANG-AUTO | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- complete the render family so no ad-hoc bash config emitter remains and the phase ratchet can reach zero.
**What+How:** Beyond the named render ports (ports, quadlets, kargs, uki, chrony, nut, firewall, repos), four small phases still render live config from SSOT in bash: `46-sshd-port.sh` (sshd Port drop-in), `48-mios-dropin-fanout.sh` (88 lines, systemd drop-in fanout), `12-hostname.sh` and `10-locale-theme.sh`. Port each into a `miosd render-sshd` / `render-dropins` / `render-hostname` / `render-locale` subcommand emitting the same files, demote all four to toggle shims, and prove byte-parity via golden fixtures.
**Where:** `automation/46-sshd-port.sh, automation/48-mios-dropin-fanout.sh, automation/12-hostname.sh, automation/10-locale-theme.sh, src/mios-rs/miosd/src/, usr/share/mios/mios.toml`
**Done When:** all four phases emit byte-identical config via `miosd render-*`, they are shims behind the toggle, no bash emitter remains in the render family, and `cargo test` plus the drift-gate are green.
**Why:** The headline render ports leave a tail of small emitters in bash, and while any remain the bash-phase count ratchet cannot be driven down and "every projection is compiled" stays a claim rather than a checked fact.
**Dep:** Port 35-render-ports.sh (awk stack_id offset) to miosd render-ports behind a shim

### AGY-1011 -- Build miosd as a static musl binary so one artifact runs at build, in-image, and in the firstboot-tier  (WS-LANG-AUTO | P3 | S)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one immutable image whose compiled tools are context-independent, so the same binary serves the builder, the runtime and the stripped firstboot-tier.
**What+How:** The Containerfile currently builds miosd under `FROM docker.io/library/rust:slim` (glibc) and copies `/miosd` into `/usr/libexec/mios/miosd`, which couples the binary to the builder's libc. Retarget the rust-builder stage at `x86_64-unknown-linux-musl`, pin a rustls/no-openssl feature set in `src/mios-rs/Cargo.toml` so nothing links OpenSSL, and add the musl target install + `--target` flag to the builder stage and to the CI job that compiles it.
**Where:** `Containerfile, src/mios-rs/Cargo.toml, src/mios-rs/miosd/, .github/workflows/mios-ci.yml`
**Done When:** `ldd /usr/libexec/mios/miosd` reports "not a dynamic executable"; the binary executes on the base image and in a distroless/firstboot context; image size does not regress; `cargo test` and `just drift-gate` are green.
**Why:** miosd is becoming the executor for postcheck and the renderers while the firstboot-tier deliberately evicts the vLLM/SGLang whales; a glibc-dynamic binary can fail to load in exactly that shrunk tier, and cannot be reused as a host build tool on any other base.
**Dep:** Unify the two Rust workspaces and make the Containerfile build+ship every native crate

### AGY-1012 -- Repair the tools/native cargo workspace by adopting the 5 orphaned crates the drift-gate already builds  (WS-LANGX | P1 | S)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one Cargo workspace with every crate a member, the precondition for `cargo build --workspace` in a single cached Containerfile stage.
**What+How:** `tools/native/Cargo.toml` declares `members = [mios-drift-runner, mios-version-check, mios-wallpaperd]` only, yet `generate-names-registry`, `mios-aiplane-lint`, `mios-bake-plan`, `mios-ssot-lint` and `mios-ssot-walk` are real crate directories with their own `Cargo.toml` (none declaring its own `[workspace]`) that `automation/98-drift-checks.sh` already builds via `cargo build --manifest-path tools/native/Cargo.toml -p mios-ssot-lint` and consumes from `target/release/`. Add all 5 to the `members` array, regenerate `tools/native/Cargo.lock`, and stage both explicit paths (never `git add -A`, since AGY shares the C:\MiOS tree).
**Where:** `tools/native/Cargo.toml, tools/native/Cargo.lock`
**Done When:** `cargo build --workspace --manifest-path tools/native/Cargo.toml` and `cargo build -p mios-ssot-lint -p mios-aiplane-lint -p mios-bake-plan` both succeed from a clean checkout; the drift-gate steps referencing `target/release/mios-{ssot-lint,aiplane-lint,bake-plan}` find their binaries; `just drift-gate` green.
**Why:** Today `-p mios-ssot-lint` resolves against a workspace that excludes the crate — a latent break in the very governance tooling the campaign depends on, and a blocker for every later port that assumes one coherent workspace.

### AGY-1013 -- Build mios-toml-core: one figment-based typed layered mios.toml resolver crate with a --shell emitter  (WS-LANGX | P1 | XL)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the compiled resolver core that makes SSOT-operator-defined a single typed engine instead of three hand-synced twins.
**What+How:** Create lib+bin crate `tools/native/mios-toml-core` that deserializes the vendor < host < user mios.toml overlay (the exact layering `tools/lib/userenv.sh` and `usr/lib/mios/mios_toml.py` implement) into typed structs via figment (Toml + Env providers with defined precedence), with miette/thiserror diagnostics pointing AT the offending line/span for a bad `[colors]`/`[ports]` value. Expose `mios-toml-core --shell` emitting the exact sorted `MIOS_*` KEY=value projection userenv.sh produces — canonical `MIOS_AI_VLLM_*` long form plus the short `MIOS_VLLM_*` aliases consumers actually read — so it drops in behind the existing shell seam. Keep userenv.sh as a strangler-façade fallback until byte-parity is drift-gated; type the canonical ports in `[security.nohc_allowlist]` so the zero-hardcodes campaign has a schema to lint against.
**Where:** `tools/native/mios-toml-core/ (new), tools/native/Cargo.toml, tools/lib/userenv.sh + usr/lib/mios/userenv.sh (kept as fallback), usr/lib/mios/mios_toml.py (kept as fallback)`
**Done When:** `mios-toml-core --shell` diffs byte-identical against `source tools/lib/userenv.sh && env | grep ^MIOS_ | sort` for the vendor SSOT; a malformed value yields a miette diagnostic with the correct byte-span; `cargo test` green; the crate is a workspace member.
**Why:** Three parallel resolvers (mios_toml.py, the userenv.sh twins, globals.ps1) held together only by twin-equivalence testing is the duplication the operator flagged; every divergence between them silently mints wrong values into Quadlets and install.env.
**Dep:** AGY-1012 -- Repair the tools/native cargo workspace by adopting the 5 orphaned crates

### AGY-1014 -- Stand up the golden-master parity harness (trycmd + insta) that every libexec port must pass  (WS-LANGX | P1 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the characterization safety net that makes strangler-fig deletion of a bash tool provably safe.
**What+How:** Add a `tests/langx-golden/` fixture tree plus a trycmd/insta test crate `tools/native/langx-golden` that records each target bash tool's CURRENT observable behavior — stdout, stderr, exit code, and for generators the written artifact bytes — as a snapshot taken BEFORE any port. Wire `cargo test -p langx-golden` and a `just golden-capture` / `just golden-verify` pair so every later port asserts byte-for-byte equivalence against the captured legacy output. Seed it with the two lowest-risk generators as worked examples: the `mios-resolve-latest` SBOM-CSV line and the `mios-sync-theme` theme.json + mios-theme.css pair.
**Where:** `tools/native/langx-golden/ (new), tests/langx-golden/ (fixtures), Justfile`
**Done When:** `cargo test -p langx-golden` runs the captured fixtures green against the current bash tools; deliberately mutating a tool's output makes the snapshot test fail; the crate README documents the capture/verify workflow for reuse by later port tasks.
**Why:** The 144 libexec bash tools have no unit tests at all; without recorded golden output, every port is an unverifiable rewrite and the first regression ships silently into a bootc image that fleets pull.
**Dep:** AGY-1012 -- Repair the tools/native cargo workspace by adopting the 5 orphaned crates

### AGY-1015 -- Add a proptest differential oracle proving mios-toml-core == userenv.sh over random mios.toml  (WS-LANGX | P1 | L)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the proptest differential oracle that mechanizes the Global-Unification-Plan lossless-diff invariant.
**What+How:** Add a proptest suite under `tools/native/mios-toml-core/tests/` that generates random-but-valid mios.toml documents (varying `[ports].*`, `[colors].*`, `[ai].*`, `[pgvector].*`, and section presence/absence), feeds each to BOTH the legacy resolver (`source tools/lib/userenv.sh`) and `mios-toml-core --shell`, and asserts the emitted `MIOS_*` key SET and every value are identical, auto-shrinking to a minimal failing document on divergence. Treat the bash resolver as the oracle for the duration of the migration; promote any divergence found into a fixture in the golden harness. This is also where the short-vs-long alias reconciliation (`MIOS_AI_VLLM_*` vs `MIOS_VLLM_*`) is proven rather than asserted.
**Where:** `tools/native/mios-toml-core/tests/ (new proptest), tools/lib/userenv.sh (oracle)`
**Done When:** `cargo test -p mios-toml-core` runs >=1000 proptest cases with zero key/value divergence vs userenv.sh; a seeded intentional bug (drop one short alias) shrinks to a minimal reproducing mios.toml; the suite runs in CI before the main drift gate.
**Why:** Example-based tests miss the edge-case value divergences that matter — one wrong resolved value silently breaks a Quadlet, which is precisely the recurring SSOT-projection drift class the gate keeps catching after the fact.
**Dep:** AGY-1013 -- Build mios-toml-core, the figment-based typed layered resolver crate

### AGY-1016 -- Add cargo fmt/clippy -Dwarnings plus cargo-deny/cargo-audit as native quality drift-gates  (WS-LANGX | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the shellcheck-equivalent the Rust plane never had, plus a supply-chain story for the compiled tier.
**What+How:** Add `check_native_lint` to `automation/98-drift-checks.sh` running `cargo fmt --check` and `cargo clippy --workspace -- -D warnings` (RUSTFLAGS=-Dwarnings) with curated restriction lints — `clippy::unwrap_used`, `clippy::panic`, `clippy::todo` denied in system verbs — and `check_native_supplychain` running cargo-deny (license allow-list, banned/duplicate crates, advisories) and cargo-audit against the committed `tools/native/Cargo.lock`. Place both AFTER the negatives job and BEFORE the main gate, mirroring the existing NO-HARDCODE / pod-quadlets ordering, and degrade-open when cargo is absent. Author `tools/native/deny.toml` with the FOSS-aligned license allow-list; if a law row is added, register it in `usr/share/mios/mios.toml [laws]`.
**Where:** `automation/98-drift-checks.sh (new check_native_lint + check_native_supplychain), tools/native/deny.toml (new), usr/share/mios/mios.toml [laws] numbering if a law row is added`
**Done When:** `cargo clippy --workspace -- -D warnings` and `cargo deny check` pass on the repo; both new checks appear in the generated gate index and fail on an injected `unwrap()`/`panic!` or a banned-license crate; the negatives-before-main ordering is preserved.
**Why:** The tech-debt map records that MiOS has no shellcheck CI at all; as code moves to Rust, a panic or an unvetted transitive dependency would ship into the image with nothing checking it, and `Cargo.lock` would be a reproducibility SSOT nobody audits.
**Dep:** AGY-1012 -- Repair the tools/native cargo workspace by adopting the 5 orphaned crates

### AGY-1017 -- Add a monotonic bash-tool ratchet drift-check over usr/libexec/mios  (WS-LANGX | P1 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- turn "minimize loose scripts" into an enforced fitness function that can only move one direction.
**What+How:** Add `check_libexec_bash_ratchet` to `automation/98-drift-checks.sh` counting executables under `usr/libexec/mios` whose shebang is bash/sh (145 at measurement) and failing if the count EXCEEDS a ceiling recorded in a new `usr/share/mios/mios.toml [langx].libexec_bash_ceiling` key. Print the live count and remaining budget, degrade-open when git/awk is unavailable, and follow the soft->hard ratchet convention already used by `check_hint_coverage`. Seed the ceiling at the measured baseline; each port task's PR decrements it by one. Regenerate the gate index via `tools/generate-gate-index.py`.
**Where:** `automation/98-drift-checks.sh (new check_libexec_bash_ratchet), usr/share/mios/mios.toml ([langx].libexec_bash_ceiling), tools/generate-gate-index.py (regenerate index)`
**Done When:** the check prints live count vs ceiling and passes at baseline; adding one new bash tool under `usr/libexec/mios` makes it fail; each later port lowers the ceiling by one with the gate staying green; per Law 15 the SSOT key is mirrored in mios-bootstrap.git if that repo carries the surface.
**Why:** Without a ratchet, the campaign is reversible by accident: a single new hand-written bash tool erases a port's progress with nothing flagging it, and the 145-script surface never provably shrinks.

### AGY-1018 -- Build mios-verb-dispatcher to replace the eval-bearing verb routing seam  (WS-LANGX | P1 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the strangler seam where the verb NAME is the stable façade and compiled ports slot in behind it unchanged.
**What+How:** Author a Rust `mios-verb-dispatcher` binary implementing a hardened dispatch table (assoc-array/case equivalent, no string-built commands) covering the verbs routed by the `KNOWN_VERBS` dict in `usr/bin/mios` and the `mios()` shell function in `etc/profile.d/mios-verbs.sh` — build, dash, config, theme, sync-env, env, vfio, tune and the rest. Route BOTH callers through the one binary so old and new coexist; agent-supplied args become an explicit allowlisted verb lookup, structurally removing the 9 eval-on-agent-args sites the tech-debt map flags. Keep the existing Python/shell dispatch as fallback until a golden snapshot of `mios help` plus each verb's exec target proves the two tables identical.
**Where:** `tools/native/mios-verb-dispatcher/ (new), usr/bin/mios (KNOWN_VERBS -> delegate), etc/profile.d/mios-verbs.sh (route through binary), tools/native/Cargo.toml`
**Done When:** every verb in KNOWN_VERBS resolves through mios-verb-dispatcher to the identical exec target (golden snapshot passes); a fuzzed or hostile arg cannot reach `eval` or an unlisted command; `mios build`, `mios theme` and `mios sync-env` behave byte-identically; `cargo test` green.
**Why:** Verb arguments arrive from an untrusted agent boundary and today reach `eval` in nine places — a command-injection surface sitting in the OS's primary CLI, and the one seam that must be stable before any verb body can be ported.
**Dep:** AGY-1013 -- Build mios-toml-core, the figment-based typed layered resolver crate

### AGY-1019 -- Add the [migration] SSOT toggle for oxidizr-style per-verb Rust/bash opt-out and rollback  (WS-LANGX | P1 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- rollback as a first-class deliverable so adopting a compiled verb never risks the bake.
**What+How:** Add a `[migration]` section to `usr/share/mios/mios.toml` (`use_rust_verbs = false` default plus per-verb keys, e.g. `verb.sync_env = "rust"|"bash"`) projected through mios-toml-core, and teach `mios-verb-dispatcher` to select the Rust binary or the bash shim per verb from that value. Ship it inert with bash as the default — the same convention as the inert `[accounts].db_backed` key — so the operator flips one SSOT value to adopt Rust and flips it back to roll a regression out WITHOUT reverting a build. Document the one-command revert in the section comment and mirror the key into mios-bootstrap.git per Law 15.
**Where:** `usr/share/mios/mios.toml ([migration]), tools/native/mios-verb-dispatcher/ (toggle read), mios-bootstrap.git mios.toml mirror`
**Done When:** setting `[migration].verb.sync_env="rust"` routes `mios sync-env` to the Rust binary and `"bash"` routes back to the shim, with both producing byte-identical output; unset keeps bash; drift-gate green; the Law-15 mirror is confirmed present in both repos.
**Why:** Without a per-verb switch, every port is an all-or-nothing commit: a regression discovered after the bake requires reverting and rebuilding the whole image instead of changing one SSOT value.
**Dep:** AGY-1018 -- Build mios-verb-dispatcher to replace the eval-bearing verb routing seam

### AGY-1020 -- Port system-sync-env.sh to a mios-toml-core install.env writer with proven byte-parity  (WS-LANGX | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- a typed serializer for the one file three different parsers must agree on.
**What+How:** Reimplement `usr/libexec/mios/system-sync-env.sh` — `generate_env`, the `emit()` bare-KEY=value safety filter against `_ENV_UNSAFE`, the atomic mktemp+rename, and the self-tests rejecting `="` and `MIOS_*PASSWORD_HASH`/`GITHUB_TOKEN` — as a `mios-toml-core sync-env` subcommand that serializes install.env FROM the typed model, structurally evicting secrets and staying tri-parser-safe (systemd `EnvironmentFile`, bash `source`, podman `--env-file`). Demote `system-sync-env.sh` to a thin shim exec-ing the binary, kept as the fallback behind the `[migration]` toggle; prove byte-identical `/etc/mios/install.env` through the golden harness, then delete the generator body from the shim.
**Where:** `usr/libexec/mios/system-sync-env.sh (-> shim), tools/native/mios-toml-core/ (sync-env subcommand), tests/langx-golden/install-env/`
**Done When:** `mios-toml-core sync-env --dry-run` is byte-identical to `system-sync-env.sh --dry-run` for the vendor SSOT; the R4 self-test invariants (no `="`, no secrets, sources clean under `set -u`) hold in Rust; the install-env-safe drift-check is green; the bash generator body no longer exists, leaving only the shim.
**Why:** install.env is consumed by three parsers with incompatible quoting rules; the shell string-munging that builds it is the class that produced the historic `$6: unbound variable` service cascade, and it can leak a secret into a world-readable env file.
**Dep:** AGY-1013 -- Build mios-toml-core, the figment-based typed layered resolver crate

### AGY-1021 -- Port mios-env-snapshot to a byte-identical mios-toml-core snapshot emitter  (WS-LANGX | P2 | M)
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- a host-stable snapshot so the lossless-diff invariant holds on any runner.
**What+How:** Reimplement `usr/libexec/mios/mios-env-snapshot` — the `env -i` clean-room with `HOME=/nonexistent`, `LC_ALL=C` sort, `grep ^MIOS_`, and the exclusion of the self-referential meta-keys `MIOS_ROOT`/`MIOS_TOML_ROOT`/`MIOS_VENDOR_TOML`/`MIOS_CONFIG_DIR`/`MIOS_USER_TOML*` — as `mios-toml-core snapshot`, resolving only the vendor SSOT plus in-tree `mios.d` fragments and normalizing root paths deterministically so output is byte-stable across hosts and CI. Demote the bash to a shim behind the `[migration]` toggle; the Rust path must reproduce the sorted `MIOS_*` baseline that `check_resolved_env_lossless` consumes.
**Where:** `usr/libexec/mios/mios-env-snapshot (-> shim), tools/native/mios-toml-core/ (snapshot subcommand)`
**Done When:** `mios-toml-core snapshot` is byte-identical to `mios-env-snapshot` under the C locale on two machines with different `$HOME`; `check_resolved_env_lossless` passes against the Rust-produced baseline; the golden snapshot is recorded.
**Why:** This snapshot is the evidence the GUP lossless gate reads; today locale and `$HOME` differences between the capture host and the CI runner fake drift, which trains everyone to ignore a red gate.
**Dep:** AGY-1013 -- Build mios-toml-core, the figment-based typed layered resolver crate

### AGY-1022 -- Port mios-resolve-latest to a Rust OCI-digest + SBOM recorder that reads refs from SSOT  (WS-LANGX | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- digest capture becomes build-resolved provenance instead of an in-code ref list plus a fallback literal.
**What+How:** Reimplement `usr/libexec/mios/mios-resolve-latest` — `resolve_ref` shelling to skopeo/podman for `{{.Digest}}` on the floating `:latest` refs, then dedup-appending `ref,tag,digest,container_image,date` to `MiOS-SBOM.csv` — as a Rust binary that reads the ref list FROM the SSOT (`[image.sidecars]` / plan.d) rather than the hardcoded refs array in the bash, shelling out only for the network step. Preserve the ADR-0003 contract: `:latest` remains the recorded intent, the resolved digest is SBOM output and is never hand-pinned. Demote the bash to a shim and prove the appended CSV line byte-identical via the golden harness against a mocked digest.
**Where:** `usr/libexec/mios/mios-resolve-latest (-> shim), tools/native/ (new crate or mios-bake-plan subcommand), MiOS-SBOM.csv (output), usr/share/mios/mios.toml (ref source)`
**Done When:** the Rust tool resolves the same ref set from SSOT with no ref array in code, and writes a byte-identical `MiOS-SBOM.csv` line versus the bash for a stubbed digest; the skopeo-absent path degrades open; the golden test is green.
**Why:** The bash hardcodes both the sidecar ref list and a fallback sha256 literal, so adding a sidecar to SSOT silently fails to appear in the SBOM and a stale pinned digest can be recorded as provenance.
**Dep:** AGY-1014 -- Stand up the golden-master parity harness (trycmd + insta)

### AGY-1023 -- Fold mios-bake-group into the mios-bake-plan crate as a native bake-group subcommand  (WS-LANGX | P2 | L)
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- consolidate group baking into the crate that already owns the plan.d projection.
**What+How:** Reimplement `usr/libexec/mios/mios-bake-group` — reading the `plan.d/*-<group>.list` assignment, the 5-try podman pull with backoff, the per-group `storage.conf`/`registries.conf` mirror.gcr.io scratch, and the resolved-digest append to `$SBOM_DIR/bound-images.tsv` — as a `mios-bake-plan bake-group <name>` subcommand. Keep the `MIOS_BAKE_BOUND_IMAGES=0` skip and the SBOM tsv contract intact. Demote `mios-bake-group` to a shim exec-ing the subcommand, and prove plan.d parsing plus the tsv line byte-identical via golden fixtures against a stubbed pull.
**Where:** `usr/libexec/mios/mios-bake-group (-> shim), tools/native/mios-bake-plan/ (bake-group subcommand), usr/lib/mios/bake/plan.d/ (input), automation/85-bake-plan.sh (invocation)`
**Done When:** `mios-bake-plan bake-group heavy` selects the same images and appends a byte-identical `bound-images.tsv` line as the bash for a mocked pull; `MIOS_BAKE_BOUND_IMAGES=0` skips identically; the workspace builds; golden test green.
**Why:** The bake path is the disk-fragile CI machinery that fails with podman exit 125 on layer commit; splitting its logic across a bash tool and a Rust sibling that already parses plan.d means every fix has to be made twice.
**Dep:** AGY-1012 -- Repair the tools/native cargo workspace by adopting the 5 orphaned crates

### AGY-1024 -- Port mios-sync-theme to a native theme-surface renderer driven by mios-toml-core  (WS-LANGX | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- theme surfaces born FROM `[colors]` SSOT by a compiled renderer rather than an embedded python heredoc.
**What+How:** Reimplement `usr/libexec/mios/mios-sync-theme` — the embedded python heredoc that reads `[colors]`, `[theme]` and `[theme.font]` through mios_toml, resolves the `_SEMANTIC` palette, and writes `/etc/mios/theme/theme.json` plus `mios-theme.css` — as a Rust renderer driven by mios-toml-core, keeping the `--check` dry-run and the optional `--apply-home` hand-off. Codegen the CSS and JSON deterministically from the typed palette so no surface is hand-authored twice. Leave the downstream `mios-theme-render` / `mios-dotfiles-render` / `mios-generate-icons` chaining shelled out for now; demote only the resolver body, and prove both outputs byte-identical via the golden harness.
**Where:** `usr/libexec/mios/mios-sync-theme (-> shim), tools/native/ (theme subcommand of mios-toml-core or a new crate), /etc/mios/theme/ (outputs), usr/lib/systemd/system/mios-sync-theme.service`
**Done When:** the Rust renderer writes byte-identical `theme.json` + `mios-theme.css` versus the bash for the vendor palette; `--check` prints without writing; drift-check 25 (theme SSOT projection) is green; golden test green.
**Why:** An embedded python heredoc inside a bash tool is untestable and unlintable, and it is the single point where every theme surface is derived — a silent divergence there puts drift-check 25 red across the whole desktop.
**Dep:** AGY-1013 -- Build mios-toml-core, the figment-based typed layered resolver crate

### AGY-1025 -- Port the greenboot AI-plane required health check to a static Rust binary  (WS-LANGX | P2 | M)
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- greenboot health coverage that is memory-safe and cannot false-fail a fleet into rollback.
**What+How:** Reimplement `usr/lib/greenboot/check/required.d/40-mios-ai-plane.sh` — `check_service` with the `systemctl is-enabled` guard, `_tcp_up` via bounded connect, `_http_up` treating <500 as up, ports read from `/etc/mios/install.env` (`MIOS_PORT_AGENT_PIPE`, `LLM_LIGHT`, `PGVECTOR`), and skip-not-guess on an unresolved port — as a small static Rust binary that takes ports from mios-toml-core and probes mios-agent-pipe, mios-llm-light and mios-pgvector. Preserve the degrade-open and bounded-retry semantics EXACTLY: a config-bridge glitch must never roll the OS back. Demote the `.sh` to a shim exec-ing the binary.
**Where:** `usr/lib/greenboot/check/required.d/40-mios-ai-plane.sh (-> shim), tools/native/ (new greenboot crate), /etc/mios/install.env (input)`
**Done When:** the Rust check returns the identical exit code to the bash for the all-up, one-down, unit-disabled and port-unresolved fixtures; a non-zero exit still triggers greenboot rollback; the golden trycmd fixtures are green.
**Why:** This check decides whether a freshly-upgraded host keeps its new image or rolls back; a shell probe that mis-parses an unresolved port can roll an entire fleet back on a false negative, and greenboot itself is moving to Rust for bootc.
**Dep:** AGY-1013 -- Build mios-toml-core, the figment-based typed layered resolver crate

### AGY-1026 -- Port the remaining greenboot required.d/wanted.d checks into the shared Rust greenboot crate  (WS-LANGX | P2 | M)
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- one audited health-check crate so every critical service has a real check and a rollback path.
**What+How:** Port the sibling greenboot scripts — `required.d/10-mios-composefs.sh`, `10-mios-role.sh`, `15-composefs-verity.sh`, `20-podman.sh`, `30-network.sh`; `wanted.d/30-nvidia-cdi.sh`, `40-role-target.sh`, `50-mios-ha-cluster.sh`, `60-k3s.sh`; and `etc/greenboot/check/wanted.d/55-mios-cephfs.sh`, `56-mios-desktop.sh` — into the greenboot Rust crate as named subchecks sharing the probe helpers from the AI-plane binary. Each script becomes a thin shim exec-ing the binary; required checks keep rollback-on-fail, wanted checks stay advisory. Capture each script's current exit behavior as golden fixtures BEFORE porting.
**Where:** `usr/lib/greenboot/check/required.d/*.sh (-> shims), usr/lib/greenboot/check/wanted.d/*.sh (-> shims), etc/greenboot/check/wanted.d/*.sh (-> shims), tools/native/ (greenboot crate)`
**Done When:** each ported check returns the identical exit code to its bash predecessor across pass/fail/skip fixtures; required-vs-wanted rollback semantics are preserved; the bash-tool ratchet ceiling drops by the number ported; golden tests green.
**Why:** A dozen scripts each re-implement the `/dev/tcp` probe idiom slightly differently, so a fix to one probe never reaches the others and boot-integrity self-healing is only as good as the sloppiest copy.
**Dep:** AGY-1025 -- Port the greenboot AI-plane required health check to a static Rust binary

### AGY-1027 -- Port mios-db to a Rust shared-state CLI with out-of-band SQL binding  (WS-LANGX | P2 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- remove the string-spliced SQL injection surface from the OS's database CLI.
**What+How:** Reimplement `usr/libexec/mios/mios-db` — the `--pg`/`--owui`/`--embed`/`--pg-json`/`--config` modes, the psql -> podman-exec -> mios-pg-query fallback ladder, and the `sed s/'/''/g` escaping in `--config get/set` — as a Rust binary resolving `MIOS_PG_*`, `MIOS_PORT_PGVECTOR`, `MIOS_OWUI_DB` and `MIOS_EMBED_*` from mios-toml-core and passing values OUT-OF-BAND to the pg wire, never string-spliced, routing `--pg-json` straight to `mios-pg-query`'s parameterized path. Demote `mios-db` to a shim; keep the three-backend routing contract and the 0/1/2 exit codes. Golden-test the `--config get/set` SQL and the mode-dispatch error messages.
**Where:** `usr/libexec/mios/mios-db (-> shim), tools/native/ (new mios-db crate), usr/libexec/mios/mios-pg-query (parameterized backend, unchanged)`
**Done When:** the Rust `mios-db` produces identical stdout and exit codes for `--config get/set/show`, `--owui`, `--embed` and the no-flag error versus the bash on fixtures; SQL values are bound, not concatenated, with the sed-escape path gone; golden tests green; the ratchet ceiling is decremented.
**Why:** Quoting SQL by `sed`-escaping single quotes is a known-broken defense — one crafted config value written through `mios-db --config set` reaches the database as statement text, in a tool that holds the credentials for the agent datastore.
**Dep:** AGY-1013 -- Build mios-toml-core, the figment-based typed layered resolver crate

### AGY-1028 -- Port mios-doctor to a miosd doctor health-probe subcommand  (WS-LANGX | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one memory-safe diagnostic entry point for agents and operators, with no embedded python.
**What+How:** Reimplement `usr/libexec/mios/mios-doctor` — the ok/fail/warn/info probes for sudo grants, mount-namespace escapability, podman `additionalimagestores` permissions, hermes session state, OWUI registration and the primary services, with ports resolved from install.env — as `miosd doctor`, reusing the greenboot probe helpers and mios-toml-core for ports. Keep the grep-able `── ── ──` section headers byte-stable so existing agent investigation flows keep parsing. Demote `mios-doctor` to a shim and snapshot the section structure plus exit code via trycmd.
**Where:** `usr/libexec/mios/mios-doctor (-> shim), src/mios-rs/miosd/src/ (doctor module) or tools/native/, /etc/mios/install.env (input)`
**Done When:** `miosd doctor` emits the same section headers and pass/fail verdicts as the bash on a fixture host; the exit code matches; the embedded `python3 -c` probes are gone; golden trycmd green; ratchet decremented.
**Why:** mios-doctor is the structured replacement for throwaway diagnostic scripts, but its embedded python heredocs make it unlintable and dependent on a python that the firstboot-tier may not carry — so the one tool used when things are broken is itself fragile.
**Dep:** AGY-1013 -- Build mios-toml-core, the figment-based typed layered resolver crate

### AGY-1029 -- Absorb mios-ssot-regen orchestration into a miosd ssot-regen subcommand with real exit aggregation  (WS-LANGX | P2 | S)
**Goal:** E-07 The drift-gate as the enforcement plane -- `mios-ssot-regen` becomes a trustworthy regenerator whose failures are visible.
**What+How:** Reimplement `usr/libexec/mios/mios-ssot-regen` — which sets `MIOS_TOML`, `MIOS_AGENT_PIPE_DIR`, `MIOS_VERB_MANIFEST` and `MIOS_CAPS_OUT`, then runs `mios-ai-manifest-gen`, `mios-ai-capabilities-gen`, `tools/roadmap-index.py`, `tools/generate-gate-index.py` and `tools/mios-dup-report` — as a `miosd ssot-regen` subcommand that sequences the same generators with typed env from mios-toml-core and aggregates exit codes properly, dropping the `|| true` that currently swallows every failure. Leave the individual generators unchanged and still invoked; demote the bash to a shim.
**Where:** `usr/libexec/mios/mios-ssot-regen (-> shim), src/mios-rs/miosd/src/ (ssot-regen module), tools/roadmap-index.py + tools/generate-gate-index.py + tools/mios-dup-report (invoked)`
**Done When:** `miosd ssot-regen` regenerates the same ai/v1 manifests plus roadmap and gate indices with byte-identical output versus the bash, but exits non-zero if any generator fails (no silent `|| true`); drift-gate green; ratchet decremented.
**Why:** A generator can fail today and the orchestrator still reports success, so a broken projection is committed and only surfaces later as a red drift-gate with no clue which generator produced it.

### AGY-1030 -- Port hermes-worker-firstboot to a small idempotent Rust unit binary (the firstboot port template)  (WS-LANGX | P2 | S)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- establish the reusable idempotent-unit template on the smallest firstboot before the big ones.
**What+How:** Port `usr/libexec/mios/hermes-worker-firstboot` (43 lines; the atomic + retried + idempotent firstboot pattern for the hermes worker) to a small Rust binary invoked unchanged by `hermes-worker-firstboot.service`. Establish the template here: env sourced via mios-toml-core, degrade-open so a failure never wedges boot, and sentinel-gated idempotence. Demote the bash to a shim behind the `[migration]` toggle and golden-test the observable side effects on a fixture.
**Where:** `usr/libexec/mios/hermes-worker-firstboot (-> shim), tools/native/ (new firstboot crate), usr/lib/systemd/system/hermes-worker-firstboot.service`
**Done When:** the Rust binary reproduces hermes-worker-firstboot's side effects and exit code idempotently (a second run no-ops); the `.service` invokes it unchanged; degrade-open is verified (a non-zero step never aborts boot); golden test green; ratchet decremented.
**Why:** Without an audited template proven on a 43-line unit, each of the larger firstboot ports (696 and 1916 lines) reinvents idempotence and degrade-open, and a mistake there wedges boot on real hardware.
**Dep:** AGY-1014 -- Stand up the golden-master parity harness (trycmd + insta)

### AGY-1031 -- Port libvirtd-firstboot to Rust on the firstboot-crate template  (WS-LANGX | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- harden VM-plane provisioning against bash's silent-failure defaults.
**What+How:** Port `usr/libexec/mios/libvirtd-firstboot` (118 lines) to a Rust binary using the firstboot-crate template: idempotent, degrade-open, env from mios-toml-core, invoked unchanged by its firstboot service. Capture the current virsh/libvirt provisioning side effects as golden fixtures first, then reproduce them in Rust with explicit error handling instead of bash's silent-failure defaults. Demote the script to a shim behind the `[migration]` toggle.
**Where:** `usr/libexec/mios/libvirtd-firstboot (-> shim), tools/native/ (firstboot crate), usr/lib/systemd/system/ (its firstboot unit)`
**Done When:** the Rust binary reproduces libvirtd-firstboot's side effects idempotently with the same exit code on fixtures; a second run no-ops; degrade-open is preserved; golden test green; ratchet decremented.
**Why:** Firstboot orchestrators are the hot, fragile class where an unquoted expansion or an unchecked failure leaves the virtualization plane half-provisioned with the unit still reporting success.
**Dep:** AGY-1030 -- Port hermes-worker-firstboot to a small idempotent Rust unit binary

### AGY-1032 -- Port wsl-firstboot to Rust with the WSL-detect gating preserved  (WS-LANGX | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- remove the silent-failure class from the developer-environment boot path.
**What+How:** Port `usr/libexec/mios/wsl-firstboot` (85 lines) to a Rust binary via the firstboot template, sourcing install.env-derived config through mios-toml-core and preserving both the WSL-detect gate (`systemd-detect-virt` == wsl) and the idempotent link/seed steps. Keep it degrade-open so a partial WSL provision never wedges the developer environment. Demote the bash to a shim behind `[migration]` and golden-test the WSL-path side effects.
**Where:** `usr/libexec/mios/wsl-firstboot (-> shim), tools/native/ (firstboot crate), usr/lib/systemd/system/mios-wsl-firstboot.service`
**Done When:** the Rust binary reproduces wsl-firstboot's WSL-gated side effects idempotently with an identical exit code; a non-WSL host no-ops; degrade-open preserved; golden test green; ratchet decremented.
**Why:** WSL persistence-link provisioning is a recurring developer-environment failure that fails silently and is then debugged by hand every time the dev VM is rebuilt.
**Dep:** AGY-1030 -- Port hermes-worker-firstboot to a small idempotent Rust unit binary

### AGY-1033 -- Port forge-firstboot.sh to Rust with explicit secret sourcing from secrets.env  (WS-LANGX | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- harden the release-topology plane where admin credentials and tokens are handled.
**What+How:** Port `usr/libexec/mios/forge-firstboot.sh` (307 lines of Forgejo admin/token provisioning) to a Rust binary via the firstboot template, reading the non-secret `MIOS_FORGE_*` half from mios-toml-core and secrets only from `/etc/mios/secrets.env`, with explicit idempotent "already provisioned" detection. Preserve degrade-open and the retry cadence owned by its service/timer. Demote `forge-firstboot.sh` to a shim behind `[migration]`, golden-test the API-call sequence against a mocked Forgejo, and keep the bash as fallback until parity is confirmed.
**Where:** `usr/libexec/mios/forge-firstboot.sh (-> shim), tools/native/ (firstboot crate), usr/lib/systemd/system/mios-forge-firstboot.service, /etc/mios/secrets.env (input)`
**Done When:** the Rust binary provisions Forgejo idempotently (second run no-ops) with the same observable API sequence and exit code as the bash against a mock; secrets are read only from `secrets.env`; golden test green; ratchet decremented.
**Why:** Forge provisioning handles admin credentials and API tokens in shell string context, where a mis-quoted value can leak into a process listing or a log — and Forgejo is a co-equal publisher in the release topology, so a half-provisioned forge breaks publishing parity.
**Dep:** AGY-1030 -- Port hermes-worker-firstboot to a small idempotent Rust unit binary

### AGY-1034 -- Port mios-swarm-pack-firstboot to Rust on the shared firstboot crate  (WS-LANGX | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- reuse the audited idempotent-unit template to shrink the bash surface again.
**What+How:** Port `usr/libexec/mios/mios-swarm-pack-firstboot` (112 lines) to a Rust binary via the firstboot template with idempotent, degrade-open provisioning and mios-toml-core-sourced config. Capture the current side effects as golden fixtures, reproduce them in Rust with real error propagation instead of bash defaults, and demote the script to a shim behind `[migration]`.
**Where:** `usr/libexec/mios/mios-swarm-pack-firstboot (-> shim), tools/native/ (firstboot crate), usr/lib/systemd/system/mios-swarm-pack-firstboot.service`
**Done When:** the Rust binary reproduces mios-swarm-pack-firstboot's side effects idempotently with a matching exit code on fixtures; degrade-open preserved; golden test green; ratchet decremented.
**Why:** Another firstboot orchestrator whose failures are invisible to its unit; leaving it in bash keeps a duplicate, unaudited copy of the idempotence logic the shared crate already owns.
**Dep:** AGY-1030 -- Port hermes-worker-firstboot to a small idempotent Rust unit binary

### AGY-1035 -- Port mios-ai-firstboot to an idempotent sentinel-gated Rust provisioner (bash kept as fallback)  (WS-LANGX | P2 | XL)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- make the hottest firstboot in the tree memory-safe without ever wedging a boot.
**What+How:** Port `usr/libexec/mios/mios-ai-firstboot` (696 lines: the agent venv build via `automation/72-hermes-agent.sh`, the BROKEN-VENV cleanup, the OFFLINE OCI-image venv seed via a podman tar-pipe, the llama.cpp GGUF fetch, the WSL persistence symlinks, and the `/var/lib/mios/.ai-firstboot-done` sentinel written ONLY when every piece is in place) to a Rust binary via the firstboot template. Preserve the `set +eu` degrade-open semantics EXACTLY, the timer-owned retry (`ConditionPathExists=!sentinel`), and every idempotence guard. Keep the bash as fallback behind `[migration]` until the sentinel-writing conditions and all observable side effects are proven identical on fixtures; only then delete the body.
**Where:** `usr/libexec/mios/mios-ai-firstboot (-> shim), tools/native/ (firstboot crate), usr/lib/systemd/system/mios-ai-firstboot.{service,timer}, /var/lib/mios/.ai-firstboot-done (sentinel)`
**Done When:** the Rust provisioner writes the sentinel under the identical all-pieces-present condition, seeds the venv offline from the OCI image byte-for-byte, and returns 0 (degrade-open) on any partial failure exactly like the bash; the `.timer` re-fire no-ops once complete; golden fixtures green; parity confirmed before the shim body is deleted.
**Why:** This is where the operator-reported "outages / must deploy every time" failures lived — a premature sentinel write leaves the AI plane permanently half-provisioned because the timer never retries, and 696 lines of `set +eu` shell is the least verifiable place in the boot path.
**Dep:** AGY-1019 -- Add the [migration] SSOT toggle for oxidizr-style per-verb Rust/bash opt-out and rollback

### AGY-1036 -- Extract the hermes config-YAML renderer out of the 1916-line mios-hermes-firstboot into a `mios-toml-core hermes-config` subcommand  (WS-LANGX | P2 | XL)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- moves the single largest firstboot bash mass onto the compiled tier by porting its pure SSOT-projection slice first (strangler-fig), so no shell is deleted before byte-parity is proven.
**What+How:** `usr/libexec/mios/mios-hermes-firstboot` regenerates `~/.hermes/config.yaml` and `/var/lib/mios/hermes/config.yaml` from `mios.toml [ai]` on every boot. Add a `hermes-config` subcommand to the `mios-toml-core` crate that deserializes `[ai]` into a typed struct and serializes both YAML files deterministically. Leave the service-restart/orchestration steps in bash for a later slice. Demote only the rendering slice of the bash to an exec of the binary, after golden-testing both rendered YAML files byte-for-byte against current bash output.
**Where:** `usr/libexec/mios/mios-hermes-firstboot (rendering slice -> binary call), tools/native/mios-toml-core/ (hermes-config subcommand), usr/lib/systemd/system/mios-hermes-firstboot.service, ~/.hermes/config.yaml + /var/lib/mios/hermes/config.yaml (outputs)`
**Done When:** `mios-toml-core hermes-config` writes both config.yaml files byte-identical to the current bash for the vendor SSOT; the firstboot script calls it instead of the inline renderer; the remaining bash line count is measurably smaller; the golden test is green; the bash-tool ratchet is unaffected until the whole tool becomes a shim.
**Why:** 1916 lines of boot-critical bash re-derive AI config from SSOT on every boot with no test; a silent render bug there ships a broken Hermes config to every host, and the mass keeps growing while it stays in shell.
**Dep:** Build mios-toml-core: one figment-based typed layered mios.toml resolver crate with a --shell emitter

### AGY-1037 -- Port mcp-server-runner to a Rust launcher with explicit argv (no string-built commands)  (WS-LANGX | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- removes a string-interpolating bash launcher from the AI plane's process-spawn seam.
**What+How:** Reimplement `usr/libexec/mios/mcp-server-runner` (161 lines of bash glue that launches MCP servers) as a Rust binary that resolves endpoints/ports through `mios-toml-core` and spawns each server process with an explicit arg vector rather than an interpolated command string. Keep the launch contract and the unknown-server error path identical; demote the bash to a shim behind `[migration]`. Golden-test the resolved argv + env for every configured server against the bash.
**Where:** `usr/libexec/mios/mcp-server-runner (-> shim), tools/native/ (new crate), usr/share/mios/mios.toml (mcp config)`
**Done When:** the Rust runner spawns each MCP server with byte-identical argv/env vs the bash on fixtures; the unknown-server error message and exit code match; the golden test is green; the bash-tool ratchet is decremented by 1.
**Why:** MCP server names and args flow from config into a shell command string today, which is an injection surface on the agent tool plane, and the endpoints it resolves are not provably SSOT-derived.
**Dep:** Build mios-toml-core: one figment-based typed layered mios.toml resolver crate with a --shell emitter

### AGY-1038 -- Replace the mios-dashboard python zipapp with a compiled Rust reporter binary  (WS-LANGX | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- retires an opaque packed-zipapp artifact (neither loose script nor hardened binary) from the image.
**What+How:** `usr/libexec/mios/mios-dashboard` is a ~18KB python zipapp fronted by `mios-dashboard.sh` and reached through the `dash`/`mini`/`mon` entries in KNOWN_VERBS in `usr/bin/mios`. Reimplement it as a Rust TUI/reporter (ratatui or a plain framed renderer) that reads service/port state via `mios-toml-core` plus the greenboot probe helpers. Keep `mios-dashboard.sh` as the thin `--dash`/`--mini`/`--monitor` dispatcher, repointed at the binary. Capture current `--dash` and `--mini` snapshot output as golden fixtures and match the framed layout before deleting the zipapp.
**Where:** `usr/libexec/mios/mios-dashboard (zipapp -> Rust binary), usr/libexec/mios/mios-dashboard.sh (thin dispatcher), automation/59-tools.sh + automation/01-system-files-overlay.sh (build/link), usr/bin/mios (dash/mini/mon verbs)`
**Done When:** the Rust dashboard renders `--dash`/`--mini` snapshots matching the zipapp's framed output on a fixture with ports driven from SSOT; the zipapp artifact no longer exists in the built image; `mios dash` / `mios mini` are unchanged for the operator; the golden test is green.
**Why:** A packed zipapp is un-reviewable in-tree and drags a Python runtime into the hot `mios mon` path, and its telemetry duplicates the greenboot probe helpers instead of using them.
**Dep:** none

### AGY-1039 -- Port mios-build-driver to a `miosd build` orchestrator behind the verb dispatcher  (WS-LANGX | P2 | XL)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- puts the hottest verb backend, the `mios build` pipeline driver, under a compiled orchestrator that owns step order and gating explicitly.
**What+How:** `mios-build-driver` (1021 lines) backs the `mios build` verb and runs the inside-MiOS-DEV pipeline: clone/overlay, identity save to `install.env`, `_hash_password`, and the full multi-format build with dashboard rendering. Port it to a `miosd build` subcommand, preserving idempotent resume (shallow clone, podman cache, saved identity) and degrade-open logging (`_log`/`_fail`). Run the Rust driver and the bash side-by-side on a dev build and diff the emitted `install.env` plus the pipeline step order to identical, then demote the bash to a shim. Use `mios-toml-core` for all config; the 66 `automation/NN-*.sh` steps stay thin shell RUNs and are not reimplemented.
**Where:** `usr/libexec/mios/mios-build-driver (-> shim), src/mios-rs/miosd/src/ (build module), usr/bin/mios (build verb), automation/build-mios.sh (invocation)`
**Done When:** `miosd build` drives the same pipeline step order, writes an identical `/etc/mios/install.env` identity block, and resumes idempotently like the bash on a dev build; the 66 NN-*.sh steps are byte-unchanged; the side-by-side diff is clean; the ratchet is decremented once the shim is the only bash left.
**Why:** TD-7 flags the driver's hand-maintained ordinal/gating blobs; today a silent bash failure aborts a multi-GB build with no typed error and no explicit DAG, and the ordering lives in untested shell.
**Dep:** Build mios-verb-dispatcher to replace the eval-bearing verb routing seam

### AGY-1040 -- Port mios-hardcode-lint to Rust and anchor the allowlist regexes that spuriously exempt real port literals  (WS-LANGX | P2 | L)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- restores the NO-HARDCODE (Law 7) gate's actual coverage by fixing the allowlist bug that lets `:8080`/`:8033` slip through.
**What+How:** Reimplement `usr/libexec/mios/mios-hardcode-lint` (Python: `_date_in_comment_py`/`_date_in_string_py` tokenize+AST scans, `_check_ports_ips_py`, `_header_risk` BOM/shebang rules, `scan`, `_load_allowlist` reading `[security.nohc_allowlist].exempt_files`/`exempt_patterns`) as a Rust tool folded into `mios-ssot-lint`, with a real tokenizer/AST-aware pass and, critically, ANCHORED allowlist regexes so canonical ports are no longer matched as substrings and exempted. Preserve `MIOS_HARDCODE_LINT_SOFT` advisory mode and the exact violation categories and exit codes. Demote the Python to a shim; golden-test the violation set against a fixture repo covering each class.
**Where:** `usr/libexec/mios/mios-hardcode-lint (Python -> shim), tools/native/mios-ssot-lint/ (folded), usr/share/mios/mios.toml ([security.nohc_allowlist]), automation/98-drift-checks.sh (invocation)`
**Done When:** the Rust linter reports an identical violation set to the Python on the fixture repo for the date-in-comment / date-in-string / header-risk / port-IP classes, EXCEPT that previously-mis-exempted ports are now flagged; `MIOS_HARDCODE_LINT_SOFT=1` remains advisory; the drift-gate is green after the newly-surfaced violations are floated; Law 15 mirrored in mios-bootstrap.git.
**Why:** The unanchored allowlist silently exempts hardcoded canonical ports, so the NO-HARDCODE law reports green while real violations accumulate; the scanner is also hot Python on the drift-check path.
**Dep:** Repair the tools/native cargo workspace: adopt the 5 orphaned crates as members

### AGY-1041 -- Port the vfio-config / vfio-check / vfio-toggle family to a `miosd vfio` subcommand set  (WS-LANGX | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- collapses ~1.8k lines of /sys-munging bash on the MiOS-Metal passthrough path into one audited compiled subcommand.
**What+How:** Port `usr/libexec/mios/vfio-config.sh` (891 lines) plus `vfio-check.sh` and `vfio-toggle.sh` (the `vfio`/`vfio-check`/`vfio-toggle` entries in KNOWN_VERBS) into `miosd vfio [config|check|toggle]` behind the verb dispatcher. Read IOMMU/passthrough config from `mios-toml-core` and parse `/sys` IOMMU groups natively instead of through awk/grep chains. Preserve the reporting format so `mios vfio-check` output is unchanged. Demote each `.sh` to a shim behind `[migration]` only after golden-testing the check/report output against real `/sys` fixtures.
**Where:** `usr/libexec/mios/vfio-config.sh + vfio-check.sh + vfio-toggle.sh (-> shims), src/mios-rs/miosd/src/ (vfio module), usr/bin/mios (vfio/vfio-check/vfio-toggle verbs)`
**Done When:** `miosd vfio check` emits a byte-identical binding/IOMMU report vs `vfio-check.sh` on a `/sys` fixture; config/toggle side effects match; the three verbs behave unchanged for the operator; golden tests green; the ratchet is decremented by 3.
**Why:** GPU passthrough binding is decided by fragile text-parsing of `/sys`; a misparse silently binds the wrong device or leaves the host GPU claimed, and this sits on the split-plane critical path.
**Dep:** Build mios-verb-dispatcher to replace the eval-bearing verb routing seam

### AGY-1042 -- Port capability-audit.sh (2632 lines, the largest bash tool) to a `miosd assess` subcommand  (WS-LANGX | P2 | XL)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- eliminates the single largest loose-script liability in usr/libexec/mios.
**What+How:** Port `capability-audit.sh`, which backs the `mios assess` verb in KNOWN_VERBS, to a `miosd assess` subcommand that enumerates hardware/system capabilities through native `/proc`, `/sys` and `/dev` reads and structured command output instead of long awk/grep chains, taking thresholds and config from `mios-toml-core`. Port section-by-section behind a feature flag, diffing each section's report against the bash to identical and keeping `capability-audit.sh` as the fallback until every section matches, then demote it to a shim. Snapshot the full report with trycmd on a fixture host.
**Where:** `usr/libexec/mios/capability-audit.sh (-> shim), src/mios-rs/miosd/src/ (assess module), usr/bin/mios (assess verb)`
**Done When:** `miosd assess` reproduces `capability-audit.sh`'s full report section-for-section on a fixture host under a golden snapshot; `mios assess` is unchanged for the operator; the 2632-line bash file is a shim; the ratchet is decremented.
**Why:** 2632 untested lines of capability-probing shell decide what MiOS believes the hardware can do; a parse regression there silently mis-provisions a host and nothing catches it.
**Dep:** Build mios-toml-core: one figment-based typed layered mios.toml resolver crate with a --shell emitter

### AGY-1043 -- Port system-env.sh and system-summary.sh to `miosd env` / `miosd summary` subcommands  (WS-LANGX | P3 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- consolidates the read-only diagnostics reporters onto the one resolver that already owns the MIOS_* projection.
**What+How:** Port `usr/libexec/mios/system-env.sh` (160 lines, the `mios env` verb, inspects the layered MIOS_* environment) and `system-summary.sh` (252 lines, the `mios summary` verb, ASCII system overview) into `miosd env` and `miosd summary`, driven by `mios-toml-core` plus native `/proc` reads. Keep the human-readable output shape so both verbs are unchanged. Demote both `.sh` files to shims behind `[migration]`; golden-test each output on a fixture.
**Where:** `usr/libexec/mios/system-env.sh + system-summary.sh (-> shims), src/mios-rs/miosd/src/ (env + summary modules), usr/bin/mios (env/summary verbs)`
**Done When:** `miosd env` and `miosd summary` produce byte-identical output vs their bash predecessors on a fixture; the two verbs are unchanged; golden tests green; the ratchet is decremented by 2.
**Why:** Both re-implement env-inspection logic that `mios-toml-core` already owns, so the layered resolution shown to an operator can disagree with the resolver the system actually uses.
**Dep:** Build mios-toml-core: one figment-based typed layered mios.toml resolver crate with a --shell emitter

### AGY-1044 -- Port tune-performance.sh to a `miosd tune` subcommand with checked sysfs writes  (WS-LANGX | P3 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- replaces 1265 lines of privileged bash redirects with explicit, error-checked writes driven by SSOT parameters.
**What+How:** Port `usr/libexec/mios/tune-performance.sh` (the `mios tune` verb: system-wide CPU isolation and latency tuning) to `miosd tune`, reading isolation/tuning parameters from `mios-toml-core` and applying sysfs/cmdline changes through explicit checked writes rather than bash's silent `>` redirects. Land a `--dry-run` parity mode first that prints the intended change set, diff it against the bash's intended actions to identical, then enable apply and demote the bash to a shim behind `[migration]`.
**Where:** `usr/libexec/mios/tune-performance.sh (-> shim), src/mios-rs/miosd/src/ (tune module), usr/bin/mios (tune verb), usr/share/mios/mios.toml (tuning params)`
**Done When:** `miosd tune --dry-run` lists the identical set of sysfs/cmdline mutations as `tune-performance.sh` on a fixture; apply mode produces the same effect and returns non-zero on a failed write; `mios tune` is unchanged; golden test green; ratchet decremented.
**Why:** A failed sysfs redirect in bash is invisible, so today a partial tune leaves the host in a half-isolated state that reports success, and the tuning parameters are not provably SSOT-sourced.
**Dep:** Build mios-verb-dispatcher to replace the eval-bearing verb routing seam

### AGY-1045 -- Port the flatpaks-manage.sh + flatpak-launch cluster into one Rust flatpak subcommand set  (WS-LANGX | P3 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- collapses a coupled bash cluster that builds `flatpak` commands by string interpolation into one audited crate.
**What+How:** Port `usr/libexec/mios/flatpaks-manage.sh` (the `mios flatpaks` verb) together with the tightly-coupled `flatpak-launch`, `mios-flatpak-init`, `mios-flatpak-install`, `mios-flatpak-overrides-apply` and `mios-flatpak-preflight` into a cohesive Rust flatpak subcommand set that shells out to `flatpak` with explicit argv and reads app/override config from `mios-toml-core`. Port the manager first since it is the verb backend, keep the leaf helpers as shims exec-ing the binary, and golden-test the install/override argv against the bash before deleting bodies.
**Where:** `usr/libexec/mios/flatpaks-manage.sh + flatpak-launch + mios-flatpak-init + mios-flatpak-install + mios-flatpak-overrides-apply + mios-flatpak-preflight (-> shims), tools/native/ (new flatpak crate), usr/bin/mios (flatpaks verb)`
**Done When:** the Rust flatpak tool issues byte-identical `flatpak` argv and override sets vs the bash family on fixtures; `mios flatpaks` is unchanged; golden tests green; the flatpak cluster's contribution to the bash-tool ratchet drops accordingly.
**Why:** Six scripts each build `flatpak` argv by interpolation, so app ids and override strings are duplicated and unquoted across the cluster, and every one of them counts against the 145-tool bash surface.
**Dep:** Build mios-verb-dispatcher to replace the eval-bearing verb routing seam

### AGY-1046 -- Add `check_langx_shim_retirement`: a rust-defaulted verb may not keep a fat bash body  (WS-LANGX | P2 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- makes the strangler-fig "delete the slice once rerouted" step a machine-enforced fitness function instead of a convention.
**What+How:** Add `check_langx_shim_retirement` to `automation/98-drift-checks.sh`. For every tool whose `[migration]` toggle default has flipped to `"rust"`, assert (a) a passing golden-parity fixture exists and (b) the bash file is either a <=15-line pure-exec shim or deleted, failing when a full bash body coexists with a rust-defaulted verb (the oxidizr gap-free-window rule). Also flag any bash tool ported to Rust that still carries live logic behind its shim. Record retired tools in `usr/share/mios/mios.toml [langx].retired` so the list is append-only. Degrade open when git/cargo are absent; keep negatives-before-main gate ordering.
**Where:** `automation/98-drift-checks.sh (new check_langx_shim_retirement), usr/share/mios/mios.toml ([langx].retired + [migration]), tools/generate-gate-index.py`
**Done When:** the check passes on the current tree, fails when a rust-defaulted verb still has a fat bash body, and fails when a retired tool reappears carrying logic; the generated gate index lists it; Law 15 mirrored in mios-bootstrap.git.
**Why:** Without it, a verb defaulted to Rust can silently keep a divergent bash body that a rollback or a `[migration]` flip re-activates, so the two implementations drift apart unnoticed and the ratchet never actually falls.
**Dep:** Add a monotonic bash-tool ratchet drift-check over usr/libexec/mios

### AGY-1047 -- Add a pyo3 face on mios-toml-core and retire the mios_toml.py + userenv.sh resolver twins  (WS-LANGX | P2 | XL)
**Goal:** E-09 One value, one name: the full de-duplication campaign -- collapses the three parallel SSOT resolvers into one crate so the Law-13 twin-parity drift class ends by construction (TD-3).
**What+How:** Expose `mios-toml-core` as a pyo3 extension module named `mios_toml` carrying the same public surface the Python libexec consumers use (`load_merged`, `colors`, `section`), so the ~120 Python tools that `import mios_toml` (including `mios-hardcode-lint`, the `mios-sync-theme` heredoc, `mios-dotfiles-render`) call the ONE Rust resolver. With the pyo3 face plus the existing `--shell` face covering shell consumers, reduce `usr/lib/mios/mios_toml.py`, `tools/lib/userenv.sh` and `usr/lib/mios/userenv.sh` to thin delegating shims and remove the `check_userenv_parity` twin-equivalence gate, whose reason to exist is gone. Prove identical results across the proptest oracle corpus before removing any Python/shell body; stage explicit paths when committing.
**Where:** `tools/native/mios-toml-core/ (pyo3 module), usr/lib/mios/mios_toml.py + tools/lib/userenv.sh + usr/lib/mios/userenv.sh (-> shims), automation/98-drift-checks.sh (retire check_userenv_parity), mios-bootstrap.git resolver mirror`
**Done When:** `python3 -c 'import mios_toml; ...'` returns structures identical to the retired pure-Python resolver across the proptest corpus; the shell, python and rust faces all derive from the one crate; `check_userenv_parity` is removed with a recorded justification; drift-gate green; Law 15 mirrored in both repos.
**Why:** Three hand-maintained resolvers held together by a parity gate is the duplication the operator flagged: every SSOT schema change must be made three times, and any miss surfaces only as a red gate rather than as a compile error.
**Dep:** Add a proptest differential oracle proving mios-toml-core == userenv.sh over random mios.toml

### AGY-1048 -- Fold the usr/bin/hermes launcher into the verb dispatcher  (WS-LANGX | P3 | S)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- removes a standalone bash launcher by keeping venv-binary resolution in one audited place.
**What+How:** `usr/bin/hermes` is a 39-line bash wrapper that exec's `/usr/lib/mios/agents/.venv/bin/hermes` after an existence check. Move its logic (venv-binary resolution plus the helpful not-installed error) into `mios-verb-dispatcher` as the `hermes` entry, reducing `usr/bin/hermes` to a one-line shim or a dispatcher symlink. Keep the exec-through-to-the-upstream-CLI contract identical. Golden-test both the not-installed error path and argv passthrough.
**Where:** `usr/bin/hermes (-> shim/symlink), tools/native/mios-verb-dispatcher/ (hermes entry), /usr/lib/mios/agents/.venv/bin/hermes (target)`
**Done When:** `hermes <subcommand>` passes argv through byte-identically and the not-installed path prints the same guidance with exit 127 as the bash; `usr/bin/hermes` is a shim; golden test green; ratchet decremented.
**Why:** Venv-path resolution duplicated outside the dispatcher means a venv layout change has to be fixed in two places, and this file counts against the loose-script surface for no benefit.
**Dep:** Build mios-verb-dispatcher to replace the eval-bearing verb routing seam

### AGY-1049 -- Extend the Containerfile rust-builder to build the tools/native workspace and COPY every release binary into /usr/libexec/mios  (WS-LANGX | P1 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the enabler that makes any Rust port actually ship inside the bootc image.
**What+How:** The Containerfile rust-builder stage (lines 40-70) only runs `cargo build --release` on `src/mios-rs` and COPYs a single `/miosd` into the image. The `tools/native` crates (`mios-ssot-lint`, `mios-bake-plan`, `mios-aiplane-lint`, `mios-drift-runner`, `mios-version-check`, `mios-wallpaperd`, `generate-names-registry`) and every newly-ported libexec binary never reach the running image; they are only built ad hoc on the host at drift-check time (`98-drift-checks.sh:4880` runs `cargo build --manifest-path tools/native/Cargo.toml`). Add a second builder stage that builds the `tools/native` workspace with the same registry and target cache mounts, then COPY each `target/release/<bin>` into `/usr/libexec/mios/`.
**Where:** `Containerfile, tools/native/Cargo.toml, src/mios-rs/Cargo.toml, automation/98-drift-checks.sh`
**Done When:** `podman build` produces an image containing `/usr/libexec/mios/<bin>` for every declared native crate; a new drift-check asserts each declared binary exists and is executable in the built layer; the bake is green.
**Why:** Every port is inert today: the compiled artifact is not in the image, so a ported tool cannot be verified end-to-end and the running system keeps executing the bash it was supposed to replace.
**Dep:** Repair the tools/native cargo workspace: adopt the 5 orphaned crates as members

### AGY-1050 -- Consolidate the two divergent Cargo workspaces (src/mios-rs/miosd + tools/native) under one root with shared workspace.dependencies  (WS-LANGX | P1 | L)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- one consolidated workspace with a single pinned Cargo.lock as the reproducibility SSOT for the whole native tier.
**What+How:** Two independent workspaces with two `Cargo.lock` files exist today: `src/mios-rs` (miosd, `drift.rs`/`main.rs`) and `tools/native` (the lint/plan/walk crates), each with its own edition/version and dependency graph. The workspace-repair work only adopts orphans WITHIN `tools/native` and leaves this split untouched, so miosd and the ported libexec binaries can never build or version-sync together. Pick one authoritative workspace root (or a virtual manifest including both trees), hoist edition/version and the common deps (clap, figment, serde) into `[workspace.dependencies]`, and unify to a single lockfile so one `cargo build --release` yields miosd plus all native crates.
**Where:** `src/mios-rs/Cargo.toml, src/mios-rs/miosd/Cargo.toml, tools/native/Cargo.toml, src/mios-rs/Cargo.lock, tools/native/Cargo.lock`
**Done When:** one workspace root builds miosd and every native crate against a single `Cargo.lock`; shared deps are declared exactly once; `cargo test` and the drift-gate are green.
**Why:** Two workspaces double the build, cache and lint surface, let miosd's dependency versions drift from the native tier, and block both the single-stage Containerfile build and a workspace-wide version-sync gate.
**Dep:** Extend the Containerfile rust-builder to compile the tools/native workspace and COPY every release binary into /usr/libexec/mios

### AGY-1051 -- Generalize the version-sync drift-check to walk every Cargo workspace member instead of a hardcoded 3-path list  (WS-LANGX | P2 | S)
**Goal:** E-07 The drift-gate as the enforcement plane -- closes a gate that is currently blind to most crates, including miosd.
**What+How:** `automation/98-drift-checks.sh` (~lines 3629-3636) enforces crate `version` == SSOT version over a HARDCODED list of exactly three manifests: `tools/native/Cargo.toml`, `mios-version-check/Cargo.toml`, `mios-wallpaperd/Cargo.toml`. It silently misses `src/mios-rs/miosd/Cargo.toml` and every crate a port adds, so a mismatched crate version passes. Replace the fixed `for _toml in ...` loop with a `cargo metadata --format-version=1` walk (or a `git ls-files '**/Cargo.toml'` walk) that checks the `version` of every workspace member against the `mios.toml` SSOT version.
**Where:** `automation/98-drift-checks.sh (~3625-3640)`
**Done When:** setting any crate, including miosd or a newly-added port crate, to a non-SSOT version makes the check fail; the check is green on a correct tree.
**Why:** A hand-maintained manifest list rots as ports add crates, so the exact gate meant to catch version drift is the thing carrying the drift; miosd's version is unchecked today.
**Dep:** Consolidate the two divergent Cargo workspaces (src/mios-rs/miosd + tools/native) under one root with shared workspace.dependencies

### AGY-1052 -- Enroll native /usr/libexec/mios binaries into fapolicyd file-trust so ComposeFS execution is not denied  (WS-LANGX | P1 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- keeps application whitelisting intact while the libexec fleet turns into non-rpm binaries.
**What+How:** `automation/40-fapolicyd-trust.sh` sets `trust = file,rpmdb` for application whitelisting on ComposeFS. Binaries COPY'd in by the Containerfile (miosd today, every port tomorrow) are not in the rpmdb, so they depend on the `file` trust backend and will be DENIED at exec unless explicitly enrolled in the fapolicyd trust db. Add a build phase that enumerates ELF binaries under `/usr/libexec/mios` and writes a generated fapolicyd trust drop-in (`fapolicyd-cli --file add` or a `trust.d` file, with fs-verity), plus a drift-check asserting every native libexec binary is trusted.
**Where:** `automation/40-fapolicyd-trust.sh, /etc/fapolicyd/trust.d, usr/lib/fapolicyd, automation/98-drift-checks.sh`
**Done When:** the fapolicyd trust db lists every native `/usr/libexec/mios` binary; a boot smoke-test executes each one with no fapolicyd denial; the drift-check fails when a binary is missing from the trust drop-in; gate green.
**Why:** A ported binary that fapolicyd blocks is strictly worse than the bash it replaced -- it fails closed at exec on exactly the hardened installs MiOS targets, and nothing today would catch it before boot.
**Dep:** Extend the Containerfile rust-builder to compile the tools/native workspace and COPY every release binary into /usr/libexec/mios

### AGY-1053 -- Preserve AI-hint discoverability for compiled ports via an out-of-band sidecar registry  (WS-LANGX | P1 | L)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- extends the AI-hint header convention to artifacts that have no shell header, so Law 16 conformance and AI-hint coverage survive compilation.
**What+How:** Every libexec bash tool carries `# AI-hint:` / `# AI-related:` / `# AI-functions:` headers consumed by `mios-ai-tag` (the taggability SSOT), `mios-ai-hint-coverage` (98-drift-checks check 5 ratchet), `check-template-conformance` (Law 16 / check 46) and `automation/manifest.json` (119 `full_content` entries via `tools/generate-ai-manifest.py`). A compiled binary has no such header, so deleting a ported bash tool regresses AI-hint coverage AND template conformance and drops the tool from the AI manifest/index. Define a sidecar registry (e.g. `usr/share/mios/aihints/<tool>.toml` or a `mios.toml [aihints]` block carrying hint/related/functions) and teach `mios-ai-tag`, `mios-ai-hint-coverage` and `check-template-conformance` to treat a binary with a registered sidecar as tagged.
**Where:** `usr/libexec/mios/mios-ai-tag, usr/libexec/mios/mios-ai-hint-coverage, usr/libexec/mios/check-template-conformance, tools/generate-ai-manifest.py, usr/share/mios/mios.toml, automation/98-drift-checks.sh (checks 5, 46)`
**Done When:** a ported binary whose bash source has been deleted keeps checks 5 and 46 green and still appears in the generated AI manifest; the sidecar registry is the SSOT for that tool's hint metadata.
**Why:** The AI plane discovers tools by header metadata, so each completed port would silently shrink agent capability and redden two already-green gates -- which in practice blocks deleting the bash at all.
**Dep:** Add the shim-retirement / delete-on-parity drift-check

### AGY-1054 -- Migrate the 70 systemd ExecStart references onto ported binaries with prefixes intact, gated by an ExecStart-resolves drift-check  (WS-LANGX | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- lets ports land without converting soft-fail provisioners into boot blockers.
**What+How:** 70 units under `usr/lib/systemd/system` invoke `/usr/libexec/mios/*` (verified: `mios-adguard-firstboot`, `mios-ai-firstboot`, `mios-cdi-detect`, `bootc-switch-from-build.sh`, `ceph-bootstrap.sh`, and others), many carrying load-bearing prefixes: `-` (degrade-open, Law 12), `+` (full privilege), `@`, plus ExecStartPre/Post ordering. A port must land its binary at the SAME `/usr/libexec/mios/<name>` path as a drop-in replacement and keep every prefix. Audit each ported unit for prefix preservation and add a drift-check that every `ExecStart*=/usr/libexec/mios/...` token resolves to an existing, executable path in the built tree.
**Where:** `usr/lib/systemd/system/*.{service,path}, automation/98-drift-checks.sh`
**Done When:** the drift-check fails on any dangling or non-executable ExecStart libexec path; all 70 units resolve after porting; ported units keep identical paths and `-`/`+` prefixes; gate green.
**Why:** A path rename or a dropped `-` prefix during a port silently turns a degrade-open provisioner into a boot-blocking one, and the tree has no gate today that even asserts an ExecStart target exists.
**Dep:** Extend the Containerfile rust-builder to compile the tools/native workspace and COPY every release binary into /usr/libexec/mios

### AGY-1055 -- Port the WSL early-boot bootstrap family (wsl-early, wsl-init, mios-wslg-env-import, wsl-theme-bridge.sh) to a `miosd wsl` subcommand  (WS-LANGX | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- hardens the pre-sysinit namespace/device shell on the cross-platform WSL leg.
**What+How:** `wsl-firstboot` is already targeted, but the rest of the boot-critical WSL2 chain is not: `wsl-early` (`mios-wsl-early.service`, pre-sysinit) marks `/` rshared and mknods `/dev/net/tun` + `/dev/fuse` for cockpit and rootless podman; `wsl-init` (`mios-wsl-init.service`) enforces `/etc/wsl.conf` integrity, maps WSLg X11 sockets and de-conflicts the podman/WSL2 IP subnet; plus `mios-wslg-env-import` and `wsl-theme-bridge.sh`. Fold them into `miosd wsl {early,init,wslg-env,theme-bridge}` using idempotent mount/mknod syscalls and real wsl.conf parsing, keeping the bash as a degrade-open fallback shim until byte-parity is proven.
**Where:** `usr/libexec/mios/{wsl-early,wsl-init,mios-wslg-env-import,wsl-theme-bridge.sh}, src/mios-rs/miosd, usr/lib/systemd/system/mios-wsl-early.service, usr/lib/systemd/system/mios-wsl-init.service`
**Done When:** the `mios-wsl-*` units invoke the `miosd wsl` subcommands; characterization tests match the bash on mounts, device nodes, `wsl.conf` content and the subnet fix; drift-gate green.
**Why:** This runs before sysinit and is the least-tested provisioner class in the tree; a partial failure leaves podman and cockpit broken on the Windows leg with no diagnostic.
**Dep:** Stand up the golden-master parity harness (trycmd + insta) for libexec verb ports

### AGY-1056 -- Port the GPU/CDI detection + passthrough family (gpu-detect, gpu-pv-detect, mios-cdi-detect, mios-gpu-passthrough, mios-sriov-init) to a `miosd gpu` subcommand  (WS-LANGX | P2 | XL)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- puts the boot-time hardware enumerators that gate all containerized GPU access on tested PCI/sysfs parsing.
**What+How:** Distinct from the `vfio-*.sh` work: these are the enumerators feeding the container GPU plane. `mios-cdi-detect` (293 lines) detects NVIDIA/AMD/Intel and writes `/run/cdi` specs before `nvidia-cdi-refresh.service`; `gpu-detect` toggles the nvidia blacklist and `GSK_RENDERER` based on virt status; `mios-gpu-passthrough`, `mios-sriov-init` and `gpu-pv-detect` complete the set (units `mios-cdi-detect.service`, `mios-gpu-detect.service`, and `50-gpu-passthrough.conf`). Port to `miosd gpu {detect,cdi,passthrough,sriov}` with robust `/sys/bus/pci` parsing and structured CDI YAML emission.
**Where:** `usr/libexec/mios/{gpu-detect,gpu-pv-detect,mios-cdi-detect,mios-gpu-passthrough,mios-sriov-init}, src/mios-rs/miosd, usr/lib/systemd/system/mios-cdi-detect.service, usr/lib/systemd/system/mios-gpu-detect.service`
**Done When:** `/run/cdi` output is byte-identical vs the bash across golden multi-vendor sysfs fixtures; the two units invoke `miosd gpu`; gate green.
**Why:** Multi-vendor sysfs/PCI text-parsing in shell decides whether containers see a GPU at all; a misparse silently disables acceleration for the whole AI plane or breaks passthrough, and there are no fixtures proving otherwise.
**Dep:** Stand up the golden-master parity harness (trycmd + insta) for libexec verb ports

### AGY-1057 -- Port the atomic bootc/OCI-delta swap family to a transactional `miosd bootc-apply` subcommand  (WS-LANGX | P1 | L)  **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- gives the highest-blast-radius operation in the tree a compiled, transactional implementation with explicit rollback.
**What+How:** `bootc-switch-from-build.sh` parses a `last_build.txt` sentinel and runs `bootc switch` to the freshly-built local image, driven by `mios-bootc-switch.path` + `mios-bootc-switch.service`; `mios-oci-delta-service.sh` with `mios-oci-delta-apply.service` performs the edge-delta OS swap; `mios-stage-oci-archive` stages the archive. Port to `miosd bootc-apply` with strict sentinel validation (present / absent / corrupt), transactional pre-checks and explicit rollback, keeping the bash as a fallback only until parity is proven.
**Where:** `usr/libexec/mios/{bootc-switch-from-build.sh,mios-oci-delta-service.sh,mios-stage-oci-archive}, src/mios-rs/miosd, usr/lib/systemd/system/mios-bootc-switch.path, usr/lib/systemd/system/mios-bootc-switch.service, usr/lib/systemd/system/mios-oci-delta-apply.service`
**Done When:** characterization tests cover sentinel present/absent/corrupt and an interrupted swap; the operation is idempotent and rolls back on failure; the units invoke `miosd bootc-apply`; gate green.
**Why:** A partial or interrupted swap driven by string-parsing shell can leave the host unbootable -- the difference between a self-updating machine and a brick -- with no test asserting the corrupt-sentinel path.
**Dep:** Stand up the golden-master parity harness (trycmd + insta) for libexec verb ports

### AGY-1058 -- Port the security-enrollment tools (mios-enroll-secure-boot, mios-clevis-luks-gen, mios-keyring-autounlock, selinux-init) to fail-closed Rust binaries  (WS-LANGX | P2 | L)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- makes the fail-closed contract on Secure Boot, LUKS and SELinux explicit and testable instead of implicit in shell exit codes.
**What+How:** These untargeted bash tools sit on the security path: `mios-enroll-secure-boot` enrolls the akmods MOK via `mokutil` so signed NVIDIA/ZFS kmods load under Secure Boot; `mios-clevis-luks-gen` binds LUKS via clevis; `mios-keyring-autounlock` handles keyring autounlock; `selinux-init` initializes SELinux. Unlike the degrade-open provisioners, silent failure here weakens Secure Boot or disk encryption. Port each to a small static Rust binary that FAILS CLOSED on its security assertion (non-zero exit, audit-logged) rather than degrading open, with characterization tests over `mokutil`/clevis fixtures.
**Where:** `usr/libexec/mios/{mios-enroll-secure-boot,mios-clevis-luks-gen,mios-keyring-autounlock,selinux-init}, src/mios-rs/miosd`
**Done When:** each replacement matches the bash on golden fixtures, provably exits non-zero when its security assertion fails, and emits an audit trail entry; gate green.
**Why:** Today a failed MOK enrollment or LUKS bind can exit 0 through shell fallthrough, so a host boots believing it is protected when it is not, and no test distinguishes the two.
**Dep:** Stand up the golden-master parity harness (trycmd + insta) for libexec verb ports

### AGY-1059 -- Port the eight-tool mios-flatpak-* lifecycle family to a native JSON-envelope binary  (WS-LANGX | P3 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- stabilizes the agent-facing JSON envelope shape by generating it from typed structs instead of hand-written bash.
**What+How:** The flatpaks-manage port covers only `flatpaks-manage.sh` + `flatpak-launch`; the real per-app install/override plane is the `mios-flatpak-*` family: `mios-flatpak` (a 286-line agent-facing JSON-envelope CLI with search/install/upgrade/run) plus `-init` and `-install` (units `mios-flatpak-init.service`, `mios-flatpak-install.service`), `-preflight`, `-overrides-apply`, `-icon-sanitize` and `-beta-migrate`. Port to a native `mios-flatpak` that still shells `flatpak` but emits stable JSON envelopes with `_emit_envelope` / `_emit_json_error` parity, reading scope and app ids from `mios.toml`.
**Where:** `usr/libexec/mios/mios-flatpak, usr/libexec/mios/mios-flatpak-init, usr/libexec/mios/mios-flatpak-install, usr/libexec/mios/mios-flatpak-preflight, usr/libexec/mios/mios-flatpak-overrides-apply, usr/libexec/mios/mios-flatpak-icon-sanitize, usr/libexec/mios/mios-flatpak-beta-migrate, tools/native, usr/lib/systemd/system/mios-flatpak-init.service`
**Done When:** JSON-envelope output is byte-parity with the bash under insta snapshots across search/install/upgrade/run and the error path; the init and install units invoke the native binary; gate green.
**Why:** Agent tool-calling parses that exact envelope, and a hand-built bash JSON emitter breaks it on any unescaped app name or error string -- on a path invoked per app launch, so it is hot as well as brittle.
**Dep:** Stand up the golden-master parity harness (trycmd + insta) for libexec verb ports

### AGY-1060 -- Port the root-as-git-tree machinery (git-root-init.sh, mios-sync-to-root, verify-root.sh) to a `miosd root` subcommand  (WS-LANGX | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- gives the "/ IS $ROOT" self-updating-tree design compiled guarantees on the code that mutates the live system root.
**What+How:** `verify-root.sh` is an early-boot composefs/ostree integrity check (`mios-verify-root.service`, basic.target); `git-root-init.sh` initializes the deployed `/` as a git working tree of the local repo; `mios-sync-to-root` applies the war-room ext4 replica LIVE onto the system root, gated to mutable MiOS-DEV roots only and never to an immutable bootc/ostree root. Port to `miosd root {verify,git-init,sync}`, preserving the mutable/immutable gate exactly and making verify fail closed. Keep the bash as a fallback until parity is proven.
**Where:** `usr/libexec/mios/{git-root-init.sh,mios-sync-to-root,verify-root.sh}, src/mios-rs/miosd, usr/lib/systemd/system/mios-verify-root.service, usr/lib/systemd/system/mios-git-root-init.service`
**Done When:** the mutable-vs-immutable gate is preserved and covered by a test that fails if sync is attempted against an immutable root; `miosd root verify` exits non-zero on an integrity mismatch; the units invoke miosd; characterization tests pass; gate green.
**Why:** A gating regression in `mios-sync-to-root` would write onto an immutable bootc root, and a soft-failing `verify-root` lets a tampered composefs boot unnoticed -- both under the code that underpins the self-updating tree.
**Dep:** Stand up the golden-master parity harness (trycmd + insta) for libexec verb ports

### AGY-1061 -- Collapse the three build-if-missing firstboot bash scripts into one SSOT-driven `miosd build-if-missing` provisioner  (WS-LANGX | P3 | M)  **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one compiled, sentinel-gated provisioner replaces triplicated shell so the image's firstboot path is code, not copy-paste.
**What+How:** Three uncovered bash firstboots implement the identical pattern -- probe whether a podman image exists, `podman build` it when missing, degrade-open on failure: `mios-agents-firstboot.sh` (A2O super-container, invoked as ExecStartPre), `mios-forgejo-runner-firstboot.sh` (register-once runner token), `mios-webtools-firstboot.sh` (crawl4ai + firecrawl stack). Replace all three with one parameterized `miosd build-if-missing <spec>` subcommand in `src/mios-rs/miosd` that reads its image build specs from `usr/share/mios/mios.toml`, so the sentinel/retry/idempotency/degrade-open logic exists exactly once. Repoint each `*.service` unit at the Rust subcommand and keep the bash scripts as an explicit fallback until parity holds.
**Where:** `usr/libexec/mios/{mios-agents-firstboot.sh,mios-forgejo-runner-firstboot.sh,mios-webtools-firstboot.sh}, src/mios-rs/miosd, usr/share/mios/mios.toml, the corresponding *.service units`
**Done When:** one Rust build-if-missing provisioner drives all three container builds from mios.toml specs; the run is sentinel-gated, idempotent and degrade-open; bash retained as fallback; `just drift-gate` green.
**Why:** Three independently-drifting copies of the same build-if-missing shell mean a retry or idempotency fix lands in one and rots in the other two, and a firstboot that is not idempotent re-builds images on every boot.
**Dep:** Port mios-ai-firstboot to an idempotent sentinel-gated Rust provisioner (bash kept as fallback)

### AGY-1062 -- Build a hermetic fake-root / recorded-invocation harness that proves side-effectful firstboot ports match bash  (WS-LANGX | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- no stateful script is deleted until its port is proven to issue the same external commands and filesystem writes.
**What+How:** The trycmd+insta golden-master harness verifies stdout/exit for read-only verbs only; the firstboot orchestrators (mios-ai-firstboot, the hermes/libvirtd/forge/webtools/agents provisioners, bootc-apply) mutate the filesystem and shell out to `podman`/`systemctl`/`bootc`/`mokutil`, so stdout proves nothing about them. Add a new test-support crate under `tools/native` that stands up a tempdir fake root plus PATH-shimmed fake `podman`/`systemctl`/`bootc`/`flatpak` binaries which RECORD their argv to a log. Tests under `src/mios-rs/miosd/tests` then assert a port emits the identical ordered external-command sequence and the identical FS effects as the bash original. Wire it into `cargo test` and the CI job.
**Where:** `tools/native (new test-support crate), src/mios-rs/miosd/tests, usr/libexec/mios firstboot family`
**Done When:** every firstboot/provisioner port carries a recorded-invocation characterization test matching bash's external-command sequence and FS effects; the tests run under `cargo test` and in CI; `just drift-gate` green.
**Why:** Stateful ports are the riskiest and least covered surface in the campaign -- without an invocation-recording oracle a Rust firstboot can silently skip a `podman build` or write to the wrong path and ship undetected.
**Dep:** Stand up the golden-master parity harness (trycmd + insta) for libexec verb ports

### AGY-1063 -- Regenerate the AI manifest/catalog after each shim retirement and gate on tracked build cruft  (WS-LANGX | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the agent plane's view of the tree stays truthful as bash tools are retired.
**What+How:** `automation/manifest.json` embeds `full_content` for 119 script entries and is produced by `tools/generate-ai-manifest.py`; retiring a ported bash tool leaves a stale `full_content` entry the AI plane keeps serving, and the `materialize-build-catalog.py` / `mios-codebase-index` indexes go stale alongside it. The tree already proves this cleanup is not happening: `usr/libexec/mios/57-mios-sys-build.sh.retrytest.bak`, `usr/libexec/mios/mios-metal-vfio-gen.bak` and committed `__pycache__` are all tracked. Add a mandatory post-retirement regeneration step (run `generate-ai-manifest.py` + reindex, staging explicit paths -- never `git add -A`, which strips digest pins) and add a check in `automation/98-drift-checks.sh` that fails on any tracked `*.bak`, `*.retrytest*` or `__pycache__` under `usr/libexec/mios`.
**Where:** `automation/manifest.json, tools/generate-ai-manifest.py, usr/libexec/mios/57-mios-sys-build.sh.retrytest.bak, usr/libexec/mios/mios-metal-vfio-gen.bak, usr/libexec/mios/__pycache__, automation/98-drift-checks.sh`
**Done When:** manifest and catalog are regenerated with retired entries removed; `git ls-files usr/libexec/mios` shows no `*.bak`/`*.retrytest*`/`__pycache__`; the new drift-check fails when one is reintroduced; `just drift-gate` green.
**Why:** A manifest full of ghost source content actively misleads the agent plane into editing files that no longer exist, and the .bak/pyc files already in the tree show the cleanup step does not exist today.
**Dep:** Add the shim-retirement / delete-on-parity drift-check

### AGY-1064 -- Settle the Law 14 target-language policy in SSOT so the drift-check matches the campaign's conditional Go allowance  (WS-LANGX | P2 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the per-domain language-enforcement law is machine-checked against a policy actually recorded in SSOT.
**What+How:** The Law 14 native-language check in `automation/98-drift-checks.sh` (~lines 3812-3833) hard-fails on any git-tracked `*.go`/`*.bat`/`*.cmd`/`*.cpp`, with `tools/mios-portal-app` as the sole hardcoded allowlist entry. The campaign brief permits Go "only if a lib demands it", so the first justified Go crate would immediately red the gate. Pick one policy and record it: either commit this domain to Rust-only, writing that into ADR-0011 and the `mios.toml [laws]` row, or replace the hardcoded exception with a `mios.toml`-driven allowlist key the check reads, so a Go crate can be admitted deliberately rather than by editing the gate. Resolve this BEFORE any non-Rust port is attempted.
**Where:** `automation/98-drift-checks.sh (~3812-3833), usr/share/mios/mios.toml ([laws]), docs ADR-0011`
**Done When:** the language policy is stated in `mios.toml [laws]` + ADR-0011 and the check's behavior matches it (Rust-only, or reading an SSOT allowlist key instead of the inline exception); `just drift-gate` green.
**Why:** Today the brief and the gate disagree -- the documented Go escape-hatch is an automatic build failure, so the conflict will surface mid-port as a red gate rather than as a decision.
**Dep:** Add cargo fmt/clippy -Dwarnings + cargo-deny/cargo-audit as native quality drift-gates

### AGY-1065 -- Co-port the build-observability companions (status, tail, log-copy, blade) with the mios-build-driver orchestrator  (WS-LANGX | P3 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the build plane and its readers move together so no bash/Rust seam forms over the shared sentinels.
**What+How:** `mios-build-driver` is already targeted for port, but its companion surface is not: `mios-build-status` and `mios-build-tail` read and stream build state, `copy-build-log.sh` is the ExecStart of the copy-build-log service and persists the log, and `mios-blade` is the related build UI helper. All four read the same sentinel files and log paths the driver writes. Fold them into the miosd build orchestrator as `miosd build {status,tail,log-copy}` subcommands sharing the driver's state model, and repoint the units at miosd.
**Where:** `usr/libexec/mios/{mios-build-status,mios-build-tail,copy-build-log.sh,mios-blade}, src/mios-rs/miosd`
**Done When:** status/tail/log-copy exist as miosd subcommands over the build-driver state model; their output matches the bash originals on golden build-log fixtures; the systemd units invoke miosd; `just drift-gate` green.
**Why:** Porting the writer while leaving the readers in bash splits one sentinel/log format across two languages, and the first format change silently breaks `mios-build-status` mid-build.
**Dep:** Port mios-build-driver to a miosd build orchestrator

### AGY-1066 -- Package the 122 libexec Python tools as an installable `mios_tools` module, killing the 52 sys.path hacks  (WS-DEBT | P1 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- an importable package boundary is the strangler-fig façade every later typing, test and Rust-port task lands into.
**What+How:** There is NO `pyproject.toml`/`setup.py` anywhere in the tree (verified), and 52 files under `usr/libexec/mios` do `sys.path.insert(0, '.../usr/lib/mios')` to reach the shared resolver. Create `usr/lib/mios/pyproject.toml` declaring a PEP 621 `mios_tools` package (hatchling/setuptools backend) owning `mios_toml.py`, `mios_env.py` and `mios_db_config.py` as importable submodules, and register the extensionless hyphenated tools (`mios-a2a-discover`, `mios-ai-tag`, `mios-hardcode-lint`, ...) as `[project.scripts]` console entry-points, e.g. `mios-a2a-discover -> mios_tools.a2a_discover:main`. Strangler-fig: land the package + entry-points FIRST while the tools stay at their current paths (the build installs the wheel into the image site-packages), then flip each tool body to `from mios_tools import config` in follow-ups. Keep each current path as a thin `runpy.run_module('mios_tools.<x>')` launcher until parity is proven, then delete the loose script.
**Where:** `usr/lib/mios/pyproject.toml (new), usr/lib/mios/mios_toml.py, usr/lib/mios/mios_env.py, usr/lib/mios/mios_db_config.py, usr/libexec/mios/* (122 python tools), automation/*.sh (install phase)`
**Done When:** `python -m build` produces a wheel and `pip install` exposes `import mios_tools.config` with NO `sys.path.insert`; the 3 shared modules import cleanly as `mios_tools.*`; `just drift-gate` green; changes staged by explicit path (no `git add -A`).
**Why:** 52 fragile `sys.path.insert` calls and the SourceFileLoader workarounds they force break on any file move, and without importable modules there is nothing for mypy, pytest coverage or a typed model layer to attach to.
**Dep:** none

### AGY-1067 -- Capture every SSOT `--check` projector's stdout/stderr/exit in a trycmd golden-master harness before any Rust port  (WS-DEBT | P1 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- a recorded parity oracle exists before a single security projector is rewritten.
**What+How:** `generate-cosign-policy.py`, `generate-uki-cmdline.py`, `generate-egress-firewall.py`, `generate-pod-quadlets.py`, `generate-names-registry.py` and `compile-templates.py` all expose a deterministic `--check` drift mode (verified: each diffs its generated artifact against the tracked file and exits 1 on drift). Before porting ANY of them, add a Rust `trycmd` crate at `tools/native/mios-projector-golden/` that runs each current `.py` against a frozen fixture `mios.toml` and records stdout + stderr + exit code into committed `.trycmd`/`.md` snapshots. Capture BOTH modes: the generate mode (the emitted file bytes) and the `--check` mode (the diff/OK message). Add `cargo test -p mios-projector-golden` to the `Justfile` drift-gate recipe (~line 81) and document the trycmd/insta accept workflow in the crate README.
**Where:** `tools/native/mios-projector-golden/ (new crate), tools/generate-cosign-policy.py, tools/generate-uki-cmdline.py, tools/generate-egress-firewall.py, tools/generate-pod-quadlets.py, tools/generate-names-registry.py, tools/compile-templates.py, Justfile (drift-gate recipe ~line 81)`
**Done When:** `cargo test -p mios-projector-golden` passes against committed fixtures for >=6 projectors; snapshots include exit code plus full stdout/stderr; the harness runs inside `just drift-gate`; fixtures are byte-frozen with an accept workflow documented.
**Why:** Without a recorded oracle, a Rust rewrite of a projector that emits `policy.json` or the kernel cmdline can diverge byte-wise and nobody will notice until a boot-integrity control silently stops enforcing.
**Dep:** none

### AGY-1068 -- Consolidate the 8 tools/native crates into ONE cargo workspace with a committed Cargo.lock and cargo-deny/audit gates  (WS-DEBT | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- one workspace, one lockfile, one clippy/audit posture that every Rust port inherits.
**What+How:** `tools/native/Cargo.toml` declares a workspace with only 3 members (`mios-drift-runner`, `mios-version-check`, `mios-wallpaperd`), yet 5 more crate directories exist and build out-of-tree: `generate-names-registry`, `mios-aiplane-lint`, `mios-bake-plan`, `mios-ssot-lint`, `mios-ssot-walk` (verified by ls). Add all 8 as workspace members so they share one `Cargo.lock` and one target dir. Add `tools/native/deny.toml` covering the advisories DB, a license allow-list matching the FOSS posture, and banned/duplicate-crate rules; wire `cargo deny check` and `cargo audit` as new drift-gate steps in the `Justfile` and `.github/workflows/mios-ci.yml`. Commit `Cargo.lock` as a reproducibility SSOT (SBOM-not-hardcode: dependency digests recorded at build, not hand-pinned). Add `cargo fmt --check` and `cargo clippy --workspace -- -D warnings` alongside.
**Where:** `tools/native/Cargo.toml, tools/native/Cargo.lock, tools/native/deny.toml (new), tools/native/{generate-names-registry,mios-aiplane-lint,mios-bake-plan,mios-ssot-lint,mios-ssot-walk}/Cargo.toml, Justfile, .github/workflows/mios-ci.yml`
**Done When:** `cargo build --workspace` builds all 8 crates from `tools/native/`; `cargo deny check` and `cargo audit` pass and run in CI; `cargo clippy --workspace -- -D warnings` is clean; exactly one `Cargo.lock` is committed; `just drift-gate` green.
**Why:** Five compiled tools currently build outside the workspace with no shared lockfile, no clippy, and no advisory scanning -- the bash they replace at least had shellcheck, so today's ports are less vetted than the scripts.
**Dep:** none

### AGY-1069 -- Eliminate the os.system() command-injection sites in mios-models and mios-v2v-import  (WS-DEBT | P1 | S)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the shell-injection surface is removed from the OS-plane tool fleet.
**What+How:** Two injection surfaces are confirmed: `usr/libexec/mios/mios-models:46` runs `os.system(f"python3 {fb}")`, interpolating a path into a shell string, and `usr/libexec/mios/mios-v2v-import:88` runs `os.system(cmd_str)` on a fully string-built virt-v2v command. Replace both with `subprocess.run([...], check=...)` passing an argv LIST so no value is re-parsed by `/bin/sh`: `mios-models` becomes `subprocess.run([sys.executable, fb], ...)`, and `mios-v2v-import` builds its command as a list from the start rather than joining a string. Preserve degrade-open behavior (`mios-v2v-import` already guards with `shutil.which('virt-v2v')`). Add a ruff/grep drift-check banning `os.system(` under `usr/libexec/mios`.
**Where:** `usr/libexec/mios/mios-models (line ~46), usr/libexec/mios/mios-v2v-import (line ~88)`
**Done When:** `grep -r 'os.system(' usr/libexec/mios` returns nothing; both tools invoke subprocess with an argv list; behavior is unchanged whether the target binary is present or absent; the drift-gate fails on any reintroduced `os.system`; `just drift-gate` green.
**Why:** `os.system` on an f-string or joined command is textbook shell injection in a tool that runs with OS-plane privilege -- a filename containing `;` is arbitrary code execution today, and the fix is a few lines.
**Dep:** none

### AGY-1070 -- Harden sqlite identifier interpolation in the CLIs behind a validated table allowlist  (WS-DEBT | P1 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the residual SQL-injection surface the parameterized-postgres gate does not cover is closed.
**What+How:** f-string interpolation of table names into SQL is confirmed at `usr/libexec/mios/mios-cache-clear:176` (`DELETE FROM {t}`), `mios-owui-apply-knowledge:143` (`PRAGMA table_info({table})`) and `:214` (`SELECT 1 FROM {tbl}`), and `mios-owui-bootstrap-admin:313` (a dynamic column list spliced into an INSERT). sqlite cannot bind identifiers, so wrap each splice in a strict validator: assert the identifier matches `^[A-Za-z_][A-Za-z0-9_]*$` AND is a member of an explicit allowlist derived from the known schema, raising a typed error before it ever reaches `execute()`. Add a shared `mios_tools.sql.safe_ident()` helper (after packaging) so all four sites use one validated path, and extend the existing `check_cli_sql_safety` gate (`automation/98-drift-checks.sh` ~line 685, with `test_mios_cli_sqlsafety.py` present) to fail on any new unvalidated f-string identifier reaching `execute()`.
**Where:** `usr/libexec/mios/mios-cache-clear (line ~176), usr/libexec/mios/mios-owui-apply-knowledge (lines ~143,214), usr/libexec/mios/mios-owui-bootstrap-admin (line ~313), usr/libexec/mios/test_mios_cli_sqlsafety.py, automation/98-drift-checks.sh`
**Done When:** every identifier splice passes through `safe_ident()` with regex + allowlist validation; a malformed table name raises before `execute()`; `test_mios_cli_sqlsafety.py` gains a rejection case; the drift-gate fails on a new raw f-string identifier; `just drift-gate` green.
**Why:** These four call sites are the only remaining path where an attacker-influenced string reaches SQL unvalidated, and the existing gate does not see them, so the class can grow unnoticed.
**Dep:** none

### AGY-1071 -- Make ruff lint+format a mandatory Python drift-gate across libexec, lib and tools  (WS-DEBT | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the Python plane gets the static analysis the shell plane already has.
**What+How:** No ruff/mypy/pytest/pip-audit config exists anywhere (verified -- CI runs only shellcheck plus the agent-pipe tests). Add `usr/lib/mios/ruff.toml` (or `[tool.ruff]` in the new pyproject) enabling at minimum E/F/W, B (bugbear), S (bandit rules -- these flag `eval`/`exec`/`os.system`/`subprocess shell=True`), E722 (bare except -- 7 live in `MiOS-Mon.py`), UP (pyupgrade), plus format checking. Run `ruff check usr/libexec/mios tools usr/lib/mios` and `ruff format --check` as a new step in `automation/98-drift-checks.sh` (or `97-ssot-lint.sh`) and in the `Justfile` drift-gate, keeping the negatives-before-main-gate ordering. Seed a curated per-file `# noqa`/ignore baseline for pre-existing violations so the gate lands green, then burn the baseline down strangler-style.
**Where:** `usr/lib/mios/ruff.toml (new), automation/98-drift-checks.sh, Justfile (drift-gate ~line 81), .github/workflows/mios-ci.yml (~line 123)`
**Done When:** `ruff check` and `ruff format --check` run in CI and in `just drift-gate`; the S/B/E722 rule sets are active; a newly-added bare-except or `eval` fails the gate; the baseline is documented; the gate is green on the current tree.
**Why:** The Python half of the tree -- 122 libexec tools plus tools/ -- has zero static analysis today, so every injection, bare-except and dead-import fix in this campaign is a one-off with nothing preventing the next one.
**Dep:** none

### AGY-1072 -- Pin and audit the Python third-party dependency surface with a constraints lock + pip-audit gate  (WS-DEBT | P1 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- Python dependencies become build-resolved SBOM data with an advisory gate, matching the cargo side.
**What+How:** The libexec tools import unpinned third-party packages (verified imports: `fastapi`, `uvicorn`, `cryptography`, `websockets`, `huggingface_hub`, `psycopg`, `tomli`) with no `requirements.txt` or constraints file anywhere. Declare them in the new pyproject `[project.dependencies]` (or `usr/lib/mios/requirements.txt`) with lower bounds only -- SSOT carries version INTENT -- then generate a fully-pinned `usr/lib/mios/constraints.lock` at build time via pip-compile/uv, and add `pip-audit -r constraints.lock` as a drift-gate step mirroring `cargo audit`. Feed the resolved dependency digests into `automation/90-generate-sbom.sh` as SBOM data (ADR-0003 SBOM-not-hardcode: resolved and recorded at build, never hand-pinned into SSOT prose).
**Where:** `usr/lib/mios/pyproject.toml (or requirements.txt, new), usr/lib/mios/constraints.lock (new, generated), automation/90-generate-sbom.sh, automation/98-drift-checks.sh, .github/workflows/mios-ci.yml`
**Done When:** every third-party import appears in the dependency manifest; a pinned `constraints.lock` is generated at build; `pip-audit` runs in CI and fails on a known-vuln advisory; the lock's contents appear in the generated SBOM; `just drift-gate` green.
**Why:** `cryptography`, `fastapi` and `huggingface_hub` are attack-surface-heavy, currently float unversioned and unaudited, and are invisible to the SBOM -- so a published image cannot answer which Python versions it actually shipped.
**Dep:** none

### AGY-1073 -- Wire the already-written Rust generate-names-registry into the build and check 30; demote the .py to a toggled fallback, then delete it  (WS-DEBT | P1 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the first real cutover demonstrates the strangler-fig + SSOT-toggle + byte-parity playbook.
**What+How:** A Rust port ALREADY exists at `tools/native/generate-names-registry/src/main.rs` (6115 bytes, references check 30), but the drift-gate at `automation/98-drift-checks.sh:2103+` still invokes `tools/generate-names-registry.py` (164 lines) -- the compiled implementation is dead code. Cut over in four steps: (1) add the crate to the consolidated workspace and build it into the image; (2) run BOTH implementations against the current tree and assert `usr/share/mios/names.generated.txt` and `usr/share/mios/referenced_names.txt` come out byte-identical; (3) flip check 30 to call the Rust binary with the `.py` retained as a `--fallback` behind a new SSOT toggle `mios.toml [migration] use_rust_verbs`; (4) after a clean window, delete `tools/generate-names-registry.py`. Mirror the SSOT surface change in BOTH repos (Law 15).
**Where:** `tools/native/generate-names-registry/, tools/generate-names-registry.py, automation/98-drift-checks.sh (lines ~2069-2152), usr/share/mios/names.generated.txt, usr/share/mios/referenced_names.txt, usr/share/mios/mios.toml [migration]`
**Done When:** check 30 invokes the Rust binary; Rust and `.py` outputs are proven byte-identical on the current tree; flipping one `[migration] use_rust_verbs` value rolls back to the `.py`; `just drift-gate` green; the `.py` deleted or shimmed with the change staged by explicit path.
**Why:** A finished Rust binary sits unused while the gate runs the Python it was written to replace -- so the campaign carries maintenance cost for two implementations and has proven neither the toggle nor the parity workflow that 20 later ports depend on.
**Dep:** Consolidate the fragmented tools/native crates into ONE cargo workspace

### AGY-1074 -- Replace `eval "$@"` on agent-controlled args in mios-ai-clear/reset/launch/window with hardened dispatch (TD-1)  (WS-DEBT | P1 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the eval-on-agent-args injection surface is gone from the verb library.
**What+How:** The TD-1 eval-on-input sites are confirmed: `usr/libexec/mios/mios-ai-clear:65` `eval "$@"`, `mios-ai-reset:91` `eval "$@"`, `mios-launch:507` `eval exec "$_hit_cmd"` (the file already carries a TD-1 note at :721 recommending shlex.split), and `mios-window:386` `eval "$(python3 - ...)"`. Replace each string-eval with a safe form: the `run()` helpers in `mios-ai-clear`/`mios-ai-reset` invoke the argv array directly (`"$@"`) instead of `eval "$@"`; `mios-launch` tokenizes the launch field with shlex and execs the resulting array; `mios-window` has its python helper emit a NUL-separated array the shell reads without eval. Where a command genuinely must be data-driven, use an explicit `case`/associative-array dispatch table of allowed verbs. Add a drift-check banning bare `eval ` in `usr/libexec/mios` verbs, allowlisting any proven-safe residual.
**Where:** `usr/libexec/mios/mios-ai-clear (line ~65), usr/libexec/mios/mios-ai-reset (line ~91), usr/libexec/mios/mios-launch (lines ~507,721), usr/libexec/mios/mios-window (line ~386), automation/98-drift-checks.sh`
**Done When:** no `eval` on agent- or user-derived strings remains in the four verbs; launch dispatch uses shlex plus array exec; the drift-gate fails on a new `eval` in a libexec verb; existing launch behavior is held by a characterization test; `just drift-gate` green.
**Why:** Agent-supplied arguments are an explicitly untrusted boundary, and `eval` on them is the Gemini-CLI-class remote-code-execution bug -- a crafted tool argument today runs arbitrary shell as the verb's user.
**Dep:** none

### AGY-1075 -- Replace the SourceFileLoader dynamic-import hacks with clean `mios_tools.*` package imports  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- runtime code-loading-by-file-path disappears once the tools are importable modules.
**What+How:** Because the hyphenated tools are not importable, three files load siblings via `importlib.machinery.SourceFileLoader` (verified): `check-template-conformance:25` loads `mios-ai-tag`, `mios-a2a-discover:27`/`:67` loads `mios-a2a-mdns`, and `mios-ai-hint-coverage:56` loads `mios-ai-tag`. Once the `mios_tools` package lands (entry-points give underscore module names), rewrite these to `from mios_tools import ai_tag` / `from mios_tools import a2a_mdns` and delete the SourceFileLoader plus `sys.path.insert` scaffolding. Behavior is unchanged -- the tagger and the mDNS browse helper become ordinary package attributes.
**Where:** `usr/libexec/mios/check-template-conformance (line ~25), usr/libexec/mios/mios-a2a-discover (lines ~27,67), usr/libexec/mios/mios-ai-hint-coverage (line ~56)`
**Done When:** no `SourceFileLoader` or `spec_from_loader` remains in the three tools (test_*.py fixtures may keep theirs); they import siblings via `mios_tools.*`; the tag-coverage and a2a tests still pass; `just drift-gate` green.
**Why:** Loading code by hardcoded file path breaks the moment a tool is renamed or ported, and it is a dynamic-exec smell that ruff's S rules will flag -- the packaging work makes it entirely unnecessary.
**Dep:** Package the 122 libexec Python tools as an installable `mios_tools` module

### AGY-1076 -- Fold mios_toml.py, mios_env.py and mios_db_config.py into one typed `mios_tools.config` surface  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the shared resolver becomes one facade, the seam the typed-model and compiled-resolver work plugs into.
**What+How:** The three shared modules (`usr/lib/mios/mios_toml.py` 601 lines, `mios_env.py` an 18-line strip_empty helper, `mios_db_config.py`) are imported ad-hoc by 43+ tools. After packaging, merge them behind one `mios_tools.config` facade exposing `load_merged`/`load_vendor`/`section`/`colors`/`layer_paths` (from mios_toml) plus `strip_empty_mios_env` (from mios_env) plus the DB-config accessor, so callers write `from mios_tools import config` once. Keep the three files as thin re-export shims (`from mios_tools.config import *`) during migration so no consumer breaks, update consumers in batches, then delete the shims. This is a pure re-home with no behavior change -- assert that by keeping `tools/check-resolver-twin.py` green, which proves twin-equivalence with `userenv.sh` still holds.
**Where:** `usr/lib/mios/mios_toml.py, usr/lib/mios/mios_env.py, usr/lib/mios/mios_db_config.py, usr/lib/mios/mios_tools/config.py (new), tools/check-resolver-twin.py, usr/libexec/mios/* (43 importers)`
**Done When:** `from mios_tools import config` exposes the union API; the three legacy modules are re-export shims or removed with consumers updated; `check-resolver-twin.py` still passes; `just drift-gate` green; SSOT twin surfaces mirrored in both repos (Law 15).
**Why:** Three separate shared modules reached 43 different ways is the duplication the operator flagged, and there is no single place to attach types, so a resolver contract change today means auditing 43 import sites.
**Dep:** Package the 122 libexec Python tools as an installable `mios_tools` module

### AGY-1077 -- Introduce pydantic v2 typed models for the mios.toml sections the tools consume, replacing raw dict .get() plumbing  (WS-DEBT | P2 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the SSOT schema becomes code, so a mistyped key fails loudly instead of projecting a wrong value.
**What+How:** `mios_toml.load_merged()` returns an untyped dict and consumers chain `.get()` into it (e.g. `tools/generate-cosign-policy.py` does `data.get('security',{}).get('sigstore',{})`; `seed-db-config.py` does the same for env+toml plumbing; dozens more). Define pydantic v2 `BaseModel`s in `usr/lib/mios/mios_tools/models.py` for the high-traffic sections first -- `[security.sigstore]`, `[ports]`, `[colors]`, `[ai.vllm]`/`[ai.sglang]`, `[templates]`, `[migration]` -- with real types (ports as int, colors as validated hex, `[security.nohc_allowlist]` typed). Add `config.load_typed() -> MiosConfig` that deserializes the merged overlay and raises on any shape/type error. Migrate the projectors and `seed-db-config.py` to the typed accessor incrementally, leaving raw `load_merged()` available as a fallback until every consumer is converted.
**Where:** `usr/lib/mios/mios_tools/models.py (new), usr/lib/mios/mios_toml.py, tools/generate-cosign-policy.py, usr/libexec/mios/seed-db-config.py, usr/share/mios/mios.toml`
**Done When:** `MiosConfig` models cover >=6 sections; `config.load_typed()` round-trips the shipped `mios.toml` without error and raises on a deliberately malformed fixture; >=3 consumers are off raw `.get()`; mypy is clean on the models; `just drift-gate` green.
**Why:** Chained `.get()` turns a renamed or mistyped SSOT key into a silent default that flows straight into a Quadlet or `policy.json` -- the failure appears as a misconfigured running system, not as an error.
**Dep:** Fold mios_toml.py, mios_env.py, and mios_db_config.py into a single typed mios_tools.config surface

### AGY-1078 -- Add a mypy --strict drift-gate seeded on the resolver, models and projectors  (WS-DEBT | P2 | L)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the Python that stays Python gets a type guarantee on the SSOT-critical modules.
**What+How:** No mypy config exists. Add `[tool.mypy]` to the new pyproject with `strict = true` and run `mypy` as a drift-gate step over a curated, growing include-list starting with `mios_tools/config.py`, `mios_tools/models.py` and the SSOT projectors (`tools/generate-*.py`) -- the smallest, most safety-critical modules. Use per-module `ignore_errors` overrides for the untyped long tail so the gate lands green, then advance the strict frontier module-by-module (strangler-fig for types). Fully annotate the resolver's public API (`load_merged`/`section`/`colors` return types) so downstream callers are checked too.
**Where:** `usr/lib/mios/pyproject.toml ([tool.mypy]), usr/lib/mios/mios_tools/config.py, usr/lib/mios/mios_tools/models.py, tools/generate-*.py, automation/98-drift-checks.sh, Justfile`
**Done When:** `mypy` runs in CI and `just drift-gate` over the strict include-set with zero errors; the resolver public API is fully annotated; a newly-added untyped function in an in-scope module fails the gate; the include-list ships with a documented shrink plan.
**Why:** The modules that decide what gets projected into `policy.json`, the kernel cmdline and the Quadlets are exactly where a wrong type silently corrupts an SSOT projection, and today nothing checks them.
**Dep:** Introduce pydantic v2 typed models for the mios.toml sections the tools consume

### AGY-1079 -- Wire the scattered libexec test_*.py into a pytest + coverage drift-gate with a ratcheting floor  (WS-DEBT | P2 | M)
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- every test that exists actually runs, and coverage only goes up.
**What+How:** Ten `test_*.py` files live under `usr/libexec/mios` (`test_mios_cli_sqlsafety`, `test_mios_daemon_*`, `test_mios_docgen`, `test_mios_find_ranker`, `test_mios_pgwire`, `test_mios_rechunk`, `test_mios_skills_passk`, `test_mios_window_region`) alongside a large agent-pipe suite, but only the agent-pipe tests plus `test_mios_docgen` run in the `Justfile` drift-gate (verified ~lines 86-95). Add `[tool.pytest.ini_options]` and `[tool.coverage]` to the pyproject, collect `usr/libexec/mios/test_*.py` under one `pytest` invocation, and add that invocation as a drift-gate step. Enforce a coverage floor via `pytest --cov --cov-fail-under`, starting at today's measured percentage and ratcheting up. Extend the existing `check_module_test_coverage` gate to require a sibling test for any newly-added libexec tool above a size threshold.
**Where:** `usr/lib/mios/pyproject.toml ([tool.pytest.ini_options],[tool.coverage]), usr/libexec/mios/test_*.py (10 files), Justfile (drift-gate ~line 86), automation/98-drift-checks.sh (check_module_test_coverage)`
**Done When:** `pytest usr/libexec/mios` runs all 10 test files inside the drift-gate; `--cov-fail-under` enforces a floor matching today's measurement; adding a large untested tool trips `check_module_test_coverage`; `just drift-gate` green.
**Why:** Eight of the ten existing libexec test files never execute in CI, so they can rot to failing without anyone knowing -- and the Rust ports are supposed to characterize against them.
**Dep:** Package the 122 libexec Python tools as an installable `mios_tools` module

### AGY-1080 -- Port generate-cosign-policy.py to Rust with miette diagnostics and a trycmd golden, demoting the .py to a toggled fallback  (WS-DEBT | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the signature-enforcement projector becomes typed, memory-safe compiled code with proven byte-parity.
**What+How:** `tools/generate-cosign-policy.py` (64 lines) renders `usr/lib/containers/policy.json` from `mios.toml [security.sigstore].policy_mode` and has a `--check` drift mode. Port it to a workspace crate `mios-cosign-policy` using the `toml` crate with a serde typed struct for `[security.sigstore]`, `serde_json` for byte-identical output (indent=2 plus trailing newline -- match exactly), `miette::Result` so a bad `policy_mode` points at the offending `mios.toml` span, and `thiserror` for internal errors. Prove byte-parity against the trycmd golden fixtures for both generate and `--check`. Strangler-fig: point the drift-check (`automation/49-cosign-policy.sh` path) at the Rust binary behind `[migration] use_rust_verbs`, keep the `.py` as fallback, delete once green. This artifact is a boot-integrity control -- parity is mandatory, not best-effort.
**Where:** `tools/native/mios-cosign-policy/ (new crate), tools/generate-cosign-policy.py, usr/lib/containers/policy.json, automation/49-cosign-policy.sh, usr/share/mios/mios.toml [security.sigstore], usr/share/mios/mios.toml [migration]`
**Done When:** the Rust binary emits `policy.json` byte-identical to the `.py` across the trycmd goldens in both modes; a malformed `policy_mode` yields a miette span diagnostic; the drift-check calls Rust with `.py` fallback via the SSOT toggle; `cargo test` and `just drift-gate` green; `.py` deletion staged explicitly.
**Why:** `policy.json` is what makes `sigstoreSigned` enforcement real -- a silent formatting or field divergence there disables image signature checking with no visible symptom.
**Dep:** Build a trycmd golden-master characterization harness

### AGY-1081 -- Port generate-uki-cmdline.py to Rust with byte-parity on the signed kernel cmdline  (WS-DEBT | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the kernel-cmdline projector becomes compiled code without changing a byte of the signed UKI payload.
**What+How:** `tools/generate-uki-cmdline.py` (59 lines) flattens the `usr/lib/bootc/kargs.d/*.toml` drop-ins (sorted, each with a `kargs` list array) into `usr/lib/kernel/cmdline`, and its `--check` mode emits a unified diff on drift. Port to a workspace crate `mios-uki-cmdline`: glob and sort the kargs.d TOMLs, concatenate the `kargs` arrays with a single space and a trailing newline (match the `.py`'s `' '.join(...).strip()+'\n'` exactly), and reproduce the `[drift]` diff output format for `--check`. Use miette for parse errors keyed to the offending fragment. Prove against the trycmd golden, demote the `.py` behind `[migration] use_rust_verbs`, then delete. The cmdline carries `enforcing=1` and LUKS args baked into the signed UKI, so parity is safety-critical.
**Where:** `tools/native/mios-uki-cmdline/ (new crate), tools/generate-uki-cmdline.py, usr/lib/bootc/kargs.d/*.toml, usr/lib/kernel/cmdline, automation/76-uki-render.sh, usr/share/mios/mios.toml [migration]`
**Done When:** the Rust binary reproduces `usr/lib/kernel/cmdline` byte-identically and its `--check` diff format matches; trycmd goldens pass; the UKI render step calls Rust with the `.py` fallback toggle; `just drift-gate` green; `.py` demoted or deleted with explicit staging.
**Why:** The cmdline is embedded in the signed UKI and cannot be edited at boot -- a dropped or reordered karg ships a system that boots permissive or without LUKS args, and only a reinstall fixes it.
**Dep:** Build a trycmd golden-master characterization harness

### AGY-1082 -- Port generate-egress-firewall.py to Rust with typed SSOT input and golden parity  (WS-DEBT | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- a security-boundary projection is generated by typed compiled code that validates its inputs.
**What+How:** `tools/generate-egress-firewall.py` (109 lines) projects the egress-firewall rules from `mios.toml` SSOT and is invoked/validated by `check_egress_firewall` in `automation/97-ssot-lint.sh`. Port to a workspace crate `mios-egress-firewall` consuming the typed `[security]`/`[network]` model, emitting the same rule artifact byte-for-byte, with a `--check` drift mode matching the current output. Validate ports and CIDRs at parse time as real types rather than string-munging, with miette diagnostics on a bad value. Prove against a captured golden, wire behind `[migration] use_rust_verbs` with the `.py` as fallback, then delete.
**Where:** `tools/native/mios-egress-firewall/ (new crate), tools/generate-egress-firewall.py, automation/97-ssot-lint.sh (check_egress_firewall), usr/share/mios/mios.toml [security]/[network], usr/share/mios/mios.toml [migration]`
**Done When:** the Rust binary's output is byte-identical to the `.py` across the goldens; an invalid CIDR or port yields a miette diagnostic instead of a malformed rule; `check_egress_firewall` calls Rust with `.py` fallback; `cargo test` and `just drift-gate` green; `.py` demoted or deleted with explicit staging.
**Why:** A typo'd CIDR in SSOT currently passes straight through string assembly into a live egress rule -- the firewall silently allows or blocks the wrong network with nothing rejecting the bad value.
**Dep:** Build a trycmd golden-master characterization harness

### AGY-1083 -- Port generate-pod-quadlets.py to Rust to kill the quadlet-digest-drift class at the generator root  (WS-DEBT | P2 | XL)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the largest projector becomes reproducible compiled output, making the recurring digest-drift failure structurally impossible.
**What+How:** `tools/generate-pod-quadlets.py` (504 lines) generates the pod/container/network/volume Quadlets with `@sha256` pins from `mios.toml [pods.*]` and is the drift subject of `check_pod_quadlets` (`automation/98-drift-checks.sh` ~lines 881-900). Port to a workspace crate `mios-pod-quadlets` consuming a typed `[pods]` model and emitting each unit deterministically (stable key ordering, exact whitespace) so the artifact is reproducible rather than hand-staged -- directly attacking the recurring "a broad `git add` strips the `@sha256` pins" failure, per SBOM-not-hardcode (digests resolved and recorded at build). Match the current `--check` drift behavior. Because it is the largest projector, port incrementally: emit ONE unit type (`.network`) in Rust and diff against the `.py`, expand type-by-type behind `[migration] use_rust_verbs`, keep the `.py` as fallback until every unit type reaches byte-parity, then delete.
**Where:** `tools/native/mios-pod-quadlets/ (new crate), tools/generate-pod-quadlets.py, automation/98-drift-checks.sh (check_pod_quadlets), usr/share/containers/systemd/*.{pod,container,network,volume}, usr/share/mios/mios.toml [pods], usr/share/mios/mios.toml [migration]`
**Done When:** the Rust generator emits every Quadlet unit type byte-identical to the `.py`, proven per-type against goldens; `check_pod_quadlets` passes with Rust as the generator and `.py` as fallback; regenerating after a broad stage no longer strips pins; `just drift-gate` green; `.py` demoted or deleted with explicit staging.
**Why:** Quadlet-digest drift is a documented recurring outage in this tree -- the pod-quadlets check goes red, someone re-stages pins by hand, and it happens again next commit; generating-not-authoring ends the cycle.
**Dep:** Build a trycmd golden-master characterization harness

### AGY-1084 -- Extend the 800-line module ceiling from agent-pipe to a tree-wide Python drift-gate  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the module-cohesion ceiling becomes a ratcheting fitness function across all 122 tools instead of one subtree.
**What+How:** `automation/98-drift-checks.sh:6014-6033` enforces the <=800-line ceiling ONLY for `agent-pipe`/`mios_pipe` modules. Generalize the check to scan ALL tracked `.py` plus python-shebang tools under `usr/libexec/mios`, `usr/lib/mios` and `tools/`, failing on any file over 800 lines. Seed a grandfathered allowlist in SSOT (`mios.toml [debt.module_ceiling_grandfathered]`) naming today's known offenders -- `mios-daemon` (3371), `mios-dotfiles-render` (1239), `mios-computer-use` (1113), `mios-skills` (991), `MiOS-Mon.py` (911), `mios-mcp-server` (759) -- so the gate lands green while forbidding NEW oversized modules, then shrink the allowlist as the split tasks land.
**Where:** `automation/98-drift-checks.sh (lines ~6014-6033), usr/share/mios/mios.toml [debt.module_ceiling_grandfathered] (new), usr/libexec/mios/*, usr/lib/mios/*, tools/*.py`
**Done When:** the ceiling check covers all tracked Python tree-wide; the 6 current offenders are grandfathered by explicit SSOT entry; adding a new 801-line `.py` fails the gate; `just drift-gate` green; the SSOT allowlist mirrored in both repos (Law 15).
**Why:** Outside agent-pipe nothing stops a new 3000-line tool from landing, so the debt this campaign is paying down regrows in the 122-tool surface faster than it is removed.
**Dep:** none

### AGY-1085 -- Split mios-daemon (3371 lines) into cohesive sub-800-line modules behind a stable entry-point  (WS-DEBT | P2 | XL)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the worst module-cohesion offender is decomposed so typing and coverage can reach the most operationally critical tool.
**What+How:** `usr/libexec/mios/mios-daemon` is 3371 lines -- 4x the ceiling and by far the largest Python tool. Decompose it into a `mios_tools.daemon` package with cohesive submodules along its existing seams (scheduling/gate-eval, launch-claim, postcheck, refusal-handling -- each already has a dedicated test: `test_mios_daemon_launchclaim.py`, `test_mios_daemon_postcheck.py`, `test_mios_daemon_refusal.py`). Keep `mios-daemon` as a thin entry-point that imports and wires the submodules so CLI behavior is unchanged. Strangler-fig: extract one responsibility at a time, keeping the three daemon tests green after each extraction, and remove the grandfather entry only once every submodule is <=800 lines.
**Where:** `usr/libexec/mios/mios-daemon, usr/lib/mios/mios_tools/daemon/ (new package), usr/libexec/mios/test_mios_daemon_launchclaim.py, usr/libexec/mios/test_mios_daemon_postcheck.py, usr/libexec/mios/test_mios_daemon_refusal.py, usr/share/mios/mios.toml [debt.module_ceiling_grandfathered]`
**Done When:** no `mios_tools.daemon` module exceeds 800 lines; the three daemon tests plus find-ranker pass unchanged; `mios-daemon` behaves identically under a characterization test of the start/gate-eval path; the daemon grandfather entry is deleted from SSOT; `just drift-gate` green.
**Why:** A 3371-line untyped daemon is unreviewable and untestable as a unit, mypy and coverage cannot usefully reach it, and it is the tool whose failure most directly stalls the autonomous plane.
**Dep:** Extend the 800-line module ceiling from agent-pipe to a tree-wide Python drift-gate

### AGY-1086 -- Prove the shell and Python mios.toml resolvers equivalent with a shrinking proptest differential oracle  (WS-DEBT | P2 | L)
**Goal:** E-05 Test and CI governance -- gives the resolver collapse the randomized differential oracle that makes a compiled single implementation provably safe.
**What+How:** `tools/check-resolver-twin.py` (173 lines) only spot-checks `mios_toml.py` against `userenv.sh` on the one shipped `mios.toml`. Replace that with a differential property harness (Rust proptest crate `tools/native/mios-resolver-parity/`, or hypothesis inside the existing checker) that GENERATES random-but-valid layered documents — vendor<host<user, empty-string-does-not-override, drop-in fragments — runs BOTH resolvers on each, and asserts the emitted `MIOS_*` key set and values are byte-identical, auto-shrinking any divergence to a minimal failing document. Wire the harness as a drift-gate check next to the existing twin check (`automation/98-drift-checks.sh` ~L3191). The known short-vs-long key mismatch (`MIOS_AI_VLLM_*` emitted vs `MIOS_VLLM_*` consumed) should surface as a concrete failing generated case, not a hand-noticed one.
**Where:** `tools/native/mios-resolver-parity/ (new crate) or tools/check-resolver-twin.py (extend), usr/lib/mios/mios_toml.py, usr/lib/mios/userenv.sh, tools/lib/userenv.sh, automation/98-drift-checks.sh (line ~3191)`
**Done When:** The test generates >=100 random valid `mios.toml` documents and both resolvers produce identical `MIOS_*` projections (or the shrunk case is fixed); the harness runs inside `just drift-gate`; a deliberately divergent key mapping planted in one resolver is caught and reported; the gate is green on the current tree.
**Why:** Today equivalence of the three parallel resolvers rests on one hand-written spot check over a single file, so any collapse-to-one-implementation is a leap of faith — a randomized oracle is the missing safety proof and also mechanizes the GUP lossless-diff invariant.
**Dep:** none

### AGY-1087 -- Replace the 7 bare `except:` clauses in MiOS-Mon.py with typed exception handling  (WS-DEBT | P2 | S)
**Goal:** E-02 Technical-debt retirement (TD-1..TD-8) -- removes the silent-failure class from the runtime monitor and clears the ruff baseline.
**What+How:** Every bare `except:` in the whole libexec tree lives in one file: `usr/libexec/mios/MiOS-Mon.py` at ~L122, 127, 145, 152, 160, 173, 178, each written `except: pass`. Bare except swallows `KeyboardInterrupt`/`SystemExit` and masks genuine faults. Rewrite each to name the narrowest applicable type (e.g. `except (OSError, ValueError)` around the psutil/IO probes) or, where the probe truly may raise anything, `except Exception` with a logged reason — preserving the existing degrade-open behavior in every case. The ruff E722 rule from the ruff-gate task catches regressions forward; this task burns down the existing baseline so that gate can be turned on.
**Where:** `usr/libexec/mios/MiOS-Mon.py (lines ~122,127,145,152,160,173,178)`
**Done When:** `ruff --select E722` over `MiOS-Mon.py` reports zero findings; every handler names its exception type(s); the monitor still renders when a probe is unavailable; `just drift-gate` green.
**Why:** A system monitor that swallows all exceptions hides exactly the errors it exists to surface and cannot be interrupted with Ctrl-C, and its 7 findings are the sole blocker to enabling the E722 rule tree-wide.
**Dep:** none

### AGY-1088 -- Port generate-gate-index.py and generate-pipeline-index.py to a single Rust index generator with parity goldens  (WS-DEBT | P3 | M)
**Goal:** E-01 Compiled native tier -- retires two loose Python projectors into the compiled tier and proves the batch-port pattern.
**What+How:** `tools/generate-gate-index.py` (87 lines) and `tools/generate-pipeline-index.py` (79 lines) are sibling deterministic index projectors (drift-gate enumeration and pipeline enumeration) that share tree-walk logic. Build one workspace crate `tools/native/mios-index-gen/` exposing both as subcommands over that shared walk, emitting the current index artifacts byte-identical and matching each `--check` mode's drift semantics. Capture goldens first, wire the crate behind the `[migration]` toggle in `usr/share/mios/mios.toml` with the two `.py` files as fallback, repoint `automation/98-drift-checks.sh`, then delete both scripts with explicitly staged paths.
**Where:** `tools/native/mios-index-gen/ (new crate), tools/generate-gate-index.py, tools/generate-pipeline-index.py, automation/98-drift-checks.sh, usr/share/mios/mios.toml [migration]`
**Done When:** Both indexes are emitted byte-identical to the `.py` output across the captured goldens; `--check` drift behavior matches; the drift-checks invoke the Rust binary with `.py` fallback; `cargo test` and `just drift-gate` are green; both `.py` files are deleted with explicit staging.
**Why:** Two near-identical small projectors staying in interpreted, untested Python keeps the loose-script count high and leaves the batch-port technique unproven for the larger projector fleet behind it.
**Dep:** Build a trycmd golden-master characterization harness

### AGY-1089 -- Batch-port four small SSOT projectors (cargo-manifests, ipa-enroll-env, cockpit-conf, blade-dropins) into one Rust codegen crate  (WS-DEBT | P3 | L)
**Goal:** E-01 Compiled native tier -- collapses the long tail of tiny generate-*.py scripts into one typed, audited compiled projector.
**What+How:** Four deterministic SSOT->artifact projectors — `tools/generate-cargo-manifests.py` (64), `tools/generate-ipa-enroll-env.py` (72), `tools/generate-cockpit-conf.py` (75), `tools/generate-blade-dropins.py` (58) — each render one config artifact from `mios.toml`. Fold all four into a single crate `tools/native/mios-config-gen/` with four subcommands over one shared typed config model, each with a `--check` mode. Extend the golden harness to capture all four artifacts BEFORE any port; cut over subcommand-by-subcommand behind the `[migration]` toggle keeping each `.py` as an independent fallback; repoint the `automation/*.sh` invocation and drift sites; delete the scripts last.
**Where:** `tools/native/mios-config-gen/ (new crate), tools/generate-cargo-manifests.py, tools/generate-ipa-enroll-env.py, tools/generate-cockpit-conf.py, tools/generate-blade-dropins.py, usr/share/mios/mios.toml [migration], automation/*.sh (their drift/invocation sites)`
**Done When:** All four artifacts are emitted byte-identical to their `.py` sources against goldens; each subcommand's `--check` matches the old drift semantics; invocation and drift sites call the Rust crate with per-tool `.py` fallback; `cargo test` and `just drift-gate` green; all four `.py` files deleted with explicit staging.
**Why:** The loose-script count lives in this long tail, and porting each tiny script as its own crate would pay four crates' overhead for four small jobs — batching is the only economical way to retire them while keeping each cutover independently gated.
**Dep:** Build a trycmd golden-master characterization harness

### AGY-1090 -- Retire the hardcoded-path one-shot scratch scripts tools/fix-agy.py and tools/apply-unification.py  (WS-DEBT | P3 | S)
**Goal:** E-02 Technical-debt retirement -- removes non-portable, re-runnable tree mutators from the shipped tools surface.
**What+How:** `tools/fix-agy.py` (137 lines) and `tools/apply-unification.py` (83 lines) both open with `root = r"c:\MiOS"` (verified) — one-shot migration scripts that rewrite other tracked files and whose edits are already committed. Confirm via `git log` on the files they targeted that those edits landed, `grep` that no automation phase or gate references either script, then delete both. If any transformation is genuinely still needed, it must be re-expressed as a drift-gate check in `automation/98-drift-checks.sh`, not a hardcoded-path scratch mutator. Stage the two deletions by explicit path (never `git add -A` — AGY shares the tree).
**Where:** `tools/fix-agy.py, tools/apply-unification.py`
**Done When:** Both files no longer exist; git history shows their edits already in-tree; `grep -r 'fix-agy\|apply-unification'` over the repo is empty; `just drift-gate` green; the deletions staged by explicit path.
**Why:** Two dead scripts hardcoding `c:\MiOS` are unrunnable off this one machine and actively dangerous if re-executed, since they rewrite tracked files in place with no guard.
**Dep:** none

### AGY-1091 -- Split mios-dotfiles-render (1239) and mios-computer-use (1113) below the module ceiling  (WS-DEBT | P3 | L)
**Goal:** E-02 Technical-debt retirement -- shrinks the module-ceiling grandfather list toward zero so the tools become typeable and testable.
**What+How:** Two libexec tools exceed the 800-line ceiling: `usr/libexec/mios/mios-dotfiles-render` (1239) and `usr/libexec/mios/mios-computer-use` (1113). Decompose each into a package under `usr/lib/mios/mios_tools/` along its cohesive seam — `dotfiles_render/` splitting per-surface renderers from the apply-to-live-HOME core, `computer_use/` splitting screen/input drivers from orchestration — leaving a thin entry-point at the original path that preserves CLI behavior exactly. Capture a characterization test of stdout and exit code for a representative invocation of each BEFORE touching them, then extract one unit at a time. Drop each tool from `[debt.module_ceiling_grandfathered]` in `usr/share/mios/mios.toml` only once every one of its submodules is <=800 lines.
**Where:** `usr/libexec/mios/mios-dotfiles-render, usr/libexec/mios/mios-computer-use, usr/lib/mios/mios_tools/dotfiles_render/ (new), usr/lib/mios/mios_tools/computer_use/ (new), usr/share/mios/mios.toml [debt.module_ceiling_grandfathered]`
**Done When:** No module under `mios_tools.dotfiles_render` or `mios_tools.computer_use` exceeds 800 lines; the characterization tests for both entry-points pass unchanged; both names are gone from the grandfather list; `just drift-gate` green.
**Why:** After mios-daemon these are the two largest ceiling offenders, and while they stay monolithic the ceiling gate is permanently softened by a grandfather list that never shrinks.
**Dep:** Extend the 800-line module ceiling from agent-pipe to a tree-wide Python drift-gate

### AGY-1092 -- Route every tool through the shared resolver, deleting the ~13 re-rolled `try tomllib/except tomli` + hardcoded-layer copies  (WS-DEBT | P3 | M)
**Goal:** E-02 Technical-debt retirement -- finishes the consolidation `mios_toml.py` was created for, so one resolver semantics holds everywhere.
**What+How:** `mios_toml.py`'s own docstring says it exists to collapse ~13 independently re-rolled `try: import tomllib / except: import tomli` + `deep_merge` + hardcoded-layer-path blocks — yet those blocks are still inline in consumers (verified in `generate-cosign-policy.py`, `generate-uki-cmdline.py`, `seed-db-config.py`, and `compile-templates.py`'s fallback). Sweep `usr/libexec/mios` and `tools/` for the local tomllib/tomli try-import plus ad-hoc `mios.toml` path resolution and replace each with `from mios_tools import config` (or the packaged `mios_toml`). Tools already slated for a Rust port need no change. Then add a drift-gate check forbidding a local `import tomli` fallback anywhere outside `mios_tools.config`.
**Where:** `usr/libexec/mios/seed-db-config.py, tools/generate-cosign-policy.py, tools/generate-uki-cmdline.py, tools/compile-templates.py, usr/lib/mios/mios_toml.py, automation/98-drift-checks.sh`
**Done When:** `grep -r 'import tomli' usr/libexec/mios tools` matches only `mios_tools/config`; every consumer resolves `mios.toml` solely through the shared resolver; a deliberately reintroduced local tomli fallback fails the new drift-gate check; `just drift-gate` green.
**Why:** A dozen divergent copies of the layer-resolution boilerplate means a fix to vendor<host<user semantics lands in one place and silently misses the other twelve — the exact drift the shared resolver was supposed to end.
**Dep:** Fold mios_toml.py, mios_env.py, and mios_db_config.py into a single typed mios_tools.config surface

### AGY-1093 -- Give the migrated Rust projectors a miette/thiserror typed-diagnostic error surface pointing at mios.toml spans  (WS-DEBT | P3 | M)
**Goal:** E-01 Compiled native tier -- makes every compiled verb report SSOT errors identically, as compiler-grade diagnostics.
**What+How:** As projectors move to Rust (cosign-policy, uki-cmdline, egress-firewall, pod-quadlets, index-gen, config-gen), standardize their error handling: library-internal errors become `thiserror` enums the drift-gates can match on programmatically, and each `main` returns `miette::Result` so a bad value in `mios.toml` prints with source span, label and help text. Add a shared crate `tools/native/mios-diagnostics/` providing the `SourceSpan` helper that maps a failing `[section].key` back to its line and BYTE range in the merged TOML (byte offsets, not char counts, so UTF-8 highlights correctly), and have every ported tool depend on it. Lock the output shape with a trycmd fixture that feeds a deliberately malformed `mios.toml`.
**Where:** `tools/native/mios-diagnostics/ (new crate), tools/native/mios-cosign-policy/, tools/native/mios-uki-cmdline/, tools/native/mios-egress-firewall/, tools/native/mios-pod-quadlets/, usr/share/mios/mios.toml`
**Done When:** Each ported projector depends on `mios-diagnostics` and returns `miette::Result`; a malformed `[security.sigstore]` or `[pods]` value produces a spanned diagnostic naming the offending `mios.toml` line; a trycmd fixture pins the diagnostic format; `cargo test` and `just drift-gate` green.
**Why:** Without a shared span-mapper each ported tool invents its own error text, and an operator who mistypes a port value gets an opaque traceback instead of "this value is invalid, here, try X" — losing the main UX dividend of compiling the tier.
**Dep:** Port generate-cosign-policy.py to a Rust binary with miette diagnostics

### AGY-1094 -- Eliminate the shell=True recipe-template command-injection in mios-os-recipe  (WS-DEBT | P1 | M)
**Goal:** E-02 Technical-debt retirement -- closes the last argument-injection execution sink in the libexec Python surface.
**What+How:** `usr/libexec/mios/mios-os-recipe` builds `cmd` from `_expand(template, params)` — params sourced from spec/agent input — and runs it through `subprocess.run(cmd, shell=True, ...)` at ~L194, a template-injection sink the separate os.system task (mios-models / mios-v2v-import) does not cover. Rewrite to a `shlex`-tokenized argv list with no shell, splitting the WSL/binfmt `.exe` exec path out explicitly (the `appendWindowsPath=false` rationale in the L186-192 comment is satisfied by passing the absolute `/mnt/c/...` path as argv[0]), and validate param interpolation against an allowlist of declared placeholder keys. Keep the existing timeout and degrade-open `TimeoutExpired`/`Exception` fallbacks intact. Add a characterization test asserting a param containing `; rm -rf` arrives as one literal argv element.
**Where:** `usr/libexec/mios/mios-os-recipe (L180-205)`
**Done When:** No `shell=True` remains in `mios-os-recipe`; the injection characterization test proves metacharacters in params are inert; ruff/bandit S602/S604 report clean on the file; `just drift-gate` green.
**Why:** This is the only surviving `shell=True` in the entire libexec Python surface and it interpolates agent-controllable recipe params — a live remote-code-execution path reachable from the agent dispatch chain today.
**Dep:** none

### AGY-1095 -- Replace predictable /tmp temp-file paths with tempfile mkstemp/mkdtemp and gate literal-/tmp  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement -- removes a local-escalation hazard and adds the gate that keeps it removed.
**What+How:** Three Python tools write fixed, world-predictable temp paths: `mios-finetune-dataset` (`/tmp/mios-finetune-smoke.jsonl`, L413), `oscap-scan.py` (`/tmp/tailoring.xml`, L92) and `MiOS-Mon.py` (`/tmp/mios-cat-install.log`, L553) — symlink/TOCTOU-prone, and the oscap scan runs privileged. Replace each with `tempfile.mkstemp` / `NamedTemporaryFile(dir=...)` or an `XDG_RUNTIME_DIR`-anchored path, cleaning up on exit; oscap specifically should write inside a 0700 `mkdtemp`. Then enable flake8-bandit/ruff `S108` (hardcoded-tmp) in the Python drift-gate so regressions are blocked, allowlisting only the `test_*.py` fixtures that legitimately embed `/tmp` string literals.
**Where:** `usr/libexec/mios/mios-finetune-dataset (L413), usr/libexec/mios/oscap-scan.py (L92), usr/libexec/mios/MiOS-Mon.py (L553)`
**Done When:** `grep -r '/tmp/' usr/libexec/mios --include='*.py'` excluding tests returns empty; the ruff S108 gate is active and green; oscap-scan writes under a 0700 `mkdtemp`; `just drift-gate` green.
**Why:** A privileged compliance scanner writing to a guessable `/tmp` filename is a symlink-attack primitive an unprivileged local user can exploit, and nothing else in the campaign touches temp-file safety.
**Dep:** none

### AGY-1096 -- Normalize Python shebangs and retire the two hardcoded venv-path interpreters  (WS-DEBT | P2 | S)
**Goal:** E-02 Technical-debt retirement -- unblocks console-script packaging by removing absolute-venv coupling.
**What+How:** 120 of the 122 libexec tools use `#!/usr/bin/env python3`; `mios-cdp-fetch` and `mios-smart-resize` instead pin `#!/usr/bin/env /usr/lib/mios/agents/.venv/bin/python3` (with a raw `#!/usr/lib/mios/agents/.venv/bin/python3` variant), which breaks the moment tools ship as console-scripts and defeats interpreter selection entirely. Move both onto the packaged entry-point, or onto plain `env python3` with the agent dependencies declared as a packaging extra. Add a one-line shebang-uniformity check to the Python drift-gate that fails on any libexec Python shebang other than the sanctioned `env python3` form.
**Where:** `usr/libexec/mios/mios-cdp-fetch (L1), usr/libexec/mios/mios-smart-resize (L1); new shebang check in automation/98-drift-checks.sh`
**Done When:** Both tools run under the standard interpreter or packaged entry-point; the shebang-uniformity gate is active; `grep '.venv/bin/python' usr/libexec/mios` is empty; `just drift-gate` green.
**Why:** Two hardcoded venv shebangs will silently break the packaging migration mid-flight, and they are enumerated nowhere else in the task set.
**Dep:** none

### AGY-1097 -- Pin requires-python and add a Python 3.14 forward-compat drift-gate  (WS-DEBT | P2 | M)
**Goal:** E-05 Test and CI governance -- ends the unmanaged 3.13/3.14 interpreter straddle by making the version a gated fact.
**What+How:** No `pyproject.toml` / `requires-python` exists anywhere in the repo, yet the committed tree carries bytecode from BOTH cpython-313 and cpython-314 (mixed `__pycache__` tags) while MEMORY records 3.14 as the real CI path. Add `requires-python = ">=3.14"` to the new `mios_tools` pyproject so the floor is declared, then add a compat check to `automation/98-drift-checks.sh` that import- and exec-smokes every libexec tool under `python3.14 -W error::DeprecationWarning`, surfacing removed-stdlib and deprecation breakage before ship. Pin the gate to the exact interpreter the bake uses so the 313/314 ambiguity cannot recur.
**Where:** `new pyproject.toml (requires-python); automation/98-drift-checks.sh (new compat check); resolves mixed usr/libexec/mios/__pycache__ tags`
**Done When:** `requires-python` is pinned to the bake's interpreter; the `-W error` import-smoke of all 122 tools passes on 3.14; CI produces no cpython-313 artifacts; `just drift-gate` green.
**Why:** With zero declared version floor a stdlib removal in the next interpreter breaks the bake with no warning, and the mypy task pins types while nothing pins the interpreter those types are checked against.
**Dep:** Package the 122 libexec Python tools as an installable mios_tools module

### AGY-1098 -- Standardize CLI argument parsing across the 27 argparse-less tools with a --help/--version contract test  (WS-DEBT | P2 | L)
**Goal:** E-02 Technical-debt retirement -- gives a third of the runtime tools an actual, testable CLI contract.
**What+How:** 27 libexec Python tools hand-roll `sys.argv` with no argparse/click/typer — `mios-dotfiles-render`, `mios-os-control`, `mios-pg-query`, `mios-registry`, `mios-sync-toml`, `mios-template-engine`, `mios-theme-render`, `mios-toml-get`, `mios-account-sync`, `mios-cephfs-provision` and the rest — so flags are inconsistent, there is no `--help` or `--version`, and positional parsing is brittle. Add a shared argparse-based CLI scaffold in `mios_tools` (uniform `--help`, `--version` read from package metadata, `--json` for the tools that already emit JSON) and convert all 27 onto it. Add a contract smoke-test asserting every packaged entry-point exits 0 with non-empty usage on `--help`, feeding the same trycmd/golden harness the projectors use.
**Where:** `the 27 argparse-less tools under usr/libexec/mios/ (mios-dotfiles-render, mios-os-control, mios-pg-query, mios-registry, mios-sync-toml, mios-template-engine, mios-theme-render, mios-toml-get, mios-account-sync, mios-cephfs-provision, ...)`
**Done When:** All 27 tools use the shared scaffold; every entry-point passes the `--help`/`--version` contract test; goldens capture the usage strings; `just drift-gate` green.
**Why:** The trycmd work only characterizes `--check` projectors, so these 27 hand-rolled parsers have no baseline at all — they cannot be safely packaged, ported, or refactored, and each behaves differently for the operator.
**Dep:** Package the 122 libexec Python tools as an installable mios_tools module

### AGY-1099 -- Build a golden characterization harness for the non-projector runtime tools before the refactors touch them  (WS-DEBT | P1 | L)
**Goal:** E-01 Compiled native tier -- supplies the byte-parity safety net every strangler-fig demotion is supposed to prove itself against.
**What+How:** The trycmd task captures only SSOT `--check` projectors, but the packaging, resolver-fold, module-split and dispatch tasks all rewrite RUNTIME tools that have no behavioral baseline: `mios-daemon` (3371 lines), `mios-dotfiles-render` (1239), `mios-computer-use` (1113) and the ~10 CLIs covered by existing `test_*.py`. Build a fixture-driven harness under `tests/characterization/` that records stdout, stderr and exit code — with subprocess and DB calls mocked — for each tool's representative command paths, and COMMIT those goldens before any demotion begins. Downstream split/fold/package tasks diff against these fixtures to prove parity.
**Where:** `new tests/characterization/ harness covering usr/libexec/mios/{mios-daemon,mios-dotfiles-render,mios-computer-use} + the 10 existing test_*.py subjects`
**Done When:** Golden fixtures exist for each targeted runtime tool; the harness runs inside the pytest gate; the module-split and resolver-fold tasks can diff against these goldens; `just drift-gate` green.
**Why:** Refactoring a 3k-line runtime tool with only projector goldens for cover means a behavior regression ships undetected; this harness is the ordering prerequisite the split/fold/package tasks are otherwise blocked on.
**Dep:** none

### AGY-1100 -- Introduce a stable mios-generate dispatch so the drift-gate stops hardcoding python3 tools/generate-*.py  (WS-DEBT | P1 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- decouples the gate from projector implementation language so ports become atomic.
**What+How:** `automation/98-drift-checks.sh` invokes each projector as a literal `MIOS_DRIFT_ROOT=$ROOT python3 $ROOT/tools/generate-X.py` (see `_gen_diag` L109-116 and the per-check calls at L149/881/912/935/3850), so every Rust port must hand-edit the gate and a half-ported gate can call the wrong implementation. Add a thin packaged console-script dispatcher `usr/libexec/mios/mios-generate <name> [--check]` that resolves name -> implementation (Python today, the Rust binary after each port) behind one stable contract, and repoint both the gate call sites and the build phases that regenerate projectors onto it.
**Where:** `new usr/libexec/mios/mios-generate dispatcher; automation/98-drift-checks.sh (_gen_diag L109-116 + call sites); build phases that regenerate projectors`
**Done When:** The gate and build invoke `mios-generate <name>` rather than raw `python3 generate-*.py`; swapping a projector's backing implementation requires no gate edit; every projector `--check` gate passes through the dispatcher; `just drift-gate` green.
**Why:** Without this indirection each of the ~8 Rust-port tasks must patch `98-drift-checks.sh` itself, and a mid-port language straddle can leave the gate validating the old implementation while the build ships the new one.
**Dep:** none

### AGY-1101 -- Convert the drift-gate's degrade-open python3/tool skips into hard failures for security and law checks  (WS-DEBT | P2 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- closes the fail-open hole that lets security and law checks pass by being un-runnable.
**What+How:** `automation/98-drift-checks.sh` fails OPEN when `python3` or a tool is missing: `_require_python3` (L193-198) only records a violation under `MIOS_DRIFT_REQUIRE_TOOLS`, and several security/law checks print `WARNING ... skipping` and then PASS (node+manifest L267-268, AI-hint coverage L398-399, names-registry L2080). Once `mios_tools` is an installed package, make the Python toolchain a hard build and gate dependency: default `MIOS_DRIFT_REQUIRE_TOOLS=1` in CI, and reclassify the security/law-tier skips (SSOT projection, RBAC, egress, cosign) from WARNING-pass to violation when their toolchain is absent, leaving benign optional checks degrade-open.
**Where:** `automation/98-drift-checks.sh (_require_python3 L193-198; WARNING-skip sites L267-268, L398-399, L2080)`
**Done When:** CI runs with `MIOS_DRIFT_REQUIRE_TOOLS=1`; removing the interpreter or `mios_tools` turns the security/law checks RED rather than skipped; benign optional checks still degrade-open; `just drift-gate` green.
**Why:** A security gate that silently passes when its runtime is missing enforces nothing — the whole Python-hardening campaign is void if the gate policing it can be no-op'd by an absent interpreter in one CI image change.
**Dep:** Package the 122 libexec Python tools as an installable mios_tools module

### AGY-1102 -- Port generate-ai-manifest.py (verb-catalog projector, gate check 8) to Rust with a byte-parity golden  (WS-DEBT | P2 | L)
**Goal:** E-01 Compiled native tier -- closes the coverage hole where the highest-traffic SSOT projector has no port assignment.
**What+How:** `tools/generate-ai-manifest.py` is the `[verbs.*]` -> ai/v1 verb-catalog projector wired as drift-gate check 8 (`automation/98-drift-checks.sh` L39), yet it appears in no port task — the roster covers cosign, uki, egress, pod-quadlets, gate-index, pipeline-index, cargo, ipa, cockpit, blade and names, but not this one. Port it into the consolidated Rust codegen crate, resolving `[verbs.*]` through the typed SSOT input, expose it behind the `mios-generate` dispatcher, and demote the `.py` to a toggled fallback. Commit a golden of the current manifest and prove byte-identical output before deleting the script.
**Where:** `tools/generate-ai-manifest.py -> the consolidated Rust codegen crate; automation/98-drift-checks.sh check 8 (L39)`
**Done When:** The Rust binary emits a byte-identical ai/v1 verb manifest against the golden; check 8's `--check` is green through the dispatcher; the `.py` is demoted and then deleted; `cargo test` and `just drift-gate` green.
**Why:** The verb catalog is core AI-plane contract surface and the most-exercised projector in the gate, so leaving it as the one un-hardened interpreted script undermines the parity story the other ports establish.
**Dep:** Introduce a stable mios-generate dispatch so the drift-gate stops hardcoding python3 tools/generate-*.py

### AGY-1103 -- Port/consolidate the materialize-*.py DB round-trip build-context projectors  (WS-DEBT | P3 | L)
**Goal:** E-01 Compiled native tier -- brings the untargeted materialize cluster under typed models and the shared dispatcher.
**What+How:** `materialize-config-toml.py` (gate check 31, L2335/L2480) and `materialize-build-ctx.py` (gate check 32, L2782/L2923), plus `materialize-build-catalog.py` and `materialize-user-config.py`, are SSOT/DB -> build-context projectors with round-trip drift semantics that no port task covers. Inventory each one's `[section]` inputs and DB mappings, give them typed models, and fold them into the codegen crate — or, minimally, route them through the shared typed resolver and the `mios-generate` dispatcher. The `seed-db-config.py` <-> materialize round-trip invariants that checks 31 and 32 assert must be preserved exactly, with committed goldens for the emitted TOML and build ctx.
**Where:** `usr/libexec/mios/materialize-{config-toml,build-ctx,build-catalog,user-config}.py; automation/98-drift-checks.sh checks 31 (L2335) + 32 (L2782)`
**Done When:** The materialize family shares typed models and the shared resolver; the DB->TOML and DB->/ctx round-trip checks 31/32 are green; goldens are committed; `just drift-gate` green.
**Why:** Four drift-gated projectors with subtle round-trip semantics sit outside every port and hardening task, so a change to the DB schema or SSOT shape can break the build context with no typed model to catch it.
**Dep:** Introduce pydantic v2 typed models for the mios.toml sections the tools consume

### AGY-1104 -- Inventory and relocate the remaining dev-only one-shot scratch scripts in tools/  (WS-DEBT | P3 | M)
**Goal:** E-02 Technical-debt retirement -- pulls author-workflow tooling out of the shipped image and inside the packaging boundary.
**What+How:** The retirement task names only `fix-agy.py` and `apply-unification.py`, but `tools/` still ships `ascii-sweep.py`, `standardize-docs.py`, `sync-wiki.py`, `roadmap-index.py`, `update-agy-tasks.py`, `journal-sync.py` and `refresh-env.py` — author-workflow scripts that ride into the image and dodge the `mios_tools` packaging boundary. Classify each as dev-only or load-bearing with evidence, cross-checking call sites in `automation/*.sh` and `automation/98-drift-checks.sh`; move the dev-only ones under a non-shipped `tools/dev/` or into a `dev` optional-dependency group; delete anything already dead. Stage explicit paths only.
**Where:** `tools/{ascii-sweep,standardize-docs,sync-wiki,roadmap-index,update-agy-tasks,journal-sync,refresh-env}.py`
**Done When:** Every script carries a recorded classification with call-site evidence; the dev-only ones are excluded from the shipped image and package; no gate or build phase references a moved path; `just drift-gate` green.
**Why:** Retiring 2 of ~9 scratch scripts leaves seven author-workflow tools inside the production image, growing its surface and sitting outside the packaging and typing boundary everything else is being pulled into.
**Dep:** none

### AGY-1105 -- Promote mios_toml.py to the canonical resolver that EMITS the shell/PS twins, retiring the hand-synced copies  (WS-DEBT | P2 | L)
**Goal:** E-10 One canonical name: the unified names/keys registry -- makes one resolver the generator-of-record so the other two stop being maintained by hand.
**What+How:** Three parallel resolvers are kept in lockstep manually: `usr/lib/mios/mios_toml.py`, the shell twins (`tools/lib/userenv.sh` authoritative plus a byte-copy at `usr/lib/mios/userenv.sh` policed by `check_userenv_parity`, check 27, L1757), and `automation/lib/globals.ps1`. Once the proptest differential parity proves agreement, make the packaged `mios_toml.py` EMIT `tools/lib/userenv.sh` and `automation/lib/globals.ps1`, converting check 27 from a hand-sync twin comparison into a regenerate-and-diff projector check, and regenerating the committed byte-copy rather than copying it. Mirror the shared surface in BOTH `mios.git` and `mios-bootstrap.git` per Law 15.
**Where:** `usr/lib/mios/mios_toml.py -> emits tools/lib/userenv.sh + automation/lib/globals.ps1; automation/98-drift-checks.sh check_userenv_parity (L1757, check 27); mirror in mios.git + mios-bootstrap.git`
**Done When:** `userenv.sh` and `globals.ps1` are generated from `mios_toml.py`; check 27 is a regenerate-and-diff check; no hand-maintained resolver copy remains in the tree; both repos are updated; proptest parity and `just drift-gate` green.
**Why:** The proptest only proves the three resolvers currently agree — it does nothing to stop the next key from being added to one and forgotten in the other two, which is the triplicate-maintenance cost the operator flagged.
**Dep:** Add a proptest differential parity test proving the shell resolver (userenv.sh) and Python resolver (mios_toml.py) agree on random mios.toml

### AGY-1106 -- Add pyproject.toml + strict mypy config + an agent-pipe mypy drift-gate  (WS-DEBT | P1 | M)
**Goal:** E-05 Test and CI governance -- installs the static-typing foundation every later agent-pipe typing task ratchets against.
**What+How:** There is NO `pyproject.toml` or `mypy.ini` anywhere in the repo (verified by a repo-wide find), so the 123-file agent-pipe has zero static type enforcement. Create `usr/lib/mios/agent-pipe/pyproject.toml` with a strict `[tool.mypy]` block (`disallow_untyped_defs`, `no_implicit_optional`, `warn_return_any`, `warn_unused_ignores`) scoped to `mios_pipe/**`, plus a `[tool.ruff]` block. Add `check_agentpipe_mypy` to `automation/98-drift-checks.sh` near the existing check-11 sibling-test gate (~L691), ordered AFTER the negatives per the documented CI ordering, seeded with a per-module allowlist starting at the already-typed leaves (`mios_pipe/access/arbiter.py`, `mios_pipe/memory/pg.py`) so it is green today and ratchets as hints land. Degrade-open when mypy is absent; mirror the gate and `[laws]` note in BOTH repos per Law 15.
**Where:** `usr/lib/mios/agent-pipe/pyproject.toml (new), automation/98-drift-checks.sh, usr/share/mios/mios.toml`
**Done When:** `mypy mios_pipe` runs clean over the allowlisted modules; `just drift-gate` shows `check_agentpipe_mypy` green; the pyproject and drift-check are staged as explicit paths (never `git add -A`); the `[laws]`/gate note is mirrored in both repos.
**Why:** Every typing and pydantic task below is unenforceable without a config file to check against, so type hints added now would silently rot instead of being gated.
**Dep:** none

### AGY-1107 -- Replace the run_tests.py glob-runner with pytest + a coverage-floor gate  (WS-DEBT | P1 | M)
**Goal:** E-05 Test and CI governance -- gives the agent-pipe a real harness with coverage, the prerequisite for parity testing every migration slice.
**What+How:** `usr/lib/mios/agent-pipe/run_tests.py` is a naive 20-line `subprocess.run(['python3', t])` glob loop — no parallelism, no coverage, no JUnit — and the 189 `test_mios_*.py` files are bespoke assert-scripts with a custom `check()` (only 44 import pytest). Add `[tool.pytest.ini_options]` and `[tool.coverage]` to the pyproject plus a thin `conftest.py` reproducing `run_tests.py`'s `MIOS_*` env scrub, then replace `run_tests.py`'s body with `pytest -q --cov=mios_pipe --cov-report=xml -n auto` while KEEPING the file as a name-stable shim so existing callers still work (strangler: assert-scripts and pytest coexist). Add `check_agentpipe_coverage` to the drift-gate enforcing `--cov-fail-under` on `mios_pipe`, degrade-open when pytest is absent.
**Where:** `usr/lib/mios/agent-pipe/run_tests.py, usr/lib/mios/agent-pipe/pyproject.toml, usr/lib/mios/agent-pipe/conftest.py (new), automation/98-drift-checks.sh`
**Done When:** `pytest -q` collects and passes the existing suite; the coverage XML is produced; the coverage-floor gate is green and staged by explicit path.
**Why:** Without coverage or a real runner, the god-module decomposition has no way to show a slice was actually exercised, and the glob loop reports nothing beyond exit status per file.
**Dep:** Add pyproject.toml + strict mypy config + an agent-pipe mypy drift-gate

### AGY-1108 -- Introduce pydantic wire-schema models for the FastAPI request/response bodies  (WS-DEBT | P1 | L)
**Goal:** E-02 Technical-debt retirement -- puts typed wire schemas under the agent-pipe HTTP boundary ahead of the server.py decomposition.
**What+How:** `server.py` handles bodies as raw dicts (`body = await request.json()` at L1743 and L5309, across 451 route-definition sites) with NO pydantic anywhere (verified: zero `BaseModel` imports). Create a `mios_pipe/schemas/` package with pydantic v2 models for the core wire contracts — chat-completions request/response, embeddings, the `/v1/dispatch` envelope, `/v1/agents/directory` — mirroring the OpenAI shapes already proxied. Convert the two `await request.json()` handlers (`lora_load` L1735, `v1_agents_directory` L5309) to typed params as the pilot slice and leave the other routes raw behind the same paths so typed and untyped coexist. Validation failures return 422 with field locations; set `model_config = ConfigDict(extra='allow')` so unmodeled fields still pass through (degrade-open).
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/schemas/ (new), usr/lib/mios/agent-pipe/server.py`
**Done When:** The two pilot handlers accept typed models; a malformed body returns 422 naming the field; `test_server_import.py` and the surface-parity gate stay green; the schemas package has a sibling test.
**Why:** Raw-dict bodies at the HTTP boundary let a wrong-typed value travel deep into dispatch before failing (or worse, succeed with the wrong meaning), and there is no shared substrate for the per-cluster pydantic tasks until these models exist.
**Dep:** Add pyproject.toml + strict mypy config + an agent-pipe mypy drift-gate

### AGY-1109 -- Add a typed exception hierarchy + operator-facing config diagnostics  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement -- replaces silently-swallowed SSOT failures with typed, spanned, gate-matchable diagnostics.
**What+How:** Config and resolver failures in `mios_pipe/kernel/config.py` currently do `log.warning(...); out = {}` and swallow (the `_toml_section` except block ~L40), so a malformed `mios.toml` degrades silently to defaults with no pointer to the offending line. Define `mios_pipe/errors.py` as a typed exception tree — `MiosConfigError`, `MiosVerbError`, `MiosDispatchError` (the thiserror analogue) — plus a diagnostic renderer that reports the offending section/key with a BYTE-offset span (not char count, for correct UTF-8 highlighting) and actionable help text (the miette analogue). Wire it into `_toml_section` and `_cfg_num` so a bad `[colors]` or port value raises a typed spanned diagnostic drift-gates can match programmatically, while genuinely optional sections still degrade-open.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/errors.py (new), usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py`
**Done When:** A malformed `[colors]` value raises a typed `MiosConfigError` naming the section/key with a span; absence of an optional section still returns `{}` degrade-open; a sibling test covers both paths.
**Why:** An operator who mistypes one SSOT value today gets a silently-defaulted system and a buried warning line — the worst failure mode in a config-projected OS, since the wrong behavior looks like correct behavior.
**Dep:** none

### AGY-1110 -- Kill the eval-on-agent-args launcher path in mios-launch/mios-window (TD-1)  (WS-DEBT | P1 | M)
**Goal:** E-02 Technical-debt retirement -- eliminates the named TD-1 eval-injection surface from the verb library and gates its return.
**What+How:** `usr/libexec/mios/mios-launch` L507 still runs `eval exec "$_hit_cmd" '"$@"'` on an agent-resolved launch string (L721 even carries a stale `TD-1: Replace unsafe eval with shlex split` TODO), and `usr/libexec/mios/mios-window` L386 evals python-heredoc output — both argument-level injection boundaries reachable from the agent-pipe dispatch chokepoint `mios_dispatch.dispatch_mios_verb`. Replace each eval with a `shlex.split`-parsed argv array exec or an explicit case-based dispatch table keyed by the resolved verb, rejecting any token the SSOT `[verbs.*]` catalog did not declare. Add a `check_no_eval_on_args` drift-gate scanning `usr/libexec/mios/*` for `eval ` applied to agent-derived variables (allowlisting reviewed-safe sites), register it as candidate Law 14, and mirror the `[laws]` entry in BOTH repos per Law 15.
**Where:** `usr/libexec/mios/mios-launch, usr/libexec/mios/mios-window, automation/98-drift-checks.sh, usr/share/mios/mios.toml`
**Done When:** Neither file evals an agent-derived string; launcher behavior is verified against a golden of prior launch commands; the no-eval gate is green and RED against a deliberately planted eval; the `[laws]` entry is mirrored in both repos.
**Why:** `eval` on agent-controlled arguments is direct remote-code-execution surface (cf. the Gemini-CLI eval-injection CVE) reached straight from the dispatch chokepoint, and the system's own rule is that agent input is never authorization.
**Dep:** none

### AGY-1111 -- Land the 800-line agent-pipe module ceiling as a seeded, ratcheting drift-gate (TD-5)  (WS-DEBT | P1 | S)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- turns the TD-5 module-size limit from a stated intent into a build-failing invariant that every later split task decrements.
**What+How:** Add `check_agentpipe_line_ceiling` to `automation/98-drift-checks.sh`: fail any non-test agent-pipe `.py` over 800 lines, seeded with an explicit `_OVER_CEILING_ALLOW` register holding exactly today's twelve measured offenders (server.py 7812, mios_dispatch.py 1475, routing/chat.py 2140, routing/portal.py 1826, federation/a2a.py 1720, dag_exec.py 1595, native_loop.py 1527, swarm.py 1375, refine.py 1349, agent_call.py 1328, web_research.py 1302, memory/knowledge.py 1092) so the gate is green on day one and only ever ratchets down. Mirror the ceiling constant into the `mios.toml [laws]` numbering SSOT in BOTH repos per Law 15.
**Where:** `automation/98-drift-checks.sh, usr/share/mios/mios.toml`
**Done When:** the gate passes today with the seeded allowlist, FAILS on a newly-oversized file, each split PR must delete its own allowlist row, and `[laws]` carries the ceiling in both repos.
**Why:** Without a mechanical ceiling the twelve god-modules keep growing and every "split it later" task has no objective finish line.
**Dep:** none

### AGY-1112 -- Continue the server.py R-wave strangler: extract ceph-health and LoRA blocks  (WS-DEBT | P2 | XL)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- dissolves the 7812-line monolith the whole modular-monolith refactor exists to remove.
**What+How:** Keep the established R-wave pattern under the WS R0 surface-parity gate (`98-drift-checks.sh` lines ~811-868, diffing `usr/share/mios/ai/v1/surface.generated.json`). Move the next two cohesive blocks: the ceph/cluster-health blocking probes (server.py ~1655-1725) to `mios_pipe/kernel/clusterhealth.py`, and the LoRA endpoints (~1735) to a new `mios_pipe/routing/lora.py`. Re-import every moved name under its ORIGINAL alias so `python3 mios_surface.py server.py --package` diffs to zero, regenerate `surface.generated.json` after each move and stage it EXPLICITLY (never `git add -A`).
**Where:** `usr/lib/mios/agent-pipe/server.py, usr/lib/mios/agent-pipe/mios_pipe/kernel/clusterhealth.py, usr/lib/mios/agent-pipe/mios_pipe/routing/lora.py (new), usr/share/mios/ai/v1/surface.generated.json`
**Done When:** the moved blocks live in `mios_pipe` with sibling tests, the surface-parity gate diff is zero, the regenerated `surface.generated.json` is staged explicitly, server.py's `wc -l` drops, and the matching ceiling-allowlist entries are removed.
**Why:** server.py is the worst ceiling offender; every hour it stays a 7812-line monolith, any change to it risks the whole /v1 surface.
**Dep:** Enforce the <=800-line module ceiling as a ratcheting drift-gate (TD-5)

### AGY-1113 -- Split mios_dispatch.py behind a golden command-builder snapshot  (WS-DEBT | P2 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- decomposes the security-critical verb->bash chokepoint without altering a single emitted command.
**What+How:** `mios_dispatch.py` (1475 lines: `_template_to_cmd`, `_build_dispatch_cmd`, `dispatch_mios_verb`, `_dispatch_bounded`, `_dispatch_mios_verb_inner`, `configure`) is the seam every verb passes through. Extract the PURE per-verb command builder — `_build_dispatch_cmd`'s launch_app/window_op/os_recipe/pkg/pc_*/text_*/powershell_run guard registry — into `mios_pipe/routing/dispatch_build.py`, leaving the async launcher plus its PDP/quota/firewall-taint/HITL gates and broker socket I/O behind. BEFORE moving, capture a golden of `_build_dispatch_cmd(verb, args)` for the top ~20 verbs and assert byte-identical after. Every gate stays NAME-KEYED (the module's own AI-hint header forbids renames) and is re-imported verbatim.
**Where:** `usr/lib/mios/agent-pipe/mios_dispatch.py, usr/lib/mios/agent-pipe/mios_pipe/routing/dispatch_build.py (new), usr/lib/mios/agent-pipe/test_mios_dispatch.py`
**Done When:** the `_build_dispatch_cmd` golden is byte-identical pre/post, both files are under 800 lines, `test_mios_dispatch.py` passes unchanged, and mios_dispatch leaves the ceiling allowlist.
**Why:** The largest security-critical file is also the one every verb traverses; splitting it without a golden oracle risks silently changing what gets executed on the host.
**Dep:** Enforce the <=800-line module ceiling as a ratcheting drift-gate (TD-5)

### AGY-1114 -- Port jsonsalvage `loads_lenient` to a pyo3 crate proven by differential proptest  (WS-DEBT | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- establishes the pyo3 strangler + differential-parity pattern the rest of the port campaign copies.
**What+How:** `mios_pipe/routing/jsonsalvage.py`'s `loads_lenient` is pure and dependency-free (`re` + `json` only) and is the tool-loop rescue path used by `mios_dispatch` and `routing/toolexec`. Create `tools/native/mios-jsonsalvage` (added to `tools/native/Cargo.toml` `members`) exposing a pyo3 `loads_lenient`; reduce the Python module to a thin shim importing the compiled function, retaining today's body as a degrade-open fallback. Prove parity with a proptest differential harness that generates malformed-JSON inputs, runs BOTH the Python oracle and the Rust port, and asserts identical parsed output. Gate on a `[migration] use_rust_jsonsalvage` mios.toml toggle (oxidizr-style) so a regression rolls back by flipping ONE SSOT value.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/jsonsalvage.py, tools/native/mios-jsonsalvage/ (new), tools/native/Cargo.toml, usr/share/mios/mios.toml`
**Done When:** `cargo test` differential proptest shows Rust == Python for all generated inputs, the toggle switches implementations at runtime, `test_mios_jsonsalvage.py` is green under BOTH toggle states, and the crate is a workspace member staged explicitly.
**Why:** Without a proven lowest-risk first slice, every later Rust port is an unbounded rewrite with no parity method behind it.
**Dep:** none

### AGY-1115 -- Port mios_argval enum/synonym validation to a pyo3 crate with byte-identical reject messages  (WS-DEBT | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- moves a security boundary into memory-safe compiled code with zero operator-visible change.
**What+How:** `mios_argval.py` (70 lines: `_arg_with_synonyms`, `_validate_enum_args`) is the pure pre-broker gate rejecting verb args outside the SSOT `[verbs.<tool>.params.<arg>].enum`. Port it to `tools/native/mios-argval` (pyo3); keep the Python file as a shim importing the compiled functions with today's body as degrade-open fallback, gated on `[migration] use_rust_argval`. BEFORE porting, capture the current `_validate_enum_args` reject-message strings as an insta golden (`test_mios_argval.py` asserts exact message text) so the Rust port reproduces them byte-for-byte, and add a proptest differential over randomly-generated `[verbs]` catalogs.
**Where:** `usr/lib/mios/agent-pipe/mios_argval.py, tools/native/mios-argval/ (new), tools/native/Cargo.toml, usr/share/mios/mios.toml`
**Done When:** the insta golden of reject messages matches byte-for-byte, the proptest differential is green, `test_mios_argval.py` passes under both toggle states, and the crate is in `members` and staged explicitly.
**Why:** Arg-enum validation stands between model output and the broker; leaving it in untyped Python keeps a security boundary in the least-hardened tier.
**Dep:** Port jsonsalvage loads_lenient to a Rust pyo3 crate with differential proptest

### AGY-1116 -- Complete the tools/native Cargo workspace and gate it with cargo-deny/cargo-audit  (WS-DEBT | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- gives the Rust tier one governed workspace with a pinned lockfile as its reproducibility SSOT.
**What+How:** `tools/native/Cargo.toml` lists only 3 members (mios-drift-runner, mios-version-check, mios-wallpaperd) while 8 crate directories exist — generate-names-registry, mios-aiplane-lint, mios-bake-plan, mios-ssot-lint and mios-ssot-walk build ad-hoc and escape workspace lints and the shared lockfile. Add all crates to `members`, treat the workspace `Cargo.lock` as the reproducibility SSOT (SBOM-not-hardcode, ADR-0003), author `tools/native/deny.toml` with a RustSec advisory check plus a license allow-list operationalizing the FOSS posture, and add a `check_cargo_supply_chain` drift-gate running `cargo deny check` + `cargo audit`. Degrade-open when `cargo` is absent; mirror the gate and its `[laws]` note in both repos (Law 15).
**Where:** `tools/native/Cargo.toml, tools/native/Cargo.lock, tools/native/deny.toml (new), automation/98-drift-checks.sh, usr/share/mios/mios.toml`
**Done When:** `cargo metadata` lists all 8 crates as members, `cargo deny check` and `cargo audit` pass and are wired as a gate, `Cargo.lock` is committed and staged explicitly, and the gate exists in both repos.
**Why:** Five crates currently ship with no lint, no lock and no advisory scan, and the incoming pyo3 ports would land into that same ungoverned space.
**Dep:** none

### AGY-1117 -- Wire clippy -D warnings, rustfmt --check and cargo test as one Rust drift-gate  (WS-DEBT | P2 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- gives the compiled tier the linting discipline the shell tier never got.
**What+How:** Add `check_rust_quality` to `automation/98-drift-checks.sh` running `cargo fmt --check`, `cargo clippy --workspace -- -D warnings` with curated restriction lints (`clippy::unwrap_used`, `clippy::panic`, `clippy::todo` DENIED in the system crates), and `cargo test --workspace`. Scope it to `tools/native`, `src/mios-rs` and the new pyo3 crates; order it AFTER the negatives per the documented CI ordering; degrade-open when `cargo` is absent.
**Where:** `automation/98-drift-checks.sh, tools/native/Cargo.toml, src/mios-rs/Cargo.toml`
**Done When:** `cargo fmt --check`, `cargo clippy -- -D warnings` and `cargo test` all pass across both workspaces as ONE green drift-gate that goes red on a deliberately planted warning.
**Why:** MiOS has no shellcheck CI and no Rust lint gate either, so a panic/unwrap in a system verb can reach an image with nothing objecting.
**Dep:** Fix the tools/native Cargo workspace + add cargo-deny/audit supply-chain gate

### AGY-1118 -- Remove blocking subprocess.run from async handlers and lint the class shut  (WS-DEBT | P1 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- removes a live event-loop stall from the /v1 front door every agent request traverses.
**What+How:** `server.py` calls blocking `subprocess.run(..., timeout=5)` inside async request paths — the ceph health/df probes at lines 1661, 1715 and 1722 stall the loop for up to 5s under load. Replace them with `asyncio.create_subprocess_exec` + `await proc.communicate()` (the non-blocking pattern already present at line 3513) wrapped in `asyncio.wait_for`, or offload via `asyncio.to_thread`. Audit the whole file for `subprocess.run` and other blocking I/O inside `async def`, and add an AST-based `check_no_blocking_in_async` drift-lint. Float the timeout as `${MIOS_CEPH_PROBE_TIMEOUT:-5}` (SSOT with literal fallback).
**Where:** `usr/lib/mios/agent-pipe/server.py, automation/98-drift-checks.sh`
**Done When:** the three ceph probes run non-blocking, the AST lint reports zero `subprocess.run` inside `async def` and fails on a planted one, health endpoints return byte-identical bodies (golden parity), and the timeout resolves from env.
**Why:** One blocking call head-of-lines every concurrent request on the AI front door — this is measurable latency today, not cosmetics.
**Dep:** none

### AGY-1119 -- Route every outbound call through the pooled httpx client and gate ad-hoc clients out  (WS-DEBT | P2 | S)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- makes connection pooling structural rather than a convention one code path already ignores.
**What+How:** `server.py` maintains a shared `_client: httpx.AsyncClient` via `_get_client` (line 1792), yet the planner path opens a FRESH `async with httpx.AsyncClient(...)` per call (line 4518; the line-1809 comment already flags the connect/teardown-per-write cost). Repoint the planner and any other per-call client through `_get_client()`, and add a `check_no_adhoc_httpx_client` drift-gate forbidding `httpx.AsyncClient(` anywhere outside the `_get_client` factory (factory allowlisted). Preserve degrade-open: fall back to a per-call client only when the shared pool is unset.
**Where:** `usr/lib/mios/agent-pipe/server.py, automation/98-drift-checks.sh`
**Done When:** no `httpx.AsyncClient(` remains outside `_get_client`, the gate rejects a planted ad-hoc client, and planner requests reuse the pool with unchanged behavior.
**Why:** Every planner call currently pays a TCP connect and teardown on a hot outbound path that already has a pool sitting unused.
**Dep:** none

### AGY-1120 -- Derive the DCI act schema from a pydantic model instead of a hand-written dict  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- puts the deliberation layer's structured-output contract under a typed schema that cannot drift from its vocabulary.
**What+How:** `mios_pipe/routing/dci.py` (733 lines) declares the DCI epistemic-act vocabulary as raw dicts (`_DCI_ACTS`, `_DCI_ACT_SCHEMA`, `_DCI_ACT_NAMES`) driving structured output, with `test_mios_dci.py` pinning the 14-act/6-family contract. Add full type hints (mypy-strict clean) and replace the hand-written `_DCI_ACT_SCHEMA` with a pydantic model whose `.model_json_schema()` REGENERATES the existing schema, asserted byte-identical against a golden of today's schema so the wire contract is untouched. Keep the `configure()` DI seam and the one-way server-free boundary (98-drift-checks check 6) intact.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/dci.py, usr/lib/mios/agent-pipe/test_mios_dci.py`
**Done When:** dci.py is mypy-strict clean, the pydantic-generated schema is byte-identical to the golden of `_DCI_ACT_SCHEMA`, and `test_mios_dci.py` plus the server-free boundary gate stay green.
**Why:** Acts and schema are maintained separately today, so adding an act can silently produce a schema the model is never told about.
**Dep:** Introduce pydantic wire-schema models for the FastAPI request/response bodies

### AGY-1121 -- Type the agent/node registry builders in routing/agentreg with pydantic specs  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- validates the routing table at parse time instead of discovering a malformed entry as a mis-route.
**What+How:** `mios_pipe/routing/agentreg.py` (511 lines) parses `mios.toml [agents.*]`/`[nodes.*]` into nested `{name:{endpoint,model,role,...,engines}}` dicts via `_load_agent_registry`, `_load_node_pool` and `_build_agent_engines`. Introduce pydantic `AgentSpec`/`NodeSpec`/`EngineBinding` models so the registry is typed at the boundary (health_gate default, per-engine binding fold, WS-FED per-agent auth index), returning `.model_dump()` so the dispatcher — which routes over the plain dict — sees byte-identical structure. Snapshot the registry built from a representative mios.toml BEFORE and assert an identical dump AFTER. Keep the `catalog_fail_mode` degrade-open path and the `configure()` DI seam.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/agentreg.py, usr/lib/mios/agent-pipe/test_mios_agentreg.py`
**Done When:** the registry `.model_dump()` is byte-identical to the pre-change dict for the sample mios.toml, the module is mypy-strict clean, `test_mios_agentreg.py` is green, and degrade-open is preserved.
**Why:** A typo in `[agents.*]` currently surfaces as traffic routed to the wrong endpoint rather than a load-time error.
**Dep:** Introduce pydantic wire-schema models for the FastAPI request/response bodies

### AGY-1122 -- Type toolsearch and pin the embedding-cache fingerprint with a golden  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- makes cache invalidation across the fleet an explicit reviewed decision instead of a refactor side effect.
**What+How:** `mios_pipe/routing/toolsearch.py` (602 lines) owns cosine-over-nomic verb/app retrieval plus a fingerprint-keyed disk embedding cache (`_ensure_verb_embeddings`, `_load/_save_persisted_embeddings`, `_verb_embed_fingerprint`, `_cosine`). Add full type hints (mypy-strict) and a golden test pinning `_verb_embed_fingerprint`'s output AND the persisted-cache JSON shape. Keep `_embed_one` DI-injected via `configure()`, keep the one-way server-free boundary, and keep degrade-open when the embed lane is down.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/toolsearch.py, usr/lib/mios/agent-pipe/test_mios_toolsearch.py`
**Done When:** the module is mypy-strict clean, fingerprint and persisted-cache JSON match the golden, and `test_mios_toolsearch.py` plus the boundary gate are green.
**Why:** A one-character fingerprint change today silently invalidates every operator's on-disk cache and triggers a fleet-wide re-embed storm.
**Dep:** none

### AGY-1123 -- Golden-snapshot the parameterized SQL builders in memory/pg  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- makes the agent datastore's injection defense a reviewed snapshot diff rather than a code-reading exercise.
**What+How:** `mios_pipe/memory/pg.py` (804 lines) is the pgvector client whose PURE builders (`build_insert`, `build_recall`, `build_set_owner`, `vector_literal`, `dsn`) return `(sql, params)` with `%(name)s` placeholders — injection-safe by design, with psycopg v3 imported lazily so the builders test with no database. Add insta/golden snapshots of the emitted SQL for representative inputs (recall with RLS on and off, insert with and without owner), hypothesis property-test the `vector_literal` and `rid_to_pg_id` round-trips, and add full type hints while preserving the lazy-psycopg boundary.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/memory/pg.py, usr/lib/mios/agent-pipe/test_mios_pg.py`
**Done When:** golden SQL snapshots cover the recall/insert/RLS matrix, the property tests pass, the module is mypy-strict clean, and `test_mios_pg.py` is green with no DB required.
**Why:** Nothing currently fails if someone splices a value into a query string, so the datastore's whole injection defense rests on reviewer vigilance.
**Dep:** none

### AGY-1124 -- Type and expand tests for memory/worker_tools and embed_backfill  (WS-DEBT | P3 | S)
**Goal:** E-23 DB-driven configuration and vector recall -- makes the embed-backfill worker that fills the pgvector columns provably correct at its bounds.
**What+How:** `mios_pipe/memory/worker_tools.py` (302 lines) and `mios_pipe/memory/embed_backfill.py` (333 lines) back the pgvector memory worker/backfill path. Add full type hints (mypy-strict) and expand the stdlib assert-script sibling tests that the check-11 gate (`98-drift-checks.sh` line ~691) already requires, specifically covering the backfill batching bounds and the embed-dimension guard — a 768-d mismatch must be REJECTED, not silently inserted. Keep degrade-open when pg is unreachable.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/memory/worker_tools.py, usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py, usr/lib/mios/agent-pipe/test_mios_worker_tools.py, usr/lib/mios/agent-pipe/test_mios_embed_backfill.py`
**Done When:** both modules are mypy-strict clean, tests cover batch bounds and dimension mismatch, and the sibling-test gate is green.
**Why:** The backfill path writes the vectors recall reads; a wrong-dimension insert corrupts retrieval with no visible error.
**Dep:** none

### AGY-1125 -- Replace kernel/config's ad-hoc readers with a typed settings model proven byte-equal to userenv.sh  (WS-DEBT | P1 | L)
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- collapses one of the three parallel resolvers into a typed schema and mechanizes the lossless-diff gate.
**What+How:** `mios_pipe/kernel/config.py` (625 lines) is the agent-pipe arm of the THREE parallel resolvers (`usr/lib/mios/mios_toml.py`, the `userenv.sh` shell twins, `automation/lib/globals.ps1`) the operator flagged as the duplication smell; its `_toml_section`/`_cfg_num`/`_dispatch_toml` do ad-hoc string-munging with silent-default fallback. Introduce a pydantic-settings typed model (figment-analogue) deserializing the relevant mios.toml sections with defaults -> TOML -> env precedence, and make the `MIOS_*` env projection a SERIALIZATION of that model. Prove byte-parity with a differential test: over a corpus of mios.toml documents, assert the typed resolver emits the SAME `MIOS_*` key/value set as `tools/lib/userenv.sh` (the oracle). Mirror any new/renamed keys in both repos (Law 15) and keep `${VAR:-LIT}` fallbacks.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py, tools/lib/userenv.sh, usr/share/mios/mios.toml`
**Done When:** the typed resolver's `MIOS_*` projection is byte-identical to userenv.sh across the corpus, the module is mypy-strict clean, the no-raw-`MIOS_TOML` gate (check 51) is still green, and new keys exist in both repos.
**Why:** Three resolvers reading one SSOT means a wrong value silently reaches a Quadlet, and nothing today proves the agent-pipe arm agrees with the shell one.
**Dep:** Introduce pydantic wire-schema models for the FastAPI request/response bodies

### AGY-1126 -- Generate the AI manifest from SSOT via a pydantic model  (WS-DEBT | P2 | M)
**Goal:** E-09 One value, one name: the full de-duplication campaign -- stops the agent's advertised identity being authored a second time alongside the config it describes.
**What+How:** `mios_pipe/lifecycle/manifest.py` (111 lines) builds the agntcy/AI manifest served at `/v1/agntcy/manifest` (`test_mios_ai_manifest.py`). Replace the hand-assembled dict with a pydantic `AiManifest` model whose fields DERIVE from mios.toml (agent name/version/capabilities), making the manifest a projection of SSOT rather than a parallel hand-maintained copy. Snapshot the served manifest BEFORE and assert `.model_dump()` byte-parity AFTER; degrade-open to a minimal manifest when a source section is absent.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/lifecycle/manifest.py, usr/lib/mios/agent-pipe/test_mios_ai_manifest.py`
**Done When:** the served manifest is byte-identical pre/post, the module is mypy-strict clean, every manifest field traces to a mios.toml key, and `test_mios_ai_manifest.py` is green.
**Why:** A hand-written manifest drifts from the configuration it claims to describe, so federated peers are told capabilities the image may not ship.
**Dep:** Introduce pydantic wire-schema models for the FastAPI request/response bodies

### AGY-1127 -- Property-test the council diversity/arbiter/blades invariants  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- proves the fail-closed and degrade-open invariants of the pure policy cluster instead of asserting them in comments.
**What+How:** The cluster is pure geometry/policy: `mios_pipe/routing/council_diversity.py` (212 lines; `select_diverse`, `should_bypass`, `medoid_index` over 768-d cosine), `mios_pipe/access/arbiter.py` (69 lines; deterministic `decide()` with deny-list/allow-list/tier-ceiling), `mios_pipe/scheduler/blades.py` (173 lines; SSOT blade capacity). Add full type hints plus hypothesis property tests encoding each stated invariant: `select_diverse` always returns a subset with pairwise similarity below threshold; `arbiter.decide` is FAIL-CLOSED for an unknown tier (ranks above the top); `blade_vram_budget` degrades open to the local scalar for an unknown blade. Keep all three server-free (one-way boundary) and their default-OFF byte-parity.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/council_diversity.py, usr/lib/mios/agent-pipe/mios_pipe/access/arbiter.py, usr/lib/mios/agent-pipe/mios_pipe/scheduler/blades.py`
**Done When:** the three invariants hold under hypothesis for all generated inputs, the modules are mypy-strict clean, and the existing council tests plus the boundary gate are green.
**Why:** arbiter is a second-opinion security gate; example-based tests can pass while an unknown tier quietly ranks BELOW the ceiling and is allowed.
**Dep:** none

### AGY-1128 -- Type-harden the access security gates with a verdict golden and taint property tests  (WS-DEBT | P1 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- hardens the taint/HITL/quarantine boundary between agent input and the broker without touching its name-keyed contract.
**What+How:** The access cluster is imported directly and name-keyed by `mios_dispatch`: `mios_pipe/access/policy.py` (445 lines; `_hitl_block_reason`, `_dispatch_pdp_reason`, `_dispatch_quota_reason`), `mios_pipe/access/firewall.py` (179; `_classify_verb_taint`, `_session_is_tainted`, `_is_external_url`), `mios_pipe/access/quarantine.py` (167; the CaMeL dual-context gate). BEFORE touching types, capture current verdicts for a matrix of `(verb, tier, taint)` as a characterization golden and assert unchanged. Then add full type hints and hypothesis property tests for the security invariants: a tainted session NEVER auto-allows a high-privilege verb; `_is_external_url` classification is TOTAL (no input returns None); the quarantine gate is a strict superset of the HITL gate. Rename nothing.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/access/policy.py, usr/lib/mios/agent-pipe/mios_pipe/access/firewall.py, usr/lib/mios/agent-pipe/mios_pipe/access/quarantine.py`
**Done When:** the `(verb,tier,taint)` verdict golden is unchanged, the three property invariants hold under hypothesis, the modules are mypy-strict clean, dispatch tests are green, and no symbol was renamed.
**Why:** These untyped gates are the last thing standing between model-supplied input and host command execution, and nothing currently proves a tainted session cannot reach a privileged verb.
**Dep:** none

### AGY-1129 -- Split federation/a2a and extract a pydantic AgentCard schema  (WS-DEBT | P2 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- brings the biggest federation module under the ceiling while leaving the signed identity bytes untouched.
**What+How:** `mios_pipe/federation/a2a.py` (1720 lines, over the ceiling) builds/serves the A2A agent-card and the five `/a2a` routes (`test_mios_a2a.py` plus the passport/principal/loopback suites). Extract the agent-card BUILDER and its JSON schema into `mios_pipe/federation/agentcard.py` as a pydantic `AgentCard` model whose `.model_json_schema()` reproduces the current card, leaving the route handlers and signing (`federation/agentcard_sign.py`) in a2a.py. Snapshot the served `/v1/agent-card` BEFORE and assert byte-parity AFTER; the `a2a_router` mount and the surface-parity gate stay green.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/federation/a2a.py, usr/lib/mios/agent-pipe/mios_pipe/federation/agentcard.py (new), usr/lib/mios/agent-pipe/mios_pipe/federation/agentcard_sign.py`
**Done When:** the served agent-card is byte-identical pre/post, both files are under 800 lines, the a2a suites and surface-parity gate are green, and a2a.py leaves the ceiling allowlist.
**Why:** The agent-card is a SIGNED federation identity document; any unguarded edit to a 1720-line module can change bytes that peers verify.
**Dep:** Enforce the <=800-line module ceiling as a ratcheting drift-gate (TD-5)

### AGY-1130 -- Split routing/chat.py behind a chat-transcript golden  (WS-DEBT | P2 | XL)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- breaks up the hottest and largest agent-pipe module with proof that request behavior is unchanged.
**What+How:** `mios_pipe/routing/chat.py` (2140 lines) holds the `/v1/chat/completions` router-classified chain. Extract the cohesive sub-phases — the REFINE/CRITIC/POLISH passes and the OWUI narration-collapse block documented in server.py's header — into `mios_pipe/routing/chat_phases.py`, re-importing under original aliases so the surface-parity gate and `test_mios_chat.py` (24k of tests) pass unchanged. Capture a golden of a representative chat-completion transcript BEFORE and assert identical output AFTER EACH extraction.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/chat.py, usr/lib/mios/agent-pipe/mios_pipe/routing/chat_phases.py (new), usr/lib/mios/agent-pipe/test_mios_chat.py`
**Done When:** the chat transcript golden is byte-identical pre/post, chat.py is under 800 lines with sibling tests, `test_mios_chat.py` and surface-parity are green, and the ceiling allowlist entry is removed.
**Why:** Every operator chat request runs through this 2140-line file, so it is simultaneously the riskiest to touch and the most expensive to leave unsplit.
**Dep:** Enforce the <=800-line module ceiling as a ratcheting drift-gate (TD-5)

### AGY-1131 -- Split routing/portal.py and schema-validate Portal config writes  (WS-DEBT | P2 | L)
**Goal:** E-11 Unified config surface: mios.toml, the configurator and the Portal are one door at :8640/ -- closes the last hand-edit gap where the operator's single config door can persist an invalid value.
**What+How:** `mios_pipe/routing/portal.py` (1826 lines, over the ceiling) serves the operator Portal/configurator on `:8640` (ADR-0009, the one SSOT config surface; `test_mios_portal.py` 15k). Extract the config read/write handlers from the page-render handlers into `mios_pipe/routing/portal_config.py`, and type the Portal's mios.toml read/write payloads with the pydantic settings model from the kernel/config task so a Portal write is SCHEMA-VALIDATED — a bad operator edit returns 422 with a field span instead of persisting a silent bad value. Surface-parity and `test_mios_portal.py` stay green.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/portal.py, usr/lib/mios/agent-pipe/mios_pipe/routing/portal_config.py (new), usr/lib/mios/agent-pipe/test_mios_portal.py`
**Done When:** a malformed Portal config write is rejected with 422 + field span, both files are under 800 lines, `test_mios_portal.py` and surface-parity are green, and the ceiling allowlist entry is removed.
**Why:** Today the one operator-facing config door accepts an invalid value and writes it, which then propagates through every SSOT projection downstream.
**Dep:** Replace kernel/config ad-hoc readers with a typed settings model + golden parity vs userenv.sh

### AGY-1132 -- Harden mios_codemode, the model-controlled code-execution surface  (WS-DEBT | P1 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- makes the highest-risk agent path sandbox-mandatory and degrade-CLOSED, and moves it into the governed package.
**What+How:** `mios_codemode.py` (230 lines, still an un-migrated top-level module) is the `code_mode` execution branch reached from the tool loop (`routing/toolexec.py` `_exec_tool_calls`). Migrate it to `mios_pipe/routing/codemode.py` (satisfying the modular-boundary and sibling-test gates), add full type hints, and harden it: enforce the `mios-sandbox` wrapper on EVERY execution path, forbid `eval`/`exec` of model output (asserted by a targeted drift-lint), and add a characterization test of sandbox-profile selection. Gate any behavior change behind a `[migration]` toggle; degrade CLOSED — refuse to run — when the sandbox is unavailable.
**Where:** `usr/lib/mios/agent-pipe/mios_codemode.py, usr/lib/mios/agent-pipe/mios_pipe/routing/codemode.py (new), usr/lib/mios/agent-pipe/mios_pipe/routing/toolexec.py, automation/98-drift-checks.sh`
**Done When:** codemode lives under `mios_pipe` with a sibling test, every exec path is sandbox-wrapped, the no-eval/exec-of-model-output lint is green and fails on a planted violation, and degrade-closed is verified.
**Why:** Executing model-authored code is the most dangerous verb path in MiOS, and today nothing mechanically guarantees it is sandboxed or that it refuses to run unsandboxed.
**Dep:** none

### AGY-1133 -- Migrate mios_skills into mios_pipe with type hints and a parity golden  (WS-DEBT | P2 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- completes the modular-monolith by pulling the last large tool-loop module into the package.
**What+How:** `mios_skills.py` (720 lines, un-migrated top-level) provides `execute_skill`, imported directly by `routing/toolexec.py`. Move it to `mios_pipe/routing/skills.py` (the strangler target dir), leave a re-export shim following the established `_ShimModule` pattern from `mios_dci.py`, and add full type hints plus a sibling test satisfying the check-11 gate. Snapshot `execute_skill` results for a representative skill catalog BEFORE and assert byte-parity AFTER the move; keep the `configure()` DI seam and the one-way server-free boundary.
**Where:** `usr/lib/mios/agent-pipe/mios_skills.py, usr/lib/mios/agent-pipe/mios_pipe/routing/skills.py (new), usr/lib/mios/agent-pipe/test_mios_skills.py`
**Done When:** `execute_skill` output is byte-identical pre/post, the shim re-exports every public name, the module is mypy-strict clean, and `test_mios_skills.py` plus the sibling-test and boundary gates are green.
**Why:** A top-level module on the tool loop sits outside the package's boundary and sibling-test gates, so it is typed by nobody and gated by nothing.
**Dep:** none

### AGY-1134 -- Type the federation MCP + http_caps planes and bring http_caps under the ceiling  (WS-DEBT | P3 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- types an untrusted external boundary and shrinks it in the same change.
**What+How:** `mios_pipe/federation/mcp.py` (649 lines) and `federation/http_caps.py` (921 lines) implement the MCP client/tool surface (`test_mios_mcp.py`, `test_mios_http_caps.py`). Add pydantic models for the MCP tool-manifest and capability-advertisement wire shapes so tool discovery is typed at the boundary, plus full type hints. Since http_caps.py is over the 800 ceiling, extract the capability-builder from the route handlers into a helper module in the SAME PR. Snapshot the advertised capability document BEFORE/AFTER for parity; degrade-open when a remote MCP is unreachable.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/federation/mcp.py, usr/lib/mios/agent-pipe/mios_pipe/federation/http_caps.py`
**Done When:** the capability document is byte-identical pre/post, http_caps.py is under 800 lines, both modules are mypy-strict clean, the mcp/http_caps tests are green, and http_caps leaves the ceiling allowlist.
**Why:** MCP manifests arrive from outside the host and are consumed as untyped dicts, so a malformed remote advertisement is parsed rather than rejected.
**Dep:** Introduce pydantic wire-schema models for the FastAPI request/response bodies

### AGY-1135 -- Type observability audit/trace/cost as pydantic structured events  (WS-DEBT | P3 | S)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- schema-validates the forensic and billing trail without changing a byte of the stored event JSON.
**What+How:** `mios_pipe/observability/audit.py` (410 lines), `trace.py` (177) and `cost.py` (117) emit the audit/trace/cost-ledger events (`test_mios_audit.py`, `test_mios_trace.py`, `test_mios_cost.py`). Replace the ad-hoc event dicts with pydantic `AuditEvent`/`TraceSpan`/`CostRecord` models so every record is schema-validated and self-documenting, with `.model_dump()` preserving the current DB-event JSON shape (asserted byte-identical against a golden of current events). Add full type hints; degrade-open when the DB sink is down.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/observability/audit.py, usr/lib/mios/agent-pipe/mios_pipe/observability/trace.py, usr/lib/mios/agent-pipe/mios_pipe/observability/cost.py`
**Done When:** emitted event JSON is byte-identical to the golden, the three modules are mypy-strict clean, the audit/trace/cost tests are green, and degrade-open is preserved.
**Why:** The audit and cost ledgers are the only record of what the agent plane did and what it spent, and an ad-hoc dict can drop a field with nothing noticing.
**Dep:** Introduce pydantic wire-schema models for the FastAPI request/response bodies

### AGY-1136 -- Split context/grounding.py (842) under the 800-line ceiling and type it, with an output-parity golden  (WS-DEBT | P3 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- proves the split+type+parity pattern on the smallest over-ceiling module so the rest of the agent-pipe decomposition can follow it.
**What+How:** `mios_pipe/context/grounding.py` is 842 lines (42 over the ceiling) and performs RAG grounding/context assembly. Extract the retrieval and scoring helpers out of the assembly orchestrator into a new sibling `mios_pipe/context/grounding_retrieve.py`, add full type hints to both, and snapshot a representative grounding output as a parity golden before the move. Preserve the degrade-open empty-context fallback when pgvector recall returns nothing, then delete `grounding.py` from the module-ceiling allowlist.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/context/grounding.py, usr/lib/mios/agent-pipe/mios_pipe/context/grounding_retrieve.py (new), usr/lib/mios/agent-pipe/test_mios_grounding.py`
**Done When:** grounding output is byte-identical pre/post split; both files are under 800 lines; `mypy --strict` is clean; `test_mios_grounding.py` is green; the ceiling-allowlist entry for grounding.py no longer exists.
**Why:** Today the ceiling gate can only ever be advisory while allowlisted modules sit above it, and the campaign has no demonstrated, low-risk template for splitting a live RAG module without changing its output.
**Dep:** Enforce the <=800-line module ceiling as a ratcheting drift-gate (TD-5)

### AGY-1137 -- Make mios_surface.py deterministic: type it, add a pydantic surface record, prove idempotent regeneration  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- hardens the fitness-function generator every server.py split task depends on, so the parity gate emits signal rather than noise.
**What+How:** `mios_surface.py` (686 lines) generates `usr/share/mios/ai/v1/surface.generated.json` (1390 lines), which the WS R0 drift-gate (98-drift-checks.sh ~lines 811-868) diffs `server.py` against to catch a route dropped or added during a refactor. Add full type hints, a pydantic model for the surface-record shape, and a self-test that round-trips `--package` output with stable key ordering and no absolute host paths embedded. Because every split task regenerates this file, stage the regenerated JSON with an explicit path (never `git add -A`; AGY shares the tree).
**Where:** `usr/lib/mios/agent-pipe/mios_surface.py, usr/share/mios/ai/v1/surface.generated.json, automation/98-drift-checks.sh`
**Done When:** regenerating twice yields byte-identical JSON (stable ordering, no host paths); `mypy --strict` clean; the WS R0 parity gate green; the regenerated file staged by explicit path.
**Why:** A generator with unstable ordering or leaked host paths makes the parity gate flap on every refactor, which trains reviewers to ignore the one check that guarantees no public route silently disappears.

### AGY-1138 -- Split routing/dag_exec (1595) and native_loop (1527) behind transcript goldens  (WS-DEBT | P3 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- brings the routing cluster's two heaviest execution engines under the module ceiling without altering execution behavior.
**What+How:** `mios_pipe/routing/dag_exec.py` (1595 lines) drives multi-step DAG execution and `mios_pipe/routing/native_loop.py` (1527) drives the universal native tool-loop. From each, extract one cohesive sub-block: DAG validation and topological-order helpers into a new `mios_pipe/routing/dag_plan.py`, tool-loop turn/step helpers into a new `mios_pipe/routing/native_loop_step.py`, re-importing under the original aliases so the surface-parity gate still resolves the same names. Record a golden execution transcript for one representative DAG and one representative native-loop run BEFORE the split and assert byte-identity AFTER; then remove both ceiling-allowlist entries.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/dag_exec.py, usr/lib/mios/agent-pipe/mios_pipe/routing/native_loop.py, usr/lib/mios/agent-pipe/mios_pipe/routing/dag_plan.py (new), usr/lib/mios/agent-pipe/mios_pipe/routing/native_loop_step.py (new)`
**Done When:** DAG and native-loop transcripts are byte-identical pre/post; all four files are under 800 lines; `test_mios_dag_exec.py`, `test_mios_native_loop.py` and the surface-parity check are green; both allowlist entries removed.
**Why:** These are the hottest code paths in the router; while they stay 2x over the ceiling, every change to them is reviewed by eye instead of by gate, and the ratcheting ceiling can never close.
**Dep:** Enforce the <=800-line module ceiling as a ratcheting drift-gate (TD-5)

### AGY-1139 -- Sandbox conductor.py: replace create_subprocess_shell with exec+StepSpec and Jinja2 Template with SandboxedEnvironment  (WS-DEBT | P1 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- removes the last model-reachable shell/template injection surface in agent-pipe, distinct from the mios_codemode path already covered.
**What+How:** `mios_pipe/routing/conductor.py` (89 lines, imported by `server.py`) executes `asyncio.create_subprocess_shell(args.get('cmd',''), ...)` on a model-supplied shell string inside `run_step()`, and builds the workflow via unsandboxed `jinja2.Template(template_str).render(**params)` (server-side template injection). Replace the shell call with `create_subprocess_exec` over an argv list validated by a pydantic `StepSpec` (action enum + `argv: list[str]`, no free-form `cmd` field), swap the raw Template for `jinja2.sandbox.SandboxedEnvironment` with autoescape, allowlist the permitted actions, and route non-shell actions through the existing verb broker instead of today's `Mock executed` stub. Add negative/injection tests proving a `$(...)` or `;rm` payload can no longer spawn a shell, plus a drift-gate grep that fails on any reintroduction.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/conductor.py, usr/lib/mios/agent-pipe/server.py (conductor wiring), usr/lib/mios/agent-pipe/test_mios_conductor.py, automation/98-drift-checks.sh`
**Done When:** no `create_subprocess_shell` or bare `jinja2.Template` remains anywhere in agent-pipe (new drift-gate grep negative added), `StepSpec` rejects a free-form `cmd`, the injection proptest is green, and `pytest` + `just drift-gate` are green.
**Why:** Any prompt that reaches the conductor today can run arbitrary shell on the host as the agent-pipe user; this is the sharpest remaining RCE-class hazard in the AI plane and nothing else in the queue closes it.

### AGY-1140 -- Delete the global `os.environ = _StrippedEnviron` monkeypatch; strip quotes at read via a typed accessor  (WS-DEBT | P1 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- removes a process-global singleton mutation that silently undercuts the typed-config effort across the whole agent-pipe.
**What+How:** `server.py` lines 67-89 define `class _StrippedEnviron(collections.abc.MutableMapping)` and then run `os.environ = _StrippedEnviron(os.environ)` at import time, replacing the process environment mapping for every imported module and every child process just to strip surrounding quotes on read. Delete both the reassignment and the class. Add a typed `env_str(key, default) -> str` helper in `mios_pipe/kernel/config.py` beside the typed settings model that strips quotes at the single read site and preserves `${VAR:-LIT}` fallback semantics, then route the `MIOS_*` reads through it. Characterize with a golden that resolves the same values pre/post for a fixed env fixture.
**Where:** `usr/lib/mios/agent-pipe/server.py (67-89 + all os.environ.get sites), usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py, usr/lib/mios/agent-pipe/tests, automation/98-drift-checks.sh`
**Done When:** the `os.environ =` assignment no longer exists (drift-gate grep negative added), env reads go through the typed accessor, a parity golden shows identical resolved values against the current stripped mapping, and mypy + pytest are green.
**Why:** Every module and subprocess inherits a mutated environment mapping with async/thread-surprising behavior, `os.environ` cannot be statically typed, and any typed settings model built on top of it is built on sand.
**Dep:** Replace kernel/config ad-hoc readers with a typed settings model + golden parity vs userenv.sh

### AGY-1141 -- Add one fast-fail `just agent-pipe-check` gate DAG and wire it into the drift-gate and CI  (WS-DEBT | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- gives the Python plane one ordered entrypoint so its gates run in a defined sequence rather than ad hoc.
**What+How:** The campaign introduces several independent Python gates (mypy, pytest + coverage floor, ruff, the 800-line ceiling) with no single ordered entrypoint; `Justfile`/`justfile` and `automation/98-drift-checks.sh` already carry scattered agent-pipe checks (~lines 424-870). Add a `just agent-pipe-check` recipe that runs in fast-fail cost order: `ruff check` -> `mypy --strict` -> `pytest -q` with the coverage floor -> the module-ceiling scan, all scoped to `usr/lib/mios/agent-pipe`. Add one `check_*` function in 98-drift-checks.sh that shells the recipe and degrades open when `/usr/lib/mios/agents/.venv` is absent (mirroring the unit's `ConditionPathExists` pattern), and register the corresponding CI job.
**Where:** `Justfile, justfile, automation/98-drift-checks.sh, .github/workflows (or the repo CI entrypoint that runs the drift gate)`
**Done When:** `just agent-pipe-check` runs the four gates in fail-fast order, a new drift-check invokes it and degrades open without the venv, the CI job exists, and the aggregate is green.
**Why:** Without one ordered target the per-tool gates run inconsistently between local and CI, so a contributor can land code that passes locally and fails the bake, and the WS-DEBT tasks have no defined sequencing backbone.
**Dep:** Add pyproject.toml + strict mypy config + an agent-pipe mypy drift-gate

### AGY-1142 -- Add a ruff lint + format drift-gate over the 123-module agent-pipe Python tree  (WS-DEBT | P2 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- gives Python the lint/format/async-antipattern gate that Rust already has via clippy/rustfmt.
**What+How:** No Python linter or formatter config exists anywhere in the tree (verified: no `ruff.toml`, no `[tool.ruff]`, no `[tool.black]`), so unused imports, import-order rot and async antipatterns go unenforced across 123 agent-pipe modules. Add a `[tool.ruff]` section to the agent-pipe `pyproject.toml` with a curated ruleset -- pyflakes `F`, pycodestyle `E`, bugbear `B`, the `ASYNC` rules for the async service, isort `I` -- plus `ruff format --check`. Land it green using a `per-file-ignores` baseline that ratchets down over time, and register it as the lint leg of the agent-pipe gate DAG.
**Where:** `usr/lib/mios/agent-pipe/pyproject.toml, automation/98-drift-checks.sh, Justfile/justfile`
**Done When:** `ruff check` and `ruff format --check` run clean over `usr/lib/mios/agent-pipe` modulo the ratcheting ignore baseline, a drift-check enforces both, and the lint leg is wired into `just agent-pipe-check`.
**Why:** mypy sees types but not blocking-in-async calls, dead imports or format drift, so the exact antipattern class the runtime tasks are chasing keeps re-entering the async service unnoticed.
**Dep:** Add pyproject.toml + strict mypy config + an agent-pipe mypy drift-gate

### AGY-1143 -- Move the six residual top-level modules into mios_pipe and kill gateway_queue's server.py global DI  (WS-DEBT | P2 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- completes the modular-monolith package boundary the drift-gate is already trying to enforce.
**What+How:** Six real modules still live at the agent-pipe top level outside `mios_pipe`: `mios_gateway_queue.py` (486 lines; a smolagents `ToolCallingAgent` + `LiteLLMModel` seam whose `_VERB_CATALOG`/`_recipes` globals are injected by server.py), `mios_endpoints.py` (106), `mios_registry.py` (103), `mios_template.py` (133), `mios_cold_evict.py` (103), and `mios_scratchpad.py` (89, duplicated by `mios_pipe/context/scratchpad.py`). Strangler-move each into its correct subpackage -- gateway_queue and endpoints to `routing`, registry to `lifecycle`, template to `context`, cold_evict to `scheduler`, scratchpad deduplicated into `context/scratchpad.py` -- leaving each top-level file as the standard `_ShimModule` re-export. Add full type hints plus a pydantic model for the gateway_queue smolagents tool manifest and LiteLLMModel config, and replace the server.py-injected module globals with a typed injected context object. Prove parity with the sibling tests.
**Where:** `usr/lib/mios/agent-pipe/mios_gateway_queue.py, mios_endpoints.py, mios_registry.py, mios_template.py, mios_cold_evict.py, mios_scratchpad.py -> usr/lib/mios/agent-pipe/mios_pipe/{routing,lifecycle,context,scheduler}/, usr/lib/mios/agent-pipe/server.py`
**Done When:** all six modules live under `mios_pipe.*` with scratchpad de-duplicated, the top-level files are thin shims, gateway_queue no longer reads server.py module globals, sibling tests and mypy pass, and the drift-gate is green.
**Why:** The tool-execution seam currently only works if server.py imported first and monkey-set its globals, which makes it untestable in isolation and leaves the package migration permanently half-finished.
**Dep:** Migrate mios_skills into mios_pipe with full type hints and parity golden

### AGY-1144 -- Retire the 101 _ShimModule re-exports and the two `__import__("mios_...")` string wirings in server.py  (WS-DEBT | P3 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- the terminal cleanup that makes static typing and import-graphing of agent-pipe actually complete.
**What+How:** 101 top-level `mios_*.py` files are pure lazy `_ShimModule` re-exports of `mios_pipe.*` (pattern verified in `mios_a2a.py`/`mios_chat.py`), and `server.py` wires modules by dynamic string -- `__import__('mios_chat')` at line 7484 and `__import__('mios_clusterhealth')` at 7683 -- which defeats mypy and import graphing. After the split/migration/typing tasks land, mechanically rewrite every `import mios_X` / `from mios_X import` across callers (server.py, the 157 tests, `usr/libexec` consumers) to `from mios_pipe.<pkg> import <mod>`, replace the two `__import__()` calls with explicit imports, delete the 101 shim files with explicit `git rm` (never `git add -A`; AGY shares the tree), and drop the sibling-test drift-check's dependence on top-level names.
**Where:** `usr/lib/mios/agent-pipe/mios_*.py (the 101 shims), usr/lib/mios/agent-pipe/server.py (7484, 7683), usr/lib/mios/agent-pipe/test_mios_*.py, usr/libexec/mios/*, automation/98-drift-checks.sh`
**Done When:** no `_ShimModule` file and no `__import__("mios_...")` call remains, every import references `mios_pipe.*`, `surface.generated.json` parity and the full pytest run are green, and the drift-gate is green.
**Why:** While the shims stand, every module has two importable names and two of them are invisible to the type checker, so the migration's benefits are only nominal and new code keeps binding to the legacy names.
**Dep:** Migrate the residual top-level non-shim modules into mios_pipe (finish the package move)

### AGY-1145 -- Relocate the 157 flat test_mios_*.py into a mirrored tests/ tree and repoint the sibling-test drift-check  (WS-DEBT | P2 | M)
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- makes the test layout mirror the package so collection, coverage and the sibling-test gate all agree.
**What+How:** 157 standalone `test_mios_*.py` scripts sit at the agent-pipe top level while `tests/` holds only 3 files, and the drift-check at 98-drift-checks.sh ~lines 691-729 requires a `test_mios_<module>.py` NEXT TO each `mios_pipe` submodule, hard-coupling test location to module location. Relocate the tests into a `tests/` tree mirroring the package (`tests/routing/test_chat.py`, `tests/scheduler/test_preempt.py`, ...), update that drift-check to resolve the mirrored path instead of the alongside path, resolve the name collisions the flat layout hid (e.g. `mios_scratchpad` vs `context/scratchpad`), and set the pytest rootdir and import mode so collection works without the `sys.path.insert` hacks. Land after the pytest runner replaces `run_tests.py`.
**Where:** `usr/lib/mios/agent-pipe/test_mios_*.py -> usr/lib/mios/agent-pipe/tests/**, usr/lib/mios/agent-pipe/run_tests.py, automation/98-drift-checks.sh (~691-729)`
**Done When:** all 157 tests live under `tests/` mirroring `mios_pipe`, the sibling-test drift-check resolves the mirrored layout, pytest collects and passes the full suite, and the drift-gate is green.
**Why:** The flat directory forces subprocess-per-file execution and `sys.path` hacks, and the location-coupled drift-check actively blocks any package reorganization until it is repointed.
**Dep:** Replace the run_tests.py glob-runner with pytest + a coverage-floor gate

### AGY-1146 -- Type and property-test the scheduler cluster (admission/preempt/slo/vram/evict/batch/bench/stress)  (WS-DEBT | P2 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- puts typed schemas and invariants under the GPU admission logic that gates every AI route.
**What+How:** `mios_pipe/scheduler/` is ~2493 lines across admission, batch, bench, evict, preempt (643), sched, slo, stress and vram with near-zero return-type coverage and no owning task (blades belongs to the council task and is excluded here). Add full type hints plus pydantic models for `VramBudget`, `AdmissionDecision`, `PreemptPlan` and `SloTarget`, and property-test the VRAM-fit and preemption math with hypothesis over three invariants: admission never over-commits a `VramBudget`, preemption is monotonic in priority, and evict never drops a pinned lane. Keep the existing degrade-open behavior on missing signals and fill any sibling tests the drift-check requires.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/scheduler/{admission,batch,bench,evict,preempt,sched,slo,stress,vram}.py, usr/lib/mios/agent-pipe/tests`
**Done When:** `mypy --strict` is clean over `scheduler/` excluding blades, the hypothesis invariants are green, every module has a sibling test, no scheduler file exceeds 800 lines, and the drift-gate is green.
**Why:** An over-commit or non-monotonic preemption bug in this cluster strands the GPU for every lane at once, and today nothing -- no type, no test, no invariant -- would catch it before an operator's box wedges.
**Dep:** Type + property-test the council diversity/arbiter/blades cluster

### AGY-1147 -- Type and golden the identity cluster (crl/principal/reputation) signing, revocation and scoring  (WS-DEBT | P2 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- pins the trust root that the A2A federation plane consults on every inbound agent card.
**What+How:** `mios_pipe/identity/{crl,principal,reputation}.py` (~432 lines) implements principal identity, certificate revocation and reputation scoring feeding a2a/agentcard verification, but is untyped and untested. Add type hints plus pydantic models for `Principal`, `CRLEntry` and `ReputationScore`; add typed errors for revoked, expired and unknown principals wired into the typed exception hierarchy; and add deterministic golden vectors using a fixed test keypair proving signature verification, CRL membership and reputation aggregation are stable. Ensure the revoked and expired paths fail closed rather than degrading open.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/identity/{crl,principal,reputation}.py, usr/lib/mios/agent-pipe/tests`
**Done When:** `mypy --strict` is clean over `identity/`, the signature/CRL/reputation golden vectors are byte-stable, revoked and expired principals fail closed with typed errors, sibling tests exist, and the drift-gate is green.
**Why:** A regression that made verification pass for a revoked principal would be invisible today, and it would admit an untrusted federated peer into the agent plane.
**Dep:** Split federation/a2a and extract a pydantic AgentCard schema

### AGY-1148 -- Type and golden the KV-cache/context cluster (kvfork/kvgc/compact/ctxpack/tokenize/promptfmt/promptver)  (WS-DEBT | P2 | L)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- pins the token-budget and cache-reuse logic that every single turn passes through.
**What+How:** `mios_pipe/context/` holds ~1130 lines beyond grounding (kvfork, kvgc, compact, ctxpack, tokenize, promptfmt, promptver, scratchpad) governing KV-cache forking/GC and token-budget packing; none are typed and only grounding has an owning task. Add type hints plus pydantic models for `KvForkSpec`, `KvGcPolicy` and `CtxPack`, capture deterministic golden snapshots of token-budget and compaction behavior on a fixed corpus, and property-test two invariants: kvgc never evicts a pinned fork, and ctxpack never exceeds the budget. De-duplicate `context/scratchpad.py` against the top-level `mios_scratchpad.py` migration.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/context/{kvfork,kvgc,compact,ctxpack,tokenize,promptfmt,promptver,scratchpad}.py, usr/lib/mios/agent-pipe/tests`
**Done When:** `mypy --strict` is clean over `context/` excluding grounding, the token-budget and compaction goldens are byte-stable, the kvgc pinned-fork proptest is green, sibling tests exist, and the drift-gate is green.
**Why:** A ctxpack overrun truncates prompts or blows the context window at inference time, and a kvgc bug that evicts a pinned fork silently destroys cache reuse -- both surface as vague model-quality complaints rather than as test failures.
**Dep:** Split and type context/grounding (842) under the ceiling

### AGY-1149 -- Split the four over-ceiling routing modules (swarm/refine/agent_call/web_research) behind transcript goldens  (WS-DEBT | P2 | XL)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- clears the largest remaining blockers to the ratcheting module-ceiling gate going green.
**What+How:** Four routing modules blow the 800-line ceiling with no owning split task: `swarm.py` (1375), `refine.py` (1349), `agent_call.py` (1328), `web_research.py` (1302). Strangler-split each along its natural seams -- swarm into fanout/aggregate/vote, refine into refine/critic/polish passes, agent_call into request-build/transport/parse, web_research into fetch/extract/synthesize -- keeping the current public entrypoints as re-export facades so callers and tests are untouched. Characterize each with a transcript golden recorded on a fixed corpus before the split and asserted byte-identical after, and preserve `web_research`'s `create_subprocess_exec` argv form (lines ~492-553) rather than regressing it to a shell call.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/{swarm,refine,agent_call,web_research}.py, usr/lib/mios/agent-pipe/tests`
**Done When:** each of the four files is <=800 lines, the transcript goldens are byte-identical pre/post, the public facades are unchanged, and the ceiling drift-gate is green.
**Why:** These are the biggest agent-pipe modules after chat/portal/a2a and nothing else in the queue touches them, so the ceiling gate stays permanently allowlisted-open while they sit at 1.3-1.7x the limit.
**Dep:** Enforce the <=800-line module ceiling as a ratcheting drift-gate (TD-5)

### AGY-1150 -- Split oscontrol/vision/toolexec and memory/knowledge, typing the OS-control action schema on the way  (WS-DEBT | P2 | XL)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- finishes the over-ceiling inventory and types the OS-action exec surface while it is open.
**What+How:** Four more modules exceed the ceiling with no owning task: `routing/oscontrol.py` (925 lines, with `create_subprocess_exec` at 452/464), `routing/vision.py` (841), `routing/toolexec.py` (808), and `memory/knowledge.py` (1092). Strangler-split each into cohesive submodules -- oscontrol into action-schema/exec/parse (adding a typed pydantic OS-control action model and keeping the exec argv form), vision into decode/describe/route, toolexec into resolve/invoke/collect, knowledge into ingest/query/embed -- keeping public entrypoints as facades, and characterize each with transcript/golden snapshots asserting byte-parity. Fill any sibling tests the drift-check requires.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/routing/{oscontrol,vision,toolexec}.py, usr/lib/mios/agent-pipe/mios_pipe/memory/knowledge.py, usr/lib/mios/agent-pipe/tests`
**Done When:** all four files are <=800 lines, goldens are byte-identical pre/post, oscontrol still uses exec-form subprocess behind a typed action schema, and the ceiling + sibling-test drift-gates are green.
**Why:** These are the last unowned over-ceiling modules, and oscontrol in particular executes host actions on model-shaped input with no schema between the two.
**Dep:** Enforce the <=800-line module ceiling as a ratcheting drift-gate (TD-5)

### AGY-1151 -- Register the 5 orphaned native crates as workspace members and add a fmt/clippy build-gate  (WS-RESOLVER | P1 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- makes the Rust plane actually buildable and lint-gated so every downstream compiled-resolver task is verifiable.
**What+How:** `tools/native/Cargo.toml` declares `members = [mios-drift-runner, mios-version-check, mios-wallpaperd]` only, yet `mios-ssot-walk`, `mios-ssot-lint`, `generate-names-registry`, `mios-aiplane-lint` and `mios-bake-plan` each set `version.workspace = true` (verified by grep), which requires membership -- so they are orphaned and `cargo build -p mios-ssot-lint` inside `check_ssot_lint_equivalence` (98-drift-checks.sh ~line 4880) fails and is swallowed by `|| true`, silently skipping the twin gate. Add all five crate directories to `members[]`, run `cargo build --workspace --manifest-path tools/native/Cargo.toml` to confirm they compile as members, then add a new drift-check `check_native_workspace_clean` running `cargo fmt --manifest-path tools/native/Cargo.toml --check` plus `RUSTFLAGS=-Dwarnings cargo clippy --workspace -- -D warnings`, registered in the `check_*` dispatch list.
**Where:** `tools/native/Cargo.toml, automation/98-drift-checks.sh`
**Done When:** `members[]` lists all 8 crate dirs; `cargo build --workspace` is green; `check_native_workspace_clean` is in the dispatch array and passes `just drift-gate`; `check_ssot_lint_equivalence` no longer depends on `|| true` to hide a missing build target.
**Why:** The crate WS-RESOLVER must extend does not compile as a workspace member and the ssot-lint twin gate is a silent no-op today, so the tree reports green on a check that never ran.

### AGY-1152 -- Promote mios-ssot-walk from a 12-line stub to the typed SSOT of the section-partition constants  (WS-RESOLVER | P1 | S)
**Goal:** E-10 One canonical name: the unified names/keys registry -- single-sources the sets that decide which keys are emitted, so the Rust and Python resolvers cannot disagree about the namespace.
**What+How:** `tools/native/mios-ssot-walk/src/lib.rs` currently exports only `EXCLUDED_SECTIONS = ["meta","laws"]`, which DIVERGES from the authoritative Python sets in `usr/lib/mios/mios_toml.py` (`EXCLUDED_SECTIONS = {containers,verbs,recipes,packages,dotfiles,btop,theme,install_phases,messages}`; `WALK_MOSTLY_DEAD = {ai,image,bootstrap,profile,sandbox,security}`; `WALK_EMIT_KEEP` = 21 names). Replace the stub with three `pub const &[&str]` arrays mirroring the Python sets verbatim plus `is_excluded` / `is_mostly_dead` / `is_emit_keep` predicates, and add a test that regex-extracts the Python literals from mios_toml.py and asserts set-equality token-for-token. Keep the Python sets as the temporary source of truth the test reads FROM, so this lands non-behaviorally and fails loudly if either side is edited alone.
**Where:** `tools/native/mios-ssot-walk/src/lib.rs, tools/native/mios-ssot-walk/Cargo.toml, usr/lib/mios/mios_toml.py`
**Done When:** lib.rs exports the three reconciled constant arrays plus predicates; `cargo test -p mios-ssot-walk` includes the mios_toml.py set-equality test; the `meta/laws` stub list no longer exists; the crate compiles under `-Dwarnings`.
**Why:** The crate designated as the compiled resolver library ships partition sets that contradict the real resolver, so every function ported on top of it would emit the wrong key set from day one.
**Dep:** Register the 5 orphaned native crates as explicit workspace members and add a fmt/clippy build-gate

### AGY-1153 -- Design the mios-resolver crate: typed layered model, figment providers, thiserror/miette error surface  (WS-RESOLVER | P1 | M)**[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- establishes the crate boundary that will subsume the three parallel SSOT resolvers.
**What+How:** Add a new binary+lib crate `tools/native/mios-resolver` as a workspace member, intended to subsume `mios_toml.py`, `userenv.sh` and `globals.ps1`. Define the layered provider stack with figment (one Toml provider per layer, in the exact order `layer_paths()` returns: vendor monolith, vendor `mios.d/*.toml`, host, host mios.d, user, user mios.d) and deserialize into typed structs mirroring the mios.toml tables consumed today (`[identity]`, `[locale]`, `[ports]`, `[colors]`, `[image]`, `[ai]`, `[network]`, ... enumerated from the `[sect]` map at `tools/lib/userenv.sh` lines 22-56). Model errors as a `thiserror` enum (`LayerParse`, `TypeShape`, `MissingLayer`) wrapped by `miette::Report` at the binary boundary so a bad value points at the offending mios.toml span. API and skeleton ONLY -- no emit logic, gated behind a `--check-parse` subcommand; mios_toml.py stays the authority until later slices reroute consumers.
**Where:** `tools/native/mios-resolver/ (new: Cargo.toml, src/lib.rs, src/model.rs, src/error.rs, src/main.rs), tools/native/Cargo.toml, tools/lib/userenv.sh (schema reference only)`
**Done When:** `cargo build -p mios-resolver` is green; `mios-resolver --check-parse --root .` parses the real `usr/share/mios/mios.toml` without error and returns nonzero with a miette span on a deliberately malformed fixture; the crate is a workspace member; no consumer is rerouted yet.
**Why:** Without a stable crate boundary landed first, every resolver port has to be one giant untestable change instead of a series of small independently-gated slices.
**Dep:** Promote mios-ssot-walk from a 12-line stub to the single typed SSOT of section-partition constants

### AGY-1154 -- Port tier/layer discovery (layer_paths + _frags + _tier_dirs) into mios-resolver  (WS-RESOLVER | P1 | M)**[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- reproduces the vendor<host<user precedence contract in the compiled resolver byte-for-byte.
**What+How:** Reimplement `mios_toml.py` `_tier_dirs()`, `_frags()` and `layer_paths()` (lines 43-87) in Rust: honor the same env overrides (`MIOS_TOML_ROOT`, `MIOS_VENDOR_TOML`/`MIOS_TOML`, `MIOS_HOST_TOML`, `MIOS_USER_TOML`, `XDG_CONFIG_HOME`, the `*_TOML_D` fragment dirs), the `/usr/lib/mios/mios.d` vendor-fragment location (Law 1 USR-OVER-ETC), and lexical-by-basename ordering within a tier with tier-major precedence. Reproduce the Windows path normalization the userenv.sh heredoc performs (the `/c/... -> c:/...` regex at userenv.sh lines 126-135) so CI on Windows resolves identically. Add characterization tests that build a temp tree of monolith + mios.d fragments and assert the returned `Vec<PathBuf>` ordering matches a golden list captured from the current Python `layer_paths()` on the same fixture.
**Where:** `tools/native/mios-resolver/src/layers.rs, usr/lib/mios/mios_toml.py (oracle), usr/lib/mios/userenv.sh (path-normalize oracle)`
**Done When:** `layers::resolve()` returns an identical ordered path vector to `mios_toml.layer_paths()` across 4 fixtures (no mios.d, vendor-only fragment, all-tier fragments, env-overridden root); `cargo test -p mios-resolver` green including the Windows-normalization case.
**Why:** If tier ordering or fragment discovery differs by even one entry, every value the compiled resolver emits can come from the wrong layer, and a host override would silently stop applying.
**Dep:** Design the mios-resolver crate: typed layered model + figment providers + thiserror/miette error surface

### AGY-1155 -- Port deep_merge overlay semantics (empty-string-never-overrides) with golden unit tests  (WS-RESOLVER | P1 | S)**[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- moves the subtlest overlay invariant out of two hand-maintained twins into one tested function.
**What+How:** Reimplement the overlay rule shared by `mios_toml.py` `deep_merge()` (lines 89-99) and its byte-parity copy in the userenv.sh heredoc (lines 146-153): recursive table merge, non-empty scalars and lists overwrite, and the critical rule that an empty string NEVER overrides a non-empty lower-layer value. Implement over `toml::Value` (or figment's `Dict`) rather than the typed struct so unknown/free-form tables such as `[env]` survive the merge. Capture golden fixtures by running the Python `deep_merge` on ~6 layered inputs (empty-string shadowing, list replace-not-append, nested `[ai.vllm]` override, absent-layer no-op) and assert structural equality. Library function only -- not yet wired to emit.
**Where:** `tools/native/mios-resolver/src/merge.rs, usr/lib/mios/mios_toml.py, usr/lib/mios/userenv.sh`
**Done When:** `merge::deep_merge` matches the Python result on all 6 golden fixtures including the empty-string-shadow case; a small property test confirms merge is associative over the layer order; `cargo test -p mios-resolver` green.
**Why:** The empty-string rule is duplicated in two twins and easy to break; a wrong merge silently writes a wrong value into a rendered Quadlet with no error anywhere.
**Dep:** Port the tier/layer discovery (layer_paths + _frags + _tier_dirs) into mios-resolver

### AGY-1156 -- Port walk() + process_val() value transforms (stack_offset, port!=53, bool, CSV) with characterization tests  (WS-RESOLVER | P1 | M)**[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- locks the non-obvious value-transform business rules into a characterized Rust function.
**What+How:** Reimplement `mios_toml.py` `walk()` (lines 564-576, which skips `routing.domains`) and `process_val()` (lines 578-589): booleans render as `"true"`/`"false"`; a `ports.*` value (except `ports.stack_id`) gets `int(v) + stack_offset` unless it equals 53; lists comma-join; and `stack_offset = int(ports.stack_id) * 10000` as computed in the userenv.sh heredoc lines 172-176. Emit `(dotted_path, value)` pairs from the typed/merged model in the same recursion order. Golden-test against the current Python by feeding the real merged `usr/share/mios/mios.toml` and asserting the `(path, processed)` list is identical, with dedicated cases for stack_id offsetting and the port-53 exemption.
**Where:** `tools/native/mios-resolver/src/walk.rs, usr/lib/mios/mios_toml.py, usr/lib/mios/userenv.sh`
**Done When:** walk + process_val produce an identical ordered `(path, value)` list to the Python pair-builder on the real SSOT and on a `stack_id=3` fixture; port-53, boolean and CSV cases are unit-tested; `cargo test` green.
**Why:** The DNS port-53 exemption and the stack-offset multi-tenancy rule are invisible business logic that a hand-port gets wrong quietly -- and a wrongly-offset port breaks every stacked deployment.
**Dep:** Port deep_merge overlay semantics (empty-string-never-overrides) with golden unit tests

### AGY-1157 -- Port get_aliases() -- the ~50-branch alias map -- as a data-driven table with a differential test vs Python  (WS-RESOLVER | P1 | L)**[DONE]**
**Goal:** E-09 One value, one name: the full de-duplication campaign -- consolidates the COMPAT-ALIAS logic that is currently forked across three implementations into one proven table.
**What+How:** `mios_toml.py` `get_aliases()` (lines 224-557) is the single largest divergence risk: ~50 prefix/exact-match branches mapping dotted paths to legacy `MIOS_*` aliases (`ai.vllm.* -> MIOS_VLLM_*`, `identity.username -> [MIOS_USER, MIOS_DEFAULT_USER]`, `portal.*`, `a2a.*`, the `image.sidecars.*` version-split, the `services.webtools` special cases, `bootstrap.dev_vm.*`), and `generate-names-registry/src/main.rs` already re-implements a SUBSET as `alias_for` -- a third copy. Encode the full map in Rust as an ordered match (or a table of `(matcher, emitter)` closures) preserving branch order and the multi-alias `extend` cases. Add a differential test that iterates EVERY dotted path from the real merged mios.toml, calls the Python `get_aliases` via subprocess and the Rust function, and asserts identical alias vectors. Do NOT retire the Python -- it remains the oracle.
**Where:** `tools/native/mios-resolver/src/aliases.rs, usr/lib/mios/mios_toml.py, tools/native/generate-names-registry/src/main.rs`
**Done When:** `aliases::for_path` matches `mios_toml.get_aliases` for 100% of paths in the real SSOT (differential test green); every multi-alias/extend branch (`identity.*`, `locale.*`, `ports.*`, `image.sidecars.*`, `services.webtools.*`) is covered; `cargo test` green.
**Why:** This map is already forked three ways, and a single mismatched alias silently breaks whichever consumer reads the short name -- a failure that shows up as a missing env var at runtime, not as a build error.
**Dep:** Port walk() + process_val() value transforms (stack_offset, port!=53, bool, CSV) with characterization tests

### AGY-1158 -- Port the [ports] dual-alias emission (MIOS_PORT_x + MIOS_x_PORT + guacamole canon)  (WS-RESOLVER | P1 | S)**[DONE]**
**Goal:** E-13 Ports are allocated from SSOT, not hand-assigned -- makes the compiled resolver the engine that floats every port out of `[ports]` on both platform twins.
**What+How:** Isolate and port the `ports.*` branch of `get_aliases` (mios_toml.py lines 423-431): every `[ports].<x>` emits BOTH `MIOS_PORT_<canon>` and `MIOS_<canon>_PORT`, where canon remaps `GUACAMOLE_WEB -> GUACAMOLE`, combined with the already-ported `process_val` stack_offset. Cross-check against the two static allowlists that must stay in sync: the `_ssot_lint_ports_dummy` array at `usr/lib/mios/userenv.sh` lines 312-325 and the `MIOS_PORT_*` fallback block in `automation/lib/globals.ps1` lines 95-113 (drift-gated by check 28). Add a unit test asserting the emitted port-alias set for the real `[ports]` table equals the Python output AND is a superset of the globals.ps1 hardcoded `MIOS_PORT_*` names, surfacing any Windows-side port with no backing `[ports]` key.
**Where:** `tools/native/mios-resolver/src/aliases.rs, usr/lib/mios/mios_toml.py, usr/lib/mios/userenv.sh, automation/lib/globals.ps1`
**Done When:** port dual-alias emission matches Python for all `[ports]` keys including the guacamole canon and stack offset; the test flags any globals.ps1 `MIOS_PORT_*` with no backing `[ports].<key>`; `cargo test` green.
**Why:** globals.ps1 still hardcodes a `MIOS_PORT_*` fallback block, so a Windows-side port can exist with no SSOT key and no one finds out until check 28 goes red or a service binds the wrong port.
**Dep:** Port get_aliases() -- the ~50-branch alias map -- as a data-driven table with a differential test vs Python

### AGY-1159 -- Port the WALK_MOSTLY_DEAD / WALK_EMIT_KEEP canonical-emit gating  (WS-RESOLVER | P1 | S)**[DONE]**
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- reproduces the suppression rule that keeps the exported namespace at ~2523 keys instead of tens of thousands.
**What+How:** Reimplement the emit-suppression the userenv.sh heredoc applies (lines 209-213) using the sets single-sourced in `mios-ssot-walk`: for a walked path whose top section is in `WALK_MOSTLY_DEAD` (ai, image, bootstrap, profile, sandbox, security), the section-generic canonical `MIOS_<PATH>` is SUPPRESSED unless it appears in `WALK_EMIT_KEEP` (the 21 names such as `MIOS_AI_ENDPOINT`, `MIOS_IMAGE_REF`); legacy aliases from `get_aliases` are still emitted regardless. Consume the constants from `mios-ssot-walk` rather than redeclaring them. Golden-test that the set of canonical `MIOS_*` keys emitted for the real SSOT equals the Python `exports_map` canonical key set (aliases handled separately).
**Where:** `tools/native/mios-resolver/src/emit.rs, tools/native/mios-ssot-walk/src/lib.rs, usr/lib/mios/userenv.sh`
**Done When:** canonical-key emission matches Python for the real SSOT including every MOSTLY_DEAD-suppressed and EMIT_KEEP-retained case; the constants are sourced from `mios-ssot-walk` with no redeclaration; `cargo test` green.
**Why:** Get this gate wrong and the compiled resolver either floods the environment with thousands of dead keys or drops a key like `MIOS_AI_ENDPOINT` that live consumers read -- either way the lossless env-snapshot diff fails.
**Dep:** Promote mios-ssot-walk from a 12-line stub to the single typed SSOT of section-partition constants

### AGY-1160 -- Port the [env] verbatim table, referenced_names passthrough and shlex.quote export serialization  (WS-RESOLVER | P1 | M)**[DONE]**
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- delivers the byte-identical export stream that lets userenv.sh be demoted to a shim losslessly.
**What+How:** Reimplement the final serialization steps of the userenv.sh heredoc: the `[env]` table emitted verbatim in sorted order (lines 221-226), the `referenced_names.txt` passthrough that emits `export V="${V:-}"` for names not already set (lines 231-240), and the shell-safe `export NAME=<shlex.quote(value)>` line format (lines 228-229). Implement shlex.quote equivalently (POSIX single-quote escaping) so the bytes match exactly. Golden-test the full stdout against a captured snapshot of `source usr/lib/mios/userenv.sh; env | grep ^MIOS_ | sort` produced by `mios-env-snapshot` under the same `env -i` / `LC_ALL=C` isolation.
**Where:** `tools/native/mios-resolver/src/emit_shell.rs, usr/lib/mios/userenv.sh, usr/libexec/mios/mios-env-snapshot, usr/share/mios/referenced_names.txt`
**Done When:** `mios-resolver --emit=shell` stdout is byte-identical to the mios-env-snapshot capture of the current userenv.sh on the vendor SSOT (`env -i`, `HOME=/nonexistent`, `LC_ALL=C`); shlex-quote parity is unit-tested including values with spaces, quotes and `$`; `cargo test` green.
**Why:** 22 consumers source userenv.sh today; without proven byte-identical output there is no safe way to swap the emitter, and a single quoting difference would break a sourcing script at boot.
**Dep:** Port the WALK_MOSTLY_DEAD / WALK_EMIT_KEEP canonical-emit gating

### AGY-1161 -- Port the semantic palette (PALETTE_DEFAULTS + colors()) into the mios-resolver crate  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- one compiled resolver owns the most-projected SSOT surface (colors) so no downstream twin re-declares it.
**What+How:** Reimplement `mios_toml.py` `PALETTE_DEFAULTS` (12 semantic hexes + 16 ansi slots, lines 187-198) and `colors()` (mios.toml `[colors]` layered over the defaults, SSOT wins, lines 201-215) as a typed `Palette` struct exposing `resolve(merged) -> BTreeMap<String,String>`. This is the identical map `globals.ps1` hardcodes at lines 209-220 as `MIOS_COLOR_*` and that `mios-theme-render` consumes. Golden-test the Rust palette against Python `colors()` output on the real SSOT and on a `[colors]`-override fixture, and add an assertion that the Rust defaults equal the `globals.ps1` `MIOS_COLOR_*` else-branch literals so any hardcoded PowerShell drift surfaces as a test failure.
**Where:** `tools/native/mios-resolver/src/palette.rs, usr/lib/mios/mios_toml.py, automation/lib/globals.ps1`
**Done When:** `palette::resolve` matches Python `colors()` for both the default and override fixtures; the parity test against `globals.ps1` `MIOS_COLOR_*` literals passes; `cargo test` is green.
**Why:** Colors are projected to theme-render (check 25), the configurator, oh-my-posh and PowerShell, and PowerShell is the one consumer that hardcodes them; until the crate owns the palette the PS-emitter slice cannot retire those literals.
**Dep:** Design the mios-resolver crate: typed layered model + figment providers + thiserror/miette error surface

### AGY-1162 -- Decide and implement the mios_db_config authoritative-overlay boundary for the Rust resolver  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- the compiled resolver reproduces every branch of the Python resolver, including the DB overlay, or scopes it out on the record.
**What+How:** `mios_toml.py`'s `load_merged`/`get`/`colors` each consult `mios_db_config.is_db_authoritative()` and deep-merge a DB-sourced config on top (lines 128-136, 170-179, 204-212). Choose one: (a) port a Rust client reading the same DB config through the pg loopback-in-pod path used by `mios-pg-query`, or (b) scope it out for v1 by having mios-resolver shell to the existing Python hook only when `is_db_authoritative()` is true, documented as the one retained subprocess boundary. Since `db_backed` ships inert, prefer (b): add a `--db-overlay` opt-in that invokes the Python hook and merges its JSON. Add a test asserting default-path output is identical whether or not the hook is compiled in.
**Where:** `tools/native/mios-resolver/src/db_overlay.rs, usr/lib/mios/mios_toml.py, usr/lib/mios/mios_db_config.py, usr/libexec/mios/mios-pg-query`
**Done When:** On the default (DB-inert) path mios-resolver output is byte-identical to Python; the DB-authoritative path merges the same overlay; a test proves default-path equivalence with the hook absent; the chosen boundary is documented in the crate.
**Why:** The DB overlay is a live branch in all three public entry points — ignoring it silently breaks parity the moment `db_backed` is wired, and an undocumented omission makes the strangler slice dishonest.
**Dep:** Port the [env] verbatim table + referenced_names passthrough + shlex.quote export serialization

### AGY-1163 -- Cut userenv.sh over to `mios-resolver --emit=shell` with the Python heredoc as fallback  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- the first strangler cutover: the shell façade is unchanged while the compiled binary takes over behind it.
**What+How:** Wire the ported pipeline into `mios-resolver --emit=shell` so it produces the exact `export ...` stream. Then replace the embedded Python heredoc in `tools/lib/userenv.sh` (`_mios_load_unified`, lines 86-249) with `if command -v mios-resolver; then eval "$(mios-resolver --emit=shell)"; else <existing heredoc as FALLBACK>; fi`, leaving the pgvector bind post-step (lines 258-261) and the legacy-file compat block (lines 267-309) untouched. Because the file is byte-mirrored to `usr/lib/mios/userenv.sh` (check_userenv_parity, check 27) and staged into mios-bootstrap.git, apply the identical edit to both copies and mirror in both repos (Law 15).
**Where:** `tools/lib/userenv.sh, usr/lib/mios/userenv.sh, tools/native/mios-resolver/src/main.rs (mios-bootstrap.git mirror)`
**Done When:** With mios-resolver on PATH, sourcing userenv.sh yields a byte-identical `MIOS_*` env to the pre-change heredoc (`mios-env-snapshot` diff empty); without the binary the fallback still resolves; check_userenv_parity green; the change appears in mios-bootstrap.git.
**Why:** All 22 consumers source userenv.sh; without a façade-preserving cutover every one of them would need editing at once, and without the retained heredoc a host lacking the binary resolves nothing.
**Dep:** Emit the [env] verbatim table + referenced_names passthrough + shlex.quote export serialization

### AGY-1164 -- Generate automation/lib/globals.ps1 from the resolver and delete its ~200 hand-maintained literals  (WS-RESOLVER | P2 | L) **[DONE]**
**Goal:** E-03 PowerShell surface -- the duplicated mios.toml resolvers collapse to ONE generated PowerShell module derived from SSOT.
**What+How:** `automation/lib/globals.ps1` is the divergent third resolver: ~200 hand-maintained `if($env:X){...}else{'literal'}` fallbacks for USERS/UIDs/PORTS/URLS/IMAGES/COLORS/UNITS (lines 36-221), kept aligned to mios.toml only by drift-checks 28 (ports) and 52 (images). Emit a PS module assigning `$script:MIOS_* = if($env:MIOS_*){...}else{'<SSOT-resolved value>'}` for exactly the keys globals.ps1 defines today, retaining only genuinely PS-host-specific bits (`Resolve-MiosVersion` fallback, Windows APPDATA/USERPROFILE paths at lines 164-166). Regenerate and stage the exact paths (never `git add -A`). LANDED DIFFERENTLY AND BETTER: there is no `.generated` sidecar — `automation/lib/globals.ps1` IS the generated artefact (same filename, so every dot-sourcing consumer is untouched), rendered in full by `tools/render-globals.py` from the SSOT resolver (~2000 constants, zero hand-written literals, no shim indirection), gated by check 130 `check_globals_generated` which byte-compares against a fresh render; generation runs through the Python resolver (`mios_toml`) rather than the not-yet-installed binary, with the emit logic mirrored in `emit_ps.rs` for when it is.
**Where:** `tools/native/mios-resolver/src/emit_ps.rs, automation/lib/globals.ps1, tools/render-globals.py`
**Done When:** A fresh render of globals.ps1 produces zero diff against the committed file (check 130 `check_globals_generated` green); ports/colors/images derive from mios.toml so checks 28/52 are satisfied by generation rather than hand-editing.
**Why:** globals.ps1 was the only resolver not derived from mios.toml, so Windows values drifted from SSOT by default and two drift-checks existed solely to police that divergence.
**Dep:** Port PALETTE_DEFAULTS + colors() palette resolution into mios-resolver

### AGY-1165 -- Demote mios_toml.py's public API to a signature-preserving shim over `mios-resolver --emit=json`  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- the widest-imported resolver surface reroutes through the compiled crate without a flag day.
**What+How:** Add `mios-resolver --emit=json` producing the merged config plus the full resolved `MIOS_*` map. Rewrite the PUBLIC functions of `usr/lib/mios/mios_toml.py` (`load_merged`, `load_vendor`, `get`, `section`, `colors`, `get_aliases`, `walk`, `process_val`) to subprocess the binary and parse JSON when present, falling back to the existing pure-Python implementation when absent, preserving exact signatures so the ~13 importers and the agent-pipe tree need no edits. Keep `clear_cache()` semantics. Gate behind an `MIOS_RESOLVER_NATIVE=0` kill-switch. Ensure the userenv.sh fallback heredoc, which imports this module, can never recurse back into the binary.
**Where:** `usr/lib/mios/mios_toml.py, tools/native/mios-resolver/src/main.rs`
**Done When:** A unit test comparing both modes across the real SSOT shows every public function byte-identical; `MIOS_RESOLVER_NATIVE=0` forces pure-Python; the importer count is unchanged; `just drift-gate` and the agent-pipe tests are green.
**Why:** Thirteen importers plus the whole agent-pipe tree depend on these signatures; without a shim the Python plane either stays on a second resolver forever or requires a simultaneous rewrite of every caller.
**Dep:** Emit the shell binding from mios-resolver and demote userenv.sh's Python heredoc to a thin shim

### AGY-1166 -- Emit /etc/mios/install.env from the resolver and reduce mios-sync-env to a wrapper  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- one typed serializer produces the derived env consumed fleet-wide, retiring a second quoting codepath.
**What+How:** `usr/libexec/mios/system-sync-env.sh` (`mios-sync-env`) sources userenv.sh to regenerate `/etc/mios/install.env` in a strict BARE `KEY=value` form simultaneously safe for systemd `EnvironmentFile=`, `bash source` and `podman --env-file` (documented in its header). Add `mios-resolver --emit=install-env` applying the identical rules — evict `$`-bearing and whitespace values, emit no quotes — and the same secret-eviction policy the script enforces. Demote `generate_env()` to call the binary when present, keeping the sourcing path as fallback. Golden-test the emitter byte-for-byte against current script output on the vendor SSOT.
**Where:** `usr/libexec/mios/system-sync-env.sh, tools/native/mios-resolver/src/emit_install_env.rs`
**Done When:** Binary-produced install.env is byte-identical to current `mios-sync-env` output on the vendor SSOT including secret eviction and bare quoting; `mios-sync-env --dry-run` output is unchanged; the fallback path works with the binary absent.
**Why:** install.env is parsed by three mutually incompatible parsers; keeping a second hand-written serializer alive is a standing source of quoting bugs that break systemd units and podman at once.
**Dep:** Emit the shell binding from mios-resolver and demote userenv.sh's Python heredoc to a thin shim

### AGY-1167 -- Drive referenced_names.txt + names.generated.txt from the shared crate and delete generate-names-registry's forked alias_for  (WS-RESOLVER | P2 | S) **[DONE]**
**Goal:** E-10 One canonical name -- the names registry is generated by the same alias table the resolver uses, so a name cannot mean two things.
**What+How:** `tools/native/generate-names-registry/src/main.rs` maintains its OWN `walk_value` + `alias_for` (a partial fork of `get_aliases`) to emit `usr/share/mios/referenced_names.txt` and `names.generated.txt` (~27KB each). Once mios-resolver carries the full aliases table, either add mios-resolver as a path dependency and call it as a library, or emit both files directly via `mios-resolver --emit=names`. Delete the duplicated `alias_for`/`walk_value` from the registry generator, regenerate both txt files, confirm no diff, and stage the exact paths.
**Where:** `tools/native/generate-names-registry/src/main.rs, tools/native/generate-names-registry/Cargo.toml, usr/share/mios/referenced_names.txt, usr/share/mios/names.generated.txt`
**Done When:** generate-names-registry depends on the mios-resolver crate with no local `alias_for`; regeneration yields byte-identical `referenced_names.txt` and `names.generated.txt` (check 30 green); the forked walk/alias code no longer exists.
**Why:** Three copies of the alias logic exist today (mios_toml.py, this generator, the stub) — the second copy silently diverges and the registry gate then certifies the wrong names.
**Dep:** Port get_aliases() — the ~50-branch alias map — as a data-driven table with a differential test vs Python

### AGY-1168 -- Add check_resolver_shell_equivalence plus trycmd/insta snapshots for the shell emitter  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the shell cutover gets the same byte-parity fitness function ssot-lint already has.
**What+How:** Follow the exact template of `check_ssot_lint_equivalence` (98-drift-checks.sh ~4870, which runs bash + Rust, path-normalizes and asserts identity): capture the current userenv.sh export stream under `mios-env-snapshot` isolation (`env -i`, `HOME=/nonexistent`, `LC_ALL=C`) as the golden, run `mios-resolver --emit=shell` under the same isolation, path-normalize (`/mnt/c/MiOS|C:\MiOS -> /ROOT`) and `_violation` on any diff. Additionally add a trycmd/insta test crate under `tools/native/mios-resolver/tests` capturing `--emit=shell` and `--emit=json` against committed `.snap` fixtures for the cargo-insta accept/review workflow. Register the new check in the dispatch array ahead of the main gate.
**Where:** `automation/98-drift-checks.sh, tools/native/mios-resolver/tests/cli.rs, tools/native/mios-resolver/tests/snapshots/, usr/libexec/mios/mios-env-snapshot`
**Done When:** `check_resolver_shell_equivalence` is in the `check_*` dispatch and passes on the current SSOT; `cargo test -p mios-resolver` runs the trycmd/insta snapshots for both emitters; a deliberate value change turns both the gate and the snapshot red.
**Why:** Without a gate the compiled emitter can drift from the bash it replaced and nothing notices until a host resolves the wrong env — the recurring SSOT-projection drift class this campaign exists to end.
**Dep:** Emit the shell binding from mios-resolver and demote userenv.sh's Python heredoc to a thin shim

### AGY-1169 -- Add check_resolver_ps_equivalence and fold checks 28/52 into the generation gate  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- two hand-written literal-parsing checks collapse into one generation-equivalence fitness function.
**What+How:** Add `check_resolver_ps_equivalence` that re-renders the PowerShell globals module from `mios-resolver --emit=powershell` into a temp file and `_violation`s on any difference from the committed artefact, proving no hand-drift. Because ports and images now come FROM mios.toml by generation, subsume `check_globals_ports_ssot` (check 28, 98-drift-checks.sh ~1822-1888) and the image-default twin (check 52, ~1892-1993): keep both as-is initially, then once generation is the source replace their bodies with a call into the equivalence check and delete the bespoke `MIOS_PORT_*`/image literal parsers. Register the new check in dispatch order.
**Where:** `automation/98-drift-checks.sh, tools/native/mios-resolver/src/emit_ps.rs, automation/lib/globals.ps1`
**Done When:** `check_resolver_ps_equivalence` passes; a hand-edit to the generated module or a mios.toml `[ports]`/`[image]` change is caught by the regeneration diff; checks 28 and 52 either delegate to the new gate or are retired with coverage preserved.
**Why:** Checks 28/52 parse literals out of a hand-maintained file — they can only detect the drift they were taught about, and every new key added to globals needs a new parser rule.
**Dep:** Generate globals.generated.ps1 from mios-resolver and demote globals.ps1 hardcodes to the generated module

### AGY-1170 -- Add a proptest differential gate proving mios-resolver == mios_toml.py == userenv.sh on random valid mios.toml  (WS-RESOLVER | P2 | M)
**Goal:** E-05 Test and CI governance -- proptest differential oracles make the three-resolver equivalence a property, not a handful of examples.
**What+How:** Build a proptest generator emitting random-but-valid mios.toml documents sampling real sections and keys with type-correct values (`[ports]` ints including 53 and stack_id, `[colors]` hexes, `[ai.vllm]`/`[ai.sglang]` tables, `[image.sidecars]` `name:tag`, `[env]` free-form, bools, lists). For each document run all three resolvers — `mios-resolver --emit=json`, the mios_toml.py export reconstruction (reuse `tools/check-resolver-twin.py`'s `toml_vars` builder), and `source userenv.sh; env | grep ^MIOS_` — and assert the resolved key/value SETS are identical, letting proptest auto-shrink to a minimal failing document. Wire it as a Rust test invoked from a new soft-then-gating drift-check mirroring `check_resolver_twin_equivalence` (98-drift-checks.sh ~3180).
**Where:** `tools/native/mios-resolver/tests/differential.rs, tools/check-resolver-twin.py, usr/lib/mios/userenv.sh, automation/98-drift-checks.sh`
**Done When:** `cargo test -p mios-resolver differential` runs N proptest cases with all three resolvers agreeing, or shrinks and reports the minimal divergence; a new drift-check runs it in CI; generated `ai.vllm` inputs exercise the short-vs-long alias reconciliation (`MIOS_VLLM_*` vs `MIOS_AI_VLLM_*`).
**Why:** The example-based twin checks pass on the inputs someone thought of; port 53, empty-string shadows and stack offsets are exactly the edge cases that slip through and land as a wrong port on a live host.
**Dep:** Add a Python subprocess/JSON binding and demote mios_toml.py public API to a thin shim over mios-resolver

### AGY-1171 -- Extend check_resolver_twin_equivalence/parity to a three-way crate==python==bash assertion  (WS-RESOLVER | P2 | S)
**Goal:** E-07 The drift-gate as the enforcement plane -- one gating check proves the compiled resolver has not diverged from either legacy twin during the migration window.
**What+How:** `tools/check-resolver-twin.py` today diffs mios_toml.py-reconstructed exports against `source userenv.sh` (invoked by `check_resolver_twin_equivalence`, check 45, gating, 98-drift-checks.sh ~3180). Extend it to ALSO run `mios-resolver --emit=json` when the binary is present and assert all three agree, keeping the two-way python/bash assertion as the fallback when it is absent so the gate never regresses on a bare host. Likewise upgrade the soft `check_resolver_twin_parity` (check 41, ~3125) to include the crate in its `MIOS_AI_*` spot-check. Preserve the existing env isolation (`MIOS_VENDOR/HOST/USER_TOML` pins).
**Where:** `tools/check-resolver-twin.py, automation/98-drift-checks.sh`
**Done When:** check-resolver-twin.py asserts crate==python==bash when the binary exists and python==bash otherwise; `check_resolver_twin_equivalence` stays gating and green; `check_resolver_twin_parity` includes the crate in its AI-set diff.
**Why:** During the migration window three resolvers are live at once; without the third leg in the existing gate, a crate-only divergence ships unnoticed on hosts that have the binary.
**Dep:** Add a Python subprocess/JSON binding and demote mios_toml.py public API to a thin shim over mios-resolver

### AGY-1172 -- Register the resolver-equivalence checks as first-class entries in mios-drift-runner  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the checks migrate into native `miosd` Rust so the gate stops being fragile bash.
**What+How:** `tools/native/mios-drift-runner/src/main.rs` is the Rust drift infrastructure coexisting with bash `98-drift-checks.sh`. Add the resolver parity and generation-equivalence checks (shell, PS, install-env, names) as first-class runner entries so they execute without the bash harness, mirroring how `mios-ssot-lint` is invoked: each shells to `mios-resolver --emit=<surface>` and diffs the committed artifact, reporting through the runner's existing result type. Keep the bash `check_*` functions authoritative until the runner is proven at parity by running both in CI and comparing pass/fail.
**Where:** `tools/native/mios-drift-runner/src/main.rs, tools/native/mios-resolver/src/main.rs, automation/98-drift-checks.sh`
**Done When:** mios-drift-runner exposes resolver-shell/ps/install-env/names equivalence checks; runner and bash gate report the same pass/fail on the current tree; the runner is documented as the migration target for these checks.
**Why:** Every new check added only to the 6000-line bash gate deepens the dependency this epic exists to remove, and the native runner has no resolver coverage to prove itself against.
**Dep:** Add a byte-identical golden gate for the shell binding using trycmd/insta snapshots

### AGY-1173 -- Route mios-env-snapshot through mios-resolver while keeping the lossless baseline byte-identical  (WS-RESOLVER | P2 | S) **[DONE]**
**Goal:** E-08 Global Unification Plan -- the compiled resolver becomes the source of the very lossless-diff baseline the GUP gates against.
**What+How:** `usr/libexec/mios/mios-env-snapshot` resolves the full layered SSOT under `env -i`/`LC_ALL=C` and sorts `MIOS_*` for the lossless-diff gate, feeding `check_resolved_env_lossless` via `automation/lib/mios_var_closure.py`. Point it at `mios-resolver --emit=shell` when the binary is present, falling back to sourcing userenv.sh, preserving the exact isolation (`HOME=/nonexistent`, XDG pins, the `MIOS_ROOT`/`MIOS_TOML` meta-key exclusion grep). Since the gate compares before/after snapshots this must be a no-op diff; confirm the closure emitter still reports the same emitted count.
**Where:** `usr/libexec/mios/mios-env-snapshot, automation/lib/mios_var_closure.py, tools/native/mios-resolver/src/main.rs`
**Done When:** mios-env-snapshot output is byte-identical whether it sources userenv.sh or calls mios-resolver (empty diff); `check_resolved_env_lossless` green; the fallback still works with the binary absent.
**Why:** If the oracle for losslessness keeps resolving through bash while consumers move to the binary, the GUP gate stops measuring what actually ships.
**Dep:** Add a byte-identical golden gate for the shell binding using trycmd/insta snapshots

### AGY-1174 -- Characterize every PowerShell entry point against the generated globals module  (WS-RESOLVER | P2 | S) **[DONE]**
**Goal:** E-03 PowerShell surface -- the Windows leg of the strangler completes with the dot-source façade unchanged for every consumer.
**What+How:** After the generated globals module exists, verify the PS consumers that dot-source `automation/lib/globals.ps1` — `build-mios.ps1`, `usr/libexec/mios/mios-oscontrol-server.ps1` and any other `. globals.ps1` site found by grep — still see identical `$script:MIOS_*` values. No consumer edits should be needed, but characterize each entry point by dumping its resolved `MIOS_*` set before and after the demotion and diffing. Fix any consumer that reached into a globals.ps1-only local (non-`MIOS_`) variable.
**Where:** `build-mios.ps1, usr/libexec/mios/mios-oscontrol-server.ps1, automation/lib/globals.ps1`
**Done When:** Each PS entry point's captured `MIOS_*` dump diffs empty before vs after the demotion; no consumer references a removed globals.ps1 internal; the Windows drift-gate is green.
**Why:** globals.ps1 carried non-`MIOS_` internals as well as constants; swapping its body without characterizing consumers can break `build-mios.ps1` or the oscontrol server at runtime with no gate to catch it.
**Dep:** Generate globals.generated.ps1 from mios-resolver and demote globals.ps1 hardcodes to the generated module

### AGY-1175 -- Collapse the userenv.sh byte-mirror to one canonical shim and shrink or retire check 27  (WS-RESOLVER | P2 | S) **[DONE]**
**Goal:** E-01 Compiled native tier -- the payoff of the cutover: two hand-maintained bash copies and the gate policing them both disappear.
**What+How:** `check_userenv_parity` (check 27, 98-drift-checks.sh ~1757) exists solely because `tools/lib/userenv.sh` is byte-mirrored to `usr/lib/mios/userenv.sh` (`59-tools.sh` installs the former to the latter). Once both are the same thin `eval "$(mios-resolver --emit=shell)"` shim, install ONE canonical copy from a single source — have `59-tools.sh` install `tools/lib/userenv.sh` and delete the committed `usr/lib/mios/userenv.sh` — or keep both and shrink the parity check to the shim. Update the drift-check accordingly, stage exact paths, and mirror in mios-bootstrap.git (Law 15).
**Where:** `tools/lib/userenv.sh, usr/lib/mios/userenv.sh, automation/59-tools.sh, automation/98-drift-checks.sh (mios-bootstrap.git mirror)`
**Done When:** A single canonical userenv.sh shim is installed at build with no hand-maintained byte-mirror, or check_userenv_parity is reduced to the shim; the build install path is verified; the change is mirrored in both repos.
**Why:** Once the logic lives in the binary, maintaining two byte-identical bash files plus a check to police them is pure carrying cost that also makes every future edit a two-repo, two-file operation.
**Dep:** Emit the shell binding from mios-resolver and demote userenv.sh's Python heredoc to a thin shim

### AGY-1176 -- Delete the embedded Python heredoc from userenv.sh once the parity gates are green  (WS-RESOLVER | P2 | S) **[DONE]**
**Goal:** E-01 Compiled native tier -- the strangler's final deletion: the legacy slice is removed only after its replacement is mechanically proven.
**What+How:** Once `mios-resolver --emit=shell` is drift-gated byte-identical, the differential proptest is green, and the binary is guaranteed present in the built image (cargo-built and installed by an automation phase), remove the ~160-line Python heredoc (`_mios_load_unified` body, lines 86-249) from userenv.sh, leaving only the `eval "$(mios-resolver --emit=shell)"` invocation plus the pgvector post-step and legacy-file compat. Make a missing binary a hard error rather than silently resolving nothing — fail-closed for the resolver, degrade-open only for optional overlays. Mirror to both repos.
**Where:** `tools/lib/userenv.sh, usr/lib/mios/userenv.sh (mios-bootstrap.git mirror), automation phase that installs the binary`
**Done When:** userenv.sh embeds no Python resolver; sourcing it with the binary present gives an identical env (snapshot diff empty); the missing-binary path errors loudly; shell-equivalence and differential gates green; mirrored in both repos.
**Why:** A retained fallback that silently resolves an older, divergent view of SSOT is worse than an error — and leaving it forever means the duplication this campaign removed is only ever half-removed.
**Dep:** Add a proptest differential gate proving mios-resolver == mios_toml.py == userenv.sh on random valid mios.toml

### AGY-1177 -- Add an oxidizr-style [migration] SSOT table for per-surface Rust/bash resolver selection  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-11 Unified config surface -- rollback becomes an operator-defined mios.toml value rather than a build revert.
**What+How:** Following the ships-inert-until-wired convention (`db_backed`) and the oxidizr non-destructive-swap playbook, add a `[migration]` table to `usr/share/mios/mios.toml` with per-surface booleans (`use_rust_resolver_shell`, `_powershell`, `_python`, `_install_env`), defaulting true only on already-proven surfaces. Have each shim — userenv.sh, globals.ps1, mios_toml.py, system-sync-env.sh — read its toggle from the resolved SSOT and choose binary vs legacy accordingly. Add the keys to referenced_names.txt generation and a drift-check asserting the toggles are actually honored. Mirror the mios.toml addition in mios-bootstrap.git (Law 15).
**Where:** `usr/share/mios/mios.toml, tools/lib/userenv.sh, usr/lib/mios/userenv.sh, automation/lib/globals.ps1, usr/lib/mios/mios_toml.py, usr/libexec/mios/system-sync-env.sh (mios-bootstrap.git mirror)`
**Done When:** The `[migration]` toggles resolve through SSOT and each shim honors its flag (flip one off, confirm the legacy path runs); the default posture matches the proven surfaces; the keys appear in referenced_names.txt; the mios.toml change is mirrored in both repos.
**Why:** Without a per-surface switch, one regressing surface forces a whole-image revert — and the bake stays red while the remaining surfaces stabilize.
**Dep:** Emit the shell binding from mios-resolver and demote userenv.sh's Python heredoc to a thin shim

### AGY-1178 -- Gate the new crate dependency surface with cargo-deny and cargo-audit  (WS-RESOLVER | P1 | S) **[DONE]**
**Goal:** E-05 Test and CI governance -- the compiled tier gets the supply-chain story bash never needed, mirroring the OS-plane cosign gates.
**What+How:** The resolver crate pulls figment/toml/serde/miette/thiserror/proptest — a dependency surface bash never had. Commit a `deny.toml` encoding the license allow-list (operationalizing the FOSS posture), banned- and duplicate-crate rules, and RustSec advisory policy, then wire `cargo deny --manifest-path tools/native/Cargo.toml check` and `cargo audit` as new drift-checks alongside the existing NO-HARDCODE/pod-quadlets gates, honoring the negatives-before-main-gate ordering. `tools/native/Cargo.lock` (already committed) becomes the reproducibility SSOT feeding SBOM-not-hardcode. Keep the checks soft while the tools are missing in CI, then flip them gating.
**Where:** `tools/native/deny.toml (new), tools/native/Cargo.lock, automation/98-drift-checks.sh, .github/workflows/mios-ci.yml`
**Done When:** `cargo deny check` and `cargo audit` run in CI as drift-checks; deny.toml encodes the license allow-list; a planted advisory or banned crate fails the gate; Cargo.lock is committed and referenced by the SBOM step.
**Why:** Every crate added to the resolver is unreviewed third-party code shipped into the bootc image with no advisory or license check between it and the operator's machine.
**Dep:** Design the mios-resolver crate: typed layered model + figment providers + thiserror/miette error surface

### AGY-1179 -- Wire miette source-span diagnostics so a bad [colors]/[ports] value points at the mios.toml line  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- the typed figment model pays off as compiler-grade operator diagnostics instead of opaque bash errors.
**What+How:** Use the thiserror/miette scaffolding from the crate skeleton: when figment deserialization or a domain validation fails — a non-hex `[colors]` value, a non-integer `[ports].x`, a malformed `[image.sidecars]` `name:tag` — attach a miette source span (byte offsets, not char counts, so UTF-8 highlights correctly) into the specific mios.toml LAYER file and emit a diagnostic with help text naming the offending SSOT key. Return `miette::Result` from `main` so `mios-resolver --check-parse` prints the highlighted report. Add malformed fixtures asserting the diagnostic names the right `[sect].key` and points at the right layer path.
**Where:** `tools/native/mios-resolver/src/error.rs, tools/native/mios-resolver/src/model.rs, tools/native/mios-resolver/src/main.rs`
**Done When:** A malformed `[colors]`/`[ports]`/`[image.sidecars]` value produces a miette diagnostic with a correct byte-offset span into the offending layer file plus actionable help; a fixture covers each case; `mios-resolver --check-parse` exits nonzero with the report.
**Why:** Today a typo in a layered mios.toml surfaces as an unattributed resolver failure, and the operator has to bisect three layer files by hand to find which one they broke.
**Dep:** Design the mios-resolver crate: typed layered model + figment providers + thiserror/miette error surface

### AGY-1180 -- Add a cargo xtask for cross-platform build+install+regen, replacing scattered raw-cargo invocations  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-05 Test and CI governance -- one consolidated, shell-free workspace surface builds and regenerates everything the compiled tier owns.
**What+How:** Drift-checks and build phases invoke `cargo build --manifest-path tools/native/Cargo.toml -p <crate>` ad hoc today (e.g. `check_ssot_lint_equivalence`). Add an xtask crate at `tools/native/xtask`, invoked through a `.cargo/config.toml` alias, with a type-checked `build-resolver` task that compiles mios-resolver in release mode and installs it to the image path the automation phase expects plus the Windows path the PS surface expects, and a `regen` task that runs every `--emit=*` generator and stages the exact output files. Repoint the relevant automation phase and drift-checks at the xtask instead of raw cargo.
**Where:** `tools/native/xtask/ (new), tools/native/.cargo/config.toml (new), automation/59-tools.sh (or the native-build phase), automation/98-drift-checks.sh`
**Done When:** `cargo xtask build-resolver` compiles and installs mios-resolver on both Linux and Windows; `cargo xtask regen` reproduces every generated artifact with no diff; the ad-hoc `cargo build -p` calls in drift-checks route through the xtask; `just drift-gate` green.
**Why:** Scattered raw-cargo glue is where the two build scripts diverge and where a Windows path silently differs from a Linux one — the exact hazard the launcher-unification work exists to remove.
**Dep:** Generate referenced_names.txt + names.generated.txt from mios-resolver and retire generate-names-registry's forked alias_for

### AGY-1181 -- Author the unified-resolver ADR and populate the WS-RESOLVER roadmap section  (WS-RESOLVER | P2 | S) **[DONE]**
**Goal:** E-06 Test and documentation harness -- the three-resolvers-to-one decision is recorded and cross-linked so an arriving agent picks the campaign up cold.
**What+How:** Write an ADR (next number after ADR-0012) recording the decision to collapse the three parallel resolvers (mios_toml.py, the userenv.sh twins, globals.ps1) into the compiled mios-resolver crate with figment layered providers; the strangler-fig cutover ORDER (shell -> PS -> python -> install.env -> names); the oxidizr `[migration]` toggle rollback model; and the parity/differential fitness functions that gate it. Populate the barely-there WS-RESOLVER section of `ROADMAP.md` with this task set, cross-linking ADR-0011 (languages), ADR-0003 (SBOM-not-hardcode) and Laws 13/15, and naming checks 27/28/45/52 as the duplication being eliminated.
**Where:** `docs/adr/ (new ADR file), ROADMAP.md`
**Done When:** The ADR is merged describing the unified resolver, cutover order, rollback toggle and parity gates; ROADMAP.md's WS-RESOLVER section lists the tasks and cross-links the relevant ADRs and Laws; the superseded checks are named.
**Why:** Without the record, the rationale for retiring four long-standing drift-checks lives only in this batch, and the next agent re-adds the resolver duplication it removed.
**Dep:** Design the mios-resolver crate: typed layered model + figment providers + thiserror/miette error surface

### AGY-1182 -- Generate automation/lib/globals.sh from the resolver and extend checks 28/52 to the shell twin  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-08 Global Unification Plan -- the build-time shell resolver derives its ports/images/URLs from the single declaration instead of carrying its own.
**What+How:** `automation/lib/globals.sh` (292 lines) is the shell sibling of globals.ps1 and a FOURTH hardcoded resolver the task list ignored: it is dot-sourced in the pre-userenv bootstrap context by `automation/44-firewall-ports.sh`, `45-firewall.sh`, `46-sshd-port.sh` and `automation/lib/common.sh`, carrying its own `MIOS_PORT_*`/image/URL literals. Generate it from the resolver so the `${VAR:-LIT}` fallbacks come from SSOT, and extend `check_globals_ports` and `check_globals_image_parity` (98-drift-checks.sh ~1829/~1894), which already gate both globals files, to assert the generated shell module. LANDED DIFFERENTLY AND BETTER: `automation/lib/globals.sh` IS the generated artefact (no `.generated` sidecar, no shim), rendered by the same `tools/render-globals.py` pass as its PowerShell sibling so the two cannot diverge; checks 28 and 52 still pass because the renderer deliberately emits `: "${VAR:=value}"` and bare `else { <digits> }`, the exact shapes those gates parse; values carrying `}` or a quote fall back to `[ -n "${VAR+x}" ] ||`, and `.gitattributes` EOLs (`*.sh` LF, `*.ps1` CRLF) are honoured per file.
**Where:** `automation/lib/globals.sh, automation/98-drift-checks.sh, automation/44-firewall-ports.sh, automation/45-firewall.sh, automation/46-sshd-port.sh`
**Done When:** globals.sh is reproduced byte-identically by a fresh render; checks 28/52 assert the generated shell module against mios.toml `[ports]`/`[image]`; the firewall phases still resolve their ports; `just drift-gate` green.
**Why:** The PowerShell-only migration left the resolver the build-time firewall phases actually use unmigrated, so the duplication the operator flagged would only half-collapse and the build could open ports SSOT never declared.
**Dep:** Generate globals.generated.ps1 from mios-resolver and demote globals.ps1 hardcodes to the generated module

### AGY-1183 -- Route agent-pipe kernel/config.py's re-rolled TOML readers through the resolver shim  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-02 Technical-debt retirement -- the largest duplicate resolver, sitting in the hottest request path, is folded into the shared one.
**What+How:** `usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py` independently re-rolls the layered read (`_toml_section`, `_cfg_num`, `_dispatch_toml`/`_DISPATCH_TOML` with a lazy `import tomllib` around line 523) instead of calling `mios_toml.load_merged` — one of the ~13 independently re-rolled tomllib copies the crate exists to collapse. `gateway-agent/server.py` re-imports these names verbatim on the hot request path, and the one-way boundary enforced by check 6 must stay intact. Add characterization tests capturing current `[section]`/`[dispatch]` resolution, then replace the readers with calls into the mios_toml shim (which fronts the crate), preserving the module-level constant surface byte-for-byte.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py, usr/lib/mios/gateway-agent/server.py, automation/98-drift-checks.sh (check 6)`
**Done When:** config.py has no private `deep_merge`/tomllib read path; server.py's imported surface diffs zero; agent-pipe tests and the drift-gate are green.
**Why:** Fronting only the mios_toml public API leaves agent-pipe resolving SSOT its own way, so the AI plane can read a different config than the rest of the OS on the same host.
**Dep:** Add a Python subprocess/JSON binding and demote mios_toml.py public API to a thin shim over mios-resolver

### AGY-1184 -- Reconcile the generated workspace member list and gate generated-vs-committed Cargo.toml  (WS-RESOLVER | P2 | S) **[DONE]**
**Goal:** E-05 Test and CI governance -- one consolidated Cargo workspace whose membership is generator-owned and gated, not hand-edited.
**What+How:** `tools/native/Cargo.toml` is GENERATED by `tools/generate-cargo-manifests.py`, yet the committed file lists 3 members (mios-drift-runner, mios-version-check, mios-wallpaperd) while the generator emits a DIFFERENT 7-member literal that OMITS mios-drift-runner — so any hand-registered crate is clobbered on the next regen. Add mios-resolver, the 5 orphaned crates and mios-drift-runner to the generator's member list, and add a drift-check (e.g. `check_cargo_manifest_generated`) asserting that regenerating `tools/native/Cargo.toml` yields zero diff against the committed file, staging explicit paths.
**Where:** `tools/generate-cargo-manifests.py, tools/native/Cargo.toml, automation/98-drift-checks.sh`
**Done When:** Generator members equal committed members including mios-resolver and mios-drift-runner; the new drift-check fails on any regen diff; the drift-gate is green.
**Why:** Crates registered by hand are silently reverted on the next regeneration, and the generator currently drops mios-drift-runner entirely — a live drift no gate catches.
**Dep:** Register the 5 orphaned native crates as explicit workspace members and add a fmt/clippy build-gate

### AGY-1185 -- Add a bake phase that compiles the native workspace and installs mios-resolver into the image  (WS-RESOLVER | P1 | M) **[DONE]**
**Goal:** E-16 The bake plane -- the compiled resolver is actually PRESENT in every published image, so the shims have something to delegate to.
**What+How:** No automation phase compiles Rust into the bootc image today — only `98-drift-checks.sh` references cargo — and native tools ship as prebuilt binaries while `85-bake-plan.sh` selects native -> `tools/native/target/release` -> python at runtime (lines 23-33). Add `automation/NN-native-build.sh` running `cargo build --release` for the workspace and installing mios-resolver and its siblings to `/usr/libexec/mios` during bake, staging explicit paths, and wire userenv.sh and the globals surfaces to prefer the installed binary and degrade-open to the retained bash/python resolver when it is absent, mirroring the mios-bake-plan fallback pattern.
**Where:** `new automation/NN-native-build.sh, automation/85-bake-plan.sh (degrade pattern to mirror), usr/libexec/mios/`
**Done When:** The bake installs `/usr/libexec/mios/mios-resolver`; consumers use it when present and fall back when absent; bake-green and drift-gate green.
**Why:** Every migration in this workstream terminates at a binary nothing currently builds into the image — without this phase the crate is unreachable at runtime and every shim permanently takes its legacy branch.
**Dep:** Add a cargo xtask to build+install the resolver binary cross-platform, replacing scattered cargo invocations

### AGY-1186 -- Port the post-resolve derived transforms (loopback-bool bind address and composed *_URL defaults) into mios-resolver  (WS-RESOLVER | P1 | M) **[DONE]**
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one compiled resolver emits the COMPLETE env projection, including the values the Python resolver never produced, so no shell post-processing survives the port.
**What+How:** `process_val()` only covers stack_offset / port!=53 / bool / CSV, so the values computed AFTER the python resolver are invisible to it: `usr/lib/mios/userenv.sh:258-261` maps `[pgvector].listen_loopback` (bool) to `MIOS_PG_BIND_ADDR` = 127.0.0.1 or 0.0.0.0, and `automation/lib/globals.ps1:116-132` plus `automation/lib/globals.sh` compose the `*_URL` defaults (e.g. `http://localhost:$MIOS_PORT_HERMES/v1`, the pgvector DSN) from already-resolved port values. Model both classes as typed derived-value rules inside the `mios-resolver` crate — a bool→address rule and a port→URL composition rule — so the single binary emits them, and keep the degrade-open fallbacks (loopback-safe bind address, localhost URLs) when the source key is absent.
**Where:** `usr/lib/mios/userenv.sh:251-261, automation/lib/globals.ps1:116-132, automation/lib/globals.sh, tools/native/mios-resolver`
**Done When:** the crate emits `MIOS_PG_BIND_ADDR` and every composed `*_URL` byte-identical to today's shell/PowerShell post-load steps, and the differential test against the captured output is green.
**Why:** A naive port ships a resolver that silently drops the pgvector bind hardening (exposing the DB on 0.0.0.0) and every service URL, breaking Quadlets and firstboot with no gate red to warn anyone.
**Dep:** Port walk() + process_val() value transforms (stack_offset, port!=53, bool, CSV) with characterization tests

### AGY-1187 -- Port float_allowlist() / [build.float] into mios-resolver and repoint mios-version-lint off mios_toml  (WS-RESOLVER | P1 | S) **[DONE]**
**Goal:** E-14 Float latest globally -- the `[build.float]` allowlist stays the ONE place a legitimately-pinned build input may live, and its enforcing lint keeps working after the resolver is demoted to a shim.
**What+How:** `mios_toml.float_allowlist()` (`usr/lib/mios/mios_toml.py:218-222`) resolves the `[build.float]` table from the vendor-only view and has exactly one real consumer, `usr/libexec/mios/mios-version-lint` — it is a PUBLIC resolver API that the shim-demotion tasks never enumerate. Add an equivalent accessor to the `mios-resolver` crate, expose it through the Python shim passthrough so the call signature is unchanged, and repoint `mios-version-lint` to read the allowlist via the shim/binary rather than importing `mios_toml` directly.
**Where:** `usr/lib/mios/mios_toml.py:218-222, usr/libexec/mios/mios-version-lint, tools/native/mios-resolver`
**Done When:** the crate and the shim both expose `float_allowlist`, `mios-version-lint` reads it via the shim, and both the version-lint check and `just drift-gate` are green.
**Why:** Today the demotion plan would remove a public entry point that a live gate depends on, so the version-lint gate goes red (or worse, silently allows a hand-pinned version) the moment the shim lands.
**Dep:** Add a Python subprocess/JSON binding and demote mios_toml.py public API to a thin shim over mios-resolver

### AGY-1188 -- Preserve or explicitly retire userenv.sh's legacy split-file reader and the _ssot_lint_ports_dummy whitelist before the heredoc is deleted  (WS-RESOLVER | P1 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- the bash resolver is deleted only after every block inside it is reproduced, migrated, or ADR-retired, so the strangler-fig cut loses no behaviour.
**What+How:** The heredoc-deletion task removes two unrelated blocks that ride along in the same file: (1) the legacy split-file reader at `usr/lib/mios/userenv.sh:263-309` that still parses `env.toml` / `images.toml` / `build.toml` / `flatpaks.list` / `env` for pre-unified deployments driven by `just init-user-space`, and (2) `_ssot_lint_ports_dummy` at lines 311-325, a static-analysis whitelist that `automation/97-ssot-lint.sh` scans so it does not report false "undefined MIOS_PORT_*" hits. Decide and implement both: fold the legacy reader into the crate OR retire it with an ADR carrying a migration note, and regenerate the ssot-lint port whitelist FROM the crate's emitted port set instead of maintaining it as a hand-written list.
**Where:** `usr/lib/mios/userenv.sh:263-325, automation/97-ssot-lint.sh, tools/lib/userenv.sh`
**Done When:** the legacy path is either crate-reproduced or ADR-retired with a migration note, the ssot-lint port whitelist is generated rather than hand-listed, and `97-ssot-lint.sh` plus the drift-gate are green.
**Why:** Cutting the heredoc as-is silently breaks backward compat for legacy split-file installs and re-opens the 97-ssot-lint false-positive class that the dummy whitelist was written to close.
**Dep:** Delete the embedded Python heredoc fallback from userenv.sh after the crate is proven in CI+bake

### AGY-1189 -- Rebind the R-subset-of-E var-closure gate (check 37) and the names.generated.txt emitter set to the resolver crate output  (WS-RESOLVER | P1 | M) **[DONE]**
**Goal:** E-08 Global Unification Plan -- the 767-to-~240 key minification keeps its safety net when the resolver it measures becomes a binary.
**What+How:** `automation/lib/mios_var_closure.py:emitted_set()` shells out to the `userenv.sh` resolver to collect every emitted `MIOS_*` name — that set is the whole basis of check 37 (referenced-set must be a subset of emitted-set) — and its `EMITTER_SUFFIXES` list enumerates `userenv.sh`, `tools/lib/userenv.sh`, `system-sync-env.sh` and `names.generated.txt` as emitters-not-consumers. Once the shims delegate to the binary, repoint `emitted_set()` at the shim/binary so it still captures the FULL emitted set, add the resolver binary path to `EMITTER_SUFFIXES`, and make `usr/share/mios/names.generated.txt` a crate-generated artifact.
**Where:** `automation/lib/mios_var_closure.py, usr/share/mios/names.generated.txt, usr/libexec/mios/system-sync-env.sh`
**Done When:** check 37 collects the emitted set via the crate/shim and computes an identical closure result, `names.generated.txt` is crate-generated, and the drift-gate is green.
**Why:** If the shim's emitted set differs by even one name the closure gate either false-fails and blocks every merge, or false-passes and orphans a consumer referencing a variable nothing emits.
**Dep:** Emit the shell binding from mios-resolver and demote userenv.sh's Python heredoc to a thin shim

### AGY-1190 -- Repoint the referenced_names staleness gate at the Rust generator and delete tools/generate-names-registry.py  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- one generator owns the alias map, ending the three-way fork behind the names registry.
**What+How:** The referenced_names/names staleness check in `automation/98-drift-checks.sh` (~lines 2093-2141) regenerates the registry using the PYTHON `tools/generate-names-registry.py`, while a Rust `generate-names-registry` crate already exists and `mios_toml.get_aliases` carries a third copy of the alias map. After the crate owns `get_aliases`, switch the staleness check to invoke the native generator (or the resolver binary directly), then delete `tools/generate-names-registry.py`, staging the explicit paths rather than a broad `git add`.
**Where:** `tools/generate-names-registry.py, tools/native/generate-names-registry, automation/98-drift-checks.sh (~2093-2141)`
**Done When:** the staleness check invokes the native generator, `tools/generate-names-registry.py` no longer exists in the tree (deletion staged), `referenced_names.txt` and `names.generated.txt` regenerate byte-identical, and the drift-gate is green.
**Why:** Retiring the Rust crate's forked `alias_for` while leaving the Python generator alive keeps the fork the gate ACTUALLY runs, so the alias map can still drift between three implementations.
**Dep:** Generate referenced_names.txt + names.generated.txt from mios-resolver and retire generate-names-registry's forked alias_for

### AGY-1191 -- Implement MSYS/Windows drive-letter path normalization in mios-resolver matching userenv.sh normalize_path  (WS-RESOLVER | P2 | M) **[DONE]**
**Goal:** E-08 Global Unification Plan -- the lossless env-snapshot baseline holds byte-identically whether it is captured on the Windows host or the Linux CI runner.
**What+How:** The `userenv.sh` heredoc normalizes `/c/...` to `C:/...` on win32 (lines 126-135) and `automation/lib/globals.ps1` resolves the Windows-side layer paths; the crate must reproduce that or layer paths diverge by host. Add a `normalize_path` equivalent in `mios-resolver`, gated on `target_os` or an explicit flag rather than inferred from the input string, and cover it with a unit test over `/c/`-style MSYS inputs, `C:\` drive-letter inputs and plain POSIX inputs.
**Where:** `usr/lib/mios/userenv.sh:126-135, automation/lib/globals.ps1, tools/native/mios-resolver`
**Done When:** the crate emits identical normalized layer paths on win32 and linux, the unit test covers all three input forms, and the env-snapshot lossless gate is green cross-host.
**Why:** The GUP lossless-diff gate deliberately normalizes root paths; a crate that skips MSYS drive-letter normalization fails env-snapshot parity the first time it is driven from the Windows `build-mios.ps1` path.
**Dep:** Port the tier/layer discovery (layer_paths + _frags + _tier_dirs) into mios-resolver

### AGY-1192 -- Build the golden characterization corpus of layered mios.toml overlays capturing all three resolvers' current output  (WS-RESOLVER | P1 | L) **[DONE]**
**Goal:** E-01 Compiled native tier -- the strangler-fig rule that byte-parity is PROVEN before any shell is deleted gets the captured pre-migration baseline it currently lacks.
**What+How:** Every byte-identical / insta / proptest gate task in this campaign assumes committed baselines exist; none do. Curate fixture overlays covering the real layering cases — vendor-only, vendor+host, +user, +`mios.d` fragments, an `[env]` verbatim table, and db-authoritative — and capture the CURRENT output of `mios_toml.py`, `userenv.sh` and `globals.{ps1,sh}` for each via `usr/libexec/mios/mios-env-snapshot` into committed fixture files under the crate's `tests/fixtures/`. This becomes the one shared characterization corpus every migration and gate task diffs against.
**Where:** `new tools/native/mios-resolver/tests/fixtures/, usr/libexec/mios/mios-env-snapshot, usr/share/mios/mios.toml`
**Done When:** the fixtures and their captured baselines are committed (staged explicitly), `cargo test` consumes them, and every downstream golden/proptest/insta gate references this corpus rather than inventing its own.
**Why:** Without a captured pre-migration baseline every equivalence claim in the resolver port is unfalsifiable — "byte-identical" has nothing to be identical TO, so a regression ships green.
**Dep:** Design the mios-resolver crate: typed layered model + figment providers + thiserror/miette error surface

### AGY-1193 -- Mirror the resolver shim and binary into mios-bootstrap.git per Law 15  (WS-RESOLVER | P1 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- the compiled resolver lands in BOTH publishing repos at once, so the installer never runs a stale forked copy.
**What+How:** `userenv.sh`, `/etc/mios/install.env` and the alias/name surfaces are shared SSOT that the bootstrap installer depends on, and `mios-bootstrap.git` ships its own copies of the resolver. Law 15 DOUBLE-REPO-TRIPLE-CHECK requires shared SSOT surfaces to update byte-identically in both trees, so land the demoted shim plus the resolver binary (or its build recipe) in `mios-bootstrap.git` as well, and add a cross-repo parity assertion — a manifest of shared files with hashes, or a check that compares the two copies.
**Where:** `mios.git usr/lib/mios/userenv.sh + tools/lib/userenv.sh, mios.git install.env producers, mios-bootstrap.git resolver copies`
**Done When:** the bootstrap repo carries the identical shim and binary/build recipe, a parity check or manifest asserts the two repos match, and both trees drift-gate green.
**Why:** Migrating only `mios.git` leaves the bootstrap installer resolving config with a stale fork — the exact cross-repo divergence Law 15 exists to prevent, and it surfaces as a mis-installed system, not a red gate.
**Dep:** Emit the shell binding from mios-resolver and demote userenv.sh's Python heredoc to a thin shim

### AGY-1194 -- Reconcile the two divergent EXCLUDED_SECTIONS semantics into one typed model in mios-ssot-walk  (WS-RESOLVER | P1 | M) **[DONE]**
**Goal:** E-10 One canonical name -- one confusable name stops standing for two different rules, so the walk's exclusion semantics are explicit and gated.
**What+How:** `usr/lib/mios/mios_toml.py:591` excludes `{containers, verbs, recipes, packages, dotfiles, btop, theme, install_phases, messages}` at WALK time (i.e. these sections are not emitted into the env), while `tools/native/mios-ssot-walk/src/lib.rs` excludes `{meta, laws}` for names-registry section-partitioning — two different sets serving two different roles under one name. Encode BOTH in the crate as distinct named typed constants (emission-exclusion vs partition-exclusion) so each consumer selects the right one, and add a differential test asserting `mios_toml.EXCLUDED_SECTIONS` equals the crate's emission-exclusion constant.
**Where:** `usr/lib/mios/mios_toml.py:591, tools/native/mios-ssot-walk/src/lib.rs`
**Done When:** the crate exposes both exclusion sets as named typed constants, the differential test asserting Python-set == crate emission-exclusion passes, and the drift-gate is green.
**Why:** The section-partition promotion task moves `meta`/`laws` and would conflate the two sets; a single merged constant makes the walk start emitting `containers`/`verbs` into the environment, polluting every consumer's env with structured tables.
**Dep:** Promote mios-ssot-walk from a 12-line stub to the single typed SSOT of section-partition constants

### AGY-1195 -- Add a bootstrap-ordering degrade-open guard so resolver consumers survive before the binary is installed  (WS-RESOLVER | P0 | M) **[DONE]**
**Goal:** E-01 Compiled native tier -- the shim degrades open, so replacing a sourced bash resolver with a binary can never brick a login shell or firstboot.
**What+How:** `etc/profile.d/mios-env.sh` sources `userenv.sh` on every login, and the firstboot producers (`usr/libexec/mios/mios-ai-firstboot`, `forge-firstboot.sh`, `usr/libexec/mios/mios-hermes-firstboot`) source `/etc/mios/install.env` — all in early-boot or pre-bake windows where `/usr/libexec/mios/mios-resolver` may legitimately not exist yet. Make the shim's binary-selection fall back to the retained bash/python path when the binary is absent, never fatal under `set -e`, and make that fallback INDEPENDENT of the `[migration]` toggle (the toggle expresses preference, not availability). Add a smoke test that a fresh image with no resolver binary still resolves the env.
**Where:** `etc/profile.d/mios-env.sh, usr/libexec/mios/mios-ai-firstboot, usr/libexec/mios/mios-hermes-firstboot, usr/lib/mios/userenv.sh`
**Done When:** sourcing the shim with the binary absent degrades to the bash path with no error, the firstboot smoke test passes on a binary-less image, and the drift-gate is green.
**Why:** The `[migration]` toggle picks a resolver but does not cover the chicken-and-egg where the binary is simply not there yet; a degrade-closed shim bricks every login shell and every firstboot unit on the first boot of a new image.
**Dep:** Add an oxidizr-style [migration] SSOT toggle for per-surface Rust/bash resolver selection

### AGY-1196 -- Establish the mios-drift lib crate + Check-trait harness in miosd, absorbing mios-drift-runner  (WS-DRIFTRUST | P1 | L) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the seam that lets 123 bash checks become native `miosd` checks without the gate ever going dark.
**What+How:** Create `src/mios-rs/miosd/src/drift/mod.rs` defining a `Check` trait (`fn id(&self)->&str`, `fn describe(&self)->&str`, `fn run(&self,ctx:&DriftCtx)->Verdict`), a `Registry` Vec, and `DriftCtx{root, soft, in_image, git_ok, incomplete_tree}`. Port the harness scaffolding from `automation/98-drift-checks.sh` lines 56-93 and 6198-6210 — `MIOS_DRIFT_CHECK_ROOT` resolution, `MIOS_DRIFT_CHECK_SOFT`, the `_violation` counter — and crucially the in-image skip semantics at lines 6180-6187 (`CTX` set OR `$ROOT == /tmp/build` implies in_image; non-empty `git ls-files --deleted` implies incomplete_tree) so ported checks skip identically. Move the two existing checks in `src/mios-rs/miosd/src/drift.rs` (SSOT-parse and `check_backfill_coverage`) into the registry as the first `Check` impls, then rewrite `tools/native/mios-drift-runner/src/main.rs` (today a pure `bash 98-drift-checks.sh` shim, lines 15-30) to exec `miosd drift-check`, keeping the bash exec path reachable behind `MIOS_DRIFT_LEGACY=1`.
**Where:** `src/mios-rs/miosd/src/drift/mod.rs, src/mios-rs/miosd/src/drift.rs, src/mios-rs/miosd/src/main.rs, tools/native/mios-drift-runner/src/main.rs, automation/98-drift-checks.sh`
**Done When:** `cargo build -p miosd` yields `miosd drift-check --root <tree>` running the two seeded checks through the Registry, `miosd drift-check --list` prints their ids and descriptions, `mios-drift-runner` delegates to miosd with bash reachable only via `MIOS_DRIFT_LEGACY=1`, and `just drift-gate` is still green.
**Why:** Without a façade that encodes the exact ROOT/soft/CTX skip semantics, every family port re-implements them slightly differently and the gate starts skipping (or failing) in the bake where nobody is watching.
**Dep:** none

### AGY-1197 -- Build the bash-vs-Rust golden-master + differential parity harness for drift checks  (WS-DRIFTRUST | P1 | L) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- no bash check is deleted until its Rust twin is proven to red and green in exactly the same situations.
**What+How:** Add `tests/drift-parity.sh` plus a `miosd drift-check --parity` mode that (1) runs `bash automation/98-drift-checks.sh` and parses each `[98-drift-checks] ...` PASS line and each `_violation` line into a verdict map keyed by check id, (2) runs the Rust Registry, and (3) asserts an identical PASS/FAIL verdict for every id defined on BOTH sides — on the clean tree AND on every mutation fixture. Use the 89 `test_*` functions already in `tests/drift-gate-negatives.sh` as the fault-injection oracle, so a ported check must red exactly when its bash twin reds. Add `insta` snapshots of the structured verdict Vec and a `trycmd` fixture at `tests/cmd/drift-check.trycmd` pinning stdout/stderr/exit-code, wire both into the `cargo test` job, and capture today's `usr/share/mios/reference/drift-gate-index.tsv` (123 dense ordinals) as the golden ordinal-to-description index that `--list` must reproduce.
**Where:** `tests/drift-parity.sh, src/mios-rs/miosd/src/drift/mod.rs, tests/cmd/drift-check.trycmd, tests/drift-gate-negatives.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** `bash tests/drift-parity.sh` exits 0 with every co-defined id agreeing bash-vs-Rust on the clean tree and on its negative fixture, `cargo insta test` and the trycmd fixture pass, and the parity harness runs as a `just drift-gate` step.
**Why:** The bash gate has no unit tests, so a rewrite without a differential oracle is a blind rewrite: a check that silently always-passes in Rust looks identical to a green gate and disables an invariant permanently.
**Dep:** INFRA-1 harness

### AGY-1198 -- Wire the 5 orphan native crates into the workspace and add clippy/fmt/deny/audit gates  (WS-DRIFTRUST | P1 | M) **[DONE]**
**Goal:** E-05 Test and CI governance -- the Rust plane gets the lint/supply-chain gate the shell plane never had, before the drift-gate is rewritten on top of it.
**What+How:** `tools/native/Cargo.toml` lists only 3 workspace members (`mios-drift-runner`, `mios-version-check`, `mios-wallpaperd`) while 5 more crates carry a `Cargo.toml` on disk and never build under `cargo build --workspace`: `generate-names-registry`, `mios-aiplane-lint`, `mios-bake-plan`, `mios-ssot-lint`, `mios-ssot-walk`. Add all five to `members`, commit `tools/native/Cargo.lock` and `src/mios-rs/Cargo.lock` as the reproducibility SSOT (ADR-0003 SBOM-not-hardcode), and add a `deny.toml` (advisories + license allow-list + bans) at both workspace roots. Add a `check_rust_quality` check to the Rust Registry that shells `cargo fmt --check`, `cargo clippy --workspace -- -D warnings` with `clippy::unwrap_used` / `clippy::panic` / `clippy::todo` denied in the drift crate, `cargo-deny check` and `cargo-audit` — degrade-open with a clear skip message when cargo is absent, mirroring the existing `command -v python3` guards.
**Where:** `tools/native/Cargo.toml, src/mios-rs/Cargo.toml, tools/native/Cargo.lock, src/mios-rs/Cargo.lock, tools/native/deny.toml, src/mios-rs/deny.toml, src/mios-rs/miosd/src/drift/mod.rs, Justfile`
**Done When:** `cargo build --workspace` builds all 8 native crates from both roots, `cargo clippy --workspace -- -D warnings` / `cargo fmt --check` / `cargo deny check` / `cargo audit` all pass, and `check_rust_quality` runs inside `miosd drift-check` and `just drift-gate`.
**Why:** Five crates the campaign is about to depend on (names/aiplane/bake/ssot-walk) are not compiled by any CI job today, so they can be broken for months undetected — and the compiled plane has no advisory or license story at all.
**Dep:** INFRA-1 harness

### AGY-1199 -- Add the [migration] SSOT toggle with oxidizr-style per-family Rust/bash opt-out and rollback  (WS-DRIFTRUST | P1 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- rollback is a first-class deliverable: any ported family reverts by flipping one operator-visible SSOT value, never a build revert.
**What+How:** Add a `[migration]` table to `usr/share/mios/mios.toml` with `use_rust_drift = true` and a per-family map `drift_families.<family> = "rust"|"bash"` (defaulting to rust once a family passes parity). Teach `miosd drift-check` to read it through the resolver and, per family, run either the Rust `Check` or the still-present bash `check_*` function — since sourcing `98-drift-checks.sh` is not side-effect-safe, add a `--only <fn>` dispatch shim inside the bash file that runs one named check. Both implementations keep shipping. Mirror the `[migration]` block into the bootstrap repo per Law 15 (both CIs consume it) and keep the literal default (`${...:-rust}`) so a missing key degrades to Rust.
**Where:** `usr/share/mios/mios.toml, /c/MiOS-bootstrap/usr/share/mios/mios.toml, src/mios-rs/miosd/src/drift/mod.rs, automation/98-drift-checks.sh`
**Done When:** setting `[migration].drift_families.ports = "bash"` makes `miosd drift-check` run the bash ports family and `"rust"` runs the Rust one, the parity harness is green under both settings, `[migration]` is byte-identical in both repos, and `check_law_enforcers` / gate-index are unaffected.
**Why:** Without a per-family switch, a single bad port can only be undone by reverting the build, which means a red gate blocks the whole tree while the fix is written.
**Dep:** INFRA-1 harness

### AGY-1200 -- Make the gate-index / gate-registry / negatives-ratchet meta-gates hybrid-aware  (WS-DRIFTRUST | P1 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- one global numbering system spans bash and Rust checks so the ordinal SSOT survives the migration.
**What+How:** Three self-referential gates assume every check is a bash function registered in `main()`: `check_gate_registry` (lines 4620-4691, parses `check_*(){` definitions against `main()` calls 1:1), `check_gate_index` (line 4917, runs `tools/generate-gate-index.py --check` to regenerate `drift-gate-index.tsv`), and `check_negative_test_coverage` (lines 4769-4852, requires a `test_*` per listed check in `tests/drift-gate-negatives.sh`). Porting a check to Rust deletes its bash function and false-fails all three. Extend `tools/generate-gate-index.py` to merge the bash `main()` registration with `miosd drift-check --list` (id + description) into ONE dense 1..N ordinal SSOT, and update the three checks to consume that union so a Rust-only check keeps its ordinal and still requires a negative test. `drift-gate-index.tsv` is a shared SSOT surface, so mirror the regenerated file into the bootstrap repo (Law 15).
**Where:** `tools/generate-gate-index.py, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, usr/share/mios/reference/drift-gate-index.tsv, /c/MiOS-bootstrap/usr/share/mios/reference/drift-gate-index.tsv, src/mios-rs/miosd/src/drift/mod.rs`
**Done When:** after moving a check to Rust and deleting its bash function, `check_gate_registry`, `check_gate_index` and `check_negative_test_coverage` stay green, `drift-gate-index.tsv` remains dense 1..N with the Rust check present, and both repos hold the identical index.
**Why:** Until the meta-gates span the hybrid population, the very FIRST port turns the gate red and blocks every subsequent migration task in the workstream.
**Dep:** INFRA-1 harness

### AGY-1201 -- Port the SSOT regenerate-and-diff runner abstraction to Rust  (WS-DRIFTRUST | P1 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- one compiled engine backs ~20 projection checks instead of 20 near-identical bash blobs.
**What+How:** Nearly every projection check has one shape: run a `tools/generate-*.py` generator in a CLEAN env and diff its output against the committed artifact (see `check_pod_quadlets` lines 896-897, `env -i PATH=... MIOS_ROOT=$ROOT python3 gen --check`, and `check_egress_firewall` lines 918-925, render-to-tmp then `diff -q`). Implement `drift::regen::regen_and_diff(gen_relpath, targets, clean_env)` in `src/mios-rs/miosd/src/drift/regen.rs` reproducing the `env -i PATH/HOME/LANG` isolation exactly — build.sh's sourced userenv leaks ~200 `MIOS_*` that bake configured values in and cause false STALE drift — executing the generator via `std::process::Command` and diffing. Also port `_emit_projection_evidence` (lines 107-210: snapshot targets, re-render in write mode, unified diff capped at 200 lines, restore the tree byte-for-byte, net read-only) as `regen::emit_evidence` so failure output stays greppable on `[98-drift-checks][diff]`.
**Where:** `src/mios-rs/miosd/src/drift/regen.rs, src/mios-rs/miosd/src/drift/mod.rs, automation/98-drift-checks.sh`
**Done When:** `regen_and_diff` reproduces `check_egress_firewall`'s verdict on the clean tree and on a mutated `egress.nft` fixture, `emit_evidence` output byte-matches the bash `[98-drift-checks][diff]` block for a forced drift, and both are unit-tested with `insta`.
**Why:** Porting 20 projection checks one-by-one without this shared runner re-implements the clean-env isolation 20 times, and any copy that gets it wrong reproduces the recurring quadlet-digest false-drift.
**Dep:** INFRA-1 harness

### AGY-1202 -- Port the heavy regen-and-diff projections: pod-quadlets, egress-firewall, blade-dropins  (WS-DRIFTRUST | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the highest-churn SSOT projections are gated by compiled, clean-env diffs.
**What+How:** Port `check_pod_quadlets` (line 872, `tools/generate-pod-quadlets.py --check` rendering `.pod/.container/.network/.volume` from `[pods.*]`), `check_egress_firewall` (line 904, `tools/generate-egress-firewall.py` vs `usr/share/mios/security/egress.nft` from `[security.egress]`) and `check_blade_dropins` (line 929, `[blade.requires]`) to Rust `Check` impls that call `regen::regen_and_diff`. Preserve the `env -i` clean-env determinism. Prove parity via `tests/drift-parity.sh` against each check's negative fixture, then delete the three bash functions and their `main()` registrations and regenerate `drift-gate-index.tsv`.
**Where:** `src/mios-rs/miosd/src/drift/projections.rs, automation/98-drift-checks.sh, tools/generate-pod-quadlets.py, tools/generate-egress-firewall.py, usr/share/mios/security/egress.nft, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** the Rust checks reproduce all three verdicts on the clean tree and on the negative fixtures, the three bash functions are gone, `check_gate_registry` / `check_gate_index` / the negatives ratchet stay green, and `just drift-gate` is green.
**Why:** Quadlet and egress generation is the exact surface where a broad `git add` strips `@sha256` pins and reds the gate for reasons unrelated to the change under review, costing an investigation every time.
**Dep:** A1 regen runner, INFRA-5 hybrid registry

### AGY-1203 -- Port the nine small render-and-diff SSOT projections to Rust  (WS-DRIFTRUST | P2 | L) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the derive-don't-duplicate fitness functions for the host-config surfaces run on the shared compiled runner.
**What+How:** Port to Rust `Check` impls over `regen::regen_and_diff`: `check_kargs_projection` (line 3223, `[kargs]`), `check_chrony_projection` (3315), `check_nut_projection` (3340), `check_toml_projection` (3791), `check_ipa_enroll_projection` (5355), `check_uki_cmdline_projection` (5367, the enforcing=1/LUKS cmdline baked into the signed UKI), `check_composefs_projection` (5379), `check_cockpit_projection` (5409) and `check_chrony_ptp_dropin` (5421). Each names its generator plus its committed target; keep the degrade-open `command -v python3` skip as a Rust `which` guard. Snapshot each clean-tree verdict first, port, prove parity on the matching negative fixtures, then delete the nine bash functions and re-register.
**Where:** `src/mios-rs/miosd/src/drift/projections.rs, automation/98-drift-checks.sh, tools/, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** all nine Rust checks match the bash verdicts on the clean tree and on negatives, the nine bash functions are deleted, the index stays dense, the ratchet still finds a negative test per check, and `just drift-gate` is green.
**Why:** Nine near-identical Python-in-bash blobs each carry their own copy of the render/diff/skip logic, so a fix to one (e.g. the python3 guard) has to be made nine times and usually is not.
**Dep:** A1 regen runner, INFRA-5 hybrid registry

### AGY-1204 -- Port the projection-registry integrity + renderer/dotfiles coverage meta-checks  (WS-DRIFTRUST | P2 | M) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- Law 8's projection registry stays the meta-fitness-function while its governed checks move to Rust.
**What+How:** Port `check_projection_registry` (line 5811, reads `[laws.projection_registry].surfaces` and asserts each `generator` exists on disk and each named `check` function exists in `98-drift-checks.sh`), `check_renderer_gate_coverage` (line 5470) and `check_dotfiles_projection` (line 1732, the `[dotfiles.registry.*]` theme/dotfiles surface, check 25) to Rust, parsing `mios.toml` through the `mios-ssot-walk` lib crate. Because the registry check verifies that named check functions are RESOLVABLE, change its resolution to consult the hybrid registry from INFRA-5 (bash functions in `main()` UNION `miosd drift-check --list`) so Rust-ported checks referenced in `[laws.projection_registry]` still resolve. Prove parity, delete the bash functions, re-register.
**Where:** `src/mios-rs/miosd/src/drift/registry_checks.rs, tools/native/mios-ssot-walk/src/lib.rs, automation/98-drift-checks.sh, usr/share/mios/mios.toml, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** Rust `check_projection_registry` resolves both bash and Rust check references and matches the bash verdict, the renderer and dotfiles coverage checks match on clean tree and negatives, the three bash functions are deleted, and the Law-8 registry is green.
**Why:** If the registry check cannot resolve a Rust-ported enforcer it reds on a check that exists and works, which reads as a Law 8 violation and stalls the port queue.
**Dep:** A1 regen runner, INFRA-5 hybrid registry

### AGY-1205 -- Port the Laws + filesystem-layout enforcement family to Rust  (WS-DRIFTRUST | P2 | L) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the law enforcers become type-checked code that can match law ids programmatically instead of by string-grep.
**What+How:** Port into a Rust `laws` module reading `mios.toml` via `mios-ssot-walk`: `check_law_enforcers` (line 5698, parses `[laws].laws[].enforced_by` `file:ref` targets and asserts each resolves in 98/99 — must consult the INFRA-5 hybrid registry so Rust-ported enforcers resolve), `check_quadlet_privilege` (line 2975 plus `_privileged_quadlet_array` at line 2962, the `[security.privileged_quadlets].root` 10-unit roster), `check_council_gate_ssot` (line 4427), `check_usr_over_etc` (line 5761), `check_etc_duplicates` (line 2545), `check_no_mkdir_in_var` (line 2940) and `check_var_closure` (line 3024). Prove parity against the negatives (`check_quadlet_privilege` and `check_no_mkdir_in_var` already have them), then delete the seven bash functions plus `_privileged_quadlet_array` and re-register.
**Where:** `src/mios-rs/miosd/src/drift/laws.rs, automation/98-drift-checks.sh, usr/share/mios/mios.toml, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** the seven Rust law/layout checks match the bash verdicts on clean tree and negatives, `enforced_by` resolves across the hybrid registry, the bash functions are deleted, Rust `check_law_enforcers` is green, and the index stays dense.
**Why:** The law enforcers are the non-bypassable audit trail for governance-as-code, and today they are bash string matching over a shell file — a renamed function silently unenforces a law.
**Dep:** INFRA-5 hybrid registry

### AGY-1206 -- Port the digest/pin/SBOM (SBOM-not-hardcode) check family to Rust  (WS-DRIFTRUST | P2 | M) **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the ADR-0003 posture (digests are build outputs, never hand-written inputs) is enforced by compiled code with a typed error taxonomy.
**What+How:** Port `check_containerfile_pinned_clones` (line 4488, git clones in Containerfiles must be commit-pinned), `check_sbom_metadata` (line 4096), `check_vendored_assets_non_stub` (line 6038), `check_bake_ref_defaults` (line 3947) and `check_vendor_urls` (line 3102) into a Rust `supply_chain` module. Encode ADR-0003 explicitly: the checks assert `:latest` INTENT plus SBOM presence rather than literal pins. Use `thiserror` enums for the internal failure taxonomy so `tests/drift-gate-negatives.sh` can match failure CLASSES rather than message substrings. Prove parity, delete the bash functions, re-register.
**Where:** `src/mios-rs/miosd/src/drift/supply_chain.rs, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv, tests/drift-gate-negatives.sh`
**Done When:** the five Rust checks match the bash verdicts on clean tree and negatives (`check_containerfile_pinned_clones` already has one), the bash functions are deleted, the index stays dense, and the SBOM/pin gates are green.
**Why:** The supply-chain verdicts are currently unmatchable except by grepping English error strings, so a message reword silently detaches a negative test from the check it was written to prove.
**Dep:** A1 regen runner, INFRA-5 hybrid registry

### AGY-1207 -- Port check_names_registry onto the existing generate-names-registry Rust crate  (WS-DRIFTRUST | P2 | S) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the strangler pattern is proven end-to-end (build native tool, prove byte-parity, delete the script) on a low-risk check.
**What+How:** `check_names_registry` (line 2064) runs `tools/generate-names-registry.py` over `git ls-files` and diffs `usr/share/mios/names.generated.txt` plus `referenced_names.txt`, with git-work-tree and `ls-files --deleted` incomplete-tree skips at lines 2079-2086. A native `generate-names-registry` crate already exists (`tools/native/generate-names-registry/src/main.rs`, 171 lines) but is an orphan until INFRA-3 wires it. First prove the native generator emits byte-identical output to the Python one over the full tree (golden diff), then port the check to a Rust `Check` invoking the NATIVE generator and diffing, reusing `DriftCtx.git_ok` / `incomplete_tree` from INFRA-1 for identical skip semantics, then delete the bash function.
**Where:** `src/mios-rs/miosd/src/drift/names.rs, tools/native/generate-names-registry/src/main.rs, automation/98-drift-checks.sh, usr/share/mios/names.generated.txt, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** the native generator's output equals the Python generator's output on the full tree, Rust `check_names_registry` matches the bash verdict AND its skip behaviour, the bash function is deleted, the negative test is retained, and the index stays dense.
**Why:** A written, tested Rust generator sits unused while the gate runs the Python one, so the campaign carries the maintenance of two generators and gets the benefit of neither.
**Dep:** INFRA-3 workspace wiring, INFRA-5 hybrid registry

### AGY-1208 -- Build a figment-based Rust resolver and differential-test it against the userenv.sh/mios_toml.py twins  (WS-DRIFTRUST | P1 | L) **[DONE]**
**Goal:** E-01 Compiled native tier -- one typed resolver replaces the three parallel string-munging resolvers that TD-3 and the GUP both point at.
**What+How:** Create a `mios-resolver` crate deserializing `usr/share/mios/mios.toml` into typed structs via `figment` (defaults -> vendor TOML -> vendor.d -> host -> host.d -> user, matching the layering exercised by `check_resolver_twin_parity` lines 3145-3165) and emitting the `MIOS_*` projection FROM the typed model. Port `check_resolver_twin_equivalence` (line 3181, today `tools/check-resolver-twin.py`), `check_resolver_twin_parity` (3132), `check_resolved_env_lossless` (6061), `check_raw_toml_readers` (735), `check_value_aliases` (6205, the `reference/value-aliases.tsv` derive/delete/keep-distinct decisions) and `check_no_duplicate_value_key` (6097). Add a `proptest` differential oracle generating random-but-valid `mios.toml` documents, running BOTH twins and the Rust resolver, and asserting the emitted key/value set is identical (shrinking divergences automatically). Keep the twins as fallback until parity is drift-gated, and return `miette::Result` so a bad `[colors]` entry or port points at the offending line.
**Where:** `tools/native/mios-resolver/, tools/check-resolver-twin.py, usr/lib/mios/mios_toml.py, usr/lib/mios/userenv.sh, tools/lib/userenv.sh, usr/share/mios/reference/value-aliases.tsv, src/mios-rs/miosd/src/drift/resolver.rs`
**Done When:** the proptest differential test passes for 1000+ random configs (Rust == both twins), the six Rust checks match bash verdicts on clean tree and negatives, the twins remain reachable behind the toggle, and the `check_userenv_parity` port is unblocked.
**Why:** Three hand-written resolvers must agree by discipline alone; when they silently disagree the wrong value reaches a Quadlet and the failure shows up as a broken service, not a red gate.
**Dep:** INFRA-1 harness, INFRA-2 parity

### AGY-1209 -- Port the globals/userenv cross-language parity checks to Rust  (WS-DRIFTRUST | P2 | M) **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- the PowerShell resolver stops being a third independent implementation and is measured against the one typed model.
**What+How:** Port `check_userenv_parity` (line 1757), `check_globals_ports` (line 1829) and `check_globals_image_parity` (line 1894) — the checks asserting that `automation/lib/globals.ps1` agrees with the shell/Python twins on ports and image refs. Reuse the `mios-resolver` typed model from D2 as the single reference, so the Rust check compares `globals.ps1`'s emitted values against the RESOLVER rather than against a second bash twin. This retires `check_userenv_parity` per the ROADMAP T-272 accept criteria. Prove parity, delete the three bash functions, re-register.
**Where:** `src/mios-rs/miosd/src/drift/resolver.rs, automation/lib/globals.ps1, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** the three Rust checks match the bash verdicts on clean tree and negatives, `check_userenv_parity` is retired against the typed resolver, the bash functions are deleted, and the index stays dense.
**Why:** Comparing two hand-written twins to each other can only detect that they differ, not which is right; the Windows build path keeps drifting from the Linux one with no authoritative answer.
**Dep:** D2 resolver crate

### AGY-1210 -- Port the pipeline/gate numbering + DAG integrity family to Rust  (WS-DRIFTRUST | P2 | L) **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the self-referential numbering layer (OCI layer = stage = step = check number) runs in Rust and stays consistent across a hybrid bash+Rust check population.
**What+How:** Port `check_gate_registry` (line 4620), `check_gate_index` (4917), `check_pipeline_numbering` (6139), `check_dag_integrity` (1998), `check_drift_build_catalog` (2566) and `check_roadmap_index` (4011) into a Rust `numbering` module. These parse `98-drift-checks.sh`'s `main()`, the `automation/NN-*.sh` ordinals, `drift-gate-index.tsv`, `pipeline-index.tsv` and the ROADMAP indices. Implement `check_gate_registry` / `check_gate_index` against the INFRA-5 hybrid registry (bash `main()` UNION Rust `--list`), and preserve the CTX/ROOT in-image skip in `check_pipeline_numbering` (lines 6180-6187) via `DriftCtx.in_image`. Prove parity (all have negatives), delete the bash functions, re-register.
**Where:** `src/mios-rs/miosd/src/drift/numbering.rs, automation/98-drift-checks.sh, tools/generate-pipeline-index.py, tools/generate-gate-index.py, usr/share/mios/reference/drift-gate-index.tsv, ROADMAP.md`
**Done When:** the six Rust checks match the bash verdicts on clean tree, on negatives and on the in-image skip path, Rust `check_gate_registry` self-validates the hybrid registry with no duplicate or unregistered ids across bash+Rust, the bash functions are deleted, and the index stays dense.
**Why:** These gates are what guarantee no check is silently unregistered and no pipeline stage number collides; left in bash they misread the hybrid population and either block merges or stop noticing a dropped check.
**Dep:** INFRA-5 hybrid registry

### AGY-1211 -- Port the six BIB installer-family and partition-label deploy checks into a compiled `miosd` deploy module  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the deploy-plane invariants stop being fragile bash and become compiled `miosd` checks that cannot regex-drift.
**What+How:** Move `check_installer_family_roles` (98-drift-checks.sh:5117), `check_offline_install_invariant` (:5090), `check_bib_single_config_invariant` (:5189), `check_bib_configs_projection` (:5148), `check_repo_partition_label_ssot` (:5160) and `check_bib_rootfs_label_policy` (:5028) into a Rust `drift/deploy.rs` module that reads `config/artifacts/*.toml` plus `mios.toml` through the typed resolver instead of grepping them. Preserve the degrade-open skip convention used by `check_oci_archive_path` (:4933-4936) so an absent producer/consumer file skips rather than fails. Prove verdict parity against the existing negatives listed in `check_negative_test_coverage` (:4813-4819), then delete the six bash functions and re-register the checks in the hybrid registry so `drift-gate-index.tsv` stays dense.
**Where:** `src/mios-rs/miosd/src/drift/deploy.rs, automation/98-drift-checks.sh, config/artifacts/, usr/share/mios/reference/drift-gate-index.tsv, tests/drift-gate-negatives.sh`
**Done When:** The six Rust checks emit verdicts identical to bash on a clean tree AND on each of their negatives; the six bash functions no longer exist in `98-drift-checks.sh`; `drift-gate-index.tsv` renumbers dense 1..N; `just drift-gate` is green.
**Why:** Today the invariants deciding whether a fleet host installs a signed bootc MiOS versus plain Fedora are enforced by grep over TOML — the least-done, most fragile plane in the tree guarded by the most breakable checker.
**Dep:** A1 regen runner, INFRA-5 hybrid registry

### AGY-1212 -- Port the deploy-artifact, kickstart-syntax and VM-template checks into the Rust deploy module  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- completes the deploy family so no bare-metal installer invariant is left in bash.
**What+How:** Port `check_deploy_plane` (:3517), `check_oci_archive_path` (:4929 — producer `mios-stage-oci-archive` must emit the exact `/mnt/mios-repo/*.tar` path consumer `tools/install.sh` reads), `check_replaceme_mount_substitution` (:4949 — Justfile BIB recipes must sed-substitute the REPLACEME credential placeholders), `check_kickstart_shell_syntax` (:5003), `check_build_artifacts_output_dir` (:5260) and `check_win11_vm_template_xml` (:5285) into `drift/deploy.rs`. Keep `check_kickstart_shell_syntax` shelling `bash -n` over the extracted `%post` body, and give `check_win11_vm_template_xml` a real libvirt-XML parse rather than a line match. Prove parity against the already-required negatives, delete the bash functions, re-register.
**Where:** `src/mios-rs/miosd/src/drift/deploy.rs, automation/98-drift-checks.sh, Justfile, usr/share/mios/ventoy/mios-kickstart.cfg, config/artifacts/iso.toml, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** All six Rust checks match bash verdicts on a clean tree and on the oci-archive, replaceme, kickstart, build-artifacts and win11-vm negatives; bash functions deleted; index dense.
**Why:** Producer/consumer path parity and credential substitution are currently asserted by brittle regex — a silent drift ships an ISO whose installer cannot find its own OCI tarball or bakes REPLACEME creds into a real image.
**Dep:** A1 regen runner, INFRA-5 hybrid registry, F1 installer-family

### AGY-1213 -- Port template-conformance and verb-template checks into a Rust templates module with miette spans  (WS-TEMPLATE | P2 | M)  **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- co-locates the Law-14/16 conformance fitness function with the scaffolder that generates the files it judges.
**What+How:** Port `check_template_conformance` (:3202, today a delegate to `usr/libexec/mios/check-template-conformance`), `check_templates_compilation` (:3458), `check_verb_templates` (:5608) and `check_verb_backends` (:1776) into `drift/templates.rs`, reading `[templates.<type>]` and `[verbs.*]` from `mios.toml`. Follow the existing delegate pattern (`check_hint_coverage -> mios-ai-hint-coverage`) but enforce the required AI-hint headers and template markers natively instead of shelling Python, emitting byte-offset spans with `miette` labels that point at the offending line. Wire it against the `miosd scaffold` stub (`main.rs:46`) so authoring and validation live in one binary. Prove parity, delete bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/templates.rs, usr/libexec/mios/check-template-conformance, automation/98-drift-checks.sh, usr/share/mios/mios.toml, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The four Rust checks match bash verdicts on a clean tree and negatives; a non-conformant file produces a miette report labelling the exact offending byte range; bash funcs deleted; index dense.
**Why:** Conformance failures today report only a filename, so an author must bisect the template by hand — and the validator lives in a different language from the scaffolder that must satisfy it.
**Dep:** A1 regen runner, INFRA-5 hybrid registry

### AGY-1214 -- Port the hardcode-lint family and fix the unanchored-allowlist bug that exempts real port literals  (WS-ZEROHC | P1 | M)  **[DONE]**
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- restores the zero-hardcodes gate to actually catching the `:80xx` literals it currently waves through.
**What+How:** Port `check_no_hardcode` (:978, delegate to `usr/libexec/mios/mios-hardcode-lint`), `check_no_hardcode_version` (:998, `mios-version-lint`), `check_no_hardcoded_ssot_literal` (:6255), `check_canonical_bools` (:2485) and `check_fluff_tokens` (:3386) into `drift/hardcode.rs`. Reimplement the lint natively over `[security.nohc_allowlist]` (see `mios-hardcode-lint::_load_list` line 270) and FIX the known bug: allowlist entries match UNANCHORED today, so `:8080`, `:8033` and friends are silently exempted anywhere they appear. Anchor every allowlist pattern (full-token or `^...$` regex). Golden-capture the current buggy verdict set first, port with the fix, then show the diff is confined to the enumerated spuriously-exempted literals. Delete the bash funcs and retire the Python lints behind the migration toggle.
**Where:** `src/mios-rs/miosd/src/drift/hardcode.rs, usr/libexec/mios/mios-hardcode-lint, usr/libexec/mios/mios-version-lint, usr/share/mios/mios.toml, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The Rust family matches bash on a clean tree EXCEPT the documented spuriously-exempted literals, which are now flagged or floated; a unit test asserts `:8080` is no longer blanket-exempt; bash funcs deleted; index dense.
**Why:** A live hazard, not cleanup: the unanchored allowlist silently defeats the entire zero-hardcodes campaign, so ports the operator believes are floating are in fact frozen in code and no gate says so.
**Dep:** INFRA-2 parity, INFRA-5 hybrid registry

### AGY-1215 -- Port the bare-port-literal / container-ports checks onto the typed ports model  (WS-PORTFLOAT | P2 | S)  **[DONE]**
**Goal:** E-13 Ports are allocated from SSOT, not hand-assigned -- grounds port enforcement in the derived `[ports.categories]` model instead of a string allowlist.
**What+How:** Port `check_container_ports` (:1390), `check_bootstrap_ports_drift` (:1468) and `check_no_bare_port_literals` (:1622) into `drift/ports.rs`, resolving the canonical port set from the typed `[ports.*]` / `[network]` model exposed by the D2 resolver crate — so a legitimate derived default such as `:11441` is recognised STRUCTURALLY rather than by matching an allowlist string. These enforce that container, Quadlet and bootstrap port references derive from SSOT. Prove parity against the negatives, delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/ports.rs, automation/98-drift-checks.sh, usr/share/mios/mios.toml, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The three Rust checks match bash verdicts on a clean tree and negatives with the canonical set resolved from the typed model (no literal allowlist consulted); bash funcs deleted; index dense.
**Why:** Ports are the densest hardcode surface in the tree, and the current string-allowlist coupling means every new allocated port needs a hand-edited exemption that then rots into a blanket exemption.
**Dep:** D2 resolver crate, H1 hardcode family

### AGY-1216 -- Activate the orphan `mios-version-check` crate and port the version-SSOT family onto it  (WS-DRIFTRUST | P2 | S)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- one compiled checker over the ~79 version-dupe pairs, feeding one-value-derived-everywhere.
**What+How:** Port `check_version_ssot` (:3598), `check_root_toml_subset` (:3722) and `check_impossible_eol_regressions` (:3478) by activating the already-written but unwired `mios-version-check` crate (`tools/native/mios-version-check/src/main.rs`, 98 lines; workspace-wired by INFRA-3). The check asserts no `mios_version` drift across the version-dupe pairs and that the root TOML is a subset of the vendor SSOT. Golden the native verdicts against the current bash/Python path FIRST, then register it as a `Check`, then delete the three bash functions. `check_version_ssot` has a required negative test — carry it forward.
**Where:** `src/mios-rs/miosd/src/drift/version.rs, tools/native/mios-version-check/src/main.rs, automation/98-drift-checks.sh, usr/share/mios/mios.toml, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** `mios-version-check` verdict equals the bash verdict on a clean tree and on the `check_version_ssot` negative; three bash funcs deleted; index dense; a mutated version literal is caught natively.
**Why:** A finished Rust crate sits unwired in the tree while three bash checks do its job worse — and the 3x mios.toml at conflicting versions class stays live until one compiled checker owns it.
**Dep:** INFRA-3 workspace wiring, INFRA-5 hybrid registry

### AGY-1217 -- Port the seven cross-subsystem converge/consistency checks to Rust  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the cross-subsystem SSOT consistency gates become typed reads instead of unit-file greps.
**What+How:** Port `check_converge_ssot` (:1269), `check_cephfs_ssot` (:1170), `check_v2v_import_ssot` (:5960), `check_guacamole_consistency` (:5652 — note the `_GUACAMOLE_WEB` false-friend key from the zero-hardcodes memo; there is no `MIOS_PORT_GUACAMOLE`), `check_metal_vfio` (:4339, MiOS-Metal vfio bind), `check_router_parity` (:4360) and `check_hummingbird` (:1320, RH distroless convergence) into `drift/converge.rs`, reading `mios.toml` and the relevant units in `usr/lib/systemd/system/` through the typed resolver. Prove parity against the negatives (`check_router_parity` has a required one), delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/converge.rs, automation/98-drift-checks.sh, usr/share/mios/mios.toml, usr/lib/systemd/system/, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** All seven Rust checks match bash verdicts on a clean tree and negatives; bash funcs deleted; index dense; the converge gates are green in `just drift-gate`.
**Why:** These are the only guards on ceph/v2v/guacamole/mini/router SSOT, and both the guacamole false-friend key and the mini-vfio literal coupling are documented ways the current bash silently passes real drift.
**Dep:** D2 resolver crate, INFRA-5 hybrid registry

### AGY-1218 -- Port the DB-seed coverage, account-column parity and RBAC/SQL-safety checks to Rust  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- compiles the boundary that guarantees every verb and scope is actually seeded and every CLI query is injection-shaped-free.
**What+How:** Port `check_db_seed_coverage` (:5857 — every `[verbs.*]` and scope must be seeded by `seed-db-config.py`), `check_account_column_parity` (:5904), `check_rbac_tiers` (:450 — `max_permission` tiers valid and the PDP fails closed) and `check_cli_sql_safety` (:664) into `drift/db.rs`, reading `mios.toml`, `usr/share/mios/postgres/schema-init.sql` and `seed-db-config.py`. `check_cli_sql_safety` becomes a static scan for unsafe query-string concatenation in CLI verbs rather than a grep. Prove parity, delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/db.rs, usr/libexec/mios/seed-db-config.py, usr/share/mios/postgres/schema-init.sql, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The four Rust checks match bash verdicts on a clean tree and negatives; bash funcs deleted; index dense; DB-seed and RBAC gates green.
**Why:** A verb that never gets seeded, a schema column that drifts from the account model, or an injectable CLI query all break the AI plane silently today — the only thing standing between them and a shipped image is grep.
**Dep:** INFRA-5 hybrid registry

### AGY-1219 -- Port the DB->TOML materialize round-trip check (the 320-line embedded-Python blob) to Rust  (WS-DRIFTRUST | P2 | L)  **[DONE]**
**Goal:** E-23 DB-driven configuration and vector recall -- makes the lossless TOML->DB->TOML round-trip oracle a typed, reviewable check instead of Python-inside-bash.
**What+How:** Port `check_drift_projection` (:2157-2482, gate check 31): 320 lines of embedded Python that mock psycopg (`MockCursor`/`MockConnection`), exec `seed-db-config.py` then `materialize-config-toml.py`, and assert the round-trip is lossless for the ports/ai/routing/pgvector/a2a/mcp/observability/sandbox/security/agent_passport/agent_pipe scopes, `routing.domains` and `verbs`, under the field-normalization rules at :2446-2470. Choose per parity feasibility between (a) reimplementing the mock-psycopg round-trip in Rust by parsing the SQL the two scripts emit, or (b) a strangler intermediate that runs the two scripts under a thin Rust-provided mock shim and diffs through the typed resolver. Validate on a clean tree AND on a deliberately-drifted verb-field fixture before deleting the bash function and its heredoc.
**Where:** `src/mios-rs/miosd/src/drift/db_roundtrip.rs, usr/libexec/mios/seed-db-config.py, usr/libexec/mios/materialize-config-toml.py, usr/share/mios/mios.toml, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The Rust round-trip check matches the bash verdict on a clean tree and reds on the mutated-verb-field fixture exactly as bash does; the bash function and its 320-line heredoc are gone from `98-drift-checks.sh`; index dense.
**Why:** This is the single largest Python-in-bash blob in the gate; it is effectively unreviewable, and one wrong normalization rule would let real DB drift pass unnoticed while everyone reads the gate as green.
**Dep:** D2 resolver crate, INFRA-2 parity

### AGY-1220 -- Port the module-hygiene family (boundary, coverage, length, unwired, surface parity) to Rust  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- keeps the agent-pipe god-module decomposition enforced by a compiled size/boundary gate.
**What+How:** Port `check_module_boundary` (:423), `check_module_test_coverage` (:697), `check_module_length` (:6016), `check_unwired_modules` (:1031 — the imported-but-dead `mios_*.py` detector plus its self-cleaning `_UNWIRED_ALLOW` register) and `check_surface_parity` (:824) into `drift/modules.rs`. These do line-count and AST-ish analysis over `usr/lib/mios/agent-pipe/`; implement the import/reference scan with either a lightweight Python-AST invocation or a Rust tokenizer — whichever exactly preserves the `imported-not-called` semantics documented at :1005-1030. Prove parity against negatives (`check_module_test_coverage` has a required one), delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/modules.rs, usr/lib/mios/agent-pipe/, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The five Rust checks match bash verdicts on a clean tree and negatives, including the `_UNWIRED_ALLOW` stale-entry self-clean; bash funcs deleted; index dense.
**Why:** The gate that stops server.py-sized modules from regrowing is itself grep-and-`wc -l` coupled, so dead imported substrate accumulates and the allow-register rots instead of self-cleaning.
**Dep:** INFRA-5 hybrid registry

### AGY-1221 -- Activate the orphan `mios-aiplane-lint` crate and port the four AI-plane lint checks onto it  (WS-DRIFTRUST | P2 | S)  **[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- mechanizes the `MIOS_AI_VLLM_*` emitted vs `MIOS_VLLM_*` consumed reconciliation in compiled code.
**What+How:** Port `check_agent_pipe_budgets` (:1538), `check_pipe_boundaries` (:5619), `check_vllm_name_canonical` (:5631) and `check_pipe_extraction_parity` (:5639) by activating and extending the already-written but unwired `mios-aiplane-lint` crate (`tools/native/mios-aiplane-lint/src/main.rs`, 202 lines; workspace-wired by INFRA-3). Golden the native lint against the bash verdicts, register the four as `Check`s, then delete the bash functions. `check_agent_pipe_budgets` has a required negative test — carry it forward.
**Where:** `src/mios-rs/miosd/src/drift/aiplane.rs, tools/native/mios-aiplane-lint/src/main.rs, usr/lib/mios/agent-pipe/, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** `mios-aiplane-lint` verdicts equal the bash verdicts on a clean tree and on the `check_agent_pipe_budgets` negative; four bash funcs deleted; index dense; the vLLM canonical-name parity is enforced by the binary.
**Why:** A 202-line finished crate is dead weight in the tree while four bash checks do the same job, and the short-vs-long vLLM key mismatch stays a hand-tracked memo item rather than a machine-checked invariant.
**Dep:** INFRA-3 workspace wiring, INFRA-5 hybrid registry

### AGY-1222 -- Port the AI-hint coverage ratchet and the retired-lane/model checks to Rust  (WS-DRIFTRUST | P2 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- keeps the AI-hint coverage ratchet monotone under a typed counter rather than bash arithmetic.
**What+How:** Port `check_hint_coverage` (:396, delegate to `usr/libexec/mios/mios-ai-hint-coverage`, ratchet ceiling), `check_dead_lane` (:212, no retired `:11434` lane in active config) and `check_retired_models` (:240, no `gemma4` / `qwen3:1.7b` ids) into `drift/aihint.rs`. Reimplement the ratchet and the `SCAN_DIRS` walk (:81-87) natively; source the retired-lane and retired-model identifier sets from SSOT where a key exists, falling back to the documented literal set otherwise. Prove parity, delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/aihint.rs, usr/libexec/mios/mios-ai-hint-coverage, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The three Rust checks match bash verdicts on a clean tree and negatives; the ratchet ceiling is preserved (coverage cannot regress); bash funcs deleted; index dense.
**Why:** The ratchet is currently bash arithmetic over a grep count — a silent off-by-one lets hint coverage slide backwards, and retired lane/model ids can creep back into active config unchallenged.
**Dep:** INFRA-5 hybrid registry

### AGY-1223 -- Port the ai/v1 manifest and agent-schema reference-integrity checks to Rust  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the AI plane's projection contract is validated by typed parsing instead of JSON-in-bash.
**What+How:** Port `check_structured` (:266 — every `[nodes.local-*]` lane is served and the ai/v1 manifest parses with all refs resolving), `check_ai_manifest` (:582, the `tools/generate-ai-manifest.py` verb catalog), `check_package_registry` (:625), `check_capability_manifest` (:771, `mios_capreg` / capabilities.generated) and `check_agent_schema` (:499, the health_gate/cli/node/single-default contract) into `drift/aimanifest.rs`. The regen-shaped checks reuse `regen::regen_and_diff`; the schema and structured checks parse the ai/v1 JSON plus `[nodes.*]` / `[verbs.*]` / `[recipes.*]` from `mios.toml`. Prove parity, delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/aimanifest.rs, tools/generate-ai-manifest.py, usr/share/mios/ai/v1/, automation/98-drift-checks.sh, usr/share/mios/mios.toml, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The five Rust checks match bash verdicts on a clean tree and negatives; a dangling ai/v1 manifest reference fails natively; bash funcs deleted; index dense.
**Why:** ai/v1 is the contract every agent, lane and federated peer reads; a dangling ref or an unserved `local-*` lane currently escapes through fragile JSON handling in bash and surfaces only as a runtime 404.
**Dep:** A1 regen runner, INFRA-5 hybrid registry

### AGY-1224 -- Port the exec/eval-safety and lint security checks to Rust  (WS-DRIFTRUST | P1 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- puts the eval-on-agent-args injection surface behind a compiled, non-bypassable static scan.
**What+How:** Port `check_cli_eval_safety` (:4027 — no `eval` on agent-supplied args in verbs, the Gemini-CLI-class injection surface), `check_shellcheck` (:4082), `check_python_lint` (:4693, delegate to `automation/lint-python.sh`), `check_nested_podman_caps` (:4220) and `check_curl_retry` (:4177) into `drift/security.rs`. `check_shellcheck` shells `shellcheck` and degrades open when the binary is absent; `check_python_lint` shells `python -m py_compile`; `check_cli_eval_safety` becomes a static scan of verb scripts for `eval` applied to untrusted arguments. Prove parity against the required negatives (eval-safety, shellcheck, curl-retry, nested-podman-caps, listed :4788-4794), delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/security.rs, automation/lint-python.sh, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv, tests/drift-gate-negatives.sh`
**Done When:** The five Rust checks match bash verdicts on a clean tree and on each of their negatives; bash funcs deleted; index dense; a newly-added `eval "$AGENT_ARG"` in any verb fails the gate.
**Why:** The eval-on-agent-args verbs are a live untrusted boundary on an autonomous agent host, and the check guarding it is bash grepping bash — the easiest gate in the tree to accidentally evade.
**Dep:** INFRA-5 hybrid registry

### AGY-1225 -- Port the governance-hygiene checks (lint-is-final, coordination, heredoc, target-languages) to Rust  (WS-DRIFTRUST | P2 | S)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the Law-14 language contract that legitimizes the Rust migration is itself enforced from SSOT in Rust.
**What+How:** Port `check_lint_is_final` (:3048), `check_coordination_hygiene` (:3431), `check_hyprland_conf_heredoc` (:4160) and `check_target_languages` (:3814 — Law 14 language-per-domain, no new C#/Batch/Go, C# grandfathered) into `drift/governance.rs`, reading `mios.toml`. Keep the grandfather list authoritative from `[laws.target_languages]` rather than a literal in the check body. Prove parity, delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/governance.rs, automation/98-drift-checks.sh, usr/share/mios/mios.toml, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The four Rust checks match bash verdicts on a clean tree and negatives; the target-language grandfather list is read from `[laws.target_languages]` with no in-check literal; bash funcs deleted; index dense.
**Why:** If Law 14's own check stays in bash while every other check is ported, the governance layer becomes self-inconsistent and a new C#/Go/Batch file can land while the gate that forbids it is mid-migration.
**Dep:** INFRA-5 hybrid registry

### AGY-1226 -- Port the boot-integrity / firstboot family (degrade-open, tier, greenboot, clevis) to Rust  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- compiles the gates over greenboot, clevis/LUKS and the firstboot tier so the self-healing controls stay wired.
**What+How:** Port `check_firstboot_degrade_open` (:3073 — every firstboot producer must degrade open), `check_firstboot_tier` (:4526 — the tier that evicts the vLLM/SGLang whales from the bake so it fits a standard runner), `check_greenboot` (:4298), `check_greenboot_enablement` (:3280) and `check_clevis_luks` (:4321, PCR-bound LUKS auto-unlock) into `drift/boot.rs`, reading the firstboot units plus `mios.toml`. Preserve the required negatives at :4802 and :4808. Prove parity, delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/boot.rs, usr/libexec/mios/mios-ai-firstboot, usr/libexec/mios/forge-firstboot.sh, usr/libexec/mios/mios-hermes-firstboot, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The five Rust checks match bash verdicts on a clean tree and on the firstboot-degrade-open and firstboot-tier negatives; bash funcs deleted; index dense; greenboot/clevis/firstboot gates green.
**Why:** These are the checks that stop a bad publish from rolling a whole fleet forward into an unbootable or unrecoverable state, and the firstboot tier is what keeps the publish bake inside standard-runner capacity at all.
**Dep:** INFRA-5 hybrid registry

### AGY-1227 -- Activate the orphan `mios-bake-plan` crate and port the bake-plan/budget checks onto it  (WS-BAKEGATE | P2 | S)  **[DONE]**
**Goal:** E-16 The bake plane: what is present in the image, and can a stock runner hold it -- the capacity budgets that gate publishing become compiled arithmetic.
**What+How:** Port `check_bake_plan` (:3845), `check_bake_plan_integrity` (:3857), `check_bake_budget` (:4255) and `check_rechunk_budget` (:4597) by activating and extending the already-written but unwired `mios-bake-plan` crate (`tools/native/mios-bake-plan/src/main.rs`, 369 lines; workspace-wired by INFRA-3), reading `usr/lib/mios/bake/plan.d/`. Golden the native verdicts against bash first, register the four as `Check`s, then delete the bash functions. `check_bake_plan`, `check_bake_budget` and `check_rechunk_budget` have required negatives — carry them forward.
**Where:** `src/mios-rs/miosd/src/drift/bake.rs, tools/native/mios-bake-plan/src/main.rs, usr/lib/mios/bake/plan.d/, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** `mios-bake-plan` verdicts equal the bash verdicts on a clean tree and on the three required negatives; four bash funcs deleted; index dense; layer and rechunk budgets enforced by the binary.
**Why:** A third finished crate sits unwired while bash does budget arithmetic that, when wrong, blows the GitHub publish runner's capacity and fails the bake hours in.
**Dep:** INFRA-3 workspace wiring, INFRA-5 hybrid registry

### AGY-1228 -- Finish `mios-ssot-lint` to parity, demote `97-ssot-lint.sh` to a shim, and retire the equivalence check  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- eliminates a bash/Rust twin by finishing the Rust half, so there is one implementation left to trust.
**What+How:** `check_ssot_lint_equivalence` (:4870) exists only to assert that bash `automation/97-ssot-lint.sh` (195 lines — every `${MIOS_*}` used in a Quadlet `Exec=`/`Environment=` is two-side-wired in `tools/lib/userenv.sh` AND `automation/34-render-quadlets.sh`) agrees with its native sibling `mios-ssot-lint` (`tools/native/mios-ssot-lint/src/main.rs`, 259 lines; workspace-wired by INFRA-3). Bring the native lint to full parity, port the equivalence check to a Rust `Check`, demote `97-ssot-lint.sh` to a thin shim that execs the binary (keeping the bash body reachable via `MIOS_SSOT_LINT_LEGACY=1`), repoint the Justfile `drift-gate` step (Justfile:83) and CI at the native tool, then delete the bash body. The equivalence check has a required negative — carry it forward until the twin is gone.
**Where:** `tools/native/mios-ssot-lint/src/main.rs, automation/97-ssot-lint.sh, tools/lib/userenv.sh, automation/34-render-quadlets.sh, src/mios-rs/miosd/src/drift/ssot_lint.rs, Justfile, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** `mios-ssot-lint` equals `97-ssot-lint.sh` verdict on a clean tree and on the equivalence negative; `97-ssot-lint.sh` is a thin shim or deleted with Justfile and both CIs driving the native lint; the equivalence check is ported and its twin-reconciliation reason is gone; index dense.
**Why:** Two implementations of the same lint means every Quadlet-wiring rule must be edited twice and a whole extra check exists solely to police the duplication — cost with no product value.
**Dep:** INFRA-3 workspace wiring, INFRA-5 hybrid registry

### AGY-1229 -- Port the self-test family (negatives coverage, hermeticity, soft-mode, smoke manifest) and make the ratchet hybrid-aware  (WS-TESTDOC | P1 | M)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- the fitness functions that guard the fitness functions keep holding across the bash-to-Rust migration.
**What+How:** Port `check_negative_test_coverage` (:4769), `check_negative_coverage` (:5558), `check_test_hermeticity` (:4707), `check_soft_mode_not_committed` (:4854 — no `MIOS_DRIFT_CHECK_SOFT=1` / `MIOS_SSOT_LINT_SOFT=1` committed into workflows, build.sh or the Justfile) and `check_smoke_manifest` (:5514) into `drift/selftest.rs`. Per INFRA-5, `check_negative_test_coverage` must be widened to require a `test_*` function in `tests/drift-gate-negatives.sh` for every check in the HYBRID registry, so Rust-ported checks keep the ratchet instead of falling out of it. Verify `check_soft_mode_not_committed` still scans BOTH `.github/workflows/` and `.forgejo/workflows/` (Law 15 dual-CI). Prove parity, delete the bash funcs, re-register.
**Where:** `src/mios-rs/miosd/src/drift/selftest.rs, tests/drift-gate-negatives.sh, .github/workflows/mios-ci.yml, .forgejo/workflows/, automation/98-drift-checks.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** The five Rust checks match bash verdicts on a clean tree and negatives; the negatives ratchet spans bash AND Rust checks (a Rust check with no negative fails the gate); soft-mode scanning covers both CI directories; bash funcs deleted; index dense.
**Why:** Without a hybrid-aware ratchet, every check ported to Rust silently drops out of the negative-test requirement — the migration would quietly dismantle the proof that the gate can fail at all.
**Dep:** INFRA-5 hybrid registry, INFRA-2 parity

### AGY-1230 -- Adopt miette+thiserror typed diagnostics across every ported drift check  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- gate failures become compiler-grade, actionable diagnostics pointing at the offending SSOT line.
**What+How:** Retrofit the ported `Check` impls to return `miette::Result`: internal errors as `thiserror` enums the negatives harness can match on, user-facing failures as `miette` reports with source-span labels and help text pointing AT the offending `mios.toml` line/token. Spans must use BYTE offsets, not char counts, so UTF-8 content highlights correctly. Provide a shared `miette::SourceSpan` helper in `drift/diag.rs` mapping a `[section].key` to its byte range in `mios.toml`. Preserve the existing machine-greppable prefixes (`[98-drift-checks][diff]`, `[gate-registry-drift]`, etc.) by carrying them as miette `code`/`help` so downstream greps and `drift-gate-index.tsv` description capture keep working unchanged.
**Where:** `src/mios-rs/miosd/src/drift/mod.rs, src/mios-rs/miosd/src/drift/diag.rs, usr/share/mios/mios.toml`
**Done When:** A bad `[colors]` or port value produces a miette diagnostic labelling the correct byte span; every existing greppable prefix still appears in output; `cargo test` insta diagnostic snapshots pass; the parity harness shows no verdict regression.
**Why:** Bash emits an opaque one-line failure string, so an operator hitting a bad SSOT value has to hand-bisect `mios.toml` — and downstream tooling that greps those prefixes would break the moment the runner changes without this contract preserved.
**Dep:** D2 resolver crate

### AGY-1231 -- Cut the default drift runner over to Rust and delete the retired bash families  (WS-DRIFTRUST | P1 | L)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- `miosd drift-check` becomes the single source of drift truth, retiring `98-drift-checks.sh`.
**What+How:** Once all 28 family ports are parity-green under the toggle, flip `[migration].use_rust_drift` and the `drift_families.*` defaults to `rust` in `mios.toml`, mirrored into the bootstrap repo (Law 15 DOUBLE-REPO-TRIPLE-CHECK); repoint the Justfile `drift-gate` step (Justfile:100) and both CI workflows from `bash automation/98-drift-checks.sh` to `miosd drift-check`; delete the now-empty `98-drift-checks.sh` or reduce it to a `MIOS_DRIFT_LEGACY=1` shim, including its `main()` registration. Regenerate `drift-gate-index.tsv` from the pure-Rust `--list` and confirm it is dense 1..N and byte-identical in both repos. Document an oxidizr-style one-flip rollback (`use_rust_drift=false`) for one release before final deletion. Stage every path explicitly — never `git add -A`, AGY shares the tree — and land via a real `git merge --no-ff`.
**Where:** `usr/share/mios/mios.toml, /c/MiOS-bootstrap/usr/share/mios/mios.toml, automation/98-drift-checks.sh, Justfile, .github/workflows/mios-ci.yml, .forgejo/workflows/, usr/share/mios/reference/drift-gate-index.tsv, tools/native/mios-drift-runner/src/main.rs`
**Done When:** `just drift-gate` and both CIs invoke `miosd drift-check` as the authoritative gate; all 123 check verdicts are reproduced by Rust under the parity harness; `98-drift-checks.sh` is deleted or shimmed; `drift-gate-index.tsv` is dense and byte-identical across both repos; the rollback flip is documented.
**Why:** Until the default flips, every ported check is dead code and the tree pays the full cost of maintaining two runners — with the bash one still authoritative, so none of the migration's robustness is actually realised.
**Dep:** all family ports (A2, A3, A4, B, C, D1, D2, D3, E, F1, F2, G, H1, H2, I, J, K1, K2, L, M1, M2, M3, N1, N2, O, P, Q1, Q2)

### AGY-1232 -- Port `99-postcheck.sh` into miosd and unify 98/99 under one binary with source-tree and in-image modes  (WS-DRIFTRUST | P1 | L)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- one binary owns both the source-tree gate and its deployed-rootfs twin, so the port does not manufacture a new bash/Rust twin.
**What+How:** `automation/99-postcheck.sh` (810 lines) is the DEPLOYED-rootfs twin of `98-drift-checks.sh`: it re-enforces the same invariants against `/usr`, `/etc` and a running image inside the OCI bake (`sshd -V` >= 9.6, Cockpit posture, sysusers materialization, tmpfiles resolution). Add a `miosd drift-check --mode=in-image` path in `src/mios-rs/miosd` alongside the existing source-tree `run_checks` in `drift.rs`, sharing the `Check` trait and the mios.toml/resolver context but scanning absolute deployed paths instead of `$ROOT`-relative ones. Characterize against a real built image until verdicts are byte-parity, then demote `99-postcheck.sh` to a thin shim calling the binary and repoint `automation/build.sh` (`CONTAINERFILE_SCRIPTS`) at it.
**Where:** `automation/99-postcheck.sh, automation/98-drift-checks.sh, src/mios-rs/miosd/src/drift.rs, automation/build.sh (CONTAINERFILE_SCRIPTS)`
**Done When:** `miosd drift-check --mode=in-image` reproduces every 99-postcheck invariant against a deployed rootfs with verdicts byte-identical to the bash gate on the same built image; `99-postcheck.sh` is a thin shim; build.sh's post-loop step calls the binary; `cargo test` and `just drift-gate` green.
**Why:** 99-postcheck is the largest untargeted piece of the drift plane; porting only 98 leaves the same invariants expressed twice in two languages — recreating exactly the twin-duplication smell this campaign exists to kill.
**Dep:** Establish the mios-drift lib crate + Check-trait harness in miosd

### AGY-1233 -- Replicate the gitignored-projection-absent skip and in-loop vs post-loop ordering semantics in the Rust runner  (WS-DRIFTRUST | P0 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- encodes the vacuous-PASS ordering invariant every ported projection check depends on, so the Rust gate cannot abort a bake.
**What+How:** `automation/build.sh` documents that 98's projection checks — uki-cmdline (check 93), ipa-enroll (92), cockpit (95) — MUST vacuously PASS when their gitignored derived files (`etc/mios/ipa-enroll.env`, `usr/lib/kernel/cmdline`, `etc/cockpit/cockpit.conf`) are ABSENT at `$ROOT`, because the gate runs BEFORE the post-loop SSOT re-projection. A naive Rust port treating absent-target as FAIL aborts the bake. Encode `target absent -> skip`, the `/etc`-overlay-usually-absent semantics (`SCAN_DIRS` guarded by `-d`, ~line 84) and the CTX/ROOT in-image incomplete-tree skip as ONE shared `DriftContext` primitive consumed by every projection `Check`. Prove it with a characterization test that deletes those three files and asserts PASS.
**Where:** `src/mios-rs/miosd/src/drift.rs, automation/build.sh, automation/98-drift-checks.sh (SCAN_DIRS ~line 84; checks 92/93/95)`
**Done When:** The Rust runner returns vacuous-PASS for absent gitignored projections and absent `/etc` overlays identically to bash; a test that removes ipa-enroll.env, cmdline and cockpit.conf passes; no false-fail when invoked pre-reprojection or against an incomplete in-image tree.
**Why:** This exact false-fail bankrupts the bake — it is the documented failure recorded in build.sh and in the drift-gate-local-verify notes; getting it wrong makes every ported projection check a build-breaker rather than a gate.
**Dep:** Establish the mios-drift lib crate + Check-trait harness in miosd

### AGY-1234 -- Make regen-and-diff snapshot/restore crash-safe and port `tests/drift-gate-readonly.sh` onto the binary  (WS-DRIFTRUST | P0 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- preserves the gate's read-only promise under the compiled runner, on a tree AGY edits concurrently.
**What+How:** 98's `_emit_projection_evidence` (~line 96) snapshots each target on a failing projection check, re-runs the generator IN WRITE MODE onto disk, diffs, then RESTORES the tree byte-for-byte — netting read-only. The Rust port must make that restore panic-safe via an RAII `Drop` guard so a mid-render panic or SIGINT cannot leave a mutated working tree. Port `tests/drift-gate-readonly.sh` to run against the binary and assert `git status` is clean after a full run INCLUDING a forced failure, and preserve the machine-greppable `[98-drift-checks][diff]` evidence prefix that downstream tooling greps.
**Where:** `automation/98-drift-checks.sh (_emit_projection_evidence ~line 96), tests/drift-gate-readonly.sh, src/mios-rs/miosd`
**Done When:** Rust regen-and-diff checks restore the tree byte-for-byte even under an injected panic; the drift-gate-readonly self-test passes against the binary; `git status` is clean after a full run with a forced failure; the `[diff]` evidence contract is byte-preserved.
**Why:** The whole gate's read-only promise rests on this one restore path, and C:\MiOS is shared with AGY — a non-crash-safe port leaves phantom diffs in a tree another agent is concurrently committing.
**Dep:** Port the SSOT regenerate-and-diff runner abstraction to Rust

### AGY-1235 -- Make the Rust drift runner Windows-native and drop the bash + python3 shim so the gate runs git-bash-free on win32  (WS-DRIFTRUST | P2 | L)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the gate becomes runnable on the primary dev host without a POSIX emulation layer.
**What+How:** `98-drift-checks.sh` opens with a `python3`->`python` fallback shim (~lines 62-72) and is otherwise pure bash, so on the win32 dev host it runs only under git-bash — and `mios-drift-runner` today merely shells `bash automation/98-drift-checks.sh`. Make `miosd drift-check` the genuine cross-platform entrypoint: no bash, no `python3` in PATH, with CRLF and backslash-safe path handling (relevant because `check_impossible_eol_regressions` and the CRLF hazards live in this surface). Normalize separators and line endings inside the runner, remove the shell-out from `mios-drift-runner`, and differential-test git-bash vs native execution to prove identical verdicts.
**Where:** `automation/98-drift-checks.sh (python3 shim ~lines 62-72), src/mios-rs/miosd, tools/native/mios-drift-runner/src/main.rs, automation/lib/globals.ps1`
**Done When:** `miosd drift-check` runs on win32 with neither bash/git-bash nor python3 available and yields verdicts byte-identical to the bash gate on the same tree; path and EOL normalization are verified by test; `mios-drift-runner` no longer shells out to bash.
**Why:** The repo is developed on Windows with PowerShell primary while the entire gate is bash-only, so the conscience of the tree is flaky-to-untestable on the machine where the code is actually written.
**Dep:** Cut over the default drift runner to Rust and delete the retired bash families

### AGY-1236 -- Bless the Rust drift plane in Law 14 and regenerate the gate's own AI-hint self-reference as check families migrate  (WS-DRIFTRUST | P1 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- keeps the gate GREEN on its own artifacts while its checks move from bash to native `miosd`.
**What+How:** `check_target_languages` (automation/98-drift-checks.sh ~3814) enforces Law 14 (Rust is the native tier; `grandfathered_cs` may only shrink) and `check_hint_coverage` (~396) parses the `AI-functions:` header on line 4 that enumerates 98's `check_*` functions. Every family that moves to `src/mios-rs/miosd/src/checks/*.rs` and deletes its bash function invalidates that self-reference, so the gate flags ITSELF. Add/extend `[laws.target_languages]` in `usr/share/mios/mios.toml` to bless the `.rs` drift plane, teach `usr/libexec/mios/mios-ai-hint-coverage` and the `AI-functions:` header to reflect the relocated checks, and re-run both after each strangler step. Mirror the shared mios.toml surface into mios-bootstrap.git (Law 15).
**Where:** `automation/98-drift-checks.sh (check_target_languages ~3814, AI-functions header line 4, check_hint_coverage ~396), usr/share/mios/mios.toml [laws.target_languages], usr/libexec/mios/mios-ai-hint-coverage`
**Done When:** New drift `.rs` files pass `check_target_languages`; the `AI-functions:` header and `mios-ai-hint-coverage` name no retired bash functions; `check_hint_coverage` is green after each family retires; mios.toml is byte-identical in mios.git and mios-bootstrap.git.
**Why:** Porting a gate that lints its own languages and headers is self-referential -- ignore it and the migration red-fails on the very files it just produced, blocking every downstream port mid-flight.
**Dep:** Port the governance-hygiene checks (lint-is-final, coordination, heredoc, target-languages) to Rust

### AGY-1237 -- Model shellcheck and python-lint as a typed ExternalLinter check that survives cutover -- never delete them  (WS-DRIFTRUST | P1 | S)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- no lint coverage is lost for the bash/python surface that legitimately remains after the Rust cutover.
**What+How:** `check_shellcheck` (~4082, wrapping automation/lint-shell.sh) and `check_python_lint` (~4693, wrapping automation/lint-python.sh + py-compile) are not portable to Rust: they invoke external tools over the REMAINING bash/python code. Give the miosd check harness an `ExternalLinter` check variant that shells out to the tool, degrades-open when it is absent (matching today's WARNING/skip verdict), and stays registered in the dispatch set through and after cutover. Add an explicit exemption so the "delete retired bash families" sweep cannot remove these two.
**Where:** `automation/98-drift-checks.sh (check_shellcheck ~4082, check_python_lint ~4693), automation/lint-shell.sh, automation/lint-python.sh`
**Done When:** `miosd` runs shellcheck and py-compile through a typed ExternalLinter check with verdict parity vs bash; both degrade-open on a host lacking the tool; neither is removed by the final cutover; `just drift-gate` green.
**Why:** A naive port-then-delete plan silently drops shell and python lint coverage over thousands of surviving lines -- exactly the regression class the rubric marks automatic P0.
**Dep:** Cut over the default drift runner to Rust and delete the retired bash families

### AGY-1238 -- Retarget the negative-test harness per-check to whichever implementation OWNS the check  (WS-DRIFTRUST | P1 | M)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- every gate keeps a negative test that proves it can actually FAIL, across a mixed bash/Rust check set.
**What+How:** `tests/drift-gate-negatives.sh` (2111 lines) injects a real violation, asserts `98-drift-checks.sh <check_name>` FAILS, restores, then asserts PASS -- hard-wired to the bash single-check dispatch in `main()` (~6267). As checks flip to Rust behind the `[migration]` toggle, each negative must instead dispatch to the owning implementation (bash function or `miosd drift-check --only <name>`), or it silently exercises retired code. Add an owner-resolution helper that reads the toggle, and add the `--only`/single-check flag to `src/mios-rs/miosd/src/main.rs`.
**Where:** `tests/drift-gate-negatives.sh, src/mios-rs/miosd/src/main.rs (add --only/single-check flag), the [migration] toggle in mios.toml`
**Done When:** Every negative dispatches to the toggle-selected owner; all injections still FAIL and then restore GREEN with a mixed bash/Rust set; the harness passes under `just drift-gate` mid-migration.
**Why:** The negatives harness is the correctness proof for the gate; pointed at a retired bash path it validates dead code while the live check could be broken and nobody would know.
**Dep:** Add the [migration] SSOT toggle with oxidizr-style per-family Rust/bash opt-out and rollback

### AGY-1239 -- Port check_drift_build_catalog -- the 370-line embedded MockCursor SQL-DB emulation -- to Rust  (WS-DRIFTRUST | P2 | L)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the largest bash check becomes native so the gate stops being fragile embedded Python.
**What+How:** `check_drift_build_catalog` (~2566-2940) is distinct from `check_drift_projection`: it embeds a Python `MockCursor` that emulates a database, replays the schema/seed SQL (`INSERT INTO system_config/config_kv/verb ...`), materializes the build catalog, and diffs it against `usr/share/mios/build_phases.json` plus the `automation/*.sh` phase enumeration. Port the cursor emulation and catalog materialization to Rust, reusing the DB-seed parsing built for the account/RBAC checks, and characterize the diff verdict byte-for-byte before deleting the embedded Python.
**Where:** `automation/98-drift-checks.sh (check_drift_build_catalog ~2566-2940), usr/share/mios/build_phases.json, usr/share/mios/postgres/*.sql, automation/*.sh`
**Done When:** Rust materializes the catalog from the seed SQL and diffs `build_phases.json` + the phase list with byte-parity verdicts vs bash on a clean tree and on injected drift; the embedded Python MockCursor path no longer exists in 98-drift-checks.sh; `cargo test` green.
**Why:** It is the single largest un-targeted check function and is trivially conflated with the smaller `check_drift_projection`; left unassigned it becomes the one bash body that blocks the whole "zero bash check_*" cutover.
**Dep:** Port the DB-seed coverage + account-column + RBAC/SQL checks to Rust

### AGY-1240 -- Port the multi-copy mios.toml integrity family: check_root_toml_subset + check_toml_projection  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the guard against the 3x-mios.toml duplication becomes compiled and stays enforcing.
**What+How:** Three mios.toml copies exist (the operator-flagged smell). `check_root_toml_subset` (~3722) asserts the root mios.toml key set is a strict SUBSET of `usr/share/mios/mios.toml`; `check_toml_projection` (~3791) runs `usr/libexec/mios/mios-sync-toml --check` to assert the derived `[ports]`/`[colors]` copies project verbatim from the canonical SSOT. Port both: a recursive key-set subset walk over parsed TOML, and either a native equivalent of `mios-sync-toml --check` or a degrade-open ExternalLinter invocation of it. Keep this family separate from the version-string checks.
**Where:** `automation/98-drift-checks.sh (check_root_toml_subset ~3722, check_toml_projection ~3791), usr/libexec/mios/mios-sync-toml, mios.toml, usr/share/mios/mios.toml`
**Done When:** Rust asserts the root-toml key subset and the verbatim `[ports]`/`[colors]` projection with byte-parity verdicts vs bash on clean and injected-drift trees; negatives green; the shared surface is mirrored in both repos.
**Why:** No other assigned title covers root-toml subset or the mios-sync-toml projection -- drop them and the exact duplication class the debt register names goes unguarded.
**Dep:** Port the version-SSOT family onto the existing mios-version-check crate

### AGY-1241 -- Port the GUP value-unification trio to Rust, with root-path normalization baked into the env snapshot  (WS-DRIFTRUST | P1 | M)  **[DONE]**
**Goal:** E-08 Global Unification Plan -- the losslessness invariant that gates every namespace-collapse step runs natively and stops false-failing in CI.
**What+How:** Three checks drive the GUP and all consume `usr/libexec/mios/mios-env-snapshot`: `check_value_aliases` (~6255) asserts derive/delete pairs in `reference/value-aliases.tsv` resolve EQUAL while keep-distinct pairs resolve DIFFERENT (false-friends such as `MIOS_PGVECTOR_USER` vs `MIOS_PG_USER`); `check_no_duplicate_value_key` (~6097) flags the ~79 duplicate-value keys; `check_resolved_env_lossless` (~6061) diffs the snapshot against `reference/env-baseline.txt`. Port all three onto the Rust resolver's snapshot and normalize root paths inside the snapshot itself, or the lossless check false-fails whenever CI checks out at a different root.
**Where:** `automation/98-drift-checks.sh (check_value_aliases ~6255, check_no_duplicate_value_key ~6097, check_resolved_env_lossless ~6061), usr/libexec/mios/mios-env-snapshot, usr/share/mios/reference/{value-aliases.tsv,env-baseline.txt}`
**Done When:** The three Rust checks consume the native resolver snapshot; alias pairs resolve per polarity, no duplicate-value keys remain, env is lossless vs baseline with root paths normalized; verdict parity vs bash holds under CI-style differing roots; negatives green.
**Why:** These are the enforcement backbone of the 2523-key collapse, and the root-normalization hazard has already broken CI once -- porting without it reintroduces a known outage.
**Dep:** Build a figment-based Rust resolver and differential-test it against the userenv.sh/mios_toml.py twins

### AGY-1242 -- Port the container-privilege family: check_quadlet_privilege + check_nested_podman_caps  (WS-DRIFTRUST | P1 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- Law 10's privilege allowlist keeps being machine-checked after the bash gate retires.
**What+How:** `check_quadlet_privilege` (~2975) walks `usr/share/containers/systemd` and `etc/containers/systemd` and flags any Quadlet whose `User=` is empty/root/0 yet is absent from `mios.toml [security.privileged_quadlets].root` (Law 10, 10 units), via the `_privileged_quadlet_array` helper; `check_nested_podman_caps` (~4220) asserts the CI workflow and `57-mios-sys-build.sh` carry the required nested-podman flags (`--device /dev/fuse`, `--cap-add`, `seccomp=unconfined`) plus the build-retry loop. Port both as one Rust container-privilege family, degrading-open when the SSOT arrays are empty to match today's WARNING skip.
**Where:** `automation/98-drift-checks.sh (check_quadlet_privilege ~2975, _privileged_quadlet_array, check_nested_podman_caps ~4220), mios.toml [security.privileged_quadlets], usr/share/containers/systemd, usr/libexec/mios/57-mios-sys-build.sh`
**Done When:** Rust flags root/`User=` Quadlets missing from the `[security.privileged_quadlets].root` allowlist and missing nested-podman flags with byte-parity verdicts vs bash; degrades-open on empty arrays; negatives and `just drift-gate` green.
**Why:** These sit between the assigned "Laws/filesystem-layout" and "exec/eval-safety" families and would be orphaned -- leaving a container able to run as root fleet-wide with no gate saying so.
**Dep:** Port the Laws + filesystem-layout enforcement family to Rust

### AGY-1243 -- Port the resolver-discipline lints: check_raw_toml_readers + check_verb_backends  (WS-DRIFTRUST | P1 | M)  **[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- nothing may bypass the single resolver, and every declared verb must have a real backend.
**What+How:** `check_raw_toml_readers` (~735) scans `usr/lib/mios/agent-pipe/**.py` and flags any module reading `os.environ['MIOS_TOML']` directly or hardcoding an `open()` of mios.toml, enforcing rule B11 (all reads go through `mios_toml.load_merged()`/`load_vendor()`); `check_verb_backends` (~1776) parses `mios.toml [verbs.*]` and asserts each declared backend actually exists on disk. Port both to Rust -- a regex/AST-lite scan for the raw-reader anti-pattern and a TOML walk for backend existence -- preserving the `test_*` exemptions.
**Where:** `automation/98-drift-checks.sh (check_raw_toml_readers ~735, check_verb_backends ~1776), usr/lib/mios/agent-pipe, usr/lib/mios/mios_toml.py, mios.toml [verbs.*]`
**Done When:** Rust flags raw `MIOS_TOML`/mios.toml readers in agent-pipe and verbs with a missing backend, byte-parity vs bash, honoring `test_*` exemptions; negatives green.
**Why:** B11 is the guardrail that keeps the new Rust resolver the ONLY config entrypoint; unported, agent-pipe modules silently reacquire private config paths and the SSOT stops being single.
**Dep:** Build a figment-based Rust resolver and differential-test it against the userenv.sh/mios_toml.py twins

### AGY-1244 -- Port the network-fetch degrade-open and retired-technology regression guards to Rust  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- four small, load-bearing architectural-decision lints survive the cutover.
**What+How:** Port four unassigned single-purpose lints with identical file globs, comment-stripping and skip semantics: `check_curl_retry` (~4177) flags any curl/wget to a remote https URL in Containerfiles/automation/libexec lacking `--retry`/`--tries`/scurl (the degrade-open producer rule); `check_vendor_urls` (~3102) flags direct vendor API hosts (api.openai/anthropic/googleapis/...) in ACTIVE, non-comment config under `SCAN_DIRS`; `check_impossible_eol_regressions` (~3478) rejects resurrected sunset tech (glusterfs/tang packages, unqualified "mdevctl vGPU" claims); `check_vendored_assets_non_stub` (~6038) flags sub-100-byte stubs under `usr/share/mios/vendored`.
**Where:** `automation/98-drift-checks.sh (check_curl_retry ~4177, check_vendor_urls ~3102, check_impossible_eol_regressions ~3478, check_vendored_assets_non_stub ~6038), SCAN_DIRS, mios.toml, usr/share/mios/vendored`
**Done When:** Rust reproduces all four verdicts byte-for-byte vs bash on clean trees and on each injected drift (un-retried fetch, vendor URL in active config, glusterfs/tang reintroduced, sub-100B stub); negatives green.
**Why:** These encode the degrade-open directive and the recorded sunset decisions; with no assigned title covering them, a build could quietly reacquire an unretried network fetch or a resurrected gluster dependency.
**Dep:** Build the bash-vs-Rust golden-master + differential parity harness for drift checks

### AGY-1245 -- Port the mios_pipe extraction-integrity checks: check_pipe_boundaries + check_pipe_extraction_parity  (WS-DRIFTRUST | P2 | M)  **[DONE]**
**Goal:** E-02 Technical-debt retirement -- the agent-pipe decomposition keeps its one-way-import boundary enforced by compiled code.
**What+How:** `check_pipe_extraction_parity` (~5639) delegates to `tools/pipe-parity-check.py` to assert the `mios_pipe` extraction-surface parity and the one-way import rule across the 123 agent-pipe modules; `check_pipe_boundaries` (~5619) validates the freshness of `usr/share/mios/pipe-boundaries.manifest.json`. These are distinct from the `check_module_*` hygiene family and from the `agent_pipe_budgets` check already assigned to `mios-aiplane-lint`. Port the import-graph/boundary analysis and the manifest freshness check to Rust (standalone or folded into mios-aiplane-lint) and reduce `tools/pipe-parity-check.py` to a shim.
**Where:** `automation/98-drift-checks.sh (check_pipe_boundaries ~5619, check_pipe_extraction_parity ~5639), tools/pipe-parity-check.py, usr/share/mios/pipe-boundaries.manifest.json, usr/lib/mios/agent-pipe`
**Done When:** Rust reproduces extraction-surface parity, the one-way import rule and manifest freshness with byte-parity verdicts vs bash; `tools/pipe-parity-check.py` is retired or a shim; negatives green.
**Why:** Otherwise the boundary gate stays as bash calling a Python side-tool -- the exact fragile chain the port exists to remove, guarding the decomposition of a ~9k-line god-module.
**Dep:** Port the AI-plane lint family onto the existing mios-aiplane-lint crate

### AGY-1246 -- Sweep the five orphan checks and delete the shared bash scaffolding so ZERO bash check_* bodies survive  (WS-DRIFTRUST | P2 | L)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the cutover finishes completely instead of leaving a half-migrated gate.
**What+How:** After the family ports, five orphans remain: `check_canonical_bools` (~2485, TOML bool canonicalization), `check_council_gate_ssot` (~4427), `check_package_registry` (~625, dormant behind `[ai].package_registry`), `check_replaceme_mount_substitution` (~4949, REPLACEME mount placeholders in the Justfile), `check_fluff_tokens` (~3386, anti-fluff token lint over automation/*.sh). Port each to Rust with parity plus a negative, then delete the now-empty bash scaffolding -- the `_violation` counter, `SCAN_DIRS`, the python3 shim, `_emit_projection_evidence`, and the `main()` dispatch table -- and reconcile the `check_gate_index`/`check_gate_registry` counts.
**Where:** `automation/98-drift-checks.sh (check_canonical_bools ~2485, check_council_gate_ssot ~4427, check_package_registry ~625, check_replaceme_mount_substitution ~4949, check_fluff_tokens ~3386, shared helpers + main() ~6267)`
**Done When:** All five are ported with verdict parity plus negatives; `automation/98-drift-checks.sh` contains no remaining `check_*` function body; the shared bash helpers are gone; `check_gate_index`/`check_gate_registry` counts reconcile; `just drift-gate` green.
**Why:** Without an explicit backstop these orphans and the shared scaffolding survive the delete step, so the tree carries two gate implementations forever and the gate-count checks stay red.
**Dep:** Cut over the default drift runner to Rust and delete the retired bash families

### AGY-1247 -- Mirror the Rust drift binary and [migration] toggle into mios-bootstrap.git behind a cross-repo parity gate (Law 15)  (WS-DRIFTRUST | P1 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- the installer repo runs the same compiled gate as the OS repo instead of a divergent bash copy.
**What+How:** `mios-bootstrap.git` consumes shared SSOT surfaces (mios.toml, the drift config) and, once the gate is a binary, must ship and consume the same `miosd` plus the `[migration]` toggle. Per Law 15 (double-repo-triple-check), add a check in `src/mios-rs/miosd` that asserts the drift binary source, the `[migration]` toggle and any shared drift config are byte-identical across the two repos, and land the migration in BOTH repos in the same change set. Preserve the `${VAR:-LIT}` literal-fallback discipline on any floated value so the bootstrap path degrades open.
**Where:** `usr/share/mios/mios.toml (shared SSOT + [migration]), mios-bootstrap.git (shadow: M:\MiOS\bootstrap-shadow), a new cross-repo parity check in src/mios-rs/miosd`
**Done When:** The new check fails on any divergence of the drift binary, the `[migration]` toggle or shared drift config between the repos; both repos are updated in the same commit set; the check is green after mirroring.
**Why:** No other assigned title propagates the binary and toggle into the installer repo, so the two repos silently fork -- a guaranteed drift source, and Law 15 is unenforced without a check.
**Dep:** Add the [migration] SSOT toggle with oxidizr-style per-family Rust/bash opt-out and rollback

### AGY-1248 -- Unify the template placeholder vocabulary into one SSOT-declared token set  (WS-TEMPLATE | P1 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- one token vocabulary so every generator renders the same templates identically.
**What+How:** Three consumers disagree on placeholders: `usr/libexec/mios/mios-new` `render_template()` substitutes `{{name}},{{PascalName}},{{id}},{{title}},{{status}},{{date}},{{description}},{{image}},{{uid}},{{gid}},{{theme}},{{priority}},{{task_title}},{{task_id}}`; `usr/libexec/mios/mios-template-engine` `render_template()` substitutes only `{{filename}},{{path}},{{description}},{{date}}` -- tokens that do not appear in the templates, so it leaves `{{name}}` unrendered; `tools/compile-templates.py` `MOCK_VALS` is a third partial set. Add a canonical `[templates.placeholders]` table (token -> description/mock-value) to `usr/share/mios/mios.toml`, point all three consumers at it, and extend the `check_templates_compilation` region (automation/98-drift-checks.sh:3458) to assert every `{{token}}` literal in `usr/share/mios/templates/` is declared and that scaffolding leaves zero `{{...}}` residue. Mirror the table into mios-bootstrap.git (Law 15).
**Where:** `usr/share/mios/mios.toml ([templates.placeholders]); usr/libexec/mios/mios-new; usr/libexec/mios/mios-template-engine; tools/compile-templates.py; automation/98-drift-checks.sh`
**Done When:** No template file contains an undeclared `{{token}}`; `mios new <type> <name>` for every registered type emits zero residual `{{...}}`; the new drift-check fails when an undeclared placeholder is introduced; the table is byte-identical in both repos.
**Why:** The orphan engine is silently broken (wrong placeholder scheme) and the three schemes drift independently, so scaffolded files ship with literal `{{name}}` in them -- and porting any consumer to Rust would bake the divergence in.
**Dep:** none

### AGY-1249 -- Retire the divergent duplicate generator mios-template-engine behind mios-new  (WS-TEMPLATE | P1 | S)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- exactly one reachable generator to serve as the parity oracle.
**What+How:** `usr/libexec/mios/mios-template-engine` is a second scaffolder duplicating `usr/libexec/mios/mios-new` with an incompatible placeholder scheme (the `{{filename}}`/`{{path}}` mismatch) and is not wired into the CLI (`usr/bin/mios` line 422 dispatches `new` -> mios-new; `Justfile:500` `new` -> mios-new). Grep both repos for callers; if none are load-bearing, either demote it to a thin shim that `exec`s mios-new with translated args, or delete it and stage the removal with explicit paths.
**Where:** `usr/libexec/mios/mios-template-engine; usr/libexec/mios/mios-new; usr/bin/mios; Justfile`
**Done When:** `grep -rn mios-template-engine` across both repos returns only the shim or the removal commit and no functional callers; one generator remains reachable; drift gate green.
**Why:** Two divergent generators is the duplication smell the operator flagged; compile it as-is and the Rust port inherits a second, broken renderer that no golden harness can arbitrate.
**Dep:** Unify the template placeholder vocabulary into one SSOT-declared token set

### AGY-1250 -- Capture golden-master fixtures and a byte-parity harness for `mios new` across all registered types  (WS-TEMPLATE | P1 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- a characterization oracle exists BEFORE any scaffolder code is rewritten.
**What+How:** Record current observable output as the oracle. Add a fixtures dir (`tests/templates/golden/` or `tools/native/mios-template-conform/tests/golden/`) holding the byte-exact result of `usr/libexec/mios/mios-new <type> <name>` for EVERY key in `[templates.*]` (adr, drift-check, automation-step, bash-verb, bash-tool, bash, python-module, python-test, python-tool, rust, typescript, powershell, toml-config, yaml, json-schema, roadmap, roadmap-ws, markdown-doc, systemd-unit, quadlet) with a fixed mock name and a pinned date (patch `datetime.now` via env or a `--date` flag). Add a trycmd-style runner that re-invokes the generator and asserts byte-identity against each `.snap`; commit fixtures with explicit `git add` of the fixture paths.
**Where:** `usr/libexec/mios/mios-new; usr/share/mios/mios.toml ([templates.*]); tests/templates/golden/ (new)`
**Done When:** The harness runs green against current mios-new output; editing any template or the generator in a way that changes output fails the harness until the snapshot is reviewed; every registered type has a committed golden fixture.
**Why:** Without a characterization oracle the coming Python->Rust scaffolder port is a blind rewrite whose regressions surface only as malformed files scaffolded fleet-wide.
**Dep:** Unify the template placeholder vocabulary into one SSOT-declared token set

### AGY-1251 -- Capture golden CLI snapshots (stdout/stderr/exit) for check-template-conformance  (WS-TEMPLATE | P1 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- the Law 16 fitness function's observable behavior is pinned before it is ported.
**What+How:** Snapshot the exact CLI behavior of `usr/libexec/mios/check-template-conformance` as the oracle for its Rust port. Add trycmd `.md` fixtures for four cases: a fully conformant mini-tree (exit 0 plus the `[conformance] checked=.. unconforming=0 ceiling=0` line); a file missing its AI-hint header; a file missing a required body marker (e.g. a systemd-unit with no `[Install]`); and a grandfathered file (skipped). Pin `MIOS_THEME_ROOT`/`MIOS_TOML_ROOT` and `--root` at a fixture tree for determinism, asserting stdout, stderr and exit code.
**Where:** `usr/libexec/mios/check-template-conformance; automation/98-drift-checks.sh (check_template_conformance:3202); tests/templates/conform-cli/ (new)`
**Done When:** The fixtures reproduce current pass/fail stdout+stderr+exit byte-for-byte; deliberately removing a header flips the snapshot to the failing case.
**Why:** A gate whose output silently changes during a port stops being trustworthy -- and this one is the enforcer of the template law, so a behavior drift here disables enforcement without turning anything red.
**Dep:** Capture golden-master fixtures + byte-parity harness for mios new across all registered types

### AGY-1252 -- Port check-template-conformance to a Rust crate and demote the Python to a fallback shim  (WS-TEMPLATE | P2 | L)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- Law 16's enforcer becomes compiled, with the Python retained as a one-flag rollback.
**What+How:** Create `tools/native/mios-template-conform` (toml 0.8 + regex, matching the miosd/mios-ssot-lint dependency style) reproducing `usr/libexec/mios/check-template-conformance`: read `[templates.*]` from mios.toml, compile each `match` regex, walk the tree porting `mios-ai-tag.walk`'s ignore rules, verify `required_header` via the AI-hint header, verify `required_markers`, honor `usr/share/mios/templates/conformance-grandfathered.list`, and enforce the `[ai_tag].max_unconforming=0` ceiling. Add it to `tools/native/Cargo.toml` members. Then make `check_template_conformance` (98-drift-checks.sh:3202) prefer the binary via `${MIOS_CONFORM_BIN:-<repo>/target/release/mios-template-conform}` and degrade-open to the Python tool when absent, reducing the Python script to that fallback role. Prove byte-parity against the golden CLI snapshots with `cargo test`.
**Where:** `tools/native/mios-template-conform/ (new); tools/native/Cargo.toml; automation/98-drift-checks.sh; usr/libexec/mios/check-template-conformance`
**Done When:** `cargo test` parity is green against the conformance golden snapshots; the drift gate is green with the binary present and SOFT-skips/falls back to Python when absent; the Python only executes as fallback.
**Why:** The most load-bearing template fitness function currently runs through shell+python fragility on every build; leaving it there keeps a Law-16 failure one missing interpreter away.
**Dep:** Capture golden CLI snapshots (stdout/stderr/exit) for check-template-conformance

### AGY-1253 -- Port the compile-templates.py golden round-trip compiler to Rust and demote the Python  (WS-TEMPLATE | P2 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- template validation consolidates into the native workspace and off the build gate's python dependency.
**What+How:** Reimplement `tools/compile-templates.py` (invoked by `check_templates_compilation`, 98-drift-checks.sh:3458) as `tools/native/mios-template-compile`: substitute the SSOT mock placeholder values into each `usr/share/mios/templates/` file, then validate per registered type -- TOML and JSON natively in Rust, python and bash by shelling to `python -c compile` / `bash -n` exactly as today, keeping the `os.name != 'nt'` skip semantics. Add the crate to the workspace, wire `check_templates_compilation` to prefer the binary behind a `${MIOS_TCOMPILE_BIN:-...}` guard with Python fallback (degrade-open), and prove parity against the scaffold golden fixtures.
**Where:** `tools/compile-templates.py; tools/native/mios-template-compile/ (new); tools/native/Cargo.toml; automation/98-drift-checks.sh`
**Done When:** The Rust compiler reports the same PASS/FAIL set as `tools/compile-templates.py` over the current template set; `check_templates_compilation` is green via the binary and falls back to Python when the cargo output is absent.
**Why:** The round-trip compiler is what guarantees every template can render to a valid file; keeping it in Python leaves the build gate dependent on an interpreter and outside clippy/CI coverage.
**Dep:** Capture golden-master fixtures + byte-parity harness for mios new across all registered types

### AGY-1254 -- Implement `miosd scaffold` (fold mios-new into Rust) and demote mios-new to a shim  (WS-TEMPLATE | P2 | L)  **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- the scaffolder itself becomes compiled while `mios new` stays the stable facade.
**What+How:** `src/mios-rs/miosd/src/main.rs` already has a `Commands::Scaffold` arm that is a stub (lines 46-50, `// TODO: Fold mios-new Python logic into this Rust command`). Port `mios-new`'s `render_template()` and `get_dest_path()` into miosd (which already deps clap/toml/serde/regex), reading destination routing from the SSOT fields added by the dest-routing task instead of hardcoding. Prove byte-parity against the scaffold golden fixtures via `cargo test`, then demote `usr/libexec/mios/mios-new` to a thin shim that execs `miosd scaffold` when the binary exists (`${MIOS_MIOSD:-...}`) and runs the Python body only as fallback; keep `usr/bin/mios` line 422 and `Justfile:500` dispatching through the shim.
**Where:** `src/mios-rs/miosd/src/main.rs; usr/libexec/mios/mios-new; usr/bin/mios; Justfile; usr/share/mios/mios.toml ([templates.*])`
**Done When:** `miosd scaffold <type> <name>` output is byte-identical to the golden fixtures for every registered type; mios-new runs only as fallback; `mios new` and `just new` still produce conformant files.
**Why:** The verb dispatcher is the ideal strangler seam; without taking it, the scaffolder stays interpreted script in the NS-5 compiled tier and every new file type is authored twice.
**Dep:** Move the hardcoded dest-path routing table into the [templates.*] SSOT

### AGY-1255 -- Move the hardcoded dest-path routing table into the [templates.*] SSOT  (WS-TEMPLATE | P1 | M)  **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- routing becomes data in mios.toml so Rust and Python read one source.
**What+How:** `usr/libexec/mios/mios-new` `get_dest_path()` (lines 61-152) hardcodes per type the destination directory plus filename normalization: `name_prefix` (e.g. `mios-`), suffix (`.sh`/`.py`/`.service`), ordinal defaults (`99-`/`0012-`), and stdout-only emission for drift-check. Promote these to per-type fields on `[templates.<type>]` in `usr/share/mios/mios.toml` (`dest_dir`, `name_prefix`, `name_suffix`, `ordinal_default`, `emit=file|stdout`) and rewrite `get_dest_path()` to read only the SSOT. Add a drift-check that every registered type has a resolvable dest rule, and mirror the added fields into mios-bootstrap.git (Law 15).
**Where:** `usr/libexec/mios/mios-new; usr/share/mios/mios.toml ([templates.*]); automation/98-drift-checks.sh`
**Done When:** `get_dest_path` derives every path from `[templates.*]` with no per-type Python branches; adding a template type requires only a mios.toml row; the drift-check is green; the fields exist in both repos.
**Why:** The routing table is config authored twice -- once in Python, once implied by the templates -- so the Rust port and the Python shim would immediately diverge on where files land.
**Dep:** Unify the template placeholder vocabulary into one SSOT-declared token set

### AGY-1256 -- Generate the `mios new` type list and usage text from [templates.*]  (WS-TEMPLATE | P2 | S)  **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- the type roster is derived, not hand-listed.
**What+How:** `usr/libexec/mios/mios-new` `main()` line 157 prints a hand-maintained usage string listing the 20 types, which silently drifts whenever a `[templates.*]` row is added. Replace the literal with a list rendered from the loaded `[templates.*]` config (plus a `--list` mode), add a drift-check asserting the emitted type set equals the `[templates.*]` key set exactly, and carry the same generated list into `miosd scaffold`'s help once that lands.
**Where:** `usr/libexec/mios/mios-new; usr/share/mios/mios.toml ([templates.*]); automation/98-drift-checks.sh`
**Done When:** No hardcoded type list remains in mios-new; `mios new` with no args lists exactly the registered types; the drift-check fails when usage and `[templates.*]` diverge.
**Why:** A hand-listed roster is a projection authored twice -- new template types stay invisible to anyone reading the CLI help, so they never get used.
**Dep:** Move the hardcoded dest-path routing table into the [templates.*] SSOT

### AGY-1257 -- Extend conformance to validate ordered BODY STRUCTURE, not just substring markers  (WS-TEMPLATE | P2 | M)  **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- the promised body-structure validation, not just header presence, is enforced.
**What+How:** `check-template-conformance` today verifies only the AI-hint header plus `marker in content` (substring, anywhere, unordered). Add structural rules: `[templates.systemd-unit].required_markers` (`[Unit]`,`[Service]`,`[Install]`) must appear IN ORDER; python-tool/python-module must contain `def main(`, `if __name__ ==` and a helper def; a `[templates.drift-check]` body must define `check_<name>()`; quadlet must contain `[Container]`/`[Unit]`. Encode these as new per-type fields `required_ordered` and/or `body_regex` in mios.toml, enforce them in the Rust `mios-template-conform` checker with the Python fallback kept in sync, and add negative fixtures (a systemd unit with reordered sections) to the conformance golden set.
**Where:** `usr/share/mios/mios.toml ([templates.*]); tools/native/mios-template-conform/; usr/libexec/mios/check-template-conformance; tests/templates/conform-cli/`
**Done When:** A systemd unit with `[Install]` before `[Unit]`, or a python-tool lacking `def main(`, fails conformance; the new `required_ordered`/`body_regex` fields are drift-gated and mirrored in both repos.
**Why:** The WS-TEMPLATE acceptance explicitly promised body-structure validation; substring markers pass files that are structurally wrong, so the law reads as enforced while malformed units keep shipping.
**Dep:** Port check-template-conformance to a Rust crate and demote the Python to a fallback shim

### AGY-1258 -- Self-consistency gate: every registered template must scaffold to a conforming file  (WS-TEMPLATE | P2 | M)  **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- the templates themselves become ground truth, verified round-trip.
**What+How:** Add a meta-check that, for each key in `[templates.*]`, scaffolds a mock file (via `miosd scaffold`/`mios-new`) into a temp dir and pipes it through the conformance checker, delivering the ROADMAP acceptance "a template that can't produce a conformant file fails the build." Implement it either as an extension of `check_templates_compilation` (98-drift-checks.sh:3458) or as a dedicated `check_template_self_conformance`, degrading-open when python3/cargo are missing (mirroring the SOFT-skip at check_template_conformance:3203). Add a negative in `tests/drift-gate-negatives.sh` proving that dropping a template's AI-hint header turns the gate red.
**Where:** `automation/98-drift-checks.sh; tools/compile-templates.py or tools/native/mios-template-compile/; usr/share/mios/templates/; tests/drift-gate-negatives.sh`
**Done When:** Every template round-trips to a conforming file; deliberately corrupting one template (removing its AI-hint or a required marker) fails the gate; the negative test asserts that failure.
**Why:** A template can otherwise drift into producing non-conforming files that the scaffolder then propagates fleet-wide, with the conformance gate blaming the generated files instead of the template.
**Dep:** Extend conformance to validate ordered BODY STRUCTURE, not just substring markers

### AGY-1259 -- Add the 5 orphan native crates to the tools/native workspace members  (WS-TEMPLATE | P1 | S)  **[DONE]**
**Goal:** E-05 Test and CI governance -- one consolidated Rust workspace so no crate is invisible to compilation and lint.
**What+How:** `tools/native/Cargo.toml` `members` lists only mios-drift-runner, mios-version-check and mios-wallpaperd, yet `tools/native/` also holds generate-names-registry, mios-aiplane-lint, mios-bake-plan, mios-ssot-lint (each with `src/main.rs`) and mios-ssot-walk (`src/lib.rs`) -- all excluded, so `cargo build` never compiles them and no lint or CI gate covers them. Add the five to `members`, fix the resulting compile errors, and add a drift-check asserting every directory containing a `tools/native/*/Cargo.toml` appears in the members array. Stage `tools/native/Cargo.toml` and fixups with explicit paths (never `git add -A` -- AGY shares the tree).
**Where:** `tools/native/Cargo.toml; tools/native/{generate-names-registry,mios-aiplane-lint,mios-bake-plan,mios-ssot-lint,mios-ssot-walk}/; automation/98-drift-checks.sh`
**Done When:** `cargo build --workspace` in tools/native builds all 8 crates; the new membership drift-check fails when a crate dir is missing from `members`; all changes staged path-explicit.
**Why:** Crates outside the workspace are invisible to compilation, clippy and CI -- they bitrot silently and can ship broken, and no workspace-wide lint or audit gate can exist until they are in.
**Dep:** none

### AGY-1260 -- Add a `cargo fmt --check` + `clippy -D warnings` quality gate for the native workspace  (WS-TEMPLATE | P2 | M)  **[DONE]**
**Goal:** E-05 Test and CI governance -- the compiled tier finally gets the static-analysis gate the shell tier never had.
**What+How:** Add a `check_native_lint` drift-check running `cargo fmt --check` and `cargo clippy --workspace -- -D warnings` over `tools/native` and `src/mios-rs`, with curated restriction lints (`clippy::unwrap_used`, `clippy::panic`, `clippy::todo`) so system tooling cannot panic or unwrap. Degrade-open with a SOFT skip when cargo is absent, exactly like the `command -v python3` guard at 98-drift-checks.sh:3203. Slot it after the negatives and before the main gate, per the documented CI ordering.
**Where:** `automation/98-drift-checks.sh; tools/native/; src/mios-rs/; tools/native/Cargo.toml`
**Done When:** The gate runs `cargo fmt --check` + `cargo clippy -- -D warnings` green on the workspace, fails on a deliberate `unwrap()` or formatting violation, and SOFT-skips cleanly on a host without cargo.
**Why:** MiOS has no static analysis on its compiled code, so every ported crate can reintroduce panic/unwrap crash classes into system verbs with nothing to catch them.
**Dep:** Add the 5 orphan native crates to the tools/native workspace members

### AGY-1261 -- Cover the two uncovered file types: add cargo-manifest and systemd-timer templates  (WS-TEMPLATE | P2 | S)  **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- closes two holes in the per-type coverage set so no shipped file type is authored ad hoc.
**What+How:** No template exists for a Rust crate manifest (every crate this campaign adds -- `mios-template-conform`, `mios-template-compile` -- hand-writes its `Cargo.toml`) nor for systemd `.timer` units (`[templates.systemd-unit]` covers only `.service`). Author `usr/share/mios/templates/cargo-manifest` (AI-hint comment header, `[package]` with `version.workspace`/`edition.workspace` matching the existing crates, placeholder name) and `usr/share/mios/templates/systemd-timer` (AI-hint header + `[Unit]`/`[Timer]`/`[Install]`). Register both in `[templates.*]` with `match`/`comment`/`required_header`/`required_markers`, add their tokens to `[templates.placeholders]`, and include them in the round-trip compiler validation in `tools/compile-templates.py`.
**Where:** `usr/share/mios/templates/cargo-manifest (new); usr/share/mios/templates/systemd-timer (new); usr/share/mios/mios.toml ([templates.*]); tools/compile-templates.py`
**Done When:** `mios new cargo-manifest foo` and `mios new systemd-timer foo` emit files that pass `check_template_conformance` and the round-trip compiler; both types appear in `[templates.*]` and are mirrored into mios-bootstrap per Law 15.
**Why:** Uncovered file types get hand-authored and drift from convention immediately -- and the cargo-manifest gap is actively costing time now that the campaign is adding crates one hand-written `Cargo.toml` at a time.
**Dep:** Unify the template placeholder vocabulary into one SSOT-declared token set

### AGY-1262 -- Split the one-size quadlet template into per-kind scaffolds (.container/.pod/.network/.volume)  (WS-TEMPLATE | P2 | M)  **[DONE]**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- every Quadlet kind is born conformant instead of born broken.
**What+How:** `[templates.quadlet].match` (mios.toml:11437) claims `.container|.pod|.network|.volume|.image`, but the single `usr/share/mios/templates/quadlet` body is a `.container` skeleton, so `mios new quadlet foo.network` emits an invalid unit. Split into per-kind bodies `quadlet-container`, `quadlet-pod`, `quadlet-network`, `quadlet-volume`, each carrying the correct section (`[Container]`/`[Pod]`/`[Network]`/`[Volume]`) and the AI-hint generator-relation header, and teach the `usr/libexec/mios/mios-new` scaffolder to select the body from the requested extension. Register each in `[templates.*]` and add them to the compiler + conformance validation. Quadlet is `generated=true`, so these stay scaffolds only -- never committed outputs.
**Where:** `usr/share/mios/templates/quadlet; usr/share/mios/templates/quadlet-{pod,network,volume} (new); usr/share/mios/mios.toml ([templates.*]); usr/libexec/mios/mios-new`
**Done When:** Scaffolding each Quadlet kind produces a syntactically valid unit with the correct section header; all four pass the round-trip compiler; all four registered and mirrored per Law 15.
**Why:** A `.container` template masquerading as universal silently emits broken `.pod`/`.network`/`.volume` units that fail only at podman-generate time, far from the authoring step.
**Dep:** Move the hardcoded dest-path routing table into the [templates.*] SSOT

### AGY-1263 -- Ratchet down the grandfather list: conform a ~20-unit tranche of the 75 systemd units  (WS-TEMPLATE | P2 | L)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- shrinks the exemption set that lets Law 16 be green while most files are unconformed.
**What+How:** `usr/share/mios/templates/conformance-grandfathered.list` exempts 75 `usr/lib/systemd/system/*.service` entries from `[templates.systemd-unit]` (which requires an AI-hint header plus `[Unit]`/`[Service]`/`[Install]`). Take a ~20-unit tranche: add the missing AI-hint header with `usr/libexec/mios/mios-ai-tag`, confirm the three sections are present and correctly ordered, then DELETE those exact lines from `conformance-grandfathered.list` while `[ai_tag].max_unconforming` stays 0. Stage each edited unit and the list with explicit paths (never `git add -A` -- the tree is shared).
**Where:** `usr/lib/systemd/system/*.service; usr/share/mios/templates/conformance-grandfathered.list; usr/libexec/mios/mios-ai-tag; automation/98-drift-checks.sh`
**Done When:** ~20 unit paths are gone from `conformance-grandfathered.list`, those units pass `check_template_conformance` with the ceiling still 0, the list is strictly shorter than before, and every change was staged path-explicit.
**Why:** The 430-line grandfather list is the escape hatch that makes Law 16 technically-green while the tree is largely unconformed; without a tranche-by-tranche ratchet the law never becomes real enforcement.
**Dep:** Extend conformance to validate ordered BODY STRUCTURE, not just substring markers

### AGY-1264 -- Ratchet down the grandfather list: conform a tranche of the 171 bash tools / 131 python tests  (WS-TEMPLATE | P2 | L)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- attacks the highest-count block of exemptions so ONE-TEMPLATE-PER-TYPE actually holds tree-wide.
**What+How:** `conformance-grandfathered.list` also exempts 171 `usr/libexec/mios` entries and 131 `test_mios_*.py` files from `[templates.bash-tool]`/`[templates.python-test]`. Take a bounded tranche (~25 tools + ~25 tests): run `usr/libexec/mios/mios-ai-tag` to add the AI-hint header, add any required body markers (`[templates.python-test]` expects a `Test<Pascal>` class; bash tools expect the header), then remove exactly those lines from the list. Keep `[ai_tag].max_unconforming` at 0 and stage explicit paths.
**Where:** `usr/libexec/mios/*; usr/lib/mios/agent-pipe/test_mios_*.py; usr/share/mios/templates/conformance-grandfathered.list; usr/libexec/mios/mios-ai-tag`
**Done When:** ~50 paths removed from the grandfather list; those files pass conformance at ceiling 0; the list is strictly shorter; `just drift-gate` green.
**Why:** Bash tools and Python tests are the bulk of the exemption set -- leaving them exempt means the majority of MiOS source is outside the template law no matter how good the templates get.
**Dep:** Extend conformance to validate ordered BODY STRUCTURE, not just substring markers

### AGY-1265 -- Make the grandfather list a monotonic, never-grow ratchet  (WS-TEMPLATE | P2 | S)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- makes de-grandfathering permanent progress rather than a reversible win.
**What+How:** Today nothing stops a new non-conforming file from being papered over by APPENDING it to `usr/share/mios/templates/conformance-grandfathered.list` instead of conforming it. Add a drift-check that the list's line count never exceeds a ceiling recorded in `mios.toml` (e.g. `[ai_tag].grandfather_ceiling`) -- or, when git is available, that the list is a strict subset of its `git HEAD` version -- so the exemption set can only shrink. Wire it as its own numbered check in `automation/98-drift-checks.sh` and add a negative test in `tests/drift-gate-negatives.sh` asserting that appending a line fails.
**Where:** `usr/share/mios/templates/conformance-grandfathered.list; usr/share/mios/mios.toml ([ai_tag]); automation/98-drift-checks.sh; tests/drift-gate-negatives.sh`
**Done When:** Appending any new line to the grandfather list turns the gate RED; removing lines passes; the recorded ceiling decreases as each ratchet tranche lands.
**Why:** A ratchet with no pawl is just a suggestion -- without monotonicity the two de-grandfathering tranches get silently undone by the next author who finds appending easier than conforming.
**Dep:** none

### AGY-1266 -- Emit miette-style spanned diagnostics from the Rust conformance checker  (WS-TEMPLATE | P3 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- cashes in the compiled-checker port as a real operator-UX upgrade.
**What+How:** The Python checker prints opaque lines like `path: Missing AI-hint header (required by systemd-unit)` with no location. In the `mios-template-conform` crate, model internal errors as `thiserror` enums and render operator-facing failures with `miette`: point the diagnostic at the exact byte span (the first non-comment line for a missing header, or where the missing marker was expected), using byte offsets so UTF-8 highlighting is correct, with help text (`run \`mios new <type>\`` / `add \`# AI-hint:\`` / `add an [Install] section`). Keep the one-line summary format that the drift-gate greps for byte-identical so the golden CLI snapshots still match -- the rich report goes to stderr only.
**Where:** `tools/native/mios-template-conform/; automation/98-drift-checks.sh; tests/templates/conform-cli/`
**Done When:** A non-conforming file yields a spanned, help-annotated diagnostic on stderr while the machine-parseable summary line is unchanged; internal errors are matchable `thiserror` variants; golden snapshots updated and green.
**Why:** Today an author gets a path and a vague noun and has to hunt the offending line by hand; that friction is why people reach for the grandfather list instead of fixing the file.
**Dep:** Port check-template-conformance to a Rust crate and demote the Python to a fallback shim

### AGY-1267 -- Add a cross-repo parity gate for the [templates.*] SSOT and the templates directory  (WS-TEMPLATE | P3 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- makes the mirrored template surface Law-15 enforceable instead of trust-based.
**What+How:** The `[templates.*]` table and the `usr/share/mios/templates/` file set are shared SSOT under Law 15, yet no check enforces that mios.git and mios-bootstrap.git agree. Add a parity drift-check modeled on the existing cross-repo parity checks 22/27 (referenced in the `[laws]` Law 15 row) asserting that the `[templates.*]` key set and each template file's bytes are identical -- or a declared, justified subset -- across both repos, given a resolvable path to the sibling checkout. Degrade-open with a SOFT skip when the sibling repo is absent, and document the intentional-divergence escape hatch.
**Where:** `usr/share/mios/mios.toml ([templates.*]); usr/share/mios/templates/; automation/98-drift-checks.sh`
**Done When:** Divergence between the two repos' template surface fails the gate when both checkouts are present; the check SOFT-skips cleanly when the sibling is absent; declared subsets pass.
**Why:** Two hand-mirrored copies with no comparator is exactly the drift Law 15 exists to stop -- and template drift means the two repos scaffold different files from the same command.
**Dep:** Move the hardcoded dest-path routing table into the [templates.*] SSOT

### AGY-1268 -- Register the template toolchain in [laws.projection_registry] so Law 16 is audited like Law 8  (WS-TEMPLATE | P3 | S)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- lands Law 16 as ADR + registry row + check, with no surface exempt from the meta-audit.
**What+How:** Law 16 (ONE-TEMPLATE-PER-TYPE, mios.toml:723) is enforced by `check_template_conformance`, but the scaffolder and the round-trip compiler are not listed in `[laws.projection_registry].surfaces` (mios.toml:735-743), so `check_projection_registry` does not audit the template generators the way it audits `mios-theme-render` and `mios-sync-toml`. Add registry rows mapping the template generator (`mios new` / `miosd scaffold`) and the compiler to their enforcing checks (`check_template_conformance`, `check_templates_compilation`), so a template surface with no backing drift-check fails. Mirror the added rows to mios-bootstrap per Law 15.
**Where:** `usr/share/mios/mios.toml ([laws.projection_registry]); automation/98-drift-checks.sh (check_projection_registry)`
**Done When:** `check_projection_registry` lists and verifies the template generator + compiler surfaces; deleting either backing check turns the gate RED; the rows are present in both repos.
**Why:** The projection registry is the meta-gate that guarantees every projection surface has an enforcer; a template system invisible to it can lose its enforcement silently and nothing notices.
**Dep:** Implement `miosd scaffold` (fold mios-new into Rust) and demote mios-new to a shim

### AGY-1269 -- Add a generative end-to-end conformance negative test to the drift-gate harness  (WS-TEMPLATE | P2 | M)
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- proves the Law 16 gate can actually FAIL, on the exact path the build uses.
**What+How:** Distinct from the round-trip compiler self-check: add a case to `tests/drift-gate-negatives.sh` that scaffolds each `[templates.*]` type into a temp tree and runs the full `check_template_conformance` path over it (the real gate, not the compiler), proving generator and checker are mutually consistent end-to-end. Include a negative variant where a template is corrupted (header removed) and assert the gate goes RED, following the documented negatives-before-main-gate CI ordering. Keep it degrade-open when `python3`/`cargo` is unavailable.
**Where:** `tests/drift-gate-negatives.sh; automation/98-drift-checks.sh (check_template_conformance); usr/libexec/mios/mios-new; usr/share/mios/templates/`
**Done When:** The negative test shows a corrupted template caught by the real conformance gate and a clean scaffold of every registered type passing; it runs in the negatives phase before the main gate.
**Why:** The compiler self-check and the live conformance gate can drift apart, and when they do the gate reports green on files the scaffolder is producing wrong -- a green gate that enforces nothing.
**Dep:** Self-consistency gate: every registered template scaffolds to a conforming file

### AGY-1270 -- Add the TD-8 doc-metric re-derivation drift-check  (WS-TEMPLATE | P3 | M)
**Goal:** E-02 Technical-debt retirement: the TD-1..TD-8 register -- closes TD-8 so documented line-count claims are derived, not asserted.
**What+How:** ROADMAP TD-8 (line 335) assigns the metric-re-derivation gate to WS-TEMPLATE/WS-DEBT: docs claim line counts (e.g. `server.py` "~26k" against an actual ~8.1k) with no gate. Add a `check_doc_metric_derivation` drift-check that scans tracked docs and the roadmap for `~<N>k` / `<N> lines` / `<N>-ln` claims adjacent to a tracked file path and fails when the claim deviates from the real `wc -l` (or a `git ls-files`-derived count) beyond a tolerance. Degrade-open when git/wc are unavailable, allow an explicit per-claim exemption comment, and seed the check by correcting the known `server.py` overclaim.
**Where:** `automation/98-drift-checks.sh; ROADMAP.md; usr/lib/mios/*/server.py; usr/share/doc/mios/`
**Done When:** An over-claimed line-count metric turns the gate RED; corrected claims pass; the `server.py` "~26k" claim is fixed and now gated.
**Why:** Wrong metrics send agents to the wrong file -- the operator already got burned by the 3x `server.py` overclaim when planning a decomposition that was aimed at mass that wasn't there.
**Dep:** none

### AGY-1271 -- Commit Cargo.lock as reproducibility SSOT and add a cargo-deny/audit supply-chain gate  (WS-TEMPLATE | P3 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- gives the growing Rust tier the dependency-integrity gate bash never had.
**What+How:** This campaign adds Rust crates (`mios-template-conform`, `mios-template-compile`) and folds in orphan crates, expanding the dependency surface. Commit `tools/native/Cargo.lock` (and `src/mios-rs`'s lock) as a reproducibility SSOT per SBOM-not-hardcode/ADR-0003, author `tools/native/deny.toml` (license allow-list operationalizing the FOSS posture, plus advisory and duplicate/banned-crate rules), and add a `check_supply_chain` drift-check running `cargo deny check` and `cargo audit`, SOFT-skipping when the tools are absent (degrade-open like the other cargo gates). Stage `Cargo.lock` and `deny.toml` with explicit paths.
**Where:** `tools/native/Cargo.lock; tools/native/deny.toml (new); automation/98-drift-checks.sh; tools/native/Cargo.toml`
**Done When:** `Cargo.lock` is committed; `cargo deny check` + `cargo audit` run as a gate, green on the clean tree and RED on a banned-license or advisory-flagged dep; the gate SOFT-skips when the tools are not installed.
**Why:** Every crate added right now widens an unaudited transitive dependency surface with no lockfile in the tree -- so builds are not reproducible and a known-vulnerable or non-FOSS dep can land unnoticed.
**Dep:** Add cargo fmt --check + clippy -D warnings quality gate for the native workspace

### AGY-1272 -- Harden the templates-dir walk so a stray __pycache__ cannot false-RED the gate  (WS-TEMPLATE | P2 | S)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- removes an intermittent false-RED that undermines trust in the Law 16 gate.
**What+How:** `tools/compile-templates.py` `main()` does `for fn in sorted(os.listdir(templates_dir))` and marks any name absent from `[templates.*]` as "Not registered", so a working-tree `usr/share/mios/templates/__pycache__/` (gitignored at .gitignore:766-767 but present on disk during a build or a local drift-run) turns `check_templates_compilation` (Check 59) falsely RED. The same naive `listdir` in `usr/libexec/mios/mios-template-engine` `list_templates()` (which filters only `.list`) also pollutes the `mios new` kind menu. Guard both to iterate only regular files whose name is a registered template key, skipping directories and names starting with `.` or `__`. Add a characterization test that plants a `__pycache__` dir and asserts Check 59 stays green.
**Where:** `tools/compile-templates.py, usr/libexec/mios/mios-template-engine, usr/share/mios/templates/`
**Done When:** Planting `usr/share/mios/templates/__pycache__/x.pyc` leaves `check_templates_compilation` green; `mios new` help lists only real kinds; the characterization test and `just drift-gate` are green.
**Why:** This is the documented "stale `__pycache__` pollution fakes SSOT drift" local-verify hazard biting a second gate -- a RED that depends on whether someone ran Python recently trains people to ignore the gate.
**Dep:** none

### AGY-1273 -- Scope the over-broad [templates.*] match regexes and add a shared vendored/generated exclude  (WS-TEMPLATE | P2 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- fixes the root cause of the 430-line grandfather list before any ratchet is attempted.
**What+How:** In mios.toml, `[templates.json-schema].match=^[\w./-]+\.json$` claims EVERY json in the repo (package.json, tsconfig, SBOM manifests, generated files); `[templates.typescript]` `\.ts$`, `[templates.toml-config]` `\.toml$` and `[templates.powershell]` `\.ps1$` are equally unscoped. Anchor each `match` to MiOS-owned directories (`^usr/share/mios/`, `^tools/`, etc.) and add a shared `[templates].exclude` glob list (`target/`, `dist/`, `node_modules/`, `web_dist/`, `__pycache__/`, `.git/`, `vendor/`) consumed by `usr/libexec/mios/check-template-conformance` -- its walk already skips some via `mios-ai-tag` `SKIP_DIR` (mios-ai-tag:51) but the MATCH still claims them. Mirror the `[templates.*]` edits across mios.git and mios-bootstrap.git per Law 15.
**Where:** `usr/share/mios/mios.toml ([templates.*]), usr/libexec/mios/check-template-conformance, usr/share/mios/templates/conformance-grandfathered.list`
**Done When:** `check-template-conformance`'s `checked_count` drops to MiOS-owned files only; `conformance-grandfathered.list` shrinks after re-derivation; the both-repo parity check and `just drift-gate` are green.
**Why:** Unscoped patterns are why the grandfather list is enormous and why vendored/generated artifacts get conformance-claimed as MiOS source -- de-grandfathering without scoping first just conforms files MiOS does not own.
**Dep:** none

### AGY-1274 -- Declare explicit template match precedence and detect ambiguous multi-matches  (WS-TEMPLATE | P2 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- makes type resolution deterministic so no file is validated as the wrong language.
**What+How:** `check-template-conformance` compiles `[templates.*]` into a list and `break`s at the FIRST dict-order match (check-template-conformance:87-99), so precedence is implicit insertion order and language-ambiguous: the extensionless Python tool `usr/libexec/mios/mios-new` matches `[templates.bash-tool]` (`^usr/libexec/mios/mios-[\w-]+$`) BEFORE `[templates.python-tool]`, and `automation/NN-x.sh` matches `automation-step`, `bash` and `bash-verb` with conflicting `comment` styles. Add an explicit `priority` field (or most-specific-wins resolution) to `[templates.*]` plus a lint that FAILS when one file satisfies two templates whose `comment` or `required_markers` disagree, and make `mios-new`/the scaffolder resolve the winner identically to the checker.
**Where:** `usr/share/mios/mios.toml ([templates.*]), usr/libexec/mios/check-template-conformance, usr/libexec/mios/mios-new`
**Done When:** Every tracked file maps deterministically to exactly one winning template; a planted conflicting-comment double-match turns the gate RED; scaffolder and checker agree on the winner; `just drift-gate` green.
**Why:** A Python tool is currently being validated as bash; the moment the patterns are scoped or reordered the mislabels move, silently changing which files are checked against which syntax.
**Dep:** Scope the over-broad [templates.*] match regexes and add a shared exclude for vendored/generated trees

### AGY-1275 -- Make the Law-16 conformance gate fail-closed in CI instead of silently no-op'ing  (WS-TEMPLATE | P1 | S)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- turns Law 16 from an asserted law into an actually-enforced one on every runner.
**What+How:** `check-template-conformance` `main()` returns 0 with only a stderr warning when `mios-ai-tag` is absent (lines 46-49), and `98-drift-checks.sh:check_template_conformance()` (~3202-3206) SOFT-skips (`return 0`) when `python3` or the tool is missing -- so the entire Law 16 gate reports green while enforcing nothing. Because the checker is python3-only yet Law 16 is `applies_to="both"` (mios.toml:723), Windows CI never enforces it at all. Add a strict mode keyed on `CI`/`MIOS_STRICT` converting these soft-skips into hard failures (missing tagger or interpreter => non-zero), keeping degrade-open only for interactive runtime, and assert the tagger is importable before declaring PASS.
**Where:** `usr/libexec/mios/check-template-conformance, automation/98-drift-checks.sh (check_template_conformance ~3202)`
**Done When:** Removing `mios-ai-tag` or `python3` in a CI-flagged run turns the gate RED rather than green; interactive runs still degrade-open; `just drift-gate` is green with tooling present.
**Why:** A Law-level gate that passes while enforcing nothing is worse than no gate -- right now Law 16 has effectively zero enforcement on Windows and on any runner missing the tagger, and the dashboard says green.
**Dep:** none

### AGY-1276 -- Extend the golden compiler to validate every registered type and fail-closed on missing validators  (WS-TEMPLATE | P2 | L)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- makes the round-trip compiler a real validator rather than a decorative pass.
**What+How:** `tools/compile-templates.py` `compile_template()` validates only python/json/toml/yaml/bash and returns `None` (unconditional PASS) for rust, typescript, powershell, systemd-unit, quadlet, adr, markdown-doc, roadmap*, and drift-check -- so most types are never compiled. It also skips bash validation entirely on Windows (`if os.name != 'nt'`, line 80) and lets TOML/YAML pass silently when `tomllib`/`pyyaml` import fails (lines 57-62, 67-70): FALSE GREEN. Add real validators (`systemd-analyze verify` / a podman quadlet dry-parse where present, a syn/tree-sitter parse for rust+ts, a structural required-marker parse for adr/markdown/roadmap) and make an unavailable validator a HARD skip only under a documented allowlist, never a silent pass. This validation coverage must survive the later Rust port of the compiler.
**Where:** `tools/compile-templates.py, usr/share/mios/templates/ (rust, typescript, powershell, systemd-unit, quadlet, adr, markdown-doc)`
**Done When:** Every registered type is either validated or explicitly allowlisted-as-unvalidated with a recorded reason; a deliberately broken rust/systemd-unit/quadlet template turns Check 59 RED; toml/yaml with a missing parser fails closed under CI; `just drift-gate` green.
**Why:** The "golden round-trip compiler" green-lights the majority of template types without parsing them, and silently passes on Windows or without pyyaml -- so the compiled and structured types the campaign cares most about are the ones least checked.
**Dep:** none

### AGY-1277 -- Meta-lint the [templates.*] schema and enforce templates-dir <-> registry bijection  (WS-TEMPLATE | P2 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- guarantees the registry and the directory cannot half-land out of sync.
**What+How:** Nothing validates the `[templates.*]` table itself: that each `match` compiles as a regex and is anchored `^...$`, that `comment` is one of {hash,slash,md}, that `required_markers` is a list, and that the mapping is a full bijection -- every registered type has a file in `usr/share/mios/templates/` AND every file has a registered type. `compile-templates.py` checks only the file->cfg direction (flagging unregistered files, lines 106-108); the cfg->file direction and field validity are unchecked, so a half-landed cargo-manifest/systemd-timer/quadlet-split entry ships silently until scaffold time. Add `check_template_registry` (or fold it into compile-templates) covering field-schema plus bijection, mirrored per Law 15 in both repos.
**Where:** `usr/share/mios/mios.toml ([templates.*]), tools/compile-templates.py, usr/share/mios/templates/, automation/98-drift-checks.sh`
**Done When:** A `[templates.X]` with no matching file, a template file with no registry entry, a non-compiling or unanchored `match`, or a bad `comment` enum each turn the gate RED; both-repo parity green; `just drift-gate` green.
**Why:** The registry and the directory are two hand-maintained halves that diverge silently, and the incoming cargo-manifest/systemd-timer/quadlet-split additions are precisely the half-landed edits this bijection catches at commit time instead of at `mios new` time.
**Dep:** none

### AGY-1278 -- Hoist mios-new's hardcoded scaffold defaults (ADR next-id, drift-check id, roadmap-ws enums) into SSOT  (WS-TEMPLATE | P3 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- puts the scaffolder's default VALUES under operator-defined SSOT instead of frozen in code.
**What+How:** `usr/libexec/mios/mios-new` freezes literals that go stale: `get_dest_path()` prefixes new ADRs with `0012-` (line 64) though ADRs already run past 0011, and `render_template()` hardcodes drift-check `{{id}}=99`, adr `{{status}}=accepted`, and roadmap-ws `{{theme}}='OS-Image & Build'`/`{{priority}}=P2`/`{{status}}=proposed`/`{{task_id}}=999`. Compute the ADR next-id by scanning `usr/share/doc/mios/adr/` for `max(NNNN)+1`, and move the enum defaults into `[templates.<type>].defaults` in mios.toml so they are SSOT-defined, keeping the literal as a `${...:-LIT}`-style fallback. This is distinct from the dest-path routing table move (paths) -- it targets the interpolated default VALUES.
**Where:** `usr/libexec/mios/mios-new (render_template, get_dest_path), usr/share/mios/mios.toml ([templates.*])`
**Done When:** `mios new adr foo` assigns the true next sequential id; enum defaults resolve from `[templates.*]` with literal fallback; a golden fixture pins the computed output; `just drift-gate` green.
**Why:** The frozen `0012-` and `id=99` defaults are already wrong today, so every ADR and drift-check scaffolded now needs a manual fix-up that the author has to know about.
**Dep:** Move the hardcoded dest-path routing table into the [templates.*] SSOT

### AGY-1279 -- Enforce shebang/LF-only/exec-bit for script templates and fix templates' self-referential AI-related  (WS-TEMPLATE | P2 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- extends conformance from substring markers to the physical file properties bootc actually depends on.
**What+How:** `check-template-conformance` checks only for an AI-hint header and substring markers; it never verifies a script's shebang, line endings, or exec bit -- even though `mios-new` writes `newline='\n'` + `chmod 0o755` (mios-new:186-193) while `mios-template-engine` prepends a shebang only when `AI-hint:` is absent (mios-template-engine:37-43), an inconsistency. Separately, the shipped templates set `AI-related` to the TEMPLATE's own path (e.g. `usr/share/mios/templates/python-tool` line 3 points at `/usr/share/mios/templates/python-tool`), so every scaffold inherits a wrong `AI-related`. Extend conformance for bash/python/powershell types to require a first-line shebang, LF-only endings, and (on bootc) the exec bit; and rewrite the templates so the scaffolder emits an `AI-related` pointing at the produced file.
**Where:** `usr/libexec/mios/check-template-conformance, usr/share/mios/templates/(python-tool,python-module,python-test,bash,bash-verb,bash-tool,drift-check,automation-step,powershell), usr/libexec/mios/mios-new`
**Done When:** A CRLF, shebang-less, or non-executable script of a registered type turns the gate RED; scaffolded files carry an `AI-related` pointing at themselves; golden fixtures updated; `just drift-gate` green.
**Why:** LF/shebang/exec-bit are load-bearing on bootc and completely unenforced, and the self-referential `AI-related` means every scaffold ships a header pointing at the wrong file -- which the AI-tagger and every downstream agent then trust.
**Dep:** none

### AGY-1280 -- Reconcile Law-16 enforced_by to name both template gates and supply the missing compile-gate negative test  (WS-TEMPLATE | P2 | S)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- closes the law-to-check registry gap so Law 16's metadata matches its real enforcers.
**What+How:** mios.toml Law 16 (line 723) declares `enforced_by="98-drift-checks.sh:check_template_conformance"`, omitting `check_templates_compilation` (Check 59), which is wired into the run at 98-drift-checks.sh:6336 and also enforces the one-template rule. Separately, `check_template_conformance` is listed in `[testing.negative_coverage_exempt]` (mios.toml:11513) but `check_templates_compilation` is NOT, so the negative-coverage gate expects a negative test for it that does not exist. Add `check_templates_compilation` to Law 16's `enforced_by` string and author the negatives entry (a deliberately invalid template must turn Check 59 RED). Mirror the mios.toml edits across mios.git and mios-bootstrap.git per Law 15.
**Where:** `usr/share/mios/mios.toml ([laws] id=16 enforced_by, [testing.negative_coverage_exempt]), automation/97/98 negatives harness`
**Done When:** Law 16's `enforced_by` names both checks; a broken-template negative test drives Check 59 RED and is registered so `check_negative_test_coverage` is satisfied; both-repo parity green; `just drift-gate` green.
**Why:** A real Law 16 enforcer is invisible to the law's own metadata and to negative-coverage accounting, so the governance registry misreports what is enforced -- and an expected-but-absent negative test is an accounting hole that hides a missing eval.
**Dep:** none

### AGY-1281 -- Gate that generated=true templates are byte-reproducible from their generator  (WS-TEMPLATE | P3 | M)
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- makes the `generated=true` claim verified rather than asserted.
**What+How:** `[templates.quadlet]` is `generated=true` (mios.toml:11441) -- produced by a generator, not hand-edited -- yet nothing verifies the shipped `usr/share/mios/templates/quadlet` matches what its generator would emit; this is the same drift class as the recurring "broad `git add` strips `@sha256` from generated Quadlets" hazard. Add a check (extending `tools/compile-templates.py` or a dedicated gate) that, for every `[templates.X]` with `generated=true`, re-running its generator yields byte-identical output and goes RED on divergence. This also gives the incoming `.container`/`.pod`/`.network`/`.volume` split a home as verified generated artifacts rather than hand-maintained files.
**Where:** `usr/share/mios/mios.toml ([templates.*] generated flag), usr/share/mios/templates/quadlet, tools/compile-templates.py, automation/98-drift-checks.sh`
**Done When:** Editing a `generated=true` template so it diverges from generator output turns the gate RED; regenerating restores green; the quadlet-split scaffolds are covered.
**Why:** `generated=true` is currently an honor-system label -- one hand-edit or one broad `git add` desyncs a generated template from its source of truth, and the tree keeps shipping the stale copy.
**Dep:** Split the quadlet template into per-kind scaffolds (.container/.pod/.network/.volume)

### AGY-1282 -- Add `just template-gate`: run the toolchain in strangler-fig order with a no-delete-before-parity guard  (WS-TEMPLATE | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- encodes the port ordering so no fallback shim is deleted before its replacement is proven.
**What+How:** The template work has hard ordering constraints the individual tasks do not encode: golden fixtures + workspace membership + fmt/clippy must land BEFORE the Rust ports; vocab/routing SSOT unification BEFORE `miosd scaffold`; and no python producer (`mios-new`, `mios-template-engine`, `check-template-conformance`, `tools/compile-templates.py`) may be DELETED until the Rust binary passes ALL golden fixtures byte-for-byte. Add a `just template-gate` target (and matching CI job) running compile-templates -> scaffold-every-registered-type -> check-template-conformance -> cross-repo parity in dependency order as ONE gate, plus a guard that FAILS if any listed python producer is removed while its golden-parity test is not green.
**Where:** `justfile (or Justfile), .github/workflows or automation CI entry, tools/compile-templates.py, usr/libexec/mios/{mios-new,check-template-conformance}`
**Done When:** `just template-gate` runs the four stages in order and is wired into CI; deleting a python producer without a green byte-parity fixture fails the guard; `just drift-gate` green.
**Why:** Without one ordered gate and a delete guard, a port can remove the working fallback before the Rust replacement is proven -- and scaffolding breaks for everyone with no obvious culprit.
**Dep:** Capture golden-master fixtures + byte-parity harness for mios new across all registered types

### AGY-1283 -- Add a PowerShell AST parse-gate to CI (the single highest-value PS gate)  (WS-PWSH | P1 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- the 36 PowerShell scripts pass a real parse gate in CI.
**What+How:** `build-mios.ps1` was unparseable for ~70 commits undetected because no PS parse check runs in CI. `audit_all.ps1` (lines 12-20) ALREADY implements the correct check via `[System.Management.Automation.Language.Parser]::ParseFile` but is never invoked by `mios-ci.yml` (its drift-gate job, .github/workflows/mios-ci.yml lines 59-127, runs only lint-shell.sh/lint-python.sh/97-ssot-lint.sh/98-drift-checks.sh). Create `automation/lint-powershell.sh` mirroring `lint-shell.sh`'s structure (SCRIPT_DIR/ROOT resolve, degrade-open exit 2 when pwsh is absent, best-effort pwsh self-provision via apt/dnf) that globs every tracked `*.ps1` and runs `pwsh -NoProfile -Command` calling `Parser::ParseFile`, failing on any parse error. Wire it as a new step in BOTH `.github/workflows/mios-ci.yml` (drift-gate job, before 98-drift-checks) and `.forgejo/workflows/build-mios.yml`, and add a `check_powershell_parse` function to `automation/98-drift-checks.sh` so `just drift-gate` gates it locally too.
**Where:** `automation/lint-powershell.sh (new), automation/lint-shell.sh (pattern), audit_all.ps1, .github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml, automation/98-drift-checks.sh`
**Done When:** `lint-powershell.sh` exists and is invoked by both CI workflows and the build.sh drift phase; deliberately breaking any `.ps1` syntax fails the gate; a `check_powershell_parse` drift-check is registered; `bash automation/lint-powershell.sh` returns 0 on a clean tree and non-zero on an injected parse error; changes staged with explicit paths.
**Why:** A syntactically dead build script survived ~70 commits because nothing parsed it -- the parser logic already exists in-repo, so the only thing standing between MiOS and that whole bug class was wiring.
**Dep:** none

### AGY-1284 -- Add a PSScriptAnalyzer static-analysis gate with a curated ruleset  (WS-PWSH | P1 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- gives PowerShell the clippy-equivalent lint tier the Rust and shell planes have.
**What+How:** MiOS has NO PowerShell static analysis (grepping for `Invoke-ScriptAnalyzer`/`PSScriptAnalyzer` across the tree finds only doc mentions). Add `automation/lint-ps-analyzer.sh` that ensures the PSScriptAnalyzer module (`Install-Module -Scope CurrentUser` best-effort, degrade-open like `lint-shell.sh`) then runs `Invoke-ScriptAnalyzer -Recurse -Settings automation/PSScriptAnalyzerSettings.psd1` over all tracked `*.ps1`, failing on Error-severity findings. Author `automation/PSScriptAnalyzerSettings.psd1` as the curated ruleset (enable `PSAvoidUsingInvokeExpression`, `PSAvoidUsingPlainTextForPassword`, `PSUseDeclaredVarsMoreThanAssignments`, `PSAvoidAssignmentToAutomaticVariable`, `PSUseShouldProcessForStateChangingFunctions`; style rules start Warning-only to keep the tree green, security rules at Error). Wire the step into `.github/workflows/mios-ci.yml` and `.forgejo/workflows/build-mios.yml` after the parse-gate.
**Where:** `automation/lint-ps-analyzer.sh (new), automation/PSScriptAnalyzerSettings.psd1 (new), .github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml`
**Done When:** `Invoke-ScriptAnalyzer` runs in both CI workflows against the committed settings file; injecting an `Invoke-Expression` on untrusted input fails the gate; the clean tree passes at the chosen severities; settings file and runner staged explicitly.
**Why:** A parse-gate only proves a script parses -- `Invoke-Expression` injection, automatic-variable clobber and plaintext secrets all parse perfectly, and MiOS has 36 PS scripts with zero coverage for them.
**Dep:** Add a PowerShell AST parse-gate to CI (the single highest-value gate)

### AGY-1287 -- Extract the duplicated mios.toml resolver into one shared PowerShell module  (WS-PWSH | P2 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- collapses the duplicated PS TOML resolvers to ONE implementation matching mios_toml.py's layer order.
**What+How:** `Resolve-MiosTomlText` + `Get-MiosTomlValue` are copy-pasted into BOTH `build-mios.ps1` (lines 95 and 138) and `Get-MiOS.ps1` (lines 401 and 438) -- a third parallel resolver alongside `mios_toml.py` and `userenv.sh`. Extract the canonical regex-based reader into `automation/lib/MiOS.Toml.psm1` (or fold it into `installation/mios-common.ps1`'s existing `Get-MiosSsotValue` at line 30, itself a fourth partial copy) and replace all call sites with an `Import-Module`/dot-source of the single implementation. Keep behavior byte-identical: FIRST capture the current `Get-MiosTomlValue` output for a spread of `[section].key` lookups from `usr/share/mios/mios.toml` as golden fixtures, then prove the extracted module reproduces them exactly. This is the strangler seam that later lets a compiled resolver (`tools/native`) back the whole PS config surface.
**Where:** `build-mios.ps1 (95,138), Get-MiOS.ps1 (401,438), installation/mios-common.ps1 (30), automation/lib/MiOS.Toml.psm1 (new)`
**Done When:** `MiOS.Toml.psm1` holds the single shared reader; both caller scripts import it with no inlined duplicate functions; a test script proves `[ports].ssh`, `[desktop].theme` and `[ai].vllm_port` resolve byte-identically before and after; the parse-gate stays green.
**Why:** Four parallel copies of the TOML regex already cost a real bug -- the UTF-8 PUA glyph fix landed in `build-mios.ps1` and never reached `Get-MiOS.ps1`, so the two entry points read the same SSOT differently.
**Dep:** Fix the $args automatic-variable clobber and enforce Set-StrictMode across PS scripts

### AGY-1285 -- Collapse the two same-named Setup-MiOSLanPortProxy.ps1 into one SSOT-floated proxy and delete the retired ollama port literals  (WS-PWSH | P2 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- one canonical implementation per PowerShell capability, with no second copy carrying stale constants.
**What+How:** Two DIFFERENT scripts ship under the name `Setup-MiOSLanPortProxy.ps1`: the 118-line `usr/libexec/mios/` copy floats every port through its `_MiosPort` helper off `MIOS_PORT_*` with literal fallbacks, while the 47-line `usr/share/mios/windows/` copy hardcodes retired ollama ports 11434/11435 and the 11450 AI-node Tailscale v4tov6 bridge. Fold the tailnet-bridge behavior into the floated libexec implementation (an `-AiNode` switch or a `MIOS_PORT_LLM_LIGHT`-driven v4tov6 branch) so ONE hardened script serves both the LAN-NIC and Tailscale cases; demote the `usr/share/mios/windows/` file to a thin shim that dot-sources/invokes the canonical one, keeping the `98-drift-checks.sh` allowlist entry at ~line 1665 valid. Strip 11434/11435/11450 entirely — they are retired by the /v1-only purge. Mirror into mios-bootstrap.git per Law 15.
**Where:** `usr/share/mios/windows/Setup-MiOSLanPortProxy.ps1, usr/libexec/mios/Setup-MiOSLanPortProxy.ps1, automation/98-drift-checks.sh (allowlist line ~1665)`
**Done When:** `grep -n '11434\|11435\|11450'` over both files returns nothing; only one file contains a real implementation and the other is a verified forwarding shim; every port resolves from `MIOS_PORT_*` with a literal fallback; the PS parse-gate and `just drift-gate` are green; both repos carry the change.
**Why:** Two different bodies under one filename is exactly the duplication smell the operator flagged — a caller cannot know which behavior it gets — and the retired ollama ports are live hardcodes violating both the zero-hardcodes and /v1-only campaigns.
**Dep:** none

### AGY-1286 -- Stop clobbering the `$args` automatic variable and put every hardened .ps1 under Set-StrictMode  (WS-PWSH | P2 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- the loose-script surface gets the cheapest available bug-class elimination before it is ported.
**What+How:** `usr/libexec/mios/Setup-MiOSLanPortProxy.ps1` assigns `$args = @('-NoProfile',...)` in its self-elevate block (~line 20), overwriting PowerShell's automatic `$args` — a latent argument-forwarding bug and a `PSAvoidAssignmentToAutomaticVariable` violation. Rename that local to `$psArgs`, then sweep every .ps1 with `grep -nE '\$args\s*='` for the same pattern. Add `Set-StrictMode -Version Latest` plus `$ErrorActionPreference='Stop'` to the ~15 `windows/*.ps1` and `libexec/*.ps1` that set neither (`mios-window-foreground.ps1`, `mios-uia-dump.ps1`, `mios-tailscale-serve.ps1`, …), using `installation/mios-common.ps1` lines 11-12 as the model. Guard adoption by running the parse-gate plus a smoke dot-source of each edited file so nothing trips on newly-strict undefined-variable access.
**Where:** `usr/libexec/mios/Setup-MiOSLanPortProxy.ps1, usr/share/mios/windows/*.ps1, usr/libexec/mios/*.ps1, installation/mios-common.ps1 (reference)`
**Done When:** No script assigns to `$args`/`$input`/`$_`; every hardened script opens with StrictMode + `$ErrorActionPreference='Stop'`; `Invoke-ScriptAnalyzer` reports zero `PSAvoidAssignmentToAutomaticVariable`; all edited scripts still dot-source and parse clean; edits staged per explicit path.
**Why:** Silent `$args` clobbering breaks argument forwarding at the elevation boundary (the user's flags simply vanish), and without StrictMode a typo'd variable reads as empty instead of failing, so provisioning scripts "succeed" while doing nothing.
**Dep:** Add a PSScriptAnalyzer static-analysis gate with a curated ruleset

### AGY-1714 -- Extract the four-times-duplicated mios.toml resolver into one shared PowerShell module  (WS-PWSH | P2 | M)
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- the PowerShell config plane reads SSOT through exactly one resolver, the seam a compiled resolver later slots into.
**What+How:** `Resolve-MiosTomlText` and `Get-MiosTomlValue` are copy-pasted into BOTH `build-mios.ps1` (lines 95 and 138) and `Get-MiOS.ps1` (lines 401 and 438), forming a third parallel resolver beside `mios_toml.py` and `userenv.sh`, with `installation/mios-common.ps1`'s `Get-MiosSsotValue` (line 30) as a fourth partial copy. Capture the CURRENT `Get-MiosTomlValue` output for a spread of `[section].key` lookups against `usr/share/mios/mios.toml` as golden fixtures FIRST, then move the canonical regex reader into `automation/lib/MiOS.Toml.psm1` (or fold it into `mios-common.ps1`) and replace every call site with an `Import-Module`/dot-source. Behavior must be byte-identical against the captured fixtures, including the vendor < host < user layer order that `mios_toml.py` defines.
**Where:** `build-mios.ps1 (95,138), Get-MiOS.ps1 (401,438), installation/mios-common.ps1 (30), automation/lib/MiOS.Toml.psm1 (new)`
**Done When:** `grep -rn 'function Resolve-MiosTomlText\|function Get-MiosTomlValue'` returns exactly one hit each; `build-mios.ps1` and `Get-MiOS.ps1` import the module; the golden-fixture suite shows byte-identical lookups pre/post; parse-gate + drift-gate green; module present in mios-bootstrap.git.
**Verify:** `grep -rn 'function Resolve-MiosTomlText|function Get-MiosTomlValue'` returns exactly one hit each, and every caller still resolves.
**Do NOT:** leave one copy behind as a fallback. Four copies became four behaviours.
**Why:** Four parallel TOML readers means four different answers to "what does SSOT say" — a key added to mios.toml can be visible to the installer and invisible to the builder — and no compiled resolver can land until there is a single seam to back.
**Dep:** Build a Pester + golden-master parity harness for PowerShell verbs (the migration oracle)

### AGY-1288 -- Strangler-decompose the 11k-line build-mios.ps1 into a MiOS.Build module behind a stable shim  (WS-PWSH | P2 | L)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- the Windows build monolith becomes testable, per-slice-hardenable modules without changing its CLI contract.
**What+How:** `build-mios.ps1` is 11011 lines / 92 functions spanning dashboard rendering (`Show-Dashboard` 1155, `Start-Phase` 1444), WSL/podman provisioning (`New-BuilderDistro` 2804, `Set-PodmanMachineStorageOn` 2463), build/export (`Invoke-WslBuild` 4942, `Export-WslTar` 5072, `Invoke-BibBuild` 5135) and branding/launcher install (`Install-WindowsBranding` 6032, `Install-MiosLauncher` 6532). Extract cohesive groups into `automation/lib/MiOS.Build/*.psm1` (Dashboard, Provision, Bake, Branding), reducing `build-mios.ps1` to a thin orchestrator that imports and dispatches; the top-level entrypoint name and CLI stay stable so old and new coexist. Characterize each group with Pester golden tests (invoke `-WhatIf`/dry-run where possible) BEFORE deleting the inline copy. Land each sub-module as its own `git merge --no-ff`, mirrored to mios-bootstrap.git per Law 15.
**Where:** `build-mios.ps1, automation/lib/MiOS.Build/ (new module dir)`
**Done When:** `build-mios.ps1` is under ~1500 lines and imports the extracted modules; every extracted function has a passing Pester characterization test; a full build path still completes on MiOS-DEV; parse-gate + PSScriptAnalyzer + drift-gate green; each slice visible as a real `--no-ff` merge in both repos.
**Why:** An 11k-line monolith stayed silently broken for 70 commits because nothing could parse or test it; until it is split, no piece of the Windows build can be individually verified, ported or compiled.
**Dep:** Build a Pester + golden-master parity harness for PowerShell verbs (the migration oracle)

### AGY-1289 -- Strangler-decompose the 420KB Get-MiOS.ps1 web entrypoint into a MiOS.Install module  (WS-PWSH | P2 | L)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- the `irm|iex` front door shrinks to a bootstrap loader while its behavior stays byte-frozen.
**What+How:** `Get-MiOS.ps1` is 7193 lines / 65 functions / 420847 bytes and mixes the agreement gate (`Show-MiOSAgreement` 642, `Invoke-MiOSAgreementGate` 732), Windows tool install (`Install-MiOSWindowsTerminal` 1577, `Install-MiOSPwsh7` 1620, `Install-MiOSGeistFont` 1685), terminal theming (`Install-MiOSTerminalProfile` 1974), the live dashboard (`Show-MiosDashboard` 3913) and the `mios-*` verb wrappers (`mios-build` 4433 … `mios` 4747). Extract these into `automation/lib/MiOS.Install/*.psm1` (Agreement, WinTools, Theme, Dashboard, Verbs) and shrink `Get-MiOS.ps1` to a bootstrap that fetches deps then imports them, preserving the exact `irm|iex` contract as the stable facade. Golden-master the agreement-gate text and the verb-dispatch table before moving them; delete each inline copy only once its Pester parity test passes. Mirror to mios-bootstrap.git — this file is a shared SSOT surface (Law 15).
**Where:** `Get-MiOS.ps1, automation/lib/MiOS.Install/ (new module dir)`
**Done When:** `Get-MiOS.ps1` is a thin loader importing the extracted modules; an `irm|iex` install still completes end-to-end on a clean Windows host; every extracted group has a passing Pester test; parse/analyzer/drift gates green; slices merged `--no-ff` in both repos.
**Why:** A 420KB script delivered over the wire and executed unread is both unmaintainable and a supply-chain liability — nobody can review what a user just piped into their shell — and per-slice hardening/compilation is impossible while it is one blob.
**Dep:** Build a Pester + golden-master parity harness for PowerShell verbs (the migration oracle)

### AGY-1290 -- Build the Pester v5 + golden-master parity harness that every PowerShell port depends on  (WS-TESTGOV | P1 | M)  **[DONE]**
**Goal:** E-05 Test and CI governance -- the PowerShell plane gets the shellcheck-equivalent oracle it never had, so refactors are provable rather than hopeful.
**What+How:** Create `tests/powershell/` with a Pester v5 project, a `golden-fixtures/` dir and a runner `tests/powershell/run-pester.sh` that degrades open when pwsh/Pester are absent (the `lint-shell.sh` pattern). Seed it by capturing the CURRENT observable output of the behavioral functions as golden masters: `Get-MiosTomlValue` over a table of `[section].key` inputs, the `automation/lib/globals.ps1` `MIOS_*` env projection (dot-source and dump all `$script:MIOS_*`), `Get-MiosPalette` RGB tuples, and the `mios-oscontrol-server` route dispatch for canned request bodies. Assert the recorded snapshots so any output-changing refactor fails. Wire the runner into `.github/workflows/mios-ci.yml` and `.forgejo/workflows/build-mios.yml` so both forges run it as equals.
**Where:** `tests/powershell/ (new), tests/powershell/golden-fixtures/ (new), automation/lib/globals.ps1, installation/mios-common.ps1, .github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml`
**Done When:** The Pester suite runs in CI on both forges; golden snapshots exist for the TOML resolver, env projection, palette and oscontrol dispatch; deliberately mutating any of those outputs turns the suite red; the runner exits 0 with a skip notice when Pester is unavailable; fixtures and runner staged by explicit path.
**Why:** Every downstream decomposition and compile-port names this harness as its dependency; without a characterization oracle, each refactor of an untested 10k-line script is an uncontrolled behavior change shipped to operators.
**Dep:** Add a PowerShell AST parse-gate to CI (the single highest-value gate)

### AGY-1291 -- Port mios-oscontrol-server.ps1 to a compiled .NET executor behind an SSOT-toggled shim  (WS-PWSH | P2 | L)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- the highest-risk network surface on Windows moves from loose script to type-checked compiled code, reversibly.
**What+How:** `mios-oscontrol-server.ps1` is a 1375-line / 28-function `System.Net.HttpListener` server exposing 20+ OS-control routes (`/launch`, `/window/move|resize|focus|state`, `/input/type|click|key-combo`, `/screenshot`, `/windows`) over Win32 P/Invoke. Port it to a compiled .NET executable reusing the `src/mios-launch.cs` precedent (which already P/Invokes `SetWindowPos`/`EnumWindows`), producing `src/mios-oscontrol/` built the same way `automation/support/deploy-tooling-live.sh` ships source-only tools. Strangler: keep the .ps1 as a thin shim that launches the binary when present and falls back to the PS body otherwise, gated by an SSOT toggle `mios.toml [migration] use_compiled_oscontrol` (oxidizr-style non-destructive opt-out). Capture every route's JSON response from the current PS server as golden fixtures and prove byte-parity BEFORE defaulting the toggle on.
**Where:** `usr/share/mios/windows/mios-oscontrol-server.ps1, src/mios-launch.cs (precedent), src/mios-oscontrol/ (new), usr/share/mios/mios.toml ([migration] toggle)`
**Done When:** The compiled binary serves every documented route with responses byte-identical to the captured fixtures; the .ps1 contains only shim logic; flipping the single `[migration]` key rolls back to the PS body; parse-gate green; the new key exists in both repos.
**Why:** An HttpListener that accepts keystroke, click and process-launch requests is the exact injection-prone boundary that must not be a loose script — a parse error or an unquoted string there is remote code execution on the interactive desktop.
**Dep:** Build a Pester + golden-master parity harness for PowerShell verbs (the migration oracle)

### AGY-1292 -- Converge the four hand-rolled Win32 P/Invoke PS scripts onto the compiled src/mios-launch.cs  (WS-PWSH | P2 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- one compiled binary owns Win32 window control; the scripts become shims.
**What+How:** Four scripts reimplement overlapping Win32 P/Invoke window control: `mios-pc-control.ps1` (440 lines), `mios-window-foreground.ps1` (55), `mios-uia-dump.ps1` (110) and the launch/center logic in `Update-MiOSStartMenuShortcuts.ps1` (438) — duplicating what the compiled `src/mios-launch.cs` (127 lines: `SetWindowPos`, `EnumWindows`, `GetWindowRect`) already provides. Extend `mios-launch.cs` into a subcommand-driven executable (`foreground`/`move`/`resize`/`dump`/`click`), demote each PS script to a thin shim that shells the binary, and keep the Linux-side callers (`/usr/libexec/mios/mios-pc-control`, `mios-windows`, `mios-window-active`) invoking the same names. Capture each PS verb's current stdout/JSON as golden fixtures, port, prove parity, then delete the inline `Add-Type` P/Invoke blocks. Verify the compile path matches how `automation/support/deploy-tooling-live.sh` deploys source-only tools.
**Where:** `src/mios-launch.cs, usr/share/mios/windows/mios-pc-control.ps1, usr/share/mios/windows/mios-window-foreground.ps1, usr/share/mios/windows/mios-uia-dump.ps1, usr/libexec/mios/Update-MiOSStartMenuShortcuts.ps1`
**Done When:** One binary provides foreground/move/resize/dump; `grep -n 'Add-Type'` finds no P/Invoke block in the four scripts; golden fixtures confirm identical output; Linux callers are unchanged; parse/analyzer gates green; mirrored per Law 15.
**Why:** Four independent copies of the same unsafe interop surface drift apart and each carries its own marshalling bugs; a single compiled implementation removes both the divergence and three quarters of the memory-unsafe code.
**Dep:** Build a Pester + golden-master parity harness for PowerShell verbs (the migration oracle)

### AGY-1293 -- Port the Windows AI-node HTTP servers (mios-igpu-server, mios-ai-node) to a compiled binary  (WS-PWSH | P2 | L)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- the always-on Windows inference lane runs compiled, port-floated code instead of a long-lived script.
**What+How:** `mios-igpu-server.ps1` (286 lines) hosts a llama.cpp Vulkan OpenAI-compatible inference server and `mios-ai-node.ps1` (453) provisions a podman-machine AI node; both are long-lived network services in loose PS, with hardcoded ports (`mios-ai-node` still references the retired 11450) and duplicated `Info`/`Ok`/`Warn` helpers and scheduled-task install code. Port the persistent-server plus firewall/portproxy logic to a compiled binary (Rust with windows-rs for the IP Helper/service bits, or .NET reusing the oscontrol pattern), floating every port from `MIOS_PORT_*` / `mios.toml [ports]`. Strangler: the .ps1 stays as a shim invoking the binary behind an SSOT toggle; golden-master the `/health` and `/v1` responses, then retire the PS body. Because these are long-running units, pair the port with a least-privilege run identity rather than inheriting whatever rights the task had.
**Where:** `usr/share/mios/windows/mios-igpu-server.ps1, usr/share/mios/windows/mios-ai-node.ps1, src/mios-ainode/ (new), usr/share/mios/mios.toml ([ports])`
**Done When:** The compiled server answers `/health` and `/v1/*` with fixture-identical responses behind the SSOT toggle; no `11450` or other port literal remains in either script; parse-gate green; the port keys exist in both repos.
**Why:** A permanently-running network listener written as an unparsed script is the worst combination of blast radius and fragility on the AI plane, and it is where the last retired-port hardcodes still live.
**Dep:** Port mios-oscontrol-server.ps1 to a compiled C#/.NET executor behind a thin shim

### AGY-1294 -- Consolidate the five copies of UAC self-elevation into one hardened Invoke-MiosSelfElevate  (WS-PWSH | P2 | S)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- one audited privilege-escalation path instead of five subtly different ones.
**What+How:** The `IsInRole(Administrator)` + `Start-Process -Verb RunAs` block is copy-pasted at least five times: `installation/mios-common.ps1` `Invoke-MiosSelfElevate` (line 130, the intended canonical one), `preflight.ps1` (top), `usr/libexec/mios/Setup-MiOSLanPortProxy.ps1` (~17-22), `usr/share/mios/windows/mios-oscontrol-server.ps1` `-Install` path (105-112), and `mios-fix-wslg.ps1` — each differing in arg quoting, `PSCommandPath` handling and the `$args` clobber. Make `Invoke-MiosSelfElevate` the single hardened implementation (printf-`%q`-style argument escaping, no automatic-variable clobber, the existing NONINTERACTIVE guard) and replace the other four with a dot-source plus call. Characterize with a Pester test asserting the constructed elevated relaunch command line is byte-identical across all callers for a fixed argument set, including args containing spaces and quotes.
**Where:** `installation/mios-common.ps1 (130), preflight.ps1, usr/libexec/mios/Setup-MiOSLanPortProxy.ps1, usr/share/mios/windows/mios-oscontrol-server.ps1, mios-fix-wslg.ps1`
**Done When:** Exactly one elevation implementation remains; the other four scripts dot-source and call it; the Pester parity test passes for quoted/spaced arguments; parse/analyzer gates green; mirrored per Law 15.
**Why:** Five divergent RunAs builders means five chances of unescaped argument injection into an elevated process, and a fix applied to one copy leaves the other four exploitable.
**Dep:** Fix the $args automatic-variable clobber and enforce Set-StrictMode across PS scripts

### AGY-1295 -- Centralize scheduled-task registration (and the WindowsApps-alias avoidance) in one helper  (WS-PWSH | P2 | S)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- one place resolves the interpreter path, so a known-fatal bug can only be fixed once.
**What+How:** Scheduled-task registration is reimplemented in `mios-wsl-keepalive.ps1` (116), `mios-wsl-session-task.ps1` (56), `mios-oscontrol-server.ps1 -Install` (105-120) and `mios-igpu-server.ps1 -Install` — each independently re-solving the concrete interpreter path to avoid the unresolvable pwsh MSIX `WindowsApps` alias that yields `0x80070002` (a lesson documented in oscontrol lines 114-120), then building the action/trigger/principal and registering. Extract one `Register-MiosScheduledTask` helper into `automation/lib/MiOS.Win.psm1` (or `mios-common.ps1`) owning interpreter resolution, the hidden-window argline and the elevated principal, and refactor all four callers onto it. Golden-master the resolved interpreter path plus argline per task so the alias avoidance cannot silently regress.
**Where:** `usr/share/mios/windows/mios-wsl-keepalive.ps1, usr/share/mios/windows/mios-wsl-session-task.ps1, usr/share/mios/windows/mios-oscontrol-server.ps1, usr/share/mios/windows/mios-igpu-server.ps1, automation/lib/MiOS.Win.psm1 (new)`
**Done When:** All four installers call `Register-MiosScheduledTask` and none resolves an interpreter inline; a Pester assertion proves the resolved path never contains `\WindowsApps\`; parse/analyzer gates green; the helper exists in both repos.
**Why:** The MSIX-alias bug already broke task registration once; with four copies, the next Windows change breaks it in the three nobody remembered to patch, and the tasks fail at boot with an opaque 0x80070002.
**Dep:** none

### AGY-1296 -- Project the PowerShell VT-console palette from mios.toml [colors] instead of four hardcoded RGB copies  (WS-DEDUP | P2 | M)  **[DONE]**
**Goal:** E-09 One value, one name: the full de-duplication campaign -- every hex is declared once in SSOT and derived everywhere, on the Windows side too.
**What+How:** The console palette is redefined in at least four PS places: `automation/lib/globals.ps1` `MIOS_COLOR_*` (lines 209-220), `installation/mios-common.ps1` `Get-MiosPalette` + `C`/`B`/`Rule`/`Write-MiosLine` (60-119), `Get-MiOS.ps1` `Get-MiosPalette` (1407) and `build-mios.ps1` — all hardcoding the same Hokusai RGBs while the Linux side already derives them via `mios-theme-render` under drift-check 25. Create `automation/lib/MiOS.Console.psm1` that resolves the 12 palette tokens from `mios.toml [colors]` through the shared TOML resolver (current RGBs kept only as `${...:-LIT}` fallbacks) and exposes `C`/`B`/`Rule`/`Write-MiosLine`/`Write-MiosKV`; repoint every hardcoded palette to an import. Extend drift-check 25 (or add a sibling check) to assert the PS palette matches `[colors]`.
**Where:** `automation/lib/globals.ps1 (209-220), installation/mios-common.ps1 (60-119), Get-MiOS.ps1 (1407), build-mios.ps1, automation/lib/MiOS.Console.psm1 (new), usr/share/mios/mios.toml [colors], automation/98-drift-checks.sh (check 25)`
**Done When:** One PS module resolves the palette from `[colors]`; the four hardcoded RGB blocks are gone; editing a hex in `mios.toml` changes the PowerShell console output with no code edit; the drift-check goes red if the PS palette diverges from `[colors]`; parse-gate green; mirrored per Law 15.
**Why:** Today an operator retheming `mios.toml` gets a Linux desktop in the new colors and a Windows console still painted in the old ones — the theme SSOT is silently false on half the product.
**Dep:** Extract the duplicated mios.toml resolver into one shared PowerShell module

### AGY-1297 -- Float the remaining port/CIDR literals in windows/*.ps1 and extend the port drift-check to cover them  (WS-ZEROHC | P2 | M)  **[DONE]**
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- the last unscanned PowerShell blind spot gets a key to float to and a gate to keep it floated.
**What+How:** `98-drift-checks.sh` check 28 (lines ~1852-1888) already gates `automation/lib/globals.ps1` port defaults against `mios.toml [ports]`, but does not scan the `windows/*.ps1` helpers, several of which still carry raw literals: `mios-oscontrol-server.ps1`'s `fwRemote` CIDRs (line 92) and its 11437 default, `mios-ai-node.ps1`'s 11450, `mios-tailscale-serve.ps1`'s served-port list and the `usr/share` Setup-proxy ports. Refactor each to resolve from `MIOS_PORT_*`/`mios.toml` with the current literal as a `${VAR:-LIT}` fallback (oscontrol lines 83-85 already model this), and extend the check-28 function to also scan `windows/*.ps1` for un-floated port literals against `[ports]`, recording legitimately-fixed values in `mios.toml [security.nohc_allowlist]` — anchored, to avoid the known `mios-hardcode-lint` UNANCHORED-allowlist bug that spuriously exempts real `:80xx` hits.
**Where:** `usr/share/mios/windows/mios-oscontrol-server.ps1 (92), usr/share/mios/windows/mios-ai-node.ps1, usr/share/mios/windows/mios-tailscale-serve.ps1, automation/98-drift-checks.sh (check 28 ~1852), usr/share/mios/mios.toml ([ports],[security.nohc_allowlist])`
**Done When:** Every `windows/*.ps1` port resolves from SSOT with a literal fallback; introducing a new raw port literal into any .ps1 turns check 28 red; intentional fixed values appear anchored in `[security.nohc_allowlist]`; parse/drift gates green; SSOT keys mirrored in both repos.
**Why:** An operator who moves a port in `mios.toml` today gets a half-moved system — the Windows helpers keep talking to the old number — and retired ports keep reappearing because nothing scans that directory.
**Dep:** Unify the two divergent Setup-MiOSLanPortProxy.ps1 and purge retired ollama ports

### AGY-1298 -- Add a Law-15 byte-parity drift-check for the PS files shared by mios.git and mios-bootstrap.git  (WS-DRIFT | P1 | M)  **[DONE]**
**Goal:** E-07 The drift-gate as the enforcement plane -- Law 15 becomes a machine-checked fitness function instead of a manual promise that has already been broken.
**What+How:** `build-mios.ps1`, `Get-MiOS.ps1`, `automation/lib/globals.ps1` and `installation/mios-common.ps1` ship in BOTH repos and must stay byte-identical under Law 15 (DOUBLE-REPO-TRIPLE-CHECK), yet nothing enforces it and the two `build-mios.ps1` have already diverged. Add a `check_ps_repo_parity` function to `automation/98-drift-checks.sh` that, when a sibling mios-bootstrap checkout is resolvable (via `MIOS_BOOTSTRAP_DIR` or a known path), computes and compares SHA256 of each enrolled file across both trees and fails on divergence, degrading open with a WARN when the sibling tree is absent (the existing optional-check convention). Declare the enrolled roster in `mios.toml` as `[law15.shared_ps_surfaces]` so the file list is SSOT-defined rather than hardcoded inside the check.
**Where:** `automation/98-drift-checks.sh, usr/share/mios/mios.toml ([law15.shared_ps_surfaces] new), build-mios.ps1, Get-MiOS.ps1, automation/lib/globals.ps1, installation/mios-common.ps1`
**Done When:** The check compares SHA256 across both repos and fails on mismatch; it WARN-skips cleanly when the sibling tree is missing; appending a byte to one copy locally trips it; the roster lives in `mios.toml` and exists in both repos.
**Why:** Undetected divergence between the two published copies means an `irm|iex` user and a repo user get different installers from the same version tag — which is how the two `build-mios.ps1` silently drifted apart before.
**Dep:** none

### AGY-1299 -- Make the tracked powershell/profile.ps1 the sole profile source and stop embedding a heredoc copy  (WS-PWSH | P2 | M)
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- one file owns the shipped PowerShell profile; the installer copies rather than regenerates.
**What+How:** `powershell/profile.ps1` (1078 lines) is the declared SSOT for the MiOS profile (oh-my-posh/fastfetch/PSReadLine self-heal), but `Get-MiOS.ps1`'s `Install-MiOSPowerShellProfile` (~line 3613) writes a thin `C:\` redirector AND embeds a full profile body inline — a second 1000-line copy free to drift from the tracked file. Reduce `Install-MiOSPowerShellProfile` to emitting ONLY the marker-delimited redirector block (`# >>> MiOS oh-my-posh init >>>`) that dot-sources `powershell/profile.ps1`, and have the installer COPY the tracked file to `M:\MiOS\powershell\` verbatim instead of regenerating it. Golden-master the redirector block and assert the deployed profile is byte-identical to the tracked source.
**Where:** `powershell/profile.ps1, Get-MiOS.ps1 (Install-MiOSPowerShellProfile ~3613)`
**Done When:** The installer function contains no full profile body — only the redirector block; the deployed profile hashes equal to tracked `powershell/profile.ps1`; a Pester test asserts that byte-identity; parse-gate green; mirrored per Law 15.
**Why:** Two 1000-line copies of the same profile guarantee drift, so a fix committed to the tracked file never reaches installed machines and the two Windows hosts behave differently for no visible reason.
**Dep:** Decompose Get-MiOS.ps1 (420KB) into a MiOS.Install module behind a thin shim

### AGY-1300 -- Harden mios-pipeline.ps1 as the FULL-FOLD orchestrator with StrictMode and phase characterization tests  (WS-PWSH | P2 | M)
**Goal:** E-21 One deploy front door: flatten every install path -- the named single Windows orchestrator becomes a safe facade the monoliths can be migrated behind.
**What+How:** `mios-pipeline.ps1` (415 lines) is the canonical linear 11-phase Windows orchestrator that its own `.SYNOPSIS` declares as the replacement for the historical `build-mios.ps1` + `install.ps1` + `Get-MiOS.ps1` + `preflight.ps1` tangle, and `run-pipeline.ps1` (3 lines) already drives it non-interactively. Harden it: add `Set-StrictMode -Version Latest`, give the phase functions (`Invoke-PhaseStage`/`DevDistro`/`Overlay`/`Account`/`Install`/`Smoketest`) typed params with `[ValidateSet]`, make `Invoke-LegacyScript`'s fall-through to `build-mios.ps1` explicit and logged rather than implicit, and add a Pester test exercising each phase in `-DryRun`/`-NoPrompt` mode that asserts the phase ORDER and the legacy-script dispatch table. That makes it able to absorb `build-mios.ps1`'s logic incrementally as that monolith is modularized.
**Where:** `mios-pipeline.ps1, run-pipeline.ps1, build-mios.ps1 (legacy dispatch target)`
**Done When:** The script runs under StrictMode with validated phase params; the Pester dry-run test pins the 11-phase order and the dispatch table and fails if either changes; `run-pipeline.ps1` still drives it non-interactively; parse/analyzer/drift gates green; mirrored per Law 15.
**Why:** The FULL-FOLD names this file as the one orchestrator, but migrating four monoliths behind an untested, non-strict 415-line facade would move the tangle rather than remove it.
**Dep:** Build a Pester + golden-master parity harness for PowerShell verbs (the migration oracle)

### AGY-1301 -- Fold the dozen Write-Step/Write-Ok/Write-Warn copies into one SSOT-palette-backed logging module  (WS-PWSH | P2 | S)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- one logging vocabulary, routed through the SSOT palette, across every PowerShell script.
**What+How:** The same colored-logging helpers are redefined everywhere: `Write-Step`/`Write-Ok`/`Write-Warn`/`Write-Bad` in `mios-windows-export.ps1`, `mios-cloud-build.ps1`, `push-to-github.ps1`, `tools/windows/Build-MiOS.ps1` and `preflight.ps1`; `Info`/`Ok`/`Warn` in `mios-oscontrol-server.ps1` (95-97), `mios-igpu-server.ps1`, `mios-ai-node.ps1`, `mios-pc-control.ps1`; and `Write-Info`/`Good`/`Err` in `Get-MiOS.ps1` (5046-5048). Extract one `automation/lib/MiOS.Log.psm1` beside `MiOS.Console.psm1`, wired to the SSOT palette, exposing the canonical set; replace every inline definition with an import. Keep the output format exactly as-is (`[*]`/`[+]`/`[!]` glyphs, same colors) so captured logs and the dashboards that parse them are unaffected — verify with a golden snapshot.
**Where:** `mios-windows-export.ps1, mios-cloud-build.ps1, push-to-github.ps1, tools/windows/Build-MiOS.ps1, preflight.ps1, usr/share/mios/windows/mios-oscontrol-server.ps1 (95-97), Get-MiOS.ps1 (5046-5048), automation/lib/MiOS.Log.psm1 (new)`
**Done When:** One module backs every script's status output; `grep -rn 'function Write-Step\|function Write-Ok\|function Info'` finds only the module; a golden snapshot confirms identical prefixes and colors; parse/analyzer gates green; module in both repos.
**Why:** A dozen near-identical helper sets is high-count duplication that keeps every script's output outside the theme SSOT and lets prefixes drift apart until log-parsing dashboards break.
**Dep:** Consolidate the VT-console palette/theme cluster onto one SSOT-projected module

### AGY-1302 -- Harden the live mios-oscontrol-server.ps1 request boundary: input validation, launch allowlist, source scope  (WS-PWSH | P2 | M)
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- the still-shipping PS server stops being exploitable before the compiled port defaults on.
**What+How:** Until the compiled port lands, the live server accepts `POST /input/type` (`Invoke-TypeText` 779), `/input/key-combo` (`Invoke-KeyCombo` 929) and `/launch` (`Invoke-LaunchAndVerify` 1038) — an untrusted boundary that drives real keystrokes and process launches. Harden it in place: validate the app name in `Resolve-LaunchTarget` (1007) against an allowlist instead of a free `Start-Process`; cap `/input/type` payload length; enforce the tailnet/WSL source scope (100.64.0.0/10 + 172.16/12) in `Read-JsonBody`/request handling rather than relying on the firewall-only `fwRemote` rule at line 92; and confirm no request field reaches `Invoke-Expression` or an unquoted command string. Add Pester tests feeding malicious bodies — path traversal, oversized payloads, non-allowlisted app names, off-tailnet source — asserting each is rejected.
**Where:** `usr/share/mios/windows/mios-oscontrol-server.ps1 (Invoke-TypeText 779, Invoke-KeyCombo 929, Resolve-LaunchTarget 1007, Invoke-LaunchAndVerify 1038, Read-JsonBody 1137, fwRemote 92)`
**Done When:** The server rejects non-allowlisted launch targets, over-length type payloads and off-tailnet source addresses at the request layer; no request field flows into `Invoke-Expression` or an unquoted command; the malicious-input Pester suite passes; parse/analyzer gates green; mirrored per Law 15.
**Why:** Anything that reaches this port today can launch arbitrary processes and synthesize keystrokes on the logged-in desktop, and the only current control is a firewall rule that a compromised LAN or misconfigured interface bypasses.
**Dep:** none

### AGY-1303 -- Unify the two divergent bootstrap repo-fetch paths into one integrity-checked, SSOT-sourced function  (WS-INSTALL | P2 | S)  **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- one repo fetch to one canonical checkout, with the clone source operator-defined.
**What+How:** Bootstrap fetching exists twice with different semantics: `Get-MiOS.ps1`'s `Ensure-MiosBootstrapRepo` (line 115 — `git clone --depth 1` then a `codeload.github.com` zip fallback with TLS12 + `Expand-Archive`) and `installation/mios-common.ps1`'s `Ensure-MiosRepo` (line 150 — git only, exits on failure). Unify into one hardened `Ensure-MiosBootstrapRepo` in `mios-common.ps1`: git-first with the zip fallback, explicit TLS, and a post-fetch integrity check that the expected sentinel file exists (no silent success); have `Get-MiOS.ps1` call it. Float the repo URL and target dir from `mios.toml [bootstrap]` — `build-mios.ps1` already reads `bootstrap.mios_repo`/`bootstrap_repo` at lines 349-351 — with the current literals as fallbacks.
**Where:** `Get-MiOS.ps1 (Ensure-MiosBootstrapRepo 115), installation/mios-common.ps1 (Ensure-MiosRepo 150), build-mios.ps1 (349-351), usr/share/mios/mios.toml ([bootstrap])`
**Done When:** One function backs both call sites with git+zip fallback and sentinel verification; a failed/partial fetch fails loudly on both paths; the repo URL and dir resolve from `mios.toml [bootstrap]` with literal fallback; parse/analyzer gates green; the shared function is in both repos.
**Why:** One path exits on failure and the other continues silently, so a bare-Windows install can proceed on an empty or half-extracted checkout and fail later with an unrelated error — and the clone source is hardcoded where an operator cannot redirect it.
**Dep:** none

### AGY-1304 -- Reduce the three legacy PS redirectors to forward-only shims and gate them with a line budget  (WS-DEPLOY | P2 | S)  **[DONE]**
**Goal:** E-21 One deploy front door: flatten every install path -- surviving redirectors stay thin, documented arg-forwarders and cannot regrow into implementations.
**What+How:** Three legacy redirectors preserve old `irm|iex` one-liners: `install.ps1` (46 lines -> `bootstrap.ps1`), `mios-build-local.ps1` (18 -> `build-mios.ps1`) and `run-pipeline.ps1` (3 -> `mios-pipeline.ps1`). Normalize each to a minimal StrictMode-clean shim that forwards `@args` to its canonical target and does nothing else, then add a `check_ps_redirectors` function to `automation/98-drift-checks.sh` asserting each stays under a small line budget and contains no logic beyond the forward. Confirm the forwarding is byte-parity with a Pester test over an argument set including spaces, quotes and switch parameters.
**Where:** `install.ps1, mios-build-local.ps1, run-pipeline.ps1, bootstrap.ps1, build-mios.ps1, mios-pipeline.ps1, automation/98-drift-checks.sh`
**Done When:** Each redirector is forward-only and under its line budget; adding logic to any of them turns the new drift-check red; the Pester test shows `@args` arriving intact at the target; parse-gate green; mirrored per Law 15.
**Why:** Redirectors that quietly accrete logic are how the current four-monolith tangle formed in the first place, and without a budget check the FULL-FOLD is undone one convenience edit at a time.
**Dep:** Harden mios-pipeline.ps1 as the FULL-FOLD orchestrator with StrictMode and characterization tests

### AGY-1305 -- Retire audit_all.ps1 into the portable CI lint gates and drop its hardcoded drive letters  (WS-TESTDOC | P2 | S)  **[DONE]**
**Goal:** E-06 Test and documentation harness -- syntax coverage lives in gates that actually run in CI, not in a machine-local pseudo-gate.
**What+How:** `audit_all.ps1` (77 lines) is a local-only 4-language syntax auditor hardcoded to scan `C:\MiOS` and `M:\` (lines 68-69) with a no-op `Test-BashSyntax` stub (lines 22-25) — real value trapped behind non-portable paths and never invoked by CI. Once `lint-powershell.sh` exists, fold in its still-unique contribution (the `ConvertFrom-Json` JSON syntax check), replace the `C:\`/`M:\` roots with a repo-relative walk, and delegate Bash coverage to `automation/lint-shell.sh` instead of the stub — or delete the file entirely if `lint-powershell.sh` + `lint-python.sh` + a small JSON check fully subsume it. Update any docs or skills that reference it.
**Where:** `audit_all.ps1, automation/lint-powershell.sh, automation/lint-python.sh, automation/lint-shell.sh`
**Done When:** `audit_all.ps1` is deleted, or is a repo-relative wrapper with no `C:\`/`M:\` literal and no no-op stub; JSON syntax coverage exists somewhere in the CI gate set; references updated; drift-gate green.
**Why:** A "gate" that only runs on one machine, silently no-ops on Bash and points at absolute drive letters gives false assurance that the tree is syntax-clean while CI checks nothing.
**Dep:** Add a PowerShell AST parse-gate to CI (the single highest-value gate)

### AGY-1306 -- Authenticode-sign the shipped Windows .ps1 from an SSOT-defined cert and verify it in a gate  (WS-SEC | P2 | M)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the Windows script plane gets an execution-integrity story to match Linux fapolicyd/UKI.
**What+How:** The Windows-delivered scripts (`Get-MiOS.ps1`, `build-mios.ps1`, `bootstrap.ps1`, `usr/share/mios/windows/*.ps1`) ship unsigned and run under `-ExecutionPolicy Bypass`, so nothing attests what executes. Add a build-time `Set-AuthenticodeSignature` step whose operator signing cert is defined in `mios.toml [security]` and projected — never hardcoded, mirroring the MOK-cert-from-SSOT pattern — plus a verification gate using `Get-AuthenticodeSignature` status that runs on the Windows host and as a drift-check confirming every shipped script either carries a valid signature block or is explicitly allowlisted. Keep signing opt-in: degrade open when no cert is configured so unsigned dev builds still work.
**Where:** `usr/share/mios/windows/*.ps1, Get-MiOS.ps1, build-mios.ps1, bootstrap.ps1, usr/share/mios/mios.toml ([security] signing cert key), automation/98-drift-checks.sh`
**Done When:** The build signs the shipped .ps1 from the SSOT cert and skips cleanly when the key is unset; the verification check reports valid-signature-or-allowlisted for every shipped script; the cert path appears only in `mios.toml`; drift-gate green; keys in both repos.
**Why:** Today an operator pipes unsigned scripts into an elevated shell with policy bypassed, so a tampered mirror or MITM is undetectable — the Linux half of the same product refuses to run unattested code.
**Dep:** none

### AGY-1307 -- Provision pwsh + .NET SDK in the build/CI so the PowerShell and C# gates can actually execute  (WS-TESTGOV | P1 | M)  **[DONE]**
**Goal:** E-05 Test and CI governance -- the runtime root of the whole PowerShell gate tree, without which every PS gate silently no-ops.
**What+How:** Every proposed PS gate (AST parse, PSScriptAnalyzer, Pester parity, .cs compile) needs a PowerShell runtime, but grepping `automation/*.sh`, `Containerfile*` and `.github/workflows/mios-ci.yml` shows `pwsh`/`powershell`/`dotnet-sdk` are installed nowhere in the bootc build or CI and no build phase invokes pwsh. Add a dedicated lint/test stage: either install `powershell` + `dotnet-sdk` (or `mono-devel` for the existing net-fx .cs) into a CI-only layer, or add a Windows/pwsh matrix entry in `.github/workflows/mios-ci.yml`, so `[System.Management.Automation.Language.Parser]::ParseFile` and `Invoke-ScriptAnalyzer` can run. Wire a single `just ps-gate` (or automation phase) that all downstream PS gates hook into, and prove it green on the current tree.
**Where:** `.github/workflows/mios-ci.yml, automation/97-ssot-lint.sh, automation/98-drift-checks.sh, Containerfile`
**Done When:** CI has a stage where `pwsh -c '$PSVersionTable'` and `dotnet --version` both succeed; the AST/PSSA gates run from it and report across all 43 `*.ps1`; the stage is green on main and turns red when any .ps1 is made unparseable.
**Why:** Without a runtime, every PowerShell gate is a no-op that reports success — which is precisely how `build-mios.ps1` stayed unparseable for 70 commits with CI green.
**Dep:** Add a PowerShell AST parse-gate to CI (the single highest-value gate)

### AGY-1308 -- Replace the two committed Windows .exe binaries with a reproducible source build plus an SBOM gate  (WS-SBOM | P1 | M)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- no opaque artifact ships without a build-recorded provenance chain (ADR-0003).
**What+How:** `git ls-files` shows exactly two prebuilt binaries tracked in the tree — `usr/share/mios/windows/MiOS-iGPU-Server.exe` and `MiosServiceTool.exe` (PE32 Mono/.NET, 8192 bytes each) — with no source-build step anywhere (no `csc`/`dotnet build` in `automation/`), so neither can be verified against `MiosServiceTool.cs`. Add a build phase that compiles `MiosServiceTool.exe` from `usr/share/mios/windows/MiosServiceTool.cs` in CI (and locates/compiles the iGPU-server source, or records its absence), writes the compiler and source digest into the baked SBOM, and verifies the artifact byte-wise/functionally. Then stop committing the .exe, or gate that a committed .exe must match a fresh build. Incrementally: keep the committed binary as fallback until the built one proves parity, then delete it and gate. Stage only explicit paths.
**Where:** `usr/share/mios/windows/MiosServiceTool.cs, usr/share/mios/windows/MiosServiceTool.exe, usr/share/mios/windows/MiOS-iGPU-Server.exe, automation/98-drift-checks.sh`
**Done When:** `MiosServiceTool.exe` is produced from source in CI with its provenance in the SBOM; the drift-check fails when a tracked .exe has no reproducible source build or diverges from a fresh compile; `just drift-gate` green.
**Why:** `MiosServiceTool.exe` is load-bearing — it is the run-hidden wrapper behind every WSL scheduled task — yet nobody can prove the shipped bytes came from the committed .cs, which is an unverifiable supply-chain surface and a direct ADR-0003 violation.
**Dep:** Provision pwsh + .NET SDK in the build/CI so the PowerShell and C# gates actually execute

### AGY-1309 -- Pin and integrity-verify the runtime-downloaded Windows model and binary artifacts  (WS-SBOM | P1 | M)  **[DONE]**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- `:latest` stays an intent in SSOT while the fetched bytes carry a recorded, verified digest.
**What+How:** `mios-igpu-server.ps1` fetches the llama.cpp Vulkan release with `LlamaTag='latest'` (via `api.github.com/repos/ggml-org/llama.cpp/releases/latest` then `browser_download_url`) and a default GGUF from `huggingface.co/Qwen/...resolve/main` with NO checksum; `mios-ai-node.ps1` carries a HuggingFace model registry (granite4.1:8b, lfm2:700m, nomic-embed-text) fetched the same unverified way. Add SHA-256 pinning and verification, with the digest resolved and recorded at build time per ADR-0003, plus an offline cache path so first run is reproducible. Keep the literal URL as a `${VAR:-URL}` fallback and degrade open when the cache is warm. Prove the control with a characterization test that a tampered download is rejected.
**Where:** `usr/share/mios/windows/mios-igpu-server.ps1, usr/share/mios/windows/mios-ai-node.ps1, usr/share/mios/mios.toml`
**Done When:** Both scripts verify a pinned SHA-256 before using an artifact and fail closed on a corrupted or swapped file; the pins live in `mios.toml` SSOT / the baked SBOM; a drift-check confirms no unpinned runtime fetch remains in `windows/*.ps1`.
**Why:** An always-on inference node that pulls unverified `latest` binaries and multi-GB weights from GitHub and HuggingFace on first run has no reproducibility and no defense against a swapped artifact executing on the host.
**Dep:** Port the Windows AI-node HTTP servers (mios-igpu-server, mios-ai-node) to a compiled binary

### AGY-1310 -- Route every PowerShell secret prompt and secret file through one hardened Read-MiosSecret (kill the plaintext %TEMP% credential file)  (WS-PWSH | P1 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- collapses four ad-hoc credential-capture implementations into one hardened, gated helper so the Windows entry plane stops leaking secrets to disk.
**What+How:** `config/bootstrap/bootstrap.ps1` writes credentials in plaintext to `$env:TEMP\mios-secrets.env` (its `SecretsFile`) and carries a hand-rolled `Read-Secret`; `tools/fix-token-input.ps1` documents a broken `[Console]::ReadKey` masking loop that `Read-Host -MaskInput` already solves; `Get-MiOS.ps1` and `mios-build-local.ps1` each roll their own token capture. Write ONE `Read-MiosSecret` helper (SecureString in memory, `ConvertFrom-SecureString`/DPAPI if anything must persist, ACL'd to the invoking user, scrubbed on exit, never a world-readable `%TEMP%` path) in the shared `installation/mios-common.ps1` contract. Strangler-fig: land the helper, migrate call sites one at a time keeping the current prompt text byte-parity, then delete the ad-hoc loops and the plaintext temp path. Add a drift-check grepping for `mios-secrets.env` and for `Read-Host` without `-MaskInput` in secret contexts.
**Where:** `config/bootstrap/bootstrap.ps1, tools/fix-token-input.ps1, mios-build-local.ps1, Get-MiOS.ps1, installation/mios-common.ps1`
**Done When:** No tracked PS script writes a secret to a plaintext `%TEMP%` file; every secret prompt calls `Read-MiosSecret`; the new gate FAILS on a synthetic plaintext-secret write and PASSES on the cleaned tree; paste-capture is covered by a test.
**Why:** Today a real GHCR/API credential lands unencrypted in a world-readable temp directory on every operator box that runs bootstrap, and the duplicated masking loop is already known-broken for pasted tokens -- a live security defect, not a hygiene item.
**Dep:** Consolidate self-elevation (UAC RunAs) into one hardened helper

### AGY-1311 -- Port the Windows computer-use surface (mios-pc-control.ps1 + mios-uia-dump.ps1) to a compiled binary  (WS-PWSH | P2 | L)
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- moves the largest remaining PowerShell-as-program surface onto the compiled tier so the agent's GUI control path is typed and tested (Law 14 TARGET-LANGUAGES).
**What+How:** `mios-pc-control.ps1` is a 440-line Win32 P/Invoke computer-use surface (`SetCursorPos`/`mouse_event`/SendKeys/screenshot/`EnumWindows` plus a UI-Automation read-back `type` verifier), and `mios-uia-dump.ps1` is its 110-line UIA tree dumper; both are distinct from the window-launch/positioning in `mios-launch.cs` and from the oscontrol HTTP server. Port both into ONE compiled C#/.NET (or windows-rs) executor invoked by the Linux helper `/usr/libexec/mios/mios-pc-control`, preserving the strict SendKeys read-back verification (the anti-"LIAR" logic at lines 112-196) and the JSON envelopes verbatim. Strangler-fig: build the binary emitting byte-identical JSON, characterize every subcommand (screenshot/click/type/key/key-combo/window-*/screen-layout) against golden outputs, demote the .ps1 to a thin shim, then delete.
**Where:** `usr/share/mios/windows/mios-pc-control.ps1, usr/share/mios/windows/mios-uia-dump.ps1, usr/libexec/mios/mios-pc-control, src/`
**Done When:** A compiled executor replaces both scripts; golden-master tests show byte-identical JSON for every subcommand including the `type` read-back reason codes; the .ps1 files are thin shims (or deleted) with shim parity proven; `cargo`/`dotnet test` and the drift-gate are green.
**Why:** 550 lines of untested P/Invoke drive every GUI action the agent takes on Windows; without a golden oracle any reimplementation silently changes click/type semantics, and the read-back verifier that catches "the agent lied about typing" is exactly the logic a blind rewrite would break.
**Dep:** Build a Pester + golden-master parity harness for PowerShell verbs (the migration oracle)

### AGY-1312 -- Unify WSL2 distro-name resolution behind Resolve-MiosDistro and gate the 'podman-MiOS-DEV' literal  (WS-PWSH | P2 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- removes a 16-file cross-cutting hardcode by lifting an existing generative resolver into the shared globals module.
**What+How:** The literal `podman-MiOS-DEV` is hardcoded across 16 tracked `*.ps1` (mios-fix-wslg, mios-tailscale-serve, mios-wsl-keepalive, mios-wsl-session-task, Update-MiOSStartMenuShortcuts, mios-cloud-build, mios-windows-export, Reinstall-MiOSDEV, refresh-flatpak-shortcuts, Uninstall-MiOS, bootstrap.ps1, build-mios.ps1, Get-MiOS.ps1, powershell/profile.ps1, automation/mios-build-builder.ps1), while `mios-claude-mcp-setup.ps1` ALREADY resolves it generatively from `HKCU\...\Lxss` (preferring a distro whose name matches `MiOS`, else the default distro). Lift that registry resolver into `automation/lib/globals.ps1` as `Resolve-MiosDistro` with a `${MIOS_WSL_DISTRO:-podman-MiOS-DEV}` SSOT fallback, replace the literals script-by-script keeping the literal only as last-resort default, and add a drift-check banning a bare `podman-MiOS-DEV` outside the resolver plus its allowlist.
**Where:** `automation/lib/globals.ps1, usr/share/mios/windows/mios-claude-mcp-setup.ps1, usr/share/mios/windows/mios-wsl-keepalive.ps1, usr/share/mios/windows/mios-tailscale-serve.ps1, usr/share/mios/mios.toml, automation/98-drift-checks.sh`
**Done When:** `Resolve-MiosDistro` is the single source of the distro name; the new drift-check FAILS on any new hardcoded `podman-MiOS-DEV` outside the allowlist; every migrated script still resolves the distro after the post-bootstrap rename `podman-MiOS-DEV -> MiOS-DEV` (registry match wins).
**Why:** The post-bootstrap rename already breaks the literal, so 16 scripts silently target a distro that no longer exists -- and the generative fix is sitting unused in one file while the other 15 hardcode.
**Dep:** Extract the duplicated mios.toml resolver into one shared PowerShell module

### AGY-1313 -- Sweep the ad-hoc regex TOML parsers onto the shared resolver and reconcile their conflicting port/color fallbacks  (WS-PWSH | P1 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- makes ONE resolver with ONE ordered layer-search list the only way a PowerShell script may read mios.toml (Law 8 SSOT-PROJECTION).
**What+How:** Beyond `globals.ps1` there are independent hand-rolled TOML readers that search DIFFERENT candidate paths with DIVERGENT fallbacks: `Set-MiOSWallpaper.ps1`'s `Get-ColorsFromToml` (layers `~/.config/mios`, `C:\ProgramData\MiOS`, `C:\Windows\Web\MiOS`), `Heal-MiOSLocalhostForwarding.ps1`'s `Get-MiosPort` (layers `M:\etc\mios`, `M:\usr\share\mios`, `C:\mios-bootstrap`; fallbacks open_webui=8033, cockpit=8090, searxng=8899, hermes_dashboard=8119), and `mios-tailscale-serve.ps1`'s baked `$SERVICES` table (open-webui=3030, cockpit=9090, searxng=8888, hermes-dash=9119) -- the same lanes resolve to CONFLICTING ports across scripts. Migrate every consumer onto the shared `globals.ps1` resolver with one ordered layer list, delete the ad-hoc parsers, reconcile the per-key `${...:-LIT}` fallbacks to `[ports]`/`[colors]`, and add a gate banning any `^\[section\]`-style regex TOML parsing in `*.ps1` outside the resolver.
**Where:** `usr/share/mios/windows/Set-MiOSWallpaper.ps1, usr/libexec/mios/Heal-MiOSLocalhostForwarding.ps1, usr/share/mios/windows/mios-tailscale-serve.ps1, automation/lib/globals.ps1, automation/98-drift-checks.sh`
**Done When:** No PS script parses mios.toml with its own regex; all read via the shared resolver over one layer-path list; the conflicting port/color fallbacks match `[ports]`/`[colors]`; drift-check 28 (or a sibling) covers these files, not just `globals.ps1`.
**Why:** Two MiOS scripts on the same host currently resolve DIFFERENT values for the same lane (cockpit 8090 vs 9090, searxng 8899 vs 8888) -- a live correctness drift that the globals-only check cannot see, so healing and serve-publishing target the wrong ports.
**Dep:** Extract the duplicated mios.toml resolver into one shared PowerShell module

### AGY-1314 -- Establish an SSOT owned-artifact registry and gate install/uninstall symmetry  (WS-PWSH | P2 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- binds every host artifact MiOS creates on Windows to the uninstaller by construction, so create-sites can never outrun removal.
**What+How:** MiOS PS scripts create host artifacts across many files -- scheduled tasks `MiOS-Autostart` and `MiOS-Resume-Bootstrap` (build-mios.ps1/Get-MiOS.ps1), `MiOS-WSL-KeepAlive`, `MiOS-WSL-Session`, the `MiOS-iGPU-Server` service+task, firewall rule `MiOS - igpu-llm ($Port/tcp)`, netsh portproxy maps, `HKLM\SOFTWARE\MiOS` + `\Wallpaper`, and Start-menu `.lnk` files -- but `Uninstall-MiOS.ps1` removes only a subset (distros, portproxy, some services), so each new create-site orphans silently. Declare an SSOT list of MiOS-owned artifact identifiers (task names, service names, firewall DisplayNames, registry roots, shortcut folders) in `mios.toml` and drive BOTH `Uninstall-MiOS.ps1` and a drift-check from it: the gate greps `Register-ScheduledTask`/`New-Service`/`New-NetFirewallRule`/`Set-ItemProperty` create-sites and fails if any produced identifier is outside the uninstall coverage set. Preserve Uninstall's dry-run default and KEEP-list safety.
**Where:** `Uninstall-MiOS.ps1, usr/share/mios/windows/mios-wsl-keepalive.ps1, usr/share/mios/windows/mios-wsl-session-task.ps1, usr/share/mios/windows/mios-igpu-server.ps1, usr/share/mios/mios.toml, automation/98-drift-checks.sh`
**Done When:** One owned-artifact list exists in SSOT; the drift-check FAILS when a create-site produces an artifact `Uninstall-MiOS.ps1` does not remove; `Uninstall-MiOS.ps1 -WhatIf` enumerates every current task/service/firewall/registry/shortcut artifact.
**Why:** Uninstalling MiOS today leaves live scheduled tasks, a running service and open firewall rules behind on the operator's machine, and the 6+ create-sites have no mechanical link to the uninstaller, so the gap widens with every new script.
**Dep:** Consolidate scheduled-task registration into one Register-MiosScheduledTask helper

### AGY-1315 -- Consolidate the three Start-Menu shortcut + icon generators into one hardened module  (WS-PWSH | P2 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- deletes triplicated WSLg icon/.lnk logic so the flatpak-shortcut workaround exists in exactly one place.
**What+How:** Three scripts re-implement overlapping WSL-flatpak Start-Menu `.lnk`/icon generation: `usr/libexec/mios/Update-MiOSStartMenuShortcuts.ps1` (438 lines: `resolve_icon`, `Convert-PngToIco`, `Clear-WindowsIconCache`), `tools/refresh-flatpak-shortcuts.ps1` (130 lines), and `usr/share/mios/ventoy/autorun/mios-stage-icons.ps1` (139 lines, icon staging). Extract ONE shared module covering icon resolve/convert, `wslg.exe` `.lnk` emission, `.desktop` enumeration and icon-cache clearing, and reduce the three entry points to thin wrappers. Strangler-fig: land the module, port `Update-MiOSStartMenuShortcuts` first with golden `.lnk` parity on Target/Arguments/WindowStyle/IconLocation, then fold the other two and delete the duplicated bodies.
**Where:** `usr/libexec/mios/Update-MiOSStartMenuShortcuts.ps1, tools/refresh-flatpak-shortcuts.ps1, usr/share/mios/ventoy/autorun/mios-stage-icons.ps1`
**Done When:** One shortcut/icon module backs all three entry points; generated `.lnk` fields are byte-identical to the golden pre-refactor output; the icon-conversion and `.desktop` enumeration code exists exactly once; the PowerShell gate is green.
**Why:** The same fragile WSLg workaround is maintained in three copies, so a fix to icon resolution or cache clearing lands in one and silently rots in the other two -- the operator has already hit "apps not populating natively" repeatedly for this reason.
**Dep:** Provision pwsh + .NET SDK in the build/CI so the PowerShell and C# gates actually execute

### AGY-1316 -- Consolidate the Windows OCI export/convert plane into a MiOS.Export module with characterization tests  (WS-PWSH | P2 | M)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- de-duplicates the registry-auth and image-conversion logic that two self-described complementary scripts each carry.
**What+How:** `mios-cloud-build.ps1` (338 lines, BIB-via-podman) and `mios-windows-export.ps1` (421 lines, self-described "Complement to mios-cloud-build.ps1") duplicate the WSL-tarball export, VHDX conversion, and GHCR layer pull/auth paths (`Get-GhcrToken`, `Resolve-ImageRef`, `Save-ImageLayers`), and both re-declare `Write-Step`/`Write-Ok`/`Write-Warn`/`Write-Bad`. Extract the shared conversion + registry-fetch functions into a `MiOS.Export` module (reusing the shared logging module), enable `Set-StrictMode`, and add characterization tests asserting the produced manifest/target matrix (qcow2/raw/vhdx/wsl/iso) against golden fixtures without needing a real image. Strangler-fig: module first, migrate each script behind it proving identical CLI and output, then delete the duplicated function bodies.
**Where:** `mios-cloud-build.ps1, mios-windows-export.ps1, tools/windows/Build-MiOS.ps1`
**Done When:** WSL-export, VHDX-convert and GHCR-fetch logic lives once in the shared module used by both scripts; `Set-StrictMode` is on in both; characterization tests cover the full target matrix; both scripts pass PSScriptAnalyzer and the AST parse gate.
**Why:** Two 300-400 line scripts hold divergent copies of the GHCR token/auth path, so a security fix or registry-API change must be found and applied twice -- and neither is covered by the build-mios decomposition, which only targets the local in-VM builder.
**Dep:** Consolidate the Write-Step/Write-Ok/Write-Warn logging helpers into one module

### AGY-1317 -- Disambiguate the two bootstrap.ps1 entry points and normalize the GitHub repo slug casing  (WS-PWSH | P2 | S)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- gives the `irm | iex` web door one unambiguous filename and one canonical slug, mirrored in both repos per Law 15.
**What+How:** Two unrelated scripts share the filename `bootstrap.ps1`: the root one (99 lines, the `curl`/`iex` web entry for the dev VM, shipping with a UTF-8 BOM) and `config/bootstrap/bootstrap.ps1` (240 lines, an env/secrets provisioner with `Read-Secret`/`Import-EnvFile` and a PrivateInstaller pointing at `install.ps1`). The one-liners also disagree on slug casing: root uses `mios-dev/mios-bootstrap`, config uses `MiOS-DEV/MiOS-bootstrap`, the private installer uses `MiOS-DEV/mios`. Rename one (e.g. `config/bootstrap/provision-env.ps1`), normalize every `raw.githubusercontent` slug to the canonical lowercase `org/repo`, mirror the canonical entry surface in BOTH mios.git and mios-bootstrap.git per Law 15, and keep the working one-liner URLs as literals through the migration.
**Where:** `bootstrap.ps1, config/bootstrap/bootstrap.ps1, install.ps1, installation/mios-common.ps1`
**Done When:** No two tracked scripts are named `bootstrap.ps1` with divergent purpose; every GitHub slug uses one canonical casing; the canonical web entry is Law-15 mirrored in both repos; a gate flags mixed-case `mios`/`mios-bootstrap` slugs.
**Why:** An operator (or an agent) fetching "bootstrap.ps1" can land on the secrets provisioner instead of the web entry, and three casings of the same slug make the `irm | iex` door fail non-deterministically depending on which one-liner was copied.
**Dep:** Consolidate the mios-bootstrap repo-fetch logic into one hardened function

### AGY-1318 -- Add an encoding/BOM + line-ending normalization gate for shipped .ps1 files  (WS-PWSH | P2 | S)  **[DONE]**
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- adds the byte-level policy gate that the AST parse-gate and the Authenticode gate each assume but neither enforces.
**What+How:** BOM handling has already bitten this tree: the root `bootstrap.ps1` ships with a UTF-8 BOM, a `Setup-MiOSLanPortProxy.ps1.bom-bak` artifact is TRACKED (and explicitly skipped in the `98-drift-checks.sh` list around line 1665), and `build-mios.ps1` carries multiple hand-written BOM workarounds because PS 5.1 `Set-Content -Encoding UTF8` emits a BOM. Add a drift-check enforcing ONE policy for shipped `*.ps1` (UTF-8, a defined BOM rule, LF or CRLF applied consistently), normalize the existing files to it, and prune the `.bom-bak` artifact. This complements the AST parse-gate (which proves parseability, not encoding) and the Authenticode gate (signatures die on silent BOM/EOL rewrites).
**Where:** `bootstrap.ps1, usr/share/mios/windows/Setup-MiOSLanPortProxy.ps1.bom-bak, build-mios.ps1, automation/98-drift-checks.sh`
**Done When:** The drift-check FAILS on any shipped `.ps1` violating the encoding/EOL policy; `bootstrap.ps1`'s BOM is resolved per policy; the `.bom-bak` file no longer exists in the tree; the gate is green on main.
**Why:** BOM/EOL drift has already produced a committed backup artifact, a permanent drift-check skip, and scattered per-script workarounds -- and it silently invalidates Authenticode signatures, so signed scripts fail verification for a reason no existing gate can name.
**Dep:** Authenticode-sign the shipped Windows .ps1 and add a verification gate

### AGY-1319 -- Fix the stale mios-ai.target Wants=mios-letta-server.service and gate dangling unit references  (WS-SYSTEMD | P1 | S)  **[DONE]**
**Goal:** E-18 Generate the 168 systemd units from SSOT -- gives the unit migration a clean DAG oracle by making dangling `After=`/`Wants=`/`Requires=` edges a hard gate failure.
**What+How:** `usr/lib/systemd/system/mios-ai.target` carries `Wants=mios-letta-server.service`, but no such unit exists in the tree (letta was removed in the legacy `/v1`-only purge) -- a dead DAG edge systemd silently ignores. Repoint it at the live AI-plane units (`mios-agent-pipe.service` / `hermes-worker.service` per `[services.hermes]`), then extend `check_dag_integrity()` in `automation/98-drift-checks.sh` (around line 1998) so every `After=`/`Wants=`/`Requires=` token in `usr/lib/systemd/system/*.{service,target}` and `usr/share/containers/systemd/*` must resolve to a real unit file, a quadlet-derived generated service, or a well-known systemd target -- failing otherwise. Stage only the two edited files (never `git add -A`).
**Where:** `usr/lib/systemd/system/mios-ai.target, automation/98-drift-checks.sh`
**Done When:** `mios-ai.target` Wants only live units; the new dangling-ref assertion inside `check_dag_integrity` FAILS on a synthetic bad edge and PASSES on the cleaned tree; `just drift-gate` (or `bash automation/98-drift-checks.sh`) is green.
**Why:** A load-bearing target currently pulls a unit that does not exist, so the AI plane's boot ordering is a lie no gate catches -- and without the dangling-ref assertion the compiled generator would faithfully reproduce the dead edge into all 168 units.
**Dep:** none

### AGY-1320 -- Deduplicate ceph-bootstrap.service vs mios-ceph-bootstrap.service into one canonical unit  (WS-SYSTEMD | P2 | S)  **[DONE]**
**Goal:** E-18 Generate the 168 systemd units from SSOT -- shrinks the hand-maintained surface the generator must reproduce and kills a double-run hazard before migration starts.
**What+How:** `usr/lib/systemd/system/ceph-bootstrap.service` and `usr/lib/systemd/system/mios-ceph-bootstrap.service` both `ExecStart=/usr/libexec/mios/ceph-bootstrap.sh` as `Type=oneshot` first-boot units, differing only in `ConditionPathExists` (a `.bootstrapped` sentinel vs `/etc/ceph/ceph.conf`) and one `After=podman.socket` edge. Keep the `mios-`prefixed unit as canonical (Law 9 ONE-CANONICAL-NAME), fold the missing guards into it (sentinel + `StartLimitIntervalSec=0` + `ConditionVirtualization=no`), delete the unprefixed twin, and grep the tree -- `automation/*.sh` enable symlinks, greenboot checks, `78-greenboot.sh` -- to repoint every reference. Record the canonical name so the future `[units.*]` generator emits exactly one.
**Where:** `usr/lib/systemd/system/ceph-bootstrap.service, usr/lib/systemd/system/mios-ceph-bootstrap.service`
**Done When:** Exactly one ceph bootstrap unit remains; no automation or greenboot reference points at the deleted name; `grep -r ceph-bootstrap.service` returns a single unit; drift-gate green.
**Why:** Two enabled units invoking the same first-boot script can both fire on a host whose sentinel and `/etc/ceph/ceph.conf` disagree, and duplicated hand-authored units are exactly the class WS-SYSTEMD exists to eliminate.
**Dep:** none

### AGY-1321 -- Capture byte-exact golden-master snapshots of all 161 hand-maintained units before migration  (WS-SYSTEMD | P1 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- establishes the deviance oracle without which no strangler-fig unit migration can prove parity.
**What+How:** Before touching any unit, characterize current behavior: add a Rust `trycmd`/`insta`-style fixture set under `tools/native/mios-unit-gen/tests/golden/` recording every file in `usr/lib/systemd/system/` byte-for-byte -- 84 `.service`, 10 `.timer`, 5 `.path`, 1 `.socket`, 11 `.target`, 4 `.mount` and 46 `.d` drop-ins -- as the frozen baseline. Provide a `cargo test` that diffs the committed tree against the snapshots plus a documented `cargo insta accept` regen path, mirroring how `generate-pod-quadlets.py --selftest` guards its renderer. Any later generator output must match these bytes or the diff is deliberately reviewed and accepted.
**Where:** `tools/native/mios-unit-gen/tests/golden/, tools/native/Cargo.toml`
**Done When:** `cargo test -p mios-unit-gen` reproduces the 161-file baseline with zero unexpected diffs; deliberately mutating one unit makes the test FAIL; the regen workflow is documented in the crate README.
**Why:** 161 units have no tests at all, so a generator rewrite would ship silent boot regressions across the whole fleet with nothing to compare against -- and once units start moving to SSOT, the pre-migration bytes are unrecoverable.
**Dep:** Fix stale mios-ai.target; Deduplicate ceph-bootstrap

### AGY-1322 -- Scaffold the compiled mios-unit-gen crate (serde/miette/thiserror) in the native workspace  (WS-SYSTEMD | P1 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- stands up the compiled façade behind which hand-authored units get replaced one family at a time.
**What+How:** Create `tools/native/mios-unit-gen` (bin + lib) and add it to `members` in `tools/native/Cargo.toml` (currently `mios-drift-runner`, `mios-version-check`, `mios-wallpaperd`). Model it on `generate-pod-quadlets.py`'s proven contract: a PURE renderer function, a `--check` drift mode, and a `--selftest`. Use serde+toml for typed deserialization of the mios.toml unit model, `thiserror` for internal resolver error enums, and `miette::Result` from `main` so a bad value points AT the offending mios.toml line/span. Ship workspace-level `cargo fmt --check` and `cargo clippy -- -D warnings` config with the `clippy::unwrap_used` / `clippy::panic` restriction lints. No unit logic yet -- skeleton, arg parsing, TOML load, and a stub renderer returning empty.
**Where:** `tools/native/mios-unit-gen/, tools/native/Cargo.toml`
**Done When:** `cargo build -p mios-unit-gen`, `cargo fmt --check`, and `cargo clippy -p mios-unit-gen -- -D warnings` all pass; `mios-unit-gen --selftest` exits 0; the crate is a workspace member.
**Why:** Every unit family migrated after this inherits typed diagnostics and clippy gating for free; skipping the discipline up front means retrofitting it across a dozen families later, and the Rust plane still lacks the shellcheck-equivalent bash already has.
**Dep:** Capture golden-master snapshots

### AGY-1323 -- Define the [units.*] typed SSOT schema in mios.toml, mirroring the [containers.*] nested convention  (WS-SYSTEMD | P2 | L)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- turns 161 hand-authored files into projections of one typed source (Law 8 SSOT-PROJECTION, NS-3).
**What+How:** Add a `[units.<name>.<type>.<Section>]` nested-table block to `usr/share/mios/mios.toml`, modeled on the existing `[containers.mios-*.Container/Unit/Install/Service]` convention that `render_nested_quadlet` already consumes, where `<type>` is `service|timer|path|socket|target|mount`. Deserialize it into typed Rust structs in `mios-unit-gen`: `Unit{description,after,wants,requires,condition,...}`, `Service{type,exec_start,environment,sandbox,...}`, `Install{wanted_by}`, plus Timer/Path/Socket variants. Preserve `resolve_env_vars`' rule that `${MIOS_PORT_*}` placeholders are KEPT, not resolved at generate time (they are baked at deploy by `34-render-quadlets.sh`). Per Law 15, mirror the schema stub/keys into the mios-bootstrap.git copy of the shared SSOT. Start by encoding ~5 representative simple units (`mios-podman-gc.service` + `.timer`, `mios-firstboot.target`) to prove round-trip.
**Where:** `usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/lib.rs, (mios-bootstrap.git mios.toml mirror)`
**Done When:** `mios-unit-gen` deserializes `[units.*]` into typed structs and emits a miette span error on a bad shape; the ~5 encoded units render byte-identical to their golden snapshots; both repos' mios.toml parse under `tomllib` with a sane `wc -l`.
**Why:** Without one schema there is nothing for the generator to read, and every unit stays a hand-edited file whose values (ports, ordering, hardening) are invisible to the operator's single config surface.
**Dep:** Scaffold the compiled mios-unit-gen crate

### AGY-1324 -- Bake deny-by-default sandbox directives into the generator's Service renderer  (WS-SYSTEMD | P1 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- makes the hardened posture the emitted default so no generated unit can regress into the unconfined majority.
**What+How:** Only 20/84 `.service` files set `NoNewPrivileges`, 18/84 set `ProtectSystem`, and 1/84 sets `SystemCallFilter` -- roughly 40 units have zero confinement. In `mios-unit-gen`'s Service renderer, emit a hardened baseline for every generated long-running unit unless explicitly overridden in `[units.<name>.service.sandbox]`: `NoNewPrivileges=yes`, `ProtectSystem=strict`, `ProtectHome=yes`, `PrivateTmp=yes`, `SystemCallFilter=@system-service`, `RestrictAddressFamilies`, and `CapabilityBoundingSet` drop-all-plus-add-back, with `ReadWritePaths` as the only writable exception. Model the writable-path and `NoNewPrivileges=false` exceptions on the real `hermes-worker.service` (`ReadWritePaths=/etc/mios /var/lib/mios`, sudo grant) so migrated units keep working, and add a `[security.privileged_units]` roster for documented exceptions, parallel to `[security.privileged_quadlets].root`.
**Where:** `tools/native/mios-unit-gen/src/, usr/share/mios/mios.toml`
**Done When:** Generating a minimal `[units.*]` service yields every baseline hardening key; a unit listing an override omits/relaxes exactly that key and nothing else; the hermes-worker equivalent renders byte-identical to its golden snapshot; unit tests cover the default+override matrix.
**Why:** About 40 shipped services run unconfined today, and every hand-authored unit added since has depended on an author remembering to harden it -- a posture that has demonstrably not held and that the generator can fix permanently in one place.
**Dep:** Define the [units.*] typed SSOT schema

### AGY-1325 -- Add a systemd-analyze security ratchet as a fitness-function drift-check  (WS-SYSTEMD | P1 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- converts unit hardening from an episodic audit into a continuously enforced, mios.toml-thresholded gate (NS-6, governance as policy-as-code).
**What+How:** Add `check_unit_security()` to `automation/98-drift-checks.sh`, registered in `main()` near line 6298 alongside `check_quadlet_privilege`, and in `tools/generate-gate-index.py` / `tools/generate-pipeline-index.py`. Statically lint every `usr/lib/systemd/system/*.service` for the high-value directives (`NoNewPrivileges`, `ProtectSystem=strict|full`, `PrivateTmp`, `SystemCallFilter`, `CapabilityBoundingSet`) and, where a live systemd is present in CI, run `systemd-analyze security <unit>` and fail any long-running unit whose exposure score exceeds a mios.toml-defined threshold -- exempting only the `[security.privileged_units]` roster. Keep it SOFT-first (WARN when `systemd-analyze` is absent, as `check_var_closure` does) so the bare Windows dev host does not false-fail; the hard gate is CI/bake.
**Where:** `automation/98-drift-checks.sh, tools/generate-gate-index.py, tools/generate-pipeline-index.py, usr/share/mios/mios.toml`
**Done When:** `check_unit_security` FAILS on a synthetic unconfined unit and PASSES on the hardened tree; the score threshold and exempt roster are read from mios.toml; the gate index is regenerated and staged; the drift-gate is green on Linux CI.
**Why:** Nothing currently stops a new unit from shipping with zero confinement, so the ~40 unconfined services keep multiplying and the hardening the migration delivers is unmeasurable and unprotected.
**Dep:** Bake deny-by-default systemd sandbox directives

### AGY-1326 -- Migrate the 10 timer units to generated [units.*.timer] with SSOT-declared cadence  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- the first strangler slice, proving the generator reaches byte-parity on real shipped units.
**What+How:** Port the 10 `.timer` files (`mios-ai-firstboot`, `mios-podman-gc`, `mios-pgvector-backup`, `mios-embed-backfill`, `mios-aios-refresh`, `mios-dashboard-issue`, `mios-podman-ps`, `mios-skills-miner`, `mios-suggestion-refresh`, `mios-sys-env-refresh`) into `[units.<name>.timer]` blocks carrying `OnCalendar`/`OnBootSec`/`OnUnitInactiveSec`/`Persistent` plus `Unit=` from SSOT (observed values include `OnBootSec=2min` on four of them, and weekly cadences). Generate them via `mios-unit-gen`, proving each output byte-identical to its golden snapshot; demote automation to only invoke the binary and keep the checked-in `.timer` files as the generated artifact.
**Where:** `usr/lib/systemd/system/*.timer, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** All 10 timers render byte-identical to golden; `mios-unit-gen --check` reports the timer family in sync; drift-gate and `cargo test` green.
**Why:** Until one real family renders byte-identical, the generator is unproven and no higher-risk service migration can be justified -- and every cadence stays a hand-edited literal the operator cannot see or tune from the config surface.
**Dep:** Define the [units.*] typed SSOT schema

### AGY-1327 -- Migrate the 11 .target units to generated [units.*.target] grouping  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- moves the install-ordering DAG itself into SSOT where the After/Wants/Requires graph can be validated centrally.
**What+How:** Port the 11 targets (`mios-ai`, `mios-firstboot`, `mios-compute`, `mios-controller`, `mios-desktop`, `mios-endpoint`, `mios-headless`, `mios-hybrid`, `mios-ha-node`, `mios-k3s-master`, `mios-k3s-worker`) into `[units.<name>.target]` with `Description`/`After`/`Wants`/`WantedBy` from SSOT. `mios-firstboot.target`'s `Wants=(mios-cdi-detect mios-libvirtd-setup mios-grd-setup)` and the now-repaired `mios-ai.target` grouping become SSOT-declared DAG edges. Generate via `mios-unit-gen` with each output byte-identical to golden, and reuse the dangling-ref assertion from AGY-1319 so no generated target can Want a non-existent unit.
**Where:** `usr/lib/systemd/system/*.target, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** All 11 targets render byte-identical to golden; the DAG grouping is declared exactly once in `[units.*.target]`; the dangling-ref gate is green; drift-gate and `cargo test` green.
**Why:** Targets are how MiOS selects a machine role (compute/controller/desktop/headless/k3s), and that role graph is currently 11 hand-edited files where a typo produces a role that silently starts nothing.
**Dep:** Migrate the 10 timer units; Fix stale mios-ai.target

### AGY-1328 -- Float ListenStream from [ports] and migrate the socket unit family  (WS-SYSTEMD | P2 | S)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- proves the generator handles the port-float requirement, closing a bare-port-literal hole that feeds the zero-hardcodes campaign (Law 7 NO-HARDCODE).
**What+How:** `mios-cockpit-link.socket` hardcodes `ListenStream=0.0.0.0:8091` and `cockpit.socket.d/listen.conf` pins another literal `ListenStream` -- both must float from `[ports].cockpit_link` / `[ports].cockpit`. Migrate `mios-cockpit-link.socket` to `[units.mios-cockpit-link.socket]` whose `ListenStream` is emitted as `0.0.0.0:${MIOS_PORT_COCKPIT_LINK}`, preserving the deploy-time envsubst convention `34-render-quadlets.sh` bakes, and regenerate the `cockpit.socket.d` drop-in from SSOT. Prove byte-parity against golden after accounting for the placeholder -- either re-take the golden baseline in placeholder form or diff post-render.
**Where:** `usr/lib/systemd/system/mios-cockpit-link.socket, usr/lib/systemd/system/cockpit.socket.d/listen.conf, usr/share/mios/mios.toml`
**Done When:** The socket `ListenStream` carries a `${MIOS_PORT_*}` placeholder resolving to 8091 at deploy; generator output matches golden; no bare `:8091` or `ListenStream=[0-9]` literal remains in the socket family; drift-gate green.
**Why:** Changing the cockpit-link port in mios.toml today leaves the socket listening on the old 8091, so SSOT and the running listener disagree -- and the `.d` drop-in makes the same literal wrong in two places.
**Dep:** Define the [units.*] typed SSOT schema

### AGY-1329 -- Collapse the three parallel firewall port lists into one SSOT-derived generator  (WS-SYSTEMD | P1 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- makes one `[firewall.open_ports]` selector the single declaration of what MiOS exposes, driving both the boot-time unit and the build-time firewall config.
**What+How:** Exposed ports are enumerated THREE times: `[ports]` in `mios.toml`, `automation/44-firewall-ports.sh` (a `firewall-offline-cmd --add-port` per service), and `mios-firewall-ports.service`'s inline `ports=(...)` bash array with its own `:-` defaults. Introduce a `[firewall.open_ports]` SSOT selector -- a list of `[ports]` keys to expose on the public zone -- and have `mios-unit-gen` render BOTH the `mios-firewall-ports.service` ExecStart port array AND (via a small emitter) the port set `44-firewall-ports.sh` opens, from that one list. Demote both consumers to read the generated list, keeping the `${MIOS_PORT_*:-LIT}` fallback form, and hold byte-parity vs golden for the `.service`.
**Where:** `usr/lib/systemd/system/mios-firewall-ports.service, automation/44-firewall-ports.sh, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** Adding one key to `[firewall.open_ports]` changes both the boot-time service and the build-time firewall config with zero hand-editing; the `.service` renders byte-identical (modulo the SSOT-driven list) to golden; drift-gate green.
**Why:** Three divergent copies of the exposed-port list mean a port can be opened at build but closed at boot (or the reverse) -- a live security and availability hazard where the firewall state depends on which of three files was last edited.
**Dep:** Float ListenStream from [ports]

### AGY-1330 -- Migrate the 5 .path units to generated [units.*.path]  (WS-SYSTEMD | P2 | S)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- broadens generator type coverage across the passive unit families at near-zero risk.
**What+How:** Port the 5 path units -- `mios-bootc-switch.path` (watching `/var/lib/mios/forge-runner/last-build.txt`), `mios-additionalimagestores-perms.path`, `mios-libexec-perms.path`, `mios-wsl-flatpak-export-sync.path`, `hermes-worker.path` -- into `[units.<name>.path]` with `PathChanged`/`PathExists` plus `Unit=` from SSOT. These are trivially templatable (Description + `[Path]` + `[Install]`), so they validate the `.path` renderer cheaply. Generate via `mios-unit-gen` with byte-parity vs golden.
**Where:** `usr/lib/systemd/system/*.path, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** All 5 `.path` units render byte-identical to golden; `mios-unit-gen --check` reports the path family in sync; drift-gate and `cargo test` green.
**Why:** These watchers trigger the bootc switch and permission repairs; while they stay hand-authored their watched paths are invisible to SSOT and a stale path silently disables the trigger with no gate to notice.
**Dep:** Migrate the 10 timer units

### AGY-1331 -- Migrate the 4 .mount units to generated [units.*.mount]  (WS-SYSTEMD | P2 | S)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- puts the `/var` persistence contract (NS-1) into SSOT and completes the passive-unit families before the Service migrations.
**What+How:** Port `var-home.mount`, `var-lib-containers.mount`, `var-lib-machines.mount` and `var-lib-nfs-rpc_pipefs.mount` into `[units.<name>.mount]` with `What`/`Where`/`Type`/`Options` from SSOT, keeping the exact `What=`/`Where=` values and mount options intact. Generate via `mios-unit-gen` with byte-parity vs golden, and respect Law 2 NO-MKDIR-IN-VAR -- mount units DECLARE the topology, they do not create directories.
**Where:** `usr/lib/systemd/system/*.mount, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** All 4 `.mount` units render byte-identical to golden; the generator handles the `.mount` type; drift-gate and `cargo test` green; `check_no_mkdir_in_var` still passes.
**Why:** The `/var` persistence topology is what survives a `bootc upgrade`, yet it lives in four hand-edited files the operator cannot inspect from the config surface -- and a wrong `Where=` costs the persisted data on the next boot.
**Dep:** Migrate the 5 .path units

### AGY-1332 -- Migrate the GPU-detect service family to one templated generator base  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- demonstrates the base+overrides DRY payoff on real services, collapsing ~7 near-duplicate files to one declaration.
**What+How:** The 7 GPU units (`mios-gpu-detect`, `mios-gpu-amd`, `mios-gpu-intel`, `mios-gpu-nvidia`, `mios-gpu-pv-detect`, `mios-gpu-status` services plus the `mios-gpu-nvidia.service.d` and `mios-cdi-detect` drop-ins) are near-identical `Type=oneshot` detectors ExecStart-ing `/usr/libexec/mios/mios-gpu-*`. Encode them as `[units.mios-gpu-*.service]` sharing a common base (`After=`, `ConditionVirtualization`, the sandbox baseline) with only ExecStart/backend differing per unit, and generate via `mios-unit-gen`. Fold the `mios-gpu-nvidia.service.d` drop-in into a `[units.*.service.dropins]` concept. Hold byte-parity vs golden.
**Where:** `usr/lib/systemd/system/mios-gpu-*.service, usr/lib/systemd/system/mios-gpu-nvidia.service.d/, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** All 7 GPU units and the nvidia drop-in render byte-identical to golden from shared SSOT; adding a hardening key to the base propagates to all seven; drift-gate and `cargo test` green.
**Why:** Seven hand-copied detectors drift apart on every edit -- a hardening or ordering fix applied to one vendor's unit and forgotten in the others is the exact failure mode, and multi-vendor GPU support depends on them behaving identically.
**Dep:** Bake deny-by-default systemd sandbox directives

### AGY-1333 -- Migrate the *-firstboot.service family with degrade-open and timer pairing enforced  (WS-SYSTEMD | P2 | L)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- encodes the Law-12 (BAKE-NOT-FETCH) firstboot contract once so a single hand-edit cannot brick boot across a fleet.
**What+How:** Port the firstboot family (`mios-ai-firstboot`, `mios-hermes-firstboot`, `mios-forge-firstboot`, `mios-adguard-firstboot`, `mios-models-firstboot`, `mios-bound-images-firstboot`, `mios-webtools-firstboot`, `mios-forgejo-runner-firstboot`, `hermes-worker-firstboot`, `mios-swarm-pack-firstboot`, `mios-wsl-firstboot`) into `[units.*.service]` carrying the canonical `Type=oneshot` + `RemainAfterExit` + `ConditionPathExists=!<sentinel>` + `EnvironmentFile=-/etc/mios/install.env` pattern, with each unit's retry `.timer` paired in SSOT (mirroring `mios-ai-firstboot.service`/`.timer`). The generator must EMIT the degrade-open contract that `check_firstboot_degrade_open` (`98-drift-checks.sh` around line 3073) enforces on the backend scripts. Hold byte-parity vs golden.
**Where:** `usr/lib/systemd/system/*firstboot*.service, usr/lib/systemd/system/mios-ai-firstboot.timer, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** All firstboot units render byte-identical to golden; generated units retain their `ConditionPathExists` sentinels and optional `EnvironmentFile`; `check_firstboot_degrade_open` and `check_dag_integrity` pass; drift-gate and `cargo test` green.
**Why:** These 11 units run on every first boot of every deployed host; one that fails closed on a fetch error takes the machine down, and the sentinel/timer/degrade-open shape is currently re-typed by hand in each file with only a backend-script gate watching.
**Dep:** Migrate the 10 timer units; Add systemd-analyze security ratchet

### AGY-1334 -- Migrate the AI-plane service units with ${MIOS_PORT_*} float  (WS-SYSTEMD | P2 | L)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- generates the highest-value runtime surface from SSOT and clears the densest cluster of hardcoded ports in the `.service` tree (Law 7 NO-HARDCODE).
**What+How:** Port the AI-plane services (`hermes-worker`, `mios-agent-pipe`, `mios-agents`, `mios-daemon`, `mios-mcp`, `mios-opencode-gateway`, `mios-hermes-browser`, `mios-hermes-browser-worker`, `mios-policy-arbiter`, `mios-finetune-serve`) into `[units.*.service]`. Several hardcode ports in `ExecStart`/`Environment` -- e.g. `hermes-worker.service` sets `API_SERVER_PORT=8643`, `PORT=8643`, `BROWSER_CDP_URL=:9223` -- so float each through `${MIOS_PORT_HERMES_WORKER}` and siblings from `[ports]`, preserving the `${VAR:-LIT}` fallback so a bare render stays valid. Keep the genuine per-unit operator exceptions (`ReadWritePaths`, `NoNewPrivileges=false`) intact. Hold byte-parity vs golden in placeholder form.
**Where:** `usr/lib/systemd/system/hermes-worker.service, usr/lib/systemd/system/mios-agent-pipe.service, usr/lib/systemd/system/mios-daemon.service, usr/lib/systemd/system/mios-mcp.service, usr/lib/systemd/system/mios-opencode-gateway.service, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** AI-plane units render from SSOT; every `ExecStart`/`Environment` port carries a `${MIOS_PORT_*}` placeholder resolving to the `[ports]` value; no bare AI-plane port literal remains in the `.service` tree; drift-gate and `cargo test` green.
**Why:** Repointing the canonical `:8642`/`:8643` front door in mios.toml today leaves these units on the old literals, so the ONE OpenAI `/v1` contract (NS-2) silently breaks with services listening where nothing connects.
**Dep:** Migrate the GPU-detect service family; Collapse the three parallel firewall port lists

### AGY-1335 -- Generate the pgvector/backup/embed-backfill unit family from `[units.*]` with the DB port floated  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- no unit in the DB plane is hand-maintained and none ships unconfined.
**What+How:** Declare `mios-pgvector-backup.service`/`.timer`, `mios-embed-backfill.service`/`.timer`, `mios-account-sync.service` and `mios-userdb-render.service` as `[units.*]` blocks in `usr/share/mios/mios.toml` and render them through `tools/native/mios-unit-gen`. Each pairs its service with its timer in one SSOT block, keeps `EnvironmentFile=/etc/mios/install.env`, and floats the loopback-in-pod pgvector port (`[services.pgvector]` uid 826, `[ports].pgvector=8432`) as `${MIOS_PORT_PGVECTOR}` instead of the literal. Apply the sandbox baseline these units currently lack — `ProtectSystem=strict` plus a tight `ReadWritePaths=/var/lib/mios`. Prove byte-parity against the golden snapshots before flipping the committed files to generated.
**Where:** `usr/lib/systemd/system/mios-pgvector-backup.service, usr/lib/systemd/system/mios-pgvector-backup.timer, usr/lib/systemd/system/mios-embed-backfill.service, usr/lib/systemd/system/mios-account-sync.service, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** The DB-adjacent units render byte-identical to golden from SSOT with the pg port floated, backup service+timer are paired in one `[units.*]` block, and `cargo test` plus `just drift-gate` are green.
**Why:** These units front the sole agent datastore yet carry a hardcoded 8432 and zero confinement — a port move breaks backups silently, and any compromise of a backup oneshot runs with full filesystem write.
**Dep:** Migrate the AI-plane service units

### AGY-1336 -- Generate the web-plane host-side helper services (cockpit-link, ttyd, dashboard-issue) from SSOT  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- the last hardcoded-port host services become a projection, with a clean host-unit vs container-quadlet split.
**What+How:** Port the `.service` helpers under `usr/lib/systemd/system` that sit beside (not inside) the generated quadlets — `mios-cockpit-link.service`, `mios-ttyd-bash/powershell/expose.service`, the `mios-webtools-firstboot.service` adjuncts and `mios-dashboard-issue.service` — into `[units.*.service]`, floating `cockpit_link`, `ttyd_bash=8681` and `ttyd_powershell=8682` from `[ports]` as `${MIOS_PORT_*}`. Apply the sandbox baseline and assert byte-parity vs golden. Explicitly leave the `.container` quadlets alone: those remain `tools/generate-pod-quadlets.py`'s domain, so the two generators must not both claim a file.
**Where:** `usr/lib/systemd/system/mios-cockpit-link.service, usr/lib/systemd/system/mios-ttyd-*.service, usr/lib/systemd/system/mios-dashboard-issue.service, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** Web-plane host services render from SSOT with ports floated, no ttyd/cockpit-link port literal remains in `usr/lib/systemd/system`, no file is emitted by both generators, and `cargo test` + drift-gate are green.
**Why:** These helpers are where the port-float sweep currently stops — hardcoded 8681/8682/8091 in units means changing a port in `mios.toml` produces a half-moved service, and the generator ownership boundary stays undefined.
**Dep:** Migrate DB/pgvector-adjacent units

### AGY-1337 -- Model the 46 drop-in `.d/*.conf` override fragments as `[units.*.dropins]` and generate them  (WS-SYSTEMD | P2 | L)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- resource tuning becomes a one-line operator change instead of an edit across 46 override directories.
**What+How:** Enumerate the 46 `*.service.d` / `*.socket.d` / `*.target.d` directories (e.g. `mios-daemon.service.d/20-cpu-limit.conf` pinning `CPUQuota=800%`/`CPUWeight=20`/`Nice=10`, `mios-gpu-nvidia.service.d`, `greenboot-healthcheck.service.d`) and declare each as `[units.<base>.dropins.<fragment>]`, with resource limits promoted to a first-class `[units.*.resources]` sub-table (`CPUQuota`/`CPUWeight`/`Nice`/`MemoryMax`). Teach `mios-unit-gen` to emit the `.d/*.conf` files deterministically. Distinguish MiOS-owned drop-ins (regenerate fully) from drop-ins that override an upstream vendor unit (keep the file, but still declare it in SSOT so it is visible).
**Where:** `usr/lib/systemd/system/*.d/, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** All 46 fragments render byte-identical to golden from `[units.*.dropins]`, resource limits are SSOT-typed rather than free text, regenerating after a single SSOT edit changes only the intended fragment, and `cargo test` + drift-gate are green.
**Why:** Drop-ins are the densest hand-maintenance tax in the unit surface — tuning the daemon's 800% CPU burst today means finding and editing one of 46 scattered `.conf` files with no schema, no validation and no gate.
**Dep:** Migrate web-plane service units

### AGY-1338 -- Generate the oneshot infra tail (perms/init/WSL/flatpak families) with shared unit bases  (WS-SYSTEMD | P2 | L)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- the whole `usr/lib/systemd/system` surface becomes a projection, the WS-SYSTEMD end-state.
**What+How:** Port the long tail of `Type=oneshot` units into `[units.*.service]`: `mios-libexec-perms`, `mios-additionalimagestores-perms`, `mios-git-root-init`, `mios-selinux-init`, `mios-role`, `mios-verify(-root)`, `mios-boot-diag`, `mios-cpu-isolate`, `mios-kvmfr-load`, `mios-sriov-init`, the `mios-wsl-*` family (early / init / runtime-dir / interop-priority / flatpak-export-sync / wslg-permissions-fix) and `mios-flatpak-init/install`. Group them by shared shape (perms fixers, WSL bring-up) behind a common base so duplicated `After=`/`Condition=` boilerplate collapses. Preserve every `ExecStart` path into `/usr/libexec/mios/*` verbatim (59 distinct backends are referenced tree-wide) and apply the sandbox baseline wherever a oneshot tolerates it.
**Where:** `usr/lib/systemd/system/mios-wsl-*.service, usr/lib/systemd/system/mios-*-perms.service, usr/lib/systemd/system/mios-*-init.service, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/`
**Done When:** The oneshot tail renders byte-identical to golden, shared bases visibly collapse the repeated `After=`/`Condition=` blocks, every referenced `/usr/libexec/mios` backend still resolves on the built image, and `cargo test` + drift-gate are green.
**Why:** This tail is the bulk of the 84 services; while it stays hand-authored the campaign cannot claim the unit surface is SSOT-derived, and each new perms/WSL unit is another copy-pasted file nobody validates.
**Dep:** Fold the 46 drop-in .d fragments

### AGY-1339 -- Gate privileged/root service generation behind a justified `[security.privileged_units]` roster  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- elevation in generated units is explicit, justified and auditable rather than incidental.
**What+How:** Port the units that legitimately need elevation — `k3s.service`, `mios-k3s-init.service`, `mios-libvirtd-setup.service`, `mios-ha-bootstrap.service`, `mios-mok-enroll.service`, `mios-luks-enroll.service`, the deduped ceph bootstrap and `mios-freeipa-enroll.service` — into `[units.*.service]`, and cross-list each in a new `[security.privileged_units]` roster (parallel to `[security.privileged_quadlets].root`'s 13 entries) with a written justification. Make `mios-unit-gen` REFUSE to emit an unconfined/root `.service` that is absent from the roster, mirroring `check_quadlet_privilege`'s Law 6 logic for `.service` files, and back it with a new (or extended `check_unit_security`) drift-check.
**Where:** `usr/lib/systemd/system/k3s.service, usr/lib/systemd/system/mios-mok-enroll.service, usr/lib/systemd/system/mios-luks-enroll.service, usr/share/mios/mios.toml, tools/native/mios-unit-gen/src/, automation/98-drift-checks.sh`
**Done When:** Privileged units render from SSOT and appear on `[security.privileged_units]` with justifications; adding a root service that is not on the roster makes the generator exit non-zero; the roster check is registered in the gate index; drift-gate green.
**Why:** Root units are the highest-blast-radius surface in the image, and a generator with no privilege policy makes privilege creep a one-line accident that no reviewer or gate would notice.
**Dep:** Migrate remaining oneshot infra services; Add systemd-analyze security ratchet

### AGY-1340 -- Run mios-unit-gen in the build and enforce it with a `check_units_generated` drift-gate  (WS-SYSTEMD | P2 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- a hand-edited unit fails exactly like a drifted quadlet.
**What+How:** Add `automation/33b-generate-units.sh` (sibling to `33-generate-quadlets.sh`, which invokes `generate-pod-quadlets.py`) that runs the compiled `mios-unit-gen` to re-emit `usr/lib/systemd/system/*` from `[units.*]`. Add `check_units_generated()` to `automation/98-drift-checks.sh` invoking `mios-unit-gen --check` under a CLEAN environment (`env -i`, the same determinism trick `check_pod_quadlets` uses at ~line 896, so exported `MIOS_*` vars cannot bake values into the comparison) and failing on any drift. Register it in `main()` (~line 6298) and in `tools/generate-gate-index.py` and `tools/generate-pipeline-index.py`. Stage explicit paths only — never `git add -A` in this tree.
**Where:** `automation/33b-generate-units.sh, automation/98-drift-checks.sh, tools/generate-gate-index.py, tools/generate-pipeline-index.py`
**Done When:** The build phase regenerates units from SSOT; `mios-unit-gen --check` runs bare and deterministically and turns RED on a deliberate hand-edit; `check_units_generated` appears in the gate index and in `main()`; the negatives-before-main-gate CI ordering still holds; drift-gate green.
**Why:** A generator with no gate is decorative — without this, the first hand-edit to a generated unit is silently overwritten on the next build or silently persists, and 'derive, don't duplicate' stays an unenforced convention.
**Dep:** Migrate the 10 timer units

### AGY-1341 -- Extend the bare-port-literal check to systemd units (ListenStream / ExecStart / Environment)  (WS-SYSTEMD | P1 | S)
**Goal:** E-12 ZERO-HARDCODES -- systemd stops being the unscanned blind spot where floated ports regress to constants.
**What+How:** `check_no_bare_port_literals` (`automation/98-drift-checks.sh` ~line 1622) guards the config surfaces but not units, so `mios-cockpit-link.socket`'s `ListenStream=0.0.0.0:8091` and the AI-plane `ExecStart=`/`Environment=` port literals pass today. Extend it (or add `check_unit_port_float`) to scan `usr/lib/systemd/system/*.{service,socket,timer,path}` for any bare `:PORT` that maps to a known `[ports]` value but is not written as `${MIOS_PORT_*}`, honoring the exempt patterns in `[security.nohc_allowlist]`. Keep it SOFT while the families migrate and ratchet it hard once they land.
**Where:** `automation/98-drift-checks.sh, usr/share/mios/mios.toml`
**Done When:** The check FAILS on a synthetic unit containing a bare mapped port and PASSES on the floated tree; allowlist exemptions are honored; it is registered in the gate index; drift-gate green after the socket and AI-plane migrations.
**Why:** Without a unit-scoped gate, every port floated by the migration tasks can be quietly re-hardcoded by the next contributor, and the `[ports]` SSOT goes back to being advisory for the systemd surface.
**Dep:** Float ListenStream from [ports]; Migrate the AI-plane service units

### AGY-1342 -- Prove the Rust unit renderer with a Python reference oracle and a proptest differential harness  (WS-SYSTEMD | P2 | M)
**Goal:** E-05 Test and CI governance -- the compiled generator is proven equivalent to the legacy renderer, not spot-checked.
**What+How:** Keep a thin Python reference renderer for `[units.*]` by extending `generate-pod-quadlets.py`'s `render_nested_quadlet` (it already renders arbitrary nested `Section.Key` tables) to target `usr/lib/systemd/system` output. Add a proptest suite in `mios-unit-gen` that generates random-but-valid `[units.*]` documents, renders each with BOTH the Rust generator and the Python reference (via subprocess), and asserts byte-identical output, letting proptest auto-shrink any divergence to a minimal failing document. Wire it into CI beside the golden tests.
**Where:** `tools/native/mios-unit-gen/tests/, tools/generate-pod-quadlets.py, automation/98-drift-checks.sh`
**Done When:** `cargo test -p mios-unit-gen` runs the differential suite green over N random unit documents; a deliberately introduced Rust/Python divergence is caught and shrunk to a minimal case; the suite runs in CI.
**Why:** Example-based golden tests miss exactly the edge cases that break a renderer port — quoting, list ordering, empty-value drop — and a divergence found after the strangler cut means shipping malformed units to a fleet.
**Dep:** Wire mios-unit-gen into the build

### AGY-1343 -- Add an oxidizr-style `[migration]` toggle for per-family Rust-vs-Python unit generation  (WS-SYSTEMD | P2 | S)
**Goal:** E-01 Compiled native tier -- the strangler-fig port carries a one-flip rollback so the bake never goes red waiting on a regressed family.
**What+How:** Add a `[migration]` block to `usr/share/mios/mios.toml` (`use_rust_unit_gen = true`, plus optional per-family keys under `unit_families.timers` / `.services` / …) and have `automation/33b-generate-units.sh` choose the compiled `mios-unit-gen` or fall back to the Python reference renderer per family. Ship it inert-by-default per MiOS convention (like `[accounts].db_backed`), flipping a family to Rust only after that family's drift-gate is green in CI. Mirror the SSOT change into `mios-bootstrap.git` per Law 15.
**Where:** `usr/share/mios/mios.toml, automation/33b-generate-units.sh, (mios-bootstrap.git mios.toml mirror)`
**Done When:** Setting `use_rust_unit_gen=false` regenerates units via the Python reference with byte-identical output; a per-family flip works independently; the default flips to Rust only for gate-green families; both repos' `mios.toml` agree.
**Why:** Without a rollback switch, any regression in a single unit family becomes an emergency revert of the whole compiled generator mid-campaign — the exact failure mode the oxidizr playbook exists to avoid.
**Dep:** Add a Python reference renderer + proptest differential parity harness

### AGY-1344 -- Register Law 17 UNITS-FROM-SSOT in `[laws]` with its enforcing checks wired  (WS-SYSTEMD | P2 | S)
**Goal:** E-07 The drift-gate as the enforcement plane -- the last un-generated unit surface gets a numbered, gated invariant instead of a convention.
**What+How:** Add Law 17 (slug `UNITS-FROM-SSOT` / `HARDENED-UNITS`) to the `[laws]` table in `usr/share/mios/mios.toml` (which currently ends at id=16 `ONE-TEMPLATE-PER-TYPE`), with `enforced_by = '98-drift-checks.sh:check_units_generated,check_unit_security'`, stating that every hand-maintainable systemd unit is generated from `[units.*]` and confined above the exposure threshold. Mirror the law text into `CLAUDE.md` and `AGENTS.md` and the `mios-bootstrap.git` copies (Law 15), and confirm `tools/generate-manual.py` picks it up from the `[laws]` SSOT rather than a second hand-written list.
**Where:** `usr/share/mios/mios.toml, CLAUDE.md, AGENTS.md, (mios-bootstrap.git mirrors)`
**Done When:** `[laws]` contains id=17 with the correct `enforced_by` mapping; the law renders in the generated manual; the cross-repo parity checks 22 and 27 are green; drift-gate green.
**Why:** A rule enforced only by two checks nobody has tied to a law is bypassable by anyone who deletes the checks; laws with a single numbering SSOT are how Laws 1-16 stopped regressing, and the unit surface has no such anchor today.
**Dep:** Wire mios-unit-gen into the build; Add systemd-analyze security ratchet

### AGY-1345 -- Add a greenboot required.d check that the SSOT-generated core units actually come up  (WS-SYSTEMD | P3 | S)
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- greenboot health coverage extends from the AI plane to the generated unit surface, so a bad publish rolls back.
**What+How:** `usr/lib/greenboot/check/required.d/40-mios-ai-plane.sh` already health-checks the AI plane; add a sibling `required.d` check asserting the SSOT-generated core units — the `[units.*]` entries with `WantedBy=multi-user.target` plus the `mios-*.target` roots — are `is-enabled`/active. Derive the required-unit list from `[units.*]` (grouped by `WantedBy`) rather than hardcoding names, and make it executable via the `automation/78-greenboot.sh` chmod pass.
**Where:** `usr/lib/greenboot/check/required.d/, usr/share/mios/mios.toml, automation/78-greenboot.sh`
**Done When:** The new required.d check asserts the generated core units are enabled/loadable with its list derived from SSOT; masking a core unit trips the check in a VM boot test; the check is wired and executable through `78-greenboot.sh`.
**Why:** A generator regression today rolls forward across the fleet — boot-counted rollback is the only mechanism that catches a broken generated unit after the image is already published.
**Dep:** Wire mios-unit-gen into the build

### AGY-1346 -- Complete the strangler cut: delete the bash unit-authoring path once every family is gate-green  (WS-SYSTEMD | P2 | S)
**Goal:** E-01 Compiled native tier -- the campaign actually reduces the codebase, leaving one compiled generator instead of scattered shell.
**What+How:** Once every unit family is drift-green through `mios-unit-gen` and the `[migration]` toggle defaults to Rust, reduce `automation/33b-generate-units.sh` (and any transitional shell that hand-emitted unit content) to a thin invocation of the compiled binary and delete the dead per-unit shell fragments. Do NOT delete `tools/generate-pod-quadlets.py` — it still owns `.container`/`.pod`/`.network`/`.volume`; only the NEW unit-authoring shell goes. Keep the Python reference renderer reachable behind the `[migration]` toggle as documented rollback. Land it as a real `git merge --no-ff` so the graph shows the merge.
**Where:** `automation/33b-generate-units.sh, automation/33-generate-quadlets.sh`
**Done When:** No shell hand-authors `usr/lib/systemd/system/*` content; the build path is `mios-unit-gen` only with the Python reference reachable via the toggle; `generate-pod-quadlets.py` is untouched; full drift-gate + golden + proptest green; the merge is visible in the git graph.
**Why:** A strangler port that never deletes the legacy slice leaves two authoring paths forever — double maintenance, and the next contributor edits the shell one because it is still there.
**Dep:** Add an oxidizr-style [migration] toggle; Migrate privileged/root service units; Migrate remaining oneshot infra services

### AGY-1347 -- Add cargo-deny / cargo-audit supply-chain gating for the native workspace  (WS-SYSTEMD | P3 | S)
**Goal:** E-05 Test and CI governance -- the new compiled tier ships with the vuln and license gating bash never had.
**What+How:** `mios-unit-gen` introduces a real dependency surface (serde, toml, miette, thiserror, proptest) under `tools/native`. Commit `tools/native/deny.toml` covering RustSec advisories, banned and duplicate crates, and an SPDX license allow-list that operationalizes the FOSS posture; keep `tools/native/Cargo.lock` committed as the reproducibility SSOT (SBOM-not-hardcode: resolved versions recorded, never hand-pinned in SSOT); and add a CI/drift-gate step running `cargo deny check` and `cargo audit` that respects the existing negatives-before-main-gate job ordering.
**Where:** `tools/native/deny.toml, tools/native/Cargo.lock, tools/native/Cargo.toml, .github/workflows/mios-ci.yml`
**Done When:** `cargo deny check` and `cargo audit` run in CI and fail the build on a banned or vulnerable crate; `Cargo.lock` is committed; the license allow-list is enforced; the existing CI job order still holds.
**Why:** Moving system tooling into Rust without dependency gating imports an unaudited transitive tree into the image that produces every systemd unit — the highest-trust build artifact MiOS has.
**Dep:** Scaffold the compiled mios-unit-gen crate

### AGY-1348 -- Document the `[units.*]` SSOT surface in the manual, ROADMAP and projection registry  (WS-SYSTEMD | P3 | S)
**Goal:** E-06 Test and documentation harness -- a new SSOT surface is discoverable cold, like `[ports]` and `[services]`.
**What+How:** Add a populated WS-SYSTEMD workstream section to `ROADMAP.md` (beside the barely-populated WS-LANG/WS-TEMPLATE/WS-DEBT) describing the unit-from-SSOT migration with its task ids, and teach `tools/generate-manual.py` to render the `[units.*]` schema plus the `mios-unit-gen` contract (`--check`, `--selftest`) into the generated manual. Add `[units.*]` and `mios-unit-gen` to the `[laws.projection_registry].surfaces` list so `check_projection_registry` knows the generator is drift-backed. Continue AGY/T ids from the current max.
**Where:** `ROADMAP.md, tools/generate-manual.py, usr/share/mios/mios.toml`
**Done When:** ROADMAP carries a populated WS-SYSTEMD entry with task ids; the generated manual documents the `[units.*]` schema and the generator CLI; `[laws.projection_registry].surfaces` lists the unit generator and its check; `check_projection_registry` green.
**Why:** An undocumented generated surface drifts straight back to hand-editing, because the next agent has no way to learn that units are a projection at all.
**Dep:** Add Law 17 UNITS-FROM-SSOT

### AGY-1349 -- Reconcile hardcoded `Environment=` service URLs in migrated units against `[ports]`/`[services.webtools]`  (WS-SYSTEMD | P3 | M)
**Goal:** E-12 ZERO-HARDCODES -- endpoints in units derive from SSOT instead of freezing stale literals into generated files.
**What+How:** Several units bake service URLs in `Environment=`: `hermes-worker.service` sets `SEARXNG_URL=http://localhost:8888`, `MIOS_CRAWL_SERVICE_URL=http://127.0.0.1:11235`, `FIRECRAWL_API_URL=http://127.0.0.1:3002`, `HERMES_BACKEND_BASE_URL=http://localhost:11441` and `BROWSER_CDP_URL=:9223` — several of which disagree with canonical `[ports]` values (searxng=8899, firecrawl=8302, crawl4ai=8235). Reconcile each against `[ports]`/`[services.webtools]`, express them as `${MIOS_*}` placeholders in the generated `[units.*.service.Environment]` with `${VAR:-LIT}` fallback, and add the missing `[ports]`/`[network]` keys where a literal has no SSOT home. Flag genuine internal-vs-published-port distinctions instead of blindly rewriting them.
**Where:** `usr/lib/systemd/system/hermes-worker.service, usr/lib/systemd/system/mios-hermes-browser*.service, usr/share/mios/mios.toml`
**Done When:** Every `Environment=` endpoint in the generated AI-plane units resolves from SSOT or carries a justified internal-port note; the searxng/firecrawl/crawl4ai disagreements are reconciled or documented; no unaccounted bare service URL remains; drift-gate green.
**Why:** A worker pointing at `:8888` when searxng canonically moved to `:8899` is live breakage today, and generating the units as-is would freeze those wrong values in as the SSOT-blessed answer.
**Dep:** Migrate the AI-plane service units; Extend check_no_bare_port_literals to systemd units

### AGY-1350 -- Bring the two `.image` quadlets under generate-pod-quadlets.py coverage  (WS-SYSTEMD | P3 | S)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- zero un-generated artifacts remain under `usr/share/containers/systemd`.
**What+How:** `usr/share/containers/systemd/` holds two `.image` Quadlets (`mios-llm-heavy.image`, `bootc-image-builder.image`) that `generate-pod-quadlets.py` does not emit — it currently handles container, pod, network and volume only. Extend its category loop (the `categories = [(containers,'container'),(networks,'network'),(volumes,'volume')]` list near line 358) to also render `images` from an `[images.*]` SSOT block through `render_nested_quadlet`, and add `--check` coverage for them. Keep this in the sibling generator's domain (not `mios-unit-gen`), so both generators stay cleanly separated and both stay drift-gated. Assert byte-parity vs the committed `.image` files.
**Where:** `tools/generate-pod-quadlets.py, usr/share/containers/systemd/mios-llm-heavy.image, usr/share/containers/systemd/bootc-image-builder.image, usr/share/mios/mios.toml`
**Done When:** Both `.image` quadlets render from `[images.*]` byte-identical to the committed files; `--check` covers them; `check_pod_quadlets` is green; no hand-maintained `.image` file remains.
**Why:** Two hand-maintained files in an otherwise-generated directory are the ones that drift — they are edited directly, skip the SSOT, and are invisible to the quadlet drift-check.
**Dep:** none

### AGY-1351 -- Generate the system and user `90-mios.preset` enablement rosters from `[units.*].enable`  (WS-SYSTEMD | P1 | L)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- the enablement SSOT stops being a 271-line hand-maintained list.
**What+How:** `usr/lib/systemd/system-preset/90-mios.preset` (271 lines, ~150 enable/disable decisions including role-gated defaults and WSL fallbacks) and `usr/lib/systemd/user-preset/90-mios.preset` have no generator at all. Extend `mios-unit-gen` to emit both from `[units.<name>].enable` (bool) and `.scope` (`system|user`), preserving the rationale comments as `[units.*].comment`. Strangler-fig: generate to a temp path first, add a `--check` mode asserting byte-parity with the committed preset, flip the committed file to generated once green, and keep the hand-written list as the golden fixture. Upstream units MiOS only toggles (sshd, podman.socket, libvirtd, cockpit.socket) stay declarable via an `[units.<name>].external=true` marker so the generator sets enablement without authoring a unit body.
**Where:** `usr/lib/systemd/system-preset/90-mios.preset, usr/lib/systemd/user-preset/90-mios.preset, src/mios-rs (or tools/native/mios-unit-gen), usr/share/mios/mios.toml`
**Done When:** `mios-unit-gen --emit-preset` produces byte-identical system and user preset files vs the committed golden; `cargo test` parity is green; `just drift-gate` is green; the preset is regenerated during the build and staged by explicit path.
**Why:** The preset's own comments (lines 212-225) record the `mios-ai-firstboot` incident where dropped enable lines left the AI plane silently DEAD on first boot — it is the most fragile surface in this domain and nothing generates or validates it.
**Dep:** Scaffold the compiled mios-unit-gen crate; Define the [units.*] typed SSOT schema

### AGY-1352 -- Add `check_preset_roster_complete`: no orphan enable lines, no undeclared MiOS unit  (WS-SYSTEMD | P1 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- the 'silently dead on first boot' class becomes a gate failure, not a field report.
**What+How:** Generalize the ad-hoc spot-check at `automation/98-drift-checks.sh:3369-3373` (which only asserts nut-server/nut-monitor have enable lines) into a full fitness function with two axes: (1) every enable/disable target in `90-mios.preset` resolves to a real unit under `usr/lib/systemd/system` or a known upstream unit, and (2) every MiOS-owned unit (`mios-*.service/.timer/.path/.target/.socket`) carries an explicit enable OR disable decision — under Fedora bootc, an undeclared unit is a silently disabled one. Implement it natively in `src/mios-rs/miosd/src/drift.rs` with a bash `check_preset_roster` wrapper registered in the CHECKS array (~line 4794) and the dispatch (~line 6298), printing the offending unit names to stderr like sibling checks.
**Where:** `automation/98-drift-checks.sh (CHECKS registry + dispatch), src/mios-rs/miosd/src/drift.rs, usr/lib/systemd/system-preset/90-mios.preset`
**Done When:** Deleting any `mios-*.service` preset line, or renaming a unit without updating the preset, turns the gate RED naming the exact unit; adding the missing decision returns it green; the check is registered in both the negatives suite and the main gate.
**Why:** The documented `mios-ai-firstboot` incident is precisely this orphan/undeclared-unit class; the existing dangling-ref task covers `Wants=`/`After=` inside units, leaving the enablement-roster-vs-unit-files axis completely ungated.
**Dep:** Generate 90-mios.preset enablement rosters from SSOT

### AGY-1353 -- Unify role-apply's runtime enablement with the generated `.target` units via `[units.*].roles`  (WS-SYSTEMD | P1 | L)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- one enablement authority, not a build-time preset and a runtime case statement that can disagree.
**What+How:** `usr/libexec/mios/role-apply` resolves a blade archetype (lines 86-117), runs `systemctl set-default <TARGET>` (line 184) and toggles role-gated units (the preset's 'Role-gated: default-off' block, lines 239-253: libvirtd, k3s, gdm, ceph@ …), reading capabilities from `[blade.archetypes]` (lines 122-126) through `mios_toml`. Add an `[units.<name>].roles = ["desktop","k3s-master"]` membership field so `mios-unit-gen` emits BOTH the `.target`'s `Wants=` and a generated `/usr/share/mios/role-units.json` role→unit map that `role-apply` consumes in place of its hand-coded case statement. Strangler-fig: generate the map, have `role-apply` prefer it with the case block as fallback, prove parity per archetype, then delete the case block.
**Where:** `usr/libexec/mios/role-apply, usr/libexec/mios/mios-gen-role-system, usr/share/mios/mios.toml ([blade.archetypes]), the generated .target units under usr/lib/systemd/system`
**Done When:** `role-apply` drives enablement from the SSOT-generated role map; a characterization test shows each archetype enables an identical unit set before and after; the `.target` `Wants=` and the role map both derive from one `[units.*].roles` source; drift-gate green.
**Why:** Otherwise the target-migration task ships `.target` files whose grouping silently diverges from what `role-apply` actually enables at boot — two authorities for the same fact, and the runtime one wins invisibly.
**Dep:** Migrate the 11 .target units to generated [units.*.target] grouping

### AGY-1354 -- Subsume `generate-pod-quadlets.py` into mios-unit-gen behind a differential-parity harness  (WS-SYSTEMD | P1 | XL)
**Goal:** E-01 Compiled native tier -- one compiled generator owns every systemd artifact instead of a Rust and a Python one running side by side.
**What+How:** `tools/generate-pod-quadlets.py` (504 lines) already renders ~30 files under `usr/share/containers/systemd`: `.container`/`.network`/`.volume` via `render_nested_quadlet` (line 242) from `[containers.*]`/`[networks.*]`/`[volumes.*]`, and `.pod` via `render_pod_quadlet` (line 146) from `[pods.*]`, with env and `${VAR:-default}` and sidecar-digest resolution in `resolve_env_vars` (line 44). First capture golden snapshots of the ENTIRE `usr/share/containers/systemd` tree (the existing golden task covers only the 161 `usr/lib/systemd/system` units). Then port `render_nested_quadlet`, `render_pod_quadlet` and `resolve_env_vars` to Rust and run a differential harness feeding both generators the same `mios.toml`, asserting byte-identical output per file — only then demote the Python generator to a thin `exec mios-unit-gen` shim and delete it once the Rust path is drift-green.
**Where:** `tools/generate-pod-quadlets.py, usr/share/containers/systemd/*.{container,network,volume,pod,image}, automation/33-generate-quadlets.sh, src/mios-rs (or tools/native/mios-unit-gen)`
**Done When:** `mios-unit-gen` emits every file under `usr/share/containers/systemd` byte-identical to `generate-pod-quadlets.py` across the full SSOT; the Python generator is a shim or deleted and `33-generate-quadlets.sh` calls the binary; `cargo test`, the differential harness and the drift-gate are green.
**Why:** Leaving the already-SSOT-generated container side un-converged ships TWO unit generators with two rendering semantics — and the quadlet tree the existing golden task omits has no parity net at all.
**Dep:** Scaffold the compiled mios-unit-gen crate; Capture golden-master snapshots of all 161 hand-maintained units

### AGY-1355 -- Preserve `Condition*`/`Assert*` substrate gating in the schema with a round-trip fidelity check  (WS-SYSTEMD | P1 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- generated units keep firing only on the platform they were written for.
**What+How:** The 161 hand-maintained units carry verified substrate gating: 60 `ConditionPathExists=`, 40 `ConditionVirtualization=`, 4 `ConditionPathIsDirectory=`, 2 `ConditionPathExistsGlob=`, 1 `ConditionSecurity=` (e.g. `ConditionVirtualization=wsl` on the WSL-early units, `ConditionVirtualization=microsoft` on `mios-hyperv-enhanced`). Add first-class `[units.<name>].conditions` / `.asserts` lists to the schema, make the Service/Container renderer emit them verbatim, and add `check_condition_fidelity` in `automation/98-drift-checks.sh` asserting every `Condition*`/`Assert*` line in the golden set round-trips into generated output with identical operator, negation and glob semantics.
**Where:** `usr/lib/systemd/system/mios-wsl-early.service, usr/lib/systemd/system/mios-hyperv-enhanced.service, the golden snapshots, mios-unit-gen renderer, automation/98-drift-checks.sh`
**Done When:** Every `Condition*`/`Assert*` directive in the 161 golden units appears identically in the generated units; deliberately dropping a `ConditionVirtualization` in a fixture turns the check red; drift-gate green.
**Why:** Losing a `ConditionVirtualization=wsl` or `=microsoft` gate makes a unit run on bare metal or QEMU where it bricks boot (226/NAMESPACE, per the preset's own `mios-wsl-early` warning) — a correctness axis the sandbox and port-float tasks do not touch.
**Dep:** Define the [units.*] typed SSOT schema; Capture golden-master snapshots of all 161 hand-maintained units

### AGY-1356 -- Generate the Windows/WSL quadlet variants from the same SSOT with a Law 15 cross-repo parity gate  (WS-SYSTEMD | P1 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- the cross-platform unit surface projects from one SSOT instead of a hand-copied Windows fork.
**What+How:** `usr/share/mios/windows/quadlets/{mios-llm-light.container, mios.network}` are hand-maintained duplicates of the Linux quadlets (the Windows `mios-llm-light.container` hardcodes `Image=ghcr.io/mostlygeek/llama-swap:cuda`, listen `0.0.0.0:11450` and a WSL `LD_LIBRARY_PATH`). Add a Windows target/profile to `mios-unit-gen` so the Windows-scoped quadlets project from the SAME `[containers.*]`/`[networks.*]` SSOT with a `[units.<name>].platform` overlay for WSL-specific paths such as `/usr/lib/wsl/lib`, and add a drift-check asserting the Windows and Linux renders agree on image, port and network and differ only by declared platform deltas. Per Law 15 the shared SSOT change lands in BOTH `mios.git` and `mios-bootstrap.git`, and the port stays a `${MIOS_PORT_*:-11450}` float with the literal as fallback.
**Where:** `usr/share/mios/windows/quadlets/mios-llm-light.container, usr/share/mios/windows/quadlets/mios.network, usr/share/containers/systemd/mios-llm-light.container, mios-unit-gen, usr/share/mios/mios.toml (mirror in mios-bootstrap.git)`
**Done When:** The Windows quadlets are generated from SSOT byte-identical to a golden capture; image/port/network parity vs the Linux quadlet is drift-gated; the SSOT change is present in both repos; `:latest`+SBOM intent is preserved with no hand-pinned digest.
**Why:** MiOS is explicitly cross-platform, yet the Windows quadlet tree is an ungenerated hand copy that silently diverges from the Linux SSOT — the exact divergence class this campaign exists to eliminate, and no other task touches it.
**Dep:** Subsume tools/generate-pod-quadlets.py into mios-unit-gen

### AGY-1357 -- Model templated `@` instance units and preserve systemd `%`-specifiers in the generator  (WS-SYSTEMD | P2 | L)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- parameterized units survive generation as templates, not as flattened instances.
**What+How:** Several units are systemd TEMPLATES: `usr/share/containers/systemd/mios-llm-worker@.container`, `usr/share/containers/systemd/users/mios-coderun-sandbox@.container`, plus drop-ins on `sshd-keygen@.service` and `user@.service`. Add `[units.<name>].template=true` and `.instances = [...]` to the schema, teach the renderer to emit the `@`-suffixed template file with `%i`/`%N`/`%p`/`%I` specifiers preserved VERBATIM (never resolved at generation time), and have the preset generator enable specific instances (e.g. `user@1000.service`, preset line 179). Golden-master each template file before porting, then assert the `%`-specifiers survive untouched.
**Where:** `usr/share/containers/systemd/mios-llm-worker@.container, usr/share/containers/systemd/users/mios-coderun-sandbox@.container, usr/lib/systemd/system/user@.service.d, usr/lib/systemd/system/sshd-keygen@.service.d, mios-unit-gen`
**Done When:** Generated `@`-template units are byte-identical to golden with every `%`-specifier intact; declared instances such as `user@1000.service` are enabled via the generated preset; `cargo test` + drift-gate green.
**Why:** A naive per-unit renderer either resolves `%i` at build time (producing one wrong frozen instance) or drops the template entirely; the GPU-detect-family task covers a static family, so nothing else guards specifier semantics.
**Dep:** Define the [units.*] typed SSOT schema; Subsume tools/generate-pod-quadlets.py into mios-unit-gen

### AGY-1358 -- Generate the user-scope quadlets and the user-preset (rootless user-manager scope)  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- the user manager scope stops being an entirely hand-maintained island.
**What+How:** MiOS ships USER-scope units distinct from system scope: `usr/share/containers/systemd/users/mios-coderun-sandbox@.container` (rootless, user systemd) and a user-preset enabling `mios-launcher.service`, `mios-computer-use-server.service` and the `mios-wsl-*` services under `WantedBy=default.target` / `graphical-session.target`. Add `[units.<name>].scope = "user"` so `mios-unit-gen` writes user-scope quadlets under `usr/share/containers/systemd/users/` and the user preset under `usr/lib/systemd/user-preset/`, applying the correct install target (`default.target`, never `multi-user.target`) and NO root/privileged sandbox posture. Golden-capture the user tree, port, then differential-check.
**Where:** `usr/share/containers/systemd/users/mios-coderun-sandbox@.container, usr/lib/systemd/user-preset/90-mios.preset, usr/lib/systemd/system/mios-launcher.service, usr/lib/systemd/system/mios-computer-use-server.service, mios-unit-gen`
**Done When:** User-scope quadlets and the user-preset generate byte-identical to golden; install targets are `default.target`/`graphical-session.target` rather than `multi-user.target`; drift-gate green.
**Why:** Every other migration task is system-scope, so the user manager — different install targets, no root, its own preset file — would be left hand-maintained and would silently receive system-scope defaults if generated carelessly.
**Dep:** Generate 90-mios.preset enablement rosters from SSOT

### AGY-1359 -- Retire the deploy-time `34-render-quadlets.sh` envsubst pass once values float at generation time  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- one render authority, so nothing re-mutates a generated unit after the build.
**What+How:** `automation/34-render-quadlets.sh` performs IN-PLACE deploy-time rendering (`_render_with_envsubst` / `_render_with_bash`) over every deployed `.container`/`.network`/`.volume`/`.pod`/`.image`/`.service` (the `find` at line 184), substituting `${MIOS_*}` from the runtime env — a SECOND render mechanism parallel to the build-time generator. Once `mios-unit-gen` floats values from `[ports]`/`[services.*]` at generation with `${VAR:-LIT}` fallbacks baked in, this pass is redundant for generated units. Strangler-fig: mark generated units as already-floated, shrink the phase to touch only remaining non-generated files, prove it is a no-op on generated units (`rendered_count==0` for them), then delete the phase and its multi-LoRA special case (line 165) once all quadlets are generated.
**Where:** `automation/34-render-quadlets.sh, automation/33-generate-quadlets.sh, usr/share/containers/systemd/, mios-unit-gen`
**Done When:** `34-render-quadlets.sh` reports zero rewrites across all generated units; the phase is removed or reduced to legacy-only with no runtime placeholder left unrendered; boot-time smoke and drift-gate green.
**Why:** Two authorities rendering the same placeholders means the deploy-time pass can silently rewrite a correctly generated unit; the demote-33 task retires the AUTHORING path but leaves this separate RENDER path live.
**Dep:** Float ListenStream from [ports] and migrate the socket unit(s); Demote 33-generate-quadlets orchestration and delete the bash unit-authoring path once gate-green

### AGY-1360 -- Gate every generated unit through `systemd-analyze verify` so structural breakage fails the build, not the boot  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- no generated unit ships that systemd itself cannot load, so mechanical generation is safe to trust.
**What+How:** Add a drift/build gate that runs `systemd-analyze verify` over every rendered `.service/.timer/.socket/.path/.target/.container`, catching unknown directives, malformed `Exec` lines, dangling referenced units and load errors before the image ships. This is the STRUCTURAL counterpart to the security ratchet (which only scores hardening). Implement the step natively in the `miosd` drift runner (`src/mios-rs/miosd/src/drift.rs`) with a bash `check_units_verify` wrapper registered in the `98-drift-checks.sh` CHECKS registry; SOFT-skip when `systemd-analyze` is absent (Windows dev host) using the same pattern as `check_resolver_twin_parity` (~line 4180) so it hard-gates only on Linux/CI.
**Where:** `automation/98-drift-checks.sh (new check + CHECKS registry), src/mios-rs/miosd/src/drift.rs, usr/lib/systemd/system, usr/share/containers/systemd`
**Done When:** A fixture unit with a bad directive or a dangling `Requires=` fails the gate and prints the `systemd-analyze verify` output; a clean generation passes; the check SOFT-skips off-Linux and hard-gates in CI/bake.
**Why:** Today a subtly-malformed generated directive surfaces only as a boot-time load failure on a deployed host — the most expensive place to find it — and generating 168 units mechanically multiplies that exposure.
**Dep:** Wire mios-unit-gen into the build and add check_units_generated drift-gate

### AGY-1361 -- Detect After/Requires/Wants ordering cycles inside mios-unit-gen before any unit is rendered  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- the generated ordering DAG is provably acyclic, so SSOT-driven units cannot deadlock boot.
**What+How:** Build the dependency graph inside `mios-unit-gen` with petgraph from the `[units.*]` `After/Requires/Wants/BindsTo` edges plus implied pod membership (`Pod=` implies `Wants=`/`After=` on the pod service), then run a topological sort / SCC check and FAIL generation on any cycle, printing the offending edge path. Also flag self-`Requires` and `Requires` without a matching `After` (a start-order race). Emit the resolved topo order as an artifact the ordering-migration tasks consume.
**Where:** `mios-unit-gen (petgraph), usr/share/mios/mios.toml [units.*], usr/share/containers/systemd/*.pod`
**Done When:** A fixture with a deliberately-introduced cycle fails generation with the cycle path printed; the real SSOT generates a clean topo order; `cargo test` covers the cycle and self-dep cases; drift-gate green.
**Why:** The existing tasks fix one known stale ref (`mios-ai.target Wants=`) and gate dangling refs, but nothing validates acyclicity — the failure mode that HANGS boot (systemd breaks a cycle arbitrarily) rather than merely 404ing a unit.
**Dep:** Define the [units.*] typed SSOT schema

### AGY-1362 -- Make the generator emit the EnvironmentFile that backs every floated `${MIOS_PORT_*}`, and gate the contract  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- floated SSOT values in units resolve at runtime by construction, closing the loop between the generator and the env producers.
**What+How:** Floated port/URL vars only expand if an `EnvironmentFile` supplies them; the survey shows units already rely on `EnvironmentFile=-/etc/mios/install.env` (14x) and `-/etc/mios/userenv.sh` (6x), populated by `system-sync-env.sh` / `mios-env-snapshot`. Teach the Service/Container renderer that whenever it floats `${MIOS_PORT_*}` / `${MIOS_*_URL}` into `ExecStart`/`Environment`/`ListenStream` it must ALSO emit the matching `EnvironmentFile=-/etc/mios/userenv.sh` line. Add a native drift-check verifying every floated `MIOS_*` referenced by a generated unit is (a) actually produced by `userenv.sh`/`install.env` and (b) backed by an `EnvironmentFile` in that unit; SOFT-skip off-Linux.
**Where:** `usr/libexec/mios/system-sync-env.sh, usr/libexec/mios/mios-env-snapshot, /etc/mios/userenv.sh (produced), generated units, mios-unit-gen renderer, automation/98-drift-checks.sh`
**Done When:** Every generated unit referencing a floated `MIOS_*` carries the `EnvironmentFile` that supplies it and that var is emitted by the resolver; a fixture floating an unbacked var fails the gate; drift-gate green.
**Why:** The port-float tasks all assume the var exists at runtime; an unbacked float expands to EMPTY, so the socket binds nothing or `ExecStart` runs a truncated command — a hard break that no current check catches.
**Dep:** Float ListenStream from [ports] and migrate the socket unit(s); Migrate the AI-plane service units with ${MIOS_PORT_*} float

### AGY-1363 -- Bind generated `User=`/`Group=` to the existing `[services.*]` uid/gid table so identity has exactly one source  (WS-SYSTEMD | P2 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- unit generation adds a projection, never a THIRD parallel declaration of service identity.
**What+How:** `mios.toml` already carries identity in `[services.<name>]` with user/uid/gid (`[services.forge]` user=mios-forge uid=816, `[services.pgvector]` uid=826, `[services.llamacpp]` uid=827, `[services.webtools]` uid=824), and that same table drives `usr/lib/sysusers.d/50-mios-services.conf`. Teach `mios-unit-gen` to resolve `User=`/`Group=`/`PrivateUsers` by looking up `[services.*]`/`[accounts]` through a `[units.<name>].service_account` reference instead of declaring identity in `[units.*]`. Add a drift-check that no generated unit hardcodes a `User=`/`Group=` disagreeing with `[services.*]`, and that every non-external MiOS service unit resolves an owner from that one table.
**Where:** `usr/share/mios/mios.toml ([services.*], lines 8054-8135; [accounts], line 92), usr/lib/sysusers.d/50-mios-services.conf, mios-unit-gen, automation/98-drift-checks.sh`
**Done When:** Generated units derive `User=`/`Group=` solely from `[services.*]`/`[accounts]`; a mismatch between a unit's owner and the `[services.*]` uid fails the gate; no unit re-declares a uid; drift-gate green.
**Why:** The campaign exists to kill parallel definitions (the operator flagged the triple-resolver smell); letting `[units.*]` carry its own identity would recreate exactly that divergence and desync units from `sysusers.d`, producing units that run as a uid the system never created.
**Dep:** Define the [units.*] typed SSOT schema; Migrate web-plane service units; Migrate DB/pgvector-adjacent units

### AGY-1364 -- Retire dead unit files together with their preset lines and SSOT blocks so the generator roster equals the live set  (WS-SYSTEMD | P3 | S)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- only units MiOS actually ships enter the `[units.*]` roster, so generation does not immortalize corpses.
**What+How:** Enumerate genuinely-dead units by cross-checking preset `disable`-forever entries against inbound `Wants=`/`Requires=` refs, then remove — in one change — the unit file, its preset line, its `[containers.*]`/`[units.*]` SSOT block, and any dangling `Wants=`. Known candidates: `mios-code-server.service` (marked RETIRED and `disable`d at preset lines 153-156 yet `[containers.mios-code-server.*]` still sits inert at mios.toml 10126-10165), `mios-llm-heavy-alt.container` (alt variant), and the `[services.*]` migration notes referencing retired datastore UID 821 / CPU-inference UID 823. Verify each is truly orphaned first — the flatten-consolidation memo lists "legacy" units that are load-bearing. Stage explicit paths only; never `git add -A` (AGY shares the tree).
**Where:** `usr/lib/systemd/system/ (retired *.service), usr/share/containers/systemd/mios-llm-heavy-alt.container, usr/lib/systemd/system-preset/90-mios.preset, usr/share/mios/mios.toml ([containers.mios-code-server.*] 10126-10165)`
**Done When:** Each removed unit has no remaining unit file, preset line, SSOT block or inbound `Wants=`/`Requires=`; the generator's `[units.*]` roster equals the live shipped set; drift-gate + `systemd-analyze verify` green; changes staged by explicit path.
**Why:** Migrating dead units into the generator would carve them permanently into SSOT and keep shipping inert `[containers.*]` blocks that mislead every future reader about what MiOS runs.
**Dep:** Capture golden-master snapshots of all 161 hand-maintained units

### AGY-1365 -- Gate that generated pods project faithfully to k3s so the cluster path cannot silently diverge  (WS-SYSTEMD | P3 | M)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- the pods SSOT stays authoritative for BOTH downstreams (Quadlets and k3s), not just the one the generator touches.
**What+How:** `tools/generate-k3s-manifests.sh` projects the live pods under `usr/share/containers/systemd` (via `podman kube generate`) into k3s manifests under `usr/share/mios/k3s`, making the `.pod`/`.container` files the k8s workload SSOT too. Add a drift-check (or extend the existing pod-quadlets gate) asserting every `[pods.*]` member plus its floated image/port appears consistently in the generated k3s manifest set, and document that regenerating units REQUIRES re-running the k3s projection. SOFT-skip where podman is unavailable (off-Linux), matching the existing soft-gate pattern.
**Where:** `tools/generate-k3s-manifests.sh, usr/share/mios/k3s, usr/share/containers/systemd/*.pod, automation/98-drift-checks.sh, mios-unit-gen`
**Done When:** Changing a pod's membership or port in SSOT without re-projecting k3s FAILS the gate; a consistent regeneration passes; the check SOFT-skips without podman; drift-gate green on Linux/CI.
**Why:** The generator work touches only the Quadlet side, so today a pod change lands in systemd while the k3s manifests keep the old membership/port — a silent third divergence surface, exactly what this campaign is meant to eliminate.
**Dep:** Subsume tools/generate-pod-quadlets.py into mios-unit-gen

### AGY-1366 -- Build the `mios-config` typed layered SSOT resolver crate (figment) as the compiled peer of `mios_toml.py`  (WS-DOTFILES | P1 | L)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- one compiled resolver every projected surface derives from, starting the collapse of the three parallel resolvers.
**What+How:** Create library crate `tools/native/mios-config` (added to the `tools/native/Cargo.toml` workspace alongside mios-drift-runner/mios-version-check/mios-wallpaperd) that deserializes the three-layer overlay into typed structs via figment Toml providers in the EXACT order used by `mios-sync-theme` lines 27-30 and `mios_toml.load_merged`: vendor `usr/share/mios/mios.toml` < host `/etc/mios/mios.toml` < user `~/.config/mios/mios.toml`. Mirror `mios_toml.py` semantics exactly — expose `load_merged()`, `colors() -> BTreeMap`, `section(path)` — and reuse `tools/native/mios-ssot-walk`'s `EXCLUDED_SECTIONS`. Errors are thiserror enums wrapped by miette so a bad `[colors]` hex points at the offending span (byte offsets, not char counts). Do NOT touch the Python resolver yet.
**Where:** `tools/native/mios-config/ (new), tools/native/Cargo.toml, tools/native/mios-ssot-walk/src/lib.rs, usr/lib/mios/mios_toml.py, usr/share/mios/mios.toml`
**Done When:** `cargo build -p mios-config` green; a `cargo test -p mios-config` differential test asserts `colors()` and `section()` byte-match the JSON emitted by `python3 usr/lib/mios/mios_toml.py` for the committed mios.toml; the crate is a workspace member.
**Why:** Without a compiled resolver every Rust port would re-implement layering ad hoc, and the three divergent resolvers (`mios_toml.py`, the userenv.sh twins, globals.ps1) the operator flagged stay three forever.
**Dep:** none

### AGY-1367 -- Capture golden-master trycmd/insta fixtures of every dotfiles verb and gate output BEFORE any porting  (WS-DOTFILES | P1 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- a recorded parity oracle exists, so the projection engine can be rewritten without behavior regressions.
**What+How:** Add a characterization suite under `src/mios-rs/miosd/tests/` recording the CURRENT observable output of `usr/libexec/mios/mios-dotfiles-render {render,check,diff,capture}` and of the `check_dotfiles_projection` drift-gate against the committed mios.toml, across all 14 registry surfaces — plus insta `.snap` snapshots of every rendered artifact (`etc/btop/themes/mios.theme`, `etc/btop/btop.conf`, `usr/share/mios/oh-my-posh/mios.omp.json`, `usr/share/mios/quickshell/Theme.qml`, `usr/share/mios/fastfetch/config.jsonc`, `etc/skel/.gitconfig`, ...). Run inside a `MIOS_THEME_ROOT`-pinned temp tree so fixtures are host-independent.
**Where:** `src/mios-rs/miosd/tests/ (new), usr/libexec/mios/mios-dotfiles-render, usr/share/mios/theme/templates/, usr/share/mios/theme/fixtures/, automation/98-drift-checks.sh`
**Done When:** `cargo test` replays all captured fixtures green against the existing Python engine; `.snap` and `.trycmd` fixtures committed via explicit `git add` (never `git add -A`).
**Why:** The projection engine has no unit tests today, so any port is a blind rewrite — the only safe precondition is a recorded result to measure deviance against.
**Dep:** none

### AGY-1368 -- Scaffold the `miosd dotfiles` clap subcommand as the compiled facade that initially execs the Python engine  (WS-DOTFILES | P1 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- a stable verb facade exists so surfaces migrate one slice at a time behind an unchanged CLI.
**What+How:** Extend the `src/mios-rs/miosd/src/main.rs` Commands enum with `Dotfiles { verb, names, dry_run }` (verbs render/check/capture/apply/diff) and add `src/mios-rs/miosd/src/dotfiles/mod.rs`, mirroring the argv dispatch of `mios-dotfiles-render` main (lines 1212-1233). Depend on the `mios-config` crate and return `miette::Result` from each verb. Strangler seam: each verb initially just execs `usr/libexec/mios/mios-dotfiles-render <verb> <names>`, forwarding the child's stdout/stderr/exit BYTE-FOR-BYTE. Add the serde/miette/insta dependency set to the miosd crate.
**Where:** `src/mios-rs/miosd/src/main.rs, src/mios-rs/miosd/src/dotfiles/mod.rs (new), src/mios-rs/miosd/Cargo.toml, usr/libexec/mios/mios-dotfiles-render`
**Done When:** `miosd dotfiles check|render|diff` produce byte-identical stdout/stderr/exit vs the Python engine (the golden-master trycmd fixtures replay green); `cargo build -p miosd` green.
**Why:** Without a stable facade every ported slice would change the caller contract, forcing drift-checks, wrappers and the Justfile to be rewired repeatedly instead of once.
**Dep:** Build the mios-config typed layered SSOT resolver crate (figment) as the compiled peer of mios_toml.py

### AGY-1369 -- Port the template derive + `@MIOS:token@` substitution engine to Rust behind the miosd facade  (WS-DOTFILES | P2 | L)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the shared token primitive every surface depends on is compiled and golden-tested first.
**What+How:** Reimplement `_derive_template` (684), `_render_text`/`sub` (125-135), `_resolve_arbitrary_token`/`_walk_path` (96-122), `_token_map` (85-93) and the `_SENTINEL`/`_HEX` regexes from `mios-dotfiles-render` into `src/mios-rs/miosd/src/dotfiles/template.rs`, consuming `mios-config` `colors()` and preserving `_fmt_conf` formatting (bool -> True/False, ints bare, strings verbatim). Keep the KeyError-on-unknown-token behavior as a thiserror variant surfaced through miette with the offending token's span. Gate parity against the golden-master insta snapshots for the six pure-template color surfaces (btop, oh-my-posh, quickshell, fastfetch, app-shell, term-osc). Leave the Python `_derive_template` in place — Rust is selected only under the migration toggle.
**Where:** `src/mios-rs/miosd/src/dotfiles/template.rs (new), usr/libexec/mios/mios-dotfiles-render (85-135, 684), usr/share/mios/theme/templates/{btop-mios.theme,oh-my-posh.omp.json,quickshell-Theme.qml,fastfetch-config.jsonc,mios-app-shell.css,mios-colors.sh}.tmpl`
**Done When:** Rust `derive_template` output byte-matches the committed artifacts and the insta snapshots for all six color surfaces; `cargo test -p miosd` green.
**Why:** Every other surface builds on this primitive; porting anything else first would mean re-porting against a moving token engine, and it is the leaf with the strongest existing golden coverage.
**Dep:** Capture golden-master trycmd/insta fixtures of every dotfiles verb and drift-gate output BEFORE porting

### AGY-1370 -- Port the SETTINGS-surface projector (`section -> @MIOS:section_key@`) to Rust for btop-conf and gitconfig  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the engine projects typed non-color config, not just hex, so standalone settings generators can fold in later.
**What+How:** Reimplement `_settings` (62-70), `_resolved_for` (73-82) and the always-injected `[identity]` keys in `src/mios-rs/miosd/src/dotfiles/template.rs`, so a surface carrying `section=` projects its FLAT scalar keys as `@MIOS:<section>_<key>@` conf-formatted tokens (nested tables and arrays skipped). Cover the two committed settings surfaces: btop-conf (`section=btop` -> `etc/btop/btop.conf`, ~60 keys) and gitconfig (`section=gitconfig` plus injected identity username/fullname -> `etc/skel/.gitconfig`). Verify against the golden-master snapshots.
**Where:** `src/mios-rs/miosd/src/dotfiles/template.rs, usr/share/mios/theme/templates/{btop.conf,gitconfig}.tmpl, etc/btop/btop.conf, etc/skel/.gitconfig, usr/share/mios/mios.toml`
**Done When:** Rust render of btop-conf and gitconfig byte-matches the committed artifacts and the golden-master snapshots; `cargo test -p miosd` green.
**Why:** The cockpit/ipa/chrony consolidation tasks all reuse the section-scalar path; without it proven in Rust, those parallel Python projectors cannot be demoted and keep multiplying.
**Dep:** Port the template derive + @MIOS:token@ substitution to Rust behind the miosd facade

### AGY-1371 -- Deserialize `[dotfiles.registry.*]` into typed Surface structs (serde), replacing `_load_surfaces`  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the operator-editable surface map becomes a typed schema that fails loudly instead of silently mis-shaping.
**What+How:** Port `_load_surfaces` (through line 932), the `_Surface`/`_Fixture` shapes (44-45) and the `_KINDS`/`_MERGE_KINDS` validation (854-860) into `src/mios-rs/miosd/src/dotfiles/registry.rs` as serde-deserialized structs off `mios-config` `section("dotfiles.registry")`. Preserve every guard EXACTLY: unknown-kind -> exit 3, merge-kind-without-fixture -> exit 3, empty/absent-registry -> exit 3, plus the `_committed_path` (940-942) and `_entry` (945-951) helpers. The struct IS the schema — a malformed surface fails via miette naming the surface.
**Where:** `src/mios-rs/miosd/src/dotfiles/registry.rs (new), usr/libexec/mios/mios-dotfiles-render (44-45, 854-951), usr/share/mios/mios.toml ([dotfiles.registry.*] lines 11081-11252)`
**Done When:** Rust loads all 14 committed surfaces with identical (name,template,target,section,kind,fixture,policy) tuples; the three exit-3 guards reproduced as trycmd negative fixtures; `cargo test -p miosd` green.
**Why:** The registry is the ADR-0009/ADR-0010 operator surface; untyped dict loading admits a class of silent shape bugs where a mistyped `kind` or missing fixture degrades quietly instead of refusing, and every per-surface migration below needs this schema anchor.
**Dep:** Build the mios-config typed layered SSOT resolver crate (figment) as the compiled peer of mios_toml.py

### AGY-1372 -- Port the `check` verb (drift compare + orphan-template completeness floor) to Rust with byte-exact diagnostics  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- drift-check 25 can be rewired to Rust without changing one character of operator-visible failure text.
**What+How:** Reimplement `cmd_check` (974-1023) in `src/mios-rs/miosd/src/dotfiles/mod.rs`: iterate registered surfaces, derive committed bytes via each kind's `derive_fn`, byte-compare against the on-disk artifact, and emit the IDENTICAL `name: target drifted from SSOT projection (line N: committed=... projected=...)` message plus the full-gate ORPHAN TEMPLATE sweep (982-994) that fails when any `*.tmpl` is unclaimed. Match exact stderr wording and exit codes (0/1) via trycmd snapshots on both a clean tree and a seeded-drift tree.
**Where:** `src/mios-rs/miosd/src/dotfiles/mod.rs, usr/libexec/mios/mios-dotfiles-render (974-1023), automation/98-drift-checks.sh (check_dotfiles_projection line 1732)`
**Done When:** `miosd dotfiles check` output+exit byte-match the Python check on a clean tree AND a seeded-drift tree (trycmd); `cargo test -p miosd` green.
**Why:** `check` is the verb drift-check 25 shells out to; any wording or exit-code drift in the port turns a green gate red (or worse, a red gate green) and breaks the enforcement plane the whole campaign leans on.
**Dep:** Deserialize [dotfiles.registry.*] into typed Surface structs (serde) replacing _load_surfaces

### AGY-1373 -- Port the `render` and `capture` verbs, including the lossy-round-trip refusal, to Rust  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the whole template kind (six color + two settings surfaces) can run Rust-first behind the toggle.
**What+How:** Reimplement `cmd_render` (954-971) and `cmd_capture` (1026-1082) in the miosd dotfiles module. `render` writes each surface's derived bytes to its committed target with `newline=''` fidelity (NO CRLF translation). `capture` is template-only: it replaces hex with `@MIOS:token@` via `_token_map` and REFUSES a lossy round-trip (a re-render must reproduce the artifact byte-for-byte). Preserve the capture guards `settings surface is hand-authored -- skipping` and `kind!=template -- skipping`. Verify render against the golden-master snapshots and capture round-trips on all six color templates.
**Where:** `src/mios-rs/miosd/src/dotfiles/mod.rs, usr/libexec/mios/mios-dotfiles-render (954-1082)`
**Done When:** `miosd dotfiles render` reproduces every committed artifact byte-for-byte; `capture` round-trip-verifies all six color surfaces; `cargo test -p miosd` green.
**Why:** Without render/capture the Rust path can only observe drift, never fix it, so the toggle stays permanently unflippable and the Python engine stays canonical; a CRLF or newline slip here would also mass-drift every committed artifact.
**Dep:** Port the `check` verb (drift compare + orphan-template completeness floor) to Rust

### AGY-1374 -- Port the json-merge kind (JSONC strip + array-merge-by-key) to Rust for vscode-colors and windows-terminal  (WS-DOTFILES | P2 | L)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- MiOS splices its owned block into foreign operator JSON without ever clobbering a key it does not own.
**What+How:** Reimplement `_jsonc_to_json` (153+, the string-aware `//` and `/* */` comment and trailing-comma strip that must never mangle an `https://` inside a value), `_derive_json_merge` (691) and the `_ARRAY_MERGE_KEYS` / `schemes->name` merge-by-key path (231, 289-303) into `src/mios-rs/miosd/src/dotfiles/merge.rs` using serde_json — preserving every foreign key byte-for-byte and REFUSING (exit 2) an unparseable base. Cover the two committed json-merge surfaces (vscode-colors, windows-terminal) against their `fixture.base`/`fixture.expected` pairs; the offline check merges the owned subtree onto `fixture.base` and diffs `fixture.expected`, NEVER reading a live `settings.json`.
**Where:** `src/mios-rs/miosd/src/dotfiles/merge.rs (new), usr/libexec/mios/mios-dotfiles-render (153-303, 691-716), usr/share/mios/theme/fixtures/{vscode-settings,windows-terminal-settings}.{base,expected}.json`
**Done When:** Rust json-merge derive byte-matches both committed `fixture.expected` files; the unparseable-base exit-2 refusal reproduced; `tests/test-theme-merge.py` negative cases pass against the Rust path; `cargo test -p miosd` green.
**Why:** json-merge is the highest-risk kind because it writes into files the operator also owns — a wholesale array replace would silently delete their other Windows Terminal schemes, and that bug class is unrecoverable once applied to a live HOME.
**Dep:** Deserialize [dotfiles.registry.*] into typed Surface structs (serde) replacing _load_surfaces

### AGY-1375 -- Port the ini-merge, ssh-merge and registry derive kinds to Rust (gitconfig-live, ssh-config, windows-registry)  (WS-DOTFILES | P2 | L)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- every registry surface has a compiled `derive_fn`, closing the projection half of the strangler slice.
**What+How:** Reimplement `_derive_ini_merge` (717), `_derive_ssh_merge` (662) and `_derive_registry` (733) in `src/mios-rs/miosd/src/dotfiles/merge.rs`, covering gitconfig-live (`policy=enforce` UPSERT of the ~9 owned keys with every foreign line byte-preserved), ssh-config (ssh-merge, `section=ssh`) and windows-registry (registry kind, `section=colors`). Each is offline-gated against its committed `fixture.base`/`fixture.expected` pair; preserve the enforce-vs-seed policy branch and the per-kind fixture-required guard from `_load_surfaces` (912-921).
**Where:** `src/mios-rs/miosd/src/dotfiles/merge.rs, usr/libexec/mios/mios-dotfiles-render (662-760), usr/share/mios/theme/fixtures/{gitconfig,ssh-config,windows-registry}.*`
**Done When:** Rust derive byte-matches all three committed `fixture.expected` files; `tests/test-theme-merge.py` negative cases pass against the Rust path; `cargo test -p miosd` green.
**Why:** Until every kind has a compiled derive, the toggle can only route a subset of surfaces, so BOTH engines must stay maintained in parallel — the exact duplication this workstream exists to end.
**Dep:** Port the json-merge kind (JSONC strip + array-merge-by-key) to Rust for vscode-colors + windows-terminal

### AGY-1376 -- Port the live-HOME apply/diff path (HOME-scope + symlink refusal, timestamped RAW backup) to Rust  (WS-DOTFILES | P2 | L)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the single write path into the operator's real HOME is compiled, panic-free and refuses every unsafe target.
**What+How:** Reimplement `cmd_apply`/`cmd_diff` (1154+), `_apply_target`/`_resolve_target`/`_unsafe_reason`/`_utc_stamp` (1102-1151), the `_TargetError` refusals and the `_apply_live_*` variants (701-760, 856-860) into `src/mios-rs/miosd/src/dotfiles/apply.rs`: per-platform `[.apply.target]` selection, `~`-and-var expansion with unexpanded-var refusal, symlink/outside-HOME/cross-drive refusal (NEVER follow a symlink out of HOME), a timestamped RAW backup (copy preserving mode+mtime) before any overwrite, and merge-onto-live for json/ini/ssh. Keep the exact `[mios-dotfiles-render] apply <surface>: ...wrote/WOULD-write/REFUSED` wording that the `mios-dotfiles` wrapper's `_LINE` regex (line 51) parses.
**Where:** `src/mios-rs/miosd/src/dotfiles/apply.rs (new), usr/libexec/mios/mios-dotfiles-render (1085-1211), usr/libexec/mios/mios-dotfiles (line 51)`
**Done When:** `miosd dotfiles diff|apply` output, backups and refusals byte-match the Python engine on a synthetic tempdir HOME (trycmd); `cargo test -p miosd` green.
**Why:** `apply` is the only code path that mutates the operator's real files; a missed symlink or unquoted-path case here destroys user data irrecoverably, and a wording change silently breaks the wrapper that parses its output.
**Dep:** Port the ini-merge, ssh-merge, and registry derive kinds to Rust (gitconfig-live, ssh-config, windows-registry)

### AGY-1377 -- Add the `[migration]` SSOT toggle so the operator flips Rust vs bash projector per surface with one value  (WS-DOTFILES | P1 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- rollback is a first-class deliverable: one SSOT flip, not a build revert.
**What+How:** Add a `[migration]` table to `usr/share/mios/mios.toml` (`use_rust_projector = false` plus optional per-surface `rust_surfaces = [...]`), type it into the `mios-config` resolver AND `mios_toml.py`, and have `usr/libexec/mios/mios-dotfiles-render` consult it: true -> exec `miosd dotfiles`, false -> run the existing Python path. Default OFF (bash canonical) until parity is drift-gated. Use `${MIOS_PROJECTOR:-miosd}` so a toolchain-less environment degrades open. Mirror the new key in BOTH repos (mios.git + mios-bootstrap.git) per Law 15, staging explicit paths.
**Where:** `usr/share/mios/mios.toml (new [migration]), usr/libexec/mios/mios-dotfiles-render, usr/lib/mios/mios_toml.py, mios-bootstrap.git mirror of usr/share/mios/mios.toml`
**Done When:** With `use_rust_projector=false` behavior is byte-unchanged (drift-gate green); flipping true routes every ported verb to `miosd` and the gate stays green; both repos carry the identical `[migration]` block.
**Why:** Without a toggle the cutover is an all-or-nothing commit: a regression on the operator's live HOME could only be undone by reverting and rebuilding the image, and no surface-by-surface rollout is possible.
**Dep:** Scaffold `miosd dotfiles` projection subcommand (clap) as the compiled facade over the Python engine

### AGY-1378 -- Demote `mios-dotfiles-render` to a thin Rust-first shim and prove byte-parity with proptest differential testing  (WS-DOTFILES | P1 | L)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- equivalence is PROVEN over generated inputs, not spot-checked, before bash stops being canonical.
**What+How:** Once every verb is ported, reduce `usr/libexec/mios/mios-dotfiles-render` to a shim that (under the `[migration]` toggle) execs `miosd dotfiles "$@"` with identical argv, keeping the Python engine importable as the `${MIOS_PROJECTOR:-}` fallback. Add `src/mios-rs/miosd/tests/differential.rs`: a proptest that generates random-but-valid mios.toml `[colors]`/`[btop]`/`[gitconfig]`/`[identity]` documents, runs BOTH the Python engine (oracle) and `miosd`, and asserts every surface's rendered bytes are identical, auto-shrinking any divergence to a minimal failing mios.toml. This mechanizes the Global-Unification-Plan lossless-diff gate.
**Where:** `usr/libexec/mios/mios-dotfiles-render, src/mios-rs/miosd/tests/differential.rs (new), usr/lib/mios/mios_toml.py`
**Done When:** proptest differential (>=256 cases) green with zero divergence; the shim routes to `miosd` under the toggle and to Python when unset; `just drift-gate` green.
**Why:** Example-based fixtures only cover the inputs someone thought of; without a generative oracle the port ships with unknown edge-case divergences that surface as corrupted operator config long after cutover.
**Dep:** Port the live-HOME apply/diff path (HOME-scope + symlink refusal, RAW backup) to Rust

### AGY-1379 -- Rewire drift-check 25 to the Rust projector and add cargo fmt/clippy/test gates for the crate  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the migrated surface gains a static-analysis gate strictly stronger than the shellcheck it never had.
**What+How:** Point `check_dotfiles_projection` (`automation/98-drift-checks.sh` line 1732) at `${MIOS_PROJECTOR:-miosd} dotfiles check` with a fallback to `usr/libexec/mios/mios-dotfiles-render` so a toolchain-less environment degrades open, keeping the existing `_violation` message. Add Justfile drift-gate steps running `cargo fmt --check`, `cargo clippy -p miosd -- -D warnings` and `cargo test -p miosd`, placed AFTER `tests/drift-gate-negatives.sh` per the negatives-before-main-gate ordering. Deny the restriction lints `clippy::unwrap_used`, `clippy::panic`, `clippy::todo` in the miosd crate.
**Where:** `automation/98-drift-checks.sh (1732-1749), Justfile (drift-gate 81-111), src/mios-rs/miosd/src/dotfiles/mod.rs`
**Done When:** Check 25 passes via `miosd`; `just drift-gate` runs fmt/clippy/test and FAILS on any warning or unwrap/panic lint; the Python fallback still passes when `miosd` is absent.
**Why:** A ported engine with no lint gate regresses the quality story — an `unwrap()` in the live-HOME write path would panic mid-apply, and today nothing in CI would stop that landing.
**Dep:** Demote mios-dotfiles-render to a thin Rust-first shim and prove byte-parity via proptest differential testing

### AGY-1380 -- Fold cockpit.conf into the dotfiles registry and demote `tools/generate-cockpit-conf.py` to a shim  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- one standalone parallel projector is removed and the settings path is proven to generalize off the theme engine.
**What+How:** Add `[dotfiles.registry.cockpit]` (kind=template, section=cockpit, template `usr/share/mios/theme/templates/cockpit.conf.tmpl`, target `etc/cockpit/cockpit.conf`) reproducing the current output of `tools/generate-cockpit-conf.py` — `allow_unencrypted`/`login_to`/`idle_timeout` from `[cockpit]` with the same true/false/str formatting and defaults. Verify `miosd dotfiles render cockpit` byte-matches the committed `etc/cockpit/cockpit.conf`, then reduce `generate-cockpit-conf.py` to a <=15-line shim calling the engine's render/check (so `tools/` callers keep working) and rewire `check_cockpit_projection` (98-drift-checks.sh line 5409) onto the engine. Mirror the registry addition in BOTH repos (Law 15), staging explicit paths.
**Where:** `usr/share/mios/mios.toml ([dotfiles.registry.cockpit]), usr/share/mios/theme/templates/cockpit.conf.tmpl (new), etc/cockpit/cockpit.conf, tools/generate-cockpit-conf.py, automation/98-drift-checks.sh (5409), mios-bootstrap.git mirror`
**Done When:** Render byte-matches committed `cockpit.conf`; `check_cockpit_projection` green via the engine; `generate-cockpit-conf.py` is a thin shim handing to `miosd dotfiles`; both repos mirrored.
**Why:** Every standalone generator is a second projector with its own resolver, defaults and formatting rules that can drift from SSOT independently — cockpit.conf is one such file living outside the one engine today.
**Dep:** Port the SETTINGS-surface projector (section -> @MIOS:section_key@) to Rust for btop-conf + gitconfig

### AGY-1381 -- Fold ipa-enroll.env into the dotfiles registry and demote `tools/generate-ipa-enroll-env.py`  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- identity config becomes operator-defined in mios.toml and drift-gated through the one engine.
**What+How:** Add `[dotfiles.registry.ipa-enroll]` projecting `etc/mios/ipa-enroll.env` from `[identity.ipa]`, reproducing `generate-ipa-enroll-env.py`'s `MIOS_IPA_* KEY="value"` env lines and defaults EXACTLY (enabled / `realm=${realm:-MIOS.INTERNAL}` / server / domain / enroll_principal / otp placeholder). Because the target is env-var lines rather than a color file, add a small env-lines template (`usr/share/mios/theme/templates/ipa-enroll.env.tmpl`) fed by the section projector; verify byte-match, then shim the generator to `miosd dotfiles render/check ipa-enroll` and rewire `check_ipa_enroll_projection` (98-drift-checks.sh line 5355). Mirror the registry block in BOTH repos per Law 15.
**Where:** `usr/share/mios/mios.toml, usr/share/mios/theme/templates/ipa-enroll.env.tmpl (new), etc/mios/ipa-enroll.env, tools/generate-ipa-enroll-env.py, automation/98-drift-checks.sh (5355), mios-bootstrap.git mirror`
**Done When:** Render byte-matches committed `etc/mios/ipa-enroll.env`; `check_ipa_enroll_projection` green via the engine; the generator is demoted to a shim; both repos mirrored.
**Why:** A one-off SSOT->env projector keeps its own default handling (the `MIOS.INTERNAL` realm fallback in particular) outside the resolver, so the operator's enrollment config can diverge from mios.toml with nothing but a bespoke check to notice.
**Dep:** Port the SETTINGS-surface projector (section -> @MIOS:section_key@) to Rust for btop-conf + gitconfig

### AGY-1382 -- Fold chrony.conf into the dotfiles registry and demote the render half of `automation/42-chrony-render.sh`  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the engine can replace a BUILD-PHASE renderer, including one that previously risked baking host-specific config.
**What+How:** Add `[dotfiles.registry.chrony]` projecting `etc/chrony.conf` from `[network.ntp].servers` — the repeated `server <s> iburst` list plus the fixed driftfile/makestep/maxslewrate/refclock/rtcsync/logdir tail — reproducing `42-chrony-render.sh`'s embedded-python output EXACTLY, including its deterministic build-host-independent `has_ptp=False` canonical render (lines 53-66; NEVER key off the build host having `/dev/ptp0`). Add a small list-repeat derive (or template-with-repeat) for the server loop, verify byte-match vs committed `etc/chrony.conf`, reduce `42-chrony-render.sh` to a build shim calling `miosd dotfiles render chrony`, and rewire `check_chrony_projection` (98-drift-checks.sh line 3315). Mirror the registry block in BOTH repos.
**Where:** `usr/share/mios/mios.toml ([network.ntp] + registry), etc/chrony.conf, automation/42-chrony-render.sh, automation/98-drift-checks.sh (3315), mios-bootstrap.git mirror`
**Done When:** Render byte-matches committed `chrony.conf`; `check_chrony_projection` green via the engine; `42-chrony-render.sh` is a thin shim; both repos mirrored.
**Why:** Today an NTP server list change means editing embedded python inside a build phase, and the build-host `/dev/ptp0` sensitivity is one refactor away from making the image non-reproducible per builder.
**Dep:** Port the `render` and `capture` verbs (hex->token round-trip self-verify) to Rust

### AGY-1383 -- Add a kargs-flatten kind and fold `usr/lib/kernel/cmdline` off `tools/generate-uki-cmdline.py`  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- the boot-integrity projection baked inside the signed UKI is produced by the one compiled, tested engine.
**What+How:** Implement a `kargs-flatten` derive kind in `src/mios-rs/miosd/src/dotfiles/` that concatenates the kargs arrays from every `usr/lib/bootc/kargs.d/*.toml` (sorted by filename; 20+ drop-ins verified present) into a single-line `usr/lib/kernel/cmdline`, reproducing `tools/generate-uki-cmdline.py` (lines 23-33) including its trailing-newline shape. Register `[dotfiles.registry.uki-cmdline]` (kind=kargs-flatten, a SOURCE DIR rather than a single template), verify byte-match vs the committed cmdline, shim the generator to the engine, and rewire `check_uki_cmdline_projection` (98-drift-checks.sh line 5367). Parity is boot-integrity-critical — this cmdline is baked INSIDE the signed UKI (`enforcing=1`, LUKS args). Mirror in BOTH repos.
**Where:** `src/mios-rs/miosd/src/dotfiles/ (new kargs-flatten kind), usr/share/mios/mios.toml, usr/lib/bootc/kargs.d/*.toml, usr/lib/kernel/cmdline, tools/generate-uki-cmdline.py, automation/98-drift-checks.sh (5367), mios-bootstrap.git mirror`
**Done When:** The engine's flatten byte-matches committed `usr/lib/kernel/cmdline`; `check_uki_cmdline_projection` green via the engine; `generate-uki-cmdline.py` demoted to a shim; both repos mirrored.
**Why:** A mis-ordered or dropped karg in this file silently weakens Secure Boot / SELinux enforcement inside a SIGNED artifact — the failure is invisible at build time and unfixable at runtime without a re-sign.
**Dep:** Deserialize [dotfiles.registry.*] into typed Surface structs (serde) replacing _load_surfaces

### AGY-1384 -- Add a mode-select kind for prepare-root.conf and demote the render half of `automation/77-composefs-verity.sh`  (WS-DOTFILES | P2 | L)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- projection and runtime side-effect are cleanly split, hardening the composefs/verity root-integrity control.
**What+How:** Implement a `mode-select` derive emitting `usr/lib/ostree/prepare-root.conf` from `[security].composefs_mode` (verity | yes | off): the two committed cat-heredoc bodies in `77-composefs-verity.sh` (lines 92-124) become mode-keyed templates and `off` = skip. Register the surface, verify byte-match vs the committed `prepare-root.conf` and the `check_composefs_projection` temp-render (98-drift-checks.sh line 5379), then reduce only the RENDER half of the script to `miosd dotfiles render prepare-root`. CRITICAL: leave the RUNTIME side-effects in bash — the `.orig` backup (81-86) and the `systemd-remount-fs.service` masking (129-133, the `ln -sf /dev/null`) — because the engine owns the projected FILE, not the live symlink/mask. Mirror the registry block in BOTH repos.
**Where:** `src/mios-rs/miosd/src/dotfiles/ (mode-select kind), usr/share/mios/mios.toml [security], usr/lib/ostree/prepare-root.conf, automation/77-composefs-verity.sh, automation/98-drift-checks.sh (5379), mios-bootstrap.git mirror`
**Done When:** Engine render byte-matches committed `prepare-root.conf` for both `verity` and `yes`; `check_composefs_projection` green via the engine; the remount-mask and `.orig` side-effects remain in bash; both repos mirrored.
**Why:** The root-integrity mode currently lives as two heredocs entangled with runtime mutations inside a build script, so changing `composefs_mode` in SSOT does not reliably change what ships — and no one can review the projection independently of the side-effects.
**Dep:** Deserialize [dotfiles.registry.*] into typed Surface structs (serde) replacing _load_surfaces

### AGY-1385 -- Split the chrony PTP drop-in: projector owns the CONTENT, the service keeps the /dev/ptp0 probe  (WS-DOTFILES | P3 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- moves one more standalone renderer behind the single `[dotfiles.registry.*]` engine without losing hardware-conditional behavior.
**What+How:** Port `usr/libexec/mios/mios-chrony-ptp-dropin` (the chrony.d drop-in generator) so the miosd dotfiles engine derives the drop-in CONTENT from the `[network.ntp]`/PTP keys in mios.toml, while the runtime `/dev/ptp0` presence probe stays in `mios-chrony-ptp.service` (degrade-open: no ptp0 -> no drop-in installed). Rewire `check_chrony_ptp_dropin` (automation/98-drift-checks.sh line 5421) so it compares the engine's render against the expected drop-in instead of syntax-checking the bash producer. The split is the point: engine = projection, service = hardware decision. Per Law 15, mirror any new mios.toml key into mios-bootstrap.git.
**Where:** `usr/libexec/mios/mios-chrony-ptp-dropin, usr/lib/systemd/system/mios-chrony-ptp.service, automation/98-drift-checks.sh (5421), src/mios-rs/miosd/src/dotfiles/, mios-bootstrap.git mirror if mios.toml changes`
**Done When:** `check_chrony_ptp_dropin` is green while comparing an engine render (not bash syntax); the drop-in content is byte-derived from SSOT; the ptp0 gate in `mios-chrony-ptp.service` is textually unchanged; both repos carry any added mios.toml key.
**Why:** Today a hardware probe and a config projection are welded into one bash script, so the drift-gate can only lint the script rather than verify the output -- and any future SSOT change to NTP settings silently bypasses the projector.
**Dep:** Fold chrony.conf into the dotfiles registry and demote automation/42-chrony-render.sh's render half

### AGY-1386 -- Convert the mios-sync-theme heredoc bridge (theme.json + mios-theme.css) into two registry surfaces  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- removes the last hand-written projection living inside the global theme-refresh script.
**What+How:** Replace the embedded-Python heredoc at `usr/libexec/mios/mios-sync-theme` lines 55-135 (which writes `theme.json` + `mios-theme.css` from `[colors]`/`[theme]`/`[theme.font]`) with two registry surfaces, `theme-bridge-json` and `theme-bridge-css`, backed by new templates `usr/share/mios/theme/templates/theme.json.tmpl` and `mios-theme.css.tmpl`. Promote the heredoc's computed values (acrylic, `opacity_pct=int(theme.opacity)`, `radius_px=10`, font family/size, the /100 opacity divide) into `[theme]`-derived tokens so nothing is computed in bash. Collapse the heredoc to `miosd dotfiles render theme-bridge-json theme-bridge-css`, keeping `OUT_DIR=${MIOS_THEME_OUT_DIR:-/etc/mios/theme}` as the target override. Register both surfaces with the check gate and mirror the registry block into both repos.
**Where:** `usr/libexec/mios/mios-sync-theme (55-135), usr/share/mios/theme/templates/theme.json.tmpl + mios-theme.css.tmpl (new), usr/share/mios/mios.toml, automation/98-drift-checks.sh, mios-bootstrap.git mirror`
**Done When:** the engine emits `theme.json` and `mios-theme.css` byte-identical to today's heredoc output; the heredoc is gone from mios-sync-theme; a drift surface gates both files; the registry block is identical in mios.git and mios-bootstrap.git.
**Why:** The CSS/QML-facing bridge that every desktop surface reads is currently generated by hex-interpolating Python inside a shell script -- invisible to check 25, and a second place a color can be wrong.
**Dep:** Port the `render` and `capture` verbs (hex->token round-trip self-verify) to Rust

### AGY-1387 -- Reimplement the mios-sync-theme global refresh as a shell-free `miosd dotfiles sync`  (WS-DOTFILES | P3 | L)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- makes the one global theme refresh a single compiled cross-platform entrypoint.
**What+How:** Port the orchestration in `usr/libexec/mios/mios-sync-theme` lines 137-186 (render all surfaces -> optional `--apply-home` -> icon regen) into a `miosd dotfiles sync [--apply-home] [--check]` subcommand, so the refresh stops being bash gluing Python children. Demote `mios-sync-theme` to a thin shim that invokes miosd while preserving the `mios-sync-theme.service` contract (the no-arg invocation must keep working). Icon regeneration stays a delegated call to `mios-generate-icons` -- out of scope here. Add a `[migration]`-toggled fallback to the existing bash path so a missing miosd degrades open rather than failing the boot oneshot.
**Where:** `usr/libexec/mios/mios-sync-theme, usr/lib/systemd/system/mios-sync-theme.service, src/mios-rs/miosd/src/dotfiles/mod.rs`
**Done When:** `miosd dotfiles sync` refreshes every surface plus the bridge byte-identically to the bash script; `mios-sync-theme.service` still starts and exits clean; the shim degrades open with miosd absent; `cargo test -p miosd` green.
**Why:** The single most-invoked theme path is bash calling Python calling bash, which cannot run on the Windows half and re-diverges every time a surface is added.
**Dep:** Fold mios-sync-theme's /etc/mios/theme bridge (theme.json + mios-theme.css) into registry surfaces

### AGY-1388 -- Add `check_projector_coverage`: prove no un-migrated standalone projector remains  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- makes "every projection routes through the one engine" a machine-checked invariant instead of a campaign intention.
**What+How:** Add `check_projector_coverage` to `automation/98-drift-checks.sh` (register it in the check list near line 4820) asserting two things: (a) every committed projected artifact under the known target dirs -- `etc/btop`, `etc/cockpit`, `etc/chrony.conf`, `etc/mios/ipa-enroll.env`, `usr/lib/kernel/cmdline`, `usr/lib/ostree/prepare-root.conf`, `usr/share/mios/{oh-my-posh,quickshell,fastfetch,branding}` -- is claimed by some `[dotfiles.registry.*]` surface; and (b) the demoted producers (`generate-cockpit-conf.py`, `generate-ipa-enroll-env.py`, `generate-uki-cmdline.py`, `42-chrony-render.sh`, the render half of `77-composefs-verity.sh`) each hand off to `miosd dotfiles` rather than rendering standalone, detected by grepping for the handoff token. Add negative cases for BOTH failure modes to `tests/drift-gate-negatives.sh`.
**Where:** `automation/98-drift-checks.sh (check list ~4820, new check_projector_coverage), tests/drift-gate-negatives.sh, usr/share/mios/mios.toml`
**Done When:** the check goes red if any listed artifact has no registry surface OR any producer still renders standalone; both negative tests fail the gate as designed; `just drift-gate` is green on the migrated tree.
**Why:** Nothing today stops someone de-registering a surface or reviving a shadow projector; the engine's own orphan-template sweep only sees templates it already owns, so the cross-producer hole is invisible until a surface silently stops updating.
**Dep:** Add a mode-select kind for prepare-root.conf and demote automation/77-composefs-verity.sh's render half

### AGY-1389 -- Gate the projector crate with cargo-deny/cargo-audit and give it spanned miette diagnostics  (WS-DOTFILES | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- gives the compiled projector the dependency-provenance and error-UX story bash never had.
**What+How:** Add `src/mios-rs/deny.toml` covering banned/duplicate crates, a license allow-list that operationalizes the FOSS-upstream posture, and RustSec advisories. Add Justfile drift-gate steps running `cargo deny check` + `cargo audit` over both the `src/mios-rs` and `tools/native` workspaces, with `Cargo.lock` committed as the reproducibility SSOT (per SBOM-not-hardcode: dependency digests are recorded at build, never hand-pinned). Convert every projector verb to return `miette::Result` so a bad `[colors]` hex or an unknown `@MIOS:token@` renders a source-spanned diagnostic pointing at the offending mios.toml line (byte offsets, not char counts). Reconfirm the clippy restriction lints still ban `panic`/`unwrap` on the projector path.
**Where:** `src/mios-rs/deny.toml (new), src/mios-rs/Cargo.lock, Justfile (drift-gate), src/mios-rs/miosd/src/dotfiles/`
**Done When:** `cargo deny check` and `cargo audit` run as drift-gate steps and pass; a deliberately malformed hex in mios.toml produces a spanned miette diagnostic naming the line; Cargo.lock is committed; `just drift-gate` green.
**Why:** The projector is becoming a compiled dependency-carrying binary in the boot path with zero advisory scanning and zero license policy, and its failure mode today is an opaque error string that tells an operator nothing about which mios.toml line broke.
**Dep:** Rewire drift-check 25 to the Rust projector and add cargo fmt/clippy/test gates for the crate

### AGY-1390 -- Delete mios-generate-icons' private regex `[colors]` resolver and feed it the engine-resolved palette  (WS-DOTFILES | P1 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- eliminates the fourth independent palette resolver so the palette is resolved exactly once.
**What+How:** `usr/libexec/mios/mios-generate-icons` reads `/etc/mios/theme/theme.json` first and otherwise falls back to its own hand-rolled regex scan of mios.toml `[colors]` (`_load_theme_json` / `_load_mios_toml`, the `_TOML_COLOR_KEYS` list) to stamp brand hex into XDG SVGs -- a color-projection surface entirely outside `[dotfiles.registry.*]`. Register the icon SVGs (`usr/share/icons/MiOS/**`) as a projector-owned surface (a new svg/icon derive kind, or a post-render hook handing the engine's already-resolved palette to shared codegen). Strangler-fig: keep the Python emitting SVGs but feed it the engine-resolved palette and drop `_load_mios_toml`, prove the emitted SVG set is byte-identical against a golden fixture, then demote the script to a thin shim.
**Where:** `usr/libexec/mios/mios-generate-icons, usr/libexec/mios/mios-sync-theme (invocation site ~L176), usr/share/icons/MiOS/, usr/share/mios/mios.toml [dotfiles.registry.*]`
**Done When:** `mios-generate-icons` contains no mios.toml or `[colors]` reader of its own; the icon SVG set is drift-gated against a committed golden; `cargo test` and `just drift-gate` stay green with byte-identical SVG output versus the pre-change tree.
**Why:** A private regex scanner is exactly the divergent-resolver smell this campaign exists to kill, and this one silently drops the accent/info palette keys the SSOT owns -- so a palette change produces icons that disagree with every other surface.
**Dep:** Build the mios-config typed layered SSOT resolver crate (figment)

### AGY-1391 -- Fold the build-time Hyprland theme bake into the registry and retire the `@@MIOS_COLOR_*@@` sed dialect  (WS-DOTFILES | P1 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- brings the compositor (the liquid-glass north-star surface) under the one token dialect and under check 25.
**What+How:** `automation/65-bake-hyprland.sh` projects `[colors]` into `usr/share/mios/hyprland/hyprland.conf` at BUILD time using a SECOND token dialect -- `@@MIOS_COLOR_ACCENT@@` / `@@MIOS_COLOR_INFO@@` / `@@MIOS_COLOR_MUTED@@` substituted by an in-script sed, with a hardcoded fallback `col.active_border` and `: "${MIOS_COLOR_ACCENT:=#1A407F}"`. Convert hyprland.conf into a registry template surface (`usr/share/mios/theme/templates/hyprland.conf.tmpl`, canonical `@MIOS:accent@`/`@MIOS:info@`/`@MIOS:muted@` tokens) targeting `usr/share/mios/hyprland/hyprland.conf`, so compositor colors AND the liquid-glass `decoration.rounding`/opacity derive through the one projector. Strangler-fig: `capture` the current file, delete only the sed block from 65-bake-hyprland.sh (leave the rest of the bake), prove byte-parity.
**Where:** `automation/65-bake-hyprland.sh (L38-39, L166-184), usr/share/mios/hyprland/hyprland.conf, usr/share/mios/theme/templates/, usr/share/mios/mios.toml [dotfiles.registry.hyprland]`
**Done When:** hyprland.conf is a registered template surface containing no `@@...@@` tokens and no hardcoded hex; 65-bake-hyprland.sh runs no color sed; check 25 gates hyprland.conf; the build stays green with byte-identical output.
**Why:** The compositor is a live theme surface using a rival token syntax plus a hardcoded fallback color -- a projection hole the drift-gate cannot see today, while the liquid-glass north star explicitly requires compositor effects to project FROM mios.toml.
**Dep:** Port the template derive + @MIOS:token@ substitution to Rust behind the miosd facade

### AGY-1392 -- Fold the surfer/webshell XML bake into the registry and delete its hardcoded palette fallbacks  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- closes the browser-shell color surface, the last un-gated build-time projector.
**What+How:** `automation/67-bake-surfer.sh` (L121-140) emits the MiOS webshell XML sidebar from a heredoc interpolating `${MIOS_COLOR_BG}` / `${MIOS_COLOR_ACCENT}` / `${MIOS_COLOR_SUBTLE}` with hardcoded fallbacks (`:=#282262`, `:=#1A407F`, `:=#B7C9D7`). Extract that XML into a registry template surface `usr/share/mios/theme/templates/surfer-shell.xml.tmpl` using `@MIOS:bg@`/`@MIOS:accent@`/`@MIOS:subtle@`, targeting the committed webshell artifact under `usr/lib/mios/webshell/`, and demote the bake script to writing the projector's rendered output. Prove byte-parity of the emitted XML before deleting the inline heredoc.
**Where:** `automation/67-bake-surfer.sh (L121-140), usr/lib/mios/webshell/, usr/share/mios/theme/templates/, usr/share/mios/mios.toml [dotfiles.registry.surfer-shell]`
**Done When:** the surfer shell XML is a registered template surface with no hardcoded hex; 67-bake-surfer.sh renders via the projector; check 25 gates the artifact; webshell output is byte-identical to the current tree.
**Why:** The browser shell still ships Catppuccin-era fallback hex that activates whenever the env vars are unset, producing a shell whose colors disagree with the operator's palette -- and no gate would notice.
**Dep:** Port the template derive + @MIOS:token@ substitution to Rust behind the miosd facade

### AGY-1393 -- Repoint the three runtime call sites (service unit, `mios dotfiles` verb, completion) at miosd, degrade-open  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- makes the compiled engine the thing that actually runs at boot and at the operator's prompt.
**What+How:** Three non-drift-gate call sites still hardcode the Python scripts: `usr/lib/systemd/system/mios-sync-theme.service` (`ExecStart=/usr/libexec/mios/mios-sync-theme`, `SuccessExitStatus=0 1`, ordered `Before=mios-agent-pipe.service hermes-dashboard.service`); the `dotfiles)` case in `etc/profile.d/mios-verbs.sh` (L110-122, dispatching to `/usr/libexec/mios/mios-dotfiles` with a `/mnt/m/...` fallback); and the bash-completion word list (L185). Point ExecStart and the verb at the miosd binary (`miosd dotfiles sync`, `miosd dotfiles <status|diff|sync>`), preserving `SuccessExitStatus=0 1`, the boot ordering, and the `/mnt/m` fallback, and keep the Python path as a `command -v miosd || fallback` last resort so a missing binary never blocks boot.
**Where:** `usr/lib/systemd/system/mios-sync-theme.service, etc/profile.d/mios-verbs.sh (L108-122, L162, L185), usr/lib/tmpfiles.d/mios-theme.conf`
**Done When:** unit, verb and completion all invoke miosd with Python only as degrade-open fallback; boot ordering and SuccessExitStatus are unchanged; `mios dotfiles status/diff/sync` behave identically on a booted MiOS-DEV VM.
**Why:** The Rust port is inert until its real invokers call it; these two entry points are the boot oneshot and the operator verb, and neither is covered by the check-25 rewire task -- so the binary could ship complete and never execute.
**Dep:** Scaffold `miosd dotfiles` projection subcommand (clap) as the compiled facade over the Python engine

### AGY-1394 -- Cross-compile miosd for Windows and add a windows-latest CI job for the live-apply path  (WS-DOTFILES | P1 | L)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- keeps the Windows half of the projection at parity instead of stranding it on the deprecated Python.
**What+How:** The apply/diff path is explicitly platform-branched: `_apply_live_registry` short-circuits on `os.name != 'nt'`, `_os_key()`/`_apply_target` select windows targets, and the Windows surfaces (`windows-registry` via `reg.exe add/query` read-back, `windows-terminal` LocalState settings.json, `vscode-colors` under `%APPDATA%`) only execute on Windows -- today via the Python engine reached from `Get-MiOS.ps1` / `mios-oscontrol-server.ps1`. Add an `x86_64-pc-windows-msvc` target to the crate build, gate the reg.exe backup/add/query-verify logic behind `cfg(windows)`, ship the binary through the Windows install leg, and add a GitHub CI job running `cargo test` on windows-latest that exercises registry/WT/vscode apply against a mock `HKCU\Console\MiOS-Test`. Keep the Python engine as the Windows fallback until the binary lands.
**Where:** `src/mios-rs/miosd/, tools/native/Cargo.toml, usr/libexec/mios/mios-dotfiles-render (_apply_live_registry L758-846), *.ps1 install/oscontrol entry points, .github/ CI`
**Done When:** a windows-target miosd is built and distributed; `cargo test` is green on a Windows CI runner covering registry/WT/vscode apply; `miosd dotfiles apply` on Windows produces the same HKCU and settings.json result as the Python engine.
**Why:** The entire reason the merge kinds exist is the Windows live-HOME apply; a Linux-only port would leave the registry/terminal/vscode surfaces on an engine slated for deletion, with no CI proving they still work.
**Dep:** Port the live-HOME apply/diff path (HOME-scope + symlink refusal, RAW backup) to Rust

### AGY-1395 -- Add a hex-literal-leak gate: no `#RRGGBB` outside templates, fixtures and an explicit allowlist  (WS-DOTFILES | P2 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- stops tomorrow's hardcoded color from re-opening the hole today's migrations are closing.
**What+How:** The projector already recognises the palette via `_HEX` (`#[0-9A-Fa-f]{6}`) and `capture` templatizes it, but nothing PREVENTS a new hardcoded color from being committed outside the projector -- proven by the live `:=#1A407F` fallbacks in `65-bake-hyprland.sh` and `67-bake-surfer.sh`. Add a scan (a Rust sibling in the projector crate plus a `check_*` in `automation/98-drift-checks.sh`, adjacent to `check_dotfiles_projection` ~L1732) that fails on any palette hex whose match does not live in `usr/share/mios/theme/templates/*.tmpl`, `usr/share/mios/theme/fixtures/*`, or an explicit mios.toml allowlist in the `[security.nohc_allowlist]` style. This is the color-literal complement to the registry-coverage fitness function.
**Where:** `automation/98-drift-checks.sh (new check adjacent to check_dotfiles_projection ~L1732), src/mios-rs/miosd/, usr/share/mios/mios.toml (allowlist)`
**Done When:** the gate red-flags a newly hardcoded `#hex` anywhere outside templates/fixtures/allowlist; the current tree passes once the hyprland and surfer folds land; `cargo test` and `just drift-gate` are green.
**Why:** Without a leak gate the projector-is-SSOT invariant decays exactly the way the zero-hardcodes campaign documents, and two live fallbacks already prove the hole is real, not theoretical.
**Dep:** Add a registry-coverage fitness function proving no un-migrated standalone projector remains

### AGY-1396 -- Promote the runtime registry-schema invariants to a build-time drift-gate  (WS-DOTFILES | P2 | S)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- turns a latent runtime `SystemExit(3)` into a caught PR failure and hardens the apply-target safety contract.
**What+How:** Today `_load_surfaces` (usr/libexec/mios/mios-dotfiles-render L874-929) enforces its invariants at RUNTIME -- missing template/target, unknown kind, a merge kind lacking `fixture.base`/`fixture.expected`, a `.` in a surface name -- so a mis-authored `[dotfiles.registry.*]` entry only fails when someone runs the engine. Promote them to a compiled fitness function (miosd plus a `check_*` in 98-drift-checks.sh) that deserializes the WHOLE registry and asserts: every `template`/`target`/`fixture.base`/`fixture.expected` is a git-TRACKED, present path; every `apply.target` is `HOME`/`%USERPROFILE%`/`%LOCALAPPDATA%`/`%APPDATA%`-relative (never absolute outside HOME) or an `HKCU\` key; each merge kind carries its fixture pair; no surface name contains a dot. Reuse the typed `Surface` structs from the serde deserialization task.
**Where:** `usr/libexec/mios/mios-dotfiles-render (_load_surfaces L874-929), src/mios-rs/miosd/, automation/98-drift-checks.sh, usr/share/mios/mios.toml [dotfiles.registry.*]`
**Done When:** a registry entry with a missing or untracked path, an absolute `apply.target`, or a dotted surface name fails the drift-gate at build time rather than at first run; the current registry passes unchanged.
**Why:** An operator editing the registry through the Portal (ADR-0009) can author a surface that stays green in CI and only explodes at runtime -- including an `apply.target` that writes outside HOME.
**Dep:** Deserialize [dotfiles.registry.*] into typed Surface structs (serde) replacing _load_surfaces

### AGY-1397 -- Delete the mios-theme-render deprecation alias and purge its 21 references (mios-sync-theme still calls it)  (WS-DOTFILES | P3 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- removes the retired name so exactly one projector name exists in the tree.
**What+How:** `usr/libexec/mios/mios-theme-render` is now an 11-line shim that warns and re-execs `mios-dotfiles-render`, yet 21 tracked files still name it -- and `usr/libexec/mios/mios-sync-theme` (L142) STILL invokes the deprecated alias via `$(dirname $0)/mios-theme-render` instead of the real engine, so every theme refresh prints the deprecation warning. Sweep every reference (mios-sync-theme, ADR-0010 and ADR-0011, ROADMAP.md/TASKS.md/AGY-TASKS.md, `usr/share/mios/reference/env-baseline.txt`, `automation/manifest.json`, the drift-check comment) to name the miosd binary, or `mios-dotfiles-render` during transition, then delete the alias file once no non-doc reference remains. Stage explicit paths only -- never `git add -A`.
**Where:** `usr/libexec/mios/mios-theme-render, usr/libexec/mios/mios-sync-theme (L142-151), usr/share/doc/mios/adr/0010-ssot-as-system-dotfiles.md, ROADMAP.md, TASKS.md, AGY-TASKS.md, automation/manifest.json, usr/share/mios/reference/env-baseline.txt`
**Done When:** the alias file no longer exists; `grep -rn mios-theme-render` returns only intentional history/changelog mentions; mios-sync-theme emits no deprecation warning on a refresh.
**Why:** The primary orchestrator calls its own deprecated alias, so every boot-time refresh logs a warning and every reader of the tree sees two names for one engine.
**Dep:** Port the mios-sync-theme global-refresh orchestration into a `miosd dotfiles sync` command (shell-free)

### AGY-1398 -- Terminal step: native status/diff summary in miosd, then delete the Python engine and its wrapper  (WS-DOTFILES | P3 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- ends the strangler-fig with ONE projector in the tree rather than two forever.
**What+How:** `usr/libexec/mios/mios-dotfiles` (255 lines) is a read-only orchestrator that SHELLS the engine and re-parses its stdout with a regex (`_LINE`) to print a per-surface OK/skip/change/refuse table plus tally (`_classify`/`_dest_of`/`_print_summary`/`_rc_for`). Reimplement that summary NATIVELY in `miosd dotfiles status/diff` -- no stdout scraping, since the Rust code already holds each surface's outcome. Then, after the `[migration]` toggle has defaulted every surface to Rust for the agreed soak and the goldens plus drift-gate have stayed green for N releases, DELETE both `usr/libexec/mios/mios-dotfiles-render` (the engine) and `usr/libexec/mios/mios-dotfiles` (the wrapper).
**Where:** `usr/libexec/mios/mios-dotfiles (L49-220), usr/libexec/mios/mios-dotfiles-render, src/mios-rs/miosd/, usr/share/mios/mios.toml [migration]`
**Done When:** miosd renders the status/diff summary and tally natively; both Python files are deleted; no tracked file references them; `cargo test` and `just drift-gate` green with the `[migration]` toggle removed.
**Why:** Keeping the Python engine and a stdout-scraping wrapper beside the Rust binary is precisely the duplication this campaign exists to remove, and without a dependency-ordered final delete the tree ships two projectors indefinitely.
**Dep:** Demote mios-dotfiles-render to a thin Rust-first shim and prove byte-parity via proptest differential testing

### AGY-1399 -- Add a Law-15 cross-repo equivalence gate for the registry, templates and fixtures  (WS-DOTFILES | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- keeps the shared dotfiles SSOT byte-identical across mios.git and mios-bootstrap.git through the riskiest phase of the migration.
**What+How:** The `[dotfiles.registry.*]` block, `usr/share/mios/theme/templates/*.tmpl` and `usr/share/mios/theme/fixtures/*` are shared SSOT surfaces, and mios-bootstrap is the sibling repo (present at `C:\mios-bootstrap`). Per Law 15 every change to them must land byte-identically in BOTH repos. Add a cross-repo equivalence fitness function -- a `check_*` that runs when the sibling repo is present, plus a documented `just verify-both-repos` recipe -- hashing the registry section and the templates/fixtures trees and FAILING on any divergence. Stage explicit paths in each repo; never `git add -A`, since AGY shares the tree.
**Where:** `usr/share/mios/mios.toml [dotfiles.registry.*], usr/share/mios/theme/templates/, usr/share/mios/theme/fixtures/, automation/98-drift-checks.sh, C:\mios-bootstrap (sibling repo)`
**Done When:** the gate detects an injected registry/template/fixture divergence between the two repos; both repos currently pass; the recipe is documented in the migration runbook.
**Why:** Law 15 mandates mirroring shared SSOT surfaces, and a per-surface migration is exactly when the two repos skew apart unnoticed -- producing a bootstrap that projects a different theme than the OS.
**Dep:** Deserialize [dotfiles.registry.*] into typed Surface structs (serde) replacing _load_surfaces

### AGY-1400 -- Move wsl-theme-bridge's light/dark mapping into `[theme]` SSOT and make a flip re-project surfaces  (WS-DOTFILES | P3 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- closes the last theme consumer that neither reads from nor re-triggers the SSOT.
**What+How:** `usr/libexec/mios/wsl-theme-bridge.sh` polls the Windows `AppsUseLightTheme` registry value and maps it to a GNOME color-scheme plus gtk-theme, but the mapping is HARDCODED (`adw-gtk3-dark`/`adw-gtk3`, `prefer-dark`/`default`) and the 15s interval is a magic default. Move the poll interval, the gtk-theme names and the color-scheme mapping into mios.toml `[theme]` (read as `${VAR:-LIT}` with the current values as fallback), and on a detected light/dark flip trigger a projector re-render/apply through the miosd theme path so dependent surfaces re-project -- not just a `gsettings` write. Keep the WSL-only guard and the degrade-open exits verbatim.
**Where:** `usr/libexec/mios/wsl-theme-bridge.sh (L34, L65-77), usr/lib/systemd/system/mios-wsl-theme-bridge.service, usr/share/mios/mios.toml [theme], src/mios-rs/miosd/`
**Done When:** the bridge sources interval and gtk/color-scheme mapping from mios.toml `[theme]`; a light/dark flip re-projects theme surfaces; the WSL-only guard and degrade-open behavior are byte-unchanged.
**Why:** The one surface that reacts to the host OS carries hardcoded theme names and a magic interval outside the SSOT, so an operator changing their theme in mios.toml gets a WSL session that ignores them.
**Dep:** Fold mios-sync-theme's /etc/mios/theme bridge (theme.json + mios-theme.css) into registry surfaces

### AGY-1401 -- Add a determinism gate: render twice under varied TZ/LANG/hash-seed and byte-compare  (WS-DOTFILES | P3 | S)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- makes the drift-gate sound by proving projection is a pure function of the SSOT.
**What+How:** The drift-gate assumes render/derive are pure, but the code reads a clock (`_utc_stamp` at L1149, for `.mios-bak` names) and relies on map ordering (`_derive_registry` uses `sort_keys=True`; `_json_deep_merge` preserves insertion order) -- so a Rust port could leak HashMap iteration order, locale-sensitive sorting, or an env/clock read into a COMMITTED artifact. Add a CI fitness function that runs `miosd dotfiles render` and each merge derive TWICE under different TZ, LANG and Rust hash seed and byte-compares the outputs, plus a static rule forbidding any clock or env read on the derive path except the apply-only backup timestamp. Wire it into the crate's `cargo test` and the drift-gate.
**Where:** `src/mios-rs/miosd/, usr/libexec/mios/mios-dotfiles-render (_utc_stamp L1149, _derive_registry sort_keys L755, _json_deep_merge L273), automation/98-drift-checks.sh`
**Done When:** the twice-render byte-comparison passes under varied TZ/LANG/hash seed; a deliberately injected nondeterminism (e.g. unordered map iteration) fails it; `cargo test` and `just drift-gate` are green.
**Why:** A Rust==Python parity proptest does not prove run-to-run stability, so nondeterministic ordering could land in a committed artifact and show up as phantom drift-gate failures nobody can reproduce.
**Dep:** Port the `render` and `capture` verbs (hex->token round-trip self-verify) to Rust

### AGY-1402 -- Sync ADR-0010/0011, the in-file AI-hints and the auto-memory to name miosd  (WS-DOTFILES | P3 | S)
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- so an arriving agent reads the current topology instead of re-deriving the retired one.
**What+How:** The projector's design docs and in-file AI-hints all describe the Python engine: `usr/share/doc/mios/adr/0010-ssot-as-system-dotfiles.md` and `0011-unified-languages-and-file-patterns.md` name `mios-theme-render`, and the AI-hint headers in `mios-dotfiles-render` / `mios-dotfiles` / `mios-sync-theme` point at Python paths. After the binary lands, update those docs and headers to name the miosd projector, document the `[migration]` toggle and the surface registry as the operator contract, and refresh the theme-ssot-projection / dotfiles-registry auto-memory notes. Docs-only; stage explicit paths.
**Where:** `usr/share/doc/mios/adr/0010-ssot-as-system-dotfiles.md, usr/share/doc/mios/adr/0011-unified-languages-and-file-patterns.md, usr/libexec/mios/mios-dotfiles-render (header), usr/libexec/mios/mios-dotfiles (header), usr/libexec/mios/mios-sync-theme (header)`
**Done When:** ADR-0010/0011 and the projector file headers describe miosd rather than mios-theme-render; the `[migration]` toggle is documented; no doc claims the Python engine is authoritative.
**Why:** Stale ADRs and AI-hints pointing at a deleted engine send both operators and future agents down the wrong path, which is how a "migrated" surface quietly gets a second projector re-added.
**Dep:** Rewire drift-check 25 to the Rust projector and add cargo fmt/clippy/test gates for the crate

### AGY-1403 -- Repair the fragmented tools/native Cargo workspace so all eight SSOT crates build as one graph  (WS-GUP | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the one consolidated workspace every later clippy/test/deny gate depends on.
**What+How:** `tools/native/Cargo.toml` declares `members = [mios-drift-runner, mios-version-check, mios-wallpaperd]` ONLY, yet five sibling crates on disk (`generate-names-registry`, `mios-ssot-lint`, `mios-ssot-walk`, `mios-aiplane-lint`, `mios-bake-plan`) set `version.workspace = true` / `edition.workspace = true` and therefore REQUIRE this workspace while not being members -- so `cargo build` at the workspace root errors or silently skips them, which is why check bodies shell out to ad-hoc per-crate `cargo build --manifest-path ... -p mios-ssot-lint` (98-drift-checks.sh:4880). Add all five to `members`, confirm each still resolves version/edition from `[workspace.package]`, and prove a single clean graph with `cargo build --workspace` + `cargo metadata`. Pure infrastructure repair, no behavior change.
**Where:** `tools/native/Cargo.toml, tools/native/generate-names-registry/Cargo.toml, tools/native/mios-ssot-lint/Cargo.toml, tools/native/mios-ssot-walk/Cargo.toml, tools/native/mios-aiplane-lint/Cargo.toml, tools/native/mios-bake-plan/Cargo.toml`
**Done When:** `cargo build --workspace --manifest-path tools/native/Cargo.toml` compiles all 8 crates; `cargo metadata` lists every crate as a workspace member; the updated Cargo.lock is committed by staging explicit paths only.
**Why:** No workspace-wide clippy, test or deny gate can exist while five crates are invisible to the root manifest, and the drift-checks paper over it with per-crate builds that mask compile failures.
**Dep:** none

### AGY-1404 -- Add cargo build/test/clippy/fmt Just targets plus `check_native_lint` (the missing shellcheck-equivalent)  (WS-GUP | P1 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- gives the Rust plane the CI lint gate the shell plane never got.
**What+How:** The Justfile has ZERO cargo/clippy/fmt targets and native crates are compiled ad hoc (Containerfile:49 `cargo build --release`; check bodies build on demand). Add `native-build`, `native-test`, `native-lint` recipes running `cargo build --workspace`, `cargo test --workspace`, and `cargo clippy --workspace -- -D warnings` plus `cargo fmt --workspace --check` against `tools/native/Cargo.toml`. Then add `check_native_lint` to `automation/98-drift-checks.sh` (registered near `check_lint_is_final`) running clippy with `RUSTFLAGS=-Dwarnings` and `fmt --check`, SOFT-skipping when cargo is absent, mirroring the python3-missing SOFT pattern in `check_var_closure`. Curate targeted restriction lints (`clippy::unwrap_used`, `clippy::panic`, `clippy::todo`) as crate-level `#![deny(...)]` in each SSOT crate rather than blanket-enabling the group.
**Where:** `Justfile, automation/98-drift-checks.sh, tools/native/mios-ssot-lint/src/main.rs, tools/native/mios-version-check/src/main.rs`
**Done When:** `just native-lint` is green; `check_native_lint` appears in the drift-gate check list and fails a deliberately warning-ridden crate in `tests/drift-gate-negatives.sh`; clippy runs with `-D warnings`.
**Why:** The tech-debt map flags "no shellcheck CI", and every crate landing after this one would otherwise accrue unlinted `unwrap`/`panic` in system-critical paths with nothing to catch it.
**Dep:** Repair the fragmented tools/native Cargo workspace

### AGY-1405 -- Freeze the resolver output as versioned golden-master fixtures before any port  (WS-GUP | P1 | M)
**Goal:** E-08 Global Unification Plan: collapse the MIOS_* namespace to one-value-derived -- installs the losslessness safety net that every later resolver rewrite is measured against.
**What+How:** Capture the current observable resolver output as immutable golden masters so rewrites are checked against a recorded result rather than hand-written expectations. The three artifacts already exist: `usr/share/mios/reference/env-baseline.txt` (2475 emitted `MIOS_*` lines), `usr/share/mios/names.generated.txt` (503 canonical path->name rows), and `usr/share/mios/referenced_names.txt` (1181). Add a `mios-resolver-goldens` test target under `tools/native` (or a `tests/` dir) that copies all three into trycmd/insta `.snap` fixtures and asserts a fresh `bash usr/libexec/mios/mios-env-snapshot` run reproduces `env-baseline.txt` byte-for-byte under `LC_ALL=C`. Document the accept/review workflow (`cargo insta review`) in a short reference note so fixtures can only change deliberately.
**Where:** `usr/share/mios/reference/env-baseline.txt, usr/share/mios/names.generated.txt, usr/share/mios/referenced_names.txt, usr/libexec/mios/mios-env-snapshot, tools/native/`
**Done When:** a cargo test snapshot-compares a live `mios-env-snapshot` run to `env-baseline.txt` and passes byte-identical; the `.snap` fixtures are committed via explicit path staging; the note records the insta review flow.
**Why:** The resolver is untested shell plus Python emitting 2475 keys; without a frozen golden, any port silently changes a key nobody notices until a service reads the wrong value at boot.
**Dep:** Repair the fragmented tools/native Cargo workspace

### AGY-1406 -- Extract the get_aliases prefix-family and per-key rules into ONE declarative derivation-graph table  (WS-GUP | P1 | L)
**Goal:** E-08 Global Unification Plan: collapse the MIOS_* namespace to one-value-derived -- the structural core: one table defines every dotted-path -> `MIOS_*` derivation.
**What+How:** The derivation is currently a ~330-line hand-coded if/elif ladder in `usr/lib/mios/mios_toml.py::get_aliases` (lines ~224-557), partially and INCOMPLETELY re-implemented in `tools/native/generate-names-registry/src/main.rs::alias_for`, and again in `tools/generate-names-registry.py`. Author a single declarative data file -- `usr/share/mios/reference/alias-derivation.tsv` (or .toml) -- expressing the ~13 systematic prefix families (`ai.vllm.*` -> `MIOS_VLLM_*`, `ports.*` -> `MIOS_PORT_*`/`MIOS_*_PORT`, `identity.*` -> `MIOS_USER...`, `image.sidecars.*` -> `..._IMAGE`/`..._VERSION`, `services.*`, etc.) plus the enumerated per-key exceptions, in the same `canonical<TAB>alias<TAB>disposition` spirit as the existing `usr/share/mios/reference/value-aliases.tsv`. Lands as pure data with a schema-doc header -- no consumer is repointed yet. Mirror into mios-bootstrap.git per Law 15.
**Where:** `usr/share/mios/reference/alias-derivation.tsv, usr/lib/mios/mios_toml.py, usr/share/mios/reference/value-aliases.tsv, usr/share/doc/mios/reference/`
**Done When:** `alias-derivation.tsv` exists and, parsed by a check script, reproduces the EXACT alias set `get_aliases` emits for every path in `names.generated.txt` (503 rows) with zero diff; the file is mirrored in both repos.
**Why:** Three divergent copies of the same derivation logic mean the Rust and Python name registries already disagree, and no compiled resolver can be written until there is one authoritative statement of the rules.
**Dep:** Freeze the resolver output as versioned golden-master fixtures

### AGY-1407 -- Make `mios_toml.py::get_aliases` read the derivation table instead of its if/elif ladder  (WS-GUP | P2 | L)
**Goal:** E-08 Global Unification Plan: collapse the MIOS_* namespace to one-value-derived -- converts the authoritative resolver from hand-maintained code to SSOT-derived data.
**What+How:** Strangler-fig the Python resolver onto the new table: rewrite `usr/lib/mios/mios_toml.py::get_aliases` to load `usr/share/mios/reference/alias-derivation.tsv` once (module-level cache) and resolve a dotted path by longest-prefix-family match plus exact-key override, deleting the ~330-line if/elif body. Keep the old ladder callable behind a `MIOS_ALIAS_LEGACY=1` fallback for one release (degrade-open). Prove byte-parity by regenerating `names.generated.txt` and running `mios-env-snapshot`: the diff against the frozen 2475-line `env-baseline.txt` golden must be empty, and `check_resolver_twin_equivalence` (tools/check-resolver-twin.py, which imports `get_aliases`) must stay green because both twins now read the same table.
**Where:** `usr/lib/mios/mios_toml.py, usr/share/mios/reference/alias-derivation.tsv, usr/share/mios/names.generated.txt, tools/check-resolver-twin.py, usr/libexec/mios/mios-env-snapshot`
**Done When:** `get_aliases` contains no per-section if/elif ladder; `names.generated.txt` and `env-baseline.txt` regenerate byte-identical; `check_names_registry`, `check_resolver_twin_equivalence` and `check_resolved_env_lossless` are all green.
**Why:** Until the authoritative resolver consumes the table, the table is unproven documentation and the compiled crates would adopt rules nothing validates.
**Dep:** Extract the get_aliases prefix-family + per-key rules into ONE declarative derivation-graph SSOT table

### AGY-1408 -- Grow mios-ssot-walk from a stub into the one shared native walk + derivation library  (WS-GUP | P2 | M)
**Goal:** E-08 Global Unification Plan: collapse the MIOS_* namespace to one-value-derived -- one compiled library so the derivation logic cannot re-diverge across tools.
**What+How:** `tools/native/mios-ssot-walk/src/lib.rs` is a near-empty stub (only `EXCLUDED_SECTIONS` and `is_excluded_section`). Grow it into the single shared Rust SSOT engine by porting `walk()` (mios_toml.py:564-576, the recursive section flattener that skips `routing.domains`), `process_val()` (mios_toml.py:578-589, bool/list/ports-stack-offset handling), the `WALK_MOSTLY_DEAD`/`WALK_EMIT_KEEP` sets (mios_toml.py:591-601), and an `alias_lookup()` that parses `usr/share/mios/reference/alias-derivation.tsv`. Add unit tests asserting parity with known path->name rows from `names.generated.txt`. This crate becomes the dependency that `generate-names-registry` and the future `mios-resolve` both import.
**Where:** `tools/native/mios-ssot-walk/src/lib.rs, tools/native/mios-ssot-walk/Cargo.toml, usr/share/mios/reference/alias-derivation.tsv, usr/lib/mios/mios_toml.py`
**Done When:** mios-ssot-walk exposes `walk`/`process_val`/`alias_lookup` with `cargo test -p mios-ssot-walk` green; the crate is a workspace member; `alias_lookup` reproduces the `alias-derivation.tsv` rows.
**Why:** The empty stub is exactly why `generate-names-registry` shipped its own incomplete `alias_for`; without a real shared lib every new compiled tool re-implements the walk and re-introduces the divergence.
**Dep:** Make mios_toml.py.get_aliases read the derivation-graph table instead of the hardcoded if/elif ladder

### AGY-1409 -- Rewrite the Rust generate-names-registry onto the shared lib and kill its incomplete `alias_for`  (WS-GUP | P2 | M)
**Goal:** E-10 One canonical name: the unified names/keys registry -- makes the compiled generator a faithful projector of the same registry the Python one emits.
**What+How:** `tools/native/generate-names-registry/src/main.rs::alias_for` handles ONLY `ai.vllm.*` and `ai.sglang.*` and otherwise falls back to a naive uppercase `MIOS_<PATH>` -- so its `names.generated.txt` output diverges from Python's `get_aliases` on identity, ports, services and image.sidecars aliases. Replace `alias_for` and the local `walk_value` with calls into the mios-ssot-walk lib (`alias_lookup` + `walk`), so the Rust generator emits exactly what the SSOT table dictates. Prove it by regenerating `usr/share/mios/names.generated.txt` with the rewritten binary and diffing against the Python-produced golden -- empty diff required. Add the crate to the workspace members and a trycmd snapshot of its stdout.
**Where:** `tools/native/generate-names-registry/src/main.rs, tools/native/generate-names-registry/Cargo.toml, tools/native/mios-ssot-walk/src/lib.rs, usr/share/mios/names.generated.txt`
**Done When:** the Rust generator's output is byte-identical to `tools/generate-names-registry.py`'s output and to the committed `names.generated.txt`; the crate is a workspace member with a passing trycmd snapshot.
**Why:** Two generators for one registry currently produce different names, so whichever one runs decides what `names.generated.txt` says -- a live divergence sitting under the check that is supposed to guarantee one canonical name.
**Dep:** Populate mios-ssot-walk into the one shared native SSOT-walk + derivation library

### AGY-1410 -- Cut check_names_registry over to the compiled generate-names-registry and demote the Python generator to a thin exec shim  (WS-GUP | P2 | M)
**Goal:** E-01 Compiled native tier -- finishes one strangler-fig lane so a libexec-tier bash/Python generator is replaced by a compiled binary only after byte-parity is proven.
**What+How:** `automation/98-drift-checks.sh::check_names_registry` (line 2064) today shells `tools/generate-names-registry.py` to validate that `names.generated.txt` and `referenced_names.txt` are fresh. Switch the check to invoke `tools/native/target/release/generate-names-registry` when that binary is present and fall back to the Python script only when it is absent (degrade-open, matching the existing python3-missing pattern). Then reduce `tools/generate-names-registry.py` to a shim that execs the binary, preserving the stable entrypoint name and the `git ls-files` referenced-name scan. Keep the byte-parity assertion inside the check so any future shim-vs-binary divergence fails loudly rather than silently picking a winner.
**Where:** `automation/98-drift-checks.sh, tools/generate-names-registry.py, tools/native/generate-names-registry/src/main.rs, usr/share/mios/names.generated.txt, usr/share/mios/referenced_names.txt`
**Done When:** check_names_registry validates against the compiled generator and is green on the full tree with the Python script out of the hot path; a negative test that stales `names.generated.txt` still fails the gate.
**Why:** Two live implementations of the registry generator can drift apart the moment either side is edited, and the drift-gate would keep validating the slower, unhardened one.
**Dep:** Rewrite the Rust generate-names-registry to consume the derivation table + mios-ssot-walk

### AGY-1411 -- Add an inert `[migration]` toggle table to mios.toml so every Rust cutover has a one-value rollback  (WS-GUP | P1 | S)
**Goal:** E-01 Compiled native tier -- makes rollback a first-class deliverable of the strangler migration so no compiled slice can wedge the bake.
**What+How:** No `[migration]` section exists in `usr/share/mios/mios.toml` (verified). Add one carrying `use_rust_resolver`, `use_rust_names_registry`, `use_rust_version_check`, all defaulting to `false`, so the deploying operator picks Rust-vs-bash per surface from the one config surface and reverts a regression by editing one SSOT value instead of reverting a build. Declare the alias emission in `usr/share/mios/reference/alias-derivation.tsv` so each key projects as `MIOS_MIGRATION_*` for consumers, and add a small `check_migration_toggles` to `automation/98-drift-checks.sh` asserting the table exists and every documented toggle still defaults to the safe (bash) value. Ship inert, matching the `db_backed` convention. Mirror the mios.toml change into mios-bootstrap.git per Law 15.
**Where:** `usr/share/mios/mios.toml, usr/share/mios/reference/alias-derivation.tsv, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh`
**Done When:** the `[migration]` table is present in both repos with safe defaults; `MIOS_MIGRATION_*` keys appear in `env-baseline.txt` under a deliberate baseline bump; check_migration_toggles is green and a flipped-default negative test fails.
**Why:** Without a declared rollback substrate, every later compiled slice is a one-way cutover -- a regression in the resolver or version checker means reverting commits mid-campaign while the publish bake stays red.
**Dep:** Extract the get_aliases prefix-family + per-key rules into ONE declarative derivation-graph SSOT table

### AGY-1412 -- Scaffold the mios-resolve crate: a typed layered resolver whose `--emit-env` matches env-baseline exactly  (WS-GUP | P2 | XL)
**Goal:** E-01 Compiled native tier -- replaces the highest-traffic shell string-munging surface in the tree with a compiled, type-checked resolver core.
**What+How:** Create `tools/native/mios-resolve` composing the same ordered providers `userenv.sh` does: vendor `/usr/share/mios/mios.toml` < host `/etc/mios/mios.toml` < user `~/.config/mios/mios.toml`, each expanded by its `mios.d/*.toml` fragments in tier-major lexical order, with empty-string-never-overrides. Use figment (or serde+toml) to deserialize into typed structs -- the struct IS the schema, failing loudly on shape errors -- then serialize the `MIOS_*` projection from the typed model through `mios-ssot-walk::alias_lookup`, applying `process_val`'s ports `stack_offset` and the post-load pgvector `listen_loopback -> MIOS_PG_BIND_ADDR` transform (userenv.sh:258-261). Gate activation behind `[migration].use_rust_resolver` (default off). Do NOT reroute `userenv.sh` in this slice; it produces only the binary plus a `--emit-env` mode compared against `env-baseline.txt`.
**Where:** `tools/native/mios-resolve/, tools/native/Cargo.toml, tools/native/mios-ssot-walk/src/lib.rs, usr/lib/mios/userenv.sh, usr/lib/mios/mios_toml.py, usr/share/mios/reference/env-baseline.txt`
**Done When:** `cargo run -p mios-resolve -- --emit-env` under `HOME=/nonexistent` with vendor-only layers diffs empty against `env-baseline.txt`; the crate builds clean under `clippy -D warnings`; the toggle stays off so nothing is rerouted.
**Why:** Today a shell/Python resolver silently yields a wrong string on an edge-case layer merge and that value flows straight into a Quadlet -- there is no type system anywhere between mios.toml and a running container.
**Dep:** Populate mios-ssot-walk into the one shared native SSOT-walk + derivation library

### AGY-1413 -- Golden trycmd/insta snapshots pinning mios-resolve to the shell oracle across all four layer scenarios  (WS-GUP | P2 | L)
**Goal:** E-01 Compiled native tier -- supplies the characterization harness that must prove byte-parity BEFORE any shell resolver path is deleted.
**What+How:** Add snapshot tests covering the layered scenarios the twins support: (1) vendor-only (`env-baseline.txt`), (2) vendor+host overlay, (3) vendor+host+user overlay, (4) vendor plus a `mios.d/*.toml` fragment tier-major merge. Use trycmd for whole-CLI stdout and exit code of `mios-resolve --emit-env`, and insta `.snap` files for the structured typed-model dump. Reuse the frozen goldens and add small fixture TOMLs under the crate's `tests/` for the overlay cases, pinning `LC_ALL=C` for deterministic ordering. Any deviation becomes an explicit accept/review event.
**Where:** `tools/native/mios-resolve/tests/, usr/share/mios/reference/env-baseline.txt, usr/libexec/mios/mios-env-snapshot, tools/lib/userenv.sh`
**Done When:** `cargo test -p mios-resolve` runs all four scenario snapshots green, and deliberately altering one fixture value flips a snapshot -- proving the tests bite rather than rubber-stamp.
**Why:** Without recorded goldens the compiled resolver can drift from the shell result in a scenario nobody hand-wrote an expectation for, and the divergence surfaces as a broken service at first boot.
**Dep:** Scaffold the mios-resolve crate: a typed figment/serde layered resolver emitting MIOS_* to match env-baseline

### AGY-1414 -- Differential proptest harness running random-valid mios.toml through both the userenv.sh oracle and mios-resolve  (WS-GUP | P2 | L)
**Goal:** E-05 Test and CI governance -- lands the proptest differential oracle that makes the compiled plane trustworthy rather than merely tested by example.
**What+How:** Mechanize the Global-Unification lossless-diff gate as property-based differential testing. Add a proptest generator emitting random-but-valid mios.toml documents (varying `[ports]` ints, `[identity]`/`[locale]` strings, `[ai.vllm]`/`[ai.sglang]` toggles, `[image.sidecars]` refs, `[colors]` hexes); for each document run BOTH the bash oracle (`source tools/lib/userenv.sh; env | grep ^MIOS_`) and `mios-resolve --emit-env`, asserting the emitted key set AND every value are identical. proptest auto-shrinks any divergence to a minimal failing mios.toml. Wire it as `cargo test -p mios-resolve --features difftest` and as a SOFT CI step (it needs bash), mirroring `check_resolver_twin_equivalence`'s Linux-only posture.
**Where:** `tools/native/mios-resolve/tests/, tools/lib/userenv.sh, usr/lib/mios/mios_toml.py, tools/check-resolver-twin.py`
**Done When:** the difftest passes over >=256 generated documents with zero divergence, and an intentionally-introduced resolver bug is caught and shrunk to a minimal counterexample; the harness is documented alongside `check-resolver-twin.py`.
**Why:** Hand-written examples only cover the shapes someone thought of; the divergences that ship are the ones nobody imagined, and today nothing generates them.
**Dep:** Golden trycmd/insta snapshot tests for mios-resolve across all layer scenarios

### AGY-1415 -- Route userenv.sh through mios-resolve behind the toggle, keeping the Python heredoc as the degrade-open fallback  (WS-GUP | P2 | L)
**Goal:** E-01 Compiled native tier -- the actual strangler cutover: old and new resolvers coexist behind one SSOT value, provably lossless in both states.
**What+How:** `tools/lib/userenv.sh` (byte-identical twin `usr/lib/mios/userenv.sh`, enforced by flatten check 27) resolves by running an embedded Python heredoc (lines 114-241) that imports `mios_toml` and prints exports. Add a front door in `_mios_load_unified`: when `[migration].use_rust_resolver` is true AND `tools/native/target/release/mios-resolve` exists, eval its `--emit-env` output; otherwise fall through to the existing heredoc unchanged in `${...:-}` degrade-open style. Because both twins must stay byte-identical, apply the edit to `tools/lib/userenv.sh` and re-run the resync so `check_userenv_parity` holds. With the toggle OFF the resolution path must be bit-for-bit what it is today.
**Where:** `tools/lib/userenv.sh, usr/lib/mios/userenv.sh, usr/share/mios/mios.toml, tools/native/mios-resolve/, automation/98-drift-checks.sh`
**Done When:** with `use_rust_resolver=false` the resolved env is byte-identical to today; with `=true` both `just` and `mios-env-snapshot` reproduce `env-baseline.txt`; `check_userenv_parity` stays green and the lossless gate passes in both toggle states.
**Why:** Until the compiled resolver is actually on the path it is dead code -- and a cutover without a toggle means the only rollback for a bad resolve is a revert-and-rebuild while every service reads wrong values.
**Dep:** Differential proptest harness: random-valid mios.toml through both userenv.sh oracle and mios-resolve

### AGY-1416 -- Give mios-resolve miette/thiserror diagnostics that point at the offending mios.toml span  (WS-GUP | P3 | M)
**Goal:** E-01 Compiled native tier -- the operator-facing half of the compiled resolver: config failures become compiler-grade, span-anchored reports.
**What+How:** Adopt the two-layer CLI error standard. Define thiserror enums for internal/library errors (parse failure, unknown section, type mismatch, bad hex in `[colors]`, non-int in `[ports]`) that drift-gates can match programmatically, and wrap them with miette for end-user diagnostics carrying source spans, labels and help text, returning `miette::Result` from `main`. Compute spans from byte offsets rather than char counts so UTF-8 highlights land correctly. A malformed `[colors].bg`, or a string where a port int is required, must print a report pointing AT the line in mios.toml.
**Where:** `tools/native/mios-resolve/src/, usr/share/mios/mios.toml`
**Done When:** feeding mios-resolve a mios.toml with a bad `[ports]` value or malformed `[colors]` hex prints a miette diagnostic with the correct highlighted span; internal errors are typed thiserror variants; `clippy -D warnings` is clean.
**Why:** Today a mistyped SSOT value produces an opaque bash failure string or, worse, a silent empty default -- the operator gets no line number and the gates get no matchable error variant.
**Dep:** Reroute userenv.sh through mios-resolve behind the toggle, keeping the python heredoc as fallback

### AGY-1417 -- Unify the emitted `MIOS_AI_VLLM_*` vs consumed `MIOS_VLLM_*` mismatch through the derivation table  (WS-GUP | P2 | M)
**Goal:** E-10 One canonical name -- closes the named canonical-walk/consumer mismatch so no capability carries a second name (Law 9).
**What+How:** The alias engine emits BOTH the canonical `MIOS_AI_VLLM_*` (from the `ai.vllm.*` walk) and the short `MIOS_VLLM_*` (a `get_aliases` special case, `mios_toml.py:227-232`), while consumers reference the short form -- a value-duplicating pair. In `alias-derivation.tsv` declare the short `MIOS_VLLM_*`/`MIOS_SGLANG_*` as the single canonical key and the long `MIOS_AI_VLLM_*` as an explicit derive alias, or drop it entirely if `referenced_names.txt` shows no consumer. Confirm with `automation/lib/mios_var_closure.py` that the referenced set stays a subset of the emitted set. Repoint straggler consumers (grep `MIOS_AI_VLLM_` across `usr/lib/mios/agent-pipe/server.py` and the vllm Quadlet/unit) to the short canonical, and bump `env-baseline.txt` deliberately if the emitted set legitimately shrinks.
**Where:** `usr/share/mios/reference/alias-derivation.tsv, usr/lib/mios/mios_toml.py, automation/lib/mios_var_closure.py, usr/lib/mios/agent-pipe/server.py, usr/share/mios/reference/env-baseline.txt`
**Done When:** exactly one canonical vllm/sglang key family remains per concept; `check_var_closure` is green; no consumer references a dropped alias; the env-baseline diff is reviewed and intentional.
**Why:** An emitter and a consumer that disagree on a key name is a silent-empty bug class -- the inference lane reads an unset variable and falls back rather than failing.
**Dep:** Make mios_toml.py.get_aliases read the derivation-graph table instead of the hardcoded if/elif ladder

### AGY-1418 -- Expand Rust mios-version-check to subsume the whole check_version_ssot surface, then demote the bash check  (WS-GUP | P2 | L)
**Goal:** E-08 Global Unification Plan -- collapses enforcement of the ~79 version-dupe pairs into one compiled gate so a version is declared once.
**What+How:** `tools/native/mios-version-check/src/main.rs` today compares only `VERSION`, mios.toml `[meta].mios_version` and the single root Containerfile ARG. The authoritative bash `check_version_ssot` (98-drift-checks.sh:3598-~3700) does far more: `os-release` VERSION/VERSION_ID/BUILD_ID/PRETTY_NAME/CPE_NAME, ALL `*Containerfile*` via `git ls-files`, every `tools/native` Cargo.toml version, the root mios.toml, plus a git-ls-files literal scan for stray `v?0.x.y`. Port each of those into the Rust binary (git ls-files or walkdir) emitting the same violation lines, then demote `check_version_ssot` to delegate to the binary when `[migration].use_rust_version_check` is true, keeping the bash body as fallback.
**Where:** `tools/native/mios-version-check/src/main.rs, automation/98-drift-checks.sh, VERSION, usr/lib/os-release, usr/share/mios/mios.toml, tools/native/Cargo.toml`
**Done When:** the Rust binary raises every violation `check_version_ssot` can raise (verified by seeding a mismatch in each surface) and the bash check delegates under the toggle with its own path still green; negatives cover os-release and Cargo.toml drift.
**Why:** The version family is the largest value-dupe cluster in the tree and its only enforcement is a sprawling bash function nobody can safely extend -- so new version surfaces get added ungated.
**Dep:** Add an inert oxidizr-style [migration] toggle table to mios.toml for per-surface Rust/bash rollback

### AGY-1419 -- Generate the globals.{ps1,sh} identity and UID/GID fallback literals from mios.toml instead of authoring them twice  (WS-GUP | P2 | M)
**Goal:** E-12 ZERO-HARDCODES -- removes a hand-maintained literal family from the resolver fallback path so there is a key to float TO.
**What+How:** `automation/lib/globals.ps1` (and its sibling `automation/lib/globals.sh`) hand-maintain `else { N }` fallbacks -- `MIOS_USER='mios'`, `MIOS_UID=1000`, `MIOS_FORGE_UID=816`, `MIOS_CEPH_UID=819`, `MIOS_SEARXNG_UID=818`, `MIOS_HERMES_UID=820`, `MIOS_SUBUID_START=100000` (globals.ps1:33-59) -- duplicating values that belong in mios.toml `[identity]`, `[accounts]` and per-service uid/gid. Add the missing keys to mios.toml, then generate the fallback blocks at build time (extend the existing sync path or add a `mios-sync-globals` codegen step that writes the else-literals from the resolved env) so neither globals file is authored by hand. Extend `check_globals_ports` (98-drift-checks.sh:1829) to gate the uid/gid/user literals the same way it already gates `MIOS_PORT_*` defaults.
**Where:** `automation/lib/globals.ps1, automation/lib/globals.sh, usr/share/mios/mios.toml, automation/98-drift-checks.sh`
**Done When:** every uid/gid/user/subuid literal in globals.{ps1,sh} is generated from mios.toml with no hand-authored `else {N}`; `check_globals_ports` fails a seeded uid drift; the mios.toml change is mirrored into mios-bootstrap.git per Law 15.
**Why:** A service uid changed in mios.toml but not in the two globals fallbacks yields containers whose files are owned by the wrong account on any host where the resolver degrades to defaults.
**Dep:** Reroute userenv.sh through mios-resolve behind the toggle, keeping the python heredoc as fallback

### AGY-1420 -- Collapse the ports triple-emission (`MIOS_PORT_x` / `MIOS_x_PORT` / `MIOS_PORTS_x`) to a canonical+alias minimum  (WS-GUP | P2 | M)
**Goal:** E-09 One value, one name -- removes a 3x name multiplication so each port has a single representation across the namespace.
**What+How:** Each of the 35 `[ports]` keys currently fans out to `MIOS_PORTS_<x>` (the raw walk) plus `MIOS_PORT_<x>` AND `MIOS_<x>_PORT` (get_aliases:423-431) -- three emitted names per port. Using `referenced_names.txt` as the demand signal, choose the single canonical form per port and declare the others as explicit derive aliases in `alias-derivation.tsv`, dropping the unreferenced ones so the emitted count falls. Keep the historical service-canonical rename (`GUACAMOLE_WEB -> GUACAMOLE`) as an exact-key override in the table rather than a special case in code. Prove via `mios_var_closure.py` that referenced remains a subset of emitted, and bump `env-baseline.txt` deliberately for the intentional shrink.
**Where:** `usr/share/mios/reference/alias-derivation.tsv, usr/lib/mios/mios_toml.py, usr/share/mios/referenced_names.txt, automation/lib/mios_var_closure.py, usr/share/mios/reference/env-baseline.txt`
**Done When:** emitted names per port are reduced to the referenced minimum with the remainder declared derive/keep-distinct; `check_var_closure` green; the env-baseline shrink reviewed; `check_container_ports` and `check_no_bare_port_literals` still green.
**Why:** Three names per port means three chances for a consumer to read the one that a future refactor stops emitting, and it inflates the very namespace the GUP is trying to shrink.
**Dep:** Unify the MIOS_AI_VLLM_* (emitted) vs MIOS_VLLM_* (consumed) canonical/short mismatch through the derivation table

### AGY-1421 -- Normalize root paths in mios-env-snapshot so a differing `$ROOT`/`$HOME` can never false-drift the lossless gate  (WS-GUP | P1 | M)
**Goal:** E-08 Global Unification Plan -- makes the lossless invariant hold in CI, which every later namespace-collapse step depends on.
**What+How:** `env-baseline.txt` line 332 bakes a host-specific path -- `MIOS_BOOTSTRAP_REPO_URL=C:/mios-bootstrap`, captured on the Windows dev host -- so `check_resolved_env_lossless` (98-drift-checks.sh:6061) would diff and fail on a Linux runner. Extend `usr/libexec/mios/mios-env-snapshot` (which already pins `HOME=/nonexistent` and `MIOS_ROOT`) to rewrite any absolute repo-root or bootstrap-root prefix in emitted values to a stable placeholder token (`@ROOT@` / `@BOOTSTRAP@`) before the final sort, and mirror the same normalization in the `normalize_path` helper in `usr/lib/mios/userenv.sh:126-133`, which is Windows-drive-only today. Regenerate a root-agnostic `env-baseline.txt` and add a `tests/drift-gate-negatives.sh` case that runs the snapshot from a DIFFERENT `$ROOT` and still matches.
**Where:** `usr/libexec/mios/mios-env-snapshot, usr/lib/mios/userenv.sh, tools/lib/userenv.sh, usr/share/mios/reference/env-baseline.txt, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh`
**Done When:** `env-baseline.txt` contains no host-specific absolute prefixes; `check_resolved_env_lossless` passes from two different `$ROOT`/`$HOME` locations; a negative test proves a genuine value change still fails; both userenv.sh copies stay byte-identical.
**Why:** A baseline embedding its capture host's path is a latent CI-red that will block every future resolver change the first time the gate runs off that machine.
**Dep:** Freeze the resolver output as versioned golden-master fixtures

### AGY-1422 -- Make check_no_duplicate_value_key actually call `_violation` instead of unconditionally passing  (WS-GUP | P1 | M)
**Goal:** E-09 One value, one name -- turns the campaign's keystone fitness function from decorative into enforcing.
**What+How:** `automation/98-drift-checks.sh::check_no_duplicate_value_key` (line 6097) computes value-duplicate groups inside a Python heredoc but only prints `DUPLICATE_VALUES_FOUND:{len(dups)}` and then unconditionally echoes `passed` -- it never calls `_violation`, so it enforces nothing. Rework it to ratchet against an allowlist in `usr/share/mios/reference/value-aliases.tsv` (`canonical<TAB>alias<TAB>disposition`): a group of >=2 keys sharing a value is permitted only when every pair is declared derive/delete/keep-distinct there, and any NEW unallowlisted value-duplicating key raises `_violation`. Keep the existing `EXEMPT_VALUES` (true/false, small ints, canonical ports) and reuse the `mios-env-snapshot` resolution the check already performs.
**Where:** `automation/98-drift-checks.sh, usr/share/mios/reference/value-aliases.tsv, usr/libexec/mios/mios-env-snapshot, tests/drift-gate-negatives.sh`
**Done When:** the check raises `_violation` on an undeclared value-dup and is green on the current tree; a seeded undeclared dup key fails in `tests/drift-gate-negatives.sh`; allowlisted pairs still pass.
**Why:** A gate that can never fail is worse than no gate -- the whole de-duplication campaign is currently reporting success while new duplicate keys land unimpeded.
**Dep:** none

### AGY-1423 -- Compile the forbid-new-value-duplicate rule into mios-ssot-lint reading value-aliases.tsv  (WS-GUP | P2 | M)
**Goal:** E-09 One value, one name -- makes the de-dup fitness function fast, typed and panic-free so it stays permanently enforced.
**What+How:** With `check_no_duplicate_value_key` enforcing (previous slice) and `mios-ssot-lint` (`tools/native/mios-ssot-lint/src/main.rs`, the Rust port of `97-ssot-lint.sh`) already in the native workspace, port the value-duplication rule into the compiled linter: resolve the env (invoke `mios-resolve --emit-env` or read the snapshot), group keys by value, and fail on any >=2-key group not covered by a derive/keep-distinct disposition in `usr/share/mios/reference/value-aliases.tsv`, honoring the same `EXEMPT_VALUES`. Emit stable, matchable violation lines, then have the drift-gate prefer the compiled linter when `[migration]` permits, keeping the bash check as fallback.
**Where:** `tools/native/mios-ssot-lint/src/main.rs, usr/share/mios/reference/value-aliases.tsv, automation/98-drift-checks.sh, tools/native/mios-resolve/`
**Done When:** mios-ssot-lint reproduces `check_no_duplicate_value_key`'s verdicts against seeded dup and allowlisted cases, runs under `clippy -D warnings` with no unwrap/panic, and the gate delegates to it under the toggle.
**Why:** The rule currently lives as a Python heredoc inside bash -- slow, untypeable, untestable in isolation, and one of the surfaces the minimize-loose-scripts directive targets.
**Dep:** Make check_no_duplicate_value_key actually enforce instead of always passing (no-op today)

### AGY-1424 -- Emit a generated minimal key-library from mios-resolve and delete userenv.sh's hand-maintained tuples  (WS-GUP | P2 | L)
**Goal:** E-08 Global Unification Plan -- delivers the auto-derived minimal key library that the ~200 manual userenv.sh tuples were always standing in for.
**What+How:** `userenv.sh` carries plumbing the resolver should own: the `_ssot_lint_ports_dummy` allowlist array (`usr/lib/mios/userenv.sh:312-325`) and the `referenced_names.txt` re-export loop (userenv.sh:231-240). Add a `mios-resolve --emit-key-library` mode producing a typed, deterministic key registry (name + provenance section + derive-vs-canonical flag) from the SSOT plus `alias-derivation.tsv`, written to `usr/share/mios/key-library.generated.tsv`. Repoint `automation/97-ssot-lint.sh` -- which the dummy array feeds -- to read the generated library instead of the inline array, then delete the manual tuple block from both byte-identical userenv.sh twins.
**Where:** `tools/native/mios-resolve/src/, usr/lib/mios/userenv.sh, tools/lib/userenv.sh, usr/share/mios/key-library.generated.tsv, automation/97-ssot-lint.sh`
**Done When:** `key-library.generated.tsv` is produced by mios-resolve and consumed by `97-ssot-lint.sh`; the manual `_ssot_lint_ports_dummy` array is gone from both userenv.sh copies; ssot-lint and var-closure are green.
**Why:** Hand-maintained key tuples go stale the moment a key is added elsewhere, and today the lint's own allowlist is one of them -- so the linter's notion of the namespace silently diverges from the resolver's.
**Dep:** Reroute userenv.sh through mios-resolve behind the toggle, keeping the python heredoc as fallback

### AGY-1425 -- Bake the native SSOT workspace once, deterministically, instead of running cargo inside drift-check bodies  (WS-GUP | P2 | M)
**Goal:** E-05 Test and CI governance -- makes the compiled tooling a digest-stable build artifact with a pinned lockfile as the reproducibility SSOT.
**What+How:** Native binaries are built three different ways today: `Containerfile:49` has a bare `cargo build --release`, `98-drift-checks.sh:4880` runs `cargo build --manifest-path ... -p mios-ssot-lint` on demand inside a check, and `automation/85-bake-plan.sh` merely probes for a prebuilt `mios-bake-plan`. Consolidate into one deterministic step (an automation phase or `just native-build --release`) that compiles the whole workspace with `--locked` and `SOURCE_DATE_EPOCH` keyed off the git commit time, staging `mios-resolve`, `generate-names-registry`, `mios-ssot-lint` and `mios-version-check` to a known path. Check bodies then only EXEC the prebuilt binary and hard-fail when it is missing rather than compiling it themselves. Commit and honor `tools/native/Cargo.lock`.
**Where:** `Containerfile, automation/85-bake-plan.sh, automation/98-drift-checks.sh, Justfile, tools/native/Cargo.lock`
**Done When:** one build step compiles the workspace `--release --locked` with a deterministic epoch; no check body invokes `cargo build`; two independent builds produce identical binary digests for the SSOT crates.
**Why:** Compiling inside a check makes the gate's runtime depend on a toolchain and network state that the bake does not control -- it is slow, non-reproducible, and can pass on one machine while failing on the runner.
**Dep:** Scaffold the mios-resolve crate: a typed figment/serde layered resolver emitting MIOS_* to match env-baseline

### AGY-1426 -- Add cargo-deny + cargo-audit supply-chain drift-gates with Cargo.lock as the SSOT  (WS-GUP | P3 | M)
**Goal:** E-05 Test and CI governance -- gives the Rust plane the dependency-surface gate that bash never needed and Rust cannot ship without.
**What+How:** The native workspace has no dependency gate at all. Add `tools/native/deny.toml` covering banned and duplicate crates, a license allow-list operationalizing the FOSS-upstream posture, and RustSec advisories; then add `check_native_supply_chain` to `automation/98-drift-checks.sh` running `cargo deny --manifest-path tools/native/Cargo.toml check` and `cargo audit --file tools/native/Cargo.lock`, SOFT-skipping when the tools are absent per the established python3/cargo-missing pattern. Commit `tools/native/Cargo.lock` as the reproducibility SSOT -- consistent with SBOM-not-hardcode, dependency digests recorded rather than hand-pinned. Register the gate immediately after the clippy/fmt gate so the native lint chain runs as one block.
**Where:** `tools/native/deny.toml, tools/native/Cargo.lock, automation/98-drift-checks.sh, Justfile`
**Done When:** `cargo deny check` and `cargo audit` pass on the committed lockfile; `check_native_supply_chain` is registered and fails on a seeded banned or advisory-carrying crate; the license allow-list is documented.
**Why:** Every crate added to the workspace pulls a transitive tree nobody reviews -- an advisory or an incompatible license lands in the shipped image with no signal at all.
**Dep:** Add cargo build/test/clippy/fmt Justfile targets and a Rust-plane lint drift-gate (the missing shellcheck-equivalent)

### AGY-1427 -- Fold the two resolver-twin gates into one golden-anchored check_resolver_parity and delete the scaffolding  (WS-GUP | P2 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- retires gates that exist only to police a duplication the compiled resolver removes.
**What+How:** Two overlapping gates exist purely because there are two shell/Python resolver twins: `check_resolver_twin_parity` (98-drift-checks.sh:3132, the `MIOS_AI_*` subset) and `check_resolver_twin_equivalence` (3181, full, via `tools/check-resolver-twin.py`). Once `userenv.sh` routes through mios-resolve under the toggle -- flipped on for a release after the difftest and snapshots are green -- collapse both into a single `check_resolver_parity` asserting `mios-resolve --emit-env == env-baseline.txt` (the golden) and `== ` a live source of `tools/lib/userenv.sh`. Delete `tools/check-resolver-twin.py` and the redundant subset check, updating `drift-gate-index.tsv` ordinals densely because `check_pipeline_numbering` enforces 1..N. Keep the bash heredoc path only as the toggle-off fallback, still covered by the golden.
**Where:** `automation/98-drift-checks.sh, tools/check-resolver-twin.py, usr/share/mios/reference/env-baseline.txt, usr/share/mios/reference/drift-gate-index.tsv, tools/lib/userenv.sh`
**Done When:** one `check_resolver_parity` replaces both twin checks; `check-resolver-twin.py` no longer exists; drift-gate-index ordinals stay dense (`check_pipeline_numbering` green); parity holds in both toggle states.
**Why:** Carrying two twin-equivalence gates past the cutover means every future resolver change must satisfy three overlapping definitions of correctness, and the gate index keeps growing with checks that guard nothing.
**Dep:** Reroute userenv.sh through mios-resolve behind the toggle, keeping the python heredoc as fallback

### AGY-1428 -- Write the compiled-resolver ADR and the toggles-all-off reversibility negative tests (AGY-479..730 lineage)  (WS-GUP | P3 | M)
**Goal:** E-06 Test and documentation harness -- records the decision and proves, by test, that every migration slice degrades open.
**What+How:** Record the compiled continuation of the 6-phase lossless-gated GUP under `usr/share/doc/mios/adr/`, stating the four load-bearing pieces: the derivation-graph SSOT (`alias-derivation.tsv`), the typed figment resolver, the golden/insta/trycmd plus proptest gates, and the `[migration]` toggle as the rollback contract. Add explicit reversibility coverage to `tests/drift-gate-negatives.sh`: with every `[migration]` toggle OFF, the full resolver/version/names path must be byte-identical to pre-migration bash behavior against the golden. Update the ROADMAP WS-DEDUP/WS-GUP entries and mirror the ADR into mios-bootstrap.git per Law 15.
**Where:** `usr/share/doc/mios/adr/, tests/drift-gate-negatives.sh, ROADMAP.md, usr/share/mios/mios.toml`
**Done When:** the ADR is committed in both repos; a toggles-all-off negative test proves bit-for-bit bash-path equivalence to the golden; ROADMAP reflects the compiled continuation; the full drift-gate suite is green in both toggle states.
**Why:** An untested rollback path is an assumption, not a contract -- and an unrecorded migration leaves the next agent unable to tell which resolver is authoritative or why.
**Dep:** Retire the python-vs-bash resolver twin gates once mios-resolve is canonical, folding into one compiled parity gate

### AGY-1429 -- Build the three-set namespace census and an orphan-consumer / dead-key drift-gate  (WS-GUP | P1 | L)
**Goal:** E-08 Global Unification Plan -- produces the ground-truth shrink map without which no key can be safely retired.
**What+How:** `usr/share/mios/names.generated.txt` (503 defined dotted->env pairs), `usr/share/mios/referenced_names.txt` (1181 consumed `MIOS_*` names) and `usr/share/mios/reference/env-baseline.txt` (2475 resolved vars) all exist, but nothing reconciles them. Write a census tool (start as `tools/mios-namespace-census.py`, target `tools/native/mios-ssot-lint`) computing referenced-MINUS-resolved (a consumer reading a `MIOS_*` the resolver never emits -- a silent empty/fallback bug) and resolved-MINUS-referenced (dead keys, the shrink candidates), emitting a committed `usr/share/mios/reference/namespace-census.tsv` classified `{orphan-consumer, dead-key, live}`. Add `check_namespace_census` to `automation/98-drift-checks.sh` that fails on any NEW orphan-consumer and regenerates the census, with a documented allowlist for deliberate degrade-open producers.
**Where:** `usr/share/mios/names.generated.txt, usr/share/mios/referenced_names.txt, usr/share/mios/reference/env-baseline.txt, tools/mios-namespace-census.py, tools/native/mios-ssot-lint/src/main.rs, automation/98-drift-checks.sh`
**Done When:** `namespace-census.tsv` is generated and committed; `check_namespace_census` is registered in `main()` and green; a deliberately planted `MIOS_TYPO_XYZ` read in a `*.service` fails the gate; `cargo test` and `just drift-gate` green.
**Why:** With no way to distinguish a live key from a dead one, every shrink is a guess -- and a consumer referencing an unresolved alias fails silently at runtime instead of at the gate.
**Dep:** Freeze the resolver output as versioned golden-master fixtures before any port (characterization)

### AGY-1430 -- Reconcile the two divergent alias-derivation implementations behind a get_aliases parity gate  (WS-GUP | P1 | M)
**Goal:** E-10 One canonical name -- makes `names.generated.txt` a complete registry so the one-canonical-name check has the full alias surface to police.
**What+How:** `tools/generate-names-registry.py._alias_for` maps only `ai.vllm.*` and `ai.sglang.*`, while `usr/lib/mios/mios_toml.py.get_aliases` maps roughly 30 families (identity, locale, portal, a2a, `ai.*`, `paths.*`, pgvector/pg, `image.sidecars.*`, `ports.*`, ...) -- so `names.generated.txt` silently under-reports the real alias surface. Add `check_alias_derivation_parity` to `automation/98-drift-checks.sh` that imports `get_aliases` and asserts, for every dotted path in the walked SSOT, that the registry generator emits an identical alias set, failing loudly on any family `_alias_for` omits. As the interim fix, make `generate-names-registry.py` delegate to `get_aliases` (or the shared derivation table once it lands) so the registry becomes complete.
**Where:** `tools/generate-names-registry.py, usr/lib/mios/mios_toml.py, usr/share/mios/names.generated.txt, automation/98-drift-checks.sh`
**Done When:** `names.generated.txt` includes the identity/locale/portal/a2a/ai/ports/sidecar aliases it omits today; `check_alias_derivation_parity` is registered and green; the regenerated registry is staged by explicit path.
**Why:** Two hand-maintained alias derivations that disagree mean the registry -- and anything built on it, including the orphan census -- reports a namespace that does not exist.
**Dep:** Extract the get_aliases prefix-family + per-key rules into ONE declarative derivation-graph SSOT table

### AGY-1431 -- Bring `/etc/mios/install.env` under the one resolver with a lossless parity and quoting-safety gate  (WS-GUP | P1 | L)
**Goal:** E-08 Global Unification Plan -- eliminates a third, ungated serialization of the namespace so the lossless invariant covers what services actually read.
**What+How:** `usr/libexec/mios/system-sync-env.sh` generates `/etc/mios/install.env` -- a third namespace serialization consumed by three incompatible parsers (systemd `EnvironmentFile=`, `bash source`, `podman --env-file`) -- yet the only lossless gate, `check_resolved_env_lossless`, snapshots `userenv.sh` exports and never looks at it. Re-derive install.env generation from the same resolver path `mios-env-snapshot` uses, then add `check_install_env_parity` that renders install.env in a clean env and diffs its `KEY=value` set against the resolved `MIOS_*` baseline, accounting for the documented secret-eviction and bare-value quoting rules. Add a quoting-safety invariant asserting no value contains shell metacharacters, whitespace or `$` that would break any of the three parsers.
**Where:** `usr/libexec/mios/system-sync-env.sh, usr/lib/mios/userenv.sh, usr/libexec/mios/mios-env-snapshot, automation/98-drift-checks.sh, usr/share/mios/reference/env-baseline.txt`
**Done When:** install.env is provably a projection of the resolved namespace (parity check green); a planted `$`-bearing or space-bearing value fails the quoting-safety invariant; the check is registered in `main()`.
**Why:** install.env feeds systemd units and podman at first boot -- if the namespace collapses while install.env tracks a stale SSOT, services read values the resolver stopped emitting, and the lossless gate reports green throughout.
**Dep:** Harden mios-env-snapshot root-path normalization so a differing $ROOT/$HOME never false-drifts the lossless gate

### AGY-1432 -- Add a PowerShell env-snapshot leg and a cross-plane parity gate (globals.ps1 vs userenv.sh)  (WS-GUP | P1 | L)
**Goal:** E-03 PowerShell surface -- puts the Windows-plane resolver under the same output-parity discipline as the bash twin.
**What+How:** `mios-env-snapshot` resolves only the bash twin, so `automation/lib/globals.ps1` has no OUTPUT-parity gate and its ~40 hand-written `$script:MIOS_*` defaults (users, UID/GID, ports, images) can drift silently from the resolved SSOT. Add a `usr/libexec/mios/mios-env-snapshot.ps1` sibling that dot-sources `globals.ps1` in a pinned env and emits sorted `MIOS_*` in the identical format, then add `check_ps_env_parity` to `automation/98-drift-checks.sh` diffing the SHARED-key intersection (globals.ps1 is a strict subset) against `env-baseline.txt` values. Guard with a pwsh-availability skip so Linux-only CI degrades open, mirroring the python3-missing pattern already used in `check_names_registry`.
**Where:** `automation/lib/globals.ps1, usr/libexec/mios/mios-env-snapshot, usr/libexec/mios/mios-env-snapshot.ps1, usr/share/mios/reference/env-baseline.txt, automation/98-drift-checks.sh`
**Done When:** `mios-env-snapshot.ps1` emits the same sorted format; `check_ps_env_parity` is green where pwsh is present and skips cleanly where absent; a deliberately drifted `MIOS_FORGE_UID` in globals.ps1 fails the gate.
**Why:** The bash twin has a lossless gate and the PowerShell twin has none, so the entire Windows plane is free to drift on exactly the UID/GID and port keys the campaign is trying to single-source.
**Dep:** Derive globals.ps1 / globals.sh identity+UID/GID literals from mios.toml SSOT via codegen

### AGY-1433 -- Add a namespace bijection / collision invariant gate over the derivation table  (WS-GUP | P2 | M)
**Goal:** E-10 One canonical name -- guarantees key injectivity so no two SSOT facts can collapse onto one env name.
**What+How:** `check_no_duplicate_value_key` targets equal VALUES; nothing asserts KEY injectivity -- that no two distinct dotted paths derive the same `MIOS_*` name and no alias collides with a canonical emitted from a different path. Given `get_aliases`' ~30-family if/elif ladder (`ports.*` emits both `MIOS_PORT_x` and `MIOS_x_PORT`; `image.sidecars.*` emits `_IMAGE` and `_VERSION`), an accidental collision silently makes one path shadow another. Add `check_namespace_bijection` to `automation/98-drift-checks.sh` -- and fold it into `mios-ssot-lint` -- walking the full SSOT, building the `path -> {canonical, aliases}` map, and failing on any env name reachable from more than one distinct path unless explicitly whitelisted as an intentional shared alias.
**Where:** `usr/lib/mios/mios_toml.py, automation/98-drift-checks.sh, tools/native/mios-ssot-lint/src/main.rs, usr/share/mios/mios.toml`
**Done When:** `check_namespace_bijection` is registered and green on the current tree; a planted colliding alias fails it; intentional shared aliases (e.g. `MIOS_HOSTNAME` from `identity.hostname` vs `user.hostname`) live in an explicit allowlist rather than being tolerated silently.
**Why:** A two-paths-to-one-name collision is a data-loss bug -- one operator setting overwrites another with no error -- and the value-duplicate check cannot see it.
**Dep:** Make mios_toml.py.get_aliases read the derivation-graph table instead of the hardcoded if/elif ladder

### AGY-1434 -- Census and collapse the ~79 version-dupe pairs rooted in `image.sidecars.*` to one SBOM-derived source  (WS-GUP | P2 | L)
**Goal:** E-15 SBOM and supply-chain hardening -- enforces ADR-0003, that a resolved version is build-recorded SBOM data and never a hand-authored SSOT literal.
**What+How:** The `get_aliases` `image.sidecars.*` branch emits `MIOS_{name}_IMAGE` plus `MIOS_{name}_VERSION` per sidecar which, combined with per-component version keys elsewhere, is where the ~79 version-dupe pairs live -- distinct from the `meta.mios_version` parity that `mios-version-check` covers. Enumerate every `MIOS_*_VERSION` in `env-baseline.txt`, group equal values, and collapse each family to one canonical resolved from the SBOM/baked manifest (float `:latest` intent, record the digest at build) instead of duplicated literals in `[image.sidecars]`. Add `check_version_family_collapse` asserting no two version keys carry the same hand-authored literal that should instead derive from a single SBOM source.
**Where:** `usr/lib/mios/mios_toml.py, usr/share/mios/mios.toml, usr/share/mios/reference/env-baseline.txt, automation/98-drift-checks.sh`
**Done When:** the version-family census is committed; duplicated per-sidecar version literals derive from one SBOM-recorded source; `check_version_family_collapse` is green; the full drift-gate is green.
**Why:** Nothing today collapses the per-component version family -- `mios-version-check` only validates the single top-level `mios_version` -- so a sidecar bump must be hand-edited in several places and any missed copy pins an image to a stale version.
**Dep:** Expand Rust mios-version-check to subsume the full check_version_ssot surface, then demote the bash check

### AGY-1435 -- Replace the MIOS_ENV_BASELINE_BUMP=1 blanket whitewash with a reviewed baseline-regen tool + an added/removed changes manifest  (WS-GUP | P2 | M)
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- makes the losslessness invariant that guards every collapse step auditable instead of overridable by one env var.
**What+How:** `check_resolved_env_lossless` in `automation/98-drift-checks.sh` today honors `MIOS_ENV_BASELINE_BUMP=1` as a blanket override that blesses ANY change to `env-baseline.txt`, so an unintended namespace mutation passes unexamined. Add `tools/mios-baseline-update.py` plus a `just baseline-regen` recipe that regenerates `usr/share/mios/reference/env-baseline.txt` AND emits a machine-readable `usr/share/mios/reference/env-baseline.changes.tsv` enumerating the added / removed / renamed keys for that commit. Then rewrite the gate so a bump is accepted only when the committed changes manifest matches the actual computed diff of the baseline -- turning the escape hatch into a reviewed artifact.
**Where:** `automation/98-drift-checks.sh, usr/share/mios/reference/env-baseline.txt, tools/mios-baseline-update.py, usr/share/mios/reference/env-baseline.changes.tsv, Justfile`
**Done When:** `env-baseline.txt` can only be regenerated through `just baseline-regen`; a bump whose `env-baseline.changes.tsv` disagrees with the real added/removed set FAILS `check_resolved_env_lossless`; the full drift-gate is green.
**Why:** A lossless gate whose only bypass is an unaudited env var is not lossless. Mid-collapse, an accidentally dropped key ships silently and the regression surfaces later as a missing runtime value with no commit to bisect to.
**Dep:** Freeze the resolver output as versioned golden-master fixtures before any port (characterization)

### AGY-1436 -- Enforce LF/no-BOM/trailing-newline determinism for the Rust resolver, golden fixtures and env-baseline on a Windows checkout  (WS-GUP | P2 | M)
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- keeps the byte-exact lossless diff trustworthy on the cross-platform checkout the resolver is actually developed in.
**What+How:** `mios-env-snapshot` already needed an `LC_ALL=C` fix to stop locale-driven sort drift; the same determinism hazard hits the compiled resolver and its golden fixtures on the `C:\MiOS` Windows checkout, where `core.autocrlf` rewrites LF to CRLF and editors prepend a BOM, false-drifting the lossless and golden-master gates. Add a `.gitattributes` pinning `eol=lf` (and `-text` binary-safety where appropriate) over `usr/share/mios/reference/*.txt`, `usr/share/mios/names.generated.txt`, the `tools/native/**/tests` fixture trees and `mios.toml`; add a normalization assertion (no CRLF, no BOM, file ends in a newline) to the census and lossless checks; and make the file reads in `mios-ssot-walk` / `mios-resolve` strip CRLF and a leading BOM on ingest so a mixed checkout can never change resolved output.
**Where:** `.gitattributes, usr/share/mios/reference/env-baseline.txt, usr/share/mios/names.generated.txt, tools/native/mios-ssot-walk/src/lib.rs, tools/native/mios-ssot-lint/src/main.rs, automation/98-drift-checks.sh`
**Done When:** a deliberately CRLF/BOM-injected fixture is normalized on ingest and produces unchanged resolved output; a CRLF-committed baseline FAILS the new normalization assertion; the drift-gate is green on both an LF checkout and a CRLF checkout.
**Why:** The resolver is explicitly cross-platform and this very tree lives on Windows; a compiled resolver reading files byte-for-byte will false-drift on CRLF/BOM, and the bash tools only ever escaped this partially via `LC_ALL=C`.
**Dep:** Scaffold the mios-resolve crate: a typed figment/serde layered resolver emitting MIOS_* to match env-baseline

### AGY-1437 -- Law-15: mirror the derivation-table SSOT into mios-bootstrap.git and audit its hardcoded namespace literals  (WS-GUP | P2 | M)
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- extends the one-value-derived rule to the one repo every other GUP task ignores.
**What+How:** `mios-bootstrap.git` (`bootstrap.ps1` / `bootstrap.sh`) ships no resolver copy, yet it is a Law-15 shared surface that can hardcode ports, image names, users and `MIOS_*` keys the SSOT owns. Grep both entrypoints for `MIOS_*` and for literal ports/hosts/image refs; route each through the SSOT-derived set using the `${VAR:-LIT}` degrade-open form that retains the literal as a fallback; and where the derivation-graph table / names registry is genuinely shared, mirror it into `mios-bootstrap.git` so both repos stay byte-synced. Add a cross-repo parity note or test asserting the shared tokens match `mios.git`.
**Where:** `mios-bootstrap/bootstrap.ps1, mios-bootstrap/bootstrap.sh, usr/share/mios/names.generated.txt, usr/share/mios/mios.toml`
**Done When:** no un-fallback-guarded namespace literal remains in `bootstrap.ps1` or `bootstrap.sh`; the shared SSOT surface is byte-identical across `mios.git` and `mios-bootstrap.git`; every commit stages explicit paths only (never `git add -A`).
**Why:** Law 15 requires shared SSOT surfaces to be verified and mirrored in BOTH repos; the namespace collapse is exactly such a surface, so bootstrap silently keeps serving retired spellings after the waves land.
**Dep:** Extract the get_aliases prefix-family + per-key rules into ONE declarative derivation-graph SSOT table

### AGY-1438 -- Execute the staged, lossless-gated alias RETIREMENT waves that actually shrink the 2523-key namespace  (WS-GUP | P2 | XL)
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- this is the demolition step that turns the GUP machinery into a measurably smaller namespace.
**What+How:** Every other GUP task builds machinery (derivation table, resolver, census, gates) but none REMOVE keys, so 2523 never drops. Drive N waves off the dead-key column of `usr/share/mios/reference/namespace-census.tsv`: per wave, delete one batch of resolved-but-never-referenced aliases from the derivation table in `usr/lib/mios/mios_toml.py`, regenerate `env-baseline.txt` and `names.generated.txt` through the reviewed baseline-regen tool, and prove the ONLY diff is the intended removals (lossless gate plus census both green). Keep `${VAR:-lit}` degrade-open fallbacks for producers, stage explicit paths, and land each wave as its own reversible commit.
**Where:** `usr/lib/mios/mios_toml.py, usr/share/mios/mios.toml, usr/share/mios/names.generated.txt, usr/share/mios/reference/env-baseline.txt, usr/share/mios/reference/namespace-census.tsv`
**Done When:** the namespace key count measurably drops wave over wave; each wave's baseline diff equals exactly its intended removals; the lossless, census and bijection gates are green after every wave; each wave reverts independently.
**Why:** Without a gated demolition campaign the GUP ships tooling and the duplication the operator flagged stays on disk -- 2523 keys of hand-maintained surface that every new consumer keeps binding to.
**Dep:** Build the three-set namespace reconciliation census + orphan / dead-key drift-gate

### AGY-1439 -- Map and guardrail the ~738 direct os.environ['MIOS_*'] short-alias reads in the Python consumers  (WS-GUP | P3 | L)
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- makes alias retirement provably safe by knowing which aliases Python is load-bearing on.
**What+How:** Roughly 738 `os.environ[...]` / `os.environ.get('MIOS_*')` reads live under `usr/lib/mios` and `usr/libexec/mios`; each hardwires a consumer to one alias spelling, so retiring that alias fails silently (empty default, not an error). Using `namespace-census.tsv`, produce a consumer map of which retire-candidate aliases are actually read from Python, mark those aliases blocked from retirement until migrated, and move the highest-risk direct reads onto a single typed accessor (`mios_toml.env('dotted.path')` or a `get_env` helper) that resolves through the canonical name. Add a drift-check lint flagging any NEW raw `os.environ['MIOS_*']` read of a non-canonical alias name.
**Where:** `usr/lib/mios/mios_toml.py, usr/lib/mios/**/*.py, usr/libexec/mios/*, usr/share/mios/reference/namespace-census.tsv, automation/98-drift-checks.sh`
**Done When:** the consumer map cross-references the census retire-candidate column; the highest-risk direct alias reads resolve through the typed accessor; a newly introduced raw alias read is flagged by the lint; drift-gate green.
**Why:** 738 alias-bound reads are the tripwire that turns a safe-looking retirement wave into a runtime regression that presents as an empty config value rather than a crash.
**Dep:** Build the three-set namespace reconciliation census + orphan / dead-key drift-gate

### AGY-1440 -- Generate naming-unification.md FROM the derivation table so the human-facing key registry stops being hand-maintained  (WS-GUP | P3 | S)
**Goal:** E-08 Global Unification Plan: collapse the 2523-key MIOS_* namespace to one-value-derived -- removes the last place the namespace is described by hand instead of projected.
**What+How:** `usr/share/doc/mios/reference/naming-unification.md` is a hand-maintained namespace document that `tools/generate-names-registry.py` already special-cases as a non-consumer `emitter_suffix`, meaning it can drift from the real derivation with nothing catching it. Render it (and any other human-facing key index) directly from the derivation-graph table as a generation step, add a `--check` mode to the generator, and add a drift-check asserting the committed doc equals a fresh render -- the same SSOT-projection pattern already used for the theme and dotfiles surfaces.
**Where:** `usr/share/doc/mios/reference/naming-unification.md, tools/generate-names-registry.py, usr/share/mios/names.generated.txt, automation/98-drift-checks.sh`
**Done When:** `naming-unification.md` is produced by the generator and `--check` exists; a hand-edit to the doc FAILS the new drift-check; the regenerated doc is staged by explicit path; drift-gate green.
**Why:** A naming doc the registry generator explicitly ignores is a guaranteed divergence surface -- agents and operators read it as authoritative while the real derivation has already moved.
**Dep:** Extract the get_aliases prefix-family + per-key rules into ONE declarative derivation-graph SSOT table

### AGY-1441 -- Anchor the mios-hardcode-lint localhost:80 / :443 allowlist patterns so they stop masking the whole :80xx range  (WS-ZEROHC | P1 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- repairs the lint bug that silently disarms the NO-HARDCODE gate, making the entire campaign enforced rather than advisory.
**What+How:** In `usr/share/mios/mios.toml` `[security.nohc_allowlist].exempt_patterns` (lines 842-861) the entries `localhost:80`, `localhost:443`, `127\.0\.0\.1:80` and `127\.0\.0\.1:443` are consumed as UNANCHORED `re.search` patterns by `mios-hardcode-lint` (lines 176-182 and 221-227 iterate `exempt_rx` and set `is_line_exempt` on the whole line). Because `localhost:8090` / `8091` / `8033` / `8053` / `8080` all begin with the literal `localhost:80`, any line carrying those ports is spuriously exempted and its real hardcode goes unflagged. Anchor each with a non-digit lookahead: `localhost:80(?![0-9])`, `localhost:443(?![0-9])`, `127\.0\.0\.1:80(?![0-9])`, `127\.0\.0\.1:443(?![0-9])`. This is a shared SSOT surface, so mirror the edit in BOTH `usr/share/mios/mios.toml` and `C:/mios-bootstrap/mios.toml` (Law 15). Land it COUPLED with the float edits it newly exposes so the gate goes green in the same PR rather than red on merge.
**Where:** `usr/share/mios/mios.toml (lines 842-861), C:/mios-bootstrap/mios.toml, usr/libexec/mios/mios-hardcode-lint`
**Done When:** `exempt_patterns` are anchored in both repos' `mios.toml`; running `mios-hardcode-lint` over a fixture line containing `http://localhost:8090` now REPORTS the `:8090` hardcode where it previously PASSed; the coupled float edits keep `check_no_hardcode` in `automation/98-drift-checks.sh` green.
**Why:** This one substring bug exempts every published `:80xx` port (cockpit, cockpit_link, open_webui, adguard_ui, guacamole) from the NO-HARDCODE gate -- the gate reports green today while the violations it exists to catch sit in the tree.
**Dep:** none

### AGY-1442 -- Extend mios-hardcode-lint's _CODE_EXT to systemd units so usr/lib/systemd/system stops being an unscanned blind spot  (WS-ZEROHC | P1 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- makes the gate-red unit and quadlet trees visible to the lint so every float below is verifiable.
**What+How:** `_CODE_EXT` in `usr/libexec/mios/mios-hardcode-lint` (line 25) is `('.py','.sh','.bash','.toml','.yml','.yaml','.ps1','.psm1')` -- it omits `.service`, `.socket`, `.container`, `.timer` and `.conf`, so `usr/lib/systemd/system/**` and `usr/share/containers/systemd/**` are never port/IP-scanned despite carrying dozens of literals (`cockpit.socket.d/listen.conf` `ListenStream=0.0.0.0:8090`; `hermes-worker.service` `:8888`/`:11235`/`:3002`/`:11441`/`:9223`; `mios-account-sync.service` `127.0.0.1:8432`; `mios-agents.service` `0.0.0.0:8800`). Add the unit and quadlet extensions to `_CODE_EXT` and route them through the existing generic `_check_ports_ips` path (line 212), which is already `#`/`;`/`//`-comment aware; treat `Environment=`, `ExecStart=`, `ListenStream=` and `--bind` values as code. Ship a golden fixture (a synthetic unit with one hardcoded and one `${VAR}`-floated port) proving the scan before wiring it. Keep the `${MIOS_*:-lit}` form exempt, and mirror the `_CODE_EXT` change into the Rust port's scope so the two implementations stay identical.
**Where:** `usr/libexec/mios/mios-hardcode-lint (line 25 _CODE_EXT, line 212 _check_ports_ips), usr/lib/systemd/system/, usr/share/containers/systemd/`
**Done When:** the lint scans `.service`/`.socket`/`.container`/`.timer`/`.conf`; a fixture unit with a bare `ListenStream=0.0.0.0:8090` is REPORTED while `${MIOS_NETWORK_BIND_ALL}:${MIOS_PORT_COCKPIT}` PASSes; `check_no_hardcode` exercises the unit tree.
**Why:** The operator named `usr/lib/systemd/system/**` as gate-red, but the lint literally cannot see it -- every unit float in this workstream would land unverified and could silently regress.
**Dep:** none

### AGY-1443 -- Add the [network] SSOT keys (loopback / bind_all / win_tailscale_ip) and derive MIOS_PG_BIND_ADDR from them  (WS-ZEROHC | P1 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- creates the SSOT home the IP literals must float TO, so the campaign has a key rather than an exception.
**What+How:** Add to the existing `[network]` section (`usr/share/mios/mios.toml` line 298): `loopback = "127.0.0.1"`, `bind_all = "0.0.0.0"`, `win_tailscale_ip = "100.79.3.50"`. The canonical walk (`tools/lib/userenv.sh` lines 191-207 via `mios_toml.walk`) auto-emits `MIOS_NETWORK_LOOPBACK` / `MIOS_NETWORK_BIND_ALL` / `MIOS_NETWORK_WIN_TAILSCALE_IP`. Then replace the two hardcoded address literals in the `MIOS_PG_BIND_ADDR` derivation (`tools/lib/userenv.sh` lines 258-261 AND its deployed twin `usr/lib/mios/userenv.sh` lines 258-261) with `${MIOS_NETWORK_BIND_ALL:-0.0.0.0}` / `${MIOS_NETWORK_LOOPBACK:-127.0.0.1}`, preserving the degrade-open fallback. Mirror the `[network]` additions into BOTH `usr/share/mios/mios.toml` and `C:/mios-bootstrap/mios.toml` per Law 15.
**Where:** `usr/share/mios/mios.toml (line 298 [network]), C:/mios-bootstrap/mios.toml, tools/lib/userenv.sh (lines 258-261), usr/lib/mios/userenv.sh (lines 258-261)`
**Done When:** `MIOS_NETWORK_LOOPBACK`/`BIND_ALL`/`WIN_TAILSCALE_IP` resolve in a userenv dump; `MIOS_PG_BIND_ADDR` still emits `127.0.0.1` by default and `0.0.0.0` when `listen_loopback=false`, now sourced from SSOT; the twin-equivalence drift-check is green; both repos' `mios.toml` carry the keys.
**Why:** Loopback, bind-all and the tailnet IP are the most-repeated raw literals across the unit and quadlet trees; until they have one SSOT home, every IP-float task below has nowhere legitimate to point.
**Dep:** none

### AGY-1444 -- Add the [ports] internal false-friend keys (cockpit/searxng/crawl4ai/firecrawl internal, igpu, win_llm_light)  (WS-ZEROHC | P1 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- registers the genuinely-missing SSOT port keys so floating cannot collapse an internal port into its published sibling.
**What+How:** In `[ports]` (`usr/share/mios/mios.toml` line 7915) add `cockpit_internal = 9090`, `searxng_internal = 8888`, `crawl4ai_internal = 11235`, `firecrawl_internal = 3002`, `igpu = 11436`, `win_llm_light = 11450`, each with a comment stating it is the CONTAINER/HOST-internal or Windows-lane port and is DISTINCT from its published sibling (`searxng=8899` line 7922, `crawl4ai=8235` line 7923, `firecrawl=8302` line 7924, `llm_light=8450` line 7937). The canonical walk auto-emits `MIOS_PORT_COCKPIT_INTERNAL` / `SEARXNG_INTERNAL` / `CRAWL4AI_INTERNAL` / `FIRECRAWL_INTERNAL` / `IGPU` / `WIN_LLM_LIGHT`. Extend the `_ssot_lint_ports_dummy` whitelist (`tools/lib/userenv.sh` lines 312-325 AND the `usr/lib/mios/userenv.sh` twin) with the six new names so `97-ssot-lint` stays green. Mirror the `[ports]` additions in BOTH repos' `mios.toml` (Law 15). Do NOT touch published-port values.
**Where:** `usr/share/mios/mios.toml (line 7915 [ports]), C:/mios-bootstrap/mios.toml, tools/lib/userenv.sh (lines 312-325), usr/lib/mios/userenv.sh`
**Done When:** `tomllib` parses both `mios.toml` files; `MIOS_PORT_SEARXNG_INTERNAL=8888` and its five siblings resolve; `_ssot_lint_ports_dummy` lists all six; `automation/97-ssot-lint.sh` is green; the published sibling values are unchanged.
**Why:** Internal is not published. Without dedicated keys the cluster-float tasks would rewrite `3002` as `8302` (and `11235` as `8235`), silently breaking the hermes-worker retrieval path in a way only a live crawl would reveal.
**Dep:** none

### AGY-1445 -- Register the new port/network vars in the 34-render-quadlets envsubst + bash-fallback allowlists  (WS-ZEROHC | P1 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- supplies the build-time substitution plumbing without which every unit-level float is inert text.
**What+How:** `automation/34-render-quadlets.sh` is the build-time renderer that substitutes `${MIOS_*}` placeholders across `/usr/share/containers/systemd`, `/usr/lib/systemd/system` and `/usr/lib/systemd/system/cockpit.socket.d` (dir list lines 46-53). A placeholder resolves only if it appears in BOTH the envsubst allowlist string (line 76) AND the `_render_with_bash` var loop (line ~90). Add `MIOS_PORT_COCKPIT_INTERNAL`, `MIOS_PORT_SEARXNG_INTERNAL`, `MIOS_PORT_CRAWL4AI_INTERNAL`, `MIOS_PORT_FIRECRAWL_INTERNAL`, `MIOS_PORT_IGPU`, `MIOS_PORT_WIN_LLM_LIGHT`, `MIOS_NETWORK_LOOPBACK` and `MIOS_NETWORK_BIND_ALL` to both. This is build tooling, not a dual-repo SSOT surface, so it lives only in `mios.git` -- but it is what makes floating feasible at all, since systemd does NOT expand `${VAR}` in `Environment=`/`ListenStream=` at runtime (see the comment in `cockpit.socket.d/listen.conf` lines 33-38).
**Where:** `automation/34-render-quadlets.sh (line 76 envsubst allowlist, line ~90 _render_with_bash var loop)`
**Done When:** a unit shipping `${MIOS_PORT_SEARXNG_INTERNAL:-8888}` renders to `8888` after the phase runs; the bash-fallback path (envsubst absent) produces identical output; the drift-gate render checks are green.
**Why:** Every unit float in this workstream is a no-op until its placeholder is allowlisted -- the rendered image would ship a literal `${MIOS_PORT_...}` string into a `ListenStream=` and the service would fail to start.
**Dep:** Add the [ports] internal false-friend keys (cockpit/searxng/crawl4ai/firecrawl internal, igpu, win_llm_light)

### AGY-1446 -- Float the hermes-worker.service retrieval-endpoint cluster (searxng / crawl4ai / firecrawl / worker-port literals)  (WS-ZEROHC | P2 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- clears the densest single-file literal cluster in the gate-red zone and proves the internal-key scheme end to end.
**What+How:** `usr/lib/systemd/system/hermes-worker.service` hardcodes its retrieval and bind ports: line 44 `SEARXNG_URL=http://localhost:8888`, line 45 `MIOS_CRAWL_SERVICE_URL=http://127.0.0.1:11235`, line 46 `FIRECRAWL_API_URL=http://127.0.0.1:3002`, lines 48/49 `PORT=8643` / `API_SERVER_PORT=8643`. Float to `http://localhost:${MIOS_PORT_SEARXNG_INTERNAL:-8888}`, `http://${MIOS_NETWORK_LOOPBACK:-127.0.0.1}:${MIOS_PORT_CRAWL4AI_INTERNAL:-11235}`, `http://${MIOS_NETWORK_LOOPBACK:-127.0.0.1}:${MIOS_PORT_FIRECRAWL_INTERNAL:-3002}` and `${MIOS_PORT_HERMES_WORKER:-8643}` (`hermes_worker=8643` already exists at `[ports]` line 7926), keeping each literal as the `:-` degrade-open fallback. `34-render-quadlets` substitutes these at build; verify the vars are in its allowlist and confirm this unit is NOT in the nohc `exempt_files` so the extended unit-scan actually covers it.
**Where:** `usr/lib/systemd/system/hermes-worker.service (lines 44-46, 48-49)`
**Done When:** the rendered `hermes-worker.service` resolves to the identical byte values (8888/11235/3002/8643) as today; `mios-hardcode-lint` reports no port literal in the file; the unit still parses under systemd; `check_no_hardcode` is green.
**Why:** This file is the exact false-friend trap -- its `:8888`/`:3002`/`:11235` are INTERNAL siblings, not the published `:8899`/`:8302`/`:8235` -- so leaving it hand-numbered is both the biggest remaining literal cluster and the most likely place a careless float breaks retrieval.
**Dep:** Register the new port/network vars in the 34-render-quadlets envsubst + bash-fallback allowlists

### AGY-1447 -- Float the hermes-worker/llm-heavy backend + browser-CDP ports and register their missing SSOT keys  (WS-ZEROHC | P2 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- adds the SSOT keys the operator's representative six did not cover, so the gate-red cluster is fully literal-free.
**What+How:** Two backend/browser literals sit outside the six internal keys and need new `[ports]` entries: `HERMES_BACKEND_BASE_URL=http://localhost:11441` (`hermes-worker.service` line 53, also `mios-llm-heavy.container` labels lines 32-33) and `BROWSER_CDP_URL=http://localhost:9223` (`hermes-worker.service` line 62), plus the primary CDP `:9222` (`mios-webtools-crawl4ai.container` line 10 `MIOS_CRAWL_CDP_URL`). Add `[ports]` `llm_heavy = 11441`, `browser_cdp = 9222`, `browser_cdp_worker = 9223` at `usr/share/mios/mios.toml` line 7915 (mirrored into `C:/mios-bootstrap/mios.toml` per Law 15), register `MIOS_PORT_LLM_HEAVY`/`BROWSER_CDP`/`BROWSER_CDP_WORKER` in the userenv `_ssot_lint_ports_dummy` list and the `34-render-quadlets` allowlist, then float the three consumer lines to `${MIOS_PORT_*:-lit}` keeping the numeric literal as fallback.
**Where:** `usr/lib/systemd/system/hermes-worker.service (lines 53,62), usr/share/containers/systemd/mios-llm-heavy.container (lines 32-33), usr/share/containers/systemd/mios-webtools-crawl4ai.container (line 10), usr/share/mios/mios.toml (line 7915), C:/mios-bootstrap/mios.toml`
**Done When:** `11441`/`9222`/`9223` exist as `[ports]` keys in both repos; the three consumers render to identical byte values; `mios-hardcode-lint` reports zero port literals on those lines; drift-gate green.
**Why:** The heavy-lane backend and the dual CDP browser ports are live consumer literals in the same gate-red files; leaving them means `check_no_hardcode` stays red after the rest of the cluster is floated.
**Dep:** Register the new port/network vars in the 34-render-quadlets envsubst + bash-fallback allowlists

### AGY-1448 -- Float the cockpit socket-render drop-ins (ListenStream 0.0.0.0:8090 and 0.0.0.0:8091)  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- kills the "ListenStream cannot take a variable" excuse that shields a whole class of socket units.
**What+How:** `cockpit.socket.d/listen.conf` line 15 (`ListenStream=0.0.0.0:8090`) and `mios-cockpit-link.socket` line 8 (`ListenStream=0.0.0.0:8091`) carry raw bind literals. systemd does not expand `${VAR}` in `ListenStream` at runtime (documented in the listen.conf comment lines 33-38), but `automation/34-render-quadlets.sh` already renders both `cockpit.socket.d` (dir list line 52) and `/usr/lib/systemd/system` (line 53) at BUILD time -- so float to `ListenStream=${MIOS_NETWORK_BIND_ALL:-0.0.0.0}:${MIOS_PORT_COCKPIT:-8090}` and `...:${MIOS_PORT_COCKPIT_LINK:-8091}`. Rewrite the listen.conf comment to say "bake-time envsubst, not runtime" so no stale note claims floating is impossible. Verify `MIOS_NETWORK_BIND_ALL` and `MIOS_PORT_COCKPIT`/`COCKPIT_LINK` are in the 34-render allowlist.
**Where:** `usr/lib/systemd/system/cockpit.socket.d/listen.conf (line 15), usr/lib/systemd/system/mios-cockpit-link.socket (line 8)`
**Done When:** the post-render `listen.conf` and socket contain literal `0.0.0.0:8090` / `0.0.0.0:8091`, byte-identical to today; `mios-hardcode-lint`'s unit scan reports no literal on the placeholder lines; cockpit still binds on boot.
**Why:** These two sockets are the canonical cited reason floating "can't be done" for socket units; while the myth stands, every future socket ships hand-numbered and the gate carries a permanent exemption.
**Dep:** Add the [network] SSOT keys (loopback/bind_all/win_tailscale_ip) and derive MIOS_PG_BIND_ADDR from them

### AGY-1449 -- Float mios-cockpit-link.service's 127.0.0.1:9090 proxy target to loopback + cockpit_internal  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- enforces the internal-vs-published disambiguation the new `[ports]` keys exist for.
**What+How:** `usr/lib/systemd/system/mios-cockpit-link.service` line 10 hardcodes `ExecStart=/usr/lib/systemd/systemd-socket-proxyd 127.0.0.1:9090` -- the host Cockpit INTERNAL listener (9090), distinct from the published `cockpit=8090`. Float to `${MIOS_NETWORK_LOOPBACK:-127.0.0.1}:${MIOS_PORT_COCKPIT_INTERNAL:-9090}`, keeping the literal as the `:-` fallback. This is an `ExecStart` on a rendered unit, substituted by `34-render-quadlets` at build time; confirm both vars are in that allowlist. Also update the AI-hint header comment (line 25 references `localhost:9090`) to timeless phrasing naming the SSOT key so the comment stays accurate.
**Where:** `usr/lib/systemd/system/mios-cockpit-link.service (line 10)`
**Done When:** the rendered unit resolves to `127.0.0.1:9090` byte-identical; `mios-hardcode-lint` reports no literal; the cockpit-link proxy still forwards to host cockpit; drift-gate green.
**Why:** 9090 is the precise `cockpit_internal` false friend (internal 9090 vs published 8090); left as a literal, a future port renumber conflates the two and the proxy silently points at the wrong listener.
**Dep:** Add the [ports] internal false-friend keys (cockpit/searxng/crawl4ai/firecrawl internal, igpu, win_llm_light)

### AGY-1450 -- Float the pg_isready loopback+8432 cluster across the four Postgres-touching units  (WS-ZEROHC | P2 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- collapses one derived value copy-pasted into four units back to a single SSOT source.
**What+How:** Four units hardcode `-h 127.0.0.1 -p 8432` for pgvector health and backup: `mios-account-sync.service` line 16, `mios-sys-env-refresh.service` line 36, `mios-userdb-render.service` line 13 (all `podman exec ... pg_isready -q -h 127.0.0.1 -p 8432`) and `mios-pgvector-backup.service` line 77 (`pg_dump -h 127.0.0.1`). Float the host to `${MIOS_NETWORK_LOOPBACK:-127.0.0.1}` and the port to `${MIOS_PORT_PGVECTOR:-8432}` (`pgvector=8432` already exists at `[ports]` line 7936 and `MIOS_PORT_PGVECTOR` is already in the 34-render allowlist). `mios-pgvector-backup.service` line 77 already floats its port via `$$PORT` -- align that `$$PORT` to source from `MIOS_PORT_PGVECTOR` and float only its `-h` literal. Keep numeric and literal fallbacks; these are `ExecStartPre`/`ExecStart` lines on rendered units, so 34-render substitutes at build.
**Where:** `usr/lib/systemd/system/mios-account-sync.service (line 16), usr/lib/systemd/system/mios-sys-env-refresh.service (line 36), usr/lib/systemd/system/mios-userdb-render.service (line 13), usr/lib/systemd/system/mios-pgvector-backup.service (line 77)`
**Done When:** all four units render to `127.0.0.1` / `8432` byte-identical; `mios-hardcode-lint` reports zero port/IP literals across them; `pg_isready` still passes on boot; drift-gate green.
**Why:** pgvector is the sole agent datastore and its address is authored four times; moving it once means editing four files and hoping, which is exactly the drift class the campaign exists to remove.
**Dep:** Add the [network] SSOT keys (loopback/bind_all/win_tailscale_ip) and derive MIOS_PG_BIND_ADDR from them

### AGY-1451 -- Float mios-agents.service's code-server bind 0.0.0.0:8800  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- exercises the new `bind_all` key on a published listener in an execution path.
**What+How:** `usr/lib/systemd/system/mios-agents.service` line 47 hardcodes `--bind-addr 0.0.0.0:8800 /mnt/mios-root`. Float to `--bind-addr ${MIOS_NETWORK_BIND_ALL:-0.0.0.0}:${MIOS_PORT_CODE_SERVER:-8800}` (`code_server=8800` already at `[ports]` line 7931; `MIOS_PORT_CODE_SERVER` is already in the 34-render allowlist), keeping the literal fallback. The unit already carries `EnvironmentFile=-/etc/mios/install.env` (line 19) and lives in the rendered `/usr/lib/systemd/system` set, so build-time substitution applies; verify `MIOS_NETWORK_BIND_ALL` is in the 34-render allowlist (added by the network-keys task).
**Where:** `usr/lib/systemd/system/mios-agents.service (line 47)`
**Done When:** the rendered unit resolves to `0.0.0.0:8800` byte-identical; `mios-hardcode-lint` reports no literal; code-server still binds; drift-gate green.
**Why:** A bind-all plus published-port literal on a live `ExecStart` is exactly the gate-red class; it also leaves the operator unable to re-scope the code-server listener from the one config surface.
**Dep:** Register the new port/network vars in the 34-render-quadlets envsubst + bash-fallback allowlists

### AGY-1452 -- Float the remaining loopback-host literals in hermes-dashboard.service and mios-opencode-gateway.service  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- finishes the loopback consolidation onto one shared `MIOS_NETWORK_LOOPBACK` key.
**What+How:** `usr/lib/systemd/system/hermes-dashboard.service` line 53 passes `--host 127.0.0.1` and `usr/lib/systemd/system/mios-opencode-gateway.service` line 51 sets `Environment=MIOS_OPENCODE_HOST=127.0.0.1`. Float both to `${MIOS_NETWORK_LOOPBACK:-127.0.0.1}` with the literal retained as fallback. Both are rendered units (34-render covers `/usr/lib/systemd/system`); confirm `MIOS_NETWORK_LOOPBACK` is in that allowlist. For the opencode `Environment=` line, note the value is substituted only at BUILD render -- runtime systemd will not expand it -- which is the intended behavior here.
**Where:** `usr/lib/systemd/system/hermes-dashboard.service (line 53), usr/lib/systemd/system/mios-opencode-gateway.service (line 51)`
**Done When:** both units render to `127.0.0.1` byte-identical; the unit-scanning `mios-hardcode-lint` reports no IP literal; dashboard and opencode-gateway still bind loopback; drift-gate green.
**Why:** These are the last two loose raw loopback IPs in the unit tree; leaving them means the loopback consolidation is incomplete and the lint stays red once the unit scan is enabled.
**Dep:** Add the [network] SSOT keys (loopback/bind_all/win_tailscale_ip) and derive MIOS_PG_BIND_ADDR from them

### AGY-1453 -- Float the mios-webtools quadlet IP/CDP/redis cluster and reconcile the crawl4ai 8235-vs-11235 mismatch  (WS-ZEROHC | P2 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- removes the last dense IP cluster in the quadlet tree and settles a live internal-vs-published contradiction.
**What+How:** The webtools pod quadlets carry raw literals: `mios-webtools-crawl4ai.container` line 10 `MIOS_CRAWL_CDP_URL=http://127.0.0.1:9222` and line 12 `MIOS_CRAWL_BIND=127.0.0.1`; `mios-webtools-firecrawl-api.container` lines 11-13 `REDIS_URL=redis://127.0.0.1:6380` (twice) plus `HOST=127.0.0.1`; `mios-webtools-firecrawl-worker.container` lines 11-13 the same; `mios-searxng.container` line 15 `GRANIAN_HOST=127.0.0.1` (its port already floats via `${MIOS_PORT_SEARXNG}`). Add `[ports]` `redis_internal = 6380` to both repos' `mios.toml` (reusing `browser_cdp = 9222` from the backend-CDP task), register `MIOS_PORT_REDIS_INTERNAL` in the userenv `_ssot_lint_ports_dummy` and the 34-render allowlist, then float the loopback hosts to `${MIOS_NETWORK_LOOPBACK:-127.0.0.1}`, the CDP to `${MIOS_PORT_BROWSER_CDP:-9222}` and redis to `${MIOS_PORT_REDIS_INTERNAL:-6380}`. The crawl4ai container's real service port is the INTERNAL 11235 (Label line 19 `http://localhost:11235/healthz`) -- float that Label to `${MIOS_PORT_CRAWL4AI_INTERNAL:-11235}`, resolving the current `MIOS_PORT_CRAWL4AI` (8235) vs 11235 mismatch.
**Where:** `usr/share/containers/systemd/mios-webtools-crawl4ai.container (lines 10,12,19), usr/share/containers/systemd/mios-webtools-firecrawl-api.container (lines 11-13,26), usr/share/containers/systemd/mios-webtools-firecrawl-worker.container (lines 11-13), usr/share/containers/systemd/mios-searxng.container (line 15), usr/share/mios/mios.toml (line 7915), C:/mios-bootstrap/mios.toml`
**Done When:** `redis_internal` and `browser_cdp` keys exist in both repos; all webtools quadlets render byte-identical (`127.0.0.1`/`9222`/`6380`/`11235`); `check_container_ports` and `mios-hardcode-lint` are green; the webtools pod still starts.
**Why:** The webtools pod is where the 8235-vs-11235 false friend actually bites today -- the healthz Label points one way and `MIOS_PORT_CRAWL4AI` another, so the pod's own health contract is internally inconsistent.
**Dep:** Float the hermes-worker/llm-heavy backend + browser-CDP ports and add their SSOT keys

### AGY-1454 -- Route Setup-MiOSLanPortProxy.ps1's doubled 100.79.3.50 tailscale fallback through [network].win_tailscale_ip  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- makes the only routable-IP literal in the tree operator-defined rather than baked.
**What+How:** `usr/share/mios/windows/Setup-MiOSLanPortProxy.ps1` lines 42-43 hardcode the tailnet IP twice as a degrade-open fallback: `$tsIp = try { (& tailscale ip --4).Trim() } catch { '100.79.3.50' }` plus the validation reassignment. Replace the literal with a value sourced from SSOT -- read `MIOS_NETWORK_WIN_TAILSCALE_IP` as projected through the PowerShell twin `automation/lib/globals.ps1`, keeping the `tailscale ip` probe as the primary and falling back to `$script:MIOS_NETWORK_WIN_TAILSCALE_IP` (which itself carries `100.79.3.50` as its documented `:-lit` default in globals.ps1). This file is currently in the nohc `exempt_files` list, but the literal should be SSOT-derived regardless; add the globals.ps1 default in the same PR. No bootstrap mirror is needed for the helper itself, but the `win_tailscale_ip` key it reads IS mirrored under Law 15 by the `[network]` task.
**Where:** `usr/share/mios/windows/Setup-MiOSLanPortProxy.ps1 (lines 42-43), automation/lib/globals.ps1`
**Done When:** the `.ps1` contains no bare `100.79.3.50` (it reads `$script:MIOS_NETWORK_WIN_TAILSCALE_IP`); the `tailscale ip` probe still wins when tailscale is present; `globals.ps1` exposes the key with `100.79.3.50` as its documented default; the script still runs on a host without tailscale.
**Why:** A specific tailnet address baked twice into a shipped Windows helper means any operator on a different tailnet silently gets the wrong proxy target with no config surface to correct it.
**Dep:** Add the [network] SSOT keys (loopback/bind_all/win_tailscale_ip) and derive MIOS_PG_BIND_ADDR from them

### AGY-1455 -- Retire or float the dead SurrealDB DB_URL localhost:8000 that the allowlist anchor exposes  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- clears the first genuine violation the anchored lint uncovers inside `usr/lib/mios`.
**What+How:** `usr/lib/mios/agent-pipe/mios_pipe/db.py` line 14 (`DB_URL = "http://localhost:8000"`) and `usr/lib/mios/agent-pipe/server.py` line 1348 (`os.environ.get("MIOS_DB_URL", "http://localhost:8000")`) reference the retired SurrealDB endpoint. `db.py` is NOT in nohc `exempt_files`, so once the `localhost:80` anchor lands (it currently masks `localhost:8000` through the substring bug) `db.py:14` becomes a hard violation. Per the legacy-purge decision the AI plane is Postgres/pgvector-only, so first VERIFY `:8000` is dead -- grep for live importers of `mios_pipe.db.DB_URL` and readers of `MIOS_DB_URL`. If dead, delete the constant or module, or repoint it to the pgvector DSN; if a live shim remains, float it to a `${MIOS_PORT_PGVECTOR}`-derived value or a dedicated key. `server.py` is in `exempt_files` and will not red the gate, but fix it in the same PR for consistency. Stage explicit paths only.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/db.py (line 14), usr/lib/mios/agent-pipe/server.py (line 1348)`
**Done When:** post-anchor `mios-hardcode-lint` reports no `localhost:8000` in `db.py`; either the dead constant is removed with no broken importers (agent-pipe tests pass) or it is floated to a real SSOT port; `server.py` is aligned.
**Why:** A retired SurrealDB relic still sitting in the agent-pipe import path is both a live gate violation and a trap for the next reader, who has no signal that `:8000` no longer exists.
**Dep:** Anchor the mios-hardcode-lint localhost:80 / :443 allowlist patterns so they stop masking real :80xx violations

### AGY-1456 -- Capture golden-master trycmd fixtures for mios-hardcode-lint as the parity oracle before the Rust port  (WS-ZEROHC | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- establishes the characterization harness that lets a bash/Python verb be replaced with a proven-equivalent binary.
**What+How:** Characterize the current Python `usr/libexec/mios/mios-hardcode-lint` as the parity oracle before any porting. Build a fixture corpus under a new `tools/native/mios-hardcode-lint/tests/cli/` with one case per observable class the tool emits: PASS (clean tree), DATE-IN-COMMENT, DATE-IN-STRING (py), HEADER risk (stranded BOM / shebang not on line 1), HARDCODED-PORT/IP (both the py tokenizer path and the generic path), PLAINTEXT-CHPASSWD (ventoy autorun), and the `MIOS_HARDCODE_LINT_SOFT=1` advisory exit-0 path. Record each as a trycmd `.trycmd`/`.toml` fixture capturing argv, stdout, stderr and exit code as produced by the Python tool today. These become the accept/review snapshots the Rust port must reproduce byte for byte.
**Where:** `usr/libexec/mios/mios-hardcode-lint, tools/native/mios-hardcode-lint/tests/cli/ (new)`
**Done When:** the fixtures directory exists covering all seven violation classes plus PASS and SOFT; each fixture's expected output was generated by running the current Python tool; the corpus is committed by explicit path and documented as the port oracle.
**Why:** Without a captured oracle the Rust rewrite can only be hoped equivalent, and a lint that changes behavior during a port either goes quietly permissive (violations ship) or noisily strict (the gate reds on nothing).
**Dep:** Extend mios-hardcode-lint to scan systemd unit files so usr/lib/systemd/system stops being an unscanned gate-red blind spot

### AGY-1457 -- Port mios-hardcode-lint to a std-only Rust crate behind the identical CLI facade (strangler)  (WS-ZEROHC | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the template slice for migrating a leaf libexec verb to compiled, memory-safe code with no interface change.
**What+How:** Create `tools/native/mios-hardcode-lint` modeled on the existing zero-dependency std-only crate `mios-ssot-lint` (`tools/native/mios-ssot-lint/src/main.rs`). Reimplement every scan the Python tool performs: the `_DATE` regex in `#`-comments and py docstrings (`_date_in_comment_py` / `_generic`), whitespace-led date-in-string (`_date_in_string_py`), `_header_risk` (BOM/shebang), `_check_ports_ips[_py]` with the `_PORT_PATTERNS` + `_IP_PATTERN` + `_is_routable_ip` logic (RFC1918 plus CGNAT 100.64/10 plus loopback exclusions), the ventoy chpasswd check, and reading `[security.nohc_allowlist]` `exempt_files`/`exempt_patterns` from `mios.toml` through the layered resolver (a small std TOML read or the shared `mios-ssot-walk` lib). Match the anchored exempt patterns and the extended `_CODE_EXT` (units). Keep the Python tool in place as the fallback -- do NOT delete it. Add trycmd tests running the golden-master corpus asserting byte-identical stdout/stderr/exit.
**Where:** `tools/native/mios-hardcode-lint/ (new: Cargo.toml, src/main.rs), tools/native/mios-ssot-lint/ (reference), usr/libexec/mios/mios-hardcode-lint (unchanged fallback)`
**Done When:** `cargo build -p mios-hardcode-lint` succeeds; `cargo test -p mios-hardcode-lint` passes every trycmd golden fixture byte for byte; the Rust binary and the Python tool produce identical violation sets over the live repo; the Python tool is untouched and the fallback is intact.
**Why:** The lint that enforces NO-HARDCODE is itself an unhardened interpreted script with no type safety and no test suite; until it is compiled and characterized, the gate's own correctness rests on nothing.
**Dep:** Capture golden-master trycmd fixtures for mios-hardcode-lint before the Rust port

### AGY-1458 -- Add mios-hardcode-lint to the Rust workspace and gate it with clippy -D warnings, fmt, cargo-deny/audit  (WS-ZEROHC | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- gives the Rust plane the shellcheck-equivalent MiOS has never had.
**What+How:** `tools/native/Cargo.toml` `[workspace].members` currently lists only `mios-drift-runner`, `mios-version-check` and `mios-wallpaperd`; the `mios-ssot-lint`/`-walk`/`-aiplane-lint`/`-bake-plan`/`generate-names-registry` crates build standalone. Add `mios-hardcode-lint` to `members` so it builds under the workspace and shares `Cargo.lock` (the reproducibility SSOT). Wire CI/drift-gate steps: `cargo fmt --check`; `cargo clippy -p mios-hardcode-lint -- -D warnings` with targeted restriction lints (`clippy::unwrap_used`, `clippy::panic`, `clippy::todo` denied, so the tool can never panic on a malformed source file, matching the Python tool's degrade-open try/except); plus `cargo deny check` (bans/licenses/advisories) and `cargo audit` against the committed `Cargo.lock`. Order these BEFORE the main drift gate, consistent with the negatives-before-main-gate CI ordering.
**Where:** `tools/native/Cargo.toml (members), tools/native/mios-hardcode-lint/Cargo.toml, .github/workflows/mios-ci.yml or automation drift-gate wiring`
**Done When:** `cargo build`, `clippy`, `fmt`, `deny` and `audit` all pass for the crate under `-D warnings`; `unwrap`/`panic`/`todo` are denied and absent from the source; the crate is a workspace member sharing `Cargo.lock`; the gate steps run and are green.
**Why:** MiOS has no shellcheck CI at all; a compiled crate landing without clippy, fmt and supply-chain checks just relocates the unlinted surface from Python into Rust and adds a dependency tree nobody audits.
**Dep:** Port mios-hardcode-lint to a std-only Rust crate behind the same CLI facade (strangler)

### AGY-1459 -- Wire the Rust mios-hardcode-lint into drift-gate 98 as a strangler shim with a dual-run parity window  (WS-ZEROHC | P2 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- starts porting the checks themselves to native Rust so the gate stops being fragile bash, without a risky big-bang cutover.
**What+How:** `check_no_hardcode()` in `automation/98-drift-checks.sh` (lines 978-996) invokes the Python `usr/libexec/mios/mios-hardcode-lint`. Change it to prefer the built Rust binary at `$ROOT/tools/native/target/release/mios-hardcode-lint` (or debug) when present and fall back to the Python tool otherwise, keeping the verb-name facade identical to callers. For an initial parity window run BOTH and raise `_violation` only if the results DIFFER or either fails (dual-run oracle), logging every divergence; once a gap-free window is observed, demote the invocation to the Rust binary with Python as pure fallback. Gate the swap behind an SSOT toggle in `mios.toml` (e.g. `[migration] use_rust_hardcode_lint`, oxidizr-style per-verb opt-out) so a regression rolls back by flipping one value rather than reverting the build. Keep exit semantics, including soft-mode env passthrough, identical.
**Where:** `automation/98-drift-checks.sh (check_no_hardcode lines 978-996), usr/share/mios/mios.toml ([migration] toggle), tools/native/target/release/mios-hardcode-lint`
**Done When:** `check_no_hardcode` runs the Rust binary when built and the Python tool otherwise; the parity window logs zero divergence across the repo; flipping the `[migration]` toggle switches implementation without a rebuild; drift-gate green under both settings.
**Why:** A hard cutover of a gate check has no rollback short of reverting a build; without the dual-run window and the SSOT toggle, any behavioral gap in the new binary reds (or worse, greens) the whole tree with no way to bisect which implementation was at fault.
**Dep:** Port mios-hardcode-lint to a std-only Rust crate behind the same CLI facade (strangler)

### AGY-1460 -- Mirror the six new internal-port and three [network] keys into the globals.ps1 resolver twin and fix its crawl4ai URL  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- one SSOT value must resolve identically on Linux and Windows, so the PowerShell resolver twin cannot lag the keys the campaign introduces.
**What+How:** `automation/lib/globals.ps1` is the PowerShell twin of `userenv.sh`, held equivalent by the twin-equivalence drift-check. It already declares `$script:MIOS_PORT_COCKPIT=8090` (line 98), `SEARXNG=8899` (101), `CRAWL4AI=8235` (102) and `COCKPIT_LINK=8091` (106) with the `if($env:X){...}else{lit}` degrade-open shape. Add the six new port defaults in that same shape -- `MIOS_PORT_COCKPIT_INTERNAL=9090`, `MIOS_PORT_SEARXNG_INTERNAL=8888`, `MIOS_PORT_CRAWL4AI_INTERNAL=11235`, `MIOS_PORT_FIRECRAWL_INTERNAL=3002`, `MIOS_PORT_IGPU=11436`, `MIOS_PORT_WIN_LLM_LIGHT=11450` -- plus the three network keys `MIOS_NETWORK_LOOPBACK=127.0.0.1`, `MIOS_NETWORK_BIND_ALL=0.0.0.0`, `MIOS_NETWORK_WIN_TAILSCALE_IP=100.79.3.50`, and any llm_heavy/browser_cdp/redis_internal keys added by sibling tasks. Then repoint the `MIOS_CRAWL_SERVICE_URL` derivation at line 124, which today builds the URL from `MIOS_PORT_CRAWL4AI=8235` while the live crawl service listens on 11235, to read `MIOS_PORT_CRAWL4AI_INTERNAL`.
**Where:** `automation/lib/globals.ps1 (lines 98-124)`
**Done When:** globals.ps1 exposes every new port + network var with the correct default; the userenv.sh-vs-globals.ps1 twin-equivalence drift-check is green; `MIOS_CRAWL_SERVICE_URL` resolves against 11235.
**Why:** The three parallel resolvers must move in lockstep or the twin-equivalence gate reds the moment the vendor keys land -- and until then the PowerShell side hands out a crawl4ai URL that points at a port nothing is listening on.
**Dep:** Add the [ports] internal false-friend keys (cockpit/searxng/crawl4ai/firecrawl internal, igpu, win_llm_light)

### AGY-1461 -- Derive check_container_ports' internal-port skip set from SSOT so newly floated 3002/8888/11235/9090 stop being re-flagged  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- the gate that polices hardcodes must itself learn the false-friend set from mios.toml rather than from two literals.
**What+How:** `check_container_ports` in `automation/98-drift-checks.sh` (lines 1390-1466) flags any `[ports]` value appearing as a bare word in a quadlet unless it sits inside a `${MIOS_PORT_*:-val}` placeholder (line 1442), with a hardcoded special-case skip for only 8080 and 3002 (line 1448). Now that `searxng_internal=8888`, `crawl4ai_internal=11235`, `firecrawl_internal=3002`, `cockpit_internal=9090`, `redis_internal=6380` and `browser_cdp=9222` are first-class keys floated as `${MIOS_PORT_*_INTERNAL:-val}`, replace that literal pair: read the `[ports].*_internal` keys (plus igpu/win_llm_light/browser_cdp) programmatically from mios.toml and build the skip set from them, and treat a value seen inside its OWN `${MIOS_PORT_<NAME>:-val}` placeholder as satisfied. Genuinely bare literals must still fail.
**Where:** `automation/98-drift-checks.sh (check_container_ports, lines 1416-1450)`
**Done When:** check_container_ports passes on the floated webtools/hermes-worker/cockpit quadlets; a bare `8888` with no placeholder still reds the gate; the 8080/3002 special case is gone, replaced by an SSOT-derived internal-port set.
**Why:** Without this, the very floats WS-ZEROHC introduces turn an already-green gate red -- the campaign would be blocked by its own progress, and derive-don't-duplicate would be violated inside the enforcement plane itself.
**Dep:** Add the [ports] internal false-friend keys (cockpit/searxng/crawl4ai/firecrawl internal, igpu, win_llm_light)

### AGY-1462 -- Add check_internal_ports_distinct as a fitness function asserting every [ports].*_internal differs from its published sibling  (WS-ZEROHC | P3 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- encode the internal!=published false-friend rule as machine-checked policy instead of human vigilance.
**What+How:** Add `check_internal_ports_distinct` to `automation/98-drift-checks.sh`, following the existing `check_*` shape (a python3 heredoc reading `usr/share/mios/mios.toml` via tomllib, as `check_container_ports` does at line 1400). It FAILS if any internal key equals its published sibling: `searxng_internal != searxng`, `firecrawl_internal != firecrawl`, `crawl4ai_internal != crawl4ai`, `cockpit_internal != cockpit`. The pairing map must come from a small `[ports.internal_pairs]` table in mios.toml -- never hardcoded in the gate -- mirroring the read-allowlists-from-TOML pattern already used at 98-drift-checks.sh line 2929, so future pairs are declared once. Register the check in the gate's dispatch list and assign it a check number per ADR-0012. Mirror the SSOT table into `C:/mios-bootstrap/mios.toml`.
**Where:** `automation/98-drift-checks.sh (new check_internal_ports_distinct + dispatch registration), usr/share/mios/mios.toml ([ports.internal_pairs]), C:/mios-bootstrap/mios.toml`
**Done When:** the check runs in the gate and PASSes on current SSOT; setting `searxng_internal=8899` (equal to published) reds it; the pairing map is read from TOML, not embedded in the check; the check carries an ADR-0012 number.
**Why:** Today nothing stops an editor from "tidying" firecrawl 3002 into 8302 and silently collapsing an internal bind onto its published port -- a runtime break discovered only in production rather than at build time.
**Dep:** Add the [ports] internal false-friend keys (cockpit/searxng/crawl4ai/firecrawl internal, igpu, win_llm_light)

### AGY-1463 -- Anchor every remaining bare-port exempt_pattern and document the false-friend pairs inline in both repos  (WS-ZEROHC | P3 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- close the unanchored-allowlist class that spuriously exempts real `:80xx` violations.
**What+How:** Beyond localhost:80/443, the bare-port exemptions in `usr/share/mios/mios.toml [security.nohc_allowlist].exempt_patterns` (lines 850-861: `:8640`, `:8642`, `:4317`, `:11436`, `:8644`, `:11435`, `:9222`, `:8283`) are unanchored substrings consumed by `re.search`, so each can match inside a longer numeric run. Anchor each with a leading colon plus a trailing non-digit lookahead (e.g. `":8640(?![0-9])"`), and drop entries that become redundant once the port floats to a key -- `:11436` once `[ports].igpu` ships, `:8283` if letta is retired. Add a comment block above the list enumerating the internal!=published false-friends (firecrawl 3002!=8302, searxng 8888!=8899, crawl4ai 11235!=8235, forgejo 3000!=8300, pg 5432!=8432, SSH 2222!=8222, and the intentional `MIOS_AI_ENDPOINT :8640`) so a future editor cannot silently merge them. Mirror both edits into `C:/mios-bootstrap/mios.toml` per Law 15.
**Where:** `usr/share/mios/mios.toml (lines 842-861), C:/mios-bootstrap/mios.toml`
**Done When:** every bare-port exempt pattern is anchored in both repos; the false-friend comment block is present; `mios-hardcode-lint` still PASSes the intentionally-exempt canonical ports (:8640/:8642/:4317) while no longer exempting their numeric neighbours.
**Why:** The same unanchored-substring defect that let `localhost:80` mask real violations lives in every one of these entries, so the lint currently under-reports and the campaign cannot trust its own green.
**Dep:** Anchor the mios-hardcode-lint localhost:80 / :443 allowlist patterns so they stop masking real :80xx violations

### AGY-1464 -- Prove Python/Rust hardcode-lint equivalence with a proptest differential-parity harness before deleting the Python tool  (WS-ZEROHC | P3 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- a proptest differential oracle is what converts a strangler shim into an actual retirement.
**What+How:** In `tools/native/mios-hardcode-lint` add a proptest-based differential test (dev-dependency `proptest`) that generates random-but-plausible corpora: synthetic `.py`/`.sh`/`.ps1`/`.service`/`.container` files mixing floated `${MIOS_*:-lit}` placeholders, bare ports (1-65535), RFC1918/CGNAT/loopback IPs, dated comments and header-BOM/shebang permutations, plus randomized `[security.nohc_allowlist]` exempt_files/exempt_patterns. For each generated case run BOTH implementations -- the Python `usr/libexec/mios/mios-hardcode-lint` via subprocess as the oracle and the Rust binary -- and assert the emitted `(path, line, class)` violation SETS are identical; proptest auto-shrinks any divergence to a minimal failing file. Feature-gate it as a nightly/opt-in `cargo test` so it never slows the main gate.
**Where:** `tools/native/mios-hardcode-lint/tests/ (new proptest module), usr/libexec/mios/mios-hardcode-lint (oracle)`
**Done When:** `cargo test --features difftest` runs proptest over the generated corpora; Python and Rust violation sets match across all cases; a deliberately introduced Rust regression is caught and shrunk to a minimal reproducer.
**Why:** Example-based golden fixtures only spot-check, so the Python tool stays shipped forever as an untrusted fallback -- two lints to maintain and two chances to disagree about what counts as a hardcode.
**Dep:** Wire the Rust mios-hardcode-lint into drift-gate 98 as a strangler shim with a parity window

### AGY-1465 -- Add check_no_residual_placeholders so a floated unit can never ship an unexpanded ${MIOS_*} to systemd  (WS-ZEROHC | P1 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- guard the one failure mode the float campaign itself creates, so floating a port never becomes a mis-bind.
**What+How:** `34-render-quadlets.sh` only rewrites files whose content matches its `'\${MIOS_'` grep gate (line 159) and bakes `${VAR:-lit}` at build time because systemd `Environment=` does NOT expand shell syntax at unit load. Every float task in this workstream adds a `${MIOS_PORT_*:-lit}` to a `.service`/`.socket`/`.conf`; if the render step is skipped or the var is missing from the allowlists, systemd stores the LITERAL string `${MIOS_PORT_X:-8643}` as the value and the service mis-binds. Add `check_no_residual_placeholders()` to `automation/98-drift-checks.sh`, registered in the run list near `check_container_ports` (~line 6305), walking the deployed unit and quadlet trees and FAILING on any surviving `${MIOS_` token -- proving the render ran to completion. Implement it as a small std-only Python heredoc mirroring the existing checks' shape (exit 1 on violation).
**Where:** `automation/98-drift-checks.sh (new check_no_residual_placeholders, run-list ~6305); asserts over usr/lib/systemd/system/**, usr/share/containers/systemd/**, etc/containers/systemd/**`
**Done When:** check_no_residual_placeholders is registered and green; leaving one un-allowlisted `${MIOS_PORT_FOO}` in a test unit turns the gate red; `just drift-gate` passes on the real tree.
**Why:** This is the highest-severity hazard the campaign introduces and nothing guards it today -- grepping 98-drift-checks.sh for residual/unrendered/placeholder returns zero hits -- so an unrendered placeholder ships as a hard runtime mis-bind, not a lint smell.
**Dep:** Register the new port/network vars in the 34-render-quadlets envsubst + bash-fallback allowlists

### AGY-1466 -- Reconcile the two divergent render allowlists and gate their parity with check_render_allowlist_parity  (WS-ZEROHC | P1 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- a floated var must render identically on every host, so the two hand-maintained renderer allowlists must be one set.
**What+How:** The two allowlists in `34-render-quadlets.sh` already DISAGREE: the envsubst list (line 76) names `MIOS_K3S_API_PORT` while the bash-fallback loop (line 99) names `MIOS_PORT_K3S_API` for the same port, and `MIOS_PORT_OPENCODE_GATEWAY` appears only in the bash list -- so a var covered by one renderer but not the other renders differently on hosts with vs without envsubst, a cross-host reproducibility break. First reconcile the lists to an identical set, picking the canonical `MIOS_PORT_*` name and keeping the literal fallback. Then add `check_render_allowlist_parity()` to `automation/98-drift-checks.sh` that (a) asserts the envsubst allowlist set equals the bash-fallback set and (b) scans every rendered unit/quadlet for `${MIOS_*}` placeholders and asserts each appears in BOTH lists.
**Where:** `automation/34-render-quadlets.sh:76 (envsubst) vs :90-130 (bash-fallback); new check in automation/98-drift-checks.sh`
**Done When:** the two allowlists are verified equal-set; check_render_allowlist_parity is registered and green; adding a placeholder to only one list reds the gate; the K3S and OPENCODE naming is reconciled.
**Why:** The K3S name mismatch and bash-only OPENCODE entry are an already-present, already-shipped bug proving the duplication diverges silently; without the gate every new floated var can land in one list only and behave differently per host.
**Dep:** Register the new port/network vars in the 34-render-quadlets envsubst + bash-fallback allowlists

### AGY-1467 -- Mirror the new [network] + [ports.*_internal] keys into mios-bootstrap.git and reconcile check_bootstrap_ports_drift  (WS-ZEROHC | P1 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- shared SSOT surfaces move in both repos together (Law 15) or CI hard-fails the moment the vendor keys land.
**What+How:** `check_bootstrap_ports_drift` (`automation/98-drift-checks.sh:1468`) parses `../mios-bootstrap/mios.toml` (the sibling exists, 76KB) and FAILS when its `[ports]` set diverges from the vendor mios.toml -- so every `[network]` key and `[ports].*_internal` false-friend added by the sibling WS-ZEROHC tasks reds this gate until mirrored. Add the identical `[network]` keys (loopback / bind_all / win_tailscale_ip) and the internal-port keys to the bootstrap repo's mios.toml, staging the explicit path only (never `git add -A`; AGY shares the tree). Then verify the check's comparison scope still treats internal keys correctly, extending its allow-diff logic if it currently compares strict equality over the full `[ports]` table.
**Where:** `../mios-bootstrap/mios.toml ([network] + [ports]); automation/98-drift-checks.sh:1468 check_bootstrap_ports_drift`
**Done When:** both repos' mios.toml carry the same `[network]`/`[ports.*_internal]` keys; check_bootstrap_ports_drift is green; a tomllib parse plus a `wc -l` sanity check of the bootstrap file succeeds.
**Why:** This is a blocking dependency rather than cleanup -- the bootstrap-ports drift-check hard-fails CI the instant the vendor keys land, so the whole float campaign stalls behind it.
**Dep:** Add the [network] SSOT keys (loopback/bind_all/win_tailscale_ip) and derive MIOS_PG_BIND_ADDR from them

### AGY-1468 -- Float the redundant MIOS_PORT_AGENT_PIPE and the llama.cpp lane-hint literals in mios-agent-pipe.service  (WS-ZEROHC | P1 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- a unit that both sources install.env AND re-pins the same value is a live drift source that must resolve to one place.
**What+How:** `mios-agent-pipe.service` re-hardcodes ports the SSOT already supplies: line 49's `Environment=MIOS_PORT_AGENT_PIPE=8640` duplicates the value `35-render-ports.sh` already writes to `/etc/mios/install.env` (loaded by the `EnvironmentFile` at line 48), and lines 79-80 pin the iGPU/light-lane ports as `MIOS_NO_TOOL_CHOICE_HINTS=11436` and `MIOS_KV_PAGING_HINTS=11436,11450`. Convert line 49 to the render placeholder `${MIOS_PORT_AGENT_PIPE:-8640}` -- `34-render-quadlets` processes `*.service` per its find at line 184 -- and rewrite the hint lists to `${MIOS_PORT_IGPU:-11436}` / `${MIOS_PORT_WIN_LLM_LIGHT:-11450}`, keeping the literal as the `:-` fallback so the unit still degrades open.
**Where:** `usr/lib/systemd/system/mios-agent-pipe.service:49,79,80`
**Done When:** rendering the unit through 34-render-quadlets on a default host yields byte-identical port values (8640/11436/11450); no bare 8640/11436/11450 literal remains outside a `:-` fallback; drift-gate green.
**Why:** Today an operator can change the agent-pipe port in mios.toml and the unit keeps 8640 -- the AI front door named by `MIOS_AI_ENDPOINT` silently ignores the SSOT.
**Dep:** Add the [ports] internal false-friend keys (cockpit/searxng/crawl4ai/firecrawl internal, igpu, win_llm_light)

### AGY-1469 -- Lift the hardcoded llama.cpp lane ports across mios_pipe/** onto the shared SSOT resolver, module by module  (WS-ZEROHC | P1 | L)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- `usr/lib/mios` is the gate-red zone and must carry no port constants, including in the hottest path of the agent pipe.
**What+How:** `server.py` already composes lane endpoints from `_LIGHT_BASE`, but its sibling modules still pin lane ports as literals: `mios_pipe/kernel/config.py` (`:11450` at 113/194/240, `:11434` at 274), `mios_pipe/routing/{planner,agentreg,chat,dci,refine,swarm}.py` (`:11434`/`:11436`/`:11450`), `mios_pipe/vram_scheduler.py` (`:11434` at 134-135) and `mios_endpoints.py` (`:11436`). Only server.py is allowlisted, so these are governed today solely by `check_no_bare_port_literals`' hardcoded banned list. Lift each to the shared resolver -- read `MIOS_PORT_WIN_LLM_LIGHT` / `MIOS_PORT_LLM_CUDA_INTERNAL` / `MIOS_PORT_LLM_HEAVY_INTERNAL` / `MIOS_PORT_IGPU` via `os.environ`, or via a `_LANE_BASE` helper mirroring server.py's `_LIGHT_BASE` -- keeping the literal as the `dict.get` default. Work strangler-style module by module, using each module's own unit tests as characterization.
**Where:** `usr/lib/mios/agent-pipe/mios_pipe/kernel/config.py; mios_pipe/routing/{planner,agentreg,chat,dci,refine,swarm}.py; mios_pipe/vram_scheduler.py; usr/lib/mios/agent-pipe/mios_endpoints.py`
**Done When:** no lane-port integer literal remains outside an env-default in these modules; the existing agent-pipe tests pass; check_no_bare_port_literals is green; endpoints resolve to the same values as before via env.
**Why:** server.py was floated but the kernel/routing modules that actually BUILD the request URLs were not, so moving a lane in mios.toml still leaves every real dispatch pointed at the old port.
**Dep:** Add the [ports] internal false-friend keys (cockpit/searxng/crawl4ai/firecrawl internal, igpu, win_llm_light)

### AGY-1470 -- Derive check_no_bare_port_literals' banned lane-port list from the [ports] SSOT instead of hardcoding it  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- a zero-hardcodes gate that hardcodes its own port table cannot be the enforcement plane for the rule.
**What+How:** `automation/98-drift-checks.sh:1630` sets `banned_ports = ["11450","11441","11440","11451","11434","11435"]` -- a hand-maintained list that already omits `:11436` (igpu) and drifts whenever a lane moves. Rewrite the heredoc to load the lane-internal keys from `usr/share/mios/mios.toml [ports]` (the new llm_light / llm_cuda / llm_heavy / igpu internal keys) via tomllib and build `banned_ports` from those values, so the check floats from SSOT like every other surface. Keep a small explicit set of retired-but-still-banned ports (11440/11451) as a documented literal only if they have no SSOT home.
**Where:** `automation/98-drift-checks.sh:1622-1718 check_no_bare_port_literals (banned_ports at 1630)`
**Done When:** banned_ports is computed from the mios.toml `[ports]` lane-internal keys; moving a lane value in mios.toml moves the ban with it; the check is green; 11436 is now covered.
**Why:** The list is already stale -- igpu 11436 can be hardcoded anywhere in the tree today and the gate will not notice -- and every future lane move silently widens that hole.
**Dep:** Add the [ports] internal false-friend keys (cockpit/searxng/crawl4ai/firecrawl internal, igpu, win_llm_light)

### AGY-1471 -- Add [ports].forge_internal=3000 and float the localhost:3000 git-origin literals in the Forgejo firstboot producers  (WS-ZEROHC | P2 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- every named false-friend needs an SSOT key to float TO, including forgejo 3000 != 8300.
**What+How:** Forgejo publishes at `[ports].forge_http=8300` but binds its own default `:3000` internally, and the firstboot producers talk to `localhost:3000` directly: `mios-git-root-init.service` (unit doc + Description), `mios-forge-firstboot.service:14`, and the libexec producers `usr/libexec/mios/git-root-init.sh` and `forge-firstboot.sh`. Add `forge_internal = 3000` to `[ports]`, register `MIOS_PORT_FORGE_INTERNAL` in BOTH `34-render-quadlets` allowlists, and rewrite the active `:3000` references to `${MIOS_PORT_FORGE_INTERNAL:-3000}`. These are degrade-open firstboot units, so keep the literal fallback; stage explicit paths only.
**Where:** `usr/share/mios/mios.toml [ports] (forge_internal); usr/lib/systemd/system/mios-forge-firstboot.service:14, mios-git-root-init.service; usr/libexec/mios/git-root-init.sh, forge-firstboot.sh`
**Done When:** the forge_internal key is present; the rendered producers resolve to `:3000`; no active `localhost:3000` literal remains outside a `:-` fallback; drift-gate and check_container_ports are green with 3000 recognized as an internal false-friend.
**Why:** The false-friend list explicitly names forgejo 3000 != 8300 to preserve, yet nothing in the campaign adds the key or floats the firstboot git-origin references that hardcode it -- so the one place a Forgejo port move would break is the one place still hand-written.
**Dep:** Teach check_container_ports the internal false-friends so floated 3002/8888/11235/9090 are not re-flagged

### AGY-1472 -- Add cdp_primary/cdp_worker SSOT keys and float the dedicated Chromium launcher units plus mios-hermes-browser  (WS-ZEROHC | P2 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- ports that survive only because the nohc allowlist exempts them are hardcodes with a permission slip.
**What+How:** The standalone browser units pin their CDP ports: `mios-hermes-browser.service` (`127.0.0.1:9222` primary) and `mios-hermes-browser-worker.service:33` (`HERMES_BROWSER_CDP_PORT=9223`), with the launcher `usr/libexec/mios/mios-hermes-browser` consuming them; `:9222` is masked today only by the nohc exempt list. Add `[ports]` `cdp_primary = 9222` and `cdp_worker = 9223`, register `MIOS_PORT_CDP_PRIMARY` / `MIOS_PORT_CDP_WORKER` in both render allowlists, rewrite the units' `Environment=` assignments to `${MIOS_PORT_CDP_PRIMARY:-9222}` / `${MIOS_PORT_CDP_WORKER:-9223}`, and align the launcher's own default fallbacks to the same SSOT values.
**Where:** `usr/lib/systemd/system/mios-hermes-browser.service; mios-hermes-browser-worker.service:33; usr/libexec/mios/mios-hermes-browser; usr/share/mios/mios.toml [ports]`
**Done When:** the cdp_primary/cdp_worker keys exist; the rendered units bind 9222/9223; the launcher fallback matches SSOT; no active 9222/9223 literal remains outside a `:-` fallback; drift-gate green.
**Why:** The existing browser-CDP task only covers hermes-worker.service's consumer env, leaving the two launcher units and the libexec launcher that actually OWN those ports uncovered -- a whole cluster that can drift from SSOT unnoticed.
**Dep:** Register the new port/network vars in the 34-render-quadlets envsubst + bash-fallback allowlists

### AGY-1473 -- Add a tailnet-IP (100.64.0.0/10) regression fitness function so floated tailscale IPs cannot creep back  (WS-ZEROHC | P2 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- a float without a gate is a temporary state; the guard is what makes it permanent.
**What+How:** `_is_routable_ip` in `usr/libexec/mios/mios-hardcode-lint` (line 153) returns False for the 100.64.0.0/10 CGNAT/tailnet range, so `100.79.3.50` and any other hardcoded tailscale IP is NEVER flagged -- meaning after the `Setup-MiOSLanPortProxy.ps1` float nothing prevents the literal returning. Add a dedicated scan: either a new branch in the lint that flags 100.64/10 literals unless they equal `[network].win_tailscale_ip`, or a `check_no_tailnet_ip_literal()` in `automation/98-drift-checks.sh` reading that SSOT value -- so the ONLY permitted tailnet literal is the SSOT-declared one. Port the same rule into the Rust lint when it lands.
**Where:** `usr/libexec/mios/mios-hardcode-lint:137-155 (_is_routable_ip); automation/98-drift-checks.sh (new check) reading usr/share/mios/mios.toml [network].win_tailscale_ip`
**Done When:** a stray `100.x.y.z` literal that is not the SSOT value reds the gate; the SSOT `win_tailscale_ip` value itself does not; the check is green on the real tree.
**Why:** The CGNAT range is currently invisible to the lint, so the tailscale IP the campaign just floated can be re-pasted anywhere tomorrow and no gate will say a word.
**Dep:** Float the 100.79.3.50 tailscale fallback in Setup-MiOSLanPortProxy.ps1 to the [network].win_tailscale_ip SSOT key

### AGY-1474 -- Narrow the blanket exempt_files globs that hide usr/libexec, usr/share/mios and firstboot from the port/IP scan  (WS-ZEROHC | P2 | L)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- the tree must default to SCANNED, with exemptions enumerated per file rather than per directory.
**What+How:** The `[security.nohc_allowlist].exempt_files` block (mios.toml lines 812-841) exempts whole trees from the port/IP scan: `.*/usr/libexec/.*` (273 tools), `.*/usr/share/mios/.*`, `.*firstboot\.sh$`, `.*firstboot$`, `.*/automation/.*`, `.*/windows/.*`. Once the gate-red zone (`usr/lib/mios` + systemd) is clean, `usr/libexec/mios` becomes the next blind spot -- and the named firstboot producers (`mios-ai-firstboot`, `forge-firstboot.sh`, `mios-hermes-firstboot`) are double-exempt. Incrementally replace each blanket directory glob with narrow per-file exemptions for the files that legitimately need one, after their clusters have floated. Do this LAST, per cluster, staging explicit mios.toml edits and mirroring to the bootstrap repo (Law 15).
**Where:** `usr/share/mios/mios.toml:812-841 [security.nohc_allowlist].exempt_files; mirror in ../mios-bootstrap/mios.toml`
**Done When:** the `.*/usr/libexec/.*` and `.*/usr/share/mios/.*` blanket globs are removed or replaced by an enumerated minimal file list; mios-hardcode-lint is green over the newly-scanned tree; both repos are mirrored.
**Why:** Zero-hardcodes reports green today largely because a regex hides 273 tools and the very firstboot producers this campaign names -- the blanket globs are the structural blind spot the whole effort exists to remove.
**Dep:** Anchor the mios-hardcode-lint localhost:80 / :443 allowlist patterns so they stop masking real :80xx violations

### AGY-1475 -- Generate the nohc_allowlist bare-port exempt_patterns from the [ports] loopback keys and diff-gate them  (WS-ZEROHC | P3 | M)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- the closure step: the allowlist itself becomes a projection of SSOT, not a hand-curated list.
**What+How:** The bare-port block of `exempt_patterns` (mios.toml lines 852-860: `:8640`/`:8642`/`:4317`/`:11436`/`:8644`/`:11435`/`:9222`/`:8283`) is hand-maintained and already drifts from the `[ports]` table -- `:8283` has no obvious SSOT home and `:9222` duplicates the new CDP key. After the anchoring task lands, add a build-time generator (extend `automation/35-render-ports.sh`, or a small `tools/native` step) that emits the loopback/internal exempt_patterns FROM the `[ports]` entries tagged loopback-only, plus a drift-check asserting the committed exempt_patterns equal the SSOT-derived set.
**Where:** `usr/share/mios/mios.toml:852-860 exempt_patterns + [ports]; automation/35-render-ports.sh (or tools/native); new drift-check in automation/98-drift-checks.sh`
**Done When:** exempt_patterns is generated from `[ports]` and diff-gated; moving a loopback port in `[ports]` regenerates the allowlist; committed-vs-generated compare green.
**Why:** Anchoring fixes the regex shape but leaves the list hand-curated, so it keeps accumulating orphans like `:8283` and duplicates like `:9222` and silently diverges from the real port table.
**Dep:** Anchor all remaining bare-port exempt_patterns and document the false-friends inline

### AGY-1476 -- Correct the mios-ttyd-launch fallbacks to the SSOT ttyd ports and strip the stale :7681/:7682 comment literals  (WS-ZEROHC | P3 | S)
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- a `:-` fallback that disagrees with SSOT is a latent wrong-port bind, not a safe default.
**What+How:** `usr/libexec/mios/mios-ttyd-launch` defaults PORT to `${MIOS_PORT_TTYD_BASH:-7681}` / `${MIOS_PORT_TTYD_POWERSHELL:-7682}` (lines 70, 73), but `[ports]` defines `ttyd_bash=8681` / `ttyd_powershell=8682` -- the launcher's fallback is ttyd's UPSTREAM default, not MiOS's SSOT value, so a host with an empty env silently binds the wrong port. Correct the fallbacks to `:-8681` / `:-8682`, and strip the stale `localhost:7681` / `:7682` literals from the unit doc-headers (`mios-ttyd-bash.service:2,5`; `mios-ttyd-powershell.service:5`) that would otherwise confuse the systemd-unit lint once it starts scanning comments.
**Where:** `usr/libexec/mios/mios-ttyd-launch:70,73; usr/lib/systemd/system/mios-ttyd-bash.service:2,5; mios-ttyd-powershell.service:5`
**Done When:** the launcher's `:-` fallbacks equal the SSOT ttyd_bash/ttyd_powershell values; no 7681/7682 literal remains in the ttyd units or launcher except an explicit SSOT-matching fallback; drift-gate green.
**Why:** Any host that starts ttyd without the env populated binds 7681/7682 instead of 8681/8682 today -- a silent wrong-port terminal -- and the stale comments will red the systemd lint the moment it scans them.
**Dep:** Extend mios-hardcode-lint to scan systemd unit files so usr/lib/systemd/system stops being an unscanned gate-red blind spot

### AGY-1477 -- Add cosign signing to the Forgejo publisher so both forges sign every pushed tag  (WS-SBOM | P1 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- restore the equal-publisher invariant by making Forgejo-published digests as verifiable as GitHub's.
**What+How:** `.github/workflows/mios-ci.yml` signs every pushed tag with keyless cosign (the 'Install cosign' / 'Cosign keyless sign' steps, lines 442-471), but grepping for cosign/attest/sign in `.forgejo/workflows/build-mios.yml` returns NOTHING -- Forgejo-published digests ship unsigned. Add a matching signing step to build-mios.yml: install cosign v2.x (see the cosign-v3 pinning task), `source ./tools/lib/userenv.sh`, `cosign login` the registry host derived from `MIOS_IMAGE_NAME` (parity with the GitHub push-login), then `cosign sign --yes` each pushed tag. Forgejo has no GitHub OIDC issuer, so sign with the shared MiOS key (the `COSIGN_PRIVATE_KEY` secret already referenced in generate-mok-key.sh's secrets note) resolving to ONE identity policy, while GitHub stays keyless. Mirror the workflow change in both mios.git and mios-bootstrap.git (Law 15); stage explicit paths.
**Where:** `.forgejo/workflows/build-mios.yml, .github/workflows/mios-ci.yml (lines 442-471), tools/lib/userenv.sh`
**Done When:** build-mios.yml pushes then signs each published tag; `cosign verify` succeeds against a Forgejo-published digest; grep for cosign in build-mios.yml is non-empty; drift-gate green; the change is present in both repos.
**Why:** Half the fleet's images are unverifiable today, so the release-topology promise that GitHub and Forgejo are equal bit-for-bit publishers is false wherever it matters most -- at signature verification.
**Dep:** none

### AGY-1478 -- Collapse the two divergent LUKS SSOT tables onto [security.luks] and make the recovery keyslot unconditional  (WS-SBOM | P1 | M)
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- clevis/LUKS TPM binding must be SSOT-wired and drift-gated rather than shipped as dead code.
**What+How:** Two conflicting LUKS config surfaces exist: `usr/libexec/mios/mios-clevis-luks-gen` reads `[security.luks]` (enabled/pin/pcrs, mios.toml lines 8745-8748) while `usr/libexec/mios/mios-luks-enroll` reads `config['security']['disk_encryption']` (mode/backend/pcrs/recovery, lines 24-27) -- a table that does NOT exist in mios.toml, so mios-luks-enroll is a permanent no-op with `mode` defaulting to 'none'. Collapse both consumers onto `[security.luks]`, adding `mode`, `backend` and `recovery` keys there, and default `pcrs` to '7' (Secure-Boot state, which survives kernel/initramfs bumps unlike PCR0/9). Make the recovery keyslot UNCONDITIONAL: today `recovery = de.get('recovery', True)` lets an operator set false and brick a fleet on any firmware or MOK change, exactly the hazard enroll-mok.sh lines 207-213 warn about. Update `check_clevis_luks` (`automation/98-drift-checks.sh:4321`) and add a check that mios-luks-enroll parses the unified table. Mirror the SSOT edit in both repos (Law 15); stage explicit paths.
**Where:** `usr/libexec/mios/mios-luks-enroll, usr/libexec/mios/mios-clevis-luks-gen, usr/share/mios/mios.toml [security.luks] (line 8745), automation/98-drift-checks.sh (line 4321)`
**Done When:** both tools read `[security.luks]`; mios-luks-enroll enrolls a recovery keyslot unconditionally and rejects `recovery=false`; no consumer references `[security.disk_encryption]`; drift-gate and negatives are green; the SSOT is mirrored in both repos.
**Why:** Disk-unlock enrollment is dead code reading a table that does not exist, so operators believe they have TPM-bound encryption they do not have -- and the one escape hatch, a recovery keyslot, is optional enough to brick a fleet after a firmware update.
**Dep:** none

### AGY-1479 -- Pin CI cosign to v2.x legacy-attachment format so bootc/rpm-ostree policy can discover the signatures  (WS-SBOM | P1 | S)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- signatures must land in a format the image-policy enforcement layer can actually read.
**What+How:** `automation/49-cosign-policy.sh` pins the in-image cosign to v2.x because 'v3+ breaks rpm-ostree OCI 1.1 bundle format' (header lines 11/25/32), yet CI installs `sigstore/cosign-installer@v3` and signs with the v3 CLI (`.github/workflows/mios-ci.yml` line 444). cosign v3's signing-bundle format is NOT discoverable by containers-image/rpm-ostree policy (bootc #528 / rpm-ostree #5509), so a strict sigstoreSigned policy would silently fail to verify CI signatures. Pin cosign-installer with `cosign-release: v2.6.4` -- matching `COSIGN_FALLBACK_VERSION` at 49-cosign-policy.sh line 32 -- and/or export the legacy-attachment env so signatures land as sigstore attachments; the `usr/lib/containers/registries.d/` yamls already set `use-sigstore-attachments: true`. Apply identically to the new Forgejo signing step. Mirror in both repos (Law 15).
**Where:** `.github/workflows/mios-ci.yml (lines 442-444, 451-471), automation/49-cosign-policy.sh (lines 11/25/32), .forgejo/workflows/build-mios.yml, usr/lib/containers/registries.d/`
**Done When:** CI signs with cosign v2.x; the pushed digest carries a `.sig` sigstore attachment rather than a v3 referrers bundle, verifiable by rpm-ostree/containers-image policy; both workflows are pinned; drift-gate green; Law 15 mirrored.
**Why:** Until this lands every downstream verification silently fails on format grounds, so the strict-policy work built on top of it would appear to work while enforcing nothing.
**Dep:** none

### AGY-1480 -- Generate a real sigstoreSigned policy.json from [security.sigstore] instead of insecureAcceptEverything  (WS-SBOM | P1 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- convert signing effort into actual boot integrity for the immutable host.
**What+How:** `tools/generate-cosign-policy.py` emits ONLY `{"default":[{"type":"insecureAcceptEverything"}]}` (the `[security.sigstore].policy_mode` default, mios.toml lines 781-784) and the committed `usr/lib/containers/policy.json` matches -- so an immutable bootc host boots and upgrades into ANY image. Extend the generator so `policy_mode='sigstoreSigned'` emits a `transports.docker.<repo>` rule of type `sigstoreSigned` with `keyPath: /usr/share/pki/containers/mios-cosign.pub` and `signedIdentity: {type: matchRepository}`, plus `allowed_identities` from the SSOT list (currently empty) for the keyless path. Preserve the `--check` byte-parity mode (JSON comparison, lines 40-56). registries.d already advertises attachments, so only the policy body and the SSOT flag change; flip `policy_mode` to sigstoreSigned only AFTER signatures land on both forges. Mirror the SSOT plus generated policy.json in both repos (Law 15); stage explicit paths, never `git add -A`.
**Where:** `tools/generate-cosign-policy.py, usr/lib/containers/policy.json, usr/share/mios/mios.toml [security.sigstore] (line 781), automation/49-cosign-policy.sh`
**Done When:** with `policy_mode=sigstoreSigned` the generated policy.json carries a sigstoreSigned rule keyed to the MiOS repo and mios-cosign.pub; `generate-cosign-policy.py --check` passes; `skopeo copy` admits a signed image and rejects an unsigned one under the policy; drift-gate green; mirrored in both repos.
**Why:** An immutable OS that trusts every image has no boot integrity at all -- the strongest property of the bootc model is currently switched off by a one-line default.
**Dep:** none

### AGY-1481 -- Attach the Syft SBOM to the published digest as a signed cosign attestation on both forges  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- provenance means SBOM + baked manifest, which requires the SBOM to travel WITH the image.
**What+How:** `automation/90-generate-sbom.sh` writes CycloneDX and SPDX SBOMs into `/usr/share/mios/artifacts/sbom` as in-tree files only (lines 60-66); they are never attached to the image, so downstream bootc hosts and scanners cannot run `cosign verify-attestation --type spdx`. Add a CI step on BOTH forges, after Push and alongside the sign step, that reads the baked SPDX from the pushed image (or re-runs `syft <digest>`) and runs `cosign attest --yes --type spdx --predicate <sbom.spdx.json> <digest>` under the same cosign v2 identity as the signing step. Record the attestation in `artifacts/sbom/binaries.tsv` provenance per ADR-0003. Do NOT touch 90-generate-sbom.sh's degrade-open build path -- it must still exit 0 with no egress. Mirror in both repos (Law 15).
**Where:** `.github/workflows/mios-ci.yml (after Push, ~line 440), .forgejo/workflows/build-mios.yml, automation/90-generate-sbom.sh, usr/share/doc/mios/adr/0003-sbom-not-hardcode.md`
**Done When:** `cosign verify-attestation --type spdx <published digest>` returns the SBOM predicate on both forges; the CI step is green; ADR-0003 references the attestation; Law 15 mirrored.
**Why:** An SBOM nobody outside the build can pull is not provenance -- a consumer of the published image has no verifiable inventory of what they just booted.
**Dep:** Pin CI cosign to v2.x legacy-attachment format

### AGY-1482 -- Emit signed SLSA build provenance for the published image, recording which forge built the digest  (WS-SBOM | P2 | L)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- two independent publishers of the 'same' digest are only trustworthy if each build is independently attributable.
**What+How:** Grepping for slsa/provenance in both workflows returns nothing -- there is no build-provenance attestation. On GitHub, add `actions/attest-build-provenance@v1` (or the slsa-github-generator reusable workflow) bound to the pushed image digest, using the job's existing `permissions: id-token: write`. The predicate MUST record which forge built the digest (source repo + workflow ref) so GitHub- and Forgejo-built digests remain distinguishable under the equal-publishers model. For Forgejo, which has no GitHub OIDC, emit an equivalent in-toto predicate signed with the shared MiOS cosign key via `cosign attest --type slsaprovenance`. Add a companion CI/drift-gate verification with slsa-verifier or `cosign verify-attestation`. Mirror in both repos (Law 15).
**Where:** `.github/workflows/mios-ci.yml (build/push job), .forgejo/workflows/build-mios.yml, SECURITY.md`
**Done When:** a signed SLSA provenance attestation is attached to the published digest on both forges; slsa-verifier or `cosign verify-attestation --type slsaprovenance` resolves it; the predicate names the building forge; Law 15 mirrored.
**Why:** Without provenance there is no non-falsifiable record of HOW a digest was built, so an operator cannot tell a legitimate Forgejo build from an injected one.
**Dep:** Pin CI cosign to v2.x legacy-attachment format

### AGY-1483 -- Make the image build reproducible via a git-derived SOURCE_DATE_EPOCH and timestamp rewrite  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- byte-identical digests across publishers are the precondition for the equal-publisher release topology.
**What+How:** The CI `sudo podman build` (`.github/workflows/mios-ci.yml` lines 301-312) sets no `SOURCE_DATE_EPOCH` and no timestamp normalization, so file and layer mtimes vary run to run and a third party cannot reproduce the published digest. Add `--timestamp "$SOURCE_DATE_EPOCH"` (and `--rewrite-timestamp` where buildah supports it) with `SOURCE_DATE_EPOCH=$(git -C "$ROOT" log -1 --format=%ct)` so the epoch is DETERMINISTIC and immune to build.sh's git-reset -- per the `/ IS $ROOT` note in the drift-gate local-verify memo. Ensure base images are digest-pinned. Apply to both forges and to `automation/build.sh` where it drives the OCI build. Mirror in both repos (Law 15).
**Where:** `.github/workflows/mios-ci.yml (line 301), automation/build.sh, .forgejo/workflows/build-mios.yml`
**Done When:** two independent CI builds of the same commit produce byte-identical image digests; SOURCE_DATE_EPOCH is derived from git commit time; base images are digest-pinned; both forges patched; Law 15 mirrored.
**Why:** Today the two forges provably cannot publish the same digest for the same commit, so the 'bit-for-bit equal publishers' invariant is unenforceable and nobody can independently reproduce a shipped image.
**Dep:** none

### AGY-1484 -- Add a cargo-deny gate (licenses, advisories, bans) covering every native crate in both workspaces  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the compiled tier needs the dependency-surface gate that bash never had.
**What+How:** No cargo-deny config exists (grep for deny.toml returns nothing). Add `tools/native/deny.toml` and `src/mios-rs/deny.toml` with `[advisories]` denying RustSec advisories, `[licenses]` carrying an allow-list that operationalizes the FOSS posture (Apache-2.0/MIT/BSD-*/ISC/Unicode-3.0), and `[bans]` denying duplicate crate versions and wildcard deps. The `tools/native` workspace `members` list (Cargo.toml lines 3-7) currently names only mios-drift-runner, mios-version-check and mios-wallpaperd, while crate directories also exist for generate-names-registry, mios-aiplane-lint, mios-bake-plan, mios-ssot-lint and mios-ssot-walk -- ADD them so the gate covers ALL native crates. Wire `cargo deny check` into `just drift-gate` (Justfile ~line 81) and a CI step, preserving the negatives-before-main-gate ordering. Mirror the config in both repos (Law 15).
**Where:** `tools/native/Cargo.toml (members, lines 3-7), tools/native/deny.toml (new), src/mios-rs/Cargo.toml, src/mios-rs/deny.toml (new), .github/workflows/mios-ci.yml, Justfile`
**Done When:** `cargo deny check` passes for both workspaces covering every member crate; a deliberately GPL or duplicated dependency fails it; CI and `just drift-gate` both run it; config mirrored in both repos.
**Why:** Five native crates are outside the workspace members list and therefore outside every gate, and no license or advisory check exists at all -- a banned-license or vulnerable transitive dep can ship into the image today with nothing to stop it.
**Dep:** none

### AGY-1485 -- Gate both Rust workspaces on `cargo audit --deny warnings` over committed lockfiles  (WS-SBOM | P2 | S)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the compiled tier's dependency set is continuously checked against RustSec, not merely locked.
**What+How:** `grep cargo-audit` over the tree returns empty: no advisory scan exists. Add a `cargo audit --deny warnings` step to `.github/workflows/mios-ci.yml` and a matching `just drift-gate` entry, run over BOTH `tools/native/Cargo.lock` (already committed) and `src/mios-rs/Cargo.lock` (created by the lockfile task). Keep it a SEPARATE step from cargo-deny so an advisory-only failure is diagnosable on its own. Preserve the negatives-before-main gate ordering. Cache the RustSec advisory DB in CI to dodge rate limits and degrade-open on a DB-fetch miss, mirroring the `api.github.com` fallback posture already used in `automation/49-cosign-policy.sh`. Mirror the workflow + Justfile change into mios-bootstrap.git (Law 15).
**Where:** `tools/native/Cargo.lock, src/mios-rs/ (lockfile), .github/workflows/mios-ci.yml, Justfile`
**Done When:** `cargo audit` runs green in CI and in `just drift-gate` over both lockfiles; injecting a known-vulnerable dependency version turns the gate red; a failed advisory-DB fetch warns and continues instead of failing the build; both repos carry the change.
**Why:** Build scripts and proc-macros execute arbitrary code during `cargo build`, so today every crate pulled into the OS build plane is unscreened against published advisories and a known-CVE dependency can ship inside `miosd` with nothing to notice.
**Dep:** Commit Cargo.lock for src/mios-rs and build --locked

### AGY-1486 -- Make `clippy -Dwarnings` + rustfmt the compiled plane's missing shellcheck, with panic/unwrap bans  (WS-SBOM | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the Rust tier gets the static gate the shell tier never had.
**What+How:** The tech-debt map records that MiOS has NO shellcheck CI job; migrated code can close that class for free. Add a CI step plus a `just drift-gate` step (Justfile ~line 81) running `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` for BOTH workspaces (`tools/native`, `src/mios-rs`). In each migrated crate root add targeted restriction lints -- `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic, clippy::todo)]` -- rather than blanket `clippy::restriction`, so a supply-chain verb physically cannot ship a panic path. This becomes the enforcement backstop every subsequent WS-SBOM port lands against. Mirror the config into both repos (Law 15).
**Where:** `tools/native/* (crate roots), src/mios-rs/* (crate roots), .github/workflows/mios-ci.yml, Justfile (drift-gate ~line 81), automation/lint-shell.sh`
**Done When:** `cargo clippy -- -D warnings` and `cargo fmt --check` are green and gate both CI and `just drift-gate` across both workspaces; the restriction lints are active in migrated crate roots; adding a stray `.unwrap()` to a verb fails the build; both repos carry the config.
**Why:** Ported verbs currently land with no static gate at all, so formatting drift and panic-on-`unwrap` paths enter the signed image unchallenged -- the exact defect class shellcheck would have caught in the bash they replaced.

### AGY-1487 -- Commit `src/mios-rs/Cargo.lock` and build miosd with `--locked`  (WS-SBOM | P2 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the pinned lockfile becomes the dependency reproducibility SSOT every audit gate consumes.
**What+How:** `tools/native` ships a committed `Cargo.lock`; `src/mios-rs` does NOT (only `Cargo.toml` + `miosd/`), and the Containerfile rust-builder runs a FLOATING `cargo build --release` (Containerfile line 49), so miosd's dependency graph can silently drift build-to-build -- contradicting ADR-0003's "dependency digests resolved+recorded at build". Generate and commit `src/mios-rs/Cargo.lock`, confirm `.gitignore` does not exclude it, and change the Containerfile rust-builder (lines 44-50) plus every CI cargo invocation to `cargo build --release --locked`. Stage explicit paths only -- AGY shares the tree, never `git add -A`. Mirror into both repos (Law 15).
**Where:** `src/mios-rs/Cargo.lock (new), Containerfile (rust-builder, lines 44-50), .github/workflows/mios-ci.yml, .gitignore`
**Done When:** `src/mios-rs/Cargo.lock` is committed and not ignored; `cargo build --locked` builds miosd; bumping a dependency now requires an explicit lockfile update rather than happening implicitly; cargo-audit/vet/auditable have a fixed set to scan; both repos carry it.
**Why:** Without a committed lockfile the shipped daemon is non-reproducible and unauditable -- two bakes of the same commit can embed different dependency versions, and no advisory gate downstream can name what was actually built.

### AGY-1488 -- Add a pip-audit gate over the agent-plane venv and the python libexec fleet  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the Python half of the image gets the same dependency-vuln screening as the Rust half.
**What+How:** MiOS ships ~122 python libexec tools plus the agent-pipe FastAPI plane in a venv built by `automation/72-hermes-agent.sh`, with no dependency-vuln scan at all (`grep pip-audit` -> empty) -- the python analog of the Rust gap. Add a pip-audit gate: run `pip-audit -r <requirements>` (or `pip-audit --strict` against the built `/usr/lib/mios/agents/.venv`) in `.github/workflows/mios-ci.yml` and in `just drift-gate`. DEGRADE-OPEN in the offline OCI bake (no egress -> warn and skip, matching `automation/90-generate-sbom.sh`'s exit-0 posture) but FAIL-CLOSED in CI where egress exists. Emit the report into `usr/share/mios/artifacts/sbom/` beside `binaries.tsv`. Mirror into both repos (Law 15).
**Where:** `usr/lib/mios/agent-pipe/, usr/lib/mios/agents/.venv, automation/72-hermes-agent.sh, .github/workflows/mios-ci.yml, Justfile`
**Done When:** pip-audit runs in CI over the agent venv/requirements and fails on a known-vulnerable pin; the offline bake warns and continues; the report lands in `artifacts/sbom`; both repos carry it.
**Why:** The 122-tool python plane plus the FastAPI agent front door is the largest unaudited dependency surface in the image, and a vulnerable transitive pin reaches the operator's machine today with nothing between it and them.

### AGY-1489 -- Port generate-cosign-policy.py to a typed compiled `mios-cosign-policy` with golden parity  (WS-SBOM | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the boot-integrity policy generator becomes compiled, typed and parity-proven.
**What+How:** Once the sigstoreSigned work makes the policy non-trivial, port the generator into a new `tools/native/mios-cosign-policy` workspace member that deserializes `[security.sigstore]` into a typed serde/toml struct -- the struct IS the schema and fails loudly on shape errors, unlike today's best-effort try/except that silently falls back to `insecureAcceptEverything` (`tools/generate-cosign-policy.py` lines 21-28). Strangler-fig: keep the python as a thin shim exec'ing the binary when present (current python path otherwise), prove byte-parity of BOTH `--check` and the generated `usr/lib/containers/policy.json` against the committed file via a trycmd golden fixture, THEN delete the python body. Emit miette diagnostics from the `mios-diag` crate pointing at the offending SSOT line on a malformed rule. Mirror into both repos (Law 15).
**Where:** `tools/generate-cosign-policy.py, tools/native/mios-cosign-policy/ (new crate), usr/lib/containers/policy.json, usr/share/mios/mios.toml [security.sigstore], automation/49-cosign-policy.sh`
**Done When:** `mios-cosign-policy` reproduces the committed `policy.json` byte-for-byte (trycmd green) and `--check` parity holds; `generate-cosign-policy.py` is a delegating shim; `cargo test`, `just drift-gate` and clippy -Dwarnings are green; both repos carry it.
**Why:** The file that decides which container signatures the OS trusts is currently produced by a script that degrades to "trust everything" on any parse hiccup -- a malformed SSOT edit disarms boot integrity with no error.
**Dep:** Generate a real sigstoreSigned policy.json

### AGY-1490 -- Establish a shared `mios-diag` thiserror+miette crate for the migrated supply-chain verbs  (WS-SBOM | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- one error/diagnostic foundation every port builds on instead of each reinventing one.
**What+How:** Add a shared library crate `tools/native/mios-diag` providing the two-layer error model the WS-SBOM ports need: `thiserror`-defined matchable error enums for internal/resolver failures (so drift-gates can match programmatically) and `miette` end-user diagnostics with source spans, labels and help text, so a bad `[security.*]` or port value in `mios.toml` is highlighted AT its line with actionable help. Use byte offsets, not char counts, so UTF-8 spans highlight correctly. Register it in the `tools/native` workspace members and depend on it from the ported verbs (`mios-cosign-policy`, `mios-mok`, `mios-oscap-gate`). Mirror into both repos (Law 15).
**Where:** `tools/native/mios-diag/ (new lib crate), tools/native/Cargo.toml (workspace members)`
**Done When:** `mios-diag` builds, exports a Result alias plus a span-carrying TOML-error helper, is a workspace member covered by the clippy/deny/audit gates, and is consumed by at least one ported verb; `cargo test` green; both repos carry it.
**Why:** Without a shared foundation each port invents its own error handling, producing the same opaque bash-grade error strings in compiled form -- and the operator still gets no pointer to the mios.toml line that broke.

### AGY-1491 -- Stand up a trycmd + insta golden-master harness as the parity oracle for every port  (WS-SBOM | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- no script is deleted until its exact current behavior is recorded and reproduced.
**What+How:** Before any WS-SBOM port touches behavior, add `trycmd` (whole-CLI stdout/stderr/exit snapshots) and `insta` (structured snapshots with `cargo insta review`) as dev-dependencies in `tools/native`, and capture the CURRENT observable output of each migration target as committed fixtures: `tools/generate-cosign-policy.py` (including `--check`), `usr/libexec/mios/mios-oscap-gate` over sample ARF files, `automation/enroll-mok.sh --status` over recorded mokutil fixtures, `usr/libexec/mios/mios-clevis-luks-gen`, plus the current `usr/lib/containers/policy.json` and the `MIOS_*` env projection. These fixtures become the oracle every port must reproduce byte-for-byte, mechanizing the recurring quadlet-digest-drift and SSOT-projection-drift classes into reviewable accept/review snapshots. Follows the existing `automation/test_generate-sbom.sh` test pattern. Mirror into both repos (Law 15).
**Where:** `tools/native/ (dev-deps + fixtures), automation/test_generate-sbom.sh (existing pattern), usr/lib/containers/policy.json`
**Done When:** `cargo test` runs trycmd+insta over fixtures encoding the exact current bash/python output; a deliberate output change fails the snapshot and is reviewable via `cargo insta review`; both repos carry it.
**Why:** Every port scheduled behind this one would otherwise be a rewrite with no oracle -- a silent behavior change in the cosign policy, the MOK probe or the compliance gate would ship undetected.

### AGY-1492 -- Add a systemd sandbox fitness function over every long-running unit  (WS-SBOM | P2 | L)
**Goal:** E-18 Generate the 168 systemd units from SSOT -- no unit silently loses its hardening, enforced as a check rather than a convention.
**What+How:** Only ~10 of the 84 units carry the high-value confinement directives (`grep NoNewPrivileges/ProtectSystem=strict` in `usr/lib/systemd/system/` hits `hermes-worker.service` lines 87-95, hermes-dashboard, a few `mios-*` units); a newly-added long-running unit can regress to zero confinement with nothing catching it. Add a compiled `tools/native/mios-unit-sandbox-lint` plus a new check in `automation/98-drift-checks.sh` that STATIC-lints each `[Service]` unit against a required directive floor -- `NoNewPrivileges`, `ProtectSystem=strict`, `PrivateTmp`, a bounded `CapabilityBoundingSet`, `SystemCallFilter=@system-service` -- and FAILS units below it, EXEMPTING the operator-defined `[security.privileged_quadlets].root` roster (`mios.toml` lines 786-804, the only sanctioned exceptions). Honor documented per-unit deviations (e.g. hermes-worker's `NoNewPrivileges=false` sudo grant, line 92) through an SSOT exempt list, never a hardcode. Where `systemd-analyze security` is available in the runtime CI phase, score units and fail above an exposure threshold. Mirror into both repos (Law 15).
**Where:** `tools/native/mios-unit-sandbox-lint/ (new), automation/98-drift-checks.sh (new check), usr/lib/systemd/system/*.service, usr/share/mios/mios.toml [security.privileged_quadlets] (line 786)`
**Done When:** the linter/gate flags any non-exempt Service unit below the directive floor; the privileged roster and documented deviations are exempt via SSOT; adding a bare new unit fails the gate; negatives-before-main ordering preserved; `just drift-gate` green; both repos carry it.
**Why:** An immutable image is only as hardened as its services -- 74 of 84 units run today with no confinement floor, and the next daemon someone adds widens the attack surface with nothing to object.

### AGY-1493 -- Set fapolicyd `integrity=sha256` and derive the trust DB from built image content  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- allowlisting moves from path identity to content identity, making the image an immutable execution environment.
**What+How:** `automation/40-fapolicyd-trust.sh` sets `trust = file,rpmdb` (line 22) but never sets `integrity`, which defaults to `none` -- so fapolicyd allowlists by PATH only and a writable or overlay-tampered binary sitting at a trusted path still executes. Add an SSOT-driven `integrity = sha256` line through the same sed loop over BOTH the `/usr/lib` and `/etc` `fapolicyd.conf` copies (USR-OVER-ETC parity, lines 19-23), and confirm the trust DB is generated FROM built image content: `automation/51-hardening.sh` already runs `fagenrules --load` + `fapolicyd-cli --update` (lines 35-40) over the RPM/OCI file set -- verify that covers OCI-added binaries, not just rpmdb. Keep the `|| true` degrade-open so a missing fagenrules cannot brick the bake. Add a drift-check asserting `integrity=sha256` on both conf paths. Mirror the SSOT key into both repos (Law 15).
**Where:** `automation/40-fapolicyd-trust.sh (line 22), automation/51-hardening.sh (lines 35-40), usr/lib/fapolicyd/fapolicyd.conf, usr/share/mios/mios.toml [security]`
**Done When:** `fapolicyd.conf` ships `integrity = sha256` on both the `/usr/lib` and `/etc` paths; the trust DB derives from image content; a binary overlaid at a trusted path is denied execution; a drift-check asserts the setting; both repos carry it.
**Why:** Path-only allowlisting means fapolicyd currently approves whatever occupies a trusted path, so an overlay or `/var` tamper defeats the entire immutable-execution premise the image is built on.

### AGY-1494 -- Derive greenboot's critical-service list from SSOT and probe Hermes :8642 at boot  (WS-SBOM | P2 | M)
**Goal:** E-19 Wire the shipped-but-unwired runtime capabilities -- greenboot health coverage becomes SSOT-derived and actually covers the AI plane.
**What+How:** `automation/98-drift-checks.sh` `check_greenboot` HARDCODES `critical_services=("agent-pipe" "llm-light" "pgvector")` (line 4305), duplicating `[greenboot].critical_services` in `mios.toml` (line 8751) -- the exact derive-don't-duplicate smell -- and the `required.d` health-check scripts never probe the AI-plane endpoint. Make `check_greenboot` READ `[greenboot].critical_services` from SSOT via `mios_toml.py` with a `${VAR:-agent-pipe llm-light pgvector}` fallback instead of the literal array, and add a `usr/lib/greenboot/check/required.d/` check that curls the canonical Hermes endpoint `:8642` plus the core Quadlets, so a signed-but-broken publish trips the boot counter and rolls back rather than rolling a fleet forward into a dead AI plane. Optionally compile the health check into a `mios-greenboot-healthcheck` binary reading the same SSOT (greenboot-rs is the upstream bootc direction). Mirror the SSOT into both repos (Law 15).
**Where:** `automation/98-drift-checks.sh check_greenboot (line 4298) + check_greenboot_enablement (line 3280), usr/lib/greenboot/check/required.d/, usr/share/mios/mios.toml [greenboot] (line 8750)`
**Done When:** `check_greenboot` derives its service list from `[greenboot].critical_services` with no hardcoded triple; a `required.d` check probes Hermes `:8642`; editing the SSOT list visibly changes what the gate enforces; greenboot rolls back on a failed AI-plane boot; drift-gate and negatives green; both repos carry it.
**Why:** The gate asserts a service list copied by hand into the checker, so it drifts from the real SSOT the moment either side changes -- and with no AI-plane probe a broken publish boots "healthy" with a dead front door.

### AGY-1495 -- Assert the required security kargs so `enforcing=1` cannot drop out of the signed UKI cmdline  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the signed UKI's security properties become a fitness function, not an assumption.
**What+How:** The UKI cmdline is flattened by `tools/generate-uki-cmdline.py` and drift-checked for PROJECTION parity (`automation/98-drift-checks.sh` `check_uki_cmdline_projection`, line 5367), but NOTHING asserts the security-critical kargs are actually present -- `enforcing=1` (SELinux, which must be baked INSIDE the signed UKI so it cannot be toggled at the boot menu), `lockdown=`, and the LUKS/rootfs args could silently disappear and still pass. Add an SSOT `[security.kargs].required` list plus a drift-check that FAILS when `usr/lib/kernel/cmdline` (rendered by `automation/76-uki-render.sh`) is missing any required token, with a `${...:-enforcing=1 lockdown=integrity}` fallback. Mirror the SSOT into both repos (Law 15).
**Where:** `automation/76-uki-render.sh, tools/generate-uki-cmdline.py, usr/lib/kernel/cmdline, usr/lib/bootc/kargs.d/*.toml, automation/98-drift-checks.sh (check_uki_cmdline_projection, line 5367), usr/share/mios/mios.toml`
**Done When:** the gate fails when a required security karg is absent from the rendered cmdline; the required set is SSOT-defined; `enforcing=1` presence is explicitly asserted; drift-gate and negatives green; both repos carry it.
**Why:** Projection parity only proves the cmdline matches its generator -- an edit that drops `enforcing=1` or `lockdown=` today ships a signed UKI with SELinux permissive and every gate stays green.

### AGY-1496 -- Make MOK key generation and enrollment operator-defined via `[security.secureboot]`  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- Secure Boot trust custody becomes an operator SSOT input projected into the build, not a MiOS constant.
**What+How:** `automation/generate-mok-key.sh` HARDCODES the cert CN ("MiOS Module Signing Key", line 54), key size (2048, lines 47/65) and validity (3650d, line 72), and `automation/enroll-mok.sh` HARDCODES the key search paths (`/etc/pki/mios/mok.der`, `akmods-ublue.der`; `pick_key` lines 100-110) -- none operator-defined, contradicting the SSOT-operator-defined north star. Add `[security.secureboot]` (`cn`, `key_bits`, `validity_days`, `cert_path`, `enroll_on_firstboot`) and project it into both scripts through `mios_toml.py` using `${VAR:-<current literal>}` fallbacks so behavior is byte-unchanged when unset. PRIVATE key material stays out of SSOT (it remains a GitHub secret per `generate-mok-key.sh` lines 105-107); only public policy/identity is operator-defined. Mirror the SSOT into both repos (Law 15).
**Where:** `automation/generate-mok-key.sh (lines 40-73), automation/enroll-mok.sh (pick_key, lines 100-110), usr/share/mios/mios.toml [security.secureboot] (new), specs/SECUREBOOT.md`
**Done When:** both scripts read CN, key bits, validity and cert path from `[security.secureboot]` with literal fallbacks; changing the CN in SSOT visibly changes the generated cert; no private key material enters `mios.toml`; drift-gate green; both repos carry it.
**Why:** An operator deploying MiOS cannot today name their own signing identity or key policy without editing shipped automation, which is precisely the hand-edited-config class the SSOT north star forbids.

### AGY-1497 -- Port enroll-mok.sh's `status_probe` to a compiled `mios-mok --status` with differential parity  (WS-SBOM | P2 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- Secure-Boot state parsing stops being sed/grep string-munging.
**What+How:** `automation/enroll-mok.sh` `status_probe` (lines 47-98) parses `mokutil --sb-state/--list-enrolled/--list-new` and `openssl x509` output through brittle sed/grep/tr pipelines (fingerprint normalization lines 74-78, CN extraction line 87). Port the READ-ONLY probe into a new `tools/native/mios-mok` crate that still shells `mokutil`/`openssl` but parses in typed Rust, emitting exactly one of `enrolled|pending|not-enrolled|no-secureboot|conflict`. Strangler-fig: demote `status_probe` to call the binary when present with the bash path as fallback, prove byte-parity of the emitted state against the bash version via a differential/trycmd golden over recorded mokutil fixtures, THEN delete the bash probe. Leave the WRITE path (`mokutil --import --root-pw`, line 188) in bash for now since it needs an interactive root password. Return miette diagnostics via `mios-diag`. Mirror into both repos (Law 15).
**Where:** `automation/enroll-mok.sh (status_probe, lines 47-98), tools/native/mios-mok/ (new crate), usr/share/mios/mios.toml [security.secureboot]`
**Done When:** `mios-mok --status` emits the identical state string as the bash probe across the fixtures; `enroll-mok.sh` delegates to it with a bash fallback; differential/trycmd parity green; clippy -Dwarnings clean; `just drift-gate` green; both repos carry it.
**Why:** Enrollment decisions branch on a state string produced by fragile text pipelines -- a formatting change in `mokutil` output silently misclassifies Secure Boot state and the wrong enrollment path runs.
**Dep:** Stand up a trycmd + insta golden-master harness

### AGY-1498 -- Enforce cosign signature + attestation at k3s admission for runtime sidecars  (WS-SBOM | P2 | L)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the signature policy extends past the host boundary to everything pulled at runtime.
**What+How:** MiOS runs k3s (`automation/36-ceph-k3s.sh`, `automation/37-k3s-selinux.sh`, `usr/share/containers/systemd/mios-k3s.container`), so sidecar/workload images pulled at RUNTIME are an unverified surface the OS-plane `policy.json` never sees. Add a Kyverno (or sigstore policy-controller) admission policy TEMPLATED FROM `[security.sigstore]` -- the same cosign identity and attestation checks the OS plane enforces -- admitting only images with a valid cosign signature plus an SBOM/SLSA attestation matching the MiOS identity, rejecting the rest, so the AI-plane `/v1` sidecars cannot be swapped for unsigned images. Render the policy from SSOT under the canonical-port/NO-HARDCODE allowlist model, and ship it DISABLED by default behind an operator toggle (the "inert until wired" convention). Mirror into both repos (Law 15).
**Where:** `automation/36-ceph-k3s.sh, automation/37-k3s-selinux.sh, usr/share/containers/systemd/mios-k3s.container, k3s/, usr/share/mios/mios.toml [security.sigstore]`
**Done When:** a rendered admission policy enforces cosign identity + attestation on k3s image pulls; an unsigned test image is rejected and a MiOS-signed one admitted; the policy is SSOT-templated and toggled off by default; drift-gate green; both repos carry it.
**Why:** Host-plane signing stops at the boundary, so k3s can pull an unsigned inference sidecar onto a fully-verified host today -- reopening precisely the substitution gap signing exists to close.
**Dep:** Generate a real sigstoreSigned policy.json

### AGY-1499 -- Bootstrap on-disk signature-policy enforcement at firstboot, gated on the SSOT flag  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the sigstoreSigned policy.json actually gates `bootc upgrade` instead of sitting inert.
**What+How:** `bootc upgrade` still has no `--enforce-container-sigpolicy` flag, so enforcement must live in the on-disk `policy.json` and be bootstrapped once. The registries.d yamls ALREADY advertise `use-sigstore-attachments: true` (`usr/lib/containers/registries.d/ghcr.io*.yaml`, verified), so add a DEGRADE-OPEN firstboot producer plus unit that runs the one-time `bootc switch --enforce-container-sigpolicy` bootstrap ONLY when `[security.sigstore].policy_mode = sigstoreSigned`, keeping it inert until signatures are proven on both forges. Follow the existing firstboot-producer conventions (`usr/libexec/mios/*firstboot*`, degrade-open, `ConditionPathExists`). Mirror the unit and SSOT into both repos (Law 15).
**Where:** `usr/libexec/mios/ (new firstboot enforcer), usr/lib/systemd/system/ (new firstboot unit), usr/lib/containers/policy.json, automation/49-cosign-policy.sh, usr/share/mios/mios.toml [security.sigstore]`
**Done When:** a firstboot producer bootstraps enforce-container-sigpolicy gated on `policy_mode=sigstoreSigned`; an unsigned `bootc upgrade` is refused on an enforcing host; the producer no-ops cleanly when disabled; drift-gate green; both repos carry it.
**Why:** A strict `policy.json` on disk enforces nothing until enforcement is switched on once, so today a host with the strictest possible policy still accepts an unsigned upgrade.
**Dep:** Generate a real sigstoreSigned policy.json

### AGY-1500 -- Gate ADR-0003: every floating binary download must record a binaries.tsv row  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- "resolved and recorded at build" becomes an enforced fitness function instead of a convention.
**What+How:** `automation/49-cosign-policy.sh` correctly records its cosign download to `binaries.tsv` (`record_version` line 42, append line 62), but ADR-0003 requires EVERY floating binary download to be resolved and recorded -- and roughly 11 automation steps (`06-enable-external-repos`, `21-virt`, `25-gpu-cdi-toolkits`, `36-ceph-k3s`, `56-fonts`, `57-gnome`, `62-oh-my-posh`, `90-generate-sbom`, others) run `scurl` / `curl -sfL … | sh` with no matching row. Add a compiled linter plus a new `automation/98-drift-checks.sh` check that scans `automation/*.sh` for network-fetch sites (`scurl`, `curl -sfL`, `| sh -s`) and FAILS any that installs a binary without a corresponding `record_version`/binaries.tsv append. Allowlist genuine non-binary fetches (repo files, checksums, install scripts) through an SSOT exempt list, never inline. Mirror into both repos (Law 15).
**Where:** `automation/*.sh (download sites, e.g. 49-cosign-policy.sh lines 46-47, 90-generate-sbom.sh line 41), automation/lib/common.sh (record_version), usr/share/mios/artifacts/sbom/binaries.tsv, automation/98-drift-checks.sh (new check)`
**Done When:** the gate enumerates every download site and fails on an unrecorded binary fetch; the exempt list is SSOT-defined; adding a new un-recorded `curl|sh` fails the build; drift-gate and negatives green; both repos carry it.
**Why:** Most fetched binaries in the image are absent from the SBOM manifest, so the published provenance record understates what actually shipped and ADR-0003 reproducibility does not hold.

### AGY-1501 -- Build miosd and the native tools with cargo-auditable to embed the dependency manifest  (WS-SBOM | P3 | S)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the shipped artifact itself carries what it was built from, so it can be re-scanned later.
**What+How:** The `miosd` binary (Containerfile rust-builder, lines 44-50) and the native tools embed no machine-readable dependency manifest, so a DEPLOYED binary cannot be scanned for known-vuln deps post-hoc -- the runtime complement to the build-time SBOM. Install `cargo-auditable` in the rust-builder stage and build with `cargo auditable build --release` so the exact dependency tree is embedded in the ELF; downstream `cargo audit bin /usr/libexec/mios/miosd` then scans the shipped binary directly. Keep `--locked` so the embedded manifest matches the committed `Cargo.lock`, and record the auditable-build fact into binaries.tsv provenance. Mirror into both repos (Law 15).
**Where:** `Containerfile (rust-builder, lines 44-50), src/mios-rs/Cargo.toml, tools/native/Cargo.toml, .github/workflows/mios-ci.yml`
**Done When:** miosd and the native tools are built with cargo-auditable; `cargo audit bin` reads the embedded manifest out of the shipped binary; Containerfile and CI updated; the provenance row is recorded; both repos carry it.
**Why:** A CVE published after the bake cannot be checked against an already-deployed host today -- the binary on disk is opaque about its own dependencies.
**Dep:** Commit Cargo.lock for src/mios-rs and build --locked

### AGY-1502 -- Port mios-oscap-gate to a compiled Rust verb with golden ARF fixtures  (WS-SBOM | P3 | L)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- the fail-closed compliance verdict runs on memory-safe, tested code.
**What+How:** `usr/libexec/mios/mios-oscap-gate` is a pure-stdlib python ARF/XCCDF severity parser that decides the BOOT-02 compliance build VERDICT (it counts FAILED rules at or above `[compliance].severity_gate` and is called from `automation/86-oscap-compliance.sh` line 111). Port it to a compiled, typed `tools/native/mios-oscap-gate` using quick-xml, namespace-agnostic by local tag name, preserving the `high>medium>low` plus `any` ranking and the fail-closed exit codes 0/1/2. Strangler-fig: capture current python output over a set of golden ARF fixtures via insta, keep the python as a thin shim exec'ing the binary when present, prove byte-parity of the printed count and exit code, THEN delete the python body. `86-oscap-compliance.sh`'s `GATE_BIN` resolution already prefers the installed path (lines 106-108), so the swap-in is transparent. Mirror into both repos (Law 15).
**Where:** `usr/libexec/mios/mios-oscap-gate, tools/native/mios-oscap-gate/ (new crate), automation/86-oscap-compliance.sh (line 111), usr/share/mios/mios.toml [compliance] (line 8598)`
**Done When:** the Rust gate reproduces the python count and exit code over the golden ARF fixtures (insta green); the python is a shim; `86-oscap-compliance.sh` drives it unchanged; `cargo test` and clippy -Dwarnings green; both repos carry it.
**Why:** An untested python script parsing untrusted XML currently holds a veto over whether the image is compliant enough to ship, and a parser bug either blocks a good build or passes a non-compliant one.
**Dep:** Stand up a trycmd + insta golden-master harness

### AGY-1503 -- Add a scheduled vuln scan that verify-pulls the attested SBOM and scans the digest  (WS-SBOM | P3 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the attestation is continuously consumed, closing the loop on `:latest`-intent reproducibility.
**What+How:** The attested SBOM enables scanning the exact shipped bits, but nothing consumes it. Add a scheduled (cron) GitHub workflow reusing the `.github/workflows/url-probe.yml` scheduled-workflow shape that pulls the published digest's SPDX attestation via `cosign verify-attestation --type spdx` and runs grype (or trivy) against it, failing or filing an issue on new CRITICAL/HIGH CVEs. Because GitHub and Forgejo publish equal digests, scan the DIGEST, not a tag. Mirror as a Forgejo sibling or a shared reusable workflow (Law 15).
**Where:** `.github/workflows/ (new scheduled workflow), .github/workflows/url-probe.yml (pattern), automation/90-generate-sbom.sh, usr/share/mios/artifacts/sbom/`
**Done When:** the scheduled workflow verify-pulls the SBOM attestation and scans it with grype/trivy; a seeded CVE fails or reports; it runs against the digest rather than a tag; the Forgejo sibling or shared workflow exists.
**Why:** A published image ages into vulnerability silently -- nothing rechecks the shipped bits between bakes, so the SBOM is written and never read.
**Dep:** Attach the Syft SBOM to the published image as a signed cosign attestation

### AGY-1504 -- Initialize cargo-vet trusted-review for both Rust workspaces  (WS-SBOM | P3 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- dependency assurance rises from advisory-only to human trusted-review.
**What+How:** Complement cargo-deny/audit with `cargo vet`: initialize `supply-chain/audits.toml` + `config.toml` for BOTH workspaces (`tools/native`, `src/mios-rs`), import the well-known Mozilla / Google / Bytecode-Alliance audit sets, and gate CI on `cargo vet --locked` so every dependency is audited, trusted, or explicitly exempted. Start in a permissive audit-as-needed posture with exemptions committed so it cannot block the bake, then tighten over time. The committed `Cargo.lock` is the input. Mirror config into both repos (Law 15) and stage explicit paths.
**Where:** `tools/native/supply-chain/ (new), src/mios-rs/supply-chain/ (new), .github/workflows/mios-ci.yml, Justfile`
**Done When:** `cargo vet` is initialized for both workspaces with imported audit sets, runs in CI, and flags a newly-added unaudited crate; exemptions are committed and reviewable; both repos carry it.
**Why:** Advisory and license gates only catch what someone already reported -- an unreviewed new crate entering the OS build plane passes both today.
**Dep:** Commit Cargo.lock for src/mios-rs and build --locked

### AGY-1505 -- Enforce RPM package + repo GPG verification from an SSOT `[security.rpm]` policy  (WS-SBOM | P1 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the base package layer is signature-verified, so the signing chain has a verified root.
**What+How:** `automation/05-repos.sh` writes the vendored Fedora-44 mirror with `gpgcheck=0` AND `repo_gpgcheck=0` (lines 61, 63, 71, 73), so packages install with NO signature check, and `repo_gpgcheck=0` recurs across the metalink repos. Add a `[security.rpm]` SSOT table (`require_gpgcheck=true`, plus an explicit allowlist of dev-tree repos permitted `repo_gpgcheck=0` for unsigned metalink metadata only) and a compiled fitness function -- extend the `tools/native/mios-ssot-lint` crate plus a `check_rpm_gpgcheck` in `automation/98-drift-checks.sh` -- that FAILS if any generated `.repo` sets `gpgcheck=0` outside the allowlist. For the vendored mirror, import the F44 RPM-GPG key locally, flip `gpgcheck=1`, and record the key fingerprint to `artifacts/sbom`. Strangler: land the gate WARN-only, then flip to enforce.
**Where:** `automation/05-repos.sh, automation/06-enable-external-repos.sh, usr/share/mios/mios.toml [security], automation/98-drift-checks.sh, tools/native/mios-ssot-lint`
**Done When:** no generated `.repo` sets `gpgcheck=0` outside the SSOT allowlist; `check_rpm_gpgcheck` is green in `just drift-gate`; the vendored-mirror bake installs RPM-signature-verified packages.
**Why:** `gpgcheck=0` means the RPM layer accepts unsigned packages -- the largest untargeted supply-chain surface in the image, and every cosign/SBOM control downstream is moot while the base package layer is unverified.
**Dep:** Enforce SBOM-not-hardcode: gate that every floating binary download records to binaries.tsv

### AGY-1506 -- Verify and digest-pin the base image at build, recording it to provenance  (WS-SBOM | P1 | L)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the largest ingested artifact in the OS is signature-verified and digest-recorded like everything else.
**What+How:** The Containerfile builds `FROM ARG BASE_IMAGE=ghcr.io/ublue-os/ucore-hci:stable-nvidia`, a floating tag with no build-host verification policy, so the entire OS inherits an unverified, unpinned base. Add a build-host containers-policy requiring a `sigstoreSigned` match for `ghcr.io/ublue-os/*` (ublue publishes cosign signatures), resolve tag->digest at bake time, build FROM the resolved digest for reproducibility, and append the base digest row to `usr/share/mios/artifacts/sbom/bound-images.tsv` per ADR-0003. Keep the `:stable-nvidia` tag as the `:latest`-intent fallback (`${BASE_DIGEST:-tag}`). Wire the resolution into `automation/02-materialize-build-ctx.sh` and `automation/85-bake-plan.sh`.
**Where:** `Containerfile, automation/02-materialize-build-ctx.sh, automation/85-bake-plan.sh, automation/49-cosign-policy.sh, usr/lib/containers/policy.json, usr/share/mios/mios.toml [image]`
**Done When:** the bake resolves and records the ucore-hci digest and builds FROM that digest; a build-host policy rejects an unsigned or foreign base image; `bound-images.tsv` carries the base-image row.
**Why:** An unpinned, unverified `FROM` tag is a silent whole-OS substitution vector: two bakes can produce different systems from the same commit, and the single largest ingested artifact is currently the least verified thing in the tree.
**Dep:** Generate a real sigstoreSigned policy.json from [security.sigstore] instead of insecureAcceptEverything

### AGY-1507 -- Delete `curl|sh` from the bake and float the Sigstore toolchain from SSOT  (WS-SBOM | P1 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- nothing unverified executes inside the build that produces the signed image.
**What+How:** `automation/90-generate-sbom.sh` installs syft via `curl .../install.sh | sh -s` (lines 41-42) -- an unverified pipe-to-shell -- and both the syft fallback (v1.19.0, line 39) and the cosign fallback (v2.6.4, `49-cosign-policy.sh:32`) are hand-hardcoded. Add a `[security.toolchain]` SSOT table (cosign, syft, rekor-cli, each with version + sha256), install each through the existing checksum-verified pattern (download binary, `grep | sha256sum -c`, as `49-cosign-policy.sh` already does for cosign), and DELETE the pipe-to-shell. Add a compiled fitness function forbidding `curl ... | (ba)sh` across `automation/*.sh` and `usr/libexec`, and record every installed tool to `binaries.tsv`.
**Where:** `automation/90-generate-sbom.sh, automation/49-cosign-policy.sh, usr/share/mios/mios.toml [security], automation/98-drift-checks.sh`
**Done When:** no pipe-to-shell installer remains anywhere in the bake path; syft and cosign versions/digests resolve from `[security.toolchain]`; `check_no_curl_pipe_sh` is green and each tool is recorded to `binaries.tsv`.
**Why:** `curl|sh` runs attacker-controllable code with no integrity check inside the build that produces the signed image, undermining the signing chain at its root, and the hardcoded fallback versions violate ADR-0003 besides.
**Dep:** Pin CI cosign to v2.x legacy-attachment format so bootc policy can discover signatures

### AGY-1508 -- Fail-closed signing of out-of-tree kernel modules under Secure Boot  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the boot chain has no unsigned-module gap left between UKI and userspace.
**What+How:** `automation/68-bake-kvmfr.sh` signs `kvmfr.ko` only when the akmods private key is present and otherwise WARNs and ships the module UNSIGNED (lines 126-131); under kernel lockdown a required unsigned kmod (kvmfr, for Looking Glass) then silently fails to load. Add `[security.secureboot].enforce_module_signing`; when set, the signer FAILS the bake if any `/usr/lib/modules/**/extra/*.ko` lacks a signature (verified via `modinfo -F sig_id`), and a drift-gate enumerates every baked out-of-tree `.ko` and asserts each is signed. Port the signer into a small compiled `mios-modsign` verb reusable by kvmfr and future kmods; keep the degrade-open behavior only while `enforce_module_signing` is false.
**Where:** `automation/68-bake-kvmfr.sh, automation/22-akmod-guards.sh, usr/share/mios/mios.toml [security.secureboot], automation/98-drift-checks.sh, src/mios-rs`
**Done When:** in enforce mode the bake fails on any unsigned out-of-tree module; the drift-gate lists and verifies every baked `.ko` signature; kvmfr is signed in the standard bake with byte-parity against the old shell signer.
**Why:** Secure Boot plus UKI signing is worthless if out-of-tree modules load unsigned -- and in the failure case that exists today the module instead vanishes silently, breaking Looking Glass with no build error.
**Dep:** Drive MOK key generation and enrollment from an operator-defined [security.secureboot] SSOT

### AGY-1509 -- Verify bound-image digests at firstboot before the 47GB whale pull  (WS-SBOM | P1 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- the digests recorded at bake are actually enforced where the artifacts land.
**What+How:** `usr/libexec/mios/mios-bound-images-firstboot` (66 lines) web-pulls the evicted vLLM/SGLang whales (~47GB) on first boot with NO signature or digest verification, even though `mios-bake-group` already recorded their resolved digests to `usr/share/mios/artifacts/sbom/bound-images.tsv` at bake (lines 129-134). Pin the firstboot pull to the recorded digest (`podman pull image@sha256:...`, cross-checked against `bound-images.tsv`) and enforce the on-disk `policy.json` so a compromised registry cannot swap the inference engine. Degrade-open: when the SSOT flag is off, fall back to the current tag pull. Fold the logic into a compiled verb sharing `usr/libexec/mios/mios-policy-arbiter`.
**Where:** `usr/libexec/mios/mios-bound-images-firstboot, usr/share/mios/artifacts/sbom/bound-images.tsv, usr/lib/containers/policy.json, usr/share/mios/mios.toml [security.sigstore]`
**Done When:** firstboot pulls the whales by their bake-recorded digest and verifies signatures when the SSOT flag is on; a digest mismatch aborts the pull; the tag-pull degrade-open path still works when the flag is off.
**Why:** The firstboot tier is a live TOCTOU hole -- digests are captured at bake but the two biggest artifacts are re-pulled unverified on the operator's machine, exactly where a registry MITM does the most damage, and the producer never consults the policy that was installed for it.
**Dep:** Bootstrap on-disk signature-policy enforcement at firstboot gated on the SSOT flag

### AGY-1510 -- Gate both CI publishers on a gitleaks scan of the git tree AND the exported OCI rootfs  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- no credential ever reaches a published layer, so "signed" also means "clean".
**What+How:** No gitleaks/trufflehog/detect-secrets is wired anywhere today; the tooling exists only as written intent in `usr/share/doc/mios/reference/audit-security.md`. Add a gitleaks step to BOTH publishers (`.github/workflows/mios-ci.yml` and `.forgejo/workflows/build-mios.yml`, kept identical per the GitHub-equals-Forgejo release topology and Law 15) that scans (a) the git tree and (b) the exported OCI rootfs, so credentials baked into a layer are caught even when they were never committed. Add a lightweight companion check in `automation/98-drift-checks.sh` asserting `usr/share/mios` carries no secrets, and commit a `.gitleaks.toml` allowlisting the shipped public keys (`fulcio_v1.crt.pem`, `rekor.pub`, `mios-cosign.pub`) so the gate does not cry wolf on legitimate material.
**Where:** `.github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml, automation/98-drift-checks.sh, .gitleaks.toml (new)`
**Done When:** The gitleaks gate runs in both CI publishers and in the drift-gate; a deliberately planted test secret turns all three red; the three shipped public keys are allowlisted and do not trip it.
**Why:** A token baked into a layer or committed into the SSOT is an immediate supply-chain compromise that signing cannot detect -- a correctly signed image with an embedded secret is still poisoned -- and Law-15 dual-repo work multiplies the chance of an accidental credential commit while the control is entirely absent.
**Dep:** Stand up a trycmd + insta golden-master harness capturing current verb output as the parity oracle

### AGY-1511 -- Fold the three duplicate MOK/secure-boot scripts into one compiled mios-mok verb  (WS-SBOM | P3 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- one authoritative Secure Boot codepath instead of three hand-maintained divergent ones.
**What+How:** Three overlapping MOK tools exist: `automation/generate-mok-key.sh` (RSA key generation), `automation/enroll-mok.sh` (mokutil enroll, 215 lines), and `usr/libexec/mios/mios-enroll-secure-boot` (enrolls the ublue akmods MOK). Once the `mios-mok` verb lands from the status_probe port, fold key-gen, enroll and ublue-key enroll into subcommands of that one compiled verb in `src/mios-rs`, keyed off `[security.secureboot]` in mios.toml rather than script-local constants. Demote the three scripts to <=3-line shims that call the binary, prove byte-parity against the captured golden, then delete the shims -- staging explicit paths only, never `git add -A`, because AGY shares the tree.
**Where:** `automation/generate-mok-key.sh, automation/enroll-mok.sh, usr/libexec/mios/mios-enroll-secure-boot, src/mios-rs`
**Done When:** A single `mios-mok` binary exposes gen/enroll/status subcommands; the three scripts are gone (or reduced to <=3-line shims); the drift-gate is green and the binary's output is byte-identical to the captured golden.
**Why:** Three divergent MOK codepaths are a correctness hazard with a hard failure mode -- an enroll that disagrees with the key it was generated against bricks Secure Boot on the target machine -- and the retirement/cleanup half is what the two MOK porting tasks set up but never finish.
**Dep:** Port enroll-mok.sh's status_probe to a compiled mios-mok verb with differential parity

### AGY-1512 -- Add a greenboot required.d self-attestation check that auto-rolls-back a tampered running image  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- runtime integrity is continuously attested, not just verified once at publish and once at firstboot.
**What+How:** `automation/78-greenboot.sh` already wires greenboot's `required.d`/`green.d`/`red.d` directories, but nothing verifies the RUNNING image's own signature, so an offline-tampered deployment boots green forever. Add an SSOT-derived check under `/etc/greenboot/check/required.d` (shipped via the system_files overlay) that verifies the booted bootc deployment digest against the on-disk `usr/lib/containers/policy.json` (cosign verify / `bootc image verify`). A failed verify marks the boot RED, which makes greenboot roll back to the last-good deployment automatically. Derive the enable flag and the identity constraints from `[greenboot]` and `[security.sigstore]` so the check composes with the firstboot policy install instead of hardcoding an issuer or a digest.
**Where:** `automation/78-greenboot.sh, /etc/greenboot/check/required.d (system_files overlay), usr/lib/containers/policy.json, usr/share/mios/mios.toml [greenboot]`
**Done When:** A tampered or unsigned running image fails the greenboot required check and triggers a rollback to the previous deployment; a correctly signed image passes; the check reads its identity constraints from SSOT with no literals.
**Why:** Signing at publish plus verifying at firstboot leaves every subsequent boot completely unattested, so a machine tampered with after install stays green indefinitely; the greenboot-health and firstboot-policy tasks each cover one end and neither closes the loop.
**Dep:** Bootstrap on-disk signature-policy enforcement at firstboot gated on the SSOT flag

### AGY-1513 -- Hash-pin the agent-plane and finetune pip installs with --require-hashes  (WS-SBOM | P2 | M)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- install-time wheel integrity for the highest-trust runtime in the image.
**What+How:** `usr/lib/mios/agent-pipe/requirements.txt` and `usr/share/mios/finetune/requirements.txt` are installed by `automation/72-hermes-agent.sh` and `automation/73-model-prep.sh` with no hashes at all -- the string `--require-hashes` appears nowhere in the tree -- so a compromised PyPI or mirror can inject a malicious wheel straight into the agent plane. Generate fully hash-locked requirement files via `pip-compile --generate-hashes`, commit them, switch both installers to `pip install --require-hashes`, record the resolved wheels into the SBOM artifacts, and add a drift-gate check asserting every requirement line carries a hash. This is install-time integrity and is deliberately distinct from the advisory-only pip-audit gate.
**Where:** `usr/lib/mios/agent-pipe/requirements.txt, usr/share/mios/finetune/requirements.txt, automation/72-hermes-agent.sh, automation/73-model-prep.sh, automation/98-drift-checks.sh`
**Done When:** Both pip installs run with `--require-hashes` against committed hash-locked files; a tampered wheel is rejected at install time; the new drift-gate check fails if any requirement lacks a hash.
**Why:** The agent plane is the most privileged runtime in MiOS -- it drives OS-control verbs -- yet it currently pulls Python dependencies with zero integrity pinning, and pip-audit only reports known CVEs while doing nothing whatsoever against a substituted wheel.
**Dep:** Add a pip-audit gate for the agent-plane venv and python libexec tools

### AGY-1514 -- Make a non-empty SBOM a fail-closed publish gate while bake stays degrade-open  (WS-SBOM | P2 | S)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- every published image is guaranteed to carry the provenance record that reproducibility is defined by.
**What+How:** `automation/90-generate-sbom.sh` is deliberately degrade-open (always exits 0), so the CycloneDX + SPDX SBOM can silently vanish on any syft or egress hiccup, and `check_sbom_metadata` (`automation/98-drift-checks.sh:4096`) only validates STRUCTURE of files that exist -- it never asserts existence. Add a PUBLISH-gated fitness function (in `automation/99-postcheck.sh` plus a CI step guarded on `PUBLISH == 'true'`) asserting the published image carries a non-empty CycloneDX AND SPDX SBOM plus all three TSVs (binaries, bound-images, models). Bake keeps its degrade-open behaviour; only publish is fail-closed. Distinct from the binaries.tsv recording gate and from the attach-attestation task.
**Where:** `automation/90-generate-sbom.sh, automation/99-postcheck.sh, automation/98-drift-checks.sh, .github/workflows/mios-ci.yml`
**Done When:** A PUBLISH build with a missing or empty SBOM/TSV fails the gate; the bake step still degrades open; local and non-publish builds are unaffected.
**Why:** Degrade-open silently ships images with no SBOM at all, which means the attach-SBOM-attestation and scan-the-attested-SBOM tasks downstream have nothing to attest or scan; a presence gate at publish is the missing floor under the entire SBOM control stack.
**Dep:** Attach the Syft SBOM to the published image as a signed cosign attestation

### AGY-1515 -- Add a CI determinism job that bakes twice and diffs the resulting image manifests  (WS-SBOM | P3 | L)
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- reproducibility becomes a measured CI fact rather than an asserted property.
**What+How:** `SOURCE_DATE_EPOCH` appears nowhere in the tree today, and once the reproducible-build task lands it only ENABLES determinism without proving it. Add a job to both publishers that bakes the OCI image twice back-to-back and asserts byte-identical rootfs/manifest digests, or else emits a diffoscope report containing only allowlisted nondeterministic paths (logs, timestamps outside SOURCE_DATE_EPOCH's control). Strangler-style rollout: ship it report-only and non-blocking first, then promote it to a required check once it has been consistently green.
**Where:** `.github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml, tests/`
**Done When:** Two back-to-back bakes produce identical manifest digests (or a diffoscope report showing only allowlisted deltas), and the job has been promoted from report-only to required.
**Why:** An unverified SOURCE_DATE_EPOCH claim is exactly the aspirational security assertion this campaign exists to convert into something gated; without the verification half, the SLSA/provenance story rests on an untested assumption.
**Dep:** Make the image build reproducible via git-derived SOURCE_DATE_EPOCH + timestamp rewrite

### AGY-1516 -- Consolidate the two Rust workspaces into one and re-home all eight native crates as members  (WS-TESTGOV | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- a closed workspace so one cargo invocation can gate every line of compiled MiOS code.
**What+How:** `tools/native/Cargo.toml` lists only 3 members (mios-drift-runner, mios-version-check, mios-wallpaperd) while 5 crate directories on disk (generate-names-registry, mios-aiplane-lint, mios-bake-plan, mios-ssot-lint, mios-ssot-walk) sit OUTSIDE the workspace, and `src/mios-rs/` is a SECOND, separate workspace holding miosd. Any `cargo test`/`cargo clippy --workspace` therefore silently skips the orphans. Add every crate as an explicit `members` entry (keeping mios-ssot-walk as a lib) and fold `src/mios-rs/miosd` into the same workspace so one cargo invocation covers all compiled code. Retarget -- do not break -- the `cargo build --release` of src/mios-rs at `Containerfile:46-49` so it points at the unified manifest; stage explicit paths only.
**Where:** `tools/native/Cargo.toml, src/mios-rs/Cargo.toml, src/mios-rs/miosd/Cargo.toml, tools/native/{generate-names-registry,mios-aiplane-lint,mios-bake-plan,mios-ssot-lint,mios-ssot-walk}/Cargo.toml, Containerfile:46-49`
**Done When:** `cargo metadata --manifest-path tools/native/Cargo.toml --no-deps` lists all 8 crates plus miosd as workspace members; `cargo build --workspace --locked` compiles every one; no Cargo.toml directory in the tree remains outside a workspace.
**Why:** Orphaned crates escape every cargo gate, so the entire test/clippy/deny/audit stack this workstream builds is meaningless until the workspace is closed -- five crates are currently uncompiled and untested by CI.
**Dep:** none

### AGY-1517 -- Pin Cargo.lock as a reproducibility SSOT and move crate versions to workspace inheritance  (WS-TESTGOV | P2 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the version literal is single-sourced and dependency resolution is recorded, not re-resolved.
**What+How:** `tools/native/mios-version-check/Cargo.toml` hardcodes `version = "0.3.0"` while its siblings use `version.workspace = true`, and `check_version_ssot` (`automation/98-drift-checks.sh:3629-3634`) only inspects 3 hand-listed Cargo.tomls, so that literal can drift undetected. Switch every literal-version member to `version.workspace = true`, commit the resolved `tools/native/Cargo.lock` and `src/mios-rs/Cargo.lock`, and make every CI and build cargo call pass `--locked`. Extend the Cargo loop in check_version_ssot to iterate ALL members discovered via `cargo metadata` instead of the hand-listed three, so a newly added crate cannot dodge the version gate.
**Where:** `tools/native/mios-version-check/Cargo.toml, tools/native/Cargo.lock, src/mios-rs/Cargo.lock, automation/98-drift-checks.sh:3598-3650`
**Done When:** `cargo build --workspace --locked` succeeds with zero lockfile churn; check_version_ssot fails when any member Cargo.toml version differs from mios.toml's mios_version; a grep finds no literal `version = "` in any member manifest.
**Why:** A committed lockfile operationalizes SBOM-not-hardcode (dependency versions recorded at build rather than hand-pinned in source) and closes the TD-2 version-duplication drift class that currently lets one crate quietly ship the wrong version string.
**Dep:** Consolidate the two Rust workspaces into one

### AGY-1518 -- Add a fast, cached pre-bake rust-gate CI job running cargo build + test on every PR  (WS-TESTGOV | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- a 4-minute fail lane so a broken Rust port never reaches the hour-long bake.
**What+How:** CI has ZERO cargo steps today: the drift-gate and build jobs in `.github/workflows/mios-ci.yml` never compile or test any Rust, leaving miosd/drift.rs and 7 tools crates entirely ungated. Add a `rust-gate` job (`runs-on: ubuntu-24.04`, `needs: []` so it runs in parallel with drift-gate and before build) that installs the pinned toolchain via rustup, restores `~/.cargo` and `target/` with actions/cache keyed on Cargo.lock, then runs `cargo build --workspace --locked` and `cargo test --workspace --locked`. Mirror the job byte-for-byte into `.forgejo/workflows/`, since GitHub and Forgejo are equal publishers (Law 15).
**Where:** `.github/workflows/mios-ci.yml (new job alongside drift-gate ~51-128), .forgejo/workflows/, tools/native/Cargo.toml, src/mios-rs/Cargo.toml`
**Done When:** PRs show a rust-gate check that builds and tests the whole workspace in under 5 minutes with a warm cache; a failing `cargo test` blocks merge; the identical job exists in the Forgejo workflow.
**Why:** A compiled-tested-code campaign with no cargo CI is unenforced -- every subsequent port in this workstream assumes this gate exists, and without it a Rust regression is only discovered an hour later at bake time, or not at all.
**Dep:** Consolidate the two Rust workspaces into one

### AGY-1519 -- Enforce cargo clippy -D warnings with restriction lints banning panic/unwrap in system verbs  (WS-TESTGOV | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the strictly-stronger shellcheck the compiled tier needs, tuned so a verb cannot panic on bad config.
**What+How:** Add a `[workspace.lints.clippy]` table to `tools/native/Cargo.toml` and `src/mios-rs/Cargo.toml` setting `-D warnings` plus targeted restriction lints at deny: `unwrap_used`, `expect_used`, `panic`, `todo`, `unimplemented` -- because a system verb that panics on a malformed mios.toml is a fleet-wide hazard. Add `cargo clippy --workspace --all-targets --locked -- -D warnings` to the rust-gate job. Where existing code trips the lints (`src/mios-rs/miosd/src/drift.rs` has an `.unwrap()` on a Regex and an `unwrap_or_default()`), fix it or add a narrowly scoped `#[allow]` carrying a justification comment -- never a blanket allow at crate root.
**Where:** `tools/native/Cargo.toml ([workspace.lints]), src/mios-rs/Cargo.toml, src/mios-rs/miosd/src/drift.rs, .github/workflows/mios-ci.yml (rust-gate)`
**Done When:** `cargo clippy --workspace -- -D warnings` is green and blocks CI on any regression; `clippy::unwrap_used` and `clippy::panic` are denied workspace-wide; miosd/drift.rs contains no unjustified unwrap.
**Why:** The tech-debt map records that MiOS has no shellcheck-equivalent for its compiled tier, so panic-on-bad-config bugs that bash never guarded against are shipping unreviewed into binaries that run as system verbs.
**Dep:** Add a fast, cached pre-bake rust-gate CI job

### AGY-1520 -- Add a cargo fmt --check formatting gate plus a shared rustfmt.toml  (WS-TESTGOV | P2 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- deterministic Rust formatting so review effort goes to logic, not whitespace.
**What+How:** No rustfmt.toml exists anywhere. Add a minimal `tools/native/rustfmt.toml` (mirrored for `src/mios-rs`) pinning edition 2021 and stable defaults, run `cargo fmt --all` once to normalize the tree, then add `cargo fmt --all --check` as a step in the rust-gate job. Keep the normalization commit strictly mechanical (formatting only, no logic edits) and stage explicit paths -- never `git add -A`, since AGY shares the tree.
**Where:** `tools/native/rustfmt.toml (new), src/mios-rs/rustfmt.toml, .github/workflows/mios-ci.yml (rust-gate), */src/*.rs`
**Done When:** `cargo fmt --all --check` exits 0 on a clean tree and turns CI red on any unformatted hunk.
**Why:** Without a formatting gate every strangler-fig port PR carries incidental whitespace churn that hides the real diff and makes byte-parity review harder than it needs to be.
**Dep:** Add a fast, cached pre-bake rust-gate CI job

### AGY-1521 -- Add a cargo-deny supply-chain gate covering licenses, bans, sources and advisories  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the FOSS-licensing posture encoded as machine-checked policy rather than as an assumption.
**What+How:** No deny.toml exists. Author `tools/native/deny.toml` with an SPDX license allow-list operationalizing the FOSS posture (permit MIT/Apache-2.0/BSD/ISC/Unicode; deny copyleft-incompatible), a duplicate-crate ban, a `[sources]` allow-list restricted to crates.io, and `[advisories]` set to deny. Add a `cargo deny --manifest-path tools/native/Cargo.toml check` step to CI, plus a `check_cargo_deny` fitness function in `automation/98-drift-checks.sh` that degrades open ONLY when cargo-deny is genuinely absent (mirroring the shellcheck degrade-open pattern in lint-shell.sh). Register the new check in `main()` and mirror the CI step into the Forgejo workflow.
**Where:** `tools/native/deny.toml (new), .github/workflows/mios-ci.yml, automation/98-drift-checks.sh (new check_cargo_deny + main registration), .forgejo/workflows/`
**Done When:** `cargo deny check` passes on the current tree and fails on an injected GPL, duplicate, or yanked dependency; the check appears in the drift-gate's main() registration and in Forgejo CI.
**Why:** The compiled tier currently has no license or dependency-provenance story at all, so a copyleft-incompatible or yanked transitive crate can enter the shipped image with nothing to stop it.
**Dep:** Pin Cargo.lock as a reproducibility SSOT

### AGY-1522 -- Add a fast cargo-audit RustSec advisory gate to the pre-bake lane  (WS-TESTGOV | P2 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- a per-PR security tripwire that fires in seconds, not at bake time.
**What+How:** Distinct from the heavier cargo-deny full check, add a quick `cargo audit --deny warnings` step (RustSec advisory database only) to the fast rust-gate job, so a newly disclosed CVE in a transitive dependency fails a PR immediately rather than waiting on the license and ban scans. Cache the advisory-DB clone between runs, and pin the tool itself with `cargo install --locked cargo-audit@<ver>` so the gate is as reproducible as what it gates.
**Where:** `.github/workflows/mios-ci.yml (rust-gate job), tools/native/Cargo.lock, .forgejo/workflows/`
**Done When:** rust-gate runs `cargo audit` against the committed Cargo.lock and fails on any advisory; a deliberately vulnerable pinned dependency turns the job red.
**Why:** Without a fast advisory check, a known-vulnerable dependency can sit in the lockfile across many merges before any slower scan notices, and a security regression discovered at bake time has already cost an hour of runner budget.
**Dep:** Add a fast, cached pre-bake rust-gate CI job

### AGY-1523 -- Add a just gate-rust recipe and call it from drift-gate so local equals CI  (WS-TESTGOV | P3 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- one command reproduces the full CI verdict on a developer machine.
**What+How:** The `drift-gate` recipe in the Justfile (lines 81-113) runs the shell and python gates but no cargo at all. Add a `gate-rust` recipe running `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test --workspace --locked`, `cargo deny check` and `cargo audit`, then invoke it from `drift-gate` so a local `just drift-gate` reproduces the CI rust-gate exactly. Guard each cargo invocation behind a `command -v cargo` degrade-open so the recipe still runs the shell gates unchanged on a cargo-less host.
**Where:** `Justfile (drift-gate ~81-113, new gate-rust recipe)`
**Done When:** `just gate-rust` runs all five cargo gates locally, `just drift-gate` invokes it, and both are green on a clean tree.
**Why:** When local and CI gates diverge, developers push work that only fails remotely, which burns runner time and erodes trust in the ratchet that the rest of this workstream depends on.
**Dep:** Enforce cargo clippy -D warnings

### AGY-1524 -- Stand up a trycmd CLI-snapshot harness pinning Rust mios-ssot-lint byte-for-byte to 97-ssot-lint.sh  (WS-TESTGOV | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the reusable golden-master pattern every strangler-fig verb port will copy.
**What+How:** `check_ssot_lint_equivalence` (`automation/98-drift-checks.sh:4870-4917`) already proves the Rust `mios-ssot-lint` is byte-identical to bash `automation/97-ssot-lint.sh`, but only via an ad-hoc shell diff. Formalize it as a real trycmd snapshot test INSIDE the crate: add trycmd as a dev-dependency, capture the current bash output over the repo fixture as a `.trycmd` golden (stdout, stderr and exit code), and assert the Rust binary reproduces it exactly. This establishes the pattern the whole migration follows -- snapshot bash first, port, prove parity, only then delete bash.
**Where:** `tools/native/mios-ssot-lint/Cargo.toml (dev-dep), tools/native/mios-ssot-lint/tests/cli.rs (new), tools/native/mios-ssot-lint/tests/cmd/*.trycmd (new), automation/97-ssot-lint.sh, automation/98-drift-checks.sh:4870`
**Done When:** `cargo test -p mios-ssot-lint` runs the trycmd snapshot; changing the Rust output without updating the golden fails the test; the committed golden matches current 97-ssot-lint.sh output.
**Why:** Without an accept/review characterization oracle, every verb port is a hope-based rewrite, and the existing ad-hoc twin-diff cannot be reused by the other 143 libexec tools that still need porting.
**Dep:** Consolidate the two Rust workspaces into one

### AGY-1525 -- Add insta snapshots of the userenv.sh MIOS_* env projection as the golden the Rust resolver must reproduce  (WS-TESTGOV | P1 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the resolver port becomes a measured deviation-from-golden exercise instead of a rewrite.
**What+How:** Create a resolver-parity test module that shells `tools/lib/userenv.sh` against the committed `usr/share/mios/mios.toml`, captures the full sorted `MIOS_*` KEY=VALUE projection, and stores it as an insta `.snap` golden -- normalizing root paths the same way `check_resolver_twin_equivalence` does with `env -i`. That snapshot becomes the byte-for-byte target for the future figment/serde Rust resolver: when a value is ported, the `.snap` must not change. Wire `cargo insta test` into the rust-gate job and commit the accepted `.snap` files.
**Where:** `tools/native/ (new resolver-parity crate or tests/), tools/lib/userenv.sh, usr/lib/mios/userenv.sh, usr/share/mios/mios.toml, tools/check-resolver-twin.py (normalization reference)`
**Done When:** `cargo insta test` produces a stable MIOS_* projection snapshot, and any resolver change that alters a key or value fails until `cargo insta accept` is run under review.
**Why:** A single wrong resolved value silently breaks a Quadlet at runtime with no build-time signal, so porting the resolver without a committed golden risks a fleet-visible regression that nothing in CI would catch.
**Dep:** Consolidate the two Rust workspaces into one

### AGY-1526 -- Capture a goldenfile master of full 98-drift-checks.sh stdout before any check is ported to miosd  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the governance surface itself is ported under proof, slice by slice.
**What+How:** Before the roughly 150-function bash drift-gate is incrementally moved into `miosd drift-check` (`src/mios-rs/miosd/src/drift.rs` currently implements only the SSOT-parse and backfill-coverage checks), record the full normalized stdout and exit status of `automation/98-drift-checks.sh` over a frozen fixture tree as a goldenfile master stored under a `tests/golden` directory. Every subsequently ported check must leave the combined, path-normalized output byte-identical, making parity provable per slice. Use the goldenfile crate's `UPDATE_GOLDENFILES=1` review flow so any intentional wording change is an explicit reviewed act.
**Where:** `src/mios-rs/miosd/tests/golden_drift.rs (new), src/mios-rs/miosd/tests/golden/ (new), automation/98-drift-checks.sh, src/mios-rs/miosd/src/drift.rs`
**Done When:** `cargo test -p miosd` diffs miosd output against the golden master; a ported check that changes its wording or drops a line fails until the golden is reviewed and updated.
**Why:** The drift-gate IS the enforcement plane -- porting it without a golden master risks silently dropping a check, which would leave a law unenforced while the gate still reports green.
**Dep:** none

### AGY-1527 -- Add trycmd fixtures locking the miosd drift-check contract including the backfill-coverage FAIL path  (WS-TESTGOV | P2 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the one real compiled drift target gets its own regression floor before it grows.
**What+How:** miosd already ships `drift-check --root --soft` plus `check_backfill_coverage` in drift.rs. Add trycmd fixtures capturing its PASS and FAIL stdout and exit codes against two small fixture repos: one with a matching schema, and one with an emb-vector table missing from PK_MAP to force the FAIL path. This pins the daemon's user-facing contract so the Scaffold stub and future check additions cannot regress existing behaviour unnoticed.
**Where:** `src/mios-rs/miosd/tests/cli.rs (new), src/mios-rs/miosd/tests/cmd/*.trycmd (new), src/mios-rs/miosd/src/main.rs, src/mios-rs/miosd/src/drift.rs`
**Done When:** `cargo test -p miosd` exercises both the pass and the fail drift-check paths through trycmd, with exit codes 0/1 and full output pinned.
**Why:** miosd is where the entire drift-gate is headed, and it currently has no test of its own -- so any change to its CLI surface or check logic can regress silently, and there is no suite for a later mutation gate to measure.
**Dep:** Consolidate the two Rust workspaces into one

### AGY-1528 -- Snapshot generated Quadlets with insta so digest-drift becomes an accept/review event  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the highest-recurrence drift class is caught pre-bake as a reviewable diff.
**What+How:** The recurring quadlet-digest-drift class (a broad `git add` strips `@sha256` pins and turns `check_pod_quadlets` red) is fundamentally a projection problem: the `.container` files are generated but hand-staged. Add an insta snapshot test that renders the Quadlets from mios.toml through the `automation/34-render-quadlets.sh` / userenv.sh path into a temp directory and snapshots each unit's normalized content, so a stripped pin or a drifted Exec line surfaces as a reviewable `.snap` diff instead of a build-time surprise. Pair it with `check_pod_quadlets` (`automation/98-drift-checks.sh:872`) -- the snapshot is the early warning ahead of the gate.
**Where:** `tools/native/ (new render-parity test), automation/34-render-quadlets.sh, usr/share/containers/systemd/, automation/98-drift-checks.sh:872`
**Done When:** `cargo insta test` snapshots the rendered Quadlet set, removing an `@sha256` pin fails the snapshot, and the accepted `.snap` files are committed.
**Why:** This drift recurs constantly and is currently only discovered when the gate goes red mid-build, costing a regenerate-and-restage cycle every time instead of failing fast with a diff that shows exactly which pin was lost.
**Dep:** none

### AGY-1529 -- Add a golden parity harness pinning Rust mios-bake-plan to generate-bake-plan.py output  (WS-TESTGOV | P2 | S)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- a differential golden that de-risks demoting the Python bake-planner to fallback.
**What+How:** `check_bake_plan` (`automation/98-drift-checks.sh:3845`) runs `tools/generate-bake-plan.py --check`, and a Rust `mios-bake-plan` binary already exists which `automation/85-bake-plan.sh` prefers at `tools/native/target/release/mios-bake-plan`. Add a trycmd/insta golden asserting the Rust planner emits byte-identical output to the Python generator over the committed `[build.bake]` section of mios.toml, so the two implementations cannot diverge as the SSOT changes.
**Where:** `tools/native/mios-bake-plan/tests/ (new), tools/native/mios-bake-plan/src/main.rs, tools/generate-bake-plan.py, automation/85-bake-plan.sh, automation/98-drift-checks.sh:3845`
**Done When:** `cargo test -p mios-bake-plan` proves Rust output equals Python output; a `[build.bake]` change honoured by only one implementation fails the test.
**Why:** Two live bake-planners already run in the same pipeline with nothing asserting they agree, so a divergence would silently change what gets baked into the published image depending on which binary was present.
**Dep:** Consolidate the two Rust workspaces into one

### AGY-1530 -- Golden-fixture mios-hardcode-lint (including its unanchored-allowlist bug) ahead of the Rust port  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- faithful port and bug fix stay separable, and the fix feeds the zero-hardcodes campaign.
**What+How:** `usr/libexec/mios/mios-hardcode-lint` (360 lines of Python, the Law 7 NO-HARDCODE enforcer) has a known unanchored-allowlist bug that spuriously exempts ports such as `:8080` and `:8033`. Before porting it, build a fixture tree exercising each violation class (date-in-comment, dated string-literal prose, header BOM/shebang risk) plus the buggy allowlist case, and capture current stdout and exit code as a trycmd golden. The Rust port must FIRST reproduce that golden byte-for-byte, proving faithful parity; only THEN does a separate, reviewed golden update encode the allowlist-anchor fix -- so the behaviour change is explicit rather than smuggled inside the port.
**Where:** `usr/libexec/mios/mios-hardcode-lint, tools/native/ (future mios-hardcode-lint crate + tests/cmd/*.trycmd), automation/98-drift-checks.sh:978`
**Done When:** A trycmd golden captures current hardcode-lint output across all violation classes, the Rust port matches it exactly, and a subsequent reviewed snapshot update encodes the anchored-allowlist fix.
**Why:** Porting a buggy tool without characterizing it first makes it impossible to tell a port regression from the pre-existing bug -- and the unanchored allowlist is currently hiding real `:80xx` violations that the zero-hardcodes campaign has to find.
**Dep:** none

### AGY-1531 -- Characterization golden for agent-pipe verb dispatch and the eval-on-agent-args path before hardening  (WS-TESTGOV | P2 | L)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the eval removal at a security boundary is proven safe rather than hoped safe.
**What+How:** The tech-debt map flags 9 eval-on-agent-args verbs, and `check_cli_eval_safety` (`automation/98-drift-checks.sh:4027`) guards that surface; agent-supplied args are an untrusted boundary. Capture the current observable dispatch behaviour -- verb to resolved action, across a table of representative AND adversarial inputs -- as a golden characterization test, seeded from the existing `tests/test-directory-dispatch.sh` and `tests/test-dispatcher-defense.py`. That golden is the parity oracle for replacing the eval dispatch with a hardened case/associative-array form (or a Rust dispatcher): the replacement must reproduce every legitimate mapping and reject every injection.
**Where:** `usr/lib/mios/mios_dispatch.py, usr/lib/mios/agent-pipe/server.py, tests/test-directory-dispatch.sh, tests/test-dispatcher-defense.py, automation/98-drift-checks.sh:4027`
**Done When:** A committed golden fixes the dispatch mapping for both legitimate and adversarial verbs; the hardened dispatcher passes it with zero `eval`; injection inputs are rejected under test.
**Why:** Dispatch is the boundary where untrusted agent arguments meet OS-control verbs, and rewriting it without a characterization oracle risks either reintroducing the injection surface or silently breaking a legitimate verb the agent plane depends on.
**Dep:** none

### AGY-1532 -- proptest differential resolver oracle: random mios.toml asserts mios_toml.py and userenv.sh emit identical MIOS_*  (WS-TESTGOV | P1 | L)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- Law 13 twin-equivalence becomes a fuzzed invariant instead of a spot check.
**What+How:** `tools/check-resolver-twin.py` only spot-checks the Python-vs-shell resolver twins; upgrade it to property-based differential testing. Write a proptest harness that generates random-but-valid mios.toml documents (varying `[ports]`, `[colors]`, `[ai.*]`), writes each to a temp vendor path, runs BOTH `usr/lib/mios/mios_toml.py` and `tools/lib/userenv.sh`, and asserts the emitted MIOS_* key-set and values are identical -- auto-shrinking to a minimal failing document on divergence. This mechanizes the short-vs-long key mismatch class (`MIOS_AI_VLLM_*` emitted vs `MIOS_VLLM_*` consumed) and becomes the acceptance oracle for the future single Rust resolver.
**Where:** `tools/native/ (new resolver-parity crate), usr/lib/mios/mios_toml.py, tools/lib/userenv.sh, usr/lib/mios/userenv.sh, tools/check-resolver-twin.py, automation/98-drift-checks.sh:3181`
**Done When:** `cargo test` runs at least 256 proptest cases through both resolvers; a seeded divergence shrinks to a minimal mios.toml and fails; the harness is wired into rust-gate.
**Why:** The resolver is the highest-leverage component in the tree -- a single divergent value silently breaks a Quadlet at runtime -- and example-based spot checks systematically miss the edge cases that the GUP lossless-diff gate later depends on.
**Dep:** Consolidate the two Rust workspaces into one

### AGY-1533 -- proptest round-trip: typed serde config to MIOS_* and back is identity for the resolver-core crate  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the compiled resolver proves its env projection is lossless before bash is retired.
**What+How:** As the ad-hoc parsers collapse into one typed resolver (figment/serde deserializing mios.toml into structs, with MIOS_* becoming a serialization FROM that model), add a proptest round-trip invariant: for a generated typed config value, serialize to MIOS_* env, re-deserialize, and assert structural identity with no lossy string-munging. This is the resolver-core crate's self-consistency proof and directly supports collapsing `mios_toml.py`, `mios_env.py` and userenv.sh's roughly 200 manual tuples into a single schema.
**Where:** `tools/native/ (resolver-core crate), usr/lib/mios/mios_toml.py, usr/lib/mios/mios_env.py, tools/lib/userenv.sh`
**Done When:** `cargo test` round-trips generated typed configs through MIOS_* and back under proptest asserting identity, with failures shrinking to the minimal offending field.
**Why:** Retiring the bash resolver without a losslessness proof means a config field can silently degrade through the env projection, and the failure would only appear as a misbehaving service on a deployed host.
**Dep:** proptest differential resolver oracle

### AGY-1534 -- proptest mios-ssot-walk for determinism, stable ordering and key-collision freedom  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- the one-canonical-name law is hardened against fuzzed SSOT input.
**What+How:** `mios-ssot-walk` (a lib crate) is the canonical SSOT walk that feeds the names registry. Add proptest cases generating nested TOML tables and asserting the walk is (a) deterministic across repeated runs, (b) stable-ordered under systemd `.d` lexical semantics, and (c) collision-free -- no two distinct paths may flatten to the same MIOS_* key, the exact class guarded by `check_names_registry` (`automation/98-drift-checks.sh:2064`) and `check_no_duplicate_value_key` (`:6097`). Failures shrink to the minimal colliding table.
**Where:** `tools/native/mios-ssot-walk/src/lib.rs, tools/native/mios-ssot-walk/tests/ (new), automation/98-drift-checks.sh:2064, :6097`
**Done When:** `cargo test -p mios-ssot-walk` runs the determinism, ordering and collision invariants under proptest, and a synthetic colliding table fails and shrinks to a minimal case.
**Why:** The walk is the mechanical half of names-registry codegen, so a nondeterministic or colliding walk would produce a registry that silently maps two distinct SSOT facts onto one env var -- a Law 9 violation that the existing checks only catch for the shapes that happen to exist today.
**Dep:** Consolidate the two Rust workspaces into one

### AGY-1535 -- Add a mypy --strict gate over usr/lib/mios with a shrink-only per-module opt-out baseline  (WS-TESTGOV | P2 | L)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- brings mypy to the Python plane so the AI-plane half of the SSOT resolver is machine-checked, not asserted.
**What+How:** `automation/lint-python.sh` today only does `ast.parse`/py_compile -- zero type checking. Add a `mypy.ini` with global `strict = True` plus a `[mypy-*]` per-module opt-out roster seeded permissive for the legacy agent-pipe modules, and enforce that the roster may only SHRINK (a newly-added untyped module fails). Add a `check_mypy` step to `lint-python.sh` and to the drift-gate Python step in CI, degrading open ONLY when mypy is genuinely absent. Type the two resolver leaves first -- `usr/lib/mios/mios_toml.py` and `usr/lib/mios/mios_env.py` -- to strict-clean, since they are the code being ported to Rust.
**Where:** `mypy.ini (new), automation/lint-python.sh, .github/workflows/mios-ci.yml (drift-gate Python step ~67-70), usr/lib/mios/mios_toml.py, usr/lib/mios/mios_env.py`
**Done When:** `mypy --strict` passes for the ratcheted module set and gates CI; adding a module to the opt-out roster fails; `mios_toml.py` and `mios_env.py` are strict-clean.
**Why:** `ast.parse` catches only syntax, so every type-confusion bug in the AI plane ships today; without it the resolver port has no typed contract to port against.
**Dep:** none

### AGY-1536 -- Add a ruff lint gate plus a pyproject [tool.ruff] config over the whole Python surface  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance -- gives Python the fast static hazard scan bash already gets from shellcheck.
**What+How:** No ruff or pyproject config exists. Add `pyproject.toml` `[tool.ruff]` selecting a curated rule set -- pyflakes `F`, bugbear `B`, the bandit subset `S`, pycodestyle `E` -- with an explicit per-path ignore list, and extend `automation/lint-python.sh` to run `ruff check` after the ast.parse pass (degrade-open if ruff is absent). Wire the same invocation into the drift-gate Python step in CI. The `S` rules directly flag the `subprocess`/`eval` hazards that the eval-on-agent-args debt work targets, so this gate doubles as the sentinel keeping that class from returning.
**Where:** `pyproject.toml (new), automation/lint-python.sh, .github/workflows/mios-ci.yml (drift-gate ~67), usr/lib/mios/**/*.py, tools/*.py`
**Done When:** `ruff check` is clean on the curated rule set and gates CI; a seeded unused import or a new bandit `S`-rule violation turns the gate red.
**Why:** Every dead import, mutable-default and `eval`/`subprocess` hazard in the Python plane is currently invisible to CI, and the eval-injection debt class can silently reappear once fixed.
**Dep:** none

### AGY-1537 -- Convert the ad-hoc test_mios_*.py loop to pytest with a ratcheting coverage floor  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance -- turns AI-plane testing from an unaggregated shell loop into a measurable, monotonically-improving gate.
**What+How:** The drift-gate runs agent-pipe tests through a hand-rolled `for t in test_mios_*.py` loop (`Justfile:86-98`, `mios-ci.yml:76-112`) with an inline SKIP list, which aggregates nothing and can swallow failures. Replace it with pytest + pytest-cov: encode the SKIP list as proper pytest markers instead of a shell string, and add `--cov=usr/lib/mios --cov-fail-under=<N>` where N is a committed baseline that may only RISE.
**Where:** `pyproject.toml or pytest.ini (new), Justfile:86-98, .github/workflows/mios-ci.yml:76-112, usr/lib/mios/agent-pipe/test_mios_*.py`
**Done When:** `pytest` runs the agent-pipe suite with coverage reporting; a coverage drop below the committed baseline fails CI; the shell for-loop and inline SKIP string no longer exist in the Justfile or workflow.
**Why:** A for-loop that swallows exit codes is not a gate -- agent-pipe test strength is currently unmeasured and free to regress.
**Dep:** none

### AGY-1538 -- Add a PowerShell AST-parse gate over every tracked *.ps1  (WS-TESTGOV | P2 | M)
**Goal:** E-03 PowerShell surface: parse-gate, deduplicate, compile -- lands the parse-gate leg so the grandfathered PS surface has a floor while it awaits port.
**What+How:** No PowerShell gate exists at all: `build-mios.ps1` (~619KB), `Get-MiOS.ps1` (~420KB), `automation/lib/globals.ps1` and the launcher .ps1s are wholly unchecked. Add `automation/lint-powershell.ps1` that iterates every tracked `*.ps1` and calls `[System.Management.Automation.Language.Parser]::ParseFile($path,[ref]$null,[ref]$errors)`, failing on any parse error. Add a `check_powershell_lint` fitness-function to `automation/98-drift-checks.sh` that invokes it via `pwsh` (present on the ubuntu-24.04 runner) and degrades open only if pwsh is truly absent. Register it in `main()` and add a matching negative test.
**Where:** `automation/lint-powershell.ps1 (new), automation/98-drift-checks.sh (new check + main registration), automation/lib/globals.ps1, build-mios.ps1, Get-MiOS.ps1, tests/drift-gate-negatives.sh`
**Done When:** The AST gate parses every tracked `.ps1` and fails on a seeded syntax error; `check_powershell_lint` appears in `main()` and has a negative in `tests/drift-gate-negatives.sh`.
**Why:** ~1MB of load-bearing PowerShell -- including the Windows build driver and the install front door -- can currently be committed syntactically broken and only fail on an operator's machine.
**Dep:** none

### AGY-1539 -- Add a PSScriptAnalyzer Error-severity gate with a committed settings file  (WS-TESTGOV | P3 | L)
**Goal:** E-03 PowerShell surface -- adds the shellcheck-equivalent layer above the parse gate so the grandfathered PS surface can only improve.
**What+How:** Layer PSScriptAnalyzer on the AST gate: add `PSScriptAnalyzerSettings.psd1` selecting Error-severity rules plus a curated Warning subset, and extend `automation/lint-powershell.ps1` to run `Invoke-ScriptAnalyzer -Settings <file> -Severity Error` across the PS surface, best-effort installing the module on the runner (degrade-open). Start Error-only so the large legacy files pass on day one, then ratchet the settings file toward Warning as files are ported to `miosd` or retired.
**Where:** `PSScriptAnalyzerSettings.psd1 (new), automation/lint-powershell.ps1, .github/workflows/mios-ci.yml (drift-gate), automation/98-drift-checks.sh (check_powershell_lint)`
**Done When:** `Invoke-ScriptAnalyzer -Severity Error` is clean over the tree and gates CI; a seeded Error-level violation fails the gate.
**Why:** Parse-clean PowerShell can still be semantically broken (unreachable code, unapproved verbs, unsafe defaults), and with no severity floor the port-pending surface is free to get worse.
**Dep:** Add a PowerShell AST-parse gate over all *.ps1

### AGY-1540 -- Add cargo-mutants mutation testing on resolver-core and miosd with a shrink-only survivor budget  (WS-TESTGOV | P3 | L)
**Goal:** E-05 Test and CI governance -- proves the compiled-tier test suites actually have teeth before they are trusted as the byte-parity net for the Rust port.
**What+How:** Once the resolver-parity proptest and the `miosd` goldenfile master exist, run `cargo mutants` against the `resolver-core` and `miosd` crates: a surviving mutant is a behavior no test observes. Add a `just mutants` recipe and a SCHEDULED (not per-PR -- mutants is slow) CI job carrying a committed max-survivors budget file that may only shrink. Triage survivors by adding golden/proptest cases until the count is under budget.
**Where:** `tools/native/ (resolver-core), src/mios-rs/miosd/, Justfile (new mutants recipe), .github/workflows/ (scheduled job)`
**Done When:** `cargo mutants` runs against both crates; surviving mutants are at or below the committed budget; the budget file is enforced shrink-only.
**Why:** Snapshot and proptest suites can be vacuous -- without mutation evidence, the compiled port is greenlit by tests that may assert nothing.
**Dep:** proptest differential resolver oracle

### AGY-1541 -- Ratchet check_negative_test_coverage to auto-derive its required set from the gate registry  (WS-TESTGOV | P2 | M)
**Goal:** E-06 Test and documentation harness: negative self-tests, coverage, doc integrity -- makes "every gate proves it can fail" mechanical instead of hand-listed.
**What+How:** `check_negative_test_coverage` (`automation/98-drift-checks.sh:4769`) hardcodes a 42-item `required_checks` list, so a brand-new `check_*` ships with no negative and nobody notices. Replace the static list with an auto-derived one: parse every `check_*()` defined in `98-drift-checks.sh` (reusing the `check_gate_registry:4620` regex) and require a matching `test_*` in `tests/drift-gate-negatives.sh`, with an explicit, shrink-only exemption roster for checks that are structurally untestable-negative.
**Where:** `automation/98-drift-checks.sh:4769, :4620, tests/drift-gate-negatives.sh`
**Done When:** Adding a new `check_*` with no negative test fails `check_negative_test_coverage`; the required set is derived from defined checks rather than a hand list; exemptions live in a shrink-only roster.
**Why:** The 42-item list is already stale relative to 123+ checks, so an unknown number of fitness-functions today have never been proven capable of failing.
**Dep:** none

### AGY-1542 -- Add check_migration_ratchet: bash-script count may only fall, cargo-test count may only rise  (WS-TESTGOV | P2 | M)
**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and libexec tool fleet -- gives the strangler-fig migration a directional gate so it cannot stall or backslide.
**What+How:** Add a `check_migration_ratchet` fitness-function recording a committed baseline of two numbers: the count of non-shim bash scripts under `automation/` and `usr/libexec/mios/`, and the count of `#[test]`/trycmd/proptest cases in the Cargo workspace. Fail the gate if the bash count GROWS or the test count SHRINKS between commits. Store the baseline in `usr/share/mios/mios.toml` `[migration]` so it is operator-visible SSOT rather than a hidden file, and mirror that surface into mios-bootstrap.git per Law 15.
**Where:** `automation/98-drift-checks.sh (new check_migration_ratchet + main), usr/share/mios/mios.toml ([migration] baseline), tests/drift-gate-negatives.sh, mios-bootstrap repo mirror`
**Done When:** Adding a new non-shim bash script fails the ratchet; deleting a cargo test fails it; the baseline is in `mios.toml [migration]` and mirrored in mios-bootstrap.git; a negative test exists.
**Why:** Nothing today stops net-new bash from landing beside the Rust port, so the 121 automation scripts and 144 libexec tools can grow faster than they are retired.
**Dep:** Add a fast, cached pre-bake rust-gate CI job

### AGY-1543 -- Land candidate Law 17 THIN-GLUE-BUDGET + check_bash_thin_glue_budget  (WS-TESTGOV | P1 | L)
**Goal:** E-05 Test and CI governance -- turns "bash is thin glue only" from a stated policy into a numbered, registered, enforced law (ADR + `[laws]` row + check).
**What+How:** Law 14 TARGET-LANGUAGES (`mios.toml [laws]`, enforced by `check_target_languages:3814`) only blocks NEW non-target-language files -- it puts no cap on bash size or complexity, so a 900-line bash program is legal today. Add Law 17 THIN-GLUE-BUDGET to the `[laws]` table plus a `check_bash_thin_glue_budget` fitness-function failing any bash script under `automation/` or `usr/libexec/mios/` that exceeds the threshold (>200 lines OR >8 functions OR non-trivial control flow) unless it sits on a shrink-only roster in a new `mios.toml [laws.thin_glue].grandfathered`. Register in `main()` and in `check_law_enforcers:5698`, add a negative test, mirror the `[laws]`/`[laws.thin_glue]` SSOT into mios-bootstrap.git (Law 15), and update the law list in CLAUDE.md/AGENTS.md. Seed the roster from today's oversized scripts so the gate lands green and can only tighten.
**Where:** `usr/share/mios/mios.toml ([laws] + new [laws.thin_glue]), automation/98-drift-checks.sh (new check + main + :5698), tests/drift-gate-negatives.sh, mios-bootstrap repo [laws] mirror, CLAUDE.md/AGENTS.md law list`
**Done When:** A new >200-line non-shim bash script fails `check_bash_thin_glue_budget`; the grandfather roster is shrink-only; Law 17 appears in `mios.toml [laws]` in BOTH repos; a negative test exists.
**Why:** Without a size budget the language law is toothless -- new bash programs keep landing legally, and every later port has no defined ceiling to shrink under.
**Dep:** none

### AGY-1544 -- Add check_shim_delegates: a demoted bash verb must be a thin shim that execs its Rust binary  (WS-TESTGOV | P2 | M)
**Goal:** E-01 Compiled native tier -- keeps the strangler-fig parallel-run window honest so bash and Rust cannot silently diverge mid-port.
**What+How:** As leaf verbs are ported, the bash original is demoted to a thin shim behind the new binary. Add a `check_shim_delegates` fitness-function that reads a new `mios.toml [migration.ported_verbs]` roster mapping verb -> crate and asserts, for each listed verb, that it (a) is under a tiny line budget, (b) actually execs/dispatches to its Rust binary or the `${VAR:-LIT}` fallback, and (c) names a crate that is a resolved workspace member and builds. The deprecated alias `usr/libexec/mios/mios-theme-render` -- an 11-line delegator to `mios-dotfiles-render` -- is the shape precedent. Register in `main()` and add a negative.
**Where:** `usr/share/mios/mios.toml ([migration.ported_verbs]), automation/98-drift-checks.sh (new check + main), usr/libexec/mios/mios-theme-render (precedent), tools/native/Cargo.toml, tests/drift-gate-negatives.sh`
**Done When:** A verb listed as ported that still carries business logic fails the check; a ported verb whose crate is not a workspace member fails; a negative test exists.
**Why:** A "ported" verb whose bash still holds real logic means two divergent implementations of the same behavior ship in the image, with no signal for which one ran.
**Dep:** Add candidate Law 17 THIN-GLUE-BUDGET

### AGY-1545 -- Add check_native_workspace_gated: every crate dir is a workspace member and CI compiles/tests it  (WS-TESTGOV | P1 | S)
**Goal:** E-05 Test and CI governance -- closes the workspace so no compiled code can escape the cargo gates.
**What+How:** The orphaned-crate hazard is real and measured (5 of 8 `tools/native` crates sat outside `members`). Add a `check_native_workspace_gated` fitness-function that (a) enumerates every directory containing a `Cargo.toml` under `tools/native/` and `src/mios-rs/` and asserts each is a resolved workspace member via `cargo metadata`, and (b) asserts the CI workflow contains a `cargo test --workspace` step. Register it in `main()` and in `check_gate_index:4917` the way the other checks are, and add a negative.
**Where:** `automation/98-drift-checks.sh (new check + main registration + check_gate_index:4917), tools/native/Cargo.toml, src/mios-rs/Cargo.toml, .github/workflows/mios-ci.yml (rust-gate), tests/drift-gate-negatives.sh`
**Done When:** Dropping a new crate dir outside `members` fails `check_native_workspace_gated`; removing the `cargo test --workspace` step fails it; a negative test exists.
**Why:** A crate outside the workspace is invisible to clippy, fmt, deny and the test run -- exactly how 5 crates shipped ungated, and it will recur on the next crate added.
**Dep:** Add a fast, cached pre-bake rust-gate CI job

### AGY-1546 -- Mirror every test/lint gate into .forgejo and add check_ci_workflow_parity  (WS-TESTGOV | P1 | M)
**Goal:** E-16 The bake plane / release topology -- makes the declared GitHub == Forgejo equality mechanically true for the gate set, not just the build.
**What+How:** `.forgejo/workflows/build-mios.yml` (187 lines) is a hand-copied and already-DIVERGENT fork of `.github/workflows/mios-ci.yml`: it carries its own inline agent-pipe test loop (no SKIP list, no shellcheck install) and lacks the rust/mypy/ruff/PS gates this campaign adds. Add `check_ci_workflow_parity` to `automation/98-drift-checks.sh` (registered in `main()`, negative in `tests/drift-gate-negatives.sh`) that parses both YAMLs and fails when the set of named gate steps -- shellcheck, lint-python, 97-ssot-lint, 98-drift-checks, negatives, rust-gate, mypy, ruff, pytest, PS gates -- is not identical, then bring the Forgejo file up to parity. Stage both workflow files explicitly.
**Where:** `.github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh`
**Done When:** `check_ci_workflow_parity` fails a step-removed mutated Forgejo copy and passes the parity'd pair; both workflows enumerate an identical gate-step set; `just drift-gate` + negatives green.
**Why:** The two publishers are declared bit-for-bit equal but already differ, so every gate added to GitHub becomes a false-green hole on the Forgejo side.
**Dep:** none

### AGY-1547 -- Pin a rust-toolchain.toml (channel + clippy/rustfmt/llvm-tools) for CI, local and build-stage parity  (WS-TESTGOV | P1 | S)
**Goal:** E-05 Test and CI governance -- makes the Rust gates reproducible by pinning the compiler, not just the dependencies.
**What+How:** No `rust-toolchain.toml` exists anywhere in the tree (verified), yet the `clippy -D warnings`, `fmt --check`, cargo-deny and cargo-mutants tasks all assume a fixed toolchain, and `mios-wallpaperd/Cargo.toml` notes the toolchain is provisioned at stage time by `Install-MiosRust`. Add `rust-toolchain.toml` at the consolidated workspace root pinning an exact stable channel plus `components = [clippy, rustfmt, llvm-tools-preview]` and the target triple(s). Wire the pre-bake rust-gate and the build-stage Rust provisioner to honor it so all three environments agree; keep the literal channel an overridable default and stage the file explicitly.
**Where:** `src/mios-rs/, tools/native/ (workspace root after consolidation), rust-toolchain.toml (new), .github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml`
**Done When:** `cargo +<pinned> --version` matches across CI and the build stage; clippy/rustfmt/llvm-cov components resolve from the pin; rust-gate is green with no rustup auto-download of a floating channel.
**Why:** `Cargo.lock` pins deps but not the compiler, so clippy/fmt/coverage/mutants results differ between a developer's machine and the runner -- gates that pass locally and fail in CI (or worse, the reverse).
**Dep:** Consolidate the two Rust workspaces into one

### AGY-1548 -- Create pyproject.toml + a pinned hermetic dev-dependency lock for the Python gates  (WS-TESTGOV | P1 | M)
**Goal:** E-05 Test and CI governance -- supplies the one reproducible tool+runtime base that mypy, ruff and pytest-coverage all require.
**What+How:** There is NO `pyproject.toml`, `pytest.ini`, `mypy.ini` or ruff config anywhere (verified), and the fast gate best-effort `pip install`s fastapi/httpx UNPINNED. Add a repo-root `pyproject.toml` defining `[tool.mypy]`, `[tool.ruff]` and `[tool.pytest.ini_options]`, plus a pinned dev lock (`requirements-dev.txt` or `uv.lock`) covering mypy, ruff, pytest, pytest-cov and the agent-pipe import deps (fastapi, httpx, tomli backport). Change the drift-gate job to install from the lock instead of ad-hoc pip. Stage the pyproject and lock explicitly.
**Where:** `pyproject.toml (new), requirements-dev.txt or uv.lock (new), .github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml`
**Done When:** A clean CI runner installs the exact pinned dev set from the lock; mypy/ruff/pytest all discover their config from `pyproject.toml`; the fast gate no longer performs unpinned best-effort pip installs.
**Why:** Unpinned tool installs make every Python gate result runner-dependent -- a lint or type finding can appear or vanish between two runs of identical code.
**Dep:** none

### AGY-1549 -- Add a shared golden-output normalizer (scrub $ROOT/timestamps/PIDs; pin LC_ALL/TZ/SOURCE_DATE_EPOCH)  (WS-TESTGOV | P1 | M)
**Goal:** E-06 Test and documentation harness -- makes characterization goldens deterministic so the byte-parity strategy for the Rust port is trustworthy.
**What+How:** The goldenfile-98-drift, insta-userenv, insta-quadlets and bake-plan golden tasks all snapshot output embedding absolute `$ROOT` paths, timestamps, durations and PIDs (`mios-drift-runner` prints elapsed time; `98-drift-checks.sh` echoes both `$ROOT`-relative and absolute paths), so raw goldens will thrash across runners. Author ONE shared normalizer -- `tools/testlib/normalize.sh` plus a Rust testutil fn -- rewriting `$ROOT`->`@ROOT@`, ISO/epoch timestamps->`@TS@`, PIDs->`@PID@`, durations->`@DUR@`, alongside a canonical env preamble (`LC_ALL=C`, `TZ=UTC`, pinned `SOURCE_DATE_EPOCH`). Every goldenfile/insta/trycmd fixture pipes producer output through it before compare.
**Where:** `tools/testlib/ (new), tests/, tools/native/*/tests/ (golden harnesses), automation/98-drift-checks.sh (producer callers)`
**Done When:** The same producer run twice from two different runner paths yields byte-identical normalized output; the goldenfile-98 and insta snapshot tasks consume the shared normalizer; no committed golden contains a raw absolute path, timestamp or PID.
**Why:** Without it every golden this workstream creates is flaky-red on path/time differences, and a flaky parity net gets disabled -- taking the safety out of the whole bash->Rust migration.
**Dep:** Capture a goldenfile master of full 98-drift-checks.sh stdout

### AGY-1550 -- Add check_no_pending_snapshots + a single `just test-accept` golden-refresh entrypoint  (WS-TESTGOV | P2 | S)
**Goal:** E-06 Test and documentation harness -- keeps the snapshot suites authoritative instead of accumulating orphan pending files that mask real drift.
**What+How:** insta and trycmd emit `*.snap.new` / pending-accept files and goldenfile only writes updates under an env flag, so a stale or half-accepted snapshot can be committed unnoticed. Add a `just test-accept` recipe running `cargo insta accept` + trycmd overwrite + goldenfile regeneration in ONE deterministic pass, and a `check_no_pending_snapshots` drift-check (in `automation/98-drift-checks.sh`, with a negative in `tests/drift-gate-negatives.sh`) that fails if any `*.snap.new` or pending-accept artifact is tracked, or if re-running the producers under the normalizer would change a committed golden.
**Where:** `Justfile, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, tools/native/*/tests/snapshots/`
**Done When:** A committed `*.snap.new` trips the check; `just test-accept` is the sole refresh path and leaves the tree clean; drift-gate + negatives green.
**Why:** A stale accepted snapshot silently blesses the exact regression the golden was created to catch, and there is currently no way to tell an accepted golden from an abandoned one.
**Dep:** Add a shared golden-output normalization filter

### AGY-1551 -- Add a three-way resolver equivalence CI job executing globals.ps1 under pwsh against the python and shell twins  (WS-TESTGOV | P1 | M)
**Goal:** E-10 One canonical name: the unified names/keys registry -- proves all THREE resolver twins emit the identical `MIOS_*` projection from one `mios.toml`.
**What+How:** The resolver has three twins, not two: `usr/lib/mios/mios_toml.py`, `tools/lib/userenv.sh` (== `usr/lib/mios/userenv.sh`) and `automation/lib/globals.ps1` (220 lines). The differential-oracle proptest compares only python vs shell; `globals.ps1` is spot-checked for ports (`check_globals_ports`) and image name (`check_globals_image_parity`) but never for the full `MIOS_*` set and never actually EXECUTED (CI is ubuntu-only). Add a CI step that installs pwsh, dot-sources `globals.ps1`, dumps its `MIOS_*` projection, normalizes, and diffs it byte-for-byte against the python and shell projections over the same `mios.toml`; wire a `check_globals_env_equivalence` into `98-drift-checks.sh` with a negative. Degrade open ONLY when pwsh genuinely cannot be provisioned.
**Where:** `automation/lib/globals.ps1, usr/lib/mios/mios_toml.py, tools/lib/userenv.sh, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, .github/workflows/mios-ci.yml`
**Done When:** CI runs `globals.ps1` under pwsh and asserts its full `MIOS_*` set equals the python + shell twins; a seeded divergence in a NON-port key trips `check_globals_env_equivalence`; negative + drift-gate green.
**Why:** Every non-port, non-image key in the PowerShell resolver can drift undetected today, so a Windows host can resolve a different SSOT than the Linux host built from the same `mios.toml`.
**Dep:** proptest differential resolver oracle

### AGY-1552 -- Add a windows-latest (or pinned pwsh container) CI leg so the PS gates actually execute  (WS-TESTGOV | P2 | M)
**Goal:** E-03 PowerShell surface -- supplies the runner the PS parse/PSSA/Pester gates need in order to exist at all.
**What+How:** The entire CI matrix is ubuntu-24.04 (drift-gate, build and smoke-test all `runs-on` ubuntu), so the queued PowerShell AST-parse and PSScriptAnalyzer gates -- and any Pester tests -- have no host that runs them. Add a dedicated CI job (windows-latest, or a pinned mcr pwsh container on linux) that installs PSScriptAnalyzer + Pester and runs the AST parse, the PSSA Error-severity gate and the Pester suite over `Get-MiOS.ps1`, `build-mios.ps1`, `mios-oscontrol-server.ps1` and `automation/lib/globals.ps1`. Gate the build job on it via `needs`, and mirror the leg into the Forgejo workflow.
**Where:** `.github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml, automation/lib/globals.ps1, build-mios.ps1, Get-MiOS.ps1, powershell/`
**Done When:** A PS syntax error or a new PSSA Error-severity finding fails the windows leg in CI; the leg is a required `needs` for the build job; both workflows carry it.
**Why:** None of the 36 `*.ps1` files -- including 420KB `Get-MiOS.ps1` and 619KB `build-mios.ps1` -- is executed or analyzed in CI today, so the Windows half of the product ships unverified.
**Dep:** Add a PowerShell AST-parse gate over all *.ps1

### AGY-1553 -- Stand up a Pester project with characterization tests for globals.ps1 and build-mios.ps1 pure helpers  (WS-TESTGOV | P2 | L)
**Goal:** E-03 PowerShell surface -- freezes current Windows behavior so the two largest PS files can be demoted behind compiled code without regression.
**What+How:** PowerShell has ZERO behavioral tests (no `*.Tests.ps1`, no Pester config -- verified); all queued PS work is static. Create `tests/pester/` with characterization tests pinning the current output of `automation/lib/globals.ps1`'s `MIOS_*` projection and of the pure helper functions extracted from `build-mios.ps1` (path, version and tag resolution). Add a `PesterConfiguration` psd1 and run it from the windows CI leg with a code-coverage floor over the tested functions. Stage the test files and config explicitly; do NOT touch MiOS-Cat files (concurrent-agent owned).
**Where:** `tests/pester/ (new), automation/lib/globals.ps1, build-mios.ps1, .github/workflows/mios-ci.yml`
**Done When:** `Invoke-Pester` runs green in CI over `globals.ps1` + `build-mios.ps1` helpers; a deliberate behavior change to a covered function fails an assertion; the coverage floor is enforced and ratcheting.
**Why:** Porting or refactoring a megabyte of untested Windows build/resolve logic has no safety net -- any behavior change lands silently on operator machines.
**Dep:** Add a windows-latest (or pinned pwsh container) CI leg

### AGY-1554 -- Add check_no_silent_tool_skips: gated checks must fail closed under MIOS_DRIFT_REQUIRE_TOOLS=1  (WS-TESTGOV | P1 | M)
**Goal:** E-07 The drift-gate as the enforcement plane -- eliminates the highest-frequency false-green class so a green gate means the checks actually ran.
**What+How:** Many drift checks self-skip when a tool is absent: `check_test_hermeticity` and `check_negative_test_coverage` `return 0` without python3, `check_globals_image_parity` returns 0 without tomllib, `automation/lint-python.sh` exits 2 (treated as pass) with no python3 -- so a tool-poor runner reports green even though CI sets `MIOS_DRIFT_REQUIRE_TOOLS=1`. Add `check_no_silent_tool_skips` that scans `98-drift-checks.sh` and `lint-*.sh` for `command -v ... || return 0` and bare-skip patterns and, under `MIOS_DRIFT_REQUIRE_TOOLS=1`, requires each site to `_violation` (fail loud). Convert the offending sites to honor REQUIRE_TOOLS and add a negative.
**Where:** `automation/98-drift-checks.sh, automation/lint-python.sh, automation/lint-shell.sh, tests/drift-gate-negatives.sh`
**Done When:** With `MIOS_DRIFT_REQUIRE_TOOLS=1` and python3/tomllib/shellcheck stubbed absent, the affected checks FAIL rather than skip; the negative proves the fail-closed path; drift-gate stays green when the tools are present.
**Why:** A green drift-gate on a tool-poor runner is a lie -- real violations merge because the check that would have caught them quietly returned 0.
**Dep:** none

### AGY-1555 -- Add check_skip_list_covered: prove every pre-bake-skipped agent-pipe test runs in the venv build-job gate  (WS-TESTGOV | P2 | S)
**Goal:** E-06 Test and documentation harness -- turns "skipped here, runs there" from an unverified claim into a gated fact.
**What+How:** The drift-gate job hardcodes `SKIP=" test_mios_config_audit.py test_mios_redact.py test_mios_vector.py test_mios_gateway_agent.py test_mios_gateway_queue.py test_mios_admission.py test_mios_mcp_pool.py test_mios_bench_harness.py "` and asserts these run in the build-job venv gate -- but nothing proves the build job invokes them, and the Forgejo copy has a DIFFERENT (empty) skip loop. Add `check_skip_list_covered` that (a) parses the SKIP list out of the fast gate, (b) confirms each skipped file is invoked unskipped in the build-job/venv test step, and (c) confirms the GitHub and Forgejo skip sets are identical. Add a negative and stage the check + workflow edits explicitly.
**Where:** `.github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, usr/lib/mios/agent-pipe/`
**Done When:** Removing a skipped test from the venv step trips `check_skip_list_covered`; mismatched GitHub/Forgejo skip sets fail; drift-gate + negatives green.
**Why:** Eight agent-pipe tests -- covering redaction, admission, vector and gateway paths -- may be running nowhere at all, and the two publishers already disagree about which are skipped.
**Dep:** Mirror every test/lint gate into .forgejo

### AGY-1556 -- Add check_negatives_are_effective: each test_ fn must inject, assert-FAIL, restore, assert-PASS  (WS-TESTGOV | P1 | M)
**Goal:** E-06 Test and documentation harness -- makes the negatives harness real mutation testing rather than a name-substring check.
**What+How:** `check_negative_test_coverage` only asserts that a required check's NAME appears as a substring somewhere in `tests/drift-gate-negatives.sh` -- it never verifies the 89 `test_` functions mutate state and observe a red->green transition, so an always-passing stub satisfies it. Add `check_negatives_are_effective` that structurally verifies each `test_` fn calls its target check TWICE around a mutation (once expecting failure via the `if ... ; then die` form, once expecting success after restore), mirroring the established `test_version_ssot` contract, and flags any `test_` fn that never invokes `98-drift-checks.sh` with a failing expectation. Add a self-negative.
**Where:** `tests/drift-gate-negatives.sh, automation/98-drift-checks.sh`
**Done When:** A stubbed always-pass `test_` fn trips `check_negatives_are_effective`; all 89 existing negatives satisfy the structural contract; drift-gate green.
**Why:** An inert negative gives false assurance that a fitness-function is guarded -- the gate can silently lose its ability to fail and the harness still reports full coverage.
**Dep:** Ratchet check_negative_test_coverage to auto-derive the required check set

### AGY-1557 -- Add a cargo-llvm-cov coverage gate with a ratcheting floor for resolver-core and miosd  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance -- gives the compiled tier the same monotonic coverage ratchet the Python plane gets.
**What+How:** The pytest task gains a ratcheting coverage floor, but the Rust crates have clippy + tests and NO line-coverage gate, so ported logic can land uncovered. Add a `cargo llvm-cov` step to the pre-bake rust-gate producing a coverage number for `resolver-core` and `miosd`, enforced against a committed floor file (like the mypy/pytest baselines) that may only move up. This requires the `llvm-tools-preview` component from the toolchain pin. Fold the recipe into `just gate-rust` and stage the floor file + workflow edits explicitly.
**Where:** `src/mios-rs/miosd/, tools/native/ (resolver-core crate), .github/workflows/mios-ci.yml, Justfile, coverage baseline file (new)`
**Done When:** `cargo llvm-cov` runs in CI; a drop below the committed floor fails the rust-gate; the floor ratchets monotonically upward; local `just gate-rust` reproduces the CI number.
**Why:** As bash logic migrates into `resolver-core`/`miosd`, critical paths can land entirely untested while the suite still reports green.
**Dep:** Pin a rust-toolchain.toml

### AGY-1558 -- Characterization golden for mios-sync-theme (check-25 [colors] -> surfaces projection) before its port  (WS-TESTGOV | P2 | M)
**Goal:** E-22 Dotfiles projection: one engine, every surface, both platforms -- freezes the theme projection byte-for-byte before it becomes the compiled render engine.
**What+How:** `usr/libexec/mios/mios-theme-render` is a 521-byte dispatcher; the real projection work lives in `usr/libexec/mios/mios-sync-theme` (8.5KB), the producer behind drift-check 25, and it has no characterization golden among the eight already queued. Capture a goldenfile/insta master of EVERY surface `mios-sync-theme` renders from `mios.toml [colors]` (btop, hypr, terminal, GTK -- the dotfiles registry surfaces) over a fixed `mios.toml`, piped through the shared normalizer. Add a `just` recipe to regenerate deterministically plus a drift assertion that the live render equals the golden. Stage the golden dir + recipe explicitly.
**Where:** `usr/libexec/mios/mios-sync-theme, usr/libexec/mios/mios-theme-render, tests/goldens/theme/ (new), automation/98-drift-checks.sh (check 25 area), Justfile`
**Done When:** A change to `mios-sync-theme` output fails the golden compare; the golden covers every `[colors]`-derived surface; the regenerate recipe is deterministic; drift-gate green.
**Why:** A drift-gated SSOT producer with no byte-parity net can be refactored or ported into silent color-surface drift across both platforms, visible only on an operator's desktop.
**Dep:** Add a shared golden-output normalization filter

### AGY-1559 -- Characterization golden locking mios-env-snapshot root-path/PID/timestamp normalization  (WS-TESTGOV | P1 | M)
**Goal:** E-08 Global Unification Plan -- pins the differential oracle that every lossless resolver-port step diffs against.
**What+How:** `usr/libexec/mios/mios-env-snapshot` (2.6KB) plus `usr/libexec/mios/system-sync-env.sh` (17.5KB) produce the `MIOS_*` env snapshot the entire lossless-diff strategy compares against, and the snapshot MUST normalize root paths or the gate fails in CI. Capture a goldenfile master of the normalized snapshot (root path -> `@ROOT@`, PIDs and timestamps scrubbed) over a fixed `mios.toml`, and add a drift assertion that the live snapshot equals the golden after normalization -- making the snapshot's own normalization contract a first-class tested invariant instead of an implicit CI assumption.
**Where:** `usr/libexec/mios/mios-env-snapshot, usr/libexec/mios/system-sync-env.sh, tests/goldens/env-snapshot/ (new), automation/98-drift-checks.sh`
**Done When:** A leaked absolute path, PID or timestamp in the snapshot fails the golden; the live snapshot equals the golden across two different runner paths; drift-gate green.
**Why:** If snapshot normalization drifts, every downstream byte-parity gate fails non-deterministically and the GUP losslessness invariant stops being provable at all.
**Dep:** Add a shared golden-output normalization filter

### AGY-1560 -- Extend the sibling-test ratchet from agent-pipe to `tools/*.py` and `usr/libexec/mios/*.py` via a shrink-only grandfather baseline  (WS-TESTGOV | P2 | M)
**Goal:** E-05 Test and CI governance: the quality gates that make compiled code trustworthy -- makes "every Python surface carries a characterization test" machine-enforced instead of true only for agent-pipe, so compiled/ported code has a parity oracle to be trusted against.
**What+How:** `check_module_test_coverage` (automation/98-drift-checks.sh, ~line 697) demands a sibling `test_*.py` only for `usr/lib/mios/agent-pipe` modules; the 33 `tools/*.py` and 122 Python files under `usr/libexec/mios` are wholly exempt and only `test_mios_docgen.py` exists there. Add a per-directory grandfather baseline file enumerating today's untested modules, then extend the check so (a) any NEW `tools/` or `libexec` `.py` file without a sibling test FAILS, and (b) the baseline may only shrink monotonically -- adding a name to it is a violation. Write characterization tests for the highest-risk producers first (`seed-db-config.py`, `mios-pg-query`, the `generate-*` scripts). Add a matching negative in `tests/drift-gate-negatives.sh` and stage the check plus the baseline as explicit paths.
**Where:** `automation/98-drift-checks.sh (check_module_test_coverage), tools/, usr/libexec/mios/, tests/drift-gate-negatives.sh, a test-coverage baseline file (new)`
**Done When:** adding a new untested `tools/` or `libexec` `.py` module trips the check; growing the grandfathered baseline fails; agent-pipe behavior is unchanged; `just drift-gate` and the negatives run green.
**Why:** 155 of MiOS's Python files -- two of its three Python surfaces -- can be edited or rewritten today with zero test obligation, so any port or refactor there has no failure signal at all.
**Dep:** Create a pyproject.toml + pinned hermetic dev-dependency lock

## AGY-1561 -- Make native `mios-config` schema-GENERIC so a mios.toml section change never forces a Rust recompile  (WS-RESOLVER | P1 | M)  **[DONE]**
**Goal:** E-10 One canonical name: the unified names/keys registry -- one generic resolver walk shared by every surface, so the native tier reads exactly the operator-defined key space the Python resolver does rather than a second, hand-typed copy of the schema.
**What+How:** `usr/share/mios/configurator/mios.html` declares 619 `data-key="section.field"` dotted paths; a generic writer sets them in mios.toml; `usr/lib/mios/mios_toml.py` then WALKS the file generically (`deep_merge` / `section()` / `isinstance(dict)` recursion, zero hardcoded schema). `src/mios-rs/mios-config` regressed this by hard-typing every section as a Rust struct, which broke `[packages]` (a `sections=[...]` master list plus per-group `[packages.<name>]` tables) and forced a `skip_deserializing` band-aid to green the gate. Keep hard types for ONLY the few stable fields a `run_*` actually reads (`meta.mios_version`, `meta.fedora_version`, `build.ratchet.max_phase_scripts`, `build.phases.list`, `identity.username`); read everything else (packages, repos, security, network, power, compliance) through `toml::Value` plus a dotted-path lookup mirroring `mios_toml.py`'s `section(data, name)`. Delete dead `PackageSectionConfig`/`RepoConfig` unless a `run_*` constructs them (watch clippy `dead_code` on pub items) and remove `skip_deserializing`.
**Where:** `src/mios-rs/mios-config/src/lib.rs`, `src/mios-rs/miosd/src/main.rs` (run_* consumers); reference `usr/lib/mios/mios_toml.py` and `usr/share/mios/configurator/mios.html`
**Done When:** a test proves adding/removing an arbitrary `[foo.bar]` section needs ZERO Rust edits; `cargo build && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace` all pass; no `skip_deserializing` hack remains in the crate.
**Why:** every operator edit to mios.toml -- the one config surface the product promises -- otherwise requires a Rust recompile, and the struct mismatch already silently disabled `[packages]` deserialization.
**Dep:** none

## AGY-1562 -- Inventory EVERY hardcoded version literal in the tree into one classified TSV burn-down report  (WS-FLOAT | P1 | M)  **[DONE]**
**Goal:** E-14 Float latest globally -- produces the enumerated worklist without which "SSOT carries version INTENT only" cannot be finished or measured.
**What+How:** The live bug was `rancher/k3s` bumped to `v1.36.2-k3s1` in mios.toml while the Kubernetes DNF repo in `automation/06-enable-external-repos.sh` stayed pinned at `v1.32` -- proof that ad-hoc pinning is spread across container tags, DNF repo minors, Python wheel pins, model tags, COPR `fedora-NN` endpoints and Quadlet labels. Write `tools/audit-version-literals.py` (pure, offline, no third-party deps) that scans for version-shaped tokens (`:v?\d+\.\d+(\.\d+)?`, `stable:/v\d+\.\d+`, `fedora-\d+`, `@sha256:`, `==\d+\.\d+`, image `:tag` refs), classifies each as (a) SSOT-definition, (b) SSOT-derived/placeholder or (c) HARDCODED-literal, and writes `usr/share/mios/reference/version-literals-audit.tsv` with path, line, token, class and a suggested SSOT key. Exempt SBOM/lockfiles, `.repo` fixtures, CHANGELOG/history and the SSOT itself. The class-(c) count is the campaign metric driving AGY-1563..1566.
**Where:** new `tools/audit-version-literals.py`; output under `usr/share/mios/reference/`; inputs `usr/share/mios/mios.toml [image.sidecars]`, `automation/06-enable-external-repos.sh`, `usr/share/containers/systemd/*.container`
**Done When:** the TSV carries a class for every version token, two consecutive runs are byte-identical, and the class-(c) total is logged in full with no silent truncation.
**Why:** without the inventory each stale pin is only found by an operator noticing a broken repo months later, exactly as the k3s/k8s mismatch was.
**Dep:** none

## AGY-1563 -- Establish ONE version SSOT and convert every consumer to a derivation (the k3s→k8s cascade, generalized)  (WS-FLOAT | P1 | L)  **DONE**

**Goal:** E-14 Float latest globally -- one declared value, every downstream version derived, so a single SSOT bump propagates without a second hand edit.
**What+How:** Consolidate version definitions into `mios.toml [image.sidecars]` (image refs) plus a new `[versions]` table for non-image numbers (k8s minor derivable from the k3s tag, wheel pins, tool versions), each mapped to a `MIOS_*` by `usr/lib/mios/userenv.sh` and its `.ps1`/`globals.ps1` twins (Law 15 double-repo). Then walk AGY-1562's class-(c) list and convert each literal to a derivation: DNF repo minors computed off `$MIOS_K3S_VERSION` (`grep -oE 'v?[0-9]+\.[0-9]+'`, the shape the `5793d4ac` hotfix used), COPR endpoints on `${FEDORA_VERSION}`, Quadlet labels keeping `${VAR}` placeholders. Never reintroduce a bare literal; use `${VAR:-safe-default}` only where a fallback is genuinely required.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/mios/userenv.sh` (+ `.ps1` / `globals.ps1` twins), every consumer named in the 1562 audit
**Done When:** a test that bumps the k3s tag in a temp copy of the SSOT and diffs derived outputs shows the k8s repo minor, the Quadlets and the manifests all moved with ZERO other edits; the 3-way py/sh/ps1 resolver-equivalence checks stay green.
**Why:** today `MIOS_K3S_VERSION` exists but most versions have no single home, so each bump is a manual multi-file sweep that silently half-lands and ships a mismatched repo.
**Dep:** AGY-1562

## AGY-1564 -- Widen `check_no_hardcoded_ssot_literal` from `fedora-NN` only to ALL version literals, with an ANCHORED allowlist  (WS-FLOAT | P1 | M)  **[DONE]**
**Goal:** E-12 ZERO-HARDCODES: float every remaining literal out of code -- turns the float-latest cascade into a permanent invariant rather than a one-time cleanup.
**What+How:** The gate in `automation/98-drift-checks.sh` greps only `fedora-[0-9]{2}`, so it would not have caught the stale k8s `v1.32`. Generalize it to flag `stable:/v\d+\.\d+`, `rancher/k3s:v…`, other pinned image `:vX.Y.Z` tags and bare `fedora-\d+` across `automation/`, `usr/share/mios/` and `usr/share/containers/`, exempting only (a) the SSOT `[image.sidecars]`/`[versions]` definitions, (b) `${VAR}` / `${VAR:-default}` derivations, or (c) entries explicitly allowlisted in `mios.toml [security.nohc_allowlist]` -- using the ANCHORED allowlist pattern, not the unanchored form that spuriously exempted real `:80xx` violations. Rendered Quadlets are already covered by `check_pod_quadlets`, so once they float `${FEDORA_VERSION}` no blanket directory exemption is needed. Add negatives that inject a bare `v1.32`, assert FAIL, restore, assert PASS.
**Where:** `automation/98-drift-checks.sh`, `tests/drift-gate-negatives.sh`, `usr/share/mios/mios.toml [security.nohc_allowlist]`, `usr/share/mios/reference/drift-gate-index.tsv` (regenerate)
**Done When:** re-hardcoding any k8s/k3s/fedora version outside the SSOT turns the gate RED; the negative test proves the failure path; the full drift-gate plus negatives are green and the gate index is regenerated.
**Why:** with the check scoped to one regex, the exact class of drift the campaign just fixed can reappear on the next commit and no gate will notice.
**Dep:** AGY-1563

## AGY-1565 -- Give image refs `:latest` INTENT with digests resolved at BUILD, and record the offline-vendored pin exception in an ADR  (WS-FLOAT | P2 | M)  **DONE**
**Goal:** E-15 SBOM and supply-chain hardening as compiled, gated policy -- makes "reproducible = baked manifest + SBOM" true, with every legitimate pin justified rather than assumed.
**What+How:** Per ADR-0003, digests and versions are SBOM data resolved at build, never hand-typed in SSOT. For registry-pulled sidecars express `:latest` (or a floating major) in `[image.sidecars]` and let the build resolve and record the `@sha256` into the baked SBOM manifest -- reconciling this with the `_sidecar_image` digest-SSOT fallback already in `tools/generate-pod-quadlets.py`. For OFFLINE-vendored artifacts (the k3s tarball, the hermes wheel) do NOT float: write an ADR under `docs/adr/` stating why that specific version is pinned for the mirror and how a bump flows (bump SSOT → re-vendor → digest re-recorded), so the float-vs-pin tension is an explicit recorded decision instead of undocumented drift.
**Where:** `usr/share/mios/mios.toml [image.sidecars]`, `tools/generate-pod-quadlets.py` (`_sidecar_image`), the build SBOM step, a new/updated ADR under `docs/adr/`
**Done When:** every sidecar's float-or-pin decision is recorded in the ADR; the baked SBOM manifest holds the resolved digests; no `@sha256` is hand-typed anywhere in the SSOT.
**Why:** hand-pinned digests in SSOT go stale invisibly and are the root of the recurring Quadlet-digest-drift class, while blind floating would break byte-reproducible offline install.
**Dep:** AGY-1563

## AGY-1566 -- Gate AI-hint manifest freshness so `manifest.json` stops carrying stale version snapshots  (WS-FLOAT | P2 | S)  **DONE**

**Goal:** E-07 The drift-gate as the enforcement plane -- every generated projection is regenerated-or-red, never quietly stale.
**What+How:** `automation/manifest.json` still embeds the OLD `stable:/v1.32` and its old comments inside the `full_content` snapshot AFTER the script itself was fixed, because `tools/generate-ai-manifest.py` runs only best-effort from `automation/ai-bootstrap.sh` and is not gated. Add a `--check` mode to the generator (regenerate to a temp dir, diff against the committed file, exit non-zero on drift), make output deterministic (sorted keys, normalized newlines) so it does not thrash, wire it into the docgen CI leg, and add a `check_ai_manifests_fresh` drift-check that fails closed under `MIOS_DRIFT_REQUIRE_TOOLS=1`. Regenerate every `manifest.json` and `root-manifest.json` once to clear the current staleness.
**Where:** `tools/generate-ai-manifest.py`, `automation/98-drift-checks.sh`, the docgen CI job, all `*/manifest.json` and `root-manifest.json`
**Done When:** editing a script without regenerating its manifest trips the new check; every manifest is byte-fresh; grepping the manifests for `v1.32` returns nothing.
**Why:** the manifests are the AI-hint layer's view of the tree, so a stale snapshot re-teaches an agent the exact literal the campaign just purged.
**Dep:** none

## AGY-1567 -- Move the Quadlet renderer's placeholder-preserve list into SSOT instead of per-var `if` branches  (WS-FLOAT | P3 | S)  **[DONE]**
**Goal:** E-14 Float latest globally -- which variables float is itself operator-declared config, not a fact compiled into a generator.
**What+How:** `tools/generate-pod-quadlets.py` preserves `${MIOS_PORT_*}` and `${FEDORA_VERSION}` as placeholders through two hand-coded `if var_name == …` branches added by the hotfix -- the same hardcoding anti-pattern in miniature. Replace both branches with a declared float-list read from SSOT (e.g. `mios.toml [render.float_vars] = ["MIOS_PORT_*", "FEDORA_VERSION", …]`, with glob support) so an operator can add a floating variable without touching Python. Keep the `_sidecar_image` digest fallback intact.
**Where:** `tools/generate-pod-quadlets.py`, `usr/share/mios/mios.toml`
**Done When:** adding a new float var requires only an SSOT edit; `generate-pod-quadlets.py --check` and `--selftest` are green with all units matching; no per-var `if` branch remains in the renderer.
**Why:** the renderer currently decides policy that belongs to the operator, so every new floating variable is a code change plus a review round-trip.
**Dep:** AGY-1563

## AGY-1568 -- Render `usr/share/applications/mios-svc-*.desktop` from SSOT ports instead of hand-editing them  (WS-PORTFLOAT | P1 | M)  **DONE**

**Goal:** E-13 Ports are allocated from SSOT, not hand-assigned -- closes the last user-visible surface where a port literal is maintained by hand.
**What+How:** The `mios-svc-*.desktop` launchers and `mios-configurator.desktop` carry `Exec=`/`URL=` lines with literal `localhost:<port>`; four of them (cockpit 9090, forge 3000, code-server 8080, llm-light 11450) were silently wrong for months because only `mios-svc-guacamole.desktop` had any coverage, via `check_guacamole_consistency`. Add `tools/render-desktop.py` rendering every launcher from a new `[desktop.launchers.<name>]` SSOT table (`title`, `icon`, `port_key`, `path`, `categories`), map each existing file onto that table, and give it a `--check` mode. Register the renderer in `tools/sync-generated.sh` between the globals and quadlet steps, add `check_desktop_launchers` to fail when a rendered launcher differs from the committed one, and retire `check_guacamole_consistency` into it once coverage is complete.
**Where:** `tools/render-desktop.py (new), usr/share/mios/mios.toml, usr/share/applications/*.desktop, tools/sync-generated.sh, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** every `.desktop` port derives from `[ports]`; `--check` is clean on a synced tree; both a hand-edit to any launcher and a `[ports.categories]` base change are caught; the negative injects port-agnostically with no literal in the test.
**Why:** a wrong launcher port is what the operator clicks first, and four of them shipped broken with nothing able to detect it.
**Dep:** none

## AGY-1569 -- Vendor `aiohttp` and the full hermes-agent dependency closure into the offline wheels dir, and fail the phase when it is incomplete  (WS-PORTFLOAT | P1 | M)  **DONE**

**Goal:** E-16 The bake plane: what is present in the image -- the baked image must actually contain a working agent plane, not a venv that merely appears installed.
**What+How:** `automation/72-hermes-agent.sh` fails its offline install with `ERROR: Could not find a version that satisfies the requirement aiohttp (from versions: none)` and degrades to `WARN Agent-venv offline install incomplete`, so the published image ships a hermes-agent venv with no HTTP client. `usr/share/mios/vendored/wheels/` lacks the full closure. Resolve hermes-agent's `pyproject.toml` against `usr/share/mios/agents/constraints.txt` using `pip download --only-binary=:all:` for the image's Python (3.13), commit the resulting wheels, and change the phase to FAIL rather than WARN when the closure is incomplete so the regression cannot recur silently.
**Where:** `automation/72-hermes-agent.sh, usr/share/mios/vendored/wheels/, usr/share/mios/agents/constraints.txt, automation/98-drift-checks.sh`
**Done When:** the phase builds the venv with no network and emits no WARN; deliberately deleting one wheel fails the phase; the bake log reports 0 warned for this step.
**Why:** the agent plane is the product, and a WARN-not-FAIL path means every published image today carries a hermes venv that cannot make an HTTP request.
**Dep:** none

## AGY-1570 -- Fix `37-k3s-selinux.sh` exiting 1 on the flat-layout vendored policy tarball  (WS-PORTFLOAT | P2 | S)  **[DONE]**
**Goal:** E-16 The bake plane: what is present in the image -- a clean, unambiguous bake where a non-zero phase exit always means a real failure.
**What+How:** The bake logged `[WARN] 37-k3s-selinux.sh (2s) exit=1`. Root cause: the vendored tarball is k3s-selinux-0.1.1-rc2, whose sources (`k3s.te k3s.fc k3s.if k3s.sh`) sit FLAT at the archive root with no `policy/` directory, while the detector knew only the >=0.2 nested `policy/<distro>/` layout and fell back to an unguarded `find policy ...` that aborted the phase under `set -euo pipefail`. Fix: add a flat-layout branch, guard the `find`, skip the self-copy (`cp ./k3s.* .` errors under `set -e`), turn missing sources into a `mios_skip` that logs the locations searched instead of `exit 1`, and verify extraction actually produced `k3s.te` before trusting it.
**Where:** `automation/37-k3s-selinux.sh`
**Done When:** the bake reports 0 warned; on a host where the policy is genuinely unavailable the script emits an explicit `mios_skip` with a logged reason rather than a bare non-zero exit.
**Why:** it was the only warning left in an otherwise clean 67-script bake, and a bare `exit=1` is indistinguishable from a real build failure for anyone reading the log.
**Dep:** none

## AGY-1571 -- Fold drift-checks 28 and 52 into `check_globals_generated` and delete their literal parsers  (WS-PORTFLOAT | P2 | S)  **DONE**

**Goal:** E-07 The drift-gate as the enforcement plane -- one byte-level check per generated artifact, with no hidden coupling constraining the generator.
**What+How:** `check_globals_ports` (28) and `check_globals_image_parity` (52) scrape literals out of `automation/lib/globals.{sh,ps1}` with bespoke regexes (`else { <digits> }`, `MIOS_IMAGE_NAME:=`). Those files are now generated in full and byte-gated by check 130, so 28/52 are redundant AND actively constrain the renderer's output shape -- it must keep emitting `:=` and bare integers, and keep a `$defaultImageName` line nothing reads, purely to satisfy them. Replace their bodies with a delegation to `check_globals_generated` (or retire them with coverage preserved by 130), delete the literal parsers, drop the shape workarounds from `tools/render-globals.py`, and regenerate the gate index.
**Where:** `automation/98-drift-checks.sh, tools/render-globals.py, usr/share/mios/reference/drift-gate-index.tsv, tests/drift-gate-negatives.sh`
**Done When:** 28/52 delegate or are retired with 130 still covering them; `render-globals.py` carries no formatting workaround for either; the negatives still catch a hand-edit to `globals.sh`/`globals.ps1`.
**Why:** the coupling already cost two CI round-trips and it blocks AGY-1169, which stays open until the renderer can choose its own output format.
**Dep:** none

## AGY-1572 -- Build and install the `mios-resolver` binary into the image and cut generation over to it behind a differential gate  (WS-PORTFLOAT | P1 | L)  **DONE**

**Goal:** E-01 Compiled native tier: Rust-port the build orchestrator and the libexec tool fleet -- moves the resolver from a crate that merely compiles to the code the product actually runs.
**What+How:** `tools/render-globals.py` and the `[ports]` derivation run through the PYTHON resolver (`usr/lib/mios/mios_toml.py`); the Rust `mios-resolver` mirrors that logic in `ports.rs` / `emit_ps.rs` / `emit_shell.rs` but is never built into the image, so `userenv.sh`'s `command -v mios-resolver` branch is dead in production. Add a bake phase that compiles `tools/native` and installs the binary to `/usr/libexec/mios/mios-resolver`, keeping a degrade-chain to the Python path when the binary is absent. Then add a differential drift-check asserting `mios-resolver --emit=shell|powershell|json` is byte-identical to the Python render against the real SSOT.
**Where:** `automation/ (new phase), Containerfile, tools/native/mios-resolver/, automation/98-drift-checks.sh`
**Done When:** the binary is present in the baked image; the differential gate is green; a booted image demonstrably takes the native branch in `userenv.sh`.
**Why:** the entire WS-RESOLVER crate is inert today -- it compiles and passes its own tests while nothing in the shipped product ever calls it -- and AGY-1577 cannot start until it does.
**Dep:** none

## AGY-1573 -- Wire or delete the inert `[migration]` and `[versions]` SSOT tables  (WS-PORTFLOAT | P2 | M)  **DONE**

**Goal:** E-08 Global Unification Plan: collapse the MIOS_* namespace to one-value-derived -- every SSOT key has exactly one live consumer, and version facts are declared once.
**What+How:** `[migration].use_rust_resolver_*` has ZERO consumers (grep-verified), and `[versions]` duplicates data already flowing from `[image.sidecars]` (`MIOS_K3S_VERSION` is emitted from the sidecar tag today) -- both read as live config to anyone opening mios.toml or the configurator. Pick one: (a) make `userenv.sh` / `mios_toml.py` actually honour `[migration]` when choosing the native vs Python resolver, and make `[versions]` the single source that `[image.sidecars]` tags derive FROM; or (b) delete both tables and record the reasoning in the ADR. Then add a drift-check that fails on any re-introduced inert top-level table.
**Where:** `usr/share/mios/mios.toml, usr/lib/mios/mios_toml.py, usr/lib/mios/userenv.sh, tools/lib/userenv.sh, automation/98-drift-checks.sh`
**Done When:** every key in both tables has a consumer a grep can find, or the tables no longer exist; a newly added inert top-level table fails the gate.
**Why:** inert SSOT is worse than absent SSOT -- an operator flips `use_rust_resolver_*` in the configurator, nothing changes, and trust in the one config surface is gone.
**Dep:** none

## AGY-1574 -- Clear the `check_var_closure` "referenced but NOT emitted" backlog and flip the check from SOFT to hard-fail  (WS-PORTFLOAT | P2 | M)  **DONE**

**Goal:** E-10 One canonical name: the unified names/keys registry -- closes the referenced-but-unemitted key-library drift class the epic names explicitly.
**What+How:** `check_var_closure` reports ~180 referenced-but-unemitted names. With the generated resolvers and renderers now excluded as emitters, the remainder is real signal in three classes: (1) PREFIX ARTEFACTS -- `MIOS_A2A_`, `MIOS_AGENT_`, `MIOS_PORT_` are regex captures of dynamic expansions like `MIOS_PORT_${name}`, so teach `automation/lib/mios_var_closure.py` to drop a trailing-`_` capture that is a strict prefix of a known name; (2) DOUBLE-PREFIX -- `[mios]` walks to `MIOS_MIOS_NAME` / `MIOS_MIOS_FIND_ALIASES_*`, so either alias the `mios.*` section to a single `MIOS_` prefix in `get_aliases()` or rename the section; (3) GENUINELY MISSING -- `MIOS_CONVERGE_*` and `MIOS_COLOR_ANSI_*` are emitted only into `globals.sh`, so confirm the resolver emits them too. Then convert the check from SOFT WARNING to a hard violation.
**Where:** `automation/lib/mios_var_closure.py, usr/lib/mios/mios_toml.py, automation/98-drift-checks.sh`
**Done When:** the closure report is empty and the check hard-fails; introducing a newly referenced-but-unemitted variable turns the gate RED.
**Why:** a soft warning printing 180 lines every run is not a gate -- real key-library regressions are already hiding inside that noise.
**Dep:** none

## AGY-1575 -- Enforce `tools/sync-generated.sh` in CI instead of trusting contributors to run the renderers in order  (WS-PORTFLOAT | P2 | S)  **DONE**

**Goal:** E-07 The drift-gate as the enforcement plane -- the regeneration ordering constraint becomes executable rather than documented.
**What+How:** Several generated artefacts (AI manifests, `globals.{sh,ps1}`, pod Quadlets, the names registry, `env-baseline.txt`) go stale on any edit under `automation/` or `tools/`, and the only defence is remembering to run the renderers in the right dependency order. Add a CI step that runs `bash tools/sync-generated.sh` on a clean checkout and fails when `git status --porcelain` is non-empty, printing the exact list of files that moved. Add it to both publisher workflows so GitHub and Forgejo stay equal.
**Where:** `.github/workflows/mios-ci.yml, .forgejo/workflows/, tools/sync-generated.sh`
**Done When:** a deliberately stale manifest fails the new step with the precise file list; the step is green on a synced tree; both workflows carry it.
**Why:** stale-artefact failures cost more CI round-trips than any real defect during the port campaign, and each one arrives as five separate unhelpful "X is stale" gate messages.
**Dep:** none

## AGY-1576 -- Make `mios-template-engine` honour its documented contract or retire it into `mios-new`  (WS-PORTFLOAT | P3 | S)  **DONE**
**Goal:** E-04 One template per file type + the `mios new` scaffolder -- one generator with one contract, which is the precondition for a conformance gate.
**What+How:** `usr/libexec/mios/mios-template-engine` advertises `<kind> <target_filepath> [description]` but delegates to `mios-new`, which is name-addressed and picks its own canonical destination; the shim then relocates the result and re-substitutes `{{description}}` -- it works, but it is a workaround. It also has ZERO callers (grep-verified: only itself and AGY-TASKS.md). Either give `mios-new` real `--out <path>` and `--description` flags and reduce the engine to a straight pass-through, or delete `mios-template-engine` and update every doc under `usr/share/doc/mios/` that mentions it.
**Where:** `usr/libexec/mios/mios-template-engine, usr/libexec/mios/mios-new, usr/share/doc/mios/`
**Done When:** exactly one scaffolder with one contract remains; no post-generation relocation workaround exists; no orphaned entry point or doc reference is left behind.
**Why:** two overlapping scaffolders is precisely the duplication this campaign exists to remove, and the documented contract currently lies to anyone who reads it.
**Dep:** none

## AGY-1577 -- Delete the ~200-line Python heredoc from both `userenv.sh` twins once the native resolver is proven  (WS-PORTFLOAT | P2 | M) **DONE**
**Goal:** E-01 Compiled native tier -- removes the last duplicated copy of resolver logic, so the compiled resolver is the only implementation.
**What+How:** AGY-1176 claimed this was done, but the embedded Python heredoc in `usr/lib/mios/userenv.sh` and its byte-parity twin `tools/lib/userenv.sh` is still present and is still the production resolver. It may only be removed after AGY-1572 ships the binary and the differential gate is green. Then reduce both twins to a short chain -- try `mios-resolver --emit=shell`, else `miosd`, else fail loudly -- and retire `check_userenv_parity` (27), since a one-line file cannot drift.
**Where:** `usr/lib/mios/userenv.sh, tools/lib/userenv.sh, automation/98-drift-checks.sh`
**Done When:** neither twin contains a heredoc; the twin-equivalence checks are retired or repointed at the binary; a booted image resolves an identical `MIOS_*` set before and after the change.
**Why:** the heredoc is the third live copy of the resolver and the sole reason the twin-parity gates exist, so every resolver change is currently a three-place edit.
**Dep:** AGY-1572

## AGY-1578 -- Add an end-to-end `stack_id` offset test across all three port surfaces  (WS-PORTFLOAT | P3 | S)  **DONE**
**Goal:** E-13 Ports are allocated from SSOT, not hand-assigned -- proves the multi-stack offset that lets two MiOS hosts share one LAN.
**What+How:** `[ports].stack_id` shifts every non-pinned port by `stack_id * 10000` and is implemented three times -- `mios_toml.process_val`, `walk.rs::process_val` and `automation/35-render-ports.sh` -- with no end-to-end test; the Rust CLI ignored it entirely until this campaign. Add a test that sets `stack_id = 1`, resolves through each of the three surfaces, and asserts every port shifted by 10000 EXCEPT `adguard_dns` (53, pinned) and `stack_id` itself, and that no two shifted ports collide.
**Where:** `tools/test_render_ports.py, tools/native/mios-resolver/src/ports.rs, tests/drift-gate-negatives.sh`
**Done When:** the offset is asserted on all three surfaces and a deliberate regression in any single one fails the test.
**Why:** multi-stack co-existence is a shipped feature that is currently unverified, and one of its three implementations was silently a no-op.
**Dep:** none

## AGY-1579 -- Reconcile in-container port fallbacks with the host allocation and record the rule in SSOT  (WS-PORTFLOAT | P3 | M) **DONE**
**Goal:** E-13 Ports are allocated from SSOT, not hand-assigned -- the last place a port literal legitimately differs from SSOT becomes documented and gated rather than folklore.
**What+How:** Several Quadlets pass `${MIOS_PORT_X:-<upstream-default>}` where the fallback is the UPSTREAM in-container port (pgvector 5432, vllm 11441, cpu-node 11451, crawl4ai 11235), so fallback and SSOT deliberately disagree -- defensible, but nowhere recorded. Meanwhile `[ports.categories.sidecar]` now allocates real keys for the sidecar daemons (guacd, redis, chrome_cdp, otelcol, pxe_hub_api, forge_ssh_git). Decide per service whether the container binds the SSOT port or the upstream default behind a host-side publish, write that decision into the `[ports.categories]` `doc` field, and make each `:-N` fallback consistent with the decision.
**Where:** `usr/share/mios/mios.toml, usr/share/containers/systemd/*.container, tools/generate-pod-quadlets.py`
**Done When:** every `:-N` fallback in a Quadlet is either the SSOT value or a documented upstream default; the rule is stated in SSOT and enforced by a gate.
**Why:** a reader cannot currently tell an intentional upstream-default fallback from a stale literal, so both are copied forward and neither is checked.
**Dep:** none

---

## WS-DOCGEN -- generative documentation

Programme: `docs/agy/doc-generative-documentation.md`. Epic ids D-# below are that
document's own sub-taxonomy for the documentation programme.

## AGY-1580 -- Fix mios-ai-tag's multi-line hint orphaning and repair the 22 damaged headers  (WS-DOCGEN | P0 | M)  **DONE**

**Goal:** D-1 The comment corpus is lossless -- a re-tag can never truncate or split a header, so every downstream doc generator reads whole prose.
**What+How:** `AI_LINE_RE` matches only lines containing an `AI-*:` marker, so a wrapped continuation survives `retag()`'s strip and the fresh block is inserted MID-SENTENCE between the hint and its orphan; `existing_hint()` then regexes a single line and drops the remainder -- a lossy round-trip. Replace the line filter with a REGION strip (first `AI_LINE_RE` match, then consume forward while the line matches `^<marker>\s{2,}\S` or `AI_LINE_RE`), and make `existing_hint()` join continuation lines with a single space before `_clean()`. Then repair the 22 already-damaged files, detectable as `AI-related:` lines ending in a dangling comma: `usr/libexec/mios/mios-sync-theme`, `mios-sync-toml`, `mios-sync-to-root`, `usr/lib/systemd/system/mios-sync-theme.service`, `installation/mios-install.sh`, `installation/README.md`, and mios-ai-tag's OWN header (line 2 is truncated at "...(AI-hint purpose"). Add `--selftest` round-tripping fixtures for shebang / BOM / CRLF / md front-matter / wrapped hint.
**Where:** `usr/libexec/mios/mios-ai-tag, tests/fixtures/ai-tag/`
**Done When:** `mios-ai-tag --selftest` passes; a full `--no-llm --dry-run` over the tree reports zero diffs on already-tagged files; no `AI-related:` line in the tree ends in a comma.
**Why:** generating a manual from a corpus with silent truncation bakes the truncation into the product, and every later step reads this corpus.
**Dep:** none

## AGY-1581 -- Close mios-ai-tag's SKIP_DIR and EXT gaps (65k vendored files in, 98 orphaned tags out)  (WS-DOCGEN | P0 | S)  **DONE**

**Goal:** D-2 The taggable corpus is exactly the authored corpus -- no vendored tree can enter it and no authored file can be locked out of its own tagger.
**What+How:** `SKIP_DIR` excludes `.venv`/`node_modules`/`target`/`site-packages` but NOT `.rustup`, so `mios-ai-tag --root /mnt/c/MiOS` would walk 65,133 vendored Rust-toolchain files (61,792 `.html` + 3,108 `.js`), find no existing hint, and issue ~65k teacher calls before writing `AI-hint` comments into the Rust standard-library docs. Add `\.rustup`, `\.tmp\.driveupload` (Drive's hardlink dedup cache), `tr_clone`, `tr_remote`, `shellcheck-v[0-9.]+`. Conversely 98 files already carry hand-written headers whose extensions are absent from `EXT` and can therefore never be refreshed: `.rs` 52, `.snap` 19, `.psm1` 15, `.qml` 7, `.nft` 2, `.ks`/`.template`/`.defaults` 1 each. Add all eight to `EXT` and map `.rs` to the `slash` comment style.
**Where:** `usr/libexec/mios/mios-ai-tag`
**Done When:** a walk from the drive root yields 2,142 authored files and zero `.rustup` paths; `mios-ai-hint-coverage --json` denominator includes the 52 Rust files.
**Why:** one accidental root-scoped run currently rewrites 65k vendored files and burns 65k inference calls; meanwhile the whole Rust native tier is invisible to its own tagger.
**Dep:** none

## AGY-1582 -- Resolve mios-ai-tag's hint cap, teacher endpoint and teacher model from SSOT  (WS-DOCGEN | P0 | S)  **DONE**

**Goal:** E-7 Law 7 NO-HARDCODE holds in the tagger itself -- the tool that documents the codebase must not be the one carrying phantom constants.
**What+How:** `_clean()` hardcodes a 260-char cap, `--teacher-endpoint` defaults to `localhost:${MIOS_PORT_LLM_LIGHT:-8450}` while mios.toml uses 8500 and the tool's own header says 11450, and `--teacher-model` defaults to `gemma4:12b`, a string that appears nowhere in SSOT. Resolve all three through the shared `mios_toml` resolver exactly as the sibling `mios-ai-hint-coverage` already does: `[ai_tag].hint_max_chars`, `[ai_tag].teacher_port_key` -> `[ports]`, `[ai_tag].teacher_model` (validated against `[ai].available_models`). Ship `hint_max_chars = 3000` so the 363 hand-written over-cap hints (max 2,837 chars) survive the next run; AGY-1588 ratchets it back to 260 once they are harvested.
**Where:** `usr/libexec/mios/mios-ai-tag, usr/share/mios/mios.toml`
**Done When:** no numeric or model literal remains in the tagger; `mios-hardcode-lint` is clean on the file; a re-tag leaves the 363 long hints byte-identical.
**Why:** three different values for one port and a model that does not exist mean the teacher path is untested folklore, and the 260 cap is a live data-loss trigger worth ~100k characters of prose.
**Dep:** AGY-1580

## AGY-1583 -- Build the comment lexer + classifier library with the 30-fixture unit test  (WS-DOCGEN | P1 | L)  **DONE**

**Goal:** D-3 "Stay in code" vs "migrate to docs" is a mechanical, testable function -- not a judgement call made once per file by whoever is reading it.
**What+How:** New `usr/lib/mios/mios_comments.py` (beside `mios_toml.py`, the established shared-library home) exporting `lex(path) -> list[Block]` and `classify(block, policy, refindex) -> Verdict`. Lex per language via `mios_ai_tag.comment_style()`, with three overrides: Python via `tokenize` COMMENT tokens + `ast` docstrings (NEVER regex -- regex miscounts multi-line data strings as prose), PowerShell `<# #>`, Rust `///`/`//!`. Classify with the ordered first-match rule set R0..R7 in BUILD-SPEC section 2: generated-artifact -> llm-payload -> header -> commented-out-code -> banner -> size+signal -> inline -> stale axis. Every threshold and every signal regex comes from `[docs]`/`[docs.signals]`, never from code. Ship `usr/libexec/mios/test_mios_manual.py` with the 30 named fixtures, each asserting an exact `(class, reason)` pair, including the boundary case `mios-tailscale-serve.ps1:67` (2 lines, ~28 words, why-signal, no narrative-signal -> STAY via the mid-size rule).
**Where:** `usr/lib/mios/mios_comments.py, usr/libexec/mios/test_mios_manual.py, usr/share/mios/mios.toml, Justfile`
**Done When:** all 30 fixtures pass; aggregate counts over the real tree land within 3% of STAY ~5,000 / MIGRATE ~1,900 / DROP ~400 / READONLY 42; `test_mios_manual.py` runs inside `just drift-gate` beside `test_mios_docgen.py`.
**Why:** without a fixed classifier the ratchet has no comparable number and "we migrated some comments" is unmeasurable.
**Dep:** AGY-1581

## AGY-1584 -- Land `mios-manual` with the corpus ledger and retire generate-manual.py  (WS-DOCGEN | P1 | L)  **DONE**

**Goal:** D-4 One documentation CLI exists, it reads the repo, and its output is reproducible byte-for-byte between a developer and CI.
**What+How:** New `usr/libexec/mios/mios-manual` with six subcommands (`render`, `audit`, `coverage`, `harvest`, `prune`, `ledger`). Corpus = `mios_ai_tag.walk()` INTERSECT `git ls-files` -- adopt `generate-ai-manifest.py`'s `tracked_files()` determinism rule verbatim, since a filesystem walk makes the artifact a function of the developer's working directory and can then never match a clean CI regeneration. Emit `usr/share/mios/reference/manual-corpus.tsv` (path, span, lines, words, sha12, class, reason, as, stale, landed_doc, landed_anchor, landed_words, pruned), carrying `landed_*` forward by `sha12` so reformatting the surrounding code never loses a landing record, and retaining pruned rows as tombstones. Retire `tools/generate-manual.py`: it imports only `os/argparse/shutil`, performs zero repo reads, is 1311 lines of hardcoded literal, ships 21 of 43 links broken, and `shutil.rmtree`s `usr/share/doc/mios/manual/` unconditionally BEFORE and independent of `--output`. `mios-manual --out` must never delete a path it was not asked to write.
**Where:** `usr/libexec/mios/mios-manual, usr/share/mios/reference/manual-corpus.tsv, tools/generate-manual.py (delete), Justfile`
**Done When:** `mios-manual ledger --check` is green on a clean tree; two runs from different CWDs produce byte-identical output; `tools/generate-manual.py` and the `manual:` Justfile target are gone.
**Why:** the two surfaces in this repo that are actually documentation are the only two with no drift gate, which is exactly why the shipped manual has 21 dead links and 50 `file:///C:/MiOS/` paths inside a Linux OS's docs.
**Dep:** AGY-1583

## AGY-1585 -- Create the authored manual tree and gate manual.md with `render --check` (the real AGY-238)  (WS-DOCGEN | P1 | L)  **DONE**

**Goal:** D-5 Regeneration can never destroy hand-written prose, and a stale manual fails the build.
**What+How:** Create `usr/share/doc/mios/manual/NN-*.md`, each with YAML front matter (`chapter`, `part`, `title`, `status`, `sources`, `harvest`). Migrate `generate-manual.py`'s 154 `{title, desc, content, citations}` records into those files as DATA -- they are genuine hand-written narrative no extractor can synthesize -- dropping the `credits.md#L39` line-number citations, the `file:///C:/MiOS/` URI scheme, the per-page boilerplate and the hardcoded "Seven Architectural Laws" list that contradicts `[laws]` (16 rows). The generator writes ONLY inside `<!-- MIOS-GEN:id --> ... <!-- /MIOS-GEN -->` marker pairs plus its own declared output list; prose outside markers is invisible to it. Add drift check 152 `check_manual_generated` (byte-diff of manual.md, the derived reference docs and every marker interior; plus zero `file://` and zero `C:[\\/]`), its `test_manual_generated` with BOTH phases -- a marker-interior edit must go red, an outside-the-marker edit must stay green -- and add `mios-manual render` as step 7 of `tools/sync-generated.sh`. Note `AGY-238` is marked `[DONE]` in AGY-TASKS.md:2423 and is NOT done: there is no `--check` flag and no `check_manual_generated` anywhere.
**Where:** `usr/share/doc/mios/manual/, usr/share/doc/mios/manual.md, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, tools/sync-generated.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** `render --check` is green; appending a line to manual.md turns gate 152 red; appending a paragraph OUTSIDE a marker in a chapter file leaves it green; `just sync` regenerates the manual.
**Why:** today regenerating the manual DELETES its H1 title while simultaneously fixing 20 dead links -- the committed file and its generator diverged in both directions, the signature of an ungated generator.
**Dep:** AGY-1584

## AGY-1586 -- Generate the five derived reference docs and the missing `/usr/share/doc/mios/` entry point  (WS-DOCGEN | P1 | M)  **DONE**
**Goal:** E-13/Law 8 Ports, laws, pipeline, verbs and units have exactly ONE generated answer, and a reader landing on a booted host has somewhere to start.
**What+How:** `mios-manual render` emits `reference/ports.md` (from `[ports.categories]` + `[ports]`), `reference/laws.md` (from `[laws]`, 16 rows, plus the 12 root exceptions), `reference/pipeline.md` (from `[build.pipeline]`), `reference/cli.md` (113 `[verbs.*]` + the `usr/libexec/mios/` backends), `reference/units.md` (from the UNIT FILES, not `mios.toml [units.*]` and never `globals.*`), and `usr/share/doc/mios/README.md` -- 130 baked doc files currently have NO entry point on a host. Every derived section ends with a `<!-- derived from ... -->` provenance line. These are the single answers the 1,155 stale port refs across 122 of 225 files can then be rewritten against; note the SSOT's OWN inline comments are stale too (`mios.toml:9438` says Open WebUI is at `:3033`, `:9439-9440` cites a port 9119 that exists nowhere), so a prose fix that trusts mios.toml comments re-imports the errors.
**Where:** `usr/libexec/mios/mios-manual, usr/share/doc/mios/reference/{ports,laws,pipeline,cli,units}.md, usr/share/doc/mios/README.md`
**Done When:** all six files are generated, gate 152 covers them, and every port/law/stage number in them equals the SSOT value.
**Why:** a reader cannot get a straight answer to "what port is X on", "how many laws are there" or "what does build step N do"; every doc that answers directly is wrong, and one agent-facing contract file states a law that does not exist.
**Dep:** AGY-1585

## AGY-1587 -- Add `check_doc_refs_resolve` and clear the ~70 verified stale references  (WS-DOCGEN | P1 | M)  **DONE**

**Goal:** D-6 A path, unit or `mios-*` name printed in a header or a generated doc is guaranteed to exist.
**What+How:** Drift check 153: `mios-manual audit --stale --json` over every `AI-hint:`/`AI-related:`/`AI-doc:` line and every generated doc; dangling count must be `<= [docs].max_stale_refs`. Dangling = absent from the whole-tree `RefIndex` (a token is not stale if it appears on any CODE line anywhere), absent from disk, and unmatched by `[docs].ref_allowlist` (runtime-created `/etc/ceph/ceph.conf`, `/etc/cdi/nvidia.yaml`, `/var/**`, `@@MIOS_*@@` template tokens). Then fix the ~70 verified stale refs, starting with the nine renumbered `automation/` stage refs -- three of which now resolve to a DIFFERENT script and are therefore actively misleading: `38-hermes-agent.sh` -> `72-` (38- is now selinux), `37-selinux.sh` -> `38-` (37- is now k3s-selinux), `19-k3s-selinux.sh` -> `37-`, plus `15-render-quadlets.sh` -> `34-`, `45-coderun-sandbox-build.sh` -> `54-bake-coderun-sandbox.sh`, `41-mios-dropin-fanout.sh` -> `48-`, `13-ceph-k3s.sh` -> `36-`, `08-system-files-overlay.sh` -> `01-`, `globals.generated.ps1` -> `globals.ps1`. Do NOT "fix" the 29 `ollama` and 12 `:8080` mentions -- they are history, upstream defaults being overridden, or container-internal ports behind a host remap; allowlist them.
**Where:** `automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, usr/share/mios/mios.toml, usr/share/doc/mios/adr/*, upstream/selinux.md, Containerfile, tools/render-ports.py`
**Done When:** `max_stale_refs = 0` and the gate is green; injecting `# AI-related: automation/99-nonexistent.sh` into a scratch file turns it red.
**Why:** 24 of the ~70 stale refs sit inside AI-hint/AI-related header lines, which never self-correct because `mios-ai-tag` reuses an existing hint verbatim forever -- so without a gate they are permanent.
**Dep:** AGY-1586

## AGY-1588 -- Harvest the 363 over-cap AI-hints into docs, then prune them behind the landing gate  (WS-DOCGEN | P1 | L) **DONE**
**Goal:** D-7 The ~100k characters of prose currently hiding in the `AI-hint:` field live in documentation, and the tagger's own cap becomes safe to re-impose.
**What+How:** 363 hints (18.9%) exceed the tagger's 260-char `_clean()` cap and were therefore NOT written by `mios-ai-tag` -- they are hand-written mini-documents topping out at 2,837 chars (`mios_pipe/routing/agent_call.py`), 2,642 (`swarm.py`), 2,158 (`a2a.py`), 2,128 (`web_research.py`), 1,980 (`vision.py`), 1,947 (`mios_dispatch.py`). Run `mios-manual harvest --class=overlong-hint --apply` to write each into `usr/share/doc/mios/manual/_harvest/<slug>.md` annotated `<!-- mios-src: <path>:<a>-<b> sha=<sha12> -->`, filling the ledger's `landed_*` columns; SOURCE IS NOT TOUCHED in this step. Land drift check 154 `check_comment_landing` (pointer resolution + landing predicate for pruned rows + `landed_doc` may never be cleared). ONLY THEN run `prune --class=overlong-hint --only-landed --apply`, which replaces each hint with a `<=260`-char summary plus an `AI-doc:` row and REFUSES any block whose predicate is false. Finally ratchet `[ai_tag].hint_max_chars` back to 260 and `max_overlong_hints` to 0.
**Where:** `usr/libexec/mios/mios-manual, usr/share/doc/mios/manual/_harvest/, usr/share/mios/reference/manual-corpus.tsv, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, usr/share/mios/mios.toml`
**Done When:** harvest and prune are SEPARATE commits with `audit --deletions` green between them; a full `mios-ai-tag --no-llm` pass is byte-identical afterwards; gate 154 goes red if a harvested passage is gutted.
**Why:** this is the single largest data-loss hazard in the tree -- `existing_hint()` re-applies the 260-char cap on reuse, so the very next tagger run silently truncates all 363.
**Dep:** AGY-1587, AGY-1582

## AGY-1589 -- Land the comment ratchet and its monotone guard  (WS-DOCGEN | P1 | M) **DONE**
**Goal:** D-8 Narrative comment volume can only go down, and "raise the ceiling" is a gate violation rather than a one-character fix.
**What+How:** Drift check 155 `check_comment_ratchet` asserts `unmigrated_narrative <= [docs].max_unmigrated_narrative`, `stale_refs <= [docs].max_stale_refs`, `overlong_hints <= [ai_tag].max_overlong_hints` and `undocumented_components <= [docs].max_undocumented_components`, resolving each ceiling with the layered pattern `mios-ai-hint-coverage` already uses (CLI > env > mios.toml > permissive report-only fallback). Drift check 156 `check_doc_ratchet_monotone` then asserts every one of those four ceilings is `<=` the value in `usr/share/mios/reference/doc-ratchet-floor.tsv`, which `mios-manual coverage --write-floor` can only ever LOWER (it writes `min(recorded, current)`). Both need negative tests: a 12-line 60-word narrative block added to a scratch `usr/lib/mios/*.py` must turn 155 red, and incrementing any ceiling by 1 must turn 156 red. Neither may be added to `[testing].negative_coverage_exempt` -- both are exercisable with a pure text edit on a Linux runner.
**Where:** `automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, usr/libexec/mios/mios-manual, usr/share/mios/reference/doc-ratchet-floor.tsv, usr/share/mios/mios.toml`
**Done When:** both gates are enforcing at the measured baseline, both RED edits have been observed red and reverted, and the ratchet number appears in `just drift-gate` output.
**Why:** gate 155 alone is unfalsifiable in practice -- this repo has a documented history of gates that cannot fail, and a ratchet whose ceiling is freely raisable is exactly one of them.
**Dep:** AGY-1584

## AGY-1590 -- Harvest the narrative mass: mios.toml, the two Windows entry points, and the agent-pipe tree  (WS-DOCGEN | P2 | XL) **DONE**
**Goal:** D-9 The 1,764 narrative blocks / 16,481 lines / ~150k words of design prose living in comments become chapters and ADRs.
**What+How:** Harvest in yield order, one wave per commit, each followed by a gated prune. Wave A: `usr/share/mios/mios.toml` (36,159 narrative words / 4,050 lines), `build-mios.ps1` (23,791 / 2,975) and `Get-MiOS.ps1` (17,041 / 2,032) -- three files hold 55% of all narrative. Wave B: `usr/lib/mios/agent-pipe/**`, 14 of the top 25 files (`memory/pg.py`, `routing/dag_exec.py`, `mios_surface.py`, `scheduler/preempt.py`, `federation/a2a.py`, `routing/agentreg.py`, `server.py`, `mios_dispatch.py`, ...), assembled as ONE "agent-pipe architecture" chapter -- which is what those module docstrings collectively already are. Wave C: every `as=adr-candidate` block becomes a real ADR, starting with `.github/workflows/mios-ci.yml:282` (42 lines giving root cause, exact error text and operator confirmation for the rootful-podman GID-remap bake failure) and `mios.toml:5457` (90 lines, the crawl4ai container scrapped, citing Law 5). Wave D: systemd/Quadlet prologues into `reference/units.md` -- config formats are comment-DOMINATED (`.rules` 78%, `.conf` 64%, `.preset` 54%, `.service`/`.timer` 43%, `.toml` 39%) and are the real design-intent surface. Lower `[docs].max_unmigrated_narrative` and the floor after each wave.
**Where:** `usr/share/doc/mios/manual/, usr/share/doc/mios/adr/, usr/share/mios/mios.toml, build-mios.ps1, Get-MiOS.ps1, usr/lib/mios/agent-pipe/**`
**Done When:** each wave lands as harvest-commit then prune-commit with `audit --deletions` green between; `max_unmigrated_narrative` has fallen by the wave size; no source block was deleted without a ledger row proving its landing.
**Why:** this is the extraction prize -- ~150k words of decisions, rejected alternatives and incident history that today only an agent reading whole files can find.
**Dep:** AGY-1588, AGY-1589

## AGY-1591 -- Stop projecting AI-hint prose into globals.{sh,ps1} and gate it  (WS-DOCGEN | P2 | M) **DONE**
**Goal:** Law 8 The generated resolvers carry VALUES, not documentation, and a unit's narrative has exactly one home.
**What+How:** `automation/lib/globals.sh` and `globals.ps1` each carry 129 `AI-hint:` blocks as `MIOS_UNITS_*_COMMENT` environment-variable VALUES -- 1.19 MB of generated shell/PowerShell that is mostly prose, and the fourth home of every unit sentence (unit file -> `mios.toml [units.*]`, 182 tables / 115 `comment=` keys -> globals.sh -> globals.ps1), which is why both resolvers show exactly 486 narrative lines. Teach `tools/render-globals.py` to skip the `comment` key, re-run `bash tools/sync-generated.sh`, and add drift check 157 `check_no_generated_prose_in_resolvers` asserting zero `AI-hint:` and zero `MIOS_UNITS_[A-Z0-9_]*_COMMENT=` in either file. THIS MUST LAND AFTER AGY-1590 wave D: the unit narrative it deletes must already be harvested from the unit files, or the projection removes the only copy some readers have.
**Where:** `tools/render-globals.py, automation/lib/globals.sh, automation/lib/globals.ps1, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh`
**Done When:** both resolvers shrink by ~1.1 MB combined, gate 157 is green, and appending one `MIOS_UNITS_TEST_COMMENT='# AI-hint: x'` line turns it red.
**Why:** one stale sentence about `hermes-agent.service` on `:8642` currently exists in four materially different file formats, and fixing the doc fixes none of the others.
**Dep:** AGY-1590

## AGY-1592 -- Retire the comment graveyard and the unified-knowledge dump  (WS-DOCGEN | P2 | M) **DONE**
**Goal:** D-10 No documentation surface is built by scraping deleted comments or dumping whole files.
**What+How:** `usr/share/doc/mios/knowledge/recovered_comments.md` is 4.13 MB / 60,019 lines / 801 file sections -- 42% of the entire markdown corpus -- and `tools/lib/extract_comments.py:29-34` builds it from git diffs by harvesting ONLY DELETED comment lines: a graveyard, stale by construction, whose first entry is already contradicted by `automation/lib/globals.sh:170`. Run `mios-manual audit --corpus-extra` over it ONCE as a rescue pass, harvest anything classified MIGRATE whose target still resolves, then delete both the file and the extractor. Separately retire `tools/generate-unified-knowledge.py`: it walks CWD not `--root`, has no CLI and no `--check`, its category taxonomy keys off `specs/core`/`specs/memory`/`evals/` (only `specs/engineering` exists, so nearly everything lands in "other" and never enters the semantic index), its extension filter is inverted so any dotfile sweeps in, and its three-regex redaction misses `sk-proj-*`, `ghp_*`, PEM and JWT while the output is copied into agent-visible scratch. Replace the RAG-seeding use with `mios-manual audit --json`.
**Where:** `usr/share/doc/mios/knowledge/recovered_comments.md (delete), tools/lib/extract_comments.py (delete), tools/generate-unified-knowledge.py (delete), automation/ai-bootstrap.sh, tools/sync-wiki.py`
**Done When:** all three are gone, `ai-bootstrap.sh` and `sync-wiki.py` consume the audit JSON, and the markdown corpus drops from 9.99 MB to ~5.9 MB.
**Why:** 42% of the doc corpus by bytes is mechanically-transcribed deleted comments shipped into `/usr/share/doc/`, and one commit has already re-duplicated part of it back into manual.md.
**Dep:** AGY-1590

## AGY-1593 -- Collapse the 69-copy boilerplate, the 3 duplicate doc pairs, and mios-codebase-index's rival taggability  (WS-DOCGEN | P2 | M) **DONE**
**Goal:** D-11 Every fact has one home; the codebase is quickly auditable because there is nothing to cross-check.
**What+How:** (1) The two-paragraph "MiOS is one thing built two ways at once ... `bootc upgrade` it like a `git pull` ... `bootc rollback` it like a Ctrl-Z" block appears in 69 files (~48 KB) and every copy drifts independently -- some name GNOME 50, some name ports, some name lanes. Replace all 69 with `<!-- MIOS-GEN:boilerplate:what-mios-is -->` rendered from the single authored chapter. (2) De-duplicate the three near-verbatim doc pairs (~116 KB, already diverging on facts): `docs/agy/doc-mios-metal.md` <-> `concepts/mios-metal-architecture.md`, `doc-container-runtime.md` <-> `concepts/container-os-runtime.md`, `doc-foss-upstream.md` <-> `concepts/foss-upstream-map.md`; add `docs/agy/README.md` marking that tree process-only, the pattern `usr/share/doc/mios/archive/` already models. (3) Delete `mios-codebase-index`'s private `EXT`/`BASENAMES`/`SKIP_DIR`/`SKIP_SUFFIX` -- a second, already-diverged definition of the corpus that includes `.json` (which mios-ai-tag explicitly skips as comment-less) -- and import `mios-ai-tag` through the same `SourceFileLoader` shim `mios-ai-hint-coverage` uses.
**Where:** `usr/share/doc/mios/**, docs/agy/**, usr/libexec/mios/mios-codebase-index, usr/share/mios/**`
**Done When:** the boilerplate exists once; the three pairs are one file each with a pointer; `mios-codebase-index` defines zero taggability constants of its own and its file count matches `mios-ai-hint-coverage`'s denominator.
**Why:** "fewer, more feature-complete components" is unachievable while three tools disagree about which files exist and one paragraph has 69 independently-drifting copies.
**Dep:** AGY-1592

## AGY-1594 -- Port the comment lexer hot path to Rust behind the unchanged mios-manual CLI  (WS-DOCGEN | P3 | L)  **DONE**
**Goal:** Law 14 The whole-tree lexing pass runs in the native tier without creating a second definition of the corpus.
**What+How:** `lex()` over 2,142 files is the only compute-bound part of the system; classification, rendering and the ledger stay in Python because they are policy and because taggability must remain defined exactly once, in `mios-ai-tag`, which four tools import. Add a `mios-comment-lex` crate under `tools/native/` alongside the existing `mios-ssot-walk`/`mios-ssot-lint` crates, emitting the same `Block` JSON records. `mios_comments.lex()` becomes a two-branch dispatcher: call the binary when present, fall back to the Python lexer otherwise, and add a differential gate asserting the two produce identical `sha12` sets over the tree -- the same strangler-fig shape `check_resolver_shell_equivalence` already uses for `mios-resolver`. Only start after AGY-1583's 30 fixtures are frozen, so the Rust implementation is validated against a fixed oracle rather than a moving one.
**Where:** `tools/native/mios-comment-lex/, tools/native/Cargo.toml, usr/lib/mios/mios_comments.py, automation/98-drift-checks.sh`
**Done When:** the differential gate is green, the binary is optional (a bare checkout still works), and full-tree `mios-manual audit` runtime drops below 5s.
**Why:** Law 14 puts native tooling in Rust, but porting the classifier or the taggability rules would fork the corpus definition -- the lexer is the one piece that can move without that cost.
**Dep:** AGY-1583, AGY-1589
```

---

---

## WS-BLADE -- workload mobility (ADR-0017)

Programme: `usr/share/doc/mios/adr/0017-blade-workload-mobility.md`, extending
ADR-0016. The capability model in `[blade.archetypes]`/`[blade.requires]` already
decides WHERE a service may land; these finish what happens when it cannot, or
when the machine holding it dies.

## AGY-1595 -- Project [blade.requires] to k3s nodeSelectors and Pacemaker location rules  (WS-BLADE | P1 | M)  **DONE**

**Goal:** E-18 One SSOT declaration drives every consumer -- a placement constraint stops being hand-maintained in two scheduler dialects.
**What+How:** `[blade.requires]` maps a service to the capabilities it needs and `[blade.archetypes]` maps an archetype to the capabilities it grants; today only systemd `ConditionPathExists` gating consumes them. Extend `tools/generate-blade-dropins.py` to ALSO emit k3s `nodeSelector`/`tolerations` for container workloads and `pcs constraint location` rules for VM resources, both derived from the same two tables. Neither scheduler gets a hand-written constraint.
**Where:** `tools/generate-blade-dropins.py, usr/share/mios/mios.toml, automation/98-drift-checks.sh`
**Done When:** every service in `[blade.requires]` yields a k3s selector or a Pacemaker location rule; a new capability added to SSOT appears in both without editing either scheduler's config; a drift-check fails when a hand-written constraint exists that SSOT did not produce.
**Why:** two schedulers with hand-maintained constraints drift silently, and the drift only surfaces as a workload that mysteriously will not schedule.
**Dep:** none

## AGY-1596 -- Give every GPU-gated service a CPU fallback lane so placement never refuses  (WS-BLADE | P1 | M)  **DONE**

**What+How:** ADR-0017 D2: a service requiring `gpu-serving` that lands on a GPU-less blade must start its CPU lane rather than fail to schedule. Add a `cpu_fallback` alongside the requirement for `mios-llm-heavy`, `mios-llm-heavy-alt` and `mios-llm-worker@`, pointing at the already-shipped `mios-llm-light`. The fallback must present the SAME `/v1` contract on the SAME SSOT-allocated port so no caller can tell which kind of blade answered.
**Where:** `usr/share/mios/mios.toml [blade.requires], usr/libexec/mios/role-apply, tools/generate-blade-dropins.py`
**Done When:** a GPU-gated unit started on a GPU-less blade serves `/v1/models` from the CPU lane; the caller's endpoint is unchanged; a drift-check asserts every `gpu-serving` requirement declares a fallback.
**Why:** without this, "migrate anywhere" is false for exactly the services that matter most, and the failure presents as a hang rather than a decision.
**Dep:** AGY-1595

## AGY-1597 -- Implement local-first failover ordering with asymmetric anti-flap dwell  (WS-BLADE | P1 | M)  **DONE**
**What+How:** Implement `[blade.placement].failover_order = ["local","localhost","cluster"]` and the `[blade.collapse]` dwell values. A failed workload is retried in place before the cluster is asked to allocate it elsewhere. Release back to the mesh uses `recover_dwell_s`, which is deliberately several times `dwell_s`.
**Where:** `usr/libexec/mios/role-apply, usr/share/mios/mios.toml [blade.placement] [blade.collapse], usr/lib/systemd/system/`
**Done When:** killing a service restarts it in place without a migration event; killing the machine migrates it; a link flapping faster than `recover_dwell_s` produces exactly one migration, not one per flap.
**Why:** most failures are a crashed process on a healthy machine where a restart is the whole fix; migrating first turns two seconds into a cold start and possibly a volume move.
**Dep:** AGY-1595

## AGY-1598 -- Add origin-node id and logical timestamp to the pgvector schema  (WS-BLADE | P0 | M)  **DONE**

**What+How:** ADR-0017 D5 states the merge rules are not implementable without provenance on every row. Add an origin-node id and a logical timestamp to `knowledge`, `agent_memory`, `event`, `session`, `scratch` and `config_kv` in `usr/share/mios/postgres/schema-init.sql`, backfilled to the local node for existing rows. This is a PREREQUISITE, not an optimisation: `[blade.reconcile].enabled` ships `false` and must not be flipped until this lands.
**Where:** `usr/share/mios/postgres/schema-init.sql, usr/libexec/mios/seed-db-config.py, usr/share/mios/mios.toml [blade.reconcile]`
**Done When:** every row in the six tables carries origin + logical timestamp; existing rows are backfilled; a drift-check fails if `[blade.reconcile].enabled = true` while any of the six lacks the columns.
**Why:** enabling divergence without provenance produces a merge nobody can compute and silent data loss on rejoin.
**Dep:** none

## AGY-1599 -- Implement the per-class reconcile rules, with config_kv conflicts raised to the operator  (WS-BLADE | P2 | L)  **DONE**
**What+How:** Implement the four merge classes from ADR-0017 D5: union-by-hash for `knowledge`/embeddings, append-ordered for `agent_memory`/`event`, last-writer-wins for `session`/`scratch`, and conflict-is-error for `config_kv`. The last is deliberately NOT auto-merged -- a `config_kv` divergence surfaces to the operator, because config is intent and silently picking a winner lets a partition change policy.
**Where:** `usr/libexec/mios/, usr/share/mios/postgres/, usr/share/mios/mios.toml [blade.reconcile]`
**Done When:** a simulated partition with writes on both sides reconciles losslessly for the first three classes; a divergent `config_kv` key blocks reconcile and names the conflicting keys; only then may `enabled` flip to true.
**Why:** the availability choice in ADR-0017 is only affordable if each data class has a fixed rule decided in advance; a vague rule is how this design fails.
**Dep:** AGY-1598

## AGY-1600 -- Make "blade unreachable" legible instead of surfacing as a model error  (WS-BLADE | P1 | S)  **DONE**

**What+How:** ADR-0016 §6 notes the detection exists but is unwired: a seat whose blade is gone should say "blade unreachable", not return an inference error. Wire the `[blade.discovery]` health chain result into the dashboard and the `/v1` error path so the failure names its own cause, and record blade reachability in the boot record as a non-critical check per ADR-0016 §8.
**Where:** `usr/lib/mios/agent-pipe/, usr/share/mios/hermes/, usr/share/mios/mios.toml [blade.discovery] [greenboot]`
**Done When:** with the blade unreachable, the dashboard states "blade unreachable" and `/v1` returns a named error rather than a model failure; greenboot records the check without failing the boot.
**Why:** the seat's whole contract is that its front door is off-box, so the one failure it must explain clearly is the one where the box behind the door is gone.
**Dep:** none

## AGY-1601 -- Ratchet the 150 stale AI-related references down to zero  (WS-DOCGEN | P2 | M) **DONE**
**Goal:** D-4 Documentation references resolve -- an `AI-related` line that names a file which no longer exists sends the next reader (human or agent) to a dead path.
**What+How:** `check_doc_refs_resolve` reports 150 dangling references after the false-positive fix (it now understands that `usr/share/mios/mios.toml [laws]` is a file plus a TOML section, not a path). Work the list down: for each, either correct the path, drop the reference if the target was deliberately removed, or add it to `[docs].ref_allowlist` when it is genuinely runtime-created. Lower `[docs].max_stale_refs` with each batch.
**Where:** `usr/share/mios/mios.toml [docs].max_stale_refs, automation/*.sh, tools/*.py, usr/libexec/mios/*`
**Done When:** `max_stale_refs` reaches 0 and `check_doc_refs_resolve` passes with it; no reference is silenced by widening the allowlist without a stated reason.
**Why:** the count was 0 only because the check could never fail -- its violations went to stderr while the wrapper captured stdout, so it reported success on every run. The real backlog was invisible.
**Dep:** none

---

## WS-THESIS -- the four open campaigns

MiOS is a proof of an idea (ROADMAP.md, "What this roadmap is evidence for").
These four close the gaps between what the thesis claims and what the tree can
demonstrate. Nothing new starts until they do.

## AGY-1602 -- Audit every drift-check for falsifiability  (WS-THESIS | P0 | L)  **DONE**
**Goal:** Thesis part 1 -- a check that cannot fail asserts a proof that does not exist, so a hollow gate does not merely miss bugs, it falsifies the claim that drift is mechanically impossible.
**What+How:** For each of the 181 checks in `automation/98-drift-checks.sh`, establish the edit that makes it RED and prove it with a negative test. Known failure shapes to look for, all found in this tree: violations written to stderr while the wrapper captures stdout; `2>&1` placed after a heredoc delimiter where it redirects nothing; inverted polarity (`if out=$(...)` instead of `if ! out=...`); a target path or generator that does not exist so `regen_and_diff` returns Skip; an empty SSOT list so the loop runs zero times; a `-maxdepth` that cannot reach the files the check names.
**Where:** `automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, tools/*.py`
**Done When:** every dispatched check has a negative test that demonstrably fails on an injected defect; `check_negative_coverage` passes with an empty exempt list, or each exemption states why the check is structurally unfalsifiable.
**Why:** four hollow checks were found in a single session, three of them shipped green for weeks. The count of undiscovered ones is unknown, which is the problem.
**Dep:** none

## AGY-1603 -- Make all 66 units reproduce from SSOT  (WS-THESIS | P0 | L)  **DONE**
**Goal:** Thesis part 1 -- systemd is the largest surface in the image, so 39 units that do not regenerate is the biggest single hole in "one file defines the OS".
**What+How:** `mios-unit-gen --check` reports 27 of 66 units matching. Work `[unit_projection].drift` down: for each registered unit, diff the rendered output against `usr/lib/systemd/system` and either fix the renderer or correct the `[units.*]` declaration. Lower `max_drift` with each batch; the ratchet only descends.
**Where:** `tools/native/mios-unit-gen/, usr/share/mios/mios.toml [units], [unit_projection]`
**Done When:** `max_drift` reaches 0 with `check_unit_projection` green, and the golden fixtures are regenerated from SSOT rather than copied from the tree they verify.
**Why:** the claim is that the OS is a projection. For 39 units it currently is not, and the gate that should have said so was reporting success without comparing anything.
**Dep:** none

## AGY-1604 -- Close the documentation campaign  (WS-THESIS | P1 | L)  **DONE**
**Goal:** Legibility is the deliverable, so the doc programme is not tidying -- it is the artifact a reader receives.
**What+How:** Drive `[docs].max_unmigrated_narrative` from 1724 to 0 via `mios-manual harvest -> verify -> prune`, and `[docs].max_stale_refs` from 150 to 0 (AGY-1601). Then retire the four surfaces the build spec names once their content has provably landed.
**Where:** `usr/libexec/mios/mios-manual, usr/share/doc/mios/, usr/share/mios/reference/manual-corpus.tsv`
**Done When:** both ratchets read 0, `check_comment_landing` proves every pruned block still lands, and the generated manual builds from SSOT plus harvested prose alone.
**Why:** 1,724 narrative blocks are knowledge trapped where no reader will find it, and the repo is meant to be the thing that carries the idea.
**Dep:** none

## AGY-1605 -- Prove projection completeness over every shipped file  (WS-THESIS | P1 | L)  **DONE**
**Goal:** Thesis part 1 -- a surface with no generator and no gate is a surface the claim does not cover, and today nothing enumerates them.
**What+How:** Enumerate every tracked file that ships in the image and classify it: GENERATED (names its generator and its gate), AUTHORED (explicitly declared, with a reason), or UNCLASSIFIED. Add a gate that fails on any UNCLASSIFIED file. The 9 `.desktop` launchers found ungoverned this session are the pattern -- they looked covered because a vacuous check said so.
**Where:** `usr/share/mios/mios.toml [laws.projection_registry], automation/98-drift-checks.sh`
**Done When:** zero UNCLASSIFIED files; the projection registry names a generator and a gate for every generated surface; adding an ungoverned file to the image fails the gate.
**Why:** "everything is projected" is currently an assertion about an unenumerated set. Until the set is enumerated the claim is unfalsifiable, which is the same defect as a hollow gate.
**Dep:** AGY-1602

## AGY-1606 -- Drive the legibility floors down  (WS-THESIS | P2 | L)  **DONE**
**Goal:** The repository IS the deliverable -- a person should be able to read it and see the idea.
**What+How:** `[legibility]` now ratchets tracked files, size, shell and PowerShell lines, automation phases and libexec verbs, all floored at today's measurement. Reduce them by folding: PowerShell is 33,168 lines against 9,011 of Rust while Law 14 makes Rust the native tier; 282 libexec verbs and 72 automation phases carry heavy duplication; two vendored assets are most of the 202 MB.
**Where:** `usr/share/mios/mios.toml [legibility], usr/libexec/mios/, automation/, tools/native/`
**Done When:** each floor is materially lower with no capability lost, and the PowerShell-to-Rust ratio has inverted for anything that is a program rather than glue.
**Why:** an idea nobody can read is not demonstrated. Size is therefore a correctness property here, not hygiene.
**Dep:** none

## AGY-1607 -- Sweep for gates defined but never registered  (WS-EVIDENCE | P0 | M) **[DONE]**
**Goal:** No check exists that nothing runs.
**What+How:** `check_comment_lex_equivalence` sat defined in `98-drift-checks.sh` and absent from `main()`, so it never executed; `check_gate_registry` caught it only after it had been dormant. Run the registry check FIRST in CI rather than mid-suite, and extend it to the inverse direction: a name listed in `main()` with no function definition currently fails at runtime instead of at registry time. Also cover the tools tier -- `tools/check-*.py` files whose AI-hint claims a drift-check identity while no gate invokes them (`tools/check-comment-ratchet.py` claimed "Drift check 155" and was wired nowhere).
**Where:** `automation/98-drift-checks.sh (check_gate_registry, main), tools/check-*.py`
**Done When:** every `check_*` function is registered or deleted, every `tools/check-*.py` is invoked by a registered gate or renamed to say it is a report, and a negative test proves the registry check fails when a definition is unregistered AND when a registration is undefined.
**Verify:** `bash automation/98-drift-checks.sh check_gate_registry` passes, AND `grep -c '^check_' automation/98-drift-checks.sh` equals the count registered in main(), AND a deliberately unregistered stub function makes it red.
**Do NOT:** count the checks and call it proof. The gate must go RED under a planted unregistered function -- an unrun check is exactly what this task exists to find.
**Why:** an unregistered gate is worse than no gate: it reads as coverage and delivers none.
**Dep:** none

## AGY-1608 -- Make stale-ref measurement enforceable, then enforce it  (WS-EVIDENCE | P0 | L) **[DONE]**
**Goal:** `[docs].max_stale_refs` becomes a real ceiling instead of a number nobody measured.
**What+How:** The measurement was broken outright -- the allowlist was matched with `re.search`, where `C:\mios-bootstrap\Get-MiOS.ps1` is an invalid pattern (bad escape `\m`), so the checker crashed rather than counted. Repaired, it reported 1101, of which 376 hits were `/usr/bin/env` from shebang lines. `RefIndex` now knows runtime path prefixes, units and containers declared in mios.toml, `emit_exports()` keys and `referenced_names.txt`, leaving 446. Drive that to a number that is entirely real: resolve pod/short names (`mios-hermes`, `mios-gpu`, `mios-heavy`, `mios-resolver`), decide whether `/etc/mios/*` runtime-created paths are references or state, and treat `mios-bootstrap` as the sibling repo it is. THEN set the ceiling and remove the "(reported, not a ceiling)" qualifier.
**Where:** `usr/lib/mios/mios_comments.py (RefIndex), automation/98-drift-checks.sh (check_docs_ratchet), usr/share/mios/mios.toml [docs].max_stale_refs`
**Done When:** every remaining dangling token is a genuine broken reference, the ceiling equals that count, a negative test breaches it, and `check_docs_ratchet` fails when the count rises.
**Verify:** `bash automation/98-drift-checks.sh check_docs_ratchet` reports `stale-refs=N/N` with the ceiling equal to the measurement, AND adding an `# AI-related: automation/no-such-file.sh` line to a tracked file turns it red.
**Do NOT:** raise [docs].max_stale_refs to whatever the count happens to be without first making every remaining hit a genuine broken reference. A ceiling over noise measures nothing.
**Why:** a ceiling of 0 sat in the SSOT next to a measurement that had never once completed. Both a fake zero and an enshrined 446 of noise are the same defect.
**Dep:** none

## AGY-1609 -- Strengthen check_negatives_are_effective past substring matching  (WS-EVIDENCE | P1 | M) **[DONE]**
**Goal:** The gate that judges negative tests must judge behaviour, not spelling.
**What+How:** It currently reads the first 1500 characters of each `test_*` function and requires the substring `die`/`return 1`/`FAIL`/`exit 1` AND one of `98-drift-checks.sh`/`97-ssot-lint.sh`/`check_`. A test satisfies it by MENTIONING a check name in a log line while never invoking one, and a test longer than 1500 characters can hide its assertion past the window. Replace with a structural check: parse each function, require at least one invocation of a gate entry point, and require that invocation to appear in a condition whose failure branch calls `die`.
**Where:** `automation/98-drift-checks.sh (check_negatives_are_effective), tests/drift-gate-negatives.sh`
**Done When:** a test that only logs a check name, and a test whose assertion sits beyond 1500 characters, are both reported; the existing suite still passes.
**Verify:** a test that only mentions a check name in a `log` line, and a test whose assertion sits beyond 1500 characters, are BOTH reported by `bash automation/98-drift-checks.sh check_negatives_are_effective`.
**Do NOT:** keep substring matching. A test satisfied by spelling `check_x` in a log line is the defect, not the fix.
**Why:** this is the gate guarding every other gate's evidence. Substring matching makes it the easiest one in the tree to satisfy without meaning it.
**Dep:** AGY-1607

## AGY-1610 -- Lint for detections discarded by pipefail  (WS-EVIDENCE | P1 | S) **[DONE]**
**Goal:** No test can pass its detector and fail its harness.
**What+How:** `tests/drift-gate-negatives.sh` runs under `set -euo pipefail`. `generator | grep -q "msg"` returns the GENERATOR's exit status when it exits non-zero -- which a validation failure always does -- so a matched grep is thrown away and the test dies reporting the opposite of what happened. This cost a CI round trip on `test_bake_unresolved_image`. Add a lint that flags any `| grep` in the suite whose left side is not `echo`/`printf`, and require the captured-variable form.
**Where:** `tests/drift-gate-negatives.sh, automation/98-drift-checks.sh`
**Done When:** the lint is a registered gate with a negative test, and every piped grep in the suite either reads from `echo`/`printf` or is converted to capture-then-match.
**Verify:** `bash automation/98-drift-checks.sh check_pipefail_grep_lint` goes red when a `somecmd | grep -q x` is added to tests/drift-gate-negatives.sh, and green when removed.
**Do NOT:** exempt the suite's own `echo ... | grep` lines by name. Match on the LEFT side being echo/printf, so a new piped grep is caught automatically.
**Why:** the harness silently inverting a result is indistinguishable from a broken detector, and sends the reader to the wrong file.
**Dep:** none

## AGY-1611 -- Audit every hash-keyed round trip for silent row loss  (WS-EVIDENCE | P0 | M) **[DONE]**
**Goal:** No generator drops data by deduplicating on a content hash.
**What+How:** `mios-manual`'s `_read_ledger` keyed rows by `sha12`; identical comment text is common, so reading `manual-corpus.tsv` into that dict and writing `list(rows.values())` back turned 9669 rows into 8564. A harvest followed by a commit without `sync-generated` would have deleted 1105 census rows. Fixed by a `Ledger` type that keeps every row. Now sweep: any place a generated artifact is read into a dict keyed by hash, name, or path-stem and written back. Check `tools/generate-names-registry.py`, `automation/lib/mios_var_closure.py`, `tools/generate-pod-quadlets.py`, `tools/generate-bake-plan.py` and the manifest writers.
**Where:** `usr/libexec/mios/mios-manual, tools/generate-*.py, automation/lib/*.py`
**Done When:** each round trip either preserves row count or asserts it, and a test writes a fixture containing duplicates and proves nothing is lost.
**Verify:** a fixture containing duplicate rows survives a read-write round trip: row count in equals row count out, asserted by a test.
**Do NOT:** eyeball the row count once. mios-manual's ledger lost 1105 of 9669 rows silently and the file still parsed -- only an asserted count catches it.
**Why:** the loss is invisible: the file still parses, the gate still passes, and a ninth of the evidence is gone.
**Dep:** none

## AGY-1612 -- Prove every generator is host-independent  (WS-PORTABILITY | P0 | L) **[DONE]**
**Goal:** A generated artifact must not depend on which machine regenerated it.
**What+How:** `usr/share/doc/mios/reference/tool-index.md` was reported out of sync by CI while clean locally, through several rounds of guessing. `fnmatch.fnmatch` normalises case on Windows, so `usr/libexec/mios/mios-*` matched `MiOS-Mon.py` there and not on Linux, and each side reverted the other. Seven call sites are now `fnmatchcase`. Sweep the remaining host-dependent inputs: `os.walk` ordering, `sorted()` on paths without an explicit key, locale-dependent case folding, `os.sep` leaking into content, line endings written by generators, and any use of the filesystem where `git ls-files` is meant. Then add a gate that regenerates under a forced-case-insensitive and a forced-case-sensitive configuration and requires identical bytes.
**Where:** `usr/libexec/mios/mios-manual, usr/libexec/mios/mios-daemon, usr/libexec/mios/mios-version-lint, tools/generate-names-registry.py, automation/lib/mios_var_closure.py, tools/*.py`
**Done When:** the differential gate is registered with a negative test, and every generator produces byte-identical output under both configurations.
**Verify:** `bash automation/98-drift-checks.sh check_generator_host_parity` goes red when any `fnmatchcase` is reverted to `fnmatch`, and every generator emits byte-identical output under forced case-sensitive and case-insensitive git config.
**Do NOT:** fix only the fnmatch instances. os.walk ordering, bare sorted(), locale case-folding, os.sep leaking into content and filesystem-vs-git-tracked are the same class.
**Why:** an artifact whose content depends on the host cannot be gated at all -- CI and the contributor take turns reverting each other and neither is wrong.
**Dep:** none

## AGY-1613 -- Make CI failures name the cause, not the file  (WS-EVIDENCE | P1 | M) **[DONE]**
**Goal:** A red build should not require a reproduction round trip.
**What+How:** The sync step printed `M usr/share/doc/mios/reference/tool-index.md` and nothing else, which is not actionable when the generator behaves differently in CI; it now prints `git diff --stat` and 200 lines of diff. Apply the same standard everywhere a gate reports a name: `check_bake_plan` said "core image is not referenced by any Quadlet" while pointing three lines of SSOT away from the actual cause (an Image= that resolved nowhere and was skipped by a bare `continue`). Audit every `_violation` message for whether it names the thing a reader must change. Both arms of `test_bake_tokens` used the identical message `die "Generate-bake-plan.py"`, so the log could not say which fired.
**Where:** `.github/workflows/mios-ci.yml, automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, tools/generate-*.py`
**Done When:** no two `die` calls in the suite share a message, and every `_violation` names a file AND the change required.
**Verify:** no two `die` calls in tests/drift-gate-negatives.sh share a message: `grep -oE 'die "[^"]+"' tests/drift-gate-negatives.sh | sort | uniq -d` prints nothing.
**Do NOT:** improve only the message that bit you last. `die "Generate-bake-plan.py"` appeared twice and the log could not say which arm fired.
**Why:** a message that names the symptom sends the reader to the wrong file; that is worse than silence because it looks like information.
**Dep:** none

## AGY-1614 -- Delete or fold tools/check-comment-ratchet.py  (WS-MANUAL | P2 | S) **[DONE]**
**Goal:** One measurement, in the place that runs it.
**What+How:** Its header claimed "Drift check 155 check_comment_ratchet" while no gate invoked it; the hint now says it is a developer report. Its unique measurements -- stale refs and undocumented components -- have moved into `check_docs_ratchet`, which the gate runs. Decide: fold `max_undocumented_components` in too and delete the tool with its test, or keep it as an explicitly-labelled local report. Do not leave a third path that computes the same numbers differently -- its ceilings defaulted to 999999, so a renamed SSOT key would have made it silently unfailable.
**Where:** `tools/check-comment-ratchet.py, tools/test_check-comment-ratchet.py, automation/98-drift-checks.sh (check_docs_ratchet)`
**Done When:** exactly one code path computes each comment metric, and no ceiling anywhere defaults to a value that cannot fail.
**Verify:** exactly one code path computes each comment metric: `grep -rl 'max_unmigrated_narrative' --include='*.py' --include='*.sh' .` names one enforcement site.
**Do NOT:** leave a second implementation as a 'developer report'. Two implementations of one measurement disagree, and the unrun one rots.
**Why:** two implementations of one measurement disagree eventually, and the one nobody runs is the one that rots.
**Dep:** AGY-1608

## AGY-1615 -- Drive the narrative ratchet down by harvesting, not by raising  (WS-MANUAL | P1 | L) **[DONE]**
**Goal:** 692 unmigrated narrative blocks becomes materially fewer, with nothing lost.
**What+How:** Use the harvest -> landing -> prune flow, which now works end to end: `mios-manual harvest --path X --to Y --apply`, confirm with `mios-manual landing`, then `mios-manual prune --path X --block SHA --only-landed --apply`. Work in batches by subsystem so each commit is reviewable. Harvest refuses generated destinations now (a header saying GENERATED or DO NOT EDIT), because harvesting into `usr/share/doc/mios/reference/metal-vs-hosted.md` put prose in a file its generator overwrites and broke that document's round-trip test. Tighten `[docs].max_unmigrated_narrative` after each batch; it is monotone, so it can only go down.
**Where:** `usr/libexec/mios/mios-manual, usr/share/doc/mios/manual/, usr/share/mios/mios.toml [docs]`
**Done When:** the count is below 400, `mios-manual landing` reports every pruned block still documented, and no harvested prose landed in a generated file.
**Verify:** `bash automation/98-drift-checks.sh check_docs_ratchet` shows narrative below 400, AND `python3 usr/libexec/mios/mios-manual landing` reports every pruned block still documented.
**Do NOT:** prune without harvesting. `--only-landed` exists so a block is deleted ONLY after its prose is provably in a doc.
**Why:** the comments hold the reasoning; the manual is where a reader looks for it. Migration is the point, deletion is not.
**Dep:** none

## AGY-1616 -- Give the manual a landing page a person actually reads  (WS-MANUAL | P1 | M) **[DONE]**
**Goal:** The harvested corpus becomes a document, not a pile.
**What+How:** `usr/share/doc/mios/manual/_harvest/` accumulates one file per source file, which is a staging area and not a manual. Use `mios-manual distill` to organise harvested passages by subsystem, and `mios-manual landing` to prove nothing is lost in the move. The corpus ledger keys by content hash, so passages can be reorganised freely as long as anchors survive.
**Where:** `usr/libexec/mios/mios-manual (distill, landing), usr/share/doc/mios/manual/`
**Done When:** the manual has a table of contents derived from the SSOT, every `_harvest` passage is either placed or explicitly deferred, and `landing` stays green.
**Verify:** `ls usr/share/doc/mios/manual/_harvest/*.md | wc -l` is 0 or explicitly registered, and `mios-manual landing` stays green across the move.
**Do NOT:** harvest into a generated document. metal-vs-hosted.md is rendered by its own generator and the added section broke its round-trip test.
**Why:** the deliverable is a repository small enough to read; a staging directory of 300 files is not that.
**Dep:** AGY-1615

## AGY-1617 -- Resolve the MiOS-Mon.py naming exclusion  (WS-MANUAL | P2 | S) **[DONE]**
**Goal:** A shipped tool is either in the index or deliberately out of it.
**What+How:** `usr/libexec/mios/MiOS-Mon.py` does not match the index glob `usr/libexec/mios/mios-*` under case-exact matching, so it is absent from `tool-index.md` while being a real shipped surface. Decide: rename it to `mios-mon.py` for consistency with the other 209 tools (and update every reference), or add a second index marker that covers it. Do not "fix" it by making the glob case-insensitive again -- that is what made the artifact host-dependent.
**Where:** `usr/libexec/mios/MiOS-Mon.py, usr/share/doc/mios/reference/tool-index.md, usr/libexec/mios/mios-manual`
**Done When:** the tool appears in the index under a deliberate rule, and `check_manual_generated` is byte-stable on Linux and Windows.
**Verify:** `bash automation/98-drift-checks.sh check_manual_generated` passes on Linux AND Windows with byte-identical tool-index.md.
**Do NOT:** make the index glob case-insensitive again. That is what made the artifact depend on which machine regenerated it.
**Why:** a shipped surface missing from the generated index makes the index a partial truth, which is the hardest kind to notice.
**Dep:** AGY-1612

## AGY-1618 -- Drive mios-unit-gen --check to zero drift  (WS-EVIDENCE | P0 | L)
**Goal:** Every systemd unit in the tree is what the SSOT renders.
**What+How:** `--check` used to render units in memory and print PASS without comparing; repaired, it reported 66 rendered / 66 drifted / 0 matching. Work the remainder down unit by unit: for each, decide whether the SSOT or the shipped file is right, fix the losing side, and re-run. Do not regenerate wholesale -- some shipped units carry hand-edits that encode real behaviour and must move into `[units]` first.
**Where:** `tools/native/mios-unit-gen/, usr/lib/systemd/system/, usr/share/mios/mios.toml [units]`
**Done When:** `mios-unit-gen --check` reports zero drifted units, a negative test proves it fails when one unit is edited, and the gate runs in CI.
**Verify:** `cargo run -p mios-unit-gen -- --check` reports zero drifted units, and editing one shipped unit makes it non-zero.
**Do NOT:** regenerate wholesale. Some shipped units carry hand-edits that encode real behaviour; those must move into [units] first.
**Why:** a projection that does not match its output is not a projection; it is two hand-maintained copies with extra steps.
**Dep:** none

## AGY-1619 -- Audit every check for vacuous pass conditions  (WS-EVIDENCE | P0 | L)
**Goal:** No gate passes because it found nothing to look at.
**What+How:** Known instances: `check_desktop_launchers` passed because `[desktop.launchers]` was absent, leaving 9 shipped launchers ungoverned; `check_no_inert_ssot_tables` and `check_doc_refs_resolve` wrote violations to stderr while the wrapper read stdout, so `_violation` was never called; `check_resolver_differential_parity` skipped silently when a tool was missing; bake-smoke prints OK on empty lists. Systematically: for each registered check, construct the empty-input case and confirm it FAILS rather than passes. Where a skip is legitimate, it must be loud and gated by `MIOS_DRIFT_REQUIRE_TOOLS`.
**Where:** `automation/98-drift-checks.sh, tests/drift-gate-negatives.sh`
**Done When:** every check has a documented empty-input behaviour, and `check_negative_coverage` requires a negative test for each.
**Verify:** for every registered check, the empty-input case is constructed and shown to FAIL, recorded in tests/drift-gate-negatives.sh.
**Do NOT:** accept a check that passes when it finds nothing to look at. That is the repository's most expensive recurring defect.
**Why:** this is the repository's most expensive recurring defect: a check that cannot fail also stops anyone looking.
**Dep:** AGY-1609

## AGY-1620 -- Make the drift gate runnable and green on Linux from a clean tree  (WS-EVIDENCE | P1 | M)
**Goal:** A contributor can reproduce CI locally without guessing.
**What+How:** Local Windows runs cannot validate checks that shell out to Python with MSYS paths -- `automation/**/*.sh` globbed zero files under `/tmp/...`, so `check_curl_retry` reported a false failure that had nothing to do with the change under test. Document and script the clean-worktree Linux procedure, including the environment variables the negatives suite forwards (`MIOS_DRIFT_ROOT`, `MIOS_DRIFT_REQUIRE_TOOLS`) and the fact that a stale installed MiOS on the dev VM fakes SSOT-projection drift.
**Where:** `tests/drift-gate-negatives.sh, automation/98-drift-checks.sh, docs, .devcontainer/`
**Done When:** one documented command runs the full gate and negatives on a clean checkout and matches CI's result.
**Verify:** one documented command runs the full gate plus negatives on a clean Linux checkout and matches CI's result exactly.
**Do NOT:** trust a Windows run. `automation/**/*.sh` globs zero files under an MSYS path, so check_curl_retry reports a failure that has nothing to do with the change.
**Why:** every unreproducible failure is paid for in CI round trips, and the log alone is rarely enough.
**Dep:** none

## AGY-1621 -- Sync the bootstrap repo at build time, not by hand  (WS-BOOTSTRAP | P0 | M)
**Goal:** `mios.git` and `mios-bootstrap.git` cannot drift on shared surfaces.
**What+How:** `tools/sync-bootstrap.py` exists; wire it into the main CI pipeline so a successful build pushes the shared files to the bootstrap repo. Law 15 requires both repos be verified before any change to a shared surface. Shared today: `installation/mios-common.{ps1,sh}`, port definitions, theme surfaces. Note `installation/mios-common.sh` is deliberately NOT synced -- the bootstrap copy is richer and its comments were never harvested; resolve that asymmetry rather than papering over it.
**Where:** `tools/sync-bootstrap.py, .github/workflows/mios-ci.yml, installation/`
**Done When:** a build publishes the synced files, a gate fails when a shared surface differs between repos, and the `mios-common.sh` asymmetry is resolved in one direction with the reason recorded.
**Verify:** a build publishes the synced files, and a gate fails when a shared surface differs between mios.git and mios-bootstrap.git.
**Do NOT:** skip silently when the bootstrap repo is absent. A skip that reports success is why these two repos drifted in the first place.
**Why:** Law 15 exists because these two repos have drifted before, and the drift is invisible from either side alone.
**Dep:** none

## AGY-1622 -- Finish the bare-metal install leg  (WS-DEPLOY | P0 | L)
**Goal:** MiOS-Cat actually installs MiOS.
**What+How:** The Ventoy/MediCat USB machinery works but installs plain Fedora, not bootc: `tools/install.sh` is absent and the correct bootc-image-builder installer is disconnected and unbuilt. Use `bootc install --transport oci` for the offline path. This is the least-complete area of the roadmap at roughly 15-25%.
**Where:** `cat/, tools/install.sh, usr/share/mios/sys/Containerfile*, automation/build/`
**Done When:** a USB built by the documented flow installs a bootc MiOS system that boots to the expected archetype, verified on real hardware or a VM with the same firmware path.
**Verify:** a USB built by the documented flow installs a bootc MiOS system that boots to the expected archetype, on real hardware or a VM with the same firmware path.
**Do NOT:** call it done when the USB boots. It currently installs plain Fedora -- the test is that the installed system is bootc MiOS.
**Why:** every other plane assumes a deployed system; without this the project runs only where someone has already hand-built it.
**Dep:** none

## AGY-1623 -- Prove the deploy plane on something that is not MiOS-DEV  (WS-DEPLOY | P0 | L)
**Goal:** Move "runs on a VM" to "runs where it claims to run".
**What+How:** Bare metal is untried, and blade/mesh/vfio work is design rather than observation. Pick the smallest real target, install from the artifact the build publishes, and record what actually happened -- including what failed. Do not extend the design further until one non-VM install is observed.
**Where:** `docs/agy/, ROADMAP.md, cat/`
**Done When:** one non-VM install is recorded with its failures, and the roadmap's honest-state table is updated from observation rather than intent.
**Verify:** one non-VM install is recorded, including what failed, and ROADMAP.md's state table is updated from that observation.
**Do NOT:** extend the blade/mesh design further first. Every unobserved deployment target is a claim with no evidence behind it.
**Why:** the thesis is a claim about a deployable system. Every unobserved deployment target is a claim with no evidence behind it.
**Dep:** AGY-1622

## AGY-1624 -- Implement blade workload mobility per ADR-0017  (WS-BLADE | P1 | L)
**Goal:** The mobility model in ADR-0017 exists in code, not only in a decision record.
**What+How:** k3s owns containers, Pacemaker owns VMs; GPU-gated services degrade to a CPU lane on non-GPU blades; failover tries local, then localhost, then cluster; anti-flap dwell is asymmetric; only blades diverge, with per-class merge rules (`union-by-hash`, `append-ordered`, `last-writer-wins`, and `conflict-is-error` for config_kv). `[blade.placement]`, `[blade.collapse]`, `[blade.discovery]` and `[blade.reconcile]` are declared in the SSOT; `usr/libexec/mios/reconcile-blade` and its test exist. Wire the reconciler to real cluster state.
**Where:** `usr/share/doc/mios/adr/0017-blade-workload-mobility.md, usr/libexec/mios/reconcile-blade, usr/share/mios/mios.toml [blade.*]`
**Done When:** a two-node fleet demonstrates a container failing over under k3s and a VM under Pacemaker, with the dwell behaviour observed and the merge rules exercised including a `conflict-is-error` case.
**Verify:** a two-node fleet shows a container failing over under k3s and a VM under Pacemaker, with the dwell behaviour observed and a `conflict-is-error` case exercised.
**Do NOT:** implement the happy path only. The merge classes are only tested when a case would produce the WRONG result under a neighbouring rule.
**Why:** `[blade.*]` currently declares behaviour nothing implements, which `check_no_inert_ssot_tables` should be catching.
**Dep:** AGY-1623

## AGY-1625 -- Verify no SSOT table ships inert  (WS-EVIDENCE | P1 | M) **[DONE]**
**Goal:** Every table in mios.toml has a consumer.
**What+How:** `check_no_inert_ssot_tables` was silently broken -- violations went to stderr while the wrapper read stdout, so it never fired. With it repaired, audit the result: `[accounts].db_backed` ships inert by design and is documented; `[blade.*]` is partly inert pending AGY-1624. Each inert table must be either wired, removed, or listed in an explicit register with the reason and a ratchet that only shrinks.
**Where:** `automation/98-drift-checks.sh (check_no_inert_ssot_tables), usr/share/mios/mios.toml`
**Done When:** the inert register is explicit and shrinking, and the check fails when a new unconsumed table appears.
**Verify:** `bash automation/98-drift-checks.sh check_no_inert_ssot_tables` goes red when a new unconsumed table is added, and the inert register only shrinks.
**Do NOT:** trust the check because it passes. It wrote violations to stderr while the wrapper read stdout, so `_violation` was never called.
**Why:** the SSOT is the whole claim. A table nothing reads is a promise the system does not keep.
**Dep:** none

## AGY-1626 -- Invert the PowerShell-to-Rust ratio for anything that is a program  (WS-LANG | P1 | L)
**Goal:** Law 14 in fact, not only in text.
**What+How:** 33,168 lines of PowerShell against 9,011 of Rust while Law 14 makes Rust the native tier. Classify every PowerShell file as glue (invokes things, stays) or program (parses, decides, transforms -- moves). Port programs to Rust with tests, keeping the shim name stable so callers do not change. Do this in small, independently reviewable steps; each port must land with a test that fails against the old behaviour if the port is wrong.
**Where:** `usr/share/mios/windows/, tools/native/, src/mios-rs/`
**Done When:** the ratio has inverted for the program tier, every port has tests, and `[legibility]` PowerShell line count is materially lower.
**Verify:** `[legibility].max_ps_lines` is materially lower and every ported program has a test that fails against the pre-port behaviour.
**Do NOT:** port glue. Classify each file as glue (invokes things, stays) or program (parses, decides, transforms -- moves).
**Why:** minimal static binaries and few shell scripts is the stated architecture; the measured tree currently says the opposite.
**Dep:** none

## AGY-1627 -- Fold the 282 libexec verbs and 72 automation phases  (WS-THESIS | P1 | L)
**Goal:** Fewer files, each doing more, all readable.
**What+How:** Heavy duplication across `usr/libexec/mios/` verbs and `automation/` phases. Identify clusters that share a resolver, an argument shape, or a projection target and fold them into one component with subcommands. Do not blind-delete: several verbs previously judged "legacy" turned out to be load-bearing, and doc bundles and entry points reference files that look dead.
**Where:** `usr/libexec/mios/, automation/, usr/share/mios/mios.toml [legibility]`
**Done When:** verb and phase counts are materially lower with no capability lost, each fold is covered by a test, and the legibility floors are tightened to the new measurement.
**Verify:** verb and phase counts in `[legibility]` are materially lower with no capability lost, each fold covered by a test.
**Do NOT:** blind-delete anything called legacy. Several such verbs turned out to be load-bearing, and doc bundles reference files that look dead.
**Why:** the deliverable is a repository a person can read; 282 verbs is not readable regardless of how good each one is.
**Dep:** none

## AGY-1628 -- Get the pushed repo state under 100 MB  (WS-THESIS | P1 | M)
**Goal:** The combined pushed state is under 100 MB.
**What+How:** Roughly 202 MB today, most of it two vendored assets. Decide per asset: fetch at build time with a checksum recorded in the SBOM, move to a release artifact, or drop. Per ADR-0003, digests and checksums are SBOM data resolved at build, never hand-pinned in the SSOT, so a build-time fetch is consistent with the architecture.
**Where:** `usr/share/mios/vllm/, vendored assets, automation/build/, usr/share/mios/mios.toml [legibility]`
**Done When:** the pushed state is under 100 MB, every removed asset is reproducibly fetchable at build with its checksum recorded, and the size floor is tightened.
**Verify:** the pushed state is under 100 MB and every removed asset is reproducibly fetchable at build with its checksum in the SBOM.
**Do NOT:** hand-pin a digest in the SSOT. ADR-0003: digests are SBOM data resolved at build.
**Why:** size is a correctness property here: the repository is the artifact being read.
**Dep:** none

## AGY-1629 -- Purge stale references from docs and credits  (WS-MANUAL | P1 | M)
**Goal:** Reading materials describe the system that exists.
**What+How:** Credits and licences still name ollama, which was removed entirely when the AI plane became OpenAI `/v1`-only. Legitimate surviving "ollama" tokens exist in enforcement, cleanup, upstream-compat and history and must NOT be re-flagged. Sweep READMEs, docs and credits for anything describing a removed subsystem, and add a gate that fails when a doc names a component the SSOT no longer declares.
**Where:** `README.md, docs/, usr/share/doc/mios/, credits and licence files`
**Done When:** no reading material describes a removed subsystem, the legitimate tokens are allowlisted with reasons, and the gate has a negative test.
**Verify:** no reading material names a removed subsystem, and a gate fails when a doc names a component the SSOT no longer declares.
**Do NOT:** mass-delete every 'ollama' token. Enforcement, cleanup, upstream-compat and history references are legitimate and must be allowlisted with reasons.
**Why:** documentation that names a removed component teaches a reader something false on their first contact with the project.
**Dep:** none

## AGY-1630 -- Make the ADR set navigable and prove its index  (WS-MANUAL | P2 | S) **[DONE]**
**Goal:** Seventeen ADRs a reader can move through.
**What+How:** ADRs are immutable once accepted (`usr/share/doc/mios/adr/README.md:23`) -- supersede, never rewrite. ADR-0017 extends ADR-0016 for exactly that reason. Ensure `ADR.md` is generated from the ADR directory, that supersession chains are explicit and rendered, and that `check_adr_index` fails when an ADR is added without an index entry.
**Where:** `ADR.md, usr/share/doc/mios/adr/, automation/98-drift-checks.sh (check_adr_index)`
**Done When:** the index is generated, supersession is visible, and a new ADR without an entry fails the gate.
**Verify:** `bash automation/98-drift-checks.sh check_adr_index` goes red when an ADR is added without an index entry, and supersession chains render.
**Do NOT:** edit an accepted ADR. usr/share/doc/mios/adr/README.md:23 -- supersede, never rewrite.
**Why:** decision records are the reasoning trail; an unnavigable trail is not one.
**Dep:** none

## AGY-1631 -- Close the zero-hardcodes campaign on the gate-red literals  (WS-SSOT | P1 | L) **[DONE]**
**Goal:** All ports and values float from the SSOT.
**What+How:** Only `usr/lib/mios` and systemd literals are gate-red; the rest is hygiene. Fix the `mios-hardcode-lint` unanchored-allowlist bug that spuriously exempts `:8080`, `:8033` and others. Do batches 1A/1B mechanically, then 1C/1D behind the gate, then generate from SSOT, adding the new `[network]` and `[ports.*_internal]` keys. Never float the false friends: `MIOS_PORT_GUACAMOLE` does not exist -- it is `MIOS_PORT_GUACAMOLE_WEB`. Note that ports are ALLOCATED from `[ports.categories]` by base plus stride, not hand-assigned, so sed over old values is unsafe where old and new alias.
**Where:** `usr/lib/mios/, usr/lib/systemd/system/, tools/render-ports.py, usr/share/mios/mios.toml [ports.*]`
**Done When:** the hardcode lint has no allowlist entries that are unanchored, the gate-red literals are zero, and a negative test proves a reintroduced literal fails.
**Verify:** `mios-hardcode-lint` has no unanchored allowlist entries, gate-red literals are zero, and a reintroduced literal fails a negative test.
**Do NOT:** sed old port values to new. Ports are ALLOCATED from [ports.categories] by base+stride and old/new values alias.
**Why:** a value defined twice is a value that will disagree, and the SSOT stops being single.
**Dep:** none

## AGY-1632 -- Give the quadlet containers real AI-hints  (WS-MANUAL | P2 | S) **[DONE]**
**Goal:** Generated quadlet headers describe the service, not only the generator.
**What+How:** The four webtools and llm-heavy-alt containers carry hints that are mostly boilerplate about regeneration, close to the 260-character prose cap. Since they are generated by `tools/generate-pod-quadlets.py` from `[containers.*]`, improve the TEMPLATE: emit a short purpose line derived from the SSOT's own description field, and keep the DO-NOT-EDIT notice on a separate non-prose line so it does not consume the hint budget.
**Where:** `tools/generate-pod-quadlets.py, usr/share/containers/systemd/*.container, usr/share/mios/mios.toml [containers.*]`
**Done When:** every generated quadlet's hint describes its service in under the cap, and the regeneration notice is outside the prose measurement.
**Verify:** every generated quadlet's AI-hint prose is under the cap and describes its service, with the DO-NOT-EDIT notice outside the prose measurement.
**Do NOT:** wrap the hint across lines. The cap counts prose continuation, so wrapping moves the metric without changing what a reader sees.
**Why:** the hint is what the index and the manual read; boilerplate there produces an index that says nothing.
**Dep:** none

## AGY-1633 -- Establish an undocumented-components ceiling that means something  (WS-MANUAL | P2 | M) **[DONE]**
**Goal:** `max_undocumented_components` becomes a measured, enforced ratchet.
**What+How:** It exists in `[docs]` but its only consumer was the unwired report, whose ceilings defaulted to 999999. Define precisely what a component is (a libexec verb, an automation phase, a tools entry point, a Rust binary), measure how many lack a doc, set the ceiling at the measurement and enforce it in `check_docs_ratchet`.
**Where:** `usr/share/mios/mios.toml [docs], automation/98-drift-checks.sh (check_docs_ratchet), usr/lib/mios/mios_comments.py`
**Done When:** the definition is written down, the ceiling equals the measurement, a negative test breaches it, and it only decreases.
**Verify:** the component definition is written down, the ceiling equals the measurement, and a negative test breaches it.
**Do NOT:** leave the ceiling defaulting to 999999. That is a check that cannot fail dressed as a ratchet.
**Why:** a ceiling with no measurement behind it is decoration, and this one defaulted to a value nothing could exceed.
**Dep:** AGY-1608

## AGY-1634 -- Record the honest state table from measurement  (WS-THESIS | P1 | S)
**Goal:** ROADMAP.md's state table is generated, not asserted.
**What+How:** The roadmap carries a measured honest-state table. Generate its numbers from the gates that produce them -- legibility floors, ratchet counts, unit drift, deploy-plane completeness -- so it cannot quietly go stale. Where a number has no gate behind it, say so in the table rather than estimating.
**Where:** `ROADMAP.md, automation/98-drift-checks.sh, usr/libexec/mios/mios-manual (render)`
**Done When:** every number in the table is either derived from a gate or explicitly marked as unmeasured, and the table regenerates in `sync-generated`.
**Verify:** every number in ROADMAP.md's state table is derived from a gate or explicitly marked unmeasured, and the table regenerates in sync-generated.
**Do NOT:** hand-maintain the numbers. They drift exactly like hand-maintained config.
**Why:** the roadmap is the project's own account of itself; hand-maintained numbers in it drift exactly like hand-maintained config.
**Dep:** none

## AGY-1635 -- Cover the reconcile-blade merge classes with adversarial tests  (WS-BLADE | P1 | M)
**Goal:** The per-class merge rules are proven, including the failure class.
**What+How:** ADR-0017 defines `union-by-hash`, `append-ordered`, `last-writer-wins` and `conflict-is-error` for config_kv. `usr/libexec/mios/test_reconcile-blade.py` exists; extend it so each rule has a case that would produce the WRONG result under a neighbouring rule, and so `conflict-is-error` genuinely raises rather than resolving. Include clock-skew and simultaneous-write cases for `last-writer-wins`.
**Where:** `usr/libexec/mios/reconcile-blade, usr/libexec/mios/test_reconcile-blade.py`
**Done When:** each merge class has a discriminating test, `conflict-is-error` is proven to raise, and the tests fail if two classes are swapped.
**Verify:** each merge class has a test that would produce the wrong result under a neighbouring rule, and `conflict-is-error` is proven to raise.
**Do NOT:** test only on agreeing inputs. Merge rules tested on agreement are untested.
**Why:** merge rules that are only tested on agreeing inputs are untested.
**Dep:** AGY-1624

## AGY-1636 -- Make the firstboot tier's contents provable  (WS-BUILD | P1 | M) **[DONE]**
**Goal:** The firstboot tier holds exactly what the bake cannot.
**What+How:** The tier exists so the vLLM and SGLang images (~47 GB) leave the bake and it fits a standard runner. `check_firstboot_tier` and `generate-bake-plan.py` validate token matching, but nothing proves the tier's membership is minimal -- an image could sit there without needing to. Add a check that each firstboot entry exceeds a size threshold declared in the SSOT, or is justified in an explicit register.
**Where:** `usr/lib/mios/bake/plan.d/firstboot.list, tools/generate-bake-plan.py, usr/share/mios/mios.toml [build.bake]`
**Done When:** every firstboot entry is justified by a declared rule, and a negative test proves an unjustified entry fails.
**Verify:** every firstboot.list entry is justified by a declared rule, and an unjustified entry fails a negative test.
**Do NOT:** let the tier become where things are put to avoid thinking about them. It exists for images the bake cannot hold.
**Why:** the tier is a capacity workaround; without a rule it becomes a place things are put to avoid thinking about them.
**Dep:** none

## AGY-1637 -- Prove the bake plan against a real registry resolution  (WS-BUILD | P1 | M)
**Goal:** The plan's images exist and are pullable.
**What+How:** `generate-bake-plan.py --check` now validates that every core image is referenced by a quadlet and that every Image= resolves against the SSOT. It does not validate that the resolved reference EXISTS. Add an opt-in gate (network-gated, skipped loudly without credentials) that resolves each reference to a digest and records it in the SBOM, per ADR-0003 -- resolved at build, never hand-pinned.
**Where:** `tools/generate-bake-plan.py, automation/build/, usr/share/mios/mios.toml [build.bake]`
**Done When:** a build resolves every image to a digest, records them in the SBOM, and fails loudly rather than silently when the network is absent under `MIOS_DRIFT_REQUIRE_TOOLS`.
**Verify:** a build resolves every core image to a digest and records it in the SBOM, failing loudly without network under MIOS_DRIFT_REQUIRE_TOOLS=1.
**Do NOT:** skip silently when the network is absent. A silent skip reports the same green as a pass.
**Why:** a plan listing an image that cannot be pulled fails at bake time, which is the most expensive place to find out.
**Dep:** none

## AGY-1638 -- Audit the drift gate's runtime and parallelise the census  (WS-EVIDENCE | P2 | M)
**Goal:** The gate stays fast enough that people run it.
**What+How:** `check_docs_ratchet` now builds a `RefIndex` by walking the tree and lexes every tracked source file; several other checks re-lex the same files independently. Build the census once per invocation and share it. Measure before and after -- do not assume.
**Where:** `automation/98-drift-checks.sh, usr/lib/mios/mios_comments.py, usr/libexec/mios/mios-manual`
**Done When:** the full gate's wall-clock is measured, the census is computed once, and the improvement is recorded.
**Verify:** the full gate's wall-clock is measured before and after, the census is computed once, and the improvement is recorded.
**Do NOT:** assume it is faster. Measure both sides.
**Why:** a gate too slow to run locally becomes a gate that only CI runs, which pushes every failure into a round trip.
**Dep:** none

## AGY-1639 -- Register every SSOT-declared launcher and prove the desktop surface  (WS-SSOT | P1 | S) **[DONE]**
**Goal:** `[desktop.launchers]` governs the shipped `.desktop` files.
**What+How:** `check_desktop_launchers` passed vacuously because the table was absent, leaving 9 shipped launchers ungoverned; the table now exists with entries derived from the shipped files. Verify each entry matches its file, add the missing fields, and make the negative test mutate a RENDERED FIELD rather than adding a stray `.desktop` -- adding a file proves nothing about projection.
**Where:** `usr/share/mios/mios.toml [desktop.launchers], tools/render-desktop.py, usr/share/applications/`
**Done When:** every shipped launcher is declared, a mutated rendered field fails the gate, and `tools/test_render-desktop.py` covers the empty-table case.
**Verify:** mutating a RENDERED field in a .desktop file makes `check_desktop_launchers` red; adding a stray .desktop file is not the test.
**Do NOT:** add a file as the probe. That proves nothing about projection -- the check passed vacuously for exactly this reason.
**Why:** the table was added to close a vacuous gate; if its contents are not verified the gate is still vacuous, just quieter.
**Dep:** none

## AGY-1640 -- Make `mios build` reproducible from a clean clone  (WS-BUILD | P0 | L)
**Goal:** Someone who clones the repo can build the image.
**What+How:** The Day-0 build reached green once. Re-establish it from a clean clone with no local state: document every prerequisite, make missing ones fail loudly with the install command, and record the wall-clock and disk cost. Note that `/` is now a self-updating git work tree after Phase 1, which changes what a "clean" build means.
**Where:** `automation/build/, build-mios.sh, docs, .devcontainer/`
**Done When:** a clean clone builds to a bootc-lint-green image with one documented command, and the prerequisites fail loudly rather than silently degrading.
**Verify:** a clean clone builds to a bootc-lint-green image with one documented command, and a missing prerequisite fails loudly with the install command.
**Do NOT:** test on a machine that has already built it. `/` is now a self-updating git work tree, which changes what clean means.
**Why:** a build that only works on machines that have already built it is not reproducible, and reproducibility is half the thesis.
**Dep:** none

## AGY-1641 -- Unify the three MiOS-Cat launchers  (WS-DEPLOY | P1 | M)
**Goal:** One canonical launcher per platform, with stable names.
**What+How:** `.ps1` is canonical on Windows, `.bat` a name-stable thin shim, `.sh` a fresh Linux canonical. `Get-MiOS` via `irm | iex` orphans MiOS-Cat and needs a handoff. Pins must stay byte-identical across the three. The port is untestable from here, so land it incrementally with each step verified on the platform it targets.
**Where:** `cat/, installation/, Get-MiOS.ps1`
**Done When:** one canonical implementation per platform, shims that only delegate, byte-identical pins, and the `Get-MiOS` handoff closed.
**Verify:** one canonical implementation per platform, shims that only delegate, byte-identical pins, and the Get-MiOS handoff closed.
**Do NOT:** assume the .bat ships from the repo. installation/*.bat is gitignored (40a3566a) -- edits there do not reach CI or a user.
**Why:** three launchers that drift produce three different installs from the same instructions.
**Dep:** AGY-1621

## AGY-1642 -- Decide the fate of the two build-mios.sh copies  (WS-DEPLOY | P1 | M)
**Goal:** One build entry point.
**What+How:** The two `build-mios.sh` files diverge in functionality and there is a loop hazard between the shims. The chosen direction is a full fold of `mios-install.sh` and `cat/MiOS-Cat.sh` into one `build-mios.sh`. The "dead legacy" files are doc-bundled entry points and are NOT blind-deletable. This needs a bake-green baseline and a Linux environment first.
**Where:** `build-mios.sh, cat/MiOS-Cat.sh, installation/mios-install.sh`
**Done When:** one entry point exists, the loop hazard is gone, every doc reference resolves, and the fold is verified on Linux.
**Verify:** one build entry point exists, the loop hazard is gone, every doc reference resolves, and the fold is verified on Linux.
**Do NOT:** delete the 'dead legacy' files. They are doc-bundled entry points.
**Why:** two entry points that diverge means the instructions are right for one of them and nobody knows which.
**Dep:** AGY-1640

## AGY-1643 -- Prove the theme projection end to end on both platforms  (WS-SSOT | P2 | M)
**Goal:** Every theme surface derives from `[colors]`.
**What+How:** `mios-theme-render` plus drift-check 25 make this a projection, and `mios-sync-theme` is the one global refresh. Extend the proof to the dotfiles registry generalisation (ADR-0010): btop is the landed example on both platforms. Add a second surface end to end, and make the negative test mutate a rendered value rather than adding a file.
**Where:** `usr/libexec/mios/mios-theme-render, usr/share/mios/mios.toml [colors] [dotfiles.registry.*]`
**Done When:** a second surface is projected and drift-gated on both platforms, with a negative test that mutates rendered output.
**Verify:** a second theme surface is projected and drift-gated on both platforms, with a negative test that mutates rendered output.
**Do NOT:** count one worked example as a projection. One is a demo; two sharing a mechanism is a projection.
**Why:** one worked example is a demo; two with a shared mechanism is a projection.
**Dep:** none

## AGY-1644 -- Make agent-operability demonstrable, not asserted  (WS-THESIS | P0 | L)
**Goal:** Show an agent operating the system through the SSOT.
**What+How:** The thesis has four parts that stand or fall together: one file defines the OS, agents operate it natively, projection is what makes it agent-operable, and it is one reproducible artifact. The third is the least evidenced. Build a demonstration in which an agent changes `mios.toml`, the projection propagates, the gates confirm consistency, and the running system reflects it -- recorded as a transcript with the failures included.
**Where:** `usr/lib/mios/agent-pipe/, usr/libexec/mios/, docs/agy/, ROADMAP.md`
**Done When:** a recorded end-to-end run exists showing an agent making a change through the SSOT and the system converging, with what did not work stated plainly.
**Verify:** a recorded end-to-end run shows an agent changing mios.toml, the projection propagating, the gates confirming, and the system converging -- failures included.
**Do NOT:** argue it from architecture. The claim is about observation.
**Why:** the claim that projection is what makes the system agent-operable currently rests on architecture rather than observation.
**Dep:** AGY-1618

## AGY-1645 -- Ratchet the header-integrity guarantee  (WS-EVIDENCE | P1 | S)
**Goal:** Absorbed shebangs and build directives cannot recur.
**What+How:** A tagger commit absorbed shebangs and build directives into headers across 216 files, repaired against git history, with `check_header_integrity` added. Extend it to the cases the repair surfaced: BOM handling (an orphan-header heuristic reported 34 false positives, then 20, and was judged unreliable), files whose first line must stay first, and generated headers. Prove it fails on a file whose shebang has been absorbed.
**Where:** `automation/98-drift-checks.sh (check_header_integrity), usr/libexec/mios/mios-ai-tag`
**Done When:** the check covers shebangs, build directives and BOMs, with a negative test per case.
**Verify:** check_header_integrity covers shebangs, build directives and BOMs, with a negative test per case, and goes red on a file whose shebang was absorbed.
**Do NOT:** trust a heuristic that reported 34 false positives then 20. Verify against git history before editing any file.
**Why:** the tagger writes to every file in the tree; a defect there is a defect everywhere at once.
**Dep:** none

## AGY-1646 -- Give every tools/*.py a test that can fail  (WS-EVIDENCE | P1 | M)
**Goal:** The tools tier is covered by tests that discriminate.
**What+How:** Several `tools/test_*.py` exist and pass. For each, confirm it fails when the tool is broken -- mutate the tool, run the test, and require red. A test that passes against a deliberately broken tool is the same defect class as a gate that cannot fail, and this repository has a documented history of both.
**Where:** `tools/test_*.py, usr/lib/mios/test_*.py, usr/libexec/mios/test_*.py`
**Done When:** every test is proven to fail against at least one deliberate mutation of its subject, recorded in the test's docstring.
**Verify:** every tools/test_*.py is proven to fail against at least one deliberate mutation of its subject, recorded in the test's docstring.
**Do NOT:** count a green suite as evidence. A test that passes against a deliberately broken tool is the same defect as an unfailable gate.
**Why:** a green test suite is evidence only if the tests can go red.
**Dep:** AGY-1619

## AGY-1647 -- Reconcile TASKS.md with AGY-TASKS.md  (WS-PROCESS | P2 | M) **[DONE]**
**Goal:** One view of the work.
**What+How:** 1222 AGY tasks and a separate global TASKS.md whose 251 `T-` entries were rewritten alongside them. Make one the source and derive the other, or make the split explicit with a stated rule for which work goes where. `check_tasks_status_parity` exists -- ensure it actually compares the two rather than passing vacuously.
**Where:** `AGY-TASKS.md, TASKS.md, automation/98-drift-checks.sh (check_tasks_status_parity)`
**Done When:** the relationship is stated and gated, and the parity check fails when a task exists in one file and not the other under the stated rule.
**Verify:** `check_tasks_status_parity` goes red when a task exists in one file and not the other under the stated rule.
**Do NOT:** leave the relationship implicit. Both files are edited constantly and disagree the moment either is.
**Why:** two task lists disagree the moment either is edited, and both are edited constantly.
**Dep:** none

## AGY-1648 -- Record what the session's repairs imply for the AI-tagger  (WS-MANUAL | P2 | S)
**Goal:** The tagger stops producing headers the ratchet then has to fix.
**What+How:** Sixteen files breached the hint cap; twelve breached it only because `AI-related`/`AI-functions` were counted, and four carried genuine paragraphs. The cap now measures hint prose with wrapped continuation lines included, so wrapping cannot dodge it. Teach the tagger the same rule: emit hints under the cap, put detail in the docstring, and never wrap a hint to fit.
**Where:** `usr/libexec/mios/mios-ai-tag, usr/lib/mios/mios_comments.py`
**Done When:** the tagger cannot emit an over-cap hint, and a test proves it truncates into the docstring rather than wrapping.
**Verify:** the tagger cannot emit an over-cap hint, and a test proves it truncates into the docstring rather than wrapping.
**Do NOT:** let the tagger keep producing violations the ratchet then cleans up. That makes the ratchet a treadmill.
**Why:** a generator that produces violations the ratchet must clean up makes the ratchet a treadmill.
**Dep:** none

## AGY-1649 -- Close the loop on MIOS_AI_ENDPOINT and retired ports  (WS-SSOT | P2 | S) **[DONE]**
**Goal:** No surface points at a retired endpoint.
**What+How:** `MIOS_AI_ENDPOINT` is Hermes on `:8642`, not the retired `:8080`. The NO-HARDCODE lint allowlists canonical ports in `[security.nohc_allowlist]`, and certain `:8080` references are intentionally kept. Verify each remaining reference is one of the intentional ones, and record why in the allowlist rather than in a commit message.
**Where:** `usr/share/mios/mios.toml [security.nohc_allowlist], usr/lib/mios/, usr/libexec/mios/`
**Done When:** every retained `:8080` reference carries a recorded reason, and an unexplained one fails the lint.
**Verify:** every retained `:8080` reference carries a recorded reason in [security.nohc_allowlist], and an unexplained one fails the lint.
**Do NOT:** leave the allowlist without reasons. It becomes a list nobody dares to shrink.
**Why:** an allowlist without reasons becomes a list nobody dares to shrink.
**Dep:** AGY-1631

## AGY-1650 -- Write the reader's entry point  (WS-THESIS | P0 | M)
**Goal:** A person arriving at the repository can find the idea in one file.
**What+How:** The deliverable is the repository, small enough to read. Write the entry document that states the four-part thesis, points at the one file that defines the OS, shows one projection end to end, names what is observed versus designed, and links the ADRs in reading order. Derive its numbers from the gates so it cannot go stale.
**Where:** `README.md, ROADMAP.md, usr/share/doc/mios/manual/`
**Done When:** a reader unfamiliar with the project can follow it to a working mental model in one sitting, and every claim in it is either gate-backed or marked as design.
**Verify:** a reader unfamiliar with the project reaches a working mental model in one sitting, and every claim is gate-backed or marked as design.
**Do NOT:** describe intent as if it were observation. The gap between scope and observation is the point.
**Why:** the project is a proof of an idea; if the idea is not legible on arrival, the proof has no audience.
**Dep:** AGY-1616, AGY-1634

## AGY-1651 -- Prove the privileged-quadlet register is minimal  (WS-SECURITY | P0 | M) **[DONE]**
**Goal:** Ten root units, each justified.
**What+How:** `[security.privileged_quadlets].root` names the units allowed to run privileged. Nothing currently proves the list is MINIMAL -- a unit could sit there having outgrown the need. For each entry, determine the specific capability it requires, record it beside the entry, and demonstrate the unit fails without it. Add a ratchet so the list can only shrink, and a check that a newly privileged unit not in the register fails.
**Where:** `usr/share/mios/mios.toml [security.privileged_quadlets], usr/share/containers/systemd/, automation/98-drift-checks.sh`
**Done When:** every entry names the capability it needs, the count only decreases, and an unregistered privileged unit fails the gate with a negative test proving it.
**Verify:** every entry in [security.privileged_quadlets].root names the capability it needs, the count only decreases, and an unregistered privileged unit fails a negative test.
**Do NOT:** record the list without per-entry justification. A register with no reasons only ever grows.
**Why:** a privilege register with no justification per entry becomes a list that only grows, which is the opposite of what a register is for.
**Dep:** none

## AGY-1652 -- Make secret handling provable end to end  (WS-SECURITY | P0 | L)
**Goal:** No secret is ever committed, logged, or baked.
**What+How:** Add a gate that scans the tracked tree and the built image for credential shapes -- tokens, private keys, connection strings with passwords -- with an allowlist of test fixtures that must be explicitly marked. Extend it to the bake: a secret reaching a layer is not removable by a later layer. Cover the paths where secrets legitimately live at runtime (clevis, the credential store) and prove those are never in the image.
**Where:** `automation/98-drift-checks.sh, automation/build/, usr/share/mios/sys/Containerfile*, usr/lib/mios/`
**Done When:** the gate has a negative test that plants a credential shape and fails, image layers are scanned, and every legitimate fixture is allowlisted with a reason.
**Verify:** a planted credential shape in the tracked tree AND in a built image layer both make the gate red, and every legitimate fixture is allowlisted with a reason.
**Do NOT:** scan the tree only. A secret reaching a layer is not removable by a later layer.
**Why:** a credential in a committed layer is permanent regardless of what a later commit does.
**Dep:** none

## AGY-1653 -- Verify greenboot actually rolls back  (WS-RUNTIME | P0 | M)
**Goal:** The health-check path is observed, not assumed.
**What+How:** MiOS ships greenboot but the rollback has not been demonstrated. Write a health check that fails deliberately on a test deployment, boot it, and confirm the system rolls back to the previous image. Record what the failure looked like from the console, because that is what an operator will see.
**Where:** `usr/lib/mios/greenboot/, usr/lib/systemd/system/, docs/agy/`
**Done When:** a deliberate health-check failure is shown to roll back on a real boot, with the console output recorded.
**Verify:** a deliberate health-check failure is shown rolling back on a real boot, with the console output recorded.
**Do NOT:** assert it from the unit files. Rollback is the property that makes immutable updates safe; untested it is a hope.
**Why:** rollback is the safety property that makes an immutable image safe to update; untested, it is a hope.
**Dep:** AGY-1622

## AGY-1654 -- Wire the shipped-but-unused runtime components  (WS-RUNTIME | P1 | L)
**Goal:** Close the documented gap between what MiOS ships and what it uses.
**What+How:** The container-runtime map records components shipped and unwired: clevis, chrony, ROCm, ceph, mdevctl, freeipa, nut, guacamole, virt-v2v. For each, decide wire-or-remove, and record the decision. Wiring means an SSOT table, a unit, and a check that it is reachable; removing means deleting the payload and reclaiming the size, which also serves the sub-100 MB goal.
**Where:** `usr/share/mios/mios.toml, usr/share/containers/systemd/, usr/lib/systemd/system/, usr/share/mios/sys/Containerfile*`
**Done When:** every component in the list is wired with a check or removed with its payload, and none remains in the ambiguous middle.
**Verify:** every component in the shipped-but-unwired list is either wired with a reachability check or removed with its payload -- none left ambiguous.
**Do NOT:** leave one in the middle. Shipped-but-unwired costs image size, review attention and reader confusion and delivers nothing.
**Why:** shipped-but-unwired is the most expensive state: it costs image size, review attention and reader confusion, and delivers nothing.
**Dep:** AGY-1625

## AGY-1655 -- Give the AI plane a health contract  (WS-AI | P1 | M)
**Goal:** `/v1` endpoints are proven up, not presumed.
**What+How:** The AI plane is OpenAI `/v1`-only with Hermes at `:8642` as `MIOS_AI_ENDPOINT`. Define a health contract each backend must satisfy -- a `/v1/models` response shape, a latency bound, a token that proves the model is loaded rather than the server merely listening -- and a verb that checks it. Distinguish "process running" from "model serving", because those fail independently.
**Where:** `usr/libexec/mios/, usr/share/containers/systemd/mios-llm-*.container, usr/share/mios/mios.toml [ai]`
**Done When:** one verb reports per-backend health against the contract, and a listening-but-not-serving backend is reported as unhealthy.
**Verify:** one verb reports per-backend health against the contract, and a listening-but-not-serving backend is reported UNHEALTHY.
**Do NOT:** check that the socket is open. That reports green through the most common failure mode.
**Why:** a health check that only proves a socket is open reports green through the most common failure mode.
**Dep:** none

## AGY-1656 -- Prove the GPU lane degrades rather than fails  (WS-AI | P1 | M)
**Goal:** A GPU-gated service on a GPU-less host degrades to CPU, per ADR-0017.
**What+How:** ADR-0017 decides that GPU-gated services degrade to a CPU lane rather than failing. Implement the gate that selects the lane from detected hardware, and prove both directions: on a GPU host the GPU lane is chosen, on a GPU-less host the CPU lane starts and serves. Remember that GPU fractioning is impossible driver-free, so the model is whole-GPU-to-one-guest.
**Where:** `usr/share/containers/systemd/mios-llm-*.container, usr/libexec/mios/, usr/share/mios/mios.toml [blade.planes]`
**Done When:** both lanes are demonstrated on the appropriate hardware, and the selection is derived from detection rather than configuration.
**Verify:** both lanes are demonstrated on the appropriate hardware and the selection is derived from detection, not configuration.
**Do NOT:** assume a GPU. A 2-6 box fleet is not uniformly equipped, and GPU fractioning is impossible driver-free.
**Why:** a fleet of 2-6 boxes will not be uniformly equipped; a service that only starts with a GPU is a service that will not start.
**Dep:** AGY-1624

## AGY-1657 -- Make the agent-pipe's failure modes visible  (WS-AI | P1 | M)
**Goal:** When an agent request fails, the reason reaches the operator.
**What+How:** The agent pipe had a use-before-definition NameError that only surfaced as a broken publish bake, and a ReDoS in the podman-exec shell-stripper found by wall-clock regression rather than by review. Add structured error reporting: every failure path names what it was doing, and the AST-checker and venv-import method that found the NameError becomes a registered gate rather than a one-off.
**Where:** `usr/lib/mios/agent-pipe/, automation/98-drift-checks.sh`
**Done When:** the AST/import check is a registered gate with a negative test, and every agent-pipe failure path emits a named reason.
**Verify:** the AST/import check is a registered gate with a negative test, and every agent-pipe failure path emits a named reason.
**Do NOT:** leave it as a one-off script. A NameError that surfaces two systems away as a failed bake costs hours.
**Why:** a NameError that shows up two systems away as a failed bake costs hours to trace back.
**Dep:** AGY-1607

## AGY-1658 -- Bound every model-controlled regex  (WS-SECURITY | P1 | M)
**Goal:** No pattern applied to model output can backtrack exponentially.
**What+How:** The dispatch_cmd stripper allowed a flag's argument to start with `-`, giving `-a -b` two legal parses and ~1.64^n backtracking on model-controlled script text. Audit every regex that touches model output, agent input, or comment text for ambiguity, and pin wall-clock bounds on pathological inputs rather than asserting pattern strings -- the defect is behavioural.
**Where:** `usr/lib/mios/agent-pipe/, usr/lib/mios/mios_comments.py, usr/libexec/mios/`
**Done When:** each such regex has a timing test on a pathological input, and the suite fails if any exceeds its bound.
**Verify:** each model-facing regex has a wall-clock timing test on a pathological input, and the suite fails if any exceeds its bound.
**Do NOT:** assert the pattern string. The defect is behavioural -- ~1.64^n was measured, not read.
**Why:** input the model controls is input an adversary can shape, and a hang is as effective as a crash.
**Dep:** none

## AGY-1659 -- Make the Portal configurator the real config surface  (WS-SSOT | P1 | L)
**Goal:** ADR-0009's "one config surface" is usable.
**What+How:** The north star is that everything is defined from `mios.toml` by the deploying operator through the Portal configurator at `:8640/`, projected to every surface and drift-gated. Establish what the configurator can actually edit today versus what still requires hand-editing the file, close the gap for the highest-traffic tables first, and make every edit go through the same validation the gates use so the Portal cannot write a file the gate rejects.
**Where:** `usr/share/mios/portal/, usr/libexec/mios/, usr/share/mios/mios.toml`
**Done When:** the highest-traffic tables are editable through the Portal, every write is validated by the gate's own rules, and the unreachable remainder is listed.
**Verify:** the highest-traffic tables are editable through the Portal, every write passes the gate's own validation, and the unreachable remainder is listed.
**Do NOT:** let the Portal write a file the gate would reject. That turns an operator typo into a system that will not boot.
**Why:** an SSOT only an expert can edit by hand is not the operator-defined system the architecture claims.
**Dep:** none

## AGY-1660 -- Decide the accounts model and stop shipping it inert  (WS-ACCOUNTS | P1 | L)
**Goal:** `[accounts].db_backed` either works or goes.
**What+How:** The DB-driven cross-platform account model is assembled from FOSS bricks: systemd userdb JSON projection on Linux, lldap over Postgres as the cross-platform SSOT face, SCIM as a pattern, and autounattend plus `New-LocalUser` as the Windows baseline since Windows has no NSS/PAM. It ships inert. Pick the smallest end-to-end slice -- one account defined in the SSOT appearing on both platforms -- and build that, or remove the table and the doc claim.
**Where:** `usr/share/mios/mios.toml [accounts], usr/lib/mios/mios_accounts.py, usr/share/mios/windows/, docs/agy/doc-postgresos-accounts.md`
**Done When:** one account defined in the SSOT is demonstrated on Linux and Windows, or the table is removed and the doc says so.
**Verify:** one account defined in the SSOT is demonstrated on Linux AND Windows, or [accounts] is removed and the doc says so.
**Do NOT:** leave it shipping inert. An inert table makes an idea look implemented when it is not.
**Why:** "PostgresOS" is the operator's coinage for a real idea; an inert table makes it look implemented when it is not.
**Dep:** none

## AGY-1661 -- Prove the Windows plane against a real Windows host  (WS-WINDOWS | P1 | M)
**Goal:** The Windows surfaces are observed, not inferred.
**What+How:** MiOS projects dotfiles and theme surfaces cross-platform (btop is the landed proof on both). Take the next Windows surface end to end on a real host, not WSL: apply from the SSOT, confirm the live HOME reflects it, and confirm the drift gate detects a hand-edit. Record what differs from the Linux path.
**Where:** `usr/share/mios/windows/, installation/mios-common.ps1, usr/share/mios/mios.toml [dotfiles.registry.*]`
**Done When:** one further Windows surface is applied, drift-gated and observed on a real Windows host, with the divergences recorded.
**Verify:** one further Windows surface is applied, drift-gated and observed on a real Windows host, with divergences recorded.
**Do NOT:** test on WSL and call it Windows. Cross-platform claims tested only where the developer sits are half-claims.
**Why:** cross-platform claims tested only on the platform the developer uses are half-claims.
**Dep:** AGY-1643

## AGY-1662 -- Establish an observability floor  (WS-RUNTIME | P1 | M)
**Goal:** An operator can see what the system is doing without reading code.
**What+How:** Define the minimum an operator needs -- which services are up, which are degraded, which projection is stale, what the last build was -- and make one surface answer all of it. `MiOS-Mon.py` is the cross-platform TUI; make it read from the same measurements the gates use so the two cannot disagree.
**Where:** `usr/libexec/mios/MiOS-Mon.py, usr/libexec/mios/mios-daemon, automation/98-drift-checks.sh`
**Done When:** one surface answers the four questions from gate-derived data, and its numbers match the gate's on a deliberately drifted tree.
**Verify:** one surface answers up/degraded/stale-projection/last-build from gate-derived data, and its numbers match the gate's on a deliberately drifted tree.
**Do NOT:** compute the numbers separately. Two sources for 'is this healthy' diverge and the operator believes the friendlier one.
**Why:** two sources for "is this healthy" always diverge, and the operator believes the friendlier one.
**Dep:** AGY-1617

## AGY-1663 -- Make the release path identical between GitHub and Forgejo  (WS-RELEASE | P1 | M)
**Goal:** Two equal publishers, bit-for-bit.
**What+How:** GitHub and Forgejo are equal publishers; the build is local-first and the registry defaults to ghcr with credentials, else local/Forgejo. GitHub's bake is capacity-gated behind `PUBLISH` until the bake shrinks. Prove the outputs are identical: same digest from either path, or an explicit account of why not.
**Where:** `.github/workflows/mios-ci.yml, .forgejo/workflows/build-mios.yml, automation/build/`
**Done When:** both publishers produce the same digest for the same input, or the difference is recorded with its cause.
**Verify:** both publishers produce the same digest for the same input, or the difference is recorded with its cause.
**Do NOT:** assert parity. Nobody compares digests until a deployment misbehaves.
**Why:** "equal publishers" is a claim that fails quietly -- nobody compares digests until a deployment misbehaves.
**Dep:** AGY-1640

## AGY-1664 -- Shrink the bake so the capacity gate can come off  (WS-BUILD | P1 | L)
**Goal:** `PUBLISH` stops being capacity-gated.
**What+How:** The firstboot tier exists because vLLM and SGLang (~47 GB) do not fit a standard runner. Reduce the bake itself: audit layer composition, apply the rechunk budget, remove images no quadlet references (the gate now proves that set is empty), and evaluate distroless convergence -- noting Hummingbird is a question, not a drop-in.
**Where:** `usr/share/mios/sys/Containerfile*, automation/build/rechunk.sh, usr/lib/mios/bake/plan.d/`
**Done When:** the bake fits a standard runner without the firstboot workaround for at least the core tier, and the capacity gate is removed or narrowed.
**Verify:** the core tier fits a standard runner without the firstboot workaround, and the capacity gate is removed or narrowed.
**Do NOT:** treat Hummingbird as a drop-in. It is a question, not a swap.
**Why:** a publish path gated on capacity is a publish path that will be skipped under pressure.
**Dep:** AGY-1636

## AGY-1665 -- Prove the storage plane survives a node loss  (WS-STORAGE | P1 | L)
**Goal:** CephFS behaves as a native service of the Mini platform.
**What+How:** ADR-0016 D14 makes CephFS a native service on bare metal, never travelling to a transient OCI image. Demonstrate it: a two-node fleet, a file written, one node lost, the file still readable. Record the recovery time. Gluster is being sunset, so do not build on it.
**Where:** `usr/share/containers/systemd/mios-ceph.container, usr/share/mios/mios.toml [blade.planes].storage`
**Done When:** node loss is demonstrated with data intact, and the recovery time is recorded.
**Verify:** node loss is demonstrated with data intact and the recovery time recorded.
**Do NOT:** build on gluster. It is being sunset.
**Why:** the storage plane is the one whose failure is not recoverable by restarting something.
**Dep:** AGY-1623

## AGY-1666 -- Make the mesh join path observable  (WS-NETWORK | P1 | M)
**Goal:** Enrolment never gates a boot, and its state is visible.
**What+How:** Law 12 is that enrolment never gates a boot; peers join natively over headscale. Prove a boot completes with the mesh unreachable, and give the operator a way to see enrolment state and why a join failed. There is no firewalld -- the rules are nft.
**Where:** `usr/share/mios/mios.toml [blade.discovery], usr/libexec/mios/, usr/lib/systemd/system/`
**Done When:** a boot with no mesh reachability completes, enrolment state is reportable, and a failed join names its reason.
**Verify:** a boot with no mesh reachability completes, enrolment state is reportable, and a failed join names its reason.
**Do NOT:** gate boot on enrolment. Law 12 -- and a boot that hangs on the network makes remote hardware unrecoverable.
**Why:** a boot that hangs waiting for a network is the failure mode that makes remote hardware unrecoverable.
**Dep:** AGY-1624

## AGY-1667 -- Prove the vTPM and signed-UKI path  (WS-SECURITY | P1 | L)
**Goal:** Measured boot works on the systems MiOS targets.
**What+How:** The runtime architecture is bootc plus Podman/Quadlet plus libvirt with a signed UKI and greenboot; MOK is not the UKI, and swtpm provides a vTPM for guests. Demonstrate a signed-UKI boot end to end, and a guest with a working vTPM. Record which firmware paths were tried, because this is where hardware differences bite.
**Where:** `automation/build/, usr/lib/bootc/, usr/share/mios/sys/`
**Done When:** a signed-UKI boot and a vTPM-backed guest are both observed, with the firmware paths recorded.
**Verify:** a signed-UKI boot and a vTPM-backed guest are both observed, with the firmware paths recorded.
**Do NOT:** conflate MOK with the UKI. They are different, and this is where hardware differences bite.
**Why:** secure boot claims that have not been observed on real firmware are the ones that fail at the customer.
**Dep:** AGY-1622

## AGY-1668 -- Establish a backup and restore story  (WS-STORAGE | P2 | L)
**Goal:** `/var` can be restored.
**What+How:** `/var` persists across image updates and holds everything stateful. Define what must survive a rebuild, implement a backup path that captures it, and prove a restore onto a freshly installed system. An untested restore is not a backup.
**Where:** `usr/libexec/mios/, usr/share/mios/mios.toml, docs/agy/`
**Done When:** a restore onto a fresh install is demonstrated and the recovery procedure is written from the observed run.
**Verify:** a restore onto a freshly installed system is demonstrated and the procedure written from that run.
**Do NOT:** call an untested backup a backup. /var is the only thing that cannot be rebuilt from the SSOT.
**Why:** the immutable-image model makes `/var` the only thing that cannot be rebuilt from the SSOT.
**Dep:** AGY-1622

## AGY-1669 -- Prove the offline install path  (WS-DEPLOY | P1 | M)
**Goal:** MiOS installs with no network.
**What+How:** `bootc install --transport oci` fills the bare-metal leg from a local image. Build a USB carrying the OCI artifact and demonstrate an install on a machine with the network physically absent, not merely unconfigured. Record how the firstboot tier behaves when it cannot pull.
**Where:** `cat/, tools/install.sh, automation/build/`
**Done When:** an install completes with no network, and the firstboot tier's degraded behaviour is recorded.
**Verify:** an install completes with the network physically absent, and the firstboot tier's degraded behaviour is recorded.
**Do NOT:** test with the network merely unconfigured. Offline is the case the deploy plane exists for.
**Why:** offline is the case the deploy plane exists for; if it only works online it is a slower `curl | bash`.
**Dep:** AGY-1622

## AGY-1670 -- Give every gate a one-line statement of what it proves  (WS-EVIDENCE | P1 | M)
**Goal:** The gate index reads as an evidence list.
**What+How:** `usr/share/mios/reference/drift-gate-index.tsv` maps names to descriptions, and several entries simply repeat the function name (`check_manual_generated | check_manual_generated`). Replace each with a sentence stating what a reader learns when it passes. A gate that cannot be described that way is one to examine for vacuity.
**Where:** `usr/share/mios/reference/drift-gate-index.tsv, automation/98-drift-checks.sh`
**Done When:** no index entry restates its own name, and every entry says what passing proves.
**Verify:** no entry in drift-gate-index.tsv restates its own name, and every entry says what passing proves.
**Do NOT:** leave `check_x | check_x`. The index is the closest thing to a summary of what the project has established.
**Why:** the index is the closest thing to a summary of what the project has actually established.
**Dep:** AGY-1619

## AGY-1671 -- Make check_negative_coverage require discrimination, not existence  (WS-EVIDENCE | P1 | M)
**Goal:** Coverage means a negative test that actually breaches its gate.
**What+How:** The check currently requires that a negative test EXISTS for each gate. Strengthen it to require the test to have been observed failing the gate: record, per gate, the mutation used and assert the gate is red under it. This is the machinery that would have caught the tests that mutated the wrong thing -- adding a stray `.desktop` file rather than a rendered field.
**Where:** `automation/98-drift-checks.sh (check_negative_coverage), tests/drift-gate-negatives.sh`
**Done When:** every gate's negative test records its mutation and is proven to turn the gate red.
**Verify:** every gate's negative test records the mutation it uses and is proven to turn that gate RED under it.
**Do NOT:** require only that a negative test exists. One that never breaches its gate looks exactly like coverage.
**Why:** a negative test that never breaches its gate is indistinguishable from no negative test, and looks like coverage.
**Dep:** AGY-1609

## AGY-1672 -- Remove the self-triggering probe hazard for good  (WS-EVIDENCE | P1 | S)
**Goal:** A test cannot trip the check it is testing by existing.
**What+How:** Three instances so far: a slug test that spelled the bad org literally, an inert-table test naming its table inside a scanned `.sh`, and a probe named `MIOS_NO_SUCH_VARIABLE` that wrote itself into `referenced_names.txt`. Establish one convention -- probe values assembled at runtime, never written as literals in a scanned file -- and add a lint that flags a literal in a test matching a pattern its own gate scans for.
**Where:** `tests/drift-gate-negatives.sh, tools/test_*.py, automation/98-drift-checks.sh`
**Done When:** the convention is documented, the lint is registered with a negative test, and every existing probe conforms.
**Verify:** the convention is documented, a lint flags a literal in a test matching a pattern its own gate scans for, and every existing probe conforms.
**Do NOT:** spell the probe's token literally. It has caught this suite four times -- the slug test, the inert-table test, MIOS_NO_SUCH_VARIABLE, and a comment quoting the pattern its own check greps.
**Why:** a probe that trips its own gate produces a failure that looks real, in a file the reader will not suspect.
**Dep:** AGY-1610

## AGY-1673 -- Audit the CI job order for gates that mask each other  (WS-EVIDENCE | P1 | S)
**Goal:** The first failure reported is the most informative one.
**What+How:** The negatives suite runs before the main gate deliberately, but within the suite a single early failure hides everything after it: fixing `test_bake_tokens` immediately surfaced the next latent failure, then the next. Make the suite report ALL failures rather than dying on the first, so one CI run yields the full list instead of one per round trip.
**Where:** `tests/drift-gate-negatives.sh, .github/workflows/mios-ci.yml`
**Done When:** the suite runs every test and reports all failures together, while still exiting non-zero.
**Verify:** one CI run reports ALL negative-test failures together while still exiting non-zero.
**Do NOT:** let the suite die on the first failure. Three sequential single-failure runs cost three round trips to learn what one could have said.
**Why:** three sequential single-failure runs cost three round trips to learn what one run could have said.
**Dep:** AGY-1620

## AGY-1674 -- Make the version SSOT provable across every surface  (WS-SSOT | P1 | M)
**Goal:** One version, everywhere.
**What+How:** Three `mios.toml` files once carried conflicting versions (0.3.0 against 0.2.4), and 79 version-duplicate pairs were catalogued by the Global Unification Plan. `check_version_ssot` exists; extend it to every surface that states a version -- docs, image labels, unit files, the Portal, the installers -- and prove it fails when any one disagrees.
**Where:** `usr/share/mios/mios.toml [versions], automation/98-drift-checks.sh (check_version_ssot), installation/`
**Done When:** every version-bearing surface derives from `[versions]`, and a single disagreement fails the gate.
**Verify:** every version-bearing surface derives from [versions], and a single disagreement fails the gate.
**Do NOT:** check the SSOT alone. Docs, image labels, unit files, the Portal and the installers all state versions.
**Why:** a version that disagrees between the image and its documentation makes every bug report ambiguous.
**Dep:** AGY-1631

## AGY-1675 -- Finish the Global Unification Plan's key collapse  (WS-SSOT | P1 | L)
**Goal:** The 2523-key `MIOS_*` namespace becomes one-value-derived-everywhere.
**What+How:** The GUP is a lossless-diff-gated six-phase refactor; the env snapshot must normalise root paths or the gate fails in CI. The canonical walk emits `MIOS_AI_VLLM_*` while consumers use the short `MIOS_VLLM_*` -- that mismatch is the thing to unify. Continue phase by phase, with the lossless diff green at each step.
**Where:** `usr/lib/mios/mios_toml.py, automation/lib/globals.sh, automation/lib/globals.ps1, usr/share/mios/reference/env-baseline.txt`
**Done When:** each phase lands with a green lossless diff, the canonical and consumed key names agree, and the key count is materially lower.
**Verify:** each GUP phase lands with a green lossless diff, canonical and consumed key names agree, and the key count is materially lower.
**Do NOT:** let the env snapshot compare unnormalised root paths. That fails in CI for a reason that has nothing to do with the refactor.
**Why:** the namespace is the interface every consumer sees; duplicates in it are duplicated truth.
**Dep:** AGY-1674

## AGY-1676 -- Give the resolver one implementation  (WS-SSOT | P1 | M)
**Goal:** Shell, PowerShell and Rust resolvers agree by construction.
**What+How:** `check_resolver_twin_equivalence` and `check_resolver_differential_parity` compare implementations, and the latter was silently skipping when a tool was absent. Rather than comparing three implementations forever, make the native resolver authoritative and the others thin callers, keeping the differential check as a regression guard during the transition. Note the wiring asymmetry: `globals.sh` is generated first, `globals.ps1` last.
**Where:** `usr/lib/mios/mios_toml.py, automation/lib/globals.{sh,ps1}, src/mios-rs/, tools/native/`
**Done When:** one implementation resolves and the others delegate, with the differential check green throughout.
**Verify:** [resolver].max_key_divergence and max_value_divergence both reach 0, one implementation is authoritative and the others delegate.
**Do NOT:** reconcile by loosening the comparison. The remaining gap is Python's repr of nested values and table ordering -- it needs preserve_order and a toolchain that can link.
**Why:** three implementations of one resolver is three chances to disagree about what the SSOT says.
**Dep:** AGY-1675

## AGY-1677 -- Make `just` targets real or delete them  (WS-PROCESS | P1 | S)
**Goal:** Every documented command works.
**What+How:** Nine of twelve `miosd` targets pointed at things that did not exist, and `Skip` was being counted as pass. Audit every target in the justfile and equivalent entry points: run each on a clean tree and record the result. A target that cannot run is worse than an absent one because it is in the documentation.
**Where:** `justfile, tools/, automation/`
**Done When:** every target runs on a clean tree or is removed, and a gate fails when a target references a missing path.
**Verify:** every justfile target runs on a clean tree or is removed, and a gate fails when a target references a missing path.
**Do NOT:** count Skip as pass. Nine of twelve miosd targets pointed at things that did not exist.
**Why:** the task runner is the project's public interface for contributors; broken targets there are the first thing a newcomer hits.
**Dep:** none

## AGY-1678 -- Prove the golden-master tests compare against something  (WS-EVIDENCE | P0 | S)
**Goal:** No test diffs a tree against a copy of itself.
**What+How:** A golden-master test was found diffing the tree against a copy of itself, which passes unconditionally. Audit every golden/snapshot test: confirm the expected output is stored independently of the generated output, and that corrupting the generator turns the test red. This is the same family as the unit-gen `--check` that printed PASS without comparing.
**Where:** `tools/native/mios-unit-gen/tests/golden/, tools/test_*.py, src/mios-rs/`
**Done When:** every golden test is proven red under a deliberate generator mutation.
**Verify:** every golden/snapshot test is proven RED under a deliberate mutation of its generator.
**Do NOT:** diff a tree against a copy of itself. That is the purest form of a check that cannot fail.
**Why:** a snapshot test comparing an output to itself is the purest form of a check that cannot fail.
**Dep:** AGY-1646

## AGY-1679 -- Record the Quadlet digest-pinning convention and enforce it  (WS-BUILD | P1 | S)
**Goal:** The recurring digest-pin strip stops recurring.
**What+How:** A broad `git add` repeatedly strips `@sha256` pins from generated Quadlets, turning the pod-quadlets gate red; the fix is to regenerate and stage explicit paths, never `git add -A`, because AGY shares the tree. Per ADR-0003 digests are SBOM data resolved at build rather than hand-pinned, so reconcile the two: decide where the pin lives and make the gate enforce that decision rather than the current ambiguity.
**Where:** `usr/share/containers/systemd/, tools/generate-pod-quadlets.py, automation/98-drift-checks.sh`
**Done When:** the convention is written down, the gate enforces it, and the failure mode cannot be reintroduced by a staging mistake.
**Verify:** the pin convention is written down, the gate enforces it, and a staging mistake cannot reintroduce the failure.
**Do NOT:** use `git add -A`. That is exactly how the @sha256 pins get stripped, and how six test fixtures reached HEAD this session.
**Why:** a defect that recurs on a workflow habit will recur until the habit is impossible.
**Dep:** AGY-1637

## AGY-1680 -- Make the install/uninstall pair symmetric and proven  (WS-DEPLOY | P1 | M)
**Goal:** Uninstall removes exactly what install added.
**What+How:** `check_install_uninstall_symmetry` is now SSOT-driven. Prove it behaviourally: install on a clean system, snapshot, uninstall, and diff. Anything left behind is either a deliberate exception recorded in the SSOT or a defect.
**Where:** `installation/, usr/libexec/mios/, automation/98-drift-checks.sh`
**Done When:** an install-uninstall cycle leaves only recorded exceptions, verified by snapshot diff.
**Verify:** an install-uninstall cycle leaves only recorded exceptions, verified by snapshot diff.
**Do NOT:** compare the file lists by eye. An uninstall that leaves state makes the next install non-reproducible.
**Why:** an uninstall that leaves state makes the next install non-reproducible, which undermines the whole immutable claim.
**Dep:** AGY-1622

## AGY-1681 -- Test the configurator against adversarial input  (WS-SSOT | P2 | M)
**Goal:** The Portal cannot write an SSOT the gate rejects.
**What+How:** Every configurator write must pass the same validation the drift gate applies. Test with adversarial input: values out of range, tables removed, types swapped, duplicate table headers -- the last being how `mios.toml` was rendered unparseable twice by duplicate `[units."...timer".*]` blocks. The Portal must refuse rather than write.
**Where:** `usr/share/mios/portal/, usr/lib/mios/mios_toml.py, automation/98-drift-checks.sh`
**Done When:** each adversarial case is refused with a message naming the problem, and a fuzz pass finds no write that the gate then rejects.
**Verify:** each adversarial input is refused with a message naming the problem, and a fuzz pass finds no write the gate then rejects.
**Do NOT:** forget duplicate table headers. mios.toml was rendered unparseable twice that way.
**Why:** a config surface that can write an invalid SSOT converts an operator's typo into a system that will not boot.
**Dep:** AGY-1659

## AGY-1682 -- Verify mios.toml integrity before every commit that touches it  (WS-PROCESS | P0 | S) **[DONE]**
**Goal:** A truncated SSOT can never be committed again.
**What+How:** A truncated `mios.toml` was committed once and broke CI, and an earlier commit landed a 2870-line truncation that needed a restore commit. Add a pre-commit or CI-first check: `tomllib` parses, line count is within a declared band of HEAD, and every top-level table present at HEAD is still present. Reject rather than warn.
**Where:** `usr/share/mios/mios.toml, .githooks/, .github/workflows/mios-ci.yml, automation/98-drift-checks.sh`
**Done When:** a deliberately truncated SSOT is rejected before it can be pushed, proven by a negative test.
**Verify:** a deliberately truncated mios.toml is REJECTED before it can be pushed, proven by a negative test.
**Do NOT:** warn instead of reject. A truncation there takes every projection with it, and it has happened twice.
**Why:** the SSOT is the single point of definition; a truncation there takes every projection with it.
**Dep:** none

## AGY-1683 -- Make the drift gate's tool requirements explicit  (WS-EVIDENCE | P1 | S)
**Goal:** No check degrades silently when a tool is missing.
**What+How:** `MIOS_DRIFT_REQUIRE_TOOLS=1` forbids silent skips in CI, and `check_no_silent_tool_skips` guards it. Extend the guarantee to the tools tier: `tools/*.py` that shell out must declare their requirements and fail closed under the same variable. Document which tools each check needs so a local run can install them up front rather than discovering them one failure at a time.
**Where:** `automation/98-drift-checks.sh, tools/*.py, .devcontainer/, docs`
**Done When:** every check declares its tool requirements, all fail closed under `MIOS_DRIFT_REQUIRE_TOOLS`, and the list is documented in one place.
**Verify:** every check declares its tool requirements, all fail closed under MIOS_DRIFT_REQUIRE_TOOLS=1, and the list is documented in one place.
**Do NOT:** let a check skip silently. It reports the same green as a pass -- and check_resolver_differential_parity hid three separate defects behind exactly that.
**Why:** a check that skips silently reports the same green as a check that passed.
**Dep:** AGY-1620

## AGY-1684 -- Give the manual a stable anchor scheme  (WS-MANUAL | P2 | S)
**Goal:** Harvested prose keeps its provenance across reorganisation.
**What+How:** `landed()` requires a `mios-src:<sha12>` anchor and at least 90% word retention, and the ledger keys by content hash so passages can move freely. Document the anchor scheme, and add a check that an anchor is never edited or duplicated -- a duplicated anchor makes two passages claim the same source and the landing proof becomes ambiguous.
**Where:** `usr/libexec/mios/mios-manual, usr/share/doc/mios/manual/, usr/share/mios/reference/manual-corpus.tsv`
**Done When:** the scheme is documented, duplicate anchors fail a gate, and `landing` distinguishes moved from lost.
**Verify:** duplicate anchors fail a gate, and `mios-manual landing` distinguishes moved from lost.
**Do NOT:** let two passages claim the same source. The anchors are what make prune safe.
**Why:** the anchors are what make the prune step safe; ambiguity there makes deletion unverifiable.
**Dep:** AGY-1616

## AGY-1685 -- Decide what happens to the _harvest area once distilled  (WS-MANUAL | P2 | S)
**Goal:** The staging area does not become a second manual.
**What+How:** `usr/share/doc/mios/manual/_harvest/` will hold hundreds of files by the time the narrative ratchet is driven down. Decide its end state: emptied into the distilled manual with the ledger retaining provenance, or retained as an appendix. Either way, add a ratchet so it cannot grow indefinitely while the narrative count falls -- otherwise the prose has moved, not migrated.
**Where:** `usr/share/doc/mios/manual/_harvest/, usr/libexec/mios/mios-manual (distill), usr/share/mios/mios.toml [docs]`
**Done When:** the end state is decided and gated, with a ratchet that prevents the staging area growing while the narrative count falls.
**Verify:** the _harvest end state is decided and gated, with a ratchet preventing it growing while the narrative count falls.
**Do NOT:** move prose into a directory nobody reads and call it documentation. That is relocation.
**Why:** moving prose from comments into a directory nobody reads is not documentation, it is relocation.
**Dep:** AGY-1616

## AGY-1686 -- Write down what MiOS is, in the repository  (WS-THESIS | P0 | S)
**Goal:** The project states its own nature.
**What+How:** MiOS is a research vehicle and a proof of an idea, not a product. Its thesis has four parts that stand or fall together, its deliverable is the repository itself, its deployment scope is universal -- blades, mesh, bare metal, VM, WSL, cloud -- and today it is observed only on a dev VM and WSL. Write that down plainly, including the gap between scope and observation, and link it from the README as the first thing a reader meets.
**Where:** `README.md, ROADMAP.md, usr/share/doc/mios/manual/`
**Done When:** the statement exists, distinguishes observed from designed, and is the first link in the README.
**Verify:** the statement exists, distinguishes observed from designed, and is the first link in README.md.
**Do NOT:** describe the deployment scope as if it were observed. It runs on a dev VM and WSL; bare metal is untried.
**Why:** a reader who thinks this is a product judges it as a broken product; a reader who knows it is a proof judges the proof.
**Dep:** none

## AGY-1687 -- Make AGY task ids unique and gate them  (WS-PROCESS | P1 | S) **[DONE]**
**Goal:** One id, one task.
**What+How:** 25 ids are used twice in AGY-TASKS.md, at different heading levels -- `## AGY-106` is a `build-mios.sh` fold and `### AGY-106` is a different task about redoing that fold in the right repo. Anything referring to "AGY-106" is therefore ambiguous, including dependency fields in other tasks. Renumber the later occurrence of each collision into the free range, update every `**Dep:**` that pointed at it, and add a gate that fails when an id appears twice or when a `**Dep:**` names an id that does not exist.
**Where:** `AGY-TASKS.md, automation/98-drift-checks.sh (check_tasks_status_parity)`
**Done When:** every id is unique, every dependency resolves to exactly one task, and a negative test proves the gate fails on a duplicated id and on a dangling dependency.
**Verify:** every AGY id is unique, every **Dep:** resolves to exactly one task, and a gate fails on a duplicated id AND on a dangling dependency.
**Do NOT:** renumber without fixing the **Dep:** fields that point at the collided id.
**Why:** dependency fields are the only ordering the task list has; ambiguous ids make that ordering unusable.
**Dep:** AGY-1647

## AGY-1715 -- Make tests/test-directory-dispatch.sh assert instead of indexing an empty result  (WS-CI | P2 | S)
**Goal:** A suite that finds nothing must say so, not crash.
**What+How:** The suite pipes dispatch output into a Python one-liner that reads element zero of the hits list with no guard. With no populated directory index the dispatch returns zero hits, the index raises, and the suite dies mid-run printing `hits=0 root=` -- which reads as a broken test rather than as an absent fixture. Guard the index, assert a non-empty hit set with a message naming what was missing, and either build the fixture the suite needs or state in the failure that the index must be populated first. Then move it from `[ci.exempt]` into the `unit` tier and lower `[ci].max_exempt_suites`.
**Where:** `tests/test-directory-dispatch.sh, usr/share/mios/mios.toml`
**Done When:** the suite runs in the `unit` tier and its failure message names the missing fixture.
**Verify:** `bash tests/run-suites.sh unit` includes the suite, and running it without the index fails with a message naming the index rather than a Python IndexError.
**Do NOT:** make it exit 0 when there are no hits. A suite that passes on an empty result is the defect class this registry exists to find.
**Why:** it was one of thirteen suites no workflow ran; the reason it could not be wired in was a crash, not a real assertion.
**Dep:** AGY-1619

## AGY-1716 -- Resolve the open-url-fallback-chain skill so tests/test-expand-from.py sees its three steps  (WS-CI | P2 | M)
**Goal:** Expansion emits the steps the skill declares.
**What+How:** The suite expects three steps naming three browsers; the dispatcher returns a not_found error for skill `open-url-fallback-chain` and zero steps, so the bound browser list is empty. Establish which is wrong -- the skill is absent from where the resolver looks, the resolver looks in an installed path rather than the tree, or the expansion no longer emits bound steps -- and fix that, not the assertion. Then move it into the `unit` tier and lower the exemption ceiling.
**Where:** `tests/test-expand-from.py, usr/lib/mios/agent-pipe/, usr/share/mios/mios.toml`
**Done When:** the suite passes in the `unit` tier against the repository tree with no MiOS installed.
**Verify:** the suite emits 3 steps with the 3 browser names, and `[ci.exempt]` no longer lists it.
**Do NOT:** relax the assertion to accept 0 steps. The not_found error is the finding.
**Why:** the fallback chain is what makes a URL open when the first browser is absent; nothing has tested it.
**Dep:** AGY-1715

## AGY-1717 -- Make tests/test-reflection.py skip deliberately when no refine backend is reachable  (WS-CI | P2 | S)
**Goal:** An absent backend is a skip that says so, not a failure and not a silent pass.
**What+How:** The suite calls reflection, every connection attempt fails, the correction comes back empty, and the suite fails. A runner has no model backend, so this can never pass there. Follow the pattern `tests/test-owui-pipe-endpoints.py` already uses: detect the backend, and when it is absent print a skip naming the endpoint it tried and exit 0; when it IS present, assert the correction. Then move it into the `unit` tier.
**Where:** `tests/test-reflection.py, usr/share/mios/mios.toml`
**Done When:** the suite skips with a named reason on a backend-less runner and asserts the correction where a backend answers.
**Verify:** running it with no backend exits 0 printing the endpoint it tried; running it against a reachable endpoint asserts a non-empty correction.
**Do NOT:** exit 0 unconditionally. A skip that cannot tell itself apart from a pass is the same defect as a gate that cannot fail.
**Why:** reflection is on the path every refined answer takes, and no CI has ever exercised it.
**Dep:** AGY-1715

## AGY-1718 -- Collapse the separator the think-stripper leaves behind  (WS-CI | P1 | S)
**Goal:** Removing a think block leaves the text as it would have been written.
**What+How:** `tests/test-think-stripper.py` measured two failures: an inline block yields a doubled space where it was removed, and a block on its own line yields a blank line between the surrounding paragraphs. The stripper removes the block but not the whitespace that surrounded it. Decide the rule -- collapse the run of whitespace the removal joins, preserving a single space inside a line and a single newline between lines -- implement it once where the stripping happens, and keep the existing cases green. Then move the suite into the `unit` tier.
**Where:** `usr/lib/mios/agent-pipe/, tests/test-think-stripper.py, usr/share/mios/mios.toml`
**Done When:** all the suite's cases pass and the suite runs in the `unit` tier.
**Verify:** `python3 tests/test-think-stripper.py` reports no FAIL lines, including the inline and multi-line cases.
**Do NOT:** change the test expectations to match the current output. The doubled separator reaches the operator.
**Why:** the stripper output is what a person reads; it has been inserting stray whitespace with no test running to notice.
**Dep:** AGY-1715

## AGY-1719 -- Re-run the exempted suites and lower [ci].max_exempt_suites to what remains  (WS-CI | P2 | S)
**Goal:** The exemption list shrinks as the reasons are removed.
**What+How:** `[ci.exempt]` carries seven entries, four of them measured failures with a task id each (AGY-1715 through AGY-1718) and three of them harnesses that take arguments or need a binary. As each of the four lands, delete its entry and lower `[ci].max_exempt_suites` in the same commit, so the ceiling always equals the count. When only the three structural entries remain, record in `[ci]` why each is structural rather than pending.
**Where:** `usr/share/mios/mios.toml`
**Done When:** the ceiling equals the number of exemptions and every remaining entry states why it can never join a tier.
**Verify:** `python3 tools/ci-suites.py --check` passes with the ceiling equal to the exemption count, and adding an exemption without raising the ceiling fails.
**Do NOT:** raise the ceiling to accommodate a new exemption. It is shrink-only; a suite that cannot run is a task, not a setting.
**Why:** an exemption list with a ceiling above its count is a list that can grow quietly, which is how thirteen suites went unrun.
**Dep:** AGY-1718

## AGY-1720 -- Give tests/drift-parity.sh and tests/drift-gate-readonly.sh a caller or delete them  (WS-CI | P2 | S)
**Goal:** No harness in the tree that nothing invokes.
**What+How:** Both are tracked and neither is invoked by any workflow, justfile target or script. `drift-parity.sh` compares the bash gate against the miosd parity subcommand and needs that binary; `drift-gate-readonly.sh` duplicates the gate tier. Decide each: wire it to a tier or a documented command with the prerequisite it needs, or delete it and say in the commit what it used to prove. Update `[ci.exempt]` and the ceiling either way.
**Where:** `tests/drift-parity.sh, tests/drift-gate-readonly.sh, usr/share/mios/mios.toml, justfile`
**Done When:** each has a caller or is gone, and `[ci.exempt]` reflects the outcome.
**Verify:** a caller exists for every remaining harness in tests/, and `python3 tools/ci-suites.py --check` passes.
**Do NOT:** leave a harness exempt with the reason that nothing calls it. That records the problem instead of resolving it.
**Why:** an uncalled harness looks like coverage in a file listing and provides none.
**Dep:** AGY-1719

## AGY-1721 -- Write mios.toml atomically so a concurrent reader cannot see a partial file  (WS-PROCESS | P1 | S)
**Goal:** A reader either sees the previous SSOT or the next one, never half of one.
**What+How:** Weakness W8. A read taken while the file was being written parsed CLEANLY as 1,879 lines and 6 tables, against ~10.7k lines and 152 tables. It parsed, so a tomllib check passed; only the line count showed the truncation, and a commit taken at that moment would have shipped it. Find every writer of `usr/share/mios/mios.toml` and make each write to a temporary file in the same directory and `os.replace` it over the target, which is atomic on both platforms. Add the same treatment to the generated projections that carry the same hazard.
**Where:** `usr/share/mios/mios.toml writers under tools/ and usr/libexec/mios/`
**Done When:** no writer truncates the target in place, and a reader looping during a write never observes a short file.
**Verify:** a loop reading the file while a writer runs sees only complete versions -- assert the line count never drops below the smaller of the two versions across 100 reads.
**Do NOT:** rely on a tomllib parse to detect truncation. The truncated file parsed; the check must be the line count as well.
**Why:** an SSOT that can be observed half-written makes every gate reading it non-deterministic, and a commit taken at the wrong moment ships the fragment.
**Dep:** none

## AGY-1722 -- Normalise every path a gate compares, and prove the gates agree on both hosts  (WS-PROCESS | P1 | M)
**Goal:** A gate returns the same verdict on Linux and Windows for an identical tree.
**What+How:** Weakness W9. `tools/check-credential-literals.py` compared `os.path.relpath` output against forward-slash entries in the SSOT, so on Windows every entry read as NEW and every allowlisted entry as removed -- green on one host, red on the other, same tree. Sweep every gate that builds a path for comparison, normalise with `.replace(os.sep, "/")` at the point of production, and add a check that fails when a gate emits a backslash in a comparison key.
**Where:** `tools/check-*.py, tools/audit-*.py, automation/98-drift-checks.sh`
**Done When:** every gate's comparison keys are forward-slash, and a planted backslash fails the new check.
**Verify:** run the full gate on Linux and on Windows against the same commit and diff the verdicts; the two lists are identical.
**Do NOT:** fix only the gate that bit you. `os.walk` ordering, `fnmatch` case folding and locale case rules are the same class and appear in the same files.
**Why:** a gate that disagrees with itself across hosts teaches people to ignore it, which costs more than not having it.
**Dep:** AGY-1612

## AGY-1723 -- Fail the gate when any shrink-only ceiling increases  (WS-PROCESS | P1 | M)
**Goal:** A ratchet that can be raised is not a ratchet.
**What+How:** Weakness W3. `[ci].max_exempt_suites` was raised 7 to 8 to accommodate an entry that belonged in a different category. The SSOT now carries many shrink-only ceilings -- `[docs].max_*`, `[legibility].max_*`, `[resolver].max_*_divergence`, `[tasks].max_duplicate_ids`, `[ci].max_exempt_suites`, `[tests].max_leaked_fixtures`. Add a check that reads every key matching the ceiling naming convention from the working tree AND from HEAD, and fails when any value increased. Record the convention so a new ceiling is covered automatically.
**Where:** `automation/98-drift-checks.sh, tools/check-ratchet-direction.py (new), usr/share/mios/mios.toml`
**Done When:** raising any ceiling fails the gate, and lowering one passes.
**Verify:** raise one ceiling by 1 and the new check exits non-zero naming the key and both values; restore it and the check passes.
**Do NOT:** hardcode the list of ceilings. Derive it from the naming convention, or the next ceiling added is unguarded.
**Why:** every ratchet in this repository depends on nobody quietly raising it, and that has now been observed happening.
**Dep:** none

## AGY-1724 -- Ratchet the AGY task schema downward from 1607 until every OPEN task is falsifiable  (WS-PROCESS | P2 | L)
**Goal:** No open task can be called done without a command that proves it.
**What+How:** `tools/check-task-schema.py` enforces the eight fields only from id 1607 up, via a hardcoded `SCHEMA_FROM`. Below that line there are 672 DONE tasks, which need nothing, and **482 OPEN tasks, every one of which is missing both Verify and Do NOT**. Move `SCHEMA_FROM` into `[tasks].schema_from` as a shrink-only ceiling, then retro-fit the open tasks in batches, lowering the key as each batch lands. A DONE task is exempt by its marker, not by its id.
**Where:** `tools/check-task-schema.py, usr/share/mios/mios.toml ([tasks].schema_from), AGY-TASKS.md`
**Done When:** `[tasks].schema_from` is 1 and every OPEN task carries all eight fields.
**Verify:** `python3 tools/check-task-schema.py` passes, and raising `[tasks].schema_from` fails the ratchet-direction check from AGY-1723.
**Do NOT:** mechanically paste a Verify line onto 482 tasks. A Verify nobody checked is worse than an absent one, because it looks like coverage. Retro-fit in batches you have actually read.
**Why:** the task list is the plan; a task with no falsifiable Verify is an opinion about what to do next.
**Dep:** AGY-1736

## AGY-1725 -- Sync mios-bootstrap on every build and gate the shared surfaces  (WS-PROCESS | P1 | M)
**Goal:** The two repositories never disagree about a surface they share.
**What+How:** Law 15 requires both `mios.git` and `mios-bootstrap.git` be verified before a change to a shared surface, but nothing enforces it and `check_bootstrap_ports_parity` reports the `[ports]` table already diverging. Make the build project the shared surfaces into `../mios-bootstrap` via `usr/libexec/mios/mios-sync-toml` as a build step, commit the projection there, and extend the parity check to every shared surface rather than `[ports]` alone. When the bootstrap checkout is absent the check must FAIL loudly, not skip -- a skip reporting success is why they drifted.
**Where:** `usr/libexec/mios/mios-sync-toml, automation/build.sh, automation/98-drift-checks.sh, .github/workflows/mios-ci.yml`
**Done When:** a build leaves both repositories carrying identical shared surfaces, and a deliberate divergence fails the gate.
**Verify:** edit `[ports]` in one repository, run the build, and confirm the other repository is updated and the parity check passes; revert one side by hand and confirm the check fails.
**Do NOT:** skip silently when the bootstrap checkout is missing. A skip that reports success is the mechanism that produced the drift.
**Why:** the bootstrap repository is what a new machine installs from; a stale one installs a system that disagrees with the image.
**Dep:** AGY-1621

## AGY-1726 -- Make the shared-tree rules mechanical rather than remembered  (WS-PROCESS | P1 | M)
**Goal:** The collaboration rules are enforced by tooling, not by recall.
**What+How:** Weaknesses W1, W5 and W7. A collaborator's untracked file was deleted twice, two rewritten files were reverted wholesale, and a probe plus a backup reached the index. Add a pre-commit hook to this repository that refuses a commit staging any path matching a backup or probe shape, refuses `git add -A` by rejecting a commit whose staged set exceeds a stated size without an explicit override, and prints the paths another process modified in the last minute so a wholesale revert is visible before it is committed.
**Where:** `.githooks/pre-commit (new), tools/install-git-hooks.sh (new), CONTRIBUTING or AGY-TASKS.md`
**Done When:** the hook is installed by the standard setup path and refuses each of the three shapes, with an override that has to be typed deliberately.
**Verify:** stage a `.bak` file and the hook refuses; stage a probe-named file and it refuses; stage 500 files and it refuses without the override.
**Do NOT:** make the hook advisory. A warning in a hook is read once and then never again.
**Why:** these three cost a whole session's rework, and every one of them is mechanically detectable before the commit that ships it.
**Dep:** AGY-1736

## AGY-1727 -- Reconcile the 18 shared files that diverged, and promote them to mirrored  (WS-PROCESS | P1 | L)
**Goal:** A file tracked in both repositories has one content, not two.
**What+How:** 35 files are tracked in both `mios.git` and `mios-bootstrap.git`. Since 0d608c84 every one is declared, but 18 sit in `[bootstrap.sync].not_mirrored` because they had already diverged: the agent rule files, `.editorconfig`, `.env.mios`, `.gitattributes`, `etc/mios/ai/config.json`, three `etc/skel` configs, `image-versions.yml`, `installation/README.md`, `installation/mios-install.{ps1,sh}`, `llms.txt`, `system-prompt.md`, `usr/share/mios/profile-headless.toml` and two generated name registries. Take them in three groups. Generated files (`names.generated.txt`, `referenced_names.txt`) each repository renders for itself -- keep them not-mirrored and say so. Pure formatting divergences (`installation/mios-install.ps1` differs ONLY in line endings; `installation/README.md` by a wrapped header) normalise and promote. The rest carry real content differences -- `installation/mios-install.sh` is 403 lines in mios.git and 1713 in bootstrap -- so decide which side is authoritative per file, reconcile, and promote.
**Where:** `usr/share/mios/mios.toml ([bootstrap.sync]), installation/, tools/sync-bootstrap.py`
**Done When:** `not_mirrored` holds only files that are per-repository BY DESIGN, each with its reason, and everything else mirrors.
**Verify:** `python3 tools/sync-bootstrap.py --root . --check` passes with the mirrored count raised, and hand-editing a mirrored file in either repository fails it.
**Do NOT:** copy one side over the other to make the gate green. `mios-install.sh` differs by 1310 lines; whichever you delete, someone wrote it.
**Why:** the bootstrap repository is what a new machine installs from; a shared file with two contents means the installed system disagrees with the image depending on which repo you read.
**Dep:** AGY-1738

## AGY-1728 -- Find every gate that skips where it should fail  (WS-PROCESS | P1 | M)
**Goal:** No check reports success for work it did not do.
**What+How:** `check_bootstrap_sync` returned 0 whenever the bootstrap checkout was absent. No runner has one, so the gate that exists to enforce Law 15 had never once compared the two repositories -- and 18 shared files diverged behind that green. The pattern is a guard of the shape "prerequisite missing, print a note, return 0". Enumerate every one in `automation/98-drift-checks.sh` and `tools/`, and for each decide: fail closed under `MIOS_DRIFT_REQUIRE_TOOLS=1`, or provide the prerequisite in CI. Record the decision beside the guard.
**Where:** `automation/98-drift-checks.sh, tools/check-*.py, tools/*-bootstrap.py, .github/workflows/mios-ci.yml`
**Done When:** every prerequisite-absent path either fails under REQUIRE_TOOLS or has its prerequisite provisioned, and the list is written down.
**Verify:** run the full gate with each prerequisite removed in turn under `MIOS_DRIFT_REQUIRE_TOOLS=1`; every removal turns some check RED, and none reports success.
**Do NOT:** count a printed "skipping" line as a report. Nobody reads a skip in a green run -- that is the whole mechanism.
**Why:** this is the repository's most expensive recurring defect, and it just cost eighteen files of undetected drift between the two published repositories.
**Dep:** AGY-1683

## AGY-1729 -- Prove each of the nineteen re-registered negative tests actually breaches its gate  (WS-GATES | P1 | M)
**Goal:** A registered test that never turns its gate red is still not coverage.
**What+How:** Nineteen negative tests were defined and never invoked; they are registered now, but registration only proves they RUN. For each -- `test_cargo_deny`, `test_unit_projection`, `test_port_fallbacks`, `test_globals_generated`, `test_node_pool`, `test_pipeline_numbering`, `test_ports_category_schema`, `test_powershell_parse`, `test_ps_redirectors`, `test_resolved_env_lossless`, `test_ssot_consumer_keys`, `test_unpinned_runtime_fetches`, `test_value_aliases`, `test_vendored_assets_non_stub`, `test_windows_exe_provenance`, `test_bash_phase_ratchet`, `test_metal_vs_hosted`, `test_no_duplicate_value_key`, `test_no_hardcoded_ssot_literal` -- confirm the mutation it injects makes its gate exit non-zero, and record the mutation in the test body. Where a test passes without ever breaching, repair the mutation.
**Where:** `tests/drift-gate-negatives.sh, automation/98-drift-checks.sh`
**Done When:** every one of the nineteen is shown red under its own mutation and green after restoration.
**Verify:** temporarily invert each gate's exit and confirm its negative test then FAILS; a test that still passes is not exercising its gate.
**Do NOT:** count a green suite as evidence. These nineteen were green for months by never running at all.
**Why:** they were written as coverage and delivered none; running them is necessary but does not by itself make them effective.
**Dep:** AGY-1671

## AGY-1730 -- Make every counting gate count the thing it claims  (WS-GATES | P1 | M)
**Goal:** A measurement that cannot distinguish the healthy case from the broken one is not a measurement.
**What+How:** `check_negative_coverage` counted the negative tests that EXIST, so nineteen that were never invoked satisfied it completely. Audit every gate whose evidence is a count -- coverage counts, registry counts, tagged/untagged ratios, "N of M" summaries -- and ask of each what state it would report as healthy while the property is broken. Convert each to measure the property directly (invoked, not defined; executed, not present; asserted, not listed).
**Where:** `automation/98-drift-checks.sh, tools/check-*.py`
**Done When:** each counting gate is either measuring the property or replaced by one that does, with the audit recorded.
**Verify:** for each converted gate, construct the state it previously reported healthy and show it now fails.
**Do NOT:** raise a count ceiling to make a gate pass. The count was never the property.
**Why:** this is the repository's most expensive recurring defect, and a count is its most common disguise.
**Dep:** AGY-1729

## AGY-1731 -- Validate the pulled image end to end on MiOS-DEV  (WS-DEPLOY | P1 | M)
**Goal:** The image that publishes is shown to boot and serve, not merely to build.
**What+How:** `ghcr.io/mios-dev/mios:latest` now exists locally: 49 GB, 79 layers, carrying `containers.bootc=1` and `ostree.bootable=1`. Run `tests/bake-smoke.sh` against it, then take the further step the smoke test cannot: `bootc switch` a VM onto it, boot, and record which units reached active, which failed, and what greenboot decided. Write the result into ROADMAP.md's state table as an observation with its date.
**Where:** `tests/bake-smoke.sh, ROADMAP.md, docs/agy/`
**Done When:** a boot from this image is recorded, including every unit that did not start.
**Verify:** the recorded run names the image digest, the units that failed, and greenboot's verdict; a run with no failures listed must say so explicitly.
**Do NOT:** treat a passing smoke test as a boot. The smoke test runs the image as a container; it never exercises the boot path, the units, or rollback.
**Why:** every deployment claim in the documentation is still a design claim; this is the first chance to make one an observation.
**Dep:** AGY-1669

## AGY-1732 -- Stop concurrent writers silently reverting committed work  (WS-PROCESS | P1 | M)
**Goal:** Work that was verified and committed stays in the tree.
**What+How:** Two registered gates and three negative tests were written, proven red against a planted violation, committed, and later found absent -- their tool files left in `tools/` with nothing calling them. The cause is two agents writing one working tree, where a whole-file rewrite by one drops the other's edits. Decide the mechanism: a lock, separate worktrees per writer, or a pre-commit assertion that every `tools/check-*.py` is referenced and every defined check is registered. Then make the loss detectable at commit time rather than three commits later.
**Where:** `.githooks/, automation/98-drift-checks.sh, tools/check-negatives-registered.py`
**Done When:** a rewrite that drops a registration fails before it can be committed.
**Verify:** delete one check registration, attempt a commit, and confirm it is refused naming the check.
**Do NOT:** rely on noticing it in review. It was missed three times in one session, each time costing a red CI run.
**Why:** the repository's guarantees are only as good as the edits that survive; a silent revert of a gate removes a guarantee without removing its record.
**Dep:** AGY-1607

## AGY-1733 -- Give the man pages the reader the operating system indexes  (WS-DOCS | P2 | S)
**Goal:** `apropos mios` and `man -k` answer, not just `man mios`.
**What+How:** 135 roff pages now render into `usr/share/man` and `man-db` is installed, so `man mios` resolves by filename. `apropos` and `man -k` need the index man-db builds, which lives under `/var/cache/man` and therefore is not part of the image. Establish which of Fedora's man-db units the image should enable so the index is built once after first boot, confirm the timer or path unit is present, and record what a fresh boot has to do before `apropos mios` answers.
**Where:** `usr/share/mios/mios.toml ([packages.base]), usr/lib/systemd/system-preset/, usr/share/doc/mios/`
**Done When:** `apropos mios` returns the verb pages on a booted system, and the mechanism that builds the index is named.
**Verify:** on a freshly booted image, `apropos mios` lists more than one page; before the index exists it must fail visibly rather than silently returning nothing.
**Do NOT:** ship a MiOS-specific unit that runs mandb. man-db already provides one; adding a second is a parallel mechanism to keep in step.
**Why:** a manual that only answers when you already know the page name is half a manual.
**Dep:** AGY-1731

## AGY-1734 -- Make MiOS-DEV run MiOS itself and record what the cutover needed  (WS-VARIANT | P1 | M)
**Goal:** The development host runs the system it builds.
**What+How:** MiOS-DEV is a WSL machine running "Podman Machine OS" (Fedora 44, plain rootfs, no bootc binary, no ostree) with the MiOS image sitting in its container store -- MiOS as a guest of the thing it should be. Export the image filesystem, import it as the distro, and enable systemd so the Quadlets, greenboot checks and firstboot units have a PID 1 that understands them. The image already carries podman 5.8.4, systemd, crun, conmon and the podman socket unit, so it can host itself. Record every gap the cutover exposes rather than patching around it.
**Where:** `config/artifacts/wsl2.toml, tools/, usr/share/mios/mios.toml ([variants.entries.mios-dev])`
**Done When:** the distro boots MiOS, systemd reaches a running state, and its Quadlet containers start.
**Verify:** on the machine, the release name reports MiOS, the init system answers, and at least one MiOS container is running -- and `[variants.entries.mios-dev].status` moves off partial only when all three hold.
**Do NOT:** replace the working machine before the replacement is shown to boot. The image took ninety minutes to pull and lives inside the machine being replaced.
**Why:** a development host that cannot run the image it produces cannot tell you the image is broken.
**Dep:** none

## AGY-1735 -- Give MiOS-Metal a built artifact so it stops being a design  (WS-VARIANT | P2 | L)
**Goal:** The metal-owning variant exists as something you can boot.
**What+How:** MiOS-Metal is specified in a doc and configured by `[metal]`, and builds nothing: it is one of two variants still marked design. Produce the artifact its registry entry declares -- an ISO and a BIB recipe -- containing only what a hypervisor-router needs: the vfio bindings derived from `[metal]`, the mesh configuration, no desktop and no AI plane. Then lower `[variants].max_design_variants`.
**Where:** `config/artifacts/, docs/agy/doc-mios-metal.md, usr/share/mios/mios.toml ([metal], [variants])`
**Done When:** the artifact builds, boots headless, and binds the declared devices to vfio.
**Verify:** the built image carries the bootc labels, and a boot shows the GPUs bound to vfio-pci; the design ceiling drops by one in the same commit.
**Do NOT:** claim GPU fractioning. The doc's own critic pass established it is impossible driver-free -- whole GPU to one guest.
**Why:** a variant that builds nothing is a document with a table of contents entry.
**Dep:** AGY-1734

## AGY-1736 -- Prove MiOS-Xbox differs from MiOS in the ways it claims  (WS-VARIANT | P2 | M)
**Goal:** The gaming edition is a measured difference, not a colour.
**What+How:** `[editions.mios-xbox]` sets a dev channel, posture C, a gaming debloat profile and its own accent; `tools/configure-xbox-cpu.sh` exists. Nothing asserts the built Xbox artifact actually differs from the base in those four ways. Add a comparison that builds or inspects both editions and asserts each declared difference is present in the output, then move the status.
**Where:** `usr/share/mios/mios.toml ([editions]), tools/configure-xbox-cpu.sh, tests/`
**Done When:** a test names each declared difference and fails when the built artifact does not carry it.
**Verify:** flipping one key in `[editions.mios-xbox]` and rebuilding makes the comparison fail on exactly that key.
**Do NOT:** compare the SSOT against itself. The claim is about the artifact, and a check that reads only the config passes whatever the build did.
**Why:** an edition is a promise about output; unverified, it is a promise about intent.
**Dep:** AGY-1734

## AGY-1737 -- Make MiOS-Cat install MiOS rather than boot beside it  (WS-VARIANT | P1 | L)
**Goal:** The portable edition deploys the system it carries.
**What+How:** The Ventoy machinery works and the partitions, models and repository staging are configured under `[cat]` with thirteen gates behind them. What it does not do is install MiOS: the bare-metal leg installs a plain distribution rather than the bootc image. Wire the offline install path so the carried image is what lands, using the transport that needs no network.
**Where:** `installation/, tools/install.sh, usr/share/mios/mios.toml ([cat])`
**Done When:** a machine booted from the medium installs the carried MiOS image and reboots into it, with the network physically absent.
**Verify:** the installed system reports the MiOS release and the bootc labels; pulling the network cable before the install changes nothing about the outcome.
**Do NOT:** call it done when the medium boots. Booting the medium is the part that already works; installing from it is the task.
**Why:** the portable edition exists so a machine with no network can become a MiOS machine, and that is the one thing it cannot yet do.
**Dep:** AGY-1734

## AGY-1738 -- Give MiOS-Xbox-Arm an arm64 build or retire the variant  (WS-VARIANT | P3 | M)
**Goal:** The second design variant resolves in one direction or the other.
**What+How:** `[editions.mios-xbox-arm]` declares an arm64 Windows guest and the Xbox posture. No arm64 artifact is produced and the base image is built amd64 only. Either produce an arm64 image and artifact, or remove the edition and its variant entry and say in the commit what it was for. Update `[variants].max_design_variants` either way.
**Where:** `usr/share/mios/mios.toml ([editions], [variants]), config/artifacts/, Containerfile`
**Done When:** an arm64 artifact exists and boots, or the variant and its edition are gone.
**Verify:** either the built image reports arm64 and carries the bootc labels, or no variant and no edition names arm64 and the design ceiling has dropped.
**Do NOT:** leave it in design while the ceiling stays where it is. That is the state the ceiling exists to make visible.
**Why:** a declared target nobody builds sets an expectation the project does not meet.
**Dep:** AGY-1735

## AGY-1739 -- Project the variant registry into the surfaces that name variants  (WS-VARIANT | P2 | M)
**Goal:** One statement of the product line, read everywhere it is mentioned.
**What+How:** The registry names six variants and their status, and the manual page renders from it. The installers, the Portal, the README and the roadmap still name variants in prose they maintain by hand, so those can disagree with the registry the moment either changes. Project them: derive each list from the registry and gate the derivation, the way ports and launchers already are.
**Where:** `tools/render-manpages.py, installation/, README.md, ROADMAP.md, usr/share/doc/mios/`
**Done When:** every surface that lists variants derives that list, and hand-editing one is caught.
**Verify:** adding a variant to the registry makes it appear on every listed surface after a regeneration, and editing a projected list by hand fails a gate.
**Do NOT:** hand-copy the six names into each surface. That is the duplication the registry was created to end.
**Why:** the registry only settles the naming question if the places people read are the places it reaches.
**Dep:** AGY-1736

## AGY-1750 -- Give mios.git an installer core that installs  (WS-DEPLOY | P0 | L)
**Goal:** The guided installer in this repository installs MiOS.
**What+How:** `resolve_install_core` now fails loudly (24217fb1) but still installs nothing; the real body is `do_install_core` at `mios-bootstrap/installation/mios-install.sh:1548` -- host detection, overlay merge, package phase, configurator, user profile. Either port it here, or delete this repository's copy and exec the bootstrap one. Then remove `installation/mios-install.sh` and `installation/mios-install.ps1` from `[bootstrap.sync].not_mirrored` so the two copies are compared from now on.
**Where:** `installation/mios-install.sh, installation/mios-install.ps1, usr/share/mios/mios.toml ([bootstrap.sync])`
**Done When:** `_install_core fhs` changes a scratch host and `_install_core bootc` produces a bootc deployment, and the two copies can no longer diverge unnoticed.
**Verify:** on a scratch Fedora host, run `_install_core fhs` then `ls /usr/share/mios/mios.toml && systemctl cat mios-ai-firstboot.timer`; both resolve only if the overlay landed. Re-run against a machine where the SSOT is absent and observe a non-zero exit.
**Do NOT:** accept a `--dry-run` that resolves a command string as proof. A resolved plan is not an install; that is the exact defect 24217fb1 removed.
**Why:** an operator following `installation/README.md` currently reaches a hard failure or, upstream, the wrong mode -- there is no path in this repository that installs the system it describes.
**Dep:** none

## AGY-1751 -- Stop mios-bootstrap swallowing the install mode  (WS-DEPLOY | P0 | S)
**Goal:** `_install_core bootc` runs bootc.
**What+How:** `TYPE=""` at :118, the lenient `*)` branch at :134-144 pushes a positional `bootc` into `PASSTHROUGH` and breaks, and :1605 dispatches `do_install_core "${TYPE:-fhs}"`. Every positional mode therefore degrades to fhs. Read the mode from the positional tail the way mios.git now does, and reject a mode that is neither `fhs` nor `bootc` instead of silently defaulting.
**Where:** `C:\mios-bootstrap\installation\mios-install.sh (:118, :134-144, :1605)`
**Done When:** the requested mode is the mode that runs, and an unknown mode is an error.
**Verify:** `bash installation/mios-install.sh _install_core bootc --dry-run` and `... _install_core fhs --dry-run` print different plans; `... _install_core banana` exits non-zero. Failure of the first is the proof the bug is still there.
**Do NOT:** verify with `--type bootc` only. The broken form is the positional one, and it is the form :218 generates.
**Why:** the offline kickstart path routes through this file, so an operator asking for the immutable install currently gets the mutable one and is told it succeeded.
**Dep:** none

## AGY-1752 -- One build output directory, derived, read by every consumer  (WS-DEPLOY | P0 | M)
**Goal:** Artifacts land where the things that consume them look.
**What+How:** Four consumers disagree: `iso:` writes `build/iso`, `usb-installer:` globs `build/*.iso build/bootiso/*.iso`, `verify-images` globs a third set, `stage-mios-repo.sh:167` a fourth -- and all four are wrong under bootc-image-builder's per-type layout. Declare the output root and the per-type subdirectory in the SSOT and project it into every consumer, the way ports already are.
**Where:** `Justfile, installation/stage-mios-repo.sh, build-mios.ps1, usr/share/mios/mios.toml ([deploy])`
**Done When:** one declared path, and changing it in the SSOT moves every consumer.
**Verify:** build one ISO, then `just usb-installer` and `just verify-images` both find it and `installation/stage-mios-repo.sh` stages it. Change the SSOT path, regenerate, and confirm all four moved together; hand-editing one back fails a gate.
**Do NOT:** add `build/iso/*.iso` to each glob by hand. Four hand-maintained copies of one fact is the defect, not the fix.
**Why:** `just all` currently dies at `usb-installer` after hours of building, because the recipe cannot see the ISO its own dependency produced.
**Dep:** none

## AGY-1753 -- Make verify-images fail when there is nothing to verify  (WS-DEPLOY | P1 | S)
**Goal:** The gate `just publish` depends on cannot pass over an empty set.
**What+How:** `verify-images` ends `[ "$fail" -eq 0 ]`, which is true when the loop matched nothing; run against an empty `build/` it prints `0 artifact passed, 0 failed` and returns 0, and `publish: all verify-images` proceeds. Require a minimum artifact set derived from the SSOT deploy formats, and fail when any declared format produced nothing.
**Where:** `Justfile (verify-images, publish), usr/share/mios/mios.toml ([deploy.formats])`
**Done When:** an empty or partial build cannot reach `podman push`.
**Verify:** `rm -rf build && just verify-images` exits non-zero naming the missing formats. If it exits 0, the gate is still the one that shipped.
**Do NOT:** count files without checking them against the declared format list -- a stale qcow2 from a previous run would satisfy a bare count.
**Why:** the only thing standing between an empty build tree and a publish is a test that returns success over zero files.
**Dep:** AGY-1752

## AGY-1754 -- Stage the Path-B payload where its own boot entry points  (WS-DEPLOY | P0 | M)
**Goal:** The kickstart and the image archive are on the stick, at the paths the boot entry names.
**What+How:** `stage-mios-repo.sh` creates `$REPO_MP/ventoy/` and writes only `mios-loopback.cfg`; nothing anywhere copies `usr/share/mios/ventoy/mios-oci-install.ks`, which the menu boots as `inst.ks=hd:LABEL=<repo>:/ventoy/mios-oci-install.ks`. Copy it. Stage the oci-archive to the `/mnt/mios-repo/mios-latest.tar` path `tools/install.sh:12` defaults to. And make the stager exit non-zero when a required payload is absent instead of warning and printing DONE.
**Where:** `installation/stage-mios-repo.sh, usr/share/mios/ventoy/mios-oci-install.ks, tools/install.sh`
**Done When:** a staged stick carries the kickstart and the archive, and a stager run that cannot stage them fails.
**Verify:** run the stager against empty mount points and capture `$?` -- it must be non-zero. Run it with both artifacts present and assert `find $REPO_MP -type f` lists `ventoy/mios-oci-install.ks` and `mios-latest.tar`. Today the first case exits 0 after two WARNs.
**Do NOT:** count the copy buried in the `git archive HEAD` dump under `repos/MiOS/usr/share/mios/ventoy/`. Anaconda fetches from the path in the boot argument and will not find it there.
**Why:** the immutable install is fully written and has never had its inputs delivered to the medium.
**Dep:** AGY-1752

## AGY-1755 -- Put the immutable-install entry in the menu Ventoy actually loads  (WS-DEPLOY | P0 | M)
**Goal:** The operator can select the bootc install from the boot menu.
**What+How:** `mios-loopback.cfg` is rendered onto the MiOS-Repo partition under a name Ventoy does not read, and `cat/resources/ventoy/ventoy_grub.cfg` -- the menu that is copied onto the Ventoy partition and is loaded -- chainloads Fedora-Server, MiOS-Xbox, MiOS_PE and SystemRescue with no MiOS entry at all. Emit one menu, from the SSOT labels, onto the Ventoy partition, containing the immutable entry.
**Where:** `installation/stage-mios-repo.sh, C:\mios-bootstrap\cat\resources\ventoy\ventoy_grub.cfg, C:\mios-bootstrap\installation\MiOS-Cat.bat`
**Done When:** booting the medium shows an "Install MiOS (Immutable bootc)" entry that loads.
**Verify:** build a Ventoy-formatted disk image, boot it under QEMU/OVMF, and observe the entry under F6 and its kickstart being fetched. This needs a VM; nothing short of a boot is evidence.
**Do NOT:** assert the file exists at the expected path and call it reachable. `ventoy_grub.cfg` exists, is copied to the right partition, and still offers no way to install MiOS.
**Why:** two locks on one door -- the payload is missing and the menu that would open it is not the menu that loads.
**Dep:** AGY-1754

## AGY-1756 -- Collapse the two hand-maintained boot menus into one generator  (WS-DEPLOY | P2 | S)
**Goal:** One boot menu, generated, with no dead twin.
**What+How:** `cat/loopback.cfg` (35 lines, `@@REPO_LABEL@@` placeholders) has no reader; `stage-mios-repo.sh:72-96` carries an independent inline copy of the same three menuentries, and the AI-hint on the dead file instructs maintainers to edit the dead one. Render from the SSOT labels, delete the copy that loses.
**Where:** `cat/loopback.cfg, installation/stage-mios-repo.sh (render_loopback)`
**Done When:** one source produces the menu and no unreferenced menu template remains.
**Verify:** `grep -rn loopback.cfg` finds a generator and its output, not two templates; changing `[cat.repo_partition].label` changes the rendered menu.
**Do NOT:** keep the file "for reference". An unreferenced template that instructs editors to edit it is how the two drifted apart.
**Why:** whichever of the two a maintainer edits, there is a coin-flip chance it is the one nothing reads.
**Dep:** AGY-1755

## AGY-1757 -- Join MiOS-Cat's stage verb to the archive chain that exists  (WS-DEPLOY | P0 | M)
**Goal:** `stage` produces the archive it claims to produce.
**What+How:** `cat/lib/cat.sh:68-69` is `echo "Saving localhost/mios:latest..."` above a commented-out `podman save`; :71-72 echo model and mirror work that has no body; `MiOS-Cat.psm1:78-79` is the byte-twin. The real producer is `Justfile:299-302`. Call it, and write to the MiOS-Repo path the installer reads. Also read the disk threshold from `[cat.data_partition].min_disk_gb` rather than the hardcoded 128.
**Where:** `C:\mios-bootstrap\cat\lib\cat.sh, C:\mios-bootstrap\cat\lib\MiOS-Cat.psm1, Justfile (oci-archive)`
**Done When:** running `stage` leaves a byte-identical archive on the stick at the path `tools/install.sh` opens.
**Verify:** `sha256sum` of the staged file equals `sha256sum build/oci-archive/mios-<version>.tar`. If `stage` prints "Saving..." and no file appears, it is unchanged.
**Do NOT:** uncomment the `podman save` line and stop. It writes to `MiOS-Data/images/`, while `tools/install.sh:12` reads `/mnt/mios-repo/mios-latest.tar` -- the archive would be present and still invisible to the installer.
**Why:** the verb whose entire job is producing the offline payload currently prints that it did.
**Dep:** AGY-1752

## AGY-1758 -- Install MiOS from the medium onto bare metal with the network absent  (WS-DEPLOY | P0 | L)
**Goal:** One observed end-to-end offline install.
**What+How:** With the payload staged and the menu reachable, boot the medium on a machine with no network interface attached, select the immutable install, and let `tools/install.sh` run `bootc install to-disk` from the staged archive. Record what was needed.
**Where:** `usr/share/mios/ventoy/mios-oci-install.ks, tools/install.sh, installation/stage-mios-repo.sh`
**Done When:** a machine that had no MiOS boots into the carried image.
**Verify:** after reboot, `bootc status` names the MiOS image and `cat /etc/os-release` reports the MiOS release. Physically detach the NIC (or attach none to the VM) before starting; if the install needs it, it fails and that is the finding.
**Do NOT:** call a booted medium an install, and do not test with a NIC present but unconfigured -- `mios-kickstart.cfg:97` falls back to `git clone https://github.com/mios-dev/mios-bootstrap.git`, which an unconfigured-but-present interface can still satisfy on some networks.
**Why:** the portable edition exists so a disconnected machine can become a MiOS machine, and that has never once been observed.
**Dep:** AGY-1755

## AGY-1759 -- Resolve the root redirector that points at nothing  (WS-DEPLOY | P2 | S)
**Goal:** The documented curl-pipe entry point either works or is gone.
**What+How:** `install.sh` execs `build-mios.sh` alongside it. `ls build-mios.*` at the root returns only `build-mios.ps1`, and no `build-mios.sh` exists in either repository's root -- mios-bootstrap's own `install.sh` runs `cat/MiOS-Cat.sh` instead, and its `build-mios.sh` is now a 21-line redirector into `installation/mios-install.sh`. `UNIFY.md:130-133` describes a shape neither repo has. Point it at a real target or delete it and update the docs and the role-marker gate.
**Where:** `install.sh, installation/UNIFY.md, automation/98-drift-checks.sh (check_installer_family_roles)`
**Done When:** every documented entry point resolves, or is no longer documented.
**Verify:** `bash install.sh --help` exits 0 having reached a real installer, or the file and every reference to it are gone and `check_installer_family_roles` still passes. Today it exits 1 with "build-mios.sh not found alongside this redirector".
**Do NOT:** fix the error message. The file is a redirector to a target that exists nowhere; a clearer failure is still a failure.
**Why:** it carries an installer role marker and a curl-pipe docstring, so it reads as a supported way in.
**Dep:** none

## AGY-1760 -- Make the FHS installer find the tree it is supposed to install  (WS-DEPLOY | P1 | S)
**Goal:** The overlay installer applies an overlay.
**What+How:** `automation/install-fhs.sh:19` sets `REPO_ROOT` to its own directory, `automation/`, where `usr etc var srv` do not exist -- so the loop at :23-30 never runs and the script prints `[ OK ] MiOS system installer complete`. Resolve the repository root, and fail when none of the overlay directories are found. Fix the GHCR reference at :10, which prints `ghcr.io/MiOS-DEV/mios:latest` where GHCR paths must be lowercase and the SSOT says `ghcr.io/mios-dev/mios:latest`.
**Where:** `automation/install-fhs.sh, automation/install.sh`
**Done When:** running it either syncs the overlay or exits non-zero saying it found nothing to sync.
**Verify:** run it as root with rsync stubbed on PATH and count the stub invocations -- it must be non-zero, and must be zero-with-exit-1 when run from a directory with no overlay. Today it makes zero rsync calls and exits 0.
**Do NOT:** treat "nothing in the tree calls it" as a reason to skip. It carries `MIOS_INSTALLER_ROLE=fhs-overlay-installer` and is documented as the FHS installer, so it will be reached eventually.
**Why:** it is the second installer in this repository that reports completion having done nothing.
**Dep:** none

## AGY-1761 -- Ship an image that can update itself  (WS-DEPLOY | P0 | M)
**Goal:** The published image has exactly one working OS update mechanism.
**What+How:** `automation/50-uupd-installer.sh` disables `bootc-fetch-apply-updates.timer` and `rpm-ostreed-automatic.timer` unconditionally, then only warns when `uupd.timer` is missing. Four layers swallow the failure: a skip-if-unavailable repo, `install_packages_strict` also passing `--skip-unavailable` (`automation/lib/packages.sh:181-190`), the warn, and `mios.toml:10159` `fatal = false`. In `ghcr.io/mios-dev/mios:latest` the audit found no uupd package, no uupd units, and the bootc timer disabled. Do not disable the fallback until the replacement is verified present; make the phase fatal; add a gate over the built image.
**Where:** `automation/50-uupd-installer.sh, automation/lib/packages.sh, usr/share/mios/mios.toml ([pipeline], [packages.updater]), automation/98-drift-checks.sh`
**Done When:** every published image has an enabled OS update timer, and a build that would ship without one fails.
**Verify:** `podman run --rm <image> systemctl is-enabled uupd.timer` returns `enabled` (or the bootc timer does). Remove `uupd` from `[packages.updater]` and confirm the build turns red -- if it still goes green, the fatal flag is not doing its job.
**Do NOT:** assert that `[packages.updater]` lists uupd. The SSOT says that today and the package is not in the image; asserting the config instead of the artifact is what let this ship.
**Why:** every machine installed from this image is frozen at install time and nothing anywhere says so.
**Dep:** none

## AGY-1762 -- Make `mios update` reach bootc on every surface  (WS-DEPLOY | P1 | S)
**Goal:** The update verb updates the operating system.
**What+How:** `[verbs.update]` has a description, a `surface`, and no `cmd`. Interactively, `etc/profile.d/mios-verbs.sh:95-101` routes it to `mios pull`, a `git reset --hard` of a Windows drive. Non-interactively `mios-verbs.sh:3` returns early and `/usr/bin/mios` handles it -- and `KNOWN_VERBS` at :317-348 has no `update`, so the string is sent to the Hermes agent as a prompt. `usr/bin/mios-update` is a correct bootc wrapper nothing calls. Give the verb a `cmd`, add it to `KNOWN_VERBS`, and make `check_verb_backends` treat a `cmd`-less verb as a violation instead of skipping it.
**Where:** `usr/share/mios/mios.toml ([verbs.update]), etc/profile.d/mios-verbs.sh, usr/bin/mios, automation/98-drift-checks.sh (check_verb_backends)`
**Done When:** `mios update` reaches `bootc` from an interactive shell, a script, and an ssh command.
**Verify:** `sh -c 'mios update --check'` reaches `bootc upgrade --check`; deleting the `cmd` key turns `check_verb_backends` red. If the gate stays green with the key gone, the skip is still there.
**Do NOT:** test only in an interactive shell. That is the one context where the shell function exists, and it is not the one that sends the verb to an LLM.
**Why:** the documented way to update MiOS currently pulls a git repository or asks a language model about the word "update".
**Dep:** AGY-1761

## AGY-1763 -- Make the required health checks capable of failing  (WS-DEPLOY | P0 | M)
**Goal:** A required greenboot check that detects a fault reports one.
**What+How:** `15-composefs-verity.sh` prints `ERROR: composefs requested but not active` and falls through to `exit 0`; `/usr/lib/ostree/prepare-root.conf` is present with `enabled = verity` in the published image, so this branch is live. `50-mios-core.sh` is `command -v miosd || exit 0`, and miosd ships only at `/usr/libexec/mios/miosd`, off PATH, with no `greenboot` subcommand among its 19. Make the composefs failure exit 1. For the core check, either give miosd a real greenboot subcommand and an absolute path, or remove the file -- a required check that no-ops is worse than none.
**Where:** `usr/lib/greenboot/check/required.d/15-composefs-verity.sh, etc/greenboot/check/required.d/50-mios-core.sh, src/mios-rs/miosd/`
**Done When:** each required check fails on the fault it names and passes only on a healthy system.
**Verify:** in a container with a stub `fsverity` on PATH and no composefs mount, `bash 15-composefs-verity.sh` must exit 1 -- it exits 0 today, which is the reproduction. `50-mios-core.sh` must either run a real check or not be in `required.d`.
**Do NOT:** add `exit 1` and verify only that a healthy host still passes. The healthy path was never in question; the fault path is.
**Why:** the checks that gate a rollback currently pass over the exact conditions they were written to catch.
**Dep:** none

## AGY-1764 -- Watch a greenboot rollback happen once  (WS-DEPLOY | P1 | M)
**Goal:** The automatic rollback is observed, not inferred.
**What+How:** greenboot 0.16.3, bootc 1.16.7 and `08_greenboot.cfg` are all present and correct, `greenboot-set-rollback-trigger.service` is enabled, and no rollback has ever been seen: in a container `greenboot health-check` prints "Container environment detected; skipping reboot and rollback handling", `/boot/grub2/grubenv` does not exist, and `grep -rn rollback tests/` returns zero. Deploy an image with a deliberately failing required check on real hardware or a UEFI VM and let the boot counter run out. Note the ordering hazard: `required.d` runs lexically and greenboot stops at the first failure, so `10-mios-composefs.sh` failing means `15-` and `50-` never execute.
**Where:** `usr/lib/greenboot/, etc/greenboot/, tests/`
**Done When:** a bad deployment rolls back by itself and the previous deployment boots.
**Verify:** after the third failed boot, `bootc status` names the previous image and the journal records the rollback. A run in a container proves nothing -- greenboot skips rollback handling there by design.
**Do NOT:** count `systemctl is-enabled greenboot-healthcheck.service` as evidence. Enablement is configuration; the rollback is the behaviour.
**Why:** rollback is the safety net under every future update, and nobody has seen it catch anything.
**Dep:** AGY-1763

## AGY-1765 -- Guard the ISO credential the way the sibling recipes do  (WS-DEPLOY | P1 | S)
**Goal:** The installer medium cannot ship an empty password.
**What+How:** `Justfile:251` and `:267` guard `MIOS_USER_PASSWORD_HASH` for qcow2 and vhdx; the `iso` recipe at :234-247 does not, and its sed at :237 uses the `:-` empty default. Run unset, it produces `user --name=mios --groups=wheel,render,video --iscrypted --password=` and a malformed sshkey line. Add the same guard, and make `check_replaceme_mount_substitution` assert the substituted value is non-empty rather than asserting the recipe contains `sed`.
**Where:** `Justfile (iso), config/artifacts/iso.toml, automation/98-drift-checks.sh (check_replaceme_mount_substitution)`
**Done When:** an ISO cannot be built without a real hash, and the gate notices when the guard is removed.
**Verify:** `MIOS_USER_PASSWORD_HASH= just iso` exits non-zero before invoking the builder; delete the guard and the gate must turn red. The gate passes today regardless, because the recipe contains `sed -e`.
**Do NOT:** check that the placeholder string is gone from the rendered config. An empty substitution also removes the placeholder.
**Why:** it is a wheel-group account with an empty crypted password field on an installer medium.
**Dep:** AGY-1752

## AGY-1766 -- Stop the partition-label gate substituting a literal for the SSOT  (WS-GATES | P0 | S)
**Goal:** The label gate reads the SSOT or fails.
**What+How:** `98-drift-checks.sh:4668` resolves the label as `grep -A 5 '\[cat\.repo_partition\]' â¦ | â¦ || echo "MiOS-Repo"` under `set -euo pipefail`. When the table is missing, grep returns 1, pipefail propagates, the fallback fires, and the gate compares consumers against a hardcoded literal. Parse with `tomllib` as `tools/check-variant-registry.py` does, treat a missing key as a violation, and extend the consumer set to `usr/share/mios/ventoy/mios-oci-install.ks` and `cat/loopback.cfg`, which also carry the label.
**Where:** `automation/98-drift-checks.sh (check_repo_partition_label_ssot), tests/drift-gate-negatives.sh`
**Done When:** removing or renaming `[cat.repo_partition]` turns the gate red.
**Verify:** four exit codes with `$?` captured directly -- clean-before 0, rename the table and run the un-repaired gate (0, the reproduction), clean-after 0, rename again after repair (1). Land a negative test and confirm it fails against the pre-repair gate.
**Do NOT:** keep the `|| echo "MiOS-Repo"` as a "safe default". A default is what makes it unfailable; a gate with no data must go red.
**Why:** it will report green through the entire variant rename it exists to police.
**Dep:** none

## AGY-1767 -- Stop a comment satisfying the offline-install gate  (WS-GATES | P0 | S)
**Goal:** The offline-install invariant is asserted against executable code.
**What+How:** `:4599` runs `grep -q '\--transport oci-archive' tools/install.sh` with no comment filter, and that string occurs only on line 3, inside the AI-hint. The real command at :77 carries no `--transport` flag at all. `:4589` returns 0 when the file is absent. The sibling network-token filter at :4605 applies `grep -v '^\s*#'` to `grep -n` output whose lines start with `NN:`, so it never excludes anything. Strip comments before asserting, make an absent file a violation, fix the filter, and extend the scope to `usr/share/mios/ventoy/mios-kickstart.cfg`, whose :97 `git clone https://github.com/mios-dev/mios-bootstrap.git` is the actual network call in the offline path.
**Where:** `automation/98-drift-checks.sh (check_offline_install_invariant), tools/install.sh, usr/share/mios/ventoy/mios-kickstart.cfg, tests/drift-gate-negatives.sh`
**Done When:** gutting the installer body turns the gate red, and a network call anywhere in the offline path is a violation.
**Verify:** replace `tools/install.sh`'s body with `echo "I install nothing"; exit 0`, keeping line 3, and capture `$?` -- it must be 1. It is 0 today. Then delete the file and confirm 1 as well.
**Do NOT:** satisfy the repaired gate by adding `--transport oci-archive` to the comment or to a dry-run echo. The assertion must land on the line that runs.
**Why:** this is the gate the entire offline claim rests on, and it currently reads a docstring.
**Dep:** none

## AGY-1768 -- Replace six hardcoded Rust passes and the parity harness that hides them  (WS-GATES | P0 | M)
**Goal:** The native runner does not certify the deploy plane it never inspects.
**What+How:** `src/mios-rs/miosd/src/drift/deploy.rs` is 82 lines and all six `run(&self, _ctx)` bodies are a single `Verdict::Pass(...)`, registered at `mod.rs:297-302` and counted into the summary. `Verdict::Skip` exists and is unused. Implement them against the bash equivalents, or return `Skip` and stop counting them as passes. Then make `tests/drift-parity.sh` compare: it currently runs both sides with `|| true`, greps the Rust log, deletes both logs and prints "Parity check completed successfully" without ever comparing them.
**Where:** `src/mios-rs/miosd/src/drift/deploy.rs, src/mios-rs/miosd/src/drift/mod.rs, tests/drift-parity.sh`
**Done When:** each native check either inspects the tree or declares itself skipped, and a disagreement between the two runners is an error.
**Verify:** break one deploy input (delete `tools/install.sh`) and confirm bash goes red and the native runner agrees; make them disagree deliberately and confirm `drift-parity.sh` exits non-zero. It exits 0 on every input today.
**Do NOT:** implement the six and leave the parity harness printing success. An uncompared parity check is how six hardcoded passes survived registration.
**Why:** six checks named after the deploy plane's most broken surfaces return a literal string.
**Dep:** AGY-1767

## AGY-1769 -- Make the bake-budget gate measure size  (WS-GATES | P0 | M)
**Goal:** The gate compares projected image size to the SSOT budget it names.
**What+How:** `budget` is parsed from `[build.bake].runner_disk_budget_gb` and then appears only inside the failure message; the sole live assertion is a hardcoded `[[ "$count" -gt 30 ]]` over TSV row count. Twelve mutations -- budget 1, 0, -5, a string, key deleted, 321 images, a 20.7 TB TSV, an empty TSV, a deleted TSV, malformed SSOT, absent SSOT, broken python3 -- all returned 0. The clean tree already violates the stated property: `bound-images.tsv` lines 13-14 are the ~20 GB sglang and ~27 GB vllm images against a 40 GB budget. Record real sizes at SBOM time, sum the Day-0 tier, compare to the budget, and make every missing input a violation.
**Where:** `automation/98-drift-checks.sh (check_bake_budget), usr/share/mios/artifacts/sbom/bound-images.tsv, usr/share/mios/mios.toml ([build.bake]), tests/drift-gate-negatives.sh`
**Done When:** lowering the SSOT budget below the measured total turns the gate red, and raising it above turns it green.
**Verify:** set `runner_disk_budget_gb = 1` and capture `$?` -- it must be 1. It is 0 today. Also confirm the gate goes red on the current tree until the firstboot tier is excluded, because the Day-0 set already exceeds 40 GB.
**Do NOT:** keep the row-count proxy and add the budget to the message. A row count is not a size, it includes the header line, and it is blind to `[build.bake].core` entirely.
**Why:** it advertises a guard over the exact publish-capacity failure that has already blocked this project, while the operator-facing knob is decorative.
**Dep:** none

## AGY-1770 -- Wire the package-registry switch to the gate that reads it  (WS-GATES | P2 | S)
**Goal:** Turning the feature on in the SSOT arms its gate.
**What+How:** `98-drift-checks.sh:482-487` gates on `${MIOS_PACKAGE_REGISTRY:-false}`. Nothing writes that variable: it appears only in this check, `automation/lib/generate-packages.sh:6` and `usr/libexec/mios/mios-registry`, all readers; it is in no globals resolver and in no env-snapshot output. Flipping `mios.toml:5545 package_registry = true` with no `registry.json` anywhere returns 0 and prints "package registry dormant". Resolve the flag from `[ai].package_registry` through the shared resolver, keep the env var as an override, apply the same fix to the generator, and make the disabled branch assert the dormant invariant rather than returning 0.
**Where:** `automation/98-drift-checks.sh (check_package_registry), automation/lib/generate-packages.sh, usr/share/mios/mios.toml ([ai].package_registry, [testing].negative_coverage_exempt)`
**Done When:** flipping the SSOT key alone changes the gate's behaviour.
**Verify:** set `[ai].package_registry = true` with no registry present and capture `$?` -- it must be 1. It is 0 today with the key flipped, and 1 only when the env var is also set by hand.
**Do NOT:** leave it on the negative-coverage exemption list. The exemption is why nobody noticed the key was disconnected.
**Why:** the gate will stay silently off on the day someone turns the feature on, which is the only day it matters.
**Dep:** none

## AGY-1771 -- Make the DB seed gate measure coverage, and stop its test faking the proof  (WS-GATES | P0 | M)
**Goal:** A new unseeded SSOT section is caught.
**What+How:** `98-drift-checks.sh:5294` reads `if "kv_sections = [k for k in data.keys()" not in seed_code and sec_name not in seed_code:`. `seed-db-config.py:82` contains that literal, so the first conjunct is False for every section and `and` short-circuits -- `uncovered` is unconditionally empty. Adding new top-level tables, narrowing the seeder to an allowlist, or emptying its loop while keeping the line as a comment all pass; deleting either audited file passes via the `isfile` guard at :5279. Factor the seeder's selection into a callable, import it, and diff the selected set against the SSOT keys. Make the missing-file guard a violation. And delete `tests/drift-gate-negatives.sh:1715` -- the `sed` that sabotages the seeder is the only reason the negative test passes; without it the test dies with "Check_db_seed_coverage passed despite unseeded section in mios.toml".
**Where:** `automation/98-drift-checks.sh (check_db_seed_coverage), usr/libexec/mios/seed-db-config.py, tests/drift-gate-negatives.sh`
**Done When:** appending a new top-level table to the SSOT turns the gate red without touching the seeder.
**Verify:** append `[zzz_probe]\nfoo = "bar"` to `usr/share/mios/mios.toml` and capture `$?` -- it must be 1. It is 0 today. Expect the repair to surface roughly a dozen genuinely unreachable sections needing triage.
**Do NOT:** mutate the seeder to make the test pass. That is precisely the dodge the existing negative test performs, and it is why this survived.
**Why:** it is the sole guard on the SSOT-to-config path the operator-defined model rests on, and its negative test manufactures the evidence that it works.
**Dep:** none

## AGY-1772 -- Adjudicate the 66 inconclusive gates and the nine unrefuted claims  (WS-GATES | P0 | L)
**Goal:** Every registered gate has a recorded, reproducible verdict.
**What+How:** The gate audit covered 202 checks and reached 120 effective, 16 claimed unable to fail, 66 inconclusive. Four claims were adjudicated (two repaired, two open: `check_bake_budget`, `check_package_registry`), three were overturned, and **nine were never refuted** because two refutation agents died -- of those only `check_no_hardcode` and `check_unit_security` are still named in the artifact. The identities of the 66 inconclusive gates were lost with the synthesis agent and must be re-derived. For each gate: mutate its own declared input, record clean-before / mutated-before / clean-after / mutated-after with `$?` captured directly, and land a negative test proven to fail against the un-repaired gate.
**Where:** `automation/98-drift-checks.sh, tests/drift-gate-negatives.sh, usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** the 202-row index carries a verdict per gate, each backed by a mutation whose exit codes are recorded.
**Verify:** for every gate re-verified, `mutated-before = 0` and `mutated-after = 1`. A gate with no recorded mutation is inconclusive, not effective, and must be listed as such.
**Do NOT:** count "the gate ran and printed passed" as effective, and do not capture the exit code through a pipe -- that returns the last command's status and turns this task into another check that cannot fail.
**Why:** the deploy audit's worst findings all sat behind gates the suite reported green, and two thirds of the suite is still unmeasured.
**Dep:** AGY-1730

## AGY-1773 -- Empty or justify the negative-coverage exemption list  (WS-GATES | P1 | M)
**Goal:** No gate is excused from proving it can fail.
**What+How:** `[testing].negative_coverage_exempt.exempt` holds 55 entries, and it contains exactly the gates most suspected of being unfailable -- `check_no_hardcode`, `check_unit_security`, `check_package_registry`, `check_hummingbird`, `check_greenboot`, `check_greenboot_enablement`. 157 test functions cover 202 gates. Write a negative test for each exempt gate, or record in the SSOT a reason a reader can check, and delete the rest.
**Where:** `usr/share/mios/mios.toml ([testing].negative_coverage_exempt), tests/drift-gate-negatives.sh, automation/98-drift-checks.sh (check_negative_test_coverage)`
**Done When:** the exemption list is empty, or every remaining entry names a specific technical reason.
**Verify:** for each removed exemption, the new test fails when run against the un-repaired gate. Count of tests is not evidence; the failure against the broken gate is.
**Do NOT:** add a test that runs the gate and dies only if the gate fails. That is a rubber stamp, and `test_no_duplicate_value_key` and `test_db_seed_coverage` were both exactly that.
**Why:** the exemption list is the mechanism by which unfailable gates stay unfailable and read as covered.
**Dep:** AGY-1772

## AGY-1774 -- Reconcile the two [cat] tables and the two MiOS-Cat.bat copies  (WS-DEPLOY | P1 | M)
**Goal:** One statement of the portable edition's configuration across both repositories.
**What+How:** `usr/share/mios/mios.toml:9478` `[cat]` has seven keys; `C:\mios-bootstrap\mios.toml:1624` `[cat]` has fourteen, including `monitor_enabled`, `filesystem`, `partition_scheme`, `secure_boot`, `build_xbox`, `gaming_optimize`, `build_driver` and `log_path`, which mios.git does not have -- plus `[cat.live_chat]` and `[cat.sysrescue]`, and `ar_source_label = "MiOS-Cat"` at :1678. The two `installation/MiOS-Cat.bat` copies are untracked (`.gitignore:63`) and 51 lines apart. `cat/resources/ventoy/Render-Sysrescue.ps1:18` defaults `$PartitionLabel` from `[cat].partition_label`, a key that exists in neither file. Decide which repository owns which key, mirror deliberately, and delete the dangling reference.
**Where:** `usr/share/mios/mios.toml ([cat]), C:\mios-bootstrap\mios.toml ([cat]), installation/MiOS-Cat.bat (both copies), C:\mios-bootstrap\cat\resources\ventoy\Render-Sysrescue.ps1`
**Done When:** one owner per key, the two `.bat` files are one file or one is gone, and no consumer reads a key that does not exist.
**Verify:** a script that parses both files with `tomllib` and diffs the `[cat]` key sets reports no unexplained difference; `grep -rn 'partition_label'` finds no reader without a definition.
**Do NOT:** mirror the tables wholesale to make a diff clean. Some of those keys are Windows-launcher-only by design; the task is deciding ownership, not making the files identical.
**Why:** Law 15 requires both repositories to agree on shared SSOT surfaces, and a rename across a divergence produces two half-renamed trees.
**Dep:** AGY-1766

## AGY-1775 -- Rename the portable variant from Cat to Field  (WS-VARIANT | P2 | L)
**Goal:** The portable edition has a name that is not a coreutils command or someone else's project.
**What+How:** Rename `mios-cat`/`MiOS-Cat` to `mios-field`/`MiOS-Field` across `[variants.entries]`, the `[cat]` config tables in both repositories, the partition and volume labels, the launchers, the gates, the generated env surfaces and the docs, and rename `cat/` to `field/` in mios-bootstrap. `MIOS-FIELD` is 10 characters, inside the 11-character FAT32/exFAT volume-label cap that disqualifies Courier and Outpost. Regenerate `globals.sh`/`globals.ps1`, `env-baseline.txt`, `manifest.json` and the man pages rather than hand-editing them.
**Where:** `usr/share/mios/mios.toml ([cat], [variants]), C:\mios-bootstrap\mios.toml, installation/, cat/, C:\mios-bootstrap\cat\, automation/98-drift-checks.sh, tools/render-manpages.py, usr/share/doc/mios/`
**Done When:** no surface names the old variant, and the volume label the autorun scanner mounts is the new one.
**Verify:** `tools/check-variant-registry.py` exits 0 after the rename and exits 1 on either half alone -- proven: renaming the key but not the title gives "title 'MiOS-Cat' and key 'mios-field' are not the same name in two registers"; renaming the title but not the config table gives "config names [field], which the SSOT does not define". Then flash a stick and confirm `blkid -L "MiOS-Field"` resolves on the target and the loopback and kickstart label paths still find the payload.
**Do NOT:** run a blind `sed s/cat/field/`. It breaks `MIOS_CATALOG_FAIL_MODE`, `check_drift_build_catalog`, `project_verb_catalog`, the `viking_cat` verb, `test_mios_build_catalog.py`, and every literal `cat` shell invocation. And do not rename before AGY-1766 lands -- `check_repo_partition_label_ssot` substitutes a hardcoded `MiOS-Repo` for a missing SSOT key and will report green through the entire operation.
**Why:** `dnf repoquery --file /usr/bin/cat` returns coreutils, the SSOT's own suffix rule says name the job not the medium, and the name is attached to a tool that downloads and repackages a 23 GB third-party MediCat archive.
**Dep:** AGY-1774

## AGY-1776 -- Carry the models, or stop claiming the medium does  (WS-DEPLOY | P2 | M)
**Goal:** The offline medium carries the weights it promises, or the promise is withdrawn.
**What+How:** `automation/73-model-prep.sh` is 152 lines with a top-level `exit 0` at :81; lines 83-152 are a second script with its own `set -euo pipefail` and `source lib/common.sh` that never runs, and there is no `74-*.sh` alternative. Neither stager carries weights: `grep -in model` returns nothing in `installation/stage-mios-repo.sh` and nothing in `mios-bootstrap/installation/MiOS-Cat.bat`. `[cat].models` is the prose string "see [ai].bake_models + [ai.vllm].bake_model", and `[ai.vllm]` has no `bake_model` key -- so the dead half would bake nothing if revived. Separately, `mios-llm-heavy-alt.container` gates on `/var/lib/mios/vllm/model/config.json`, so the copy target the launchers use would not feed the heavy lane anyway.
**Where:** `automation/73-model-prep.sh, installation/stage-mios-repo.sh, usr/share/mios/mios.toml ([cat].models, [ai.vllm]), usr/share/containers/systemd/mios-llm-heavy-alt.container`
**Done When:** a staged medium either contains the declared weights at a path the heavy lane reads, or the SSOT stops declaring them.
**Verify:** stage a stick and confirm the model files are present and that `mios-llm-heavy-alt.container`'s `ConditionPathExists` is satisfied after install. If the SSOT is instead corrected, `[cat].models` must name no model.
**Do NOT:** delete the `exit 0` at :81 and call it fixed. The revived half takes the empty-MODEL branch and bakes nothing, because the key it reads does not exist.
**Why:** an unreachable second half of a build script is dead code that reads as coverage, and the medium's stated purpose includes the weights.
**Dep:** AGY-1752
```

---

## 5. Naming

**Adopt `MiOS-Field` / `mios-field`.** It is the only candidate that survives all three hard constraints at once â `MIOS-FIELD` is 10 characters against the measured 11-character FAT32/exFAT volume-label cap (which rejects `MIOS-COURIER` and `MIOS-OUTPOST`), no Fedora package provides a `/usr/bin/field`, and "field service" names the whole job (install + deploy + update + repair, on site, offline) rather than one fifth of it or a property of the medium, which is the documented reason Mini became Metal. Runner-up `MiOS-Depot` loses on the same category error plus `depot build` sitting in the same terminal as MiOS's own `buildx bake`. `Cat` goes for three independently sufficient reasons: `dnf repoquery --file /usr/bin/cat` returns coreutils; "travels" is a property, not a job; and the tool downloads and repackages Jayro Jones's MediCat archive, which makes a confusingly similar name a redistribution exposure rather than an homage.

**Blast radius** (measured in the research): **392 occurrences across 50 files in `C:\MiOS`, 305 across 44 files in `C:\mios-bootstrap`.** The SSOT sides are `mios.toml:9478 [cat]` (7 keys) plus `[cat.repo_partition]`/`[cat.data_partition]` and `[variants.entries.mios-cat]` at `:10855`, against a *different, larger* `[cat]` at `mios-bootstrap/mios.toml:1624` (14 keys, plus `[cat.live_chat]`, `[cat.sysrescue]`, `ar_source_label` at `:1678`). The name is carried on real filesystem labels in `MiOS-Cat.bat:142/161/515` (both divergent copies), `Render-Sysrescue.ps1:18` and `mios-stage-icons.ps1:137`. Gates touched: `check_repo_partition_label_ssot` (body at `:4657-4683`, registry at `:4322`, call at `:5970`), the bootstrap path-candidate lists at `:2968-2973` and `:2993-2998`, the `medicat_stage` skip token at `:3104` and `tools/generate-names-registry.py:91`, and `conformance-grandfathered.list:410`. Generated surfaces to regenerate rather than edit: `globals.sh:426-433` and `:4086-4093`, `globals.ps1:451-458` and `:4111-4118`, `manifest.json:608/614`, `env-baseline.txt:391-398`/`:3911-3918`, `value-dup-report.tsv`, `referenced_names.txt:228`, and `usr/share/man/man7/mios-variants.7`. Docs: ADR-0008 (28 internal hits, plus five files that reference its filename), `TASKS.md` (29), `AGY-TASKS.md` (22), `ROADMAP.md` (18), `installation/README.md` (25). `MiOS-Repo` and `MiOS-Data` do not carry the variant name and must not change.

**Two sequencing constraints, both load-bearing.** The `[cat]` tables have already diverged between the repositories and the two `MiOS-Cat.bat` copies are 51 lines apart and untracked â reconciling that is a prerequisite (AGY-1774), not a side effect. And `check_repo_partition_label_ssot` must be repaired first (AGY-1766): its `|| echo "MiOS-Repo"` fallback under `set -euo pipefail` was reproduced on a shadow root reporting *"repo partition label consumers match"* with zero occurrences of `[cat.repo_partition]` left in the file. It will certify the entire rename as clean while it is half done.

**Untestable from here, stated as such:** whether a stick relabelled `MIOS-FIELD` still boots. That needs a real USB, `blkid -L` on the target, and a UEFI machine confirming the Ventoy `loopback.cfg` and `inst.ks=hd:LABEL=â¦` paths still resolve. The label-length results above *were* measured (`mkfs.vfat -F 32 -n` and `mkfs.exfat -n` against a 64 MiB image, with 12-character labels returning REJECT), so the cap is evidence; the boot is not.

## AGY-1780 -- Watch greenboot roll a deliberately broken AI plane back  (WS-RUNTIME | P1 | M) [from T-002]
**Goal:** A bad upgrade rolls itself back, observed rather than configured.
**What+How:** T-002 is marked done-by-code: the required-check scripts exist and `check_greenboot` passes. Neither fact establishes that a rollback happens, and the deploy audit found no evidence anyone has watched one. Boot a machine or VM on a known-good deployment, `bootc upgrade` to an image whose `mios-agent-pipe.service` is deliberately broken (a bad ExecStart is enough), reboot, and record the console and `journalctl -b -1 -u greenboot-healthcheck` output showing the health check failing and the previous deployment being restored. Then record the same for a HEALTHY boot, so the check is shown to distinguish the two. Land both transcripts under `usr/share/doc/mios/reference/`.
**Where:** `etc/greenboot/check/required.d/, usr/share/doc/mios/reference/, automation/98-drift-checks.sh (check_greenboot)`
**Done When:** two recorded boots exist -- one rolled back, one not -- from the same check set.
**Verify:** the transcript shows greenboot marking the boot failed and bootc reverting to the prior deployment, and `bootc status` after reboot names the OLD image digest; the healthy transcript from the same checks shows neither.
**Do NOT:** assert it from the unit files, from `check_greenboot` passing, or from a check script exiting non-zero in isolation. The claim is that the SYSTEM rolls back, and only a boot demonstrates that. Do not write the check scripts to always exit 0 to make the healthy case pass.
**Why:** rollback is the property that makes immutable updates safe to take automatically; untested it is a hope, and every deployed machine is one bad upgrade from being unreachable.
**Dep:** none

## AGY-1781 -- Prove every Quadlet is generated, including the ones carrying hand-edits  (WS-SYSTEMD | P1 | M) [from T-005]
**Goal:** No container unit is hand-maintained, and the gate proves it.
**What+How:** T-005 is marked done-by-code and `check_pod_quadlets` reports all 26 units matching the SSOT. Establish whether that covers every shipped `.container`/`.pod`/`.network`/`.volume` unit or only the 26 the generator knows: list every such file under `usr/share/containers/systemd/`, diff that set against what `tools/generate-pod-quadlets.py` emits, and account for every file in neither set. For any unit carrying a hand-edit that encodes real behaviour, move that behaviour into `[units]`/`[containers.*]` in the SSOT before the file is regenerated -- do not delete the edit.
**Where:** `tools/generate-pod-quadlets.py, usr/share/containers/systemd/, usr/share/mios/mios.toml, automation/98-drift-checks.sh`
**Done When:** the generated set and the shipped set are identical, with no file in one and not the other.
**Verify:** `diff <(ls usr/share/containers/systemd/) <(python3 tools/generate-pod-quadlets.py --list)` is empty, and hand-editing any one shipped unit makes `check_pod_quadlets` exit non-zero.
**Do NOT:** regenerate wholesale and call the diff clean. Some shipped units carry hand-edits that encode behaviour nobody wrote down; regenerating over them loses it silently, and the gate will then be green on a system that no longer works.
**Why:** a unit outside the generator's set is a unit the SSOT does not describe, and the gate reporting "all 26 match" says nothing about the ones it never looked at.
**Dep:** none

## AGY-1782 -- Make check_agent_schema fail on the omission it was written for  (WS-A2 | P1 | S) [from T-007]
**Goal:** A malformed agent block cannot merge.
**What+How:** T-007 is marked done-by-code and `check_agent_schema` reports the contract satisfied. That is the same shape as three gates already found this session that were green while the property they named was violated. Establish which it is: remove `health_gate` from one `[agents.*]` block, run the check, and record the exit code. Do the same for each field the schema claims to require, and for an agent block that is empty. Every omission the task named must produce a non-zero exit; repair the check for any that does not, and add the missing negative test.
**Where:** `automation/98-drift-checks.sh (check_agent_schema), tests/drift-gate-negatives.sh, usr/share/mios/mios.toml ([agents])`
**Done When:** every field the schema requires has been shown to fail the gate by its absence.
**Verify:** for each required field, deleting it from one agent block makes `bash automation/98-drift-checks.sh check_agent_schema` exit non-zero, and restoring it returns exit 0; a registered negative test performs at least one of these.
**Do NOT:** conclude the gate works because it is green, and do not test it by breaking the TOML syntax -- an unparseable file fails for a reason that has nothing to do with the schema, which is how a schema check gets mistaken for working.
**Why:** one merged agent block missing a gate field silently returns the orchestrator to single-agent behaviour, and the check exists precisely so that cannot happen quietly.
**Dep:** none

## AGY-1783 -- Give every T- task marked done-by-code a command that demonstrates it  (WS-PROCESS | P1 | L)
**Goal:** "done-by-code" means someone can re-run the proof.
**What+How:** TASKS.md carries 282 open T- entries, many marked `Status: done-by-code` with no reproducible demonstration attached. Three checked this session did not hold up: greenboot rollback was never observed, and two gates were green over violated properties. Sweep the done-by-code set, and for each attach either a command whose output demonstrates the claim or a downgrade to open with the reason. Convert the ones that fail into AGY tasks carrying a Verify line. Work in batches and record the count moved in each direction, so the sweep itself is measurable.
**Where:** `TASKS.md, AGY-TASKS.md, automation/98-drift-checks.sh (check_tasks_status_parity)`
**Done When:** no entry claims done-by-code without either a demonstration command or an explicit statement that it is unproven.
**Verify:** a scan of TASKS.md finds zero entries with `Status: done-by-code` and no demonstration line, and `check_tasks_status_parity` stays green throughout.
**Do NOT:** mark entries proven by re-reading the code that implements them. The claim is that the behaviour is present, and reading the implementation is how all three of this session's false claims were originally accepted.
**Why:** a task list where "done" is unverifiable is a list that reports progress it has not made, and every plan built on it inherits the error.
**Dep:** AGY-1780

## AGY-1784 -- Fail the build on any verb whose backend is a stub  (WS-CI | P0 | M)
**Goal:** A shipped command either works or the build stops.
**What+How:** `resolve_install_core` was `CMD=(echo "MiOS installer core executed in ${mode} mode")` -- it printed a phase banner, logged preflight success, exited 0 and installed nothing, in the path that installs the operating system. `[verbs.update]` has no `cmd` at all. Add a gate that fails when a verb's backend is a stub: an entry point whose entire body is echoes, `true`, or a comment; a declared verb with no `cmd`; a `cmd` naming a path that does not exist. Run it over `usr/libexec/mios/`, `installation/` and `[verbs.*]`, and register the stubs that are deliberate with a reason and a shrink-only ceiling.
**Where:** `automation/98-drift-checks.sh, tools/, usr/share/mios/mios.toml, usr/libexec/mios/, installation/`
**Done When:** a newly introduced echo-only backend fails the build.
**Verify:** replacing any verb backend's body with `echo "done"` makes the gate exit non-zero and name that file; restoring it returns exit 0; the ceiling equals the count of registered deliberate stubs.
**Do NOT:** match on the word TODO or FIXME. The installer stub contained neither -- it read as a working implementation, logged success, and returned 0. Match on a body that produces no effect.
**Why:** a stub that reports success is indistinguishable from a working command to an operator and to every test, and this repository has shipped at least one in its install path.
**Dep:** none

## AGY-1800 -- Give miosd tests, starting with the check registry that decides every verdict  (WS-LANG | P0 | L)
**Goal:** The daemon that runs the drift checks proves it runs them.
**What+How:** miosd is 3550 source lines across 25 files and contains not one test, so `cargo test --workspace` prints "test result: ok. 0 passed" for it -- a line indistinguishable from a crate whose tests all passed. It is also the crate with the most authority in the repository: `drift/mod.rs:352 run_all` decides whether a check passed, `run_checks_cli` at :397 is what a caller invokes, and `drift/regen.rs:11 regen_and_diff` is the regenerate-and-compare primitive most projection checks are built on. Start there rather than at the leaves. Write tests that construct a `DriftCtx` over a temporary tree and assert: `run_all` returns false when one registered check fails; a `filter` that matches nothing is an error rather than a silent pass; `regen_and_diff` reports a difference when the target is hand-edited and none when it is not; and `soft` mode is distinguishable from a real pass in the return value, not only in the output. Then work outward through the twenty-three `drift/*.rs` modules.
**Where:** `src/mios-rs/miosd/src/drift/mod.rs, src/mios-rs/miosd/src/drift/regen.rs, src/mios-rs/miosd/src/bake_plan.rs, src/mios-rs/miosd/src/main.rs`
**Done When:** miosd has tests over the registry, the regenerate-and-diff primitive and the bake plan, and is removed from `[rust.untested_crates]`.
**Verify:** `cargo test -p miosd` reports a non-zero test count; deleting the body of `run_all` so it always returns true makes at least one test fail; and `check_rust_test_coverage` fails if the entry is removed from the register while the crate still has no tests.
**Do NOT:** add a test that only constructs a struct and asserts a field. The crate's risk is in the verdict path -- a check runner that cannot report a failure is the same defect as a gate that cannot fail, and this crate decides that for every check it runs. Also do not count `#[test]` alone when judging coverage: miosd is async throughout and its tests will be `#[tokio::test]`.
**Why:** an untested check runner can report a pass it never observed, and every drift verdict in CI passes through it.
**Dep:** none

## AGY-1801 -- Cover the ten remaining untested native crates, hardest-consequence first  (WS-LANG | P1 | L)
**Goal:** No crate ships asserting nothing.
**What+How:** Ten crates besides miosd carry no test at all and are registered in `[rust.untested_crates]` with the reason each is exposed. Take them in the order their failure costs most, not by size: `mios-drift-runner` (runs checks; untested it can report an unobserved pass), `mios-ssot-lint` (asserted byte-identical to the bash twin by a gate, but asserts nothing about itself), `mios-bake-plan` (454 lines deciding what the bake pulls; a wrong plan costs a whole build), `mios-comment-lex` (the lexer the documentation ratchets measure with, so a bug there moves every doc number silently), `generate-names-registry` and the two template crates (golden-output tests are the natural shape), `mios-version-check` (its hand-rolled TOML scan has already needed hardening once), then `xtask` and `mios-wallpaperd`. Each crate leaves the register in the same commit as its tests, and `[rust].max_untested_crates` falls by one.
**Where:** `tools/native/*/src, usr/share/mios/mios.toml ([rust])`
**Done When:** `[rust].max_untested_crates` has reached zero, or every remaining entry states a reason that is structural rather than pending.
**Verify:** for each crate, `cargo test -p <crate>` reports a non-zero count, and a deliberate mutation of that crate's main decision makes one of its tests fail; `check_rust_test_coverage` reports the entry as newly-tested if it is left in the register.
**Do NOT:** satisfy this by adding an assert-true test per crate. The gate counts test attributes and would go green; the crates would still assert nothing, and the register would then be lying with a gate behind it.
**Why:** eleven crates and roughly 5,600 lines currently pass `cargo test` unconditionally, which is most of what the Rust layer's green means today.
**Dep:** AGY-1800

## AGY-1802 -- Make a Rust toolchain part of the documented development environment  (WS-LANG | P2 | S)
**Goal:** Anyone can build and test the Rust crates here.
**What+How:** The Rust layer went unverified for the whole of this session because neither available toolchain worked: the podman machine had no cargo, and the Windows toolchain fails with `error calling dlltool 'dlltool.exe': program not found`. Installing `cargo rust rustfmt clippy` into the machine took one dnf command and immediately surfaced a failing CRDT test and eleven untested crates. Record that in the development setup so the next person does not conclude the crates are unverifiable, and state what the Windows toolchain needs (a MinGW binutils providing dlltool) or that it is unsupported here.
**Where:** `usr/share/doc/mios/, Justfile, docs/`
**Done When:** a documented command produces a toolchain that builds both workspaces, and the Windows situation is stated rather than left to be rediscovered.
**Verify:** following the written steps from a machine without cargo ends with `cargo test --workspace` running in both `src/mios-rs` and `tools/native`.
**Do NOT:** write the steps from memory of this session. Run them on a machine that does not already have the toolchain, because the failure being documented is precisely that the obvious path does not work.
**Why:** a layer nobody can test is a layer whose tests nobody notices are failing -- which is what had happened.
**Dep:** none


# Batch: the four open campaigns, measured 2026-08-24

Grounded in measurements taken against this tree rather than restated from
the roadmap: 122 shipped units of which 70 are declared and 15 render
faithfully; 204 drift checks against a roadmap that says 181; 2,597 tracked
files against a roadmap that says 2,383; 109 host-dependent text writes
across 82 files; 53 Python heredocs totalling 2,340 lines inside the shell
gate; three live ports with no key in [ports]. Where a figure here differs
from ROADMAP.md, the figure here is the one that was measured -- AGY-1880
makes that table generated so the two cannot diverge again.

## AGY-1810 -- check_shellcheck warns instead of violating when shellcheck is absent  (WS-GATE | P1 | S)
**Goal:** The shell linter cannot pass by not being installed.
**What+How:** `automation/98-drift-checks.sh` `check_shellcheck()` treats `lint-shell.sh` exit 2 as `WARNING: shellcheck absent` and returns without calling `_violation`. Every other tool-dependent check routes through `_need_python`, which fails rather than warns when `MIOS_DRIFT_REQUIRE_TOOLS=1`. CI sets that variable. Give `check_shellcheck` the same two-mode behaviour: warn when the variable is unset, `_violation` when it is 1.
**Where:** `automation/98-drift-checks.sh`, `automation/lint-shell.sh`, `tests/drift-gate-negatives.sh`
**Done When:** with shellcheck removed from PATH and `MIOS_DRIFT_REQUIRE_TOOLS=1`, the gate reports a violation; with the variable unset it still warns and passes.
**Verify:** run the gate twice in a PATH without shellcheck, once with the variable and once without, and show the two differ. Then restore shellcheck and show a script with a real error-level finding still fails.
**Do NOT:** satisfy this by installing shellcheck in CI and declaring it fixed. The defect is that the check has a silent pass mode, not that a runner lacked a binary.
**Why:** a linter that reports success when it did not run is the defect class the first open campaign exists to close.
**Dep:** none

## AGY-1811 -- check_structured passes against a tree with no SSOT at all  (WS-GATE | P1 | S)
**Goal:** A missing mios.toml is a failure, not a skip.
**What+How:** `tools/drift-checks.py structured` exits 0 when run against a directory containing no `usr/share/mios/mios.toml` -- measured by running it with `MIOS_DRIFT_ROOT` pointed at an empty directory. The early return exists so a bare checkout does not fail, but it also means the check cannot distinguish "nothing to check" from "the file that defines the OS is gone". Separate the two: absent tree is a skip, absent SSOT inside a populated tree is a violation.
**Where:** `tools/drift-checks.py`, `tools/test_drift-checks.py`
**Done When:** an empty directory still skips, and a directory that has `usr/` but no `mios.toml` reports a violation.
**Verify:** build both fixtures in a temp dir and assert the two exit codes differ. Add the case to `tools/test_drift-checks.py`.
**Do NOT:** remove the skip entirely. It is what lets the module be imported and unit-tested off a real tree; deleting it trades one broken behaviour for another.
**Why:** the same early-return shape is repeated across the folded checks, so fixing it once establishes the pattern for the rest.
**Dep:** none

## AGY-1812 -- Audit the 53 remaining gate heredocs for a possible red  (WS-GATE | P1 | L)
**Goal:** Every check in 98-drift-checks.sh has a known edit that makes it fail.
**What+How:** 53 Python heredocs remain in the gate, 2340 lines total. For each, write down the concrete edit that should turn it red, then make that edit in a scratch copy of the tree and confirm the check reports a violation. Record the result per check in a table: RED-CONFIRMED, or the reason it could not be made to fail. The ones that cannot be made to fail are the deliverable.
**Where:** `automation/98-drift-checks.sh`, `docs/agy/audits/`
**Done When:** every one of the 53 has a recorded verdict and the un-failable ones have follow-up task ids.
**Verify:** for a random sample of 10 confirmed-red checks, re-apply the recorded edit and show the violation text.
**Do NOT:** count "I read it and it looks correct" as a verdict. The audit is only worth the tree-edits actually performed; reading is what let these through the first time.
**Why:** the roadmap calls this campaign 1 and says a hollow gate falsifies the thesis directly.
**Dep:** none

## AGY-1813 -- Negative tests exist for gates that never had one  (WS-GATE | P1 | L)
**Goal:** `tests/drift-gate-negatives.sh` covers every check, not a subset.
**What+How:** `check_negative_test_coverage` already measures which checks have a negative test. Read its output, then write the missing ones. Each negative test injects one defect, asserts the specific check reports it, and restores the tree. Follow the existing file's conventions for snapshot and restore -- and note the recorded hazard that a trap which removes a path recorded as absent will delete a directory if the path is a directory.
**Where:** `tests/drift-gate-negatives.sh`, `automation/98-drift-checks.sh`
**Done When:** `check_negative_test_coverage` reports full coverage and the suite passes.
**Verify:** run the negatives suite twice in a row and show `git status` is clean after each; a negative test that leaves the tree dirty has corrupted the run that follows it.
**Do NOT:** register a test that asserts only "the gate ran". The assertion must be that this specific check reported this specific injected defect.
**Why:** a gate with no negative test is a gate nobody has ever seen fail.
**Dep:** AGY-1812

## AGY-1814 -- Resolve the 66 gates the falsifiability audit left inconclusive  (WS-GATE | P1 | L)
**Goal:** No check remains in the unknown bucket.
**What+How:** A prior 202-check audit finished with 66 checks neither confirmed-failable nor confirmed-hollow. Take them in batches of ten, apply the AGY-1812 method, and give each a verdict. Where a check turns out to be hollow, fix it in the same batch rather than filing it onward.
**Where:** `automation/98-drift-checks.sh`, `tools/drift-checks.py`, `docs/agy/audits/`
**Done When:** all 66 have a verdict and every hollow one has been repaired or has an open task id.
**Verify:** the audit table shows 0 inconclusive; spot-check five repaired checks by re-injecting their defect.
**Do NOT:** mark a check conclusive because it produced output. Output is not a verdict -- several of the hollow checks printed a confident PASS line.
**Why:** an audit that ends with a third of its subjects unresolved has not measured what it claims.
**Dep:** AGY-1812

## AGY-1815 -- A check that exits non-zero with no diagnostic must fail the suite  (WS-GATE | P2 | S)
**Goal:** Silent failures are impossible to act on, so make them impossible to ship.
**What+How:** `tools/test_drift-checks.py` now asserts each subcommand either exits 0 or prints something. Lift that rule to the gate itself: the check wrappers should treat an empty diagnostic from a failing check as its own violation, naming the check.
**Where:** `automation/98-drift-checks.sh`, `tools/drift-checks.py`
**Done When:** a check stubbed to exit 1 with no output produces a violation that names it.
**Verify:** stub one check that way in a scratch copy and show the gate names it.
**Do NOT:** implement this by requiring specific wording. The rule is that something actionable was printed, not that it matched a phrase -- an over-specific assertion is what made the sibling test reject a correct violation report.
**Why:** in a log, a non-zero exit with no message is indistinguishable from a pass.
**Dep:** none

## AGY-1816 -- The gate index is generated but its ordinals are never checked against the runner  (WS-GATE | P2 | M)
**Goal:** `drift-gate-index.tsv` describes the gate that actually runs.
**What+How:** `tools/generate-gate-index.py` derives ordinal, function name and description from `main()` in the gate. Nothing asserts that the ordinal a check reports at runtime matches its ordinal in the index, so a reordering renumbers the index silently and old references point at the wrong check. Emit the ordinal from the runner and compare.
**Where:** `tools/generate-gate-index.py`, `automation/98-drift-checks.sh`, `usr/share/mios/reference/drift-gate-index.tsv`
**Done When:** reordering two checks in `main()` without regenerating the index fails a check.
**Verify:** swap two adjacent check calls in a scratch copy and show the failure; regenerate and show it clears.
**Do NOT:** rely on the file's line count or row count as the comparison. Two checks swapping places leaves both unchanged.
**Why:** every doc and task that cites "check 31" is citing this file.
**Dep:** none

## AGY-1817 -- Gate ordinals cited in prose are not verified to exist  (WS-GATE | P2 | M)
**Goal:** A doc that cites a gate number cites a real one.
**What+How:** Comments and docs across the tree cite checks by ordinal. `check_doc_refs_resolve` resolves path references but not ordinal references. Extend it: parse those citations out of tracked markdown and comments, and resolve each against `drift-gate-index.tsv`.
**Where:** `tools/drift-checks.py`, `usr/share/doc/mios/`, `docs/`
**Done When:** a citation of a nonexistent ordinal fails the check.
**Verify:** add a citation of a nonexistent ordinal to a scratch doc and show the violation naming file and line.
**Do NOT:** auto-rewrite the citations found. Renumbering prose to match code hides that the prose was describing something else.
**Why:** the roadmap's third campaign counts 150 stale references; ordinals are a category of them nothing currently catches.
**Dep:** AGY-1816

## AGY-1818 -- `_need_python` reports a warning CI cannot distinguish from a pass  (WS-GATE | P2 | S)
**Goal:** A skipped check is visible in the summary, not only in stderr.
**What+How:** When `MIOS_DRIFT_REQUIRE_TOOLS` is unset and python3 is missing, `_need_python` warns and the check returns 0. The run summary then reports the same all-passed line as a run where everything executed. Count skips, print them in the summary by name, and make the exit path distinguish passed from passed-with-skips.
**Where:** `automation/98-drift-checks.sh`
**Done When:** a run with skips prints the skip count and the skipped check names.
**Verify:** run the gate with python3 removed from PATH and show the summary names the skipped checks.
**Do NOT:** turn every skip into a failure. That is AGY-1810's job for tools CI genuinely requires; this task is about visibility.
**Why:** the recorded failure mode is a whole suite skipping and reading as green.
**Dep:** none

## AGY-1819 -- Prove the drift-gate negatives suite restores the tree it perturbs  (WS-GATE | P1 | M)
**Goal:** A negative test cannot corrupt the run that follows it.
**What+How:** The suite injects defects and restores them. A recorded incident had a restore step delete a directory because the path had been recorded as absent and the trap removed it unconditionally. Add an assertion at the end of each negative case that `git status --porcelain` is byte-identical to what it was at case start, and fail the case if not.
**Where:** `tests/drift-gate-negatives.sh`
**Done When:** each case self-verifies its own restore, and a deliberately broken restore fails that case rather than the next one.
**Verify:** break one case's restore on purpose and show that case fails, and that the following case still passes.
**Do NOT:** compare only file existence. The recorded bug replaced a directory with nothing, and a bare existence test would have been satisfied by recreating an empty one.
**Why:** a corrupting test suite produces failures in unrelated checks, which is the hardest kind of red to diagnose.
**Dep:** none

## AGY-1820 -- The gate's own runner has no test  (WS-GATE | P2 | M)
**Goal:** The dispatch in `main()` is covered like the checks it calls.
**What+How:** Nothing tests that every `check_*` function defined in the gate is actually called from `main()`. A function that is defined but never invoked is a check that does not run, and it would still appear in the index. Write a test that parses defined `check_*` functions and asserts each appears in `main()`.
**Where:** `automation/98-drift-checks.sh`, `tools/`
**Done When:** adding a `check_*` function without wiring it into `main()` fails.
**Verify:** add an unwired stub in a scratch copy and show the failure; wire it and show it clears.
**Do NOT:** assert the two counts are equal. Equal counts are satisfied by one function being unwired while an unrelated name is called twice.
**Why:** the tree already contained a module subcommand that nothing called; this is the same shape one level up.
**Dep:** none

## AGY-1821 -- A generator's --check mode must compare what its write mode produces  (WS-GATE | P1 | M)
**Goal:** No other generator can repeat the bib-configs defect.
**What+How:** `generate-bib-configs.py --check` compared only the value it projects, via a tolerant regex, while write mode also normalised whitespace -- so it reported in-sync on a file it would rewrite on sight. That one is fixed and `tools/test_generator_check_agrees_with_write.py` locks the invariant for gate-diffed generators. Extend the same audit to every tool with a `--check` flag, not only the five the gate diffs: for each, run write mode and confirm `--check` refuses any tree write mode would change.
**Where:** `tools/`, `usr/libexec/mios/`, `tools/test_generator_check_agrees_with_write.py`
**Done When:** every `--check` implementation derives its comparison from the same rendering its writer uses.
**Verify:** for each generator, perturb its output file, assert `--check` exits non-zero, regenerate, assert it exits 0.
**Do NOT:** assume a generator is fine because its `--check` currently passes. That is precisely the symptom -- the broken one passed too.
**Why:** a check that does not compare what the writer produces cannot detect the drift the writer creates.
**Dep:** none

## AGY-1822 -- Ratchet ceilings are read from the SSOT but never asserted to exist  (WS-GATE | P2 | S)
**Goal:** A missing ceiling fails loudly instead of disabling its ratchet.
**What+How:** `check_legibility_ratchet` and the docs/CI/rust ratchets read their ceilings from `mios.toml`. If a key is absent the check has nothing to compare against. Assert that every ratchet the code knows about has a ceiling present, and fail naming the missing key.
**Where:** `tools/drift-checks.py`, `usr/share/mios/mios.toml`
**Done When:** deleting any ratchet ceiling from the SSOT fails a check that names the key.
**Verify:** delete one ceiling in a scratch copy and show the failure; restore and show it clears.
**Do NOT:** default a missing ceiling to the current measurement. That silently re-baselines the ratchet at whatever the tree happens to be, which is the failure this prevents.
**Why:** an absent ceiling is a broken gate that looks like a passing one.
**Dep:** none

## AGY-1823 -- Ratchets can only be lowered, and nothing enforces it  (WS-GATE | P1 | M)
**Goal:** Raising a shrink-only floor fails CI.
**What+How:** The ratchets are documented shrink-only, but the rule lives in prose. A recorded incident had a settle loop write the current measurement back as the floor, raising it. Add a check that compares each ratchet ceiling against the value at merge-base and fails if any increased.
**Where:** `tools/drift-checks.py`, `usr/share/mios/mios.toml`, `.github/workflows/mios-ci.yml`
**Done When:** a commit that raises any ratchet ceiling fails, and one that lowers it passes.
**Verify:** in a scratch branch raise one ceiling by 1 and show the failure; lower it by 1 and show it passes.
**Do NOT:** compare against the previous commit only. A two-commit sequence that raises then partially lowers must still fail against merge-base.
**Why:** every ratchet in the tree is enforced by discipline alone, and discipline has already failed once.
**Dep:** AGY-1822

## AGY-1824 -- Suites that run nowhere cannot be detected after the fact  (WS-GATE | P2 | M)
**Goal:** A test file that no tier runs is a failure at the moment it is added.
**What+How:** `[ci]` in `mios.toml` declares tiers by directory and glob; `tools/ci-suites.py --check` asserts both workflows invoke every tier. A file that matches no glob still runs nowhere and nothing notices -- 13 such suites were found once already. Add a check that every tracked test file is claimed by exactly one tier or listed in the exempt table with a reason.
**Where:** `tools/ci-suites.py`, `usr/share/mios/mios.toml`
**Done When:** adding a test file outside every glob fails until it is claimed or exempted.
**Verify:** add a test file to a directory no glob covers and show the failure; add the glob and show it clears.
**Do NOT:** widen a glob to make the check pass without reading what the newly-matched files do. A glob that suddenly matches 40 files has changed what CI runs.
**Why:** an unrun suite is worse than no suite: it reports coverage that does not exist.
**Dep:** none

## AGY-1825 -- The exempt-suite table has a ceiling but no expiry  (WS-GATE | P3 | S)
**Goal:** An exemption is temporary by construction.
**What+How:** `[ci].max_exempt_suites` caps how many suites may be exempt (currently 7/7, at the cap). Nothing records why each is exempt or when it should return. Add a required reason and an owning task id per exemption, and fail if either is missing.
**Where:** `usr/share/mios/mios.toml`, `tools/ci-suites.py`
**Done When:** an exemption without a reason and a task id fails the check.
**Verify:** drop the reason from one entry and show the failure.
**Do NOT:** raise the ceiling to add an exemption. It is at its cap because that is the point; a new exemption should require retiring an old one.
**Why:** exemptions without expiry become permanent, and the cap then measures nothing.
**Dep:** none

## AGY-1826 -- Bake-smoke prints OK for an empty list  (WS-GATE | P1 | M)
**Goal:** A smoke test with nothing to check fails instead of passing.
**What+How:** `tests/bake-smoke.sh` iterates a list and reports success when the loop body never executes. That is the recorded shape where empty input reads as a pass. Assert the list is non-empty before iterating, and fail naming what produced the empty list.
**Where:** `tests/bake-smoke.sh`
**Done When:** an empty input list fails the suite with a message naming its source.
**Verify:** force the list empty in a scratch copy and show the failure.
**Do NOT:** fix only the one loop. Audit every loop in the file for the same shape -- the pattern repeats.
**Why:** this suite is on the exempt list, so it currently reports nothing to anyone; making it honest is a precondition for un-exempting it.
**Dep:** AGY-1825

## AGY-1827 -- miosd targets that do not exist are reported as skipped, not missing  (WS-GATE | P1 | M)
**Goal:** A target that is not there is a failure.
**What+How:** A recorded audit found 9 of 12 `miosd` targets nonexistent, with the runner treating them as Skip. Skip is not pass. Make a nonexistent target a failure naming the target, and keep Skip only for targets that exist but are not applicable to this host.
**Where:** `usr/libexec/mios/`, `tools/`, `tests/`
**Done When:** a nonexistent target fails; an existing but inapplicable one skips and says why.
**Verify:** rename one real target in a scratch copy and show it fails rather than skips.
**Do NOT:** create empty stubs for the missing targets to make the check pass. That converts a visible gap into a hidden one.
**Why:** three quarters of a suite silently not running is the defect class the first campaign is about.
**Dep:** none

## AGY-1828 -- The golden-master conformance test diffed the tree against a copy of itself  (WS-GATE | P1 | M)
**Goal:** The conformance check compares against a fixed baseline.
**What+How:** A recorded audit found the golden-master test comparing the tree to a copy made from the same tree, which cannot fail. Rebuild it against committed golden files under `tests/templates/golden/`, and assert the baseline is not regenerated as part of the run.
**Where:** `tools/test_conformance_golden.py`, `tests/templates/golden/`
**Done When:** changing a templated file without updating its golden fails.
**Verify:** edit one templated output in a scratch copy and show the failure; update the golden and show it clears.
**Do NOT:** add a refresh-goldens step to the test run. A test that regenerates its own baseline is the defect being fixed.
**Why:** it is the clearest instance of a check structurally incapable of failing.
**Dep:** none

## AGY-1829 -- Nothing asserts a drift-check subcommand is reachable from the shell gate  (WS-GATE | P2 | S)
**Goal:** A subcommand in the module is invoked by the gate.
**What+How:** `tools/drift-checks.py` grew a `drift-projection` subcommand that the gate never called, because the extraction added the module half and left the shell half on its old heredoc. `tools/test_drift-checks.py` now asserts every subcommand appears in the gate; harden it so a subcommand invoked in a comment or a disabled branch does not satisfy it.
**Where:** `tools/test_drift-checks.py`, `automation/98-drift-checks.sh`
**Done When:** a subcommand mentioned only in a comment fails the assertion.
**Verify:** comment out one real invocation, leaving the name in the comment, and show the test fails.
**Do NOT:** match on the bare subcommand name. That is what a comment satisfies; match on the invocation form.
**Why:** the duplicate-logic hazard it prevents is one the tree already had.
**Dep:** none

## AGY-1830 -- 52 shipped units are not declared in `[units.*]` at all  (WS-UNITS | P1 | L)
**Goal:** Every unit the image ships is declared in the SSOT.
**What+How:** `cargo run -p mios-unit-gen -- --check` reports "declares 70 of 122 shipped units ... 52 undeclared". Get the list with `--list`, then declare them in batches of eight. For each: transcribe the shipped unit into `[units."<name>".<Section>]`, render it with `--render <name>`, and diff against the file on disk until they match byte for byte. Repeated directives become TOML arrays; the file-level comment goes in the section's `comment` key.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/systemd/system/`, `tools/native/mios-unit-gen/`
**Done When:** undeclared reaches 0 and `--check` exits 0.
**Verify:** after each batch, `--check` must show declared rising by the batch size and undeclared falling by it. Re-run `tools/sync-generated.sh` and confirm the tree is clean afterwards.
**Do NOT:** rewrite a unit's content while declaring it. The projection must reproduce the unit, not edit it in passing -- transcribe the comment the file already carries rather than composing a better one.
**Why:** the roadmap names unit projection the largest single gap in "one file defines the OS".
**Dep:** AGY-1935

## AGY-1831 -- 55 declared units are registered as drifting from their projection  (WS-UNITS | P1 | L)
**Goal:** The drift register empties.
**What+How:** `--check` reports 15 faithful and 55 drifted against a register of 55 in `[unit_projection].drift`. Each registered unit renders differently from the file that ships. Take them in batches: run `--render <name>`, diff against the shipped file, and either correct the declaration so it reproduces the file or correct the file if the declaration is right. Remove each from the register as it becomes faithful.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/systemd/system/`, `tools/native/mios-unit-gen/`
**Done When:** the register is empty and faithful equals declared.
**Verify:** the register only shrinks -- `test_the_register_only_shrinks` already asserts this, so run it after each batch.
**Do NOT:** remove a unit from the register without making it render faithfully. The register is what keeps known drift visible; emptying it by deletion hides the gap the roadmap is measuring.
**Why:** 55 units that do not regenerate is the concrete form of part 1 of the thesis being unproven.
**Dep:** AGY-1830

## AGY-1832 -- Two copies of every unit exist and only one is compared  (WS-UNITS | P1 | M)
**Goal:** One location holds each unit, or both are provably identical.
**What+How:** `mios-node.service` and `mios-xdg-userdir-init.service` each existed in both `usr/share/mios/systemd/` and `usr/lib/systemd/system/`, with different content, and the generator compares only the latter. Determine which directory is authoritative, then either collapse to one or add a check asserting the two copies are byte-identical.
**Where:** `usr/share/mios/systemd/`, `usr/lib/systemd/system/`, `automation/`
**Done When:** a divergence between two copies of the same unit fails a check.
**Verify:** change one copy in a scratch tree and show the failure.
**Do NOT:** delete either directory before tracing what installs from it. `automation/` and the bake reference both; deleting the unread one is how a working install breaks silently.
**Why:** the generator reported a unit faithful while a second, different copy shipped alongside it.
**Dep:** none

## AGY-1833 -- The unit drift register has a count but no per-entry reason  (WS-UNITS | P2 | S)
**Goal:** Each registered drift says why it drifts.
**What+How:** `[unit_projection].drift` lists 55 unit names. Nothing records what about each one does not project -- a directive the renderer cannot emit, a value that is not in the SSOT, or a hand-edit. Add a reason per entry and require it.
**Where:** `usr/share/mios/mios.toml`, `tools/native/mios-unit-gen/`
**Done When:** an entry without a reason fails.
**Verify:** drop a reason in a scratch copy and show the failure.
**Do NOT:** write "drifts" as the reason. The reason must name the directive or value that cannot be projected, because that is what the fixing task needs.
**Why:** 55 opaque names is a backlog nobody can plan; 55 named causes is a work-list.
**Dep:** AGY-1831

## AGY-1834 -- The renderer cannot express directives some units need  (WS-UNITS | P2 | M)
**Goal:** The projection can represent every directive that ships.
**What+How:** Once AGY-1833 gives per-entry reasons, group them. Some will be renderer gaps -- a directive form the TOML schema cannot express, or ordering the renderer does not preserve. Extend the renderer for each gap, with a unit test per directive form, following the existing `test_arrays_repeat_the_key` and `test_booleans_render_as_yes_no` tests.
**Where:** `tools/native/mios-unit-gen/src/`, `tools/native/mios-unit-gen/tests/`
**Done When:** no registered drift reason is "the renderer cannot express this".
**Verify:** each new directive form has a test asserting its rendered output.
**Do NOT:** special-case a unit by name in the renderer. A projection with per-unit exceptions is a hand-maintained file with extra steps.
**Why:** the renderer is the mechanism the whole SSOT claim rests on; its gaps set the ceiling on the campaign.
**Dep:** AGY-1833

## AGY-1835 -- Quadlet container files are projected but not covered by the unit gate  (WS-UNITS | P2 | M)
**Goal:** Container units are held to the same standard as service units.
**What+How:** `[containers.*]` projects Quadlet files under `usr/share/containers/systemd/`. `mios-unit-gen` counts systemd units; the Quadlets are checked separately, and the recorded failure mode is a broad `git add` stripping digest pins from generated Quadlets. Bring them under one reporting surface so faithful/drifted/undeclared is stated for Quadlets too.
**Where:** `usr/share/containers/systemd/`, `tools/native/mios-unit-gen/`, `automation/98-drift-checks.sh`
**Done When:** one command reports projection status for both systemd units and Quadlets.
**Verify:** hand-edit one generated Quadlet and show it is reported as drifted.
**Do NOT:** merge the two registers into one number that hides which surface is behind. The roadmap tracks them as separate floors deliberately.
**Why:** Quadlets are the larger surface in the image and are currently measured less rigorously than units.
**Dep:** AGY-1831

## AGY-1836 -- A unit can be declared, faithful, and still not installed  (WS-UNITS | P2 | M)
**Goal:** Declared units reach the image.
**What+How:** `--check` reports "0 missing on disk", which asserts the declared unit exists in the source tree -- not that the bake installs it or that anything enables it. Cross-check `[units.*]` against what the built image actually contains, and against the enablement the SSOT declares.
**Where:** `automation/build.sh`, `usr/share/mios/mios.toml`, `tests/bake-smoke.sh`
**Done When:** a unit declared in the SSOT but absent from the built image fails.
**Verify:** remove one unit from the install step in a scratch copy and show the failure.
**Do NOT:** infer installation from the source tree. Presence in `usr/lib/systemd/system/` in git is not presence in the image; that inference is what makes this check worth writing.
**Why:** the thesis is about the OS the image is, not the files the repo holds.
**Dep:** AGY-1830

## AGY-1837 -- Enablement is declared in two places for some units  (WS-UNITS | P2 | M)
**Goal:** One declaration decides whether a unit is enabled.
**What+How:** Units carry `[Install] WantedBy` in their projection, and the bake separately runs enable steps. Where both exist they can disagree. Determine which is authoritative, project the other from it, and gate the pair.
**Where:** `usr/share/mios/mios.toml`, `automation/`, `usr/lib/systemd/system/`
**Done When:** changing enablement in one place without the other fails a check.
**Verify:** change a WantedBy in a scratch copy without touching the bake and show the failure.
**Do NOT:** resolve a disagreement by picking whichever is currently true in the image. Read why each was written; one of them is likely load-bearing for firstboot ordering.
**Why:** two sources for one fact is the condition the SSOT thesis exists to remove.
**Dep:** AGY-1836

## AGY-1838 -- Template units are shipped but not projected  (WS-UNITS | P2 | M)
**Goal:** `.tmpl` units are declared like the rest.
**What+How:** `usr/share/mios/systemd/` ships `home-@.automount.tmpl` and `home-@.mount.tmpl`. They are templates expanded at install time and are not counted by the projection at all. Decide whether they are projected from `[units.*]` with a template marker, or explicitly declared authored, and record which.
**Where:** `usr/share/mios/systemd/`, `usr/share/mios/mios.toml`, `tools/native/mios-unit-gen/`
**Done When:** every file in the unit directories is either projected or explicitly declared authored.
**Verify:** add an undeclared file to the directory and show it is reported.
**Do NOT:** exclude `.tmpl` by extension without recording the decision. Silent exclusions are how the 52 undeclared units accumulated.
**Why:** the fourth open campaign requires every file to be generated-and-gated or explicitly authored.
**Dep:** AGY-1830

## AGY-1839 -- The unit gate reports its own coverage as a count, not a list  (WS-UNITS | P3 | S)
**Goal:** The summary line is actionable without a second command.
**What+How:** `--check` prints counts and then the drifted names, but undeclared names appear only under `--list`. Print undeclared names in `--check` too, capped with an explicit "and N more" rather than truncated silently.
**Where:** `tools/native/mios-unit-gen/src/main.rs`
**Done When:** `--check` names what is undeclared, and says how many it did not name.
**Verify:** run with more undeclared units than the cap and confirm the count of omitted names is stated.
**Do NOT:** print an unbounded list. A 52-line failure in CI buries the summary; the cap plus an explicit remainder is the point.
**Why:** a silent truncation reads as complete coverage, which is a recorded defect class here.
**Dep:** none

## AGY-1840 -- `[units.*]` values are literals where SSOT keys exist  (WS-UNITS | P2 | L)
**Goal:** A declared unit references SSOT keys rather than repeating their values.
**What+How:** Declaring a unit moves its content into the SSOT but does not make its values derive. `mios-node.service` now carries `ExecStart = "/usr/bin/mios-node 101 8650"` and an endpoint literal. Where a value already has a key in `[ports]` or elsewhere, use the systemd `${VAR:-default}` form with the corresponding `EnvironmentFile`, as `mios-userdb-render.service` does for the pgvector port.
**Where:** `usr/share/mios/mios.toml`, `etc/mios/install.env`
**Done When:** no `[units.*]` value repeats a number that has an SSOT key.
**Verify:** change a port in `[ports]`, regenerate, and confirm the rendered unit changes with it.
**Do NOT:** substitute a variable whose key does not exist yet. That renders an empty value into a live unit; allocate the key first.
**Why:** a literal inside the SSOT is still a hardcode, just relocated.
**Dep:** AGY-1865

## AGY-1841 -- Unit ordering constraints are not verified against the units that exist  (WS-UNITS | P2 | M)
**Goal:** `After=` and `Requires=` name units that are shipped.
**What+How:** `check_unit_dependency_closure` exists as an 82-line heredoc. Confirm it resolves every `After`, `Before`, `Requires`, `Wants` and `BindsTo` target against the set of units the image actually installs, including targets provided by the base OS, and fails on a dangling reference.
**Where:** `automation/98-drift-checks.sh`, `tools/drift-checks.py`
**Done When:** a reference to a nonexistent unit fails, and a reference to a base-OS unit does not.
**Verify:** add `After=nonexistent.service` in a scratch copy and show the violation; add `After=network-online.target` and show it passes.
**Do NOT:** allowlist every unresolved name to make the check pass. Each unresolved name is either a base-OS unit, which belongs in a documented list, or a real dangling reference.
**Why:** a dangling ordering constraint is silently ignored by systemd at boot, so nothing else will report it.
**Dep:** AGY-1812

## AGY-1842 -- Privileged units are declared in the SSOT but the list is not derived  (WS-UNITS | P1 | M)
**Goal:** The privileged-unit roster is computed from the units, not maintained beside them.
**What+How:** `[security.privileged_quadlets].root` lists 10 units by name. A unit that gains privilege without being added to the list defeats the check. Derive the roster from the unit definitions themselves and compare against the declared list, so adding privilege to an unlisted unit fails.
**Where:** `usr/share/mios/mios.toml`, `automation/98-drift-checks.sh`
**Done When:** granting privilege to an unlisted unit fails a check.
**Verify:** add a privileged directive to an unlisted unit in a scratch copy and show the violation.
**Do NOT:** update the list to match the tree as the fix. The list is the security assertion; the tree is what must conform to it.
**Why:** a hand-maintained security roster drifts silently, and its drift is the failure it exists to prevent.
**Dep:** none

## AGY-1843 -- Units reference binaries that may not be installed  (WS-UNITS | P2 | M)
**Goal:** Every `ExecStart` path exists in the image.
**What+How:** `mios-node.service` runs `/usr/bin/mios-node`, built by the native build. Nothing asserts the binary is present in the built image. Resolve every `ExecStart`, `ExecStartPre`, `ExecStop` and `ExecReload` path against what the bake installs.
**Where:** `usr/share/mios/mios.toml`, `automation/build.sh`, `tests/bake-smoke.sh`
**Done When:** a unit whose binary the image does not contain fails.
**Verify:** drop one binary from the install step in a scratch copy and show the failure.
**Do NOT:** check the source tree for the path. The binary is built during the bake; its presence in git proves nothing.
**Why:** a unit that cannot start is a unit that fails at first boot, on the operator's machine rather than in CI.
**Dep:** AGY-1836

## AGY-1844 -- Drop-in directories are projected inconsistently  (WS-UNITS | P2 | S)
**Goal:** Drop-ins are declared the same way units are.
**What+How:** `[units."mios-gpu-nvidia.service.d/10-cycle-fix.conf"]` shows drop-ins declared as units with a path-like name. Confirm that form is intentional and used consistently, and that the renderer writes drop-ins to the right directory rather than a file with a slash in its name.
**Where:** `usr/share/mios/mios.toml`, `tools/native/mios-unit-gen/`
**Done When:** every drop-in in the tree is declared in the same form and renders to the right path.
**Verify:** render one drop-in and confirm the output path is the `.d` directory.
**Do NOT:** rename the existing entries before checking what reads them. The gpu drop-in is referenced by the GPU detection path.
**Why:** an inconsistent declaration form is a gap in a projection that claims to be total.
**Dep:** AGY-1830

## AGY-1845 -- User units and system units share one table with no distinction  (WS-UNITS | P2 | S)
**Goal:** A user unit is declared as a user unit.
**What+How:** `mios-xdg-userdir-init.service` is documented as a systemd user unit whose install target is `default.target`, yet it is declared in the same `[units.*]` table as system units and ships under the system directory. Add an explicit scope to the declaration and render user units to the user unit path.
**Where:** `usr/share/mios/mios.toml`, `tools/native/mios-unit-gen/`, `usr/lib/systemd/`
**Done When:** a user unit renders to the user unit directory and a system unit to the system one.
**Verify:** render both kinds and confirm the two paths differ.
**Do NOT:** infer scope from `WantedBy=default.target`. Some system units legitimately use that target; scope must be declared, not guessed.
**Why:** a user unit installed as a system unit either fails to start or starts in the wrong session.
**Dep:** AGY-1830

## AGY-1846 -- Nothing checks that a declared unit parses as a systemd unit  (WS-UNITS | P2 | M)
**Goal:** The projection cannot emit a file systemd would reject.
**What+How:** The renderer emits INI-shaped text from TOML. Nothing validates the result against systemd's own parser. Run `systemd-analyze verify` over the rendered units in CI, where systemd is available, and report per-unit results.
**Where:** `.github/workflows/mios-ci.yml`, `tools/native/mios-unit-gen/`, `tests/`
**Done When:** a unit that systemd rejects fails CI.
**Verify:** introduce an invalid directive in a scratch copy and show `systemd-analyze verify` catches it.
**Do NOT:** treat every `systemd-analyze` warning as a failure on first pass. Record the existing warning set, fail on new ones, and drive the baseline down as a ratchet.
**Why:** a projection validated only against itself proves the renderer is self-consistent, not that it is correct.
**Dep:** AGY-1831

## AGY-1847 -- The unit count in the roadmap contradicts the generator  (WS-UNITS | P1 | S)
**Goal:** The published number matches the measured one.
**What+How:** `ROADMAP.md` states "Units reproducing from SSOT | 27 of 66" with 39 registered as drifting. The generator reports 15 faithful of 122 shipped, 70 declared, 55 registered. Both cannot be true. Determine which denominator each is counting, then make the roadmap's figure come from the generator rather than from prose.
**Where:** `ROADMAP.md`, `tools/native/mios-unit-gen/`, `tools/roadmap-index.py`
**Done When:** the roadmap's unit figure is generated and matches `--check`.
**Verify:** declare one more unit and confirm the roadmap figure changes on regeneration.
**Do NOT:** edit the number in the prose. That is what produced a stale figure in a table headed "measured rather than asserted".
**Why:** a roadmap that misstates its own central metric cannot be used to decide what to work on.
**Dep:** AGY-1880

## AGY-1848 -- Firstboot units have no ordering test  (WS-UNITS | P2 | M)
**Goal:** Firstboot ordering is asserted, not assumed.
**What+How:** A dozen `*-firstboot.service` units and `mios-firstboot.target` coordinate first-boot work. Their relative order is expressed only through `After`/`Wants` in individual units. Derive the resulting order and assert the invariants that matter -- that the AI plane starts after pgvector, that enrolment never gates boot per Law 12.
**Where:** `usr/share/mios/mios.toml`, `tests/`, `usr/lib/systemd/system/`
**Done When:** an ordering change that would let enrolment gate boot fails a check.
**Verify:** add a `Requires=` from a boot-critical unit to the enrolment unit in a scratch copy and show the violation.
**Do NOT:** assert an exact total order. Systemd is free to parallelise; assert the specific pairwise constraints the laws require.
**Why:** Law 12 is a boot-time property, and boot is the surface with the least test coverage.
**Dep:** AGY-1841

## AGY-1849 -- `--render` output is not covered by the LF discipline  (WS-UNITS | P2 | S)
**Goal:** Rendered units are byte-identical on every host.
**What+How:** Seven Python generators were pinned to LF after regenerating on Windows produced CRLF and a phantom diff in every row. `mios-unit-gen` is Rust and writes through a different path; confirm it emits LF regardless of host, and add the assertion to its tests alongside the existing normalisation test.
**Where:** `tools/native/mios-unit-gen/src/`, `tools/native/mios-unit-gen/tests/`
**Done When:** a test asserts rendered output contains no carriage returns.
**Verify:** run the test on Windows and on Linux and confirm both pass.
**Do NOT:** rely on `test_normalize_forgives_only_eol_and_trailing_blanks`. Forgiving end-of-line differences in the comparison is exactly what would hide the generator emitting the wrong ones.
**Why:** the same defect in the Python generators made a whole class of checks fire on who ran them rather than on drift.
**Dep:** none

## AGY-1850 -- 109 text-mode writes across 82 files translate newlines to the host separator  (WS-HOSTDEP | P1 | L)
**Goal:** A generated file is byte-identical regardless of which OS produced it.
**What+How:** A scan of `tools/*.py`, `usr/libexec/mios/*` and `usr/lib/mios/*.py` found 109 `open(..., "w")` calls without an explicit `newline=`. Python text mode translates line endings to the host separator, so the same generator emits CRLF on Windows and LF on Linux. Seven were fixed after regenerating `drift-gate-index.tsv` on Windows produced 205 carriage returns and a phantom diff in every row. Work the remaining ones in batches of ten, prioritising anything whose output is tracked in git.
**Where:** `tools/`, `usr/libexec/mios/`, `usr/lib/mios/`
**Done When:** every text-mode write to a tracked path pins `newline`.
**Verify:** run each fixed generator on Windows and confirm `git status` is clean; count carriage returns in its output and assert zero.
**Do NOT:** convert them to binary mode wholesale. Several write text assembled with `%s` formatting where binary mode changes the call signature; pin the newline instead of changing the mode.
**Why:** a gate that diffs generated against committed fires on who ran it rather than on drift, which trains everyone to ignore it.
**Dep:** none

## AGY-1851 -- Nothing prevents a new unpinned text write from being added  (WS-HOSTDEP | P1 | M)
**Goal:** The 109 cannot become 110.
**What+How:** Add a check that scans tracked Python for `open(...)` in text write mode without `newline=`, and fails naming file and line. Allow an explicit opt-out comment for the cases where host-native endings are genuinely wanted, and require a reason on the opt-out.
**Where:** `tools/`, `automation/98-drift-checks.sh`
**Done When:** adding an unpinned text write fails the gate.
**Verify:** add one in a scratch copy and show the violation; add the opt-out with a reason and show it clears.
**Do NOT:** ship the check before AGY-1850 has cleared the backlog, or the gate lands red and gets disabled. Land it behind the cleanup, or ratchet it with a declining ceiling.
**Why:** a cleanup with no gate behind it regrows.
**Dep:** AGY-1850

## AGY-1852 -- Python text reads are equally host-dependent and unaudited  (WS-HOSTDEP | P2 | M)
**Goal:** Reading a file cannot change what it contains.
**What+How:** The write side is now understood. The read side has the mirror hazard: text-mode reads translate CRLF to LF silently, so a comparison between a file read in text mode and bytes produced elsewhere can pass while the files differ. Audit reads that feed comparisons, hashes or manifests and pin them.
**Where:** `tools/`, `usr/libexec/mios/`, `usr/lib/mios/`
**Done When:** every read whose result is compared or hashed is explicit about newline handling.
**Verify:** for each fixed site, construct a CRLF and an LF input and assert the comparison distinguishes them when it should.
**Do NOT:** pin every read. Reads that only parse content are correct in text mode; changing them adds churn without removing a hazard.
**Why:** a checker that normalises its input cannot detect the difference it normalises away.
**Dep:** AGY-1850

## AGY-1853 -- Shebang resolution differs by host and is not stated  (WS-HOSTDEP | P2 | M)
**Goal:** A check that cannot run on a host says so instead of passing.
**What+How:** `mios-env-snapshot`'s shebang does not resolve on Windows, so checks that invoke it report a missing input there and pass on Linux. That is currently handled per-check. Centralise it: one helper that resolves an interpreter and reports unavailability in one recognisable form, so the gate summary can distinguish "ran and passed" from "could not run here".
**Where:** `automation/98-drift-checks.sh`, `tools/drift-checks.py`, `usr/libexec/mios/`
**Done When:** every tool-invoking check reports unavailability through the one helper.
**Verify:** run the full gate on Windows and on Linux and diff the summaries; the difference should be exactly the set of host-unavailable checks.
**Do NOT:** make Windows unavailability silent. The point is that the difference between the two hosts is legible, not hidden.
**Why:** the tree is developed on Windows and gated on Linux, so this difference is permanent and needs to be stated.
**Dep:** AGY-1818

## AGY-1854 -- `fnmatch` is case-insensitive on Windows and case-sensitive on Linux  (WS-HOSTDEP | P2 | M)
**Goal:** Glob matching gives the same answer on both hosts.
**What+How:** `fnmatch.fnmatch` normalises case per host; `fnmatchcase` does not. Any check using the former to match tracked paths can accept a differently-cased path on Windows and reject it in CI. Audit glob use across the CI suite registry and the drift checks, and switch path matching to the case-sensitive form.
**Where:** `tools/ci-suites.py`, `tools/drift-checks.py`, `tools/`
**Done When:** path globbing behaves identically on both hosts.
**Verify:** construct a path differing only in case and assert both hosts agree on whether it matches.
**Do NOT:** switch every `fnmatch` call. Matching against user input or display strings may want host conventions; only path matching against git contents must be exact.
**Why:** git is case-sensitive, so a case-insensitive match against tracked paths is wrong on both hosts -- it just only fails on one.
**Dep:** none

## AGY-1855 -- `git ls-files` and `os.walk` disagree and both are used  (WS-HOSTDEP | P2 | M)
**Goal:** One definition of "the files in this tree".
**What+How:** Some checks enumerate with `git ls-files`, others walk the filesystem. The two differ by untracked files, ignored files and the vendored toolchain directory the tree now contains. A check that walks will count files a check that lists will not. Standardise on the git index for anything measuring the tracked surface, and state explicitly where a filesystem walk is intended.
**Where:** `tools/drift-checks.py`, `tools/`, `automation/98-drift-checks.sh`
**Done When:** each enumeration states which definition it uses and why.
**Verify:** add an untracked file matching a checked pattern and confirm only the walk-based checks see it, as documented.
**Do NOT:** convert every walk to the index. Checks that verify the built image or an install tree must walk, because those files are not in git.
**Why:** a ratchet that counts differently depending on what is lying around the working directory is not a measurement.
**Dep:** none

## AGY-1856 -- The legibility ratchet counts generated shell against a "bash is glue" limit  (WS-HOSTDEP | P2 | M)
**Goal:** The metric measures authored shell, not SSOT size.
**What+How:** `max_shell_lines` counts every tracked `.sh`, including generated `automation/lib/globals.sh` (4285 lines). Declaring two units in the SSOT grew that file by 34 lines and pushed the ratchet over its floor -- so projecting more of the OS from `mios.toml`, which is the second open campaign, mechanically registers as shell sprawl. Split the measurement: authored shell and generated shell, each with its own floor.
**Where:** `usr/share/mios/mios.toml`, `tools/drift-checks.py`
**Done When:** both figures are reported and ratcheted separately, and declaring a unit moves only the generated one.
**Verify:** declare one unit and confirm the authored figure is unchanged.
**Do NOT:** implement this while the ratchet is red, and do not use it to grant headroom. Split the metric first, set each floor at its measured value, and let the campaign earn them down.
**Why:** a metric that penalises the roadmap's own goal will eventually be raised or ignored, and both outcomes lose the ratchet.
**Dep:** AGY-1823

## AGY-1857 -- Pipefail masking hides the status of the command that mattered  (WS-HOSTDEP | P2 | M)
**Goal:** A failing command in a pipeline is not reported as success.
**What+How:** `cmd | grep` returns grep's status, so a failing `cmd` reads as success whenever grep matches. Audit pipelines in `automation/` and `tests/` for this shape, and capture the status of the interesting command directly with `PIPESTATUS` or by splitting the pipeline.
**Where:** `automation/`, `tests/`, `tools/`
**Done When:** no pipeline discards the status of a command whose failure should fail the script.
**Verify:** for each fixed site, make the upstream command fail and confirm the script now fails.
**Do NOT:** add `set -o pipefail` globally as the fix without re-running the suites. It changes the status of every existing pipeline, including ones that legitimately tolerate a non-zero producer.
**Why:** this is how a check reports success while doing nothing, which is the tree's most common defect.
**Dep:** none

## AGY-1858 -- `$` in the Justfile is a Make-ism that reaches sh as a PID  (WS-HOSTDEP | P3 | S)
**Goal:** No recipe silently does the wrong thing.
**What+How:** 23 lines of Make-style `$` were corrected to the `just` form. Two recipes failed loudly and three failed silently -- the worse case, since a silent failure looks like a passing recipe. Audit the remaining recipes for shell constructs that `just` interprets before `sh` sees them, and add a test that runs each recipe's dry form.
**Where:** `Justfile`, `tests/`
**Done When:** every recipe either runs or fails loudly; none silently no-ops.
**Verify:** run each recipe with a dry-run or a no-op target and assert a non-zero status where the recipe is broken.
**Do NOT:** assume a recipe is fine because it exits 0. Three of the five broken ones did exactly that.
**Why:** a build recipe that silently does nothing produces an image missing whatever it was meant to add.
**Dep:** none

## AGY-1859 -- The env baseline is captured from a clean env but not proven clean  (WS-HOSTDEP | P2 | M)
**Goal:** `env-baseline.txt` reflects the SSOT, not the capturing shell.
**What+How:** `sync-generated.sh` regenerates the baseline "clean env". If any `MIOS_*` variable is set in the capturing shell, it can leak into the recorded baseline, and the recorded incident is that root-path differences make the snapshot fail in CI. Assert the capture environment contains no `MIOS_*` variables before capturing, and normalise root paths.
**Where:** `tools/sync-generated.sh`, `usr/libexec/mios/mios-env-snapshot`, `usr/share/mios/reference/env-baseline.txt`
**Done When:** capturing with `MIOS_*` set in the shell fails rather than recording them.
**Verify:** export a `MIOS_*` variable, run the capture, and show it refuses.
**Do NOT:** filter the offending variables out silently. A capture that quietly discards part of its input cannot be trusted to have captured the rest.
**Why:** the baseline is the comparison every env drift-check depends on.
**Dep:** none

## AGY-1860 -- A concurrent agent shares this tree and races the verification  (WS-HOSTDEP | P2 | M)
**Goal:** A measurement is taken against a tree that is not moving.
**What+How:** AGY writes to the same checkout. A verification run can measure a tree mid-edit and report a failure that is neither reproducible nor real; the recorded incidents include a ratchet reading over its floor because a generated file had just changed, and a commit finding nothing staged because the other agent had committed it. Provide a way to take a consistent snapshot -- a scratch worktree at a named commit -- and use it for any measurement that will be reported.
**Where:** `tools/`, `tests/`, `docs/agy/`
**Done When:** a documented command produces an isolated tree at a given commit for verification runs.
**Verify:** run the same measurement twice against the snapshot while editing the main tree, and show the two agree.
**Do NOT:** solve this with a lock on the working tree. Both agents are meant to work; the fix is isolation for measurement, not exclusion.
**Why:** several red readings this session were races, and a false red costs as much investigation as a true one.
**Dep:** none

## AGY-1861 -- Running the gate as root produces different results  (WS-HOSTDEP | P2 | M)
**Goal:** A check gives the same verdict regardless of the invoking user.
**What+How:** `check_docs_ratchet` read red under a sweep that ran with `--user root` and green when run directly, twice. Whatever the cause -- permissions, HOME, or a path resolved relative to the user -- a check whose verdict depends on the invoking user cannot be trusted in either mode. Find the dependence and remove it.
**Where:** `tools/drift-checks.py`, `automation/98-drift-checks.sh`
**Done When:** the full gate gives identical results as root and as an unprivileged user.
**Verify:** run the gate both ways on the same commit and diff the outputs.
**Do NOT:** standardise on running as root to make the difference go away. The image runs services as several users; a check that only works as root will not survive being reused.
**Why:** two of this session's red readings were harness artefacts, which is a measurement problem, not a code problem.
**Dep:** AGY-1860

## AGY-1862 -- Temp fixtures are left behind by failing tests  (WS-HOSTDEP | P3 | S)
**Goal:** A failing test cleans up as reliably as a passing one.
**What+How:** `tools/check-temp-fixture-cleanup.py` and `check-leaked-fixtures.py` exist. Extend the coverage to assert cleanup happens on the failure path too, by running each suite with an induced failure and checking for leftovers.
**Where:** `tools/check-temp-fixture-cleanup.py`, `tools/check-leaked-fixtures.py`, `tests/`
**Done When:** a suite that leaks a fixture only when failing is caught.
**Verify:** induce a failure in a suite that cleans up in a `finally` and one that does not, and show only the latter is reported.
**Do NOT:** check only the happy path. Five leaked fixtures were found once already, and a leak on the failure path is the harder one to notice.
**Why:** a leaked fixture pollutes the next run, producing failures in unrelated checks.
**Dep:** none

## AGY-1863 -- Windows cannot build the Rust workspace at all  (WS-HOSTDEP | P2 | M)
**Goal:** The development host can verify the layer it edits.
**What+How:** `cargo test --workspace` on Windows fails with "error calling dlltool 'dlltool.exe': program not found" while compiling `windows-version`, `webview2-com-sys` and `windows-service`. The per-crate tests run; the workspace build does not. Either document the toolchain that makes it work, or gate the desktop-only crates behind a feature so the workspace builds without them.
**Where:** `tools/native/Cargo.toml`, `src/mios-rs/`, `docs/`
**Done When:** `cargo test --workspace` completes on a documented Windows setup, or the desktop crates are excluded with the exclusion stated.
**Verify:** follow the written steps on a machine without the toolchain and reach a passing workspace test.
**Do NOT:** claim the workspace is verified because individual crates pass. They are a subset, and the subset is the part that happens to build.
**Why:** the Rust layer went unverified for a whole session because no available toolchain worked.
**Dep:** AGY-1802

## AGY-1864 -- Line-indexed artefacts churn whenever a line is added anywhere  (WS-HOSTDEP | P3 | M)
**Goal:** A generated census does not produce a 130-line diff for a 1-line change.
**What+How:** `manual-corpus.tsv` records line numbers, so folding a heredoc shifted every entry after it and produced a 72-add/64-remove diff for one refactor. That is correct behaviour but it makes review of the real change harder. Consider anchoring entries by a stable identity rather than absolute line, or excluding line numbers from the committed form and computing them on read.
**Where:** `usr/share/mios/reference/manual-corpus.tsv`, `tools/`
**Done When:** moving code without changing it produces no corpus churn.
**Verify:** move a function within a file and confirm the corpus is unchanged.
**Do NOT:** stop recording position. The corpus is used to locate comments; losing position makes it useless.
**Why:** review fatigue from mechanical churn is how real changes get waved through.
**Dep:** none

## AGY-1865 -- Three live ports have no key in `[ports]`  (WS-SSOT | P1 | M)
**Goal:** Every port the system uses is allocated from the SSOT.
**What+How:** Ports 8650, 8640 and 8642 appear in live configuration and none has a key in `[ports]` -- confirmed by resolving each value against the flattened table. Ports are allocated from `[ports.categories]` by base and stride rather than hand-assigned, so add these to the right category in member order and let the renderer derive them.
**Where:** `usr/share/mios/mios.toml`, `tools/render-ports.py`, `automation/lib/globals.sh`
**Done When:** all three resolve to keys and the rendered value equals the current live value.
**Verify:** render and confirm each key produces the number that is in use today; changing the category base moves all three together.
**Do NOT:** hand-assign the numbers as literal keys. That reproduces the hardcode inside the SSOT, which is the thing `[ports.categories]` exists to prevent.
**Why:** the endpoint literal is already recorded as canonical in one place and different in another, which is what an unallocated port produces.
**Dep:** none

## AGY-1866 -- `MIOS_AI_ENDPOINT` points at two different ports in two places  (WS-SSOT | P1 | S)
**Goal:** One endpoint value, derived once.
**What+How:** The canonical AI endpoint is recorded as Hermes on 8642, while `mios-node.service` sets `MIOS_AI_ENDPOINT=http://127.0.0.1:8640`. One is wrong. Determine which, then derive the unit's value from the same key every other consumer reads.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/systemd/system/mios-node.service`, `etc/mios/install.env`
**Done When:** every consumer of the endpoint reads one key, and no literal endpoint remains.
**Verify:** change the key and confirm every consumer changes, including the rendered unit.
**Do NOT:** resolve the disagreement by matching whichever value currently works. Trace which service actually answers; the wrong one may be answering because a fallback is masking it.
**Why:** two values for one endpoint means one caller is talking to the wrong service or to nothing.
**Dep:** AGY-1865

## AGY-1867 -- The hardcode lint's allowlist is unanchored and exempts more than intended  (WS-SSOT | P1 | M)
**Goal:** An allowlisted port exempts that port, not every value containing it.
**What+How:** `mios-hardcode-lint` matches allowlist entries unanchored, so an entry spuriously exempts other literals that contain it as a substring. Anchor the match, then re-run the lint and expect new findings -- those are real hardcodes the bug was hiding.
**Where:** `usr/libexec/mios/`, `usr/share/mios/mios.toml`
**Done When:** the allowlist matches whole values only, and every newly-surfaced finding is fixed or explicitly allowlisted.
**Verify:** add an allowlist entry and confirm a different literal containing it is still reported.
**Do NOT:** allowlist the new findings to restore a green lint. They are the reason to fix the matcher.
**Why:** an allowlist that over-matches turns a lint into a formality.
**Dep:** none

## AGY-1868 -- Seven components had no SSOT port key and the list was never closed out  (WS-SSOT | P2 | M)
**Goal:** No component allocates its own port.
**What+How:** A prior sweep identified seven components with no SSOT port key. Re-run that identification against the current tree -- the set has moved -- and allocate a key for each from the appropriate category.
**Where:** `usr/share/mios/mios.toml`, `tools/render-ports.py`
**Done When:** every listening port in the tree resolves to a `[ports]` key.
**Verify:** enumerate listening ports from the unit and container definitions and resolve each against the table; the unresolved set must be empty.
**Do NOT:** trust the earlier list of seven. Names have changed since, and one recorded false friend is a key that does not exist under the name it was looked up by.
**Why:** an unallocated port collides silently, and collisions surface as an intermittent service failure at boot.
**Dep:** AGY-1865

## AGY-1869 -- Port keys are rendered into globals with different wiring per platform  (WS-SSOT | P2 | M)
**Goal:** The two globals files are produced by the same path.
**What+How:** A recorded asymmetry has `globals.sh` wired first and `globals.ps1` last, so a change can land in one and not the other. Both are generated from the same SSOT; make them a single generation step producing both, and assert parity.
**Where:** `tools/render-globals.py`, `automation/lib/globals.sh`, `automation/lib/globals.ps1`
**Done When:** every key present in one file is present in the other with the same value.
**Verify:** add a key and confirm both files gain it in one run; delete one from a scratch copy and show the parity check fails.
**Do NOT:** compare the two files line by line. They have different syntax; compare the key/value sets they define.
**Why:** the artefacts step already failed once because one of the two was regenerated and the other was not.
**Dep:** none

## AGY-1880 -- The roadmap's "measured rather than asserted" table is asserted  (WS-DOCS | P1 | M)
**Goal:** The headline metrics are generated from the tree they describe.
**What+How:** `ROADMAP.md` carries a table headed "Honest state, measured rather than asserted". Every figure in it is stale: tracked files 2,383 against 2,597 measured; drift checks 181 against 204 rows in the generated index; units 27 of 66 against 15 faithful of 122 shipped. The table is hand-maintained. Generate it, the way the rollup and workstream index between the existing marker comments already are.
**Where:** `ROADMAP.md`, `tools/roadmap-index.py`, `tools/sync-generated.sh`
**Done When:** the table regenerates from live measurements and a stale figure is impossible.
**Verify:** add a tracked file, regenerate, and confirm the count changes; confirm `sync-generated.sh` leaves the tree clean on a second run.
**Do NOT:** update the numbers by hand as the fix. A hand-corrected table is stale again on the next commit, which is how it reached this state.
**Why:** a document that claims measurement and delivers assertion is worse than one that admits it is a summary.
**Dep:** none

## AGY-1881 -- Six documents reference a gate file that has been renamed  (WS-DOCS | P1 | S)
**Goal:** Doc references resolve to files that exist.
**What+How:** `38-drift-checks.sh` is referenced by `AGY-TASKS.md`, `docs/agy/doc-git-root-unification.md`, `docs/agy/doc-install-family.md`, `docs/agy/impl-db-accounts.md`, `docs/agy/mios-finalization-plan.md` and `docs/agy/verbosity-changelist.md`; the finalization plan alone cites it four times, including specific line numbers. The gate is now `automation/98-drift-checks.sh` and those line numbers no longer point at what the prose describes. Update the references and re-derive the line citations against the current file.
**Where:** `docs/agy/`, `AGY-TASKS.md`
**Done When:** no tracked document references the old path, and every line citation resolves to the construct it describes.
**Verify:** `check_doc_refs_resolve` passes, and each updated line citation is opened and read to confirm it points at the right code.
**Do NOT:** rewrite the paths with a blind substitution. The line numbers must be re-derived; a corrected path with a stale line number is a reference that looks right and is not.
**Why:** the finalization plan is the document the whole P0 spine is read from.
**Dep:** none

## AGY-1882 -- 1,724 narrative comments are unharvested  (WS-DOCS | P2 | L)
**Goal:** The generated manual reflects what the code says about itself.
**What+How:** The roadmap's third campaign counts 1,724 narrative comments not yet harvested into the generated documentation. `manual-corpus.tsv` already censuses them with a STAY/DROP classification. Work the STAY set in batches, harvesting each into the manual surface its file maps to, and drive the unharvested count down as a ratchet.
**Where:** `usr/share/mios/reference/manual-corpus.tsv`, `usr/share/doc/mios/`, `tools/generate-manual.py`
**Done When:** the unharvested count reaches its declared floor and the floor is 0.
**Verify:** after each batch, the corpus census shows the count falling; the manual builds and its links resolve.
**Do NOT:** harvest by moving the comment out of the code. The comment stays where it explains the code; harvesting copies it into the manual.
**Why:** legibility is the stated deliverable, so this is the product, not tidying.
**Dep:** none

## AGY-1883 -- 150 stale references are counted but not enumerated  (WS-DOCS | P2 | M)
**Goal:** The stale-reference count is a work-list.
**What+How:** The roadmap states 150 stale references. Produce the enumerated list with file, line and target, classify each as renamed, deleted or never-existed, and fix them by class. The renamed ones are mechanical; the never-existed ones are the interesting finding.
**Where:** `tools/drift-checks.py`, `docs/`, `usr/share/doc/mios/`
**Done When:** the list is empty and `check_doc_refs_resolve` covers the reference forms that produced it.
**Verify:** re-run the enumeration after each class is cleared and confirm the count falls by the size of that class.
**Do NOT:** delete a reference to clear it without reading what it was pointing at. A reference to something that never existed usually means the thing was planned and not built, which is a finding worth keeping.
**Why:** a count with no list cannot be worked or verified.
**Dep:** AGY-1881

## AGY-1884 -- Man pages are generated but not verified to be readable by man  (WS-DOCS | P2 | M)
**Goal:** `man mios` works in the built image.
**What+How:** `tools/render-manpages.py` renders 136 pages as native roff and the bake keeps `mios*` pages while stripping the rest. Nothing asserts the installed pages are found and formatted by `man(1)` in the image. Add a bake-time check that runs `man` against a sample of pages and asserts non-empty, correctly-sectioned output.
**Where:** `tools/render-manpages.py`, `automation/build.sh`, `tests/bake-smoke.sh`
**Done When:** a malformed page fails the bake.
**Verify:** corrupt one page in a scratch build and show the failure; confirm `man mios-variants` returns section 7.
**Do NOT:** test by grepping the roff source. The question is whether `man` can render it, which only `man` answers.
**Why:** the pages are the user-facing documentation surface and are currently verified only by the generator that wrote them.
**Dep:** none

## AGY-1885 -- The docs ratchet gives different verdicts under different invocations  (WS-DOCS | P1 | S)
**Goal:** `check_docs_ratchet` is deterministic.
**What+How:** The check read red in a sweep and exited 0 when run directly, twice in the same session. Until that is explained, no docs measurement can be believed. Reproduce both invocations, find the input that differs, and remove the dependence.
**Where:** `tools/drift-checks.py`, `automation/98-drift-checks.sh`
**Done When:** the check gives one verdict for one tree state regardless of how it is invoked.
**Verify:** run it under both invocations on the same commit and show identical output.
**Do NOT:** close this by observing that it passes when run directly. It also failed when run another way, and both were the same tree.
**Why:** the docs campaign is measured by this check, so a nondeterministic verdict makes the campaign unmeasurable.
**Dep:** AGY-1861

## AGY-1886 -- The generated documentation has no test that it is complete  (WS-DOCS | P2 | M)
**Goal:** A documented surface that loses its documentation fails.
**What+How:** `generate-manual.py` builds the manual from sources. Nothing asserts every verb, unit and SSOT table has a corresponding entry. Add a coverage assertion with a declared floor, so removing a doc source is caught.
**Where:** `tools/generate-manual.py`, `usr/share/doc/mios/`, `usr/share/mios/mios.toml`
**Done When:** deleting a doc source for a live verb fails.
**Verify:** delete one in a scratch copy and show the failure.
**Do NOT:** assert only that the manual is non-empty or above a page count. Both are satisfied while the specific thing you care about is missing.
**Why:** the fourth campaign requires every surface to be covered or explicitly declared uncovered.
**Dep:** AGY-1882

## AGY-1887 -- ADR index is generated but ADR content is not checked for consistency  (WS-DOCS | P3 | M)
**Goal:** An ADR that contradicts the SSOT is caught.
**What+How:** `generate-adr-index.py` builds `ADR.md` from the ADR set. Several ADRs make claims the SSOT can confirm or refute -- ADR-0003 on SBOM-not-hardcode, ADR-0009 on the one config surface, ADR-0010 on the dotfiles registry. Add checks that the assertions each ADR makes still hold.
**Where:** `usr/share/doc/mios/adr/`, `tools/generate-adr-index.py`, `automation/98-drift-checks.sh`
**Done When:** an ADR whose decision the tree no longer follows is reported.
**Verify:** re-introduce a hand-pinned digest in the SSOT and show the ADR-0003 check fails.
**Do NOT:** check ADR formatting only. Structure conformance is not what an ADR is for.
**Why:** an architecture decision record that no longer describes the architecture is a false statement in the most authoritative document.
**Dep:** none

## AGY-1888 -- Documentation for the deploy plane overstates what works  (WS-DOCS | P1 | M)
**Goal:** The deploy docs say what actually happens.
**What+How:** The USB and Ventoy machinery works but does not install MiOS: the bare-metal leg is broken, `tools/install.sh` is absent, and what runs installs plain Fedora rather than a bootc image. The correct installer is disconnected and unbuilt. Any document describing deployment must state that, so nobody plans against a capability that does not exist.
**Where:** `docs/agy/impl-mios-cat-live-boot.md`, `docs/agy/doc-install-family.md`, `usr/share/doc/mios/`
**Done When:** each deploy document states its capability's real status, with the untested parts named.
**Verify:** follow one document's instructions end to end and record where it diverges from the text.
**Do NOT:** describe the intended design in the present tense. The recorded gap between the deploy documentation and the deploy reality is the reason this task exists.
**Why:** the deploy plane is the least-complete area and the most confidently documented, which is the worst combination.
**Dep:** none

## AGY-1889 -- Documents describe checks by ordinal in prose that the index renumbers  (WS-DOCS | P2 | S)
**Goal:** Prose survives a gate reordering.
**What+How:** Once ordinals are verified to exist (AGY-1817), the remaining hazard is that they are stable identifiers only until a check is inserted. Prefer the function name in prose, with the ordinal as supplementary, and update the existing citations.
**Where:** `docs/`, `usr/share/doc/mios/`, `AGY-TASKS.md`
**Done When:** every citation names the check function, not only its number.
**Verify:** insert a check early in `main()`, regenerate the index, and confirm no document needs editing.
**Do NOT:** remove the ordinals. They are what the gate output prints, so a reader needs both.
**Why:** the tree already carries citations whose numbers no longer match, and renumbering is silent.
**Dep:** AGY-1817

## AGY-1890 -- The finalization plan's P0 items have no completion evidence  (WS-DOCS | P1 | M)
**Goal:** The critical path states which blockers are actually cleared.
**What+How:** `docs/agy/mios-finalization-plan.md` lists ten ordered P0/P1 blockers. Several reference constructs that have since changed. For each, determine whether it is done, and record the evidence -- a commit, a passing check, a measurement -- rather than a status word.
**Where:** `docs/agy/mios-finalization-plan.md`
**Done When:** every spine item carries either evidence of completion or a current task id.
**Verify:** for each item marked done, re-run its stated evidence and confirm it still holds.
**Do NOT:** mark an item done because a related commit exists. The recorded pattern is work that landed adjacent to a blocker without clearing it.
**Why:** the plan says nothing new starts until the spine closes, so an inaccurate spine mis-sequences everything after it.
**Dep:** AGY-1881

## AGY-1891 -- 53 Python heredocs remain embedded in the shell gate  (WS-LANG | P2 | L)
**Goal:** Gate logic lives where it can be imported, linted and tested.
**What+How:** 2,340 lines of Python remain inside `98-drift-checks.sh` as heredocs. Inside one, a syntax error surfaces only when the check runs, and nothing can unit-test the logic. Extract them into `tools/drift-checks.py` subcommands, largest first, following the established procedure: copy the body verbatim, register the subcommand, prove the extracted body parses, splice the shell to call it, then prove old and new give the same verdict on both a clean tree and a broken one.
**Where:** `automation/98-drift-checks.sh`, `tools/drift-checks.py`, `tools/test_drift-checks.py`
**Done When:** no Python heredocs remain in the gate.
**Verify:** for each extraction, source the original function and the folded one side by side and compare verdicts on at least two tree states -- and confirm the interpreter actually resolves in the comparison shell, because a comparison where both sides fail to find python3 agrees for the wrong reason.
**Do NOT:** extract more than one check per commit. A batched extraction makes a behavioural difference impossible to attribute.
**Why:** Law 14 makes bash thin glue, and a check that cannot be tested is a check nobody has verified.
**Dep:** none

## AGY-1892 -- Eleven Rust crates assert nothing while cargo reports ok  (WS-LANG | P1 | L)
**Goal:** A crate's passing test run means something was checked.
**What+How:** `[rust.untested_crates]` records the crates with no meaningful assertions; `cargo test` reports ok for each because a suite with no tests passes. Write tests for the behaviour that matters in each, starting with the crates whose failure is most costly rather than the smallest ones.
**Where:** `tools/native/`, `src/mios-rs/`, `usr/share/mios/mios.toml`
**Done When:** the untested-crate register is empty and each crate's tests fail when its behaviour is broken.
**Verify:** for each crate, break one behaviour and confirm its own suite goes red.
**Do NOT:** add an assert-true test per crate to clear the register. That turns the gate green while the crates still assert nothing, and the register would then be lying with a gate behind it.
**Why:** a green test run over empty suites is the most convincing possible false signal.
**Dep:** AGY-1800

## AGY-1893 -- The Rust coverage gate counts crates, not assertions  (WS-LANG | P2 | M)
**Goal:** The gate measures whether tests can fail.
**What+How:** `tools/check-rust-test-coverage.py` checks crates against the untested register. A crate can leave the register with one trivial test. Strengthen it: require each crate's suite to contain at least one assertion against the crate's own public surface, and consider a mutation spot-check for the load-bearing crates.
**Where:** `tools/check-rust-test-coverage.py`, `tools/native/`, `src/mios-rs/`
**Done When:** a crate whose only test asserts a constant does not satisfy the gate.
**Verify:** add such a test in a scratch copy and show the gate still reports the crate untested.
**Do NOT:** count test functions. The count is what a trivial test satisfies.
**Why:** the register exists because counting was already shown to be insufficient here.
**Dep:** AGY-1892

## AGY-1894 -- Nine verbs evaluate agent-supplied arguments  (WS-LANG | P1 | L)
**Goal:** No verb passes agent input to a shell evaluator.
**What+How:** The debt register records nine verbs using `eval` on arguments that can originate from an agent; ten files under `usr/libexec/mios/` currently use `eval`. Rewrite each to dispatch on an explicit allowlist of actions with arguments passed as array elements, never re-parsed.
**Where:** `usr/libexec/mios/`, `usr/lib/mios/`
**Done When:** no verb reachable from agent input reaches `eval`, and a check enforces it.
**Verify:** for each rewritten verb, pass an argument containing shell metacharacters and confirm it is treated as data.
**Do NOT:** quote the existing `eval` calls more carefully and call it fixed. The requirement is that the evaluator is not reached, not that the current inputs happen to be safe.
**Why:** the agent plane is reachable over the network, which makes this the tree's highest-severity debt item.
**Dep:** none

## AGY-1895 -- The bare-metal install leg does not install MiOS  (WS-DEPLOY | P1 | L)
**Goal:** A bare-metal install produces a bootc MiOS system.
**What+How:** The USB and Ventoy machinery boots, but the install path runs a plain Fedora installation rather than deploying the MiOS bootc image, and `tools/install.sh` referenced by that path is absent. The correct BIB-produced installer exists in design but is disconnected and unbuilt. Wire the built installer into the boot path so the installed system is the OCI image, and make the absent script either exist or stop being referenced.
**Where:** `installation/`, `cat/`, `config/artifacts/`, `automation/`
**Done When:** a USB boot installs a system that reports the MiOS image as its bootc source.
**Verify:** install to a VM disk from the produced media and run `bootc status` on the result; it must name the MiOS image, not a Fedora base.
**Do NOT:** verify by confirming the installer runs without error. Plain Fedora installs without error too -- that is the defect.
**Why:** the deploy plane is the least-complete area of the roadmap and this is its central claim.
**Dep:** none

## AGY-1896 -- `installation/mios-install.sh` swallows the install mode  (WS-DEPLOY | P1 | S)
**Goal:** A positional install type selects that type.
**What+How:** The mirrored bug fixed in the main repo remains in `mios-bootstrap`: `TYPE` initialises empty, the permissive final branch pushes a positional argument such as `bootc` into the passthrough array instead of setting `TYPE`, and the dispatch then falls back to the default. The generator earlier in the same file emits exactly that positional form. Mirror the fix.
**Where:** `mios-bootstrap/installation/mios-install.sh`
**Done When:** the positional form selects the requested type.
**Verify:** compare the dry-run output of the core install for two different positional types and confirm they differ.
**Do NOT:** verify with the flag form only. The broken form is the positional one, and it is the form the script itself generates.
**Why:** Law 15 requires the mirrored surface to match, and an installer that silently installs the wrong type is the worst kind of quiet failure.
**Dep:** none

## AGY-1897 -- The image cannot update itself  (WS-DEPLOY | P1 | M)
**Goal:** A running MiOS can move to a newer image.
**What+How:** The self-update path is unproven end to end. Establish what `bootc switch` and the update verb do on a live system, fix what is broken, and prove a running instance can reach a newer image and roll back.
**Where:** `usr/libexec/mios/`, `usr/share/mios/mios.toml`, `installation/`
**Done When:** a running instance updates to a newer image and rolls back to the previous one.
**Verify:** perform both directions on a VM and record the boot entries at each step.
**Do NOT:** test only the happy path. Rollback after a failed boot is the property that makes updating safe, and greenboot is what should trigger it.
**Why:** an OS that cannot update itself is a one-shot install.
**Dep:** AGY-1895

## AGY-1898 -- Offline dependencies are not staged or verified  (WS-DEPLOY | P1 | L)
**Goal:** An install works with no network.
**What+How:** The offline bootstrap needs a local source, integrity verification and a real seed. `bootc install --transport oci` can install from a local OCI archive, which fills the bare-metal leg without a registry. Stage the required artefacts onto the media, verify them by digest at install time, and prove the whole path with the network disconnected.
**Where:** `installation/`, `cat/`, `automation/`, `config/artifacts/`
**Done When:** an install completes on a machine with no route to a registry.
**Verify:** disconnect the network on the installing VM and complete an install; a pull attempt during it is a failure of this task.
**Do NOT:** treat a cached registry as offline. The test is no route, not a warm cache.
**Why:** sovereignty over the deploy path is the stated purpose of the portable media.
**Dep:** AGY-1895

## AGY-1899 -- The two build entry points diverge in functionality  (WS-DEPLOY | P1 | M)
**Goal:** One build script, one behaviour.
**What+How:** Two `build-mios.sh` exist with different capabilities, and the deploy fold intends to collapse the installer and the portable-media launcher into one. They diverge today, and one path can loop into the other. Determine the union of behaviour, implement it once, and make the other a thin shim that forwards.
**Where:** `installation/`, `cat/`, `automation/`
**Done When:** one implementation carries the behaviour and the shim only forwards.
**Verify:** run the same command through both entry points and compare the resulting artefacts byte for byte.
**Do NOT:** delete the apparently-dead files without tracing what references them. The recorded finding is that several are documentation-bundled or serve as entry points, so blind deletion orphans a working path.
**Why:** two implementations of the build guarantee that one is untested.
**Dep:** none

## AGY-1900 -- The portable-media working name collides with a coreutils command  (WS-DEPLOY | P3 | S)
**Goal:** The product name does not shadow a standard command.
**What+How:** The portable USB and Ventoy edition is called MiOS-Cat as a working title, and `cat` collides with a coreutils command in documentation, shell completion and search. Research produced `MiOS-Field` as the recommendation with `MiOS-Depot` as runner-up. This is a product decision for the operator; prepare the rename so it is one commit once decided, and list every surface it touches.
**Where:** `cat/`, `installation/`, `docs/`, `usr/share/mios/mios.toml`
**Done When:** the surface list is complete and a rename can be executed mechanically.
**Verify:** the list is produced by searching the tree, not from memory, and includes the variants registry and the launcher filenames.
**Do NOT:** perform the rename before the operator chooses. Naming is theirs; preparing it is not.
**Why:** the name appears in the variants registry, so it is load-bearing rather than cosmetic.
**Dep:** none

## AGY-1901 -- The three launchers are not proven byte-identical where they must be  (WS-DEPLOY | P2 | M)
**Goal:** The pinned content in each launcher matches.
**What+How:** The launcher family is a canonical PowerShell script, a name-stable batch shim and a Linux shell script. Their pins must stay byte-identical, and the web-door install path can orphan the portable launcher unless it hands off. Add a check comparing the pinned regions across all three.
**Where:** `installation/`, `cat/`, `tools/`
**Done When:** a pin changed in one launcher and not the others fails a check.
**Verify:** change one pin in a scratch copy and show the failure.
**Do NOT:** compare whole files. They are different languages; compare the declared pinned regions.
**Why:** a launcher family that drifts installs different things depending on which door was used.
**Dep:** AGY-1899

## AGY-1902 -- Deployment formats are declared but not all exercised  (WS-DEPLOY | P2 | L)
**Goal:** Every declared deployment format is proven to work.
**What+How:** `[deploy.formats]` declares the supported formats, including the WSL and WSLg paths, and `tools/check-deploy-formats.py` checks the declaration. Nothing exercises each format end to end. Build a matrix job that produces and boots each declared format, and record which are proven versus declared.
**Where:** `usr/share/mios/mios.toml`, `tools/check-deploy-formats.py`, `.github/workflows/`
**Done When:** each format is either exercised in CI or explicitly marked unexercised with a reason.
**Verify:** the matrix output lists every declared format with a result; a format with no result fails.
**Do NOT:** mark a format supported because its build step succeeds. Producing an artefact is not booting it.
**Why:** a declared format nobody has booted is a promise, not a capability.
**Dep:** none

## AGY-1903 -- Repair tooling exists but has no failure fixtures  (WS-DEPLOY | P2 | M)
**Goal:** The repair path is tested against real breakage.
**What+How:** The deploy plane includes repair tooling. It is exercised only against healthy systems. Build fixtures for the failures it claims to repair -- a failed boot entry, a corrupt overlay, a half-applied update -- and assert the tooling recovers each.
**Where:** `usr/libexec/mios/`, `tests/`, `installation/`
**Done When:** each repair capability has a fixture that reproduces the failure it fixes.
**Verify:** run each repair against its fixture and confirm the system reaches a known-good state.
**Do NOT:** assert only that the repair command exits 0 on a healthy system. That is the case where repair has nothing to do.
**Why:** repair tooling is used exactly once, under pressure, on a system that is already broken.
**Dep:** AGY-1897

## AGY-1904 -- Ventoy media layout is not verified after build  (WS-DEPLOY | P2 | M)
**Goal:** Produced media has the layout the boot path expects.
**What+How:** The media build assembles a layout the bootloader and install path depend on. Nothing verifies the produced image contains the expected paths, or that the config the loader reads matches what was intended. Add a post-build verification that mounts the artefact and asserts its structure.
**Where:** `cat/`, `automation/`, `tests/`
**Done When:** a media build missing a required path fails before it is written to a device.
**Verify:** remove one required file from the staging tree and confirm the build fails naming it.
**Do NOT:** verify by file count or total size. A missing loader config leaves both plausible.
**Why:** the failure otherwise appears as an unbootable USB stick on someone's desk.
**Dep:** AGY-1895

## AGY-1905 -- Installed systems are not checked against the image they claim to be  (WS-DEPLOY | P2 | M)
**Goal:** An installed system can prove which image it is.
**What+How:** After install, the system should be able to state its image digest and have that match the media it came from. Record the source digest at install time and add a verb that compares the running system against it.
**Where:** `usr/libexec/mios/`, `installation/`, `usr/share/mios/artifacts/sbom/`
**Done When:** a system installed from known media reports a digest matching that media.
**Verify:** install from a built artefact and compare the reported digest to the artefact's recorded digest.
**Do NOT:** record the digest from the registry at install time. If the media is offline, the registry is not the source, and the point is provenance of what was actually installed.
**Why:** provenance is what makes the SBOM claim meaningful on a deployed machine rather than in the build log.
**Dep:** AGY-1898

## AGY-1906 -- Kickstart runs attended and blocks unattended install  (WS-DEPLOY | P1 | M)
**Goal:** The install completes without a human at the console.
**What+How:** The finalization spine records that the kickstart total-root-merge does not run unattended. Find what prompts, supply it from the declared configuration, and prove a full unattended run.
**Where:** `installation/`, `config/artifacts/`
**Done When:** an install completes with no console interaction.
**Verify:** run the install with the console detached and confirm it completes.
**Do NOT:** suppress prompts by answering them with defaults blindly. Each prompt is a decision; source each answer from the SSOT so it is declared rather than defaulted.
**Why:** an attended installer cannot be used for fleet deployment, which is the stated purpose.
**Dep:** AGY-1895

## AGY-1907 -- The Fedora ISO source is a 404 stub with no validity gate  (WS-DEPLOY | P1 | S)
**Goal:** A broken upstream source fails at build time, not at boot time.
**What+How:** The spine records a Fedora ISO 404 stub and a missing validity gate. Fetch failures currently produce an artefact that fails later. Verify the source resolves and its checksum matches before it is used, and fail the build if not.
**Where:** `installation/`, `automation/`, `config/artifacts/`
**Done When:** an unreachable or mismatched source fails the build naming the URL and expected digest.
**Verify:** point the source at a nonexistent URL and confirm the build fails immediately.
**Do NOT:** fall back to a cached copy on fetch failure without saying so loudly. A silent fallback is how a stale base image ships.
**Why:** the failure currently surfaces as an unbootable artefact, far from its cause.
**Dep:** none

## AGY-1908 -- The variants registry is declared but variants are not built  (WS-DEPLOY | P2 | L)
**Goal:** Each declared edition is produced and testable.
**What+How:** `[variants]` declares the product line including the Metal edition, and `tools/check-variant-registry.py` validates the registry. No pipeline produces a per-variant artefact. Define what differs per variant, project it, and build each.
**Where:** `usr/share/mios/mios.toml`, `tools/check-variant-registry.py`, `automation/`
**Done When:** each declared variant has a produced artefact and a statement of what makes it that variant.
**Verify:** build two variants and diff their manifests; the difference must match the declared difference.
**Do NOT:** produce variants by post-processing one image without declaring the difference in the SSOT. That reintroduces per-variant hand-maintenance.
**Why:** a registry of editions that all resolve to the same image is a naming scheme, not a product line.
**Dep:** AGY-1902

## AGY-1909 -- Firstboot tier evicts large images and the split is undocumented  (WS-DEPLOY | P2 | M)
**Goal:** The publish path's tier split is explicit and checked.
**What+How:** Publishing works by evicting the largest inference sidecars from the bake into a firstboot tier so the build fits a standard runner. Which images are in which tier is a capacity decision that currently lives in the build logic. Declare the split in the SSOT, project it, and check that the bake's total stays within the runner's capacity.
**Where:** `usr/share/mios/mios.toml`, `automation/build.sh`, `.github/workflows/mios-ci.yml`
**Done When:** the tier assignment is declared and a bake that would exceed capacity fails before it runs.
**Verify:** move an image between tiers in the SSOT and confirm the bake plan changes accordingly.
**Do NOT:** hardcode the capacity threshold in the workflow. It is a property of the runner and belongs with the other declared limits.
**Why:** the publish path has failed on capacity before, and the recovery was a manual tier decision nobody recorded.
**Dep:** none

## AGY-1910 -- Every file in the image must be generated-and-gated or declared authored  (WS-PROJ | P1 | L)
**Goal:** No file exists in the image that the thesis does not account for.
**What+How:** The fourth open campaign requires each file to be either projected from the SSOT and gated, or explicitly declared as authored. Produce the census: walk the built image, classify each path, and record the unclassified set. That set is the campaign's work-list.
**Where:** `automation/build.sh`, `usr/share/mios/mios.toml`, `tools/`
**Done When:** the unclassified set is empty and a new unclassified file fails a check.
**Verify:** add an unclassified file to the image in a scratch build and show the failure.
**Do NOT:** classify by directory. The recorded pattern is that generated and authored files share directories, so a directory rule mislabels both.
**Why:** the surfaces nobody has classified are exactly where hand-maintained configuration survives.
**Dep:** none

## AGY-1911 -- Authored files have no declared owner or reason  (WS-PROJ | P2 | M)
**Goal:** An authored file says why it is not projected.
**What+How:** Once AGY-1910 produces the classification, each authored file needs a reason: it is vendored, it is a template consumed at install time, or it is not yet projected. The third category is a backlog; the first two are permanent and should say so.
**Where:** `usr/share/mios/mios.toml`, `tools/`
**Done When:** every authored file carries a reason and the not-yet-projected count is a declining ratchet.
**Verify:** add an authored file without a reason and show the failure.
**Do NOT:** default the reason to vendored. That is the category that exempts a file permanently, so it must be chosen deliberately.
**Why:** without reasons, the authored set becomes the place things go to stop being questioned.
**Dep:** AGY-1910

## AGY-1912 -- Vendored assets are two thirds of the tracked size and unaudited  (WS-PROJ | P3 | M)
**Goal:** Vendored content is identified, versioned and justified.
**What+How:** The roadmap notes two vendored assets are most of the 198 MB tracked size, and the tree vendors an upstream install script. Record for each what it is, where it came from, its upstream version, and what would replace it.
**Where:** `usr/share/mios/vendored/`, `usr/share/mios/mios.toml`
**Done When:** every vendored asset has provenance recorded and a check asserts it.
**Verify:** add a vendored file without provenance and show the failure.
**Do NOT:** replace a vendored script with a network fetch as the fix. Vendoring is what makes the offline install possible.
**Why:** vendored code is code the project ships and does not review.
**Dep:** AGY-1911

## AGY-1913 -- The SSOT has tables nothing reads  (WS-PROJ | P2 | M)
**Goal:** Every declared table has a consumer.
**What+How:** `check_no_inert_ssot_tables` exists to catch tables nothing reads, and `[accounts].db_backed` is a recorded case of a key that ships inert deliberately. Confirm the check covers nested tables, and require an inert table to declare itself inert with a reason.
**Where:** `tools/drift-checks.py`, `usr/share/mios/mios.toml`
**Done When:** a new table with no consumer fails unless declared inert with a reason.
**Verify:** add an unread table and show the failure; declare it inert and show it clears.
**Do NOT:** delete inert tables to clear the check. Some are declared ahead of the code that will read them, which is legitimate when stated.
**Why:** a config key nobody reads is a promise to the operator that nothing keeps.
**Dep:** none

## AGY-1914 -- Generated artefacts are not all covered by the artefacts step  (WS-PROJ | P1 | M)
**Goal:** Every generated file is regenerated and diffed in CI.
**What+How:** The artefacts step runs `sync-generated.sh` and fails if the tree changes. That only covers what the script regenerates. Cross-check the set of files any generator writes against the set the script refreshes, and close the gap.
**Where:** `tools/sync-generated.sh`, `tools/`, `.github/workflows/mios-ci.yml`
**Done When:** every generator's output is refreshed by the sync step.
**Verify:** hand-edit each generated file in turn and confirm the artefacts step reports it.
**Do NOT:** add generators to the sync script without checking their ordering. One generator's output is another's input, and the recorded ordering asymmetry produced a file refreshed in one run and not the next.
**Why:** a generated file outside the sync step drifts permanently and silently.
**Dep:** none

## AGY-1870 -- `[ports]` fallbacks can mask a missing key  (WS-SSOT | P2 | M)
**Goal:** A missing port key fails instead of silently defaulting.
**What+How:** Units and scripts read ports as `${MIOS_PORT_X:-NNNN}`. If the key is never rendered, the literal fallback is used and everything appears to work until the SSOT value changes and nothing moves. `tools/check-port-fallbacks.py` exists; confirm it asserts every fallback's key is actually rendered into globals, and that the fallback equals the rendered value.
**Where:** `tools/check-port-fallbacks.py`, `automation/lib/globals.sh`, `usr/share/mios/mios.toml`
**Done When:** a fallback whose key is not rendered fails, and a fallback that disagrees with its key fails.
**Verify:** change one rendered port without changing its fallback and show the violation.
**Do NOT:** delete the fallbacks. They are what keeps a unit startable when the environment file is missing; the requirement is that they agree, not that they vanish.
**Why:** a fallback that silently disagrees with the SSOT is a hardcode with a plausible disguise.
**Dep:** AGY-1865

## AGY-1871 -- Version is declared in three places at conflicting values  (WS-SSOT | P1 | M)
**Goal:** One version value, derived everywhere.
**What+How:** The debt register records three `mios.toml` copies at conflicting versions. Establish which is authoritative, project the others from it, and add a check that all reported versions agree -- including what the built image reports.
**Where:** `usr/share/mios/mios.toml`, `tools/`, `automation/`
**Done When:** every version string in the tree and the image derives from one key.
**Verify:** bump the key and confirm every reported version moves together, including `mios --version` in the image.
**Do NOT:** reconcile by editing the other two to match. That is the state this task exists to make impossible.
**Why:** version disagreement makes provenance claims unverifiable.
**Dep:** none

## AGY-1872 -- 79 version-duplicate pairs remain in the key namespace  (WS-SSOT | P2 | L)
**Goal:** One key per value across the namespace.
**What+How:** The global unification plan records 79 pairs of keys carrying the same value under different names, within a 2,523-key `MIOS_*` namespace. Collapse each pair to one derived value, using the plan's lossless-diff gate: capture the environment before and after and require the difference to be exactly the intended one.
**Where:** `usr/share/mios/mios.toml`, `automation/lib/globals.sh`, `tools/render-globals.py`
**Done When:** the duplicate-pair count reaches zero.
**Verify:** the env snapshot before and after each batch differs only in the keys intended, with root paths normalised so the comparison works in CI.
**Do NOT:** rename keys with a text substitution. The recorded hazard is that old and new values alias each other, which makes a blind substitution silently wrong.
**Why:** duplicate keys are how two consumers end up configured differently while appearing to read the same setting.
**Dep:** AGY-1871

## AGY-1873 -- The SSOT resolver has two implementations that can disagree  (WS-SSOT | P1 | M)
**Goal:** One resolver answers every SSOT question.
**What+How:** `check_resolver_differential_parity` exists to compare resolver implementations. Confirm it covers every entry point that reads `mios.toml` -- the shared Python resolver, the shell path, and any Rust reader -- and that it compares resolved values rather than just parse success.
**Where:** `usr/lib/mios/mios_toml.py`, `tools/drift-checks.py`, `tools/native/`
**Done When:** a divergence in any resolver's answer for any key fails.
**Verify:** make one resolver return a different default in a scratch copy and show the parity check fails.
**Do NOT:** compare only keys both resolvers know. A key one resolver cannot see is the most important disagreement.
**Why:** the SSOT is only single if every reader reads it the same way.
**Dep:** none

## AGY-1874 -- Theme surfaces are projected but new surfaces are not required to be  (WS-SSOT | P2 | M)
**Goal:** A new themed surface must derive from `[colors]`.
**What+How:** The theme engine projects every surface from the SSOT colours and a drift-check enforces it. Nothing requires a newly-added surface to join the registry, so a new config file with hardcoded colours passes. Add a check that scans for colour literals outside the registry.
**Where:** `usr/libexec/mios/mios-theme-render`, `usr/share/mios/mios.toml`, `automation/98-drift-checks.sh`
**Done When:** a colour literal in an unregistered surface fails.
**Verify:** add a hex colour to an unregistered config in a scratch copy and show the violation.
**Do NOT:** flag colour literals inside the theme engine itself or the palette definition. Those are the source.
**Why:** the projection is only complete if joining it is mandatory.
**Dep:** none

## AGY-1875 -- The dotfiles registry has one proven surface  (WS-SSOT | P2 | L)
**Goal:** The cross-platform dotfiles claim is proven on more than one application.
**What+How:** `[dotfiles.registry.*]` generalises the theme projection to arbitrary settings surfaces, with one application as the landed proof on both platforms. Add the next surfaces, each with a settings projection, an apply-to-live-home path, and a drift gate.
**Where:** `usr/share/mios/mios.toml`, `usr/libexec/mios/`, `tools/`
**Done When:** at least three surfaces project settings on both platforms and are gated.
**Verify:** change a setting in the SSOT and confirm each surface's live config changes on both platforms.
**Do NOT:** project only the colours for the new surfaces. The registry's claim is settings, and colours are the part already proven.
**Why:** one proof point does not establish a cross-platform mechanism.
**Dep:** AGY-1874

## AGY-1876 -- `[code_mode]` and `[pgvector]` keys were moved to reachable tables without consumer tests  (WS-SSOT | P2 | S)
**Goal:** A relocated key is proven to still be read.
**What+How:** Several keys were moved into reachable tables to satisfy the inert-table check. Moving a key changes its resolved name; if a consumer was reading the old path, it now silently gets a default. For each relocated key, assert the consumer resolves the new location.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/mios/`, `usr/libexec/mios/`
**Done When:** every relocated key has a test asserting its consumer reads the current location.
**Verify:** change each key's value and confirm the consumer's behaviour changes.
**Do NOT:** assume the consumer follows because the check passes. The inert-table check asserts something reads the table, not that this consumer reads this key.
**Why:** satisfying a structural check by moving a key is exactly the kind of edit that quietly disconnects a setting.
**Dep:** AGY-1913

## AGY-1877 -- The memory-poisoning scan is disabled while the SSOT says it runs  (WS-SEC | P1 | S)
**Goal:** A declared security control is actually enabled.
**What+How:** The scan was found disabled in code while the SSOT declared its mode as active. Re-enable it, then add a check that compares declared security controls against their runtime state so the pair cannot diverge again.
**Where:** `usr/lib/mios/agent-pipe/`, `usr/share/mios/mios.toml`
**Done When:** the scan runs in the mode the SSOT declares, and a disagreement fails a check.
**Verify:** disable it in a scratch copy without changing the SSOT and show the violation.
**Do NOT:** align the SSOT to the code. The declaration is the intent; the code is what failed to implement it.
**Why:** a security control that is declared and not running is worse than one that is honestly absent.
**Dep:** none

## AGY-1878 -- Privileged Quadlets are enumerated but their privilege is not justified  (WS-SEC | P2 | M)
**Goal:** Each privileged unit records why it needs privilege.
**What+How:** Ten units are declared root-privileged. Record for each the specific capability it needs and why, then check that no unit holds privilege beyond its recorded justification.
**Where:** `usr/share/mios/mios.toml`, `usr/share/containers/systemd/`
**Done When:** every privileged unit has a recorded justification and no unit exceeds it.
**Verify:** add a capability to a unit beyond its justification and show the violation.
**Do NOT:** justify privilege as required by the application without naming the operation. A justification that cannot be checked is a comment.
**Why:** privilege granted without a recorded reason is never revoked, because nobody knows what would break.
**Dep:** AGY-1842

## AGY-1879 -- Secrets handling has no test that a secret cannot reach a log  (WS-SEC | P1 | M)
**Goal:** A credential never appears in output.
**What+How:** The agent plane, the registry client and the install path all handle credentials. Add tests that pass a recognisable sentinel value as each credential and assert it appears in no log, no error message and no generated artefact.
**Where:** `usr/lib/mios/agent-pipe/`, `usr/libexec/mios/`, `installation/`
**Done When:** a sentinel credential is provably absent from all output on both the success and failure paths.
**Verify:** run each path with the sentinel and grep every produced artefact and log for it.
**Do NOT:** test only the success path. Error paths print more, and are where credentials usually escape.
**Why:** a leaked credential in a build log is published the moment the log is.
**Dep:** none

## AGY-1915 -- The agent-pipe server has no boundary test for use-before-definition  (WS-AI | P1 | M)
**Goal:** A relocation cannot reintroduce a name error at import time.
**What+How:** A function relocation once produced a use-before-definition error that only surfaced at runtime and blocked the publish bake. The AST-plus-virtualenv-import method that found it should be a check: parse each module, assert every module-level name is defined before use, and import each in a clean environment.
**Where:** `usr/lib/mios/agent-pipe/`, `tools/`
**Done When:** a use-before-definition anywhere in the agent-pipe fails a check.
**Verify:** move a function below its first use in a scratch copy and show the failure.
**Do NOT:** rely on importing alone. Some paths are conditional and import cleanly while still being wrong; the AST pass is what catches those.
**Why:** this class of error blocked a publish once and is invisible to every syntax check.
**Dep:** none

## AGY-1916 -- The AI plane is /v1-only but legacy tokens survive in live paths  (WS-AI | P2 | M)
**Goal:** No live code path references a retired backend.
**What+How:** The retired local-inference backends were removed and the plane is OpenAI-compatible only. Surviving mentions fall into legitimate categories -- enforcement rules, cleanup code, upstream compatibility and history -- and those must not be re-flagged. Enumerate the remaining references, classify each, and gate the live-path ones.
**Where:** `usr/lib/mios/`, `usr/libexec/mios/`, `usr/share/mios/mios.toml`
**Done When:** every surviving reference is classified and no live path reaches a retired backend.
**Verify:** the classification is produced by reading each site, and the gate flags a newly-added live reference.
**Do NOT:** flag the enforcement and cleanup references. They mention the retired names by necessity, and re-flagging them is how a previous sweep wasted effort.
**Why:** a retired lane that is still reachable is a service that answers when it should not exist.
**Dep:** none

## AGY-1917 -- 16 references to dead ports were found once and the class is not gated  (WS-AI | P2 | M)
**Goal:** A reference to a retired port fails.
**What+How:** Sixteen live references to dead ports were found and fixed. Nothing prevents the next one. Derive the retired-port set from the SSOT's own history of allocations and flag references to it.
**Where:** `usr/share/mios/mios.toml`, `automation/98-drift-checks.sh`
**Done When:** a reference to a retired port fails, naming the port and its replacement.
**Verify:** add a reference to a retired port in a scratch copy and show the violation.
**Do NOT:** hardcode the retired list in the check. It belongs in the SSOT alongside the allocations, or it becomes another hand-maintained list.
**Why:** the recorded fix was manual, so the same drift will recur.
**Dep:** AGY-1865

## AGY-1918 -- The CRDT convergence fix has no multi-peer test  (WS-AI | P1 | M)
**Goal:** Deletes replicate, provably, across more than two peers.
**What+How:** The state store previously replicated only live elements, so tombstones never reached peers and a delete on one node was resurrected by another. The fix replicates all elements including tombstones. Add a test with three or more peers, partitions and reordered delivery, asserting convergence.
**Where:** `src/mios-rs/mios-node/src/state_sync.rs`, `src/mios-rs/mios-node/src/node.rs`
**Done When:** a multi-peer test with partition and reorder converges, and reverting the fix makes it fail.
**Verify:** revert to replicating only live elements and confirm the test goes red.
**Do NOT:** test with two peers and immediate delivery. That configuration passes with the bug present, which is why it survived.
**Why:** an eventually-consistent store that resurrects deleted records corrupts state silently and permanently.
**Dep:** AGY-1892

## AGY-1919 -- MiOS is meant to be the podman machine and is not  (WS-HOST | P2 | L)
**Goal:** The development host runs MiOS as its container host.
**What+How:** MiOS is both the hosted image and the host. The rootfs export for the development machine reached 44 GB but the cutover was never performed, and the machine must live on the larger volume because the system drive cannot hold the image; junctions break the machine initialisation, so the supported move mechanism must be used. Complete the cutover and document it.
**Where:** `docs/`, `installation/`, `automation/`
**Done When:** the development podman machine runs MiOS and builds the image.
**Verify:** run a build inside the MiOS-hosted machine and confirm the artefact matches one built elsewhere.
**Do NOT:** place the machine on the system drive. It has been measured as too small for the image, and the failure appears mid-build.
**Why:** the self-hosting claim is untested until the host is MiOS.
**Dep:** AGY-1863

## AGY-1920 -- WSL units ship but the WSL host path is unproven  (WS-HOST | P2 | M)
**Goal:** MiOS runs as a WSL distribution with its declared units active.
**What+How:** The image ships WSL-specific units and a build target for them. Prove the path: import the image as a WSL distribution, confirm the WSL firstboot unit runs, and confirm the graphical path works where declared.
**Where:** `usr/lib/systemd/system/`, `Justfile`, `usr/share/mios/mios.toml`
**Done When:** a documented command produces a working WSL distribution running the declared units.
**Verify:** import, boot, and list the active MiOS units inside it.
**Do NOT:** count a shell prompt as success. The units are the deliverable; a distribution that boots without them is a Fedora shell.
**Why:** WSL is one of the declared deployment formats and is currently declared rather than demonstrated.
**Dep:** AGY-1902

## AGY-1921 -- Database-backed accounts ship inert with no path to active  (WS-ACCT | P2 | L)
**Goal:** The account model has a working first implementation.
**What+How:** The cross-platform account design assembles from existing components: a userdb JSON projection on Linux, a directory service over the database as the cross-platform face, and the Windows baseline which has no equivalent name-service integration. The key that would enable it ships inert. Implement the Linux leg first, since it is the one with a native integration point.
**Where:** `usr/libexec/mios/mios-userdb-render`, `usr/share/mios/mios.toml`, `docs/agy/doc-postgresos-accounts.md`
**Done When:** an account declared in the database resolves through the system name service on Linux.
**Verify:** declare an account, render, and resolve it with a standard user lookup.
**Do NOT:** design for Windows parity in this task. Windows has no name-service integration point, so the two legs are different mechanisms and conflating them stalls both.
**Why:** an inert key is a promise; the Linux leg is the part that can be made real now.
**Dep:** none

## AGY-1922 -- The template system has golden files but no coverage requirement  (WS-TEMPLATE | P2 | M)
**Goal:** Every file type that has a template is produced by it.
**What+How:** The compiled template system defines one template per file type with a golden comparison. Nothing requires a new file of a templated type to be produced from the template. Add a check that files matching a templated type conform to their template.
**Where:** `usr/share/mios/templates/`, `tests/templates/golden/`, `tools/`
**Done When:** a hand-written file of a templated type fails until it conforms.
**Verify:** add a non-conforming file of a templated type and show the violation.
**Do NOT:** enforce conformance on vendored or generated files. They have other owners, and flagging them trains people to ignore the check.
**Why:** a template nothing must use is a suggestion.
**Dep:** AGY-1828

## AGY-1923 -- The CI suite registry's tier assignment is not validated against runtime  (WS-CI | P2 | M)
**Goal:** A suite runs in the tier it is declared in.
**What+How:** `[ci.tiers]` assigns suites to lint, unit and gate tiers, and `tests/run-suites.sh` runs a tier to completion reporting every failure. Nothing asserts a suite declared in one tier actually ran in that tier. Have the runner emit what it ran and compare against the declaration.
**Where:** `tools/ci-suites.py`, `tests/run-suites.sh`, `usr/share/mios/mios.toml`
**Done When:** a suite that did not run in its declared tier fails.
**Verify:** make one suite exit before running in a scratch copy and confirm the mismatch is reported.
**Do NOT:** compare counts. A suite that silently skipped and one that ran and passed produce the same count.
**Why:** the registry is the single list both publishers run from, so its accuracy is the whole point.
**Dep:** AGY-1824

## AGY-1924 -- Both publishers must produce bit-for-bit identical images and it is unverified  (WS-CI | P1 | L)
**Goal:** The two publishers are provably equal.
**What+How:** The two forges are declared equal publishers. Nothing compares their outputs. Build the same commit on both and compare the resulting image digests and manifests, then investigate every difference until the only remaining ones are declared and justified.
**Where:** `.github/workflows/mios-ci.yml`, `.forgejo/workflows/build-mios.yml`, `automation/build.sh`
**Done When:** the same commit produces the same image digest on both, or every difference is declared.
**Verify:** build one commit on both and diff the manifests.
**Do NOT:** compare only the tags. Two images can carry the same tag and different content, which is exactly what this checks for.
**Why:** equal publishers that produce different images means the provenance of a deployed system depends on which forge built it.
**Dep:** AGY-1909

## AGY-1925 -- Greenboot is shipped but no failure is proven to roll back  (WS-RUNTIME | P1 | M)
**Goal:** A failed boot returns to the previous image automatically.
**What+How:** The runtime stack ships greenboot but its health checks and rollback have not been exercised. Define the health checks that represent a working MiOS, then prove that a deliberately broken image rolls back.
**Where:** `usr/lib/systemd/system/`, `usr/libexec/mios/`, `usr/share/mios/mios.toml`
**Done When:** an image that fails its health check rolls back to the previous deployment unattended.
**Verify:** deploy a deliberately broken image to a VM and confirm it returns to the previous one without intervention.
**Do NOT:** define the health check as the system booted. That is satisfied by a system with every service failing.
**Why:** rollback is the property that makes an immutable OS safe to update automatically.
**Dep:** AGY-1897

## AGY-1926 -- Shipped runtime components are unwired  (WS-RUNTIME | P2 | L)
**Goal:** A component that ships is either wired or declared unwired.
**What+How:** The image ships several capability components -- boot integrity, time sync, GPU compute, clustered storage, device mediation, directory services, power management, remote access and virtual-machine conversion -- that are present but not wired into the SSOT or any unit. For each, either wire it with a declared configuration or record it as shipped-unwired with a reason.
**Where:** `usr/share/mios/mios.toml`, `usr/lib/systemd/system/`, `automation/build.sh`
**Done When:** every shipped component is wired or declared, and a newly-shipped unwired component fails.
**Verify:** add a package to the image without wiring or declaring it and show the failure.
**Do NOT:** wire a component without a test that it works. An unwired component is honest; a wired one that fails silently is worse.
**Why:** the image's size and attack surface are paid for whether or not the components are used.
**Dep:** AGY-1910

## AGY-1927 -- `/var` persistence assumptions are undocumented and untested  (WS-RUNTIME | P2 | M)
**Goal:** What survives an update is stated and verified.
**What+How:** In this runtime model `/var` persists across updates while the rest of the image is replaced. Any state written outside `/var` is lost on update. Enumerate what the system writes at runtime, confirm each location's persistence expectation, and test it across an update.
**Where:** `usr/lib/systemd/system/`, `usr/libexec/mios/`, `usr/share/doc/mios/`
**Done When:** every runtime write location has a stated persistence expectation, verified across an update.
**Verify:** write a marker to each location, update the image, and confirm which markers survive matches the documentation.
**Do NOT:** assume a service's default state directory is correct. The recorded hazard is exactly that a default lands outside the persisted tree.
**Why:** state loss on update is discovered by the operator, after the update.
**Dep:** AGY-1897

## AGY-1928 -- The desktop shell effects are hardcoded rather than projected  (WS-DESKTOP | P2 | M)
**Goal:** The compositor configuration derives from the SSOT.
**What+How:** The desktop shell direction is a layered-glass aesthetic built on the compositor and shell toolkit the image already carries. The compositor's effects and animation settings should be projected from `mios.toml` like every other surface, not hand-written.
**Where:** `usr/share/mios/mios.toml`, `usr/libexec/mios/mios-theme-render`, the compositor configuration
**Done When:** changing an effect value in the SSOT changes the live compositor configuration.
**Verify:** change one value, re-render, and confirm the compositor config changes.
**Do NOT:** project only the colours. Colours are already projected; the effects and animation parameters are the gap.
**Why:** a hand-written compositor config is a hand-maintained surface in the most visible part of the product.
**Dep:** AGY-1874

## AGY-1929 -- The split-plane host design has no executable proof  (WS-METAL | P2 | L)
**Goal:** The hypervisor-host design is demonstrated at the smallest useful scale.
**What+How:** The Metal direction is a small bootc host that binds discrete GPUs to passthrough and owns the physical devices, hosting the full image as a privileged guest with no network interface of its own, routed over the mesh. Critic review established that GPU fractioning without vendor drivers is not possible, so a GPU goes wholly to one guest, and the host floor is around a gigabyte rather than negligible. Build the smallest configuration that demonstrates it.
**Where:** `docs/agy/doc-mios-metal.md`, `automation/`, `usr/share/mios/mios.toml`
**Done When:** a host boots, passes a GPU through to a guest running the image, and the guest reaches the network over the mesh.
**Verify:** run a GPU workload inside the guest and confirm the host has no direct route.
**Do NOT:** plan for GPU fractioning. It was established as impossible without vendor drivers, and designing around it invalidates the topology.
**Why:** the design is currently observation-free, and the roadmap says blade and mesh behaviour is design rather than observation.
**Dep:** AGY-1902

## AGY-1930 -- Bare metal is untried  (WS-METAL | P1 | L)
**Goal:** MiOS has run on physical hardware.
**What+How:** The roadmap records that MiOS runs on a virtual machine and under the Windows subsystem, and that bare metal is untried. Install to physical hardware, record what fails, and fix the blocking issues.
**Where:** `installation/`, `automation/`, `docs/`
**Done When:** a physical machine boots MiOS from installed media and reaches its declared targets.
**Verify:** record the boot on hardware, list active units, and confirm the GPU and network devices are the physical ones.
**Do NOT:** substitute nested virtualisation. Every property this is meant to test -- firmware, secure boot, real device binding -- is exactly what virtualisation abstracts away.
**Why:** every hardware-facing claim in the tree is currently unobserved.
**Dep:** AGY-1895

## AGY-1931 -- Signed boot artefacts are described but not verified end to end  (WS-SEC | P1 | M)
**Goal:** A signed kernel image is verified by firmware at boot.
**What+How:** The runtime design uses a signed unified kernel image with enrolled keys, and the recorded correction is that the enrolled machine-owner key is not the same thing as the unified image itself. Prove the chain: build a signed image, enrol the key, boot with secure boot enabled, and confirm verification occurred.
**Where:** `automation/build.sh`, `usr/lib/kernel/`, `installation/`
**Done When:** a machine with secure boot enabled boots the signed image and reports verification.
**Verify:** boot with secure boot on, then attempt an unsigned image and confirm it is refused.
**Do NOT:** verify by checking the signature file exists. The question is whether firmware accepted it.
**Why:** an unverified boot chain makes every downstream integrity claim decorative.
**Dep:** AGY-1930

## AGY-1932 -- SBOM records exist but are not verified against the image  (WS-SBOM | P2 | M)
**Goal:** The SBOM describes the image it ships with.
**What+How:** Digests and versions are resolved at build time and recorded rather than hand-pinned, with reproducibility resting on the SBOM plus the baked manifest. Nothing verifies the recorded SBOM matches the image's actual contents. Compare them after the bake.
**Where:** `usr/share/mios/artifacts/sbom/`, `automation/build.sh`, `tools/verify-images.py`
**Done When:** an SBOM that disagrees with the image fails the build.
**Verify:** add a package to the image without regenerating the SBOM and show the failure.
**Do NOT:** regenerate the SBOM from the image as the fix and call it verified. The SBOM must be produced from the build inputs and then compared with the output; deriving it from the output makes the comparison vacuous.
**Why:** an SBOM that is generated from what shipped can never disagree with what shipped.
**Dep:** AGY-1914

## AGY-1933 -- Image references carry latest intent with no staleness check  (WS-SBOM | P2 | S)
**Goal:** A floating reference is resolved and recorded at build time.
**What+How:** Image references carry floating-tag intent deliberately, with the digest resolved and recorded at build. Add a check that every floating reference resolved to a digest during the build and that the digest was recorded, so a fetch failure that silently reused a cached layer is caught.
**Where:** `usr/share/mios/mios.toml`, `automation/build.sh`, `usr/share/mios/artifacts/sbom/`
**Done When:** a floating reference with no recorded resolution fails the build.
**Verify:** block resolution for one reference and confirm the build fails rather than proceeding from cache.
**Do NOT:** pin the references in the SSOT as the fix. That reverses the recorded architecture decision; the point is resolution at build with provenance recorded.
**Why:** an unresolved floating reference means the image contains something nobody recorded.
**Dep:** AGY-1932

## AGY-1934 -- The finalization plan and this register are not cross-linked  (WS-DOCS | P3 | S)
**Goal:** Each spine item names the tasks that clear it.
**What+How:** The finalization plan lists ten ordered blockers; this register now holds tasks that address several. Link them in both directions so the plan says which tasks clear each item and each task names the spine item it serves.
**Where:** `docs/agy/mios-finalization-plan.md`, `AGY-TASKS.md`
**Done When:** every spine item names its tasks and every task addressing a spine item says so.
**Verify:** the references resolve in both directions; a task id cited in the plan must exist in the register.
**Do NOT:** link by workstream label. Workstreams are broad, and the plan needs the specific tasks that close each blocker.
**Why:** a plan and a task register that do not reference each other diverge, and the recorded state is that both already describe the same work differently.
**Dep:** AGY-1890

## AGY-1935 -- Declaring a unit trips three shrink-only ratchets, so campaign 2 cannot proceed  (WS-UNITS | P1 | L)
**Goal:** Declaring a unit in the SSOT is not scored as a regression.
**What+How:** Declaring two units in `[units.*]` was attempted and reverted because it turned three gates red at once, all of them shrink-only so none may simply be raised. Measured, not inferred: the declarations added 17 `MIOS_UNITS_*` constants, which grew generated `globals.sh` by 34 lines and pushed `shell_lines` past its floor; raised `check_resolver_differential_parity` key divergence from 983 to 1002; and added duplicate-value groups absent from `value-dup-baseline.tsv`. That ledger already records 816 duplicate groups of which 433 are `MIOS_UNITS_*` rows, so past unit declarations were absorbed by bumping it -- meaning the ratchet does not constrain this class of change, it only records it after the fact. The cause is structural: units legitimately share values such as `WantedBy=multi-user.target` and `Type=oneshot`, so every declaration creates duplication by construction. Decide the policy before declaring the remaining 52, because each one hits all three.
**Where:** `usr/share/mios/mios.toml`, `usr/share/mios/reference/value-dup-baseline.tsv`, `tools/drift-checks.py`, `automation/lib/globals.sh`
**Done When:** declaring a unit moves only measures that should move, and the remaining 52 can be declared without a ratchet decision each time.
**Verify:** declare one unit and show each of the three gates behaves as the policy says it should -- not merely that all three are green.
**Do NOT:** resolve this by bumping `value-dup-baseline.tsv` per declaration. That is what produced 433 unit rows in an exemption ledger whose stated purpose is to shrink, and it makes the gate unable to catch the duplication it exists to catch.
**Why:** the roadmap calls unit projection the largest single gap in "one file defines the OS", and its own gates currently score closing that gap as regression.
**Dep:** AGY-1856
