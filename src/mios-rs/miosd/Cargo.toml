# AI-hint: stub
# AI-related: stub
[package]
name = "miosd"
version.workspace = true
edition.workspace = true

[dependencies]
clap = { version = "4.5", features = ["derive"] }
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
regex = "1.10"
mios-build = { path = "../mios-build" }
mios-config = { path = "../mios-config" }
