# AI-hint: Crate manifest for mios-probe -- the native host-readiness prober (ADR-0021 category: probe).
# AI-related: src/mios-rs/Cargo.toml, usr/share/mios/mios.toml, Justfile
[package]
name = "mios-probe"
version.workspace = true
edition.workspace = true
description = "MiOS native host-readiness probes (ADR-0021 probe category)"

[dependencies]
serde_json = "1.0"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
