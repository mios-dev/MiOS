<!-- AI-hint: Adversarially verified audit of the fourteen unconverted miosd renderers against the bash/python fallbacks they are meant to replace -- ranked defects, the conversion order they imply, and the stages where byte-diffing cannot prove parity. -->
<!-- AI-related: TASKS.md, automation/98-drift-checks.sh, usr/share/mios/mios.toml, usr/share/doc/mios/adr/0021-rust-static-binary-consolidation.md, docs/DOD.md -->
# miosd renderer parity audit — T-1018 stages 5..18

> Produced by a 29-agent background workflow: one auditor per stage, adversarial
> verification of every claim, then one synthesis. Committed in full per the operator's
> "summary to me, full report committed". Claims that did not survive verification are
> kept in the **Refuted claims** section rather than deleted — the refutations are the
> most useful calibration in the document.
>
> **Read the coverage gaps before trusting any conversion gate built from this.** No
> auditor ran a real `podman build`, no SELinux-enforcing host was available, and several
> stages were replayed through harnesses rather than executed end to end.

## Verdict

None of the fourteen audited stages came back clean, and the worst of it is already shipping. The single most dangerous finding is in **76-uki-render**: `tools/generate-uki-cmdline.py` wraps each `tomllib.load` in `except Exception: print(...)` and then *continues*, so a one-character syntax error in any `kargs.d` drop-in silently deletes that file's kernel arguments — corrupting `01-mios-hardening.toml` produced a cmdline with `spectre_v2`, `spectre_bhi`, `spec_store_bypass_disable`, `l1tf`, `gather_data_sampling`, `tsx`, `pti`, `vsyscall`, `randomize_kstack_offset` and `kvm.nx_huge_pages` all gone (1408 → 1214 bytes), `Generated ...`, `EXIT=0` — and the drift gate then prints `[OK] usr/lib/kernel/cmdline is in sync with kargs.d/*.toml` because `build.sh:392` reprojects the file before `98-drift-checks.sh:2690` re-runs the same generator with `2>/dev/null`. That is the live python path, not a Rust conversion hazard. Close behind, and also live: **33-generate-quadlets** bakes `Environment=POSTGRES_PASSWORD=<value>` into `0644` files under `/usr` and lets a build-env `MIOS_ADGUARD_UID=0` write `User=0` into a unit that is not on the privileged roster, under a header certifying it as SSOT-derived; **49-cosign-policy** ships `insecureAcceptEverything` with an empty `/usr/share/pki/containers` and no operator-reachable way to change it; and **85-bake-plan**'s native Rust tier is *already reachable by absolute path* and currently aborts with three wrong-cause validation errors because `globals.sh` sets but never exports `MIOS_VERSION_*`. The one piece of good news is that the `command -v miosd` dispatch (T-1018) is genuinely dead at bake — verified independently on eight stages — so most of the Rust-side defects are latent, which means the conversion sequencing below is the whole ballgame: repairing the dispatch globally before repairing the renderers would arm nine stages' worth of silent failures in a single bake.

## Defects that survived verification, ranked

Ranked by damage on a real bake. `(LIVE)` = the path that executes at bake today. `(LATENT)` = behind the dead `command -v miosd` gate, one edit from live.

| stage | defect | taxonomy | severity | law | what actually happens |
|---|---|---|---|---|---|
| 76-uki-render | Parse error in any `kargs.d` drop-in is swallowed, args silently dropped | Swallowed Failure | critical (LIVE) | — | `Error parsing .../01-mios-hardening.toml: Invalid value` to stderr, then `Generated .../usr/lib/kernel/cmdline`, `EXIT=0`. Ten CPU-mitigation args verified absent from the output. |
| 76-uki-render | `--check` reports in-sync on the tree it could not parse | Self-Certifying Predicate | high (LIVE) | Law 8 | `[OK] usr/lib/kernel/cmdline is in sync with kargs.d/*.toml`, `CHECK_EXIT=0`. `build.sh:392` writes then `98-drift-checks.sh:2690` checks with both streams to `/dev/null` — generator compared against generator. |
| 33-generate-quadlets | DB passwords rendered as literals into world-readable Quadlets | Unsafe-Env-Output | high (LIVE) | Law 11 | `MIOS_PG_PASS='S3cr3t-Real-Prod-Pw'` → `mios-pgvector.container:15 Environment=POSTGRES_PASSWORD=S3cr3t-Real-Prod-Pw`, mode `-rw-r--r--`. Committed tree already ships `=mios` at 0644. |
| 33-generate-quadlets | Build-env vars override SSOT `User=`/`Group=`, producing root units | Self-Certifying Predicate / Hardcoded-Fallback | high (LIVE) | Law 6, Law 7 | `MIOS_ADGUARD_UID=0` → `User=825 → User=0`, `Group=825 → Group=0`, `EXIT=0`, header still reads `regenerate via tools/generate-pod-quadlets.py from [containers.mios-adguard] in mios.toml`. `mios-adguard` is not in `[security.privileged_quadlets].root` (13 entries, verified). |
| 49-cosign-policy | Signature verification is off by default and cannot be turned on | Empty-Set Pass | medium→high in aggregate (LIVE) | Law 7 | Shipped `policy.json` = `{"default":[{"type":"insecureAcceptEverything"}]}`; `usr/share/pki/` does not exist, so the five trust roots install 0 files and `/usr/share/pki/containers` ships empty; `policy_mode` appears in no configurator HTML/JS; `allowed_identities` has zero consumers. |
| 49-cosign-policy | `SYSFILES=/ctx/system_files` never exists; missing policy is a warning; `jq` gate guarded out; stage non-fatal | Skip-as-Pass / Swallowed Failure | high (LIVE) | Law 8 | `[WARN] Missing policy.json` → `[OK] Validation complete`, `exit=0`. `build.sh:199` lists the script in `NON_FATAL_SCRIPTS` and `mios.toml:10563` sets `fatal = false`, so even a cosign SHA256 mismatch only warns. |
| 85-bake-plan | Native Rust tier is reachable by absolute path and hard-fails on unexported `MIOS_VERSION_*` | Swallowed Failure / Measuring the Wrong Property | critical (LIVE) | Law 12 | `globals.sh:469` is `: "${MIOS_VERSION_CEPH:=v19}"` — set, not exported (only line 26 exports anything). The binary drops the four floated-tag Quadlets at `if resolved.contains('$') { continue }` and exits 2: `VALIDATION ERROR: Core image 'quay.io/ceph/ceph:v19' is not referenced by any Quadlet` ×3, naming the wrong cause. |
| 85-bake-plan | `BakePlanCheck` / `BakeBudgetCheck` are constant `Verdict::Pass` with empty bodies, run at bake | Self-Certifying Predicate | high (LIVE) | Law 8 | `miosd drift-check --root <empty dir>` → `[PASS] check_bake_plan` / `[PASS] check_bake_budget`. Invoked at `Containerfile:103` by absolute path. |
| 98 / aiplane-lint gate | `check_agent_pipe_budgets` in miosd is `Verdict::Pass(...)` ignoring `_ctx`; 55 of 76 `Check` impls are the same hollow shape | Empty-Set Pass | critical (LIVE) | Law 8 | `miosd drift-check --root /…/definitely-not-here` → `Summary: 55 passed, 11 failed, 8 skipped`, with `[PASS] check_agent_pipe_budgets` among them. |
| 34-render-quadlets | `envsubst` eats systemd's `$$`-escaped runtime refs | Measuring the Wrong Property | critical (LIVE) | Law 8 | Real GNU envsubst 0.21 on `mios-pgvector-backup.service`: `DIR="$/var/lib/mios/backups"`, `PORT="$8600"` (positional `$8` + `600` → port 600), `KEEP="$7"`, `case "$true" in`. The `[ -z "$DIR" ]` guard never fires. Here the Rust path is the *correct* one. |
| 34-render-quadlets | Single-pass nested-default regex corrupts the AI plane's `base_url` on both tiers | Measuring the Wrong Property | critical (LIVE) | Law 5, Law 7 | `etc/mios/kb.conf.toml` renders `base_url = "http://localhost:8700/v1/v1}"`. Under miosd, `eval-criteria.json` ships the literal `"model":"${MIOS_AI_MODEL:-qwen2.5-coder:7b}"`. |
| 44-firewall-ports | Malformed / `[firewall]`-less SSOT silently falls to a hardcoded wrong port set | Skip-as-Pass / Hardcoded-Fallback | high (LIVE) | Law 7, Law 8 | `EXIT=0` opening RDP `8300`, k3s API `8450`, Ceph dashboard `8460` on the public zone — none in `[firewall].open_ports` — while omitting `8800/8210/8500/8600/8520/8530/8510`. `${#_ssot_ports[@]} -gt 0` cannot tell "open nothing" from "parse failed". |
| 44-firewall-ports | Unbound `MIOS_GUACAMOLE_PORT` aborts the else-tier mid-sequence | Swallowed Failure | medium (LIVE) | Law 9 | Three `--add-port` calls issued, then `line 46: MIOS_GUACAMOLE_PORT: unbound variable`, `EXIT=1`. `tools/render-globals.py:70` deletes that name as dead. Half-applied firewall, no rollback. |
| 44-firewall-ports | Python tier's `__file__` fallback is dead code | Swallowed Failure | high (LIVE) | Law 7 | `NameError: name '__file__' is not defined` under `python3 -c`, discarded by `2>/dev/null` + `|| true` + empty `mapfile` → silent fall-through to the hardcoded list, no diagnostic. |
| 33-generate-quadlets | Empty/renamed SSOT tables short-circuit the orphan scan | Empty-Set Pass | high (LIVE) | Law 8 | `[pod-gen] no Quadlets in SSOT -- nothing to do`, `return 0` placed *before* the check-mode scan: 30 populated units declared fine, `EXIT=0`. |
| 33-generate-quadlets | `d.get("networks") or d.get("network")` (same for volumes/images) | Unanchored Allowlist | medium (LIVE hazard) | Law 8 | Verifier *executed* the fallback: generator emitted `ntp.network`, `quadlet.network`, `retry.network`, `sidecars.image`, `sys.image`, `cuda.image` — six bogus units, `EXIT=0`, no diagnostic. |
| 33-generate-quadlets | Member warnings discarded in write mode; the check reads the previous run's leftovers | Swallowed Failure / Measuring the Wrong Property | medium (LIVE) | Law 8 | 22 `WARN ... member missing` lines, `EXIT=0`. Run 1 into an empty dir = 22 WARNs; run 2 same SSOT, dir now populated = 0 WARNs. Pods render before containers. |
| 34-render-quadlets | Bash find filter omits `*.socket` | Measuring the Wrong Property | high (LIVE) | Law 8 | `usr/lib/systemd/system/mios-cockpit-link.socket:8` ships `ListenStream=0.0.0.0:${MIOS_PORT_COCKPIT_LINK}` today — systemd cannot parse it. miosd renders it to `8120`. |
| 34-render-quadlets | No residual-placeholder gate; counters count files, not substitutions | Count-Only Ratchet | medium (LIVE) | Law 8 | Residuals after a correct-env run: 18 (envsubst), 27 (in-process), 3 (miosd). A file containing only `${MIOS_NO_SUCH_VAR_XYZ}` reports `OK Rendered 1` — the only change is the trailing newline `printf '%s'` strips. |
| 75-kargs-render | Rust renderer never opens `01-mios-vfio.toml`; irreversibly deletes pre-existing kargs | Silent output truncation | critical (LATENT) | Law 8, Law 7 | `kargs_list: Vec<String> = vec![]` at `main.rs:477`; `diff` vs bash shows `> "rd.driver.pre=vfio-pci"` / `> "kvm-intel.nested=1"` missing. `grep -rn` proves the file is their only source in the tree, so re-running bash does not restore them. |
| 75-kargs-render | `unwrap_or_default()` + no TOML parse: missing or malformed SSOT renders hardcoded kargs over the real file, exit 0 | Hardcoded-Fallback / Skip-as-Pass | critical (LATENT) | Law 7, Law 8 | `--toml /…/NOPE.toml` → `exit 0`, file rewritten to `intel_iommu=on / amd_iommu=on / iommu=pt`. Bash exits 1 and touches nothing. |
| 75-kargs-render | `trimmed == "[kargs]"` exact compare | Hardcoded-Fallback | high (LATENT) | Law 7 | `[kargs] # hardware passthrough` (valid TOML, `tomllib` returns `iommu='amd'`) → whole table ignored, `vfio-pci.ids` absent, `exit 0`. |
| 75-kargs-render | `iommu="off"` renders an empty array and the schema gate passes it | Empty-Set Pass | high (LIVE gap) | Law 8 | Rust emits literally `kargs = [\n]`; `python3 automation/validate-kargs.py` → `1/1 files passed.`, exit 0. `ALLOWED_TOP_KEYS` has no must-be-present notion. |
| 75 + 76 | `match-architectures` dropped by both renderers, ignored by the flattener | Silent truncation / Measuring the Wrong Property | low–medium (LIVE latent) | Law 8 | Verifier planted `match-architectures = ["x86_64"]` into copies: **both** renderers deleted it, and `validate-kargs.py` then passed the stripped file. 15 of 17 drop-ins carry the key; `generate-uki-cmdline.py` never reads it. |
| 01-system-files-overlay | Rust `read_dir` is non-recursive — user-scope Quadlets never bound | Empty-Set Pass | critical (LATENT) | Law 3 | bash 21 symlinks, Rust 20; the diff is exactly `mios-coderun-sandbox@.container` (the agent code-exec sandbox, which must live under `users/`). |
| 01-system-files-overlay | `symlink()`/`remove_file()` Results discarded, success printed unconditionally | Swallowed Failure | critical (LATENT) | — | Read-only dest: 20–26 `[miosd] LBI: bound ...` lines, `ls -A` = 0 files, `INNER_EXIT=0`. Also reproduced as uid 65534 against a 0555 dest. Bash aborts under `set -euo pipefail`. |
| 01-system-files-overlay | Unreadable SSOT → empty token set → every firstboot-deferred image bound | Skip-as-Pass / Empty-Set Pass | high (LATENT) | Law 7 | `exit=0`, 26 symlinks vs 20 correct, including `mios-llm-heavy{,-alt}.container`, `mios-llm-heavy.image`, the three webtools units. `99-postcheck.sh:466` globs only `*.container` and only reports *missing*, so over-binding is invisible. |
| 01-system-files-overlay | `starts_with("firstboot_tokens")` unanchored (Rust-only regression) | Unanchored Allowlist | high (LATENT) | Law 3, Law 7 | One added line `firstboot_tokens_note = "mios"` → Rust drops to 18 symlinks at exit 0, silently unbinding `mios-cockpit-link.container` and `mios-node.container`. Bash grep is anchored and unaffected. |
| 40 + 51 harden | `[miosd] enabled <unit>` printed after the symlink failed; exit 0 on two `fatal=true` stages | Self-Certifying Predicate / Swallowed Failure | critical (LATENT) | Law 8 | EROFS and unprivileged-0555 both reproduced: three `enabled` lines, zero symlinks, `EXIT=0`. Bash: `ln: failed to create symbolic link ... Read-only file system`, `EXIT=1`. Failure inversion. |
| 40 + 51 harden | Trust write discarded; key never added and not matched in common spellings | Swallowed Failure / Empty-Set Pass | high (LATENT + LIVE) | Law 8 | Read-only `/usr/lib/fapolicyd` → file stays `trust = rpmdb`, `exit=0`, bash even prints `OK Trust = file,rpmdb set`. `trust=rpmdb` (no space), `# trust = rpmdb`, and a missing key are all unchanged by **both** implementations, all `EXIT=0`. |
| 40 + 51 harden | One `harden` does the union of two stages; `[security].enable_units` has zero consumers | Measuring the Wrong Property / Hardcoded-Fallback | high (LATENT) | Law 1, Law 7 | Bash 40 creates `/etc/systemd/system/multi-user.target.wants/fapolicyd.service`; Rust creates three `/usr/lib`-tier links — the shipped image differs. `grep -rn enable_units` hits only `mios.toml:1613` and `AGY-TASKS.md`. `miosd harden --help` → `Usage: miosd harden`, no way to pass one. |
| 88-finalize | Version interpolated unescaped into a shell-sourced `os-release` | Unsafe-Env-Output | high (LIVE input path) | Law 10 | `--version '0.3.0\`id -u\`'` → `VERSION="0.3.0\`id -u\`"`; `. os-release` yields `[0.3.00]`. Rust newline payload injects arbitrary keys 7×, exit 0; bash exits 1. `&`, `\|`, `\` all diverge (Rust writes literally at exit 0, bash aborts). |
| 88-finalize | Missing `--path` is exit 0 and silent while the stage's `mios_ok` is unconditional | Skip-as-Pass | high (LATENT) | Law 8 | `miosd finalize-osrelease --path /…/NOPE/os-release --version 9.9.9` → no output, `exit=0`; stage lines 41-43 print `Os-release version projected from SSOT via miosd: ...` regardless. Plus symlink (`sed -i` destroys it), CRLF and trailing-newline divergences. |
| 05-repos | No tier reads `[repos]`; offline branch probes a directory nothing creates | Hardcoded-Fallback / Measuring the Wrong Property | high (LIVE) | Law 7, Law 12 | `MIOS_TOML=/nonexistent miosd render-repos` still writes the full 13-key metalink stanza (`priority=95`, `ip_resolve=4`). `05-repos.sh:44` probes `/usr/share/mios/vendored/rpms` while `globals.sh:1617`, `globals.ps1:1642` and `mios.toml:5497` all pin `.../rpm-mirror`, so every bake fetches from `mirrors.fedoraproject.org`. |
| 05-repos | Unvalidated `--fedora-version` injects an unsigned baseurl; Rust fails open where bash fails closed | Swallowed Failure | low–medium (LATENT) | — | Newline payload → `[fedora-44]` / `baseurl=http://evil.example/x` / `[junk]` above `gpgcheck=1`, `EXIT=0`. Bash: `ambiguous redirect`, `EXIT=1`, no file. Reachability is limited (`FEDORA_VERSION` is a declared generator placeholder). |
| build.sh driver | `[build.phases].list` (71) omits `55-native-build.sh` (72 on disk) | Skip-as-Pass / SSOT drift | critical (LATENT) | Law 8 | `TOTAL_SCRIPTS=67` vs `68`, diff = one line. `55-native-build.sh:37-39` is the only `ln -sf ... /usr/bin/${bin}` in the tree, so the first Rust-driven build is the build that stops compiling and installing the Rust binaries. `max_phase_scripts = 72  # +1: 55-native-build.sh` — the ratchet was bumped for the very script never registered. |
| build.sh driver | `load_from_toml` fails open to a 6-phase hardcoded registry; miosd exit status discarded; missing script dropped with no `else` | Hardcoded-Fallback / Skip-as-Pass | critical (LATENT) | Law 7, Law 8 | Nonexistent root / malformed TOML / no `[build]` / `list = []` all → `exit=0`, 6 lines, empty stderr → `TOTAL_SCRIPTS=3`. Stub miosd exiting 1 → `TOTAL_SCRIPTS=2`, harness exits 0. `38-selinuxx` typo → `TOTAL_SCRIPTS=66`, exit 0, SELinux stage silently gone. |
| build.sh driver | Golden tests snapshot the fallback and re-implement stages in Python string literals | Self-Certifying Predicate | high (LIVE) | Law 8 | `golden_harness.rs:34` calls `PhaseRegistry::default_registry()`, never `load_from_toml`. The chrony/nut/kargs "golden" tests pass a `concat!()` Python body to `py -c` and snapshot it — they never invoke `automation/*.sh` or any miosd subcommand. `cargo test -p mios-build`: 0 lib unit tests, 4 golden tests, all pass. |
| 98 / aiplane-lint gate | Deleting the agent-pipe tree makes the check PASS; a TODO comment satisfies it; 9 keys hardcoded twice | Self-Certifying Predicate / Measuring the Wrong Property / Hardcoded-Fallback | critical (LIVE) | Law 7 | `if !search_dir.is_dir() { search_dir = root }` → the scan finds its own `budget_keys` literal in `tools/drift-checks.py` and exits 0. A four-line `# TODO` naming the nine keys → `PASS`, exit 0. Two planted `[agent_pipe]` keys with zero consumers → `PASS`, exit 0. |
| 98 / aiplane-lint gate | Rust tier unreachable at bake; no-python short-circuits to 0; the silent-skip detector is blind to its own file | Swallowed Failure / Skip-as-Pass / Self-Certifying Predicate | high (LIVE) | Law 14, Law 8 | Candidates are only `$ROOT/tools/native/target/release/...` (gitignored, absent) and PATH — the compiled binary at `/usr/libexec/mios` is never a candidate. `_need_python || return 0` with `MIOS_DRIFT_REQUIRE_TOOLS` never armed on either invocation path. `check_no_silent_tool_skips` → `compliance clean`, exit 0, while `grep -c '_need_python || return 0'` = 86. |

## Refuted claims

Six claims were knocked down, plus one partial. The pattern matters: **every refutation was a harness artifact or a mislabelled premise, not a fabricated observation.** The mechanical readings held up in all seven cases; what failed was the causal story on top of them.

1. **76-uki-render — "with miosd on PATH the stage installs the STALE committed cmdline."** The claimant *planted* miosd on PATH, then reported the consequence as a shipped regression. `miosd` installs only to `/usr/libexec/mios` (`Containerfile:55`), which is on no `PATH` — no `ENV PATH`, no `usr/bin/miosd`, and `mios-shim-links.conf` has no miosd entry. At bake the fallback runs `${ROOT}/tools/generate-uki-cmdline.py` and regenerates from stage 75's output, so operator `[kargs]` *are* honoured. The genuine underlying issue (a dead Rust dispatch that no-ops) is already captured by two upheld findings.

2. **88-finalize — "the stage reads no SSOT at all."** The strace is correct (zero `mios.toml` opens) but `/ctx/VERSION` is not an unbound input: `Containerfile:9` is `COPY VERSION /ctx/VERSION`, and `check_version_ssot` compares `[meta].mios_version` to `$ROOT/VERSION` *and* field-by-field to the source-tree `os-release`. The claim's assertion that the gate "only compares the SOURCE-TREE usr/lib/os-release" is factually wrong. The planted-SSOT experiment edited `mios.toml` while leaving `VERSION` alone — the skew it created is exactly what the gate rejects. What survives is a naming imprecision, not a Law 7/8 violation.

3. **05-repos — "the vendored branch contradicts the SSOT (`gpgcheck` true→0, `gpgkey` dropped)."** `[repos.fedora]` declares a *metalink* repo; the SSOT has no offline stanza, so the vendored render is not a projection of that table. The four omitted keys (`timeout`, `minrate`, `max_parallel_downloads`, `ip_resolve`) are network-transfer options meaningless for a `file:///` baseurl, and `gpgcheck=0` for an image-baked mirror is the house pattern (`04-local-rpm-mirror.sh:22`). It also double-counts the real finding and is conditioned on a branch the same submission proves never runs.

4. **05-repos — "the dead `[repos]` table is pre-registered as accepted debt."** The gate works: `tables=157 consumed=154 registered-unconsumed=3 (ceiling 3)`. A shrink-only register with a documented drain procedure and a ceiling pinned at the measurement is not an Empty-Set Pass.

5. **05-repos — "the bash fallback hardcodes its output directory."** That is auditor-harness friction. `05-repos.sh` is a root-run bake stage where `/etc/yum.repos.d` is the correct destination, and every peer stage does the same. `miosd` already exposes `--output`.

6. **34-render-quadlets — "2641 of 2655 `MIOS_*` are never exported"** (and the two findings derived from it). **This is the most important refutation in the set.** The measurement came from `cd automation && bash -c 'source ./lib/common.sh'`; with that relative path, `_mios_locate_userenv` probes `./lib/../../tools/lib/userenv.sh` → `/home/user/tools/lib/userenv.sh`, outside the repo. The lookup fails silently (`|| true`), the SSOT resolver never runs, and only `globals.sh`'s non-exporting `: "${VAR:=default}"` defaults remain — hence 14. Invoked the way `build.sh:225` actually does it (absolute `$SCRIPT_DIR`), `userenv.sh` is found and evals 2607 `export MIOS_*=` lines (`shell=2720 exported=2615`). All four cited "production-shaped outputs" were fabricated by the broken harness: with real GNU envsubst the rendered values are `0.0.0.0:8110`, `tcp-listen:8120`, `http://localhost:8510/health`, `--port 8210`. The derived claim that miosd bakes a stale `chat = "qwen2.5-coder:7b"` is likewise false — it writes `granite4.1:8b`. **Trust implication: any evidence line in the original audit whose premise reads "`MIOS_*` is not exported" must be re-derived.** Note this cuts *against* the stage: it flips the env-var/vendor-name/password substitution finding from "latent trap" to live-today, and the dropped multi-LoRA post-processing from hypothetical to a real default-bake divergence.

7. **34-render-quadlets — "`MIOS_CONV_INFERENCE_HEAVY_ENGINE_MODE` is emitted by nothing, so the multi-LoRA block is dead code."** The grep of `globals.sh` is right but `globals.sh` is not the emitter that matters: `python3 usr/lib/mios/mios_toml.py --emit=shell` emits `export MIOS_CONV_INFERENCE_HEAVY_ENGINE_MODE=single`. The block fires on a default bake with no planting. (A smaller, real Law 9 smell survives: `globals.sh:605` spells the same key `MIOS_CONVERGE_...` while the resolver spells it `MIOS_CONV_...`, and the sed hardcodes `4`/`8`/`64` and `/var/lib/mios/lora-adapters`.)

8. **build.sh — "the T-1018 register forgives build.sh and carries a slack slot."** Units error: `max_unreachable` counts `command -v` *sites* (15, because `98-drift-checks.sh` carries two), the array lists *files* (14). Ceiling equals measurement — zero slack — and `mios-gate/src/dispatch.rs:187-192` makes a ceiling *above* the measurement a violation in its own right. Planting one new gate produced `exit 1` with two findings.

9. **Partial — 33-generate-quadlets, "hardcoded deprecation comment and hardcoded placeholder set behind a bare except."** Part (a) upheld: the `if name == "mios-llm-heavy-alt"` branch emits a literal unit name and prose policy string into the rendered output. Part (b) refuted: the `try` path already returns `set(phs) | {"FEDORA_VERSION","MIOS_VERSION"}`, a missing `[generator]` table still takes the `try` path and returns the identical set, and the only way to reach the `except` is an unparseable TOML — which then dies loudly at `load_sidecars` with exit 1. No silent substitution is possible.

**Evidence-hygiene corrections that did not overturn a finding** (calibrate accordingly): 44-firewall's `comm -23` block pasted one line (`port=8700/tcp`) from the *planted* run into the vendor-SSOT evidence; the aiplane-lint bash citations are uniformly ~8 lines low (content verbatim correct at the real offsets); 88's newline-injection payload as quoted lands the newline *inside* the quotes and needs a quote-closing variant to actually set the key; 01's "26 bound lines" only arises when the SSOT is *also* unreadable (20 with a valid SSOT); 76's `match-architectures` count is 15/17, not 14/17; 34's residual counts are 18/27/3, not 27/27. In every case I checked, the defect stood and only a number or a pasted line was wrong.

## Conversion order

**Sequencing rule that governs everything below.** `command -v miosd` is dead at bake (T-1018), and `mios.toml [build.tool_dispatch].unreachable` registers 14 files at a ceiling of 15 with zero slack. The one-line fix T-1018 itself proposes — `ENV PATH=/usr/libexec/mios:$PATH` — would arm the latent Rust defects in **75, 33, 34, 40, 51, 49, 88, 01 and 05 in a single bake**. Do not do that. Convert per stage, and flip *that stage's* dispatch to an absolute path in the same commit that lands its parity gate, exactly as 35/42/43 already do. Two dispatch paths are already live and already broken and are therefore not subject to this rule: `Containerfile:103 miosd drift-check` (hollow checks) and stage 85's tier-2 `/usr/libexec/mios/mios-bake-plan` (hard-fails on unexported `MIOS_VERSION_*`).

Before stage 1, land two standalone bugfixes that are not conversions and should not wait for one: (a) make `generate-uki-cmdline.py` fail on a parse error instead of `except … continue`; (b) export `MIOS_VERSION_CEPH/_K3S/_FORGEJO` from `globals.sh` (or resolve them from SSOT in `bake_plan.rs`), which unbreaks stage 85 today.

1. **`automation/build.sh` (phase driver)** — gatekeeper. While `load_from_toml` fails open to a 6-phase registry and `[build.phases]` can silently drop a stage, no other conversion's parity claim is trustworthy. Must add `55-native-build.sh` to `[build.phases]` and a regenerate-and-diff guard in the same commit, or the first Rust-driven build deletes its own compiler step. **Output diff IMPOSSIBLE** (no artifact) — proof is a byte-diff of the ordered `ALL_SCRIPTS` list produced by `build.sh` lines 185-248 with miosd absent vs present, plus a five-case error matrix asserting non-zero exit.
2. **75-kargs-render** — feeds stage 76; fully diffable; its Rust path is irreversibly destructive (`rd.driver.pre=vfio-pci` has no other source in the tree). Gate: `diff -r` of both renderers over copies of the real `kargs.d` with `miosd` off PATH, plus idempotency (render twice, second run a no-op) and a `[kargs] # comment` fixture that forces a real TOML parse.
3. **76-uki-render** — consumes 75's output; owns the boot line; holds the worst live defect. The naive diff is **vacuous** (`run_render_uki_cmdline` execs the same python), so the commit must first add a `KERNEL_CMDLINE_DST`/`--kargs-dir`/`--out` seam and write an actual Rust renderer, then prove CWD-independence (`/`, `/tmp`, repo root must produce identical bytes) and that `--check` **fails** on a corrupt drop-in.
4. **33-generate-quadlets** — feeds 34, 01 and 85; two live security defects (secrets at 0644, env-driven `User=0`). Diffable, but run from a **non-repo CWD** and assert a **file-count floor of 30**, or the current no-op passes the diff. Add assertions that no `POSTGRES_PASSWORD` literal appears and that `MIOS_ADGUARD_UID=0` is *refused*.
5. **34-render-quadlets** — consumes 33's output. **Byte-parity is the wrong acceptance test here** and chasing it ships a broken image: on `mios-pgvector-backup.service` the Rust path is correct and the shipping envsubst path is wrong. Prove correctness instead — a post-stage `grep -rn '\${MIOS_' <scan dirs>` returning zero and failing the stage, a documented no-substitute list for systemd runtime refs, and a real recursive expander asserting `base_url = "http://localhost:8700/v1"`.
6. **01-system-files-overlay (LBI block)** — consumes the Quadlet tree; Law 3 is its whole point. Diffable as a `(name → target)` set, but add a search-root override first so fixtures don't need a mount namespace. Must assert 21 entries including `mios-coderun-sandbox@.container` and exclusion of the six firstboot-tier images, and must exit non-zero on a missing/malformed SSOT.
7. **85-bake-plan** — already live and already failing; consumes `Image=` lines from 33/34. **Diff against the python generator, not against `miosd`** (miosd execs python, so that diff is python-vs-python and byte-identical for the wrong reason), with `MIOS_VERSION_*` deliberately unset. Also pipe the Rust SBOM through `drift-checks.py bake-budget` and require exit 0 — today it prints `ERROR: bound-images.tsv missing size_gb column`. Decide in this commit whether `tools/native/mios-bake-plan` or `miosd::bake_plan` owns the stage; shipping both is Law 15 double-tracking.
8. **49-cosign-policy** — highest *posture* risk, lowest coupling, but this is not a port: the one non-permissive value the renderer can express produces a spec-invalid policy and no trust roots exist to point it at. **A naive diff is meaningless** (both branches write nothing under real bake conditions). Proof: render to an explicit `--out`, validate with `skopeo standalone-verify` or `podman --signature-policy` rather than `jq -e .`, and assert the artifact lands at the absolute `/usr/lib/containers/policy.json` inside the built image. Reconcile the 50-byte hand-edited committed file against the 76-byte generator output and register the generator in the projections table.
9. **88-finalize** — single-file, fixture-diffable *once* an `OSR="${MIOS_OSRELEASE:-/usr/lib/os-release}"` seam exists. The default-input diff is degenerate (both write back the value already there), so parity must be shown on a planted version plus symlink / CRLF / no-trailing-newline / stock-Fedora fixtures, and a hostile-version matrix with **exit codes recorded** — the commit must state which failure mode it adopts, because bash exits 1 on `|`, `\` and newline while Rust exits 0.
10. **44-firewall-ports** — **output diff IMPOSSIBLE** (mutates firewalld zone state). Proof is an argv-sequence diff: a tracing `firewall-offline-cmd` shim placed both on PATH (for bash) and at the absolute `/usr/bin/firewall-offline-cmd` (Rust hardcodes that path), both sides run through the identical stage entry point, `diff` required to be exact. Repeat against a planted `mios.toml` (new `[firewall].open_ports` entry + a changed `[ports]` value) — the Rust list is a hardcoded 15-tuple with no `--toml`, so it fails this test outright today.
11. **40-fapolicyd-trust + 51-hardening — convert as one commit.** Both dispatch to the identical argument-less `miosd harden`, which performs the union of their contracts; converting either alone makes the other's fallback dead code. **Output diff IMPOSSIBLE** — proof is a filesystem-state diff inside `unshare -m` (symlink set with targets, `stat -c '%a %U:%G'` on the wants dir and both `fapolicyd.conf` files), with the baseline being *40-bash then 51-bash in one namespace*. Split `harden` into two scoped subcommands with `--root`, and drive the roster from `[security].enable_units` — that array currently has zero consumers tree-wide, which is the stated goal of the task.
12. **05-repos** — cheap, low blast radius (only the online metalink heredoc has ever run), but the diff is **vacuous today**: all three branches are byte-identical because all three restate the same literals. The test must assert **divergence** from today's output after adding `--toml`, must repoint the offline probe at `.../vendored/rpm-mirror`, and must drop `max_unconsumed` 3→2 with `"repos"` leaving the register in the same commit.
13. **98-drift-checks `check_agent_pipe_budgets` / mios-aiplane-lint** — last, because it is a gate rather than a renderer, but it must not be skipped: it is the template for 55 hollow `Check` impls that run at every bake. **Output diff IMPOSSIBLE** — proof is an exit-code/verdict matrix. Six fixtures where the current pair agrees on a *correct* answer must stay green; four where it agrees on a *wrong* answer (agent-pipe tree deleted, keys present only in a comment, new `[agent_pipe]` keys with no consumers, a key relocated to a subtable) must **invert** to non-zero. Add `/usr/libexec/mios/mios-aiplane-lint` to the dispatch candidates, lift the nine key names into SSOT, and prove `miosd drift-check --root <nonexistent>` no longer reports `check_agent_pipe_budgets` as PASS.

## Stages where parity cannot be proven by diffing

| stage | why no diff | concrete alternative proof |
|---|---|---|
| **44-firewall-ports** | Mutates firewalld zone state via `firewall-offline-cmd`; renders no file. | Tracing shim logging full argv, installed **both** on PATH and at the absolute `/usr/bin/firewall-offline-cmd` (Rust ignores PATH). Run both sides through the identical stage entry point under `env -i`, `diff` the argv logs exactly. Require `/usr/share/mios/mios.toml` to exist, or the bash SSOT tier silently takes the wrong branch. Add a shim that exits 1 and assert both sides exit non-zero. |
| **40-fapolicyd-trust + 51-hardening** | Symlinks, `chmod`, in-place config edit; no arguments, no `--root`, every path an absolute literal. | `unshare -m` with fixtures bind-mounted over the real paths. Diff three products, not one: both `fapolicyd.conf` byte-for-byte, `stat -c %a` on `usbguard-daemon.conf`, and `find <wants> -type l -printf '%p -> %l\n' \| sort` across **both** the `/usr/lib` and `/etc` tiers. Exit-code matrix for: units absent, wants dir read-only, `/usr/lib/fapolicyd` read-only, `fapolicyd.conf` unreadable, `umask 077`. |
| **build.sh phase driver** | Produces no artifact — the observable is the ordered list of scripts it decides to run. | Run `build.sh` lines 185-248 verbatim into two files (miosd absent / present) and diff the ordered `ALL_SCRIPTS`, not a count — a count ratchet already exists and is blind to the 71-vs-72 divergence. Plus a planted-SSOT run (phase added, `fatal` flipped) to prove the Rust path tracks SSOT rather than agreeing by coincidence, and five error paths asserting non-zero exit. |
| **98 / mios-aiplane-lint** | It is a gate; its output is a verdict. | Ten-fixture exit-code matrix (six must stay 0/1 as today, four must invert from 0 to non-zero), plus stdout byte-parity of the stage line, plus `miosd drift-check --root <nonexistent dir>` asserting the check does **not** report PASS. |
| **49-cosign-policy** | Both branches write nothing under real bake conditions (miosd writes a CWD-relative scratch path that `rm -rf /tmp/build` deletes; bash's `SYSFILES=/ctx/system_files` never exists), so a naive diff is byte-identical for the wrong reason. | Add `--out`/`--toml`, diff seven SSOT states — and require states (e)(f)(g) (`[security.sigstore]` absent, `mios.toml` absent, malformed) to exit **non-zero on both sides**, since byte-parity there preserves the fail-open bug. Then validate the document with the real parser and assert it lands at the absolute path inside the built image. |
| **76-uki-render** and **85-bake-plan** | Diffable in principle, but the obvious diff is tautological: `run_render_uki_cmdline` and `run_bake_plan` both `std::process::Command` the same Python the fallback runs. | Delete the shell-out first (call `miosd::bake_plan::run()` in-process; write a real `toml`-crate renderer for the cmdline), then diff **Rust vs Python**, never miosd vs Python. |
| **05-repos** and **33-generate-quadlets** | Diffs pass for the wrong reason: 05's three branches restate identical literals; 33's Rust no-ops from a non-repo CWD, so two empty dirs compare equal. | 05: assert *divergence* from today's output under a planted `[repos.fedora]`. 33: assert a file-count floor of 30 before diffing, and diff modes as well as bytes (secrets ride in these files). |

## Coverage gaps

Nothing below was observed. Every one of these is a place where a conversion commit could pass its gate and still be wrong.

- **Cross-cutting:** no auditor ran a real `podman build`. Bake-time CWD (`/`) is derived from `Containerfile` (`WORKDIR` appears only at line 20 inside `rust-builder`) plus `build.sh` never `cd`-ing, not measured. No auditor ran a full `just drift-gate`. No SELinux-enforcing host was available, so `fs::write`-vs-`sed -i` context preservation and `symlink`-vs-`ln -sf` labelling are untested everywhere.
- **75-kargs-render:** no boot was performed — the consequence of losing `rd.driver.pre=vfio-pci` is inferred from the karg's meaning plus its confirmed presence in `usr/lib/kernel/cmdline`. `src/mios-rs/` test modules were not audited exhaustively for `render_kargs` coverage. The three-layer overlay was not exercised (both sides read one `--toml`).
- **76-uki-render:** `ukify` itself was never run, so downstream consumption by `77-composefs-verity.sh` / `79-boot-config.sh` is unverified — in particular whether an empty (1-byte) cmdline aborts UKI assembly later. The `rpm -q systemd-ukify` / `install_packages_strict "uki"` branch was stubbed out (no RPM DB). No `miosd drift-check` coverage for this projection was found, but `run_drift_check` was not audited end to end.
- **88-finalize:** the stage was never run end to end (it runs `systemctl preset-all`, `dnf clean all`, `rm -f /etc/containers/auth.json` before the dispatch); the bash side is a verbatim extraction of lines 44-51. The `/ctx` EROFS scrub was reasoned from `Containerfile:65` plus a synthetic read-only bind, not from a real bake. Behaviour on an ostree-backed immutable `/usr` is untested.
- **05-repos:** `05-repos.sh` was never run end to end (it `sed`-edits `dnf.conf` and runs `dnf upgrade`); lines 37-99 were replayed through a generated harness. No bake with a populated vendored mirror, so it is unconfirmed that `dnf` would consume the vendored stanza correctly. `06-enable-external-repos.sh` (crowdsec/ublue/terra, same `FEDORA_VERSION` and `MIOS_ONLINE_BUILD`) was not audited.
- **33-generate-quadlets:** the stage script itself was never executed — `/usr/share/containers/systemd` does not exist in the audit container, so the **branch that actually ships** (root, dir present, `OUT_DIR` = the live tree that `01-system-files-overlay.sh` just laid down) was reasoned about, not run. The `User=`/`Group=` empty-string drop path is unreachable from today's SSOT and was not exercised.
- **34-render-quadlets:** no bake, so the ordering interaction with **35-render-ports** (which writes `/etc/mios/install.env` *after* 34) is unverified — whether any `MIOS_PORT_*` reaches stage 34 via `install.env` is unknown. How the 121-name envsubst list and the 123-name bash list are maintained (generated vs hand-written) was not determined; no generator was found either way. The swallowed-write probe required dropping to uid 65534, so a CI check must do the same.
- **85-bake-plan:** the native binary at `/usr/libexec/mios/mios-bake-plan` does not exist in the checkout; findings come from a rebuilt copy of `tools/native/mios-bake-plan/src/main.rs` plus a source diff against `miosd::bake_plan` (differences confined to `unwrap`/`expect` text, `fn main` vs `pub fn run`, `process::exit` vs `Err`). `85-bake-plan.sh` was never run end to end (`common.sh` pins `MIOS_TOML` to a deployed path that does not exist here).
- **49-cosign-policy:** no `podman`/`skopeo` available, so the claim that `{"default":[{"type":"sigstoreSigned"}]}` is an invalid containers policy is reasoned from `containers-policy.json(5)`, not executed. The cosign-download half (GitHub tag lookup, the hardcoded `COSIGN_FALLBACK_VERSION=v2.6.4`, the SHA256 check, the SBOM append) was not exercised. What `/ctx/system_files` historically was is unknown.
- **44-firewall-ports:** firewalld is not installed — no `firewall-cmd --list-ports`, no zone XML, no idempotency or ordering semantics, and **no observation of how stage 45 (`mios-firewall-init`) and the boot-time `mios-firewall-ports.service` interact with stage 44's output**. That last one matters: the runtime unit may paper over the Rust path's zero-port result on hosts where firewalld is active, while the baked image still ships an empty public zone. Whether any deployed host pre-exports `MIOS_PORT_*` before stage 44 (e.g. via an outer `set -a` over `install.env`) was not simulated. A `[firewall].open_ports` entry naming a key absent from `[ports]` was not planted.
- **40/51 harden:** whether `systemctl disable fapolicyd.service` still works when enablement lives only in the `/usr/lib` tier was not tested (no running systemd). The `fagenrules --load` / `fapolicyd-cli --update` tail of 51 never executed — neither binary is installed — so it is unknown whether it tolerates a conf whose `trust=` write silently failed. **The real Fedora `fapolicyd` RPM was never inspected**: if it ships the key commented or without spaces, the Empty-Set Pass stops being a variant and becomes the live production case, i.e. the stage is a no-op on every bake. Stages 40 and 51 were run in isolation, not in pipeline order.
- **01-system-files-overlay:** the full stage was never run (it tars `/ctx` over `/usr`, `chmod`s all of `/usr/lib/systemd`, can `rm -rf /home`, runs `restorecon -RFv`); only the verbatim LBI block was executed. Whether `/usr/share/containers/systemd` holds units beyond the repo tree at bake — in particular any emitted under `users/` by stages 14/34 — is unknown, and any such unit would also be lost to the flat `read_dir`.
- **98 / aiplane-lint:** `tools/native` ships no prebuilt binary, so the tested artifact was compiled from current source at cargo 1.94.1, not what a bake produces. No genuinely unreadable `mios.toml` could be constructed (running as root); both sides *look* fail-closed by source reading only. Whether any consumer actually **reads** the value it finds — as opposed to merely mentioning the name — was never executed. Whether the 55 stub `Check` impls are intended placeholders was not investigated (no TASKS.md/ADR trace looked for).
- **build.sh driver:** no full bake; the phase-selection block (lines 185-248) was extracted and sourced. The `build_catalog_authoritative=true` tier was never exercised live (needs a running `mios-pgvector`) — and note it is **doubly dead**: `materialize-build-ctx.py` writes `build_phases.json` to `$MIOS_BUILD_CTX` (default `/ctx`) while `build.sh:232` reads `$(dirname "$MIOS_TOML")/build_phases.json`. `cargo test -p mios-build` findings on `golden_harness.rs` are from reading the test source. Whether stages numbered above 55 behave differently once 55 has symlinked `miosd` into `/usr/bin` mid-run — a split-brain within a single bake — was noticed but not tested.
