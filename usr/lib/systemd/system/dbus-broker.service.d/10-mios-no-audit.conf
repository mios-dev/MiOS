# AI-hint: Overrides dbus-broker's ExecStart to remove the --audit flag, ensuring the D-Bus broker starts successfully on kernels without libaudit support (like WSL2) while maintaining core functionality across all deployment types.
[Service]
ExecStart=
ExecStart=/usr/bin/dbus-broker-launch --scope system
