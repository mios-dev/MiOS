/* AI-hint: SHELL-01 idle reaper for the persistent PTY substrate -- kills tmux sessions idle past [shell_session].idle_s so a long-lived shell plane cannot accumulate unbounded state. */
/* AI-doc: usr/share/doc/mios/manual/_harvest/usr_lib_systemd_system_mios_shell_session_gc_service.md */
[Unit]
Description=Reap idle MiOS shell sessions
ConditionPathExists=/usr/libexec/mios/mios-shell-session

[Service]
Type=oneshot
User=mios-ai
Group=mios-ai
ExecStart=/usr/libexec/mios/mios-shell-session gc
# The reaper is best-effort: a tmux hiccup must never fault the timer.
SuccessExitStatus=0 1

[Install]
WantedBy=multi-user.target
