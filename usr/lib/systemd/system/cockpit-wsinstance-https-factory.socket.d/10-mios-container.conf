/* AI-hint: Drop-in configuration for cockpit-wsinstance-https-factory.socket that disables namespace restrictions to allow the Cockpit TLS helper to bind and function correctly within containerized environments or WSL. */
[Socket]
DynamicUser=no
PrivateTmp=no
PrivateMounts=no
PrivateDevices=no
PrivateUsers=no
ProtectHome=no
ProtectSystem=no
RestrictNamespaces=no
