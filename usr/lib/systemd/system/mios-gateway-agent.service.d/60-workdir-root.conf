# AI-hint: Sets the WorkingDirectory to / for the mios-gateway-agent service to ensure it can access root-level MiOS identity files (MiOS.md, AGENTS.md) while maintaining state in HERMES_HOME.
[Service]
WorkingDirectory=/
