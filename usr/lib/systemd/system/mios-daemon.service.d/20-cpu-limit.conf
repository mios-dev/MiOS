/* AI-hint: Configures systemd resource limits for mios-daemon, allowing a 2-core baseline with a burst capacity of 8 cores (800%) and low priority (Nice=10) to balance background system management with foreground AI pipeline demands. */
[Service]
CPUQuota=800%
CPUWeight=20
Nice=10
