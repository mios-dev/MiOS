# AI-hint: Configures zram0 swap device parameters, specifically setting a dynamic size based on 50% of physical RAM (capped at 32GB) and using zstd compression for memory-efficient swap.
[zram0]
zram-size = min(ram / 2, 32768)
compression-algorithm = zstd
