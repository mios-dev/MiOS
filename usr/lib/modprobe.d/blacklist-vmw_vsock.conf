# AI-hint: modprobe.d drop-in that blacklists the vmw_vsock_vmci_transport kernel module so it never auto-loads, avoiding the VMware vsock transport binding/conflict and module-load noise on MiOS hosts that are not VMware guests.
blacklist vmw_vsock_vmci_transport
