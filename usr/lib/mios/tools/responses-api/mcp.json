{
  "type": "mcp",
  "server_label": "mios-kb",
  "server_description": "Search and fetch over the MiOS knowledge base (vector store + repo).",
  "server_url": "https://mcp.mios.dev/mcp/",
  "require_approval": "never",
  "allowed_tools": ["search", "fetch"],
  "headers": {"Authorization": "Bearer ${MIOS_MCP_TOKEN}"},
  "_comment": "Responses API only. OpenAI retains only schema/domain/subdomains of server_url between calls; auth headers must be re-sent every request."
}
