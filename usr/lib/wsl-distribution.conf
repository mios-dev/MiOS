# AI-hint: Configures WSL-specific environment parameters, defining the default user UID and the system's identifier (MiOS-OS) for integration with Windows Subsystem for Linux.
[wsl-distribution]
default-uid=1000
name=MiOS-OS
