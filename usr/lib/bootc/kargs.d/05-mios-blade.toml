# AI-hint: GENERATED from mios.toml [blade].type -- the deploy-time default blade archetype. DO NOT EDIT; regenerate via tools/generate-blade-karg.py. An installer, Butane kernel_arguments or `mios blade set` overrides it on the cmdline; role-apply reads the LAST mios.blade= token, so a later one wins.
# AI-related: usr/share/mios/mios.toml, usr/libexec/mios/role-apply, tools/generate-blade-karg.py
# bootc kargs.d: bare `kargs = [...]` only. NO [kargs] table header.

kargs = [
    "mios.blade=hybrid"
]
