/* AI-hint: Configures dracut to include the mios-verify script and systemd service into the initramfs to enable boot-time cryptographic integrity verification of the MiOS filesystem. */
install_items+=" /usr/libexec/mios-verify /usr/lib/systemd/system/mios-verify.service "
