# AI-hint: Configures systemd temporary file permissions for WSL2-specific workarounds, specifically fixing a security regression in user-generator permissions to ensure stable session management.
# 'MiOS' v0.2.4 -- WSL2 specific hacks/workarounds
# Fix for WSL v0.2.0.0 security regression (world-writable user generator breaks session)
z /run/systemd/user-generators/wsl-user-generator 0755 root root -
