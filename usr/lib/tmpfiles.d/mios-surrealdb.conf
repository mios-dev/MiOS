# AI-hint: Defines the persistent storage directory and permissions for the SurrealDB container, ensuring the data volume at /var/lib/mios/surrealdb is owned by the mios-surrealdb service user (UID/GID 821).
# AI-related: mios-surrealdb, mios-services
# /usr/lib/tmpfiles.d/mios-surrealdb.conf
# Persistent data dir for the SurrealDB container, owned by the
# canonical mios-surrealdb uid/gid (821, declared in
# usr/lib/sysusers.d/50-mios-services.conf). bootc-immutable code
# path; mutable state stays under /var.
d /var/lib/mios/surrealdb 0750 821 821 -
