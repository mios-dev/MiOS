# /usr/lib/tmpfiles.d/mios-agent-pipe.conf
# Persistent data + state dir for the standalone Agent Pipe service,
# owned by the canonical mios-agent-pipe uid/gid (822, declared in
# /usr/lib/sysusers.d/50-mios-services.conf). bootc-immutable code
# path; mutable state stays under /var.
d /var/lib/mios/agent-pipe 0750 850 850 -
