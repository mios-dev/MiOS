# AI-hint: Defines systemd tmpfiles.d rules to set permissions for /sys/kernel/iommu_groups/ nodes, ensuring the OS correctly exposes IOMMU group visibility for hardware passthrough and device isolation.
z /sys/kernel/iommu_groups/* 0755 root root -
