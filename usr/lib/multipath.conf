# AI-hint: Configures the multipathd daemon to aggregate redundant storage paths into single logical devices, enabling high-availability storage access and path failover for block devices.
defaults {
    user_friendly_names yes
    find_multipaths yes
}
