{"pinned_head":"4b86de7","generated":"2026-06-20","verdicts":[{"task_id":2,"ws_id":"WS-A1","status":"missing","confidence":"high","evidence":["_load_verb_catalog (C:/MiOS/usr/lib/mios/agent-pipe/server.py:5225) swallows ALL exceptions: except Exception as e: log.warning('verb catalog load failed: %s', e); return cat (5293-5295) -> empty catalog, no strict/warn mode."],"gap":"Entire WS unimplemented. Needed: (1) strict/warn fail-loud path in _load_verb_catalog (server.py:5225) + _load_recipe_catalog (5910) gated by new [ai].catalog_fail_mode; (2) /usr/libexec/mios/mios-ai-manifest-gen generator projecting ai/v1/{tools,agents,surface}.generated.json from _VERB_CATALOG/_AGENT_REGISTRY/_RECIPE_CATALOG with --check diff mode; (3) registry_kind demarcation key in tools.json; (4) automation/48-ai-manifest-drift.sh fitness gate; (5) 99-postcheck.sh assertion; (6) [ai].catalog_fail_mode in mios.toml; (7) test_mios_ai_manifest.py. The legacy datastore-ref cleanup sub-item already appears satisfied (no legacy-datastore refs remain in agents.json/surface.json).","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":3,"ws_id":"WS-A3","status":"missing","confidence":"high","evidence":["mios_evict.py:20-77 still builds the legacy query dialect, NOT parameterized pg: protect_where uses `(tier ?? 'warm') != 'hot'` and `??` null-coalescing; ttl_where uses `time::now() - 90d`; parse_count parses `count() AS c ... GROUP ALL`; delete_stmt emits `DELETE knowledge:a, knowledge:b;` (record-id form). Header line 1-2 still says 'the legacy query dialect fragments'. (subtask 1 = NOT done)"],"gap":"Essentially the entire WS-A3 cutover is unbuilt -- not a small delta. Needed: (1) rewrite mios_evict.py + test_mios_evict.py to emit parameterized pg builders (1-(emb<=>...), count(*) , DELETE WHERE id = ANY(%(ids)s), null-safe via COALESCE not ??); (2) rewrite server.py _evict_* wrappers onto mios_pg/_db_read/_db_update so eviction works under db_backend=postgres (today it silently no-ops); (3) point the kanban writer at the canonical `kanban` table with an idempotent INSERT ... ON CONFLICT(id) DO UPDATE (and drop kanban_shadow); (4) add an extended-protocol / parameterized path to mios-pg-query (or mios-db) and convert mios-remember/mios-kg/mios-rag off _pgesc/_pgq string concatenation; (5) delete the dead legacy datastore _sql/_esc/DEFINE-TABLE code + the legacy datastore env defaults from mios-remember/mios-rag; (6) de-rot all the legacy datastore/the legacy query dialect headers and comments; (7) add a kanban unit test. The prior backlog-clearing wave did NOT touch this WS (it built the security/federation half).","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":5,"ws_id":"WS-0B","status":"missing","confidence":"high","evidence":["98-drift-checks.sh:18-22 EXPLICITLY DEFERS WS-0B: 'Bare LIVE-lane port literals (:11441/:11440/:11450/:8643)... Converting those consumers to ${MIOS_PORT_*} placeholders is deferred WS-0B hardening; until then only the RETIRED :11434 lane is an error.' -- i.e. the core 'single owned keys' deliverable is acknowledged as not done."],"gap":"WS-0B is essentially unstarted. Three deliverables remain: (1) Collapse the scattered live-lane endpoint literals (:11441/:11440/:11450/:8643) in [chat]/[agents.*]/[nodes.*]/[ai.*]/[finetune] onto the existing owned [ports.*] keys (or render via MIOS_PORT_* placeholders) -- 98-drift-checks.sh:18-22 explicitly names this as the deferred WS-0B work. (2) Reduce the repeated 'granite4.1:8b' model literal (~10 sites) to one owned head ([ai].model) that the other roles inherit. (3) De-rot stale comments: fix the 'mios-heavy = Qwen2.5-14B-AWQ' comments (mios.toml 1112/1121/1127/1174) that contradict the authoritative Qwen3-8B-AWQ in [ai.sglang] (5106), and purge stale retired-ollama :11434/:11435 references in active-block comments (893/901/960/1695). Optionally extend 98-drift-checks.sh to FAIL on a heavy-lane model-name comment that diverges from [ai.sglang].served-model, closing the comment-drift gap permanently.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":6,"ws_id":"WS-A2","status":"missing","confidence":"high","evidence":["Embedding-version hygiene ABSENT: grep for emb_model|emb_version|embed_version across C:/MiOS/usr/lib/mios/agent-pipe returns ZERO matches; schema-init.sql knowledge table (lines 20-52) and agent_memory (55-69) have NO emb_model/emb_version columns."],"gap":"All WS-A2 deltas are unimplemented: (1) no emb_model/emb_version columns on knowledge/agent_memory; (2) no centralized versioned embedding-identity constant; (3) knowledge and agent_memory writes do not stamp emb_model/emb_version; (4) no scratch-persistence helpers writing pipe scratchpad checkpoints (chat_id/agent/lane/phase/note) to the pg scratch table; (5) no scratchpad rehydration-on-first-access-after-restart in _scratchpad_for/render; (6) mios_embed_backfill.py module absent; (7) no periodic off-hot-path backfill wiring; (8) no mios.toml SSOT flags; (9) test_mios_embed_backfill.py absent. The only pre-existing scaffolding is the bare scratch table DDL (built earlier for the daemon nudger) and generic mios_pg.insert -- none of the WS-specific behavior.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":7,"ws_id":"WS-A5","status":"missing","confidence":"high","evidence":["None of the three named sibling modules exist: Glob for C:/MiOS/usr/lib/mios/agent-pipe/{mios_tokenize,mios_ctxpack,mios_compact}.py returns 'No files found'. The 15 existing mios_*.py siblings are aci/evict/hitl/kvfork/owui/sched/stress/codemode/lanes/pg/a2a_principal/reputation/jsonsalvage/goap/selfimprove -- no tokenize/ctxpack/compact."],"gap":"Entire WS-A5 is unbuilt. Need to create mios_tokenize.py (tokenizer seam returning backend_name()/count_text()/truncate_to_tokens()), mios_ctxpack.py (priority token-budget packer), mios_compact.py (rolling-summary compaction), and their three test suites; replace the // 4 heuristics in _fit_context (server.py:7336-7337) and _usage_estimate (24653-24654) with mios_tokenize calls; route the [:200] history slices (8221, 9302) and the SLOW_LANE_BLOCK_CHARS slice (3677-3678, also 15936, 16017) through truncate_to_tokens; add config/env wiring near server.py:945-965; add the SSOT [context]/[ai] keys to mios.toml and document in /etc/mios/kb.conf.toml; optionally vendor an offline tokenizer asset under /usr/share/mios/ai/tokenizers/.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":8,"ws_id":"WS-A7","status":"done","confidence":"high","evidence":["Named module C:/MiOS/usr/lib/mios/agent-pipe/mios_toolconflict.py does NOT exist (Glob: 16 mios_*.py siblings present, none is toolconflict)."],"gap":"DONE (implemented post-grounding): WS-A7 commit 92cfe4a, test 21/21","needs_vm_to_verify":false,"suggested_action":"mark-complete"},{"task_id":9,"ws_id":"WS-1","status":"partial","confidence":"high","evidence":["mios_lanes.py EXISTS and is complete: usr/lib/mios/agent-pipe/mios_lanes.py defines class Lane (l.26), build_chain (l.44, [ai].heavy_engine -> ordered preference chain over sglang/vllm/light, comma-chain honoured, light forced terminal), class LaneResolver (l.77) with TTL health cache (_is_up l.100), per-lane cooldown park (l.102-113), terminal-floor degrade in pick() (l.118-131, returns the floor lane never None), mark_down() (l.133), snapshot() (l.140). Pure stdlib (only `time`), no server import."],"gap":"[nodes.*] are NOT generated from the [ai].heavy_engine selector -- local-vllm/local-sglang remain hand-maintained static entries (mios.toml:1149/1160); the two-heavy collapse is runtime-only in build_chain. Also: no 'least-busy' load dimension (fixed preference chain only), mark_down() has zero call sites in server.py (no closed-loop dispatch-error failover feed), and _lane_resolver().snapshot() is not exposed on /v1/cluster/health.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":10,"ws_id":"WS-4","status":"partial","confidence":"high","evidence":["[agents].role is first-class: mios.toml [agents.<name>].role declared (lines 850,925,969,1064...) under header 808-822 'agent-pipe is the always-first orchestrator; Hermes/OpenCode are sub-agents it delegates to'; parsed in _load_agent_registry at usr/lib/mios/agent-pipe/server.py:3430 ('role': str(cfg.get('role','general'))). Refine picks by role-match first (822, 2449)."],"gap":"No first-class 'effort'-scaling knob that scales orchestration intensity by query complexity (only width caps + facet self-gating exist), synthesis is gather-then-synthesize (streaming=False) rather than incremental/async-streamed, and there is no co-located unit test exercising the hop-budget guard (_HOP_HEADER/_VIA_HEADER/_depth_exhausted) or the 99-postcheck 12c worker-recursion lint.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":11,"ws_id":"WS-2","status":"partial","confidence":"high","evidence":["OBJECTIVE has 5 pieces; registry plumbing exists but each piece has a specific gap. No dedicated test_mios_<topic>.py for WS-2 capability-registry (ls of agent-pipe shows none for catalog/registry/manifest/skill)."],"gap":"Five named acceptance pieces each missing a specific element: (1) recipes are flat (not DAG); skills are pgvector miner rows with no structured [skills.*] model or DAG serialization; (2) /v1/tools is the unified projection but is NOT JSON-schema-validated and is not the sole endpoint; (3) /v1/tools (server.py:17587) applies NO per-agent RBAC -- _agent_rbac_filter is only wired at the fan-out dispatch path (14513), not at the discovery manifest; (4) [verbs.pkg] is permission=write/tier=common, not elevated to high-privilege; (5) refusal detection is still hardcoded English (refusal-patterns.txt + _DISCLAIM_MARKERS at server.py:4739) -- no generative classifier.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":12,"ws_id":"WS-8","status":"partial","confidence":"high","evidence":["VLM lane baked (objective part 1 DONE): usr/share/mios/llamacpp/mios-llm-light.yaml:145-153 defines the 'qwen3-vl:4b' slot backed by --model /models/holo1.5-7b.gguf + --mmproj /models/holo1.5-7b-mmproj-q8_0.gguf (Holo1.5-7B, qwen2vl arch, ctx 8192); inert-until-operator-places-GGUFs is by design (security classifier blocks the assistant fetch)."],"gap":"The two new verbs the objective explicitly names -- cu_verify and cu_act -- are not implemented anywhere (0 occurrences outside mios-doctor doc strings); there is no closed perception->act->verify loop verb and no WS-8 test (test_mios_computer_use.py / test_mios_cu.py absent). The other 4 of 5 objective parts (VLM baking, :11434 fix, Win+Linux perception unification, heavy_engine+health-gate gating) are present in code.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":13,"ws_id":"WS-A8","status":"done","confidence":"high","evidence":["No mios_trace.py module: Glob of C:/MiOS/usr/lib/mios/agent-pipe/mios_*.py returns 16 modules (aci, evict, hitl, kvfork, owui, sched, stress, codemode, lanes, pg, a2a_principal, reputation, jsonsalvage, goap, selfimprove) but NO mios_trace.py"],"gap":"DONE (implemented post-grounding): WS-A8 commit 4b86de7, test 30/30","needs_vm_to_verify":false,"suggested_action":"mark-complete"},{"task_id":14,"ws_id":"WS-A4","status":"missing","confidence":"high","evidence":["_kv_fork( has ZERO call sites: grep -nE '(await |= )?_kv_fork\\(' server.py returns only the definition at server.py:3150 (async def _kv_fork). Acceptance explicitly requires >=1 CALL site beyond the def -- it FAILS today, fork is still dead code (subtasks 4/5/6 unmet)."],"gap":"The entire WS-A4 delta is unimplemented. The underlying PURE fork mechanism (mios_kvfork.py + _kv_fork driver + flags + 34/34 test) exists from prior WS-8 work, but _kv_fork has zero call sites (dead code). Missing: (a) wire _kv_fork into execute_dag/_execute_dag_node and the council secondary fan-out with child conv-key derivation (mios-kv-<parent>#fork:<node>.bin); (b) add kv_forked:true to node/secondary audit envelopes; (c) create mios_kvgc.py GC sweeper module + wire into the agent-pipe background loop; (d) add kv_gc_* SSOT flags + env overrides; (e) add a tmpfiles e/D age-out backstop to mios-llamacpp.conf for the slots dir; (f) create test_mios_kvgc.py (stdlib-only, must exit 0); (g) de-dup _kv_filename to one source.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":15,"ws_id":"WS-A11","status":"missing","confidence":"high","evidence":["mios_router.py ABSENT: Glob C:/MiOS/usr/lib/mios/agent-pipe/mios_router.py -> No files found (acceptance requires this module with a pure Router class + RouteDecision dataclass)"],"gap":"Entire WS-A11 deliverable set is unimplemented. Need to create usr/lib/mios/agent-pipe/mios_router.py (pure Router class + typed RouteDecision dataclass, AI-hint header, no server.py import) and mios_kernel.py (Kernel facade composing SchedulerManager/MemoryManager/ContextManager/ToolManager/AccessManager thin facades + Dispatcher.run); instantiate KERNEL once at module load in server.py; refactor chat_completions (server.py:24747) so its inline refined.get('intent') mode cascade delegates to KERNEL.router.route(...) and KERNEL.dispatcher.run(...) with behavior parity for chat/dispatch/multi_task/DAG/vision/council/_autonomous turns; add test_mios_router.py + test_mios_kernel.py and a run_tests.sh/conftest.py harness; thread WS-A8 trace ids through the new Router/Dispatcher/manager boundaries.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":16,"ws_id":"WS-A12","status":"missing","confidence":"high","evidence":["mios_preempt.py ABSENT: Glob C:/MiOS/usr/lib/mios/agent-pipe/mios_preempt.py returns no files; this is the central deliverable module of every subtask."],"gap":"Entire workstream unimplemented. None of: mios_preempt.py snapshot-contract module; test_mios_preempt.py; PriorityGate quantum primitive + suspend-counter API in mios_sched.py; interruptible decode loop in _call_agent_stream_inner; quantum wiring into _priority_gate; suspend->requeue->resume Dispatcher cycle; RR_MAX_SUSPENDED admission cap; snapshot-slot free-list in a Context manager (which itself does not exist -- depends on unbuilt WS-A11 kernel seam); [dispatch] rr_quantum/rr_max_suspended/rr_enable TOML keys; stats() RR observability. Note dependency: subtasks 4,7,9 target a WS-A11 Context/Dispatcher seam that is also not yet built.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":17,"ws_id":"WS-A15","status":"missing","confidence":"high","evidence":["No mios_memory.py anywhere in repo: `find /c/MiOS -name mios_memory.py` returns nothing; agent-pipe dir listing (mios_a2a_principal/aci/codemode/evict/goap/hitl/jsonsalvage/kvfork/lanes/owui/pg/reputation/sched/selfimprove/stress) has no mios_memory.py."],"gap":"Entire WS-A15 unimplemented. Need: (1) new usr/lib/mios/agent-pipe/mios_memory.py defining MemoryProvider ABC + PgVectorMemoryProvider (wrapping mios_pg recall/store) + get_memory_provider(name) factory that raises ValueError on unknown (fail-closed) + MIOS_MEMORY_PROVIDER resolution + emb-version stamping helper; (2) rewire _store_knowledge_task (10555), _recall_agent_memory (10641), _recall_knowledge_pg (10706) to delegate to a module-level `_memory` resolved next to the mios_pg import at server.py:104; (3) add `[pgvector].memory_provider = \\\"pgvector\\\"` to mios.toml; (4) add standalone test_mios_memory.py (no live DB, _check harness + FakeRecall golden-parity test recording (table,k,owner)). None of these exist.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":19,"ws_id":"WS-A6","status":"missing","confidence":"high","evidence":["No module: Glob C:/MiOS/usr/lib/mios/agent-pipe/mios_batch.py -> No files found. No test: Glob test_mios_batch.py -> No files found."],"gap":"Entire workstream unimplemented. Need to create mios_batch.py (pure-stdlib coalescer in the mios_sched.py style), add _batch_key(ep, model) + SSOT/module flags under server.py:831-837, wire _BATCH_COALESCER into the single chokepoint _call_agent_complete (server.py:3917) with native-batch bypass for SGLang/vLLM lanes and turn-deadline window bounding, add a .batch block to scheduler_state() (/v1/scheduler, server.py:18124) exposing enabled/open_windows/batches_emitted, append [dispatch] batch_enable/batch_interval keys after mios.toml:1496, and create test_mios_batch.py.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":20,"ws_id":"WS-A16","status":"missing","confidence":"high","evidence":["mios_smartroute.py does NOT exist (Glob C:\\MiOS\\usr\\lib\\mios\\agent-pipe\\mios_smartroute.py = no files); test_mios_smartroute.py does NOT exist (Glob = no files). Both are named deliverables in acceptance + subtasks."],"gap":"Entire WS-A16 SmartRouting/remote-core layer is unbuilt. Missing: (1) Lane cost/quality metadata (kind/cost_in/cost_out/quality_tier) + cost_estimate() in mios_lanes.py; (2) new mios_smartroute.py policy module; (3) [ai.remote_cores.*] + smartroute_mode SSOT block in mios.toml (defaults disabled/off); (4) remote cores wired into _lane_resolver(); (5) secret-store key resolution (not TOML); (6) wiring the dead _oai_msgs_to_anthropic/_anthropic_resp_to_oai/_oai_msgs_to_gemini/_gemini_resp_to_oai adapters into _call_agent_complete_inner/_call_agent_stream_inner (the keystone task); (7) escalation decision in the extracted Router (WS-A11, itself unbuilt); (8) per-day cost ledger; (9) remote cores + spend in /v1/cluster/health|scheduler diagnostics; (10) test_mios_smartroute.py + test_mios_lanes.py extension. The WS-1 base resolver exists; nothing WS-A16-specific does.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":21,"ws_id":"WS-A9","status":"missing","confidence":"high","evidence":["mios_pdp.py does NOT exist: Glob of C:/MiOS/usr/lib/mios/agent-pipe/mios_*.py returns 16 siblings (aci, evict, hitl, kvfork, owui, sched, stress, codemode, lanes, pg, a2a_principal, reputation, jsonsalvage, goap, selfimprove) - no mios_pdp.py."],"gap":"All 10 subtasks are unimplemented. Required: (1) create mios_pdp.py pure-stdlib decision core; (2) add _dispatch_agent_var contextvar near server.py:7397; (3) fix the fail-OPEN max_permission defect at server.py:6981-6984 (unknown tier must fail-closed, not no-ceiling); (4) build _policy_decide(...); (5) wire the PDP into _dispatch_mios_verb_inner at server.py:16701 after the tool=re.sub normalize (the actual bypass-closing step - dispatch currently has zero capability check); (6) refactor _agent_rbac_filter / _user_rbac_filter surface pruning to share the PDP core so surface and dispatch cannot diverge; (7) emit a unified PDP audit event on deny (+ optional allow when [ai].pdp_audit_allow=true); (8) add [ai]/[agents.*]/[users.*] PDP config keys to mios.toml; (9) add an RBAC config lint block to /automation/99-postcheck.sh; (10) create test_mios_pdp.py (stdlib only).","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":22,"ws_id":"WS-A13","status":"missing","confidence":"high","evidence":["Glob for **/mios_sandbox.py and **/test_mios_sandbox.py returns NO files; the agent-pipe sibling-module set is 17 mios_*.py (aci/evict/hitl/kvfork/owui/sched/stress/codemode/lanes/pg/a2a_principal/reputation/jsonsalvage/goap/selfimprove/toolconflict/trace) and 20 test_mios_*.py -- none named sandbox. The core WS-A13 deliverable module + test are absent."],"gap":"Entire WS-A13 is unimplemented. Needs: (1) mios_sandbox.py pure-stdlib module (mirror mios_codemode.py/mios_sched.py) resolving a per-verb sandbox profile from the permission tier; (2) test_mios_sandbox.py; (3) a per-verb sandbox_profile projection added to [verbs.*] in mios.toml (tier-resolved at the catalog-load seam); (4) confinement wired at the _dispatch_mios_verb_inner chokepoint (server.py:16946); (5) per-verb writable workspace /var/lib/mios/ai/dispatch/<verbhash>-<uuid>/ declared in usr/lib/tmpfiles.d/mios.conf; (6) a tier seccomp profile; (7) optional heavy-isolation podman Quadlet under usr/lib/bootc/bound-images.d/; (8) launcher-broker rejection of non-host verbs as defense-in-depth; (9) a 99-postcheck.sh assertion; (10) an explicit fail-closed (do-not-degrade-open) degrade policy. The pre-existing code_mode/coderun-sandbox is only the cited mirror pattern, not this feature.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":23,"ws_id":"WS-A14","status":"missing","confidence":"high","evidence":["_HIGH_PRIVILEGE_VERBS (server.py:12949) is a HARDCODED python set literal with NO SSOT load -- subtask 1/2 (SSOT-derived resolver + replace literal) NOT done. mios.toml DOES ship [security].firewall_high_privilege_verbs (mios.toml:623-654) but server.py never reads it; the two have already DRIFTED (the server literal adds 'pkg' at 12989, the toml list omits it)."],"gap":"All 12 WS-A14 subtasks are unimplemented. Pre-existing foundations from earlier WS-6 ([hitl] gate + /v1/hitl/approve + mios_hitl.py) and #62 ([ai].hitl_mode tier gate + out-of-process arbiter) exist, but the WS-A14 deltas are absent: (1) _HIGH_PRIVILEGE_VERBS is a hardcoded literal not derived from [security].firewall_high_privilege_verbs (which already exists and has drifted); (2) no curated-union derived set; (3) no lazy/refresh-safe resolution; (4) taint-refusal set not derived from the same source; (5) _classify_verb_taint sources still hardcoded; (6) the two HITL gates not reconciled into one PDP-routed gate (mios_pdp.py absent); (7) high-risk tier not block-capable by default (hitl_mode=off, [hitl].mode=log); (8) /v1/hitl/approve has no verified-approver check; (10) introspection lacks provenance; (11) no soft-fail/shadow-block window; (12) mios_hitl.py not extended and test_mios_hitl_ssot.py does not exist. This is the security/HITL workstream the prior backlog-clearing effort did NOT build.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":24,"ws_id":"WS-9","status":"partial","confidence":"high","evidence":["WS-9 core #62 gate IS implemented + wired: _hitl_block_reason (server.py:7104-7124) refuses high-risk verbs (perm tier >= [ai].hitl_threshold) in block mode; called at the single dispatch chokepoint dispatch_mios_verb (server.py:16869-16874) returning {hitl_blocked:true, exit_code:126}. Config [ai].hitl_mode=off|audit|block + hitl_threshold at mios.toml:4365-4374."],"gap":"WS-9's own #62 block-capable HITL gate + arbiter client + A2A passport enforce ARE implemented and wired at the dispatch chokepoint. Missing pieces, all from the absorbed WS-A9/WS-A14 checklists: (1) the single dispatch-time PDP that closes the RBAC bypass -- mios_pdp.py/_policy_decide/_dispatch_agent_var/test_mios_pdp.py + the max_permission fail-OPEN fix (server.py:7186) + the 99-postcheck RBAC lint; (2) SSOT-derived high-privilege/taint resolver replacing the hardcoded _HIGH_PRIVILEGE_VERBS literal (server.py:12949) with [security].firewall_high_privilege_verbs (mios.toml:623, currently unconsumed); (3) reconcile the two parallel HITL surfaces ([hitl] _hitl_gate vs [ai].hitl_mode _hitl_block_reason) into one gate; (4) a single _shape_tool_choice(); (5) tests for the #62 gate (test_mios_pdp.py, test_mios_hitl_ssot.py) and an actual mios-policy-arbiter service (only an arbiter URL client exists).","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":25,"ws_id":"WS-A10","status":"missing","confidence":"high","evidence":["mios_principal.py, mios_crl.py, test_mios_principal.py, test_mios_crl.py are ALL ABSENT (verified via test -e in /c/MiOS/usr/lib/mios/agent-pipe/) -- the four core modules/tests the WS names do not exist."],"gap":"All 11 WS-A10 subtasks are unimplemented: (1) mios_principal.py + (2) test_mios_principal.py absent; (3) scoped-token store + _load_caller_tokens()/caller-tokens.json absent; (4) _principal_auth_mw inbound-auth ASGI middleware absent (no 401 on no-Authorization dispatch, no [access].principal_mode); (5) _client_env does not defer to a verified principal; (6) no PDP integration; (7) A2A principal_mode default is 'off' not verify+enforce and lives under [agent_passport] not [access]; (8) mios_crl.py CRL enforcement absent (only an advertised card URL); (9) persistent reputation absent (in-memory only, no pg table, no load/flush seam); (10) /v1/hitl/approve still has no authorization; (11) the [access] SSOT config surface in mios.toml is absent. Only the adjacent WS-6 signed-A2A-delegation principal (mios_a2a_principal.py + test_mios_a2a_passport.py) and #54 in-memory reputation exist -- they are NOT the edge-bound caller-principal/CRL/persistent-reputation this WS specifies.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":26,"ws_id":"WS-6","status":"partial","confidence":"high","evidence":["Per-USER authz IS built+wired: _user_rbac_filter (server.py:7214-7273) applies [users.<name>].denied_verbs/allowed_verbs/max_permission from mios.toml, called at server.py:14514 (#60 WS-6 per-user authz). SSOT documented mios.toml:865-875 ([users.guest] example)."],"gap":"Signed X-MiOS-User (ed25519 passport) edge identity into _orch_ctx is entirely missing (no X-MiOS-User header, no _principal_auth_mw / scoped-token store / mios_principal.py / mios_crl.py); per-user authz keys only on the surface-CLAIMED, unverified identity (code self-admits at server.py:7223-7226 and mios.toml:870). Also missing: [tenancy] table + per-user quota; principal-scoped coderun sandbox + per-user OS substrate (per-principal sysusers/linger/subuid/@-quadlets \u2014 current sandbox is per-RUN under one mios-ai account); per-principal lane fairness + KV-prefix scoping; persistent reputation + CRL (WS-A10 pieces WS-6 absorbs); and a test for _user_rbac_filter. Present: per-user RBAC filter (_user_rbac_filter, wired), A2A signed delegation principal (mios_a2a_principal.py + test, wired in/out), and owner_user+RLS memory tagging.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":27,"ws_id":"WS-5","status":"partial","confidence":"high","evidence":["DURABILITY done: nightly pg_dump shipped as usr/lib/systemd/system/mios-pgvector-backup.service (Type=oneshot, User=mios-pgvector uid 826, loopback pg_hba-trust, degrade-open every branch exits 0, gzip+timestamp+prune-to-KEEP) + mios-pgvector-backup.timer (OnCalendar=*-*-* 00:30:00, Persistent=true). Backup dir declared NO-MKDIR-IN-VAR in usr/lib/tmpfiles.d/mios-backups.conf (d /var/lib/mios/backups 0750 826 826)."],"gap":"Three nameable residuals: (1) RLS is app-side filter-before-retrieval (mios_pg.build_recall owner clause) not native Postgres CREATE POLICY/ENABLE ROW LEVEL SECURITY (deliberately deferred per plan doc, but the objective names 'Postgres RLS'); (2) owner_user exists only on the knowledge table -- agent_memory and scratch lack the column and mios-remember does not owner-tag them (agent_memory recall fail-closes under enforce); (3) no test exercises the owner-scoped recall path (test_mios_pg.py t_build_recall ignores the owner param). The kanban->pgvector authoritative cutover (canonical kanban table populated; pipe still writes kanban_shadow) is WS-A3's deliverable, not a WS-5 blocker.","needs_vm_to_verify":true,"suggested_action":"implement-delta"},{"task_id":28,"ws_id":"WS-7","status":"partial","confidence":"high","evidence":["k3s BRIDGE = DONE (strong): tools/generate-k3s-manifests.sh:5,48-90 uses `podman kube generate` (podman-authored, NOT hand-rolled), strips volatile fields (creationTimestamp/bind-mount-options/podman-version) for deterministic output, emits an AI-hint header per file, writes to usr/share/mios/k3s/generated/. Path is k3s/generated/ NOT the spec's literal usr/share/mios/k3s-manifests/, but the pods-as-SSOT->k8s objective is met."],"gap":"Two of four objective bullets are unmet. (1) The `[gpu]` env block is NOT centralized: no [gpu] table in mios.toml; AddDevice=nvidia.com/gpu=all remains duplicated across mios-llm-light/heavy/heavy-alt/worker@ quadlets. (2) `.pod` conversion is single-pod-only (mios-webtools.pod); no other co-resident groups were collapsed to pods. The k3s bridge (generator+11 manifests+test+README) and the Law-6 bound-image exception list are both fully done.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":29,"ws_id":"WS-A17","status":"missing","confidence":"high","evidence":["Spec WS-A17 (task_id 29) in C:/Users/mios/_mios_delta_specs.json requires: mios-registry CLI, mios-pkg.toml manifests, mios-pkg.schema.json + mios-registry.schema.json, ai/v1/packages/ tree, mios_manifest_gen.py (extended), automation/lib/generate-packages.sh, [ai].package_registry flag, 99-postcheck drift check, test_mios_manifest_gen.py."],"gap":"Entire workstream unbuilt. Missing: (1) usr/lib/mios/schemas/mios-pkg.schema.json + mios-registry.schema.json; (2) package manifest/index emission in mios_manifest_gen.py (which itself does not yet exist -- depends on WS-A1); (3) automation/lib/generate-packages.sh + build-time materialization of usr/share/mios/ai/v1/packages/<author>/<name>/<version>/mios-pkg.toml + registry.json; (4) [ai].package_registry feature flag in mios.toml + version/author SSOT keys; (5) read-only usr/libexec/mios/mios-registry CLI (verify/list); (6) packages drift check in 99-postcheck.sh that passes when the flag is false; (7) test_mios_manifest_gen.py coverage. None of the 8 subtasks have any code.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":30,"ws_id":"WS-A18","status":"missing","confidence":"high","evidence":["mios_gossip.py absent: Glob 'usr/lib/mios/agent-pipe/mios_gossip.py' and '**/*gossip*' both return no files; grep 'gossip|DHT|discovery_round|dirty_peers|load_snapshot' across usr/lib/mios/agent-pipe = no matches."],"gap":"Entire WS-A18 delta is unbuilt. Build (a) persistent reputation: add a DB-free persistence seam to mios_reputation.py (load_snapshot/dirty_peers), a pg reputation table in schema-init.sql, and load+periodic-flush wiring in server.py; (b) gossip discovery: new mios_gossip.py module, a /v1/a2a/gossip transport endpoint and periodic gossip round loop in server.py, reputation-gated discovery admission, a quarantine/clear surface (mios-a2a-quarantine + mios-a2a-discover), an additive gossip-merged peer-layer schema comment + vendor default in a2a-peers.json; plus test_mios_gossip.py and persistence cases in test_mios_reputation.py. Only the prior #54 in-memory PeerReputation, mios-a2a-discover, tailnet autodiscover, and #60 signed A2A principal exist as the foundation.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":31,"ws_id":"WS-10","status":"partial","confidence":"high","evidence":["CI drift-gate job exists but is INVARIANT/UNIT checks, NOT a regenerate-and-diff round-trip: .github/workflows/mios-ci.yml:32-69 runs only 97-ssot-lint.sh, agent-pipe test_mios_*.py, test_mios_docgen.py, 98-drift-checks.sh. Mirrored by `just drift-gate` (Justfile:68-82) and post-build sub-phases (automation/build.sh:361-389)."],"gap":"The defining WS-10 acceptance is unmet: there is no CI/build step that REGENERATES the derived surfaces (install.env, quadlet renders, ai/v1/*.json manifest, self-grounding/role-system prompts) from the mios.toml SSOT and then DIFFS the regenerated output against the committed tree to FAIL on any difference. The existing gates are static invariant checks (97-ssot-lint placeholder-wiring; 98-drift-checks manifest-parse/ref-integrity + AI-hint coverage ratchet) and unit tests \u2014 none performs a generate-then-`git diff --exit-code` round-trip. Required delta: a CI step (and matching `just` target / build sub-phase) that runs the generators (tools/generate-ai-manifest.py, 15-render-quadlets render, mios-sync-env/install.env, mios-gen-role-system) into a clean worktree and fails if the result differs from committed. The two already-satisfied clauses (100%-ratchet AI-hint coverage + index, single /MiOS.md inject point) are complete.","needs_vm_to_verify":false,"suggested_action":"implement-delta"},{"task_id":32,"ws_id":"WS-11","status":"missing","confidence":"high","evidence":["3-projection skills/recipes is INCOMPLETE: OpenAI (/v1/tools server.py:17586-17640) and MCP (/v1/resources server.py:17687-17709) both project verbs+recipes+skills, but the A2A projection _build_agent_card (server.py:17773-17859) renders ONLY _AGENT_REGISTRY agents as A2A skills -- promoted skills and OS recipes are NOT in the AgentCard. So skills/recipes are not 'finished in all three projections'."],"gap":"Three core WS-11 deliverables are absent or only half-built: (1) A2A projection of promoted skills + OS recipes into the AgentCard (today agents-only); (2) a passport-gated AGNTCY directory (register+search) plus passport/reputation-gated discovery ADMISSION (today admission is open, principal_mode defaults off); (3) the replay-reliability gate guarding a closed propose->validate->promote self-improve loop (today only capture/observability + read-only analyzer exist). Both absorbed sub-workstreams are unbuilt: WS-A17 versioned package format + mios-registry CLI + ai/v1/packages projection, and WS-A18 mios_gossip.py + pg-persistent reputation + quarantine surface.","needs_vm_to_verify":false,"suggested_action":"implement-delta"}],"note":"4 tasks complete: #1 PREFLIGHT, #4 WS-0A (verify), #8 WS-A7, #13 WS-A8. Remaining 25 = 18 missing + 7 partial (vs plan acceptance)."}
