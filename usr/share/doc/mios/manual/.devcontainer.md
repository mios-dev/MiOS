<!-- AI-hint: Manual pages distilled from the source comments of .devcontainer, sanitized, each passage anchored to the comment it came from. -->

# .devcontainer

### The verification gate must fail closed

The verification gate must fail closed: a non-zero exit here fails
postStartCommand and surfaces in the devcontainer UI/CLI. Do not mask this
with `|| true` -- that turned every failing invariant into a silent pass.

<!-- mios-src:45856ff5a50d from .devcontainer/post-start.sh:5-7 -->
