<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Systemd unit that executes mios-cdi-detect to identify and configure the correct CDI specification (WSL, VM, or bare metal) for GPU acceleration before the nvidia-cdi-refresh service.
AI-related: /usr/libexec/mios/mios-cdi-detect, mios-cdi-detect, nvidia-cdi-refresh.service, systemd-tmpfiles-setup.service, local-fs.target, multi-user.target

<!-- mios-src:6f8189146d5f from usr/lib/systemd/system/mios-cdi-detect.service:1-2 -->

