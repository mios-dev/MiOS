<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Configures the kvmfr kernel module's shared memory size (default 128MB) to allocate the buffer required for Looking Glass frame buffer transmission.
'MiOS': KVMFR shared memory for Looking Glass
128MB sufficient for 4K SDR. Increase for ultrawide or HDR.

<!-- mios-src:b00437f38000 from usr/lib/modprobe.d/kvmfr.conf:1-3 -->

