<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Systemd unit that executes mios-flatpak-init to apply global XDG directory permissions for flatpaks, ensuring all apps have read/write access to user folders like Documents and Pictures.
AI-related: /usr/libexec/mios/mios-flatpak-init, mios-flatpak-init, local-fs.target, multi-user.target

<!-- mios-src:52130b1e3737 from usr/lib/systemd/system/mios-flatpak-init.service:1-2 -->

