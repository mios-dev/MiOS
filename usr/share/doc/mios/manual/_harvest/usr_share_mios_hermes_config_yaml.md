<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### DEPRECATED

DEPRECATED: This config has been migrated to the [gateway] section of mios.toml
AI-hint: Default configuration for the Hermes AI agent, defining Ollama backend endpoints, model lists, auxiliary processing models, and SearXNG web search integration for the MiOS agent loop.
AI-related: /usr/share/mios/hermes/config.yaml, /etc/mios/hermes/config.yaml, /etc/mios/hermes/config.local.yaml., /etc/mios/mios.toml, /usr/share/mios/mios.toml, /etc/mios/hermes/api.env, /usr/libexec/mios/mios-mcp-server, /etc/mios/hermes/config.local.yaml, mios-mcp-server, mios-hermes
/usr/share/mios/hermes/config.yaml
Hermes-Agent vendor defaults. Copied to /etc/mios/hermes/config.yaml
on first boot by usr/lib/tmpfiles.d/mios-hermes.conf. Operators
override under /etc/mios/hermes/config.local.yaml.

<!-- mios-src:33ae75f1382c from usr/share/mios/hermes/config.yaml:1-7 -->

