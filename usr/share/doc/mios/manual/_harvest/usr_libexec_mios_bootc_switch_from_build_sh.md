<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### !/usr/bin/env bash AI-hint: Executes `bootc switch` to...

!/usr/bin/env bash
AI-hint: Executes `bootc switch` to update the active system partition to a local image (e.g., `localhost/mios:latest`) by parsing the `last_build.txt` sentinel file generated by the Forgejo Runner.
AI-related: /usr/libexec/mios/bootc-switch-from-build.sh, mios-bootc-switch, mios-dash
AI-functions: _log

<!-- mios-src:d93a78c598a7 from usr/libexec/mios/bootc-switch-from-build.sh:1-4 -->

