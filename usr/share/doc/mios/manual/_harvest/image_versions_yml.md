<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Defines pinned SHA256 digests for base images and bootc-image-builder tools, used by Renovate to automate image updates and by build scripts to ensure reproducible MiOS builds.
MiOS v0.2.4  Base Image Version Pinning
Managed by Renovate Bot  do NOT edit digests manually.
Renovate opens PRs when upstream images publish new digests.

To enable: install Renovate GitHub App on the repo.
Digests auto-update with a 7-day stability period.

USAGE: Renovate updates the Containerfile ARG digest directly via the
customManager regex in renovate.json. This file is reference documentation.

<!-- mios-src:3f8a606e9581 from image-versions.yml:1-10 -->

