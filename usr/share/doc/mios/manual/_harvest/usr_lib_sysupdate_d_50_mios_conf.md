<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Configures systemd-sysupdate to fetch and verify MiOS system images from a remote URL, mapping them to the local root partition for atomic A/B updates in UKI/SecureBoot environments.
AI-related: mios-dev
systemd-sysupdate declarative config for 'MiOS'
This prepares the system for atomic A/B updates in UKI/SecureBoot environments.

<!-- mios-src:33ef5bc19e66 from usr/lib/sysupdate.d/50-mios.conf:1-4 -->

