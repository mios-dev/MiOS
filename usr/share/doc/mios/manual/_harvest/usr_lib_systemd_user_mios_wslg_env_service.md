<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: A systemd user oneshot service that imports WSLg DISPLAY/WAYLAND_DISPLAY environment variables into the user manager to enable GUI applications and flatpak scopes to render windows via the mios-launcher.
AI-related: /usr/libexec/mios/mios-wslg-env-import, mios-launcher, mios-wslg-env-import, mios-wslg-env, mios-launcher.service, basic.target, default.target

<!-- mios-src:0f82fcb1ea6a from usr/lib/systemd/user/mios-wslg-env.service:1-2 -->

