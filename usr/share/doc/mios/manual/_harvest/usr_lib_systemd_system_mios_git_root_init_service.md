<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Systemd unit that executes /usr/libexec/mios/git-root-init.sh to initialize the root filesystem as a git working tree from a local Forgejo repository during first boot if /.git is missing.
AI-related: /usr/libexec/mios/git-root-init.sh, mios-dev, mios-forge-firstboot, mios-forge, mios-forge-firstboot.service, mios-forge.service, network-online.target, multi-user.target, default.target, localhost:3000

<!-- mios-src:3e2b1e54fadf from usr/lib/systemd/system/mios-git-root-init.service:1-2 -->

