<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Executes the forge-firstboot.sh script to bootstrap the Forgejo instance, creating the initial admin user and repository credentials required for the MiOS self-replication and CI runner integration.
AI-related: /etc/mios/install.env, /usr/libexec/mios/forge-firstboot.sh, /etc/mios/forge, mios-dev, mios-forge, mios-sync-env, mios-forgejo-runner, mios-forge.container, mios-forge.service, mios-forgejo-runner.service

<!-- mios-src:d64deb29adbc from usr/lib/systemd/system/mios-forge-firstboot.service:1-2 -->

