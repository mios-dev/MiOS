<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Systemd unit that triggers the mios-keyring-autounlock wrapper to initialize and unlock the gnome-keyring-daemon using credentials from mios.toml before the graphical session starts.
AI-related: /usr/libexec/mios/mios-keyring-autounlock, mios-keyring-autounlock, dbus.socket, graphical-session.target, default.target

<!-- mios-src:c70fdca43d2d from usr/lib/systemd/user/mios-keyring-autounlock.service:1-2 -->

