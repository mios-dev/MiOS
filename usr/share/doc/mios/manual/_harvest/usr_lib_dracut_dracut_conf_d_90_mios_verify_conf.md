<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Configures dracut to include the mios-verify script and systemd service into the initramfs to enable boot-time cryptographic integrity verification of the MiOS filesystem.
AI-related: mios-verify, mios-verify.service
MiOS-OS: Instruct dracut to bundle the cryptographic integrity scripts

<!-- mios-src:362d1682d735 from usr/lib/dracut/dracut.conf.d/90-mios-verify.conf:1-3 -->

