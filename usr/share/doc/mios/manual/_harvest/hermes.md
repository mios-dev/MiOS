<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: MiOS configuration and runtime asset for config-worker.yaml.
AI-related: /usr/share/mios/hermes/config-worker.yaml, /usr/libexec/mios/mios-mcp-server, mios-hermes-firstboot, mios-heavy, mios-hermes-browser-worker, mios-mcp-server, hermes-agent.service, mios-hermes-browser-worker.service, localhost:8441, localhost:8450

<!-- mios-src:499dca24689d from usr/share/mios/hermes/config-worker.yaml:1-2 -->

### AI-hint

AI-hint: DEPRECATED: This config has been migrated to the [gateway] section of mios.toml Default configuration for the Hermes AI agent, defining Ollama backend endpoints, model lists, auxiliary processing models, and SearXNG web search...
AI-doc: usr/share/doc/mios/manual/hermes.md

<!-- mios-src:a32f174028d3 from usr/share/mios/hermes/config.yaml:1-2 -->
