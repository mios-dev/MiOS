<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: User-specific configuration file for MiOS providing overrides for identity, locale, AI endpoints, and desktop themes, initialized from system defaults and used by the MiOS core to customize the local environment.
AI-related: /etc/mios/profile.toml, localhost:8080
~/.config/mios/profile.toml -- Per-user 'MiOS' profile.

Seeded from /etc/mios/profile.toml on first login by `mios init-user-space`.
Edit freely; `mios reinit-user-space` overwrites this file from the system
profile, so back up customizations first.

<!-- mios-src:38bad01efe82 from etc/skel/.config/mios/profile.toml:1-7 -->

