<!-- AI-hint: Prose harvested out of source comments by `mios-manual harvest`; each passage carries the mios-src anchor that proves which comment it came from. -->

# Harvested notes

### AI-hint

AI-hint: Systemd unit file defining the K3s Kubernetes server service, managing the lifecycle, resource limits, and startup sequence for the MiOS cluster orchestration layer.
AI-related: mios-k3s-init, mios-k3s-init.service, network-online.target, multi-user.target

<!-- mios-src:1a1b300a30da from usr/lib/systemd/system/k3s.service:1-2 -->

