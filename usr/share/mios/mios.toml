# AI-hint: The primary configuration file defining the MiOS user profile, providing the base defaults for identity, system environment, and service configurations used by tools ...
# AI-doc: usr/share/doc/mios/manual/mios.md

# ----------------------------------------------------------------------------
# [migration] -- Per-surface native Rust vs legacy resolver selection toggles.
# ----------------------------------------------------------------------------
[migration]
use_rust_resolver_shell = true
use_rust_resolver_powershell = true
use_rust_resolver_python = true
use_rust_resolver_install_env = true
use_compiled_oscontrol = true
use_compiled_ainode = true

# ----------------------------------------------------------------------------
# [security.privileged_units] -- Roster of systemd units requiring privileged access.
# ----------------------------------------------------------------------------
[security.privileged_units]
unconfined = [
  "mios-hermes-worker.service",
  "mios-ceph-bootstrap.service",
]

# ----------------------------------------------------------------------------
# [firewall] -- Firewall open ports SSOT selector for public zone.
# ----------------------------------------------------------------------------
[firewall]
open_ports = [
  "forge_http",
  "open_webui",
  "code_server",
  "hermes",
  "searxng",
  "cockpit",
  "hermes_dashboard",
  "llm_light",
  "pgvector",
  "cockpit_link",
  "adguard_ui",
  "ssh",
  "forge_ssh",
  "vllm",
  "sglang",
  "cpu_node",
]



# ----------------------------------------------------------------------------
# [ssot_consumers] -- Shrink-only register: config keys a consumer reads that
# the SSOT does not declare at that path.
# ----------------------------------------------------------------------------
[ssot_consumers]
doc = "Shipped Python reads config as _toml_section(\"<table>\").get(\"<key>\"). When <table>.<key> does not exist the consumer silently takes its compiled default -- the SSOT and the code disagree with nobody told, and every test that stubs the value still passes. Nine security controls sat unreachable this way (and one of the nine entries, the memory guard, was itself such a control) under an unclosed [security.nohc_allowlist] header (T-325). MISPLACED means the key name is declared elsewhere in the SSOT, so one side has the wrong path; UNDECLARED means it exists nowhere, so it is an optional escape hatch or a dead read. Draining an entry: decide which side is right, move the key or fix the consumer, then lower max_unresolved. Gate: check_ssot_consumer_keys."
max_unresolved = 2
unresolved = [
  "a2a.security",                      # MISPLACED -> packages.security/security
  "ai.micro_model",                    # MISPLACED -> ai.host_thresholds.micro_model
]

# ----------------------------------------------------------------------------
# [unit_projection] -- Shrink-only debt register for the [units] projection.
# ----------------------------------------------------------------------------
[unit_projection]
doc = "[units.*] is the SOURCE and usr/lib/systemd/system is the DERIVED artifact (Law 8), but the declarations went stale while nothing compared them: mios-unit-gen --check rendered into memory, printed PASSED and returned, and its golden test diffed the unit tree against tests/golden/, a byte copy of that same tree. This register lists every unit [units.*] declares whose rendering no longer matches the file it ships. It only shrinks -- tools/native/mios-unit-gen/tests/projection.rs fails an entry that has stopped drifting as loudly as one that starts, so the count cannot be padded. Draining an entry: `mios-unit-gen --render <unit> | diff - usr/lib/systemd/system/<unit>`, then correct [units.*] (the file on disk is what boots, so it wins). A unit absent from BOTH this register and [units.*] is not covered at all -- 52 of the tree's 120 units are in that state, which is the larger debt behind T-317."
max_drift = 55
drift = [
  "hermes-worker-firstboot.service",
  "hermes-worker.path",
  "hermes-worker.service",
  "mios-account-sync.service",
  "mios-additionalimagestores-perms.path",
  "mios-adguard-firstboot.service",
  "mios-agent-pipe.service",
  "mios-agents.service",
  "mios-ai-firstboot.service",
  "mios-ai-firstboot.timer",
  "mios-aios-refresh.timer",
  "mios-bound-images-firstboot.service",
  "mios-ceph-bootstrap.service",
  "mios-daemon.service",
  "mios-dashboard-issue.timer",
  "mios-desktop.target",
  "mios-embed-backfill.service",
  "mios-embed-backfill.timer",
  "mios-finetune-serve.service",
  "mios-firewall-ports.service",
  "mios-firstboot.target",
  "mios-forge-firstboot.service",
  "mios-forgejo-runner-firstboot.service",
  "mios-gpu-amd.service",
  "mios-gpu-detect.service",
  "mios-gpu-intel.service",
  "mios-gpu-nvidia.service",
  "mios-gpu-nvidia.service.d/10-cycle-fix.conf",
  "mios-gpu-pv-detect.service",
  "mios-gpu-status.service",
  "mios-ha-node.target",
  "mios-headless.target",
  "mios-hermes-browser-worker.service",
  "mios-hermes-browser.service",
  "mios-hermes-firstboot.service",
  "mios-hybrid.target",
  "mios-k3s-master.target",
  "mios-k3s-worker.target",
  "mios-libexec-perms.path",
  "mios-mcp.service",
  "mios-models-firstboot.service",
  "mios-opencode-gateway.service",
  "mios-pgvector-backup.service",
  "mios-pgvector-backup.timer",
  "mios-podman-gc.service",
  "mios-policy-arbiter.service",
  "mios-shell-session-gc.service",
  "mios-skills-miner.timer",
  "mios-suggestion-refresh.timer",
  "mios-swarm-pack-firstboot.service",
  "mios-sys-env-refresh.timer",
  "mios-userdb-render.service",
  "mios-webtools-firstboot.service",
  "mios-wsl-firstboot.service",
  "mios-wsl-flatpak-export-sync.path",
]

[units."mios-podman-gc.service".Unit]
comment = "# AI-hint: Systemd unit that executes a forced Podman system prune to reclaim disk space from unused images and containers, specifically targeting WSL and non-containerized environments."
Description = "'MiOS' Podman Garbage Collection"
ConditionVirtualization = ["|!container", "|wsl"]

[units."mios-podman-gc.service".Service]
Type = "oneshot"
ExecStart = "/usr/bin/podman system prune -a -f"

[units."mios-podman-gc.timer".Unit]
comment = "# AI-hint: Systemd timer unit that triggers the mios-podman-gc.service weekly to automate the removal of stale Podman containers, images, and networks to reclaim disk space.\n# AI-related: mios-podman-gc, mios-podman-gc.service, timers.target"
Description = "Weekly Podman Cleanup"

[units."mios-podman-gc.timer".Timer]
OnCalendar = "weekly"
Persistent = "true"
[units."mios-podman-gc.timer".Install]
WantedBy = "timers.target"

[units."mios-ai-firstboot.timer".Unit]
comment = "# AI-hint: Defines the systemd timer for mios-ai-firstboot.service, controlling post-boot AI provisioning retries until the sentinel file is present.\n# AI-related: /var/lib/mios/.ai-firstboot-done, mios-ai-firstboot.service, timers.target\n# /usr/lib/systemd/system/mios-ai-firstboot.timer\n# Retries the first-boot AI stack deployment if it didn't complete on\n# initial boot (e.g. machine rebooted mid-pull, or GPU drivers were\n# mid-dkms build). The timer is enabled by default so the attempt happens\n# automatically, but is gated on the absence of the sentinel file in\n# place. The .service degrades open (exit 0) on a partial provision and carries\n# ConditionPathExists=!<sentinel>, so once /var/lib/mios/.ai-firstboot-done is\n# written the timer-fired run no-ops. The same condition here stops the timer\n# itself from firing needlessly once provisioning is complete."
Description = "'MiOS' AI Plane First-Boot Provisioning Retry Schedule"
Documentation = "file:///usr/libexec/mios/mios-ai-firstboot-provision.sh"
ConditionPathExists = "!/var/lib/mios/.ai-firstboot-done"

[units."mios-ai-firstboot.timer".Timer]
OnBootSec = "2min"
OnUnitInactiveSec = "10min"
Persistent = "true"
Unit = "mios-ai-firstboot.service"

[units."mios-ai-firstboot.timer".Install]
WantedBy = "timers.target"

[units."mios-aios-refresh.timer".Unit]
comment = "# AI-hint: Systemd timer that triggers mios-aios-refresh.service every 15 minutes to synchronize the Single Source of Truth (SSOT) and update the A2A fleet discovery peer list.\n# AI-related: mios-aios-refresh, mios-aios-refresh.service, timers.target"
Description = "Periodic MiOS AIOS refresh (SSOT role SYSTEMs + A2A fleet discovery)"

[units."mios-aios-refresh.timer".Timer]
comment = "# 2 min after boot (agent-pipe up by then), then every 15 min so the role\n# SYSTEMs track catalog/SSOT changes and the peer list tracks the live fleet."
OnBootSec = "2min"
OnUnitActiveSec = "15min"
Persistent = "true"
[units."mios-aios-refresh.timer".Install]
WantedBy = "timers.target"

[units."mios-dashboard-issue.timer".Unit]
Description = "'MiOS' dashboard /etc/issue.d refresh timer"
Documentation = "file:///usr/libexec/mios/mios-dashboard-render-issue.sh"

[units."mios-dashboard-issue.timer".Timer]
OnBootSec = "2min"
OnUnitActiveSec = "5min"
AccuracySec = "30s"
Unit = "mios-dashboard-issue.service"
Persistent = "false"
[units."mios-dashboard-issue.timer".Install]
WantedBy = "timers.target"

[units."mios-embed-backfill.timer".Unit]
comment = "# AI-hint: Defines the systemd timer for the mios-embed-backfill.service, controlling the periodic execution interval (default 15m) for background embedding backfilling.\n# AI-related: /usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py, mios-embed-backfill.service, timers.target\n# /usr/lib/systemd/system/mios-embed-backfill.timer"
Description = "'MiOS' Embedding Backfill Timer"
Documentation = "file:///usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py"

[units."mios-embed-backfill.timer".Timer]
OnBootSec = "5min"
OnUnitActiveSec = "15min"
AccuracySec = "1min"
Unit = "mios-embed-backfill.service"
Persistent = "true"
[units."mios-embed-backfill.timer".Install]
WantedBy = "timers.target"

[units."mios-pgvector-backup.timer".Unit]
Description = "'MiOS' daily pgvector datastore backup schedule"
Documentation = "file:///usr/lib/systemd/system/mios-pgvector-backup.service"

[units."mios-pgvector-backup.timer".Timer]
comment = "# Daily, shortly after midnight local time. Persistent=true catches up a missed\n# run (machine asleep/off at the scheduled time) on the next boot. Randomized\n# delay spreads the dump off the exact minute so it doesn't collide with other\n# midnight jobs."
OnCalendar = "*-*-* 00:30:00"
Persistent = "true"
RandomizedDelaySec = "15m"
AccuracySec = "5m"

[units."mios-pgvector-backup.timer".Install]
WantedBy = "timers.target"

[units."mios-podman-ps.timer".Unit]
comment = "# AI-hint: Systemd timer that triggers mios-podman-ps.service every 15 seconds to refresh the podman container snapshot data for the MiOS dashboard display.\n# AI-related: mios-podman-ps, mios-podman-ps.service, timers.target"
Description = "'MiOS' refresh the podman container snapshot for the dashboard"

[units."mios-podman-ps.timer".Timer]
OnBootSec = "10s"
OnUnitActiveSec = "15s"
AccuracySec = "2s"

[units."mios-podman-ps.timer".Install]
WantedBy = "timers.target"

[units."mios-skills-miner.timer".Unit]
Description = "'MiOS' Phase C.2 skill-miner cadence (sequential pattern mining)"
Documentation = "file:///usr/libexec/mios/mios-skills"

[units."mios-skills-miner.timer".Timer]
OnBootSec = "10min"
OnUnitActiveSec = "60min"
AccuracySec = "2min"
Unit = "mios-skills-miner.service"
Persistent = "true"
[units."mios-skills-miner.timer".Install]
WantedBy = "timers.target"

[units."mios-suggestion-refresh.timer".Unit]
comment = "# AI-hint: Systemd timer that triggers mios-suggestion-refresh.service every 10 minutes to update OWUI starter chips based on current system state, kanban data, and recent user intents.\n# AI-related: /usr/libexec/mios/mios-suggestion-refresh, mios-suggestion-refresh, mios-suggestion-refresh.service, timers.target\n# /usr/lib/systemd/system/mios-suggestion-refresh.timer\n# Fires mios-suggestion-refresh.service every 10 minutes so the\n# OWUI starter chips revolve based on current MiOS state (recent\n# kanban, daemon nudges, recent refine intents). Operators tune\n# the cadence with:\n#   sudo systemctl edit mios-suggestion-refresh.timer\n#   [Timer]\n#   OnUnitActiveSec=30min"
Description = "'MiOS' starter-chip refresh cadence"
Documentation = "file:///usr/libexec/mios/mios-suggestion-refresh"

[units."mios-suggestion-refresh.timer".Timer]
comment = "# First refresh 2 min after boot so the chain has warmed up.\n# Then every 10 min while running."
OnBootSec = "2min"
OnUnitActiveSec = "10min"
AccuracySec = "1min"
Unit = "mios-suggestion-refresh.service"
Persistent = "true"
[units."mios-suggestion-refresh.timer".Install]
WantedBy = "timers.target"

[units."mios-sys-env-refresh.timer".Unit]
comment = "# AI-hint: Systemd timer that triggers the mios-sys-env-refresh service every 900 seconds to synchronize the sys_env environment cache with current system state, ensuring shared snapshots reflect recent app/service changes.\n# AI-related: mios-sys-env-refresh, timers.target"
Description = "'MiOS' refresh cadence for the sys_env environment cache"

[units."mios-sys-env-refresh.timer".Timer]
comment = "# Mirrors the daemon's directory_entry refresh cadence (~15 min): installed\n# apps / services / loaded models change far slower than container state, so a\n# 15-min refresh keeps the shared snapshot current without churn. The manual\n# sys_env_refresh verb covers the \"I just installed X\" case immediately."
OnBootSec = "45s"
OnUnitActiveSec = "900s"
AccuracySec = "10s"
Persistent = "true"
[units."mios-sys-env-refresh.timer".Install]
WantedBy = "timers.target"


[units."mios-firstboot.target".Unit]
comment = "# AI-hint: Defines the mios-firstboot.target unit to orchestrate initial provisioning services (CDI, libvirtd, and GRD setup) during the first boot sequence of the MiOS system.\n# AI-related: mios-firstboot, mios-cdi-detect, mios-libvirtd-setup, mios-grd-setup, mios-cdi-detect.service, mios-libvirtd-setup.service, mios-grd-setup.service, multi-user.target"
Description = "'MiOS' first-boot provisioning"
Documentation = "https://github.com/MiOS-DEV/MiOS"
After = "multi-user.target"
Wants = "mios-cdi-detect.service mios-libvirtd-setup.service mios-grd-setup.service"

[units."mios-firstboot.target".Install]
WantedBy = "multi-user.target"

[units."mios-ai.target".Unit]
comment = "# AI-hint: systemd target grouping all MiOS AI plane services (T-076).\n# usr/lib/systemd/system/mios-ai.target"
Description = "MiOS AI Services Target"
After = "multi-user.target"
Wants = "mios-agent-pipe.service"

[units."mios-ai.target".Install]
WantedBy = "multi-user.target"

[units."mios-compute.target".Unit]
comment = "# AI-hint: Defines the compute systemd target for MiOS, representing a compute/worker node.\n# AI-related: mios-compute, mios-headless.target"
Description = "'MiOS' Compute Role"
Requires = "multi-user.target"
After = "multi-user.target"
Conflicts = "mios-controller.target mios-desktop.target mios-endpoint.target mios-ha-node.target mios-headless.target mios-hybrid.target mios-k3s-master.target"
AllowIsolate = "yes"
[units."mios-compute.target".Install]
WantedBy = "multi-user.target"

[units."mios-controller.target".Unit]
comment = "# AI-hint: Defines the controller systemd target for MiOS, representing the cluster controller.\n# AI-related: mios-controller, mios-headless.target"
Description = "'MiOS' Controller Role"
Requires = "multi-user.target"
After = "multi-user.target"
Conflicts = "mios-compute.target mios-desktop.target mios-endpoint.target mios-ha-node.target mios-headless.target mios-hybrid.target mios-k3s-master.target"
AllowIsolate = "yes"
[units."mios-controller.target".Install]
WantedBy = "multi-user.target"

[units."mios-desktop.target".Unit]
comment = "# AI-hint: Defines the mios-desktop.target systemd unit to initialize the desktop environment, ensuring required virtualization services (libvirtd, virtnetworkd) are active while preventing concurrent headless or cluster-specific targets.\n# AI-related: mios-desktop, mios-headless, mios-k3s-master, mios-ha-node, gdm.service, libvirtd.service, libvirtd.socket, virtnetworkd.service, virtqemud.service, virtstoraged.service"
Description = "'MiOS' Desktop Role"
Requires = "multi-user.target gdm.service libvirtd.service libvirtd.socket virtnetworkd.service virtqemud.service virtstoraged.service virtnodedevd.service"
After = "multi-user.target"
Conflicts = "mios-compute.target mios-controller.target mios-endpoint.target mios-ha-node.target mios-headless.target mios-hybrid.target mios-k3s-master.target"
AllowIsolate = "yes"
[units."mios-desktop.target".Install]
WantedBy = "multi-user.target"

[units."mios-endpoint.target".Unit]
comment = "# AI-hint: Defines the endpoint systemd target for MiOS, representing an edge/client node.\n# AI-related: mios-endpoint, mios-headless.target"
Description = "'MiOS' Endpoint Role"
Requires = "multi-user.target"
After = "multi-user.target"
Conflicts = "mios-compute.target mios-controller.target mios-desktop.target mios-ha-node.target mios-headless.target mios-hybrid.target mios-k3s-master.target"
AllowIsolate = "yes"
[units."mios-endpoint.target".Install]
WantedBy = "multi-user.target"

[units."mios-ha-node.target".Unit]
comment = "# AI-hint: Defines the mios-ha-node.target unit to identify and configure a node as a High Availability cluster member, ensuring it conflicts with desktop/headless modes and requires corosync/pacemaker services.\n# AI-related: mios-ha-node, mios-desktop, mios-headless, mios-k3s-master, corosync.service, pacemaker.service, multi-user.target, mios-desktop.target, mios-headless.target, mios-k3s-master.target"
Description = "'MiOS' HA Cluster Node Role"
Requires = "multi-user.target corosync.service pacemaker.service"
After = "multi-user.target"
Conflicts = "mios-compute.target mios-controller.target mios-desktop.target mios-endpoint.target mios-headless.target mios-hybrid.target mios-k3s-master.target"
AllowIsolate = "yes"
[units."mios-ha-node.target".Install]
WantedBy = "multi-user.target"

[units."mios-headless.target".Unit]
comment = "# AI-hint: Defines the headless systemd target for MiOS, enforcing a non-GUI environment by conflicting with desktop, k3s-master, and ha-node targets to ensure a dedicated server-mode state.\n# AI-related: mios-desktop, mios-k3s-master, mios-ha-node, multi-user.target, mios-desktop.target, mios-k3s-master.target, mios-ha-node.target"
Description = "'MiOS' Headless Role"
Requires = "multi-user.target"
After = "multi-user.target"
Conflicts = "mios-compute.target mios-controller.target mios-desktop.target mios-endpoint.target mios-ha-node.target mios-hybrid.target mios-k3s-master.target"
AllowIsolate = "yes"
[units."mios-headless.target".Install]
WantedBy = "multi-user.target"

[units."mios-hybrid.target".Unit]
comment = "# AI-hint: Defines the mios-hybrid.target unit to orchestrate the concurrent execution of desktop environments, k3s worker nodes, and Ceph OSD services as the primary system state for hybrid-role nodes.\n# AI-related: mios-hybrid, k3s-agent.service, graphical.target, default.target"
Description = "'MiOS' Hybrid role (desktop + k3s-worker + ceph-osd)"
Requires = "graphical.target"
After = "graphical.target"
Conflicts = "mios-compute.target mios-controller.target mios-desktop.target mios-endpoint.target mios-ha-node.target mios-headless.target mios-k3s-master.target"
AllowIsolate = "yes"
Wants = "k3s-agent.service"

[units."mios-hybrid.target".Install]
WantedBy = "multi-user.target"

[units."mios-k3s-master.target".Unit]
comment = "# AI-hint: Defines the systemd target for a K3s master node role, ensuring the K3s service is active and mutually exclusive with other MiOS node profiles like desktop or headless.\n# AI-related: mios-desktop, mios-headless, mios-ha-node, k3s.service, multi-user.target, mios-desktop.target, mios-headless.target, mios-ha-node.target"
Description = "'MiOS' K3s Master Role"
Requires = "multi-user.target k3s.service"
After = "multi-user.target"
Conflicts = "mios-compute.target mios-controller.target mios-desktop.target mios-endpoint.target mios-ha-node.target mios-headless.target mios-hybrid.target"
AllowIsolate = "yes"
[units."mios-k3s-master.target".Install]
WantedBy = "multi-user.target"

[units."mios-k3s-worker.target".Unit]
comment = "# AI-hint: Defines the systemd target for the MiOS K3s worker node role, ensuring the k3s-agent.service is active and providing a specific target for orchestrating worker-node lifecycle and dependencies.\n# AI-related: mios-k3s-worker, k3s-agent.service, multi-user.target, default.target"
Description = "'MiOS' K3s worker role (agent)"
Requires = "multi-user.target"
After = "multi-user.target"
AllowIsolate = "yes"
Wants = "k3s-agent.service"

[units."mios-k3s-worker.target".Install]
WantedBy = "multi-user.target"

[units."mios-cockpit-link.socket".Unit]
comment = "# AI-hint: Socket file for mios-cockpit-link proxy. Maps port 8091 on host/WSL to the cockpit service.\n# AI-related: usr/lib/systemd/system/mios-cockpit-link.service"
Description = "'MiOS' Cockpit Link Proxy Socket"
ConditionVirtualization = "!container"

[units."mios-cockpit-link.socket".Socket]
ListenStream = "0.0.0.0:${MIOS_PORT_COCKPIT_LINK}"
BindIPv6Only = "both"

[units."mios-cockpit-link.socket".Install]
WantedBy = "sockets.target"

[units."mios-firewall-ports.service".Unit]
Description = "'MiOS': ensure firewalld has the MiOS service ports open"
After = "firewalld.service network-online.target"
Wants = "firewalld.service network-online.target"
Before = "mios-agent-pipe.service mios-open-webui.service mios-searxng.service"
ConditionPathExists = "/usr/bin/firewall-cmd"

[units."mios-firewall-ports.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStart = '''/bin/bash -c '\
    set +e; \
    if [ -f /etc/mios/install.env ]; then source /etc/mios/install.env; fi; \
    ports=(\
        "${MIOS_PORT_FORGE_HTTP:-8400}" \
        "${MIOS_PORT_OPEN_WEBUI:-8200}" \
        "${MIOS_PORT_CODE_SERVER:-8900}" \
        "${MIOS_PORT_HERMES:-8720}" \
        "${MIOS_PORT_SEARXNG:-8800}" \
        "${MIOS_PORT_COCKPIT:-8110}" \
        "${MIOS_PORT_HERMES_DASHBOARD:-8210}" \
        "${MIOS_PORT_LLM_LIGHT:-8500}" \
        "${MIOS_PORT_PGVECTOR:-8600}" \
        "${MIOS_PORT_COCKPIT_LINK:-8120}" \
        "${MIOS_PORT_ADGUARD_UI:-8050}" \
        "${MIOS_PORT_SSH:-8100}" \
        "${MIOS_PORT_FORGE_SSH:-8410}" \
        "${MIOS_PORT_VLLM:-8520}" \
        "${MIOS_PORT_SGLANG:-8530}" \
        "${MIOS_PORT_CPU_NODE:-8510}" \
        53 \
    ); \
    for p in "${ports[@]}"; do \
        /usr/bin/firewall-cmd --permanent --add-port=${p}/tcp >/dev/null 2>&1; \
    done; \
    /usr/bin/firewall-cmd --permanent --add-port=53/udp >/dev/null 2>&1; \
    /usr/bin/firewall-cmd --reload >/dev/null 2>&1; \
    echo "MiOS firewalld ports: $(/usr/bin/firewall-cmd --list-ports 2>/dev/null)"; \
    exit 0\''''
ProtectKernelTunables = "yes"
ProtectControlGroups = "yes"
[units."mios-firewall-ports.service".Install]
WantedBy = "multi-user.target"

[units."mios-bootc-switch.path".Unit]
comment = "# AI-hint: Systemd path unit that monitors /var/lib/mios/forge-runner/last-build.txt to trigger mios-bootc-switch.service, automating the bootc kernel/image switch immediately after a Forgejo Runner build completes.\n# AI-related: /usr/libexec/mios/bootc-switch-from-build.sh, mios-bootc-switch, mios-bootc-switch.service, multi-user.target\n# /usr/lib/systemd/system/mios-bootc-switch.path\n# Watches the build-output sentinel that the Forgejo Runner workflow\n# writes after a successful `podman build`. PathChanged fires on\n# either creation or modification, so re-running the workflow with\n# the same image ref still triggers a re-stage (bootc switch is\n# idempotent on identical refs -- this is fine)."
Description = "'MiOS' watch for Forgejo Runner build output -> bootc switch"
Documentation = "file:///usr/libexec/mios/bootc-switch-from-build.sh"

[units."mios-bootc-switch.path".Path]
PathChanged = "/var/lib/mios/forge-runner/last-build.txt"
Unit = "mios-bootc-switch.service"

[units."mios-bootc-switch.path".Install]
WantedBy = "multi-user.target"


[units."mios-additionalimagestores-perms.path".Unit]
comment = "# AI-hint: Systemd path unit that monitors container storage directories for permission changes and triggers mios-additionalimagestores-perms.service to restore go+rX permissions on image store subdirectories.\n# AI-related: mios-additionalimagestores-perms, mios-additionalimagestores-perms.service, multi-user.target\n# Path-watcher companion to mios-additionalimagestores-perms.service.\n# Re-runs the chmod whenever any of the additional image store's per-\n# driver subdirs change (e.g. podman extracts new layers and resets\n# the perms back to 0700 -- happens on first runtime pull into the\n# additional store, though that's atypical since this store is built\n# at OCI bake time and meant to be read-only at runtime).\n#\n# Together with the .service unit (runs at boot) this makes the\n# go+rX state self-healing: any way the perms get reset, they snap\n# back within seconds."
Description = "'MiOS': watch additionalimagestores for perm changes; retrigger chmod"
ConditionPathIsDirectory = "/usr/lib/containers/storage"

[units."mios-additionalimagestores-perms.path".Path]
PathChanged = [
  "/usr/lib/containers/storage/overlay-images",
  "/usr/lib/containers/storage/overlay-containers",
  "/usr/lib/containers/storage/overlay-layers",
  "/usr/lib/containers/storage/libpod",
]
Unit = "mios-additionalimagestores-perms.service"

[units."mios-additionalimagestores-perms.path".Install]
WantedBy = "multi-user.target"


[units."mios-libexec-perms.path".Unit]
Description = "'MiOS': watch /usr/libexec/mios for perm changes; retrigger chmod"
ConditionPathIsDirectory = "/usr/libexec/mios"

[units."mios-libexec-perms.path".Path]
PathChanged = "/usr/libexec/mios"
Unit = "mios-libexec-perms.service"

[units."mios-libexec-perms.path".Install]
WantedBy = "multi-user.target"


[units."mios-wsl-flatpak-export-sync.path".Unit]
comment = "# AI-hint: Systemd path unit that triggers a synchronization script when flatpak application or icon files are modified in the export directory, ensuring .desktop files are updated for WSL integration.\n# AI-related: mios-is-wsl, multi-user.target, default.target"
Description = "'MiOS' Re-fire flatpak->WSL .desktop sync when flatpak installs/uninstalls land"
ConditionPathExists = "/run/mios-is-wsl"

[units."mios-wsl-flatpak-export-sync.path".Path]
comment = "# Trigger whenever flatpak adds/removes an exported .desktop (any\n# system-wide install / uninstall touches this dir mtime)."
PathChanged = [
  "/var/lib/flatpak/exports/share/applications",
  "/var/lib/flatpak/exports/share/icons",
]

[units."mios-wsl-flatpak-export-sync.path".Install]
WantedBy = "multi-user.target default.target"


[units."hermes-worker.path".Unit]
Description = "'MiOS' watch for the Hermes venv -> (re)start hermes-worker"
Documentation = "file:///usr/lib/systemd/system/hermes-worker.service"
After = "hermes-worker-firstboot.service"

[units."hermes-worker.path".Path]
PathExists = "/usr/lib/mios/agents/.venv/bin/hermes"
Unit = "hermes-worker.service"

[units."hermes-worker.path".Install]
WantedBy = "multi-user.target"

[units."var-home.mount".Unit]
comment = "# AI-hint: Systemd mount unit for mapping the CephFS cluster to /var/home, providing persistent storage for user home directories using the mios.secret for authentication.\n# AI-related: ceph.target, network-online.target, x-systemd.mount, remote-fs.target"
Description = "CephFS mount for user home directories"
Documentation = "man:mount.ceph(8)"
ConditionPathExists = [
  "/etc/ceph/ceph.conf",
  "/etc/ceph/mios.secret",
]
After = "ceph.target network-online.target"
Wants = "network-online.target"

[units."var-home.mount".Mount]
What = "mios@.cephfs=/home"
Where = "/var/home"
Type = "ceph"
Options = "secretfile=/etc/ceph/mios.secret,noatime,_netdev,nofail,x-systemd.device-timeout=30,x-systemd.mount-timeout=30"

[units."var-home.mount".Install]
WantedBy = "remote-fs.target"


[units."var-lib-containers.mount".Unit]
comment = "# AI-hint: Systemd mount unit for the CephFS storage backend at /var/lib/containers, used by the system to provide persistent, shared storage for Podman containers via the mios.secret credential.\n# AI-related: ceph.target, network-online.target, x-systemd.mount, remote-fs.target"
Description = "CephFS mount for Podman container storage"
ConditionPathExists = [
  "/etc/ceph/ceph.conf",
  "/etc/ceph/mios.secret",
]
After = "ceph.target network-online.target"
Wants = "network-online.target"
RequiresMountsFor = "/var/lib"

[units."var-lib-containers.mount".Mount]
What = "mios@.cephfs=/containers"
Where = "/var/lib/containers"
Type = "ceph"
Options = "secretfile=/etc/ceph/mios.secret,noatime,_netdev,nofail,x-systemd.device-timeout=30,x-systemd.mount-timeout=30"

[units."var-lib-containers.mount".Install]
WantedBy = "remote-fs.target"


[units."var-lib-machines.mount".Unit]
comment = "#  SPDX-License-Identifier: LGPL-2.1-or-later\n#\n#  This file is part of systemd.\n#\n#  systemd is free software; you can redistribute it and/or modify it\n#  under the terms of the GNU Lesser General Public License as published by\n#  the Free Software Foundation; either version 2.1 of the License, or\n#  (at your option) any later version.\n# This unit is required for pre-240 versions of systemd that automatically set\n# up /var/lib/machines.raw as loopback-mounted btrfs file system. Later\n# versions don't do that anymore, but let's keep minimal compatibility by\n# mounting the image still, if it exists."
Description = "Virtual Machine and Container Storage (Compatibility)"
ConditionPathExists = "/var/lib/machines.raw"

[units."var-lib-machines.mount".Mount]
What = "/var/lib/machines.raw"
Where = "/var/lib/machines"
Type = "btrfs"
Options = "loop"


[units."var-lib-nfs-rpc_pipefs.mount".Unit]
comment = "# AI-hint: Mounts the RPC pipe file system at /var/lib/nfs/rpc_pipefs to provide the necessary mount points for NFS RPC services and client operations.\n# AI-related: systemd-tmpfiles-setup.service, umount.target"
Description = "RPC Pipe File System"
DefaultDependencies = "no"
After = "systemd-tmpfiles-setup.service"
Conflicts = "umount.target"

[units."var-lib-nfs-rpc_pipefs.mount".Mount]
What = "sunrpc"
Where = "/var/lib/nfs/rpc_pipefs"
Type = "rpc_pipefs"


[units."mios-gpu-detect.service".Unit]
comment = "# AI-hint: Systemd unit that executes /usr/libexec/mios/gpu-detect to identify hardware GPU capabilities during boot, setting environment flags for display managers and graphics drivers.\n# AI-related: /usr/libexec/mios/gpu-detect, mios-gpu-detected, gdm.service, display-manager.service, systemd-modules-load.service, systemd-udevd.service, systemd-journald.socket, sysinit.target"
Description = "'MiOS' GPU Environment Detection"
ConditionVirtualization = "!container"
DefaultDependencies = "no"
Before = "gdm.service display-manager.service systemd-modules-load.service systemd-udevd.service"
After = "systemd-journald.socket"
ConditionPathExists = "!/run/mios-gpu-detected"

[units."mios-gpu-detect.service".Service]
Type = "oneshot"
ExecStart = "/usr/libexec/mios/gpu-detect"
RemainAfterExit = "yes"

[units."mios-gpu-detect.service".Install]
WantedBy = "sysinit.target"


[units."mios-gpu-amd.service".Unit]
comment = "# AI-hint: Configures AMD GPU hardware acceleration by loading the amdgpu module, generating CDI specifications for containerized ROCm/KFD access, and enforcing 0660 permissions on /dev/kfd for the render group.\n# AI-related: mios-gpu, systemd-modules-load.service, systemd-udev-trigger.service, multi-user.target"
Description = "'MiOS' AMD GPU container plumbing (ROCm/KFD + DRI)"
ConditionVirtualization = "!container"
Documentation = "https://github.com/MiOS-DEV/MiOS/blob/main/docs/gpu-passthrough.md"
ConditionPathExists = "/dev/kfd"
After = "systemd-modules-load.service systemd-udev-trigger.service"

[units."mios-gpu-amd.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStartPre = "/usr/sbin/modprobe -a amdgpu"
ExecStart = '''/usr/bin/bash -c ' \
  # If amd-ctk ever lands in Fedora RPM repos, drop a CDI spec automatically. \
  # Until then containers must pass --device /dev/kfd --device /dev/dri \
  # --group-add keep-groups explicitly. \
  if command -v amd-ctk >/dev/null 2>&1; then \
    amd-ctk cdi generate --output=/run/cdi/amd.json || true; \
  fi; \
  # Defensive perms -- Fedora Rawhide already labels /dev/kfd as root:render \
  # 0660 via kernel, but pin it in case of drift. \
  chgrp render /dev/kfd 2>/dev/null || true; \
  chmod 0660   /dev/kfd 2>/dev/null || true; \
  exit 0\''''
StandardOutput = "journal"
StandardError = "journal"

[units."mios-gpu-amd.service".Install]
WantedBy = "multi-user.target"


[units."mios-gpu-intel.service".Unit]
comment = "# AI-hint: Systemd unit that initializes iGPU drivers (i915/xe/amdgpu) and configures permissions for /dev/dri/renderD128 to enable hardware acceleration and unified render node access for Intel/AMD hardware.\n# AI-related: mios-gpu, systemd-modules-load.service, systemd-udev-trigger.service, multi-user.target"
Description = "'MiOS' Intel/AMD iGPU Container Plumbing (i915/xe/amdgpu)"
ConditionVirtualization = "!container"
Documentation = "https://github.com/MiOS-DEV/MiOS/blob/main/docs/gpu-passthrough.md"
ConditionPathExists = "/dev/dri/renderD128"
After = "systemd-modules-load.service systemd-udev-trigger.service"

[units."mios-gpu-intel.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStartPre = "-/usr/sbin/modprobe -a i915 xe amdgpu"
ExecStart = '''/usr/bin/bash -c ' \
  # Defensive perms on primary render node -- do not touch card* nodes (video group \
  # handled by 99-mios-gpu.rules at udev-add time). \
  if [ -e /dev/dri/renderD128 ]; then \
    chgrp render /dev/dri/renderD128 2>/dev/null || true; \
    chmod 0660   /dev/dri/renderD128 2>/dev/null || true; \
  fi; \
  exit 0\''''
StandardOutput = "journal"
StandardError = "journal"

[units."mios-gpu-intel.service".Install]
WantedBy = "multi-user.target"


[units."mios-gpu-nvidia.service".Unit]
comment = "# AI-hint: Systemd unit that ensures NVIDIA GPU modules are loaded and CDI configurations are generated for container passthrough, acting as a fallback or prerequisite for nvidia-cdi-refresh.service.\n# AI-related: mios-gpu, nvidia-cdi-refresh.service, systemd-modules-load.service, systemd-udev-trigger.service, akmods.service, multi-user.target"
Description = "'MiOS' NVIDIA GPU container plumbing"
ConditionVirtualization = ["!container", "!wsl"]
Documentation = "https://github.com/MiOS-DEV/MiOS/blob/main/docs/gpu-passthrough.md"
ConditionPathExists = "/dev/nvidia0"
After = "systemd-modules-load.service systemd-udev-trigger.service akmods.service"
Before = "nvidia-cdi-refresh.service"

[units."mios-gpu-nvidia.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStartPre = "/usr/sbin/modprobe -a nvidia nvidia_uvm nvidia_modeset nvidia_drm"
ExecStart = '''/usr/bin/bash -c ' \
  if systemctl list-unit-files nvidia-cdi-refresh.service 2>/dev/null \
       | grep -q nvidia-cdi-refresh; then \
    echo "nvidia-cdi-refresh.service present; deferring CDI generation to upstream"; \
    exit 0; \
  fi; \
  if command -v nvidia-ctk >/dev/null 2>&1; then \
    nvidia-ctk cdi generate --output=/run/cdi/nvidia.yaml || true; \
  fi; \
  exit 0\''''
StandardOutput = "journal"
StandardError = "journal"

[units."mios-gpu-nvidia.service".Install]
WantedBy = "multi-user.target"


[units."mios-gpu-nvidia.service.d/10-cycle-fix.conf".Unit]
comment = "# AI-hint: Overrides the mios-gpu-nvidia.service unit to ensure it runs during the early sysinit.target phase, bypassing standard dependency delays to ensure GPU drivers are initialized early in the boot sequence.\n# AI-related: mios-gpu-nvidia, mios-gpu-nvidia.service, sysinit.target"
DefaultDependencies = "no"
Requires = "sysinit.target"
After = "sysinit.target"


[units."mios-gpu-pv-detect.service".Unit]
comment = "# AI-hint: Systemd unit that executes /usr/libexec/mios/gpu-pv-detect to detect Hyper-V GPU Partitioning (GPU-PV) capabilities during boot, enabling hardware acceleration for guests on Microsoft Hyper-V hosts.\n# AI-related: /usr/libexec/mios/gpu-pv-detect, systemd-modules-load.service, display-manager.service, multi-user.target"
Description = "'MiOS' Hyper-V GPU-PV Guest Detection"
DefaultDependencies = "no"
After = "systemd-modules-load.service"
Before = "display-manager.service"
ConditionVirtualization = "microsoft"

[units."mios-gpu-pv-detect.service".Service]
Type = "oneshot"
ExecStart = "/usr/libexec/mios/gpu-pv-detect"
RemainAfterExit = "yes"

[units."mios-gpu-pv-detect.service".Install]
WantedBy = "multi-user.target"


[units."mios-gpu-status.service".Unit]
comment = "# AI-hint: Systemd unit that detects GPU hardware (NVIDIA, AMD, Intel) and virtualization status, exporting the results to /run/mios/gpu-passthrough.status and enabling the container_use_devices SELinux boolean.\n# AI-related: mios-gpu, systemd-udev-trigger.service, systemd-modules-load.service, podman.socket, docker.socket, local-fs.target, basic.target, sockets.target, multi-user.target"
ConditionVirtualization = "!container"
Description = "'MiOS' GPU passthrough detection and status"
Documentation = "https://github.com/MiOS-DEV/MiOS/blob/main/docs/gpu-passthrough.md"
DefaultDependencies = "yes"
After = "systemd-udev-trigger.service systemd-modules-load.service local-fs.target"

[units."mios-gpu-status.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
RuntimeDirectory = "mios"
RuntimeDirectoryMode = "0755"
ExecStart = '''/usr/bin/bash -c ' \
  set +e; \
  HAS_NV=0; HAS_AMD=0; HAS_INTEL=0; \
  [ -e /dev/nvidia0 ]            && HAS_NV=1; \
  [ -e /dev/kfd ]                && HAS_AMD=1; \
  [ -e /dev/dri/renderD128 ]     && HAS_INTEL=1; \
  VIRT=$(systemd-detect-virt || echo none); \
  # /run/mios, /run/cdi, /var/lib/mios/gpu declared in usr/lib/tmpfiles.d/mios-gpu.conf (LAW 2). \
  { \
    echo "timestamp=$(date -Is)"; \
    echo "virtualization=${VIRT}"; \
    echo "nvidia=${HAS_NV}"; \
    echo "amd=${HAS_AMD}"; \
    echo "intel=${HAS_INTEL}"; \
    lspci -nn 2>/dev/null | grep -iE "vga|3d|display" || true; \
  } > /run/mios/gpu-passthrough.status; \
  # Non-persistent (in-memory) SELinux boolean -- build-time semanage handles \
  # persistence; this guarantees the boolean is on even if the policy merge \
  # dropped it. No-op on permissive / disabled / missing boolean. \
  if command -v setsebool >/dev/null 2>&1; then \
    setsebool container_use_devices on 2>/dev/null || true; \
  fi; \
  exit 0\''''
StandardOutput = "journal"
StandardError = "journal"

[units."mios-gpu-status.service".Install]
WantedBy = "multi-user.target"


[units."mios-ai-firstboot.service".Unit]
comment = "# AI-hint: Systemd unit that executes the mios-ai-firstboot script to provision the AI agent virtual environment and download llama.cpp GGUF models if the .ai-firstboot-done sentinel is missing.\n# AI-related: 72-hermes-agent.sh, /usr/libexec/mios/mios-ai-firstboot, mios-ai-firstboot, mios-dev, mios-llm-light.service, network-online.target\n# Completes the build-time AI setup on deployments that didn't bake it (the\n# overlay-provisioned dev VM, WSL imports, etc.). 72-hermes-agent.sh's header\n# explicitly anticipates this: \"a firstboot retry can complete it later\".\n# Needs network (pip + model pull); GGUF blobs come from Hugging Face."
Description = "'MiOS' AI first-boot provisioning (agent venv + llama.cpp GGUFs)"
Documentation = "https://github.com/mios-dev/mios"
After = "network-online.target"
Wants = "network-online.target"
comment2 = "# The script writes the sentinel ONLY when both the venv and the GGUFs are\n# present, so a network-less first boot simply retries on the next one."
ConditionPathExists = "!/var/lib/mios/.ai-firstboot-done"
comment3 = "# Retry is owned by mios-ai-firstboot.timer (OnBootSec + OnUnitInactiveSec), NOT\n# an in-unit Restart=on-failure / StartLimitBurst storm. The script degrades open\n# (exit 0) on a partial provision, so the unit shows success and the timer simply\n# re-fires it on its cadence until the sentinel above is written."

[units."mios-ai-firstboot.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
comment = "# Cap a hung fetch (multi-GB model download) at 10min so it can't wedge the unit\n# forever; the timer re-fires the run afterwards. (Was 2400s.)"
TimeoutStartSec = "1800"
EnvironmentFile = "-/etc/mios/install.env"
ExecStart = "/usr/libexec/mios/mios-ai-firstboot"

[units."mios-ai-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."mios-hermes-firstboot.service".Unit]
Description = "'MiOS' Hermes-Agent first-boot config + key generation"
Documentation = "https://github.com/MiOS-DEV/MiOS"
After = "local-fs.target systemd-tmpfiles-setup.service"
Before = "mios-agent-pipe.service multi-user.target"

[units."mios-hermes-firstboot.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
EnvironmentFile = "-/etc/mios/install.env"
ExecStart = "/usr/libexec/mios/mios-hermes-firstboot"

[units."mios-hermes-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."mios-forge-firstboot.service".Unit]
comment = "# AI-hint: Executes the forge-firstboot.sh script to bootstrap the Forgejo instance, creating the initial admin user and repository credentials required for the MiOS self-replication and CI runner integration.\n# AI-related: /etc/mios/install.env, /usr/libexec/mios/forge-firstboot.sh, mios-forge.service, mios-forgejo-runner.service\n# /etc/mios/install.env is the optional layered-TOML resolver output\n# from mios-sync-env. The firstboot script tolerates its absence:\n# admin user, email, password all resolve from MIOS_DEFAULT_* or\n# fall back to \"mios\" / mios@hostname.local. Keep the unit gated only\n# by the firstboot sentinel."
Description = "'MiOS' Forge first-boot admin-bootstrap (Forgejo)"
Documentation = "https://github.com/mios-dev/MiOS/blob/main/etc/containers/systemd/mios-forge.container"
After = "mios-forge.service network-online.target"
Wants = "mios-forge.service network-online.target"
ConditionPathExists = "!/var/lib/mios/forge/.firstboot-done"
comment2 = "# Forgejo bootstrap MUST run on WSL too (the localhost:3000 git origin\n# is the heart of the MiOS self-replication loop on every shape). A\n# plain `!container` would skip WSL because systemd reports virtualization\n# as `wsl` -- a container variant. Use OR: \"not a container OR is WSL\"."
ConditionVirtualization = ["|!container", "|wsl"]
StartLimitIntervalSec = "1200"
StartLimitBurst = "6"

[units."mios-forge-firstboot.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
EnvironmentFile = "-/etc/mios/install.env"
ExecStart = "/usr/libexec/mios/forge-firstboot.sh"
TimeoutStartSec = "600s"
Restart = "on-failure"
RestartSec = "30"
NoNewPrivileges = "yes"
ProtectSystem = "strict"
ProtectHome = "yes"
ReadWritePaths = "/etc/mios/forge /var/lib/mios/forge /var/log/mios/forge /var/lib/containers /run"
PrivateTmp = "yes"
LockPersonality = "yes"
RestrictRealtime = "yes"

[units."mios-forge-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."mios-adguard-firstboot.service".Unit]
comment = "# AI-hint: Systemd unit that executes /usr/libexec/mios/mios-adguard-firstboot to generate the AdGuardHome.yaml config from mios.toml, injecting Tailscale IPs before the AdGuard container starts.\n# AI-related: /usr/libexec/mios/mios-adguard-firstboot, /etc/mios/adguard/AdGuardHome.yaml, tailscaled.service, mios-adguard.service, network-online.target\n# /usr/lib/systemd/system/mios-adguard-firstboot.service\n# Generates /etc/mios/adguard/AdGuardHome.yaml from mios.toml [adguard]/[ports]\n# + the live Tailscale IP/MagicDNS suffix, BEFORE the AdGuard container starts.\n# Idempotent + non-destructive (skips if the config already exists), so it is\n# safe to leave enabled across boots. mios-adguard.container Requires= + After=\n# this unit, so it is pulled in automatically; it is also enabled directly for\n# fresh installs."
Description = "'MiOS' AdGuard Home first-boot config generator"
After = "network-online.target tailscaled.service"
Wants = "network-online.target"
Before = "mios-adguard.service"
ConditionPathExists = "/usr/libexec/mios/mios-adguard-firstboot"

[units."mios-adguard-firstboot.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStart = "/usr/libexec/mios/mios-adguard-firstboot"

[units."mios-adguard-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."mios-models-firstboot.service".Unit]
Description = "First-boot Large-model Provisioner"
After = "network-online.target"
Wants = "network-online.target"
ConditionPathExists = "!/var/lib/mios/.models-firstboot-done"

[units."mios-models-firstboot.service".Service]
Type = "oneshot"
ExecStart = "/usr/libexec/mios/mios-models-firstboot"
RemainAfterExit = "yes"
TimeoutStartSec = "10800"
comment = "# Degrade-open: never block boot on a failed pull"
SuccessExitStatus = "0 1 2 3"

[units."mios-models-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."mios-shell-session-gc.service".Unit]
comment = "# AI-hint: SHELL-01 idle reaper for the persistent PTY substrate -- kills tmux sessions idle past [shell_session].idle_s so a long-lived shell plane cannot accumulate unbounded state.\n# AI-related: /usr/libexec/mios/mios-shell-session, mios-shell-session-gc.timer, /usr/share/mios/mios.toml [shell_session]"
Description = "Reap idle MiOS shell sessions"
ConditionPathExists = "/usr/libexec/mios/mios-shell-session"

[units."mios-shell-session-gc.service".Service]
Type = "oneshot"
User = "mios-ai"
Group = "mios-ai"
ExecStart = "/usr/libexec/mios/mios-shell-session gc"
# The reaper is best-effort: a tmux hiccup must never fault the timer.
SuccessExitStatus = "0 1"

[units."mios-shell-session-gc.service".Install]
WantedBy = "multi-user.target"

[units."mios-shell-session-gc.timer".Unit]
comment = "# AI-hint: Fires mios-shell-session-gc.service periodically so idle persistent shells are reclaimed without an operator.\n# AI-related: mios-shell-session-gc.service"
Description = "Periodic reap of idle MiOS shell sessions"

[units."mios-shell-session-gc.timer".Timer]
OnBootSec = "10min"
OnUnitInactiveSec = "10min"
Persistent = "true"

[units."mios-shell-session-gc.timer".Install]
WantedBy = "timers.target"

[units."mios-bound-images-firstboot.service".Unit]
Description = "First-boot Bound Images Provisioner"
After = "network-online.target"
Wants = "network-online.target"
ConditionPathExists = "!/var/lib/mios/.bound-images-firstboot-done"

[units."mios-bound-images-firstboot.service".Service]
Type = "oneshot"
ExecStart = "/usr/libexec/mios/mios-bound-images-firstboot"
RemainAfterExit = "yes"
TimeoutStartSec = "10800"
SuccessExitStatus = "0 1 2 3"

[units."mios-bound-images-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."mios-webtools-firstboot.service".Unit]
Description = "'MiOS' web-tools images build-on-demand firstboot service"
After = "network-online.target"
Wants = "network-online.target"
Before = "mios-webtools-pod.service"
comment = "# WS-DEPLOY: allow the build to retry (script exits non-zero on a missing image)\n# without tripping the start-limit too fast under sustained install contention."
StartLimitIntervalSec = "1200"
StartLimitBurst = "6"

[units."mios-webtools-firstboot.service".Service]
Type = "oneshot"
ExecStart = "/usr/libexec/mios/mios-webtools-firstboot.sh"
RemainAfterExit = "yes"
comment = "# Retry the build if an image is still missing (a transient network/CDN blip\n# during first install must not leave webtools permanently down)."
Restart = "on-failure"
RestartSec = "30"

[units."mios-webtools-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."mios-forgejo-runner-firstboot.service".Unit]
comment = "# AI-hint: One-shot systemd service that executes the initial registration of the Forgejo runner using the local token if the runner is not yet configured, ensuring the runner is registered before the main service starts.\n# AI-related: /etc/mios/forge/runner-token, /usr/libexec/mios/mios-forgejo-runner-firstboot.sh, mios-forge.service, mios-forge-firstboot.service, mios-forgejo-runner.service"
Description = "'MiOS' Forgejo Runner first-boot registration"
Documentation = "https://forgejo.org/docs/latest/admin/actions/"
After = "mios-forge.service mios-forge-firstboot.service"
Wants = "mios-forge.service"
Before = "mios-forgejo-runner.service"
ConditionPathExists = ["/etc/mios/forge/runner-token", "!/srv/mios/forge-runner/.runner"]
StartLimitIntervalSec = "1200"
StartLimitBurst = "6"

[units."mios-forgejo-runner-firstboot.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStart = "/usr/libexec/mios/mios-forgejo-runner-firstboot.sh"
TimeoutStartSec = "180s"
Restart = "on-failure"
RestartSec = "30"

[units."mios-forgejo-runner-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."hermes-worker-firstboot.service".Unit]
comment = "# AI-hint: Oneshot that seeds the non-thin Hermes WORKER config (/var/lib/mios/hermes-worker/config.yaml) from the vendor template before hermes-worker.service starts. Distinct from mios-hermes-firstboot (which owns and re-thins the primary :8642 config); this one NEVER touches the primary path.\n# AI-related: /usr/libexec/mios/hermes-worker-firstboot, /usr/share/mios/hermes/config-worker.yaml, /var/lib/mios/hermes-worker/config.yaml, hermes-worker.service"
Description = "'MiOS' Hermes-Worker first-boot config seed (:8643 non-thin worker)"
Documentation = "https://github.com/MiOS-DEV/MiOS"
After = "local-fs.target systemd-tmpfiles-setup.service"
Before = "hermes-worker.service multi-user.target"
ConditionPathExists = "/usr/libexec/mios/hermes-worker-firstboot"

[units."hermes-worker-firstboot.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStart = "/usr/libexec/mios/hermes-worker-firstboot"

[units."hermes-worker-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."mios-swarm-pack-firstboot.service".Unit]
Description = "'MiOS' swarm small-model pack arming (gpu_profile=swarm only)"
After = "network-online.target mios-cdi-detect.service mios-ai-firstboot.service"
Wants = "network-online.target"
comment2 = "# Don't even try before the agent stack's prerequisites; the script is idempotent."

[units."mios-swarm-pack-firstboot.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStart = "/usr/libexec/mios/mios-swarm-pack-firstboot"
comment = "# Never wedge boot on a pack problem."
TimeoutStartSec = "120"
SuccessExitStatus = "0"

[units."mios-swarm-pack-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."mios-wsl-firstboot.service".Unit]
comment = "# AI-hint: Systemd unit that executes /usr/libexec/mios/wsl-firstboot to perform one-time initialization tasks specifically for WSL2 instances if the first-boot flag is not yet set.\n# AI-related: /usr/libexec/mios/wsl-firstboot, systemd-user-sessions.service, local-fs.target, multi-user.target"
Description = "'MiOS' WSL2 First Boot Initialization"
Documentation = "https://github.com/MiOS-DEV/MiOS"
ConditionVirtualization = "wsl"
ConditionPathExists = "!/var/lib/mios/.wsl-firstboot-done"
After = "local-fs.target"
Before = "systemd-user-sessions.service multi-user.target"

[units."mios-wsl-firstboot.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStart = "/usr/libexec/mios/wsl-firstboot"

[units."mios-wsl-firstboot.service".Install]
WantedBy = "multi-user.target"


[units."hermes-worker.service".Unit]
Description = "'MiOS' Hermes-Worker (non-thin agent, native tool loop, :8643)"
Documentation = "https://github.com/NousResearch/hermes-agent"
comment2 = "# R8: dropped After=mios-ai-firstboot.service -- the worker rides the venv + its own\n# provisioner (hermes-worker-firstboot) + the inference lanes, NOT the boot-time\n# GGUF/vLLM fetch owned by mios-ai-firstboot."
After = "network-online.target mios-llm-heavy.service mios-llm-light.service hermes-worker-firstboot.service"
Wants = "network-online.target mios-llm-heavy.service mios-llm-light.service hermes-worker-firstboot.service"
comment3 = "# Skip cleanly if the direct install didn't land."
ConditionPathExists = "/usr/lib/mios/agents/.venv/bin/hermes"

[units."hermes-worker.service".Service]
Type = "simple"
User = "mios-ai"
Group = "mios-ai"
comment = "# SEPARATE HERMES_HOME + HOME => fully isolated pid/lock/state/DBs/config AND a\n# distinct $HOME/XDG so the discord scope-lock dir (derived from $HOME) can\n# never collide with the :8642 gateway's. Discord is off here regardless."
Environment = [
  "HOME=/var/lib/mios/hermes-worker",
  "HERMES_HOME=/var/lib/mios/hermes-worker",
  "SEARXNG_URL=http://localhost:${MIOS_PORT_SEARXNG}",
  "MIOS_CRAWL_SERVICE_URL=http://127.0.0.1:${MIOS_PORT_CRAWL4AI}",
  "FIRECRAWL_API_URL=http://127.0.0.1:${MIOS_PORT_FIRECRAWL}",
  "PORT=${MIOS_PORT_HERMES}",
  "API_SERVER_PORT=${MIOS_PORT_HERMES}",
  "HERMES_BACKEND_BASE_URL=http://localhost:${MIOS_PORT_VLLM}",
  "HERMES_MAX_TOKENS=8192",
  "BROWSER_CDP_URL=http://localhost:${MIOS_PORT_CHROME_CDP_WORKER:-9223}",
]
EnvironmentFile = ["-/etc/mios/install.env", "-/etc/mios/hermes/api.env"]
ExecStartPre = [
  "+/usr/bin/install -d -o mios-ai -g mios-ai -m 0700 /var/lib/mios/hermes-worker",
  "-/usr/bin/python3 /usr/libexec/mios/mios-hermes-discord-reactions-patch /usr/lib/mios/agents/.venv/lib/python3.14/site-packages/gateway/platforms/discord.py",
  "-+/usr/libexec/mios/mios-hermes-dashboard-auth-stub",
]
ExecStart = "/usr/lib/mios/agents/.venv/bin/hermes gateway run"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "300s"
TimeoutStopSec = "240s"
LimitNOFILE = "1048576"
ProtectSystem = "strict"
ReadWritePaths = "/etc/mios /var/lib/mios /run/mios-launcher"
ProtectHome = "false"
NoNewPrivileges = "false"
PrivateTmp = "yes"

[units."hermes-worker.service".Install]
WantedBy = "multi-user.target"


[units."mios-agent-pipe.service".Unit]
comment = "# AI-hint: Systemd unit defining the agent-pipe FastAPI service which acts as a router, refiner, and critic for the Hermes gateway, routing inference requests to the llama.cpp light lane (mios-llm-light); the lane port is the single SSOT key [ports].llm_light (MIOS_PORT_LLM_LIGHT), composed in server.py via _LIGHT_BASE.\n# AI-related: /usr/lib/mios/agent-pipe/server.py, /etc/mios/install.env, /etc/mios/agent-pipe.env, mios-pgvector, mios-passport-provision, mios-ai, mios-hermes"
Description = "'MiOS' Agent Pipe (router + refine + critic FastAPI; fronts hermes for every gateway)"
Documentation = "file:///usr/lib/mios/agent-pipe/server.py"
comment2 = "# R8: NOT ordered After=mios-ai-firstboot.service. The plane needs the VENV (baked\n# into the image) + pgvector, NOT the boot-time model fetch; ordering behind\n# firstboot's multi-GB download blocked the router for the whole download. firstboot\n# `systemctl restart --no-block`s this unit once models land, so the edge is redundant."
After = "network-online.target mios-pgvector.service mios-passport-provision.service"
Wants = "network-online.target mios-passport-provision.service"
comment3 = "# R6: the pipe touches pgvector on every route, so upgrade the DB edge from After=\n# (ordering only) to a hard Requires= -- a missing DB should hold/fail the unit."
Requires = "mios-pgvector.service"
comment4 = "# R6: bound the existing Restart=on-failure so a DB-down window (many quick\n# restarts) can't trip the default 5/10s start limit and land permanently failed."
StartLimitIntervalSec = "300"
StartLimitBurst = "5"
comment5 = "# The venv python is baked into the FINAL image, but on the podman-MiOS-DEV substrate it is\n# built at first boot by mios-ai-firstboot. Skip (rather than 203/EXEC-fail) until it exists."
ConditionPathExists = "/usr/lib/mios/agents/.venv/bin/python3"

[units."mios-agent-pipe.service".Service]
Type = "simple"
User = "mios-ai"
Group = "mios-ai"
SupplementaryGroups = "mios-hermes"
WorkingDirectory = "/var/lib/mios/agent-pipe"
EnvironmentFile = ["-/etc/mios/agent-pipe.env", "-/etc/mios/install.env"]
Environment = [
  "MIOS_PORT_AGENT_PIPE=${MIOS_PORT_AGENT_PIPE:-8700}",
  "MIOS_AGENT_PIPE_BACKEND_LIGHT=1",
  "MIOS_AI_MODEL=granite4.1:8b",
  "MIOS_NO_TOOL_CHOICE_HINTS=11436",
  "MIOS_KV_PAGING_HINTS=11436,11450",
  "MIOS_LLM_API_HINTS=",
  "MIOS_REFINE_TIMEOUT_S=45",
  "MIOS_POLISH_TIMEOUT_S=45",
  "MIOS_REFINE_MAX_TOKENS=1200",
  "MIOS_AGENT_MEMORY_RECALL=1",
  "MIOS_PASSPORT_AGENT_NAME=agent-pipe",
]
ExecStart = "/usr/lib/mios/agents/.venv/bin/python3 -u /usr/lib/mios/agent-pipe/server.py"
Restart = "on-failure"
RestartSec = "5s"
TimeoutStartSec = "60s"
NoNewPrivileges = "yes"
PrivateTmp = "yes"
ProtectSystem = "strict"
ProtectHome = "yes"
ReadWritePaths = "/var/lib/mios/agent-pipe /var/lib/mios/skills /var/log /var/lib/mios/agent-env /var/lib/mios/ai"
ReadOnlyPaths = "/var/lib/mios/agent-passports"

[units."mios-agent-pipe.service".Install]
WantedBy = "multi-user.target"


[units."mios-agents.service".Unit]
comment = "# AI-hint: Runs the mios-agents A2O super-container (code-server IDE + tmux war room + Claude CLI + agy/Gemini + the mios-a2o muxer) as a systemd-managed container. ExecStartPre builds the local image if absent; ExecStart runs it every boot on the code-server port MIOS_PORT_CODE_SERVER (mios-agents REPLACES the retired mios-code-server -- one IDE, no duplicate service).\n# AI-related: /usr/share/mios/agents/Containerfile, /usr/share/mios/agents/mios-a2o, /usr/libexec/mios/mios-agents-firstboot.sh, /etc/mios/install.env, /var/lib/mios/agents\n# /usr/lib/systemd/system/mios-agents.service"
Description = "'MiOS' A2O agents super-container (Claude + agy/Gemini + tmux war room + code-server)"
Documentation = "https://github.com/mios-dev/MiOS/blob/main/usr/share/mios/agents/ACTIVATION.md"
After = "network-online.target podman.socket"
Wants = "network-online.target"
ConditionPathExists = "/usr/share/mios/agents/Containerfile"

[units."mios-agents.service".Service]
Type = "simple"
comment = "# systemd does NOT expand bash ${VAR:-default} in ExecStart -- it resolves to\n# empty (code-server then dies \"Invalid URL\"). Set the defaults here; install.env\n# (the mios.toml -> env SSOT bridge) overrides them when present. ExecStart uses\n# plain ${VAR}."
Environment = ["MIOS_PORT_CODE_SERVER=${MIOS_PORT_CODE_SERVER:-8900}", "MIOS_DEFAULT_PASSWORD=mios"]
EnvironmentFile = "-/etc/mios/install.env"
TimeoutStartSec = "1200s"
comment2 = "# Build the local super-container image on first deploy (idempotent no-op after)."
ExecStartPre = "/usr/libexec/mios/mios-agents-firstboot.sh"
ExecStart = """/usr/sbin/podman run --replace --name mios-agents \
  --network=host \
  --env PASSWORD=${MIOS_DEFAULT_PASSWORD} \
  --env MIOS_A2O_ENGINE=agy \
  --env MIOS_A2O_WORK=/mnt/mios-root \
  --env MIOS_A2O_ORCH_ENGINE=${MIOS_A2O_ORCH_ENGINE} \
  --env MIOS_A2O_ORCH_MODEL=${MIOS_A2O_ORCH_MODEL} \
  --env MIOS_A2O_ORCH_EFFORT=${MIOS_A2O_ORCH_EFFORT} \
  --env MIOS_A2O_LANE_A_ENGINE=${MIOS_A2O_LANE_A_ENGINE} \
  --env MIOS_A2O_LANE_A_MODEL=${MIOS_A2O_LANE_A_MODEL} \
  --env MIOS_A2O_LANE_A_EFFORT=${MIOS_A2O_LANE_A_EFFORT} \
  --env MIOS_A2O_LANE_B_ENGINE=${MIOS_A2O_LANE_B_ENGINE} \
  --env MIOS_A2O_LANE_B_MODEL=${MIOS_A2O_LANE_B_MODEL} \
  --env MIOS_A2O_LANE_B_EFFORT=${MIOS_A2O_LANE_B_EFFORT} \
  --env MIOS_A2O_CLAUDE_EFFORT_FLAG=${MIOS_A2O_CLAUDE_EFFORT_FLAG} \
  --env MIOS_A2O_AGY_EFFORT_FLAG=${MIOS_A2O_AGY_EFFORT_FLAG} \
  --env MIOS_A2O_GEMINI_EFFORT_FLAG=${MIOS_A2O_GEMINI_EFFORT_FLAG} \
  --volume /:/mnt/mios-root:rw,rslave \
  --volume /var/lib/mios/agents:/home/coder:rw \
  localhost/mios-agents:latest \
  --bind-addr 0.0.0.0:8800 /mnt/mios-root"""
ExecStop = "/usr/sbin/podman stop -t 10 mios-agents"
Restart = "on-failure"
RestartSec = "10s"

[units."mios-agents.service".Install]
WantedBy = "multi-user.target default.target"


[units."mios-daemon.service".Unit]
Description = "'MiOS' consolidated micro-LLM daemon (log classify + refusal detect + cron gate)"
Documentation = "file:///usr/libexec/mios/mios-daemon"
After = "mios-llm-light.service network.target"
Wants = "mios-llm-light.service"
ConditionPathExists = "/usr/libexec/mios/mios-daemon"

[units."mios-daemon.service".Service]
Type = "simple"
ExecStart = "/usr/libexec/mios/mios-daemon"
Restart = "on-failure"
RestartSec = "5s"
User = "mios-ai"
Group = "mios-ai"
Environment = "PYTHONUNBUFFERED=1"
comment2 = "# Hardening (kept loose enough to allow journalctl subscription +\n# subprocess.Popen for cron actions)."
NoNewPrivileges = "true"
ProtectSystem = "strict"
ReadWritePaths = "/var/lib/mios/daemon /var/lib/mios/scratch"
ProtectHome = "true"
PrivateTmp = "true"
ProtectKernelTunables = "true"
ProtectControlGroups = "true"
RestrictAddressFamilies = "AF_INET AF_INET6 AF_UNIX"
LockPersonality = "true"
RestrictRealtime = "true"

[units."mios-daemon.service".Install]
WantedBy = "multi-user.target"


[units."mios-mcp.service".Unit]
comment = "# AI-hint: Systemd unit file defining the mios-mcp.service daemon, which provides the Model Context Protocol (MCP) server for autonomous agents to access system context via a hardened, high-availability userspace listener.\n# AI-related: /usr/libexec/mios/mcp-init.sh, /usr/libexec/mios/mcp-server-runner, mios-ai, redis.service"
Description = "'MiOS' Agent Context Service (MCP)"
comment2 = "# High-availability context provider for autonomous agents. Pure\n# userspace daemon (no kernel/audit/hardware coupling) so it runs on\n# every MiOS shape -- bare-metal, Hyper-V, QEMU, WSL, Podman-WSL.\n# The previous `ConditionVirtualization=!container` would silently skip\n# WSL, since systemd reports WSL as a `container` virt variant."
After = "network.target redis.service"
Wants = "redis.service"

[units."mios-mcp.service".Service]
Type = "simple"
User = "mios-ai"
Group = "mios-ai"
comment = "# Execution wrapper that verifies SQLite vaults exist before starting the MCP listener"
ExecStartPre = "/usr/libexec/mios/mcp-init.sh"
ExecStart = "/usr/libexec/mios/mcp-server-runner"
Restart = "always"
RestartSec = "5"
comment2 = "# Security hardening: isolate the MCP server from writing to the core OS"
ProtectSystem = "strict"
ProtectHome = "read-only"
ReadWritePaths = "/var/lib/mios/mcp /var/log/mios/mcp"
comment3 = "# Hardening parity with sibling mios-* daemons (log-watcher, cron-\n# director, hermes-browser) -- consolidation pass 2026-05-15."
PrivateTmp = "true"
NoNewPrivileges = "true"
comment4 = "# Drain timeout: MCP serves a long-lived TCP socket but in-flight\n# context requests are sub-second; 30 s is generous."
TimeoutStopSec = "30s"

[units."mios-mcp.service".Install]
WantedBy = "multi-user.target"


[units."mios-opencode-gateway.service".Unit]
comment = "# AI-hint: Systemd unit that hosts the opencode-gateway on port 8633, providing a standard OpenAI-compatible /v1 API shim for the opencode CLI to enable multi-agent fan-out and local inference via mios-llm-light.\n# AI-related: /usr/lib/mios/agents/opencode-gateway/server.py, /usr/lib/mios/agents/opencode/bin/opencode, /usr/lib/mios/agents/.venv, mios-llm-light.service\n# R8: dropped After=mios-ai-firstboot.service -- the coder gateway rides the venv +\n# the light inference lane (mios-llm-light), NOT the boot-time model fetch."
Description = "'MiOS' OpenCode /v1 gateway (OpenAI adapter fronting the opencode CLI)"
Documentation = "file:///usr/lib/mios/agents/opencode-gateway/server.py"
After = "network-online.target mios-llm-light.service"
Wants = "network-online.target mios-llm-light.service"
comment2 = "# Skip cleanly if the opencode binary never landed (build-time fetch failure)"
ConditionPathExists = "/usr/lib/mios/agents/opencode/bin/opencode"

[units."mios-opencode-gateway.service".Service]
Type = "simple"
User = "mios-ai"
Group = "mios-ai"
WorkingDirectory = "/var/lib/mios/opencode-gateway/work"
Environment = [
  "HOME=/var/lib/mios/opencode-gateway/work",
  "MIOS_PORT_OPENCODE_GATEWAY=${MIOS_PORT_OPENCODE_GATEWAY:-8780}",
  "MIOS_OPENCODE_HOST=127.0.0.1",
  "MIOS_OPENCODE_BIN=/usr/lib/mios/agents/opencode/bin/opencode",
  "MIOS_OPENCODE_MODEL=mios-opencode:latest",
  "MIOS_OPENCODE_PROVIDER=local",
  "MIOS_OPENCODE_CONFIG=/etc/mios/opencode/opencode.json",
  "MIOS_OPENCODE_TIMEOUT_S=90",
]
ExecStart = "/usr/lib/mios/agents/.venv/bin/python3 -u /usr/lib/mios/agents/opencode-gateway/server.py"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "30s"
NoNewPrivileges = "true"
PrivateTmp = "true"
ProtectSystem = "strict"
ProtectHome = "true"
ReadWritePaths = "/etc/mios /var/lib/mios"
ProtectKernelTunables = "true"
ProtectKernelModules = "true"
ProtectControlGroups = "true"
RestrictSUIDSGID = "true"
LockPersonality = "true"

[units."mios-opencode-gateway.service".Install]
WantedBy = "multi-user.target"


[units."mios-hermes-browser.service".Unit]
Description = "'MiOS' Hermes-Browser (ChromeDev w/ CDP for Hermes-Agent)"
Documentation = "https://chromedevtools.github.io/devtools-protocol/"
After = "mios-agent-pipe.service"
Wants = "mios-agent-pipe.service"
comment2 = "# ChromeDev flatpak must be installed (system or user scope). If\n# missing, the unit no-ops cleanly instead of crash-looping."
ConditionPathExists = ["|/var/lib/flatpak/app/com.google.ChromeDev", "|%h/.local/share/flatpak/app/com.google.ChromeDev"]

[units."mios-hermes-browser.service".Service]
Type = "simple"
User = "mios-ai"
Group = "mios-ai"
comment = "# Flatpak needs HOME to bootstrap its per-user state dir."
Environment = [
  "HOME=/var/lib/mios/hermes",
  "XDG_RUNTIME_DIR=/run/mios-hermes-browser",
  "HERMES_BROWSER_HEADLESS=1",
  "NO_AT_BRIDGE=1",
]
RuntimeDirectory = "mios-hermes-browser"
RuntimeDirectoryMode = "0700"
StateDirectory = "mios/hermes-browser/profile"
StateDirectoryMode = "0700"
ExecStart = "/usr/libexec/mios/mios-hermes-browser start"
Restart = "on-failure"
RestartSec = "5s"
TimeoutStopSec = "30s"
NoNewPrivileges = "true"
PrivateTmp = "false"

[units."mios-hermes-browser.service".Install]
WantedBy = "multi-user.target"


[units."mios-hermes-browser-worker.service".Unit]
comment = "# AI-hint: Systemd unit for a SECOND headless ChromeDev flatpak providing a dedicated CDP endpoint at 127.0.0.1:9223 (own profile dir profile-w2) for the Hermes WORKER (:8643), so the worker's browser_* tool loop never stomps the primary :9222 browser's first-page target / cookies.\n# AI-related: /usr/libexec/mios/mios-hermes-browser, hermes-worker.service, mios-hermes-browser.service, mios-ai, com.google.ChromeDev\n# /usr/lib/systemd/system/mios-hermes-browser-worker.service\n#\n# A SECOND headless ChromeDev (com.google.ChromeDev flatpak) with CDP on\n# 127.0.0.1:9223 -- the dedicated browser for the Hermes WORKER (:8643).\n# Distinct from the primary :9222 browser (mios-hermes-browser.service): the CDP\n# supervisor attaches to the FIRST page target."
Description = "'MiOS' Hermes-Browser-Worker (ChromeDev CDP :9223 for the worker)"
Documentation = "https://chromedevtools.github.io/devtools-protocol/"
After = "hermes-worker.service"
Wants = "hermes-worker.service"
ConditionPathExists = ["|/var/lib/flatpak/app/com.google.ChromeDev", "|%h/.local/share/flatpak/app/com.google.ChromeDev"]

[units."mios-hermes-browser-worker.service".Service]
Type = "simple"
User = "mios-ai"
Group = "mios-ai"
Environment = [
  "HOME=/var/lib/mios/hermes-worker",
  "XDG_RUNTIME_DIR=/run/mios-hermes-browser-worker",
  "HERMES_BROWSER_HEADLESS=1",
  "HERMES_BROWSER_CDP_PORT=${MIOS_PORT_CHROME_CDP_WORKER:-9223}",
  "HERMES_BROWSER_PROFILE_DIR=/var/lib/mios/hermes-browser/profile-w2",
  "HERMES_BROWSER_LOG=/var/lib/mios/hermes-browser/launch-w2.log",
  "NO_AT_BRIDGE=1",
]
RuntimeDirectory = "mios-hermes-browser-worker"
RuntimeDirectoryMode = "0700"
StateDirectory = "mios/hermes-browser/profile-w2"
StateDirectoryMode = "0700"
ExecStart = "/usr/libexec/mios/mios-hermes-browser start"
Restart = "on-failure"
RestartSec = "5s"
TimeoutStopSec = "30s"
NoNewPrivileges = "true"
PrivateTmp = "false"

[units."mios-hermes-browser-worker.service".Install]
WantedBy = "multi-user.target"


[units."mios-policy-arbiter.service".Unit]
Description = "'MiOS' out-of-process HITL policy arbiter (WS-9)"
After = "network-online.target mios-agent-pipe.service"
Wants = "network-online.target"
ConditionPathExists = "/usr/libexec/mios/mios-policy-arbiter"

[units."mios-policy-arbiter.service".Service]
Type = "simple"
User = "mios-ai"
Group = "mios-ai"
Environment = "MIOS_AGENT_PIPE_DIR=/usr/lib/mios/agent-pipe"
EnvironmentFile = "-/etc/mios/install.env"
ExecStart = "/usr/bin/python3 /usr/libexec/mios/mios-policy-arbiter"
Restart = "on-failure"
RestartSec = "15s"
comment = "# Hardening: a stateless loopback decision service needs nothing but Python."
NoNewPrivileges = "true"
ProtectSystem = "strict"
ProtectHome = "true"
PrivateTmp = "true"

[units."mios-policy-arbiter.service".Install]
WantedBy = "multi-user.target"


[units."mios-finetune-serve.service".Unit]
comment = "# AI-hint: Systemd unit to host the fine-tuned refiner model as an OpenAI /v1-compatible endpoint on port 11438, allowing the agent-pipe to swap between the high-quality transformer-served adapter and the faster llama.cpp path.\n# AI-related: /usr/libexec/mios/mios-finetune-serve, network-online.target\n# 'MiOS' fine-tune serve -- serves the trained role adapter (base + LoRA) as an\n# OpenAI /v1 endpoint so the fine-tuned refiner can be adopted/A-B'd\n# in the agent-pipe. OPT-IN: NOT enabled by default (the transformers-served 2B is\n# correct but slower than the GGUF lane on the hot path; enable to evaluate)."
Description = "'MiOS' fine-tune serve (base+adapter refiner backend)"
After = "network-online.target"
Wants = "network-online.target"

[units."mios-finetune-serve.service".Service]
Type = "simple"
User = "mios"
Group = "mios"
Environment = ["HF_HOME=/var/home/mios/.cache/huggingface", "MIOS_FINETUNE_SERVE_PORT=${MIOS_PORT_FINETUNE_SERVE:-11438}"]
ExecStart = "/var/lib/mios/finetune/venv/bin/python /usr/libexec/mios/mios-finetune-serve"
Restart = "on-failure"
RestartSec = "5"
comment = "# model load + first request can take ~40s on a cold GPU"
TimeoutStartSec = "180"

[units."mios-finetune-serve.service".Install]
WantedBy = "multi-user.target"


[units."mios-pgvector-backup.service".Unit]
Description = "'MiOS' daily pg_dump backup of the unified agent-plane datastore (pgvector)"
Documentation = "file:///usr/lib/systemd/system/mios-pgvector-backup.service"
After = "mios-pgvector.service"
Wants = "mios-pgvector.service"
comment2 = "# Same virtualization guard as the pgvector container: the DB only runs on\n# bare-metal/WSL (not a nested container), so there's nothing to back up there."
ConditionVirtualization = ["|!container", "|wsl"]

[units."mios-pgvector-backup.service".Service]
Type = "oneshot"
comment = "# SSOT env: MIOS_PG_USER / MIOS_PG_DB / MIOS_PORT_PGVECTOR / MIOS_PG_BACKUP_*\n# all flow from mios.toml [pgvector] -> userenv.sh. '-' = tolerate absence\n# (degrade-open to the inline defaults below)."
EnvironmentFile = "-/etc/mios/userenv.sh"
comment2 = "# Inline-default the knobs so the unit is correct even with no env file present."
Environment = [
  "MIOS_PG_BACKUP_ENABLE=true",
  "MIOS_PG_BACKUP_DIR=/var/lib/mios/backups",
  "MIOS_PG_BACKUP_KEEP=7",
  "MIOS_PG_USER=mios",
  "MIOS_PG_DB=mios",
  "MIOS_PORT_PGVECTOR=${MIOS_PORT_PGVECTOR:-8600}",
]
comment3 = "# Runs as root to `podman exec` into the mios-ai.pod pgvector container.\n# ProtectSystem=strict / ReadWritePaths dropped so podman can reach its runtime."
ProtectHome = "true"
PrivateTmp = "true"
ExecStart = """/bin/sh -c '\
  case "$$MIOS_PG_BACKUP_ENABLE" in \
    0|false|False|FALSE|no|off) echo "mios-pgvector-backup: disabled (MIOS_PG_BACKUP_ENABLE=$$MIOS_PG_BACKUP_ENABLE)"; exit 0 ;; \
  esac; \
  DIR="$$MIOS_PG_BACKUP_DIR"; [ -z "$$DIR" ] && DIR="/var/lib/mios/backups"; \
  KEEP="$$MIOS_PG_BACKUP_KEEP"; [ -z "$$KEEP" ] && KEEP="7"; \
  case "$$KEEP" in *[!0-9]*|"") KEEP=7 ;; esac; \
  [ "$$KEEP" -lt 1 ] && KEEP=1; \
  PORT="$$MIOS_PORT_PGVECTOR"; [ -z "$$PORT" ] && PORT="8432"; \
  USR="$$MIOS_PG_USER"; [ -z "$$USR" ] && USR="mios"; \
  DB="$$MIOS_PG_DB"; [ -z "$$DB" ] && DB="mios"; \
  if ! command -v podman >/dev/null 2>&1; then \
    echo "mios-pgvector-backup: podman not found on PATH -- skipping (need podman to exec pg_dump inside the pod)"; \
    exit 0; \
  fi; \
  mkdir -p "$$DIR" 2>/dev/null || true; \
  TS=$$(date -u +%%Y%%m%%dT%%H%%M%%SZ); \
  OUT="$$DIR/mios-pgvector-$$TS.sql.gz"; \
  RAW="$$DIR/.mios-pgvector-$$TS.sql.partial"; \
  ERR="$$DIR/.mios-pgbackup.err"; \
  rc=0; \
  podman exec mios-pgvector pg_dump -h 127.0.0.1 -p "$$PORT" -U "$$USR" --no-password "$$DB" >"$$RAW" 2>"$$ERR" || rc=$$?; \
  if [ "$$rc" -ne 0 ] || [ ! -s "$$RAW" ]; then \
    echo "mios-pgvector-backup: pg_dump failed (rc=$$rc, degrade-open): $$(tail -n1 "$$ERR" 2>/dev/null)"; \
    rm -f "$$RAW" "$$ERR" 2>/dev/null || true; \
    exit 0; \
  fi; \
  if gzip -c "$$RAW" >"$$OUT.partial" && mv -f "$$OUT.partial" "$$OUT"; then \
    chmod 0640 "$$OUT" 2>/dev/null || true; \
    echo "mios-pgvector-backup: wrote $$OUT"; \
  else \
    echo "mios-pgvector-backup: gzip failed (degrade-open)"; \
    rm -f "$$OUT.partial" "$$RAW" "$$ERR" 2>/dev/null || true; \
    exit 0; \
  fi; \
  rm -f "$$RAW" "$$ERR" 2>/dev/null || true; \
  N=$$(ls -1 "$$DIR"/mios-pgvector-*.sql.gz 2>/dev/null | wc -l); \
  if [ "$$N" -gt "$$KEEP" ]; then \
    ls -1t "$$DIR"/mios-pgvector-*.sql.gz 2>/dev/null | tail -n +$$((KEEP+1)) | while IFS= read -r f; do rm -f "$$f" && echo "mios-pgvector-backup: pruned $$f"; done; \
  fi; \
  exit 0'"""

[units."mios-pgvector-backup.service".Install]
WantedBy = "multi-user.target"

[units."mios-embed-backfill.service".Unit]
comment = "# AI-hint: Systemd unit that runs the mios_pipe.memory.embed_backfill worker to periodically re-embed database rows with stale or missing vector versions.\n# AI-related: /usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py, mios-embed-backfill.timer, mios-pgvector.service, mios-llm-light.service\n# /usr/lib/systemd/system/mios-embed-backfill.service"
Description = "'MiOS' Embedding Backfill Worker"
Documentation = "file:///usr/lib/mios/agent-pipe/mios_pipe/memory/embed_backfill.py"
comment2 = "# R8: dropped After=mios-ai-firstboot.service -- the backfill worker needs pgvector\n# + the embedding lane (mios-llm-light), NOT the boot-time model fetch."
After = "mios-pgvector.service mios-llm-light.service mios-agent-pipe.service"
Requires = "mios-pgvector.service"

[units."mios-embed-backfill.service".Service]
Type = "oneshot"
EnvironmentFile = ["-/etc/mios/userenv.sh", "-/etc/mios/install.env"]
Environment = "PYTHONPATH=/usr/lib/mios/agent-pipe"
ExecStart = "/usr/lib/mios/agents/.venv/bin/python3 -u -m mios_pipe.memory.embed_backfill"
comment = "# Low-privilege execution"
User = "mios-ai"
Group = "mios-ai"
NoNewPrivileges = "true"
ProtectSystem = "strict"
ProtectHome = "true"
PrivateTmp = "true"

[units."mios-embed-backfill.service".Install]
WantedBy = "multi-user.target"

[units."mios-account-sync.service".Unit]
comment = "# AI-hint: Systemd unit that executes /usr/libexec/mios/mios-account-sync in daemon mode to keep local Linux accounts synchronized with PostgreSQL accounts and aliases.\n# AI-related: /usr/libexec/mios/mios-account-sync, mios-pgvector.service"
Description = "'MiOS' live PostgreSQL-to-OS user account sync daemon"
Documentation = "file:///usr/libexec/mios/mios-account-sync"
After = "mios-pgvector.service"
Requires = "mios-pgvector.service"
StartLimitIntervalSec = "300"
StartLimitBurst = "5"

[units."mios-account-sync.service".Service]
Type = "simple"
EnvironmentFile = "-/etc/mios/install.env"
comment = "# Ensure postgres container is ready before launching the daemon"
ExecStartPre = "/usr/bin/podman exec mios-pgvector pg_isready -q -h 127.0.0.1 -p ${MIOS_PORT_PGVECTOR:-8600} -U mios -d mios"
ExecStart = "/usr/libexec/mios/mios-account-sync --daemon"
Restart = "always"
RestartSec = "10s"

[units."mios-account-sync.service".Install]
WantedBy = "multi-user.target"


[units."mios-userdb-render.service".Unit]
comment = "# AI-hint: Systemd unit that executes /usr/libexec/mios/mios-userdb-render to project PostgreSQL account records into systemd userdb JSON drop-ins.\n# AI-related: /usr/libexec/mios/mios-userdb-render, mios-pgvector.service"
Description = "'MiOS' PostgreSQL account systemd userdb drop-in renderer"
Documentation = "file:///usr/libexec/mios/mios-userdb-render"
After = "mios-pgvector.service"
Requires = "mios-pgvector.service"

[units."mios-userdb-render.service".Service]
Type = "oneshot"
EnvironmentFile = "-/etc/mios/install.env"
ExecStartPre = "-/usr/bin/podman exec mios-pgvector pg_isready -q -h 127.0.0.1 -p ${MIOS_PORT_PGVECTOR:-8600} -U mios -d mios"
ExecStart = "/usr/libexec/mios/mios-userdb-render"
RemainAfterExit = "yes"

[units."mios-userdb-render.service".Install]
WantedBy = "multi-user.target"


[units."mios-ceph-bootstrap.service".Unit]



comment = "# AI-hint: Systemd unit to execute /usr/libexec/mios/ceph-bootstrap.sh script to initialize the Ceph cluster (first boot only).\n# AI-related: /usr/libexec/mios/ceph-bootstrap.sh, podman.socket, network-online.target, multi-user.target"
Description = "'MiOS' Ceph Cluster Bootstrap"
Documentation = "https://docs.ceph.com/en/latest/cephadm/"
ConditionVirtualization = "no"
ConditionPathExists = ["!/etc/ceph/ceph.conf", "!/var/lib/ceph/.bootstrapped"]
After = "network-online.target podman.socket"
Wants = "network-online.target podman.socket"
StartLimitIntervalSec = "0"

[units."mios-ceph-bootstrap.service".Service]
Type = "oneshot"
RemainAfterExit = "yes"
ExecStart = "/usr/libexec/mios/ceph-bootstrap.sh"
ExecStartPost = ["/usr/bin/touch /var/lib/ceph/.bootstrapped", "-/bin/chmod 0600 /etc/ceph/ceph.client.admin.keyring"]
TimeoutStartSec = "600"
StandardOutput = "journal+console"
StandardError = "journal+console"

[units."mios-ceph-bootstrap.service".Install]
WantedBy = "multi-user.target"



# ----------------------------------------------------------------------------
# [identity] -- Linux account, hostname, GECOS, login shell, group membership.
# Maps to MIOS_USER, MIOS_HOSTNAME, MIOS_USER_FULLNAME, MIOS_USER_SHELL,
# MIOS_USER_GROUPS.
# ----------------------------------------------------------------------------
[identity]
username  = "mios"
fullname  = "MiOS Operator"
hostname  = "mios"
shell     = "/bin/bash"
name      = "mios"
email     = "mios@localhost"
location  = ""
groups    = ["wheel", "libvirt", "kvm", "video", "render", "input", "dialout", "docker"]
default_password = "mios"

[user]
name = ""
pronouns = ""
bio = ""

[accounts]
db_backed = true
db_render_prefs = false

# ----------------------------------------------------------------------------
# [mios] -- AI Agent Persona. Replaces hardcoded identity strings in the
# orchestrator system prompt. Layered from ~/.config/mios.
# ----------------------------------------------------------------------------
[mios]
name = "MiOS AI"
role = "the ONE name you go by on EVERY surface (the `@`/`mios` CLI, OWUI, Discord, the desktop app, the API)"
developer = "MiOS"
[agent_passport]
# Read by federation/a2a.py; the shipped manual documents this path. The
# copy under [security] was inert: emitted by no resolver, read by nothing.
principal_mode = "off"
# domain          = "mios.example.ts.net"   # public host; defaults to localhost
# legal_name      = "MiOS"
# display_name    = "MiOS AI"
# contact_email   = "admin@mios.example.ts.net"
# scope           = ["assist.local", "tools.invoke", "web.search"]
# spend_ceiling_amount = 0        # 0 = no autonomous spend authority
# hitl_above_amount    = 0        # human-in-the-loop trigger threshold
# sla_hours       = 24
# data_classification  = "local-only"
# regions         = ["US"]
# validity_days   = 90

[security]
# TLS verification on agent-plane reachability probes. false = self-signed mesh only.
probe_verify_tls = true
enable_units = [
  "usbguard.service",
  "auditd.service",
  "fapolicyd.service"
]
composefs_mode = "verity"
mask_systemd_remount_fs = true


rule_of_two_mode = "off"

quarantine_mode = "off"

api_require_auth     = false
api_caller_keys_path = "/etc/mios/ai/v1/caller-keys.json"

principal_bind_mode  = "off"

firewall_high_privilege_verbs = [
    "service_restart",
    "container_restart",
    "pc_type",
    "pc_key",
    "pc_click",
    # Native text-editor write verbs -- tainted sessions can't
    # craft them to drop payloads onto operator paths.
    "text_create",
    "text_str_replace",
    "text_insert",
    # Single most dangerous verb in the catalog: arbitrary
    # Windows-side script execution. Always firewall-gated.
    "powershell_run",
    "minimize_window",
    "maximize_window",
    "restore_window",
    "resize_window",
    "position_window",
    "winget_install",
    "winget_upgrade",
    "winget_uninstall",
    "flatpak_install",
    "flatpak_upgrade",
    "flatpak_uninstall",
]
# WS-A14: verbs whose OWN execution introduces session taint (downstream
# high-privilege verbs then get firewall-checked). UNIONED with the built-in
# external-fetch set (web_search/web_extract/crawl/web_scrape) by mios_secset --
# add a verb here to taint on it without a code change. Empty by default.
taint_verbs = []
text_view_taint_prefixes = [
    "/etc/",
    "/usr/",
    "/boot/",
    "/sys/",
    "/proc/",
    "/dev/",
    "/mnt/c/Windows/",
    "/mnt/c/Program Files/",
    "/mnt/c/Program Files (x86)/",
]
internal_tld_suffixes = [".local", ".lan", ".internal"]
arbiter_deny       = []        # verbs the arbiter ALWAYS refuses (hard floor)
arbiter_allow      = ""        # exclusive allow-list when non-empty (CSV); "" = no allow-list
arbiter_block_tier = ""        # refuse verbs at/above this risk tier (read|write|interactive); "" = no tier ceiling
arbiter_port       = 8760      # loopback port mios-policy-arbiter listens on

token_ttl_s        = 3600
allowlist_hosts = [
    "localhost",
    "127.0.0.1",
    "::1",
    "host.containers.internal",
    "mios-llm-light",
    "mios-open-webui",
    "mios-hermes",
    "mios-pgvector",
    "mios-forge",
    "mios-searxng",
    "mios-crawl4ai",
    "mios-code-server",
]

provenance_taint = false

# [security.credential_literals] -- Law-11 extension: default credentials still
# baked into world-readable units. SHRINK-ONLY; T-175 rotates them to
# /etc/mios/secrets.env (gate: check_credential_literals).
[security.credential_literals]
grandfathered = [
  "usr/lib/systemd/system/mios-agents.service:MIOS_DEFAULT_PASSWORD",
  "usr/lib/systemd/system/mios-ha-bootstrap.service:HA_PASSWORD",
  "usr/share/containers/systemd/mios-guacamole.container:POSTGRESQL_PASSWORD",
  "usr/share/containers/systemd/mios-k3s.container:K3S_TOKEN",
  "usr/share/containers/systemd/mios-open-webui.container:RAG_OPENAI_API_KEY",
  "usr/share/containers/systemd/mios-open-webui.container:WEBUI_SECRET_KEY",
  "usr/share/containers/systemd/mios-pgvector.container:POSTGRES_PASSWORD",
]

# [security.redact] -- DURA-02 redaction on PERSIST; every schema table sits in
# exactly one list below (gate: check_redact_coverage).
[security.redact]
enable      = true
# A redaction error refuses the write; never fall through to an unredacted one.
fail_closed = true
# Agent-plane free text -- where a secret or PII actually lands.
tables = [
  "agent_memory", "config_event", "directory_entry", "event",
  "fact_ledger", "gateway_sessions", "kanban", "knowledge",
  "log_digest", "mios_rag", "peer_reputation", "pending_action",
  "progress_ledger", "scratch", "session", "skill_invocation",
  "tasks", "tool_call",
]
# Structured system/config tables: redaction would CORRUPT the value, not
# protect it, and they hold no free-form prose.
# drift_snapshot holds normalized LABEL HISTOGRAMS and counts; skill_tool_call
# holds two ids and a step index. Both are structure, never content. Exempt.
exempt = [
    "account",
    "account_preference",
    "agent_keypair",
    "alias",
    "app_install",
    "appx_removal",
    "build_phase",
    "build_recipe",
    "capability_removal",
    "component_removal",
    "config_kv",
    "config_layer",
    "debloat_policy",
    "debloat_profile",
    "domain_verb",
    "drift_snapshot",
    "embeddings",
    "feature_removal",
    "feature_set",
    "mios_identity",
    "mios_security",
    "package_set",
    "person",
    "person_app_install",
    "person_device",
    "person_pref",
    "preset",
    "quota_ledger",
    "resolves_to",
    "run_template",
    "skill",
    "skill_tool_call",
    "sys_env",
    "system_config",
    "verb",
    "xbox_feature"
]

[security.cosign]
policy_mode = "matchRepository"

[security.windows_binaries]
source_exempt = ["MiOS-iGPU-Server.exe"]

[security.powershell_signing]
# Certificate PATH or thumbprint only. There is deliberately no password key
# here: mios.toml is committed, so a signing secret in SSOT is a leaked secret.
# tools/sign-powershell.ps1 takes -CertPassword as a SecureString, or prompts.
signing_cert = ""

[artifacts.windows]
# Pinned SHA-256 digests for runtime downloads per ADR-0003
llama_vulkan_zip_sha256 = ""
qwen_gguf_sha256 = ""
granite_gguf_sha256 = ""
lfm2_gguf_sha256 = ""
nomic_embed_sha256 = ""

[repos.fedora]
name = "Fedora {ver} - $basearch"
metalink = "https://mirrors.fedoraproject.org/metalink?repo=fedora-{ver}&arch=$basearch"
enabled = true
repo_gpgcheck = false
repo_type = "rpm"
gpgcheck = true
gpgkey = "file:///etc/pki/rpm-gpg/RPM-GPG-KEY-fedora-{ver}-x86_64"
skip_if_unavailable = true
priority = 95
timeout = 10
minrate = "1k"
max_parallel_downloads = 10
ip_resolve = 4

[repos.fedora-updates]
name = "Fedora {ver} Updates - $basearch"
metalink = "https://mirrors.fedoraproject.org/metalink?repo=updates-released-f{ver}&arch=$basearch"
enabled = true
repo_gpgcheck = false
repo_type = "rpm"
gpgcheck = true
gpgkey = "file:///etc/pki/rpm-gpg/RPM-GPG-KEY-fedora-{ver}-x86_64"
skip_if_unavailable = true
priority = 95
timeout = 10
minrate = "1k"
max_parallel_downloads = 10
ip_resolve = 4


[ai_tag]
max_untagged = 120
max_unconforming = 0

# Hint length cap. Was hardcoded 260 in mios-ai-tag, where it also silently
# TRUNCATED hand-written hints on every re-tag. Raised so a re-tag is lossless;
# ratcheted down once the over-cap hints have been shortened deliberately.
hint_max_chars = 260
# Teacher lane for generating a hint when a file has none. The former defaults
# were port 8450 (SSOT says 8500) and model gemma4:12b, which is not in
# [ai].available_models -- so the lane could never have worked.
teacher_port_key = "llm_light"
teacher_model = "granite4.1:3b"

[selfimprove]
sample_size    = 500     # most-recent tool_call rows to analyze
min_samples    = 5       # don't judge a tool with fewer calls (no over-reaction)
fail_threshold = 0.3     # tools failing >= this fraction are flagged
slow_ms        = 10000   # tools averaging >= this many ms are flagged slow
# interval_min: how often the agent proactively runs the analyzer and SURFACES
# new high/medium findings to the log (picked up by the daemon-agent + visible in
# journalctl) -- the "loop closure" in the observe->surface sense. 0 (default) =
# off, no background task (byte-identical hot path). It only SURFACES; auto-
# remediation (self-modification) is a separate, guardrail-gated step.
interval_min   = 0

act_enabled    = false   # master switch for the ACT (propose->prove->queue) loop
# Proof-of-utility accept criterion (Autodata accept-if-non-regressing, 126/233):
# accept iff the proposed variant's pass^k does not regress the baseline by more than
# accept_margin (0 = strict >=); require_improvement additionally demands a strict
# improvement (delta > 0) where a discriminative eval applies. passhat_k = the k for
# the pass^k reliability score (the same worst-case metric the skill gate uses).
accept_margin       = 0.0   # max tolerated score regression (proposed >= baseline - margin)
require_improvement = false # also require a strict improvement (proposed > baseline)
passhat_k           = 2     # k for pass^k over the held-out eval
# Solver-gap (Autodata discriminative signal): an eval task carries signal ONLY when
# the strong solver beats the weak one by >= solver_gap_min; trivial (both pass) and
# impossible (both fail) tasks are dropped. The weak/strong pair is the light/heavy
# LANE pair, named here by lane key and resolved from the live lane registry at
# dispatch -- NOT a model id baked in code.
solver_gap_min  = 0.2      # min strong-weak pass-rate gap for a task to count as eval signal
weak_solver     = "light"  # lane key of the weak solver (the everyday light lane)
strong_solver   = "heavy"  # lane key of the strong solver (the heavy/council lane)
improvable_targets = ["prompt", "skill", "config"]
protected_targets  = ["evaluator", "eval_data", "lane_config", "selfimprove"]
max_proposals_per_pass = 3   # cap proposals drafted per loop pass (bounded change)

[portal]
# Externally-reachable name for the portal's service tiles (e.g. a Tailscale
# MagicDNS name whose HTTPS cert `tailscale serve` binds to). EMPTY by default:
# the vendor image bakes NO specific host -> agent-pipe degrades to this
# machine's own hostname. Set to YOUR public name to link tiles to it.
public_host = ""
require_login = true     # false ONLY if a reverse proxy already authenticates
user          = ""       # empty = inherits [identity].username
password      = ""       # empty = inherits [identity].default_password
session_ttl   = 604800   # signed-cookie lifetime in seconds (default 7 days)
# secret: empty = derive the cookie-HMAC key from the password (so rotating the
# password invalidates live sessions). Set a stable value to keep sessions
# alive across password changes.
secret        = ""

discord_channel_default = ""
discord_guild_default   = ""

# ----------------------------------------------------------------------------
# [locale] -- system-wide defaults applied via systemd-localed at first boot.
# Maps to MIOS_TIMEZONE, MIOS_KEYBOARD, MIOS_LOCALE.
# ----------------------------------------------------------------------------
[locale]
timezone        = "UTC"
keyboard_layout = "us"
language        = "en_US.UTF-8"

[auth]
ssh_key_type     = "ed25519"
ssh_key_action   = "generate"
existing_ssh_key = ""
password_policy  = "plain"
password          = "mios"      # SSOT: dashboard advertises this; cockpit PAM uses it
password_hash    = ""           # only when password_policy = "hashed"
luks_passphrase  = ""           # never tracked; FHS installer + LUKS only
github_pat       = ""           # never tracked; configures git credential helper

# ----------------------------------------------------------------------------
# [network] -- firewalld posture.
# Defaults match SECURITY.md (default zone "drop", ssh + cockpit allowed).
# ----------------------------------------------------------------------------
[network]
firewalld_default_zone = "drop"
allow_ssh              = true
allow_cockpit          = true   # opens 9090/tcp
allow_libvirt_bridge   = true   # for VM networking

# NTP servers configuration (Chrony)
[network.ntp]
servers = [
    "0.pool.ntp.org",
    "1.pool.ntp.org",
    "2.pool.ntp.org",
    "3.pool.ntp.org",
]

# UPS configuration (Network UPS Tools)
# Set name = "" to disable NUT (boots inert)
[power.ups]
name   = ""
driver = "usbhid-ups"
port   = "auto"
desc   = "MiOS Uninterruptible Power Supply"

# MiOS-Metal GPU arbitration and guest assignment (WS-F5)
[metal.gpu]
# Rule: Each GPU (PCI/IOMMU address) must be assigned to exactly one owner/guest.
# Forbid assigning the same PCI address to multiple active guests to prevent hardware conflicts.
assignments = { mios-guest = ["0000:01:00.0"] }
arbitration = "static"   # GPU arbitration model: "static" (pre-bind at boot), "dynamic" (libvirt hotplug/detach)

[meta]
schema_version = "1.1.0"
mios_version   = "0.3.0"
format         = "toml"
# Spec for the on-disk format -- TOML 1.0.0 (https://toml.io/en/v1.0.0).
# Editor: /usr/share/mios/configurator/mios.html (static, runs from
# file:// or behind any HTTP server) reads + writes this file via a
# round-trip parser; no installation required.
spec_url       = "https://toml.io/en/v1.0.0"
editor_url     = "/usr/share/mios/configurator/mios.html"

[verbs]
# Each entry: name = { description = "<help text>", surface = "<windows|dev_vm|both>" }
#   windows -- runs in the Windows pwsh host
#   dev_vm  -- forwards into podman-MiOS-DEV via wsl.exe
#   both    -- routes by current shell
build  = { description = "Open mios.html, save edits, then build the OCI image inside MiOS-DEV", surface = "dev_vm" }
config = { description = "Edit mios.toml in the HTML configurator (no build)",                   surface = "windows" }
dash   = { description = "Show the MiOS dashboard (framed banner + fastfetch)",                  surface = "windows" }
ai     = { description = "Open Open WebUI (rich LLM interface) in your browser",                 surface = "windows" }
dev    = { description = "Enter the MiOS-DEV podman machine",                                    surface = "dev_vm" }
xbox   = { description = "Xbox VM Secure Boot / XML repair",                                     surface = "dev_vm" }
virt   = { description = "Apply optimized VM config + CPU pinning",                              surface = "dev_vm" }
vfio   = { description = "Configure GPU/USB passthrough (Isolation)",                            surface = "dev_vm" }
tune   = { description = "System-wide CPU isolation & latency tuning",                           surface = "dev_vm" }
summary= { description = "Quick ASCII system overview",                                          surface = "dev_vm" }
profile= { description = "Interactive hardware/system profiler menu",                            surface = "dev_vm" }
assess = { description = "Comprehensive system capability report",                               surface = "dev_vm" }
iommu  = { description = "Pretty-print hardware IOMMU topology",                                 surface = "dev_vm" }
theme  = { description = "Sync bibata/GTK/Qt themes",                                            surface = "dev_vm" }
user   = { description = "Initialize user space (dotfiles/XDG)",                                 surface = "dev_vm" }
pull   = { description = "Sync M:\\ overlay to origin/main",                                      surface = "windows" }
update = { description = "Re-run the bootstrap (cache-busted)",                                  surface = "windows" }
help   = { description = "List every verb",                                                       surface = "windows" }

# Note: the canonical [apps] table is consolidated below (~line 1690)
# with the rest of the Windows-side app metadata. The verb catalog
# above ([verbs]) is the SSOT for `mios <verb>` command names; the
# [apps.shortcuts] table below maps a subset of those verbs to
# Start Menu / Desktop .lnk files.

[install_phases.bootstrap]
names = [
    "Hardware + Prerequisites",
    "Detecting environment",
    "Directories and repos",
    "MiOS-DEV distro",
    "WSL2 configuration",
    "App registration"
]

[install_phases.full]
names = [
    "Hardware + Prerequisites",
    "Detecting environment",
    "Directories and repos",
    "MiOS-DEV distro",
    "WSL2 configuration",
    "Verifying build context",
    "Identity",
    "Writing identity",
    "App registration",
    "Building OCI image",
    "Exporting WSL2 image",
    "Registering 'MiOS' WSL2",
    "Building disk images",
    "Deploying Hyper-V VM"
]


[messages.installer_banner]
# The Unified Installer banner box at the top of every build-mios.ps1 run.
# {version} is substituted from [meta].mios_version at render time.
title    = "'MiOS' {version}  --  My Personal Operating System"
taglines = [
    "Immutable Fedora AI Workstation",
    "WSL2 + Podman  │  Offline Build Pipeline"
]

[messages.eula]
display_ms = 600
lines = [
    "",
    "  MiOS -- My Personal Operating System",
    "  Immutable Fedora AI Workstation (pronounced \"MyOS\")",
    "",
    "  By invoking any MiOS entry point you acknowledge:",
    "    * MiOS is provided AS IS, NO WARRANTY (MIT license).",
    "    * Build/install scripts can modify your system globally",
    "      (registry, env vars, fonts, WT settings, WSL distros, M:\\ partition).",
    "    * Telemetry: NONE (no data leaves the host without explicit operator action).",
    "    * Full text: M:\\AGREEMENTS.md  +  M:\\LICENSE",
    "",
    "  Continued use of this terminal is treated as acknowledgment.",
    "",
]

[messages.build_pipeline]
# Linear-log mode header, shown when the host can't do in-place
# repaint (most non-WT shells).  Two lines: the headline + a hint.
linear_log_note = "Note: console doesn't support in-place repaint -- running in linear log mode."
linear_log_hint = "      Phase transitions + throttled step updates print sequentially below."

[messages.elevation]
admin_required   = "  [*] MiOS bootstrap requires admin (M:\\ partition + Podman + dev VM)."
uac_trigger_line = "  [*] Triggering UAC -- accept to continue. The install will then run"
uac_trigger_hint = "      in the elevated window, single prompt only."

[messages.pass2_exit]
exited_code_prefix = "  [!] Bootstrap exited with code "
failure_detail     = "      Output above is the failure detail (Pass-1 has no separate log)."
build_log_hint     = "      build-mios.ps1's own log at M:\\MiOS\\logs\\mios-install-*.log only kicks in on Pass-2 success."
fetch_run_failed   = "  [!] Bootstrap fetch/run failed: "
press_enter_close  = "  Press Enter to close this elevated bootstrap window..."
success_transition  = "  [+] Bootstrap complete. Loading MiOS terminal in this window..."
profile_load_failed = "  [!] MiOS profile load failed: "
profile_load_hint   = "      Open a fresh MiOS shortcut to retry."
profile_missing     = "  [!] M:\\MiOS\\powershell\\profile.ps1 not found -- open the MiOS shortcut to launch the app window."

[messages.steps]
disk_sizing_template       = "Sizing MiOS data disk ({mb} MB on {drive}:)..."
disk_already_template      = "MiOS data disk already on {drive}: ({size_gb} GB, NTFS)"
mios_git_init              = "Initializing mios.git as the M:\\ working tree"
mios_git_update            = "Updating mios.git (fetch + hard reset @ M:\\)"
mios_git_overlaid          = "mios.git overlaid at M:\\"
mios_bootstrap_clone       = "Cloning mios-bootstrap.git -> shadow M:\\MiOS\\bootstrap-shadow"
mios_bootstrap_update      = "Updating mios-bootstrap.git shadow (fetch + hard reset)"
mios_bootstrap_overlay     = "Overlaying mios-bootstrap files onto M:\\"
entry_scripts_staged       = "Entry scripts staged at M:\\MiOS\\bin"
miosdev_init_template      = "Initializing MiOS-DEV ({cpus} CPUs / {ram_gb}GB / {disk_gb}GB disk)"
smoke_header_template      = "Smoke-testing {distro} before rename..."
smoke_responsive_template  = "smoke 1/4: {name} is responsive"
smoke_systemd_template     = "smoke 2/4: systemd state '{state}' in {name}"
smoke_overlay_template     = "smoke 3/4: /usr/share/mios overlay present in {name}"
smoke_podman_api_template  = "smoke 4/4: podman API server v{version}"

[messages.install_complete]
title          = "MiOS Windows-side install complete"
installed_lead = "    Installed ..............................................................."
next_steps     = "    What's next? Type any of these in the MiOS terminal:"
hub_hint       = "    The MiOS hub shortcut is in your Start Menu / Desktop / Win+Search."
# Bullets accept {disk_gb} substitution at render time so the
# storage-size value stays current per host.
bullets = [
    "M:\\ partition ({disk_gb} GB NTFS, label MIOS-DEV)",
    "Podman Desktop + podman-MiOS-DEV machine",
    "mios.git + mios-bootstrap overlaid at M:\\",
    "MiOS terminal essentials layered into MiOS-DEV",
    "Native Windows app: Start Menu + Desktop + per-verb shortcuts",
    "MiOS PowerShell profile (oh-my-posh, dashboard, mios <verb>)",
]

[search]
endpoint = "http://localhost:${MIOS_PORT_SEARXNG}/"
enable   = true
anchor_stopwords = [
    "the", "a", "an", "of", "to", "from", "and", "or", "for", "in", "on", "at",
    "by", "with", "as", "is", "are", "was", "were", "be", "this", "that", "these",
    "those", "it", "its", "me", "my", "we", "our", "you", "your", "they", "them",
    "what", "which", "who", "when", "where", "why", "how", "do", "does", "did",
    "can", "could", "will", "would", "should", "may", "might", "near", "into",
    "about", "than", "then", "there", "here", "out", "not", "no", "all", "any",
    "some", "more", "most", "find", "get", "make", "show", "give", "tell", "list",
    "need", "want", "like", "use", "using", "best", "cheap", "cheapest",
]

[hermes]
# Hermes-Agent: OpenAI-compatible gateway and the live MiOS agent at /.
# Fronts mios-llm-light (${MIOS_PORT_LLM_LIGHT}). API key generated at first boot
# into /etc/mios/hermes/api.env. Mirrors [ai].model -- this key is
# kept for back-compat with older callers; the SSOT default is [ai].model.
endpoint = "http://localhost:${MIOS_PORT_AGENT_PIPE}/v1"
backend  = "http://localhost:${MIOS_PORT_LLM_LIGHT}"   # mios-llm-light (ALL llama.cpp)
model    = "granite4.1:8b"
enable   = true


[gpu]
device = "nvidia.com/gpu=all"

[sandbox]
enable = false
# T-230: bwrap already confined the filesystem and the network for an opted-in
# verb; the confined process still reported `Seccomp: 0`. Manual ch62.
seccomp_enable = true      # off => bwrap runs with no syscall filter (the old behaviour)
seccomp_action = "errno"   # errno => the call returns EPERM; kill => the process dies
# Extends the BASELINE floor in mios_pipe/access/seccomp.py; it cannot shrink it.
seccomp_deny   = ["swapon", "swapoff", "reboot", "settimeofday", "clock_settime",
                  "process_vm_readv", "process_vm_writev"]

# ----------------------------------------------------------------------------
[laws]
# The MiOS architectural LAW registry (v0.3.0) -- the SINGLE source of the law
# list. CLAUDE.md / AGENTS.md / MiOS.md and generate-manual.py document these;
# 98-drift-checks.sh + 99-postcheck.sh enforce them (a failing law fails the
# build). This table exists so the id<->slug<->enforcer mapping can never drift
# across the prose copies again. applies_to = bootc | wsl | both.
laws = [
  { id = 1,  slug = "USR-OVER-ETC",         applies_to = "both",  enforced_by = "98-drift-checks.sh:check_usr_over_etc" },
  { id = 2,  slug = "NO-MKDIR-IN-VAR",       applies_to = "both",  enforced_by = "98-drift-checks.sh:check_no_mkdir_in_var" },
  { id = 3,  slug = "BOUND-IMAGES",          applies_to = "bootc", enforced_by = "99-postcheck.sh:item14" },
  { id = 4,  slug = "BOOTC-CONTAINER-LINT",  applies_to = "bootc", enforced_by = "98-drift-checks.sh:check_lint_is_final" },
  { id = 5,  slug = "UNIFIED-AI-REDIRECTS",  applies_to = "both",  enforced_by = "99-postcheck.sh:item12" },
  { id = 6,  slug = "UNPRIVILEGED-QUADLETS", applies_to = "bootc", enforced_by = "98-drift-checks.sh:check_quadlet_privilege" },
  { id = 7,  slug = "NO-HARDCODE",           applies_to = "both",  enforced_by = "98-drift-checks.sh:check_no_hardcode" },
  { id = 8,  slug = "SSOT-PROJECTION",       applies_to = "both",  enforced_by = "98-drift-checks.sh:check_projection_registry" },
  { id = 9,  slug = "ONE-CANONICAL-NAME",    applies_to = "both",  enforced_by = "98-drift-checks.sh:check_var_closure" },
  { id = 10, slug = "BARE-SAFE-ENV",         applies_to = "both",  enforced_by = "99-postcheck.sh:item16" },
  { id = 11, slug = "SECRETS-NEVER-IN-ENV",  applies_to = "bootc", enforced_by = "99-postcheck.sh:item17" },
  { id = 12, slug = "BAKE-NOT-FETCH",        applies_to = "both",  enforced_by = "98-drift-checks.sh:check_dag_integrity,check_firstboot_degrade_open" },
  { id = 13, slug = "NATIVE-DROPINS",        applies_to = "both",  enforced_by = "98-drift-checks.sh:check_resolver_twin_parity" },
  { id = 14, slug = "TARGET-LANGUAGES",      applies_to = "both",  enforced_by = "98-drift-checks.sh:check_target_languages" },
  { id = 15, slug = "DOUBLE-REPO-TRIPLE-CHECK", applies_to = "both", enforced_by = "process:CLAUDE.md/AGENTS.md (both repos); parity via 98-drift-checks.sh checks 22+27" },
  { id = 16, slug = "ONE-TEMPLATE-PER-TYPE",    applies_to = "both", enforced_by = "98-drift-checks.sh:check_template_conformance" },
]

[laws.target_languages]
grandfathered_cs = [
  "usr/share/mios/windows/MiOS-Launcher.cs",
  "usr/share/mios/windows/MiosServiceTool.cs",
]

[laws.projection_registry]
surfaces = [
  { generator = "usr/libexec/mios/mios-theme-render", check = "check_dotfiles_projection", output = "etc/ (and various target registries)" },
  { generator = "usr/libexec/mios/mios-sync-toml", check = "check_toml_projection", output = "usr/share/mios/mios.toml.bak (and metadata)" },
  { generator = "automation/98-drift-checks.sh", check = "check_drift_projection", output = "stdout (drift assertions)" },
  { generator = "usr/libexec/mios/mios-manual", check = "check_manual_ledger", output = "usr/share/mios/reference/manual-corpus.tsv" },
  { generator = "usr/libexec/mios/mios-manual", check = "check_manual_generated", output = "usr/share/doc/mios/ (MIOS-GEN marker interiors)" },
  { generator = "usr/libexec/mios/mios-manual", check = "check_comment_landing", output = "usr/share/doc/mios/ (harvested passages + mios-src anchors)" },
  { generator = "usr/libexec/mios/mios-manual", check = "check_docs_ratchet_monotone", output = "usr/share/mios/reference/doc-ratchet-floor.tsv" },
  { generator = "tools/render-desktop.py", check = "check_desktop_launchers", output = "usr/share/applications/*.desktop" },
  { generator = "tools/generate-ai-manifest.py", check = "check_ai_manifests_fresh", output = "automation/manifest.json" },
  { generator = "tools/generate-blade-dropins.py", check = "check_blade_dropins", output = "usr/share/mios/dropins/" },
  { generator = "tools/generate-pod-quadlets.py", check = "check_pod_quadlets", output = "usr/share/containers/systemd/" },
  { generator = "tools/render-globals.py", check = "check_globals_generated", output = "automation/lib/globals.sh, automation/lib/globals.ps1" },
]


[security.sigstore]
cosign_major = 2
policy_mode = "insecureAcceptEverything"
allowed_identities = []

[security.privileged_quadlets]
max_privileged_root = 12
root = [
  "mios-ceph.container",                    # Ceph OSD/MON -- uid 0 for block devices
  "mios-k3s.container",                     # k3s server -- uid 0 (CNI, kubelet mounts)
  "mios-forge.container",                   # Forgejo data bootstrap as 0
  "mios-forgejo-runner.container",          # CI runner (DinD / --privileged)
  "mios-pxe-hub.container",                 # PXE/TFTP/DHCP -- privileged net
  "mios-webtools-crawl4ai.container",       # webtools pod -- upstream image runs as 0
  "mios-webtools-firecrawl-api.container",  # webtools pod -- upstream image runs as 0
  "mios-webtools-firecrawl-worker.container", # webtools pod -- upstream image runs as 0
  "mios-webtools-redis.container",          # webtools pod -- upstream image runs as 0
  "mios-llm-heavy.container",               # PyTorch/vLLM GPU serving -- upstream image runs as 0
  "mios-llm-heavy-alt.container",           # SGLang GPU serving -- upstream image runs as 0
  "mios-coderun-sandbox@.container",        # user-scope sandbox -- Network=none + ReadOnly + DropCapability=ALL
]
# Units legitimately without Group=/Delegate=yes (Law 6 exempt from those two keys only).
no_group_delegate = [
  "mios-coderun-sandbox@.container",        # UserNS=keep-id user unit -- Delegate n/a
]

[security.nohc_allowlist]
# SSOT Allowlist for legitimate exceptions to the ports/IPs hardcode check (T-125).
exempt_files = [
    "install-mios-agents\\.sh$",
    "usr/lib/mios/agent-pipe/server\\.py$",
    "usr/lib/mios/gateway-agent/.*\\.py$",
    "test_.*\\.py$",
    "Heal-MiOSLocalhostForwarding\\.ps1$",
    "Setup-MiOSLanPortProxy\\.ps1$",
    "Update-MiOSStartMenuShortcuts\\.ps1$",
    "user-setup\\.sh$",
    "system-sync-env\\.sh$",
    "mios-dashboard\\.sh$",
    ".*firstboot\\.sh$",
    ".*firstboot$",
    ".*/automation/.*",
    ".*/windows/.*",
    ".*/usr/libexec/.*",
    ".*/profile\\.d/.*",
    ".*/xrdp/.*",
    ".*/startwm\\.sh$",
    ".*/evals/.*",
    ".*/embeddings/.*",
    ".*/tools/.*",
    ".*/tests/.*",
    ".*/usr/share/mios/.*",
    ".*build-mios\\.ps1$",
    ".*Get-MiOS\\.ps1$",
    ".*patch_pods\\.py$",
    ".*mios-fix-wslg\\.ps1$",
    ".*MiOS-Cat-FileChecker\\.ps1$"
]
exempt_patterns = [
    "0\\.0\\.0\\.0",
    "localhost:80",
    "localhost:443",
    "127\\.0\\.0\\.1:80",
    "127\\.0\\.0\\.1:443",
    "localhost/mios",
    "wsl\\.localhost",
    "localhost:8700",
    "localhost:8720",
    "localhost:8575",
    ":8700",
    ":8720",
    ":8575",
    ":11436",
    ":8740",
    ":11435",
    ":9222",
    ":8283"
]


[security.fapolicyd_observe]
enable = false



[security.mcp_sandbox]
enable = false
write_allowed_paths = ["/tmp/mios-mcp", "/var/lib/mios/ai"]

[security.egress]
mode  = "off"
allow = []

[security.disk_encryption]
mode     = "tpm2"                          # Encryption mode: "tpm2", "clevis", "none"
pcrs     = [7, 14]                         # 7 = Secure Boot state, 14 = MOK enrolment (akmods-signed NVIDIA modules)
recovery = true                            # Generate and store a LUKS recovery key
backend  = "systemd-cryptenroll"           # Enrollment backend: "systemd-cryptenroll", "clevis"

# A TPM2 PCR policy binds to ONE host: PCR 7 is that machine's Secure Boot
# state, PCR 14 that machine's MOK enrolment. Neither travels on a drive.

# So a portable MiOS-Cat drive enrolls a SECOND token. ADR-0016 D13.1.
portable_token = "fido2"

[security.mtls]
enable        = false
dir           = "/etc/mios/mtls"
common_name   = ""          # empty -> hostname
validity_days = 825
# ca_file   = "/etc/mios/mtls/ca.crt"     # set these to use an org PKI instead
# cert_file = "/etc/mios/mtls/agent.crt"
# key_file  = "/etc/mios/mtls/agent.key"

[uki]
verity_uki_build = false

[fs_watcher]
watch_dirs = [
    "/var/lib/mios/hermes-tail",
    "/var/lib/mios/delegation-prefilter",
    "/var/lib/mios/log-watcher",
    "/var/lib/mios/daemon",
    "/var/lib/mios/scratch",
    "/var/lib/mios/agent-nudger",
    "/var/lib/mios/cron-director",
    "/var/lib/mios/ai/scratch",
]

[pkg]
bootstrap_per_source_cap   = 200
lookup_max_alias_results   = 3

[ttyd]
enable             = true
bind               = "127.0.0.1"
# Mobile (#82): expose the loopback bash terminal to the TAILNET (phones) over
# tailnet-only HTTPS via `tailscale serve` (NOT the public internet). When true,
# the mios-ttyd-expose.service oneshot publishes it on boot; off by default so a
# fresh install never opens a shell unprompted. Toggle here or in mios.html.
# The login is the SSOT [identity] credential (mios/mios) -- never unauth'd.
tailnet_expose     = false
# Master auth toggle: when bind != "127.0.0.1" + require_auth = true
# + auth_user/pass empty, the service refuses to start (prevents
# accidental open-network exposure).
require_auth       = true
# auth_user/auth_pass: empty = INHERIT the global [identity] login (username /
# default_password, default mios/mios) -- same as Portal/Cockpit/Forge. Set per
# instance only to override the one global credential.
auth_user          = ""
auth_pass          = ""
ssl_cert           = ""
ssl_key            = ""
# Per-instance shell overrides. Empty = use the per-service default
# (mios-ttyd-bash -> /bin/bash; mios-ttyd-powershell ->
# /usr/libexec/mios/mios-powershell --shell which resolves pwsh.exe).
bash_shell         = ""
powershell_shell   = ""
writable           = true
# Max active client sessions per instance (ttyd defaults to 1 +
# rejects further). Raise for collaborative read-only sessions.
max_clients        = 0
# xterm font size (px). The MiOS Portal sizes its inline terminal embed to a
# fixed 80x20 pixel box assuming this; raising it makes the embed show fewer
# columns/rows in that box (and vice-versa).
font_size          = 14

[agents]

[agents._defaults]
kind            = "local-http"   # local-http|remote-http|cli|mobile|edge|node|a2a
role            = "general"
job             = ""
default         = false
fanout          = true
enabled         = true           # set false on a default-off / optional unit
lane            = "gpu"
health_gate     = false          # loader upgrades -> true for local-optional / remote / cli
transport       = "http"         # cli => subprocess shim (e.g. opencode)
timeout_s       = 0              # 0 = lane default; required for kind=cli
strengths       = []
cpu_endpoint    = ""
cpu_model       = ""
failover_agents = []
denied_verbs    = []
allowed_verbs   = []
max_permission  = ""             # "" = no ceiling; e.g. "read" for an untrusted edge node
api             = ""             # openai|llamacpp|vllm|sglang
research_only   = false

[agents._defaults.auth]
scheme          = "none"         # none|bearer|oauth2|mtls
header_template = ""             # e.g. "Authorization: Bearer ${MIOS_AGENT_<NAME>_KEY}"
principal_mode  = ""             # "" = inherit [a2a].principal_mode; else audit|require

[agents._defaults.trust]
min_reputation           = 0.0   # drop a peer below this reputation (mios_reputation)
require_signed_principal = false # require a verified Ed25519 principal on inbound delegation
mtls                     = false # require mTLS for this peer

[agents.hermes]
privilege_group = "privileged"
endpoint = "http://localhost:${MIOS_PORT_HERMES}/v1"
model    = "mios-heavy"
role     = "general"
health_gate = true
job      = "General orchestration of multi-step, tool-driven tasks -- decide local-vs-web, search, inspect and operate the system, launch apps, then fan out and synthesise."
fanout   = false
default  = false
# Inference lane ("self-delegate to CPU concurrently
# to pending/future GPU operations"). gpu = the heavy dGPU lane (${MIOS_PORT_SGLANG});
# cpu = the light iGPU/CPU lane. The fan-out pairs a GPU primary with a
# CPU secondary so both lanes work in parallel -- and so Hermes is never
# the ONLY dispatched agent.
lane     = "gpu"
cpu_endpoint = "http://localhost:${MIOS_PORT_SGLANG}/v1"
cpu_model    = "mios-heavy"
# Tools Hermes is good at -- the refine pass surfaces these
# as hint_tools when the intent matches.
strengths = ["kanban", "web_search", "skill_invocation",
             "multi_step_reasoning", "tool_use"]

[agents.opencode]
privilege_group = "routine"
endpoint = "http://localhost:${MIOS_PORT_OPENCODE_GATEWAY}/v1"
# ONE canonical model id (must match [ai].opencode_model + opencode.json + the
# mios-opencode build name:tag). Was "opencode"; unified to the real tag the
# gateway advertises and mios-llm-light serves.
model    = "mios-opencode:latest"
# WS-A1 unified-template fields: opencode is a default-OFF subprocess-shim (CLI)
# agent reached via the :8780 gateway. Declaring kind=cli + enabled=false makes
# the loader auto-upgrade health_gate=true (so a dead gateway is probed + dropped,
# never a DAG sink) even if the explicit flag below were ever removed.
kind     = "cli"
transport = "cli"
enabled  = true
timeout_s = 90
role     = "coding"
job      = "Software engineering -- read, edit, review, test and commit code, configs, scripts, systemd units, Containerfiles and TOML in this repo/system."
default  = false
fanout   = true
health_gate = true
lane     = "gpu"
strengths = ["file_edit", "code_review", "git", "test_running",
             "refactor"]
failover_agents = ["hermes"]

[agents.mios-daemon-agent]
endpoint = "http://localhost:${MIOS_PORT_SGLANG}/v1"
model    = "mios-heavy"
# REASONING now (was "telemetry"): a real CPU second opinion, grounded in --
# not limited to -- system telemetry.
role     = "reasoning"
job      = "CPU reasoning grounded in live system state -- second opinions, summaries, planning, and answers grounded in the global journal/log telemetry it continuously collects."
default  = false
fanout   = true
# health_gate (merged from mios-reasoner-cpu): engage only when :8740 answers,
# drop fast otherwise -- the always-on daemon normally always passes.
health_gate = true
# CPU lane: runs off the dGPU, so the fan-out self-delegates to it CONCURRENTLY
# with a GPU primary at zero VRAM cost -- lane-diverse councils (CPU+GPU+iGPU).
lane     = "cpu"
# MERGED strengths: general CPU reasoning (from the consolidated mios-reasoner-
# cpu) PLUS the telemetry/log specialism (the daemon's core job). NOT topic-
# gated (council mode ignores relevance); these inform the A2A skill tags + the
# relevance-mode fallback. _daemon_agent_reply's system prompt -- not a strength
# deny-list -- keeps telemetry out of unrelated answers.
strengths = ["reasoning", "second_opinion", "summarize", "planning",
             "journalctl_tail", "log_search", "event_query",
             "system_followup", "what_just_happened"]
# P3.2 SPOF removal: when the always-on :8740 reasoner is down, hermes can
# stand in for the reasoning role (loses the live-telemetry grounding but
# preserves the second-opinion capability). The cpu-twin already covers the
# lane fallback; this lets the cluster health show a NAMED failover too.
failover_agents = ["hermes"]




[agents.ai-local]
endpoint    = ""
model       = "qwen2.5-3b-instruct-4bit"
role        = "mobile"
job         = "On-device mobile second opinion -- a lightweight local model on the operator's phone (AI.Local over the tailnet) for an extra perspective when it is serving."
default     = false
lane        = "mobile"
health_gate = true
strengths   = ["mobile_local_model", "on_device", "offline_edge"]

[agents.mios-node]
kind            = "node"
endpoint        = "http://localhost:${MIOS_PORT_NODE}/v1"
model           = "mios-node:latest"
role            = "edge_execution"
job             = "Distributed edge micro-node task execution, Wasm sandboxing, Ed25519 signature checks, and CRDT lock-free state synchronization."
default         = false
fanout          = true
health_gate     = true
lane            = "edge"
api             = "http"
strengths       = ["wasm_sandboxing", "crdt_state_sync", "peer_discovery", "task_offloading", "ed25519_verification"]



[gateway]
# Native Gateway Sovereign Migration Configuration (T-078)
model                     = "granite4.1:8b"
max_tokens                = 4096
context_length            = 8192
port                      = 8720
max_steps                 = 30
tool_loop_engine          = "smolagents"
mcp_refresh_seconds       = 300
skill_refresh_seconds     = 300
skill_catalog_static_path = "/var/lib/mios/skills/catalog.json"
searxng_url               = "http://mios-searxng:8080"
enable                    = false

[blades]
# Fleet shape. The operator's number: 2-3 boxes typically, 6 at most, and a
# single Mini standalone is valid. Gate: check_fleet_safety.
min_nodes     = 1
typical_nodes = 3
max_nodes     = 6

# [blades.hazards] -- SHRINK-ONLY: configs safe on one node, unsafe above it.
# Detected from the tree, never hand-listed. Gate: check_fleet_safety.
[blades.hazards]
doc = "A MiOS-Metal fleet is 2-6 boxes (T-331), so a configuration that only works standalone is a defect waiting for the operator to add a peer. Each entry here is a hazard the tree currently carries, accepted deliberately while the fleet design lands (T-333). k3s-multi-server: four archetypes grant what mios-k3s requires and the unit runs `k3s server` with no K3S_URL, so every controller stands up its OWN control plane instead of joining one -- three default `hybrid` Minis would be three clusters sharing one token. pacemaker-unfenced: mios-ha-bootstrap sets stonith-enabled=false, which is correct for the one-node cluster it creates and is how split-brain corrupts data once a peer exists. Retiring an entry means the detector stops reproducing it, not editing this list."
max_accepted = 2
accepted = [
  "k3s-multi-server",
  "pacemaker-unfenced",
]


[nodes.local-cpu]
endpoint = "http://localhost:${MIOS_PORT_CPU_NODE}/v1"
model    = "mios-agent-cpu"    # canonical CPU Modelfile tag, never a raw base
lane     = "cpu"
api      = "llamacpp"          # /slots KV-paging, no tool_choice=required
health_gate = true
# Local iGPU lane -- ships endpoint EMPTY (privacy: the real iGPU is served
# NATIVELY on the Windows host via llama.cpp+Vulkan at a tailnet IP, set in the
# /etc/mios overlay). Empty endpoint = inert node, skipped by _load_node_pool.
[nodes.local-igpu]
endpoint = ""                  # set to http://<igpu-host-tailnet>:11436/v1 in /etc/mios
model    = "mios-igpu"         # llama.cpp + Vulkan alias served on the host
lane     = "igpu"
api      = "llamacpp"
[nodes.local-vllm]
endpoint    = "http://localhost:${MIOS_PORT_VLLM}/v1"
model       = "mios-heavy"     # = [ai.vllm].served_name; the heavy reasoner vLLM serves
lane        = "gpu"            # shares the dGPU lane semaphore (VRAM-admission governs co-load)
api         = "openai"
health_gate = true             # inert until the gated mios-llm-heavy-alt.service is enabled + reachable
# Local SGLang heavy lane ("continue with SGLang") -- the
# mios-llm-heavy.container on :11441 (see [ai.sglang]). HiCache spills inactive KV to
# CPU RAM. health_gate=true => AUTO-JOINS only when reachable; gated off until a
# model is baked + the service enabled. api=openai (SGLang speaks /v1 + honours
# tool_choice=required). Mutually exclusive with local-vllm (both serve "mios-heavy").
[nodes.local-sglang]
endpoint    = "http://localhost:${MIOS_PORT_SGLANG}/v1"
model       = "mios-heavy"     # = [ai.sglang].served_name; the heavy reasoner SGLang serves
lane        = "gpu"            # shares the dGPU lane semaphore (VRAM-admission governs co-load)
api         = "openai"
health_gate = true             # inert until the gated mios-llm-heavy.service is enabled + reachable
[nodes.local-llamaswap]
endpoint    = "http://localhost:${MIOS_PORT_LLM_LIGHT}/v1"
model       = "mios-agent-cpu" # a resident alias of the light lane's model map
lane        = "cpu"            # llama.cpp serving lane, not the dGPU semaphore
api         = "llamacpp"       # /slots KV-paging, no tool_choice=required
health_gate = true             # inert until GGUFs are provisioned + the lane is up

# ─── Inference Lanes (WS-1 / WS-2) ────────────────────────────────────
[lanes.light]
stream_thinking  = true
tool_call_parser = "hermes"
reasoning_parser = "qwen3"
constrained_tools = true

[lanes.sglang]
stream_thinking  = true
tool_call_parser = "qwen25"
reasoning_parser = "qwen3"
constrained_tools = true

[lanes.vllm]
stream_thinking  = true
tool_call_parser = "hermes"
reasoning_parser = "qwen3"
constrained_tools = true


[cost]
enable              = true      # roadmap B1: observe-only ledger (records dispatches/tokens/energy; no behaviour change). /v1/cost reports it.
gpu_watts           = 350.0     # the dGPU board power used for the energy estimate
usd_per_kwh         = 0.0       # 0 => energy tracked in Wh, local $ stays 0
remote_usd_per_mtok = 0.0       # $/million-tokens for a remote escalation lane (WS-A16)
budget_usd          = 0.0       # 0 => no $ ceiling (observe only)

[audit]
chain_enable = true   # link each event into the SHA-256 tamper-evidence chain

[worker_tools]
bm25_k1                  = 1.2    # Okapi BM25 term-frequency saturation (standard 1.2-2.0)
bm25_b                   = 0.75   # Okapi BM25 document-length normalisation (standard 0.75)
# Fallback relevance score for an UNEMBEDDED verb, indexed by its _tool_priority rank
# (0 = core read/discovery .. 4 = external MCP/A2A); a rank past the list clamps to last.
priority_fallback_scores = [0.55, 0.45, 0.30, 0.25, 0.15]
tool_priority_core_first = true

[slo]
# Per-class wall-clock deadline budget (seconds the turn SHOULD meet). interactive
# = a human at the keyboard (tight); best_effort = background / autonomous /
# fan-out work that may run long.
interactive_budget_s = 8.0
best_effort_budget_s = 120.0
# Interactive-priority FLOOR: a foreground turn whose scheduling priority is
# clamped below this is downgraded to best_effort (the autonomous-clamp path).
# default_priority is the priority assumed for an unclassified turn -- at/above
# the floor by default so a plain foreground turn classifies interactive.
interactive_priority = 7.0
default_priority     = 7.0

# ----------------------------------------------------------------------------
[sched]
priority_mode = "ssot"
# Urgency tiers (the score is higher = scheduled sooner).
urgency_default = 5
urgency_high    = 9
urgency_low     = 2
# A single-action OS-control dispatch is cheap + interactive -> floor its urgency here.
urgency_dispatch_floor = 8
urgency_high_terms = ["high", "urgent", "now"]
urgency_low_terms  = ["low", "background", "defer"]
# Complexity = min(cap, base + task_count + tool_hint_count // hints_divisor).
complexity_base          = 1
complexity_hints_divisor = 2
complexity_cap           = 10
# Final blend: score = round(complexity*weight + urgency*weight, ndigits).
score_complexity_weight = 0.4
score_urgency_weight    = 0.6
score_round_ndigits     = 2

# ----------------------------------------------------------------------------
[scheduler]
preempt_enable = false
queue_enable = false
# max_suspended -- mirrors PreemptScheduler(max_suspended=...): the BOUNDED number of
# concurrently-snapshotted (preempted) turns. The snapshot-slot free-list IS the cap,
# so a runaway fan-out can never suspend unboundedly; when full the scheduler refuses
# to preempt and the turn runs to completion (bounded-suspension safety).
max_suspended = 4
# quantum_s -- the turn TIME-SLICE in seconds (the mios_preempt.Quantum limit): the
# wall-clock bound on how long the boundary will cooperatively yield to a waiter.
# <= 0 = unbounded (yielding is then bounded only by max_preempt_depth ticks).
quantum_s = 8.0
# slice_tokens -- the token-time QUANTUM (T-020): how many TOKENS a dispatched turn may
# generate before the queue re-evaluates priority at a slice boundary. This is the
# token-time slice (NOT wall-clock); token counts flow through the mios_tokenize seam.
# 0 = slicing OFF (the queue degrades to pure priority ordering; no boundary re-eval).
slice_tokens = 256
# queue_max_turns -- BOUNDED advisory capacity of the TokenSliceQueue: the cap on
# concurrently-tracked turns. The bound evicts only STALE ready bookkeeping (never an
# active turn's coroutine), so the queue can never grow without limit even if a caller
# misses a remove(). 0 = unbounded.
queue_max_turns = 64
# priority_levels -- number of DISCRETE priority buckets a turn's continuous [sched]
# score may be quantised into (0 = continuous priority, the default -- no bucketing).
# Advisory substrate knob the richer policies (T-020/T-058) consume.
priority_levels = 0
# max_preempt_depth -- bound on cooperative-yield ticks per boundary: how many times
# the hook yields the event loop to a waiter before resuming (prevents a busy-wait or
# a pathological preempt/resume cycle). 1 = yield at most once per boundary (default).
max_preempt_depth = 1

[admission]
multiblade_enable = false
tenant_quota_enable = false
# tenant_max_concurrency -- max in-flight global dispatch slots ONE tenant may hold
# while another tenant waits. 0 (DEFAULT) = unlimited (today). The cap BITES only
# under contention; a single tenant on an idle gate is never throttled, and if every
# waiter is one over-cap tenant the gate degrades OPEN (serves by priority) so it can
# never wedge. Only consulted when tenant_quota_enable = true.
tenant_max_concurrency = 0

# ----------------------------------------------------------------------------
[reflect]
# Example negative words/phrases used by the micro-LLM Definition-of-Done judge
# to determine if a response is a punt or refusal (DoD check).
judge_examples = "a punt, refusal, 'I cannot', or 'where to look'"

# ----------------------------------------------------------------------------
# [drift_monitor] -- CONS-02 Jensen-Shannon Goodhart alarm. Observe-only.
# Separate from [drift] above, which registers agent-pipe module wiring
# exceptions and has nothing to do with distribution drift. See manual ch53.
[drift_monitor]
enable      = false   # off by default: observe-only alarm, no behaviour change
threshold   = 0.2     # JSD (0..1, log base 2) at or above which drift_alert fires
window      = 200     # most recent samples per axis that form the live window
min_samples = 30      # live observations required before an axis may alert at all
axes        = ["verdict", "intent"]   # label extractors live in server.py _DRIFT_AXIS_LABELS

# ----------------------------------------------------------------------------
# [schema] -- SHRINK-ONLY dead-table register (check_schema_consumers). See ch55.
[schema]
unconsumed = [
    # WS-SEC (T-151 / ACCT-02): the PostgresOS security schema was declared
    # ahead of its readers. Nothing in the tree mentions these six at all.
    { table = "mios_security.fido2_keys",            reason = "T-151 WS-SEC: declared ahead of the FIDO2 enrolment path" },
    { table = "mios_security.usb_rules",             reason = "T-151 WS-SEC: declared ahead of the USBGuard rule sync" },
    { table = "mios_security.headscale_users",       reason = "T-151 WS-SEC: declared ahead of the headscale control-plane sync" },
    { table = "mios_security.headscale_preauth_keys", reason = "T-151 WS-SEC: declared ahead of the headscale control-plane sync" },
    { table = "mios_security.headscale_acl_rules",   reason = "T-151 WS-SEC: declared ahead of the headscale control-plane sync" },
    { table = "mios_security.keepass_vaults",        reason = "T-151 WS-SEC: declared ahead of the vault integration" },
    # T-246: a SECOND preference table, one letter from the LIVE
    # `account_preference` that materialize-user-config.py actually reads. This
    # one is the trap case -- a writer aimed at the wrong name would look
    # correct and lose every row. Resolve by dropping it or by folding the live
    # table into it; do not leave both.
    { table = "mios_identity.account_preferences",   reason = "T-246: duplicate of the live account_preference; RESOLVE, do not extend" },
    # The person_* graph: person + person_pref have consumers, these two do not.
    { table = "person_device",                       reason = "person graph: declared ahead of any device-enrolment writer" },
    { table = "person_app_install",                  reason = "person graph: declared ahead of any app-inventory writer" },
]

# ----------------------------------------------------------------------------
# [refactor] -- module-size ratchet for the agent-pipe extraction (check 149).
# `oversize` is a SHRINK-ONLY debt register; numbers may only go DOWN. See ch54.
[refactor]
max_lines = 800
oversize = [
    { path = "mios_pipe/federation/a2a.py",        lines = 1457 },
    { path = "mios_pipe/federation/http_caps.py",  lines =  742 },
    { path = "mios_pipe/memory/knowledge.py",      lines =  889 },
    { path = "mios_pipe/routing/agent_call.py",    lines = 1080 },
    { path = "mios_pipe/routing/chat.py",          lines = 1704 },
    { path = "mios_pipe/routing/dag_exec.py",      lines = 1145 },
    { path = "mios_pipe/routing/native_loop.py",   lines = 1166 },
    { path = "mios_pipe/routing/portal.py",        lines = 1609 },
    { path = "mios_pipe/routing/refine.py",        lines = 1066 },
    { path = "mios_pipe/routing/swarm.py",         lines =  993 },
    { path = "mios_pipe/routing/web_research.py",  lines =  936 },
    # Agent-pipe ROOT modules. These sat outside every earlier version of this
    # gate (maxdepth 1, then mios_pipe/ only) -- server.py is the extraction's
    # own remainder and mios_dispatch.py is T-273's named target.
    { path = "mios_dispatch.py",                   lines =  816 },
    { path = "server.py",                          lines = 4866 },
]

# ----------------------------------------------------------------------------
# [shell_session] -- SHELL-01 persistent PTY substrate. See manual ch56.
[shell_session]
enable       = false   # off by default: a long-lived shell is state the agent can accumulate
socket_name  = "mios"  # tmux -L namespace, kept off an operator's default socket
shell        = "/bin/bash"
state_dir    = "/var/lib/mios/shell-sessions"
idle_s       = 1800    # reap a session after this long with no command
max_sessions = 8       # hard cap on concurrent shells
timeout_s    = 120     # per-command wall clock before the runner gives up
max_output_chars = 24000   # ACI-elided, not raw-truncated (head+tail kept)
max_output_lines = 400
history_limit    = 50000  # tmux keeps 2000 lines by default; a long command would lose its HEAD

# ----------------------------------------------------------------------------
# [powershell] -- Windows-side `powershell_run` execution (mios-powershell).
# Object-pipeline flattening and the script/output caps: manual ch57.
[powershell]
flatten           = true    # render the success stream through Out-String
flatten_width     = 200     # column budget the formatter sizes tables against
enumeration_limit = 16      # $FormatEnumerationLimit -- nested items shown before "..."
plain_text        = true    # $PSStyle.OutputRendering: strip ANSI from output and errors
trim_trailing     = true    # drop formatter column padding before the JSON envelope
max_script_bytes  = 65536   # inbound script cap
max_output_bytes  = 262144  # per-stream cap applied after flattening
stage_dir         = '/mnt/c/Users/Public/Documents/mios-ps'   # Windows-visible staging mount

# ----------------------------------------------------------------------------
# [consensus] -- CONS-01 weighted multi-judge Definition-of-Done.
# Fold, abstention rule, quorum floor and weighting: manual ch52.
[consensus]
enable       = false   # off by default: the panel costs one model call per lane per verdict
threshold    = 0.5     # weight-share of "yes" needed to call the answer satisfied
min_lanes    = 2       # live (non-abstaining) votes required before the fold is trusted
timeout_s    = 20.0    # per-lane judge call budget
weight_floor = 0.1     # lowest weight a lane can be reduced to; never zero it out
rrf_k        = 60      # Reciprocal-Rank-Fusion damping when lanes return RANKED lists
# One entry per judge lane. Leave `endpoint`/`model` empty to reuse the refine
# lane's. Declare 2-3; more lanes cost proportionally more per verdict.
lanes = []

# ----------------------------------------------------------------------------
[drift]
# Sibling-module allowlist/denylist: agent-pipe modules that are permitted to have
# transitional unwired (imported-but-dead) status in check_unwired_modules.
denylist = [
    "mios_ctxpack",
    "mios_embed_backfill",
    "mios_provider_translate",
    "mios_smartroute",
    "mios_worker_tools"
]

# ----------------------------------------------------------------------------
[dispatch]
enable     = true
# Default max recursion depth for cross-agent fan-out dispatches (hop budget)
# used as the fallback when the max_dispatch_depth or X-MiOS-Hop header is not set.
default_hop_budget = 2
mode       = "council"
council_default = true
# Floor on engaged agents when relevant matches exist (relevance mode only).
fanout_min = 1
# Hard cap on TOTAL concurrent agents (primary + secondaries). Each extra
# agent is a full concurrent inference on the local lanes, so size it to the
# roster + the box's headroom. 3 = primary + up to 2 council members; raise
# as more [agents.*] are added (and a tool-capable box can carry them).
fanout_max = 3
fanout_select_mode      = "model"
fanout_select_timeout_s = 8       # micro-model relevance-pick timeout (degrade-open)
max_sources          = 8
sources_registry_cap = 64
dag_empty_native_fallback = true
swarm_trust_atomic = true
native_loop_query_reformulate = true
native_loop_date_in_query     = true
native_loop_date_anchor       = true
native_loop_math_hint         = true
dag_node_max_tokens      = 800
dag_node_slow_max_tokens = 350
dag_node_retry           = 1     # empty-result retries (read-only -> safe); 0 = none
dag_node_deadline_s      = 140   # seconds; runaway backstop for a FAST-lane node. Raised 75->140 ("ALL AGENTS USE SGLANG"): with every agent on the ONE SGLang lane (lane_concurrency_gpu=2, the VRAM-safe ceiling), nodes past the 2nd QUEUE for a slot, so the per-node deadline (measured from DISPATCH, not from slot-acquire) must absorb that queue wait + the node's own web-research+gen, or back-of-queue nodes get guillotined at 0 chars (grounded_nodes=0). 140 = ~queue + ~40s research + gen.
dag_node_deadline_slow_s = 150   # seconds; slow-lane (cpu/igpu/mobile) per-node deadline
# WORK-STEAL (deepen) LANES: only these FAST lanes do extra coverage passes until the barrier; a slow lane does its ONE grounded pass and waits (it can't afford a second). CSV of lane categories.
deepen_lanes         = "gpu,accelerator"
# SLOW-lane tool-surface floor: a slow lane with no explicit lane_tool_cap entry gets this many tools (NOT the full 71-tool surface a CPU daemon can't prefill). Only used when a slow node has no injected grounding to reason over. 0 = full surface (opt out).
slow_lane_tool_cap   = 12
stable_tool_prefix   = true
# How many cosine-ranked NON-core tools to append AFTER the stable core block (the only
# part of tools[] that varies per turn). Small = a longer stable cached prefix; large =
# more per-turn tool coverage at the cost of a shorter cache hit. Only used when
# stable_tool_prefix = true.
stable_prefix_tail   = 4
# Inject a user-adjacent "likely-relevant tools" TEXT hint (the relevance signal that
# can't ride the now-byte-stable tools[] order). DEFAULT false: it nudges the 8B toward
# tool-hunting + regressed memory-recall in testing (the cosine TAIL already signals
# relevance inside tools[]). A future logit-mask experiment knob -- leave off until it
# demonstrably helps. Only used when stable_tool_prefix = true.
stable_prefix_hint   = false
tool_rerank          = true
rerank_fanout        = 3     # over-fetch window K = max(fanout*N, rerank_min_k)
rerank_min_k         = 24    # floor on the rerank window so a small tail still has room
rerank_rrf_k         = 60    # reciprocal-rank-fusion constant (same value mios-web-search uses)
rerank_mmr_lambda    = 0.8   # MMR relevance(1.0) vs diversity(0.0) balance, clamped [0,1]; 0.8 held recall (no regression) + kept the rank-1 gain in the offline eval
rerank_skip_margin   = 0.08  # if the cosine cut at the N-th slot is this separated, skip stage-2 (confident, no confusable cluster)
deepen_enabled       = true
swarm_saturate       = true
deepen_iters         = 12     # RUNAWAY backstop on extra passes (the deadline is the real bound)
deepen_deadline_s    = 60     # HARD per-node deepen cap for DEEP turns (casual queries no longer deepen via the complexity gate). 60s/facet is the post-runaway deep budget.
deepen_web_timeout_s = 20     # per-iter web-research cap so one pass can't overshoot
# DETAIL-FILL ("also can loop to gather data in detail-fill passes"): a fast work-stealing node FETCHES new web data each deepen pass (bounded by deepen_web_timeout_s) and appends fresh stories to the shared grounding -> the loop enriches the FACTS, not just re-reasons them. Self-gates to web/news turns (a node carrying a web-capable refined plan). false = reason-only deepen (no new fetches).
deepen_fetch         = true
deepen_early_exit    = false
deepen_judge_timeout_s = 6
agent_concurrency    = 3      # global default per-lane concurrent-agent cap
lane_concurrency     = 3      # default per lane (each distinct hw = its own semaphore)
lane_concurrency_cpu = 2      # in-VM CPU -- cap low to avoid thrash (CPU has no VRAM elasticity)
gpu_profile     = "orchestrator"
vram_budget_mb  = 11000       # agent-lane VRAM ceiling on the shared 4090; the pack firstboot REFUSES to arm if Sigma(worker vram_mb) exceeds this (hard OOM-cascade guard)
lane_concurrency_gpu0 = 4     # per-SUB-LANE cap for the 'gpu0' small-model pack (each worker is its OWN server, so they don't pile on one daemon -- the lane=4 OOM mode is gone). Inert until workers set sub_lane="gpu0".
global_concurrency   = 16     # max concurrently-running agent dispatches across ALL lanes
# ── Runaway / abandoned-turn bounds (cancellation fix). A local llama.cpp lane
# does NOT stop generating on client disconnect, so a hard num_predict cap is the
# only real bound on an abandoned turn; the turn deadline is a wall-clock backstop
# (per-node deepen caps don't bound the whole turn). The disconnect/supersede
# cancellation lives in _execute_dag_emitting + _execute_dag_saturated.
llm_num_predict_cap = 2048   # hard per-generation token ceiling on AGENT dispatches (fast/dGPU lanes)
# PER-LANE cap (runaway fix j): a CPU/iGPU lane gens ~5 tok/s, so 2048
# tokens = ~400s of pegged cores per gen; a disconnect can't abort the gen,
# so stacked slow-lane gens drove loadavg to 387. Give the SLOW lanes a short
# ceiling (~512 ≈ ~100s) so each self-clears fast; the dGPU keeps the full 2048.
llm_num_predict_cap_cpu = 512   # token ceiling on CPU/iGPU (mios-llm-light / host-iGPU) dispatches
turn_deadline_s        = 600    # wall-clock backstop per turn (s); generous so deep research still finishes
runaway_reap           = true   # on a BLOWN deadline (not supersede), unload CPU-lane models (keep_alive:0) -- the only real release for an un-abortable gen
council_max          = 4      # max council SECONDARIES per turn (0 = all; was uncapped)
# SWARM/DAG width cap ("16-agent explosion"): the swarm/DAG
# fan-out had NO cap (council_max only bounds the council path). Bound the swarm
# to at most this many DISTINCT (endpoint,model) targets AFTER de-dup. 0=uncapped.
swarm_max_width      = 3      # "ALL AGENTS USE SGLANG": every agent now targets the ONE SGLang heavy lane, whose VRAM only safely runs lane_concurrency_gpu=2 concurrent 14B gens. Fanning out 8 nodes onto a 2-wide lane just QUEUES 6 of them past their deadline -> all time out (grounded_nodes=0). Size the fan-out to the REAL concurrency: 3 facets (2 run immediately + 1 short queue), each grounded, finishing inside dag_node_deadline_s. Work-steal/deepen still loops the 2-3 live nodes across extra coverage facets until the barrier, so coverage stays broad without a wide cold fan-out. Raise again only when SGLang gets more concurrency headroom (more VRAM / a 2nd engine).
# SLOW-LANE fan-out CEILING (runaway fix j): GPU/fast nodes fan out
# unbounded, but CPU/iGPU lanes are where stacked ~100s gens pile up. Cap how many
# slow-lane nodes a DAG BACKFILLS (primary facets never dropped). Default 2 = the
# live CPU node count (local-cpu + potato-cpu) = no-op today, but bounds a deep/
# research turn that would otherwise multiply CPU replicas across the lanes.
swarm_max_cpu_nodes  = 2      # max CPU/iGPU slow-lane nodes backfilled into one DAG (0 = uncapped)
# endpoint_concurrency RAISED 2->4 (dGPU co-load): backstop
# ceiling for concurrent calls to one daemon; VRAM-aware admission (admit_* +
# VRAM_COLOAD_*) is the real per-turn governor on the dGPU. The CPU lane stays
# protected by its own MemoryMax + the _cap_cpu_lane_model micro-only guard.
endpoint_concurrency = 4      # was 2; backstop, VRAM-admission governs dGPU co-load
# VRAM-aware co-load: admit another concurrent model onto a
# dGPU endpoint only when measured free VRAM fits it + reserve. SHARED-4090 guard:
# reserve leaves margin for the Windows host's VRAM co-tenancy (the VM can't see
# host-side usage). Read by _admit via the MIOS_VRAM_COLOAD_* env defaults.
vram_coload_reserve_mb = 3000   # MB left free for host co-tenancy + spikes
vram_coload_est_mb     = 5000   # assumed cold-model VRAM until /api/ps knows its size
admit_enable         = true   # master switch (ON: the gate for the wide all-nodes roster; was false)
admit_load_ceil      = 56     # 1-min loadavg ceiling (cpu*2 on the 28-core dev VM); over -> backpressure
admit_mem_pct        = 92     # mem% ceiling; over -> backpressure
admit_max_wait       = 8.0    # max seconds a dispatch waits at the gate before admitting anyway (no deadlock)
# ── All-nodes-enabled-by-default scheduling ("all nodes are
# enabled by default ... concurrently dispatched ... nothing idle until synthesis").
# Made safe by admit_enable=true above + COUNCIL_MAX + per-endpoint/lane semaphores.
nodes_research_only  = false  # [nodes.*] default research_only: FALSE = every node eligible on EVERY turn (per-node override wins)
# Lane -> admission PRIORITY (1..9; higher = admitted first / shorter backoff).
# Fast LOCAL lanes high; slow/remote/research lanes low so under host pressure they
# wait out admit_max_wait + self-shed -> the wide roster degrades, never stampedes.
lane_priority        = "gpu:8,cpu:7,accelerator:6,igpu:3,mobile:2,_default:5"
priority_queue_enable  = true   # ON ('everything on' for live MiOS-DEV); degrade-open to plain FIFO on any error
priority_starvation_ms = 4000   # a queued dispatch older than this is served ahead of priority (anti-starvation)
request_cancel_enable  = 1      # 1=cancel non-streaming turns on client disconnect; 0=deadline-only
request_cancel_poll_s  = 2.0    # disconnect-watcher poll interval (seconds)
# Proactively evict an IDLE resident model (refcount 0, non-kept) to free VRAM for
# a cold model a turn NEEDS -- "clear RAM/VRAM for idle agents to be loaded so
# nothing in the pipeline is idle". INERT on the /v1 plane (llama-swap owns model
# residency); the hard semaphores stay the OOM backstop. llama.cpp KV is paged
# separately (kv_paging_*).
vram_reclaim_idle    = false  # DISABLED proactive evict->reload of multi-GB model files thrashed disk (~2GB/s). De-dup + swarm_max_width cut the model churn at the source; let llama-swap's own LRU manage residency.
# CPU-offload of fan-out secondaries: OFF ("concurrent true
# swarm / unfired nodes"). ON funneled EVERY secondary onto the single CPU lane
# (:11435) -> 35-conn wedge while dGPU/potato/iGPU sat idle. OFF = each distinct
# node runs on its OWN hardware concurrently (dGPU co-loads multiple models).
offload_cpu          = false
# ── Tool-surface shaping. Per-lane tool CAP (weak lanes
# like the iGPU llama.cpp time out grammar-constraining all 71 verbs; they get a
# capped REAL subset, never zero) + endpoints that 400 on tool_choice=required.
lane_tool_cap        = "igpu:12,mobile:12"
default_tool_cap     = 16
no_tool_choice_hints = "11436"               # host:port substrings whose llama.cpp rejects tool_choice=required
parallel_tools_hints = "8520,8530"               # host:port substrings whose model reliably emits PARALLEL tool calls -> OpenAI-default parallel_tool_calls=True (capable heavy lane); everything else stays sequential (small models malform parallel calls). MIOS_PARALLEL_TOOLS_HINTS overrides.
worker_mcp_tools     = true                  # surface external MCP client tools to fan-out workers
kv_paging_enable  = true     # master switch; only ACTS on llama.cpp /slots endpoints
kv_paging_hints   = "11436"  # host:port substrings that speak llama.cpp /slots (default the iGPU lane)
kv_paging_slot    = 0        # llama-server slot id the light lane pages (single resident frame)
kv_paging_timeout = 12.0     # seconds for one save/restore POST (best-effort; never fails the turn)
kv_fork_enable       = false  # master switch (only ACTS when kv_paging is on AND the endpoint speaks llama.cpp /slots)
kv_fork_max_branches = 4      # runaway guard: max child KV files one fork request may spawn
kv_gc_enable      = true        # in-process KV-file GC (no-op when kv_slots_dir empty/remote)
kv_gc_interval_s  = 900         # sweep cadence (seconds)
kv_gc_ttl_s       = 86400       # evict a non-active KV file older than this (24h)
kv_gc_max_bytes   = 2000000000  # total KV-file budget; oldest-first eviction over this (~2GB)
kv_slots_dir      = ""          # llama.cpp --slot-save-path IF local to the agent-pipe (else "" -> tmpfiles-only GC)
rr_enable         = false       # master switch (off => _call_agent_complete never routes through _rr_run)
rr_quantum_s      = 8.0         # seconds a dispatch runs before it becomes preemptible
rr_max_suspended  = 4           # bounded snapshot-slot free-list (max concurrently-suspended generations)
rr_slice_tokens   = 512         # tokens per slice; sized so short/medium answers finish in ONE slice (no chunking)
rr_slice_timeout_s = 120.0      # per-slice completion timeout
sandbox_enforce   = false       # off => resolve+record only; on => also wrap opted-in verbs
cua_enable        = false       # off => /v1/computer-use returns a disabled notice
cua_max_steps     = 12          # bound the perceive->act->verify loop (anti-runaway)
# ── WS-A10/A18 persistent peer reputation. The in-process PeerReputation flushes
# its raw counters to the peer_reputation pg table every reputation_flush_s and
# restores them on startup, so a peer's accrued reliability (gossip trust-gates on
# it; the fan-out ranks on it) SURVIVES an agent-pipe restart. No-op when pg isn't
# primary. Env: MIOS_REPUTATION_FLUSH_S.
reputation_flush_s = 300.0      # seconds between reputation->pg flushes (0 = off, in-memory only)
kernel_route      = false       # off => no shadow log; execution always inline (unchanged)
slo_shed_enable   = false       # off => admission never sheds (capacity-only, as before)
batch_enable        = false
batch_interval_s    = 0.05      # window for a NON-native endpoint (seconds)
batch_max_size      = 8         # flush a window at this many pending calls
batch_native_hints  = "8530,8520,8500"  # SGLang / vLLM / llama-swap -> bypass coalescing
trace_enable          = true   # 0/false disables span recording (near-zero cost when off)
trace_max_traces      = 256    # LRU cap on retained traces in the buffer
trace_max_spans_per_trace = 128  # cap on stored spans per trace (further spans counted, not stored)


[council]
diversity_gate      = false
diversity_threshold = 0.92
# aggregator_bypass: when the WHOLE council converges (every pairwise cosine
# similarity exceeds aggregator_bypass_threshold) the expensive final aggregator
# LLM adds nothing -> skip it and ship the highest-confidence (medoid / consensus)
# individual response. Conservative threshold (MOSAIC ref: ~45.7% bypass at
# +0.24pp accuracy). An aggregator_bypass event is logged per bypass.
aggregator_bypass           = false
aggregator_bypass_threshold = 0.95

[web_research]
max_attempts    = 3    # judge-gated re-search attempts (was 5)
passes          = 3    # search->judge->re-search drill passes (was 4)
crawl_timeout_s = 18   # deep crawl4ai/Camoufox fallback per-page cap, seconds (was 45)
link_rank_mode = "heuristic"  # "heuristic" = structural ranker (default); "embed" = opt-in embedding-cosine, degrades OPEN to "heuristic" until an embeddings client is wired
seg_base       = 1    # score per path segment (path depth = article-ness signal)
slug_weight    = 2    # bonus: last path segment is a long hyphenated headline slug
slug_min_len   = 12   # min last-segment length to count as a headline slug
digit_weight   = 1    # bonus: path carries a digit (a date / article id)
anchor_weight  = 2    # bonus: anchor text is long (a real headline)
anchor_min_len = 25   # min anchor-text length to count as a real headline
min_score      = 2    # drop shallow generic / utility links scoring below this
top_n          = 6    # keep at most this many ranked article links

[knowledge]
recall_strict_score = 0.82   # above this cosine a recall is trusted without a shared-topic-anchor token
recall_pref_min_score = 0.50 # lower recall floor for SELF-REFERENTIAL asks (a possessive pronoun present): a "what is MY favorite X" template cosines below the stored statement; set == recall_min_score to disable
rank_outcome        = 0.05   # weight: + if the recalled answer's turn was satisfied, - if unsatisfied
rank_hot            = 0.03   # weight: + if the row is tier=hot (frequently paged in)
rank_access         = 0.02   # weight: * log1p(access_count)
rank_age            = 0.3    # RECENCY-DECAY SWING ("weigh date/time appropriately in an AIOS env"). Bounded MULTIPLIER on the blended score: (1-rank_age) + rank_age*0.5**(age_days/recall_halflife_days). 0.3 -> floor 0.7 (a fully-stale row keeps 70% of its score; freshest ~1.43x edge) per research (Generative-Agents/LangChain-TimeWeighted/Elasticsearch). 0 = inert (cosine-only). Cosine stays dominant; recency only breaks near-ties.
recall_halflife_days = 7.0   # recency half-life: at this row age the decay has removed HALF the rank_age swing. Lower = fresher-biased. age from last_access (refreshed on recall), fallback ts.
hot_threshold       = 5      # access_count at which a recalled row is promoted to tier=hot
store_skip_volatile = true   # anti-stale-recall: skip durable STORE *and* RECALL injection for model-classified VOLATILE turns (refine local_state/news/needs_location -- live system-state, current-events, location-bound). Such a turn is a point-in-time snapshot (cwd, windows, weather, latest-news) that poisons recall if cached; it is re-derived LIVE every turn. Model-classified, NOT a keyword check. false = legacy (cache/recall everything).
evict_enable        = true   # ON ('everything on'); DELETES only >ttl never-recalled neutral rows (hot/satisfied/pinned always kept, batch-capped)
evict_dryrun        = false  # inert when evict_enable=true; set true WITH evict_enable=false for log-only observation
evict_interval_s    = 3600   # seconds between sweeps
evict_ttl_days      = 90     # evict evictable rows not accessed within this many days
evict_max_rows      = 50000  # hard row cap; overflow (lowest-value first) is evicted
evict_min_access    = 1      # only evict rows recalled FEWER than this many times (1 = never recalled)
evict_batch         = 500    # max rows removed per sweep (blast-radius bound)

# ── WS-6 runtime HITL approval gate. 'enable' on by default; 'mode'
# = 'log' (NON-BLOCKING: record + emit a hitl_request event then proceed -- the
# autonomous swarm never deadlocks) or 'gate' (BLOCKING: a scoped verb is refused
# until approved via POST /v1/hitl/approve, then the agent retry passes). 'verbs'
# overrides the gated set (CSV); blank = the built-in high-privilege verb set.
[hitl]
enable = true
mode   = "log"     # log = non-blocking + observable (default); gate = block-until-approved
verbs  = ""        # CSV override of gated verbs; blank = _HIGH_PRIVILEGE_VERBS

[memory]
# CONS/MEM: periodic knowledge-table consolidation. Rows answering the SAME
# normalized question collapse into the newest, with the losers' access and
# recall counters folded in so K-LRU tiering never sees a merge as a reset.
consolidate            = true    # run the sweep at all
consolidate_interval_s = 3600    # seconds between sweeps (first sweep is one interval after boot)
consolidate_max_groups = 200     # duplicate groups merged per sweep; bounds one pass's DB work
n_ctx = 8000
compaction_interval = 20
tool_result_ttl_turns = 5
compaction_threshold_pct = 80
kv_slot_persist = true

# ── WS-6 replayable DAG run-templates. Capture every planned DAG to
# the run_template table (GET /v1/run-templates) keyed by a structural class hash
# -- the determinism foundation. Replay-reuse is a follow-up; capture is additive
# + fire-and-forget so it can never affect a live run.
[run_template]
enable = true
# T-225 REPLAY (the reuse half). Default OFF: replaying the WRONG plan costs
# more than re-planning. Model-free by design -- manual ch61.
replay_enable     = false
replay_threshold  = 0.85   # Jaccard a NON-exact intent match must reach to replay
replay_candidates = 50     # newest templates considered per turn

# ── WS-5 Agent-Computer Interface output normalizer. Head-TAIL
# truncation of tool/terminal results (keep the start + the end, elide the
# middle with an anti-fabrication marker) so the tail's error/exit/result
# survives. Read directly by the agent-pipe (_cap_verb_result).
[aci]
max_lines = 160    # cap lines per tool result (head+tail kept, middle elided); 0 = no line cap
head_frac = 0.6    # fraction of the char/line budget kept as head; the rest is tail

[dci]
flow_enabled = false

[verity]
sentence_abbreviations = [
    "approx.", "Approx.", "e.g.", "i.e.", "vs.", "etc.", "U.S.", "U.K.",
    "a.m.", "p.m.", "No.", "Inc.", "Co.", "Ltd.", "St.", "Mt.",
]
antifab_enable = true
antifab_min_entities = 3
antifab_ground_min = 0.34

[code_mode]
# Read by usr/libexec/mios/mios-codemode-api.py. Declared here because the
# resolver emits MIOS_CODEMODE_* from this table: undeclared, the names the
# code reads were emitted by nothing and the build's var-closure check failed.
socket         = "/run/coderun.sock"
call_timeout_s = 60
enable           = true    # master gate -- code execution ENABLED by default (per operator directive); set false (or an /etc drop-in) to gate off
heavy_lane_only  = true    # P5: code_mode (model authors orchestration python) runs ONLY in the heavy native-loop orchestrator, never a light swarm worker (worker emitting code_mode is refused). MIOS_CODE_MODE_HEAVY_ONLY overrides.
allow_net        = false   # even if a snippet requests net, deploy must also allow it (AND of the two)
max_output_chars = 8000    # cap on snippet stdout/stderr fed back to the model
uid              = 828     # mios-codemode service uid (50-mios-services.conf); pins workspace ownership
gid              = 828
# workspace_root = "/var/lib/mios/codemode"   # default; falls back to [paths].coderun_workspace_root if unset

[refine]
enable           = true
model            = "mios-agent"
endpoint         = "http://localhost:${MIOS_PORT_LLM_LIGHT}"   # mios-llm-light (ALL llama.cpp)
# Timeout headroom: on bare-metal Fedora-bootc with real ROCm/
# CUDA, qwen3:1.7b emits a 400-token JSON in <1s. On WSL2 dev
# (CPU only -- /dev/kfd isn't exposed for ROCm) it takes 15-30s
# for the same call. 45s is generous for either deployment;
# the bypass path still keeps trivial input fast.
timeout_seconds  = 45
# Token budget for the refine JSON answer. qwen3 reasoning models
# can consume ~200 tokens on chain-of-thought even with /no_think
# in the prompt; 1200 leaves plenty of room for both. On a non-
# reasoning model this could drop to ~300.
max_tokens       = 1200
# Trivial-input shortcut: when the user message is shorter than
# this many chars AND matches one of the bypass patterns
# (greetings, single-word commands), skip refine and emit a
# minimal plan inline. Pure-bash latency on the fast path.
bypass_chars     = 24
chat_chars             = 40
dispatch_chars         = 60
promote_chars          = 100
dispatch_arg_max_words = 3

[planner]
short_prompt_chars = 60   # below this char count, skip the planner
short_prompt_words = 10   # ...AND at or below this whitespace-token count

[polish]
enable           = true
model            = "mios-agent"
endpoint         = "http://localhost:${MIOS_PORT_LLM_LIGHT}"   # mios-llm-light (ALL llama.cpp)
# Same CPU-vs-GPU headroom rationale as [refine].timeout_seconds.
timeout_seconds  = 45
max_tokens       = 800

[passport]
enable           = true
algo             = "ed25519"
key_dir          = "/var/lib/mios/agent-passports"
rotate_days      = 365
verify_on_read   = false
# The agent identities mios-passport-provision.service enrolls at
# firstboot. Order matters only for log readability; the actual
# enrollment is parallel-idempotent.
agents = [
    "agent-pipe",
    "hermes",
    "mios-daemon",
    "opencode",
    "cron-director",
]

[daemon]
# 1-minute load-average ceiling. Above this, autonomous daemon work is held
# until load drops back under the ceiling. Sized GENEROUSLY (per-core ~1.0 is
# "busy but fine"); raise on a many-core box. Float allowed.
pressure_load_ceil      = 8.0
# GPU utilisation ceiling (percent 0-100). Above this, autonomous work that
# would touch a local inference lane is deferred so it doesn't queue behind a
# live interactive generation on the shared 4090. 90 = leave headroom for one
# in-flight turn.
pressure_gpu_util_ceil  = 90
# Master bypass for the whole back-pressure governor. false (default) = the
# governor is ACTIVE (autonomous work respects the ceilings above). true =
# legacy behaviour, autonomous work always runs regardless of load/GPU. Ships
# false but the generous ceilings keep normal use unaffected.
pressure_skip           = false
classify_dedup_s        = 600
# Hard cap on CONCURRENT cron-director-launched autonomous jobs. 1 = serialize
# scheduled autonomous work (one recurring task at a time) so a backlog can't
# fan out into a thundering herd of mios-ai runs. Raise only on a box with
# spare lanes.
cron_max_concurrent     = 1
# Limits for scheduled/cron classify and refusal loops to prevent runaway LLM calls
classify_limit_per_min  = 10
refusal_limit_per_min   = 20
# CALM-loop max tick (seconds): upper bound on a single autonomous daemon
# tick's wall-clock before it yields back to the scheduler, so one long
# autonomous unit can't monopolise the daemon. Generous so normal ticks finish
# naturally; this is a runaway backstop.
calm_max_tick_s         = 300
# Quiescence window (minutes): the daemon only initiates *new* autonomous,
# self-directed work after the system has been interactively idle for this
# long, so autonomy happens in the quiet gaps, not mid-conversation. Reactive
# (log-follow / followup) work is NOT gated by this -- only self-initiated.
quiescence_window_min   = 10
# Escalation cooldown (seconds): minimum gap between two escalations of the
# SAME concern (e.g. re-notifying / re-acting on a persistent condition) so a
# stuck state can't spam. 1800 = 30 min.
escalation_cooldown_s   = 1800
# Max escalation attempts for one concern before the daemon gives up and just
# logs (no infinite retry loop). After this many attempts within the cooldown
# cadence the concern is parked.
escalation_max_attempts = 3
launch_claim_detect     = "model"
refusal_detect          = "model"

[budget]
# Per-conversation cumulative token ceiling (prompt+completion across the whole
# turn's fan-out). Generous -- a deep council turn with web grounding stays
# well under this; it only catches a pathological loop. 0 = unlimited (off).
conversation_token_ceil = 2000000
# Per-AUTONOMOUS-run token ceiling (a daemon/cron self-initiated unit). Tighter
# than the conversation ceiling because autonomous work is unattended. 0 = off.
autonomous_token_ceil   = 400000
# Max concurrent AUTONOMOUS (self-initiated) inference runs in flight at once,
# across the whole daemon/cron plane. Bounds unattended fan-out so it can't
# saturate the lanes; interactive turns are NOT counted against this. 1 = fully
# serialized autonomy.
autonomous_max_inflight = 1
# Rolling window (seconds) over which the budget ceilings above are measured /
# reset. 3600 = per-hour accounting.
window_s                = 3600

[dispatch.autonomy]
autonomous_priority = "low"
# Max recursion depth for dispatch -> sub-dispatch chains (an agent dispatching
# to a peer that dispatches again). 2 = primary -> sub-agent -> one more hop,
# then no further nesting. Bounds runaway delegation fan-out.
max_dispatch_depth  = 2

[daemon.post_check]
open_app         = "window_visible"
open_url         = "window_visible"
focus_window     = "window_visible"
launch_app       = "window_visible"
text_create      = "file_exists"
text_str_replace = "file_nonempty"
flatpak_install  = "flatpak_installed"

[daemon.index]
enable           = true
interval_min     = 15
max_depth        = 6      # how deep to descend per root
max_entries      = 50000  # safety cap across all roots
summary_max_bytes = 240   # first N bytes of text files = summary

# Each entry: label (operator-friendly tag), path (filesystem),
# optional excludes (glob list -- skips matching basenames).
[[daemon.index.roots]]
label    = "mios-vendor"
path     = "/usr/share/mios"
excludes = ["__pycache__", "*.pyc"]

[[daemon.index.roots]]
label    = "mios-vendor-bin"
path     = "/usr/libexec/mios"

[[daemon.index.roots]]
label    = "mios-config"
path     = "/etc/mios"

[[daemon.index.roots]]
label    = "mios-state"
path     = "/var/lib/mios"
excludes = ["sessions", "vector_db", "models", "*.db", "*.db-*",
            "__pycache__", "cache"]

[[daemon.index.roots]]
label    = "operator-home"
path     = "/var/home"     # walks /var/home/$MIOS_USER and below
excludes = [".cache", ".local/share/Trash", "node_modules",
            ".git", "__pycache__"]

[[daemon.index.roots]]
label    = "windows-home"
path     = "/mnt/c/Users"   # walks Windows user homes
max_depth = 4               # shallow on Windows side
excludes = ["AppData", ".cache", "node_modules", ".git",
            "OneDrive*", "Cookies"]


[os_control]
default_position    = "center"   # placement for position="default" launches
default_monitor     = 0          # 0 = primary monitor
tile_gap_px         = 8          # gap between tiled / side-by-side windows
edge_margin_px      = 0          # inset from screen edges for edge placements
region_snap_halves  = true
restore_if_running  = true       # preflight: focus an existing instance instead of relaunching
focus_after_launch  = true       # raise + foreground a window after a successful launch
launch_category_priority = ["linux-flatpak", "linux-rpm-gui", "linux-cli", "windows-app", "windows-gui"]

executor_endpoint   = ""         # e.g. in /etc/mios: "http://100.x.y.z:8950"

  [os_control.nodes.igpu]
  endpoint = ""    # e.g. in /etc/mios: endpoint = "http://100.x.y.z:8950"
  desc     = "iGPU Windows host -- launch + verify on its own desktop over the tailnet (also runs mios-igpu-server.ps1 for inference)."

[computer_use]
verify_after_act = true
# Federation: when set + reachable, every cu_* op routes to this remote desktop's
# mios-computer-use-server (same HTTP contract as the Windows mios-oscontrol-
# server.ps1). EMPTY in vendor (no personal tailnet host in the public repo);
# set the real tailnet URL in your /etc/mios overlay. Configured-but-down =>
# honest error, NEVER a blind local fall-back.
executor_endpoint  = ""          # e.g. in /etc/mios: "http://100.x.y.z:11438"
input_backend      = "auto"      # auto | portal (GNOME/KDE, sandboxable) | uinput (compositor-agnostic)
capture_backend    = "auto"      # auto | portal | grim | gnome | spectacle
# Multiply grounded coordinates by this on a HiDPI seat (1.0 = no scaling).
# Declared as a float so the float() on the consumer side is a no-op.
hidpi_scale_factor = 1.0
restore_token_path = ""          # default: $XDG_STATE_HOME/mios/cu-restore-token (skips the portal consent dialog on re-use)
grounding_model    = ""          # vision grounding tag; empty => mios-pc-vision default (qwen3-vl:4b on mios-llm-light) or the vLLM mios-grounding lane
require_approval   = true        # write-class ops (click/type/key) go through the DoD/approval gate; read-class (screenshot/window-list/ground) auto
# mios-computer-use-server (the dual MCP+A2A node server) bind. Loopback-only in
# vendor; set bind_address to the tailnet IP in your /etc overlay + firewall the
# port to the tailnet to let the central pipe consume this desktop remotely.
enable             = true        # documents intent; the systemd --user unit + graphical-session.target are the real gate
server_port        = 11438       # MCP /mcp + A2A /.well-known/agent-card.json + /a2a + executor /input/* all on this one port
bind_address       = "127.0.0.1" # "" disables the server; set the tailnet IP for federation

docgen_enable      = true        # MASTER GATE for mios-docgen (pandoc+libreoffice installed + verified live; everything-on)
docgen_pandoc      = "pandoc"    # pandoc binary or absolute path (markdown <-> docx/pptx/html/pdf)
docgen_soffice     = "soffice"   # LibreOffice headless binary or path (office-binary + csv->xlsx + pdf)
docgen_pdf_engine  = ""           # pandoc --pdf-engine; empty => route PDF through LibreOffice (no LaTeX needed)
docgen_timeout_s   = 120         # per-conversion timeout (LibreOffice cold start can be slow)
docgen_max_bytes   = 20000000    # refuse inputs/content larger than this (DoS guard)

  [computer_use.nodes.workstation]
  endpoint = ""    # e.g. in /etc/mios: endpoint = "http://100.x.y.z:11438"
  desc     = "A second MiOS/Linux desktop -- screenshot + click + type + verify on its own Wayland session over the tailnet."

# ─── Observability (OpenTelemetry GenAI Spans, T-023) ──────────────────
# Gates OpenTelemetry instrumentation in the agent-pipe. When enabled,
# traces and spans are exported to the OTLP collector.
[observability]
otel_enable   = false
otel_endpoint = "http://localhost:${MIOS_PORT_OTELCOL_OTLP}"
record_mode   = false
replay_mode   = false
debug         = true
surface_default = "clean"   # clean = reasoning-channel; "inline" only via per-surface opt-in

[observability.channels]                 # which schema event → which SSE channel
  thinking = "reasoning"
  plan = "reasoning"
  tool_call = "status+reasoning"
  tool_result = "reasoning"
  source = "source"
  content = "content"


[gossip]
# WS-A18 epidemic peer discovery (anti-entropy over the A2A peer set). DEFAULT
# OFF: interval_min = 0 -> no loop is spawned (single-box MiOS unaffected). When
# >0, each round PULLs `fanout` known peers' /v1/peers digests and merges them
# TRUST-GATED (a peer rumor is accepted only when its reputation score >=
# min_trust), so a rogue/low-rep peer can't inject itself into the federation.
interval_min = 0    # minutes between gossip rounds (0 = disabled)
fanout       = 3    # peers contacted per round (seeded -> coverage rotates)
min_trust    = 0.0  # reject peer rumors below this reputation score (0 = accept all)

[a2a]
# A2A protocol version the published AgentCard + JSON-RPC surface conform to. SSOT
# for mios_a2a.A2A_PROTOCOL_VERSION (env MIOS_A2A_PROTOCOL_VERSION overrides). v1.0
# moved this into each AgentCard supportedInterfaces[] entry; it is a protocol
# version token, declared once here, never restated in code.
protocol_version = "1.0"
discover_port = 8700
public_domain = ""
nodes = []          # e.g. in /etc/mios: nodes = ["http://172.20.0.5:8700", "http://192.168.1.40:8700"]
discover_cidr = ""  # e.g. "192.168.1.0/24" -> auto-sweep a LAN segment (bounded to 256 hosts)
# SWARM Phase-4: opt discovered A2A peers INTO the concurrent
# swarm fan-out so remote MiOS nodes do individual subtasks in parallel. Default
# false = peers stay explicit-delegation-only (no self-loop overhead). self_id is
# the local pipe's own peer id, ALWAYS excluded from fan-out so it never calls itself.
council = false
self_id = "local-mios"
route_on_card_skills = false

mdns_discovery     = false              # browse the LAN for A2A peers (default OFF = inert)
mdns_service_type  = "_mios-a2a._tcp"   # DNS-SD service type shared by advertise + browse
mdns_advertise     = false              # announce THIS node's A2A endpoint via avahi (default OFF)
mdns_refresh_sec   = 300                # minimum seconds between live mDNS browses (throttle floor)

[mcp]
protocol_version = "2025-11-25"

[routing]
router_enable = true   # master switch; false = legacy full-surface behaviour

# Trailing courtesy/location phrases the agent-pipe fast-path (_deterministic_action_
# route) STRIPS from a launch target so the app name is clean and the launch stays
# deterministic: "open notepad for me" -> open_app(name="notepad"); "open spotify on
# my desktop" -> name="spotify". SSOT (no hardcoded English in code); stripped
# longest-match-first from the END of the target. Add phrases your launches pick up.
launch_filler_phrases = [
  "for me please", "on my desktop", "on the desktop", "right now", "real quick",
  "thank you", "for me", "please", "thanks", "now",
]

launch_target_lead_phrases = ["the", "a", "an", "my"]

# Trailing generic nouns dropped from a launch target so "open the calculator app"
# -> name="calculator", "open the photos application" -> name="photos". Single
# words, stripped from the END while the trailing word matches (word-boundary, so
# "whatsapp" is never truncated). Add nouns your phrasings pick up.
launch_target_trail_phrases = ["application", "program", "app", "window"]

remember_trigger_phrases = ["remember", "note", "save", "keep in mind", "don't forget", "make a note"]
# web_search: a TRIGGER verb phrase co-occurring (within ~40 chars) with a WEB
# CONTEXT phrase -> set web=true (e.g. "search ... online", "look up ... on the web").
web_search_trigger_phrases = ["search", "look up", "google", "find", "search the web", "search online"]
web_search_trigger_contexts = ["web", "internet", "online"]
location_sensitive_phrases = ["weather", "forecast", "near me", "nearby", "near here", "around here", "local news", "local", "my area", "things to do", "restaurants", "closest", "directions to"]
browser_action_verbs = ["quote", "read", "tell", "summarise", "summarize", "what is", "what does", "what say", "what says", "first sentence", "the content", "browse", "extract", "scrape", "headline", "article", "say"]
compound_conjunctions = ["and", "then"]
compound_actions = ["type", "write", "enter", "input", "paste", "put"]
# Two-target connectives: a bare 'launch <app>' that still carries one of these
# (a preposition/conjunction joining a SECOND target, e.g. 'open X in Y') is a
# COMPOUND phrase the deterministic open_app fast-path must DECLINE, so the LLM
# router splits content from target. SSOT (was an inline English regex in
# routing.py -- NO hardcoded keyword gate in code). Empty => guard degrades open.
compound_connectives = ["in", "and", "then", "with", "on", "to"]
model_modalities_embeddings = ["embed", "bert", "text-embedding", "bge"]
model_modalities_image      = ["diffuse", "flux", "dall", "midjourney", "sd"]
integer_param_keywords = ["limit", "count", "timeout", "port", "every", "concurrency", "maxsize"]
boolean_param_keywords = ["enable", "force", "success", "active", "dryrun"]

prefilter_conversational_bypass_mode = "model"
prefilter_classify_timeout_s = 6

launch_followup_phrases = [
  "didn't launch", "did not launch", "didn't open", "did not open",
  "didn't start", "did not start", "didn't come up", "did not come up",
  "didn't work", "did not work", "wouldn't open", "would not open",
  "no window", "nothing happened", "nothing opened", "never opened",
  "never launched", "not opening", "not launching", "isn't open",
  "is not open", "isn't running", "is not running", "won't open",
  "won't launch", "doesn't open", "does not open", "failed to open",
  "failed to launch",
]
# launch_retry_phrases -> an explicit ask to try again / launch+verify ("try
# again", "attempt to launch and verify", "launch and verify", "open it
# again", "retry"). Re-fires the launch+verify against the persisted last app.
launch_retry_phrases = [
  "attempt to launch and verify", "launch and verify", "launch it and verify",
  "try to launch and verify", "open and verify", "open it and verify",
  "try launching it again", "try launching again", "try opening it again",
  "try opening again", "launch it again", "open it again", "start it again",
  "run it again", "try again", "attempt again", "retry", "relaunch",
  "re-launch", "reopen", "re-open", "try once more", "one more time",
  "attempt to launch", "attempt the launch", "verify the launch",
  "launch and confirm", "open and confirm",
]

[routing.domains.web]
desc  = "LOOK UP INFORMATION on the internet: current events, news, trending topics, weather, prices, world facts. NOT for performing an action in an app, sending/posting/sharing a message, or controlling the screen."
verbs = ["web_search", "web_scrape", "crawl", "web_extract"]

[routing.domains.files]
desc  = "find, read, or edit LOCAL files on this machine"
verbs = ["everything_search", "text_view", "list_dir"]

[routing.domains.system]
desc  = "this machine's OS / version / kernel / whether WSL2 / the Windows host, plus state, services, processes, containers, logs, environment"
verbs = ["system_status", "sys_env", "sys_env_refresh", "system_logs", "service_status", "service_restart", "process_list", "container_status", "container_restart"]

[routing.domains.apps_windows]
desc  = "list or launch applications and manage on-screen windows"
verbs = ["open_app", "close_app", "open_url", "verify_launch", "window_state", "focus_window", "move_window", "position_window", "resize_window", "list_windows", "screen_layout", "mios_apps", "app_search", "mios_find"]

[routing.domains.packages]
desc  = "install, update, remove, or query software packages (dnf/winget/flatpak)"
verbs = ["pkg", "winget_search", "winget_list", "winget_show", "winget_install", "winget_upgrade", "winget_uninstall", "flatpak_search", "flatpak_list", "flatpak_show", "flatpak_install", "flatpak_upgrade", "flatpak_uninstall", "flatpak_preflight"]

[routing.domains.memory]
desc  = "recall or store the agent's own memory / knowledge / summaries"
verbs = ["knowledge_search", "remember", "recall", "ingest", "summarize"]

[routing.domains.computer_use]
desc  = "GUI pixel/element control of the screen via mouse and keyboard"
verbs = ["cu_screenshot", "cu_ground", "cu_atspi_query", "cu_window_list", "cu_click", "cu_type", "cu_key", "cu_key_combo", "pc_screenshot", "pc_type", "pc_key", "pc_click", "pc_find_element", "pc_click_element", "pc_list_elements"]

[routing.domains.code_shell]
desc  = "run code, shell, or PowerShell commands (incl. explaining what a script/code does)"
verbs = ["coderun", "code_mode", "os_recipe", "powershell_run"]

[routing.domains.agents_comms]
desc  = "PERFORM a communication or automation ACTION: send / post / share / DM a message (to a chat, channel, or person), delegate or hand a sub-task to another agent, schedule a recurring task, or generate a document"
verbs = ["handoff", "a2a_delegate", "discord_send", "schedule", "docgen_build", "docgen_convert", "tool_search"]

# ─── Verbs Default Policy (SSOT) ──────────────────────────────────────
# Default values applied to all [verbs.<name>] blocks. Introducing this
# block allows us to prune redundant keys across the 100+ individual verbs,
# keeping the catalog clean.
[verbs._defaults]
tier             = "common"
permission       = "read"
max_result_chars = 0
parallel_limit   = 0
hidden           = false


# MiOS-DEV self-development: apply the war-room / code-server workspace edits
# DIRECTLY onto the deployed dev root so they go live for testing without a GitHub
# round-trip. GATED to a MUTABLE MiOS-DEV root (an immutable bootc root is refused;
# ship those via bootc upgrade). Syncs only git-visible files, never deletes. The
# executable enforces every gate -- see usr/libexec/mios/mios-sync-to-root.
[verbs.sync_to_root]
section    = "MiOS-DEV / self-development"
sig        = 'mode="apply"?'
desc       = "Apply the code-server/war-room workspace edits DIRECTLY onto the deployed MiOS-DEV root so they go LIVE for testing (no GitHub round-trip). Only works on a mutable MiOS-DEV deploy; an immutable bootc root is refused. Use mode=check (or dry-run) to preview what would change without touching anything, then mode=apply to make it live."
examples   = ["apply my code-server changes to the dev root", "sync the war-room workspace live for testing", "push these edits into MiOS-DEV so I can test them", "dry-run what a sync-to-root would change"]
tier       = "rare"
permission = "write"
cmd        = "mios-sync-to-root {mode=apply}"
  [verbs.sync_to_root.params.mode]
  type    = "string"
  desc    = "apply = make the workspace edits live on the dev root; check/dry-run = preview only, touch nothing."
  enum    = ["apply", "check", "dry-run"]
  default = "apply"
  aliases = ["action", "op"]

[verbs.open_app]
hidden = true
section    = "Window / app launch"
sig        = 'name, position="default"?, args?, monitor=0?'
desc       = "LAUNCH any app/program by name (resolves Windows AND Linux/flatpak apps) and optionally place its window (position center/left/right/top-left..., monitor index) or pass CLI args. THE one launch verb -- use for any 'open / launch / start / run X'. After launching, confirm with verify_launch."
model_name = "launch_windows_app"
hidden_aliases = ["launch_app", "launch_verified", "launch_and_verify_app"]  # consolidated INTO open_app; resolve here, never re-shown on the model surface
examples   = ["open Firefox on my second monitor", "launch Spotify on the left half of the screen", "start Notepad with this file and put it top-left", "run Discord maximized on monitor 1"]
tier = "rare"
permission = "write"
conflict_group = "desktop_ui"  # WS-A7: one foreground window + keyboard -- serialize vs focus_window/pc_type so a fan-out can't race them
cmd        = "MIOS_LAUNCH_POSITION={position=default} mios-launch {name!}"
cmd_args   = "MIOS_LAUNCH_POSITION={position=default} mios-windows launch {name!}{args*}"
  [verbs.open_app.params.name]
  type    = "string"
  desc    = "App display name OR full path. Paths get basename-extracted before launch."
  aliases = ["query", "title", "app", "target", "program", "path", "file", "binary", "exe"]
  [verbs.open_app.params.position]
  type    = "string"
  desc    = "Semantic placement after launch."
  enum    = ["default", "as-is", "center", "left", "right", "top", "bottom", "top-left", "top-right", "bottom-left", "bottom-right", "maximize"]
  default = "default"
  [verbs.open_app.params.args]
  type    = "array"
  desc    = "Extra positional args passed to the launcher target."
  [verbs.open_app.params.monitor]
  type    = "integer"
  desc    = "Monitor index (0=primary)."
  default = 0

[verbs.verify_launch]
hidden = true
section    = "Window / app launch"
sig        = "app"
desc       = "CHECK whether an app/window ACTUALLY launched, read-only, in-turn (delegates to the daemon-agent's Definition-of-Done window check). Call with the SAME name AFTER you fired launch_app / launch_windows_app / focus_window to confirm before claiming success. This only CHECKS -- it does NOT launch; to launch and confirm in one go use launch_and_verify_app."
examples   = ["did Firefox actually open?", "check whether Spotify launched", "confirm the Discord window came up", "is the app I just started actually running?"]
tier = "rare"
cmd        = "mios-verify-launch {app}"
  [verbs.verify_launch.params.app]
  type    = "string"
  desc    = "App/window name that was supposedly launched (use the SAME name you passed to open_app)."
  aliases = ["name", "query", "title", "target", "program", "window", "path", "exe"]

[verbs.focus_window]
hidden = true
section    = "Window / app launch"
sig        = 'title, position="default"?'
desc       = "Raise an already-OPEN Windows window to the FRONT (by title) and optionally re-position it (same placement enum as launch_windows_app). Use to switch to / bring forward / activate a window that already exists -- it does NOT launch anything. Run list_windows first to confirm the title."
examples   = ["bring Firefox to the front", "switch to the Spotify window", "focus the Discord window and move it to the left", "activate the Notepad window"]
tier = "rare"
permission = "write"
conflict_group = "desktop_ui"  # WS-A7: serialize vs open_app/pc_type (shared foreground-window focus)
cmd            = "mios-window focus {title}"
cmd_positioned = "mios-window focus {title} && MIOS_LAUNCH_POSITION={position} mios-window {position} {title}"
  [verbs.focus_window.params.title]
  type    = "string"
  desc    = "Window title pattern (substring match)."
  aliases = ["name", "window", "app"]
  [verbs.focus_window.params.position]
  type    = "string"
  enum    = ["default", "as-is", "center", "left", "right", "top", "bottom", "top-left", "top-right", "bottom-left", "bottom-right", "maximize"]
  default = "default"

[verbs.close_app]
section    = "Window / app launch"
sig        = "title"
desc       = "Gracefully CLOSE an open Windows application or window (by title pattern). Use this when the operator asks to close, quit, exit, or kill an application."
examples   = ["close Firefox", "quit Spotify", "exit Discord"]
permission = "write"
cmd        = "mios-window close {title}"
  [verbs.close_app.params.title]
  type    = "string"
  desc    = "Window title pattern (substring match)."
  aliases = ["name", "window", "app"]

[verbs.move_window]
hidden = true
section    = "Window / app launch"
sig        = "title, position, monitor=0?"
desc       = "Move an OPEN Windows window to a SEMANTIC region: center, a half of the screen (left-half / right-half / top-half / bottom-half), a corner (top-left / ...), or maximize -- optionally on a chosen monitor. Half/corner rectangles are derived live from the monitor's work area. Use when the operator names a place ('to the left half', 'center it', 'snap it right') rather than exact coordinates. For literal pixel (x,y) use place_window_at_pixel; to change size use resize_window."
model_name = "move_window_to_region"
examples   = ["move Firefox to the right half of the screen", "center the Spotify window", "put Notepad in the top-left corner", "snap Discord to the left half on monitor 2"]
tier = "rare"
permission = "write"
cmd        = "mios-window {position=center} {title} {monitor?--monitor}"
  [verbs.move_window.params.title]
  type = "string"
  aliases = ["name", "window"]
  [verbs.move_window.params.position]
  type = "string"
  aliases = ["region", "snap"]
  enum = ["center", "maximize", "left-half", "right-half", "top-half", "bottom-half", "left", "right", "top", "bottom", "top-left", "top-right", "bottom-left", "bottom-right"]
  [verbs.move_window.params.monitor]
  type = "integer"
  default = 0

[verbs.position_window]
hidden = true
section    = "Window / app launch"
sig        = "title, x, y"
desc       = "Move an OPEN Windows window to LITERAL pixel coordinates (x, y). Use only when you have exact coords -- call screen_layout first to learn monitor geometry/resolution. For named regions (center/left/right) use move_window_to_region; to change size use resize_window."
model_name = "place_window_at_pixel"
examples   = ["move the window to pixel 100, 200", "position Firefox at coordinates 0,0", "place the window at x=1920 y=0 on the second monitor", "put Notepad exactly at 500, 300"]
tier = "rare"
permission = "write"
cmd        = "mios-window move {title} {x=0} {y=0}"
  [verbs.position_window.params.title]
  type = "string"
  aliases = ["name", "window"]
  [verbs.position_window.params.x]
  type = "integer"
  [verbs.position_window.params.y]
  type = "integer"

[verbs.resize_window]
hidden = true
section    = "Window / app launch"
sig        = "title, width, height"
desc       = "Resize an OPEN Windows window to a literal pixel size (width x height). This ONLY changes size -- it does NOT move the window. To relocate use move_window_to_region (named region) or place_window_at_pixel (exact x,y); to fill the monitor use maximize_window."
examples   = ["resize Firefox to 1280 by 720", "make the Notepad window 800x600", "set the Spotify window width to 1000 and height to 700", "shrink the Discord window to 640 by 480"]
tier = "rare"
permission = "write"
cmd        = "mios-window resize {title} {width} {height}"
  [verbs.resize_window.params.title]
  type = "string"
  aliases = ["name", "window"]
  [verbs.resize_window.params.width]
  type = "integer"
  aliases = ["w"]
  [verbs.resize_window.params.height]
  type = "integer"
  aliases = ["h"]

[verbs.minimize_window]
section    = "Window / app launch"
sig        = "title"
desc       = "Minimize an OPEN Windows window -- hide it to the taskbar without closing it. To bring it back use restore_window; to actually close it use close_window."
examples   = ["minimize the Firefox window", "hide Spotify to the taskbar", "minimize Notepad", "send the Discord window to the taskbar"]
tier = "rare"
permission = "write"
hidden     = true   # consolidated into [verbs.window_state]; kept parsed + dispatchable
cmd        = "mios-window minimize {title}"
  [verbs.minimize_window.params.title]
  type = "string"
  aliases = ["name", "window"]

[verbs.maximize_window]
section    = "Window / app launch"
sig        = "title"
desc       = "Maximize an OPEN Windows window to fill its containing monitor. To restore it back to normal size use restore_window; for a specific pixel size use resize_window."
examples   = ["maximize the Firefox window", "make Spotify fill the screen", "maximize Notepad", "blow up the Discord window to full screen"]
tier = "rare"
permission = "write"
hidden     = true   # consolidated into [verbs.window_state]; kept parsed + dispatchable
cmd        = "mios-window maximize {title}"
  [verbs.maximize_window.params.title]
  type = "string"
  aliases = ["name", "window"]

[verbs.restore_window]
section    = "Window / app launch"
sig        = "title"
desc       = "Restore an OPEN Windows window from a minimized OR maximized state back to its last normal-size geometry. Use to 'un-minimize' / 'un-maximize' / bring back a window. Pairs with minimize_window and maximize_window."
examples   = ["restore the Firefox window", "un-minimize Spotify", "bring the Notepad window back to normal size", "un-maximize the Discord window"]
tier = "rare"
permission = "write"
hidden     = true   # consolidated into [verbs.window_state]; kept parsed + dispatchable
cmd        = "mios-window restore {title}"
  [verbs.restore_window.params.title]
  type = "string"
  aliases = ["name", "window"]

[verbs.close_window]
section    = "Window / app launch"
sig        = 'title, mode="graceful"?'
desc       = "Gracefully CLOSE an OPEN Windows window by title (sends WM_CLOSE so the app saves state). Force-kill is intentionally unsupported. Use to close / quit / dismiss a window -- not to hide it (use minimize_window to send it to the taskbar)."
examples   = ["close the Firefox window", "quit Notepad", "close the Spotify window", "dismiss the dialog window"]
tier = "rare"
permission = "write"
hidden     = true   # consolidated into [verbs.window_state]; kept parsed + dispatchable (mode was never in cmd)
cmd        = "mios-window close {title}"
  [verbs.close_window.params.title]
  type = "string"
  aliases = ["name", "window", "app"]
  [verbs.close_window.params.mode]
  type = "string"
  enum = ["graceful", "force"]
  default = "graceful"

[verbs.window_state]
hidden = true
section    = "Window / app launch"
sig        = "action, title"
desc       = "Change the STATE of an OPEN Windows window (by title): minimize (hide to taskbar), maximize (fill its monitor), restore (back to normal size), or close (graceful WM_CLOSE so the app saves state). Run list_windows first to confirm the title. This is state only -- to bring a window to the front use focus_window, to relocate use move_window_to_region / place_window_at_pixel, to change size use resize_window."
examples   = ["minimize the Firefox window", "maximize Spotify", "restore the Notepad window", "close the Discord window", "send Slack to the taskbar"]
tier = "rare"
permission = "write"
cmd        = "mios-window {action} {title}"
  [verbs.window_state.params.action]
  type = "string"
  enum = ["minimize", "maximize", "restore", "close"]
  desc = "The window-state change to apply."
  aliases = ["op", "state"]
  [verbs.window_state.params.title]
  type = "string"
  desc = "Window title pattern (substring match)."
  aliases = ["name", "window", "app"]

[verbs.list_windows]
hidden = true
section    = "Window / app launch"
sig        = ""
desc       = "Enumerate the currently OPEN Windows-desktop windows (titles). Run this BEFORE focus/move/close to confirm the target window exists. NOT the installed-app inventory (use list_installed_apps) and NOT the Linux/Wayland desktop (that is linux_desktop_window_list)."
examples   = ["what windows are open right now?", "list my open windows", "show me the open application windows", "which windows are currently on screen?"]
tier = "rare"
cmd        = "mios-pc-control window-list --json"
# Fit a busy desktop's whole window list (the grounding for "what's open"). The
# old 1500-char default cut it mid-window so the model FABRICATED the omitted
# windows' PIDs/titles. _cap_verb_result still flags any
# overflow with a ⟪TRUNCATED⟫ marker the model must not invent past.
max_result_chars = 6000

[verbs.screen_layout]
section    = "Window / app launch"
sig        = ""
desc       = "Report Windows MONITOR geometry: each display's resolution, position, and which is primary. Use BEFORE place_window_at_pixel so you know valid coordinates. This describes the SCREENS/monitors, not the open windows (use list_windows for those)."
examples   = ["what's my monitor layout?", "what resolution are my screens?", "how many monitors do I have and where are they?", "which display is my primary monitor?"]
cmd        = "mios-pc-control screen-layout"

[verbs.open_url]
section    = "Window / app launch"
sig        = "url, mode?, browser?, private?, profile?, reuse_instance?"
desc       = "SHOW a web page to the operator in their VISIBLE browser. By DEFAULT opens the URL as a NEW TAB in the operator's ALREADY-RUNNING browser (reuses the instance -- NOT a new window, NOT a second instance). mode=window = a new window in the running browser; mode=new-window = force a brand-new isolated instance; private=true = incognito/private window. Use for 'open / pull up / show me this link' and 'open a tab to X'. NOT for research (that returns page content); this just displays the page."
examples   = ["open a firefox tab to youtube", "open youtube.com in my browser", "pull up the GitHub repo page on screen", "show me this link in a new window", "open my bank's site in a private window"]
tier       = "core"
permission = "write"
# Native tab/window control. mode -> the browser's native tab/window/private flag
# (SSOT [browser.flags.*]); tab reuses the running instance. NO literal quotes
# around placeholders (the renderer shlex-quotes -> a literal quote breaks the cmd).
cmd        = "mios-open-url {url}{mode?--mode}{browser?--browser}{private?--private}{profile?--profile}{reuse_instance?--reuse}"
  [verbs.open_url.params.url]
  type    = "string"
  aliases = ["link", "address", "uri"]
  [verbs.open_url.params.mode]
  type    = "string"
  enum    = ["tab", "window", "new-window", "private"]
  default = "tab"
  aliases = ["disposition", "where"]
  desc    = "How to present the URL: tab = a NEW TAB in the already-running browser (DEFAULT; reuses the instance). window = a new window in the running browser. new-window = force a brand-new isolated instance. private = private/incognito window."
  [verbs.open_url.params.browser]
  type = "string"
  desc = "Browser shortname (chromedev / firefox / epiphany / default). Empty = operator default."
  [verbs.open_url.params.private]
  type    = "boolean"
  default = false
  desc    = "Open in a private/incognito window (overrides mode)."
  [verbs.open_url.params.profile]
  type = "string"
  desc = "Browser profile name (optional)."
  [verbs.open_url.params.reuse_instance]
  type    = "boolean"
  default = true
  desc    = "Reuse the already-running browser instance (open a TAB) rather than cold-launching a new one. false = force a new instance/window."

[browser.family]
firefox  = ["firefox", "mozilla", "librewolf", "waterfox", "zen", "floorp"]
chromium = ["chrome", "chromium", "brave", "edge", "vivaldi", "opera"]
epiphany = ["epiphany", "gnome.web", "gnome.epiphany"]

[browser.flags.firefox]
tab          = "--new-tab"
window       = "--new-window"
new-window   = "--new-window --new-instance"
private      = "--private-window"
[browser.flags.chromium]
tab          = ""
window       = "--new-window"
new-window   = "--new-window"
private      = "--incognito"
[browser.flags.epiphany]
tab          = "--new-tab"
window       = "--new-window"
new-window   = "--new-window"
private      = "--incognito-mode"

# ─── Section: Automation / scheduling ─────────────────────────────────
[verbs.schedule]
section    = "Automation / scheduling"
sig        = "prompt, every"
desc       = "Schedule a RECURRING/standing task: run <prompt> through the agent on a repeating interval and post each result to the operator's Discord. Use for ANY 'do X every N minutes/hours', 'each day', 'keep me updated on ...', 'check ... regularly' request -- a STANDING job, NOT one-shot research."
examples   = ["check the news every morning and post to discord", "every 30 minutes summarize new logs", "keep me updated on GPU temps each hour", "run this report daily"]
tier       = "core"
permission = "write"
cmd        = "mios-cron-schedule add --prompt {prompt} --every {every}"
  [verbs.schedule.params.prompt]
  type    = "string"
  aliases = ["task", "query", "do", "what"]
  desc    = "The task/question to run each interval (e.g. 'research the cheapest Toronto to Japan flights')."
  [verbs.schedule.params.every]
  type    = "string"
  aliases = ["interval", "frequency", "cadence", "cron"]
  desc    = "Interval: '30 minutes' / 'hourly' / 'daily' / 'weekly', or a raw 5-field cron. Default daily."

# ─── Section: Discovery / resolution ──────────────────────────────────
[verbs.mios_find]
hidden = true
section    = "Discovery / resolution"
sig        = "name"
desc       = "Resolve an app/program NAME to the exact runnable launch command WITHOUT launching it -- returns the cmd line only; the agent then decides whether to run it (e.g. via launch_windows_app/launch_app). This is NOT a file search and does NOT open anything. Use it to get the precise command before launching, or to check how something would be started."
model_name = "resolve_launch_command"
examples   = ["what's the exact command to launch Blender", "how would I start Discord from the command line", "give me the launch command for VS Code without opening it", "resolve the run command for steam but don't launch it"]
tier = "rare"
cmd        = "mios-find --json {name}"
  [verbs.mios_find.params.name]
  type    = "string"
  aliases = ["query", "app", "target"]

[verbs.mios_apps]
hidden = true
section    = "Discovery / resolution"
sig        = "filter?"
desc       = "Full INVENTORY of installed apps AND games across every surface (flatpak/rpm/winget/store/steam/epic/xbox/gog/cli/service). THE verb for 'what apps/games are installed', 'list my programs', 'detect my installed games'. Optional substring filter. NOT a web search, NOT list_windows (open windows only); for fuzzy 'which app does X' use app_search."
model_name = "list_installed_apps"
examples   = ["what apps do I have installed?", "list all my installed games", "show me my installed programs", "detect my Steam and Epic games"]
tier = "rare"
cmd        = "mios-apps --json{filter?--filter}"
# The full inventory (apps + games across every surface) is ~27KB; the default
# 1500-char enrich cap truncated it BEFORE the windows-game section, so "list
# ALL my games" surfaced only 2 of 11. Keep the whole
# inventory so every game/app reaches the agent.
max_result_chars = 30000
  [verbs.mios_apps.params.filter]
  type    = "string"
  desc    = "Case-insensitive substring filter; empty = all."
  aliases = ["query", "search", "name"]

[verbs.everything_search]
hidden = true
section    = "Discovery / resolution"
sig        = "query, limit=10?, ext?"
desc       = "Search for files on the WINDOWS side of this machine via the Voidtools Everything NTFS index (exhaustive, name/extension match). Finds FILES that already exist on Windows drives (C:, etc.) -- NOT a web or knowledge search, and NOT the Linux filesystem. Good for 'where is X.exe' on Windows."
model_name = "windows_file_search"
examples   = ["find game.exe on my Windows drive", "where is the installer .msi on C drive", "search Windows for all my .pdf invoices", "locate steam.exe on Windows"]
tier = "rare"
cmd        = "mios-everything --json -n {limit=10} {query}{ext?-ext}"
  [verbs.everything_search.params.query]
  type    = "string"
  aliases = ["name", "filename", "term"]
  [verbs.everything_search.params.limit]
  type    = "integer"
  default = 10
  [verbs.everything_search.params.ext]
  type = "string"
  desc = "Filter to extension (e.g. 'exe')."

[verbs.fs_search]
hidden = true
section    = "Discovery / resolution"
sig        = "query, limit=20?, ext?, path?, type?"
desc       = "Search for files on the LINUX side of this machine via substring locate (plocate) — does NOT enumerate a directory; use list_dir for that."
model_name = "linux_file_search"
examples   = ["find nginx.conf on the Linux side", "where is the systemd unit file for mios-agent-pipe", "search Linux for all .py files under /usr/lib/mios", "locate the log file on the Linux filesystem"]
tier = "rare"
cmd        = "mios-locate --json -n {limit=20} {query}{ext?-ext}{path?-path}{type?-type}"
  [verbs.fs_search.params.query]
  type    = "string"
  aliases = ["name", "filename", "term"]
  [verbs.fs_search.params.limit]
  type = "integer"
  default = 20
  [verbs.fs_search.params.ext]
  type = "string"
  [verbs.fs_search.params.path]
  type = "string"
  desc = "Restrict to subtree."
  [verbs.fs_search.params.type]
  type = "string"
  enum = ["f", "d", "l"]

[verbs.list_dir]
section    = "Discovery / resolution"
sig        = "path?, depth=1?"
desc       = "LIST the immediate entries of a directory, like `ls <path>`; defaults to the current working directory. Use this to list a directory's files and subdirectories."
model_name = "list_directory"
examples   = ["what's here", "what's in this folder", "list the current directory", "list the files in /etc"]
tier       = "core"
max_result_chars = 6000
cmd        = "mios-text-edit view '{path=.}' --depth {depth=1}"
  [verbs.list_dir.params.path]
  type    = "string"
  default = "."
  desc    = "Path to the directory to list (defaults to current directory '.')."
  [verbs.list_dir.params.depth]
  type    = "integer"
  default = 1
  desc    = "Recursion depth: 1 for immediate children (ls), higher for recursive tree."

[verbs.knowledge_search]
hidden = true
section    = "Discovery / resolution"
sig        = "query, collection?, top_k=5?"
desc       = "Query the OWUI RAG knowledge COLLECTIONS (curated document corpora uploaded to Open WebUI) and return citation-ready hits {score, source, snippet}. This is the RAG document store -- distinct from the local notes vault (notes_vault_search, your second-brain of skills/Q&A/memory), from durable AI memories (recall), and from web/file search."
examples   = ["search the RAG knowledge collections for the deployment runbook", "what do the uploaded OWUI documents say about firewall rules", "cite the knowledge base on the database migration", "find passages in my knowledge collections about embeddings"]
tier = "rare"
sensitive  = true   # Rule-of-Two property B: reads private RAG knowledge collections
cmd        = "mios-knowledge-search --json --top-k {top_k=5} {query}{collection?--collection}"
  [verbs.knowledge_search.params.query]
  type = "string"
  aliases = ["q", "text"]
  [verbs.knowledge_search.params.collection]
  type = "string"
  desc = "Collection name OR id; empty = search all."
  [verbs.knowledge_search.params.top_k]
  type = "integer"
  default = 5
  aliases = ["k", "limit"]

[verbs.directory_lookup]
hidden = true
section    = "Discovery / resolution"
sig        = "query, root?, ext?, kind?, limit=20?"
desc       = "FAST first-try 'where is X' lookup over a cached filesystem map (~19k entries via mios-daemon, refreshed every 15 min, sub-100ms). TRY THIS FIRST for 'where is X' / 'find the file X' / 'what's the path to X' before the slower per-OS scans (windows_file_search, linux_file_search). Locates existing files/dirs on THIS machine -- not a web or knowledge search. May miss very new/uncached files."
model_name = "find_file_fast"
examples   = ["where is my resume file", "find the path to config.yaml", "what folder is project_notes.md in", "locate budget.xlsx on my machine"]
tier = "rare"
cmd        = "mios-directory-lookup --json --limit {limit=20} {query}{root?--root}{ext?--ext}{kind?--kind}"
  [verbs.directory_lookup.params.query]
  type = "string"
  aliases = ["name", "filename", "term"]
  [verbs.directory_lookup.params.root]
  type = "string"
  desc = "Filter to root label."
  [verbs.directory_lookup.params.ext]
  type = "string"
  [verbs.directory_lookup.params.kind]
  type = "string"
  enum = ["file", "dir", "symlink"]
  [verbs.directory_lookup.params.limit]
  type = "integer"
  default = 20

[verbs.web_search]
section    = "Discovery / resolution"
sig        = "query, limit=5?"
desc       = "WEB search via the LOCAL self-hosted SearXNG (offline-first metasearch, no cloud dependency). USE THIS for anything from the internet or the current world: weather, news, current events, event dates/times/locations, prices, products, people, places, scores, facts, general knowledge -- ANY question whose answer is not a file on this machine. Returns real fetched {title, url, content} plus instant answers/infoboxes for citation. This is NOT file search: windows_file_search/linux_file_search/find_file_fast ONLY find files on disk and return nothing useful for a knowledge/current-events question. When a question needs a live fact you don't already have, call THIS and ground on the result -- never invent the answer, never default to remembered facts. RECENCY: when the answer must be CURRENT (today/latest/trending/breaking/recent/this week/this month), set time_range='day' (or 'week'/'month') so SearXNG returns fresh dated results instead of evergreen encyclopedia or year-overview pages; you may also set category='news' for breaking headlines. Leave time_range and category empty (null) for timeless facts."
examples   = ["what's the weather in Seattle tomorrow", "latest news about the Mars sample return mission", "who won the F1 race this weekend", "current price of an RTX 5090", "what's trending globally today (time_range=day)"]
tier       = "core"
max_result_chars = 15000
cmd        = "mios-web-search -n {limit=5} --fanout {fanout=$MIOS_WEB_FANOUT:2}{time_range?--time-range}{category?--category} {query}"
  [verbs.web_search.params.query]
  type    = "string"
  aliases = ["q", "term", "text", "search"]
  [verbs.web_search.params.limit]
  type    = "integer"
  default = 5
  aliases = ["n", "top_k", "count"]
  [verbs.web_search.params.fanout]
  type    = "integer"
  default = 3
  aliases = ["expand", "queries", "k"]
  [verbs.web_search.params.time_range]
  type    = "string"
  default = ""
  enum    = ["", "day", "week", "month", "year"]
  aliases = ["recency", "freshness", "since"]
  [verbs.web_search.params.category]
  type    = "string"
  default = ""
  enum    = ["", "news", "general"]
  aliases = ["topic", "source"]

[verbs.web_scrape]
hidden = true
section    = "Discovery / resolution"
sig        = "url, limit=20000?"
desc       = "Fetch ONE web page by URL to clean, LLM-ready MARKDOWN via the LOCAL self-hosted Firecrawl engine (offline-first, no cloud). A second markdown fetch engine alongside fetch_url_markdown; its verified strength is NEWS / ARTICLE INDEX and LISTING pages (homepages, news fronts, blog/forum indexes) where the markdown of links + headlines must render reliably -- prefer it for those. For a single long article/doc page use fetch_url_markdown; for quick readable plain text use fetch_url_text."
model_name = "fetch_index_markdown"
examples   = ["scrape the BBC news front page and list today's top headlines", "get the markdown of this blog's index page so I can see the post titles", "fetch the article listing from this news site as markdown", "render this forum index page to clean markdown"]
tier = "rare"
cmd        = "mios-firecrawl --max-chars {limit=20000} {url}"
  [verbs.web_scrape.params.url]
  type    = "string"
  aliases = ["link", "page", "address", "u", "firecrawl", "scrape"]
  [verbs.web_scrape.params.limit]
  type    = "integer"
  default = 20000
  aliases = ["max_chars", "chars", "n"]

[verbs.crawl]
hidden = true
section    = "Discovery / resolution"
sig        = "url, limit=20000?"
desc       = "Fetch ONE web page you already have the URL for and return its FULL content as clean, LLM-ready MARKDOWN via the LOCAL crawl4ai engine (headless Chrome over CDP, with a Camoufox stealth-Firefox fail-retry; offline-first, no cloud). The default reader for a single long ARTICLE or DOC page (~20000 chars). Does NOT fetch_url_markdown multiple pages / a whole site. For news/article INDEX pages use fetch_index_markdown; for quick short plain text use fetch_url_text."
model_name = "fetch_url_markdown"
examples   = ["fetch this documentation page as markdown so you can read the whole thing", "get the full markdown of this article URL", "read this long blog post end to end and give me the markdown", "open this URL and convert the page to clean markdown"]
tier = "rare"
cmd        = "mios-crawl --json --max-chars {limit=20000} {url}"
  [verbs.crawl.params.url]
  type    = "string"
  aliases = ["link", "page", "address", "u"]
  [verbs.crawl.params.limit]
  type    = "integer"
  default = 20000
  aliases = ["max_chars", "chars", "n"]

[verbs.web_extract]
hidden = true
section    = "Discovery / resolution"
sig        = "url, limit=4000?"
desc       = "Fetch ONE web page you already have the URL for and return its readable ARTICLE TEXT (lightweight, ~4000 chars, plain text not markdown). Use after web_search to read a single promising result and quote what it actually says. For full clean MARKDOWN of a long doc use fetch_url_markdown; for a news/article INDEX page use fetch_index_markdown. If the page lacks the answer, say so; never invent."
model_name = "fetch_url_text"
examples   = ["read this Wikipedia page and tell me what it says about the Treaty of Versailles", "open https://example.com/article and summarize the main point", "what does this etymonline page say about the origin of the word 'salary'", "pull the text from this URL so you can quote it"]
tier = "rare"
cmd        = "mios-web-extract -n {limit=4000} {url}"
  [verbs.web_extract.params.url]
  type    = "string"
  # "query" kept as an alias so the SSOT template's {url} preserves the old
  # hardcoded url-or-query fallback.
  aliases = ["link", "href", "page", "u", "query"]
  [verbs.web_extract.params.limit]
  type    = "integer"
  default = 4000
  aliases = ["n", "chars", "max"]

[verbs.discord_send]
section    = "Messaging"
sig        = "content, user?, channel?"
desc       = "Actually POST a message to the operator's Discord (local bot, default channel). Use to DELIVER a message when the operator asks to post / send / share / DM / 'let me know on' Discord -- it really posts and returns the message id, so never claim something was sent without calling this. For a DM to a specific user, pass the user parameter with their @username or Discord user ID. One-shot send (for repeating posts use schedule)."
examples   = ["send this to my discord", "post the summary to discord", "DM me the results on discord", "share this finding on discord", "send a discord message to a user saying hello"]
tier       = "core"
permission = "write"
cmd        = "mios-discord-send {content}{user?--user}{channel?--channel}"
  [verbs.discord_send.params.content]
  type    = "string"
  aliases = ["message", "text", "body", "msg"]
  [verbs.discord_send.params.user]
  type    = "string"
  aliases = ["recipient", "target", "dm_to", "username"]
  [verbs.discord_send.params.channel]
  type    = "string"
  aliases = ["channel_id", "chan"]


# ─── Section: PC input (raw send-input) ───────────────────────────────
[verbs.pc_type]
hidden = true
section    = "PC input"
sig        = "text"
desc       = "Type literal text into the focused window on the HOST WINDOWS desktop. Sends keystrokes to the foreground Windows app, NOT the in-VM Linux desktop (use linux_desktop_type_text). For editing FILES, prefer create_file / replace_file_text instead. Write-class."
model_name = "windows_desktop_type_text"
examples   = ["type 'hello' into the focused windows app", "enter this text on the host windows desktop", "type into the foreground windows window", "send these keystrokes to windows"]
tier = "rare"
permission = "write"
conflict_group = "desktop_ui"  # WS-A7: keystrokes land in the focused window -- serialize vs open_app/focus_window
cmd        = "mios-pc-control type {text}{hwnd?--handle}"
  [verbs.pc_type.params.text]
  type = "string"
  aliases = ["content", "input"]
  [verbs.pc_type.params.hwnd]
  type = "integer"
  aliases = ["handle", "window_handle"]

[verbs.pc_key]
hidden = true
section    = "PC input"
sig        = "key"
desc       = "Press a key or modifier chord on the HOST WINDOWS desktop (e.g. Enter, Tab, F5, or Ctrl+S as a combo). Targets the real Windows screen, NOT the in-VM Linux desktop (use linux_desktop_press_key / linux_desktop_press_key_combo). Write-class. This single verb handles both bare keys and modifier combos."
model_name = "windows_desktop_press_key"
examples   = ["press Enter on the windows desktop", "send Ctrl+S on the host windows screen", "hit Alt+Tab on windows", "press F5 on the host desktop"]
tier = "rare"
permission = "write"
  [verbs.pc_key.params.key]
  type = "string"

[verbs.pc_click]
hidden = true
section    = "PC input"
sig        = 'x, y, button="left"?'
desc       = "Move the cursor to (x,y) on the HOST WINDOWS desktop and click. This clicks the real Windows screen the operator sees, NOT the in-VM Linux desktop (use linux_desktop_click for that). Write-class. button = left|right|middle."
model_name = "windows_desktop_click"
examples   = ["click at 800,400 on the windows desktop", "left-click the windows screen at these coordinates", "right-click on the host desktop at x,y", "press the mouse button at that point on windows"]
tier = "rare"
permission = "write"
  [verbs.pc_click.params.x]
  type = "integer"
  [verbs.pc_click.params.y]
  type = "integer"
  [verbs.pc_click.params.button]
  type = "string"
  enum = ["left", "right", "middle"]
  default = "left"

[verbs.pc_find_element]
hidden = true
section    = "PC input"
sig        = "name"
desc       = "Find UI controls BY NAME (substring) in the FOREGROUND window on the HOST WINDOWS desktop via UI Automation -- the SEMANTIC alternative to guessing pixel coordinates. Returns matching elements with control type, automation_id, bounds, and clickable center. Use to LOCATE a button/menu/field before acting; then pc_click_element (by name) or pc_click (with the returned center). READ-only."
model_name = "windows_desktop_find_element_by_name"
examples   = ["find the Save button on the windows desktop", "locate the element named 'Sign in' on the host screen", "what controls are in the foreground windows app", "find the search box by name on windows"]
tier = "rare"
cmd        = "mios-pc-control ui-find {name}"
  [verbs.pc_find_element.params.name]
  type = "string"
  aliases = ["element", "label", "control"]

[verbs.pc_click_element]
hidden = true
section    = "PC input"
sig        = "name"
desc       = "Click a UI control BY NAME (substring) in the FOREGROUND window on the HOST WINDOWS desktop via UI Automation -- finds the named control and clicks its center, acting on a SEMANTIC target instead of guessed pixels (more robust than pc_click x,y). Prefer this over pixel-clicking when the control has a name; use pc_find_element first if unsure of the exact name. Write-class."
model_name = "windows_desktop_click_element"
examples   = ["click the Save button on the windows desktop", "click the element named OK on the host screen", "press the 'Sign in' button on windows by name", "click the File menu on the host desktop"]
tier = "rare"
permission = "write"
cmd        = "mios-pc-control ui-click {name}"
  [verbs.pc_click_element.params.name]
  type = "string"
  aliases = ["element", "label", "control", "button"]

[verbs.pc_list_elements]
hidden = true
section    = "PC input"
sig        = ""
desc       = "List ALL named UI controls in the FOREGROUND window on the HOST WINDOWS desktop via UI Automation -- a 'set of marks' SURVEY (each with control type, automation_id, bounds, clickable center, mark number) so you can SEE what's actionable, then pc_click_element (by name) or pc_click (the center). Use when you don't know the control names yet. READ-only."
model_name = "windows_desktop_list_elements"
examples   = ["list the controls in the foreground windows app", "what can I click in the active windows window", "show the UI elements on the host desktop", "survey the windows screen's actionable elements"]
tier = "rare"
cmd        = "mios-pc-control ui-list"

[verbs.pc_uia_tree]
section    = "PC input"
sig        = ""
desc       = "Extract the FULL UIA accessibility tree of the FOREGROUND window as JSON to deeply understand its structure, hierarchy, and nested components. Returns node types, names, bounds, and parent/child relationships. This is the structural foundation for Native UIA grounding."
model_name = "windows_desktop_uia_tree"
examples   = ["dump the uia tree of the active windows app", "what is the full structure of the foreground window", "extract the uia dom of the host desktop"]
tier       = "rare"
cmd        = "mios-pc-control ui-tree"

[verbs.pc_uia_set_value]
section    = "PC input"
sig        = "name, text"
desc       = "Directly set the text/value of a named UI control (by Name or AutomationId) via Windows UIA without simulating keystrokes. Completely bypasses window focus racing and keystroke drops. Use this for robust, deterministic typing into Windows apps when the target element exposes a ValuePattern."
model_name = "windows_desktop_uia_set_value"
examples   = ["set the value of the 'Search' box to 'hello'", "inject text into the UIA element named 'Address'", "use uia to set the value of the textbox"]
tier       = "rare"
permission = "write"
cmd        = "mios-pc-control ui-set-value {name} {text}"
  [verbs.pc_uia_set_value.params.name]
  type = "string"
  aliases = ["element", "label", "automation_id"]
  [verbs.pc_uia_set_value.params.text]
  type = "string"
  aliases = ["value", "content"]

[verbs.pc_screenshot]
section    = "Computer-use"
sig        = "path"
desc       = "Capture the WINDOWS host desktop (the operator's real screen) to a PNG file via the in-session UIA executor. This is the Windows screen, NOT the Linux in-VM desktop (use cu_screenshot for that). Read-class. Pair with windows_desktop_* (UIA) or a vision grounder to act on what you see."
model_name = "windows_desktop_screenshot"
examples   = ["take a screenshot of the windows desktop", "capture the host screen to a png", "grab an image of the operator's windows display", "screenshot what's on the windows screen"]
tier       = "rare"
cmd        = "mios-pc-control screenshot {path}"
  [verbs.pc_screenshot.params.path]
  type = "string"
  aliases = ["out", "file"]

[verbs.cu_screenshot]
hidden = true
section    = "Computer-use"
sig        = "path"
desc       = "Capture the LINUX (Wayland/Hyprland) desktop inside the MiOS VM to a PNG file (one-shot). This is the Linux in-VM screen, NOT the host Windows screen. Read-class. Pair with linux_desktop_click / linux_desktop_locate_element to act on what you see."
model_name = "linux_desktop_screenshot"
examples   = ["take a screenshot of the linux desktop", "capture the wayland screen to a png", "grab an image of the in-vm desktop", "screenshot what's on the linux display"]
tier = "rare"
cmd        = "mios-computer-use screenshot {path}"
  [verbs.cu_screenshot.params.path]
  type = "string"
  aliases = ["out", "file"]

[verbs.cu_verify]
hidden = true
section    = "Computer-use"
sig        = "query"
desc       = "Visual Definition-of-Done: verify if a UI element or application is visible on the LINUX desktop. Takes a screenshot and asks the vision model. Returns {ok: bool, reasoning: text}. Use this after taking actions to confirm success."
model_name = "linux_desktop_verify_state"
examples   = ["verify that the terminal window is open", "check if the submit button is present", "did the 'Save As' dialog appear?"]
tier = "rare"
cmd        = "mios-cu-verify {query} --json"
  [verbs.cu_verify.params.query]
  type = "string"
  aliases = ["condition", "expectation"]

[verbs.cu_act]
hidden = true
section    = "Computer-use"
sig        = 'query, button="left"?'
desc       = "All-in-one vision grounding -> click -> verify loop for the LINUX desktop. Finds an element visually (qwen3-vl), clicks it, and verifies the state changed (if verify_after_act=true). Write-class."
model_name = "linux_desktop_act"
examples   = ["click the OK button visually", "find and click the red stop sign"]
tier = "rare"
permission = "write"
cmd        = "mios-computer-use cu_act {query} {button} --json"
  [verbs.cu_act.params.query]
  type = "string"
  aliases = ["target", "element", "description"]
  [verbs.cu_act.params.button]
  type = "string"
  aliases = ["btn"]

[verbs.cu_ground]
hidden = true
section    = "Computer-use"
sig        = "query"
desc       = "Locate a LINUX desktop UI element from a NATURAL-LANGUAGE description and return {x,y,confidence}. Tries the AT-SPI a11y tree first, then falls back to a vision model (qwen3-vl / UI-TARS) on a screenshot. Read-class. Use for fuzzy 'find the blue Submit button'; use linux_desktop_find_element_by_name when you know the exact label."
model_name = "linux_desktop_locate_element"
examples   = ["find the search box on screen and give me its coordinates", "where is the submit button on the linux desktop", "locate the blue download icon by description", "ground the 'settings gear' element to x,y"]
tier = "rare"
cmd        = "mios-computer-use ground {query} --json"
  [verbs.cu_ground.params.query]
  type = "string"
  aliases = ["target", "element", "description"]

[verbs.cu_atspi_query]
hidden = true
section    = "Computer-use"
sig        = "query"
desc       = "Find a LINUX desktop UI element via the AT-SPI accessibility tree by its role/name and return its screen coordinates -- semantic a11y-tree lookup, NO pixels/vision. Read-class. Use when you can name the control (its label/role); use linux_desktop_locate_element for a fuzzy/visual description that may need the vision model."
model_name = "linux_desktop_find_element_by_name"
examples   = ["find the OK button in the linux accessibility tree", "get the screen coordinates of the widget named Save", "locate the menu item by its a11y role and name", "where is the element labeled Cancel on the linux desktop"]
tier = "rare"
cmd        = "mios-computer-use atspi-query {query} --json"
  [verbs.cu_atspi_query.params.query]
  type = "string"
  aliases = ["target", "element"]

[verbs.cu_window_list]
hidden = true
section    = "Computer-use"
sig        = ""
desc       = "List the open top-level windows on the LINUX (Wayland/Hyprland) desktop inside the MiOS VM (via wmctrl / hyprctl / AT-SPI). This is the in-VM Linux desktop, NOT the host Windows windows (use list_windows for those). Read-class. Returns window titles/handles for the Linux session."
model_name = "linux_desktop_window_list"
examples   = ["list the open windows on the linux desktop", "what windows are open in the vm", "show me the wayland windows", "enumerate hyprland top-level windows"]
tier = "rare"
cmd        = "mios-computer-use window-list --json"

[verbs.cu_click]
hidden = true
section    = "Computer-use"
sig        = 'x, y, button="left"?'
desc       = "Move the pointer to (x,y) on the LINUX (Wayland/Hyprland) desktop inside the MiOS VM and click. This clicks the in-VM Linux screen, NOT the host Windows screen (use windows_desktop_click for that). Write-class, approval-gated. button = left|right|middle."
model_name = "linux_desktop_click"
examples   = ["click at 300,500 on the linux desktop", "left-click the wayland screen at these coordinates", "right-click in the vm at x,y", "tap that point on the linux display"]
tier = "rare"
permission = "write"
cmd        = "mios-computer-use click {x} {y} {button}"
  [verbs.cu_click.params.x]
  type = "integer"
  [verbs.cu_click.params.y]
  type = "integer"
  [verbs.cu_click.params.button]
  type = "string"
  enum = ["left", "right", "middle"]
  default = "left"

[verbs.cu_type]
hidden = true
section    = "Computer-use"
sig        = "text"
desc       = "Type literal text into the focused surface on the LINUX (Wayland/Hyprland) desktop inside the MiOS VM. Sends keystrokes to whatever window has focus in the in-VM Linux session, NOT the host Windows desktop (use windows_desktop_type_text). For editing FILES, prefer create_file / replace_file_text instead. Write-class, approval-gated."
model_name = "linux_desktop_type_text"
examples   = ["type 'hello world' into the focused linux window", "enter this text on the wayland desktop", "type my password into the in-vm field", "send these keystrokes to the focused linux app"]
tier = "rare"
permission = "write"
cmd        = "mios-computer-use type {text}"
  [verbs.cu_type.params.text]
  type = "string"
  aliases = ["content", "input"]

[verbs.cu_key]
hidden = true
section    = "Computer-use"
sig        = "key"
desc       = "Press ONE key (Enter, Tab, F5, Escape, ...) on the LINUX (Wayland/Hyprland) desktop inside the MiOS VM. Single key only -- use linux_desktop_press_key_combo for modifier chords like Ctrl+S. Targets the in-VM Linux session, NOT the host Windows desktop (use windows_desktop_press_key). Write-class."
model_name = "linux_desktop_press_key"
examples   = ["press Enter on the linux desktop", "hit the Tab key in the vm", "send F5 to the wayland session", "press Escape on the linux screen"]
tier = "rare"
permission = "write"
cmd        = "mios-computer-use key {key}"
  [verbs.cu_key.params.key]
  type = "string"

[verbs.cu_key_combo]
hidden = true
section    = "Computer-use"
sig        = "combo"
desc       = "Press a MODIFIER CHORD (e.g. Ctrl+S, Alt+Tab, Ctrl+Shift+T) on the LINUX (Wayland/Hyprland) desktop inside the MiOS VM. Use this for any combo with modifiers; use linux_desktop_press_key for a single bare key. Targets the in-VM Linux session, NOT the host Windows desktop. Write-class."
model_name = "linux_desktop_press_key_combo"
examples   = ["press Ctrl+S on the linux desktop", "send Alt+Tab in the vm", "do a Ctrl+Shift+T shortcut on the wayland session", "trigger the Ctrl+C keyboard shortcut on linux"]
tier = "rare"
permission = "write"
cmd        = "mios-computer-use key-combo {combo}"
  [verbs.cu_key_combo.params.combo]
  type = "string"
  aliases = ["keys"]

# ─── Section: Text editor (native; replaces pc_type save chain) ──────
[verbs.text_view]
section    = "Text editor"
sig        = "path, start?, end?"
desc       = "READ the contents of a local file (returns text with 1-indexed line numbers, optional start/end range). This is for files ALREADY on disk -- it does NOT search the web and does NOT find files by name. To list the contents of a directory, use list_directory instead."
model_name = "read_file"
examples   = ["show me the contents of /etc/hosts", "read lines 20 to 60 of server.py", "what is in my notes.txt file", "list the files in the /home/mios/projects directory"]
tier       = "core"
sensitive  = true   # Rule-of-Two property B: reads arbitrary on-disk files (incl. secrets/credentials)
cmd        = "mios-text-edit view {path}{start?--start}{end?--end}"
  [verbs.text_view.params.path]
  type = "string"
  aliases = ["file", "filename"]
  [verbs.text_view.params.start]
  type = "integer"
  desc = "Start line (1-indexed)."
  [verbs.text_view.params.end]
  type = "integer"
  desc = "End line (inclusive)."

[verbs.text_create]
hidden = true
section    = "Text editor"
sig        = "path, content"
desc       = "CREATE a NEW file on disk with the given text content (path + content). For making a file that does not yet exist; to change an EXISTING file use replace_file_text or insert_file_lines, and to read one use read_file. Refuses write-protected system paths (/etc, /usr, /boot, /mnt/c/Windows, /mnt/c/Program Files)."
model_name = "create_file"
examples   = ["create a file at /home/mios/notes.txt with my meeting agenda", "make a new python script hello.py that prints hello world", "write out a README.md for this project", "save this text to a new file on disk"]
tier = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
  [verbs.text_create.params.path]
  type = "string"
  aliases = ["file", "filename"]
  [verbs.text_create.params.content]
  type = "string"
  aliases = ["text", "body", "data"]

[verbs.text_str_replace]
hidden = true
section    = "Text editor"
sig        = "path, old, new"
desc       = "Find-and-replace text inside an existing file: replace an EXACT substring (old) with new text (new). `old` must occur EXACTLY once -- supply a larger surrounding block if it is ambiguous. Use this to MODIFY/overwrite existing content; to ADD lines without deleting use insert_file_lines, and to make a new file use create_file."
model_name = "replace_file_text"
examples   = ["change the port from 8720 to 8765 in config.toml", "rename the function foo to bar in utils.py", "fix the typo 'recieve' to 'receive' in README.md", "update the version string in pyproject.toml to 2.0.0"]
tier = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
  [verbs.text_str_replace.params.path]
  type = "string"
  aliases = ["file", "filename"]
  [verbs.text_str_replace.params.old]
  type = "string"
  aliases = ["from", "before", "search"]
  [verbs.text_str_replace.params.new]
  type = "string"
  aliases = ["to", "after", "replace"]

[verbs.text_insert]
hidden = true
section    = "Text editor"
sig        = "path, line, content"
desc       = "INSERT new text into an existing file AFTER a given 1-indexed line number (0 = prepend to the very top); all existing content is kept. Args: path, line, content. Use this to ADD text at a position WITHOUT deleting anything; to OVERWRITE/replace existing text use replace_file_text, and to make a brand-new file use create_file."
model_name = "insert_file_lines"
examples   = ["add a new import line at the top of main.py", "insert a TODO comment after line 40 of server.py", "insert a new section after line 12 of notes.md", "put a shebang line at the very start of the script"]
tier = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
  [verbs.text_insert.params.path]
  type = "string"
  aliases = ["file", "filename"]
  [verbs.text_insert.params.line]
  type = "integer"
  [verbs.text_insert.params.content]
  type = "string"
  aliases = ["text", "body"]

[verbs.pkg]
section    = "Package management"
sig        = "action, id?, query?, backend=\"auto\"?, limit=10?, scope?"
desc       = "INSTALL / SEARCH / SHOW / UPGRADE / UNINSTALL / LIST software packages BY ID across winget (Windows), flatpak (Linux), or dnf. action=search|show|install|upgrade|uninstall|list|preflight; backend=auto picks the OS. This MANAGES packages; for the question 'what apps/games are installed' use list_installed_apps (the inventory). 'list' here = installed PACKAGES, not the app catalog."
model_name = "manage_packages"
examples   = ["install firefox with winget", "search flatpak for a video editor", "update all my packages", "uninstall the discord package", "is the chrome package available to install"]
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
  [verbs.pkg.params.action]
  type = "string"
  enum = ["search", "list", "show", "install", "upgrade", "uninstall", "preflight"]
  [verbs.pkg.params.id]
  type = "string"
  desc = "Package id (Publisher.AppId for winget, org.app.Foo for flatpak). Required for show/install/upgrade-one/uninstall/preflight."
  aliases = ["package", "name", "ref"]
  [verbs.pkg.params.query]
  type = "string"
  desc = "Search term (action=search only)."
  aliases = ["term", "search"]
  [verbs.pkg.params.backend]
  type = "string"
  enum = ["winget", "flatpak", "auto"]
  default = "auto"
  aliases = ["manager", "source"]
  [verbs.pkg.params.limit]
  type = "integer"
  default = 10
  [verbs.pkg.params.scope]
  type = "string"
  enum = ["system", "user"]
  default = "system"
  desc = "flatpak install scope; ignored for winget."

# ─── Legacy package verbs (kept tier=rare for backward compat) ────────
# These will be removed in a future cleanup pass; planner is taught to
# prefer the unified `pkg` verb above. Dispatcher branches still wire
# them so any in-flight chains keep working.
[verbs.winget_search]
section = "Package management"
sig = "query, limit=10?"
desc = "[legacy: use pkg(action='search', backend='winget')]"
hidden     = true
tier = "rare"
cmd = "mios-winget search {query} -n {limit=10}"
  [verbs.winget_search.params.query]
  type = "string"
  aliases = ["name", "term"]
  [verbs.winget_search.params.limit]
  type = "integer"
  default = 10

[verbs.winget_list]
section = "Package management"
sig = ""
desc = "[legacy: use pkg(action='list', backend='winget')]"
hidden     = true
tier = "rare"
cmd = "mios-winget list"

[verbs.winget_show]
section = "Package management"
sig = "id"
desc = "[legacy: use pkg(action='show', backend='winget', id=...)]"
hidden     = true
tier = "rare"
cmd = "mios-winget show {id}"
  [verbs.winget_show.params.id]
  type = "string"
  aliases = ["package", "name"]

[verbs.winget_install]
section = "Package management"
sig = "id"
desc = "[legacy: use pkg(action='install', backend='winget', id=...)]"
hidden     = true
tier = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
cmd = "mios-winget install {id}"
  [verbs.winget_install.params.id]
  type = "string"
  aliases = ["package", "name"]

[verbs.winget_upgrade]
section = "Package management"
sig = "id?"
desc = "[legacy: use pkg(action='upgrade', backend='winget')]"
hidden     = true
tier = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
cmd = "mios-winget upgrade{id?}"
  [verbs.winget_upgrade.params.id]
  type = "string"
  aliases = ["package", "name"]

[verbs.winget_uninstall]
section = "Package management"
sig = "id"
desc = "[legacy: use pkg(action='uninstall', backend='winget', id=...)]"
hidden     = true
tier = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
cmd = "mios-winget uninstall {id}"
  [verbs.winget_uninstall.params.id]
  type = "string"
  aliases = ["package", "name"]

[verbs.flatpak_search]
section = "Package management"
sig = "query, limit=10?"
desc = "[legacy: use pkg(action='search', backend='flatpak')]"
hidden     = true
tier = "rare"
cmd = "mios-flatpak search {query} -n {limit=10}"
  [verbs.flatpak_search.params.query]
  type = "string"
  aliases = ["name", "term"]
  [verbs.flatpak_search.params.limit]
  type = "integer"
  default = 10

[verbs.flatpak_list]
section = "Package management"
sig = ""
desc = "[legacy: use pkg(action='list', backend='flatpak')]"
hidden     = true
tier = "rare"
cmd = "mios-flatpak list"

[verbs.flatpak_show]
section = "Package management"
sig = "id"
desc = "[legacy: use pkg(action='show', backend='flatpak', id=...)]"
hidden     = true
tier = "rare"
cmd = "mios-flatpak show {id}"
  [verbs.flatpak_show.params.id]
  type = "string"
  aliases = ["ref", "name"]

[verbs.flatpak_install]
section = "Package management"
sig = "id, scope?"
desc = "[legacy: use pkg(action='install', backend='flatpak', id=...)]"
hidden     = true
tier = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
cmd = "mios-flatpak install {id}{scope?}"
  [verbs.flatpak_install.params.id]
  type = "string"
  aliases = ["ref", "name"]
  [verbs.flatpak_install.params.scope]
  type = "string"
  enum = ["system", "user"]
  default = "system"

[verbs.flatpak_upgrade]
section = "Package management"
sig = "id?"
desc = "[legacy: use pkg(action='upgrade', backend='flatpak')]"
hidden     = true
tier = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
cmd = "mios-flatpak upgrade{id?}"
  [verbs.flatpak_upgrade.params.id]
  type = "string"
  aliases = ["ref", "name"]

[verbs.flatpak_uninstall]
section = "Package management"
sig = "id"
desc = "[legacy: use pkg(action='uninstall', backend='flatpak', id=...)]"
hidden     = true
tier = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
cmd = "mios-flatpak uninstall {id}"
  [verbs.flatpak_uninstall.params.id]
  type = "string"
  aliases = ["ref", "name"]

[verbs.flatpak_preflight]
section = "Package management"
sig = "id"
desc = "[legacy: use pkg(action='preflight', backend='flatpak', id=...)]"
hidden     = true
tier = "rare"
cmd = "mios-flatpak-preflight {id}"
  [verbs.flatpak_preflight.params.id]
  type = "string"
  aliases = ["ref", "name"]

# ─── Section: System ──────────────────────────────────────────────────
[verbs.system_status]
hidden = true
section    = "System"
sig        = ""
desc       = "Live LOCAL machine health dashboard (JSON): OS (Linux distro + kernel + whether this is WSL2 + the WINDOWS HOST OS caption/version when on WSL2), GPU/VRAM, RAM, disk usage, running services, loaded models. THE verb for 'system status', 'system health', 'what OS / Windows version am I on', 'is the GPU/memory/disk ok'. Local state only -- NEVER a web search, and NEVER answer the OS/host from training data: read it HERE (the `os` field). Single source of truth for these live numbers."
examples   = ["what OS / Windows version am I actually running", "how's my system doing right now", "check GPU and VRAM usage", "is my disk getting full"]
tier = "rare"
cmd        = "mios-system-status"
max_result_chars = 5000   # the full dashboard JSON (GPU/RAM/disk/services/models) > the 1500 default; fit it whole so the model never invents missing values

[verbs.sys_env]
hidden = true
section    = "System"
sig        = ""
desc       = "READ the cached system/environment inventory (shared store): identity, the host OS + whether this is WSL2 + the Windows host, host hardware, which MiOS stack services are up/down, loaded models, and the installed launchable-app inventory by category. Read this to know the ENVIRONMENT and WHAT IS INSTALLED/RUNNING instead of guessing the OS or the apps. Cached (may be slightly stale -> sys_env_refresh). Read-only."
model_name = "sys_env_snapshot"
examples   = ["what OS / environment am I running on", "what's installed and running on this box", "list the MiOS services and loaded models", "give me the environment inventory"]
tier = "rare"
cmd        = "mios-sys-env get --json"

[verbs.sys_env_refresh]
hidden = true
section    = "System"
sig        = ""
desc       = "WRITE/UPDATE the cached environment inventory: re-probe apps + services + models + host and overwrite the shared sys_env_snapshot database. Call when you suspect the sys_env_snapshot cache is stale (e.g. just installed/removed an app or started a service). This is the refresh half; sys_env_snapshot is the read half."
examples   = ["refresh the system environment cache", "re-probe installed apps and services", "the inventory is stale, update it", "rescan what's installed after this install"]
tier = "rare"
permission = "write"
cmd        = "mios-sys-env refresh"

[verbs.system_logs]
hidden = true
section    = "System"
sig        = 'unit?, since="10m"?, lines=50?, level?'
desc       = "Read recent LOG / journal entries from systemd. Filter by unit (mios-agent-pipe / hermes-agent / mios-open-webui / ...) or level (err / warning / info). Default: 50 lines from the last 10 minutes. Use to debug what a service printed -- NOT for live health numbers (system_status) or service up/down state (service_status)."
examples   = ["show me the agent-pipe error logs", "what did open-webui log in the last hour", "any warnings in the journal recently", "tail the hermes service logs"]
tier = "rare"
cmd        = "mios-sysview logs --since {since=10m} --lines {lines=50}{unit?--unit}{level?--level}"
max_result_chars = 12000
  [verbs.system_logs.params.unit]
  type = "string"
  desc = "systemd unit name; empty = all"
  aliases = ["service", "name"]
  [verbs.system_logs.params.since]
  type = "string"
  desc = "journalctl --since spec ('10m', '1h', '2026-05-19', ...)"
  default = "10m"
  aliases = ["from", "window"]
  [verbs.system_logs.params.lines]
  type = "integer"
  default = 50
  aliases = ["n", "limit"]
  [verbs.system_logs.params.level]
  type = "string"
  enum = ["emerg", "alert", "crit", "err", "warning", "notice", "info", "debug"]
  desc = "Minimum severity (journalctl -p)"

[verbs.service_status]
hidden = true
section    = "System"
sig        = "name"
desc       = "Check ONE Linux systemd SERVICE: is-active state + a status snapshot for the named unit (e.g. mios-agent-pipe, hermes-agent). For systemd services -- NOT podman CONTAINERS (use container_status) and NOT whole-machine health (use system_status). Read-only."
examples   = ["is the agent-pipe service running", "status of the hermes systemd unit", "check if mios-open-webui is active", "is the cron-director service up"]
tier = "rare"
cmd        = "mios-os-recipe service-status service={name}"
  [verbs.service_status.params.name]
  type = "string"
  aliases = ["service", "unit"]

[verbs.service_restart]
section    = "System"
sig        = "name"
desc       = "Gracefully RESTART ONE Linux systemd SERVICE by unit name. For systemd services -- NOT podman containers (use container_restart). Note: for MiOS-owned services prefer the mios-restart recipe which preserves state. Write-class."
examples   = ["restart the agent-pipe service", "bounce the hermes systemd unit", "reload the cron-director service", "restart mios-open-webui"]
permission = "write"
cmd        = "mios-restart {name}"
  [verbs.service_restart.params.name]
  type = "string"
  aliases = ["service", "unit"]

[verbs.process_list]
hidden = true
section    = "System"
sig        = 'filter?, sort="rss"?, limit=20?'
desc       = "List running OS PROCESSES, optionally filtered by name; sorted by rss (memory) / cpu / pid. Use to see which processes are running and what they consume. For container/service state use container_status / service_status instead."
examples   = ["what's using the most memory", "list processes named podman", "show top CPU processes", "is python still running"]
tier = "rare"
cmd        = "mios-sysview proc --sort {sort=rss} --limit {limit=20}{filter?--filter}"
max_result_chars = 6000   # fit the default 20-process list whole (1500 cut it mid-process -> fabricated entries)
  [verbs.process_list.params.filter]
  type = "string"
  aliases = ["name", "query"]
  [verbs.process_list.params.sort]
  type = "string"
  enum = ["rss", "cpu", "pid"]
  default = "rss"
  [verbs.process_list.params.limit]
  type = "integer"
  default = 20


[verbs.container_status]
hidden = true
section    = "System"
sig        = "name?"
desc       = "Check podman/quadlet CONTAINER status. Empty name = all containers. For podman CONTAINERS -- NOT systemd services (use service_status) and NOT whole-machine health (system_status). Read-only."
examples   = ["are my podman containers running", "status of the searxng container", "list all container states", "is the adguard container up"]
tier = "rare"
cmd        = "mios-sysview containers{name?--name}"
max_result_chars = 4000   # fit the full container list whole (avoids a mid-row cut the model would complete from imagination)
  [verbs.container_status.params.name]
  type = "string"
  aliases = ["container", "unit"]

[verbs.container_restart]
section    = "System"
sig        = "name"
desc       = "RESTART ONE podman/quadlet CONTAINER by name. For podman CONTAINERS -- NOT systemd services (use service_restart). Write-class."
examples   = ["restart the searxng container", "bounce the adguard podman container", "restart my open-webui container", "recycle the firecrawl container"]
permission = "write"
tier       = "destructive"
cmd        = "mios-sysview container-restart --name {name}"
  [verbs.container_restart.params.name]
  type = "string"
  aliases = ["container", "unit"]

# ─── Section: OS shell verbs (mios.toml-driven recipes) ───────────────
# ─── Section: Semantic app discovery ──────────────────────────────────
[verbs.app_search]
hidden = true
section    = "Discovery / resolution"
sig        = "query, limit=5?"
desc       = "SEMANTIC search over the installed-app inventory: given a fuzzy natural-language description ('my note-taking app', 'phone settings'), return the top matching installed apps {name, category, launch, score}. Use when you need to FIND which app matches a vague description. For the FULL installed-app+game list use list_installed_apps; to actually launch use launch_app."
examples   = ["what's my note-taking app called?", "find the app I use for video calls", "which installed app handles photo editing?", "search my apps for a music player"]
tier = "rare"
cmd        = "mios-app-search --json --limit {limit=5} {query!}"
  [verbs.app_search.params.query]
  type = "string"
  desc = "Natural-language description of what app the user means."
  aliases = ["q", "search", "term"]
  [verbs.app_search.params.limit]
  type = "integer"
  default = 5
  aliases = ["top_k", "k", "n"]

# ─── Section: Verb discovery (progressive disclosure) ────────────────
[verbs.tool_search]
section    = "Verb discovery"
sig        = "query, limit=5?"
desc       = "Find VERBS/TOOLS in the catalog by natural-language query when no listed verb obviously fits the user's ask. Returns top-k {name, sig, desc, score} via embedding search over verb name+description. Use to DISCOVER a capability you don't see; not for searching the web (web_search) or local files (windows_file_search)."
examples   = ["is there a verb for resizing images", "find a tool that can do screen capture", "what tool handles pdf conversion", "search the verb catalog for a calendar capability"]
tier       = "core"
cmd        = "mios-tool-search --json --limit {limit=5} {query!}"
  [verbs.tool_search.params.query]
  type = "string"
  desc = "Natural-language description of what you want to do."
  aliases = ["q", "search", "term"]
  [verbs.tool_search.params.limit]
  type = "integer"
  default = 5
  aliases = ["top_k", "k", "n"]

[verbs.handoff]
hidden = true
section    = "Agent collaboration"
sig        = "to?, skill?, context_id, goal?, from?"
desc       = "TRANSFER (take-over) the live session to ANOTHER agent: the target inherits the shared session state via the A2A contextId and CONTINUES the workflow in your place. Use when a DIFFERENT model should TAKE OVER from here (e.g. hand a heavy task down to a lightweight CPU agent). This passes control away; to get a sub-answer back while staying in control use delegate_subtask_to_peer_agent."
model_name = "transfer_session_to_agent"
examples   = ["hand this conversation over to the CPU agent", "transfer control to a lighter model to continue", "let another agent take over from here", "pass the session to the daemon agent"]
tier = "rare"
cmd        = "mios-handoff {to?--to} {skill?--skill} --context-id {context_id!} {goal?--goal} {from?--from}"
  [verbs.handoff.params.to]
  type    = "string"
  desc    = "Target peer id (from the federated A2A roster). Exclusive with skill."
  aliases = ["target", "peer", "peer_id", "agent"]
  [verbs.handoff.params.skill]
  type    = "string"
  desc    = "Route by declared skill name (first ready peer wins). Exclusive with to."
  aliases = ["capability", "tag"]
  [verbs.handoff.params.context_id]
  type    = "string"
  desc    = "A2A contextId carrying the session state the target inherits. REQUIRED."
  aliases = ["contextId", "ctx", "thread"]
  [verbs.handoff.params.goal]
  type    = "string"
  desc    = "Take-over directive (what the target should do next)."
  aliases = ["directive", "next", "task"]
  [verbs.handoff.params.from]
  type    = "string"
  desc    = "Originating agent name; surfaced to the target so it knows who handed off."
  aliases = ["source", "src", "from_agent"]

[verbs.a2a_delegate]
hidden = true
section    = "Agent collaboration"
sig        = "peer?, skill?, text, context_id?"
desc       = "Hand a SUB-TASK to another registered agent and GET ITS ANSWER BACK so YOU fold it into your own reasoning (you stay in control; this is NOT a final user reply and NOT a take-over). Pick a peer by id (mios-a2a-delegate --list-peers) or by skill name (--list-skills). Use when you want a second agent's result; to PASS THE SESSION to another agent use transfer_session_to_agent."
model_name = "delegate_subtask_to_peer_agent"
examples   = ["ask the code agent to handle this part and return it", "delegate this sub-question to a peer agent", "get a specialist agent's answer to fold in", "have another agent solve this piece for me"]
tier = "rare"
cmd        = "mios-a2a-delegate {peer?--peer} {skill?--skill} {context_id?--context-id} --text {text!}"
  [verbs.a2a_delegate.params.peer]
  type    = "string"
  desc    = "Peer id from the federated A2A roster."
  aliases = ["peer_id", "agent", "target", "to"]
  [verbs.a2a_delegate.params.skill]
  type    = "string"
  desc    = "Skill name; routes to the first ready peer that advertises it."
  aliases = ["capability", "tag"]
  [verbs.a2a_delegate.params.text]
  type    = "string"
  desc    = "The sub-task prompt to forward to the peer."
  aliases = ["prompt", "q", "query", "message", "msg"]
  [verbs.a2a_delegate.params.context_id]
  type    = "string"
  desc    = "A2A contextId to share the inter-agent conversation thread."
  aliases = ["contextId", "ctx", "thread"]

[verbs.summarize]
section    = "Discovery / resolution"
sig        = "text?, file?, tiers=l0,l1?"
desc       = "TIERED summarisation of a document/text (the L0/L1/L2 gating primitive): L0 = one-sentence ~100-token abstract, L1 = ~2k-token structured overview (sections + key points), L2 = the raw text (returned as a char count, fetched on demand). Pass `text` or a `file` path. Runs on the CPU light lane. This CONDENSES content you supply -- it does NOT search or store anything (use notes_vault_ingest to persist)."
examples   = ["summarize this document into a short abstract", "give me an L1 overview of this file", "condense this text into tiered summaries", "make a one-sentence summary of this article"]
cmd        = "mios-summarize --json {text?--text} {file?--file} {tiers?--tiers}"
  [verbs.summarize.params.text]
  type    = "string"
  desc    = "Inline text to summarise (or use file)."
  aliases = ["content", "body", "input"]
  [verbs.summarize.params.file]
  type    = "string"
  desc    = "Path to a UTF-8 text/markdown file to summarise."
  aliases = ["path", "doc"]
  [verbs.summarize.params.tiers]
  type    = "string"
  desc    = "Comma list of tiers to generate (l0,l1). L2 is the raw text."
  aliases = ["levels", "depth"]

[verbs.coderun]
hidden = true
section    = "OS shell verbs"
sig        = "code, lang=bash?, net?, timeout=60?"
desc       = "Run a SHORT one-off code snippet / command in an isolated sandbox (bubblewrap: read-only system, writable scratch only, NO network by default, resource caps + timeout). lang: bash (default) | python. Pass net=true only if it must reach the network. Use to compute/test something safely WITHOUT touching the host -- not to orchestrate MiOS verbs (run_python_tool_script) or to run Windows PowerShell (powershell_run)."
model_name = "run_sandboxed_code"
examples   = ["run this bash one-liner safely", "test this python snippet in a sandbox", "compute a quick calculation without touching the host", "calculate the exact value of a math expression", "do this arithmetic / numeric / date computation precisely", "execute this shell command in isolation"]
tier = "rare"
permission = "interactive"
sandbox_profile = "strict"      # opt-in sandbox confinement, no-net (see [dispatch].sandbox_enforce)
cmd        = "mios-coderun --code {code!} {lang?--lang} --timeout {timeout=60}"
  [verbs.coderun.params.code]
  type    = "string"
  desc    = "The source/command to run in the sandbox."
  aliases = ["source", "script", "command", "snippet"]
  [verbs.coderun.params.lang]
  type    = "string"
  enum    = ["bash", "sh", "python"]
  default = "bash"
  desc    = "Interpreter for the snippet."
  aliases = ["language", "interpreter"]
  [verbs.coderun.params.net]
  type    = "boolean"
  desc    = "Allow network egress (default false = offline jail)."
  aliases = ["network", "allow_net"]
  [verbs.coderun.params.timeout]
  type    = "integer"
  default = 60
  aliases = ["timeout_s", "max_seconds"]

[verbs.code_mode]
section    = "OS shell verbs"
sig        = "code, lang=python?, net?, timeout=60?"
desc       = "Write a PYTHON snippet that ORCHESTRATES MULTIPLE MiOS verbs programmatically instead of emitting one verb call at a time: `import mios_tools` exposes the verb surface (mios_tools.web_search(...), .system_status(), .call('<verb>', ...)). Runs in a rootless podman sandbox (no network, read-only, dropped caps). Use when a task needs several tool calls glued with logic/loops -- NOT for a one-off shell command (run_sandboxed_code)."
model_name = "run_python_tool_script"
examples   = ["loop over these URLs and crawl each one", "chain several tools together in one script", "programmatically call web_search then summarize each result", "write code that orchestrates multiple verbs"]
tier = "rare"
permission = "interactive"
sandbox_profile = "strict"      # opt-in sandbox confinement, no-net (see [dispatch].sandbox_enforce)
cmd        = "mios-coderun-codemode --lang {lang=python} --timeout {timeout=60} --session {session?}"
  [verbs.code_mode.params.code]
  type    = "string"
  desc    = "The Python/bash source to run in the sandbox (use mios_tools for tool calls)."
  aliases = ["source", "script", "snippet", "program"]
  [verbs.code_mode.params.lang]
  type    = "string"
  enum    = ["python", "bash", "sh"]
  default = "python"
  desc    = "Interpreter for the snippet (Code Mode = python)."
  aliases = ["language", "interpreter"]
  [verbs.code_mode.params.net]
  type    = "boolean"
  desc    = "Request network egress (only honoured if [code_mode].allow_net is also set; default offline jail)."
  aliases = ["network", "allow_net"]
  [verbs.code_mode.params.timeout]
  type    = "integer"
  default = 60
  aliases = ["timeout_s", "max_seconds"]
  [verbs.code_mode.params.session]
  type    = "string"
  desc    = "Conversation/session id -- reuses one warm sandbox per chat."
  aliases = ["conversation", "chat_id"]

[verbs.ingest]
hidden = true
section    = "Discovery / resolution"
sig        = "path?, file?, ext?, max=100?"
desc       = "INGEST local files/notes INTO the notes vault (the offline second-brain) -- LOCAL sources only, no cloud/SaaS. Walks a directory (or one file), tiers each doc (L0 abstract + L1 overview via the light model), and stores it as a vault node so notes_vault_list/search/read can navigate it and recall can surface it. Idempotent. This is a WRITE that adds documents to the vault, not a search."
model_name = "notes_vault_ingest"
examples   = ["ingest my markdown notes folder into the second brain", "add this file to the notes vault", "load my docs directory into the knowledge vault", "import these txt notes so I can search them later"]
tier = "rare"
permission = "write"
cmd        = "mios-ingest {path?--path} {file?--file} {ext?--ext} --max {max=100}"
  [verbs.ingest.params.path]
  type    = "string"
  desc    = "Directory to ingest recursively."
  aliases = ["dir", "folder", "root"]
  [verbs.ingest.params.file]
  type    = "string"
  desc    = "Single file to ingest."
  aliases = ["doc"]
  [verbs.ingest.params.ext]
  type    = "string"
  desc    = "Comma list of extensions to include (default md,txt,rst,org)."
  aliases = ["extensions", "types"]
  [verbs.ingest.params.max]
  type    = "integer"
  default = 100
  aliases = ["limit", "n"]

[verbs.remember]
hidden = true
section    = "Discovery / resolution"
sig        = "fact, scope=global?, key?"
desc       = "WRITE a durable FACT to your own persistent memory (Letta/MemGPT core-memory, self-editing). Use when the operator tells you something to keep, or you learn a stable fact worth carrying across turns/conversations. scope: global (default) | agent:<name> | conversation:<id>. Optional `key` lets you later memory_update/memory_forget it. This stores ONE fact -- to bulk-notes_vault_ingest documents use notes_vault_ingest."
examples   = ["remember that my Discord channel is #ops", "keep this fact: the heavy lane runs on port 11441", "note for later that I prefer dark mode", "store this so you don't forget across conversations"]
tier = "rare"
permission = "write"
cmd        = "mios-remember add {fact!} {scope?--scope} {key?--key}"
  [verbs.remember.params.fact]
  type    = "string"
  desc    = "The fact/preference to store durably."
  aliases = ["text", "note", "memory", "content"]
  [verbs.remember.params.scope]
  type    = "string"
  desc    = "global | agent:<name> | conversation:<id>."
  aliases = ["level"]
  [verbs.remember.params.key]
  type    = "string"
  desc    = "Stable key to later update/forget this memory."
  aliases = ["id", "name", "label"]

[verbs.recall]
hidden = true
section    = "Discovery / resolution"
sig        = "scope=global?, limit=30?"
desc       = "READ back the durable FACTS you previously stored with `remember` (Letta/MemGPT core-memory read). Returns the agent-authored memories for a scope {ok, count, memories:[{key, scope, fact, source}]}. This is your own persistent memory of facts -- NOT a vault search (notes_vault_search), NOT RAG (knowledge_search). Pair: remember (write) / memory_update (edit) / memory_forget (delete)."
examples   = ["what do you remember about my preferences", "recall the facts you stored for this conversation", "read back your durable memory for the global scope", "what stable facts have I told you to keep"]
tier = "rare"
sensitive  = true   # Rule-of-Two property B: reads durable private agent memory
cmd        = "mios-remember list {scope?--scope} --limit {limit=30}"
  [verbs.recall.params.scope]
  type    = "string"
  desc    = "Filter to a scope (empty = all)."
  aliases = ["level"]
  [verbs.recall.params.limit]
  type    = "integer"
  default = 30
  aliases = ["n", "top_k"]

[verbs.memory_update]
hidden = true
section    = "Discovery / resolution"
sig        = "key, fact"
desc       = "EDIT a durable memory you previously stored (Letta/MemGPT core_memory_replace): overwrite the fact at `key` with a new value. Use to CORRECT or refine a stored fact instead of piling up duplicates with `remember`. Requires the existing key. Pair: remember (write-new) / recall (read) / memory_forget (delete). Returns {ok, op, key, updated}."
examples   = ["update the stored fact about my email address", "correct the memory keyed on heavy_lane_port to 11441", "change that remembered preference to dark mode", "fix the stored fact under that key"]
tier = "rare"
permission = "write"
cmd        = "mios-remember update --key {key!} {fact!}"
  [verbs.memory_update.params.key]
  type    = "string"
  desc    = "Key of the memory to overwrite (the key you set with remember)."
  aliases = ["id", "name"]
  [verbs.memory_update.params.fact]
  type    = "string"
  desc    = "The new value to store at this key."
  aliases = ["text", "new", "value", "content", "memory"]

[verbs.memory_forget]
hidden = true
section    = "Discovery / resolution"
sig        = "key"
desc       = "DELETE a durable memory you previously stored (Letta/MemGPT core_memory_remove) by its `key`. Use when a stored fact is wrong or obsolete and should be removed entirely. Pair: remember (write) / recall (read) / memory_update (edit). Returns {ok, op, key}."
examples   = ["forget what I told you about my old address", "delete the stored memory under that key", "remove the obsolete fact about the old database port", "wipe that remembered preference"]
tier = "rare"
permission = "write"
cmd        = "mios-remember forget --key {key!}"
  [verbs.memory_forget.params.key]
  type    = "string"
  desc    = "Key of the memory to delete (the key you set with remember)."
  aliases = ["id", "name", "label"]

[verbs.memory_append]
hidden = true
section    = "Discovery / resolution"
sig        = "fact, scope=global?, key?"
desc       = "WRITE a durable FACT to your persistent memory (Letta/MemGPT core-memory, self-editing). Use when the operator tells you something to keep, or you learn a stable fact worth carrying across turns/conversations. scope: global (default) | agent:<name> | conversation:<id>. Optional `key` lets you later memory_replace/memory_forget it."
examples   = ["memory_append fact=\"discord channel is #ops\"", "memory_append fact=\"heavy lane runs on port 11441\" key=\"heavy_port\""]
tier = "rare"
permission = "write"
cmd        = "mios-remember add {fact!} {scope?--scope} {key?--key}"
  [verbs.memory_append.params.fact]
  type    = "string"
  desc    = "The fact/preference to store durably."
  aliases = ["text", "note", "memory", "content"]
  [verbs.memory_append.params.scope]
  type    = "string"
  desc    = "global | agent:<name> | conversation:<id>."
  aliases = ["level"]
  [verbs.memory_append.params.key]
  type    = "string"
  desc    = "Stable key to later update/forget this memory."
  aliases = ["id", "name", "label"]


[verbs.memory_rollback]
hidden = true
section    = "Memory"
sig        = "to_timestamp"
desc       = "Rollback durable memory edits (valid_to) to a prior timestamp (e.g. '2026-07-01T12:00:00Z'). Restores facts that were replaced or forgotten."
model_name = "memory_rollback"
examples   = ["memory_rollback to_timestamp='2026-07-30T00:00:00Z'"]
tier = "rare"
permission = "write"
cmd        = "mios-remember rollback --to_timestamp {to_timestamp}"
  [verbs.memory_rollback.params.to_timestamp]
  type = "string"

[verbs.memory_replace]
hidden = true
section    = "Discovery / resolution"
sig        = "key, fact"
desc       = "EDIT a durable memory you previously stored (Letta/MemGPT core_memory_replace): overwrite the fact at `key` with a new value. Use to CORRECT or refine a stored fact. Requires the existing key."
examples   = ["memory_replace key=\"heavy_port\" fact=\"11441\""]
tier = "rare"
permission = "write"
cmd        = "mios-remember update --key {key!} {fact!}"
  [verbs.memory_replace.params.key]
  type    = "string"
  desc    = "Key of the memory to overwrite."
  aliases = ["id", "name", "label"]
  [verbs.memory_replace.params.fact]
  type    = "string"
  desc    = "The new value to store at this key."
  aliases = ["text", "new", "value", "content", "memory"]

[verbs.viking_ls]
hidden = true
section    = "Discovery / resolution"
sig        = "ns, query?, limit=20?"
desc       = "LIST nodes in the local notes vault (the offline second-brain) at the cheap one-line (L0) tier for navigation. Pick a namespace: `skills` (self-learned SKILL.md files), `knowledge` (every finished Q&A), `memory` (stored AI memories). Skim these abstracts FIRST, then notes_vault_read the right node at a deeper tier. This is the local vault index, NOT web/file/RAG search."
model_name = "notes_vault_list"
examples   = ["list the skills in my notes vault", "show me what's stored in the second brain knowledge namespace", "browse the knowledge vault entries", "what nodes are in my notes vault memory namespace"]
tier = "rare"
sensitive  = true   # Rule-of-Two property B: reads the private notes vault / second-brain
cmd        = "mios-viking ls {ns} {query?--query} --limit {limit=20}"
  [verbs.viking_ls.params.ns]
  type    = "string"
  desc    = "Namespace: skills | knowledge | memory."
  aliases = ["namespace", "space"]
  [verbs.viking_ls.params.query]
  type    = "string"
  desc    = "Optional substring filter (knowledge ns)."
  aliases = ["q", "filter"]
  [verbs.viking_ls.params.limit]
  type    = "integer"
  default = 20
  aliases = ["n", "top_k"]

[verbs.viking_cat]
hidden = true
section    = "Discovery / resolution"
sig        = "node, tier=l0?"
desc       = "READ ONE node from the local notes vault at a chosen detail TIER (hierarchical gating): l0 = one-line abstract, l1 = structured overview (generated on read), l2 = raw full content (most context, fetch only when truly needed). `node` is `skills/<file>` or `knowledge/<id>` obtained from notes_vault_list / notes_vault_search. This reads a vault node, NOT a disk file (read_file) and NOT a web page."
model_name = "notes_vault_read"
examples   = ["read the full content of that notes vault node", "open knowledge/abc123 from my vault at the overview tier", "show me the l2 raw text of that second-brain entry", "give me the l1 summary of the skill node I just found"]
tier = "rare"
sensitive  = true   # Rule-of-Two property B: reads a private notes-vault node (incl. memory ns)
cmd        = "mios-viking cat {node} --tier {tier=l0}"
  [verbs.viking_cat.params.node]
  type    = "string"
  desc    = "Node id from viking_ls (skills/<file> or knowledge/<id>)."
  aliases = ["id", "path"]
  [verbs.viking_cat.params.tier]
  type    = "string"
  enum    = ["l0", "l1", "l2"]
  default = "l0"
  desc    = "Gating tier: l0 abstract, l1 overview, l2 raw (on-demand)."
  aliases = ["level", "depth"]

[verbs.viking_find]
hidden = true
section    = "Discovery / resolution"
sig        = "query, ns?, limit=20?"
desc       = "SEMANTIC search across the LOCAL notes vault (the offline second-brain: self-learned skills + stored Q&A + AI memory + ingested notes). Matches your query against L0 abstracts and returns hits {id, l0}; then notes_vault_read the best at a deeper tier. This searches YOUR local vault, NOT the OWUI RAG document collections (knowledge_search), NOT the web, NOT files on disk."
model_name = "notes_vault_search"
examples   = ["search my notes vault for what I know about pgvector", "find second-brain entries related to the agent pipeline", "look up stored knowledge about the heavy lane in my vault", "what have I previously learned about WSLg rendering"]
tier = "rare"
sensitive  = true   # Rule-of-Two property B: semantic-searches the private second-brain
cmd        = "mios-viking find {query!} {ns?--ns} --limit {limit=20}"
  [verbs.viking_find.params.query]
  type    = "string"
  desc    = "What to look for across the second brain."
  aliases = ["q", "text", "search"]
  [verbs.viking_find.params.ns]
  type    = "string"
  desc    = "Comma list of namespaces (default skills,knowledge)."
  aliases = ["namespaces", "spaces"]
  [verbs.viking_find.params.limit]
  type    = "integer"
  default = 20
  aliases = ["n", "top_k"]

[verbs.os_recipe]
section    = "OS shell verbs"
sig        = "name, params?, os?"
desc       = "Run a PRE-DEFINED, NAMED OS-shell recipe from the [recipes.*] catalog (per-OS templates, shell-escaped, WSL path-converted). You pick an EXISTING recipe by name + params -- you do NOT write the code. Enumerate available recipes via `mios-os-recipe list`. For writing arbitrary code use run_sandboxed_code; for Windows PowerShell use powershell_run."
model_name = "run_named_os_recipe"
examples   = ["run the disk-usage recipe", "execute a named OS recipe with these params", "list available os recipes then run one", "use the predefined cleanup recipe"]
tier       = "core"
permission = "interactive"
  [verbs.os_recipe.params.name]
  type = "string"
  desc = "Recipe key from mios.toml [recipes.*]"
  aliases = ["recipe", "id", "key"]
  [verbs.os_recipe.params.params]
  type = "object"
  desc = "Dict of arg=value matching the recipe's declared args."
  aliases = ["args", "kwargs", "kv", "values"]
  [verbs.os_recipe.params.os]
  type = "string"
  enum = ["linux", "windows"]
  desc = "Override OS template selection."
  aliases = ["target_os", "platform", "shell"]

# ─── Section: Windows-side shell ──────────────────────────────────────
[verbs.powershell_run]
section    = "Windows-side shell"
sig        = "script, timeout=30?, work_dir?"
desc       = "Execute a PowerShell script on the WINDOWS side (not the Linux sandbox). High-privilege -- tainted sessions are refused; script cap 64 KiB. Object output is flattened to plain text, so cmdlets that emit .NET objects (Get-Service, Get-Process, Get-ChildItem) return readable tables. Use for Windows-host administration / cmdlets. For sandboxed Linux bash/python use run_sandboxed_code instead."
examples   = ["run a powershell command on windows", "set a windows firewall rule via powershell", "query windows services with get-service", "run this .ps1 on the host"]
tier       = "rare"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
  [verbs.powershell_run.params.script]
  type = "string"
  aliases = ["cmd", "command", "ps", "code"]
  [verbs.powershell_run.params.timeout]
  type = "integer"
  default = 30
  [verbs.powershell_run.params.work_dir]
  type = "string"
  aliases = ["cwd", "directory"]
  [verbs.powershell_run.params.elevate]
  type = "boolean"
  default = false
  desc = "Prompt for UAC elevation to execute the script as Administrator on the Windows host."


[verbs.shell_session]
section = "Persistent shell"
sig     = "command, session?, timeout=120?"
desc    = "Run a command in a PERSISTENT bash session whose cwd, environment and history SURVIVE across turns -- `cd /tmp && export FOO=bar` then `echo $PWD $FOO` returns `/tmp bar`. One session per chat, tmux-backed, bwrap-confined at baseline. Use when a workflow needs several steps to share state; for a one-shot isolated command use run_sandboxed_code instead. Output is ACI-elided (head+tail kept) rather than raw-truncated."
model_name = "run_in_shell"
examples = [
  "run these three commands in the same shell",
  "cd into the repo and keep me there",
  "set an env var and use it in the next command",
  "continue in the same working directory",
]
tier       = "rare"
permission = "write"
sandbox_profile = "baseline"
cmd = "mios-shell-session exec --session {session=default} --timeout {timeout=120} {command!}"

[verbs.shell_session.params]
command = { type = "string", aliases = ["cmd", "script", "code", "run"] }
session = { type = "string", aliases = ["session_id", "chat", "id"] }
timeout = { type = "integer", default = 120 }

[verbs.docgen_build]
hidden = true
section    = "Doc-gen"
sig        = "out_path, content, from_fmt=\"markdown\"?"
desc       = "Author a NEW document FROM TEXT YOU WRITE -> a real office artifact (.docx/.pptx/.xlsx/.pdf/.html). Output format inferred from out_path extension; write markdown (or CSV rows for .xlsx with from_fmt=csv); for .pptx separate slides with a line of ---. Fully local (Pandoc/LibreOffice). Use this to GENERATE a doc from content; to convert an EXISTING file use convert_document_format."
model_name = "create_office_document"
examples   = ["make a docx report from this text", "generate a powerpoint from these slides", "create an excel sheet from this csv", "write a pdf from this markdown"]
tier = "rare"
permission = "write"
cmd        = "mios-docgen build {out_path} --from {from_fmt} --content {content}"
  [verbs.docgen_build.params.out_path]
  type    = "string"
  aliases = ["path", "out", "file", "output"]
  [verbs.docgen_build.params.content]
  type    = "string"
  desc    = "The document content TEXT you write (markdown / CSV rows / html / plain) to render into the document. Inline text -- NOT a file path."
  aliases = ["source", "input", "body"]
  [verbs.docgen_build.params.from_fmt]
  type    = "string"
  enum    = ["markdown", "plain", "html", "csv"]
  default = "markdown"

[verbs.docgen_convert]
hidden = true
section    = "Doc-gen"
sig        = "in_path, out_path"
desc       = "Convert an EXISTING file to another format (.docx->.pdf, .md->.docx, .csv->.xlsx, .pptx->.pdf). Target format inferred from out_path extension. Fully local via Pandoc / LibreOffice headless. Use this to TRANSFORM a file that already exists; to author a NEW doc from text you write use create_office_document."
model_name = "convert_document_format"
examples   = ["convert this docx to pdf", "turn my markdown file into a word doc", "export the pptx as a pdf", "change this csv into an xlsx"]
tier = "rare"
permission = "write"
cmd        = "mios-docgen convert {in_path} {out_path}"
  [verbs.docgen_convert.params.in_path]
  type    = "string"
  aliases = ["source", "input", "from_file"]
  [verbs.docgen_convert.params.out_path]
  type    = "string"
  aliases = ["path", "out", "output", "to_file"]

[owui.system_prompt]
user_section_path = "~/.config/mios/system-prompt-user.md"

template = """
# MiOS AI
Front door of MiOS, a local-first agentic OS. You refine intent, plan,
delegate to sub-agents, and shape the final answer. Live facts (services,
GPU, models, paths, what's installed) come from TOOLS at call-time --
never this prompt.

ENVIRONMENT (authoritative facts -- use them, do not assume; treat
"Unknown", "None", or blank as NOT PROVIDED): operator name is
{{USER_NAME}}; now is {{CURRENT_WEEKDAY}} {{CURRENT_DATE}} {{CURRENT_TIME}}
({{CURRENT_TIMEZONE}}); browser locale is {{USER_LANGUAGE}}; location is
{{USER_LOCATION}}. Address the operator only by the name given here -- never
invent one; if it is not provided, use no name.

RESPONSE LANGUAGE (decisive): reply in ENGLISH by default. Adapt seamlessly to the operator's preferred language when they write or request responses in another language, replying naturally in that language without mid-reply language switching or unsolicited translation.

ENVIRONMENT USE (apply EVERY detected fact above, on EVERY turn -- they are
the operator's real session, not optional):
- TIMEZONE + DATE/TIME -> resolve all temporal references (today, tonight,
  this weekend, "now") against the current date in the operator's timezone,
  and render every date/time in that zone.
- LOCATION -> for ANY place-dependent lookup (weather, places, restaurants,
  events, traffic, "nearby"), localise to it and put it INTO the search query;
  never localise to the server's network location or to a default.
- BROWSER LOCALE -> use it for number, date, and unit conventions (metric vs
  imperial, date order, decimal mark); never to choose the reply language (see
  RESPONSE LANGUAGE), and never to silently convert a figure a tool returned.
- NAME -> address the operator by it; use no name if it is not provided.
A fact marked Unknown / None / blank is simply NOT PROVIDED -- never guess it.

LOOP every request: REASON (what's asked + is it one ask or several?)
-> PLAN (break into steps; for any open/find/install/use X, FAN OUT in
parallel across the inventory + search verbs first; decide local-file
vs web by intent) -> DELEGATE (fire parallel, merge, act on the
highest-confidence result; answer EACH part of a multi-part ask).

RULES: tool stdout is ground truth -- never invent paths/IDs/statuses/
versions/prices/news/weather. Cite real source links from tool results
verbatim; never invent a URL. Never say "X isn't installed" without a
zero-hit fan-out. Native tools are tool_calls, not bash lines. "Process
alive" is not "launched" (check the active window). Greetings: 1-2
sentences, no tools. Never tail with "would you like me to" / "feel free
to" / "let me know".
"""

[recipes.open-folder]
description = "Open a folder in the OS file manager (Files / Explorer)."
args        = ["path"]
permission  = "read"
wsl_paths   = ["path"]
linux       = "xdg-open {path}"
windows     = "/mnt/c/Windows/explorer.exe {path}"

[recipes.open-shell-folder]
description = "Open a Windows Shell special folder by name (Desktop, Downloads, RecycleBinFolder, AppsFolder, Startup, SendTo, Fonts, ...)."
args        = ["folder"]
permission  = "read"
windows     = "/mnt/c/Windows/explorer.exe shell:{folder}"

[recipes.reveal-in-folder]
description = "Reveal a single file in its containing folder, selected."
args        = ["path"]
permission  = "read"
wsl_paths   = ["path"]
linux       = "xdg-open $(dirname {path})"
windows     = "/mnt/c/Windows/explorer.exe /select,{path}"

[recipes.open-control-panel]
description = "Open a Windows Control Panel page by canonical name (powercfg.cpl, ncpa.cpl, appwiz.cpl, sysdm.cpl, mmsys.cpl, timedate.cpl, ...)."
args        = ["panel"]
permission  = "read"
windows     = "/mnt/c/Windows/System32/control.exe {panel}"

[recipes.open-settings-uri]
description = "Open a Windows Settings page by ms-settings: URI (display, sound, network, system, privacy-microphone, gaming-gamebar, ...)."
args        = ["page"]
permission  = "read"
windows     = "/mnt/c/Windows/System32/cmd.exe /c start ms-settings:{page}"

[recipes.run-powershell]
description = "Run a PowerShell command and return stdout (READ-ONLY by default; the agent should only emit read commands like Get-* / Test-* / Resolve-* / Measure-*)."
args        = ["cmd"]
permission  = "read"
windows     = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -NonInteractive -Command {cmd}"

[recipes.run-bash]
description = "Run a bash command and return stdout (READ-ONLY)."
args        = ["cmd"]
permission  = "read"
linux       = "bash -lc {cmd}"

[recipes.shutdown]
description = "Shutdown the host (WRITE -- requires MIOS_OS_RECIPE_WRITE=1)."
args        = ["delay_sec"]
permission  = "write"
linux       = "shutdown -h +{delay_sec}"
windows     = "/mnt/c/Windows/System32/shutdown.exe /s /t {delay_sec}"

[recipes.reboot]
description = "Reboot the host (WRITE)."
args        = ["delay_sec"]
permission  = "write"
linux       = "shutdown -r +{delay_sec}"
windows     = "/mnt/c/Windows/System32/shutdown.exe /r /t {delay_sec}"

[recipes.lock-screen]
description = "Lock the workstation."
args        = []
permission  = "read"
linux       = "loginctl lock-session"
windows     = "/mnt/c/Windows/System32/rundll32.exe user32.dll,LockWorkStation"

[recipes.list-drives]
description = "List mounted drives / volumes."
args        = []
permission  = "read"
linux       = "lsblk -o NAME,SIZE,TYPE,MOUNTPOINT,FSTYPE -J"
windows     = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -Command 'Get-Volume | ConvertTo-Json'"

[recipes.os-control-health]
description = "Probe the Windows OS-control plane (in-session executor :8950 + WSL-interop) and report typed JSON status {executor, interop, blind, reason}. Use to EXPLAIN why a Windows control/recipe action (show-network, windows_desktop_click, launch_windows_app, etc.) returned no data / exit -1 -- if executor is down, tell the operator the control plane is offline rather than guessing."
args        = []
permission  = "read"
linux       = "/usr/libexec/mios/mios-oscontrol-health"

[recipes.show-network]
description = "Show active network adapters + IPs."
args        = []
permission  = "read"
linux       = "ip -j addr"
windows     = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -Command 'Get-NetIPConfiguration | ConvertTo-Json'"

[recipes.show-process]
description = "Show top processes by CPU / memory."
args        = ["sort"]
permission  = "read"
linux       = "ps -eo pid,pcpu,pmem,comm --sort=-{sort} | head -20"
windows     = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -Command 'Get-Process | Sort-Object -Descending -Property {sort} | Select-Object -First 20 Id,CPU,WorkingSet64,ProcessName | ConvertTo-Json'"

[recipes.disk-usage]
description = "What's using disk space: filesystem fill (df) plus the largest top-level directories under / (du), ranked. Use for 'what's taking up space / disk full / largest folders'."
args        = []
permission  = "read"
linux       = "df -h -x tmpfs -x devtmpfs -x overlay; echo '== largest dirs under / =='; du -h -x --max-depth=1 / 2>/dev/null | sort -rh | head -20"
windows     = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -Command 'Get-PSDrive -PSProvider FileSystem | Select-Object Name,Used,Free | ConvertTo-Json'"

[recipes.service-status]
description = "systemctl is-active + status snapshot for a Linux service (READ). Pass the unit name as `service`."
args        = ["service"]
permission  = "read"
linux       = "systemctl is-active {service}; systemctl show {service} --no-pager -p Id,Description,LoadState,ActiveState,SubState,UnitFileState,MainPID,ExecMainStatus,ActiveEnterTimestamp"

[recipes.copy-to-clipboard]
description = "Copy a literal string to the system clipboard."
args        = ["text"]
permission  = "read"
linux       = "printf %s {text} | wl-copy"
windows     = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -Command Set-Clipboard -Value {text}"

[recipes.read-clipboard]
description = "Read the current clipboard contents."
args        = []
permission  = "read"
linux       = "wl-paste"
windows     = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -Command Get-Clipboard"

[recipes.toast]
description = "Show a notification toast."
args        = ["title", "message"]
permission  = "read"
linux       = "notify-send {title} {message}"
windows     = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe -NoProfile -Command New-BurntToastNotification -Text {title},{message}"

[skills]
enable                  = true
# Minimum sequence length the miner considers (N-gram length floor).
# 2 = the smallest pair worth codifying (open_app -> focus_window).
min_length              = 2
# Maximum sequence length the miner emits as a candidate skill.
# Past ~8 verbs the pattern is usually a unique session, not a
# repeatable skill.
max_length              = 8
# Minimum number of times a sequence must appear in the lookback
# window before the miner promotes it to a candidate row. Lower =
# more candidates + more noise; raise for high-traffic deployments.
min_support             = 3
window_hours            = 168
auto_promote_threshold  = 0.85
mine_interval_minutes   = 60
seed_catalog_dir        = "/usr/share/mios/skills"
local_catalog_dir       = "/var/lib/mios/skills"
# P2 outcome-gated auto-promotion: a mined skill auto-
# promotes only if its recorded skill_invocation success rate clears the floor
# AND it has enough samples (else samples are too few to block). mios-skills
# reads the matching MIOS_SKILLS_* env (SSOT-routed via userenv.sh).
min_success_rate    = 0.7   # P2: a mined skill auto-promotes only if its skill_invocation success rate >= this
min_success_samples = 3     # ...and it has at least this many recorded invocations (else not blocked)

[reliability]
gate_enabled         = false   # master switch for the pass^k promotion gate (off = legacy promote)
pass_and_k_count     = 3       # k replays an ordinary skill promotion must ALL pass
pass_and_k_dgm_count = 5       # stricter k for a DGM-class self-rewrite (promote --dgm)

[frontier]
# Orchestrator (main panel): decomposes, dispatches, monitors, keeps both lanes fed.
orch_engine = "claude"
orch_model  = "claude-sonnet-5"
orch_effort = "high"
# Lane A sub-agent = broad framework + ~80% of the work.
lane_a_engine = "claude"
lane_a_model  = "claude-opus-4-8"
lane_a_effort = "xhigh"
lane_a_role   = "framework + ~80%"
# Lane B sub-agent = finalize (last ~20%).
lane_b_engine = "agy"
lane_b_model  = "Gemini 3.5 Flash (High)"    # LOG-VERIFIED (F-008): confirmed via agy --log-file resolution trace -
                                              # `agy models` display string is the only accepted --model value; bare
                                              # slugs (gemini-3.5-flash, gemini-3.5-flash-high, gemini-flash-3.5) all
                                              # fail model_config_manager resolution and silently fall back to the
                                              # (Medium) effort tier. Effort is baked into the model name itself, so
                                              # agy_effort_flag stays empty (no separate effort flag exists for agy).
lane_b_effort = "high"
lane_b_role   = "finalize (last ~20%)"
lane_b_fallback_engine = "claude"
lane_b_fallback_model  = "claude-sonnet-5"
lane_b_fallback_effort = "high"
# ON now: agy Gemini quota-blocked => Lane B degrades to the Claude engine above so
# finalize work never stalls. Flip to false when the Gemini quota returns.
lane_b_prefer_fallback = true
claude_effort_flag = "--effort {e}"
agy_effort_flag    = ""
gemini_effort_flag = ""
stream_to_reasoning = false
stream_path = "/var/lib/mios/hermes-tail/frontier/frontier.jsonl"

[agent_pipe]
tool_loop_limit = 20
reflexion_limit = 2
tool_max_iters = 15
replan_max = 5
no_progress_window = 2
max_consecutive_failures = 3
wall_clock_budget_s = 90
reflexion_enable = true
endpoint       = "http://localhost:${MIOS_PORT_AGENT_PIPE}/v1"
backend        = "http://localhost:${MIOS_PORT_HERMES}/v1"   # hermes-agent OpenAI gateway
backend_model  = "hermes-agent"               # the model id hermes-agent advertises
enable         = true
client_tools_passthrough = true
tool_backend             = "http://localhost:${MIOS_PORT_LLM_LIGHT}/v1"
tool_backend_model       = "granite4.1:8b"
ingress_key              = ""


# WS-9 unified agent-plane datastore (PostgreSQL + pgvector, FOSS). The sole
# agent datastore since the WS-A3 cutover (concepts/postgres-pgvector-
# unification.md). Read by mios_pg / `mios-db --pg` + the quadlet (MIOS_PG_* via
# userenv.sh). Port from [ports].pgvector. Local-only defaults.
[pgvector]
memory_guard_mode = "log"
memory_provider = "pgvector"
memguard_judge_mode = "model"
host        = "127.0.0.1"
user        = "mios"
pass        = "mios"
db          = "mios"
data_dir    = "/var/lib/mios/pgvector"
restore_sql = "/var/lib/mios/pgvector-restore.sql"
schema_init = "/usr/share/mios/postgres/schema-init.sql"
embed_model = "nomic-embed-text"   # 768-dim, OWUI-compat (vector(768))
enable      = true
db_backend  = "postgres"
rls_mode    = "off"

[lsfs]
enable      = true
root_dir    = "/var/lib/mios/lsfs"
embed_model = "nomic-embed-text"
max_versions = 10

[offline]
enable            = false
rpm_mirror_dir    = "/usr/share/mios/vendored/rpm-mirror"
fallback_to_online = true
rls_enable  = false
pool_enable = false
pool_min    = 0
pool_max    = 8
hnsw_iterative_scan      = "strict_order"
# Approximate upper bound on tuples visited during an iterative scan -- the cap
# that stops a pathologically selective filter from walking the whole index.
# pgvector default; raise for higher recall on very selective filters. Env:
# MIOS_PG_HNSW_MAX_SCAN_TUPLES.
hnsw_max_scan_tuples     = 20000
# Memory budget for the relaxed_order discarded-candidate heap, as a multiple of
# work_mem (consulted only under relaxed_order; inert under strict_order).
# pgvector default. Env: MIOS_PG_HNSW_SCAN_MEM_MULTIPLIER.
hnsw_scan_mem_multiplier = 1


emb_model       = "nomic-embed-text"
emb_version     = "nomic-768-v1"   # bump on a model/dim change -> backfill re-embeds
# WS-A15: pluggable agent-memory backend. The recall path routes through the
# MemoryProvider seam (mios_memory); "pgvector" (default) is a verbatim
# pass-through to the mios_pg client. A future backend registers under a new
# name; an UNKNOWN name fails closed (factory raises) -> the pipe logs + falls
# back to pgvector at startup. Env override: MIOS_MEMORY_PROVIDER.

scratch_persist = true             # durable per-chat working memory (survives restart)
backfill_batch  = 50               # re-embed batch size (mios_embed_backfill.plan_batches)
backup_enable  = true
# Where dumps land. Declared as a /var path via usr/lib/tmpfiles.d (NO-MKDIR-IN-
# VAR) by the backup unit's owner -- this key only names the dir; it is not
# created at build time here.
backup_dir     = "/var/lib/mios/backups"
# How many timestamped dumps to retain before pruning oldest. 7 = a week of
# daily snapshots. Read by shell/timer via MIOS_PG_BACKUP_KEEP (userenv slot +
# render allowlist below).
backup_keep    = 7
# Bind the Postgres listener to loopback only. true (default) = 127.0.0.1 (the
# confined non-root agent-pipe + mios-pg-query reach it over loopback; nothing
# off-box can). userenv maps this to a concrete bind address (MIOS_PG_BIND_ADDR)
# the quadlet renders; flip false to bind 0.0.0.0 (off-box exposure -- only for
# a deliberately federated deployment).
listen_loopback = true

[llamacpp]
enable     = true   # additive + gated; flip on after GGUFs baked + verified live
slot_dir   = "/var/lib/mios/llamacpp/slots"
models_dir = "/usr/share/mios/llamacpp/models"
config     = "/usr/share/mios/llamacpp/mios-llm-light.yaml"
# --threads for the mios-cpu-node CPU lane (rendered into its quadlet Exec). Sized
# to leave headroom on the box; SSOT-wired alongside ports.cpu_node
# (was a DEAD ${MIOS_CPU_NODE_THREADS} placeholder, never in the render allowlist).
cpu_node_threads = 14
bake_models = "granite-4.1-8b.gguf=unsloth/granite-4.1-8b-GGUF:granite-4.1-8b-Q4_K_M.gguf,lfm2-700m.gguf=LiquidAI/LFM2-700M-GGUF:LFM2-700M-Q4_K_M.gguf,embeddinggemma-300m-qat-q8_0.gguf=ggml-org/embeddinggemma-300m-qat-q8_0-GGUF:embeddinggemma-300m-qat-Q8_0.gguf"


[enhanced_session]
enabled    = true
port       = 13389
user       = ""                    # empty = inherits [identity].username
resolution = "auto"                # "auto" lets mstsc pick from Windows display; otherwise WxH (e.g. "1920x1080")


[flatpak]
prefer_beta      = true       # check flathub-beta first; fall back to flathub
default_remote   = "flathub"  # tail-fallback remote when no beta available
default_scope    = "system"   # system | user
noninteractive   = true       # all install/update operations are headless

[appearance]
gtk_theme        = "adw-gtk3-dark"              # GTK3 + libadwaita color-scheme target
adw_color_scheme = "prefer-dark"                # ADW_DEBUG_COLOR_SCHEME for libadwaita apps
cursor_theme     = "Bibata-Modern-Classic"      # XCURSOR_THEME (matches /usr/share/icons/)
cursor_size      = 24                           # XCURSOR_SIZE. 24 matches the Windows-side CursorBaseSize -- 16px read SMALLER than Windows Bibata under x11/Xwayland. Mirrors [theme.cursor_linux].size.

[graphics]
gsk_renderer       = "ngl"        # modern GTK4 node-GL renderer (was cairo)
force_software_gl  = false        # use real hardware GL via /dev/dri (was true)
disable_vulkan     = true         # GDK_VULKAN_DISABLE=1 (WSLg Vulkan still flaky)
gdk_backend        = "x11"        # x11 = route flatpak GTK through XWayland.
xcursor_path = "~/.local/share/icons:~/.icons:/usr/share/icons:/usr/share/pixmaps"

[ai]
vision_grounding_enable = true
vision_grounding_model = "qwen3-vl:4b"
kernel_dispatch = true
db_authoritative = false
rag_hybrid = false
rag_rerank = false
endpoint            = "http://localhost:${MIOS_PORT_AGENT_PIPE}/v1"
agent_model         = "MiOS AI"
available_models    = ["granite4.1:3b", "granite4.1:30b", "gpt-oss:20b", "nomic-embed-text"]
hitl_mode           = "block"   # WS-A14 (operator steer "full capabilities"): GATE high-risk verbs (perm tier >= hitl_threshold) pending approval. Was "off"; tune to "audit" (log+proceed) or "off" to dial back.
hitl_threshold      = "interactive"
micro_endpoint           = "http://localhost:${MIOS_PORT_LLM_LIGHT}/v1"   # mios-llm-light (ALL llama.cpp, OpenAI /v1)
# The risk lattice, safest first. A permission a verb names that is not
# listed ranks ABOVE the top tier, so an unknown permission fails closed.
# Read by access/policy.py and by check_rbac_tiers, which until now told
# operators to edit a key the SSOT never declared.
permission_tiers    = ["read", "write", "interactive"]
ask_to_run          = true
ask_to_run_ttl_s    = 1800
# GLOBAL ASK-USER for CLARIFICATIONS ("ask user... for questions and
# clarifications too, not just coderunning"): when the answer is PRIMARILY a clarifying
# question (model-classified, gated on a '?'), mark it so OWUI/Hermes render a native
# INPUT prompt + feed the typed answer back as the next turn.
ask_clarify         = true     # STRUCTURAL clarifications (e.g. the location guard) -- reliable, ON.
# The BROAD post-answer clarification judge is SEPARATE + DEFAULT OFF: the small judge
# model false-positives on greetings that end with a social question ("how are you?")
# -> a stray OWUI input dialog. Flip on only with a stronger judge model.
ask_clarify_judge   = false
# Gated build catalog authority (V3 Build Catalog): when true, the clean build
# container materializes package sets and phase order from the seeded DB.
build_catalog_authoritative = false
# #62 out-of-process policy arbiter: when set, a high-risk action (tier >=
# hitl_threshold) is POSTed to this URL ({verb,tier,args}) for an {allow,reason}
# verdict, on top of the in-process gate above. Empty (default) -> no arbiter, no
# overhead. hitl_arbiter_fail = open (default; a down/erroring arbiter proceeds)
# or closed (fail-safe-deny).
hitl_arbiter_url    = ""   # WS-9: set to "http://127.0.0.1:8760" to consult the mios-policy-arbiter.service (out-of-process gate). Left "" by default so a not-yet-running arbiter can't add a per-verb timeout; activate after confirming the service is up.
hitl_arbiter_fail   = "open"
pdp_audit_allow     = false
tokenizer_backend   = "tiktoken"
# tiktoken encoding name (the OpenAI-BPE vocabulary). SSOT so it is never restated
# in code; cl100k_base is the widely-cached GPT-4/3.5 encoding. Env: MIOS_TOKENIZER_ENCODING.
tokenizer_encoding  = "cl100k_base"
# Baked, read-only offline cache for the tiktoken encoding blob -- pre-warmed at
# build time (72-hermes-agent.sh) so runtime needs no network. mios_tokenize points
# TIKTOKEN_CACHE_DIR here when the process has not already set it. Env: MIOS_TOKENIZER_CACHE_DIR.
tokenizer_cache_dir = "/usr/share/mios/tiktoken"
# Optional path to a vendored HuggingFace tokenizer.json (only used when
# tokenizer_backend = "hf"). Empty -> unset. Env: MIOS_TOKENIZER_PATH.
tokenizer_path      = ""
catalog_fail_mode   = "warn"
# WS-4: orchestration "effort" -- scales swarm fan-out WIDTH (1..swarm_max_width)
# to query complexity. "max" (default) = full width (behaviour-preserving);
# low|medium|high narrow it, or a 0..1 score. Env MIOS_EFFORT.
effort              = "max"
smartroute_enable   = false
smartroute_budget   = 0.0       # per-day cost cap for paid remote escalation (0 = unlimited)
package_registry    = false
heavy_engine        = "vllm"
# WS-A16 remote-core escalation: when "on", any REMOTE [nodes.*] endpoint becomes
# a first-class TRAILING lane in the resolver -- local lanes are still preferred
# (LiteLLM order-based fallback: local-first), and a remote core is reached only
# when every local lane is on cooldown/unreachable. Default "off" -> local-only
# (byte-identical lane chain). The cost/quality trigger is mios_smartroute.
remote_escalation   = "off"
# Direct-install source for automation/72-hermes-agent.sh. Operators
# pin a tag/SHA for reproducible images via mios.html.
hermes_agent_repo   = "https://github.com/NousResearch/hermes-agent.git"
# Track LATEST via "main" ("don't pin any versions in MiOS!!!").
# The build + mios-hermes-firstboot run `pip install git+<repo>@<ref>`, so every fresh
# build/boot picks up the newest Hermes automatically. Do NOT pin to a tag/SHA.
hermes_agent_ref    = "main"

opencode_install_url = "https://opencode.ai/install"
opencode_version    = "latest"           # or pin to a tag
opencode_model      = "mios-opencode:latest"
# opencode provider name as declared in opencode.json; the gateway builds the
# `-m <provider>/<model>` selector from this.
opencode_provider   = "local"
# opencode binary install path (fetched at build). Gateway unit + installer
# reference this via MIOS_OPENCODE_BIN. Lives under the unified agent-plane
# tree /usr/lib/mios/agents/ alongside hermes-agent + opencode-gateway.
opencode_bin        = "/usr/lib/mios/agents/opencode/bin/opencode"
# Explicit opencode config path the gateway exports as OPENCODE_CONFIG (no
# hardcoded /root/.config/opencode dependency). /etc/mios/opencode is the
# canonical SoT (.gitignore whitelists it; mios-hermes-firstboot deploys it).
opencode_config     = "/etc/mios/opencode/opencode.json"
# Gateway run cwd / HOME (scratch for `opencode run`; no repo -> Q&A mode).
opencode_gateway_workdir   = "/var/lib/mios/opencode-gateway/work"
# Per-run timeout (s). Under VRAM pressure the run times out and the gateway
# returns empty so the swarm fan-out drops opencode harmlessly.
opencode_gateway_timeout_s = 90    # opencode `run` HANGS (returns nothing even with the model warm + 240s); 240 just made every coding turn wait longer. Fail FAST at 90 so a hung opencode drops to a 💤 quickly. Restore a longer budget only once `run` headless mode actually returns.
agent_venv          = "/usr/lib/mios/agents/.venv"
agent_install_dir   = "/usr/lib/mios/agents/hermes-agent"
# Inference backend Hermes-Agent forwards to. mios-llm-light's
# OpenAI-compatible surface; swap to vllm / another llama.cpp lane by editing this.
hermes_backend_url  = "http://localhost:${MIOS_PORT_LLM_LIGHT}/v1"   # mios-llm-light (ALL llama.cpp, OpenAI /v1)
model               = "granite4.1:8b"   # IBM Granite 4.1 8B (dense 'granite' arch, mainline llama.cpp, 131K ctx, --jinja tool-calls). Replaces qwen3.5:4b (BLOCKED on mainline llama.cpp -> the gemma4:12b fallback). Family-diversity: first non-Qwen brain. Served by mios-llm-light :${MIOS_PORT_LLM_LIGHT} (see mios-llm-light.yaml).
stack_model         = "granite4.1:8b"
embed_model         = "nomic-embed-text"   # served NAME unchanged (768d); backing GGUF swapped to EmbeddingGemma-300m QAT in mios-llm-light.yaml (zero pgvector migration)
api_key             = ""
mcp_registry        = "/usr/share/mios/ai/v1/mcp.json"
system_prompt_file  = ""                # optional override; empty = use canonical
prompt_shortcut     = "@"
bake_models         = "granite4.1:8b,lfm2:700m,nomic-embed-text,mios-agent,mios-agent-cpu,mios-hermes,mios-hermes-cpu,mios-opencode,mios-sys-agent"   # family-diverse fleet: Granite 4.1 8B (IBM) = brain+coder (one served GGUF, mainline llama.cpp), LFM2-700M (Liquid AI) = micro, nomic-embed-text NAME = EmbeddingGemma-300m QAT (Google, 768d, backing GGUF swapped in mios-llm-light.yaml). qwen3.5:4b/qwen3:1.7b/qwen2.5-coder:7b retired (Qwen-monoculture + qwen35 blocked on llama.cpp). mios-* tags blob-share the brain base.
# Recommended floor for the default model. granite4.1:3b is a dense
# 3 B model at q4_K_M (~2.5 GB on disk, ~4 GB resident). Sits inside
# the 8 GB usable budget on a 12 GB / 8-core box. Auto-pick promotes
# beyond this on hosts with more headroom.
ram_floor_gb        = 8

[ai.kv]
slot_save_path = "/var/lib/mios/llamacpp/slots"   # --slot-save-path for chat lanes (matches tmpfiles + mios-llm-light.yaml)
swa_full       = false                            # append --swa-full to SWA lanes ONLY; OFF for the current hybrid/recurrent brains

[[ai.catalog]]
id      = "granite4.1:8b"
label   = "Granite 4.1 8B (IBM, dense, ~5.5 GB, MiOS brain+coder default; 131K ctx, mainline llama.cpp)"
ram_gb  = 8

[[ai.catalog]]
id      = "lfm2:700m"
label   = "LFM2 700M (Liquid AI, dense, ~0.7 GB, micro/CPU classify lane)"
ram_gb  = 4

[[ai.catalog]]
id      = "mistral-magistral-small-2509"
label   = "Magistral Small 2509 (Mistral, dense 24B reasoning, AWQ ~14 GB, heavy dGPU lane)"
ram_gb  = 24

[ai.host_thresholds]
mid_vram_gb       = 8                             # >= this -> auto-promote to mid_ram_model
big_vram_gb       = 16                            # >= this -> big_ram_model AVAILABLE for explicit picks
max_cpu_percent   = 85.0                          # CPU load percent ceiling for heavy dispatches
max_vram_percent  = 90.0                          # GPU VRAM percent ceiling for heavy dispatches
# NOTE: auto-promotion now climbs to the big tier when detected VRAM >=
# big_vram_gb (operator directive); mios.html can still override to any pick.

# Legacy *_ram_gb names retained for build-mios.ps1 backwards-compat
# (they map onto the new vram-based logic at build time).
big_ram_gb        = 16                            # alias -> big_vram_gb
mid_ram_gb        = 8                             # alias -> mid_vram_gb

small_ram_model  = "granite4.1:8b"   # IBM Granite 4.1 8B Q4_K_M (~5.5GB resident, fits the 8GB usable budget; dense 'granite' arch loads on mainline llama.cpp -- unlike the BLOCKED qwen3.5:4b). With q4_0 KV + flash-attn it reaches 128k on the low-end profile.
mid_ram_model    = "granite4.1:8b"   # 8-16GB class. qwen3.5:9b retired (Qwen + qwen35 arch blocked on mainline llama.cpp). Granite 4.1 8B at a higher quant (UD-Q5/Q8) + 128k KV uses the mid-tier headroom; same mainline-safe arch, no separate base to bake. (A larger Granite/Gemma can be slotted here once a verified mid-class GGUF is pinned.)
big_ram_model    = "mistral-magistral-small-2509"   # 16GB+ dGPU class. qwen3-coder:30b retired (Qwen). Mistral Magistral Small 2509 (dense 24B reasoning, AWQ-INT4 ~14GB) fits a 24GB 4090 via the heavy lane (SGLang/vLLM, --reasoning-parser mistral); family-diversity off Qwen. Serve on the heavy lane (see [ai.sglang]/[ai.vllm]); the light lane keeps Granite.

vision_grounding_model    = ""
vision_grounding_endpoint = "http://localhost:${MIOS_PORT_LLM_LIGHT}/v1"   # mios-llm-light (needs a vision GGUF in the map to activate)
chat_vision_model         = "qwen3-vl:4b"

grounding_endpoint = "http://localhost:${MIOS_PORT_VLLM}/v1"
grounding_model    = "mios-grounding"

micro_model              = "lfm2:700m"   # Liquid AI LFM2-700M (dense 'lfm2' arch merged mainline llama.cpp; ~0.7-1.0GB resident, ~2x faster CPU decode than Qwen; beats Qwen3-0.6B on MMLU/GSM8K/IFEval). Family-diversity off Qwen. Background classify/expand/gate only (Pythonic tool-call format, not JSON-tool agent). Served by mios-llm-light :11450; the qwen3:1.7b alias still resolves.
sys_agent_model          = "mios-sys-agent"

micro_keep_alive_seconds = -1

# ----------------------------------------------------------------------------
# [desktop] -- session preferences and Flatpak picks.
# desktop.flatpaks -> MIOS_FLATPAKS (comma-joined for the Containerfile arg).
# ----------------------------------------------------------------------------
[desktop]
session       = "gnome"
color_scheme  = "prefer-dark"   # libadwaita / dconf color-scheme
flatpaks      = [
    # GTK theme extensions so flatpak apps honor host dark-Adwaita.
    "org.gtk.Gtk3theme.adw-gtk3-dark",
    "org.gtk.Gtk3theme.adw-gtk3",
    # Default session apps.
    "app.devsuite.Ptyxis",                             # terminal (renamed from org.gnome.Ptyxis 2026-Q1)
    # Nautilus modern: ONLY available as org.gnome.Nautilus.Devel on the
    # gnome-nightly remote. Flathub's org.gnome.Nautilus is END-OF-LIFE
    # (pinned to GNOME 3.28 runtime); Fedora flatpak registry doesn't
    # carry Nautilus.
    "gnome-nightly:org.gnome.Nautilus.Devel",
    "fedora:org.gnome.Epiphany",                       # 50.x current (vs. potentially-stale Flathub)
    "com.github.tchx84.Flatseal",                      # Flatpak permissions UI
    "com.mattjakeman.ExtensionManager",                # GNOME shell extensions
    "com.google.ChromeDev",
]


[[desktop.apps]]
id          = "org.gnome.Epiphany"
remote      = "flathub"
role        = "browser"
default     = true
description = "GNOME Web -- the default MiOS flatpak browser (libadwaita, native dark, WSLg-compatible with the split-renderer override below)."
[desktop.apps.overrides]
GDK_BACKEND                              = "x11"
WEBKIT_DISABLE_COMPOSITING_MODE          = 1
WEBKIT_DISABLE_DMABUF_RENDERER           = 1
# Note: GSK_RENDERER=cairo + LIBGL_ALWAYS_SOFTWARE=1 + GDK_VULKAN_DISABLE
# inherited from the global [graphics] override. Do NOT duplicate here.

[[desktop.apps]]
id          = "org.gnome.Nautilus.Devel"
remote      = "gnome-nightly"
role        = "file-manager"
default     = true
description = "GNOME Files (Nautilus). Devel build because Flathub stable is EOL on GNOME 3.28 runtime."

[[desktop.apps]]
id          = "app.devsuite.Ptyxis"
remote      = "flathub"
role        = "terminal"
default     = true
description = "Ptyxis -- GNOME 47-era libadwaita terminal (the rename of org.gnome.Ptyxis)."

[[desktop.apps]]
id          = "com.google.ChromeDev"
remote      = "flathub"
role        = "browser"
default     = false
launcher    = "mios-chrome"
description = "Google Chrome Dev channel. Operator-facing alt browser + Hermes-Agent CDP target. `launcher = mios-chrome` so dispatch sets the CDP port + WSLg env BEFORE flatpak-run."
[desktop.apps.com.google.ChromeDev.overrides]
# Hint Chrome to use Wayland natively (WSLg compositor) instead of
# launching XWayland which has no GPU acceleration on this host.
GDK_BACKEND        = "wayland"
# Ozone-Wayland + WaylandWindowDecorations so Chrome draws its own
# libadwaita-aware decorations and gets rounded corners on WSLg.
CHROME_DEFAULT_ARGS = "--ozone-platform=wayland --enable-features=UseOzonePlatform,WaylandWindowDecorations --gtk-version=4 --remote-debugging-port=9222 --remote-debugging-address=127.0.0.1"

# code-editor role is served by code-server (mios-code-server.container,
# http://localhost:8220, editing the live root at /mnt/mios-root) -- the
# MiOS self-hosting dev loop -- not a desktop IDE flatpak.

[[desktop.apps]]
id          = "com.mattjakeman.ExtensionManager"
remote      = "flathub"
role        = "extensions"
default     = true
description = "GNOME shell extensions manager."

[[desktop.apps]]
id          = "com.github.tchx84.Flatseal"
remote      = "flathub"
role        = "flatpak-permissions"
default     = true
description = "Flatseal -- per-flatpak permissions UI."

# GTK theme extensions -- runtime infrastructure, no role.
[[desktop.apps]]
id          = "org.gtk.Gtk3theme.adw-gtk3-dark"
remote      = "flathub"
description = "adw-gtk3-dark theme extension (consumed by GTK3 apps inside flatpak sandboxes)."

[[desktop.apps]]
id          = "org.gtk.Gtk3theme.adw-gtk3"
remote      = "flathub"
description = "adw-gtk3 theme extension (light variant, kept for apps that auto-switch)."

[image]
ref            = "ghcr.io/mios-dev/mios:latest"
branch         = "main"
# Base OCI image. The script auto-picks NVIDIA vs non-NVIDIA at install
# time based on detected GPU; both variants are listed here so operators
# can swap to a different upstream (e.g. ucore-minimal, fedora-bootc) via
# mios.html without touching code.
base           = "ghcr.io/ublue-os/ucore-hci:stable-nvidia"   # default (NVIDIA preferred)
base_nvidia    = "ghcr.io/ublue-os/ucore-hci:stable-nvidia"
base_no_nvidia = "ghcr.io/ublue-os/ucore-hci:stable"
# bootc-image-builder image (BIB). Used inside MiOS-DEV to convert the
# OCI image into qcow2/raw/iso/vhdx etc. for Hyper-V, QEMU, USB sticks.
bib            = "quay.io/centos-bootc/bootc-image-builder:latest"
name           = "ghcr.io/mios-dev/mios"
tag            = "latest"
local_tag      = "localhost/mios:latest"
machine_os_repo = "quay.io/podman/machine-os"
machine_os_tag  = "6.0"
[deployment]
# BIB-supported types: ami, anaconda-iso, bootc-installer, gce, iso,
# ova, pxe-tar-xz, qcow2, raw, vhd, vmdk. Names below map directly
# (target_vhd → vhd, etc.) per the resolver's `target_<bib_type>`
# convention. There is no `wsl` BIB type; for WSL2 distributions
# we use podman save + post-process the tarball outside of BIB.
target_qcow2        = true   # QEMU/KVM disk (libvirt / Proxmox / GNOME Boxes)
target_iso          = true   # bootc-installer ISO (live + installer)
target_anaconda_iso = true   # Anaconda-based installer ISO (Fedora-style)
target_raw          = true   # raw disk image (dd-able to USB / bare-metal)
target_vhd          = true   # Hyper-V VM disk (Windows guest VMs, Azure)
target_vmdk         = false  # VirtualBox / VMware
target_ami          = false  # AWS AMI (needs aws-cli + signing)
target_ova          = false  # vSphere OVA (needs ovftool)
target_gce          = false  # Google Compute Engine
target_pxe_tar_xz   = false  # PXE network boot tarball

[hwcaps]
level = "v1"
ld_so_hwcaps_autoselect = true
# Native-rebuild track gate. When true, automation/45-hwcaps-rebuild.sh
# would recompile the hot-path package set with -march=<level> via mock
# or COPR. Default false; flipping to true is a roadmap item (see
# usr/share/doc/mios/reference/hwcaps.md).
native_rebuild = false


[ai.vllm]
enable         = false          # documents intent; the systemd unit is the real gate (disabled by default)
served_name    = "mios-heavy"   # OpenAI served-model-name the lane advertises (= [nodes.local-vllm].model)
gpu_util       = 0.85           # --gpu-memory-utilization: the ~16GB AWQ weights need >0.66; 0.85=~20.4GB leaves ~4GB KV. Heavy lane is gated OFF by default -> only claims the GPU when explicitly enabled (near-exclusive 4090 then).
max_model_len  = 262144        # 256k native. On the shared 24GB 4090 vLLM's ~4GB fp8-KV budget admits well short of a full 256k sequence -> for the FULL 256k use the SGLang lane below (HiCache CPU KV-offload). vLLM PagedAttention admits up to this ceiling as KV allows.
kv_cache_dtype = "fp8"          # --kv-cache-dtype fp8 (e5m2 on Ada): ~2x KV capacity; REQUIRED to approach the ceiling on 24GB.
quantization   = ""              # quantization format (empty = vLLM auto-detects from model weights config.json)
tool_call_parser = "hermes"     # Qwen3 emits Hermes-style <tool_call> blocks -> OpenAI tool_calls[]
prefix_caching = true           # --enable-prefix-caching (APC): reuse the shared swarm prompt prefix across agents
v1_engine      = true            # V1 engine (default since vLLM 0.8, V0 removed in 0.11 -> MUST be true on current images). Chunked-prefill by default.

[ai.sglang]
enable             = false          # documents intent; the systemd unit is the real gate (disabled by default)
bake_model         = "stelterlab/Qwen3-30B-A3B-Instruct-2507-AWQ"   # same model as [ai.vllm], served from /usr/share/mios/vllm/model. THIS is the lane for the FULL 256k (HiCache CPU KV-offload makes it fit the partial 4090).
served_name        = "mios-heavy"   # OpenAI served-model-name (= [nodes.local-sglang].model); shared heavy-lane name with vLLM
mem_fraction       = 0.85           # --mem-fraction-static: the ~16GB AWQ weights need >0.66; HiCache spills the rest of the 256k KV to CPU RAM.
tool_parser        = "qwen25"       # --tool-call-parser: Qwen3-2507 emits Qwen <tool_call> blocks (qwen25 parser handles them); SGLang's qwen25 parser fits the Qwen3 family.
reasoning_parser   = ""             # Qwen3-30B-A3B-INSTRUCT-2507 is NON-thinking (no <think>) -> no reasoning parser. For CoT, bake Qwen/Qwen3-30B-A3B-Thinking-2507 and set reasoning_parser = "qwen3".
kv_cache_dtype     = "fp8_e5m2"     # --kv-cache-dtype fp8_e5m2: ~2x KV capacity; REQUIRED with HiCache to reach 256k.
hierarchical_cache = true           # --enable-hierarchical-cache (HiCache): spill inactive KV to CPU RAM -- the mechanism that makes 256k fit
unified_radix_tree = true           # --enable-unified-radix-tree (UnifiedRadixTree): enable radx-tree KV cache optimization

[packages]
sections = [
    "base", "security", "utils", "build-toolchain", "containers",
    "cockpit", "storage", "virt", "guests",
    "gpu-mesa", "gpu-nvidia", "gpu-amd-compute", "gpu-intel-compute",
    "gnome", "gnome-core-apps", "phosh",
    "ai", "k3s", "k3s-selinux-build", "cockpit-plugins-build",
    "sbom-tools", "uki", "self-build", "network-discovery", "updater",
    "wintools", "gaming", "steam", "nut", "ceph", "freeipa", "ha",
    "looking-glass-build", "android",
]

[packages.windows]
enable = true
pkgs = [
    # ── Critical runtime / toolchain ─────────────────────────────
    "Git.Git",                       # git for clone + fetch + reset
    "Microsoft.PowerShell",          # pwsh 7 (trampoline target)
    "Microsoft.WSL",                 # Windows Subsystem for Linux 2
    "Microsoft.WindowsTerminal",     # WT (also installed earlier; idempotent)
    "7zip.7zip",                     # archive support
    "Microsoft.VCRedist.2015+.x64",  # VC++ runtime
    "Python.Python.3.14",            # python 3.14 (matches MiOS-DEV 3.14.x) -- host-side MiOS python tooling
    # ── MiOS terminal CLI surface (mirrors the deployed Linux side) ─
    "sharkdp.bat",                   # syntax-highlighted cat
    "sharkdp.fd",                    # fast find
    "BurntSushi.ripgrep.MSVC",       # rg
    "junegunn.fzf",                  # fuzzy finder
    "jqlang.jq",                     # JSON query
    "GitHub.cli",                    # gh
    "aristocratos.btop4win",         # btop (Windows port)
    "fastfetch-cli.fastfetch",       # fastfetch
    "Anchore.Syft",                  # syft (CycloneDX / SPDX SBOM generator)
]

# Post-install PATH verification probes -- the binaries build-mios.ps1's
# Install-MiosWindowsTools loops through to confirm `Get-Command <name>`
# resolves after the winget installs + PATH augmentation. Operators who
# add a custom package above (or remove the default set) extend / shrink
# this list to match.
verify_probes = [
    "python",
    "fastfetch",
    "btop",
    "rg",
    "fzf",
    "jq",
    "gh",
    "bat",
    "fd",
    "pwsh",
    "oh-my-posh",
    "syft"
]

[browser_ai]
enable       = true
package      = "Zen-Team.Zen-Browser.Twilight"   # Zen Browser - Twilight (winget id)
provider_url = "http://localhost:${MIOS_PORT_OPEN_WEBUI}"            # OWUI front door = the MiOS AI pipeline
# Firefox/Zen prefs as "name|type|value" (type = bool|string|int) so the bootstrap
# can build policies.json without a full TOML parser. Add Zen-specific zen.* AI
# flags here as they stabilise -- they flow through unchanged.
prefs = [
    "browser.ml.enable|bool|true",                # master ML/AI toggle
    "browser.smartwindow.enabled|bool|true",      # the NEW AI Window (Smart Window) mode -- replaces the old sidebar
    "browser.ml.chat.enabled|bool|true",          # AI chatbot sidebar (fallback path)
    "browser.ml.chat.hideLocalhost|bool|false",   # REQUIRED so a localhost (OWUI) provider is allowed
    "browser.ml.chat.shortcuts|bool|true",        # AI prompts on text selection
    "browser.ml.chat.menu|bool|true",             # AI entries in the context menu
    "browser.ml.pageAssist.enabled|bool|true",    # page-assist AI
    "browser.ml.linkPreview.enabled|bool|true",   # AI link previews
]

[packages.dev_vm_essentials]
enable = true
pkgs = [
    "mkpasswd",            # sha512crypt password hash
    "whois",               # mkpasswd is in mkpasswd pkg on F44, but ship whois too
    "openssl",             # password fallback + general TLS
    "python3-passlib",     # password fallback (Python 3.13+ removed crypt)
    "python3-bcrypt",      # bcrypt backend for passlib + mios-adguard-firstboot admin pw
    "bootc",               # bootc switch + self-replication closure
    "git",                 # in-distro clone
    "curl",                # required for Bibata/AI CLI downloads
    "tar",                 # required for Bibata extraction
    "iptables",            # netavark backend (binary; firewall_driver=none)
    "nftables",            # netavark alternative backend
    "fastfetch",           # MiOS dashboard / motd
    "oh-my-posh",          # MiOS themed prompt
    "bash-completion",     # tab-complete in interactive shells
]

[packages.dev_overlay]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
sections = [
    "base", "security", "utils", "build-toolchain", "containers",
    "cockpit", "storage", "virt",
    "gpu-mesa", "gpu-nvidia", "gpu-amd-compute", "gpu-intel-compute",
    "gnome-flatpak-runtime",
    "ai", "sbom-tools", "self-build", "network-discovery", "updater",
    "cockpit-plugins-build", "k3s-selinux-build", "uki",
    "docgen",
]



[packages.repos]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "rpmfusion-free-release-rawhide",
    "rpmfusion-nonfree-release-rawhide",
    "fedora-workstation-repositories",
    "dnf-plugins-core",
    "dnf5-plugins",
]

[packages.base]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "policycoreutils-python-utils",
    "selinux-policy-targeted",
    "firewalld",
    "audit",
    "fapolicyd",
    "usbguard",
    "python3.13",
    # The manual reader itself: without it the rendered pages are
    # unreachable on the running system.
    "man-db",
]

[packages.crowdsec]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "crowdsec",
]

[packages.moby]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "moby-engine",
]

[packages.uki]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "systemd-ukify",
]

[packages.sbom-tools]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "syft",
]

[packages.k3s-selinux-build]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "selinux-policy-devel",
    "git",
    "make",
]

[packages.kernel]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "kernel-modules-extra",
    "kernel-devel",
    "kernel-headers",
    "kernel-tools",
    "glibc-headers",
    "glibc-devel",
    "python3",
    "python3.13",
]

[packages.gnome]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "gnome-shell",
    "gnome-session-wayland-session",
    "gnome-control-center",
    "gnome-keyring",
    "gdm",
    "gnome-remote-desktop",
    "gnome-backgrounds",
    "gnome-shell-extension-appindicator",
    "gnome-shell-extension-dash-to-dock",
    "xdg-user-dirs",
    "xdg-utils",
    "xdg-desktop-portal",
    "xdg-desktop-portal-gnome",
    "xdg-desktop-portal-gtk",
    "pipewire-alsa",
    "pipewire-pulseaudio",
    "wireplumber",
    "gstreamer1",
    "gstreamer1-plugins-base",
    "gstreamer1-plugins-good",
    "upower",
    "gnome-bluetooth",
    "bluez",
    "bluez-tools",
    "flatpak",
    "gvfs",
    "gvfs-smb",
    "gvfs-mtp",
    "NetworkManager-wifi",
    "NetworkManager-openvpn-gnome",
    "nm-connection-editor",
    "glibc-langpack-en",
    "qt6-qtbase-gui",
    "qt6-qtwayland",
    "qadwaitadecorations-qt5",
    "adw-gtk3-theme",
]

[packages.gnome-flatpak-runtime]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "xdg-user-dirs",
    "xdg-utils",
    "xdg-desktop-portal",
    "xdg-desktop-portal-gnome",
    "xdg-desktop-portal-gtk",
    "pipewire-alsa",
    "pipewire-pulseaudio",
    "wireplumber",
    "gstreamer1",
    "gstreamer1-plugins-base",
    "gstreamer1-plugins-good",
    "gvfs",
    "gvfs-smb",
    "gvfs-mtp",
    "glibc-langpack-en",
    "qt6-qtbase-gui",
    "qt6-qtwayland",
    "qadwaitadecorations-qt5",
    "adw-gtk3-theme",
    "adwaita-icon-theme",
    "gnome-themes-extra",
    "dconf",
    "gsettings-desktop-schemas",
    "gnome-keyring",
    "flatpak",
]

[packages.gnome-core-apps]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "gnome-software",
    "grim",
    "gnome-screenshot",
]

[packages.gpu-mesa]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "mesa-vulkan-drivers",
    "mesa-dri-drivers",
    "mesa-va-drivers",
    "vulkan-loader",
    "vulkan-tools",
    "libva-utils",
    "linux-firmware",
]

[packages.gpu-amd-compute]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "rocm-opencl",
    "rocm-hip",
    "rocm-runtime",
    "rocm-smi",
    "rocminfo",
]

[packages.gpu-intel-compute]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "intel-compute-runtime",
    "intel-media-driver",
    "intel-level-zero",
    "igt-gpu-tools",
]

[packages.gpu-nvidia]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "akmod-nvidia",
    "xorg-x11-drv-nvidia-cuda",
    "nvidia-container-toolkit",
    "nvidia-persistenced",
    "nvidia-settings",
    "xorg-x11-drv-nvidia-power",
    "nvidia-container-selinux",
]

[packages.gpu-cdi-toolkits]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
]

[packages.virt]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "qemu-kvm",
    "libvirt",
    "libvirt-daemon",
    "virt-install",
    "edk2-ovmf",
    "swtpm",
    "swtpm-tools",
    "dnsmasq",
    "mdevctl",                        # keep only for legitimate SR-IOV/mdev inventory; NOT a vGPU path (ref F3)
    "libguestfs-tools",
    "qemu-device-display-virtio-gpu",
    "virt-firmware",
    "python3-cryptography",
]

[packages.containers]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "podman",
    "podman-compose",
    "buildah",
    "skopeo",
    "bootc",
    "osbuild",
    "osbuild-composer",
    "osbuild-selinux",
    "composer-cli",
    "rpm-ostree",
    "crun",
    "netavark",
    "aardvark-dns",
    "slirp4netns",
    "composefs",
    "container-selinux",
    "qemu-img",
    "image-builder",
    "dracut-live",
    "squashfs-tools",
    "containers-common",
    "toolbox",
    "kubectl",
    "helm",
    "sigstore-cosign",
]

[packages.build-toolchain]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "make",
    "gcc",
    "gcc-c++",
    "cpp",
    "cmake",
    "cmake-filesystem",
    "golang",
    "golang-bin",
    "selinux-policy-devel",
    "binutils",
    "pkgconf-pkg-config",
]

[packages.self-build]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "buildah",
    "podman",
    "skopeo",
    "ostree",
    "bootc",
]

[packages.boot]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "bootupd",
    "dnf5-plugins",
    "systemd-boot-unsigned",
    "efibootmgr",
    "systemd-ukify",
    "binutils",
    "efitools",
    "sbsigntools",
    "tpm2-tss",
]

[packages.cockpit]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "sscg",
    "cockpit",
    "cockpit-system",
    "cockpit-ws",
    "cockpit-bridge",
    "cockpit-storaged",
    "cockpit-networkmanager",
    "cockpit-podman",
    "cockpit-machines",
    "cockpit-ostree",
    "cockpit-selinux",
    "cockpit-files",
    "cockpit-sosreport",
    "cockpit-kdump",
    "pcp",
    "pcp-system-tools",
    "pcp-zeroconf",
    "pcp-pmda-systemd",
    "pcp-pmda-openmetrics",
    # redis-compatible key-server -- pmproxy/pmseries time-series backend
    # that Cockpit's Metrics HISTORY queries. Required (see note above).
    "keydb",
    "ttyd",
]

[packages.wintools]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "hyperv-tools",
    "samba",
    "samba-client",
    "cifs-utils",
    "freerdp-libs",
]

[packages.security]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "firewalld",
    "fapolicyd",
    "fapolicyd-selinux",
    "usbguard",
    "setroubleshoot-server",
    "policycoreutils-python-utils",
    "audit",
    "tpm2-tools",
    # The portable-drive unlock a TPM2 PCR policy cannot be (ADR-0016 D13.1).
    "libfido2",
    "fido2-tools",
    "clevis",
    "clevis-luks",
    "aide",
    "openscap-scanner",
    "scap-security-guide",
    "openscap-utils",
    "libpwquality",
    "nftables",
    "policycoreutils",
    "setools-console",
    "sigstore-cosign",
    "iptables-legacy",
]

[packages.gaming]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "gamemode",
    "gnome-shell-extension-gamemode",
    "vulkan-tools",
    "steam-devices",
]

[packages.steam]
enable = true
pkgs = [
    "steam",                  # Steam launcher + bundled Proton runtime
    "wine",                   # Win32/Win64 compatibility layer (>=11 in Fedora 44)
    "wine-staging",           # patchset for newer-game compat
    "winetricks",             # script collection for game prereqs
    "lutris",                 # game launcher with multiple runners
    "gamescope",              # Valve's microcompositor (HDR / FSR upscaling)
    "mangohud",               # in-game perf HUD
    "vkbasalt",               # post-processing (sharpening, AO)
    "goverlay",               # GUI for mangohud / vkbasalt config
    "protontricks",           # winetricks for Steam-installed games
]

[packages.guests]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "qemu-guest-agent",
    "hyperv-daemons",
    "open-vm-tools",
    "spice-vdagent",
    "spice-webdavd",
    "libvirt-nss",
]

[packages.storage]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "nfs-utils",
    "rpcbind",
    "ceph-common",
    "iscsi-initiator-utils",
    "targetcli",
    "device-mapper-multipath",
    "sg3_utils",
    "lvm2",
    "stratis-cli",
    "stratisd",
    "xfsprogs",
    "btrfs-progs",
    "e2fsprogs",
    "mdadm",
    "ntfs-3g",
]

[packages.ceph]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "ceph-common",
    "cephadm",
    "ceph-fuse",
    "ceph-selinux",
]

[packages.k3s]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "container-selinux",
]

[packages.ha]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "pacemaker",
    "corosync",
    "pcs",
    "fence-agents-all",
    "fence-virt",
    "resource-agents",
    "sbd",
    "booth",
    "booth-core",
    "booth-test",
    "dlm",
    "corosync-qdevice",
    "corosync-qnetd",
    "pcs-web-ui",
    "libqb",
    "libibverbs",
]

[packages.hyprland]
enable = true
pkgs = [
    "hyprland",
    "xorg-x11-server-Xwayland",
    "wayland-protocols-devel",
    "grim",
    "slurp",
    "wl-clipboard",
    "python3-evdev",
    "python3-gobject"
]

[packages.quickshell-build]
enable = true
pkgs = [
    "qt6-qtbase-devel",
    "qt6-qtdeclarative-devel",
    "qt6-qtwayland-devel",
    "qt6-qtshadertools-devel",
    "qt6-qtbase-private-devel",
    "pipewire-devel",
    "cli11-devel",
    "libdrm-devel",
    "cpptrace-devel",
    "mesa-libgbm-devel",
    "polkit-devel",
    "pam-devel",
    "jemalloc-devel"
]

[packages.utils]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "git",
    "tmux",
    "vim-enhanced",
    "wget2-wget",
    "curl",
    "btop",
    "nvtop",
    "fastfetch",
    "lm_sensors",
    "smartmontools",
    "tuned",
    "tuned-ppd",
    "fuse",
    "fuse3",
    "fuse-overlayfs",
    "7zip-standalone",
    "unzip",
    "zstd",
    "rsync",
    "tree",
    # plocate: the indexed fast path for fs_search / linux_file_search. Without
    # it those verbs fall back to a full find-walk (~11 s vs sub-100 ms), so the
    # script's advertised plocate path never fires. The DB is refreshed off the
    # mios-daemon directory-indexer cadence (no separate updatedb timer).
    "plocate",
    "jq",
    "yq",
    "bc",
    "patch",
    "openssl",
    "distrobox",
    "just",
    "driverctl",
    "tmt",
    "ansible-core",
    "python3-pip",
    "python3-requests",
    "cloud-init",
    "libei",
    "strace",
    "lsof",
    "iotop",
    "socat",
]

[packages.android]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "waydroid",
]

[packages.looking-glass-build]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "cmake",
    "gcc",
    "gcc-c++",
    "make",
    "binutils",
    "pkgconf-pkg-config",
    "libglvnd-devel",
    "fontconfig",
    "fontconfig-devel",
    "spice-protocol",
    "nettle-devel",
    "gnutls-devel",
    "libXi-devel",
    "libXinerama-devel",
    "libXcursor-devel",
    "libXpresent-devel",
    "libXScrnSaver-devel",
    "libxkbcommon-x11-devel",
    "wayland-devel",
    "wayland-protocols-devel",
    "libdecor-devel",
    "pipewire-devel",
    "libsamplerate-devel",
]

[packages.cockpit-plugins-build]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "npm",
    "gettext",
]

[packages.network-discovery]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "avahi",
    "avahi-tools",
    "nss-mdns",
    # The OPTIONAL radio plane (ADR-0016 D13.2/D14.1). hostapd, not
    # NetworkManager AP mode: 6 GHz REQUIRES WPA3-SAE + MFP.
    "hostapd",
    "iw",
    "wireless-regdb",
]

[packages.phosh]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "phosh",
    "phoc",
    "gnome-calls",
    "feedbackd",
]

[packages.updater]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "uupd",
    "greenboot",
    "greenboot-default-health-checks",
]

[packages.freeipa]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "freeipa-client",
    "sssd",
    "sssd-tools",
    "libsss_nss_idmap",
]

[packages.ai]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "python3-openai",
    "nodejs",           # required by claude-code + gemini-cli (npm-installed)
    "npm",
    "nano",             # baseline text editor for operator config edits (operator-flagged: "nano: command not found")
]
npm_globals = [
    "@anthropic-ai/claude-code",     # Anthropic Claude Code CLI
    "@google/gemini-cli",            # Google Gemini CLI
]

[packages.critical]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "gnome-shell",
    "gdm",
    "podman",
    "bootc",
    "libvirt",
    "kernel-core",
    "firewalld",
    "cockpit",
    "NetworkManager",
    "pipewire",
    "tuned",
    "chrony",
    "openssh-server",
]
[packages.bloat]
enable = false  # opt-in only; gnome-tour / PackageKit / malcontent — bloat by design, default OFF
pkgs = [
    "malcontent-control",
    "malcontent-pam",
    "malcontent-tools",
    "gnome-tour",
    "gnome-initial-setup",
    "PackageKit",
    "PackageKit-command-not-found",
]

[packages.nut]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "nut",
    "nut-client",
    "nut-xml",
    "usbutils",
]

# WS-4 P0 doc-gen: FOSS, fully-offline office-document generation binaries for
# mios-docgen (Pandoc = markdown<->docx/pptx/html/pdf; LibreOffice headless =
# office-binary conversion, csv->xlsx, and PDF when no LaTeX engine is present).
# All GPL/MPL FOSS, no network at runtime. Default ON so the binaries ship, but
# the FEATURE stays gated by [computer_use].docgen_enable (default off).
[packages.docgen]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "pandoc",
    "libreoffice-core",
    "libreoffice-writer",
    "libreoffice-calc",
    "libreoffice-impress",
]

[packages.glibc-hwcaps-v3]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "glibc-hwcaps-x86-64-v3",
]

[packages.glibc-hwcaps-v4]
enable = true   # default ON; toggle via /configurator.html (configurator.packages.<section>.enable)
pkgs = [
    "glibc-hwcaps-x86-64-v4",
]




[ports]
stack_id           = 0        # Multiplier offset: ports dynamically shift by +(stack_id * 10000)
ssh                = 8100     # host admin sshd. "mios-dev ssh should be port 2222" -- hardened off :22, off the prior :49955. firewall opens this; sshd_config Port drop-in lives at /etc/ssh/sshd_config.d/09-mios-ssh-port.conf and must match. Forgejo git-ssh moved to forge_ssh=49922 to vacate 2222.
forge_http         = 8400     # Forgejo HTTP web UI
forge_ssh          = 8410    # Forgejo git+ssh (vacated 2222 for host admin sshd). Non-22 to coexist with sshd; non-2222 to leave that as the canonical operator-SSH port.
cockpit            = 8110     # host Cockpit web console
cockpit_link       = 8120    # mios-cockpit-link Podman Desktop discovery shim
searxng            = 8800     # local privacy-respecting metasearch
crawl4ai           = 8810    # local crawl engine -- the crawl4ai FastAPI (crawl4ai over Chrome CDP + Camoufox fallback), the crawl4ai member of the mios-webtools pod. LOOPBACK only (127.0.0.1), NOT a published/LAN port.
firecrawl          = 8820     # self-hosted firecrawl api -- the firecrawl-api member of the mios-webtools pod (Hermes's native web backend; FIRECRAWL_API_URL). LOOPBACK only (127.0.0.1), NOT a published/LAN port. (3002 is firecrawl's own default; free on MiOS -- 3000=forge_http, 3033=open_webui.)
hermes             = 8720     # Hermes gateway (canonical /v1). ONE key: hermes-worker.service is the only unit running `hermes gateway run` and binds this.
hermes_dashboard   = 8210     # Hermes Dashboard web UI (sessions, skills, stats)
open_webui         = 8200     # Open WebUI (rich LLM interface at http://localhost:3033)
# hermes_workspace REMOVED (port 3033 freed) -- use the
# hermes-agent dashboard at port 9119 instead.
code_server        = 8900     # code-server (VS Code in a browser; matches image default)
k3s_api            = 8450     # K3s Kubernetes API
guacamole_web      = 8220     # mios-guacamole browser desktop
ceph_dashboard     = 8460     # Ceph dashboard
rdp                = 8300     # GNOME Remote Desktop / xRDP
pgvector           = 8600     # PostgreSQL + pgvector unified agent DB (WS-9, FOSS; sole agent DB since the WS-A3 cutover)
llm_light          = 8500    # mios-llm-light: the primary llama.cpp multi-model + KV-paging lane (WS-10, FOSS; served via the upstream llama-swap proxy image)
cpu_node           = 8510    # mios-cpu-node: the always-on / gaming-immune CPU llama-server lane (granite brain on -ngl 0). Rendered into mios-cpu-node.container's --port (was a DEAD key -> empty --port -> llama.cpp stoi crash-loop; SSOT-wired).
agent_pipe         = 8700     # 'MiOS' standalone Agent Pipe (router + refine + critic; fronts hermes for every gateway)
ttyd_bash          = 8310     # ttyd browser pty -- Linux bash session
ttyd_powershell    = 8320     # ttyd browser pty -- Windows-side PowerShell session
adguard_dns        = 53       # AdGuard Home DNS resolver (ad/tracker/malware sinkhole; tailnet-wide global nameserver)
adguard_ui         = 8050     # AdGuard Home web UI + REST API (3000 is forge_http; 3053 is mnemonic for :53)
opencode_gateway   = 8780     # opencode -> OpenAI /v1 gateway shim (mios-opencode-gateway.service); makes [agents.opencode] a REAL /v1 council peer. LOOPBACK only (127.0.0.1).
vllm               = 8520    # vLLM heavy dGPU lane (re-scoped mios-llm-heavy Quadlet; OpenAI /v1, PagedAttention+APC). Off the iGPU's :11436 + finetune-serve :11438. GATED/disabled by default (VRAM). [ai.vllm] + [nodes.local-vllm].
sglang             = 8530    # SGLang heavy dGPU lane (OpenAI /v1, HiCache CPU KV-offload). GATED/disabled by default (VRAM). [ai.sglang] + [nodes.local-sglang].
prefilter          = 8710    # mios-delegation-prefilter: conversational-bypass classifier + delegation gate (OpenAI /v1 shim in front of hermes). LOOPBACK only.
arbiter            = 8760    # mios-policy-arbiter: risk-tier arbiter (verb/risk allow-list enforcer). LOOPBACK only. (Mirrors agent.arbiter_port; SSOT migrated here for port-table completeness.)
daemon_agent       = 8740    # mios-daemon-agent: background inference worker lane (low-priority; CPU-affined). LOOPBACK only.
model_router       = 8750    # mios-model-router: internal model-selection micro-router (routes by modality/task). LOOPBACK only.
oscontrol          =  8950   # mios-oscontrol-server: Windows-side UI Automation / PowerShell executor (HTTP gate; mios-computer-use-server mirrors protocol). LOOPBACK only.
mcp                = 8770    # MCP host server (/mcp + A2A /.well-known; served by hermes-agent). LOOPBACK only. (Matches [agent].server_port when that key is absent.)
guacd              = 8560    # guacd protocol daemon behind mios-guacamole (upstream default 4822). Was hardcoded in the Quadlet with no SSOT key.
redis              = 8565    # redis/valkey backing the firecrawl api+worker in the mios-webtools pod (was hardcoded 6380 in three Quadlets, no SSOT key).
chrome_cdp         = 9222    # Chrome DevTools Protocol on the PRIMARY browser (mios-hermes-browser.service). Pinned: an external contract, and its binder is a static flatpak flags file.
chrome_cdp_worker  = 9223    # CDP on the SECOND browser (mios-hermes-browser-worker.service, profile-w2). Pinned alongside its primary.
otelcol_otlp       = 8575    # OpenTelemetry OTLP gRPC receiver on mios-otelcol (upstream default 4317).
otelcol_ui         = 8580    # Jaeger all-in-one query UI on mios-otelcol (upstream default 16686).
pxe_hub_api        = 8585    # matchbox gRPC/API on mios-pxe-hub (upstream default 8081; its HTTP side rides forge_http's band).

# Files pairing a MIOS_PORT_* name with a literal that disagrees with this table.
# SHRINK-ONLY (gate: check_port_fallbacks), and currently EMPTY -- a new entry
# needs a reason here, not just a line.
stale_fallbacks = []

# Allocated ports nothing references yet. SHRINK-ONLY (gate: check_ports_bound).
# Per-key reasons: TASKS.md T-318. chrome_cdp_worker's only binder is an
# `Environment=` literal in a shipped .service -- systemd does not expand ${} in
# Environment=, and the [units] block that WOULD render the placeholder is inert
# until T-317 makes mios-unit-gen faithful.
unbound = ["chrome_cdp_worker"]

[ports.categories.edge]
base    = 8050
stride  = 1
members = ["adguard_ui"]
pinned  = { adguard_dns = 53 }
doc     = "Network edge / resolver. DNS is protocol-pinned at 53 and never floats."

[ports.categories.admin]
base    = 8100
stride  = 10
members = ["ssh", "cockpit", "cockpit_link"]
doc     = "Host administration surfaces (operator SSH, Cockpit console + discovery shim)."

[ports.categories.webui]
base    = 8200
stride  = 10
members = ["open_webui", "hermes_dashboard", "guacamole_web"]
doc     = "Browser-facing application UIs a human opens directly."

[ports.categories.desktop]
base    = 8300
stride  = 10
members = ["rdp", "ttyd_bash", "ttyd_powershell"]
doc     = "Remote desktop and browser-pty session surfaces."

[ports.categories.forge]
base    = 8400
stride  = 10
members = ["forge_http", "forge_ssh"]
doc     = "Source forge and CI (Forgejo web + git-over-ssh)."

[ports.categories.cluster]
base    = 8450
stride  = 10
members = ["k3s_api", "ceph_dashboard"]
doc     = "Cluster orchestration and storage control planes."

[ports.categories.inference]
base    = 8500
stride  = 10
members = ["llm_light", "cpu_node", "vllm", "sglang"]
doc     = "Model-serving lanes. Ordered cheapest-to-heaviest: always-on llama.cpp, CPU lane, then the GATED dGPU lanes."

[ports.categories.data]
base    = 8600
stride  = 10
members = ["pgvector"]
doc     = "Datastores (the unified agent DB)."

[ports.categories.agent]
base    = 8700
stride  = 10
members = ["agent_pipe", "prefilter", "hermes", "", "daemon_agent", "model_router", "arbiter", "mcp", "opencode_gateway"]
doc     = "Agent plane. Ordered along the request path: pipe -> prefilter -> hermes -> workers -> router -> arbiter, then the MCP host and the /v1 gateway shims. All LOOPBACK-only except hermes."

[ports.categories.webtools]
base    = 8800
stride  = 10
members = ["searxng", "crawl4ai", "firecrawl"]
doc     = "Search, crawl and scrape backends (the mios-webtools pod). LOOPBACK-only."

[ports.categories.devtools]
base    = 8900
stride  = 10
members = ["code_server"]
doc     = "Developer surfaces served to a browser."

[ports.categories.sidecar]
base    = 8560
stride  = 5
members = ["guacd", "redis", "", "otelcol_otlp", "otelcol_ui", "pxe_hub_api", ""]
# CDP is an external contract, like DNS/53: DevTools clients and Playwright
# assume 9222, and the primary binder is a static flatpak flags file.
pinned  = { chrome_cdp = 9222, chrome_cdp_worker = 9223 }
doc     = "Supporting daemons that bind a real port but are not user-facing services. Each was HARDCODED in a Quadlet with no SSOT key (guacd 4822, redis 6380, Chrome CDP 9222, OTLP 4317, Jaeger query 16686, matchbox 8081), so nothing could detect a collision when a container was added. Containers bind the SSOT port (published host-side), while upstream defaults are kept in-container only when published behind host-side SSOT mapping; all Quadlet :-N fallbacks are strictly reconciled against SSOT and enforced by TestQuadletPortFallbacks. Index 6 is a RESERVED slot -- forge_ssh_git named a second Forgejo SSH listener that does not exist, since SSH_PORT and SSH_LISTEN_PORT both resolve MIOS_PORT_FORGE_SSH."

[ports.categories.bridge]
base    = 8950
stride  = 10
members = ["oscontrol"]
doc     = "Cross-OS bridges (Windows-side UI Automation executor). LOOPBACK-only."


[units]
forge              = "mios-forge.service"
forge_runner       = "mios-forgejo-runner.service"
llm_light          = "mios-llm-light.service"
pgvector           = "mios-pgvector.service"
agent_pipe         = "mios-agent-pipe.service"
ceph               = "mios-ceph.service"
k3s                = "mios-k3s.service"
cockpit_link       = "mios-cockpit-link.service"
searxng            = "mios-searxng.service"
hermes             = "mios-hermes.service"
open_webui         = "mios-open-webui.service"
hermes_firstboot   = "mios-hermes-firstboot.service"
code_server        = "mios-code-server.service"
firstboot_target   = "mios-firstboot.target"
wsl_firstboot      = "mios-wsl-firstboot.service"
user_session       = "user@${MIOS_UID}.service"

# ----------------------------------------------------------------------------
# [urls] -- the BROWSER-OPENABLE surface: portal tiles and openInBrowser
# labels, http/https only, never the inter-service endpoint table. Values
# template a [ports] key. Gate: check_service_urls.
# ----------------------------------------------------------------------------
[urls]
forge              = "http://localhost:${MIOS_PORT_FORGE_HTTP}"
cockpit            = "https://localhost:${MIOS_PORT_COCKPIT}"
searxng            = "http://localhost:${MIOS_PORT_SEARXNG}"
open_webui         = "http://localhost:${MIOS_PORT_OPEN_WEBUI}/"
code_server        = "http://localhost:${MIOS_PORT_CODE_SERVER}/"
otelcol_ui         = "http://localhost:${MIOS_PORT_OTELCOL_UI}/"
chrome_cdp         = "http://localhost:${MIOS_PORT_CHROME_CDP}/"
local_forge_repo   = "http://localhost:${MIOS_PORT_FORGE_HTTP}/mios/mios.git"
repo               = "https://github.com/mios-dev/MiOS.git"
bootstrap_repo     = "https://github.com/mios-dev/mios-bootstrap.git"

# Ports with no browser-openable URL: either they serve no page, or their
# address is already a key their consumers resolve -- agent_pipe is
# MIOS_AI_ENDPOINT, pgvector MIOS_DB_URL, llm_light MIOS_LLM_CPU_ENDPOINT,
# hermes MIOS_HERMES_ENDPOINT, crawl4ai MIOS_CRAWL_SERVICE_URL, otelcol_otlp
# [observability].otel_endpoint.
non_addressable = [
  "adguard_dns", "adguard_ui", "agent_pipe", "arbiter", "ceph_dashboard",
  "crawl4ai", "hermes", "llm_light", "pgvector",
  "chrome_cdp_worker", "cockpit_link", "cpu_node", "daemon_agent",
  "firecrawl",
  "forge_ssh", "guacamole_web", "guacd", "hermes_dashboard",
  "k3s_api", "mcp", "model_router", "opencode_gateway", "oscontrol",
  "otelcol_otlp", "prefilter", "pxe_hub_api", "rdp", "redis",
  "sglang", "ssh", "ttyd_bash", "ttyd_powershell", "vllm",
]

# ----------------------------------------------------------------------------
# [generator] -- Quadlet & manifest generator configuration
# ----------------------------------------------------------------------------
[generator]
placeholders             = ["FEDORA_VERSION", "MIOS_VERSION"]

[versions]
fedora                   = "44"
k3s                      = "v1.36.3-k3s1"
k8s_repo                 = "v1.36"
forgejo                  = "12"
ceph                     = "v19"

# ----------------------------------------------------------------------------
# [image.sidecars] -- pinned upstream container references for every
# Quadlet sidecar. version + image (where present) are paired; consumers
# read either via MIOS_<svc>_VERSION / MIOS_<svc>_IMAGE.
# ----------------------------------------------------------------------------
[image.sidecars]
sys                = "localhost/mios-sys:latest"
cuda               = "ghcr.io/mostlygeek/llama-swap:cuda"
k3s                = "docker.io/rancher/k3s:${MIOS_VERSION_K3S}"
ceph               = "quay.io/ceph/ceph:${MIOS_VERSION_CEPH}"
forge              = "codeberg.org/forgejo/forgejo:${MIOS_VERSION_FORGEJO}"
searxng            = "docker.io/searxng/searxng:latest"
hermes             = "docker.io/nousresearch/hermes-agent:latest"
open_webui         = "ghcr.io/open-webui/open-webui:main"
# hermes_workspace sidecar REMOVED -- replaced by the
# hermes-agent's built-in dashboard at port 9119.
code_server        = "ghcr.io/coder/code-server:latest"
guacamole          = "docker.io/guacamole/guacamole:latest"
forge_runner       = "code.forgejo.org/forgejo/runner:7"
crowdsec           = "docker.io/crowdsecurity/crowdsec:latest"
postgres           = "docker.io/library/postgres:latest"
guacd              = "docker.io/guacamole/guacd:latest"
pxe_hub            = "quay.io/poseidon/matchbox:latest"
bib_alpine         = "docker.io/library/alpine:latest"
pgvector           = "docker.io/pgvector/pgvector:pg18"
# mios-llm-light lane image. The upstream llama-swap proxy (FOSS, MIT) bundles
# llama-server + on-demand model swap. Tag carries the CUDA build; verify version.
llm_light          = "ghcr.io/mostlygeek/llama-swap:cuda"
# AdGuard Home -- open-source (GPLv3) DNS sinkhole + recursive resolver. Single
# Go binary container; serves DNS on :53 and the web UI/API on :3053.
adguard            = "docker.io/adguard/adguardhome:latest"
vllm               = "docker.io/vllm/vllm-openai:latest"
sglang             = "docker.io/lmsysorg/sglang:latest"
valkey             = "docker.io/valkey/valkey:latest"
# Pinned, not floating: `all-in-one` is the Jaeger v1 line only (v2 ships as
# jaegertracing/jaeger), and the tag decides which env model the collector's
# host-port vars belong to -- so a float would silently unbind two SSOT ports.
otelcol            = "docker.io/jaegertracing/all-in-one:1.76.0"

# ----------------------------------------------------------------------------
# [image.sys] / [image.cuda] -- SSOT sections for the consolidated shared bases
# ----------------------------------------------------------------------------
[image.sys]
base = "ghcr.io/ublue-os/ucore-hci:stable-nvidia"
packages = [
  "python3", "python3-pip", "python3-devel",
  "nodejs", "npm",
  "chromium", "chromium-common", "chromium-sandbox",
  "socat", "valkey", "guacd", "git", "make", "gcc-c++"
]

[image.cuda]
base = "ghcr.io/ublue-os/ucore-hci:stable-nvidia"
packages = [
  "python3", "python3-pip", "python3-devel"
]

[services.forge]
user = "mios-forge"
uid  = 816
gid  = 816

[services.open_webui]
user = "mios-open-webui"
uid  = 817
gid  = 817

[services.searxng]
user = "mios-searxng"
uid  = 818
gid  = 818

[services.ceph]
user = "mios-ceph"
uid  = 819
gid  = 819

[services.hermes]
user = "mios-hermes"
uid  = 820
gid  = 820

# Migration note: the legacy datastore service (UID 821) is retired. Datastore is now mios-pgvector.

# WS-9 unified agent-plane Postgres+pgvector. AI tier (mios-ai) so agents read it
# like the legacy DB; own uid for /var/lib ownership stability. 826 = next free after
# mios-adguard (825) in the 810-829 sidecar range.
[services.pgvector]
user = "mios-pgvector"
uid  = 826
gid  = 826

# WS-10 mios-llm-light llama.cpp lane (upstream llama-swap proxy). AI tier (mios-ai). 827 = next free after
# mios-pgvector (826) in the 810-829 sidecar range.
[services.llamacpp]
user = "mios-llamacpp"
uid  = 827
gid  = 827

[services.agent_pipe]
user = "mios-agent-pipe"
uid  = 822
gid  = 822

# Migration note: the retired CPU-inference service (UID 823) is gone; inference is now handled by mios-llm-light.

[services.webtools]
user = "mios-crawl4ai"
uid  = 824
gid  = 824
# Runtime tunables for the crawl4ai + firecrawl members of the mios-webtools pod.
# SSOT for the ${MIOS_CRAWL_*}/${MIOS_FIRECRAWL_*} placeholders consumed by
# mios-webtools-crawl4ai.container + mios-webtools-firecrawl-{api,worker}.container;
# mapped to env in tools/lib/userenv.sh and rendered via automation/34-render-quadlets.sh.
cdp_url            = "http://127.0.0.1:${MIOS_PORT_CHROME_CDP}"  # crawl4ai -> existing local Chrome over CDP (PRIMARY engine)
camoufox           = true                     # enable the Camoufox stealth-Firefox fail-retry fallback
min_chars          = 200                      # crawl4ai: minimum extracted chars before a page is considered non-empty
firecrawl_workers  = 2                        # NUM_WORKERS_PER_QUEUE for firecrawl-api + firecrawl-worker
firecrawl_bull_key = "mios"                   # BULL_AUTH_KEY shared between the firecrawl api + worker (loopback-only pod)
firecrawl_log_level = "INFO"                  # LOGGING_LEVEL for the firecrawl stack

[pods.mios-system]
description = "'MiOS' System pod (dns, storage, admin, sec, pxe, k3s, remote-desktop)"
network     = "host"
after       = ["network-online.target"]
wants       = ["network-online.target"]
wanted_by   = ["multi-user.target", "default.target"]
members     = [
  "mios-adguard",
  "mios-ceph",
  "mios-pxe-hub",
  "mios-k3s",
  "mios-guacamole",
  "mios-guacd"
]
doc = """Consolidated system services pod."""

[pods.mios-webtools]
description = "'MiOS' Webtools pod (webtools, search, devforge)"
network     = "host"
after       = ["network-online.target", "mios-hermes-browser.service", "mios-webtools-firstboot.service"]
wants       = ["network-online.target", "mios-hermes-browser.service", "mios-webtools-firstboot.service"]
wanted_by   = ["multi-user.target", "default.target"]
members     = [
  "mios-webtools-redis",
  "mios-webtools-firecrawl-api",
  "mios-webtools-firecrawl-worker",
  "mios-webtools-crawl4ai",
  "mios-searxng",
  "mios-forge",
  "mios-forgejo-runner",
  "mios-code-server",
  "mios-otelcol"
]
doc = """Consolidated webtools pod."""

[pods.mios-ai]
description = "'MiOS' AI pod (inference, heavy, data, ui)"
network     = "host"
after       = ["network-online.target"]
wants       = ["network-online.target"]
wanted_by   = ["multi-user.target", "default.target"]
members     = [
  "mios-llm-light",
  "mios-cpu-node",
  "mios-llm-worker@",
  "mios-llm-heavy",
  "mios-llm-heavy-alt",
  "mios-pgvector",
  "mios-open-webui"
]
doc = """Consolidated AI pod."""

[services.adguard]
user = "mios-adguard"
uid  = 825
gid  = 825

[adguard]
# Upstream resolvers -- DoH for privacy (no plaintext :53 to the ISP). The agent
# can add/replace these via the UI; this is the shipped default.
upstreams = [
  "https://dns.quad9.net/dns-query",
  "https://dns.cloudflare.com/dns-query",
]
# Bootstrap resolvers: plain DNS used ONLY to resolve the DoH hostnames above.
bootstrap = ["9.9.9.9", "1.1.1.1"]
# Open-source blocklists (hosts / AdGuard-filter syntax). AdGuard fetches +
# caches them, so blocking keeps working offline on the cached copy; only the
# periodic refresh needs internet.
blocklists = [
  "https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts",
  "https://big.oisd.nl",
  "https://adguardteam.github.io/HostlistsRegistry/assets/filter_1.txt",
]
# Tailscale MagicDNS stub (universal fixed IP). firstboot writes a split-DNS
# rule sending the discovered *.<magicdns-suffix> zone here so the pretty URLs
# keep resolving while everything else goes upstream with ad-blocking.
magicdns_resolver = "100.100.100.100"
# Optional explicit MagicDNS suffix override; empty = firstboot auto-discovers
# it from `tailscale status` so we never hardcode one tailnet's name.
magicdns_suffix = ""
# Cache TTL clamp (seconds).
cache_min_ttl = 60
cache_max_ttl = 86400
# Web UI admin login. Empty admin_password_bcrypt => firstboot AUTO-GENERATES a
# strong random password and drops the plaintext in /etc/mios/adguard/initial-
# admin-password (0600) so the UI is never left no-auth (it controls tailnet DNS
# once it's the global nameserver). Pin your own with a bcrypt hash:
#   htpasswd -nbB admin '<password>' | cut -d: -f2
admin_user = "admin"
admin_password_bcrypt = ""

# ----------------------------------------------------------------------------
# [paths] -- canonical FHS paths for runtime artifacts. Consumed by
# every script that needs to know where AI memory, scratch, journal,
# install env, or first-boot sentinels live.
# ----------------------------------------------------------------------------
[paths]
ai_dir              = "/usr/share/mios/ai"
ai_models_dir       = "/srv/ai/models"
ai_mcp_dir          = "/srv/ai/mcp"
ai_scratch_dir      = "/var/lib/mios/ai/scratch"
ai_memory_dir       = "/var/lib/mios/ai/memory"
ai_journal          = "/var/lib/mios/ai/journal.md"
install_env         = "/etc/mios/install.env"
profile_toml_vendor = "/usr/share/mios/profile.toml"
profile_toml_host   = "/etc/mios/profile.toml"
wsl_firstboot_done  = "/var/lib/mios/.wsl-firstboot-done"
mios_toml           = "/usr/share/mios/mios.toml"   # this file's vendor path

usr_dir                  = "/usr/lib/mios"
libexec_dir              = "/usr/libexec/mios"
share_dir                = "/usr/share/mios"
share_ai_dir             = "${MIOS_SHARE_DIR}/ai"
share_distrobox_dir      = "${MIOS_SHARE_DIR}/distrobox"
share_branding_dir       = "${MIOS_SHARE_DIR}/branding"
share_fastfetch_dir      = "${MIOS_SHARE_DIR}/fastfetch"
share_kb_dir             = "${MIOS_SHARE_DIR}/kb"
share_configurator_dir   = "${MIOS_SHARE_DIR}/configurator"
share_k3s_manifests_dir  = "${MIOS_SHARE_DIR}/k3s-manifests"
etc_dir                  = "/etc/mios"
etc_ai_dir               = "${MIOS_ETC_DIR}/ai"
etc_forge_dir            = "${MIOS_ETC_DIR}/forge"
etc_envd_dir             = "${MIOS_ETC_DIR}/env.d"
var_dir                  = "/var/lib/mios"
var_ai_dir               = "${MIOS_VAR_DIR}/ai"
var_mcp_dir              = "${MIOS_VAR_DIR}/mcp"
var_backups_dir          = "${MIOS_VAR_DIR}/backups"
var_cache_dir            = "${MIOS_VAR_DIR}/cache"
srv_ai_dir               = "/srv/ai"
srv_ai_models_dir        = "${MIOS_SRV_AI_DIR}/models"
srv_ai_outputs_dir       = "${MIOS_SRV_AI_DIR}/outputs"
srv_ai_collections_dir   = "${MIOS_SRV_AI_DIR}/collections"
srv_ai_mcp_dir           = "${MIOS_SRV_AI_DIR}/mcp"
toml_vendor              = "${MIOS_SHARE_DIR}/mios.toml"
toml_host                = "${MIOS_ETC_DIR}/mios.toml"
firstboot_sentinel       = "${MIOS_VAR_DIR}/.wsl-firstboot-done"
ai_system_prompt         = "${MIOS_SHARE_AI_DIR}/system.md"
mcp_registry             = "${MIOS_SHARE_AI_DIR}/v1/mcp.json"

coderun_workspace_root = "/var/home/mios/coderuns"          # podman coderun-sandbox /work bind root (shared by code_mode + coderun-session)
coderun_snapshots_root = "/var/home/mios/.coderun-snapshots" # coderun-session snapshot root
codemode_workspace_root = "/var/lib/mios/codemode"          # WS-2 Code Mode generated-snippet root (uid 828, tmpfiles mios-codemode.conf)


# Voidtools Everything CLI -- probed in order; first match wins. WSL
# paths so MiOS helpers can exec via interop.
everything_cli = [
    "/mnt/m/Programs/Everything/es.exe",
    "/mnt/c/Program Files/Everything/es.exe",
    "/mnt/c/Program Files (x86)/Everything/es.exe",
    "/mnt/c/Tools/Everything/es.exe",
    "/mnt/c/Users/mios/AppData/Local/Programs/Everything/es.exe",
]
everything_cli_version = "1.1.0.37"

powershell_exe = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe"

# CMD prompt full path (same PATH-strip rationale).
cmd_exe = "/mnt/c/Windows/System32/cmd.exe"

# Operator-side launcher broker socket. Cross-user dispatch from agent
# context -> operator's interactive Windows session.
launcher_socket = "/run/mios-launcher/launcher.sock"



[[desktop.app_types]]
type            = "browser"
os_pref         = "linux-first"
default         = "epiphany"
windows_default = "zen"
description     = "Web browser -- Linux flatpak by default; Zen (the Windows default browser) on Windows / 'my browser' intent."
[[desktop.app_types]]
type        = "files"
os_pref     = "linux-first"
default     = "nautilus"
description = "File manager."
[[desktop.app_types]]
type        = "editor"
os_pref     = "linux-first"
default     = "gedit"
description = "Text editor."
[[desktop.app_types]]
type        = "terminal"
os_pref     = "linux-first"
default     = "ptyxis"
description = "Terminal emulator."
[[desktop.app_types]]
type        = "media"
os_pref     = "linux-first"
default     = "showtime"
description = "Media / video player."
[[desktop.app_types]]
type            = "games"
os_pref         = "windows"
windows_default = "steam"
description     = "Games + game launchers -- the Windows side (Steam/Epic/GOG/Xbox)."
[[desktop.app_types]]
type        = "settings"
os_pref     = "both"
default     = "gnome-control-center"
description = "System settings -- both OSes have one; agent picks by which system the ask targets."
[[desktop.app_types]]
type        = "system"
os_pref     = "both"
description = "System / OS apps -- assume either side; agent discerns from context."

# ----------------------------------------------------------------------------
[mios-find.aliases]
# Canonical Linux/freedesktop role -> app id. ONE word per role.
web        = "epiphany"
files      = "nautilus"
terminal   = "ptyxis"
editor     = "gedit"
code       = "codium"

# Windows-side role aliases. mios-launch's normaliser lowercases +
# collapses whitespace-to-hyphen, so "File Explorer", "FILE-EXPLORER",
# and "file_explorer" all hit the "file-explorer" key. The aliases
# resolve to the bare Windows shortname that mios-launch's step 3
# (Windows GUI builtin allowlist) dispatches via mios-windows.
file-explorer    = "explorer"
windows-explorer = "explorer"
notepad-app      = "notepad"
calculator-win   = "calc"
paint            = "mspaint"
snip             = "snipping-tool"
task-manager     = "taskmgr"
registry-editor  = "regedit"
control-panel    = "control"
command-prompt   = "cmd"
power-shell      = "powershell"
pwsh-shell       = "pwsh"
software   = "gnome-software"
settings   = "gnome-control-center"
mobile-settings      = "mobi.phosh.MobileSettings"
mobile-control-panel = "mobi.phosh.MobileSettings"
calculator = "gnome-calculator"
calendar   = "gnome-calendar"
mail       = "evolution"
photos     = "loupe"
documents  = "papers"
music      = "decibels"
video      = "showtime"
maps       = "gnome-maps"
clock      = "gnome-clocks"
weather    = "gnome-weather"
disks      = "gnome-disks"
extensions = "extension-manager"
help       = "yelp"
games      = "lutris"

"mios.html"     = "mios-html"
"configurator"  = "mios-html"
"customize"     = "mios-html"
"mios-settings" = "mios-html"
"miosconfig"    = "mios-html"

"install"      = "mios-installer"
"installer"    = "mios-installer"
"package"      = "mios-installer"
"winget"       = "mios-installer"

"screenshot"    = "mios-screenshot"
"screencap"     = "mios-screenshot"
"screen-capture" = "mios-screenshot"
"snap"          = "mios-screenshot"
"prtscr"        = "mios-screenshot"

"window"        = "mios-window"
"windows-mgr"   = "mios-window"
"move-window"   = "mios-window"
"center-window" = "mios-window"
"focus-window"  = "mios-window"

"steamcmd"      = "mios-steamcmd"
"steam-cmd"     = "mios-steamcmd"
"steam-game"    = "mios-steamcmd"
"steam-install" = "mios-steamcmd"

"md"          = "mios-md"
"markdown"    = "mios-md"
"preview-md"  = "mios-md"
"render-md"   = "mios-md"
"notes"       = "mios-md"

[mios-find.ranker]
tiers = [
  "name_exact",
  "name_prefix",
  "name_word",
  "name_substr",
  "desc_word",
  "desc_substr",
  "fuzzy",
]
# Fuzzy (typo) match bounds. fuzzy_min_token_len: only query tokens at least
# this long are fuzzy-matched (typos in 1-3 char queries are too ambiguous).
# fuzzy_max_edit_distance: max Levenshtein distance counted as a match.
fuzzy_min_token_len     = 4
fuzzy_max_edit_distance = 2
fuzzy_max_edit_ratio    = 0.34

# [mios-find.category_priority] -- category -> priority weight (LOWER wins).
# Tie-breaks candidates that match at the SAME tier: a REGISTERED browser or
# native Windows app outranks a linux-flatpak, which outranks an agent-cli,
# which outranks a mios-* shim (a shim must never shadow a real app). A
# category absent from this table falls back to the lowest priority (99).
[mios-find.category_priority]
windows-browser = 1   # a REGISTERED browser (incl. the OS default) is the
                      # highest-confidence resolve for a browser ask -- outranks
                      # the Microsoft.AutoGenerated UWP dupes.
windows-app     = 1
windows-gui     = 2
linux-flatpak   = 3
linux-rpm-gui   = 4
agent-cli       = 5
mios-shim       = 6   # shims are a last resort, never load-bearing
service-url     = 7

[compliance]
enabled                 = false                        # master switch (DEFAULT OFF; scan inert unless true)
profile                 = "standard"                   # SSG profile suffix -> xccdf_org.ssgproject.content_profile_<profile>
datastream              = ""                            # explicit datastream path; "" -> derive ssg-<os-release ID>-ds.xml from installed scap-security-guide
severity_gate           = "high"                       # fail build on a FAILED rule at/above this severity (high>medium>low; "any" = all fails)
remediate               = false                        # RESERVED: scan-only today; remediation (oscap-im) is a future operator-opt-in step
fetch_remote_resources  = false                        # allow oscap to fetch remote SCAP resources (offline-safe default OFF)
report_path             = "/usr/share/mios/compliance" # ARF + HTML report dir (baked into the image, not /var)
oscap_skip_rules        = []                           # list of rule IDs (e.g. xccdf_org.ssgproject.content_rule_...) to exclude/disable via XCCDF tailoring

# ----------------------------------------------------------------------------
# [build] -- build-time tunables consumed by automation/[NN]-*.sh and
# the mios-build-driver.
# ----------------------------------------------------------------------------
[build]
rechunk_max_layers = 67   # hhd-dev/rechunk: max OCI layer count post-rechunk
ai_ram_floor_gb    = 12   # informational; logged by postcheck
curl_trigger_fallback = true

[build.bake_refs]
quickshell = "v0.3.0"
surfer     = "17d9a1577170880cdac13dca7c3d6871716fc046"
hyprland   = "main"
lookingglass = "B7"
syft = "v1.19.0"
searxng = "master"

[build.bake]
runner_disk_budget_gb = 40
core = [
  "localhost/mios-sys",
  "localhost/mios-cuda",
  "localhost/mios-crawl4ai-slim:latest",
  "localhost/mios-firecrawl:v1.0.0",
  "code.forgejo.org/forgejo/runner:7",
  "codeberg.org/forgejo/forgejo:12",
  "docker.io/adguard/adguardhome:latest",
  "docker.io/guacamole/guacamole:latest",
  "docker.io/guacamole/guacd:latest",
  "docker.io/jaegertracing/all-in-one:1.76.0",
  "docker.io/lmsysorg/sglang:latest",
  "docker.io/pgvector/pgvector:pg18",
  "docker.io/rancher/k3s:v1.36.3-k3s1",
  "docker.io/searxng/searxng:latest",
  "docker.io/valkey/valkey:latest",
  "docker.io/vllm/vllm-openai:latest",
  "ghcr.io/mostlygeek/llama-swap:cuda",
  "ghcr.io/mios-dev/mios-node:latest",
  "ghcr.io/open-webui/open-webui:main",
  "quay.io/centos-bootc/bootc-image-builder:latest",
  "quay.io/ceph/ceph:v19",
  "quay.io/poseidon/matchbox:latest"
]
groups = ["sys", "cuda", "heavy", "extra"]
firstboot_tokens = ["vllm", "sglang", "crawl4ai", "firecrawl"]

[build.bake.firstboot_justifications]
"sglang" = "Heavy GPU inference image (~20GB)"
"vllm" = "Heavy GPU inference image (~27GB)"
"crawl4ai" = "Webtools heavy crawl runtime deferred from Day-0 bake"
"firecrawl" = "Webtools heavy crawl runtime deferred from Day-0 bake"

[build.bake.group_members]
sys = ["sys"]
cuda = ["cuda"]
heavy = ["open-webui", "ceph"]
extra = []

[deploy.artifacts.raw]
size = "80 GiB"

[deploy.artifacts.iso]
minsize = "150 GiB"

[build.artifacts]
output_dir = "build"

[vm.win11]
name = "win11-guest"
memory_kib = 25165824
vcpus = 12

[identity.ipa]
enabled = false
realm = "MIOS.INTERNAL"
server = "ipa.mios.internal"
domain = "mios.internal"
enroll_principal = "admin"
otp = "placeholder-one-time-password"

[cockpit]
allow_unencrypted = true
login_to = false
idle_timeout = 0


# ----------------------------------------------------------------------------
# [network.quadlet] -- internal podman bridge for sidecar containers.
# Every Quadlet declares Network=mios.network so they share a flat
# rootful bridge with the LAN-reachable PublishPort= surface above.
# ----------------------------------------------------------------------------
[metal]
enabled = false
guest_cpu_percent = 85
guest_ram_percent = 85
bind_dgpu_vfio   = true
dgpumode         = "vfio-pci"

[metal.mesh]
enabled = false
headscale_domain = "mesh.mios.local"
vnet_cidr = "100.64.0.0/10"
swtpm_vtpm = true

[gpu.vendors]
nvidia = true
amd    = true
intel  = true

[security.luks]
enabled = false
pin     = "tpm2"
pcrs    = "7"

[greenboot]
critical_services = ["agent-pipe", "llm-light", "pgvector", "hermes"]
blade_reachability_critical = false

# Probe overrides; unit/port default to mios-<n>.service and MIOS_PORT_<N>.
[greenboot.probe]
agent_pipe = { kind = "http", path = "/v1/models" }
hermes     = { unit = "hermes-worker.service" }

[agent_pipe.quality]
min_length  = 5
check_json  = true
check_punt  = true
check_empty = true

[agent_pipe.council]
diversity_gate              = false
diversity_threshold         = 0.92
aggregator_bypass           = false
aggregator_bypass_threshold = 0.95

[network.quadlet]
network = "mios.network"
subnet  = "10.89.0.0/24"
core_subnet = "10.89.0.0/24"
core_gateway = "10.89.0.1"

[colors]
# ── Named tokens (the configurator HTML, OSC-aware apps, theming) ─────────
bg          = "#282262"   # deep indigo  (Hokusai sky)            [main bg]
fg          = "#E7DFD3"   # cream         (Hokusai foam)           [main fg]
accent      = "#1A407F"   # operator blue (interactive / link)
cursor      = "#F35C15"   # sunset orange (visible cursor)
success     = "#3E7765"   # wave green
warning     = "#F35C15"   # sunset orange
error       = "#DC271B"   # coral red
info        = "#1A407F"   # operator blue
muted       = "#948E8E"   # warm grey
subtle      = "#B7C9D7"   # pale blue-grey
earth       = "#734F39"   # brown
silver      = "#E0E0E0"   # operator silver

# ── ANSI 16-color slot mapping (terminal palette via OSC-4) ───────────────
# Slot 0..7 are the canonical low colors; 8..15 are the bright variants.
# Programs that emit `\e[31m` etc. resolve through these slots.
ansi_0_black            = "#282262"   # deep indigo
ansi_1_red              = "#DC271B"   # coral red
ansi_2_green            = "#3E7765"   # wave green
ansi_3_yellow           = "#F35C15"   # sunset orange
ansi_4_blue             = "#1A407F"   # operator blue
ansi_5_magenta          = "#734F39"   # brown
ansi_6_cyan             = "#B7C9D7"   # pale blue-grey
ansi_7_white            = "#E7DFD3"   # cream
ansi_8_bright_black     = "#948E8E"   # warm grey
ansi_9_bright_red       = "#FF6B5C"   # lighter coral
ansi_10_bright_green    = "#5FAA8E"   # lighter wave-green
ansi_11_bright_yellow   = "#FF8540"   # lighter sunset
ansi_12_bright_blue     = "#3D6BA8"   # lighter operator blue
ansi_13_bright_magenta  = "#9D7660"   # lighter brown
ansi_14_bright_cyan     = "#E0E0E0"   # operator silver
ansi_15_bright_white    = "#FFFFFF"   # pure white

[bootstrap]
mode             = "auto"
mios_repo        = "C:/MiOS"
bootstrap_repo   = "C:/mios-bootstrap"
branch           = "main"
install_packages = true   # run dnf install on PACKAGES.md after FHS overlay
reboot_on_finish = true   # interactive y/N prompt at end of install

[bootstrap.host_storage]
drive_letter        = "M"
volume_label        = "MIOS-DEV"
shrink_mb           = 262656     # produces "256 GB" in Windows Explorer
display_size_gb     = 256        # what the operator should see (informational)
filesystem          = "NTFS"
allocation_unit     = 4096       # NTFS default cluster size

# ----------------------------------------------------------------------------
# [bootstrap.dev_vm] -- podman-MiOS-DEV machine specifics. The VM that
# IS the builder (per feedback_mios_dev_is_the_builder memory).
# ----------------------------------------------------------------------------
[bootstrap.dev_vm]
machine_name        = "MiOS-DEV"   # podman derives "podman-MiOS-DEV"
wsl_distro          = "MiOS"        # the eventual MiOS WSL distro (built from the OCI image)
base_image          = "quay.io/podman/machine-os:6.0"   # LOCKED base
# Maximalist allocation — the dev VM IS the builder, so it gets every CPU
# core, every GB of RAM, every GB of free disk, and every GPU the host
# offers (minus the host_reserve below). Set to "max" to auto-detect via
# Get-Hardware in build-mios.ps1; pin to a number to cap explicitly.
cpus                = "max"
memory_mb           = "max"
disk_size_gb        = "max"
gpu_passthrough     = "max"      # "max" | "none" | "nvidia" | "amd" | "intel"
rename_distro       = false

# ----------------------------------------------------------------------------
# [wsl2] -- pre-Phase-0 WSL utility VM networking + resource knobs.
# Get-MiOS.ps1 reads these and writes %USERPROFILE%\.wslconfig before any
# `wsl.exe` invocation; build-mios.ps1 Phase 4 layers RAM/CPU/swap on top.
# ----------------------------------------------------------------------------
[wsl2]
networking_mode      = "NAT"
localhost_forwarding = true
firewall             = false        # only meaningful under mirrored mode
gui_applications     = true
dns_tunneling        = true
auto_proxy           = true

[wsl2.desktop_compat]
gdk_backend     = "x11"     # GTK apps: "x11" (Xwayland) | "wayland"
moz_wayland     = "0"       # Firefox / mozilla: "0" (X11) | "1" (Wayland)
qt_platform     = "xcb"     # Qt apps: "xcb" (X11) | "wayland" | "wayland;xcb"

[wsl2.dev_vm]
quadlet_network_mode = "host"   # "host" (NAT default) | "bridge" (mirrored-safe)

[desktop.start_menu]
publish = ["forge", "cockpit", "code_server", "searxng", "hermes_dashboard", "guacamole_web"]

forge_label              = "Forge"
forge_scheme             = "http"
forge_port_key           = "forge_http"

cockpit_label            = "Cockpit"
cockpit_scheme           = "https"
cockpit_port_key         = "cockpit"

code_server_label        = "Code"
code_server_scheme       = "http"
code_server_port_key     = "code_server"

# hermes_workspace shortcut REMOVED -- use the dashboard
# shortcut (already in `publish` list above) at http://localhost:9119/.

searxng_label            = "Search"
searxng_scheme           = "http"
searxng_port_key         = "searxng"

hermes_dashboard_label   = "Dashboard"
hermes_dashboard_scheme  = "http"
hermes_dashboard_port_key = "hermes_dashboard"

guacamole_web_label      = "Guacamole"
guacamole_web_scheme     = "http"
guacamole_web_port_key   = "guacamole_web"

[bootstrap.dev_vm.host_reserve]
cpu_pct             = 15      # reserve 15% of logical CPUs for Windows host
cpu_min             = 2       # always reserve at least 2 cores
memory_pct          = 15      # reserve 15% of host RAM for Windows host
memory_gb           = 4       # always reserve at least 4 GB
disk_gb             = 32      # always keep 32 GB free on M:\ for Windows-side
                              # artifacts (build outputs, podman cache, etc.)

[bootstrap.prereqs]
git_pkg             = "Git.Git"                         # Git for Windows (Install-* uses for clone)
wsl_pkg             = "Microsoft.WSL"                   # WSL2 substrate (kernel + wsl.exe)
podman_pkg          = "RedHat.Podman-Desktop"           # Podman Desktop GUI + machine
podman_cli_pkg      = "RedHat.Podman"                   # Podman CLI MSI (lays down podman.exe + PATH)
terminal_pkg        = "Microsoft.WindowsTerminal"       # Windows Terminal (Install-MiOSWindowsTerminal)
pwsh_pkg            = "Microsoft.PowerShell"            # pwsh 7+ (Install-MiOSPwsh7)
ohmyposh_pkg        = "JanDeDobbeleer.OhMyPosh"         # oh-my-posh prompt (Install-MiOSOhMyPoshTheme)
fastfetch_pkg       = "fastfetch-cli.fastfetch"         # fastfetch (Install-MiOSFastfetch)
appinstaller_pkg    = "Microsoft.AppInstaller"          # winget itself (bootstrap winget on truly bare Win 11 ≤ 21H2)
# Direct-download URL for the App Installer MSIXBUNDLE. Used by
# Ensure-MiOSWinget when winget is missing AND no source for it exists
# (truly fresh Win 10 21H2 / OOBE-fresh hosts). aka.ms/getwinget redirects
# to the latest stable .msixbundle from microsoft/winget-cli releases.
appinstaller_url    = "https://aka.ms/getwinget"

# Windows optional features (Enable-MiOSWindowsFeatures iterates these).
# label keys are operator-readable strings used in install banners.
[bootstrap.prereqs.features]
wsl                 = "Microsoft-Windows-Subsystem-Linux"   # WSL substrate
vmp                 = "VirtualMachinePlatform"              # WSL2 + Hyper-V hypervisor
hyperv              = "Microsoft-Hyper-V"                   # Hyper-V Manager + VMs (Pro/Ent only)
require_reboot_to_continue = true

# Repo URLs cloned to M:\MiOS\repo\{mios,mios-bootstrap} during Pass-2.
# Branch defaults to main; pin to a tag/SHA for reproducible installs.
[bootstrap.prereqs.repos]
mios_url            = "https://github.com/mios-dev/mios.git"
mios_branch         = "main"
bootstrap_url       = "https://github.com/mios-dev/mios-bootstrap.git"
bootstrap_branch    = "main"
clone_root          = "M:\\MiOS\\repo"

[terminal]
cols                = 80
rows                = 20      # 20 = MiOS portal-feel default; 30 for taller "console" feel
scrollback_rows     = 9000
frame_width         = 80      # = cols, edge-to-edge (unified)
frame_height        = 19      # = rows - 1, reserves 1 row for prompt
right_margin        = 0       # 0 = edge-to-edge. Was 1 (slack against WT first-paint

[terminal.gui_min]
width               = 1600
height              = 1000

[terminal.install]
cols                = 80
rows                = 40      # taller for install output + readme

[terminal.reading]
cols                = 100
rows                = 50

[theme]
mode                = "dark"             # "dark" | "light" (rare; vendor=dark)
acrylic             = true               # WT useAcrylic ON
opacity             = 50                 # WT opacity 0..100 (50 = 50% transparency)
system_backdrop     = "acrylic"          # WT 1.18+ per-profile backdrop
cursor_shape        = "vintage"    # steady half-block; thin "bar" was almost too thin to see on 4K -- "vintage" is the substantial half-block cursor (matches xterm's default visual weight without the full filledBox feel)
scrollbar_state     = "hidden"           # no scrollbar -- frame uses every col
padding             = "0"                # zero padding -- frame flush to edges
suppress_app_title  = true               # WT doesn't override our title
launch_mode         = "focus"            # frame-less / border-less / no-titlebar / no-tab-row -- forces WT to measure the viewport at the actual cell count cols × rows from FIRST PAINT (no titlebar to strip later, no off-by-1/2 cell-count window before scrollbarState='hidden' applies). Required for true edge-to-edge in mios-bootstrap's Install-MiOSTerminalProfile.

[theme.cursor_windows]
base_size           = 24

[theme.cursor_linux]
theme               = "Bibata-Modern-Classic"
size                = 24   # x11/Xwayland renders cursors at literal px; 16 read SMALLER than the Windows base (24) --. The pre-x11 "16 lines them up" note above is stale (it was a Wayland-era observation).
# Upstream source for the cursor theme. Read by automation/57-gnome.sh
# (build seeds /usr/share/icons) AND mios-cursor-ensure (runtime self-heal
# into ~/.local/share/icons when /usr lacks it -- /usr is RO on bootc).
# archive defaults to "<theme>.tar.xz" off the latest GitHub release tag.
source_repo         = "ful1e5/Bibata_Cursor"

[theme.font]
family              = "GeistMono Nerd Font Mono"
size                = 12
weight              = "normal"
url                 = "https://github.com/ryanoasis/nerd-fonts/releases/latest/download/GeistMono.zip"   # GeistMono Nerd Font (mono + glyphs)
vercel_repo         = "https://github.com/vercel/geist-font.git"                                         # Vercel Geist (sans + mono, no glyphs)
symbols_url         = "https://github.com/ryanoasis/nerd-fonts/releases/latest/download/NerdFontsSymbolsOnly.zip"   # Nerd Symbols Only (icon glyphs the omp theme uses)
install_scope       = "auto"
# Cell metrics at 100% DPI (used to compute window pixel size for
# centering): cell_w * cols + chrome_w, cell_h * rows + chrome_h.
cell_w_px           = 10
cell_h_px           = 20
chrome_w_px         = 20
chrome_h_px         = 12

[theme.terminal]
scheme_name         = "MiOS"        # color scheme, project palette
profile_name        = "MiOS-WIN"    # WT pwsh dashboard profile (no .lnk)
dev_profile_name    = "MiOS-DEV"    # WT dev VM profile (the "MiOS.lnk" target)
hub_target_profile  = "MiOS-DEV"    # WT profile launched by the hub .lnk
# Global summon keybinding (toggle MiOS WT window open/close from
# anywhere on the desktop).  The summoned window is the dev VM
# (MiOS-DEV) per the consolidation -- one MiOS, one chord.
summon_keys         = "win+space"
summon_window_name  = "MiOS-DEV"

[theme.prompt]
omp_path                = "/usr/share/mios/oh-my-posh/mios.omp.json"

# Powerline separators between segments. Rounded by default.
powerline_right         = ""   #  rounded right (segment ends on right side)
powerline_left          = ""   #  rounded left  (segment ends on left side)
# Soft separator -- light vertical bar between same-color segments.
powerline_right_soft    = ""   #
powerline_left_soft     = ""   #

# Diamond / leading caps (start of leftmost segment, end of rightmost).
leading_diamond         = ""   #  rounded leading diamond
trailing_diamond        = ""   #  rounded trailing diamond

# Prompt cursor symbol (rendered after the last segment).
prompt_symbol           = "❯"   # ❯ heavy right angle (default user)
prompt_symbol_root      = "⚠"   # ⚠  warning (default root)

# ----------------------------------------------------------------------------
# [branding] -- assets the dashboard / fastfetch / splash screens use.
# Paths resolve relative to /usr/share/mios on Linux and to the
# corresponding M:\MiOS\usr\share\mios on Windows.
# ----------------------------------------------------------------------------
[branding]
logo_ascii          = "fastfetch/mios.txt"
logo_image          = "branding/mios-logo.png"
icon_ico            = "branding/mios.ico"
fastfetch_config    = "fastfetch/config.jsonc"
tagline             = "My Personal Operating System"
tagline_long        = "My Personal Operating System  --  Immutable Fedora AI Workstation"
tagline_app         = "My Personal Operating System"
living_wallpaper      = true
living_wallpaper_mode = "shader"

[branding.dashboard]
frame_width_cols    = 80
frame_chars         = "╭─╮│╰╯"           # WT-only Unicode box-drawing
frame_color         = "blue"             # ANSI name resolves through [colors]
show_fastfetch      = true
show_logo           = true

[theme.fastfetch]
logo_color   = "blue"
keys_color   = "yellow"
title_color  = "white"
output_color = "cyan"

[btop]
# Presets accessible via `p` 0-9 inside btop. Format: <boxes>:<mode>:<theme>
# where mode = 0 (full) or 1 (compact). Slot 4 (proc only) is the MiOS
# canonical launch (mios-btop.sh runs `btop -p 4` on plain invocation).
#   0 cpu compact  1 cpu full  2 cpu+mem compact  3 cpu+mem full
#   4 proc only  <- canonical  5 cpu+mem+net+proc full
presets              = "cpu:1:default,cpu:0:default,cpu mem:1:default,cpu mem:0:default,proc:0:default,cpu mem net proc:0:default user mios"

shown_boxes          = "proc"

# Update interval in milliseconds. Operator: "not 500ms update speed" -- the
# displayed value was 2000ms; lock to 500ms so btop refreshes 2x/sec OOTB.
update_ms            = 500

# Transparent background -- preserves the terminal's acrylic backdrop.
theme_background     = false
truecolor            = true       # 24-bit truecolor
force_tty            = false      # real terminal, not tty mode
vim_keys             = false      # add "v" mark on volatile values
rounded_corners      = true       # rounded box corners (matches MiOS frame chars)

# Graph symbols: "braille", "block", or "tty".
graph_symbol         = "block"
graph_symbol_cpu     = "block"
graph_symbol_gpu     = "block"
graph_symbol_mem     = "block"
graph_symbol_net     = "block"
graph_symbol_proc    = "block"

# Process box.
proc_sorting         = "cpu lazy"
proc_reversed        = false
proc_tree            = false      # command-line tree view
proc_colors          = true       # colliding cpu colors in tree mode
proc_gradient        = true       # per-process cpu/mem usage graph
proc_filter_kernel   = false
proc_per_core        = false
proc_mem_bytes       = true       # show memory in process box
proc_cpu_graphs      = true       # show cpu graph in process box
proc_info_smaps      = false      # use smaps for process info (Linux)
proc_left            = false      # command on the left
proc_filter          = ""         # active "/" filter string

# CPU box. Minimal single-graph layout for the 80x20 portal.
cpu_graph_upper      = "total"
cpu_graph_lower      = "total"
cpu_invert_lower     = true
cpu_single_graph     = true
cpu_bottom           = false
show_uptime          = true
check_temp           = true       # cpu temperatures (per-core if available)
cpu_sensor           = "Auto"     # which sensor to use for cpu temperature
show_coretemp        = true       # all cores in cpu temp
cpu_core_map         = ""         # cpu core map order override
custom_cpu_name      = ""         # custom cpu model name
show_cpu_freq        = true       # cpu frequency display
clock_format         = "%X"       # clock format in cpu box top
background_update     = true      # update graphs even when paused
custom_filters       = ""

# Memory / disk box.
show_swap            = true
swap_disk            = true
show_disks           = true
only_physical        = true
use_fstab            = false
zfs_hide_datasets    = false
disk_free_priv       = false      # disk meter graph (off for compact 80x20 fit)
show_io_stat         = false
io_mode              = false
io_graph_combined    = false
io_graph_speeds      = ""

# Network box (hidden via shown_boxes -- still configured for when re-enabled).
net_download         = 100
net_upload           = 100
net_auto             = true
net_sync             = false
net_iface            = ""

# Battery status (when applicable).
show_battery         = true
selected_battery     = "Auto"
show_battery_watts   = true

# btop log verbosity: ERROR | WARNING | INFO | DEBUG.
log_level            = "WARNING"

[dashboard]
title           = "MiOS  --  My Personal Operating System"
show_title      = true
show_logo       = false           # 80x20 doesn't fit the 11-row ASCII logo. Set true once rows >= 30.
show_verb_hints = true
verb_hint       = "build  config  dash  mini  ai  code  dev  summary  user  pull  update  help"

# show_services -- Linux-only.  When true (or MIOS_DASH_SERVICES=1),
# mios-dashboard.sh appends the icon-driven Services / Stack / Tree
# blocks below the metric rows. Default true now that the blocks
# fit in 80x20 (3 service rows + 1 stack-count + 1 tree row).
show_services   = true

rows = [
    ["version", "date"],
    ["user", "uptime"],
    ["cpu", "gpu_discrete"],
    ["disk_c", "disk_m"],
    ["ram", "swap"],
    ["kernel", "shell"],
    ["host", "font"],
]

# ----------------------------------------------------------------------------
# [apps] -- Windows native-app surface. Get-MiOS.ps1's
# Install-MiOSNativeApp creates Start Menu + Desktop shortcuts using
# these names + the AumID below.
# ----------------------------------------------------------------------------
[apps]
aumid               = "MiOS.Workstation"
start_menu_folder   = "MiOS"        # umbrella folder name (project)
hub_shortcut_name   = "MiOS"        # the ONE MiOS .lnk -- opens MiOS-DEV

[apps.shortcuts]
mios_dev    = { name = "MiOS-DEV",    bin = "mios-dev.ps1",    icon = "mios-dev.ico",    description = "Open MiOS-DEV (podman machine) directly to its themed dashboard" }
mios_config = { name = "MiOS Config", bin = "mios-config.ps1", icon = "mios-config.ico", description = "Open mios.html (the HTML configurator) in your default browser to edit mios.toml" }
mios_help   = { name = "MiOS Help",   bin = "mios-help.ps1",   icon = "mios-help.ico",   description = "Full verb + functionality reference (every MiOS command and where things live)" }

# ----------------------------------------------------------------------------
# [windows.owned_artifacts] -- SSOT registry of MiOS-owned Windows artifacts
# (tasks, services, processes, firewall rules, registry keys, shortcuts).
# Used by Uninstall-MiOS.ps1 and enforced by the symmetry drift check.
# ----------------------------------------------------------------------------
[windows.owned_artifacts]
task_names     = ["MiOS-Autostart", "MiOS-Resume-Bootstrap", "MiOS-WSL-KeepAlive", "MiOS-WSL-Session", "MiOS-iGPU-Server"]
service_names  = ["MiOS-Wallpaper-Service", "MiOS-iGPU-Server"]
process_names  = ["MiOS-Wallpaper", "MiOS-Wallpaper-Service", "MiOS-Launcher", "MiOS-iGPU-Server"]
firewall_rules = ["MiOS - igpu-llm", "MiOS - ai-node", "MiOS"]
registry_roots = ["HKLM:\\SOFTWARE\\MiOS", "HKCU:\\Software\\Microsoft\\Windows\\CurrentVersion\\Uninstall\\MiOS", "HKCU:\\Control Panel\\Cursors\\Schemes"]
shortcut_dirs  = ["MiOS", "podman-MiOS-DEV"]

[preflight]
min_windows_build   = 22000      # Windows 11 21H2 minimum
min_disk_free_gb    = 280        # 256 GB partition + headroom
min_ram_gb          = 8
require_virt        = true       # Hyper-V + WSL2 require virtualization
require_admin       = true       # auto-elevation handles this; check is informational

# ----------------------------------------------------------------------------
# [network.retry] -- transient-error retry schedules (seconds). Used by
# anywhere we hit upstream services (quay.io image pulls, GitHub raw
# fetches, winget repository sync, dnf metadata refresh).
# ----------------------------------------------------------------------------
[network.retry]
delays_seconds      = [0, 5, 15, 30]   # 4 attempts: immediate, 5s, 15s, 30s
http_status_retry   = [502, 503, 504]  # only these status codes trigger retry
total_timeout_sec   = 120

[quadlets.enable]
mios-ceph             = true
mios-k3s              = true
mios-forge            = true
mios-open-webui       = true
mios-code-server      = false
mios-guacamole        = true
mios-pxe-hub          = true
mios-guacd            = true
mios-adguard          = true

[env]
MIOS_URL_TERRA_REPO = "https://github.com/terrapkg/subatomic-repos/raw/main/terra.repo"
MIOS_URL_UBLUE_REPO = "https://copr.fedorainfracloud.org/coprs/ublue-os/packages/repo/fedora-${FEDORA_VERSION}/ublue-os-packages-fedora-${FEDORA_VERSION}.repo"
MIOS_URL_TAILSCALE_REPO = "https://pkgs.tailscale.com/stable/fedora/tailscale.repo"
MIOS_URL_CROWDSEC_REPO = "https://packagecloud.io/crowdsec/crowdsec/config_file.repo?os=fedora&dist=${FEDORA_VERSION}&source=script"
MIOS_URL_BIBATA_API = "https://api.github.com/repos/ful1e5/Bibata_Cursor/releases/latest"
MIOS_URL_BIBATA_DL = "https://github.com/ful1e5/Bibata_Cursor/releases/download/v{}/Bibata-Modern-Classic.tar.xz"
MIOS_URL_BIBATA_SUM = "https://github.com/ful1e5/Bibata_Cursor/releases/download/v{}/sha256-{}.txt"

[finetune]
enable          = true
target_role     = "refiner"            # MiOS role to fine-tune (refiner = mios-sys-agent)
base_model      = "granite4.1:8b"      # the role's base -- MUST match the Modelfile FROM. Repointed to Granite 4.1 8B (the new light_brain) from qwen3.5:4b (Qwen + qwen35 arch blocked on mainline llama.cpp).
hf_base         = "ibm-granite/granite-4.1-8b"   # HF repo id OR local safetensors dir for the SAME base (Granite 4.1 8B, Apache-2.0, public; matches base_model granite4.1:8b)
                                       # (PEFT/TRL train on HF weights; the llama.cpp lane serves the GGUF). One-time
                                       # offline fetch, like the framework install. This is the actual
                                       # production refiner base (qwen3.5:4b == Qwen/Qwen3.5-4B, Apache-2.0,
                                       # public). Override per-install if base_model differs.
output_tag      = "mios-sys-agent-ft"  # served-model tag the adapter is shipped as
device          = "auto"               # auto | cuda | rocm | mps | cpu
load_in_4bit    = "auto"               # auto = 4bit ONLY on CUDA+bitsandbytes; else full/bf16
prefer_unsloth  = true                 # use the Unsloth fast-path when CUDA + unsloth present

# --- LoRA adapter ---
lora_r          = 16
lora_alpha      = 32
lora_dropout    = 0.05
target_modules  = "auto"               # auto = attn+mlp projections for the detected arch

# --- SFT hyperparameters ---
epochs          = 2
learning_rate   = 2.0e-4
batch_size      = 2
grad_accum      = 8
max_seq_len     = 2048
warmup_ratio    = 0.03
grad_checkpointing = true              # saves memory; set false if a custom-op base breaks in backward

teacher_endpoint     = "http://localhost:${MIOS_PORT_LLM_LIGHT}"   # mios-llm-light (ALL llama.cpp)
pipe_url             = "http://127.0.0.1:${MIOS_PORT_AGENT_PIPE}"
seeds_per_capability = 4               # teacher-generated phrasings per catalog verb
include_knowledge    = true            # also mine REAL operator queries from the knowledge table
dataset_path         = "/var/lib/mios/finetune/refiner-sft.jsonl"
min_examples         = 24              # refuse to train on a corpus smaller than this

# --- GGUF export + llama.cpp wiring ---
gguf_convert    = true
gguf_quant      = "q8_0"               # adapter export precision
work_dir        = "/var/lib/mios/finetune"
serve_port      = 11438                # mios-finetune-serve OpenAI /v1 endpoint (opt-in)


[verbs.find_file]
section = "Discovery / resolution"
sig = "query, scope"
desc = "Search for files on disk. scope='fast' (cached DB, sub-100ms, use FIRST), 'windows' (NTFS exhaustive search), or 'linux' (exhaustive Linux search). Use 'fast' unless a recent file is missing."
model_name = "find_local_file"
tier = "core"
cmd = "if [ '{scope}' = 'fast' ]; then mios-directory-lookup --json --limit 20 {query}; elif [ '{scope}' = 'windows' ]; then mios-everything --json -n 10 {query}; else mios-locate --json -n 20 {query}; fi"
  [verbs.find_file.params.query]
  type = "string"
  aliases = ["name", "filename", "term"]
  [verbs.find_file.params.scope]
  type = "string"
  enum = ["fast", "windows", "linux"]

[verbs.apps]
section = "Discovery / resolution"
sig = "mode, query?"
desc = "Query applications. mode='list' (inventory of all installed apps), 'semantic' (fuzzy app capability search), or 'resolve' (get exact CLI launch command for an app without launching it)."
tier = "core"
# mios-apps exits non-zero when a sub-scanner is degraded (e.g. the Windows
# es.exe side on WSL) yet still emits a valid inventory on stdout -- tolerate
# that exit (|| true) so a degraded scanner does not fail the whole verb.
cmd = "if [ {mode} = list ]; then mios-apps --json || true; elif [ {mode} = semantic ]; then mios-app-search {query}; else mios-find --json {query}; fi"
  [verbs.apps.params.mode]
  type = "string"
  enum = ["list", "semantic", "resolve"]
  [verbs.apps.params.query]
  type = "string"

[verbs.fetch_page]
section = "Discovery / resolution"
sig = "url, format"
desc = "Fetch ONE web page content. format='text' (lightweight ~4000 char plain text), 'article-md' (clean markdown of a long article via crawl4ai), or 'index-md' (markdown of a news/listing/index page via firecrawl)."
tier = "core"
cmd = "if [ '{format}' = 'text' ]; then mios-web-extract -n 4000 {url}; elif [ '{format}' = 'article-md' ]; then mios-crawl --json --max-chars 20000 {url}; else mios-firecrawl --max-chars 20000 {url}; fi"
  [verbs.fetch_page.params.url]
  type = "string"
  [verbs.fetch_page.params.format]
  type = "string"
  enum = ["text", "article-md", "index-md"]

[verbs.search_store]
section = "Discovery / resolution"
sig = "query, store"
desc = "Search stored information. store='knowledge' (curated OWUI RAG document collections) or 'vault' (the local notes/skills vault)."
sensitive = true   # Rule-of-Two property B: reads private knowledge collections / notes vault
cmd = "if [ '{store}' = 'knowledge' ]; then mios-knowledge-search --json --top-k 5 {query}; else mios-viking find {query}; fi"
  [verbs.search_store.params.query]
  type = "string"
  [verbs.search_store.params.store]
  type = "string"
  enum = ["knowledge", "vault"]

[verbs.vault]
section = "Discovery / resolution"
sig = "op, target?, content?"
desc = "Manage the local second-brain vault. op='list' (ls), 'read' (cat), 'search' (find), or 'ingest' (add local file to vault)."
sensitive = true   # Rule-of-Two property B: reads the private second-brain vault
cmd = "if [ '{op}' = 'list' ]; then mios-viking ls {target=knowledge}; elif [ '{op}' = 'read' ]; then mios-viking cat {target}; elif [ '{op}' = 'search' ]; then mios-viking find {target}; else mios-ingest --file {target}; fi"
  [verbs.vault.params.op]
  type = "string"
  enum = ["list", "read", "search", "ingest"]
  [verbs.vault.params.target]
  type = "string"
  [verbs.vault.params.content]
  type = "string"

[verbs.memory]
section = "Discovery / resolution"
sig = "op, fact?, query?, id?"
desc = "Manage agent's self-directed memories/facts. op='add' (remember a new fact), 'list' (recall matching memories), 'update', or 'forget'."
tier = "core"
permission = "write"
sensitive = true   # Rule-of-Two property B: op=list reads private agent memory (write-tier -> also property C)
cmd = "if [ '{op}' = 'add' ]; then mios-remember add '{fact}'; elif [ '{op}' = 'list' ]; then mios-remember list; elif [ '{op}' = 'update' ]; then mios-remember update --key {id} '{fact}'; else mios-remember forget --key {id}; fi"
  [verbs.memory.params.op]
  type = "string"
  enum = ["add", "list", "update", "forget"]
  [verbs.memory.params.fact]
  type = "string"
  [verbs.memory.params.query]
  type = "string"
  [verbs.memory.params.id]
  type = "string"

[verbs.document]
section = "Messaging"
sig = "op, input, output?"
desc = "Generate or convert documents. op='build' (compile markdown to pdf/docx/etc.) or 'convert' (convert office docs/csv to markdown)."
permission = "write"
cmd = "if [ '{op}' = 'build' ]; then mios-docgen build {input} {output}; else mios-docgen convert {input} {output}; fi"
  [verbs.document.params.op]
  type = "string"
  enum = ["build", "convert"]
  [verbs.document.params.input]
  type = "string"
  [verbs.document.params.output]
  type = "string"

[verbs.agent_route]
section = "Messaging"
sig = "mode, agent, task"
desc = "Cross-agent routing. mode='handoff' (transfer control entirely, ending your turn) or 'delegate' (sub-task an agent and get the result back)."
permission = "write"
cmd = "if [ '{mode}' = 'handoff' ]; then mios-handoff --to {agent} --goal '{task}'; else mios-a2a-delegate --peer {agent} --text '{task}'; fi"
  [verbs.agent_route.params.mode]
  type = "string"
  enum = ["handoff", "delegate"]
  [verbs.agent_route.params.agent]
  type = "string"
  [verbs.agent_route.params.task]
  type = "string"

[verbs.run_code]
section = "Code / shell"
sig = "mode, code, language?"
desc = "Execute code. mode='sandbox' (safe isolated run) or 'orchestrate' (run a script that imports mios_tools to coordinate agentic sub-actions). language defaults to python; set 'bash' for shell."
tier = "core"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
# Placeholders are shlex-quoted by the renderer -- do NOT wrap them in literal
# quotes (that double-quotes the value and breaks any code with parens/quotes/
# newlines). Pass code via --code and select the interpreter (default python).
cmd = "if [ {mode} = sandbox ]; then mios-coderun --code {code} --lang {language=python}; else mios-coderun-codemode --code {code}; fi"
  [verbs.run_code.params.mode]
  type = "string"
  enum = ["sandbox", "orchestrate"]
  [verbs.run_code.params.code]
  type = "string"
  [verbs.run_code.params.language]
  type = "string"
  enum = ["python", "bash"]


[verbs.window_op]
section = "Window / app launch"
sig = "op, title, width?, height?, x?, y?, position?"
desc = "Perform window operations: op='focus' (bring to front), 'move-region' (snap to left/right/etc via position), 'move-pixel' (set x/y), 'resize' (set width/height), 'min', 'max', 'restore', 'close'."
tier = "core"
permission = "write"
cmd = "mios-window {op} '{title}'"
cmd_positioned = "mios-window {op} '{title}' {position}"
cmd_pixel = "mios-window {op} '{title}' {x} {y}"
cmd_resize = "mios-window {op} '{title}' {width} {height}"
  [verbs.window_op.params.op]
  type = "string"
  enum = ["focus", "move-region", "move-pixel", "resize", "min", "max", "restore", "close"]
  [verbs.window_op.params.title]
  type = "string"
  [verbs.window_op.params.width]
  type = "integer"
  [verbs.window_op.params.height]
  type = "integer"
  [verbs.window_op.params.x]
  type = "integer"
  [verbs.window_op.params.y]
  type = "integer"
  [verbs.window_op.params.position]
  type = "string"

[verbs.system]
section = "System"
sig = "query, target?"
desc = "Query system status: query='status' (overall), 'env' (environment variables), 'env-refresh' (reload shell env), 'logs' (system logs for target service), 'service-status' (systemd status), 'processes' (top), 'containers' (podman ps)."
tier = "core"
cmd = "if [ {query} = status ]; then mios-system-status; elif [ {query} = logs ]; then journalctl -u {target} -n 50 --no-pager; elif [ {query} = service-status ]; then systemctl status {target} --no-pager; elif [ {query} = processes ]; then ps aux --sort=-%cpu | head -n 20; elif [ {query} = containers ]; then podman ps; else mios-system-status; fi"
  [verbs.system.params.query]
  type = "string"
  enum = ["status", "env", "env-refresh", "logs", "service-status", "processes", "containers"]
  [verbs.system.params.target]
  type = "string"

[verbs.windows_input]
section = "PC input"
sig = "action, text?, key?, name?, x?, y?"
desc = "Inject input into the HOST WINDOWS desktop. action='type', 'key' (press a key/combo), 'click' (pixel click), 'find' (find UI element), 'click-element' (click named UI element), 'list-element' (list all UI elements)."
permission = "write"
cmd = "if [ '{action}' = 'type' ]; then mios-pc-control type '{text}'; elif [ '{action}' = 'key' ]; then mios-pc-control key '{key}'; elif [ '{action}' = 'click' ]; then mios-pc-control click {x} {y}; elif [ '{action}' = 'find' ]; then mios-pc-control ui-find '{name}'; elif [ '{action}' = 'click-element' ]; then mios-pc-control ui-click '{name}'; else mios-pc-control ui-list; fi"
  [verbs.windows_input.params.action]
  type = "string"
  enum = ["type", "key", "click", "find", "click-element", "list-element"]
  [verbs.windows_input.params.text]
  type = "string"
  [verbs.windows_input.params.key]
  type = "string"
  [verbs.windows_input.params.name]
  type = "string"
  [verbs.windows_input.params.x]
  type = "integer"
  [verbs.windows_input.params.y]
  type = "integer"

[verbs.linux_input]
section = "Computer-use"
sig = "action, text?, key?, x?, y?"
desc = "Inject input into the LINUX in-VM desktop. action='screenshot', 'ground', 'atspi', 'window-list', 'click', 'type', 'key'."
permission = "write"
cmd = "if [ '{action}' = 'screenshot' ]; then mios-computer-use screenshot /tmp/linux.png; elif [ '{action}' = 'type' ]; then mios-computer-use type '{text}'; elif [ '{action}' = 'key' ]; then mios-computer-use key '{key}'; elif [ '{action}' = 'click' ]; then mios-computer-use click {x} {y}; else mios-computer-use {action} '{text}'; fi"
  [verbs.linux_input.params.action]
  type = "string"
  enum = ["screenshot", "ground", "atspi", "window-list", "click", "type", "key"]
  [verbs.linux_input.params.text]
  type = "string"
  [verbs.linux_input.params.key]
  type = "string"
  [verbs.linux_input.params.x]
  type = "integer"
  [verbs.linux_input.params.y]
  type = "integer"

[verbs.file_edit]
section = "File edits"
sig = "op, path, content?, find?, replace?, line?"
desc = "Edit text files on disk. op='create' (write new file), 'replace' (replace matching text), 'insert' (insert text at line N, default prepend)."
tier = "core"
permission = "write"
sandbox_profile = "workspace"   # opt-in sandbox confinement (see [dispatch].sandbox_enforce)
# Backed by the real mios-text-edit primitive (create/str_replace/insert).
# Placeholders are shlex-quoted by the renderer -- do NOT wrap them in literal
# quotes. For large/multi-line content prefer the dedicated create_file /
# replace_file_text / insert_file_lines verbs (they stream content via stdin).
cmd = "if [ {op} = create ]; then mios-text-edit create {path} --content {content}; elif [ {op} = replace ]; then mios-text-edit str_replace {path} --old {find} --new {replace}; else mios-text-edit insert {path} --line {line=0} --content {content}; fi"
  [verbs.file_edit.params.op]
  type = "string"
  enum = ["create", "replace", "insert"]
  [verbs.file_edit.params.path]
  type = "string"
  [verbs.file_edit.params.content]
  type = "string"
  [verbs.file_edit.params.find]
  type = "string"
  [verbs.file_edit.params.replace]
  type = "string"
  [verbs.file_edit.params.line]
  type = "integer"

[[desktop.app_types]]
type = "games"
os_priority = "windows"

[[desktop.app_types]]
type = "settings"
os_priority = "both"

[[desktop.app_types]]
type = "system"
os_priority = "both"

[[desktop.app_types]]
type = "browser"
os_priority = "linux-first"
linux_default = "org.gnome.Epiphany"
windows_default = "zen"

[[desktop.app_types]]
type = "fallback"
os_priority = "linux-first"


[verbs.switch_app_default]
section    = "Configuration"
sig        = "type, app"
desc       = "Switch the default application for an abstract app type (e.g. 'browser', 'editor')."
examples   = ["make zen my default browser", "set vscode as the editor"]
cmd        = "mios-app-default {type} {app}"
  [verbs.switch_app_default.params.type]
  type = "string"
  desc = "The abstract app type, e.g. 'browser', 'editor', 'terminal'."
  [verbs.switch_app_default.params.app]
  type = "string"
  desc = "The concrete application name or id, e.g. 'zen', 'code'."

# ----------------------------------------------------------------------------
# [storage.cephfs] -- Part 9: XDG + CephFS Unified Storage Fabric
# SSOT for mios-cephfs-provision, automount templates, and ceph.conf generation.
# ----------------------------------------------------------------------------
[storage.cephfs]
enable                          = false          # Operator sets true after cluster is live
cluster_name                    = "ceph"
monitors                        = ["127.0.0.1:6789"]   # Override with actual MON IPs in /etc/mios/install.env
fs_name                         = "cephfs"
tenant_id                       = "mios"                # Subvolume group name prefix

# Pool layout
metadata_pool                   = "cephfs_metadata"
data_pool_hot                   = "cephfs_data_hot"
data_pool_bulk                  = "cephfs_data_bulk"

# MDS tuning
mds_cache_memory_limit_gib      = 4
mds_session_cap_max             = 1024

# Client-side mount options (rendered into /etc/fstab / systemd .mount units)
mount_options                   = "noatime,fsc,_netdev"
client_cache_size               = 16384
client_readahead_max_bytes      = 33554432
client_reconnect_stale_interval = 30

# XDG cache isolation (MUST be local, NEVER CephFS)
xdg_cache_home_override         = "/run/user/{uid}/.cache"    # {uid} expanded at runtime

# Provisioning
subvolume_mode                  = "0700"
keyring_dir                     = "/etc/ceph/keyring.d"
provision_script                = "/usr/libexec/mios/mios-cephfs-provision"

# Automount
automount_enable                = true
automount_idle_timeout_s        = 600    # Unmount idle home dirs after 10 min


# ============================================================================
# [converge.*] -- Converged-Resource Architecture (Part 10)
# Controls the phased rollout of queueing, engine consolidation, memory tiering,
# and distroless base images. All values default to safe no-ops.
# ============================================================================

[converge.gateway]
mode            = "http"          # "queue" = in-process; "http" = legacy :8720 (default)
queue_maxsize   = 64              # max pending requests in the asyncio.Queue
worker_concurrency = 4            # parallel GatewayWorker coroutines
fallback_http   = "http://localhost:${MIOS_PORT_HERMES}/v1"   # used when mode="http" or worker fails

[converge.inference]
heavy_engine_mode = "single"        # "dual" = SGLang + vLLM; "single" = vLLM multi-LoRA
vllm_lora_adapters_dir = "/var/lib/mios/lora-adapters/"
vllm_allow_runtime_lora = false   # set true + VLLM_ALLOW_RUNTIME_LORA_UPDATING=true
llama_cache_reuse_tokens = 0      # 0 = disabled; 256 = enable shared prefix caching
llama_parallel_slots     = 1      # increase to 4 with cache-reuse enabled
retire_heavy_alt         = true  # true = stop mios-llm-heavy-alt.container

[converge.memory]
sqlite_vec_enable      = false    # true = per-session sqlite-vec scratchpad
scratchpad_dir         = "/run/user/{uid}"    # {uid} expanded at runtime
cold_evict_enable      = false    # true = export evicted pg rows to zstd JSONL
cold_storage_dir       = "/var/lib/mios/history/"
cold_retention_days    = 90       # delete cold archives older than this
cold_zstd_level        = 10       # zstd compression level (1-19)

[converge.image]
distroless_enable      = false    # true = agent-pipe built from distroless base
distroless_base        = "gcr.io/distroless/python3-debian13"
rechunk_enable         = false    # true = apply rechunk in CI post-build step
rechunk_format_version = 1
mcp_pool_enable        = false    # true = unified MCPClientPool in GatewayWorker
# ============================================================================
# [networks.*] -- auto-generated from static units
# ============================================================================

[networks.mios.Network]
Gateway = "${MIOS_CORE_NET_GATEWAY:-10.89.0.1}"
Label = "io.mios.network=core"
Subnet = "${MIOS_CORE_NET_SUBNET:-10.89.0.0/24}"

# ============================================================================
# [containers.*] -- auto-generated from static units
# ============================================================================

[containers.mios-adguard.Container]
AddCapability = "CAP_NET_BIND_SERVICE"
ContainerName = "mios-adguard"
Exec = "--no-check-update --work-dir /opt/adguardhome/work --config /opt/adguardhome/conf/AdGuardHome.yaml"
Group = "${MIOS_ADGUARD_GID:-825}"
Image = "${MIOS_ADGUARD_IMAGE:-docker.io/adguard/adguardhome:latest}"
Label = [
  "org.opencontainers.image.title=mios-adguard",
  "org.opencontainers.image.url=https://github.com/AdguardTeam/AdGuardHome",
  "io.podman_desktop.openInBrowser=http://localhost:${MIOS_PORT_ADGUARD_UI:-8050}/"
]
Pod = "mios-system.pod"
User = "${MIOS_ADGUARD_UID:-825}"
Volume = [
  "/etc/mios/adguard:/opt/adguardhome/conf:Z",
  "/var/lib/mios/adguard:/opt/adguardhome/work:Z"
]

[containers.mios-adguard.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-adguard.Service]
Delegate = "yes"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "300s"

[containers.mios-adguard.Unit]
After = "network-online.target mios-adguard-firstboot.service mios-webtools-firstboot.service"
ConditionPathExists = "/etc/mios/adguard/AdGuardHome.yaml"
Description = "'MiOS' AdGuard Home (DNS ad/tracker/malware sinkhole + resolver)"
Requires = "mios-adguard-firstboot.service"
Wants = "network-online.target"

[containers.mios-ceph.Container]
ContainerName = "mios-ceph"
Exec = "mon"
Group = "root"
Image = "${MIOS_CEPH_IMAGE:-quay.io/ceph/ceph:v19}"
Pod = "mios-system.pod"
User = "root"
Volume = [
  "/var/lib/ceph:/var/lib/ceph:Z",
  "/etc/ceph:/etc/ceph:Z"
]

[containers.mios-ceph.Install]
WantedBy = "multi-user.target"

[containers.mios-ceph.Service]
Delegate = "yes"
Restart = "always"

[containers.mios-ceph.Unit]
After = "network-online.target"
ConditionPathExists = "/etc/ceph/ceph.conf"
ConditionVirtualization = "!container"
Description = "'MiOS' Ceph Monitor (Podman-native)"
Wants = "network-online.target"


[containers.mios-cockpit-link.Unit]
After = "network-online.target cockpit.socket mios-webtools-firstboot.service"
ConditionPathExists = "/usr/lib/systemd/system/cockpit.socket"
ConditionVirtualization = ["!wsl", "!container"]
Description = "'MiOS' Cockpit web-console discovery shim (Podman Desktop link)"
Documentation = "https://cockpit-project.org/"
Wants = "network-online.target"

[containers.mios-cockpit-link.Container]
ContainerName = "mios-cockpit-link"
EnvironmentFile = "/etc/mios/install.env"
Exec = "socat tcp-listen:${MIOS_PORT_COCKPIT_LINK},bind=0.0.0.0,fork,reuseaddr tcp:host.containers.internal:9090"
Group = "65534"
Image = "localhost/mios-sys:latest"
Label = [
  "org.opencontainers.image.title=mios-cockpit-link",
  "org.opencontainers.image.url=https://localhost:9090/",
  "org.opencontainers.image.documentation=https://cockpit-project.org/documentation.html",
  "io.podman_desktop.openInBrowser=https://localhost:9090/"
]
Pod = "mios-system.pod"
User = "65534"

[containers.mios-cockpit-link.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-cockpit-link.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "15s"
TimeoutStartSec = "60s"

[containers.mios-code-server.Container]
AutoUpdate = "registry"
ContainerName = "mios-code-server"
Environment = [
  "PASSWORD=mios",
  "SUDO_PASSWORD=mios",
  "DEFAULT_WORKSPACE=/mnt/mios-root",
  "BIND_ADDR=0.0.0.0:${MIOS_PORT_CODE_SERVER:-8900}",
  "GIT_CONFIG_COUNT=1",
  "GIT_CONFIG_KEY_0=safe.directory",
  "GIT_CONFIG_VALUE_0=/mnt/mios-root"
]
EnvironmentFile = "/etc/mios/install.env"
Exec = "code-server --bind-addr 0.0.0.0:${MIOS_PORT_CODE_SERVER:-8900}"
Group = "0"
Image = "${MIOS_CODE_SERVER_IMAGE:-ghcr.io/coder/code-server:latest}"
Label = [
  "org.opencontainers.image.title=mios-code-server",
  "org.opencontainers.image.url=http://localhost:${MIOS_PORT_CODE_SERVER:-8900}/",
  "org.opencontainers.image.documentation=https://github.com/coder/code-server",
  "io.podman_desktop.openInBrowser=http://localhost:${MIOS_PORT_CODE_SERVER:-8900}/"
]
Pod = "mios-webtools.pod"
SecurityLabelDisable = "true"
User = "0"
Volume = [
  "/var/lib/mios/code-server:/home/coder",
  "/:/mnt/mios-root:rw,rslave"
]

[containers.mios-code-server.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-code-server.Service]
Delegate = "yes"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "600s"

[containers.mios-code-server.Unit]
After = "network-online.target mios-forge.service"
Description = "'MiOS' code-server (VS Code in a browser)"
Documentation = "https://github.com/coder/code-server"
Wants = "network-online.target"

[containers.mios-cpu-node.Container]
ContainerName = "mios-cpu-node"
Entrypoint = "/app/llama-server"
Environment = "HOME=/var/lib/mios/llamacpp"
# Health gate: the upstream llama-swap:cuda image bakes `curl localhost:8220/` as its
# HEALTHCHECK, which never matches our SSOT listen port -> the lane reports Unhealthy
# forever even while it serves 200 on ${MIOS_PORT_CPU_NODE}. Override the baked check
# against the real runtime port's llama-server /health (200 ready / 503 loading).
# NO-HARDCODE: same ${MIOS_PORT_CPU_NODE} runtime var the Exec resolves via systemd.
HealthCmd = "curl -fsS http://localhost:${MIOS_PORT_CPU_NODE}/health || exit 1"
HealthInterval = "30s"
HealthTimeout = "10s"
HealthStartPeriod = "120s"
HealthRetries = "3"
# ctx-size: 32768 (was 131072). CPU-only (n-gpu-layers 0) granite-8b with a 128k KV
# ring is the Unhealthy-on-boot bloat (huge RAM KV even at q4_0); 32768 matches the
# light lane + the VRAM/KV co-fit finding. Raise once CPU RAM headroom is proven.
Exec = "--model /models/granite-4.1-8b.gguf --host 0.0.0.0 --port ${MIOS_PORT_CPU_NODE:-8510} --ctx-size 32768 --flash-attn on --cache-type-k q4_0 --cache-type-v q4_0 --parallel 1 --threads ${MIOS_CPU_NODE_THREADS:-14} --n-gpu-layers 0 --jinja --alias mios-cpu --slot-save-path ${MIOS_LLAMACPP_SLOT_DIR:-/var/lib/mios/llamacpp/slots}"
Group = "${MIOS_LLAMACPP_GID:-827}"
Image = "${MIOS_CUDA_IMAGE:-ghcr.io/mostlygeek/llama-swap:cuda}"
Label = "org.opencontainers.image.title=mios-cpu-node"
Pod = "mios-ai.pod"
User = "${MIOS_LLAMACPP_UID:-827}"
Volume = [
  "/usr/share/mios/llamacpp/models:/models:ro,Z",
  "${MIOS_LLAMACPP_SLOT_DIR:-/var/lib/mios/llamacpp/slots}:${MIOS_LLAMACPP_SLOT_DIR:-/var/lib/mios/llamacpp/slots}:Z"
]

[containers.mios-cpu-node.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-cpu-node.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "1800s"

[containers.mios-cpu-node.Unit]
After = "network-online.target mios-webtools-firstboot.service"
ConditionPathExists = "/usr/share/mios/llamacpp/models/granite-4.1-8b.gguf"
Description = "'MiOS' CPU inference node (always-on / gaming-immune last-resort lane)"
Wants = "network-online.target"


[containers.mios-forge.Container]
AddCapability = "CAP_NET_BIND_SERVICE"
ContainerName = "mios-forge"
Environment = [
  "USER=git",
  "FORGEJO__server__ROOT_URL=http://localhost:${MIOS_PORT_FORGE_HTTP:-8400}/",
  "FORGEJO__server__HTTP_ADDR=0.0.0.0",
  "GITEA__server__HTTP_ADDR=0.0.0.0",
  "FORGEJO__server__HTTP_PORT=${MIOS_PORT_FORGE_HTTP:-8400}",
  "GITEA__server__HTTP_PORT=${MIOS_PORT_FORGE_HTTP:-8400}",
  "FORGEJO__server__START_SSH_SERVER=true",
  "FORGEJO__server__SSH_PORT=${MIOS_PORT_FORGE_SSH:-8410}",
  "FORGEJO__server__SSH_LISTEN_HOST=0.0.0.0",
  "FORGEJO__server__SSH_LISTEN_PORT=${MIOS_PORT_FORGE_SSH:-8410}",
  "FORGEJO__database__DB_TYPE=sqlite3",
  "FORGEJO__database__PATH=/data/forgejo.db",
  "FORGEJO__service__DISABLE_REGISTRATION=true",
  "FORGEJO__service__REQUIRE_SIGNIN_VIEW=false",
  "FORGEJO__security__INSTALL_LOCK=true",
  "FORGEJO__log__LEVEL=Info",
  "FORGEJO__log__MODE=console",
  "USER=git"
]
EnvironmentFile = "/etc/mios/install.env"
Exec = "forgejo web"
Group = "1000"
Image = "${MIOS_FORGE_IMAGE:-codeberg.org/forgejo/forgejo:12}"
Label = [
  "org.opencontainers.image.title=mios-forge",
  "org.opencontainers.image.url=http://localhost:${MIOS_PORT_FORGE_HTTP:-8400}/",
  "org.opencontainers.image.documentation=https://forgejo.org/docs/latest/",
  "io.podman_desktop.openInBrowser=http://localhost:${MIOS_PORT_FORGE_HTTP:-8400}/"
]
Pod = "mios-webtools.pod"
User = "1000"
Volume = [
  "/srv/mios/forge:/data:Z",
  "/etc/mios/forge:/etc/mios/forge:Z,ro"
]

[containers.mios-forge.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-forge.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "300s"

[containers.mios-forge.Unit]
After = "network-online.target mios-webtools-firstboot.service"
ConditionPathIsDirectory = "/etc/mios/forge"
ConditionVirtualization = [
  "|!container",
  "|wsl"
]
Description = "'MiOS' Git forge (Forgejo)"
Wants = "network-online.target"

[containers.mios-forgejo-runner.Container]
ContainerName = "mios-forgejo-runner"
Environment = [
  "FORGEJO_RUNNER_NAME=mios-self-hosted",
  "FORGEJO_RUNNER_LABELS=mios-self-hosted,podman,bootc,fedora-${FEDORA_VERSION:-44}",
  "FORGEJO_RUNNER_DEFAULT_LABELS=mios-self-hosted"
]
EnvironmentFile = "/etc/mios/forge/runner-token"
Exec = "forgejo-runner daemon"
Group = "0"
Image = "${MIOS_FORGE_RUNNER_IMAGE:-code.forgejo.org/forgejo/runner:7}"
Label = [
  "org.opencontainers.image.title=mios-forgejo-runner",
  "org.opencontainers.image.documentation=https://forgejo.org/docs/latest/admin/actions/",
  "io.podman_desktop.openInBrowser=http://localhost:${MIOS_PORT_FORGE_HTTP:-8400}/-/admin/actions/runners"
]
Pod = "mios-webtools.pod"
PodmanArgs = "--privileged"
User = "0"
Volume = [
  "/var/lib/containers/storage:/var/lib/containers/storage:Z",
  "/srv/mios/forge-runner:/data:Z",
  "/etc/mios/forge:/etc/mios/forge:Z,ro",
  "/var/lib/mios/forge-runner:/var/lib/mios/forge-runner:Z"
]

[containers.mios-forgejo-runner.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-forgejo-runner.Service]
Delegate = "yes"
Restart = "on-failure"
RestartSec = "15s"
TimeoutStartSec = "300s"

[containers.mios-forgejo-runner.Unit]
After = "network-online.target mios-forge.service mios-forgejo-runner-firstboot.service mios-webtools-firstboot.service"
ConditionPathExists = "/srv/mios/forge-runner/.runner"
ConditionVirtualization = [
  "|!container",
  "|wsl"
]
Description = "'MiOS' Forgejo Runner (self-hosted CI for /=git working tree)"
Documentation = "https://forgejo.org/docs/latest/admin/actions/"
Requires = "mios-forge.service"
Wants = "network-online.target mios-forgejo-runner-firstboot.service"

[containers.mios-guacamole.Container]
AutoUpdate = "registry"
ContainerName = "mios-guacamole"
Environment = [
  "POSTGRESQL_HOSTNAME=localhost",
  "POSTGRESQL_PORT=${MIOS_PORT_PGVECTOR:-8600}",
  "POSTGRESQL_DATABASE=${MIOS_PG_DB:-mios}",
  "POSTGRESQL_USERNAME=${MIOS_PG_USER:-mios}",
  "POSTGRESQL_PASSWORD=${MIOS_PG_PASS:-mios}",
  "GUACD_HOSTNAME=localhost",
  "GUACD_PORT=${MIOS_PORT_GUACD:-8560}"
]
EnvironmentFile = "/etc/mios/install.env"
Group = "${MIOS_GUACAMOLE_GID:-810}"
Image = "${MIOS_GUACAMOLE_IMAGE:-docker.io/guacamole/guacamole:latest}"
Label = [
  "org.opencontainers.image.url=http://localhost:${MIOS_PORT_GUACAMOLE_WEB:-8220}/guacamole/",
  "org.opencontainers.image.documentation=https://guacamole.apache.org/doc/",
  "io.podman_desktop.openInBrowser=http://localhost:${MIOS_PORT_GUACAMOLE_WEB:-8220}/guacamole/"
]
Pod = "mios-system.pod"
User = "${MIOS_GUACAMOLE_UID:-810}"

[containers.mios-guacamole.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-guacamole.Service]
Delegate = "yes"
Restart = "on-failure"
RestartSec = "10"

[containers.mios-guacamole.Unit]
After = "network-online.target mios-pgvector.service"
ConditionVirtualization = "!container"
Description = "'MiOS' Apache Guacamole Web"
Wants = "network-online.target mios-pgvector.service"

[containers.mios-guacd.Container]
ContainerName = "mios-guacd"
Exec = "guacd -b 0.0.0.0 -l ${MIOS_PORT_GUACD:-8560} -f"
Group = "${MIOS_GUACD_GID:-811}"
Image = "${MIOS_GUACD_IMAGE:-docker.io/guacamole/guacd:latest}"
Pod = "mios-system.pod"
User = "${MIOS_GUACD_UID:-811}"

[containers.mios-guacd.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-guacd.Service]
Delegate = "yes"
Restart = "on-failure"
RestartSec = "10"

[containers.mios-guacd.Unit]
After = "network-online.target mios-webtools-firstboot.service"
ConditionVirtualization = "!container"
Description = "'MiOS' Apache Guacamole Daemon"
Wants = "network-online.target"

[containers.mios-k3s.Container]
ContainerName = "mios-k3s"
Environment = "K3S_TOKEN=mios-cluster-secret"
Exec = "k3s server --disable=traefik --kubelet-arg=cgroup-driver=cgroupfs"
Group = "root"
Image = "${MIOS_K3S_IMAGE:-docker.io/rancher/k3s:v1.36.3-k3s1}"
Pod = "mios-system.pod"
PodmanArgs = "--privileged"
User = "root"
Volume = [
  "/var/lib/rancher/k3s:/var/lib/rancher/k3s:Z",
  "/usr/share/mios/k3s-manifests:/var/lib/rancher/k3s/server/manifests:O"
]

[containers.mios-k3s.Install]
WantedBy = "multi-user.target"

[containers.mios-k3s.Service]
Delegate = "yes"
Restart = "always"

[containers.mios-k3s.Unit]
After = "network-online.target mios-webtools-firstboot.service"
ConditionVirtualization = [
  "!wsl",
  "!container"
]
Description = "'MiOS' K3s Service (Podman-native)"
Wants = "network-online.target"

[containers.mios-llm-heavy.Container]
AddDevice = "${MIOS_GPU_DEVICE:-nvidia.com/gpu=all}"
ContainerName = "mios-llm-heavy"
Environment = [
  "HF_HUB_OFFLINE=1",
  "TRANSFORMERS_OFFLINE=1",
  "VLLM_NO_USAGE_STATS=1",
  "DO_NOT_TRACK=1",
  "VLLM_WSL2_ENABLE_PIN_MEMORY=1",
  "VLLM_USE_V2_MODEL_RUNNER=0",
  "HF_HOME=/tmp/.cache",
  "XDG_CACHE_HOME=/tmp/.cache",
  "VLLM_CACHE_DIR=/tmp/.cache",
  "FLASHINFER_WORKSPACE_DIR=/tmp/.cache",
  "TRITON_CACHE_DIR=/tmp/.cache",
  "VLLM_HOST_IP=127.0.0.1",
  "GLOO_SOCKET_IFNAME=lo",
  "NCCL_SOCKET_IFNAME=lo",
  "HOME=/tmp"
]
Exec = "--model /models --served-model-name ${MIOS_VLLM_SERVED_NAME:-mios-heavy} --host 0.0.0.0 --port ${MIOS_PORT_VLLM:-8520} --gpu-memory-utilization ${MIOS_VLLM_GPU_UTIL:-0.80} --max-model-len ${MIOS_VLLM_MAX_MODEL_LEN:-8192} --kv-cache-dtype ${MIOS_VLLM_KV_CACHE_DTYPE:-fp8} --load-format ${MIOS_VLLM_LOAD_FORMAT:-dummy} --enable-prefix-caching --enforce-eager"
Group = "${MIOS_VLLM_GID:-815}"
Image = "${MIOS_VLLM_IMAGE:-docker.io/vllm/vllm-openai:latest}"
Label = [
  "org.opencontainers.image.title=mios-llm-heavy",
  "org.opencontainers.image.url=http://localhost:${MIOS_PORT_VLLM:-8520}/v1/models",
  "io.podman_desktop.openInBrowser=http://localhost:${MIOS_PORT_VLLM:-8520}/v1/models"
]
Pod = "mios-ai.pod"
User = "${MIOS_VLLM_UID:-0}"
Volume = "/var/lib/mios/vllm/model:/models:ro,Z"

[containers.mios-llm-heavy.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-llm-heavy.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "15"
TimeoutStartSec = "600s"

[containers.mios-llm-heavy.Unit]
After = "network-online.target mios-cdi-detect.service mios-webtools-firstboot.service"
ConditionPathExists = "/var/lib/mios/vllm/model/config.json"
Description = "'MiOS' vLLM heavy lane (OpenAI /v1, PagedAttention + APC; gated)"
Wants = "network-online.target"

[containers.mios-llm-heavy-alt.Container]
AddDevice = "${MIOS_GPU_DEVICE:-nvidia.com/gpu=all}"
ContainerName = "mios-llm-heavy-alt"
Environment = [
  "HF_HUB_OFFLINE=1",
  "TRANSFORMERS_OFFLINE=1",
  "DO_NOT_TRACK=1",
  "HF_HOME=/tmp/.cache",
  "XDG_CACHE_HOME=/tmp/.cache",
  "GLOO_SOCKET_IFNAME=lo",
  "NCCL_SOCKET_IFNAME=lo",
  "HOME=/tmp"
]
Exec = "python3 -m sglang.launch_server --model /models --served-model-name ${MIOS_SGLANG_SERVED_NAME:-mios-heavy} --host 0.0.0.0 --port ${MIOS_PORT_SGLANG:-8530} --mem-fraction-static ${MIOS_SGLANG_MEM_FRACTION:-0.85} --context-length ${MIOS_SGLANG_MAX_MODEL_LEN:-262144} --load-format ${MIOS_SGLANG_LOAD_FORMAT:-dummy} --kv-cache-dtype ${MIOS_SGLANG_KV_CACHE_DTYPE:-fp8_e5m2} --tool-call-parser ${MIOS_SGLANG_TOOL_PARSER:-qwen25} --enable-hierarchical-cache --trust-remote-code"
Group = "${MIOS_VLLM_GID:-815}"
Image = "${MIOS_SGLANG_IMAGE:-docker.io/lmsysorg/sglang:latest}"
Label = [
  "org.opencontainers.image.title=mios-llm-heavy-alt",
  "org.opencontainers.image.url=http://localhost:11442/v1/models",
  "io.podman_desktop.openInBrowser=http://localhost:11442/v1/models"
]
Pod = "mios-ai.pod"
User = "${MIOS_SGLANG_UID:-0}"
Volume = "/var/lib/mios/vllm/model:/models:ro,Z"

[containers.mios-llm-heavy-alt.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-llm-heavy-alt.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "15"
TimeoutStartSec = "600s"

[containers.mios-llm-heavy-alt.Unit]
After = "network-online.target mios-cdi-detect.service mios-webtools-firstboot.service"
ConditionPathExists = "/var/lib/mios/vllm/model/config.json"
Description = "'MiOS' SGLang heavy lane (OpenAI /v1, HiCache CPU KV-offload; native 256k context)"
Wants = "network-online.target"

[containers.mios-llm-light.Container]
AddDevice = "${MIOS_GPU_DEVICE:-nvidia.com/gpu=all}"
ContainerName = "mios-llm-light"
Entrypoint = "/app/llama-swap"
Environment = [
  "HOME=/var/lib/mios/llamacpp",
  "LD_LIBRARY_PATH=/usr/lib/wsl/lib:/usr/local/cuda/lib64"
]
EnvironmentFile = "/etc/mios/install.env"
HealthCmd = "curl -fsS http://localhost:${MIOS_PORT_LLM_LIGHT}/v1/models || exit 1"
HealthInterval = "30s"
HealthTimeout = "10s"
HealthStartPeriod = "60s"
HealthRetries = "3"
Exec = "--config /app/config.yaml --listen 0.0.0.0:${MIOS_PORT_LLM_LIGHT:-8500}"
Group = "${MIOS_LLAMACPP_GID:-827}"
Image = "${MIOS_LLM_LIGHT_IMAGE:-ghcr.io/mostlygeek/llama-swap:cuda}"
Label = [
  "org.opencontainers.image.title=mios-llm-light",
  "org.opencontainers.image.documentation=https://github.com/mostlygeek/llama-swap"
]
Pod = "mios-ai.pod"
User = "${MIOS_LLAMACPP_UID:-827}"
Volume = [
  "/usr/share/mios/llamacpp/models:/models:ro,Z",
  "/usr/share/mios/llamacpp/mios-llm-light.yaml:/app/config.yaml:ro,Z",
  "${MIOS_LLAMACPP_SLOT_DIR:-/var/lib/mios/llamacpp/slots}:${MIOS_LLAMACPP_SLOT_DIR:-/var/lib/mios/llamacpp/slots}:Z"
]

[containers.mios-llm-light.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-llm-light.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "1800s"

[containers.mios-llm-light.Unit]
After = "network-online.target mios-cdi-detect.service mios-webtools-firstboot.service"
ConditionPathExists = "/usr/share/mios/llamacpp/models/.ready"
Description = "'MiOS' LLM-Light (llama.cpp multi-model + KV-paging lane, served via the upstream llama-swap proxy, FOSS)"
Wants = "network-online.target"

[containers."mios-llm-worker@".Container]
AddDevice = "${MIOS_GPU_DEVICE:-nvidia.com/gpu=all}"
ContainerName = "mios-llm-worker-%i"
Entrypoint = "/app/llama-server"
Environment = [
  "HOME=/var/lib/mios/llamacpp",
  "LD_LIBRARY_PATH=/usr/lib/wsl/lib:/usr/local/cuda/lib64"
]
EnvironmentFile = "-/run/mios/swarm/%i.env"
Exec = "--model /models/${WORKER_MODEL} --host 0.0.0.0 --port ${WORKER_PORT} --n-gpu-layers ${WORKER_NGL:-99} --ctx-size ${WORKER_CTX:-8192} --slot-save-path ${MIOS_LLAMACPP_SLOT_DIR:-/var/lib/mios/llamacpp/slots}/%i --parallel 1 --cont-batching --jinja --alias ${WORKER_MODEL}"
Group = "${MIOS_LLAMACPP_GID:-827}"
Image = "${MIOS_CUDA_IMAGE:-ghcr.io/mostlygeek/llama-swap:cuda}"
Label = "org.opencontainers.image.title=mios-llm-worker-%i"
Pod = "mios-ai.pod"
User = "${MIOS_LLAMACPP_UID:-827}"
Volume = [
  "/usr/share/mios/llamacpp/models:/models:ro,Z",
  "${MIOS_LLAMACPP_SLOT_DIR:-/var/lib/mios/llamacpp/slots}/%i:${MIOS_LLAMACPP_SLOT_DIR:-/var/lib/mios/llamacpp/slots}/%i:Z"
]

[containers."mios-llm-worker@".Install]
WantedBy = "multi-user.target default.target"

[containers."mios-llm-worker@".Service]
Delegate = "yes"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "180s"

[containers."mios-llm-worker@".Unit]
After = "network-online.target mios-cdi-detect.service mios-webtools-firstboot.service"
ConditionPathExists = [
  "/run/mios/swarm/%i.env",
  "/usr/share/mios/llamacpp/models/.ready"
]
Description = "'MiOS' swarm worker %i (single-model llama-server, FOSS)"
Wants = "network-online.target"

[containers.mios-node.Container]
ContainerName = "mios-node"
Environment = [
  "MIOS_NODE_ID=101",
  "MIOS_PORT=8650",
  "MIOS_AI_ENDPOINT=http://127.0.0.1:8640"
]
Group = "${MIOS_NODE_GID:-830}"
Image = "ghcr.io/mios-dev/mios-node:latest"
Label = "org.opencontainers.image.title=mios-node"
Pod = "mios-ai.pod"
PublishPort = ["8650:8650/udp", "8650:8650/tcp"]
User = "${MIOS_NODE_UID:-830}"
Volume = "/var/lib/mios:/var/lib/mios:Z"

[containers.mios-node.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-node.Service]
Delegate = "yes"
Restart = "always"

[containers.mios-node.Unit]
After = "network-online.target"
Description = "'MiOS' Distributed Edge Micro-Node Quadlet Service"
Wants = "network-online.target"



[containers.mios-open-webui.Container]
AutoUpdate = "registry"
ContainerName = "mios-open-webui"
Environment = [
  "OPENAI_API_BASE_URL=http://host.containers.internal:${MIOS_PORT_AGENT_PIPE:-8700}/v1",
  "OLLAMA_BASE_URL=http://host.containers.internal:${MIOS_PORT_LLM_LIGHT:-8500}",
  "WEBUI_NAME=\"MiOS AI\"",
  "WEBUI_SECRET_KEY=mios-stable-secret-change-me",
  "ENABLE_SIGNUP=False",
  "DEFAULT_USER_ROLE=admin",
  "DEFAULT_MODELS=MiOS AI",
  "PORT=${MIOS_PORT_OPEN_WEBUI:-8200}",
  "ENABLE_WEB_SEARCH=True",
  "ENABLE_IMAGE_GENERATION=True",
  "ENABLE_CODE_INTERPRETER=True",
  "ENABLE_API_KEYS=True",
  "ENABLE_API_KEYS_ENDPOINT_RESTRICTIONS=False",
  "ENABLE_FORWARD_USER_INFO_HEADERS=True",
  "RAG_TOP_K=5",
  "RAG_RELEVANCE_THRESHOLD=0.5",
  "ENABLE_RAG_HYBRID_SEARCH=True",
  "RAG_EMBEDDING_ENGINE=openai",
  "RAG_EMBEDDING_MODEL=nomic-embed-text",
  "RAG_OPENAI_API_BASE_URL=http://host.containers.internal:${MIOS_PORT_LLM_LIGHT:-8500}/v1",
  "RAG_OPENAI_API_KEY=sk-noop",
  "TASK_MODEL_EXTERNAL=hermes-agent",
  "ENABLE_TITLE_GENERATION=True",
  "ENABLE_TAGS_GENERATION=True",
  "ENABLE_FOLLOW_UP_GENERATION=True",
  "ENABLE_AUTOCOMPLETE_GENERATION=True",
  "\"TITLE_GENERATION_PROMPT_TEMPLATE=Output a 3 to 6 word title for this chat, in the same language the chat used. Title only, no quotes, no preamble, no thinking. Chat:\\n{{MESSAGES:END:2}}\"",
  "\"TAGS_GENERATION_PROMPT_TEMPLATE=Output 1 to 3 short topic tags for this chat (single words or hyphenated, in the chat language). JSON only: {\\\"tags\\\":[\\\"\\\",\\\"\\\"]}. Chat:\\n{{MESSAGES:END:4}}\"",
  "\"FOLLOW_UP_GENERATION_PROMPT_TEMPLATE=Suggest 3 short follow up actions the user might want next, grounded in the CURRENT chat content and the operator's CURRENT system state. Each is a single phrase or short imperative the user could click, in the same language and tone the chat used. JSON only: {\\\"follow_ups\\\":[\\\"\\\",\\\"\\\",\\\"\\\"]}. Chat:\\n{{MESSAGES:END:6}}\"",
  "\"AUTOCOMPLETE_GENERATION_PROMPT_TEMPLATE=Continue the user partial input naturally, in their language. Output ONLY the completion text, no quotes, no preamble. Input so far: {{PROMPT}}\""
]
EnvironmentFile = [
  "/etc/mios/hermes/api.env",
  "/etc/mios/install.env"
]
Image = "${MIOS_OPEN_WEBUI_IMAGE:-ghcr.io/open-webui/open-webui:main}"
Group = "${MIOS_OPEN_WEBUI_GID:-817}"
Label = [
  "org.opencontainers.image.title=mios-open-webui",
  "org.opencontainers.image.url=http://localhost:${MIOS_PORT_OPEN_WEBUI:-8200}/",
  "io.podman_desktop.openInBrowser=http://localhost:${MIOS_PORT_OPEN_WEBUI:-8200}/"
]
Pod = "mios-ai.pod"
User = "${MIOS_OPEN_WEBUI_UID:-817}"
Volume = [
  "/var/lib/mios/open-webui:/app/backend/data:Z",
  "/usr/share/mios:/usr/share/mios:ro",
  "/etc/mios:/etc/mios:ro",
  "/var/lib/mios/hermes-tail:/var/lib/mios/hermes-tail:ro",
  "/var/lib/mios/delegation-prefilter:/var/lib/mios/delegation-prefilter:ro",
  "/var/lib/mios/agent-nudger:/var/lib/mios/agent-nudger:ro",
  "/var/lib/mios/log-watcher:/var/lib/mios/log-watcher:ro",
  "/var/lib/mios/cron-director:/var/lib/mios/cron-director:ro",
  "/usr/local/sbin:/usr/local/sbin:ro",
  "/usr/libexec/mios:/usr/libexec/mios:ro",
  "/var/lib/mios/agent-env:/var/lib/mios/agent-env:ro",
  "/run/mios-launcher:/run/mios-launcher",
  "/mnt/c:/mnt/c:ro",
  "/mnt/m:/mnt/m:ro"
]

[containers.mios-open-webui.Install]
WantedBy = "multi-user.target"

[containers.mios-open-webui.Service]
Delegate = "yes"
EnvironmentFile = [
  "/etc/mios/hermes/api.env",
  "/etc/mios/install.env"
]
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "300s"

[containers.mios-open-webui.Unit]
# After mios-cdi-detect.service: open-webui is in MIOS_AI_QUADLETS_DEFAULT so the
# image ships a baked AddDevice=nvidia.com/gpu=all drop-in even though the base
# needs no GPU; without ordering after cdi-detect it can start before
# mios-gpu-passthrough rewrites that to the CPU-only reset -> podman "CDI device
# not found" flap on a GPU-less/non-NVIDIA host. Mirrors mios-llm-light.
After = "network-online.target mios-agent-pipe.service mios-cdi-detect.service mios-webtools-firstboot.service"
ConditionPathExists = "/etc/mios/hermes/api.env"
Description = "'MiOS' Open WebUI"
Wants = "network-online.target mios-agent-pipe.service mios-cdi-detect.service"

[containers.mios-pgvector.Container]
ContainerName = "mios-pgvector"
Environment = [
  "POSTGRES_USER=${MIOS_PG_USER:-mios}",
  "POSTGRES_PASSWORD=${MIOS_PG_PASS:-mios}",
  "POSTGRES_DB=${MIOS_PG_DB:-mios}",
  "PGDATA=/var/lib/postgresql/data/pgdata"
]
Exec = "postgres -c port=${MIOS_PORT_PGVECTOR:-8600} -c listen_addresses=${MIOS_PG_BIND_ADDR:-127.0.0.1} -c hnsw.iterative_scan=${MIOS_PG_HNSW_ITERATIVE_SCAN:-strict_order} -c hnsw.max_scan_tuples=${MIOS_PG_HNSW_MAX_SCAN_TUPLES:-20000} -c hnsw.scan_mem_multiplier=${MIOS_PG_HNSW_SCAN_MEM_MULTIPLIER:-1}"
Group = "${MIOS_PGVECTOR_GID:-826}"
Image = "${MIOS_PGVECTOR_IMAGE:-docker.io/pgvector/pgvector:pg18}"
Label = [
  "org.opencontainers.image.title=mios-pgvector",
  "org.opencontainers.image.documentation=https://github.com/pgvector/pgvector"
]
Pod = "mios-ai.pod"
User = "${MIOS_PGVECTOR_UID:-826}"
Volume = [
  "${MIOS_PG_DATA_DIR:-/var/lib/mios/pgvector}:/var/lib/postgresql/data:Z",
  "/usr/share/mios/postgres/schema-init.sql:/docker-entrypoint-initdb.d/10-mios-schema.sql:ro",
  "${MIOS_PG_RESTORE_SQL:-/var/lib/mios/pgvector-restore.sql}:/docker-entrypoint-initdb.d/20-mios-restore.sql:ro"
]
EnvironmentFile = "/etc/mios/install.env"

[containers.mios-pgvector.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-pgvector.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
ExecStartPost = "/usr/bin/bash -c 'for i in $(seq 1 45); do /usr/bin/podman exec mios-pgvector pg_isready -p ${MIOS_PORT_PGVECTOR:-8600} -U ${MIOS_PG_USER:-mios} -d ${MIOS_PG_DB:-mios} -q && break; sleep 2; done; /usr/bin/podman exec mios-pgvector psql -p ${MIOS_PORT_PGVECTOR:-8600} -v ON_ERROR_STOP=0 -U ${MIOS_PG_USER:-mios} -d ${MIOS_PG_DB:-mios} -f /docker-entrypoint-initdb.d/10-mios-schema.sql || true'"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "180s"

[containers.mios-pgvector.Unit]
After = "network-online.target"
ConditionVirtualization = [
  "|!container",
  "|wsl"
]
Description = "'MiOS' PostgreSQL + pgvector (unified agent-plane datastore, FOSS)"
Wants = "network-online.target"

[containers.mios-pxe-hub.Container]
ContainerName = "mios-pxe-hub"
Exec = "/usr/bin/matchbox -address 0.0.0.0:${MIOS_PORT_PXE_HUB_API:-8585} -data-path /var/lib/matchbox/data -assets-path /var/lib/matchbox/assets"
Group = "0"
Image = "${MIOS_PXE_HUB_IMAGE:-quay.io/poseidon/matchbox:latest}"
Pod = "mios-system.pod"
User = "0"
Volume = [
  "/var/lib/mios/pxe/config:/var/lib/matchbox/data:Z",
  "/var/lib/mios/pxe/assets:/var/lib/matchbox/assets:Z"
]

[containers.mios-pxe-hub.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-pxe-hub.Service]
Delegate = "yes"
Restart = "on-failure"
RestartSec = "10"

[containers.mios-pxe-hub.Unit]
After = "network-online.target mios-webtools-firstboot.service"
ConditionVirtualization = [
  "!wsl",
  "!container"
]
Description = "'MiOS' PXE Boot Hub"
Wants = "network-online.target"

[containers.mios-searxng.Container]
ContainerName = "mios-searxng"
Environment = [
  "SEARXNG_BASE_URL=http://localhost:${MIOS_PORT_SEARXNG:-8800}/",
  "SEARXNG_BIND_ADDRESS=127.0.0.1:${MIOS_PORT_SEARXNG:-8800}",
  "BIND_ADDRESS=127.0.0.1:${MIOS_PORT_SEARXNG:-8800}",
  "GRANIAN_HOST=127.0.0.1",
  "GRANIAN_PORT=${MIOS_PORT_SEARXNG:-8800}",
  "GRANIAN_WORKERS=2",
  "GRANIAN_BLOCKING_THREADS=8",
  "SEARXNG_LOG_LEVEL=WARNING"
]
EnvironmentFile = "/etc/mios/install.env"
Group = "${MIOS_SEARXNG_GID:-818}"
Image = "${MIOS_SEARXNG_IMAGE:-docker.io/searxng/searxng:latest}"
Label = [
  "org.opencontainers.image.title=mios-searxng",
  "org.opencontainers.image.url=http://localhost:${MIOS_PORT_SEARXNG:-8800}/",
  "org.opencontainers.image.documentation=https://docs.searxng.org/",
  "io.podman_desktop.openInBrowser=http://localhost:${MIOS_PORT_SEARXNG:-8800}/"
]
Pod = "mios-webtools.pod"
User = "${MIOS_SEARXNG_UID:-818}"
Volume = [
  "/etc/mios/searxng:/etc/searxng:Z",
  "/var/cache/mios/searxng:/var/cache/searxng:Z"
]

[containers.mios-searxng.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-searxng.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "300s"

[containers.mios-searxng.Unit]
After = "network-online.target mios-webtools-firstboot.service"
ConditionPathIsDirectory = "/etc/mios/searxng"
Description = "'MiOS' SearXNG metasearch (privacy-respecting search proxy)"
Wants = "network-online.target"

[containers.mios-webtools-crawl4ai.Container]
ContainerName = "mios-webtools-crawl4ai"
Environment = [
  "MIOS_CRAWL_CDP_URL=${MIOS_CRAWL_CDP_URL:-http://127.0.0.1:${MIOS_PORT_CHROME_CDP:-9222}}",
  "MIOS_PORT_CRAWL4AI=${MIOS_PORT_CRAWL4AI:-8810}",
  "MIOS_CRAWL_BIND=127.0.0.1",
  "MIOS_CRAWL_CAMOUFOX=${MIOS_CRAWL_CAMOUFOX:-true}",
  "MIOS_CRAWL_MIN_CHARS=${MIOS_CRAWL_MIN_CHARS:-200}"
]
Exec = "python /app/mios-crawl4ai-service.py"
Group = "0"
Image = "${MIOS_CRAWL4AI_IMAGE:-localhost/mios-crawl4ai-slim:latest}"
Label = [
  "org.opencontainers.image.title=mios-webtools-crawl4ai",
  "io.podman_desktop.openInBrowser=http://localhost:11235/healthz"
]
Pod = "mios-webtools.pod"
User = "0"
Volume = "/usr/lib/mios/crawl4ai/mios-crawl4ai-service.py:/app/mios-crawl4ai-service.py:ro"

[containers.mios-webtools-crawl4ai.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-webtools-crawl4ai.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "120s"

[containers.mios-webtools-crawl4ai.Unit]
Description = "'MiOS' web-tools crawl4ai slim engine (Chrome-CDP primary + camoufox fallback)"
After = "mios-webtools-firstboot.service"

[containers.mios-webtools-firecrawl-api.Container]
ContainerName = "mios-webtools-firecrawl-api"
Environment = [
  "REDIS_URL=redis://127.0.0.1:${MIOS_PORT_REDIS:-8565}",
  "REDIS_RATE_LIMIT_URL=redis://127.0.0.1:${MIOS_PORT_REDIS:-8565}",
  "HOST=127.0.0.1",
  "PORT=${MIOS_PORT_FIRECRAWL:-8820}",
  "USE_DB_AUTHENTICATION=false",
  "NUM_WORKERS_PER_QUEUE=${MIOS_FIRECRAWL_WORKERS:-2}",
  "PLAYWRIGHT_MICROSERVICE_URL=",
  "PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium",
  "BULL_AUTH_KEY=${MIOS_FIRECRAWL_BULL_KEY:-mios}",
  "LOGGING_LEVEL=${MIOS_FIRECRAWL_LOG_LEVEL:-INFO}",
  "NODE_ENV=production"
]
Exec = "pnpm run start:production"
Group = "0"
Image = "${MIOS_FIRECRAWL_IMAGE:-localhost/mios-firecrawl:v1.0.0}"
Label = [
  "org.opencontainers.image.title=mios-webtools-firecrawl-api",
  "io.podman_desktop.openInBrowser=http://localhost:3002/"
]
Pod = "mios-webtools.pod"
User = "0"
WorkingDir = "/app"

[containers.mios-webtools-firecrawl-api.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-webtools-firecrawl-api.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "180s"

[containers.mios-webtools-firecrawl-api.Unit]
After = "mios-webtools-redis.service mios-webtools-firstboot.service"
Description = "'MiOS' web-tools firecrawl API (v1.0.0, self-host no-auth)"
Wants = "mios-webtools-redis.service"

[containers.mios-webtools-firecrawl-worker.Container]
ContainerName = "mios-webtools-firecrawl-worker"
Environment = [
  "REDIS_URL=redis://127.0.0.1:${MIOS_PORT_REDIS:-8565}",
  "REDIS_RATE_LIMIT_URL=redis://127.0.0.1:${MIOS_PORT_REDIS:-8565}",
  "HOST=127.0.0.1",
  "PORT=${MIOS_PORT_FIRECRAWL:-8820}",
  "USE_DB_AUTHENTICATION=false",
  "NUM_WORKERS_PER_QUEUE=${MIOS_FIRECRAWL_WORKERS:-2}",
  "PLAYWRIGHT_MICROSERVICE_URL=",
  "PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium",
  "BULL_AUTH_KEY=${MIOS_FIRECRAWL_BULL_KEY:-mios}",
  "LOGGING_LEVEL=${MIOS_FIRECRAWL_LOG_LEVEL:-INFO}",
  "NODE_ENV=production"
]
Exec = "pnpm run workers"
Group = "0"
Image = "${MIOS_FIRECRAWL_IMAGE:-localhost/mios-firecrawl:v1.0.0}"
Label = "org.opencontainers.image.title=mios-webtools-firecrawl-worker"
Pod = "mios-webtools.pod"
User = "0"
WorkingDir = "/app"

[containers.mios-webtools-firecrawl-worker.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-webtools-firecrawl-worker.Service]
Delegate = "yes"
EnvironmentFile = "/etc/mios/install.env"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "180s"

[containers.mios-webtools-firecrawl-worker.Unit]
After = "mios-webtools-redis.service mios-webtools-firstboot.service"
Description = "'MiOS' web-tools firecrawl worker (Bull queue processor)"
Wants = "mios-webtools-redis.service"

[containers.mios-webtools-redis.Container]
AutoUpdate = "registry"
ContainerName = "mios-webtools-redis"
Exec = "valkey-server --bind 127.0.0.1 --port ${MIOS_PORT_REDIS:-8565} --appendonly no"
Group = "0"
Image = "docker.io/valkey/valkey:latest"
Label = "org.opencontainers.image.title=mios-webtools-redis"
Pod = "mios-webtools.pod"
User = "0"

[containers.mios-webtools-redis.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-webtools-redis.Service]
Delegate = "yes"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "60s"

[containers.mios-webtools-redis.Unit]
After = "network-online.target mios-webtools-firstboot.service"
Description = "'MiOS' web-tools redis (firecrawl queue + ratelimit store)"
Wants = "network-online.target"

[containers.mios-otelcol.Container]
ContainerName = "mios-otelcol"
Group = "${MIOS_OTELCOL_GID:-829}"
# Pinned: `all-in-one` is the Jaeger v1 line only (v2 ships as
# jaegertracing/jaeger), and :latest == 1.76.0. The tag decides which env model
# the two host-port vars below belong to, so it cannot float.
Image = "${MIOS_OTELCOL_IMAGE:-docker.io/jaegertracing/all-in-one:1.76.0}"
# Jaeger v1 binds via viper AutomaticEnv with `-`/`.` -> `_`, so
# `collector.otlp.grpc.host-port` and `query.http-server.host-port` map to the
# two names below. Without them the SSOT keys named numbers nothing bound.
Environment = [
  "COLLECTOR_OTLP_ENABLED=true",
  "COLLECTOR_OTLP_GRPC_HOST_PORT=:${MIOS_PORT_OTELCOL_OTLP:-8575}",
  "QUERY_HTTP_SERVER_HOST_PORT=:${MIOS_PORT_OTELCOL_UI:-8580}"
]
Label = [
  "org.opencontainers.image.title=mios-otelcol",
  "org.opencontainers.image.url=http://localhost:${MIOS_PORT_OTELCOL_UI:-8580}/",
  "io.podman_desktop.openInBrowser=http://localhost:${MIOS_PORT_OTELCOL_UI:-8580}/"
]
Pod = "mios-webtools.pod"
User = "${MIOS_OTELCOL_UID:-829}"

[containers.mios-otelcol.Install]
WantedBy = "multi-user.target default.target"

[containers.mios-otelcol.Service]
Delegate = "yes"
Restart = "on-failure"
RestartSec = "10s"
TimeoutStartSec = "300s"

[containers.mios-otelcol.Unit]
After = "network-online.target mios-webtools-firstboot.service"
Description = "'MiOS' OpenTelemetry Collector and Jaeger Trace Viewer"
Wants = "network-online.target"


# ─── Editions Matrix ─────────────────────────────────────────────────────────
# Defines the different flavors/editions of MiOS that can be built by the
# pipeline (T-146). The build scripts can load an edition profile to override
# the default autounattend and branding keys from a single SSOT.
[editions.mios]
autounattend.uup_channel     = "retail"
autounattend.uup_arch        = "amd64"
autounattend.posture         = "B"
autounattend.debloat_profile  = "minimal"
autounattend.xbox.enable     = "false"
colors.accent                = "#1A407F"
branding.living_wallpaper    = "false"
metal.gpu.arbitration        = "static"
metal.gpu.assignments        = { mios-guest = ["0000:01:00.0"] }

[editions.mios-xbox]
autounattend.uup_channel     = "dev"
autounattend.uup_arch        = "amd64"
autounattend.posture         = "C"
autounattend.debloat_profile  = "gaming"
autounattend.xbox.enable     = "true"
colors.accent                = "#282262"
branding.living_wallpaper    = "true"
metal.gpu.arbitration        = "static"
metal.gpu.assignments        = { mios-guest = ["0000:01:00.0"] }

[editions.mios-xbox-arm]
autounattend.uup_channel     = "dev"
autounattend.uup_arch        = "arm64"
autounattend.posture         = "C"
autounattend.debloat_profile  = "gaming"
autounattend.xbox.enable     = "true"
colors.accent                = "#4B105C"
branding.living_wallpaper    = "true"
metal.gpu.arbitration        = "static"
metal.gpu.assignments        = { mios-guest = ["0000:01:00.0"] }

[blade]
type = "hybrid"

# Where the hardware sniff demotes to, and where role-apply falls back when the
# resolved archetype ships no target. Must itself be an archetype, and must be
# an archetype that survives on any hardware -- it is chosen precisely when the
# machine cannot do what the resolved role implies. Gate: check_role_ssot.
fallback = "headless"

# Services a seat still starts because no capability gates them yet.
# SHRINK-ONLY (gate: check_blade_coverage). Drained by T-319; keep it at [].
ungated = []

# A `mini` plane is REQUIRED unless registered here (ADR-0016 D14.1).
# A Mini with no radio is still a Mini; one with no hypervisor is not.
optional_planes = ["radio"]

# Soft pull on a gated unit, degrades without it. Reasons: ADR-0016 D4.
soft_ok = ["hermes-worker", "mios-hermes-browser"]

# Units a seat DELIBERATELY runs: what the PERSON touches. A blade runs what
# the WORK needs. See ADR-0016 D4.
seat_side = [
  "mios-agent-pipe",            # the local front door; MIOS_AI_ENDPOINT points it at a blade
  "hermes-dashboard",           # browser UI
  "mios-hermes-browser",        # CDP browser the agent tools drive
  "mios-hermes-tail",           # journal tail into the UI
  "mios-ttyd-bash",             # browser pty
  "mios-ttyd-powershell",       # browser pty
]

# ---- Workload mobility. Rationale: ADR-0017.

[blade.placement]
# Image -> k3s. libvirt domain -> Pacemaker. Never the other kind.
containers = "k3s"
vms        = "pacemaker"
# Retry in place first: most failures are a crash on a healthy machine.
failover_order = ["local", "localhost", "cluster"]

[blade.collapse]
# Asymmetric on purpose: quick to absorb, slow to release. Anti-flap.
fail_checks     = 3
dwell_s         = 30
recover_dwell_s = 120

[blade.discovery]
# First healthy tier wins; same chain answers "am I isolated".
order            = ["localhost", "mdns", "tailnet", "remote"]
health_path      = "/v1/models"
health_timeout_s = 3
remote           = ""      # operator-declared fallback endpoint; empty = none

[blade.reconcile]
# Blade-to-blade only; seats are stateless (ADR-0016 s8).
enabled      = true              # schema carries origin_node + logical_ts (AGY-1598, AGY-1599)
knowledge    = "union-by-hash"
embeddings   = "union-by-hash"
agent_memory = "append-ordered"
event        = "append-ordered"
session      = "last-writer-wins"
scratch      = "last-writer-wins"
config_kv    = "conflict-is-error"   # config is intent; never auto-pick a winner


# [blade.mesh] -- LAN first, Tailscale second. ADR-0016 D14.3.
# The ORDER is [blade.discovery].order; never restate it here (Law 9).

# blocks_boot = false is Law 12: enrolment never gates a boot.
# federate = "native": peers join via each system's OWN mechanism -- k3s
# server/agent join, corosync membership -- automatically, never by hand.
[blade.mesh]
blocks_boot = false
federate    = "native"

# [blade.cluster] -- k3s-NATIVE HA: 3 servers on embedded etcd. ADR-0016 D15.
# The 3 localhost_hosts ARE the 3 etcd members, so quorum works on ONE box.

# server: empty = the first Mini installed. Set it to pin a specific box.
[blade.cluster]
k3s_servers      = 3
server           = ""
control_plane_ha = true
localhost_hosts  = 3

# [blade.fencing] -- self-fence on quorum loss. ADR-0016 D12.1.
# No member must be reached to be fenced; that is what makes a guest vote safe.

# diskless = quorum-driven watchdog only, no shared block device.
# Packages `sbd` and `fence-agents-all` already ship.
[blade.fencing]
method   = "sbd"
diskless = true

# [blade.storage] -- CephFS shadow-copy policy. ADR-0016 D12.3.
# Every class replicates; encryption, not withholding, is the confidentiality.

# NOT [security].disk_encryption: that is TPM2-sealed to one box and cannot
# travel with a shadow copy. Who holds the travelling key is UNSETTLED.

# See ADR-0016 D12.3: whether a remote peer holds the key is UNSETTLED.
[blade.storage]
replication = "all"        # every data class, Mini-to-Mini only
at_rest     = "dmcrypt"   # ceph-volume --dmcrypt; key in the MON config-key store

# [blade.uplink] -- `router` is owner=mini and never moves; the DEFAULT ROUTE
# does. NOT [security].egress, which is an allowlist rather than a failover.

# ADR-0016 D12.4. Tension with ADR-0017 D3 (local-first) is recorded there.
[blade.uplink]
failover = ["local", "peer"]

# [blade.hardware] -- MiOS boots on ANY hardware. ADR-0016 D14.
# These gate a PLANE, never the boot: a box that misses one still boots.

# LAN alone is both uplink and downlink, so one interface is the whole floor.
# A radio is OPTIONAL (0 or 1); an AP is a capability, never a requirement.
[blade.hardware]
min_interfaces = 1
max_radios     = 1
min_ap_capable = 0

# [blade.planes] -- which planes a Mini owns and which can be shed.
# Meaning of owner/markers/wired_by: ADR-0016 Decision 10.

# owner: mini = owns metal a guest lacks, never shed. either = a movable workload.
# markers: packages proving the plane is baked. Empty = payload, not RPM.

# wired_by: the file that activates it. Verdicts are DERIVED, never written here.
# Generator + gate: tools/generate-metal-vs-hosted.py --check (Law 8).

[blade.planes.hypervisor]
role     = "runs the VMs and containers every other plane lands in"
owner    = "mini"
markers  = ["libvirt", "qemu-kvm"]
wired_by = "usr/lib/sysctl.d/99-mios-vmhost.conf"

[blade.planes.radio]
role     = "serves WiFi clients as an access point. OPTIONAL: MiOS boots on hardware with no radio at all"
owner    = "mini"
markers  = ["hostapd", "iw", "wireless-regdb"]
wired_by = ""

[blade.planes.router]
role     = "the uplink: forwards, NATs and filters. The LAN is both uplink and downlink, so one interface suffices"
owner    = "mini"
markers  = ["firewalld", "dnsmasq"]
wired_by = "usr/lib/sysctl.d/99-mios-vmhost.conf"

[blade.planes.mesh]
role     = "joins every node and service into one addressable overlay"
owner    = "mini"
markers  = ["avahi", "nss-mdns"]
wired_by = ""

[blade.planes.storage]
role     = "CephFS -- a NATIVE service of the Mini platform, on bare metal. Never travels to a transient OCI image"
owner    = "mini"
markers  = ["ceph-common", "cephadm"]
wired_by = "usr/share/containers/systemd/mios-ceph.container"

[blade.planes.orchestrator]
role     = "k3s: places containers. One server per box serving [blade.cluster].localhost_hosts logical hosts; boxes federate over the mesh and sync by hand (ADR-0016 D14)"
owner    = "either"
markers  = ["k3s"]
wired_by = "usr/share/containers/systemd/mios-k3s.container"

[blade.planes.ha]
role     = "Pacemaker/Corosync: places and live-migrates VMs (ADR-0017 D1). A NATIVE platform service on bare metal; every member self-fences via SBD"
owner    = "mini"
markers  = ["pacemaker", "corosync"]
wired_by = "usr/lib/greenboot/check/wanted.d/50-mios-ha-cluster.sh"

[blade.planes.ai]
role     = "the OpenAI-compatible front door and the lanes behind it"
owner    = "either"
markers  = []
wired_by = "usr/share/containers/systemd/mios-llm-light.container"


# An archetype expands to the capabilities its blade activates. An archetype
# with NO capabilities is a SEAT: it runs the UX and consumes remote addresses.
[blade.archetypes]
hybrid     = ["gpu-serving", "controller", "service-plane"]
compute    = ["gpu-serving", "service-plane"]
controller = ["controller", "service-plane"]
headless   = ["service-plane"]
desktop    = ["service-plane"]
endpoint   = []
# Cluster roles. They already shipped mios-k3s-master.target / mios-ha-node.target
# and role-apply already selected them from a `k3s*`/`ha*` glob -- but they were
# absent HERE, so such a blade resolved to zero capabilities and started nothing
# while its target came up.
k3s-master = ["controller", "service-plane"]
ha-node    = ["controller", "service-plane"]

# Legacy role spellings, mapped onto an archetype. This table replaces the
# `k3s*)` / `ha*)` globs in role-apply: a glob accepted `k3sx` too, selecting a
# target for a role that grants nothing. Every value must be an archetype key
# (gate: check_role_ssot).
[blade.role_aliases]
k3s = "k3s-master"
ha  = "ha-node"

[blade.requires]
# GPU lanes: gpu-serving AND service-plane (repeated ConditionPathExists is AND).
mios-llm-heavy     = ["gpu-serving", "service-plane"]
mios-llm-heavy-alt = ["gpu-serving", "service-plane"]
"mios-llm-worker@" = ["gpu-serving", "service-plane"]
# The rest of the service plane: offloaded wholesale on a seat.
mios-adguard                   = ["service-plane"]
mios-ceph                      = ["service-plane"]
mios-cockpit-link              = ["service-plane"]
mios-code-server               = ["service-plane"]
mios-cpu-node                  = ["service-plane"]
mios-forge                     = ["service-plane"]
mios-forgejo-runner            = ["service-plane"]
mios-guacamole                 = ["service-plane"]
mios-guacd                     = ["service-plane"]
mios-node                      = ["service-plane"]
mios-k3s           = ["controller", "service-plane"]
mios-llm-light                 = ["service-plane"]
mios-open-webui                = ["service-plane"]
mios-otelcol                   = ["service-plane"]
mios-pgvector                  = ["service-plane"]
mios-pxe-hub       = ["controller", "service-plane"]
mios-webtools-crawl4ai         = ["service-plane"]
mios-webtools-firecrawl-api    = ["service-plane"]
mios-webtools-firecrawl-worker = ["service-plane"]
mios-webtools-redis            = ["service-plane"]
# Native (non-Quadlet) long-running units. Same rule: a seat offloads them.
hermes-worker                  = ["service-plane"]
mios-searxng                   = ["service-plane"]
mios-hermes-browser-worker     = ["service-plane"]
k3s                            = ["service-plane"]
mios-account-sync              = ["service-plane"]
mios-agents                    = ["service-plane"]
mios-cron-director             = ["service-plane"]
mios-daemon                    = ["service-plane"]
mios-finetune-serve            = ["service-plane"]
mios-mcp                       = ["service-plane"]
mios-opencode-gateway          = ["service-plane"]
mios-policy-arbiter            = ["service-plane"]
# Units that activate a gated unit and cannot work without it. See ADR-0016 D4.
mios-embed-backfill            = ["service-plane"]
mios-forge-firstboot           = ["service-plane"]
mios-forgejo-runner-firstboot  = ["service-plane"]
mios-git-root-init             = ["service-plane"]
mios-k3s-master                = ["service-plane"]
mios-passport-provision        = ["service-plane"]
mios-pgvector-backup           = ["service-plane"]
mios-skills-miner              = ["service-plane"]
mios-sys-env-refresh           = ["service-plane"]
mios-userdb-render             = ["service-plane"]

[blade.cpu_fallbacks]
# GPU-gated service -> CPU lane, same /v1 contract. ADR-0017 D2, AGY-1596.
mios-llm-heavy     = "mios-llm-light"
mios-llm-heavy-alt = "mios-llm-light"
"mios-llm-worker@" = "mios-llm-light"

[cat]

# Target USB drive letter (without colon). Operator override; MiOS-Cat.bat defaults to D.
drivepath          = "D"

# MediCat core archive version string (used to construct the filename + cache path).
medicatver         = "21.12"

# Absolute path on the build host where the ~23 GB MediCat 7z is cached.
cache_path         = "M:\\MediCat.USB.v21.12.7z"

ventoy_version     = "latest"

xbox_builder       = ""

models             = "see [ai].bake_models + [ai.vllm].bake_model"

# MiOS-Repo shadow-config partition (the "brain" -- always present on any USB).
# Label used when formatting the small config partition.
[cat.repo_partition]
label              = "MiOS-Repo"

# MiOS-Data bulk store (OCI tar + just-all artifacts + model weights + package mirrors).
# Created ONLY on disks >= min_disk_gb; skipped on smaller sticks (degrade-open).
[cat.data_partition]
label              = "MiOS-Data"
min_disk_gb        = 512

[dotfiles]

[dotfiles.registry.btop]
template = "usr/share/mios/theme/templates/btop-mios.theme.tmpl"
target   = "etc/btop/themes/mios.theme"
[dotfiles.registry.btop.apply.target]
linux   = "~/.config/btop/themes/mios.theme"
windows = "%LOCALAPPDATA%/btop/themes/mios.theme"

[dotfiles.registry.oh-my-posh]
template = "usr/share/mios/theme/templates/oh-my-posh.omp.json.tmpl"
target   = "usr/share/mios/oh-my-posh/mios.omp.json"
[dotfiles.registry.oh-my-posh.apply.target]
linux   = "~/.config/oh-my-posh/mios.omp.json"
windows = "%LOCALAPPDATA%/oh-my-posh/mios.omp.json"

[dotfiles.registry.quickshell]
template = "usr/share/mios/theme/templates/quickshell-Theme.qml.tmpl"
target   = "usr/share/mios/quickshell/Theme.qml"
[dotfiles.registry.quickshell.apply.target]
linux = "~/.config/quickshell/Theme.qml"

[dotfiles.registry.fastfetch]
template = "usr/share/mios/theme/templates/fastfetch-config.jsonc.tmpl"
target   = "usr/share/mios/fastfetch/config.jsonc"
[dotfiles.registry.fastfetch.apply.target]
linux   = "~/.config/fastfetch/config.jsonc"
windows = "%LOCALAPPDATA%/fastfetch/config.jsonc"

[dotfiles.registry.app-shell]
template = "usr/share/mios/theme/templates/mios-app-shell.css.tmpl"
target   = "usr/share/mios/branding/mios-app-shell.css"

[dotfiles.registry.term-osc]
template = "usr/share/mios/theme/templates/mios-colors.sh.tmpl"
target   = "etc/profile.d/mios-colors.sh"

# SETTINGS surface: `section` names the [btop] table whose ~60 scalar keys
# derive the WHOLE etc/btop/btop.conf via @MIOS:btop_<key>@ tokens.
[dotfiles.registry.btop-conf]
template = "usr/share/mios/theme/templates/btop.conf.tmpl"
target   = "etc/btop/btop.conf"
section  = "btop"
[dotfiles.registry.btop-conf.apply.target]
linux   = "~/.config/btop/btop.conf"
windows = "%LOCALAPPDATA%/btop/btop.conf"

# SETTINGS surface: `section` names the [gitconfig] table; [identity] is always
# injected too (username/fullname), so etc/skel/.gitconfig derives from SSOT.
[dotfiles.registry.gitconfig]
template = "usr/share/mios/theme/templates/gitconfig.tmpl"
target   = "etc/skel/.gitconfig"
section  = "gitconfig"
# No [.apply.target]: gitconfig is skel-seed + drift-gated ONLY. Live-HOME `apply` is
# intentionally withheld until per-key merge lands -- a whole-file overwrite of the
# user's ~/.gitconfig would clobber their real identity/credentials/signing config.
# render -> etc/skel/.gitconfig seeds NEW users from [gitconfig]+[identity] SSOT.

[dotfiles.registry.vscode-colors]
template = "usr/share/mios/theme/templates/vscode-colorCustomizations.json.tmpl"
target   = "usr/share/mios/theme/fixtures/vscode-settings.expected.json"
kind     = "json-merge"
[dotfiles.registry.vscode-colors.fixture]
base     = "usr/share/mios/theme/fixtures/vscode-settings.base.json"
expected = "usr/share/mios/theme/fixtures/vscode-settings.expected.json"
[dotfiles.registry.vscode-colors.apply.target]
linux   = "~/.config/Code/User/settings.json"
windows = "%APPDATA%/Code/User/settings.json"

[dotfiles.registry.gitconfig-live]
template = "usr/share/mios/theme/templates/gitconfig.tmpl"
target   = "usr/share/mios/theme/fixtures/gitconfig.expected.gitconfig"
section  = "gitconfig"
kind     = "ini-merge"
policy   = "enforce"
[dotfiles.registry.gitconfig-live.fixture]
base     = "usr/share/mios/theme/fixtures/gitconfig.base.gitconfig"
expected = "usr/share/mios/theme/fixtures/gitconfig.expected.gitconfig"
[dotfiles.registry.gitconfig-live.apply.target]
linux   = "~/.gitconfig"
windows = "%USERPROFILE%/.gitconfig"

[dotfiles.registry.windows-terminal]
template = "usr/share/mios/theme/templates/windows-terminal-settings.json.tmpl"
target   = "usr/share/mios/theme/fixtures/windows-terminal-settings.expected.json"
kind     = "json-merge"
[dotfiles.registry.windows-terminal.fixture]
base     = "usr/share/mios/theme/fixtures/windows-terminal-settings.base.json"
expected = "usr/share/mios/theme/fixtures/windows-terminal-settings.expected.json"
[dotfiles.registry.windows-terminal.apply.target]
windows = "%LOCALAPPDATA%/Packages/Microsoft.WindowsTerminal_8wekyb3d8bbwe/LocalState/settings.json"

[dotfiles.registry.windows-registry]
template = "usr/share/mios/theme/templates/windows-registry.json.tmpl"
target   = "usr/share/mios/theme/fixtures/windows-registry.expected.json"
kind     = "registry"
section  = "colors"
[dotfiles.registry.windows-registry.fixture]
base     = "usr/share/mios/theme/fixtures/windows-registry.base.json"
expected = "usr/share/mios/theme/fixtures/windows-registry.expected.json"
[dotfiles.registry.windows-registry.apply.target]
windows  = "HKCU\\Console\\MiOS"

[dotfiles.registry.shell-aliases]
template = "usr/share/mios/theme/templates/shell-aliases.tmpl"
target   = "etc/skel/.bashrc.d/mios"
kind     = "template"
section  = "shell"
[dotfiles.registry.shell-aliases.apply.target]
linux    = "~/.bashrc.d/mios"
windows  = "%USERPROFILE%/.bashrc.d/mios"

[dotfiles.registry.ssh-config]
template = "usr/share/mios/theme/templates/ssh-config.tmpl"
target   = "usr/share/mios/theme/fixtures/ssh-config.expected"
kind     = "ssh-merge"
section  = "ssh"
[dotfiles.registry.ssh-config.fixture]
base     = "usr/share/mios/theme/fixtures/ssh-config.base"
expected = "usr/share/mios/theme/fixtures/ssh-config.expected"
[dotfiles.registry.ssh-config.apply.target]
linux    = "~/.ssh/config"
windows  = "%USERPROFILE%/.ssh/config"

[shell]
alias_ll = "ls -la"
alias_gs = "git status"
alias_gp = "git push"

[editor]
[git]

[ssh]
host          = "mios-*"
user          = "agent"
port          = 2222
identity_file = "~/.ssh/agent_ssh_key"

[gitconfig]
core_editor         = "nano"
init_default_branch = "main"
pull_rebase         = "true"
alias_co            = "checkout"
alias_br            = "branch"
alias_ci            = "commit"
alias_st            = "status"

# ----------------------------------------------------------------------------
# [templates] -- global template schema configurations.
# ----------------------------------------------------------------------------
[templates.adr]
match = "^usr/share/doc/mios/adr/\\d{4}-.*\\.md$"
comment = "md"
required_header = true
required_markers = ["## Status", "## Context", "## Decision", "## Rationale", "## Consequences"]
generated = false
scaffold = true
dest_dir = "usr/share/doc/mios/adr"
name_prefix = "0012-"
name_suffix = ".md"
emit = "file"

[templates.drift-check]
match = "^automation/98-drift-checks\\.sh$"
comment = "hash"
required_header = true
required_markers = ["check_"]
generated = false
scaffold = true
emit = "stdout"

[templates.automation-step]
match = "^automation/\\d{2}-.*\\.sh$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "automation"
name_prefix = "99-"
name_suffix = ".sh"
emit = "file"

[templates.bash-verb]
match = "^usr/libexec/mios/mios-[\\w-]+\\.sh$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/libexec/mios"
name_prefix = "mios-"
name_suffix = ".sh"
emit = "file"

[templates.bash-tool]
match = "^usr/libexec/mios/mios-[\\w-]+$|^usr/bin/[\\w-]+$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/libexec/mios"
name_prefix = "mios-"
emit = "file"

[templates.bash]
match = "^(?:tools/|usr/bin/|usr/libexec/mios/|automation/)[\\w./-]+\\.sh$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/libexec/mios"
name_suffix = ".sh"
emit = "file"

[templates.python-module]
match = "^usr/lib/mios/agent-pipe/mios_pipe/[\\w-]+\\.py$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/lib/mios/agent-pipe/mios_pipe"
name_suffix = ".py"
emit = "file"

[templates.python-test]
match = "^usr/lib/mios/agent-pipe/test_mios_[\\w-]+\\.py$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/lib/mios/agent-pipe"
name_prefix = "test_mios_"
name_suffix = ".py"
emit = "file"

[templates.python-tool]
match = "^usr/libexec/mios/mios-[\\w-]+\\.py$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/libexec/mios"
name_prefix = "mios-"
name_suffix = ".py"
emit = "file"

[templates.rust]
match = "^tools/native/[\\w./-]+\\.rs$|^src/mios-rs/[\\w./-]+\\.rs$"
comment = "slash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "tools/native"
name_suffix = ".rs"
emit = "file"

[templates.typescript]
match = "^[\\w./-]+\\.ts$"
comment = "slash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "tools"
name_suffix = ".ts"
emit = "file"

[templates.powershell]
match = "^[\\w./-]+\\.ps1$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "tools"
name_suffix = ".ps1"
emit = "file"

[templates.toml-config]
match = "^[\\w./-]+\\.toml$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/share/mios"
name_suffix = ".toml"
emit = "file"

[templates.yaml]
match = "^[\\w./-]+\\.yaml$|^[\\w./-]+\\.yml$"
comment = "hash"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/share/mios"
name_suffix = ".yaml"
emit = "file"

[templates.json-schema]
match = "^[\\w./-]+\\.json$"
comment = "hash"
required_header = false
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/share/mios"
name_suffix = ".json"
emit = "file"

[templates.roadmap]
match = "^ROADMAP\\.md$"
comment = "md"
required_header = true
required_markers = ["### Workstream Status Rollup", "# Desktop & UX", "# Fleet & Federation"]
generated = false
scaffold = true
dest_dir = "."
fixed_name = "ROADMAP.md"
emit = "file"

[templates.roadmap-ws]
match = "^usr/share/doc/mios/roadmap/[\\w-]+\\.md$"
comment = "md"
required_header = true
required_markers = ["## WS-", "acceptance:"]
generated = false
scaffold = true
dest_dir = "usr/share/doc/mios/roadmap"
name_suffix = ".md"
emit = "file"

[templates.markdown-doc]
match = "^usr/share/doc/mios/[\\w./-]+\\.md$|^README\\.md$"
comment = "md"
required_header = true
required_markers = []
generated = false
scaffold = true
dest_dir = "usr/share/doc/mios"
name_suffix = ".md"
emit = "file"

[templates.systemd-unit]
match = "^usr/lib/systemd/system/[\\w-]+\\.service$"
comment = "hash"
required_header = true
required_markers = ["[Unit]", "[Service]", "[Install]"]
required_ordered = ["[Unit]", "[Service]", "[Install]"]
generated = false
scaffold = true
dest_dir = "usr/lib/systemd/system"
name_prefix = "mios-"
name_suffix = ".service"
emit = "file"

[templates.quadlet]
match = "^usr/share/containers/systemd/[\\w-]+\\.(?:container|pod|network|volume|image)$"
comment = "hash"
required_header = true
required_markers = []
generated = true
scaffold = true
dest_dir = "usr/share/containers/systemd"
name_prefix = "mios-"
name_suffix = ".container"
emit = "file"

[templates.quadlet-container]
match = "^usr/share/containers/systemd/[\\w-]+\\.container$"
comment = "hash"
required_header = true
required_markers = ["[Unit]", "[Container]", "[Install]"]
required_ordered = ["[Unit]", "[Container]", "[Install]"]
generated = true
scaffold = true
dest_dir = "usr/share/containers/systemd"
name_prefix = "mios-"
name_suffix = ".container"
emit = "file"

[templates.quadlet-pod]
match = "^usr/share/containers/systemd/[\\w-]+\\.pod$"
comment = "hash"
required_header = true
required_markers = ["[Unit]", "[Pod]", "[Install]"]
required_ordered = ["[Unit]", "[Pod]", "[Install]"]
generated = true
scaffold = true
dest_dir = "usr/share/containers/systemd"
name_prefix = "mios-"
name_suffix = ".pod"
emit = "file"

[templates.quadlet-network]
match = "^usr/share/containers/systemd/[\\w-]+\\.network$"
comment = "hash"
required_header = true
required_markers = ["[Unit]", "[Network]", "[Install]"]
required_ordered = ["[Unit]", "[Network]", "[Install]"]
generated = true
scaffold = true
dest_dir = "usr/share/containers/systemd"
name_prefix = "mios-"
name_suffix = ".network"
emit = "file"

[templates.quadlet-volume]
match = "^usr/share/containers/systemd/[\\w-]+\\.volume$"
comment = "hash"
required_header = true
required_markers = ["[Unit]", "[Volume]", "[Install]"]
required_ordered = ["[Unit]", "[Volume]", "[Install]"]
generated = true
scaffold = true
dest_dir = "usr/share/containers/systemd"
name_prefix = "mios-"
name_suffix = ".volume"
emit = "file"

[templates.cargo-manifest]
match = "^tools/native/[\\w-]+/Cargo\\.toml$"
comment = "hash"
required_header = true
required_markers = ["[package]", "name =", "version.workspace = true"]
generated = false
scaffold = true
dest_dir = "tools/native"
name_prefix = ""
name_suffix = "/Cargo.toml"
emit = "file"

[templates.systemd-timer]
match = "^usr/lib/systemd/system/[\\w-]+\\.timer$"
comment = "hash"
required_header = true
required_markers = ["[Unit]", "[Timer]", "[Install]"]
required_ordered = ["[Unit]", "[Timer]", "[Install]"]
generated = false
scaffold = true
dest_dir = "usr/lib/systemd/system"
name_prefix = "mios-"
name_suffix = ".timer"
emit = "file"

[kargs]
iommu          = "on"                      # Enable IOMMU: "on", "amd", "intel"
vfio_ids       = ""                        # PCI IDs to bind to vfio-pci (comma-separated, e.g. "10de:1db6")
hugepages      = ""                        # Hugepages count to reserve (empty to disable)
isolcpus       = ""                        # CPUs to isolate from scheduler (empty to disable)
nohz_full      = ""                        # CPUs for adaptive-ticks (empty to disable)
rcu_nocbs      = ""                        # CPUs to offload RCU callbacks (empty to disable)
THP            = ""                        # Transparent Huge Pages configuration: "always", "madvise", "never" or empty


[testing]
# Grow-only, the inverse of the other ratchets: the smoke test asserted six
# things about a 49 GB image with 79 layers, and a manifest that can shrink
# is one that quietly stops checking.
min_smoke_components = 24

[testing.smoke_components]
shims = [
    "usr/libexec/mios/flatpak-launch",
    "usr/libexec/mios/mios-pc-control",
    "usr/libexec/mios/mios-launcher-daemon",
    "usr/libexec/mios/mios-flatpak-icon-sanitize",
    "usr/bin/mios",
    "usr/bin/mios-build",
    "usr/bin/mios-update",
    "usr/bin/mios-pull",
    "usr/bin/mios-deploy",
    "usr/libexec/mios/mios-doctor",
    "usr/libexec/mios/mios-manual",
    "usr/libexec/mios/mios-theme-render",
]
units = [
    "usr/lib/systemd/system/mios-wsl-interop-priority.service",
    "usr/lib/systemd/system/mios-agent-pipe.service",
    "usr/lib/systemd/system/mios-hermes-browser.service",
    "usr/lib/systemd/system/mios-hermes-firstboot.service",
    "usr/lib/systemd/system/mios-dashboard-issue.service",
    "usr/lib/systemd/system/mios-firstboot.target",
]
python_entries = [
    "usr/lib/mios/agent-pipe/server.py",
    "usr/lib/mios/agent-pipe/mios_dispatcher.py",
    "usr/lib/mios/agent-pipe/mios_router.py",
    "usr/lib/mios/agent-pipe/mios_kernel.py",
    "usr/lib/mios/agent-pipe/mios_sandbox.py",
    "usr/lib/mios/agent-pipe/mios_capreg.py",
]

[testing.negative_coverage_exempt]
exempt = [
  # Law-15 cross-repo parity: early-returns when the sibling mios-bootstrap
  # checkout is absent, which is always the case on a CI runner, so an injected
  # fault would be skipped rather than caught.
  "check_ps_repo_parity",
  # Authenticode verification: no-ops unless [security.powershell_signing]
  # names a cert AND a Windows PowerShell is present, neither of which holds on
  # a Linux runner -- an injected fault would be skipped, not caught.
  "check_ps_signatures",
  "check_native_lint",
  "check_resolver_ps_equivalence",
  "check_resolver_shell_equivalence",
  "check_template_self_conformance",
  "check_templates_bootstrap_sync",
  "check_agent_schema",
  "check_ai_manifest",
  "check_bib_rootfs_label_policy",
  "check_blade_dropins",
  "check_bootstrap_ports_drift",
  "check_canonical_bools",
  "check_capability_manifest",
  "check_cephfs_ssot",
  "check_cli_sql_safety",
  "check_container_ports",
  "check_converge_ssot",
  "check_coordination_hygiene",
  "check_dag_integrity",
  "check_dotfiles_projection",
  "check_drift_build_catalog",
  "check_drift_projection",
  "check_egress_firewall",
  "check_etc_duplicates",
  "check_fluff_tokens",
  "check_gate_index",
  "check_globals_image_parity",
  "check_globals_ports",
  "check_greenboot",
  "check_greenboot_enablement",
  "check_hint_coverage",
  "check_hummingbird",
  "check_kargs_projection",
  "check_module_boundary",
  "check_negative_test_coverage",
  "check_no_bare_port_literals",
  "check_no_hardcode",
  "check_package_registry",
  "check_pod_quadlets",
  "check_python_lint",
  "check_raw_toml_readers",
  "check_rbac_tiers",
  "check_resolver_twin_parity",
  "check_retired_models",
  "check_structured",
  "check_surface_parity",
  "check_template_conformance",
  "check_unwired_modules",
  "check_userenv_parity",
  "check_unit_security",
  "check_var_closure",
  "check_vendor_urls",
  "check_verb_backends",
  "check_comment_lex_equivalence"
]

# ----------------------------------------------------------------------------
[pipeline]
space = { min = 0, max = 99 }

identity_axes = ["script_prefix", "log_label"]
future_axes   = ["oci_run_step"]
progress_axis = "script_count"

label_stage = "[{nn}-{name}]"          # e.g. [42-chrony-render]
label_check = "[{nn}-{name}:{cc}]"     # e.g. [98-drift-checks:86]

map         = "usr/share/mios/reference/pipeline-index.tsv"    # NN<TAB>kind<TAB>name<TAB>file<TAB>oneline
check_index = "usr/share/mios/reference/drift-gate-index.tsv"  # CC<TAB>check_fn<TAB>description
check_stage = 98

reporter  = "usr/lib/mios/log.sh"               # mios_step / mios_check_* derive NN + CC at runtime
generator = "tools/generate-pipeline-index.py"  # extends tools/generate-gate-index.py
gate      = "98-drift-checks.sh:check_pipeline_numbering"

bands = [
  { range = [1, 1],   purpose = "git-overlay" },
  { range = [2, 2],   purpose = "build-context" },
  { range = [5, 7],   purpose = "repos/kernel" },
  { range = [10, 15], purpose = "accounts" },
  { range = [20, 27], purpose = "hardware-universal" },
  { range = [33, 54], purpose = "services" },
  { range = [56, 62], purpose = "themes" },
  { range = [65, 80], purpose = "ai/desktop/boot/distribution" },
  { range = [85, 99], purpose = "finalize/validators" },
]

[pipeline.invariants]
prefix_unique       = true
prefix_in_band      = true
map_in_sync         = true
check_dense         = true
check_derived       = true
label_not_stale     = true
single_progress     = true

[pipeline.external]
counters = [
  { tool = "podman/buildah", pattern = "STEP i/N", scope = "outer ~25, nested mios-sys ~19, go-builder 2, rust-builder 4" },
  { tool = "dnf5",           pattern = "[i/N]",     scope = "download ~55 + transaction ~57" },
  { tool = "cargo",          pattern = "Compiling c (i/m)", scope = "rust-builder crate graph" },
]
suppress_hint = "quiet flags only (dnf5 -q, buildah --quiet, cargo -q). Never parse or fold into the MiOS scheme."

[build.phases]
list = [
  { ordinal = "01", name = "system-files-overlay", script = "01-system-files-overlay.sh", fatal = true, apply_class = "containerfile" },
  { ordinal = "02", name = "materialize-build-ctx", script = "02-materialize-build-ctx.sh", fatal = true, apply_class = "universal" },
  { ordinal = "04", name = "local-rpm-mirror", script = "04-local-rpm-mirror.sh", fatal = true, apply_class = "universal" },
  { ordinal = "05", name = "repos", script = "05-repos.sh", fatal = true, apply_class = "universal" },
  { ordinal = "06", name = "enable-external-repos", script = "06-enable-external-repos.sh", fatal = false, apply_class = "universal" },
  { ordinal = "07", name = "kernel", script = "07-kernel.sh", fatal = true, apply_class = "universal" },
  { ordinal = "10", name = "locale-theme", script = "10-locale-theme.sh", fatal = true, apply_class = "universal" },
  { ordinal = "11", name = "user", script = "11-user.sh", fatal = true, apply_class = "universal" },
  { ordinal = "12", name = "hostname", script = "12-hostname.sh", fatal = true, apply_class = "universal" },
  { ordinal = "13", name = "accounts-db", script = "13-accounts-db.sh", fatal = false, apply_class = "universal" },
  { ordinal = "14", name = "podman-machine-compat", script = "14-podman-machine-compat.sh", fatal = false, apply_class = "universal" },
  { ordinal = "15", name = "freeipa-client", script = "15-freeipa-client.sh", fatal = false, apply_class = "universal" },
  { ordinal = "20", name = "hardware", script = "20-hardware.sh", fatal = true, apply_class = "universal" },
  { ordinal = "21", name = "virt", script = "21-virt.sh", fatal = true, apply_class = "universal" },
  { ordinal = "22", name = "akmod-guards", script = "22-akmod-guards.sh", fatal = false, apply_class = "universal" },
  { ordinal = "23", name = "gpu-passthrough", script = "23-gpu-passthrough.sh", fatal = true, apply_class = "universal" },
  { ordinal = "24", name = "gpu-pv-shim", script = "24-gpu-pv-shim.sh", fatal = true, apply_class = "universal" },
  { ordinal = "25", name = "gpu-cdi-toolkits", script = "25-gpu-cdi-toolkits.sh", fatal = true, apply_class = "universal" },
  { ordinal = "26", name = "nvidia-cdi-refresh", script = "26-nvidia-cdi-refresh.sh", fatal = true, apply_class = "universal" },
  { ordinal = "27", name = "vm-gating", script = "27-vm-gating.sh", fatal = false, apply_class = "universal" },
  { ordinal = "33", name = "generate-quadlets", script = "33-generate-quadlets.sh", fatal = true, apply_class = "universal" },
  { ordinal = "34", name = "render-quadlets", script = "34-render-quadlets.sh", fatal = true, apply_class = "universal" },
  { ordinal = "35", name = "render-ports", script = "35-render-ports.sh", fatal = true, apply_class = "universal" },
  { ordinal = "36", name = "ceph-k3s", script = "36-ceph-k3s.sh", fatal = false, apply_class = "universal" },
  { ordinal = "37", name = "k3s-selinux", script = "37-k3s-selinux.sh", fatal = false, apply_class = "universal" },
  { ordinal = "38", name = "selinux", script = "38-selinux.sh", fatal = true, apply_class = "universal" },
  { ordinal = "39", name = "moby-engine", script = "39-moby-engine.sh", fatal = false, apply_class = "universal" },
  { ordinal = "40", name = "fapolicyd-trust", script = "40-fapolicyd-trust.sh", fatal = true, apply_class = "universal" },
  { ordinal = "41", name = "services", script = "41-services.sh", fatal = true, apply_class = "universal" },
  { ordinal = "42", name = "chrony-render", script = "42-chrony-render.sh", fatal = true, apply_class = "universal" },
  { ordinal = "43", name = "nut-render", script = "43-nut-render.sh", fatal = true, apply_class = "universal" },
  { ordinal = "44", name = "firewall-ports", script = "44-firewall-ports.sh", fatal = true, apply_class = "universal" },
  { ordinal = "45", name = "firewall", script = "45-firewall.sh", fatal = true, apply_class = "universal" },
  { ordinal = "46", name = "sshd-port", script = "46-sshd-port.sh", fatal = true, apply_class = "universal" },
  { ordinal = "47", name = "init-service", script = "47-init-service.sh", fatal = true, apply_class = "universal" },
  { ordinal = "48", name = "mios-dropin-fanout", script = "48-mios-dropin-fanout.sh", fatal = true, apply_class = "universal" },
  { ordinal = "49", name = "cosign-policy", script = "49-cosign-policy.sh", fatal = false, apply_class = "universal" },
  { ordinal = "50", name = "uupd-installer", script = "50-uupd-installer.sh", fatal = false, apply_class = "universal" },
  { ordinal = "51", name = "hardening", script = "51-hardening.sh", fatal = true, apply_class = "universal" },
  { ordinal = "52", name = "apply-boot-fixes", script = "52-apply-boot-fixes.sh", fatal = true, apply_class = "universal" },
  { ordinal = "53", name = "enable-log-copy-service", script = "53-enable-log-copy-service.sh", fatal = false, apply_class = "universal" },
  { ordinal = "54", name = "bake-coderun-sandbox", script = "54-bake-coderun-sandbox.sh", fatal = true, apply_class = "universal" },
  { ordinal = "56", name = "fonts", script = "56-fonts.sh", fatal = true, apply_class = "universal" },
  { ordinal = "57", name = "gnome", script = "57-gnome.sh", fatal = false, apply_class = "universal" },
  { ordinal = "58", name = "gnome-remote-desktop", script = "58-gnome-remote-desktop.sh", fatal = false, apply_class = "universal" },
  { ordinal = "59", name = "tools", script = "59-tools.sh", fatal = true, apply_class = "universal" },
  { ordinal = "60", name = "flatpak-env", script = "60-flatpak-env.sh", fatal = true, apply_class = "universal" },
  { ordinal = "61", name = "flatpak-bake", script = "61-flatpak-bake.sh", fatal = false, apply_class = "universal" },
  { ordinal = "62", name = "oh-my-posh", script = "62-oh-my-posh.sh", fatal = false, apply_class = "universal" },
  { ordinal = "65", name = "bake-hyprland", script = "65-bake-hyprland.sh", fatal = false, apply_class = "universal" },
  { ordinal = "66", name = "bake-quickshell", script = "66-bake-quickshell.sh", fatal = false, apply_class = "universal" },
  { ordinal = "67", name = "bake-surfer", script = "67-bake-surfer.sh", fatal = false, apply_class = "universal" },
  { ordinal = "68", name = "bake-kvmfr", script = "68-bake-kvmfr.sh", fatal = false, apply_class = "universal" },
  { ordinal = "69", name = "bake-lookingglass-client", script = "69-bake-lookingglass-client.sh", fatal = false, apply_class = "universal" },
  { ordinal = "72", name = "hermes-agent", script = "72-hermes-agent.sh", fatal = true, apply_class = "universal" },
  { ordinal = "73", name = "model-prep", script = "73-model-prep.sh", fatal = true, apply_class = "universal" },
  { ordinal = "75", name = "kargs-render", script = "75-kargs-render.sh", fatal = true, apply_class = "universal" },
  { ordinal = "76", name = "uki-render", script = "76-uki-render.sh", fatal = false, apply_class = "universal" },
  { ordinal = "77", name = "composefs-verity", script = "77-composefs-verity.sh", fatal = true, apply_class = "universal" },
  { ordinal = "78", name = "greenboot", script = "78-greenboot.sh", fatal = true, apply_class = "universal" },
  { ordinal = "79", name = "boot-config", script = "79-boot-config.sh", fatal = true, apply_class = "universal" },
  { ordinal = "80", name = "distribution", script = "80-distribution.sh", fatal = true, apply_class = "universal" },
  { ordinal = "85", name = "bake-plan", script = "85-bake-plan.sh", fatal = true, apply_class = "universal" },
  { ordinal = "86", name = "oscap-compliance", script = "86-oscap-compliance.sh", fatal = true, apply_class = "universal" },
  { ordinal = "88", name = "finalize", script = "88-finalize.sh", fatal = true, apply_class = "universal" },
  { ordinal = "90", name = "generate-sbom", script = "90-generate-sbom.sh", fatal = true, apply_class = "universal" },
  { ordinal = "91", name = "strip-build-toolchain", script = "91-strip-build-toolchain.sh", fatal = false, apply_class = "universal" },
  { ordinal = "94", name = "cleanup", script = "94-cleanup.sh", fatal = true, apply_class = "universal" },
  { ordinal = "97", name = "ssot-lint", script = "97-ssot-lint.sh", fatal = true, apply_class = "containerfile" },
  { ordinal = "98", name = "drift-checks", script = "98-drift-checks.sh", fatal = true, apply_class = "containerfile" },
  { ordinal = "99", name = "postcheck", script = "99-postcheck.sh", fatal = true, apply_class = "containerfile" }
]

[build.ratchet]
max_phase_scripts = 72   # +1: 55-native-build.sh (builds the tools/native Rust workspace in-bake)

# ----------------------------------------------------------------------------
# [templates.placeholders] -- Unified SSOT token vocabulary for scaffolding
# ----------------------------------------------------------------------------
[templates.placeholders]
name        = "mockname"
PascalName  = "MockName"
date        = "2026-07-17"
id          = "9999"
title       = "Mock Title"
description = "Mock Description"
status      = "proposed"
priority    = "P1"
theme       = "Mock Theme"
task_title  = "Mock Task Title"
task_id     = "8888"
image       = "mock-image:latest"
uid         = "1000"
gid         = "1000"
filename    = "mockname.py"
path        = "usr/lib/mios/agent-pipe/mios_pipe/mockname.py"

[resolver]
# Keys the Python and Rust resolvers do not agree on. Shrink-only.
# The parity check never ran until the binary was found in target/debug,
# so this is the first measurement, not a regression. AGY-1676 drives it
# to zero by making one implementation authoritative.
max_key_divergence = 983
# Values that differ for keys both resolvers emit. The remainder is
# Python's repr of nested arrays/tables, which needs preserve_order on
# the toml crate to match -- AGY-1676, with a toolchain that can link.
max_value_divergence = 12

[tasks]
# AGY task hygiene ratchet. Duplicate task IDs make dependencies ambiguous.
# Shrink-only. The id below which AGY tasks need not carry the full
# schema; lowering it is how the 482 open older tasks become
# falsifiable, a batch at a time. A DONE task is exempt by its marker.
schema_from = 1607
max_duplicate_ids = 0

[docs]
# Measured floor; 0 was never enforced. Ratchet DOWN only. AGY-1601.
max_stale_refs              = 20
max_undocumented_components = 16
stay_max_lines              = 2
stay_max_words         = 25
migrate_min_lines      = 6      # ratchet: 6 -> 4 -> 3 as the mid-size band clears
migrate_min_words      = 60
landing_min_word_ratio = 0.90   # a doc passage must retain this much of the block
render_extra           = ["llms.txt", "llms-full.txt"]   # non-.md files mios-manual render also splices

# Retired AI-plane port numbers: every one has a [ports] successor key.
retired_ports = [11434, 11450, 11441, 3030, 8432, 8441, 8442, 8633, 8640, 8641, 8642, 8888, 8899]
# Living contract docs that must reference [ports] KEYS, never the numbers above.
port_clean = [
  "README.md", "CLAUDE.md", "GEMINI.md", "AGENTS.md", "MiOS.md", "SECURITY.md",
  ".github/ai-instructions.md", "llms.txt", "llms-full.txt",
  "usr/share/doc/mios/reference/api.md",
  # Runtime AI payloads and operator surfaces (T-304, batch 1).
  "system-prompt.md", "tools/README.md",
  "etc/mios/ai/system-prompt.md", "etc/mios/system-prompts/mios-reviewer.md",
  "usr/share/mios/ai/INDEX.md", "usr/share/mios/ai/audit-prompt.md",
  "usr/share/mios/security/README.md",
  "usr/share/mios/docs/agents/AI-ARCHITECTURE.md",
  "usr/share/mios/docs/ai-pipeline-map.md",
  "usr/share/mios/cookbooks/ingest-kb.md",
  "usr/share/mios/hermes/skills/mios-skill-catalog/SKILL.md",
  "usr/share/mios/hermes/skills/parallel-fanout/SKILL.md",
  # T-305 batch 2. Hint-only files stay out: their bodies are historical.
  "installation/UNIFY.md", "tools/windows/README-WINDOWS.md",
  "etc/mios/system-prompts/mios-engineer.md",
  "etc/mios/system-prompts/mios-troubleshoot.md",
  "usr/share/mios/ai/system.md", "usr/share/mios/ai/hermes-soul-full.md",
  "usr/share/mios/cookbooks/finetune-flow.md",
  "usr/share/mios/cookbooks/local-rag-day0.md",
  "usr/share/mios/docs/day-0/FIRST-BOOT.md",
  "usr/share/mios/docs/agents/PC-CONTROL-LOCAL.md",
  "usr/share/mios/docs/terminal/INVOCATIONS.md",
  "usr/share/mios/hermes/skills/mios-environment/SKILL.md",
  "usr/share/mios/hermes/skills/opencode-delegation/SKILL.md",
  "usr/share/mios/open-webui/system-prompts/mios-agent.md",
  "usr/share/doc/mios/manual.md",
  "usr/share/doc/mios/manual/ch04-the-agentic-ai-stack.md",
  "usr/share/doc/mios/manual/ch10-local-inference-lanes-and-llama-cpp.md",
  # T-306: the documentation long tail. Historical archives stay verbatim and out.
  "usr/share/doc/mios/adr/0005-sovereign-run-off-m-drive.md",
  "usr/share/doc/mios/adr/0006-openai-api-only-ai-contract.md",
  "usr/share/doc/mios/adr/0008-mios-cat-unified-entry-and-minification.md",
  "usr/share/doc/mios/adr/0009-unified-config-surface.md",
  "usr/share/doc/mios/adr/README.md",
  "usr/share/doc/mios/concepts/OFFLINE-FIRST.md",
  "usr/share/doc/mios/concepts/a2a-passport-conformance-2026-06-20.md",
  "usr/share/doc/mios/concepts/agent-pipe-openai-standards-master-plan.md",
  "usr/share/doc/mios/concepts/aios-engineering-blueprint.md",
  "usr/share/doc/mios/concepts/aios-implementation-plan.md",
  "usr/share/doc/mios/concepts/coderun-sandbox.md",
  "usr/share/doc/mios/concepts/container-os-runtime.md",
  "usr/share/doc/mios/concepts/foss-upstream-map.md",
  "usr/share/doc/mios/concepts/mios-app-browser-portal-dashboard-design-2026-07-03.md",
  "usr/share/doc/mios/concepts/multi-agent-buildout-plan.md",
  "usr/share/doc/mios/concepts/naming-refactor-plan.md",
  "usr/share/doc/mios/concepts/postgres-pgvector-unification.md",
  "usr/share/doc/mios/concepts/roadmap-snapshot-decomposition-2026-06-22.md",
  "usr/share/doc/mios/concepts/unified-ai-pipeline-2026-06-16.md",
  "usr/share/doc/mios/concepts/upstream-gap-plan-2026-06.md",
  "usr/share/doc/mios/concepts/ws-0-preflight-findings-2026-06-20.md",
  "usr/share/doc/mios/concepts/ws-a3-central-path-cutover-worklist.md",
  "usr/share/doc/mios/concepts/ws-subsystems-activation-2026-06-20.md",
  "usr/share/doc/mios/concepts/ws7-uki-fapolicyd.md",
  "usr/share/doc/mios/finetune.md",
  "usr/share/doc/mios/guides/agent-windows-ssh.md",
  "usr/share/doc/mios/guides/deploy.md",
  "usr/share/doc/mios/guides/edge-node-join.md",
  "usr/share/doc/mios/guides/engineering.md",
  "usr/share/doc/mios/guides/hummingbird-distroless.md",
  "usr/share/doc/mios/guides/inference-consolidation.md",
  "usr/share/doc/mios/guides/security.md",
  "usr/share/doc/mios/manual/ch05-federation-and-computer-use.md",
  "usr/share/doc/mios/manual/ch11-heavy-gpu-lanes-and-sglang-vllm.md",
  "usr/share/doc/mios/manual/ch14-agent-to-agent-delegation-protocols.md",
  "usr/share/doc/mios/manual/ch25-local-search-engine-and-searxng.md",
  "usr/share/doc/mios/manual/ch48-local-ai-web-consoles.md",
  "usr/share/doc/mios/manual/ch51-distilled-system-knowledge-code-invariants.md",
  "usr/share/doc/mios/manual/federation.md",
  "usr/share/doc/mios/manual/hermes.md",
  "usr/share/doc/mios/manual/llamacpp.md",
  "usr/share/doc/mios/manual/mios.md",
  "usr/share/doc/mios/manual/opencode-gateway.md",
  "usr/share/doc/mios/manual/root.md",
  "usr/share/doc/mios/manual/routing.md",
  "usr/share/doc/mios/manual/scheduler.md",
  "usr/share/doc/mios/manual/system.md",
  "usr/share/doc/mios/manual/tools.md",
  "usr/share/doc/mios/reference/PACKAGES.md",
  "usr/share/doc/mios/reference/audit-security.md",
  "usr/share/doc/mios/reference/build-scripts.md",
  "usr/share/doc/mios/reference/credits.md",
  "usr/share/doc/mios/reference/engineering-reference.md",
  "usr/share/doc/mios/reference/hwcaps.md",
  "usr/share/doc/mios/reference/maturity-and-release-runbook.md",
  "usr/share/doc/mios/reference/sources.md",
  "usr/share/doc/mios/reference/tree.md",
  "usr/share/doc/mios/upstream/cdi.md",
  "usr/share/doc/mios/upstream/deploy-targets.md",
  "usr/share/doc/mios/upstream/fedora-bootc.md",
  "usr/share/doc/mios/upstream/ghcr.md",
  "usr/share/doc/mios/upstream/nvidia.md",
  "usr/share/doc/mios/upstream/podman.md",
  "usr/share/doc/mios/upstream/related-distros.md",
  "usr/share/doc/mios/upstream/selinux.md",
  "usr/share/mios/docs/MIOS-GEMINI-TASKS-2026-06-22.md",
  "usr/share/mios/docs/MIOS-ROADMAP-PROGRESS-2026-06-22.md",
  "usr/share/mios/docs/install-robustness-2026-06-21.md",
    "automation/67-bake-surfer.sh",
    "usr/share/mios/owui/pipes/mios_agent_pipe.py"
]

# Ratchet floors, monotone decreasing. Measured, not guessed.
# Method + full census: docs/agy/doc-generative-documentation.md
max_unmigrated_narrative = 342   # MIGRATE blocks still in source
max_overlong_hints       = 0    # AI-hint headers over [ai_tag].hint_max_chars

# Generated artifacts are never a migration SOURCE: globals.{sh,ps1} embed whole
# unit-comment bodies as string literals and tests/golden holds byte-copies of
# shipped units, so extracting from them would multi-count one comment.
blocklist_globs = [
  "automation/lib/globals.sh",
  "automation/lib/globals.ps1",
  "tools/native/mios-unit-gen/tests/golden/**",
  "tools/test_*.py",
  "**/*.generated.*",
  "usr/share/mios/names.generated.txt",
]

# READONLY: these strings are sent to a model. Copy them into docs, never edit,
# reformat or prune them -- editing the text changes model behaviour.
llm_payload_globs = [
  "usr/share/mios/owui/**", "usr/share/mios/hermes/**", "usr/share/mios/prompts/**",
  "usr/share/mios/ai/**", "etc/mios/system-prompts/**", "usr/share/mios/agents/**",
  "usr/share/mios/cookbooks/**", "etc/skel/.config/mios/**",
]

# Referenced at runtime but legitimately absent from the source tree, so the
# staleness axis must not flag them.
ref_allowlist = [
  # Placeholders and templates, not references: generators and their tests
  # spell example unit names, and tmpfiles/sysusers name groups rather than
  # files. The token scanner cannot tell those from a path, so they are
  # listed rather than left to inflate a ceiling that is meant to measure
  # real broken links.
  "x.service", "unit.service", "UID.service", "UID_.service",
  "s.container", "-pod.service", "host.container", ".apply.target",
  "MIOS_FOO", "mios-XXXXX",
  # Untracked files (installation/MiOS-Cat.bat is gitignored, mios-knowledge-graph.json built at runtime).
  "installation/MiOS-Cat.bat",
  "usr/share/mios/knowledge/mios-knowledge-graph.json",
  "mios-knowledge-graph",
  "C:\\mios-bootstrap\\Get-MiOS.ps1",
  "C:\\mios-bootstrap\\bootstrap.ps1",
  "/etc/ceph/ceph.conf", "/etc/cdi/nvidia.yaml", "/var/run/cdi/nvidia.yaml",
  "/etc/containers/policy.json", "/etc/mios/manifest.json", "/var/",
  "@@MIOS_", "ollama", "8080",
  "blade-*.conf",
  "mios-codemode-api.py",
  "/usr/share/mios/vllm/model",
  "/usr/libexec/mios/mios-",
  "mios_accounts.py",
  "accounts-schema.sql",
  "mios-account-project",
  "mios-accounts-projector",
  "31-user.sh",
  "MiOS-Host.ps1",
  "MiOS-Provision.lib.ps1",
  "New-MiOSISO.ps1",
  "config/artifacts/live-chat.toml",
  "usr/share/mios/live-chat/overlay/",
  "/usr/libexec/mios/oh-my-posh/oh-my-posh",
  "build-mios.sh",
  "cat/MiOS-Cat.sh",
  "cat/MiOS-Cat.bat",
  "tests/templates/conform-cli/",
  "/usr/share/mios/tools/universal-vfio-configurator.sh",
  "automation/57-mios-sys-build.sh",
  "usr/lib/systemd/system/ceph-bootstrap.service",
  "usr/share/doc/mios/adr/*.md",
  "autounattend",
  "TASKS.md",
  "{bib",
  "wsl2}.toml",
  "automation/lib/bake.sh",
  "docs/agy/doc-mios-metal.md",
  "usr/share/mios/mini/headscale-policy.hujson",
  "usr/lib/greenboot/check/required.d/41-mios-critical-services.sh",
  "usr/libexec/mios/mios-greenboot-critical-gen",
  "automation/.shellcheck-warn-baseline",
  "usr/share/mios/value-aliases.tsv",
  "_harvest/tools_check_",
  "mios.toml",
  "server.py",
  "+",
]
link_base = "repo"

[docs.distill]
# The Day-N+1 pass: comments are scraped, sanitized and distilled into the
# manual. AI-hints are NOT scraped -- they stay in source and are projected
# forward by the index deriver instead.
enable   = true
dest_dir = "usr/share/doc/mios/manual"
# Areas never distilled: their comments are the operator-facing documentation
# where they sit, or they are generated artifacts.
skip_globs = [
  "usr/share/mios/mios.toml",
  "tools/native/mios-unit-gen/tests/golden/*",
  "usr/share/doc/*",
]

[docs.sanitize]
# Applied to every scraped comment before it reaches a doc. Order matters:
# rewrites first, then redaction.
# Dev-box paths -> the FHS canonical the doc reader actually has.
# MOST SPECIFIC FIRST: these are applied in order, so a general drive rewrite
# placed above the file:// form would corrupt it into file:////usr/share/...
path_rewrites = [
  ["file:///C:/MiOS/", ""],
  ["file:///C:/", ""],
  ["C:\\MiOS\\", "/usr/share/mios/"],
  ["C:/MiOS/", "/usr/share/mios/"],
  ["C:\\MiOS", "/usr/share/mios"],
  ["C:/MiOS", "/usr/share/mios"],
  ["/mnt/c/MiOS", "/usr/share/mios"],
]
# Secret-shaped values never reach a doc, whatever a comment happened to hold.
# The keyword may sit inside a longer identifier (HA_PASSWORD=, MY_API_KEY:),
# so no leading \\b -- that anchor is why FOO_PASSWORD=... escaped redaction.
# The value must look like one: 4+ non-space chars, and not the ">" of a "=>"
# arrow, so prose like "NO DISCORD_BOT_TOKEN => ..." is left alone.
redact_patterns = [
  "(?i)[A-Za-z0-9_]*(?:api[_-]?key|secret|passwd|password|token)[A-Za-z0-9_]*\\s*[:=]+\\s*(?![>\\s])[^\\s\"']{4,}",
  "\\bsha256:[0-9a-f]{64}\\b",
  "(?i)\\bBearer\\s+[A-Za-z0-9._-]{16,}",
]
redact_with = "[redacted]"

[docs.boilerplate]
# Canonical framing paragraphs, spliced into docs through MIOS-GEN markers.
# The what-mios-is block had been hand-copied into dozens of files, where the
# copies then drifted; one string here is the cure.
what-mios-is = """
MiOS is one thing built two ways at once: an immutable, `bootc`/OCI-shaped
Fedora workstation -- the whole OS is a single container image, so `bootc
upgrade` behaves like a `git pull` and `bootc rollback` like a Ctrl-Z -- that
is *also* a local, self-hosted, agentic AI operating system.
"""

[docs.signals]
why       = '\b(because|so that|otherwise|avoid|prevents?|must not|never|do not|fail-open|fail-closed|deliberately|intentionally|noqa|workaround|upstream bug|race|deadlock)\b'
narrative = '\b(operator|used to|no longer|previously|regression|root cause|incident|reverted|scrapped|rejected|instead of|alternative|rationale|invariant|degrade|ADR-[0-9]+|Law [0-9]+|AGY-[0-9]+|WS-[A-Z]+|[0-9]{4}-[0-9]{2}-[0-9]{2})\b'
fact      = '\b(broken|deprecated|removed|disabled|not supported|only on|requires|since|as of|WS-[A-Z]+|AGY-[0-9]+|ADR-[0-9]+|Law [0-9]+)\b'
code      = '^\s*(if|for|while|def|class|function|export|set|return|elif|else|fi|done|esac|end)\b|[;{]\s*$|^\s*[\w.]+\s*=[^=]'


[desktop.launchers.mios-configurator]
exec_cmd = "/usr/libexec/mios/mios-configurator-launch"
title = "MiOS Settings"
generic_name = "System Settings"
comment = "One unified surface for every MiOS setting — identity, AI, packages, flatpaks, desktop (writes the mios.toml SSOT)"
icon = "preferences-system"
categories = "System;Settings;PackageManager;"
keywords = "mios;configurator;system;settings;packages;flatpak;ai;"
startup_wm_class = "org.gnome.Epiphany"
port_key = "agent_pipe"
path = "/configure"
ai_hint = "Desktop entry for MiOS Settings — the one unified configuration surface. Launches mios-configurator-launch, which opens the configurator embedded in the MiOS Portal (http://localhost:{port}/configure) and falls back to the standalone HTML editor only when the Portal is unreachable. All settings serialise to the mios.toml SSOT (identity, AI models, packages, flatpaks, desktop)."
ai_related = "/etc/mios/mios.toml, /usr/libexec/mios/mios-configurator-launch, mios-configurator-launch, http://localhost:{port}/configure"
trailing_comments = [
  "# WSLg auto-publishes this entry to the Windows Start Menu under",
  "# \"<Distro> Apps\" when MiOS-DEV is the source distro, so the same",
  "# .desktop file gives Linux GNOME Dock + Activities visibility on a",
  "# deployed MiOS host AND a Windows Start Menu entry on the Win-side",
  "# dev VM. One file, two surfaces.",
]

[desktop.launchers.mios-svc-ceph]
port_key = "ceph_dashboard"
scheme = "https"
title = "MiOS Ceph Dashboard"
generic_name = "Storage Cluster Dashboard"
comment = "Open the Ceph storage dashboard at https://localhost:{port}/"
icon = "drive-multidisk-symbolic"
categories = "System;Network;"
keywords = "mios;ceph;storage;cluster;dashboard;"
no_display = false
ai_hint = "Desktop entry for the Ceph storage dashboard that provides a GUI interface for managing the Ceph cluster via a web browser at port 8443."

[desktop.launchers.mios-svc-cockpit]
port_key = "cockpit"
scheme = "https"
title = "MiOS Cockpit"
generic_name = "System Console"
comment = "Open the Cockpit host web console at https://localhost:{port}/"
icon = "utilities-system-monitor-symbolic"
categories = "System;Network;Settings;"
keywords = "mios;cockpit;admin;console;system;"
ai_hint = "Desktop entry for the MiOS Cockpit web console, providing a shortcut to the system administration interface at port 9090 for remote management and monitoring."

[desktop.launchers.mios-svc-code-server]
port_key = "code_server"
title = "MiOS Code (code-server)"
generic_name = "VS Code in a Browser"
comment = "Open code-server at http://localhost:{port}/ in the default browser"
icon = "visual-studio-code"
categories = "Development;IDE;TextEditor;"
keywords = "mios;code-server;vscode;editor;ide;git;"
ai_hint = "Desktop entry for the code-server web IDE, providing a launcher for agents to identify and open the MiOS development environment at the local port 8080 via the system's default browser."

[desktop.launchers.mios-svc-forge]
port_key = "forge_http"
title = "MiOS Forge"
generic_name = "Self-Hosted Git Forge (Forgejo)"
comment = "Open the local Forgejo git forge at http://localhost:{port}/"
icon = "text-x-generic-symbolic"
categories = "Development;RevisionControl;Network;"
keywords = "mios;forge;forgejo;git;gitea;repo;"
ai_hint = "Desktop entry for the MiOS Forge (Forgejo) service, providing a launcher to open the local Git repository management web interface at http://localhost:{port}/ via the system's default browser."

[desktop.launchers.mios-svc-guacamole]
port_key = "guacamole_web"
path = "/guacamole/"
title = "MiOS Guacamole"
generic_name = "Browser Remote Desktop"
comment = "Open Apache Guacamole at http://localhost:{port}/guacamole/"
icon = "preferences-desktop-remote-desktop-symbolic"
categories = "Network;RemoteAccess;"
keywords = "mios;guacamole;rdp;vnc;remote;desktop;"
ai_hint = "Desktop entry for the Guacamole remote desktop gateway, providing a launcher to open the local web interface at port 8080 for RDP/VNC access."

[desktop.launchers.mios-svc-hermes]
port_key = "hermes"
path = "/v1"
title = "MiOS Hermes Agent"
generic_name = "AI Agent Gateway"
comment = "Open the Hermes-Agent /v1 surface (the LIVE MiOS agent at root)"
icon = "applications-science-symbolic"
categories = "Development;Network;"
keywords = "mios;hermes;agent;api;openai;v1;"
ai_hint = "Desktop entry for the Hermes Agent gateway, providing a shortcut to the local /v1 API surface at port 8642 for interacting with the primary MiOS AI agent."

[desktop.launchers.mios-svc-llm-light]
port_key = "llm_light"
title = "MiOS LLM Light"
generic_name = "Local LLM + Embedding Backend (llama-swap)"
comment = "Open the LLM Light API surface (local model server)"
icon = "applications-engineering-symbolic"
categories = "Development;Network;"
keywords = "mios;llm-light;llama-swap;llm;embedding;ai;"
ai_hint = "Desktop entry for the LLM Light service providing the local LLM and embedding backend, used by agents to identify and launch the local inference server at port 11450."

[desktop.launchers.mios-svc-searxng]
port_key = "searxng"
title = "MiOS Search (SearXNG)"
generic_name = "Privacy Metasearch"
comment = "Open the local SearXNG metasearch proxy at http://localhost:{port}/"
icon = "system-search-symbolic"
categories = "Network;WebBrowser;"
keywords = "mios;searxng;search;metasearch;privacy;"
ai_hint = "Desktop entry for the SearXNG metasearch proxy; used by agents to identify and launch the local search interface at port 8899 via a web browser."

# [bootstrap.sync] -- Law 15 mirror manifest. Contract: installation/UNIFY.md.
[bootstrap.sync]
# Byte-identical in both repos; mios.git wins. Never list a bootstrap-owned file.
mirror_files = [
  "Get-MiOS.ps1",
  "LICENSE",
  "build-mios.ps1",
  "installation/UNIFY.md",
  "installation/mios-common.ps1",
  "etc/skel/.config/mios/system-prompt.md",
  "etc/usbguard/rules.conf",
  "installation/bin/7z.dll",
]

# SSOT tables mirrored into bootstrap's root mios.toml.
mirror_toml_tables = ["ports"]

# Deliberately NOT mirrored; reasons in installation/UNIFY.md.
not_mirrored = [
  "install.sh", "install.ps1", "bootstrap.sh", "bootstrap.ps1",
  "installation/mios-common.sh", "README.md", "CLAUDE.md", "AGENTS.md",
  "GEMINI.md", ".gitignore", "VERSION",
  ".clinerules",
  ".cursorrules",
  ".editorconfig",
  ".env.mios",
  ".gitattributes",
  "etc/mios/ai/config.json",
  "etc/skel/.config/aichat/config.yaml",
  "etc/skel/.config/mios/mios.toml",
  "etc/skel/.config/mios/profile.toml",
  "image-versions.yml",
  "installation/README.md",
  "installation/mios-install.ps1",
  "installation/mios-install.sh",
  "llms.txt",
  "system-prompt.md",
  "usr/share/mios/names.generated.txt",
  "usr/share/mios/profile-headless.toml",
  "usr/share/mios/referenced_names.txt",
]

# [legibility] -- the repo IS the deliverable. Floors fall only. See ROADMAP.md.
[legibility]
max_tracked_files = 3344
max_tracked_mb    = 201
max_shell_lines   = 44421  # bash is glue only (Law 14)
max_ps_lines      = 26987  # no new PowerShell-as-program (Law 14)
max_automation_phases = 72
max_libexec_verbs     = 285

# --- Firstboot Models (T-200) ---
[[ai.firstboot_models]]
name   = "lfm2-700m.gguf"
url    = "https://huggingface.co/stelterlab/lfm2-700m-GGUF/resolve/main/lfm2-700m.gguf"
dest   = "/usr/share/mios/llamacpp/model.gguf"
sha256 = "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855"

# The suite registry: every publisher runs this list and only this list.
# Before it existed the two ran different sets and thirteen suites ran
# nowhere. One reader consumes it; a gate keeps it whole.
[ci]
# Shrink-only. An exemption is a suite that cannot currently run, not a suite
# that need not: each carries the reason it was measured to fail.
max_exempt_suites = 7
# Harnesses, not suites. They execute the tiers rather than asserting anything
# themselves, so they are covered without consuming the exemption ratchet.
runners = ["tests/run-suites.sh"]

[tests]
# Shrink-only. A negative test that leaves its injected fixture behind ships the
# mutation; three reached this file in one session and turned five suites red.
max_leaked_fixtures = 0

[ci.python]
# Installed on a runner before the unit tier.
#
# A hand-written list was wrong twice in a row: server.py's import chain reaches
# smolagents through mios_gateway_queue and mcp through the module after that,
# and each missing name cost a CI round trip to discover. The agent-pipe already
# declares what it needs, so the runner installs THAT and the list cannot drift
# from the code again.
requirements = ["usr/lib/mios/agent-pipe/requirements.txt"]
# Only what no requirements file claims: pyflakes backs lint-python.sh's
# undefined-name pass.
packages = ["pyflakes"]

[ci.tiers]
# Static analysis. No build, no network, no image.
lint = [
    "automation/lint-shell.sh",
    "automation/lint-python.sh",
    "automation/lint-json.sh",
    "automation/lint-powershell.sh",
    "automation/lint-ps-analyzer.sh",
    "automation/97-ssot-lint.sh",
]
# Behavioural suites. Each asserts something about the tree it ships from.
unit = [
    "tests/powershell/run-pester.sh",
    "tests/test-sandbox-seccomp.sh",
    "tests/test-powershell-flatten.sh",
    "tests/test-pgvector-major-upgrade.sh",
    "tests/test-mios-manual-harvest.sh",
    "tests/test-offload-overlay.py",
    "tests/test-blade-reachability.sh",
    "tests/test-seat-auth-posture.sh",
    "tests/test-seat-activates-nothing.py",
    "tests/test-greenboot-blade-guard.sh",
    "tests/test-greenboot-blade-reachability.sh",
    "tests/test-lint-python-coverage.sh",
    "tests/test-lint-shell-coverage.sh",
    "tests/test-owui-pipe-endpoints.py",
    "tests/doc-production-evidence.sh",
    "tests/test-a2o-fallback.sh",
    "tests/test-bake-group.sh",
    "tests/test-firstboot-prestage.sh",
    "tests/test-role-apply-precedence.sh",
    "tests/test-theme-merge.py",
    "tests/test-dispatcher-defense.py",
    "tests/test-ek-smart-extract.py",
    "tests/test-ek-substitution.py",
    "tests/test-launch-args.py",
    "tests/test-refine-guard.py",
    "tests/test-refine-guards.py",
]
# Fitness functions over the whole tree. Slowest, so they run last.
gate = [
    "automation/98-drift-checks.sh",
    "tests/drift-gate-negatives.sh",
]

# Directories whose suites are discovered rather than listed, so a new sibling
# test is run the moment it lands instead of waiting to be registered.
[ci.globs.tools]
dir  = "tools"
glob = "test_*.py"
tier = "unit"

[ci.globs.agent-pipe]
dir  = "usr/lib/mios/agent-pipe"
glob = "test_mios_*.py"
tier = "unit"
skip = [
    "test_mios_config_audit.py",
    "test_mios_redact.py",
    "test_mios_vector.py",
    "test_mios_gateway_agent.py",
    "test_mios_gateway_queue.py",
    "test_mios_admission.py",
    "test_mios_mcp_pool.py",
    "test_mios_bench_harness.py",
]
skip_reason = "needs a live database or a heavy dependency; covered by the build job's virtualenv gate"

[ci.globs.libexec]
dir  = "usr/libexec/mios"
glob = "test_mios_*.py"
tier = "unit"

# Suites that are tracked but not run by a tier. Each reason is a measurement,
# not a judgement; see [ci].max_exempt_suites.
[ci.exempt]
"tests/bake-smoke.sh" = "takes an image reference as its argument; the build and smoke jobs invoke it against the image they just produced"
"tests/drift-gate-readonly.sh" = "a developer convenience wrapper over the gate tier; running it would duplicate that tier"
"tests/drift-parity.sh" = "compares the bash gate against miosd drift-check --parity and needs the miosd binary built"
"tests/test-directory-dispatch.sh" = "dispatch returns no hits without a populated directory index, and the suite indexes hits[0] unguarded -- AGY-1715"
"tests/test-expand-from.py" = "the open-url-fallback-chain skill resolves to not_found, so the suite sees 0 steps where it expects 3 -- AGY-1716"
"tests/test-reflection.py" = "needs a reachable refine backend; every connection attempt fails and reflection returns None -- AGY-1717"
"tests/test-think-stripper.py" = "the stripper leaves the separator behind, so removing a think block yields a doubled space or blank line -- AGY-1718"

# The MiOS product line, named once. Each variant used to be a different shape:
# an edition entry, its own table, or only a machine name. status is measured:
# shipping means built and observed, partial means the machinery runs but not
# the whole job, design means specified with no artifact.
[variants]
# Shrink-only: a variant may not sit in "design" forever without someone
# noticing. Raising this is how a design doc quietly becomes the deliverable.
max_design_variants = 2

[variants.naming]
# The naming convention, enforced by a gate rather than remembered. A variant
# has one name in two registers: title case for prose and interfaces, lower
# case for image tags, keys and paths.
prefix        = "MiOS"
separator     = "-"
title_pattern = "MiOS-<Suffix>"
key_pattern   = "mios-<suffix>"
key_charset   = "a-z0-9-"
# The base variant is the one case with no suffix.
base          = "mios"
# A suffix names WHAT THE VARIANT IS, never its size or its hardware: Metal owns
# the metal, Cat travels, Xbox plays, DEV builds. "Mini" was renamed to "Metal"
# because it described the image's size rather than its job.
suffix_rule   = "name the job, not the size"

[variants.entries.mios]
title     = "MiOS"
summary   = "the base bootc host: the AI plane, the container plane and a Windows guest on one immutable image"
target    = "bare metal or a virtual machine"
config    = ["image", "blade", "editions"]
edition   = "mios"
archetype = "hybrid"
artifacts = ["oci", "iso", "qcow2", "vhdx", "raw"]
doc       = "usr/share/doc/mios/manual.md"
status    = "shipping"

[variants.entries.mios-dev]
title     = "MiOS-DEV"
summary   = "the development host: builds, bakes and gates the image, and runs MiOS itself as the container machine"
target    = "a WSL2 machine on a workstation"
config    = ["image"]
archetype = "hybrid"
artifacts = ["wsl2"]
doc       = "usr/share/doc/mios/manual.md"
status    = "partial"

[variants.entries.mios-metal]
title     = "MiOS-Metal"
summary   = "the metal-owning firmware: a small headless bootc hypervisor-router that binds the GPUs and NICs and hosts MiOS as a guest"
target    = "bare metal, headless"
config    = ["metal"]
archetype = "headless"
artifacts = ["iso", "raw"]
doc       = "docs/agy/doc-mios-metal.md"
status    = "design"

[variants.entries.mios-xbox]
title     = "MiOS-Xbox"
summary   = "the gaming edition: an Xbox-tuned Windows guest, gaming debloat posture and its own branding"
target    = "bare metal or a virtual machine"
config    = ["editions"]
edition   = "mios-xbox"
archetype = "desktop"
artifacts = ["iso", "vhdx"]
doc       = "usr/share/doc/mios/manual.md"
status    = "partial"

[variants.entries.mios-xbox-arm]
title     = "MiOS-Xbox-Arm"
summary   = "the gaming edition for arm64 hardware, sharing the Xbox posture with an arm64 Windows guest"
target    = "arm64 bare metal"
config    = ["editions"]
edition   = "mios-xbox-arm"
archetype = "desktop"
artifacts = ["iso"]
doc       = "usr/share/doc/mios/manual.md"
status    = "design"

[variants.entries.mios-cat]
title     = "MiOS-Cat"
summary   = "the portable edition: a Ventoy USB or NVMe carrying the image, the repository and the models to run, install and deploy with no network"
target    = "removable USB or NVMe"
config    = ["cat"]
archetype = "endpoint"
artifacts = ["usb-installer", "iso"]
doc       = "usr/share/doc/mios/adr/0008-mios-cat-unified-entry-and-minification.md"
status    = "partial"

# Every way MiOS can be deployed, named once. The targets and recipes existed;
# what did not exist was a statement of the matrix, so [deploy].artifacts listed
# two of the seven formats the build system actually produces and nothing
# noticed. A format is declared here or it is not supported.
[deploy.formats]
# The shared bootc-image-builder recipe the disk formats sit on. Not a format.
shared_recipe = "config/artifacts/bib.toml"

[deploy.formats.oci]
title   = "OCI image"
summary = "the image itself, pulled from a registry and switched into with bootc"
target  = "build"
recipe  = ""
medium  = "container registry"
gui     = "the desktop the host provides"
artifacts = []
status  = "shipping"

[deploy.formats.oci-archive]
title   = "OCI archive"
summary = "the image as a single file, for installing where there is no network"
target  = "oci-archive"
recipe  = ""
medium  = "file"
gui     = "the desktop the host provides"
artifacts = ["oci-archive/*.tar"]
status  = "shipping"

[deploy.formats.raw]
title   = "raw disk image"
summary = "a whole-disk image written straight to bare metal or a block device"
target  = "raw"
recipe  = ""
medium  = "disk"
gui     = "full desktop session"
artifacts = ["raw/image/*.raw", "raw/*.raw", "image/*.raw", "*.raw"]
status  = "shipping"

[deploy.formats.iso]
title   = "installer ISO"
summary = "a bootable installer that lays MiOS onto the machine it boots"
target  = "iso"
recipe  = "config/artifacts/iso.toml"
medium  = "optical or USB"
gui     = "full desktop session"
artifacts = ["iso/bootiso/*.iso", "iso/*.iso", "bootiso/*.iso", "*.iso"]
status  = "shipping"

[deploy.formats.usb-installer]
title   = "portable installer"
summary = "a Ventoy USB or NVMe carrying the image, the repository and the models, installing with no network"
target  = "usb-installer"
recipe  = ""
medium  = "removable USB or NVMe"
gui     = "full desktop session"
artifacts = ["usb-installer/*.iso"]
status  = "partial"

[deploy.formats.qcow2]
title   = "QEMU disk"
summary = "a virtual machine disk for libvirt, QEMU and the cloud images built from it"
target  = "qcow2"
recipe  = "config/artifacts/qcow2.toml"
medium  = "virtual machine"
gui     = "full desktop session"
artifacts = ["qcow2/qcow2/*.qcow2", "qcow2/*.qcow2"]
status  = "shipping"

[deploy.formats.vhdx]
title   = "Hyper-V disk"
summary = "a virtual machine disk for Hyper-V and Windows virtualisation"
target  = "vhdx"
recipe  = "config/artifacts/vhdx.toml"
medium  = "virtual machine"
gui     = "full desktop session"
artifacts = ["vhdx/*.vhdx", "vhdx/vpc/*.vhd", "vhdx/*.vhd"]
status  = "shipping"

[deploy.formats.wsl2]
title   = "WSL distribution"
summary = "MiOS as a Windows Subsystem for Linux distribution, running its own systemd and its own containers"
target  = "wsl2"
recipe  = "config/artifacts/wsl2.toml"
medium  = "WSL2 on Windows"
# WSLg is not a separate artifact: it is this format with the graphical bridge
# Windows already provides, which etc/profile.d/mios-wslg.sh wires up.
gui     = "WSLg, via the graphical bridge Windows provides"
artifacts = ["wsl2/*.tar.gz", "wsl2/*.wsl2"]
status  = "partial"

# What it takes for a built artifact to count as real. The verifier walked the
# tree, matched nothing and reported success, so a build that produced no file
# at all satisfied the gate that guards the push. Every format above names the
# glob its own target writes; a format whose globs match nothing is a missing
# artifact, and no artifact at all is the loudest failure of the set.
[deploy.verify]
# The floor a file must clear to count as an artifact rather than a stub.
# Every format above writes a whole disk, an installer or a root filesystem,
# and none of those is as small as four mebibytes, so a file under this floor
# is a truncated or empty write rather than a smaller build.
min_bytes = 4194304

# [rust] -- what the Rust layer's green actually covers.
#
# `cargo test --workspace` prints "test result: ok. 0 passed" for a crate with no
# tests, which reads exactly like a crate whose tests all passed. Every entry
# below is in that state. The ceiling is shrink-only: write a test, remove the
# entry, lower the number.
[rust]
max_untested_crates = 11

[rust.untested_crates]
"src/mios-rs/miosd" = "3550 lines, the largest crate in the repository, and the drift-check daemon -- AGY-1800"
"tools/native/generate-names-registry" = "generates the names registry; a golden-output test is the obvious shape -- AGY-1801"
"tools/native/mios-bake-plan" = "454 lines deciding what the bake pulls; wrong output costs a whole build -- AGY-1801"
"tools/native/mios-comment-lex" = "the comment lexer the docs ratchets measure with -- AGY-1801"
"tools/native/mios-drift-runner" = "runs the drift checks; untested, it can report a pass it did not observe -- AGY-1801"
"tools/native/mios-ssot-lint" = "the Rust twin of 97-ssot-lint.sh, asserted byte-identical by a gate but not by its own test -- AGY-1801"
"tools/native/mios-template-compile" = "compiles the templates other surfaces render from -- AGY-1801"
"tools/native/mios-template-conform" = "asserts template conformance and asserts nothing about itself -- AGY-1801"
"tools/native/mios-version-check" = "parses versions by hand; the hand-rolled TOML scan already needed hardening once -- AGY-1801"
"tools/native/mios-wallpaperd" = "Windows-only, excluded from the CI workspace build -- AGY-1801"
"tools/native/xtask" = "a 37-line cargo task helper; the smallest of these and the easiest to cover -- AGY-1801"
