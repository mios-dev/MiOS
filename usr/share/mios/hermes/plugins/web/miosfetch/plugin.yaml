# AI-hint: Defines the miosfetch backend plugin for offline web content extraction using urllib and readability to convert URLs into clean text without external APIs or cloud dependencies.
name: miosfetch
kind: backend
version: 1.0.0
description: MiOS offline direct-fetch web-extract provider (stdlib urllib + readability HTML->text; no firecrawl SDK / no container / no cloud -- fully offline).
register: plugins.web.miosfetch:register
