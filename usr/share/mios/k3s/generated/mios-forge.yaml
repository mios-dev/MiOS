# AI-hint: GENERATED k3s/k8s manifest for the MiOS mios-forge pod (pods-as-SSOT, WS-7). DO NOT EDIT -- regenerate via tools/generate-k3s-manifests.sh. Inert until the [k3s] lane is enabled; host-net/GPU/bind-mount services need k3s adaptation first.
# AI-related: tools/generate-k3s-manifests.sh, mios-forge.container, usr/share/mios/k3s/README.md
# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#

# NOTE: If you generated this yaml from an unprivileged and rootless podman container on an SELinux
# enabled system, check the podman generate kube man page for steps to follow to ensure that your pod/container
# has the right permissions to access the volumes added.
---
apiVersion: v1
kind: Pod
metadata:
  annotations:
  labels:
    app: mios-forge-pod
  name: mios-forge-pod
spec:
  containers:
  - args:
    - /usr/bin/s6-svscan
    - /etc/s6
    env:
    - name: FORGEJO__server__SSH_PORT
      value: "49922"
    - name: FORGEJO__service__REQUIRE_SIGNIN_VIEW
      value: "false"
    - name: FORGEJO__server__SSH_LISTEN_HOST
      value: 0.0.0.0
    - name: FORGEJO__database__PATH
      value: /data/forgejo.db
    - name: FORGEJO__security__INSTALL_LOCK
      value: "true"
    - name: FORGEJO__server__HTTP_ADDR
      value: 0.0.0.0
    - name: FORGEJO__server__START_SSH_SERVER
      value: "true"
    - name: FORGEJO__log__LEVEL
      value: Info
    - name: USER_UID
      value: "816"
    - name: FORGEJO__server__SSH_LISTEN_PORT
      value: "49922"
    - name: FORGEJO__service__DISABLE_REGISTRATION
      value: "true"
    - name: FORGEJO__log__MODE
      value: console
    - name: FORGEJO__database__DB_TYPE
      value: sqlite3
    - name: FORGEJO__server__ROOT_URL
      value: http://localhost:3000/
    - name: GITEA__server__HTTP_ADDR
      value: 0.0.0.0
    - name: USER_GID
      value: "816"
    - name: FORGEJO__server__HTTP_PORT
      value: "3000"
    image: codeberg.org/forgejo/forgejo:12
    name: mios-forge
    securityContext:
      runAsGroup: 0
      runAsUser: 0
    volumeMounts:
    - mountPath: /data
      name: srv-mios-forge-host-0
    - mountPath: /etc/mios/forge
      name: etc-mios-forge-host-1
      readOnly: true
  hostNetwork: true
  hostname: MiOS-955
  volumes:
  - hostPath:
      path: /srv/mios/forge
      type: Directory
    name: srv-mios-forge-host-0
  - hostPath:
      path: /etc/mios/forge
      type: Directory
    name: etc-mios-forge-host-1
