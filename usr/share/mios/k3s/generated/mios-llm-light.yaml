# AI-hint: GENERATED k3s/k8s manifest for the MiOS mios-llm-light pod (pods-as-SSOT, WS-7). DO NOT EDIT -- regenerate via tools/generate-k3s-manifests.sh. Inert until the [k3s] lane is enabled; host-net/GPU/bind-mount services need k3s adaptation first.
# AI-related: tools/generate-k3s-manifests.sh, mios-llm-light.container, usr/share/mios/k3s/README.md
# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#

# NOTE: If you generated this yaml from an unprivileged and rootless podman container on an SELinux
# enabled system, check the podman generate kube man page for steps to follow to ensure that your pod/container
# has the right permissions to access the volumes added.
---
apiVersion: v1
kind: Pod
metadata:
  annotations:
  labels:
    app: mios-llm-light-pod
  name: mios-llm-light-pod
spec:
  containers:
  - args:
    - --config
    - /app/config.yaml
    - --listen
    - 0.0.0.0:11450
    command:
    - /app/llama-swap
    env:
    - name: HOME
      value: /var/lib/mios/llamacpp
    - name: LD_LIBRARY_PATH
      value: /usr/lib/wsl/lib:/usr/local/cuda/lib64
    image: ghcr.io/mostlygeek/llama-swap:cuda
    name: mios-llm-light
    securityContext:
      runAsGroup: 827
      runAsUser: 827
    volumeMounts:
    - mountPath: /models
      name: usr-share-mios-llamacpp-models-host-0
      readOnly: true
    - mountPath: /app/config.yaml
      name: usr-share-mios-llamacpp-mios-llm-light-yaml-host-1
      readOnly: true
    - mountPath: /var/lib/mios/llamacpp/slots
      name: var-lib-mios-llamacpp-slots-host-2
  hostNetwork: true
  hostname: MiOS-955
  volumes:
  - hostPath:
      path: /usr/share/mios/llamacpp/models
      type: Directory
    name: usr-share-mios-llamacpp-models-host-0
  - hostPath:
      path: /usr/share/mios/llamacpp/mios-llm-light.yaml
      type: File
    name: usr-share-mios-llamacpp-mios-llm-light-yaml-host-1
  - hostPath:
      path: /var/lib/mios/llamacpp/slots
      type: Directory
    name: var-lib-mios-llamacpp-slots-host-2
