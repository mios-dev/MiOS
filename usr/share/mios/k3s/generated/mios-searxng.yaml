# AI-hint: GENERATED k3s/k8s manifest for the MiOS mios-searxng pod (pods-as-SSOT, WS-7). DO NOT EDIT -- regenerate via tools/generate-k3s-manifests.sh. Inert until the [k3s] lane is enabled; host-net/GPU/bind-mount services need k3s adaptation first.
# AI-related: tools/generate-k3s-manifests.sh, mios-searxng.container, usr/share/mios/k3s/README.md
# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#

# NOTE: If you generated this yaml from an unprivileged and rootless podman container on an SELinux
# enabled system, check the podman generate kube man page for steps to follow to ensure that your pod/container
# has the right permissions to access the volumes added.
---
apiVersion: v1
kind: Pod
metadata:
  annotations:
  labels:
    app: mios-searxng-pod
  name: mios-searxng-pod
spec:
  containers:
  - env:
    - name: GRANIAN_WORKERS
      value: "2"
    - name: GRANIAN_HOST
      value: 0.0.0.0
    - name: SEARXNG_BASE_URL
      value: http://localhost:8888/
    - name: SEARXNG_BIND_ADDRESS
      value: 0.0.0.0:8888
    - name: BIND_ADDRESS
      value: 0.0.0.0:8888
    - name: SEARXNG_LOG_LEVEL
      value: WARNING
    - name: GRANIAN_BLOCKING_THREADS
      value: "8"
    - name: GRANIAN_PORT
      value: "8888"
    image: docker.io/searxng/searxng:latest
    name: mios-searxng
    securityContext:
      runAsGroup: 818
      runAsUser: 818
    volumeMounts:
    - mountPath: /etc/searxng
      name: etc-mios-searxng-host-0
    - mountPath: /var/cache/searxng
      name: var-cache-mios-searxng-host-1
  hostNetwork: true
  hostname: MiOS-955
  volumes:
  - hostPath:
      path: /etc/mios/searxng
      type: Directory
    name: etc-mios-searxng-host-0
  - hostPath:
      path: /var/cache/mios/searxng
      type: Directory
    name: var-cache-mios-searxng-host-1
