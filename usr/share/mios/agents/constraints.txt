# AI-hint: Pip constraints (lower-bound version floors) for the shared agent-plane Python ASGI stack -- reproducibility + CVE floor; passed to every hermes-agent pip install via `-c`.
# AI-related: automation/38-hermes-agent.sh, /usr/lib/mios/agents/.venv, /usr/share/mios/mios.toml
#
# MiOS agent-plane pip CONSTRAINTS -- reproducibility floor + security floor for
# the Python ASGI stack (FastAPI / Starlette / pydantic / httpx / uvicorn / anyio)
# that Hermes-Agent, the agent-pipe, and the opencode gateway all import from the
# shared venv at /usr/lib/mios/agents/.venv.
#
# WHY: automation/38-hermes-agent.sh installs hermes-agent (and its [web]/mcp
# extras) without bounding these transitive deps, so two builds can resolve
# different versions, and an unbounded Starlette can regress below the patched
# line for CVE-2026-48710 -- the Starlette "BadHost" host-header AUTH-BYPASS: an
# unvalidated Host containing `/`, `?` or `#` desyncs request.url.path from the
# routed scope, bypassing path-based auth middleware. Patched in Starlette 1.0.1.
# Passing this file with `pip install -c` makes every install resolve within the
# same safe floors.
#
# These are LOWER BOUNDS ONLY (>=): they never force a downgrade of an
# already-newer wheel, they only forbid resolving anything older/unsafe. They sit
# at or below the live stack (which currently resolves higher -- e.g. starlette
# 1.3.1, fastapi 0.136.x, pydantic 2.13.x, httpx 0.28.x, uvicorn 0.49.x, anyio
# 4.13.x), so the resolver still picks the newest satisfying wheel.
#
# To tighten reproducibility further, raise each floor to the exact live version
# or pair this with an `==` lockfile, and pin the hermes-agent git ref to a SHA
# (MIOS_HERMES_AGENT_REF) -- see MIOS-GAP-REGISTER U2.
starlette>=1.1.0    # above the CVE-2026-48710 BadHost auth-bypass fix (1.0.1); see WHY
fastapi>=0.115
pydantic>=2
httpx>=0.27
uvicorn>=0.30
anyio>=4
