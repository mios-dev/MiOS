{
  "name": "open-url-resilient",
  "description": "Open `url` in the operator's default browser; on failure, try the same url with each browser id from `fallbacks` in order. First step that lands a visible window wins. Use when sandboxed flatpak browsers may fail in the current runtime context.",
  "source": "import",
  "status": "promoted",
  "body": {
    "mode": "try-each",
    "steps": [
      {"verb": "open_url", "args": {"url": "$url"}},
      {"verb": "open_url", "args": {"url": "$url", "browser": "$fallback"}}
    ],
    "params": ["url", "fallback"]
  }
}
