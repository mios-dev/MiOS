{
  "name": "safe-open-flatpak",
  "description": "Probe the flatpak `id` sandbox via flatpak_preflight; on success, launch via open_app. Halts on preflight failure so the agent surfaces the actual sandbox error (portal_helper_env_fd / user_bus_credentials / app_not_installed / runtime_missing) instead of paraphrasing success.",
  "source": "import",
  "status": "promoted",
  "body": {
    "mode": "sequence",
    "steps": [
      {"verb": "flatpak_preflight", "args": {"id": "$id"}},
      {"verb": "open_app",          "args": {"name": "$id"}}
    ],
    "params": ["id"]
  }
}
