{
  "name": "install-windows-app",
  "description": "Search winget for a query, then install the matched package id. The agent typically resolves the id via winget_search first; this skill is the install-after-confirm path.",
  "source": "import",
  "status": "promoted",
  "body": {
    "steps": [
      {"verb": "winget_search",  "args": {"query": "$query", "limit": 5}},
      {"verb": "winget_install", "args": {"id": "$id"}}
    ],
    "params": ["query", "id"]
  }
}
