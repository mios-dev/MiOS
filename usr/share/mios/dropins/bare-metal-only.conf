# AI-hint: Configures the MiOS service to execute exclusively on physical hardware by enforcing a ConditionVirtualization=no check, preventing deployment in VM or container environments.
[Unit]
# 'MiOS': Skip this service in VMs/containers -- bare metal only
ConditionVirtualization=no
