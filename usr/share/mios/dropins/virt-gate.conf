# AI-hint: Configuration for the virt-gate service that enforces exclusion of containerized and WSL2 environments to prevent nested virtualization conflicts in the MiOS networking stack.
[Unit]
# 'MiOS': Skip in containers/WSL2
ConditionVirtualization=!container
ConditionVirtualization=!wsl
