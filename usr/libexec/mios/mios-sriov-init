#!/usr/bin/env bash
# MiOS v0.2.0 — stub: mios-sriov-init
# Implementation expected from bootstrap overlay or future automation script.
# Override by dropping a real executable at: /usr/libexec/mios/mios-sriov-init
set -euo pipefail
logger -t mios "[mios-sriov-init] stub executed — replace with real implementation"
echo "[mios/mios-sriov-init] stub: no-op" >&2
exit 0
