#!/usr/bin/env python3
# AI-hint: Deprecated compatibility alias wrapper. Delegates execution to mios-dotfiles-render.
# AI-related: usr/libexec/mios/mios-dotfiles-render
import os
import sys
import subprocess

print("[mios-theme-render] WARNING: mios-theme-render is deprecated; please use mios-dotfiles-render instead.", file=sys.stderr)
_dir = os.path.dirname(os.path.abspath(__file__))
_engine = os.path.join(_dir, "mios-dotfiles-render")
sys.exit(subprocess.run([sys.executable, _engine] + sys.argv[1:]).returncode)
