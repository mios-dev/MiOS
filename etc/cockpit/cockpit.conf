# AI-hint: Cockpit web console settings projected from mios.toml [cockpit]; regenerate with tools/generate-cockpit-conf.py, never edit.
# Cockpit configuration file
[WebService]
AllowUnencrypted = true
LoginTo = false

[Session]
IdleTimeout = 0
