# AI-hint: WSL distribution config; generates the MiOS Windows Terminal profile from the edge-to-edge template wt_profile_inject.py renders.
[windowsterminal]
enabled = true
profileTemplate = /usr/share/mios/wsl/terminal-profile.json
