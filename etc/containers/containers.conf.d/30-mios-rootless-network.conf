/* AI-hint: Configures the default rootless network backend to use "pasta" instead of "slirp4netns" to ensure rootless containers function without /dev/net/tun access while improving TCP throughput. */
/* AI-doc: usr/share/doc/mios/manual/_harvest/etc_containers_containers_conf_d_30_mios_rootless_network_conf.md */

[network]
default_rootless_network_cmd = "pasta"
