# AI-hint: Overrides the ConditionUser=!@system check for localsearch-3 to allow the service to run for the mios system user (UID 992), preventing GNOME file manager bookmark loading failures.
[Unit]
ConditionUser=

