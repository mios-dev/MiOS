/* AI-hint: Overrides the ConditionUser=!@system check for localsearch-3 to allow the service to run for the mios system user (UID 992), preventing GNOME file manager crashes caused by missing Tracker3.Miner.Files dbus activation. */
[Unit]
ConditionUser=

