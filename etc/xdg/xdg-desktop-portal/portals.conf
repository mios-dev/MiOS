# AI-hint: Defines the default backend for xdg-desktop-portal interfaces, ensuring all portal features (FileChooser, ScreenCast, etc.) route to the gnome backend as a fallback for both native sessions and WSLg environments.
# AI-related: mios-wslg
[preferred]
default=gnome
